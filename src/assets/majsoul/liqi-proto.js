// 自動生成物。出所: 雀魂 liqi.json (src/assets/majsoul/liqi.json)。再生成: npx pbjs --keep-case --no-comments -t static-module -w es6 -o src/assets/majsoul/liqi-proto.js src/assets/majsoul/liqi.json。手動編集しない。
/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const lq = $root.lq = (() => {

    const lq = {};

    lq.NotifyRoomGameStart = (function() {

        function NotifyRoomGameStart(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRoomGameStart.prototype.game_url = "";
        NotifyRoomGameStart.prototype.connect_token = "";
        NotifyRoomGameStart.prototype.game_uuid = "";
        NotifyRoomGameStart.prototype.location = "";

        NotifyRoomGameStart.create = function create(properties) {
            return new NotifyRoomGameStart(properties);
        };

        NotifyRoomGameStart.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_url != null && Object.hasOwnProperty.call(message, "game_url"))
                writer.uint32(10).string(message.game_url);
            if (message.connect_token != null && Object.hasOwnProperty.call(message, "connect_token"))
                writer.uint32(18).string(message.connect_token);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(26).string(message.game_uuid);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(34).string(message.location);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRoomGameStart.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRoomGameStart.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomGameStart(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_url = value;
                        else
                            delete message.game_url;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.connect_token = value;
                        else
                            delete message.connect_token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.location = value;
                        else
                            delete message.location;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRoomGameStart.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRoomGameStart.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_url != null && message.hasOwnProperty("game_url"))
                if (!$util.isString(message.game_url))
                    return "game_url: string expected";
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                if (!$util.isString(message.connect_token))
                    return "connect_token: string expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        NotifyRoomGameStart.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRoomGameStart)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyRoomGameStart();
            if (object.game_url != null)
                if (typeof object.game_url !== "string" || object.game_url.length)
                    message.game_url = String(object.game_url);
            if (object.connect_token != null)
                if (typeof object.connect_token !== "string" || object.connect_token.length)
                    message.connect_token = String(object.connect_token);
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.location != null)
                if (typeof object.location !== "string" || object.location.length)
                    message.location = String(object.location);
            return message;
        };

        NotifyRoomGameStart.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_url = "";
                object.connect_token = "";
                object.game_uuid = "";
                object.location = "";
            }
            if (message.game_url != null && message.hasOwnProperty("game_url"))
                object.game_url = message.game_url;
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                object.connect_token = message.connect_token;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            return object;
        };

        NotifyRoomGameStart.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRoomGameStart.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRoomGameStart";
        };

        return NotifyRoomGameStart;
    })();

    lq.NotifyMatchGameStart = (function() {

        function NotifyMatchGameStart(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyMatchGameStart.prototype.game_url = "";
        NotifyMatchGameStart.prototype.connect_token = "";
        NotifyMatchGameStart.prototype.game_uuid = "";
        NotifyMatchGameStart.prototype.match_mode_id = 0;
        NotifyMatchGameStart.prototype.location = "";

        NotifyMatchGameStart.create = function create(properties) {
            return new NotifyMatchGameStart(properties);
        };

        NotifyMatchGameStart.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_url != null && Object.hasOwnProperty.call(message, "game_url"))
                writer.uint32(10).string(message.game_url);
            if (message.connect_token != null && Object.hasOwnProperty.call(message, "connect_token"))
                writer.uint32(18).string(message.connect_token);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(26).string(message.game_uuid);
            if (message.match_mode_id != null && Object.hasOwnProperty.call(message, "match_mode_id"))
                writer.uint32(32).uint32(message.match_mode_id);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(42).string(message.location);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyMatchGameStart.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyMatchGameStart.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyMatchGameStart(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_url = value;
                        else
                            delete message.game_url;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.connect_token = value;
                        else
                            delete message.connect_token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_mode_id = value;
                        else
                            delete message.match_mode_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.location = value;
                        else
                            delete message.location;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyMatchGameStart.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyMatchGameStart.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_url != null && message.hasOwnProperty("game_url"))
                if (!$util.isString(message.game_url))
                    return "game_url: string expected";
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                if (!$util.isString(message.connect_token))
                    return "connect_token: string expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.match_mode_id != null && message.hasOwnProperty("match_mode_id"))
                if (!$util.isInteger(message.match_mode_id))
                    return "match_mode_id: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        NotifyMatchGameStart.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyMatchGameStart)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyMatchGameStart();
            if (object.game_url != null)
                if (typeof object.game_url !== "string" || object.game_url.length)
                    message.game_url = String(object.game_url);
            if (object.connect_token != null)
                if (typeof object.connect_token !== "string" || object.connect_token.length)
                    message.connect_token = String(object.connect_token);
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.match_mode_id != null)
                if (Number(object.match_mode_id) !== 0)
                    message.match_mode_id = object.match_mode_id >>> 0;
            if (object.location != null)
                if (typeof object.location !== "string" || object.location.length)
                    message.location = String(object.location);
            return message;
        };

        NotifyMatchGameStart.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_url = "";
                object.connect_token = "";
                object.game_uuid = "";
                object.match_mode_id = 0;
                object.location = "";
            }
            if (message.game_url != null && message.hasOwnProperty("game_url"))
                object.game_url = message.game_url;
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                object.connect_token = message.connect_token;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.match_mode_id != null && message.hasOwnProperty("match_mode_id"))
                object.match_mode_id = message.match_mode_id;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            return object;
        };

        NotifyMatchGameStart.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyMatchGameStart.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyMatchGameStart";
        };

        return NotifyMatchGameStart;
    })();

    lq.NotifyRoomPlayerReady = (function() {

        function NotifyRoomPlayerReady(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRoomPlayerReady.prototype.account_id = 0;
        NotifyRoomPlayerReady.prototype.ready = false;
        NotifyRoomPlayerReady.prototype.account_list = null;
        NotifyRoomPlayerReady.prototype.seq = 0;

        NotifyRoomPlayerReady.create = function create(properties) {
            return new NotifyRoomPlayerReady(properties);
        };

        NotifyRoomPlayerReady.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.ready != null && Object.hasOwnProperty.call(message, "ready"))
                writer.uint32(16).bool(message.ready);
            if (message.account_list != null && Object.hasOwnProperty.call(message, "account_list"))
                $root.lq.NotifyRoomPlayerReady.AccountReadyState.encode(message.account_list, writer.uint32(26).fork()).ldelim();
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(32).uint32(message.seq);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRoomPlayerReady.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRoomPlayerReady.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomPlayerReady(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ready = value;
                        else
                            delete message.ready;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.account_list = $root.lq.NotifyRoomPlayerReady.AccountReadyState.decode(reader, reader.uint32(), undefined, _depth + 1, message.account_list);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRoomPlayerReady.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRoomPlayerReady.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.ready != null && message.hasOwnProperty("ready"))
                if (typeof message.ready !== "boolean")
                    return "ready: boolean expected";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                let error = $root.lq.NotifyRoomPlayerReady.AccountReadyState.verify(message.account_list, _depth + 1);
                if (error)
                    return "account_list." + error;
            }
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq))
                    return "seq: integer expected";
            return null;
        };

        NotifyRoomPlayerReady.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRoomPlayerReady)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyRoomPlayerReady();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.ready != null)
                if (object.ready)
                    message.ready = Boolean(object.ready);
            if (object.account_list != null) {
                if (typeof object.account_list !== "object")
                    throw TypeError(".lq.NotifyRoomPlayerReady.account_list: object expected");
                message.account_list = $root.lq.NotifyRoomPlayerReady.AccountReadyState.fromObject(object.account_list, _depth + 1);
            }
            if (object.seq != null)
                if (Number(object.seq) !== 0)
                    message.seq = object.seq >>> 0;
            return message;
        };

        NotifyRoomPlayerReady.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.ready = false;
                object.account_list = null;
                object.seq = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.ready != null && message.hasOwnProperty("ready"))
                object.ready = message.ready;
            if (message.account_list != null && message.hasOwnProperty("account_list"))
                object.account_list = $root.lq.NotifyRoomPlayerReady.AccountReadyState.toObject(message.account_list, options);
            if (message.seq != null && message.hasOwnProperty("seq"))
                object.seq = message.seq;
            return object;
        };

        NotifyRoomPlayerReady.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRoomPlayerReady.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRoomPlayerReady";
        };

        NotifyRoomPlayerReady.AccountReadyState = (function() {

            function AccountReadyState(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountReadyState.prototype.account_id = 0;
            AccountReadyState.prototype.ready = false;

            AccountReadyState.create = function create(properties) {
                return new AccountReadyState(properties);
            };

            AccountReadyState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.ready != null && Object.hasOwnProperty.call(message, "ready"))
                    writer.uint32(16).bool(message.ready);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountReadyState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountReadyState.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomPlayerReady.AccountReadyState(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.ready = value;
                            else
                                delete message.ready;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountReadyState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountReadyState.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.ready != null && message.hasOwnProperty("ready"))
                    if (typeof message.ready !== "boolean")
                        return "ready: boolean expected";
                return null;
            };

            AccountReadyState.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyRoomPlayerReady.AccountReadyState)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyRoomPlayerReady.AccountReadyState();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.ready != null)
                    if (object.ready)
                        message.ready = Boolean(object.ready);
                return message;
            };

            AccountReadyState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.ready = false;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.ready != null && message.hasOwnProperty("ready"))
                    object.ready = message.ready;
                return object;
            };

            AccountReadyState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountReadyState.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyRoomPlayerReady.AccountReadyState";
            };

            return AccountReadyState;
        })();

        return NotifyRoomPlayerReady;
    })();

    lq.NotifyRoomPlayerDressing = (function() {

        function NotifyRoomPlayerDressing(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRoomPlayerDressing.prototype.account_id = 0;
        NotifyRoomPlayerDressing.prototype.dressing = false;
        NotifyRoomPlayerDressing.prototype.account_list = null;
        NotifyRoomPlayerDressing.prototype.seq = 0;

        NotifyRoomPlayerDressing.create = function create(properties) {
            return new NotifyRoomPlayerDressing(properties);
        };

        NotifyRoomPlayerDressing.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.dressing != null && Object.hasOwnProperty.call(message, "dressing"))
                writer.uint32(16).bool(message.dressing);
            if (message.account_list != null && Object.hasOwnProperty.call(message, "account_list"))
                $root.lq.NotifyRoomPlayerDressing.AccountDressingState.encode(message.account_list, writer.uint32(26).fork()).ldelim();
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(32).uint32(message.seq);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRoomPlayerDressing.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRoomPlayerDressing.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomPlayerDressing(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dressing = value;
                        else
                            delete message.dressing;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.account_list = $root.lq.NotifyRoomPlayerDressing.AccountDressingState.decode(reader, reader.uint32(), undefined, _depth + 1, message.account_list);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRoomPlayerDressing.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRoomPlayerDressing.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.dressing != null && message.hasOwnProperty("dressing"))
                if (typeof message.dressing !== "boolean")
                    return "dressing: boolean expected";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                let error = $root.lq.NotifyRoomPlayerDressing.AccountDressingState.verify(message.account_list, _depth + 1);
                if (error)
                    return "account_list." + error;
            }
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq))
                    return "seq: integer expected";
            return null;
        };

        NotifyRoomPlayerDressing.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRoomPlayerDressing)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyRoomPlayerDressing();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.dressing != null)
                if (object.dressing)
                    message.dressing = Boolean(object.dressing);
            if (object.account_list != null) {
                if (typeof object.account_list !== "object")
                    throw TypeError(".lq.NotifyRoomPlayerDressing.account_list: object expected");
                message.account_list = $root.lq.NotifyRoomPlayerDressing.AccountDressingState.fromObject(object.account_list, _depth + 1);
            }
            if (object.seq != null)
                if (Number(object.seq) !== 0)
                    message.seq = object.seq >>> 0;
            return message;
        };

        NotifyRoomPlayerDressing.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.dressing = false;
                object.account_list = null;
                object.seq = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.dressing != null && message.hasOwnProperty("dressing"))
                object.dressing = message.dressing;
            if (message.account_list != null && message.hasOwnProperty("account_list"))
                object.account_list = $root.lq.NotifyRoomPlayerDressing.AccountDressingState.toObject(message.account_list, options);
            if (message.seq != null && message.hasOwnProperty("seq"))
                object.seq = message.seq;
            return object;
        };

        NotifyRoomPlayerDressing.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRoomPlayerDressing.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRoomPlayerDressing";
        };

        NotifyRoomPlayerDressing.AccountDressingState = (function() {

            function AccountDressingState(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountDressingState.prototype.account_id = 0;
            AccountDressingState.prototype.dressing = false;

            AccountDressingState.create = function create(properties) {
                return new AccountDressingState(properties);
            };

            AccountDressingState.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.dressing != null && Object.hasOwnProperty.call(message, "dressing"))
                    writer.uint32(16).bool(message.dressing);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountDressingState.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountDressingState.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomPlayerDressing.AccountDressingState(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dressing = value;
                            else
                                delete message.dressing;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountDressingState.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountDressingState.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.dressing != null && message.hasOwnProperty("dressing"))
                    if (typeof message.dressing !== "boolean")
                        return "dressing: boolean expected";
                return null;
            };

            AccountDressingState.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyRoomPlayerDressing.AccountDressingState)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyRoomPlayerDressing.AccountDressingState();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.dressing != null)
                    if (object.dressing)
                        message.dressing = Boolean(object.dressing);
                return message;
            };

            AccountDressingState.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.dressing = false;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.dressing != null && message.hasOwnProperty("dressing"))
                    object.dressing = message.dressing;
                return object;
            };

            AccountDressingState.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountDressingState.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyRoomPlayerDressing.AccountDressingState";
            };

            return AccountDressingState;
        })();

        return NotifyRoomPlayerDressing;
    })();

    lq.NotifyRoomPlayerUpdate = (function() {

        function NotifyRoomPlayerUpdate(properties) {
            this.player_list = [];
            this.robots = [];
            this.positions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRoomPlayerUpdate.prototype.owner_id = 0;
        NotifyRoomPlayerUpdate.prototype.robot_count = 0;
        NotifyRoomPlayerUpdate.prototype.player_list = $util.emptyArray;
        NotifyRoomPlayerUpdate.prototype.seq = 0;
        NotifyRoomPlayerUpdate.prototype.robots = $util.emptyArray;
        NotifyRoomPlayerUpdate.prototype.positions = $util.emptyArray;

        NotifyRoomPlayerUpdate.create = function create(properties) {
            return new NotifyRoomPlayerUpdate(properties);
        };

        NotifyRoomPlayerUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.owner_id != null && Object.hasOwnProperty.call(message, "owner_id"))
                writer.uint32(24).uint32(message.owner_id);
            if (message.robot_count != null && Object.hasOwnProperty.call(message, "robot_count"))
                writer.uint32(32).uint32(message.robot_count);
            if (message.player_list != null && message.player_list.length)
                for (let i = 0; i < message.player_list.length; ++i)
                    $root.lq.PlayerGameView.encode(message.player_list[i], writer.uint32(42).fork()).ldelim();
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(48).uint32(message.seq);
            if (message.robots != null && message.robots.length)
                for (let i = 0; i < message.robots.length; ++i)
                    $root.lq.PlayerGameView.encode(message.robots[i], writer.uint32(58).fork()).ldelim();
            if (message.positions != null && message.positions.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.positions.length; ++i)
                    writer.uint32(message.positions[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRoomPlayerUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRoomPlayerUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomPlayerUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.owner_id = value;
                        else
                            delete message.owner_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.robot_count = value;
                        else
                            delete message.robot_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_list && message.player_list.length))
                            message.player_list = [];
                        message.player_list.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.robots && message.robots.length))
                            message.robots = [];
                        message.robots.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.positions && message.positions.length))
                                message.positions = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.positions.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.positions && message.positions.length))
                            message.positions = [];
                        message.positions.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRoomPlayerUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRoomPlayerUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.owner_id != null && message.hasOwnProperty("owner_id"))
                if (!$util.isInteger(message.owner_id))
                    return "owner_id: integer expected";
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                if (!$util.isInteger(message.robot_count))
                    return "robot_count: integer expected";
            if (message.player_list != null && message.hasOwnProperty("player_list")) {
                if (!Array.isArray(message.player_list))
                    return "player_list: array expected";
                for (let i = 0; i < message.player_list.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.player_list[i], _depth + 1);
                    if (error)
                        return "player_list." + error;
                }
            }
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq))
                    return "seq: integer expected";
            if (message.robots != null && message.hasOwnProperty("robots")) {
                if (!Array.isArray(message.robots))
                    return "robots: array expected";
                for (let i = 0; i < message.robots.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.robots[i], _depth + 1);
                    if (error)
                        return "robots." + error;
                }
            }
            if (message.positions != null && message.hasOwnProperty("positions")) {
                if (!Array.isArray(message.positions))
                    return "positions: array expected";
                for (let i = 0; i < message.positions.length; ++i)
                    if (!$util.isInteger(message.positions[i]))
                        return "positions: integer[] expected";
            }
            return null;
        };

        NotifyRoomPlayerUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRoomPlayerUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyRoomPlayerUpdate();
            if (object.owner_id != null)
                if (Number(object.owner_id) !== 0)
                    message.owner_id = object.owner_id >>> 0;
            if (object.robot_count != null)
                if (Number(object.robot_count) !== 0)
                    message.robot_count = object.robot_count >>> 0;
            if (object.player_list) {
                if (!Array.isArray(object.player_list))
                    throw TypeError(".lq.NotifyRoomPlayerUpdate.player_list: array expected");
                message.player_list = Array(object.player_list.length);
                for (let i = 0; i < object.player_list.length; ++i) {
                    if (typeof object.player_list[i] !== "object")
                        throw TypeError(".lq.NotifyRoomPlayerUpdate.player_list: object expected");
                    message.player_list[i] = $root.lq.PlayerGameView.fromObject(object.player_list[i], _depth + 1);
                }
            }
            if (object.seq != null)
                if (Number(object.seq) !== 0)
                    message.seq = object.seq >>> 0;
            if (object.robots) {
                if (!Array.isArray(object.robots))
                    throw TypeError(".lq.NotifyRoomPlayerUpdate.robots: array expected");
                message.robots = Array(object.robots.length);
                for (let i = 0; i < object.robots.length; ++i) {
                    if (typeof object.robots[i] !== "object")
                        throw TypeError(".lq.NotifyRoomPlayerUpdate.robots: object expected");
                    message.robots[i] = $root.lq.PlayerGameView.fromObject(object.robots[i], _depth + 1);
                }
            }
            if (object.positions) {
                if (!Array.isArray(object.positions))
                    throw TypeError(".lq.NotifyRoomPlayerUpdate.positions: array expected");
                message.positions = Array(object.positions.length);
                for (let i = 0; i < object.positions.length; ++i)
                    message.positions[i] = object.positions[i] >>> 0;
            }
            return message;
        };

        NotifyRoomPlayerUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.player_list = [];
                object.robots = [];
                object.positions = [];
            }
            if (options.defaults) {
                object.owner_id = 0;
                object.robot_count = 0;
                object.seq = 0;
            }
            if (message.owner_id != null && message.hasOwnProperty("owner_id"))
                object.owner_id = message.owner_id;
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                object.robot_count = message.robot_count;
            if (message.player_list && message.player_list.length) {
                object.player_list = Array(message.player_list.length);
                for (let j = 0; j < message.player_list.length; ++j)
                    object.player_list[j] = $root.lq.PlayerGameView.toObject(message.player_list[j], options);
            }
            if (message.seq != null && message.hasOwnProperty("seq"))
                object.seq = message.seq;
            if (message.robots && message.robots.length) {
                object.robots = Array(message.robots.length);
                for (let j = 0; j < message.robots.length; ++j)
                    object.robots[j] = $root.lq.PlayerGameView.toObject(message.robots[j], options);
            }
            if (message.positions && message.positions.length) {
                object.positions = Array(message.positions.length);
                for (let j = 0; j < message.positions.length; ++j)
                    object.positions[j] = message.positions[j];
            }
            return object;
        };

        NotifyRoomPlayerUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRoomPlayerUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRoomPlayerUpdate";
        };

        return NotifyRoomPlayerUpdate;
    })();

    lq.NotifyRoomKickOut = (function() {

        function NotifyRoomKickOut(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRoomKickOut.create = function create(properties) {
            return new NotifyRoomKickOut(properties);
        };

        NotifyRoomKickOut.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRoomKickOut.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRoomKickOut.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRoomKickOut();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRoomKickOut.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRoomKickOut.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyRoomKickOut.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRoomKickOut)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyRoomKickOut();
        };

        NotifyRoomKickOut.toObject = function toObject() {
            return {};
        };

        NotifyRoomKickOut.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRoomKickOut.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRoomKickOut";
        };

        return NotifyRoomKickOut;
    })();

    lq.NotifyFriendStateChange = (function() {

        function NotifyFriendStateChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyFriendStateChange.prototype.target_id = 0;
        NotifyFriendStateChange.prototype.active_state = null;

        NotifyFriendStateChange.create = function create(properties) {
            return new NotifyFriendStateChange(properties);
        };

        NotifyFriendStateChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.active_state != null && Object.hasOwnProperty.call(message, "active_state"))
                $root.lq.AccountActiveState.encode(message.active_state, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyFriendStateChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyFriendStateChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyFriendStateChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.active_state = $root.lq.AccountActiveState.decode(reader, reader.uint32(), undefined, _depth + 1, message.active_state);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyFriendStateChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyFriendStateChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.active_state != null && message.hasOwnProperty("active_state")) {
                let error = $root.lq.AccountActiveState.verify(message.active_state, _depth + 1);
                if (error)
                    return "active_state." + error;
            }
            return null;
        };

        NotifyFriendStateChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyFriendStateChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyFriendStateChange();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.active_state != null) {
                if (typeof object.active_state !== "object")
                    throw TypeError(".lq.NotifyFriendStateChange.active_state: object expected");
                message.active_state = $root.lq.AccountActiveState.fromObject(object.active_state, _depth + 1);
            }
            return message;
        };

        NotifyFriendStateChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.active_state = null;
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.active_state != null && message.hasOwnProperty("active_state"))
                object.active_state = $root.lq.AccountActiveState.toObject(message.active_state, options);
            return object;
        };

        NotifyFriendStateChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyFriendStateChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyFriendStateChange";
        };

        return NotifyFriendStateChange;
    })();

    lq.NotifyFriendViewChange = (function() {

        function NotifyFriendViewChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyFriendViewChange.prototype.target_id = 0;
        NotifyFriendViewChange.prototype.base = null;

        NotifyFriendViewChange.create = function create(properties) {
            return new NotifyFriendViewChange(properties);
        };

        NotifyFriendViewChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.lq.PlayerBaseView.encode(message.base, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyFriendViewChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyFriendViewChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyFriendViewChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.base = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.base);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyFriendViewChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyFriendViewChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.lq.PlayerBaseView.verify(message.base, _depth + 1);
                if (error)
                    return "base." + error;
            }
            return null;
        };

        NotifyFriendViewChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyFriendViewChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyFriendViewChange();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".lq.NotifyFriendViewChange.base: object expected");
                message.base = $root.lq.PlayerBaseView.fromObject(object.base, _depth + 1);
            }
            return message;
        };

        NotifyFriendViewChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.base = null;
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.lq.PlayerBaseView.toObject(message.base, options);
            return object;
        };

        NotifyFriendViewChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyFriendViewChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyFriendViewChange";
        };

        return NotifyFriendViewChange;
    })();

    lq.NotifyFriendChange = (function() {

        function NotifyFriendChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyFriendChange.prototype.account_id = 0;
        NotifyFriendChange.prototype.type = 0;
        NotifyFriendChange.prototype.friend = null;

        NotifyFriendChange.create = function create(properties) {
            return new NotifyFriendChange(properties);
        };

        NotifyFriendChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.friend != null && Object.hasOwnProperty.call(message, "friend"))
                $root.lq.Friend.encode(message.friend, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyFriendChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyFriendChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyFriendChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.friend = $root.lq.Friend.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyFriendChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyFriendChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.friend != null && message.hasOwnProperty("friend")) {
                let error = $root.lq.Friend.verify(message.friend, _depth + 1);
                if (error)
                    return "friend." + error;
            }
            return null;
        };

        NotifyFriendChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyFriendChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyFriendChange();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.friend != null) {
                if (typeof object.friend !== "object")
                    throw TypeError(".lq.NotifyFriendChange.friend: object expected");
                message.friend = $root.lq.Friend.fromObject(object.friend, _depth + 1);
            }
            return message;
        };

        NotifyFriendChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.type = 0;
                object.friend = null;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.friend != null && message.hasOwnProperty("friend"))
                object.friend = $root.lq.Friend.toObject(message.friend, options);
            return object;
        };

        NotifyFriendChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyFriendChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyFriendChange";
        };

        return NotifyFriendChange;
    })();

    lq.NotifyNewFriendApply = (function() {

        function NotifyNewFriendApply(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyNewFriendApply.prototype.account_id = 0;
        NotifyNewFriendApply.prototype.apply_time = 0;
        NotifyNewFriendApply.prototype.removed_id = 0;

        NotifyNewFriendApply.create = function create(properties) {
            return new NotifyNewFriendApply(properties);
        };

        NotifyNewFriendApply.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.apply_time != null && Object.hasOwnProperty.call(message, "apply_time"))
                writer.uint32(16).uint32(message.apply_time);
            if (message.removed_id != null && Object.hasOwnProperty.call(message, "removed_id"))
                writer.uint32(24).uint32(message.removed_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyNewFriendApply.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyNewFriendApply.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyNewFriendApply(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.apply_time = value;
                        else
                            delete message.apply_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.removed_id = value;
                        else
                            delete message.removed_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyNewFriendApply.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyNewFriendApply.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.apply_time != null && message.hasOwnProperty("apply_time"))
                if (!$util.isInteger(message.apply_time))
                    return "apply_time: integer expected";
            if (message.removed_id != null && message.hasOwnProperty("removed_id"))
                if (!$util.isInteger(message.removed_id))
                    return "removed_id: integer expected";
            return null;
        };

        NotifyNewFriendApply.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyNewFriendApply)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyNewFriendApply();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.apply_time != null)
                if (Number(object.apply_time) !== 0)
                    message.apply_time = object.apply_time >>> 0;
            if (object.removed_id != null)
                if (Number(object.removed_id) !== 0)
                    message.removed_id = object.removed_id >>> 0;
            return message;
        };

        NotifyNewFriendApply.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.apply_time = 0;
                object.removed_id = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.apply_time != null && message.hasOwnProperty("apply_time"))
                object.apply_time = message.apply_time;
            if (message.removed_id != null && message.hasOwnProperty("removed_id"))
                object.removed_id = message.removed_id;
            return object;
        };

        NotifyNewFriendApply.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyNewFriendApply.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyNewFriendApply";
        };

        return NotifyNewFriendApply;
    })();

    lq.NotifyClientMessage = (function() {

        function NotifyClientMessage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyClientMessage.prototype.sender = null;
        NotifyClientMessage.prototype.type = 0;
        NotifyClientMessage.prototype.content = "";

        NotifyClientMessage.create = function create(properties) {
            return new NotifyClientMessage(properties);
        };

        NotifyClientMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sender != null && Object.hasOwnProperty.call(message, "sender"))
                $root.lq.PlayerBaseView.encode(message.sender, writer.uint32(10).fork()).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(26).string(message.content);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyClientMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyClientMessage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyClientMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.sender = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.sender);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyClientMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyClientMessage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sender != null && message.hasOwnProperty("sender")) {
                let error = $root.lq.PlayerBaseView.verify(message.sender, _depth + 1);
                if (error)
                    return "sender." + error;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            return null;
        };

        NotifyClientMessage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyClientMessage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyClientMessage();
            if (object.sender != null) {
                if (typeof object.sender !== "object")
                    throw TypeError(".lq.NotifyClientMessage.sender: object expected");
                message.sender = $root.lq.PlayerBaseView.fromObject(object.sender, _depth + 1);
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            return message;
        };

        NotifyClientMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.sender = null;
                object.type = 0;
                object.content = "";
            }
            if (message.sender != null && message.hasOwnProperty("sender"))
                object.sender = $root.lq.PlayerBaseView.toObject(message.sender, options);
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            return object;
        };

        NotifyClientMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyClientMessage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyClientMessage";
        };

        return NotifyClientMessage;
    })();

    lq.NotifyAccountUpdate = (function() {

        function NotifyAccountUpdate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAccountUpdate.prototype.update = null;

        NotifyAccountUpdate.create = function create(properties) {
            return new NotifyAccountUpdate(properties);
        };

        NotifyAccountUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.update != null && Object.hasOwnProperty.call(message, "update"))
                $root.lq.AccountUpdate.encode(message.update, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAccountUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAccountUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAccountUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.update = $root.lq.AccountUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.update);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAccountUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAccountUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.update != null && message.hasOwnProperty("update")) {
                let error = $root.lq.AccountUpdate.verify(message.update, _depth + 1);
                if (error)
                    return "update." + error;
            }
            return null;
        };

        NotifyAccountUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAccountUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAccountUpdate();
            if (object.update != null) {
                if (typeof object.update !== "object")
                    throw TypeError(".lq.NotifyAccountUpdate.update: object expected");
                message.update = $root.lq.AccountUpdate.fromObject(object.update, _depth + 1);
            }
            return message;
        };

        NotifyAccountUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.update = null;
            if (message.update != null && message.hasOwnProperty("update"))
                object.update = $root.lq.AccountUpdate.toObject(message.update, options);
            return object;
        };

        NotifyAccountUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAccountUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAccountUpdate";
        };

        return NotifyAccountUpdate;
    })();

    lq.NotifyAnotherLogin = (function() {

        function NotifyAnotherLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAnotherLogin.create = function create(properties) {
            return new NotifyAnotherLogin(properties);
        };

        NotifyAnotherLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAnotherLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAnotherLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAnotherLogin();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAnotherLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAnotherLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyAnotherLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAnotherLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyAnotherLogin();
        };

        NotifyAnotherLogin.toObject = function toObject() {
            return {};
        };

        NotifyAnotherLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAnotherLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAnotherLogin";
        };

        return NotifyAnotherLogin;
    })();

    lq.NotifyAccountLogout = (function() {

        function NotifyAccountLogout(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAccountLogout.create = function create(properties) {
            return new NotifyAccountLogout(properties);
        };

        NotifyAccountLogout.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAccountLogout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAccountLogout.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAccountLogout();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAccountLogout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAccountLogout.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyAccountLogout.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAccountLogout)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyAccountLogout();
        };

        NotifyAccountLogout.toObject = function toObject() {
            return {};
        };

        NotifyAccountLogout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAccountLogout.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAccountLogout";
        };

        return NotifyAccountLogout;
    })();

    lq.NotifyAnnouncementUpdate = (function() {

        function NotifyAnnouncementUpdate(properties) {
            this.update_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAnnouncementUpdate.prototype.update_list = $util.emptyArray;

        NotifyAnnouncementUpdate.create = function create(properties) {
            return new NotifyAnnouncementUpdate(properties);
        };

        NotifyAnnouncementUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.update_list != null && message.update_list.length)
                for (let i = 0; i < message.update_list.length; ++i)
                    $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate.encode(message.update_list[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAnnouncementUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAnnouncementUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAnnouncementUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.update_list && message.update_list.length))
                            message.update_list = [];
                        message.update_list.push($root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAnnouncementUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAnnouncementUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.update_list != null && message.hasOwnProperty("update_list")) {
                if (!Array.isArray(message.update_list))
                    return "update_list: array expected";
                for (let i = 0; i < message.update_list.length; ++i) {
                    let error = $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate.verify(message.update_list[i], _depth + 1);
                    if (error)
                        return "update_list." + error;
                }
            }
            return null;
        };

        NotifyAnnouncementUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAnnouncementUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAnnouncementUpdate();
            if (object.update_list) {
                if (!Array.isArray(object.update_list))
                    throw TypeError(".lq.NotifyAnnouncementUpdate.update_list: array expected");
                message.update_list = Array(object.update_list.length);
                for (let i = 0; i < object.update_list.length; ++i) {
                    if (typeof object.update_list[i] !== "object")
                        throw TypeError(".lq.NotifyAnnouncementUpdate.update_list: object expected");
                    message.update_list[i] = $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate.fromObject(object.update_list[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyAnnouncementUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.update_list = [];
            if (message.update_list && message.update_list.length) {
                object.update_list = Array(message.update_list.length);
                for (let j = 0; j < message.update_list.length; ++j)
                    object.update_list[j] = $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate.toObject(message.update_list[j], options);
            }
            return object;
        };

        NotifyAnnouncementUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAnnouncementUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAnnouncementUpdate";
        };

        NotifyAnnouncementUpdate.AnnouncementUpdate = (function() {

            function AnnouncementUpdate(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AnnouncementUpdate.prototype.lang = "";
            AnnouncementUpdate.prototype.platform = "";

            AnnouncementUpdate.create = function create(properties) {
                return new AnnouncementUpdate(properties);
            };

            AnnouncementUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                    writer.uint32(10).string(message.lang);
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    writer.uint32(18).string(message.platform);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AnnouncementUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AnnouncementUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.lang = value;
                            else
                                delete message.lang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.platform = value;
                            else
                                delete message.platform;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AnnouncementUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AnnouncementUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.lang != null && message.hasOwnProperty("lang"))
                    if (!$util.isString(message.lang))
                        return "lang: string expected";
                if (message.platform != null && message.hasOwnProperty("platform"))
                    if (!$util.isString(message.platform))
                        return "platform: string expected";
                return null;
            };

            AnnouncementUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyAnnouncementUpdate.AnnouncementUpdate();
                if (object.lang != null)
                    if (typeof object.lang !== "string" || object.lang.length)
                        message.lang = String(object.lang);
                if (object.platform != null)
                    if (typeof object.platform !== "string" || object.platform.length)
                        message.platform = String(object.platform);
                return message;
            };

            AnnouncementUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.lang = "";
                    object.platform = "";
                }
                if (message.lang != null && message.hasOwnProperty("lang"))
                    object.lang = message.lang;
                if (message.platform != null && message.hasOwnProperty("platform"))
                    object.platform = message.platform;
                return object;
            };

            AnnouncementUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AnnouncementUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyAnnouncementUpdate.AnnouncementUpdate";
            };

            return AnnouncementUpdate;
        })();

        return NotifyAnnouncementUpdate;
    })();

    lq.NotifyNewMail = (function() {

        function NotifyNewMail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyNewMail.prototype.mail = null;

        NotifyNewMail.create = function create(properties) {
            return new NotifyNewMail(properties);
        };

        NotifyNewMail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail != null && Object.hasOwnProperty.call(message, "mail"))
                $root.lq.Mail.encode(message.mail, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyNewMail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyNewMail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyNewMail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.mail = $root.lq.Mail.decode(reader, reader.uint32(), undefined, _depth + 1, message.mail);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyNewMail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyNewMail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail != null && message.hasOwnProperty("mail")) {
                let error = $root.lq.Mail.verify(message.mail, _depth + 1);
                if (error)
                    return "mail." + error;
            }
            return null;
        };

        NotifyNewMail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyNewMail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyNewMail();
            if (object.mail != null) {
                if (typeof object.mail !== "object")
                    throw TypeError(".lq.NotifyNewMail.mail: object expected");
                message.mail = $root.lq.Mail.fromObject(object.mail, _depth + 1);
            }
            return message;
        };

        NotifyNewMail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.mail = null;
            if (message.mail != null && message.hasOwnProperty("mail"))
                object.mail = $root.lq.Mail.toObject(message.mail, options);
            return object;
        };

        NotifyNewMail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyNewMail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyNewMail";
        };

        return NotifyNewMail;
    })();

    lq.NotifyDeleteMail = (function() {

        function NotifyDeleteMail(properties) {
            this.mail_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyDeleteMail.prototype.mail_id_list = $util.emptyArray;

        NotifyDeleteMail.create = function create(properties) {
            return new NotifyDeleteMail(properties);
        };

        NotifyDeleteMail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail_id_list != null && message.mail_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.mail_id_list.length; ++i)
                    writer.uint32(message.mail_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyDeleteMail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyDeleteMail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyDeleteMail();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.mail_id_list && message.mail_id_list.length))
                                message.mail_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.mail_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.mail_id_list && message.mail_id_list.length))
                            message.mail_id_list = [];
                        message.mail_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyDeleteMail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyDeleteMail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail_id_list != null && message.hasOwnProperty("mail_id_list")) {
                if (!Array.isArray(message.mail_id_list))
                    return "mail_id_list: array expected";
                for (let i = 0; i < message.mail_id_list.length; ++i)
                    if (!$util.isInteger(message.mail_id_list[i]))
                        return "mail_id_list: integer[] expected";
            }
            return null;
        };

        NotifyDeleteMail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyDeleteMail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyDeleteMail();
            if (object.mail_id_list) {
                if (!Array.isArray(object.mail_id_list))
                    throw TypeError(".lq.NotifyDeleteMail.mail_id_list: array expected");
                message.mail_id_list = Array(object.mail_id_list.length);
                for (let i = 0; i < object.mail_id_list.length; ++i)
                    message.mail_id_list[i] = object.mail_id_list[i] >>> 0;
            }
            return message;
        };

        NotifyDeleteMail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.mail_id_list = [];
            if (message.mail_id_list && message.mail_id_list.length) {
                object.mail_id_list = Array(message.mail_id_list.length);
                for (let j = 0; j < message.mail_id_list.length; ++j)
                    object.mail_id_list[j] = message.mail_id_list[j];
            }
            return object;
        };

        NotifyDeleteMail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyDeleteMail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyDeleteMail";
        };

        return NotifyDeleteMail;
    })();

    lq.NotifyReviveCoinUpdate = (function() {

        function NotifyReviveCoinUpdate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyReviveCoinUpdate.prototype.has_gained = false;

        NotifyReviveCoinUpdate.create = function create(properties) {
            return new NotifyReviveCoinUpdate(properties);
        };

        NotifyReviveCoinUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.has_gained != null && Object.hasOwnProperty.call(message, "has_gained"))
                writer.uint32(8).bool(message.has_gained);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyReviveCoinUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyReviveCoinUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyReviveCoinUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.has_gained = value;
                        else
                            delete message.has_gained;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyReviveCoinUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyReviveCoinUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.has_gained != null && message.hasOwnProperty("has_gained"))
                if (typeof message.has_gained !== "boolean")
                    return "has_gained: boolean expected";
            return null;
        };

        NotifyReviveCoinUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyReviveCoinUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyReviveCoinUpdate();
            if (object.has_gained != null)
                if (object.has_gained)
                    message.has_gained = Boolean(object.has_gained);
            return message;
        };

        NotifyReviveCoinUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.has_gained = false;
            if (message.has_gained != null && message.hasOwnProperty("has_gained"))
                object.has_gained = message.has_gained;
            return object;
        };

        NotifyReviveCoinUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyReviveCoinUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyReviveCoinUpdate";
        };

        return NotifyReviveCoinUpdate;
    })();

    lq.NotifyDailyTaskUpdate = (function() {

        function NotifyDailyTaskUpdate(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyDailyTaskUpdate.prototype.progresses = $util.emptyArray;
        NotifyDailyTaskUpdate.prototype.max_daily_task_count = 0;
        NotifyDailyTaskUpdate.prototype.refresh_count = 0;

        NotifyDailyTaskUpdate.create = function create(properties) {
            return new NotifyDailyTaskUpdate(properties);
        };

        NotifyDailyTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.max_daily_task_count != null && Object.hasOwnProperty.call(message, "max_daily_task_count"))
                writer.uint32(16).uint32(message.max_daily_task_count);
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyDailyTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyDailyTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyDailyTaskUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_daily_task_count = value;
                        else
                            delete message.max_daily_task_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyDailyTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyDailyTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            if (message.max_daily_task_count != null && message.hasOwnProperty("max_daily_task_count"))
                if (!$util.isInteger(message.max_daily_task_count))
                    return "max_daily_task_count: integer expected";
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            return null;
        };

        NotifyDailyTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyDailyTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyDailyTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyDailyTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyDailyTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            if (object.max_daily_task_count != null)
                if (Number(object.max_daily_task_count) !== 0)
                    message.max_daily_task_count = object.max_daily_task_count >>> 0;
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            return message;
        };

        NotifyDailyTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (options.defaults) {
                object.max_daily_task_count = 0;
                object.refresh_count = 0;
            }
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            if (message.max_daily_task_count != null && message.hasOwnProperty("max_daily_task_count"))
                object.max_daily_task_count = message.max_daily_task_count;
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            return object;
        };

        NotifyDailyTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyDailyTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyDailyTaskUpdate";
        };

        return NotifyDailyTaskUpdate;
    })();

    lq.NotifyActivityTaskUpdate = (function() {

        function NotifyActivityTaskUpdate(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityTaskUpdate.prototype.progresses = $util.emptyArray;

        NotifyActivityTaskUpdate.create = function create(properties) {
            return new NotifyActivityTaskUpdate(properties);
        };

        NotifyActivityTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityTaskUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            return null;
        };

        NotifyActivityTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyActivityTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyActivityTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            return object;
        };

        NotifyActivityTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityTaskUpdate";
        };

        return NotifyActivityTaskUpdate;
    })();

    lq.NotifyActivityPeriodTaskUpdate = (function() {

        function NotifyActivityPeriodTaskUpdate(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityPeriodTaskUpdate.prototype.progresses = $util.emptyArray;

        NotifyActivityPeriodTaskUpdate.create = function create(properties) {
            return new NotifyActivityPeriodTaskUpdate(properties);
        };

        NotifyActivityPeriodTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityPeriodTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityPeriodTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityPeriodTaskUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityPeriodTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityPeriodTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            return null;
        };

        NotifyActivityPeriodTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityPeriodTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityPeriodTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyActivityPeriodTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyActivityPeriodTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityPeriodTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            return object;
        };

        NotifyActivityPeriodTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityPeriodTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityPeriodTaskUpdate";
        };

        return NotifyActivityPeriodTaskUpdate;
    })();

    lq.NotifyAccountRandomTaskUpdate = (function() {

        function NotifyAccountRandomTaskUpdate(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAccountRandomTaskUpdate.prototype.progresses = $util.emptyArray;

        NotifyAccountRandomTaskUpdate.create = function create(properties) {
            return new NotifyAccountRandomTaskUpdate(properties);
        };

        NotifyAccountRandomTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAccountRandomTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAccountRandomTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAccountRandomTaskUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAccountRandomTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAccountRandomTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            return null;
        };

        NotifyAccountRandomTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAccountRandomTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAccountRandomTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyAccountRandomTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyAccountRandomTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyAccountRandomTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            return object;
        };

        NotifyAccountRandomTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAccountRandomTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAccountRandomTaskUpdate";
        };

        return NotifyAccountRandomTaskUpdate;
    })();

    lq.NotifyActivitySegmentTaskUpdate = (function() {

        function NotifyActivitySegmentTaskUpdate(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivitySegmentTaskUpdate.prototype.progresses = $util.emptyArray;

        NotifyActivitySegmentTaskUpdate.create = function create(properties) {
            return new NotifyActivitySegmentTaskUpdate(properties);
        };

        NotifyActivitySegmentTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.SegmentTaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivitySegmentTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivitySegmentTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivitySegmentTaskUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.SegmentTaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivitySegmentTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivitySegmentTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.SegmentTaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            return null;
        };

        NotifyActivitySegmentTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivitySegmentTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivitySegmentTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyActivitySegmentTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyActivitySegmentTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.SegmentTaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivitySegmentTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.SegmentTaskProgress.toObject(message.progresses[j], options);
            }
            return object;
        };

        NotifyActivitySegmentTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivitySegmentTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivitySegmentTaskUpdate";
        };

        return NotifyActivitySegmentTaskUpdate;
    })();

    lq.NotifyActivityUpdate = (function() {

        function NotifyActivityUpdate(properties) {
            this.list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityUpdate.prototype.list = $util.emptyArray;

        NotifyActivityUpdate.create = function create(properties) {
            return new NotifyActivityUpdate(properties);
        };

        NotifyActivityUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.list != null && message.list.length)
                for (let i = 0; i < message.list.length; ++i)
                    $root.lq.NotifyActivityUpdate.FeedActivityData.encode(message.list[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.lq.NotifyActivityUpdate.FeedActivityData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (let i = 0; i < message.list.length; ++i) {
                    let error = $root.lq.NotifyActivityUpdate.FeedActivityData.verify(message.list[i], _depth + 1);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        NotifyActivityUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityUpdate();
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".lq.NotifyActivityUpdate.list: array expected");
                message.list = Array(object.list.length);
                for (let i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".lq.NotifyActivityUpdate.list: object expected");
                    message.list[i] = $root.lq.NotifyActivityUpdate.FeedActivityData.fromObject(object.list[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (message.list && message.list.length) {
                object.list = Array(message.list.length);
                for (let j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.lq.NotifyActivityUpdate.FeedActivityData.toObject(message.list[j], options);
            }
            return object;
        };

        NotifyActivityUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityUpdate";
        };

        NotifyActivityUpdate.FeedActivityData = (function() {

            function FeedActivityData(properties) {
                this.gift_inbox = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FeedActivityData.prototype.activity_id = 0;
            FeedActivityData.prototype.feed_count = 0;
            FeedActivityData.prototype.friend_receive_data = null;
            FeedActivityData.prototype.friend_send_data = null;
            FeedActivityData.prototype.gift_inbox = $util.emptyArray;

            FeedActivityData.create = function create(properties) {
                return new FeedActivityData(properties);
            };

            FeedActivityData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.feed_count != null && Object.hasOwnProperty.call(message, "feed_count"))
                    writer.uint32(16).uint32(message.feed_count);
                if (message.friend_receive_data != null && Object.hasOwnProperty.call(message, "friend_receive_data"))
                    $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.encode(message.friend_receive_data, writer.uint32(26).fork()).ldelim();
                if (message.friend_send_data != null && Object.hasOwnProperty.call(message, "friend_send_data"))
                    $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.encode(message.friend_send_data, writer.uint32(34).fork()).ldelim();
                if (message.gift_inbox != null && message.gift_inbox.length)
                    for (let i = 0; i < message.gift_inbox.length; ++i)
                        $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData.encode(message.gift_inbox[i], writer.uint32(42).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FeedActivityData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FeedActivityData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityUpdate.FeedActivityData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.feed_count = value;
                            else
                                delete message.feed_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.friend_receive_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_receive_data);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.friend_send_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_send_data);
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.gift_inbox && message.gift_inbox.length))
                                message.gift_inbox = [];
                            message.gift_inbox.push($root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FeedActivityData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FeedActivityData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                    if (!$util.isInteger(message.feed_count))
                        return "feed_count: integer expected";
                if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data")) {
                    let error = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.verify(message.friend_receive_data, _depth + 1);
                    if (error)
                        return "friend_receive_data." + error;
                }
                if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data")) {
                    let error = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.verify(message.friend_send_data, _depth + 1);
                    if (error)
                        return "friend_send_data." + error;
                }
                if (message.gift_inbox != null && message.hasOwnProperty("gift_inbox")) {
                    if (!Array.isArray(message.gift_inbox))
                        return "gift_inbox: array expected";
                    for (let i = 0; i < message.gift_inbox.length; ++i) {
                        let error = $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData.verify(message.gift_inbox[i], _depth + 1);
                        if (error)
                            return "gift_inbox." + error;
                    }
                }
                return null;
            };

            FeedActivityData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyActivityUpdate.FeedActivityData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyActivityUpdate.FeedActivityData();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.feed_count != null)
                    if (Number(object.feed_count) !== 0)
                        message.feed_count = object.feed_count >>> 0;
                if (object.friend_receive_data != null) {
                    if (typeof object.friend_receive_data !== "object")
                        throw TypeError(".lq.NotifyActivityUpdate.FeedActivityData.friend_receive_data: object expected");
                    message.friend_receive_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.fromObject(object.friend_receive_data, _depth + 1);
                }
                if (object.friend_send_data != null) {
                    if (typeof object.friend_send_data !== "object")
                        throw TypeError(".lq.NotifyActivityUpdate.FeedActivityData.friend_send_data: object expected");
                    message.friend_send_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.fromObject(object.friend_send_data, _depth + 1);
                }
                if (object.gift_inbox) {
                    if (!Array.isArray(object.gift_inbox))
                        throw TypeError(".lq.NotifyActivityUpdate.FeedActivityData.gift_inbox: array expected");
                    message.gift_inbox = Array(object.gift_inbox.length);
                    for (let i = 0; i < object.gift_inbox.length; ++i) {
                        if (typeof object.gift_inbox[i] !== "object")
                            throw TypeError(".lq.NotifyActivityUpdate.FeedActivityData.gift_inbox: object expected");
                        message.gift_inbox[i] = $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData.fromObject(object.gift_inbox[i], _depth + 1);
                    }
                }
                return message;
            };

            FeedActivityData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.gift_inbox = [];
                if (options.defaults) {
                    object.activity_id = 0;
                    object.feed_count = 0;
                    object.friend_receive_data = null;
                    object.friend_send_data = null;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                    object.feed_count = message.feed_count;
                if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data"))
                    object.friend_receive_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.toObject(message.friend_receive_data, options);
                if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data"))
                    object.friend_send_data = $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData.toObject(message.friend_send_data, options);
                if (message.gift_inbox && message.gift_inbox.length) {
                    object.gift_inbox = Array(message.gift_inbox.length);
                    for (let j = 0; j < message.gift_inbox.length; ++j)
                        object.gift_inbox[j] = $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData.toObject(message.gift_inbox[j], options);
                }
                return object;
            };

            FeedActivityData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FeedActivityData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyActivityUpdate.FeedActivityData";
            };

            FeedActivityData.CountWithTimeData = (function() {

                function CountWithTimeData(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                CountWithTimeData.prototype.count = 0;
                CountWithTimeData.prototype.last_update_time = 0;

                CountWithTimeData.create = function create(properties) {
                    return new CountWithTimeData(properties);
                };

                CountWithTimeData.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                        writer.uint32(8).uint32(message.count);
                    if (message.last_update_time != null && Object.hasOwnProperty.call(message, "last_update_time"))
                        writer.uint32(16).uint32(message.last_update_time);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                CountWithTimeData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                CountWithTimeData.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.count = value;
                                else
                                    delete message.count;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.last_update_time = value;
                                else
                                    delete message.last_update_time;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                CountWithTimeData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                CountWithTimeData.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.count != null && message.hasOwnProperty("count"))
                        if (!$util.isInteger(message.count))
                            return "count: integer expected";
                    if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                        if (!$util.isInteger(message.last_update_time))
                            return "last_update_time: integer expected";
                    return null;
                };

                CountWithTimeData.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData();
                    if (object.count != null)
                        if (Number(object.count) !== 0)
                            message.count = object.count >>> 0;
                    if (object.last_update_time != null)
                        if (Number(object.last_update_time) !== 0)
                            message.last_update_time = object.last_update_time >>> 0;
                    return message;
                };

                CountWithTimeData.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.count = 0;
                        object.last_update_time = 0;
                    }
                    if (message.count != null && message.hasOwnProperty("count"))
                        object.count = message.count;
                    if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                        object.last_update_time = message.last_update_time;
                    return object;
                };

                CountWithTimeData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                CountWithTimeData.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.NotifyActivityUpdate.FeedActivityData.CountWithTimeData";
                };

                return CountWithTimeData;
            })();

            FeedActivityData.GiftBoxData = (function() {

                function GiftBoxData(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                GiftBoxData.prototype.id = 0;
                GiftBoxData.prototype.item_id = 0;
                GiftBoxData.prototype.count = 0;
                GiftBoxData.prototype.from_account_id = 0;
                GiftBoxData.prototype.time = 0;
                GiftBoxData.prototype.received = 0;

                GiftBoxData.create = function create(properties) {
                    return new GiftBoxData(properties);
                };

                GiftBoxData.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(8).uint32(message.id);
                    if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                        writer.uint32(16).uint32(message.item_id);
                    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                        writer.uint32(24).uint32(message.count);
                    if (message.from_account_id != null && Object.hasOwnProperty.call(message, "from_account_id"))
                        writer.uint32(32).uint32(message.from_account_id);
                    if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                        writer.uint32(40).uint32(message.time);
                    if (message.received != null && Object.hasOwnProperty.call(message, "received"))
                        writer.uint32(48).uint32(message.received);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                GiftBoxData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                GiftBoxData.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.id = value;
                                else
                                    delete message.id;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.item_id = value;
                                else
                                    delete message.item_id;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.count = value;
                                else
                                    delete message.count;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.from_account_id = value;
                                else
                                    delete message.from_account_id;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.time = value;
                                else
                                    delete message.time;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.received = value;
                                else
                                    delete message.received;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                GiftBoxData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                GiftBoxData.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.id != null && message.hasOwnProperty("id"))
                        if (!$util.isInteger(message.id))
                            return "id: integer expected";
                    if (message.item_id != null && message.hasOwnProperty("item_id"))
                        if (!$util.isInteger(message.item_id))
                            return "item_id: integer expected";
                    if (message.count != null && message.hasOwnProperty("count"))
                        if (!$util.isInteger(message.count))
                            return "count: integer expected";
                    if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                        if (!$util.isInteger(message.from_account_id))
                            return "from_account_id: integer expected";
                    if (message.time != null && message.hasOwnProperty("time"))
                        if (!$util.isInteger(message.time))
                            return "time: integer expected";
                    if (message.received != null && message.hasOwnProperty("received"))
                        if (!$util.isInteger(message.received))
                            return "received: integer expected";
                    return null;
                };

                GiftBoxData.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData();
                    if (object.id != null)
                        if (Number(object.id) !== 0)
                            message.id = object.id >>> 0;
                    if (object.item_id != null)
                        if (Number(object.item_id) !== 0)
                            message.item_id = object.item_id >>> 0;
                    if (object.count != null)
                        if (Number(object.count) !== 0)
                            message.count = object.count >>> 0;
                    if (object.from_account_id != null)
                        if (Number(object.from_account_id) !== 0)
                            message.from_account_id = object.from_account_id >>> 0;
                    if (object.time != null)
                        if (Number(object.time) !== 0)
                            message.time = object.time >>> 0;
                    if (object.received != null)
                        if (Number(object.received) !== 0)
                            message.received = object.received >>> 0;
                    return message;
                };

                GiftBoxData.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.id = 0;
                        object.item_id = 0;
                        object.count = 0;
                        object.from_account_id = 0;
                        object.time = 0;
                        object.received = 0;
                    }
                    if (message.id != null && message.hasOwnProperty("id"))
                        object.id = message.id;
                    if (message.item_id != null && message.hasOwnProperty("item_id"))
                        object.item_id = message.item_id;
                    if (message.count != null && message.hasOwnProperty("count"))
                        object.count = message.count;
                    if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                        object.from_account_id = message.from_account_id;
                    if (message.time != null && message.hasOwnProperty("time"))
                        object.time = message.time;
                    if (message.received != null && message.hasOwnProperty("received"))
                        object.received = message.received;
                    return object;
                };

                GiftBoxData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                GiftBoxData.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.NotifyActivityUpdate.FeedActivityData.GiftBoxData";
                };

                return GiftBoxData;
            })();

            return FeedActivityData;
        })();

        return NotifyActivityUpdate;
    })();

    lq.NotifyAccountChallengeTaskUpdate = (function() {

        function NotifyAccountChallengeTaskUpdate(properties) {
            this.progresses = [];
            this.rewarded_season = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAccountChallengeTaskUpdate.prototype.progresses = $util.emptyArray;
        NotifyAccountChallengeTaskUpdate.prototype.level = 0;
        NotifyAccountChallengeTaskUpdate.prototype.refresh_count = 0;
        NotifyAccountChallengeTaskUpdate.prototype.match_count = 0;
        NotifyAccountChallengeTaskUpdate.prototype.ticket_id = 0;
        NotifyAccountChallengeTaskUpdate.prototype.rewarded_season = $util.emptyArray;

        NotifyAccountChallengeTaskUpdate.create = function create(properties) {
            return new NotifyAccountChallengeTaskUpdate(properties);
        };

        NotifyAccountChallengeTaskUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                writer.uint32(32).uint32(message.match_count);
            if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                writer.uint32(40).uint32(message.ticket_id);
            if (message.rewarded_season != null && message.rewarded_season.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.rewarded_season.length; ++i)
                    writer.uint32(message.rewarded_season[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAccountChallengeTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAccountChallengeTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAccountChallengeTaskUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_count = value;
                        else
                            delete message.match_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_id = value;
                        else
                            delete message.ticket_id;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.rewarded_season && message.rewarded_season.length))
                                message.rewarded_season = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_season.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_season && message.rewarded_season.length))
                            message.rewarded_season = [];
                        message.rewarded_season.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAccountChallengeTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAccountChallengeTaskUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                if (!$util.isInteger(message.match_count))
                    return "match_count: integer expected";
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                if (!$util.isInteger(message.ticket_id))
                    return "ticket_id: integer expected";
            if (message.rewarded_season != null && message.hasOwnProperty("rewarded_season")) {
                if (!Array.isArray(message.rewarded_season))
                    return "rewarded_season: array expected";
                for (let i = 0; i < message.rewarded_season.length; ++i)
                    if (!$util.isInteger(message.rewarded_season[i]))
                        return "rewarded_season: integer[] expected";
            }
            return null;
        };

        NotifyAccountChallengeTaskUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAccountChallengeTaskUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAccountChallengeTaskUpdate();
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.NotifyAccountChallengeTaskUpdate.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.NotifyAccountChallengeTaskUpdate.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            if (object.match_count != null)
                if (Number(object.match_count) !== 0)
                    message.match_count = object.match_count >>> 0;
            if (object.ticket_id != null)
                if (Number(object.ticket_id) !== 0)
                    message.ticket_id = object.ticket_id >>> 0;
            if (object.rewarded_season) {
                if (!Array.isArray(object.rewarded_season))
                    throw TypeError(".lq.NotifyAccountChallengeTaskUpdate.rewarded_season: array expected");
                message.rewarded_season = Array(object.rewarded_season.length);
                for (let i = 0; i < object.rewarded_season.length; ++i)
                    message.rewarded_season[i] = object.rewarded_season[i] >>> 0;
            }
            return message;
        };

        NotifyAccountChallengeTaskUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.progresses = [];
                object.rewarded_season = [];
            }
            if (options.defaults) {
                object.level = 0;
                object.refresh_count = 0;
                object.match_count = 0;
                object.ticket_id = 0;
            }
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                object.match_count = message.match_count;
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                object.ticket_id = message.ticket_id;
            if (message.rewarded_season && message.rewarded_season.length) {
                object.rewarded_season = Array(message.rewarded_season.length);
                for (let j = 0; j < message.rewarded_season.length; ++j)
                    object.rewarded_season[j] = message.rewarded_season[j];
            }
            return object;
        };

        NotifyAccountChallengeTaskUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAccountChallengeTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAccountChallengeTaskUpdate";
        };

        return NotifyAccountChallengeTaskUpdate;
    })();

    lq.NotifyNewComment = (function() {

        function NotifyNewComment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyNewComment.create = function create(properties) {
            return new NotifyNewComment(properties);
        };

        NotifyNewComment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyNewComment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyNewComment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyNewComment();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyNewComment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyNewComment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyNewComment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyNewComment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyNewComment();
        };

        NotifyNewComment.toObject = function toObject() {
            return {};
        };

        NotifyNewComment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyNewComment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyNewComment";
        };

        return NotifyNewComment;
    })();

    lq.NotifyRollingNotice = (function() {

        function NotifyRollingNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyRollingNotice.create = function create(properties) {
            return new NotifyRollingNotice(properties);
        };

        NotifyRollingNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyRollingNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyRollingNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyRollingNotice();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyRollingNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyRollingNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyRollingNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyRollingNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyRollingNotice();
        };

        NotifyRollingNotice.toObject = function toObject() {
            return {};
        };

        NotifyRollingNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyRollingNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyRollingNotice";
        };

        return NotifyRollingNotice;
    })();

    lq.NotifyMaintainNotice = (function() {

        function NotifyMaintainNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyMaintainNotice.create = function create(properties) {
            return new NotifyMaintainNotice(properties);
        };

        NotifyMaintainNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyMaintainNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyMaintainNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyMaintainNotice();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyMaintainNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyMaintainNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyMaintainNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyMaintainNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyMaintainNotice();
        };

        NotifyMaintainNotice.toObject = function toObject() {
            return {};
        };

        NotifyMaintainNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyMaintainNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyMaintainNotice";
        };

        return NotifyMaintainNotice;
    })();

    lq.NotifyGiftSendRefresh = (function() {

        function NotifyGiftSendRefresh(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGiftSendRefresh.create = function create(properties) {
            return new NotifyGiftSendRefresh(properties);
        };

        NotifyGiftSendRefresh.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGiftSendRefresh.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGiftSendRefresh.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGiftSendRefresh();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGiftSendRefresh.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGiftSendRefresh.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyGiftSendRefresh.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGiftSendRefresh)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyGiftSendRefresh();
        };

        NotifyGiftSendRefresh.toObject = function toObject() {
            return {};
        };

        NotifyGiftSendRefresh.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGiftSendRefresh.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGiftSendRefresh";
        };

        return NotifyGiftSendRefresh;
    })();

    lq.NotifyShopUpdate = (function() {

        function NotifyShopUpdate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyShopUpdate.prototype.shop_info = null;

        NotifyShopUpdate.create = function create(properties) {
            return new NotifyShopUpdate(properties);
        };

        NotifyShopUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.shop_info != null && Object.hasOwnProperty.call(message, "shop_info"))
                $root.lq.ShopInfo.encode(message.shop_info, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyShopUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyShopUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyShopUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.shop_info = $root.lq.ShopInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyShopUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyShopUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shop_info != null && message.hasOwnProperty("shop_info")) {
                let error = $root.lq.ShopInfo.verify(message.shop_info, _depth + 1);
                if (error)
                    return "shop_info." + error;
            }
            return null;
        };

        NotifyShopUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyShopUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyShopUpdate();
            if (object.shop_info != null) {
                if (typeof object.shop_info !== "object")
                    throw TypeError(".lq.NotifyShopUpdate.shop_info: object expected");
                message.shop_info = $root.lq.ShopInfo.fromObject(object.shop_info, _depth + 1);
            }
            return message;
        };

        NotifyShopUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.shop_info = null;
            if (message.shop_info != null && message.hasOwnProperty("shop_info"))
                object.shop_info = $root.lq.ShopInfo.toObject(message.shop_info, options);
            return object;
        };

        NotifyShopUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyShopUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyShopUpdate";
        };

        return NotifyShopUpdate;
    })();

    lq.NotifyIntervalUpdate = (function() {

        function NotifyIntervalUpdate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyIntervalUpdate.create = function create(properties) {
            return new NotifyIntervalUpdate(properties);
        };

        NotifyIntervalUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyIntervalUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyIntervalUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyIntervalUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyIntervalUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyIntervalUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyIntervalUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyIntervalUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyIntervalUpdate();
        };

        NotifyIntervalUpdate.toObject = function toObject() {
            return {};
        };

        NotifyIntervalUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyIntervalUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyIntervalUpdate";
        };

        return NotifyIntervalUpdate;
    })();

    lq.NotifyVipLevelChange = (function() {

        function NotifyVipLevelChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyVipLevelChange.prototype.gift_limit = 0;
        NotifyVipLevelChange.prototype.friend_max_count = 0;
        NotifyVipLevelChange.prototype.zhp_free_refresh_limit = 0;
        NotifyVipLevelChange.prototype.zhp_cost_refresh_limit = 0;
        NotifyVipLevelChange.prototype.buddy_bonus = 0;
        NotifyVipLevelChange.prototype.record_collect_limit = 0;

        NotifyVipLevelChange.create = function create(properties) {
            return new NotifyVipLevelChange(properties);
        };

        NotifyVipLevelChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gift_limit != null && Object.hasOwnProperty.call(message, "gift_limit"))
                writer.uint32(8).uint32(message.gift_limit);
            if (message.friend_max_count != null && Object.hasOwnProperty.call(message, "friend_max_count"))
                writer.uint32(16).uint32(message.friend_max_count);
            if (message.zhp_free_refresh_limit != null && Object.hasOwnProperty.call(message, "zhp_free_refresh_limit"))
                writer.uint32(24).uint32(message.zhp_free_refresh_limit);
            if (message.zhp_cost_refresh_limit != null && Object.hasOwnProperty.call(message, "zhp_cost_refresh_limit"))
                writer.uint32(32).uint32(message.zhp_cost_refresh_limit);
            if (message.buddy_bonus != null && Object.hasOwnProperty.call(message, "buddy_bonus"))
                writer.uint32(45).float(message.buddy_bonus);
            if (message.record_collect_limit != null && Object.hasOwnProperty.call(message, "record_collect_limit"))
                writer.uint32(48).uint32(message.record_collect_limit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyVipLevelChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyVipLevelChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyVipLevelChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gift_limit = value;
                        else
                            delete message.gift_limit;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.friend_max_count = value;
                        else
                            delete message.friend_max_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.zhp_free_refresh_limit = value;
                        else
                            delete message.zhp_free_refresh_limit;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.zhp_cost_refresh_limit = value;
                        else
                            delete message.zhp_cost_refresh_limit;
                        continue;
                    }
                case 5: {
                        if (wireType !== 5)
                            break;
                        if ((value = reader.float()) !== 0)
                            message.buddy_bonus = value;
                        else
                            delete message.buddy_bonus;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.record_collect_limit = value;
                        else
                            delete message.record_collect_limit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyVipLevelChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyVipLevelChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gift_limit != null && message.hasOwnProperty("gift_limit"))
                if (!$util.isInteger(message.gift_limit))
                    return "gift_limit: integer expected";
            if (message.friend_max_count != null && message.hasOwnProperty("friend_max_count"))
                if (!$util.isInteger(message.friend_max_count))
                    return "friend_max_count: integer expected";
            if (message.zhp_free_refresh_limit != null && message.hasOwnProperty("zhp_free_refresh_limit"))
                if (!$util.isInteger(message.zhp_free_refresh_limit))
                    return "zhp_free_refresh_limit: integer expected";
            if (message.zhp_cost_refresh_limit != null && message.hasOwnProperty("zhp_cost_refresh_limit"))
                if (!$util.isInteger(message.zhp_cost_refresh_limit))
                    return "zhp_cost_refresh_limit: integer expected";
            if (message.buddy_bonus != null && message.hasOwnProperty("buddy_bonus"))
                if (typeof message.buddy_bonus !== "number")
                    return "buddy_bonus: number expected";
            if (message.record_collect_limit != null && message.hasOwnProperty("record_collect_limit"))
                if (!$util.isInteger(message.record_collect_limit))
                    return "record_collect_limit: integer expected";
            return null;
        };

        NotifyVipLevelChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyVipLevelChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyVipLevelChange();
            if (object.gift_limit != null)
                if (Number(object.gift_limit) !== 0)
                    message.gift_limit = object.gift_limit >>> 0;
            if (object.friend_max_count != null)
                if (Number(object.friend_max_count) !== 0)
                    message.friend_max_count = object.friend_max_count >>> 0;
            if (object.zhp_free_refresh_limit != null)
                if (Number(object.zhp_free_refresh_limit) !== 0)
                    message.zhp_free_refresh_limit = object.zhp_free_refresh_limit >>> 0;
            if (object.zhp_cost_refresh_limit != null)
                if (Number(object.zhp_cost_refresh_limit) !== 0)
                    message.zhp_cost_refresh_limit = object.zhp_cost_refresh_limit >>> 0;
            if (object.buddy_bonus != null)
                if (Number(object.buddy_bonus) !== 0)
                    message.buddy_bonus = Number(object.buddy_bonus);
            if (object.record_collect_limit != null)
                if (Number(object.record_collect_limit) !== 0)
                    message.record_collect_limit = object.record_collect_limit >>> 0;
            return message;
        };

        NotifyVipLevelChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.gift_limit = 0;
                object.friend_max_count = 0;
                object.zhp_free_refresh_limit = 0;
                object.zhp_cost_refresh_limit = 0;
                object.buddy_bonus = 0;
                object.record_collect_limit = 0;
            }
            if (message.gift_limit != null && message.hasOwnProperty("gift_limit"))
                object.gift_limit = message.gift_limit;
            if (message.friend_max_count != null && message.hasOwnProperty("friend_max_count"))
                object.friend_max_count = message.friend_max_count;
            if (message.zhp_free_refresh_limit != null && message.hasOwnProperty("zhp_free_refresh_limit"))
                object.zhp_free_refresh_limit = message.zhp_free_refresh_limit;
            if (message.zhp_cost_refresh_limit != null && message.hasOwnProperty("zhp_cost_refresh_limit"))
                object.zhp_cost_refresh_limit = message.zhp_cost_refresh_limit;
            if (message.buddy_bonus != null && message.hasOwnProperty("buddy_bonus"))
                object.buddy_bonus = options.json && !isFinite(message.buddy_bonus) ? String(message.buddy_bonus) : message.buddy_bonus;
            if (message.record_collect_limit != null && message.hasOwnProperty("record_collect_limit"))
                object.record_collect_limit = message.record_collect_limit;
            return object;
        };

        NotifyVipLevelChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyVipLevelChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyVipLevelChange";
        };

        return NotifyVipLevelChange;
    })();

    lq.NotifyServerSetting = (function() {

        function NotifyServerSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyServerSetting.prototype.settings = null;

        NotifyServerSetting.create = function create(properties) {
            return new NotifyServerSetting(properties);
        };

        NotifyServerSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.settings != null && Object.hasOwnProperty.call(message, "settings"))
                $root.lq.ServerSettings.encode(message.settings, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyServerSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyServerSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyServerSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.settings = $root.lq.ServerSettings.decode(reader, reader.uint32(), undefined, _depth + 1, message.settings);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyServerSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyServerSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.settings != null && message.hasOwnProperty("settings")) {
                let error = $root.lq.ServerSettings.verify(message.settings, _depth + 1);
                if (error)
                    return "settings." + error;
            }
            return null;
        };

        NotifyServerSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyServerSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyServerSetting();
            if (object.settings != null) {
                if (typeof object.settings !== "object")
                    throw TypeError(".lq.NotifyServerSetting.settings: object expected");
                message.settings = $root.lq.ServerSettings.fromObject(object.settings, _depth + 1);
            }
            return message;
        };

        NotifyServerSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.settings = null;
            if (message.settings != null && message.hasOwnProperty("settings"))
                object.settings = $root.lq.ServerSettings.toObject(message.settings, options);
            return object;
        };

        NotifyServerSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyServerSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyServerSetting";
        };

        return NotifyServerSetting;
    })();

    lq.NotifyPayResult = (function() {

        function NotifyPayResult(properties) {
            this.resource_modify = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyPayResult.prototype.pay_result = 0;
        NotifyPayResult.prototype.order_id = "";
        NotifyPayResult.prototype.goods_id = 0;
        NotifyPayResult.prototype.new_month_ticket = 0;
        NotifyPayResult.prototype.resource_modify = $util.emptyArray;

        NotifyPayResult.create = function create(properties) {
            return new NotifyPayResult(properties);
        };

        NotifyPayResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.pay_result != null && Object.hasOwnProperty.call(message, "pay_result"))
                writer.uint32(8).uint32(message.pay_result);
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(24).uint32(message.goods_id);
            if (message.new_month_ticket != null && Object.hasOwnProperty.call(message, "new_month_ticket"))
                writer.uint32(32).uint32(message.new_month_ticket);
            if (message.resource_modify != null && message.resource_modify.length)
                for (let i = 0; i < message.resource_modify.length; ++i)
                    $root.lq.NotifyPayResult.ResourceModify.encode(message.resource_modify[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyPayResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyPayResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyPayResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pay_result = value;
                        else
                            delete message.pay_result;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.new_month_ticket = value;
                        else
                            delete message.new_month_ticket;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.resource_modify && message.resource_modify.length))
                            message.resource_modify = [];
                        message.resource_modify.push($root.lq.NotifyPayResult.ResourceModify.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyPayResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyPayResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pay_result != null && message.hasOwnProperty("pay_result"))
                if (!$util.isInteger(message.pay_result))
                    return "pay_result: integer expected";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.new_month_ticket != null && message.hasOwnProperty("new_month_ticket"))
                if (!$util.isInteger(message.new_month_ticket))
                    return "new_month_ticket: integer expected";
            if (message.resource_modify != null && message.hasOwnProperty("resource_modify")) {
                if (!Array.isArray(message.resource_modify))
                    return "resource_modify: array expected";
                for (let i = 0; i < message.resource_modify.length; ++i) {
                    let error = $root.lq.NotifyPayResult.ResourceModify.verify(message.resource_modify[i], _depth + 1);
                    if (error)
                        return "resource_modify." + error;
                }
            }
            return null;
        };

        NotifyPayResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyPayResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyPayResult();
            if (object.pay_result != null)
                if (Number(object.pay_result) !== 0)
                    message.pay_result = object.pay_result >>> 0;
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.new_month_ticket != null)
                if (Number(object.new_month_ticket) !== 0)
                    message.new_month_ticket = object.new_month_ticket >>> 0;
            if (object.resource_modify) {
                if (!Array.isArray(object.resource_modify))
                    throw TypeError(".lq.NotifyPayResult.resource_modify: array expected");
                message.resource_modify = Array(object.resource_modify.length);
                for (let i = 0; i < object.resource_modify.length; ++i) {
                    if (typeof object.resource_modify[i] !== "object")
                        throw TypeError(".lq.NotifyPayResult.resource_modify: object expected");
                    message.resource_modify[i] = $root.lq.NotifyPayResult.ResourceModify.fromObject(object.resource_modify[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyPayResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.resource_modify = [];
            if (options.defaults) {
                object.pay_result = 0;
                object.order_id = "";
                object.goods_id = 0;
                object.new_month_ticket = 0;
            }
            if (message.pay_result != null && message.hasOwnProperty("pay_result"))
                object.pay_result = message.pay_result;
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.new_month_ticket != null && message.hasOwnProperty("new_month_ticket"))
                object.new_month_ticket = message.new_month_ticket;
            if (message.resource_modify && message.resource_modify.length) {
                object.resource_modify = Array(message.resource_modify.length);
                for (let j = 0; j < message.resource_modify.length; ++j)
                    object.resource_modify[j] = $root.lq.NotifyPayResult.ResourceModify.toObject(message.resource_modify[j], options);
            }
            return object;
        };

        NotifyPayResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyPayResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyPayResult";
        };

        NotifyPayResult.ResourceModify = (function() {

            function ResourceModify(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ResourceModify.prototype.id = 0;
            ResourceModify.prototype.count = 0;
            ResourceModify.prototype.final = 0;

            ResourceModify.create = function create(properties) {
                return new ResourceModify(properties);
            };

            ResourceModify.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(24).uint32(message.final);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ResourceModify.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ResourceModify.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyPayResult.ResourceModify(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ResourceModify.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ResourceModify.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                return null;
            };

            ResourceModify.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyPayResult.ResourceModify)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyPayResult.ResourceModify();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                return message;
            };

            ResourceModify.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                    object.final = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                return object;
            };

            ResourceModify.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ResourceModify.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyPayResult.ResourceModify";
            };

            return ResourceModify;
        })();

        return NotifyPayResult;
    })();

    lq.NotifyCustomContestAccountMsg = (function() {

        function NotifyCustomContestAccountMsg(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomContestAccountMsg.prototype.unique_id = 0;
        NotifyCustomContestAccountMsg.prototype.account_id = 0;
        NotifyCustomContestAccountMsg.prototype.sender = "";
        NotifyCustomContestAccountMsg.prototype.content = "";
        NotifyCustomContestAccountMsg.prototype.verified = 0;

        NotifyCustomContestAccountMsg.create = function create(properties) {
            return new NotifyCustomContestAccountMsg(properties);
        };

        NotifyCustomContestAccountMsg.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.sender != null && Object.hasOwnProperty.call(message, "sender"))
                writer.uint32(26).string(message.sender);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(34).string(message.content);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(40).uint32(message.verified);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomContestAccountMsg.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomContestAccountMsg.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomContestAccountMsg(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sender = value;
                        else
                            delete message.sender;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomContestAccountMsg.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomContestAccountMsg.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.sender != null && message.hasOwnProperty("sender"))
                if (!$util.isString(message.sender))
                    return "sender: string expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            return null;
        };

        NotifyCustomContestAccountMsg.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomContestAccountMsg)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomContestAccountMsg();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.sender != null)
                if (typeof object.sender !== "string" || object.sender.length)
                    message.sender = String(object.sender);
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            return message;
        };

        NotifyCustomContestAccountMsg.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.account_id = 0;
                object.sender = "";
                object.content = "";
                object.verified = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.sender != null && message.hasOwnProperty("sender"))
                object.sender = message.sender;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            return object;
        };

        NotifyCustomContestAccountMsg.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomContestAccountMsg.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomContestAccountMsg";
        };

        return NotifyCustomContestAccountMsg;
    })();

    lq.NotifyCustomContestSystemMsg = (function() {

        function NotifyCustomContestSystemMsg(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomContestSystemMsg.prototype.unique_id = 0;
        NotifyCustomContestSystemMsg.prototype.type = 0;
        NotifyCustomContestSystemMsg.prototype.uuid = "";
        NotifyCustomContestSystemMsg.prototype.game_start = null;
        NotifyCustomContestSystemMsg.prototype.game_end = null;

        NotifyCustomContestSystemMsg.create = function create(properties) {
            return new NotifyCustomContestSystemMsg(properties);
        };

        NotifyCustomContestSystemMsg.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(26).string(message.uuid);
            if (message.game_start != null && Object.hasOwnProperty.call(message, "game_start"))
                $root.lq.CustomizedContestGameStart.encode(message.game_start, writer.uint32(34).fork()).ldelim();
            if (message.game_end != null && Object.hasOwnProperty.call(message, "game_end"))
                $root.lq.CustomizedContestGameEnd.encode(message.game_end, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomContestSystemMsg.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomContestSystemMsg.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomContestSystemMsg(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_start = $root.lq.CustomizedContestGameStart.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_start);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.game_end = $root.lq.CustomizedContestGameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_end);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomContestSystemMsg.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomContestSystemMsg.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.game_start != null && message.hasOwnProperty("game_start")) {
                let error = $root.lq.CustomizedContestGameStart.verify(message.game_start, _depth + 1);
                if (error)
                    return "game_start." + error;
            }
            if (message.game_end != null && message.hasOwnProperty("game_end")) {
                let error = $root.lq.CustomizedContestGameEnd.verify(message.game_end, _depth + 1);
                if (error)
                    return "game_end." + error;
            }
            return null;
        };

        NotifyCustomContestSystemMsg.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomContestSystemMsg)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomContestSystemMsg();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.game_start != null) {
                if (typeof object.game_start !== "object")
                    throw TypeError(".lq.NotifyCustomContestSystemMsg.game_start: object expected");
                message.game_start = $root.lq.CustomizedContestGameStart.fromObject(object.game_start, _depth + 1);
            }
            if (object.game_end != null) {
                if (typeof object.game_end !== "object")
                    throw TypeError(".lq.NotifyCustomContestSystemMsg.game_end: object expected");
                message.game_end = $root.lq.CustomizedContestGameEnd.fromObject(object.game_end, _depth + 1);
            }
            return message;
        };

        NotifyCustomContestSystemMsg.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.type = 0;
                object.uuid = "";
                object.game_start = null;
                object.game_end = null;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.game_start != null && message.hasOwnProperty("game_start"))
                object.game_start = $root.lq.CustomizedContestGameStart.toObject(message.game_start, options);
            if (message.game_end != null && message.hasOwnProperty("game_end"))
                object.game_end = $root.lq.CustomizedContestGameEnd.toObject(message.game_end, options);
            return object;
        };

        NotifyCustomContestSystemMsg.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomContestSystemMsg.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomContestSystemMsg";
        };

        return NotifyCustomContestSystemMsg;
    })();

    lq.NotifyMatchTimeout = (function() {

        function NotifyMatchTimeout(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyMatchTimeout.prototype.sid = "";

        NotifyMatchTimeout.create = function create(properties) {
            return new NotifyMatchTimeout(properties);
        };

        NotifyMatchTimeout.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sid != null && Object.hasOwnProperty.call(message, "sid"))
                writer.uint32(10).string(message.sid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyMatchTimeout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyMatchTimeout.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyMatchTimeout(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sid = value;
                        else
                            delete message.sid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyMatchTimeout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyMatchTimeout.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sid != null && message.hasOwnProperty("sid"))
                if (!$util.isString(message.sid))
                    return "sid: string expected";
            return null;
        };

        NotifyMatchTimeout.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyMatchTimeout)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyMatchTimeout();
            if (object.sid != null)
                if (typeof object.sid !== "string" || object.sid.length)
                    message.sid = String(object.sid);
            return message;
        };

        NotifyMatchTimeout.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.sid = "";
            if (message.sid != null && message.hasOwnProperty("sid"))
                object.sid = message.sid;
            return object;
        };

        NotifyMatchTimeout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyMatchTimeout.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyMatchTimeout";
        };

        return NotifyMatchTimeout;
    })();

    lq.NotifyMatchFailed = (function() {

        function NotifyMatchFailed(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyMatchFailed.prototype.sid = "";

        NotifyMatchFailed.create = function create(properties) {
            return new NotifyMatchFailed(properties);
        };

        NotifyMatchFailed.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sid != null && Object.hasOwnProperty.call(message, "sid"))
                writer.uint32(10).string(message.sid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyMatchFailed.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyMatchFailed.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyMatchFailed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sid = value;
                        else
                            delete message.sid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyMatchFailed.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyMatchFailed.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sid != null && message.hasOwnProperty("sid"))
                if (!$util.isString(message.sid))
                    return "sid: string expected";
            return null;
        };

        NotifyMatchFailed.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyMatchFailed)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyMatchFailed();
            if (object.sid != null)
                if (typeof object.sid !== "string" || object.sid.length)
                    message.sid = String(object.sid);
            return message;
        };

        NotifyMatchFailed.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.sid = "";
            if (message.sid != null && message.hasOwnProperty("sid"))
                object.sid = message.sid;
            return object;
        };

        NotifyMatchFailed.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyMatchFailed.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyMatchFailed";
        };

        return NotifyMatchFailed;
    })();

    lq.NotifyCustomContestState = (function() {

        function NotifyCustomContestState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomContestState.prototype.unique_id = 0;
        NotifyCustomContestState.prototype.state = 0;

        NotifyCustomContestState.create = function create(properties) {
            return new NotifyCustomContestState(properties);
        };

        NotifyCustomContestState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomContestState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomContestState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomContestState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomContestState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomContestState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            return null;
        };

        NotifyCustomContestState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomContestState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomContestState();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            return message;
        };

        NotifyCustomContestState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.state = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            return object;
        };

        NotifyCustomContestState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomContestState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomContestState";
        };

        return NotifyCustomContestState;
    })();

    lq.NotifyActivityChange = (function() {

        function NotifyActivityChange(properties) {
            this.new_activities = [];
            this.end_activities = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityChange.prototype.new_activities = $util.emptyArray;
        NotifyActivityChange.prototype.end_activities = $util.emptyArray;

        NotifyActivityChange.create = function create(properties) {
            return new NotifyActivityChange(properties);
        };

        NotifyActivityChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.new_activities != null && message.new_activities.length)
                for (let i = 0; i < message.new_activities.length; ++i)
                    $root.lq.Activity.encode(message.new_activities[i], writer.uint32(10).fork()).ldelim();
            if (message.end_activities != null && message.end_activities.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.end_activities.length; ++i)
                    writer.uint32(message.end_activities[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityChange();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.new_activities && message.new_activities.length))
                            message.new_activities = [];
                        message.new_activities.push($root.lq.Activity.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.end_activities && message.end_activities.length))
                                message.end_activities = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.end_activities.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.end_activities && message.end_activities.length))
                            message.end_activities = [];
                        message.end_activities.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.new_activities != null && message.hasOwnProperty("new_activities")) {
                if (!Array.isArray(message.new_activities))
                    return "new_activities: array expected";
                for (let i = 0; i < message.new_activities.length; ++i) {
                    let error = $root.lq.Activity.verify(message.new_activities[i], _depth + 1);
                    if (error)
                        return "new_activities." + error;
                }
            }
            if (message.end_activities != null && message.hasOwnProperty("end_activities")) {
                if (!Array.isArray(message.end_activities))
                    return "end_activities: array expected";
                for (let i = 0; i < message.end_activities.length; ++i)
                    if (!$util.isInteger(message.end_activities[i]))
                        return "end_activities: integer[] expected";
            }
            return null;
        };

        NotifyActivityChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityChange();
            if (object.new_activities) {
                if (!Array.isArray(object.new_activities))
                    throw TypeError(".lq.NotifyActivityChange.new_activities: array expected");
                message.new_activities = Array(object.new_activities.length);
                for (let i = 0; i < object.new_activities.length; ++i) {
                    if (typeof object.new_activities[i] !== "object")
                        throw TypeError(".lq.NotifyActivityChange.new_activities: object expected");
                    message.new_activities[i] = $root.lq.Activity.fromObject(object.new_activities[i], _depth + 1);
                }
            }
            if (object.end_activities) {
                if (!Array.isArray(object.end_activities))
                    throw TypeError(".lq.NotifyActivityChange.end_activities: array expected");
                message.end_activities = Array(object.end_activities.length);
                for (let i = 0; i < object.end_activities.length; ++i)
                    message.end_activities[i] = object.end_activities[i] >>> 0;
            }
            return message;
        };

        NotifyActivityChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.new_activities = [];
                object.end_activities = [];
            }
            if (message.new_activities && message.new_activities.length) {
                object.new_activities = Array(message.new_activities.length);
                for (let j = 0; j < message.new_activities.length; ++j)
                    object.new_activities[j] = $root.lq.Activity.toObject(message.new_activities[j], options);
            }
            if (message.end_activities && message.end_activities.length) {
                object.end_activities = Array(message.end_activities.length);
                for (let j = 0; j < message.end_activities.length; ++j)
                    object.end_activities[j] = message.end_activities[j];
            }
            return object;
        };

        NotifyActivityChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityChange";
        };

        return NotifyActivityChange;
    })();

    lq.NotifyAFKResult = (function() {

        function NotifyAFKResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAFKResult.prototype.type = 0;
        NotifyAFKResult.prototype.ban_end_time = 0;
        NotifyAFKResult.prototype.game_uuid = "";

        NotifyAFKResult.create = function create(properties) {
            return new NotifyAFKResult(properties);
        };

        NotifyAFKResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.ban_end_time != null && Object.hasOwnProperty.call(message, "ban_end_time"))
                writer.uint32(16).uint32(message.ban_end_time);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(26).string(message.game_uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAFKResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAFKResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAFKResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ban_end_time = value;
                        else
                            delete message.ban_end_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAFKResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAFKResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.ban_end_time != null && message.hasOwnProperty("ban_end_time"))
                if (!$util.isInteger(message.ban_end_time))
                    return "ban_end_time: integer expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            return null;
        };

        NotifyAFKResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAFKResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAFKResult();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.ban_end_time != null)
                if (Number(object.ban_end_time) !== 0)
                    message.ban_end_time = object.ban_end_time >>> 0;
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            return message;
        };

        NotifyAFKResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.ban_end_time = 0;
                object.game_uuid = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.ban_end_time != null && message.hasOwnProperty("ban_end_time"))
                object.ban_end_time = message.ban_end_time;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            return object;
        };

        NotifyAFKResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAFKResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAFKResult";
        };

        return NotifyAFKResult;
    })();

    lq.NotifyLoginQueueFinished = (function() {

        function NotifyLoginQueueFinished(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyLoginQueueFinished.create = function create(properties) {
            return new NotifyLoginQueueFinished(properties);
        };

        NotifyLoginQueueFinished.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyLoginQueueFinished.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyLoginQueueFinished.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyLoginQueueFinished();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyLoginQueueFinished.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyLoginQueueFinished.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        NotifyLoginQueueFinished.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyLoginQueueFinished)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.NotifyLoginQueueFinished();
        };

        NotifyLoginQueueFinished.toObject = function toObject() {
            return {};
        };

        NotifyLoginQueueFinished.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyLoginQueueFinished.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyLoginQueueFinished";
        };

        return NotifyLoginQueueFinished;
    })();

    lq.NotifyGameFinishRewardV2 = (function() {

        function NotifyGameFinishRewardV2(properties) {
            this.badges = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGameFinishRewardV2.prototype.mode_id = 0;
        NotifyGameFinishRewardV2.prototype.level_change = null;
        NotifyGameFinishRewardV2.prototype.match_chest = null;
        NotifyGameFinishRewardV2.prototype.main_character = null;
        NotifyGameFinishRewardV2.prototype.character_gift = null;
        NotifyGameFinishRewardV2.prototype.badges = $util.emptyArray;

        NotifyGameFinishRewardV2.create = function create(properties) {
            return new NotifyGameFinishRewardV2(properties);
        };

        NotifyGameFinishRewardV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode_id != null && Object.hasOwnProperty.call(message, "mode_id"))
                writer.uint32(8).uint32(message.mode_id);
            if (message.level_change != null && Object.hasOwnProperty.call(message, "level_change"))
                $root.lq.NotifyGameFinishRewardV2.LevelChange.encode(message.level_change, writer.uint32(18).fork()).ldelim();
            if (message.match_chest != null && Object.hasOwnProperty.call(message, "match_chest"))
                $root.lq.NotifyGameFinishRewardV2.MatchChest.encode(message.match_chest, writer.uint32(26).fork()).ldelim();
            if (message.main_character != null && Object.hasOwnProperty.call(message, "main_character"))
                $root.lq.NotifyGameFinishRewardV2.MainCharacter.encode(message.main_character, writer.uint32(34).fork()).ldelim();
            if (message.character_gift != null && Object.hasOwnProperty.call(message, "character_gift"))
                $root.lq.NotifyGameFinishRewardV2.CharacterGift.encode(message.character_gift, writer.uint32(42).fork()).ldelim();
            if (message.badges != null && message.badges.length)
                for (let i = 0; i < message.badges.length; ++i)
                    $root.lq.BadgeAchieveProgress.encode(message.badges[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGameFinishRewardV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGameFinishRewardV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishRewardV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode_id = value;
                        else
                            delete message.mode_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.level_change = $root.lq.NotifyGameFinishRewardV2.LevelChange.decode(reader, reader.uint32(), undefined, _depth + 1, message.level_change);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.match_chest = $root.lq.NotifyGameFinishRewardV2.MatchChest.decode(reader, reader.uint32(), undefined, _depth + 1, message.match_chest);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.main_character = $root.lq.NotifyGameFinishRewardV2.MainCharacter.decode(reader, reader.uint32(), undefined, _depth + 1, message.main_character);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.character_gift = $root.lq.NotifyGameFinishRewardV2.CharacterGift.decode(reader, reader.uint32(), undefined, _depth + 1, message.character_gift);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.badges && message.badges.length))
                            message.badges = [];
                        message.badges.push($root.lq.BadgeAchieveProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGameFinishRewardV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGameFinishRewardV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                if (!$util.isInteger(message.mode_id))
                    return "mode_id: integer expected";
            if (message.level_change != null && message.hasOwnProperty("level_change")) {
                let error = $root.lq.NotifyGameFinishRewardV2.LevelChange.verify(message.level_change, _depth + 1);
                if (error)
                    return "level_change." + error;
            }
            if (message.match_chest != null && message.hasOwnProperty("match_chest")) {
                let error = $root.lq.NotifyGameFinishRewardV2.MatchChest.verify(message.match_chest, _depth + 1);
                if (error)
                    return "match_chest." + error;
            }
            if (message.main_character != null && message.hasOwnProperty("main_character")) {
                let error = $root.lq.NotifyGameFinishRewardV2.MainCharacter.verify(message.main_character, _depth + 1);
                if (error)
                    return "main_character." + error;
            }
            if (message.character_gift != null && message.hasOwnProperty("character_gift")) {
                let error = $root.lq.NotifyGameFinishRewardV2.CharacterGift.verify(message.character_gift, _depth + 1);
                if (error)
                    return "character_gift." + error;
            }
            if (message.badges != null && message.hasOwnProperty("badges")) {
                if (!Array.isArray(message.badges))
                    return "badges: array expected";
                for (let i = 0; i < message.badges.length; ++i) {
                    let error = $root.lq.BadgeAchieveProgress.verify(message.badges[i], _depth + 1);
                    if (error)
                        return "badges." + error;
                }
            }
            return null;
        };

        NotifyGameFinishRewardV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGameFinishRewardV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGameFinishRewardV2();
            if (object.mode_id != null)
                if (Number(object.mode_id) !== 0)
                    message.mode_id = object.mode_id >>> 0;
            if (object.level_change != null) {
                if (typeof object.level_change !== "object")
                    throw TypeError(".lq.NotifyGameFinishRewardV2.level_change: object expected");
                message.level_change = $root.lq.NotifyGameFinishRewardV2.LevelChange.fromObject(object.level_change, _depth + 1);
            }
            if (object.match_chest != null) {
                if (typeof object.match_chest !== "object")
                    throw TypeError(".lq.NotifyGameFinishRewardV2.match_chest: object expected");
                message.match_chest = $root.lq.NotifyGameFinishRewardV2.MatchChest.fromObject(object.match_chest, _depth + 1);
            }
            if (object.main_character != null) {
                if (typeof object.main_character !== "object")
                    throw TypeError(".lq.NotifyGameFinishRewardV2.main_character: object expected");
                message.main_character = $root.lq.NotifyGameFinishRewardV2.MainCharacter.fromObject(object.main_character, _depth + 1);
            }
            if (object.character_gift != null) {
                if (typeof object.character_gift !== "object")
                    throw TypeError(".lq.NotifyGameFinishRewardV2.character_gift: object expected");
                message.character_gift = $root.lq.NotifyGameFinishRewardV2.CharacterGift.fromObject(object.character_gift, _depth + 1);
            }
            if (object.badges) {
                if (!Array.isArray(object.badges))
                    throw TypeError(".lq.NotifyGameFinishRewardV2.badges: array expected");
                message.badges = Array(object.badges.length);
                for (let i = 0; i < object.badges.length; ++i) {
                    if (typeof object.badges[i] !== "object")
                        throw TypeError(".lq.NotifyGameFinishRewardV2.badges: object expected");
                    message.badges[i] = $root.lq.BadgeAchieveProgress.fromObject(object.badges[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyGameFinishRewardV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.badges = [];
            if (options.defaults) {
                object.mode_id = 0;
                object.level_change = null;
                object.match_chest = null;
                object.main_character = null;
                object.character_gift = null;
            }
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                object.mode_id = message.mode_id;
            if (message.level_change != null && message.hasOwnProperty("level_change"))
                object.level_change = $root.lq.NotifyGameFinishRewardV2.LevelChange.toObject(message.level_change, options);
            if (message.match_chest != null && message.hasOwnProperty("match_chest"))
                object.match_chest = $root.lq.NotifyGameFinishRewardV2.MatchChest.toObject(message.match_chest, options);
            if (message.main_character != null && message.hasOwnProperty("main_character"))
                object.main_character = $root.lq.NotifyGameFinishRewardV2.MainCharacter.toObject(message.main_character, options);
            if (message.character_gift != null && message.hasOwnProperty("character_gift"))
                object.character_gift = $root.lq.NotifyGameFinishRewardV2.CharacterGift.toObject(message.character_gift, options);
            if (message.badges && message.badges.length) {
                object.badges = Array(message.badges.length);
                for (let j = 0; j < message.badges.length; ++j)
                    object.badges[j] = $root.lq.BadgeAchieveProgress.toObject(message.badges[j], options);
            }
            return object;
        };

        NotifyGameFinishRewardV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGameFinishRewardV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGameFinishRewardV2";
        };

        NotifyGameFinishRewardV2.LevelChange = (function() {

            function LevelChange(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LevelChange.prototype.origin = null;
            LevelChange.prototype.final = null;
            LevelChange.prototype.type = 0;

            LevelChange.create = function create(properties) {
                return new LevelChange(properties);
            };

            LevelChange.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    $root.lq.AccountLevel.encode(message.origin, writer.uint32(10).fork()).ldelim();
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    $root.lq.AccountLevel.encode(message.final, writer.uint32(18).fork()).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(24).uint32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LevelChange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LevelChange.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishRewardV2.LevelChange(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.origin = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.origin);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.final = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.final);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LevelChange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LevelChange.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.origin != null && message.hasOwnProperty("origin")) {
                    let error = $root.lq.AccountLevel.verify(message.origin, _depth + 1);
                    if (error)
                        return "origin." + error;
                }
                if (message.final != null && message.hasOwnProperty("final")) {
                    let error = $root.lq.AccountLevel.verify(message.final, _depth + 1);
                    if (error)
                        return "final." + error;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                return null;
            };

            LevelChange.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishRewardV2.LevelChange)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishRewardV2.LevelChange();
                if (object.origin != null) {
                    if (typeof object.origin !== "object")
                        throw TypeError(".lq.NotifyGameFinishRewardV2.LevelChange.origin: object expected");
                    message.origin = $root.lq.AccountLevel.fromObject(object.origin, _depth + 1);
                }
                if (object.final != null) {
                    if (typeof object.final !== "object")
                        throw TypeError(".lq.NotifyGameFinishRewardV2.LevelChange.final: object expected");
                    message.final = $root.lq.AccountLevel.fromObject(object.final, _depth + 1);
                }
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                return message;
            };

            LevelChange.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.origin = null;
                    object.final = null;
                    object.type = 0;
                }
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = $root.lq.AccountLevel.toObject(message.origin, options);
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = $root.lq.AccountLevel.toObject(message.final, options);
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                return object;
            };

            LevelChange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LevelChange.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishRewardV2.LevelChange";
            };

            return LevelChange;
        })();

        NotifyGameFinishRewardV2.MatchChest = (function() {

            function MatchChest(properties) {
                this.rewards = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MatchChest.prototype.chest_id = 0;
            MatchChest.prototype.origin = 0;
            MatchChest.prototype.final = 0;
            MatchChest.prototype.is_graded = false;
            MatchChest.prototype.rewards = $util.emptyArray;

            MatchChest.create = function create(properties) {
                return new MatchChest(properties);
            };

            MatchChest.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                    writer.uint32(8).uint32(message.chest_id);
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    writer.uint32(16).uint32(message.origin);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(24).uint32(message.final);
                if (message.is_graded != null && Object.hasOwnProperty.call(message, "is_graded"))
                    writer.uint32(32).bool(message.is_graded);
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(42).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MatchChest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MatchChest.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishRewardV2.MatchChest(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chest_id = value;
                            else
                                delete message.chest_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.origin = value;
                            else
                                delete message.origin;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_graded = value;
                            else
                                delete message.is_graded;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MatchChest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MatchChest.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                    if (!$util.isInteger(message.chest_id))
                        return "chest_id: integer expected";
                if (message.origin != null && message.hasOwnProperty("origin"))
                    if (!$util.isInteger(message.origin))
                        return "origin: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    if (typeof message.is_graded !== "boolean")
                        return "is_graded: boolean expected";
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                return null;
            };

            MatchChest.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishRewardV2.MatchChest)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishRewardV2.MatchChest();
                if (object.chest_id != null)
                    if (Number(object.chest_id) !== 0)
                        message.chest_id = object.chest_id >>> 0;
                if (object.origin != null)
                    if (Number(object.origin) !== 0)
                        message.origin = object.origin >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                if (object.is_graded != null)
                    if (object.is_graded)
                        message.is_graded = Boolean(object.is_graded);
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.NotifyGameFinishRewardV2.MatchChest.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.NotifyGameFinishRewardV2.MatchChest.rewards: object expected");
                        message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                return message;
            };

            MatchChest.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rewards = [];
                if (options.defaults) {
                    object.chest_id = 0;
                    object.origin = 0;
                    object.final = 0;
                    object.is_graded = false;
                }
                if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                    object.chest_id = message.chest_id;
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = message.origin;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    object.is_graded = message.is_graded;
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
                }
                return object;
            };

            MatchChest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MatchChest.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishRewardV2.MatchChest";
            };

            return MatchChest;
        })();

        NotifyGameFinishRewardV2.MainCharacter = (function() {

            function MainCharacter(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MainCharacter.prototype.level = 0;
            MainCharacter.prototype.exp = 0;
            MainCharacter.prototype.add = 0;

            MainCharacter.create = function create(properties) {
                return new MainCharacter(properties);
            };

            MainCharacter.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(8).uint32(message.level);
                if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                    writer.uint32(16).uint32(message.exp);
                if (message.add != null && Object.hasOwnProperty.call(message, "add"))
                    writer.uint32(24).uint32(message.add);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MainCharacter.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MainCharacter.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishRewardV2.MainCharacter(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.exp = value;
                            else
                                delete message.exp;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.add = value;
                            else
                                delete message.add;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MainCharacter.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MainCharacter.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.exp != null && message.hasOwnProperty("exp"))
                    if (!$util.isInteger(message.exp))
                        return "exp: integer expected";
                if (message.add != null && message.hasOwnProperty("add"))
                    if (!$util.isInteger(message.add))
                        return "add: integer expected";
                return null;
            };

            MainCharacter.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishRewardV2.MainCharacter)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishRewardV2.MainCharacter();
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.exp != null)
                    if (Number(object.exp) !== 0)
                        message.exp = object.exp >>> 0;
                if (object.add != null)
                    if (Number(object.add) !== 0)
                        message.add = object.add >>> 0;
                return message;
            };

            MainCharacter.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level = 0;
                    object.exp = 0;
                    object.add = 0;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.exp != null && message.hasOwnProperty("exp"))
                    object.exp = message.exp;
                if (message.add != null && message.hasOwnProperty("add"))
                    object.add = message.add;
                return object;
            };

            MainCharacter.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MainCharacter.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishRewardV2.MainCharacter";
            };

            return MainCharacter;
        })();

        NotifyGameFinishRewardV2.CharacterGift = (function() {

            function CharacterGift(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CharacterGift.prototype.origin = 0;
            CharacterGift.prototype.final = 0;
            CharacterGift.prototype.add = 0;
            CharacterGift.prototype.is_graded = false;

            CharacterGift.create = function create(properties) {
                return new CharacterGift(properties);
            };

            CharacterGift.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    writer.uint32(8).uint32(message.origin);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(16).uint32(message.final);
                if (message.add != null && Object.hasOwnProperty.call(message, "add"))
                    writer.uint32(24).uint32(message.add);
                if (message.is_graded != null && Object.hasOwnProperty.call(message, "is_graded"))
                    writer.uint32(32).bool(message.is_graded);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CharacterGift.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CharacterGift.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishRewardV2.CharacterGift(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.origin = value;
                            else
                                delete message.origin;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.add = value;
                            else
                                delete message.add;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_graded = value;
                            else
                                delete message.is_graded;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CharacterGift.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CharacterGift.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.origin != null && message.hasOwnProperty("origin"))
                    if (!$util.isInteger(message.origin))
                        return "origin: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                if (message.add != null && message.hasOwnProperty("add"))
                    if (!$util.isInteger(message.add))
                        return "add: integer expected";
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    if (typeof message.is_graded !== "boolean")
                        return "is_graded: boolean expected";
                return null;
            };

            CharacterGift.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishRewardV2.CharacterGift)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishRewardV2.CharacterGift();
                if (object.origin != null)
                    if (Number(object.origin) !== 0)
                        message.origin = object.origin >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                if (object.add != null)
                    if (Number(object.add) !== 0)
                        message.add = object.add >>> 0;
                if (object.is_graded != null)
                    if (object.is_graded)
                        message.is_graded = Boolean(object.is_graded);
                return message;
            };

            CharacterGift.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.origin = 0;
                    object.final = 0;
                    object.add = 0;
                    object.is_graded = false;
                }
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = message.origin;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                if (message.add != null && message.hasOwnProperty("add"))
                    object.add = message.add;
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    object.is_graded = message.is_graded;
                return object;
            };

            CharacterGift.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CharacterGift.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishRewardV2.CharacterGift";
            };

            return CharacterGift;
        })();

        return NotifyGameFinishRewardV2;
    })();

    lq.NotifyActivityRewardV2 = (function() {

        function NotifyActivityRewardV2(properties) {
            this.activity_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityRewardV2.prototype.activity_reward = $util.emptyArray;

        NotifyActivityRewardV2.create = function create(properties) {
            return new NotifyActivityRewardV2(properties);
        };

        NotifyActivityRewardV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_reward != null && message.activity_reward.length)
                for (let i = 0; i < message.activity_reward.length; ++i)
                    $root.lq.NotifyActivityRewardV2.ActivityReward.encode(message.activity_reward[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityRewardV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityRewardV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityRewardV2();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_reward && message.activity_reward.length))
                            message.activity_reward = [];
                        message.activity_reward.push($root.lq.NotifyActivityRewardV2.ActivityReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityRewardV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityRewardV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_reward != null && message.hasOwnProperty("activity_reward")) {
                if (!Array.isArray(message.activity_reward))
                    return "activity_reward: array expected";
                for (let i = 0; i < message.activity_reward.length; ++i) {
                    let error = $root.lq.NotifyActivityRewardV2.ActivityReward.verify(message.activity_reward[i], _depth + 1);
                    if (error)
                        return "activity_reward." + error;
                }
            }
            return null;
        };

        NotifyActivityRewardV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityRewardV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityRewardV2();
            if (object.activity_reward) {
                if (!Array.isArray(object.activity_reward))
                    throw TypeError(".lq.NotifyActivityRewardV2.activity_reward: array expected");
                message.activity_reward = Array(object.activity_reward.length);
                for (let i = 0; i < object.activity_reward.length; ++i) {
                    if (typeof object.activity_reward[i] !== "object")
                        throw TypeError(".lq.NotifyActivityRewardV2.activity_reward: object expected");
                    message.activity_reward[i] = $root.lq.NotifyActivityRewardV2.ActivityReward.fromObject(object.activity_reward[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityRewardV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_reward = [];
            if (message.activity_reward && message.activity_reward.length) {
                object.activity_reward = Array(message.activity_reward.length);
                for (let j = 0; j < message.activity_reward.length; ++j)
                    object.activity_reward[j] = $root.lq.NotifyActivityRewardV2.ActivityReward.toObject(message.activity_reward[j], options);
            }
            return object;
        };

        NotifyActivityRewardV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityRewardV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityRewardV2";
        };

        NotifyActivityRewardV2.ActivityReward = (function() {

            function ActivityReward(properties) {
                this.rewards = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityReward.prototype.activity_id = 0;
            ActivityReward.prototype.rewards = $util.emptyArray;

            ActivityReward.create = function create(properties) {
                return new ActivityReward(properties);
            };

            ActivityReward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityRewardV2.ActivityReward(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityReward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityReward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                return null;
            };

            ActivityReward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyActivityRewardV2.ActivityReward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyActivityRewardV2.ActivityReward();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.NotifyActivityRewardV2.ActivityReward.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.NotifyActivityRewardV2.ActivityReward.rewards: object expected");
                        message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityReward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rewards = [];
                if (options.defaults)
                    object.activity_id = 0;
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
                }
                return object;
            };

            ActivityReward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityReward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyActivityRewardV2.ActivityReward";
            };

            return ActivityReward;
        })();

        return NotifyActivityRewardV2;
    })();

    lq.NotifyActivityPointV2 = (function() {

        function NotifyActivityPointV2(properties) {
            this.activity_points = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityPointV2.prototype.activity_points = $util.emptyArray;

        NotifyActivityPointV2.create = function create(properties) {
            return new NotifyActivityPointV2(properties);
        };

        NotifyActivityPointV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_points != null && message.activity_points.length)
                for (let i = 0; i < message.activity_points.length; ++i)
                    $root.lq.NotifyActivityPointV2.ActivityPoint.encode(message.activity_points[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityPointV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityPointV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityPointV2();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_points && message.activity_points.length))
                            message.activity_points = [];
                        message.activity_points.push($root.lq.NotifyActivityPointV2.ActivityPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityPointV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityPointV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_points != null && message.hasOwnProperty("activity_points")) {
                if (!Array.isArray(message.activity_points))
                    return "activity_points: array expected";
                for (let i = 0; i < message.activity_points.length; ++i) {
                    let error = $root.lq.NotifyActivityPointV2.ActivityPoint.verify(message.activity_points[i], _depth + 1);
                    if (error)
                        return "activity_points." + error;
                }
            }
            return null;
        };

        NotifyActivityPointV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityPointV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityPointV2();
            if (object.activity_points) {
                if (!Array.isArray(object.activity_points))
                    throw TypeError(".lq.NotifyActivityPointV2.activity_points: array expected");
                message.activity_points = Array(object.activity_points.length);
                for (let i = 0; i < object.activity_points.length; ++i) {
                    if (typeof object.activity_points[i] !== "object")
                        throw TypeError(".lq.NotifyActivityPointV2.activity_points: object expected");
                    message.activity_points[i] = $root.lq.NotifyActivityPointV2.ActivityPoint.fromObject(object.activity_points[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityPointV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_points = [];
            if (message.activity_points && message.activity_points.length) {
                object.activity_points = Array(message.activity_points.length);
                for (let j = 0; j < message.activity_points.length; ++j)
                    object.activity_points[j] = $root.lq.NotifyActivityPointV2.ActivityPoint.toObject(message.activity_points[j], options);
            }
            return object;
        };

        NotifyActivityPointV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityPointV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityPointV2";
        };

        NotifyActivityPointV2.ActivityPoint = (function() {

            function ActivityPoint(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityPoint.prototype.activity_id = 0;
            ActivityPoint.prototype.point = 0;

            ActivityPoint.create = function create(properties) {
                return new ActivityPoint(properties);
            };

            ActivityPoint.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityPoint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityPoint.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityPointV2.ActivityPoint(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityPoint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityPoint.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            ActivityPoint.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyActivityPointV2.ActivityPoint)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyActivityPointV2.ActivityPoint();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            ActivityPoint.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.activity_id = 0;
                    object.point = 0;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            ActivityPoint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityPoint.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyActivityPointV2.ActivityPoint";
            };

            return ActivityPoint;
        })();

        return NotifyActivityPointV2;
    })();

    lq.NotifyLeaderboardPointV2 = (function() {

        function NotifyLeaderboardPointV2(properties) {
            this.leaderboard_points = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyLeaderboardPointV2.prototype.leaderboard_points = $util.emptyArray;

        NotifyLeaderboardPointV2.create = function create(properties) {
            return new NotifyLeaderboardPointV2(properties);
        };

        NotifyLeaderboardPointV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.leaderboard_points != null && message.leaderboard_points.length)
                for (let i = 0; i < message.leaderboard_points.length; ++i)
                    $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint.encode(message.leaderboard_points[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyLeaderboardPointV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyLeaderboardPointV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyLeaderboardPointV2();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.leaderboard_points && message.leaderboard_points.length))
                            message.leaderboard_points = [];
                        message.leaderboard_points.push($root.lq.NotifyLeaderboardPointV2.LeaderboardPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyLeaderboardPointV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyLeaderboardPointV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaderboard_points != null && message.hasOwnProperty("leaderboard_points")) {
                if (!Array.isArray(message.leaderboard_points))
                    return "leaderboard_points: array expected";
                for (let i = 0; i < message.leaderboard_points.length; ++i) {
                    let error = $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint.verify(message.leaderboard_points[i], _depth + 1);
                    if (error)
                        return "leaderboard_points." + error;
                }
            }
            return null;
        };

        NotifyLeaderboardPointV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyLeaderboardPointV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyLeaderboardPointV2();
            if (object.leaderboard_points) {
                if (!Array.isArray(object.leaderboard_points))
                    throw TypeError(".lq.NotifyLeaderboardPointV2.leaderboard_points: array expected");
                message.leaderboard_points = Array(object.leaderboard_points.length);
                for (let i = 0; i < object.leaderboard_points.length; ++i) {
                    if (typeof object.leaderboard_points[i] !== "object")
                        throw TypeError(".lq.NotifyLeaderboardPointV2.leaderboard_points: object expected");
                    message.leaderboard_points[i] = $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint.fromObject(object.leaderboard_points[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyLeaderboardPointV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.leaderboard_points = [];
            if (message.leaderboard_points && message.leaderboard_points.length) {
                object.leaderboard_points = Array(message.leaderboard_points.length);
                for (let j = 0; j < message.leaderboard_points.length; ++j)
                    object.leaderboard_points[j] = $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint.toObject(message.leaderboard_points[j], options);
            }
            return object;
        };

        NotifyLeaderboardPointV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyLeaderboardPointV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyLeaderboardPointV2";
        };

        NotifyLeaderboardPointV2.LeaderboardPoint = (function() {

            function LeaderboardPoint(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LeaderboardPoint.prototype.leaderboard_id = 0;
            LeaderboardPoint.prototype.point = 0;

            LeaderboardPoint.create = function create(properties) {
                return new LeaderboardPoint(properties);
            };

            LeaderboardPoint.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.leaderboard_id != null && Object.hasOwnProperty.call(message, "leaderboard_id"))
                    writer.uint32(8).uint32(message.leaderboard_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LeaderboardPoint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LeaderboardPoint.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.leaderboard_id = value;
                            else
                                delete message.leaderboard_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LeaderboardPoint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LeaderboardPoint.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                    if (!$util.isInteger(message.leaderboard_id))
                        return "leaderboard_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            LeaderboardPoint.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyLeaderboardPointV2.LeaderboardPoint();
                if (object.leaderboard_id != null)
                    if (Number(object.leaderboard_id) !== 0)
                        message.leaderboard_id = object.leaderboard_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            LeaderboardPoint.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.leaderboard_id = 0;
                    object.point = 0;
                }
                if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                    object.leaderboard_id = message.leaderboard_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            LeaderboardPoint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LeaderboardPoint.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyLeaderboardPointV2.LeaderboardPoint";
            };

            return LeaderboardPoint;
        })();

        return NotifyLeaderboardPointV2;
    })();

    lq.NotifySeerReport = (function() {

        function NotifySeerReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifySeerReport.prototype.report = null;

        NotifySeerReport.create = function create(properties) {
            return new NotifySeerReport(properties);
        };

        NotifySeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.report != null && Object.hasOwnProperty.call(message, "report"))
                $root.lq.SeerBrief.encode(message.report, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifySeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifySeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifySeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.report = $root.lq.SeerBrief.decode(reader, reader.uint32(), undefined, _depth + 1, message.report);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifySeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifySeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.report != null && message.hasOwnProperty("report")) {
                let error = $root.lq.SeerBrief.verify(message.report, _depth + 1);
                if (error)
                    return "report." + error;
            }
            return null;
        };

        NotifySeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifySeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifySeerReport();
            if (object.report != null) {
                if (typeof object.report !== "object")
                    throw TypeError(".lq.NotifySeerReport.report: object expected");
                message.report = $root.lq.SeerBrief.fromObject(object.report, _depth + 1);
            }
            return message;
        };

        NotifySeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.report = null;
            if (message.report != null && message.hasOwnProperty("report"))
                object.report = $root.lq.SeerBrief.toObject(message.report, options);
            return object;
        };

        NotifySeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifySeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifySeerReport";
        };

        return NotifySeerReport;
    })();

    lq.NotifyConnectionShutdown = (function() {

        function NotifyConnectionShutdown(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyConnectionShutdown.prototype.reason = 0;
        NotifyConnectionShutdown.prototype.close_at = 0;

        NotifyConnectionShutdown.create = function create(properties) {
            return new NotifyConnectionShutdown(properties);
        };

        NotifyConnectionShutdown.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(8).uint32(message.reason);
            if (message.close_at != null && Object.hasOwnProperty.call(message, "close_at"))
                writer.uint32(16).uint32(message.close_at);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyConnectionShutdown.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyConnectionShutdown.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyConnectionShutdown(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.close_at = value;
                        else
                            delete message.close_at;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyConnectionShutdown.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyConnectionShutdown.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isInteger(message.reason))
                    return "reason: integer expected";
            if (message.close_at != null && message.hasOwnProperty("close_at"))
                if (!$util.isInteger(message.close_at))
                    return "close_at: integer expected";
            return null;
        };

        NotifyConnectionShutdown.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyConnectionShutdown)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyConnectionShutdown();
            if (object.reason != null)
                if (Number(object.reason) !== 0)
                    message.reason = object.reason >>> 0;
            if (object.close_at != null)
                if (Number(object.close_at) !== 0)
                    message.close_at = object.close_at >>> 0;
            return message;
        };

        NotifyConnectionShutdown.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.reason = 0;
                object.close_at = 0;
            }
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            if (message.close_at != null && message.hasOwnProperty("close_at"))
                object.close_at = message.close_at;
            return object;
        };

        NotifyConnectionShutdown.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyConnectionShutdown.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyConnectionShutdown";
        };

        return NotifyConnectionShutdown;
    })();

    lq.NotifyCustomizedContestReady = (function() {

        function NotifyCustomizedContestReady(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomizedContestReady.prototype.unique_id = 0;
        NotifyCustomizedContestReady.prototype.ready = 0;
        NotifyCustomizedContestReady.prototype.reason = 0;

        NotifyCustomizedContestReady.create = function create(properties) {
            return new NotifyCustomizedContestReady(properties);
        };

        NotifyCustomizedContestReady.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.ready != null && Object.hasOwnProperty.call(message, "ready"))
                writer.uint32(16).uint32(message.ready);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(24).uint32(message.reason);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomizedContestReady.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomizedContestReady.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomizedContestReady(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ready = value;
                        else
                            delete message.ready;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomizedContestReady.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomizedContestReady.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.ready != null && message.hasOwnProperty("ready"))
                if (!$util.isInteger(message.ready))
                    return "ready: integer expected";
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isInteger(message.reason))
                    return "reason: integer expected";
            return null;
        };

        NotifyCustomizedContestReady.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomizedContestReady)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomizedContestReady();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.ready != null)
                if (Number(object.ready) !== 0)
                    message.ready = object.ready >>> 0;
            if (object.reason != null)
                if (Number(object.reason) !== 0)
                    message.reason = object.reason >>> 0;
            return message;
        };

        NotifyCustomizedContestReady.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.ready = 0;
                object.reason = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.ready != null && message.hasOwnProperty("ready"))
                object.ready = message.ready;
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            return object;
        };

        NotifyCustomizedContestReady.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomizedContestReady.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomizedContestReady";
        };

        return NotifyCustomizedContestReady;
    })();

    lq.NotifyCustomizedContestRuleModify = (function() {

        function NotifyCustomizedContestRuleModify(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomizedContestRuleModify.prototype.unique_id = 0;
        NotifyCustomizedContestRuleModify.prototype.auto_match = 0;

        NotifyCustomizedContestRuleModify.create = function create(properties) {
            return new NotifyCustomizedContestRuleModify(properties);
        };

        NotifyCustomizedContestRuleModify.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.auto_match != null && Object.hasOwnProperty.call(message, "auto_match"))
                writer.uint32(16).uint32(message.auto_match);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomizedContestRuleModify.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomizedContestRuleModify.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomizedContestRuleModify(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.auto_match = value;
                        else
                            delete message.auto_match;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomizedContestRuleModify.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomizedContestRuleModify.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                if (!$util.isInteger(message.auto_match))
                    return "auto_match: integer expected";
            return null;
        };

        NotifyCustomizedContestRuleModify.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomizedContestRuleModify)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomizedContestRuleModify();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.auto_match != null)
                if (Number(object.auto_match) !== 0)
                    message.auto_match = object.auto_match >>> 0;
            return message;
        };

        NotifyCustomizedContestRuleModify.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.auto_match = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                object.auto_match = message.auto_match;
            return object;
        };

        NotifyCustomizedContestRuleModify.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomizedContestRuleModify.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomizedContestRuleModify";
        };

        return NotifyCustomizedContestRuleModify;
    })();

    lq.NotifyCustomizedContestPlanReady = (function() {

        function NotifyCustomizedContestPlanReady(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomizedContestPlanReady.prototype.game_plan = null;

        NotifyCustomizedContestPlanReady.create = function create(properties) {
            return new NotifyCustomizedContestPlanReady(properties);
        };

        NotifyCustomizedContestPlanReady.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_plan != null && Object.hasOwnProperty.call(message, "game_plan"))
                $root.lq.CustomizedContestGamePlan.encode(message.game_plan, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomizedContestPlanReady.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomizedContestPlanReady.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomizedContestPlanReady(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.game_plan = $root.lq.CustomizedContestGamePlan.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_plan);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomizedContestPlanReady.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomizedContestPlanReady.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_plan != null && message.hasOwnProperty("game_plan")) {
                let error = $root.lq.CustomizedContestGamePlan.verify(message.game_plan, _depth + 1);
                if (error)
                    return "game_plan." + error;
            }
            return null;
        };

        NotifyCustomizedContestPlanReady.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomizedContestPlanReady)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomizedContestPlanReady();
            if (object.game_plan != null) {
                if (typeof object.game_plan !== "object")
                    throw TypeError(".lq.NotifyCustomizedContestPlanReady.game_plan: object expected");
                message.game_plan = $root.lq.CustomizedContestGamePlan.fromObject(object.game_plan, _depth + 1);
            }
            return message;
        };

        NotifyCustomizedContestPlanReady.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.game_plan = null;
            if (message.game_plan != null && message.hasOwnProperty("game_plan"))
                object.game_plan = $root.lq.CustomizedContestGamePlan.toObject(message.game_plan, options);
            return object;
        };

        NotifyCustomizedContestPlanReady.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomizedContestPlanReady.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomizedContestPlanReady";
        };

        return NotifyCustomizedContestPlanReady;
    })();

    lq.NotifyCustomizedContestPlanCancel = (function() {

        function NotifyCustomizedContestPlanCancel(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyCustomizedContestPlanCancel.prototype.unique_id = 0;
        NotifyCustomizedContestPlanCancel.prototype.uuid = "";

        NotifyCustomizedContestPlanCancel.create = function create(properties) {
            return new NotifyCustomizedContestPlanCancel(properties);
        };

        NotifyCustomizedContestPlanCancel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(18).string(message.uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyCustomizedContestPlanCancel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyCustomizedContestPlanCancel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyCustomizedContestPlanCancel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyCustomizedContestPlanCancel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyCustomizedContestPlanCancel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            return null;
        };

        NotifyCustomizedContestPlanCancel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyCustomizedContestPlanCancel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyCustomizedContestPlanCancel();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            return message;
        };

        NotifyCustomizedContestPlanCancel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.uuid = "";
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            return object;
        };

        NotifyCustomizedContestPlanCancel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyCustomizedContestPlanCancel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyCustomizedContestPlanCancel";
        };

        return NotifyCustomizedContestPlanCancel;
    })();

    lq.Error = (function() {

        function Error(properties) {
            this.u32_params = [];
            this.str_params = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Error.prototype.code = 0;
        Error.prototype.u32_params = $util.emptyArray;
        Error.prototype.str_params = $util.emptyArray;
        Error.prototype.json_param = "";
        Error.prototype.level = 0;
        Error.prototype.message = "";
        Error.prototype.args = "";

        Error.create = function create(properties) {
            return new Error(properties);
        };

        Error.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(8).uint32(message.code);
            if (message.u32_params != null && message.u32_params.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.u32_params.length; ++i)
                    writer.uint32(message.u32_params[i]);
                writer.ldelim();
            }
            if (message.str_params != null && message.str_params.length)
                for (let i = 0; i < message.str_params.length; ++i)
                    writer.uint32(26).string(message.str_params[i]);
            if (message.json_param != null && Object.hasOwnProperty.call(message, "json_param"))
                writer.uint32(34).string(message.json_param);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(40).uint32(message.level);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(50).string(message.message);
            if (message.args != null && Object.hasOwnProperty.call(message, "args"))
                writer.uint32(58).string(message.args);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Error.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Error.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Error(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.u32_params && message.u32_params.length))
                                message.u32_params = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.u32_params.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.u32_params && message.u32_params.length))
                            message.u32_params = [];
                        message.u32_params.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.str_params && message.str_params.length))
                            message.str_params = [];
                        message.str_params.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.json_param = value;
                        else
                            delete message.json_param;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.args = value;
                        else
                            delete message.args;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Error.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Error.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isInteger(message.code))
                    return "code: integer expected";
            if (message.u32_params != null && message.hasOwnProperty("u32_params")) {
                if (!Array.isArray(message.u32_params))
                    return "u32_params: array expected";
                for (let i = 0; i < message.u32_params.length; ++i)
                    if (!$util.isInteger(message.u32_params[i]))
                        return "u32_params: integer[] expected";
            }
            if (message.str_params != null && message.hasOwnProperty("str_params")) {
                if (!Array.isArray(message.str_params))
                    return "str_params: array expected";
                for (let i = 0; i < message.str_params.length; ++i)
                    if (!$util.isString(message.str_params[i]))
                        return "str_params: string[] expected";
            }
            if (message.json_param != null && message.hasOwnProperty("json_param"))
                if (!$util.isString(message.json_param))
                    return "json_param: string expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.args != null && message.hasOwnProperty("args"))
                if (!$util.isString(message.args))
                    return "args: string expected";
            return null;
        };

        Error.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Error)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Error();
            if (object.code != null)
                if (Number(object.code) !== 0)
                    message.code = object.code >>> 0;
            if (object.u32_params) {
                if (!Array.isArray(object.u32_params))
                    throw TypeError(".lq.Error.u32_params: array expected");
                message.u32_params = Array(object.u32_params.length);
                for (let i = 0; i < object.u32_params.length; ++i)
                    message.u32_params[i] = object.u32_params[i] >>> 0;
            }
            if (object.str_params) {
                if (!Array.isArray(object.str_params))
                    throw TypeError(".lq.Error.str_params: array expected");
                message.str_params = Array(object.str_params.length);
                for (let i = 0; i < object.str_params.length; ++i)
                    message.str_params[i] = String(object.str_params[i]);
            }
            if (object.json_param != null)
                if (typeof object.json_param !== "string" || object.json_param.length)
                    message.json_param = String(object.json_param);
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = String(object.message);
            if (object.args != null)
                if (typeof object.args !== "string" || object.args.length)
                    message.args = String(object.args);
            return message;
        };

        Error.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.u32_params = [];
                object.str_params = [];
            }
            if (options.defaults) {
                object.code = 0;
                object.json_param = "";
                object.level = 0;
                object.message = "";
                object.args = "";
            }
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.u32_params && message.u32_params.length) {
                object.u32_params = Array(message.u32_params.length);
                for (let j = 0; j < message.u32_params.length; ++j)
                    object.u32_params[j] = message.u32_params[j];
            }
            if (message.str_params && message.str_params.length) {
                object.str_params = Array(message.str_params.length);
                for (let j = 0; j < message.str_params.length; ++j)
                    object.str_params[j] = message.str_params[j];
            }
            if (message.json_param != null && message.hasOwnProperty("json_param"))
                object.json_param = message.json_param;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            if (message.args != null && message.hasOwnProperty("args"))
                object.args = message.args;
            return object;
        };

        Error.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Error.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Error";
        };

        return Error;
    })();

    lq.Wrapper = (function() {

        function Wrapper(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Wrapper.prototype.name = "";
        Wrapper.prototype.data = $util.newBuffer([]);

        Wrapper.create = function create(properties) {
            return new Wrapper(properties);
        };

        Wrapper.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(10).string(message.name);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(18).bytes(message.data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Wrapper.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Wrapper.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Wrapper(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Wrapper.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Wrapper.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        Wrapper.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Wrapper)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Wrapper();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        Wrapper.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        Wrapper.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Wrapper.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Wrapper";
        };

        return Wrapper;
    })();

    lq.NetworkEndpoint = (function() {

        function NetworkEndpoint(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NetworkEndpoint.prototype.family = "";
        NetworkEndpoint.prototype.address = "";
        NetworkEndpoint.prototype.port = 0;

        NetworkEndpoint.create = function create(properties) {
            return new NetworkEndpoint(properties);
        };

        NetworkEndpoint.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.family != null && Object.hasOwnProperty.call(message, "family"))
                writer.uint32(10).string(message.family);
            if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                writer.uint32(18).string(message.address);
            if (message.port != null && Object.hasOwnProperty.call(message, "port"))
                writer.uint32(24).uint32(message.port);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NetworkEndpoint.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NetworkEndpoint.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NetworkEndpoint(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.family = value;
                        else
                            delete message.family;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.address = value;
                        else
                            delete message.address;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.port = value;
                        else
                            delete message.port;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NetworkEndpoint.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NetworkEndpoint.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.family != null && message.hasOwnProperty("family"))
                if (!$util.isString(message.family))
                    return "family: string expected";
            if (message.address != null && message.hasOwnProperty("address"))
                if (!$util.isString(message.address))
                    return "address: string expected";
            if (message.port != null && message.hasOwnProperty("port"))
                if (!$util.isInteger(message.port))
                    return "port: integer expected";
            return null;
        };

        NetworkEndpoint.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NetworkEndpoint)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NetworkEndpoint();
            if (object.family != null)
                if (typeof object.family !== "string" || object.family.length)
                    message.family = String(object.family);
            if (object.address != null)
                if (typeof object.address !== "string" || object.address.length)
                    message.address = String(object.address);
            if (object.port != null)
                if (Number(object.port) !== 0)
                    message.port = object.port >>> 0;
            return message;
        };

        NetworkEndpoint.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.family = "";
                object.address = "";
                object.port = 0;
            }
            if (message.family != null && message.hasOwnProperty("family"))
                object.family = message.family;
            if (message.address != null && message.hasOwnProperty("address"))
                object.address = message.address;
            if (message.port != null && message.hasOwnProperty("port"))
                object.port = message.port;
            return object;
        };

        NetworkEndpoint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NetworkEndpoint.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NetworkEndpoint";
        };

        return NetworkEndpoint;
    })();

    lq.ReqCommon = (function() {

        function ReqCommon(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCommon.create = function create(properties) {
            return new ReqCommon(properties);
        };

        ReqCommon.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCommon.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCommon.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCommon();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCommon.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCommon.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        ReqCommon.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCommon)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.ReqCommon();
        };

        ReqCommon.toObject = function toObject() {
            return {};
        };

        ReqCommon.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCommon.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCommon";
        };

        return ReqCommon;
    })();

    lq.ResCommon = (function() {

        function ResCommon(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCommon.prototype.error = null;

        ResCommon.create = function create(properties) {
            return new ResCommon(properties);
        };

        ResCommon.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCommon.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCommon.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCommon(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCommon.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCommon.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResCommon.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCommon)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCommon();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCommon.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResCommon.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResCommon.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCommon.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCommon";
        };

        return ResCommon;
    })();

    lq.StringDirty = (function() {

        function StringDirty(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        StringDirty.prototype.dirty = false;
        StringDirty.prototype.value = "";

        StringDirty.create = function create(properties) {
            return new StringDirty(properties);
        };

        StringDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(18).string(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        StringDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        StringDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.StringDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        StringDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        StringDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            return null;
        };

        StringDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.StringDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.StringDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value != null)
                if (typeof object.value !== "string" || object.value.length)
                    message.value = String(object.value);
            return message;
        };

        StringDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dirty = false;
                object.value = "";
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        StringDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StringDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.StringDirty";
        };

        return StringDirty;
    })();

    lq.StringArrayDirty = (function() {

        function StringArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        StringArrayDirty.prototype.dirty = false;
        StringArrayDirty.prototype.value = $util.emptyArray;

        StringArrayDirty.create = function create(properties) {
            return new StringArrayDirty(properties);
        };

        StringArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    writer.uint32(18).string(message.value[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        StringArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        StringArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.StringArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        StringArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        StringArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i)
                    if (!$util.isString(message.value[i]))
                        return "value: string[] expected";
            }
            return null;
        };

        StringArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.StringArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.StringArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.StringArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i)
                    message.value[i] = String(object.value[i]);
            }
            return message;
        };

        StringArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = message.value[j];
            }
            return object;
        };

        StringArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StringArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.StringArrayDirty";
        };

        return StringArrayDirty;
    })();

    lq.UInt32ArrayDirty = (function() {

        function UInt32ArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        UInt32ArrayDirty.prototype.dirty = false;
        UInt32ArrayDirty.prototype.value = $util.emptyArray;

        UInt32ArrayDirty.create = function create(properties) {
            return new UInt32ArrayDirty(properties);
        };

        UInt32ArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.value.length; ++i)
                    writer.uint32(message.value[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        UInt32ArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        UInt32ArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.UInt32ArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.value && message.value.length))
                                message.value = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.value.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        UInt32ArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        UInt32ArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i)
                    if (!$util.isInteger(message.value[i]))
                        return "value: integer[] expected";
            }
            return null;
        };

        UInt32ArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.UInt32ArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.UInt32ArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.UInt32ArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i)
                    message.value[i] = object.value[i] >>> 0;
            }
            return message;
        };

        UInt32ArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = message.value[j];
            }
            return object;
        };

        UInt32ArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        UInt32ArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.UInt32ArrayDirty";
        };

        return UInt32ArrayDirty;
    })();

    lq.UInt32Dirty = (function() {

        function UInt32Dirty(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        UInt32Dirty.prototype.dirty = false;
        UInt32Dirty.prototype.value = 0;

        UInt32Dirty.create = function create(properties) {
            return new UInt32Dirty(properties);
        };

        UInt32Dirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(16).uint32(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        UInt32Dirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        UInt32Dirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.UInt32Dirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        UInt32Dirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        UInt32Dirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        UInt32Dirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.UInt32Dirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.UInt32Dirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value != null)
                if (Number(object.value) !== 0)
                    message.value = object.value >>> 0;
            return message;
        };

        UInt32Dirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dirty = false;
                object.value = 0;
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        UInt32Dirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        UInt32Dirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.UInt32Dirty";
        };

        return UInt32Dirty;
    })();

    lq.ResAccountUpdate = (function() {

        function ResAccountUpdate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountUpdate.prototype.error = null;
        ResAccountUpdate.prototype.update = null;

        ResAccountUpdate.create = function create(properties) {
            return new ResAccountUpdate(properties);
        };

        ResAccountUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.update != null && Object.hasOwnProperty.call(message, "update"))
                $root.lq.AccountUpdate.encode(message.update, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.update = $root.lq.AccountUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.update);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.update != null && message.hasOwnProperty("update")) {
                let error = $root.lq.AccountUpdate.verify(message.update, _depth + 1);
                if (error)
                    return "update." + error;
            }
            return null;
        };

        ResAccountUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountUpdate();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountUpdate.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.update != null) {
                if (typeof object.update !== "object")
                    throw TypeError(".lq.ResAccountUpdate.update: object expected");
                message.update = $root.lq.AccountUpdate.fromObject(object.update, _depth + 1);
            }
            return message;
        };

        ResAccountUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.update = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.update != null && message.hasOwnProperty("update"))
                object.update = $root.lq.AccountUpdate.toObject(message.update, options);
            return object;
        };

        ResAccountUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountUpdate";
        };

        return ResAccountUpdate;
    })();

    lq.AntiAddiction = (function() {

        function AntiAddiction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AntiAddiction.prototype.online_duration = 0;

        AntiAddiction.create = function create(properties) {
            return new AntiAddiction(properties);
        };

        AntiAddiction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.online_duration != null && Object.hasOwnProperty.call(message, "online_duration"))
                writer.uint32(8).uint32(message.online_duration);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AntiAddiction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AntiAddiction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AntiAddiction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.online_duration = value;
                        else
                            delete message.online_duration;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AntiAddiction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AntiAddiction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.online_duration != null && message.hasOwnProperty("online_duration"))
                if (!$util.isInteger(message.online_duration))
                    return "online_duration: integer expected";
            return null;
        };

        AntiAddiction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AntiAddiction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AntiAddiction();
            if (object.online_duration != null)
                if (Number(object.online_duration) !== 0)
                    message.online_duration = object.online_duration >>> 0;
            return message;
        };

        AntiAddiction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.online_duration = 0;
            if (message.online_duration != null && message.hasOwnProperty("online_duration"))
                object.online_duration = message.online_duration;
            return object;
        };

        AntiAddiction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AntiAddiction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AntiAddiction";
        };

        return AntiAddiction;
    })();

    lq.HighestHuRecord = (function() {

        function HighestHuRecord(properties) {
            this.hands = [];
            this.ming = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        HighestHuRecord.prototype.fanshu = 0;
        HighestHuRecord.prototype.doranum = 0;
        HighestHuRecord.prototype.title = "";
        HighestHuRecord.prototype.hands = $util.emptyArray;
        HighestHuRecord.prototype.ming = $util.emptyArray;
        HighestHuRecord.prototype.hupai = "";
        HighestHuRecord.prototype.title_id = 0;

        HighestHuRecord.create = function create(properties) {
            return new HighestHuRecord(properties);
        };

        HighestHuRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.fanshu != null && Object.hasOwnProperty.call(message, "fanshu"))
                writer.uint32(8).uint32(message.fanshu);
            if (message.doranum != null && Object.hasOwnProperty.call(message, "doranum"))
                writer.uint32(16).uint32(message.doranum);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(26).string(message.title);
            if (message.hands != null && message.hands.length)
                for (let i = 0; i < message.hands.length; ++i)
                    writer.uint32(34).string(message.hands[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(42).string(message.ming[i]);
            if (message.hupai != null && Object.hasOwnProperty.call(message, "hupai"))
                writer.uint32(50).string(message.hupai);
            if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                writer.uint32(56).uint32(message.title_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HighestHuRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        HighestHuRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.HighestHuRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fanshu = value;
                        else
                            delete message.fanshu;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.doranum = value;
                        else
                            delete message.doranum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hands && message.hands.length))
                            message.hands = [];
                        message.hands.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hupai = value;
                        else
                            delete message.hupai;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title_id = value;
                        else
                            delete message.title_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        HighestHuRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        HighestHuRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fanshu != null && message.hasOwnProperty("fanshu"))
                if (!$util.isInteger(message.fanshu))
                    return "fanshu: integer expected";
            if (message.doranum != null && message.hasOwnProperty("doranum"))
                if (!$util.isInteger(message.doranum))
                    return "doranum: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.hands != null && message.hasOwnProperty("hands")) {
                if (!Array.isArray(message.hands))
                    return "hands: array expected";
                for (let i = 0; i < message.hands.length; ++i)
                    if (!$util.isString(message.hands[i]))
                        return "hands: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.hupai != null && message.hasOwnProperty("hupai"))
                if (!$util.isString(message.hupai))
                    return "hupai: string expected";
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                if (!$util.isInteger(message.title_id))
                    return "title_id: integer expected";
            return null;
        };

        HighestHuRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.HighestHuRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.HighestHuRecord();
            if (object.fanshu != null)
                if (Number(object.fanshu) !== 0)
                    message.fanshu = object.fanshu >>> 0;
            if (object.doranum != null)
                if (Number(object.doranum) !== 0)
                    message.doranum = object.doranum >>> 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.hands) {
                if (!Array.isArray(object.hands))
                    throw TypeError(".lq.HighestHuRecord.hands: array expected");
                message.hands = Array(object.hands.length);
                for (let i = 0; i < object.hands.length; ++i)
                    message.hands[i] = String(object.hands[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.HighestHuRecord.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.hupai != null)
                if (typeof object.hupai !== "string" || object.hupai.length)
                    message.hupai = String(object.hupai);
            if (object.title_id != null)
                if (Number(object.title_id) !== 0)
                    message.title_id = object.title_id >>> 0;
            return message;
        };

        HighestHuRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hands = [];
                object.ming = [];
            }
            if (options.defaults) {
                object.fanshu = 0;
                object.doranum = 0;
                object.title = "";
                object.hupai = "";
                object.title_id = 0;
            }
            if (message.fanshu != null && message.hasOwnProperty("fanshu"))
                object.fanshu = message.fanshu;
            if (message.doranum != null && message.hasOwnProperty("doranum"))
                object.doranum = message.doranum;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.hands && message.hands.length) {
                object.hands = Array(message.hands.length);
                for (let j = 0; j < message.hands.length; ++j)
                    object.hands[j] = message.hands[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.hupai != null && message.hasOwnProperty("hupai"))
                object.hupai = message.hupai;
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                object.title_id = message.title_id;
            return object;
        };

        HighestHuRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HighestHuRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.HighestHuRecord";
        };

        return HighestHuRecord;
    })();

    lq.AccountMahjongStatistic = (function() {

        function AccountMahjongStatistic(properties) {
            this.final_position_counts = [];
            this.recent_10_game_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountMahjongStatistic.prototype.final_position_counts = $util.emptyArray;
        AccountMahjongStatistic.prototype.recent_round = null;
        AccountMahjongStatistic.prototype.recent_hu = null;
        AccountMahjongStatistic.prototype.highest_hu = null;
        AccountMahjongStatistic.prototype.recent_20_hu_summary = null;
        AccountMahjongStatistic.prototype.recent_10_hu_summary = null;
        AccountMahjongStatistic.prototype.recent_10_game_result = $util.emptyArray;

        AccountMahjongStatistic.create = function create(properties) {
            return new AccountMahjongStatistic(properties);
        };

        AccountMahjongStatistic.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.final_position_counts != null && message.final_position_counts.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.final_position_counts.length; ++i)
                    writer.uint32(message.final_position_counts[i]);
                writer.ldelim();
            }
            if (message.recent_round != null && Object.hasOwnProperty.call(message, "recent_round"))
                $root.lq.AccountMahjongStatistic.RoundSummary.encode(message.recent_round, writer.uint32(18).fork()).ldelim();
            if (message.recent_hu != null && Object.hasOwnProperty.call(message, "recent_hu"))
                $root.lq.AccountMahjongStatistic.HuSummary.encode(message.recent_hu, writer.uint32(26).fork()).ldelim();
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                $root.lq.HighestHuRecord.encode(message.highest_hu, writer.uint32(34).fork()).ldelim();
            if (message.recent_20_hu_summary != null && Object.hasOwnProperty.call(message, "recent_20_hu_summary"))
                $root.lq.AccountMahjongStatistic.Liqi20Summary.encode(message.recent_20_hu_summary, writer.uint32(50).fork()).ldelim();
            if (message.recent_10_hu_summary != null && Object.hasOwnProperty.call(message, "recent_10_hu_summary"))
                $root.lq.AccountMahjongStatistic.LiQi10Summary.encode(message.recent_10_hu_summary, writer.uint32(58).fork()).ldelim();
            if (message.recent_10_game_result != null && message.recent_10_game_result.length)
                for (let i = 0; i < message.recent_10_game_result.length; ++i)
                    $root.lq.AccountMahjongStatistic.GameResult.encode(message.recent_10_game_result[i], writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountMahjongStatistic.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountMahjongStatistic.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.final_position_counts && message.final_position_counts.length))
                                message.final_position_counts = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.final_position_counts.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.final_position_counts && message.final_position_counts.length))
                            message.final_position_counts = [];
                        message.final_position_counts.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.recent_round = $root.lq.AccountMahjongStatistic.RoundSummary.decode(reader, reader.uint32(), undefined, _depth + 1, message.recent_round);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.recent_hu = $root.lq.AccountMahjongStatistic.HuSummary.decode(reader, reader.uint32(), undefined, _depth + 1, message.recent_hu);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.highest_hu = $root.lq.HighestHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_hu);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.recent_20_hu_summary = $root.lq.AccountMahjongStatistic.Liqi20Summary.decode(reader, reader.uint32(), undefined, _depth + 1, message.recent_20_hu_summary);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.recent_10_hu_summary = $root.lq.AccountMahjongStatistic.LiQi10Summary.decode(reader, reader.uint32(), undefined, _depth + 1, message.recent_10_hu_summary);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recent_10_game_result && message.recent_10_game_result.length))
                            message.recent_10_game_result = [];
                        message.recent_10_game_result.push($root.lq.AccountMahjongStatistic.GameResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountMahjongStatistic.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountMahjongStatistic.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.final_position_counts != null && message.hasOwnProperty("final_position_counts")) {
                if (!Array.isArray(message.final_position_counts))
                    return "final_position_counts: array expected";
                for (let i = 0; i < message.final_position_counts.length; ++i)
                    if (!$util.isInteger(message.final_position_counts[i]))
                        return "final_position_counts: integer[] expected";
            }
            if (message.recent_round != null && message.hasOwnProperty("recent_round")) {
                let error = $root.lq.AccountMahjongStatistic.RoundSummary.verify(message.recent_round, _depth + 1);
                if (error)
                    return "recent_round." + error;
            }
            if (message.recent_hu != null && message.hasOwnProperty("recent_hu")) {
                let error = $root.lq.AccountMahjongStatistic.HuSummary.verify(message.recent_hu, _depth + 1);
                if (error)
                    return "recent_hu." + error;
            }
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu")) {
                let error = $root.lq.HighestHuRecord.verify(message.highest_hu, _depth + 1);
                if (error)
                    return "highest_hu." + error;
            }
            if (message.recent_20_hu_summary != null && message.hasOwnProperty("recent_20_hu_summary")) {
                let error = $root.lq.AccountMahjongStatistic.Liqi20Summary.verify(message.recent_20_hu_summary, _depth + 1);
                if (error)
                    return "recent_20_hu_summary." + error;
            }
            if (message.recent_10_hu_summary != null && message.hasOwnProperty("recent_10_hu_summary")) {
                let error = $root.lq.AccountMahjongStatistic.LiQi10Summary.verify(message.recent_10_hu_summary, _depth + 1);
                if (error)
                    return "recent_10_hu_summary." + error;
            }
            if (message.recent_10_game_result != null && message.hasOwnProperty("recent_10_game_result")) {
                if (!Array.isArray(message.recent_10_game_result))
                    return "recent_10_game_result: array expected";
                for (let i = 0; i < message.recent_10_game_result.length; ++i) {
                    let error = $root.lq.AccountMahjongStatistic.GameResult.verify(message.recent_10_game_result[i], _depth + 1);
                    if (error)
                        return "recent_10_game_result." + error;
                }
            }
            return null;
        };

        AccountMahjongStatistic.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountMahjongStatistic)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountMahjongStatistic();
            if (object.final_position_counts) {
                if (!Array.isArray(object.final_position_counts))
                    throw TypeError(".lq.AccountMahjongStatistic.final_position_counts: array expected");
                message.final_position_counts = Array(object.final_position_counts.length);
                for (let i = 0; i < object.final_position_counts.length; ++i)
                    message.final_position_counts[i] = object.final_position_counts[i] >>> 0;
            }
            if (object.recent_round != null) {
                if (typeof object.recent_round !== "object")
                    throw TypeError(".lq.AccountMahjongStatistic.recent_round: object expected");
                message.recent_round = $root.lq.AccountMahjongStatistic.RoundSummary.fromObject(object.recent_round, _depth + 1);
            }
            if (object.recent_hu != null) {
                if (typeof object.recent_hu !== "object")
                    throw TypeError(".lq.AccountMahjongStatistic.recent_hu: object expected");
                message.recent_hu = $root.lq.AccountMahjongStatistic.HuSummary.fromObject(object.recent_hu, _depth + 1);
            }
            if (object.highest_hu != null) {
                if (typeof object.highest_hu !== "object")
                    throw TypeError(".lq.AccountMahjongStatistic.highest_hu: object expected");
                message.highest_hu = $root.lq.HighestHuRecord.fromObject(object.highest_hu, _depth + 1);
            }
            if (object.recent_20_hu_summary != null) {
                if (typeof object.recent_20_hu_summary !== "object")
                    throw TypeError(".lq.AccountMahjongStatistic.recent_20_hu_summary: object expected");
                message.recent_20_hu_summary = $root.lq.AccountMahjongStatistic.Liqi20Summary.fromObject(object.recent_20_hu_summary, _depth + 1);
            }
            if (object.recent_10_hu_summary != null) {
                if (typeof object.recent_10_hu_summary !== "object")
                    throw TypeError(".lq.AccountMahjongStatistic.recent_10_hu_summary: object expected");
                message.recent_10_hu_summary = $root.lq.AccountMahjongStatistic.LiQi10Summary.fromObject(object.recent_10_hu_summary, _depth + 1);
            }
            if (object.recent_10_game_result) {
                if (!Array.isArray(object.recent_10_game_result))
                    throw TypeError(".lq.AccountMahjongStatistic.recent_10_game_result: array expected");
                message.recent_10_game_result = Array(object.recent_10_game_result.length);
                for (let i = 0; i < object.recent_10_game_result.length; ++i) {
                    if (typeof object.recent_10_game_result[i] !== "object")
                        throw TypeError(".lq.AccountMahjongStatistic.recent_10_game_result: object expected");
                    message.recent_10_game_result[i] = $root.lq.AccountMahjongStatistic.GameResult.fromObject(object.recent_10_game_result[i], _depth + 1);
                }
            }
            return message;
        };

        AccountMahjongStatistic.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.final_position_counts = [];
                object.recent_10_game_result = [];
            }
            if (options.defaults) {
                object.recent_round = null;
                object.recent_hu = null;
                object.highest_hu = null;
                object.recent_20_hu_summary = null;
                object.recent_10_hu_summary = null;
            }
            if (message.final_position_counts && message.final_position_counts.length) {
                object.final_position_counts = Array(message.final_position_counts.length);
                for (let j = 0; j < message.final_position_counts.length; ++j)
                    object.final_position_counts[j] = message.final_position_counts[j];
            }
            if (message.recent_round != null && message.hasOwnProperty("recent_round"))
                object.recent_round = $root.lq.AccountMahjongStatistic.RoundSummary.toObject(message.recent_round, options);
            if (message.recent_hu != null && message.hasOwnProperty("recent_hu"))
                object.recent_hu = $root.lq.AccountMahjongStatistic.HuSummary.toObject(message.recent_hu, options);
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = $root.lq.HighestHuRecord.toObject(message.highest_hu, options);
            if (message.recent_20_hu_summary != null && message.hasOwnProperty("recent_20_hu_summary"))
                object.recent_20_hu_summary = $root.lq.AccountMahjongStatistic.Liqi20Summary.toObject(message.recent_20_hu_summary, options);
            if (message.recent_10_hu_summary != null && message.hasOwnProperty("recent_10_hu_summary"))
                object.recent_10_hu_summary = $root.lq.AccountMahjongStatistic.LiQi10Summary.toObject(message.recent_10_hu_summary, options);
            if (message.recent_10_game_result && message.recent_10_game_result.length) {
                object.recent_10_game_result = Array(message.recent_10_game_result.length);
                for (let j = 0; j < message.recent_10_game_result.length; ++j)
                    object.recent_10_game_result[j] = $root.lq.AccountMahjongStatistic.GameResult.toObject(message.recent_10_game_result[j], options);
            }
            return object;
        };

        AccountMahjongStatistic.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountMahjongStatistic.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountMahjongStatistic";
        };

        AccountMahjongStatistic.RoundSummary = (function() {

            function RoundSummary(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RoundSummary.prototype.total_count = 0;
            RoundSummary.prototype.rong_count = 0;
            RoundSummary.prototype.zimo_count = 0;
            RoundSummary.prototype.fangchong_count = 0;

            RoundSummary.create = function create(properties) {
                return new RoundSummary(properties);
            };

            RoundSummary.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_count != null && Object.hasOwnProperty.call(message, "total_count"))
                    writer.uint32(8).uint32(message.total_count);
                if (message.rong_count != null && Object.hasOwnProperty.call(message, "rong_count"))
                    writer.uint32(16).uint32(message.rong_count);
                if (message.zimo_count != null && Object.hasOwnProperty.call(message, "zimo_count"))
                    writer.uint32(24).uint32(message.zimo_count);
                if (message.fangchong_count != null && Object.hasOwnProperty.call(message, "fangchong_count"))
                    writer.uint32(32).uint32(message.fangchong_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RoundSummary.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RoundSummary.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic.RoundSummary(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_count = value;
                            else
                                delete message.total_count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rong_count = value;
                            else
                                delete message.rong_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.zimo_count = value;
                            else
                                delete message.zimo_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.fangchong_count = value;
                            else
                                delete message.fangchong_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RoundSummary.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RoundSummary.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    if (!$util.isInteger(message.total_count))
                        return "total_count: integer expected";
                if (message.rong_count != null && message.hasOwnProperty("rong_count"))
                    if (!$util.isInteger(message.rong_count))
                        return "rong_count: integer expected";
                if (message.zimo_count != null && message.hasOwnProperty("zimo_count"))
                    if (!$util.isInteger(message.zimo_count))
                        return "zimo_count: integer expected";
                if (message.fangchong_count != null && message.hasOwnProperty("fangchong_count"))
                    if (!$util.isInteger(message.fangchong_count))
                        return "fangchong_count: integer expected";
                return null;
            };

            RoundSummary.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMahjongStatistic.RoundSummary)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMahjongStatistic.RoundSummary();
                if (object.total_count != null)
                    if (Number(object.total_count) !== 0)
                        message.total_count = object.total_count >>> 0;
                if (object.rong_count != null)
                    if (Number(object.rong_count) !== 0)
                        message.rong_count = object.rong_count >>> 0;
                if (object.zimo_count != null)
                    if (Number(object.zimo_count) !== 0)
                        message.zimo_count = object.zimo_count >>> 0;
                if (object.fangchong_count != null)
                    if (Number(object.fangchong_count) !== 0)
                        message.fangchong_count = object.fangchong_count >>> 0;
                return message;
            };

            RoundSummary.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_count = 0;
                    object.rong_count = 0;
                    object.zimo_count = 0;
                    object.fangchong_count = 0;
                }
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    object.total_count = message.total_count;
                if (message.rong_count != null && message.hasOwnProperty("rong_count"))
                    object.rong_count = message.rong_count;
                if (message.zimo_count != null && message.hasOwnProperty("zimo_count"))
                    object.zimo_count = message.zimo_count;
                if (message.fangchong_count != null && message.hasOwnProperty("fangchong_count"))
                    object.fangchong_count = message.fangchong_count;
                return object;
            };

            RoundSummary.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RoundSummary.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMahjongStatistic.RoundSummary";
            };

            return RoundSummary;
        })();

        AccountMahjongStatistic.HuSummary = (function() {

            function HuSummary(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            HuSummary.prototype.total_count = 0;
            HuSummary.prototype.dora_round_count = 0;
            HuSummary.prototype.total_fan = 0;

            HuSummary.create = function create(properties) {
                return new HuSummary(properties);
            };

            HuSummary.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_count != null && Object.hasOwnProperty.call(message, "total_count"))
                    writer.uint32(8).uint32(message.total_count);
                if (message.dora_round_count != null && Object.hasOwnProperty.call(message, "dora_round_count"))
                    writer.uint32(16).uint32(message.dora_round_count);
                if (message.total_fan != null && Object.hasOwnProperty.call(message, "total_fan"))
                    writer.uint32(24).uint32(message.total_fan);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            HuSummary.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            HuSummary.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic.HuSummary(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_count = value;
                            else
                                delete message.total_count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dora_round_count = value;
                            else
                                delete message.dora_round_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_fan = value;
                            else
                                delete message.total_fan;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            HuSummary.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            HuSummary.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    if (!$util.isInteger(message.total_count))
                        return "total_count: integer expected";
                if (message.dora_round_count != null && message.hasOwnProperty("dora_round_count"))
                    if (!$util.isInteger(message.dora_round_count))
                        return "dora_round_count: integer expected";
                if (message.total_fan != null && message.hasOwnProperty("total_fan"))
                    if (!$util.isInteger(message.total_fan))
                        return "total_fan: integer expected";
                return null;
            };

            HuSummary.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMahjongStatistic.HuSummary)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMahjongStatistic.HuSummary();
                if (object.total_count != null)
                    if (Number(object.total_count) !== 0)
                        message.total_count = object.total_count >>> 0;
                if (object.dora_round_count != null)
                    if (Number(object.dora_round_count) !== 0)
                        message.dora_round_count = object.dora_round_count >>> 0;
                if (object.total_fan != null)
                    if (Number(object.total_fan) !== 0)
                        message.total_fan = object.total_fan >>> 0;
                return message;
            };

            HuSummary.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_count = 0;
                    object.dora_round_count = 0;
                    object.total_fan = 0;
                }
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    object.total_count = message.total_count;
                if (message.dora_round_count != null && message.hasOwnProperty("dora_round_count"))
                    object.dora_round_count = message.dora_round_count;
                if (message.total_fan != null && message.hasOwnProperty("total_fan"))
                    object.total_fan = message.total_fan;
                return object;
            };

            HuSummary.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HuSummary.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMahjongStatistic.HuSummary";
            };

            return HuSummary;
        })();

        AccountMahjongStatistic.Liqi20Summary = (function() {

            function Liqi20Summary(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Liqi20Summary.prototype.total_count = 0;
            Liqi20Summary.prototype.total_lidora_count = 0;
            Liqi20Summary.prototype.average_hu_point = 0;

            Liqi20Summary.create = function create(properties) {
                return new Liqi20Summary(properties);
            };

            Liqi20Summary.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_count != null && Object.hasOwnProperty.call(message, "total_count"))
                    writer.uint32(8).uint32(message.total_count);
                if (message.total_lidora_count != null && Object.hasOwnProperty.call(message, "total_lidora_count"))
                    writer.uint32(16).uint32(message.total_lidora_count);
                if (message.average_hu_point != null && Object.hasOwnProperty.call(message, "average_hu_point"))
                    writer.uint32(24).uint32(message.average_hu_point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Liqi20Summary.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Liqi20Summary.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic.Liqi20Summary(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_count = value;
                            else
                                delete message.total_count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_lidora_count = value;
                            else
                                delete message.total_lidora_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.average_hu_point = value;
                            else
                                delete message.average_hu_point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Liqi20Summary.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Liqi20Summary.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    if (!$util.isInteger(message.total_count))
                        return "total_count: integer expected";
                if (message.total_lidora_count != null && message.hasOwnProperty("total_lidora_count"))
                    if (!$util.isInteger(message.total_lidora_count))
                        return "total_lidora_count: integer expected";
                if (message.average_hu_point != null && message.hasOwnProperty("average_hu_point"))
                    if (!$util.isInteger(message.average_hu_point))
                        return "average_hu_point: integer expected";
                return null;
            };

            Liqi20Summary.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMahjongStatistic.Liqi20Summary)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMahjongStatistic.Liqi20Summary();
                if (object.total_count != null)
                    if (Number(object.total_count) !== 0)
                        message.total_count = object.total_count >>> 0;
                if (object.total_lidora_count != null)
                    if (Number(object.total_lidora_count) !== 0)
                        message.total_lidora_count = object.total_lidora_count >>> 0;
                if (object.average_hu_point != null)
                    if (Number(object.average_hu_point) !== 0)
                        message.average_hu_point = object.average_hu_point >>> 0;
                return message;
            };

            Liqi20Summary.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_count = 0;
                    object.total_lidora_count = 0;
                    object.average_hu_point = 0;
                }
                if (message.total_count != null && message.hasOwnProperty("total_count"))
                    object.total_count = message.total_count;
                if (message.total_lidora_count != null && message.hasOwnProperty("total_lidora_count"))
                    object.total_lidora_count = message.total_lidora_count;
                if (message.average_hu_point != null && message.hasOwnProperty("average_hu_point"))
                    object.average_hu_point = message.average_hu_point;
                return object;
            };

            Liqi20Summary.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Liqi20Summary.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMahjongStatistic.Liqi20Summary";
            };

            return Liqi20Summary;
        })();

        AccountMahjongStatistic.LiQi10Summary = (function() {

            function LiQi10Summary(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LiQi10Summary.prototype.total_xuanshang = 0;
            LiQi10Summary.prototype.total_fanshu = 0;

            LiQi10Summary.create = function create(properties) {
                return new LiQi10Summary(properties);
            };

            LiQi10Summary.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_xuanshang != null && Object.hasOwnProperty.call(message, "total_xuanshang"))
                    writer.uint32(8).uint32(message.total_xuanshang);
                if (message.total_fanshu != null && Object.hasOwnProperty.call(message, "total_fanshu"))
                    writer.uint32(16).uint32(message.total_fanshu);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LiQi10Summary.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LiQi10Summary.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic.LiQi10Summary(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_xuanshang = value;
                            else
                                delete message.total_xuanshang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_fanshu = value;
                            else
                                delete message.total_fanshu;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LiQi10Summary.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LiQi10Summary.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_xuanshang != null && message.hasOwnProperty("total_xuanshang"))
                    if (!$util.isInteger(message.total_xuanshang))
                        return "total_xuanshang: integer expected";
                if (message.total_fanshu != null && message.hasOwnProperty("total_fanshu"))
                    if (!$util.isInteger(message.total_fanshu))
                        return "total_fanshu: integer expected";
                return null;
            };

            LiQi10Summary.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMahjongStatistic.LiQi10Summary)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMahjongStatistic.LiQi10Summary();
                if (object.total_xuanshang != null)
                    if (Number(object.total_xuanshang) !== 0)
                        message.total_xuanshang = object.total_xuanshang >>> 0;
                if (object.total_fanshu != null)
                    if (Number(object.total_fanshu) !== 0)
                        message.total_fanshu = object.total_fanshu >>> 0;
                return message;
            };

            LiQi10Summary.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_xuanshang = 0;
                    object.total_fanshu = 0;
                }
                if (message.total_xuanshang != null && message.hasOwnProperty("total_xuanshang"))
                    object.total_xuanshang = message.total_xuanshang;
                if (message.total_fanshu != null && message.hasOwnProperty("total_fanshu"))
                    object.total_fanshu = message.total_fanshu;
                return object;
            };

            LiQi10Summary.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LiQi10Summary.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMahjongStatistic.LiQi10Summary";
            };

            return LiQi10Summary;
        })();

        AccountMahjongStatistic.GameResult = (function() {

            function GameResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameResult.prototype.rank = 0;
            GameResult.prototype.final_point = 0;

            GameResult.create = function create(properties) {
                return new GameResult(properties);
            };

            GameResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(8).uint32(message.rank);
                if (message.final_point != null && Object.hasOwnProperty.call(message, "final_point"))
                    writer.uint32(16).int32(message.final_point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMahjongStatistic.GameResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.final_point = value;
                            else
                                delete message.final_point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.final_point != null && message.hasOwnProperty("final_point"))
                    if (!$util.isInteger(message.final_point))
                        return "final_point: integer expected";
                return null;
            };

            GameResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMahjongStatistic.GameResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMahjongStatistic.GameResult();
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.final_point != null)
                    if (Number(object.final_point) !== 0)
                        message.final_point = object.final_point | 0;
                return message;
            };

            GameResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.rank = 0;
                    object.final_point = 0;
                }
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.final_point != null && message.hasOwnProperty("final_point"))
                    object.final_point = message.final_point;
                return object;
            };

            GameResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMahjongStatistic.GameResult";
            };

            return GameResult;
        })();

        return AccountMahjongStatistic;
    })();

    lq.AccountStatisticData = (function() {

        function AccountStatisticData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountStatisticData.prototype.mahjong_category = 0;
        AccountStatisticData.prototype.game_category = 0;
        AccountStatisticData.prototype.statistic = null;
        AccountStatisticData.prototype.game_type = 0;

        AccountStatisticData.create = function create(properties) {
            return new AccountStatisticData(properties);
        };

        AccountStatisticData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mahjong_category != null && Object.hasOwnProperty.call(message, "mahjong_category"))
                writer.uint32(8).uint32(message.mahjong_category);
            if (message.game_category != null && Object.hasOwnProperty.call(message, "game_category"))
                writer.uint32(16).uint32(message.game_category);
            if (message.statistic != null && Object.hasOwnProperty.call(message, "statistic"))
                $root.lq.AccountMahjongStatistic.encode(message.statistic, writer.uint32(26).fork()).ldelim();
            if (message.game_type != null && Object.hasOwnProperty.call(message, "game_type"))
                writer.uint32(32).uint32(message.game_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountStatisticData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountStatisticData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountStatisticData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mahjong_category = value;
                        else
                            delete message.mahjong_category;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_category = value;
                        else
                            delete message.game_category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.statistic = $root.lq.AccountMahjongStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.statistic);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_type = value;
                        else
                            delete message.game_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountStatisticData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountStatisticData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mahjong_category != null && message.hasOwnProperty("mahjong_category"))
                if (!$util.isInteger(message.mahjong_category))
                    return "mahjong_category: integer expected";
            if (message.game_category != null && message.hasOwnProperty("game_category"))
                if (!$util.isInteger(message.game_category))
                    return "game_category: integer expected";
            if (message.statistic != null && message.hasOwnProperty("statistic")) {
                let error = $root.lq.AccountMahjongStatistic.verify(message.statistic, _depth + 1);
                if (error)
                    return "statistic." + error;
            }
            if (message.game_type != null && message.hasOwnProperty("game_type"))
                if (!$util.isInteger(message.game_type))
                    return "game_type: integer expected";
            return null;
        };

        AccountStatisticData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountStatisticData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountStatisticData();
            if (object.mahjong_category != null)
                if (Number(object.mahjong_category) !== 0)
                    message.mahjong_category = object.mahjong_category >>> 0;
            if (object.game_category != null)
                if (Number(object.game_category) !== 0)
                    message.game_category = object.game_category >>> 0;
            if (object.statistic != null) {
                if (typeof object.statistic !== "object")
                    throw TypeError(".lq.AccountStatisticData.statistic: object expected");
                message.statistic = $root.lq.AccountMahjongStatistic.fromObject(object.statistic, _depth + 1);
            }
            if (object.game_type != null)
                if (Number(object.game_type) !== 0)
                    message.game_type = object.game_type >>> 0;
            return message;
        };

        AccountStatisticData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.mahjong_category = 0;
                object.game_category = 0;
                object.statistic = null;
                object.game_type = 0;
            }
            if (message.mahjong_category != null && message.hasOwnProperty("mahjong_category"))
                object.mahjong_category = message.mahjong_category;
            if (message.game_category != null && message.hasOwnProperty("game_category"))
                object.game_category = message.game_category;
            if (message.statistic != null && message.hasOwnProperty("statistic"))
                object.statistic = $root.lq.AccountMahjongStatistic.toObject(message.statistic, options);
            if (message.game_type != null && message.hasOwnProperty("game_type"))
                object.game_type = message.game_type;
            return object;
        };

        AccountStatisticData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountStatisticData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountStatisticData";
        };

        return AccountStatisticData;
    })();

    lq.AccountLevel = (function() {

        function AccountLevel(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountLevel.prototype.id = 0;
        AccountLevel.prototype.score = 0;

        AccountLevel.create = function create(properties) {
            return new AccountLevel(properties);
        };

        AccountLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(16).uint32(message.score);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            return null;
        };

        AccountLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountLevel();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score >>> 0;
            return message;
        };

        AccountLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.score = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            return object;
        };

        AccountLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountLevel";
        };

        return AccountLevel;
    })();

    lq.ViewSlot = (function() {

        function ViewSlot(properties) {
            this.item_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ViewSlot.prototype.slot = 0;
        ViewSlot.prototype.item_id = 0;
        ViewSlot.prototype.type = 0;
        ViewSlot.prototype.item_id_list = $util.emptyArray;

        ViewSlot.create = function create(properties) {
            return new ViewSlot(properties);
        };

        ViewSlot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.slot != null && Object.hasOwnProperty.call(message, "slot"))
                writer.uint32(8).uint32(message.slot);
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(16).uint32(message.item_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.item_id_list != null && message.item_id_list.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.item_id_list.length; ++i)
                    writer.uint32(message.item_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ViewSlot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ViewSlot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ViewSlot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.slot = value;
                        else
                            delete message.slot;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.item_id_list && message.item_id_list.length))
                                message.item_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.item_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.item_id_list && message.item_id_list.length))
                            message.item_id_list = [];
                        message.item_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ViewSlot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ViewSlot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.slot != null && message.hasOwnProperty("slot"))
                if (!$util.isInteger(message.slot))
                    return "slot: integer expected";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.item_id_list != null && message.hasOwnProperty("item_id_list")) {
                if (!Array.isArray(message.item_id_list))
                    return "item_id_list: array expected";
                for (let i = 0; i < message.item_id_list.length; ++i)
                    if (!$util.isInteger(message.item_id_list[i]))
                        return "item_id_list: integer[] expected";
            }
            return null;
        };

        ViewSlot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ViewSlot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ViewSlot();
            if (object.slot != null)
                if (Number(object.slot) !== 0)
                    message.slot = object.slot >>> 0;
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.item_id_list) {
                if (!Array.isArray(object.item_id_list))
                    throw TypeError(".lq.ViewSlot.item_id_list: array expected");
                message.item_id_list = Array(object.item_id_list.length);
                for (let i = 0; i < object.item_id_list.length; ++i)
                    message.item_id_list[i] = object.item_id_list[i] >>> 0;
            }
            return message;
        };

        ViewSlot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.item_id_list = [];
            if (options.defaults) {
                object.slot = 0;
                object.item_id = 0;
                object.type = 0;
            }
            if (message.slot != null && message.hasOwnProperty("slot"))
                object.slot = message.slot;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.item_id_list && message.item_id_list.length) {
                object.item_id_list = Array(message.item_id_list.length);
                for (let j = 0; j < message.item_id_list.length; ++j)
                    object.item_id_list[j] = message.item_id_list[j];
            }
            return object;
        };

        ViewSlot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ViewSlot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ViewSlot";
        };

        return ViewSlot;
    })();

    lq.FavoriteHu = (function() {

        function FavoriteHu(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FavoriteHu.prototype.category = 0;
        FavoriteHu.prototype.type = 0;
        FavoriteHu.prototype.hu = null;
        FavoriteHu.prototype.mode = 0;

        FavoriteHu.create = function create(properties) {
            return new FavoriteHu(properties);
        };

        FavoriteHu.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(8).uint32(message.category);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.hu != null && Object.hasOwnProperty.call(message, "hu"))
                $root.lq.HighestHuRecord.encode(message.hu, writer.uint32(26).fork()).ldelim();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(32).uint32(message.mode);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FavoriteHu.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FavoriteHu.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FavoriteHu(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.hu = $root.lq.HighestHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.hu);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FavoriteHu.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FavoriteHu.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.hu != null && message.hasOwnProperty("hu")) {
                let error = $root.lq.HighestHuRecord.verify(message.hu, _depth + 1);
                if (error)
                    return "hu." + error;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isInteger(message.mode))
                    return "mode: integer expected";
            return null;
        };

        FavoriteHu.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FavoriteHu)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FavoriteHu();
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.hu != null) {
                if (typeof object.hu !== "object")
                    throw TypeError(".lq.FavoriteHu.hu: object expected");
                message.hu = $root.lq.HighestHuRecord.fromObject(object.hu, _depth + 1);
            }
            if (object.mode != null)
                if (Number(object.mode) !== 0)
                    message.mode = object.mode >>> 0;
            return message;
        };

        FavoriteHu.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.category = 0;
                object.type = 0;
                object.hu = null;
                object.mode = 0;
            }
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.hu != null && message.hasOwnProperty("hu"))
                object.hu = $root.lq.HighestHuRecord.toObject(message.hu, options);
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            return object;
        };

        FavoriteHu.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FavoriteHu.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FavoriteHu";
        };

        return FavoriteHu;
    })();

    lq.Account = (function() {

        function Account(properties) {
            this.platform_diamond = [];
            this.platform_skin_ticket = [];
            this.challenge_levels = [];
            this.achievement_count = [];
            this.loading_image = [];
            this.favorite_hu = [];
            this.badges = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Account.prototype.account_id = 0;
        Account.prototype.nickname = "";
        Account.prototype.login_time = 0;
        Account.prototype.logout_time = 0;
        Account.prototype.room_id = 0;
        Account.prototype.anti_addiction = null;
        Account.prototype.title = 0;
        Account.prototype.signature = "";
        Account.prototype.email = "";
        Account.prototype.email_verify = 0;
        Account.prototype.gold = 0;
        Account.prototype.diamond = 0;
        Account.prototype.avatar_id = 0;
        Account.prototype.vip = 0;
        Account.prototype.birthday = 0;
        Account.prototype.phone = "";
        Account.prototype.phone_verify = 0;
        Account.prototype.platform_diamond = $util.emptyArray;
        Account.prototype.level = null;
        Account.prototype.level3 = null;
        Account.prototype.avatar_frame = 0;
        Account.prototype.skin_ticket = 0;
        Account.prototype.platform_skin_ticket = $util.emptyArray;
        Account.prototype.verified = 0;
        Account.prototype.challenge_levels = $util.emptyArray;
        Account.prototype.frozen_state = 0;
        Account.prototype.achievement_count = $util.emptyArray;
        Account.prototype.loading_image = $util.emptyArray;
        Account.prototype.favorite_hu = $util.emptyArray;
        Account.prototype.badges = $util.emptyArray;

        Account.create = function create(properties) {
            return new Account(properties);
        };

        Account.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(18).string(message.nickname);
            if (message.login_time != null && Object.hasOwnProperty.call(message, "login_time"))
                writer.uint32(24).uint32(message.login_time);
            if (message.logout_time != null && Object.hasOwnProperty.call(message, "logout_time"))
                writer.uint32(32).uint32(message.logout_time);
            if (message.room_id != null && Object.hasOwnProperty.call(message, "room_id"))
                writer.uint32(40).uint32(message.room_id);
            if (message.anti_addiction != null && Object.hasOwnProperty.call(message, "anti_addiction"))
                $root.lq.AntiAddiction.encode(message.anti_addiction, writer.uint32(50).fork()).ldelim();
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(56).uint32(message.title);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(66).string(message.signature);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(74).string(message.email);
            if (message.email_verify != null && Object.hasOwnProperty.call(message, "email_verify"))
                writer.uint32(80).uint32(message.email_verify);
            if (message.gold != null && Object.hasOwnProperty.call(message, "gold"))
                writer.uint32(88).uint32(message.gold);
            if (message.diamond != null && Object.hasOwnProperty.call(message, "diamond"))
                writer.uint32(96).uint32(message.diamond);
            if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                writer.uint32(104).uint32(message.avatar_id);
            if (message.vip != null && Object.hasOwnProperty.call(message, "vip"))
                writer.uint32(112).uint32(message.vip);
            if (message.birthday != null && Object.hasOwnProperty.call(message, "birthday"))
                writer.uint32(120).int32(message.birthday);
            if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                writer.uint32(130).string(message.phone);
            if (message.phone_verify != null && Object.hasOwnProperty.call(message, "phone_verify"))
                writer.uint32(136).uint32(message.phone_verify);
            if (message.platform_diamond != null && message.platform_diamond.length)
                for (let i = 0; i < message.platform_diamond.length; ++i)
                    $root.lq.Account.PlatformDiamond.encode(message.platform_diamond[i], writer.uint32(146).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.AccountLevel.encode(message.level, writer.uint32(170).fork()).ldelim();
            if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                $root.lq.AccountLevel.encode(message.level3, writer.uint32(178).fork()).ldelim();
            if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                writer.uint32(184).uint32(message.avatar_frame);
            if (message.skin_ticket != null && Object.hasOwnProperty.call(message, "skin_ticket"))
                writer.uint32(192).uint32(message.skin_ticket);
            if (message.platform_skin_ticket != null && message.platform_skin_ticket.length)
                for (let i = 0; i < message.platform_skin_ticket.length; ++i)
                    $root.lq.Account.PlatformSkinTicket.encode(message.platform_skin_ticket[i], writer.uint32(202).fork()).ldelim();
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(208).uint32(message.verified);
            if (message.challenge_levels != null && message.challenge_levels.length)
                for (let i = 0; i < message.challenge_levels.length; ++i)
                    $root.lq.Account.ChallengeLevel.encode(message.challenge_levels[i], writer.uint32(218).fork()).ldelim();
            if (message.achievement_count != null && message.achievement_count.length)
                for (let i = 0; i < message.achievement_count.length; ++i)
                    $root.lq.Account.AchievementCount.encode(message.achievement_count[i], writer.uint32(226).fork()).ldelim();
            if (message.frozen_state != null && Object.hasOwnProperty.call(message, "frozen_state"))
                writer.uint32(232).uint32(message.frozen_state);
            if (message.loading_image != null && message.loading_image.length) {
                writer.uint32(242).fork();
                for (let i = 0; i < message.loading_image.length; ++i)
                    writer.uint32(message.loading_image[i]);
                writer.ldelim();
            }
            if (message.favorite_hu != null && message.favorite_hu.length)
                for (let i = 0; i < message.favorite_hu.length; ++i)
                    $root.lq.FavoriteHu.encode(message.favorite_hu[i], writer.uint32(274).fork()).ldelim();
            if (message.badges != null && message.badges.length)
                for (let i = 0; i < message.badges.length; ++i)
                    $root.lq.Account.Badge.encode(message.badges[i], writer.uint32(282).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Account.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Account.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.login_time = value;
                        else
                            delete message.login_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.logout_time = value;
                        else
                            delete message.logout_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.room_id = value;
                        else
                            delete message.room_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.anti_addiction = $root.lq.AntiAddiction.decode(reader, reader.uint32(), undefined, _depth + 1, message.anti_addiction);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.email = value;
                        else
                            delete message.email;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.email_verify = value;
                        else
                            delete message.email_verify;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gold = value;
                        else
                            delete message.gold;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.diamond = value;
                        else
                            delete message.diamond;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_id = value;
                        else
                            delete message.avatar_id;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vip = value;
                        else
                            delete message.vip;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.birthday = value;
                        else
                            delete message.birthday;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.phone = value;
                        else
                            delete message.phone;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.phone_verify = value;
                        else
                            delete message.phone_verify;
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if (!(message.platform_diamond && message.platform_diamond.length))
                            message.platform_diamond = [];
                        message.platform_diamond.push($root.lq.Account.PlatformDiamond.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_frame = value;
                        else
                            delete message.avatar_frame;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.skin_ticket = value;
                        else
                            delete message.skin_ticket;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if (!(message.platform_skin_ticket && message.platform_skin_ticket.length))
                            message.platform_skin_ticket = [];
                        message.platform_skin_ticket.push($root.lq.Account.PlatformSkinTicket.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if (!(message.challenge_levels && message.challenge_levels.length))
                            message.challenge_levels = [];
                        message.challenge_levels.push($root.lq.Account.ChallengeLevel.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.frozen_state = value;
                        else
                            delete message.frozen_state;
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        if (!(message.achievement_count && message.achievement_count.length))
                            message.achievement_count = [];
                        message.achievement_count.push($root.lq.Account.AchievementCount.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 30: {
                        if (wireType === 2) {
                            if (!(message.loading_image && message.loading_image.length))
                                message.loading_image = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.loading_image.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.loading_image && message.loading_image.length))
                            message.loading_image = [];
                        message.loading_image.push(reader.uint32());
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        if (!(message.favorite_hu && message.favorite_hu.length))
                            message.favorite_hu = [];
                        message.favorite_hu.push($root.lq.FavoriteHu.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 35: {
                        if (wireType !== 2)
                            break;
                        if (!(message.badges && message.badges.length))
                            message.badges = [];
                        message.badges.push($root.lq.Account.Badge.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Account.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Account.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                if (!$util.isInteger(message.login_time))
                    return "login_time: integer expected";
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                if (!$util.isInteger(message.logout_time))
                    return "logout_time: integer expected";
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                if (!$util.isInteger(message.room_id))
                    return "room_id: integer expected";
            if (message.anti_addiction != null && message.hasOwnProperty("anti_addiction")) {
                let error = $root.lq.AntiAddiction.verify(message.anti_addiction, _depth + 1);
                if (error)
                    return "anti_addiction." + error;
            }
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.email_verify != null && message.hasOwnProperty("email_verify"))
                if (!$util.isInteger(message.email_verify))
                    return "email_verify: integer expected";
            if (message.gold != null && message.hasOwnProperty("gold"))
                if (!$util.isInteger(message.gold))
                    return "gold: integer expected";
            if (message.diamond != null && message.hasOwnProperty("diamond"))
                if (!$util.isInteger(message.diamond))
                    return "diamond: integer expected";
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                if (!$util.isInteger(message.avatar_id))
                    return "avatar_id: integer expected";
            if (message.vip != null && message.hasOwnProperty("vip"))
                if (!$util.isInteger(message.vip))
                    return "vip: integer expected";
            if (message.birthday != null && message.hasOwnProperty("birthday"))
                if (!$util.isInteger(message.birthday))
                    return "birthday: integer expected";
            if (message.phone != null && message.hasOwnProperty("phone"))
                if (!$util.isString(message.phone))
                    return "phone: string expected";
            if (message.phone_verify != null && message.hasOwnProperty("phone_verify"))
                if (!$util.isInteger(message.phone_verify))
                    return "phone_verify: integer expected";
            if (message.platform_diamond != null && message.hasOwnProperty("platform_diamond")) {
                if (!Array.isArray(message.platform_diamond))
                    return "platform_diamond: array expected";
                for (let i = 0; i < message.platform_diamond.length; ++i) {
                    let error = $root.lq.Account.PlatformDiamond.verify(message.platform_diamond[i], _depth + 1);
                    if (error)
                        return "platform_diamond." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.level3 != null && message.hasOwnProperty("level3")) {
                let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                if (error)
                    return "level3." + error;
            }
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                if (!$util.isInteger(message.avatar_frame))
                    return "avatar_frame: integer expected";
            if (message.skin_ticket != null && message.hasOwnProperty("skin_ticket"))
                if (!$util.isInteger(message.skin_ticket))
                    return "skin_ticket: integer expected";
            if (message.platform_skin_ticket != null && message.hasOwnProperty("platform_skin_ticket")) {
                if (!Array.isArray(message.platform_skin_ticket))
                    return "platform_skin_ticket: array expected";
                for (let i = 0; i < message.platform_skin_ticket.length; ++i) {
                    let error = $root.lq.Account.PlatformSkinTicket.verify(message.platform_skin_ticket[i], _depth + 1);
                    if (error)
                        return "platform_skin_ticket." + error;
                }
            }
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            if (message.challenge_levels != null && message.hasOwnProperty("challenge_levels")) {
                if (!Array.isArray(message.challenge_levels))
                    return "challenge_levels: array expected";
                for (let i = 0; i < message.challenge_levels.length; ++i) {
                    let error = $root.lq.Account.ChallengeLevel.verify(message.challenge_levels[i], _depth + 1);
                    if (error)
                        return "challenge_levels." + error;
                }
            }
            if (message.frozen_state != null && message.hasOwnProperty("frozen_state"))
                if (!$util.isInteger(message.frozen_state))
                    return "frozen_state: integer expected";
            if (message.achievement_count != null && message.hasOwnProperty("achievement_count")) {
                if (!Array.isArray(message.achievement_count))
                    return "achievement_count: array expected";
                for (let i = 0; i < message.achievement_count.length; ++i) {
                    let error = $root.lq.Account.AchievementCount.verify(message.achievement_count[i], _depth + 1);
                    if (error)
                        return "achievement_count." + error;
                }
            }
            if (message.loading_image != null && message.hasOwnProperty("loading_image")) {
                if (!Array.isArray(message.loading_image))
                    return "loading_image: array expected";
                for (let i = 0; i < message.loading_image.length; ++i)
                    if (!$util.isInteger(message.loading_image[i]))
                        return "loading_image: integer[] expected";
            }
            if (message.favorite_hu != null && message.hasOwnProperty("favorite_hu")) {
                if (!Array.isArray(message.favorite_hu))
                    return "favorite_hu: array expected";
                for (let i = 0; i < message.favorite_hu.length; ++i) {
                    let error = $root.lq.FavoriteHu.verify(message.favorite_hu[i], _depth + 1);
                    if (error)
                        return "favorite_hu." + error;
                }
            }
            if (message.badges != null && message.hasOwnProperty("badges")) {
                if (!Array.isArray(message.badges))
                    return "badges: array expected";
                for (let i = 0; i < message.badges.length; ++i) {
                    let error = $root.lq.Account.Badge.verify(message.badges[i], _depth + 1);
                    if (error)
                        return "badges." + error;
                }
            }
            return null;
        };

        Account.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Account)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Account();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.login_time != null)
                if (Number(object.login_time) !== 0)
                    message.login_time = object.login_time >>> 0;
            if (object.logout_time != null)
                if (Number(object.logout_time) !== 0)
                    message.logout_time = object.logout_time >>> 0;
            if (object.room_id != null)
                if (Number(object.room_id) !== 0)
                    message.room_id = object.room_id >>> 0;
            if (object.anti_addiction != null) {
                if (typeof object.anti_addiction !== "object")
                    throw TypeError(".lq.Account.anti_addiction: object expected");
                message.anti_addiction = $root.lq.AntiAddiction.fromObject(object.anti_addiction, _depth + 1);
            }
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = String(object.signature);
            if (object.email != null)
                if (typeof object.email !== "string" || object.email.length)
                    message.email = String(object.email);
            if (object.email_verify != null)
                if (Number(object.email_verify) !== 0)
                    message.email_verify = object.email_verify >>> 0;
            if (object.gold != null)
                if (Number(object.gold) !== 0)
                    message.gold = object.gold >>> 0;
            if (object.diamond != null)
                if (Number(object.diamond) !== 0)
                    message.diamond = object.diamond >>> 0;
            if (object.avatar_id != null)
                if (Number(object.avatar_id) !== 0)
                    message.avatar_id = object.avatar_id >>> 0;
            if (object.vip != null)
                if (Number(object.vip) !== 0)
                    message.vip = object.vip >>> 0;
            if (object.birthday != null)
                if (Number(object.birthday) !== 0)
                    message.birthday = object.birthday | 0;
            if (object.phone != null)
                if (typeof object.phone !== "string" || object.phone.length)
                    message.phone = String(object.phone);
            if (object.phone_verify != null)
                if (Number(object.phone_verify) !== 0)
                    message.phone_verify = object.phone_verify >>> 0;
            if (object.platform_diamond) {
                if (!Array.isArray(object.platform_diamond))
                    throw TypeError(".lq.Account.platform_diamond: array expected");
                message.platform_diamond = Array(object.platform_diamond.length);
                for (let i = 0; i < object.platform_diamond.length; ++i) {
                    if (typeof object.platform_diamond[i] !== "object")
                        throw TypeError(".lq.Account.platform_diamond: object expected");
                    message.platform_diamond[i] = $root.lq.Account.PlatformDiamond.fromObject(object.platform_diamond[i], _depth + 1);
                }
            }
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.Account.level: object expected");
                message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
            }
            if (object.level3 != null) {
                if (typeof object.level3 !== "object")
                    throw TypeError(".lq.Account.level3: object expected");
                message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
            }
            if (object.avatar_frame != null)
                if (Number(object.avatar_frame) !== 0)
                    message.avatar_frame = object.avatar_frame >>> 0;
            if (object.skin_ticket != null)
                if (Number(object.skin_ticket) !== 0)
                    message.skin_ticket = object.skin_ticket >>> 0;
            if (object.platform_skin_ticket) {
                if (!Array.isArray(object.platform_skin_ticket))
                    throw TypeError(".lq.Account.platform_skin_ticket: array expected");
                message.platform_skin_ticket = Array(object.platform_skin_ticket.length);
                for (let i = 0; i < object.platform_skin_ticket.length; ++i) {
                    if (typeof object.platform_skin_ticket[i] !== "object")
                        throw TypeError(".lq.Account.platform_skin_ticket: object expected");
                    message.platform_skin_ticket[i] = $root.lq.Account.PlatformSkinTicket.fromObject(object.platform_skin_ticket[i], _depth + 1);
                }
            }
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            if (object.challenge_levels) {
                if (!Array.isArray(object.challenge_levels))
                    throw TypeError(".lq.Account.challenge_levels: array expected");
                message.challenge_levels = Array(object.challenge_levels.length);
                for (let i = 0; i < object.challenge_levels.length; ++i) {
                    if (typeof object.challenge_levels[i] !== "object")
                        throw TypeError(".lq.Account.challenge_levels: object expected");
                    message.challenge_levels[i] = $root.lq.Account.ChallengeLevel.fromObject(object.challenge_levels[i], _depth + 1);
                }
            }
            if (object.frozen_state != null)
                if (Number(object.frozen_state) !== 0)
                    message.frozen_state = object.frozen_state >>> 0;
            if (object.achievement_count) {
                if (!Array.isArray(object.achievement_count))
                    throw TypeError(".lq.Account.achievement_count: array expected");
                message.achievement_count = Array(object.achievement_count.length);
                for (let i = 0; i < object.achievement_count.length; ++i) {
                    if (typeof object.achievement_count[i] !== "object")
                        throw TypeError(".lq.Account.achievement_count: object expected");
                    message.achievement_count[i] = $root.lq.Account.AchievementCount.fromObject(object.achievement_count[i], _depth + 1);
                }
            }
            if (object.loading_image) {
                if (!Array.isArray(object.loading_image))
                    throw TypeError(".lq.Account.loading_image: array expected");
                message.loading_image = Array(object.loading_image.length);
                for (let i = 0; i < object.loading_image.length; ++i)
                    message.loading_image[i] = object.loading_image[i] >>> 0;
            }
            if (object.favorite_hu) {
                if (!Array.isArray(object.favorite_hu))
                    throw TypeError(".lq.Account.favorite_hu: array expected");
                message.favorite_hu = Array(object.favorite_hu.length);
                for (let i = 0; i < object.favorite_hu.length; ++i) {
                    if (typeof object.favorite_hu[i] !== "object")
                        throw TypeError(".lq.Account.favorite_hu: object expected");
                    message.favorite_hu[i] = $root.lq.FavoriteHu.fromObject(object.favorite_hu[i], _depth + 1);
                }
            }
            if (object.badges) {
                if (!Array.isArray(object.badges))
                    throw TypeError(".lq.Account.badges: array expected");
                message.badges = Array(object.badges.length);
                for (let i = 0; i < object.badges.length; ++i) {
                    if (typeof object.badges[i] !== "object")
                        throw TypeError(".lq.Account.badges: object expected");
                    message.badges[i] = $root.lq.Account.Badge.fromObject(object.badges[i], _depth + 1);
                }
            }
            return message;
        };

        Account.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.platform_diamond = [];
                object.platform_skin_ticket = [];
                object.challenge_levels = [];
                object.achievement_count = [];
                object.loading_image = [];
                object.favorite_hu = [];
                object.badges = [];
            }
            if (options.defaults) {
                object.account_id = 0;
                object.nickname = "";
                object.login_time = 0;
                object.logout_time = 0;
                object.room_id = 0;
                object.anti_addiction = null;
                object.title = 0;
                object.signature = "";
                object.email = "";
                object.email_verify = 0;
                object.gold = 0;
                object.diamond = 0;
                object.avatar_id = 0;
                object.vip = 0;
                object.birthday = 0;
                object.phone = "";
                object.phone_verify = 0;
                object.level = null;
                object.level3 = null;
                object.avatar_frame = 0;
                object.skin_ticket = 0;
                object.verified = 0;
                object.frozen_state = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                object.login_time = message.login_time;
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                object.logout_time = message.logout_time;
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                object.room_id = message.room_id;
            if (message.anti_addiction != null && message.hasOwnProperty("anti_addiction"))
                object.anti_addiction = $root.lq.AntiAddiction.toObject(message.anti_addiction, options);
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = message.signature;
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.email_verify != null && message.hasOwnProperty("email_verify"))
                object.email_verify = message.email_verify;
            if (message.gold != null && message.hasOwnProperty("gold"))
                object.gold = message.gold;
            if (message.diamond != null && message.hasOwnProperty("diamond"))
                object.diamond = message.diamond;
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                object.avatar_id = message.avatar_id;
            if (message.vip != null && message.hasOwnProperty("vip"))
                object.vip = message.vip;
            if (message.birthday != null && message.hasOwnProperty("birthday"))
                object.birthday = message.birthday;
            if (message.phone != null && message.hasOwnProperty("phone"))
                object.phone = message.phone;
            if (message.phone_verify != null && message.hasOwnProperty("phone_verify"))
                object.phone_verify = message.phone_verify;
            if (message.platform_diamond && message.platform_diamond.length) {
                object.platform_diamond = Array(message.platform_diamond.length);
                for (let j = 0; j < message.platform_diamond.length; ++j)
                    object.platform_diamond[j] = $root.lq.Account.PlatformDiamond.toObject(message.platform_diamond[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.AccountLevel.toObject(message.level, options);
            if (message.level3 != null && message.hasOwnProperty("level3"))
                object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                object.avatar_frame = message.avatar_frame;
            if (message.skin_ticket != null && message.hasOwnProperty("skin_ticket"))
                object.skin_ticket = message.skin_ticket;
            if (message.platform_skin_ticket && message.platform_skin_ticket.length) {
                object.platform_skin_ticket = Array(message.platform_skin_ticket.length);
                for (let j = 0; j < message.platform_skin_ticket.length; ++j)
                    object.platform_skin_ticket[j] = $root.lq.Account.PlatformSkinTicket.toObject(message.platform_skin_ticket[j], options);
            }
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            if (message.challenge_levels && message.challenge_levels.length) {
                object.challenge_levels = Array(message.challenge_levels.length);
                for (let j = 0; j < message.challenge_levels.length; ++j)
                    object.challenge_levels[j] = $root.lq.Account.ChallengeLevel.toObject(message.challenge_levels[j], options);
            }
            if (message.achievement_count && message.achievement_count.length) {
                object.achievement_count = Array(message.achievement_count.length);
                for (let j = 0; j < message.achievement_count.length; ++j)
                    object.achievement_count[j] = $root.lq.Account.AchievementCount.toObject(message.achievement_count[j], options);
            }
            if (message.frozen_state != null && message.hasOwnProperty("frozen_state"))
                object.frozen_state = message.frozen_state;
            if (message.loading_image && message.loading_image.length) {
                object.loading_image = Array(message.loading_image.length);
                for (let j = 0; j < message.loading_image.length; ++j)
                    object.loading_image[j] = message.loading_image[j];
            }
            if (message.favorite_hu && message.favorite_hu.length) {
                object.favorite_hu = Array(message.favorite_hu.length);
                for (let j = 0; j < message.favorite_hu.length; ++j)
                    object.favorite_hu[j] = $root.lq.FavoriteHu.toObject(message.favorite_hu[j], options);
            }
            if (message.badges && message.badges.length) {
                object.badges = Array(message.badges.length);
                for (let j = 0; j < message.badges.length; ++j)
                    object.badges[j] = $root.lq.Account.Badge.toObject(message.badges[j], options);
            }
            return object;
        };

        Account.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Account.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Account";
        };

        Account.PlatformDiamond = (function() {

            function PlatformDiamond(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PlatformDiamond.prototype.id = 0;
            PlatformDiamond.prototype.count = 0;

            PlatformDiamond.create = function create(properties) {
                return new PlatformDiamond(properties);
            };

            PlatformDiamond.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PlatformDiamond.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PlatformDiamond.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account.PlatformDiamond(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PlatformDiamond.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PlatformDiamond.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            PlatformDiamond.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.Account.PlatformDiamond)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.Account.PlatformDiamond();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            PlatformDiamond.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            PlatformDiamond.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlatformDiamond.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.Account.PlatformDiamond";
            };

            return PlatformDiamond;
        })();

        Account.PlatformSkinTicket = (function() {

            function PlatformSkinTicket(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PlatformSkinTicket.prototype.id = 0;
            PlatformSkinTicket.prototype.count = 0;

            PlatformSkinTicket.create = function create(properties) {
                return new PlatformSkinTicket(properties);
            };

            PlatformSkinTicket.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PlatformSkinTicket.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PlatformSkinTicket.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account.PlatformSkinTicket(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PlatformSkinTicket.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PlatformSkinTicket.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            PlatformSkinTicket.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.Account.PlatformSkinTicket)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.Account.PlatformSkinTicket();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            PlatformSkinTicket.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            PlatformSkinTicket.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlatformSkinTicket.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.Account.PlatformSkinTicket";
            };

            return PlatformSkinTicket;
        })();

        Account.ChallengeLevel = (function() {

            function ChallengeLevel(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChallengeLevel.prototype.season = 0;
            ChallengeLevel.prototype.level = 0;
            ChallengeLevel.prototype.rank = 0;

            ChallengeLevel.create = function create(properties) {
                return new ChallengeLevel(properties);
            };

            ChallengeLevel.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                    writer.uint32(8).uint32(message.season);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(24).uint32(message.rank);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChallengeLevel.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChallengeLevel.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account.ChallengeLevel(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.season = value;
                            else
                                delete message.season;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChallengeLevel.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChallengeLevel.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.season != null && message.hasOwnProperty("season"))
                    if (!$util.isInteger(message.season))
                        return "season: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                return null;
            };

            ChallengeLevel.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.Account.ChallengeLevel)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.Account.ChallengeLevel();
                if (object.season != null)
                    if (Number(object.season) !== 0)
                        message.season = object.season >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                return message;
            };

            ChallengeLevel.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.season = 0;
                    object.level = 0;
                    object.rank = 0;
                }
                if (message.season != null && message.hasOwnProperty("season"))
                    object.season = message.season;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                return object;
            };

            ChallengeLevel.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChallengeLevel.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.Account.ChallengeLevel";
            };

            return ChallengeLevel;
        })();

        Account.AchievementCount = (function() {

            function AchievementCount(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AchievementCount.prototype.rare = 0;
            AchievementCount.prototype.count = 0;

            AchievementCount.create = function create(properties) {
                return new AchievementCount(properties);
            };

            AchievementCount.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rare != null && Object.hasOwnProperty.call(message, "rare"))
                    writer.uint32(8).uint32(message.rare);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AchievementCount.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AchievementCount.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account.AchievementCount(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rare = value;
                            else
                                delete message.rare;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AchievementCount.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AchievementCount.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rare != null && message.hasOwnProperty("rare"))
                    if (!$util.isInteger(message.rare))
                        return "rare: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            AchievementCount.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.Account.AchievementCount)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.Account.AchievementCount();
                if (object.rare != null)
                    if (Number(object.rare) !== 0)
                        message.rare = object.rare >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            AchievementCount.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.rare = 0;
                    object.count = 0;
                }
                if (message.rare != null && message.hasOwnProperty("rare"))
                    object.rare = message.rare;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            AchievementCount.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AchievementCount.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.Account.AchievementCount";
            };

            return AchievementCount;
        })();

        Account.Badge = (function() {

            function Badge(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Badge.prototype.id = 0;
            Badge.prototype.achieved_time = 0;
            Badge.prototype.achieved_counter = 0;

            Badge.create = function create(properties) {
                return new Badge(properties);
            };

            Badge.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.achieved_time != null && Object.hasOwnProperty.call(message, "achieved_time"))
                    writer.uint32(16).uint32(message.achieved_time);
                if (message.achieved_counter != null && Object.hasOwnProperty.call(message, "achieved_counter"))
                    writer.uint32(24).uint32(message.achieved_counter);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Badge.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Badge.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Account.Badge(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.achieved_time = value;
                            else
                                delete message.achieved_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.achieved_counter = value;
                            else
                                delete message.achieved_counter;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Badge.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Badge.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                    if (!$util.isInteger(message.achieved_time))
                        return "achieved_time: integer expected";
                if (message.achieved_counter != null && message.hasOwnProperty("achieved_counter"))
                    if (!$util.isInteger(message.achieved_counter))
                        return "achieved_counter: integer expected";
                return null;
            };

            Badge.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.Account.Badge)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.Account.Badge();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.achieved_time != null)
                    if (Number(object.achieved_time) !== 0)
                        message.achieved_time = object.achieved_time >>> 0;
                if (object.achieved_counter != null)
                    if (Number(object.achieved_counter) !== 0)
                        message.achieved_counter = object.achieved_counter >>> 0;
                return message;
            };

            Badge.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.achieved_time = 0;
                    object.achieved_counter = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                    object.achieved_time = message.achieved_time;
                if (message.achieved_counter != null && message.hasOwnProperty("achieved_counter"))
                    object.achieved_counter = message.achieved_counter;
                return object;
            };

            Badge.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Badge.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.Account.Badge";
            };

            return Badge;
        })();

        return Account;
    })();

    lq.AccountOwnerData = (function() {

        function AccountOwnerData(properties) {
            this.unlock_characters = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountOwnerData.prototype.unlock_characters = $util.emptyArray;

        AccountOwnerData.create = function create(properties) {
            return new AccountOwnerData(properties);
        };

        AccountOwnerData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unlock_characters != null && message.unlock_characters.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.unlock_characters.length; ++i)
                    writer.uint32(message.unlock_characters[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountOwnerData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountOwnerData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountOwnerData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.unlock_characters && message.unlock_characters.length))
                                message.unlock_characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.unlock_characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.unlock_characters && message.unlock_characters.length))
                            message.unlock_characters = [];
                        message.unlock_characters.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountOwnerData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountOwnerData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unlock_characters != null && message.hasOwnProperty("unlock_characters")) {
                if (!Array.isArray(message.unlock_characters))
                    return "unlock_characters: array expected";
                for (let i = 0; i < message.unlock_characters.length; ++i)
                    if (!$util.isInteger(message.unlock_characters[i]))
                        return "unlock_characters: integer[] expected";
            }
            return null;
        };

        AccountOwnerData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountOwnerData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountOwnerData();
            if (object.unlock_characters) {
                if (!Array.isArray(object.unlock_characters))
                    throw TypeError(".lq.AccountOwnerData.unlock_characters: array expected");
                message.unlock_characters = Array(object.unlock_characters.length);
                for (let i = 0; i < object.unlock_characters.length; ++i)
                    message.unlock_characters[i] = object.unlock_characters[i] >>> 0;
            }
            return message;
        };

        AccountOwnerData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.unlock_characters = [];
            if (message.unlock_characters && message.unlock_characters.length) {
                object.unlock_characters = Array(message.unlock_characters.length);
                for (let j = 0; j < message.unlock_characters.length; ++j)
                    object.unlock_characters[j] = message.unlock_characters[j];
            }
            return object;
        };

        AccountOwnerData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountOwnerData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountOwnerData";
        };

        return AccountOwnerData;
    })();

    lq.AccountUpdate = (function() {

        function AccountUpdate(properties) {
            this.numerical = [];
            this.new_recharged_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountUpdate.prototype.numerical = $util.emptyArray;
        AccountUpdate.prototype.character = null;
        AccountUpdate.prototype.bag = null;
        AccountUpdate.prototype.achievement = null;
        AccountUpdate.prototype.shilian = null;
        AccountUpdate.prototype.daily_task = null;
        AccountUpdate.prototype.title = null;
        AccountUpdate.prototype.new_recharged_list = $util.emptyArray;
        AccountUpdate.prototype.activity_task = null;
        AccountUpdate.prototype.activity_flip_task = null;
        AccountUpdate.prototype.activity_period_task = null;
        AccountUpdate.prototype.activity_random_task = null;
        AccountUpdate.prototype.challenge = null;
        AccountUpdate.prototype.ab_match = null;
        AccountUpdate.prototype.activity = null;
        AccountUpdate.prototype.activity_segment_task = null;
        AccountUpdate.prototype.month_ticket = null;
        AccountUpdate.prototype.main_character = null;
        AccountUpdate.prototype.badge = null;

        AccountUpdate.create = function create(properties) {
            return new AccountUpdate(properties);
        };

        AccountUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.numerical != null && message.numerical.length)
                for (let i = 0; i < message.numerical.length; ++i)
                    $root.lq.AccountUpdate.NumericalUpdate.encode(message.numerical[i], writer.uint32(10).fork()).ldelim();
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.AccountUpdate.CharacterUpdate.encode(message.character, writer.uint32(18).fork()).ldelim();
            if (message.bag != null && Object.hasOwnProperty.call(message, "bag"))
                $root.lq.BagUpdate.encode(message.bag, writer.uint32(26).fork()).ldelim();
            if (message.achievement != null && Object.hasOwnProperty.call(message, "achievement"))
                $root.lq.AccountUpdate.AchievementUpdate.encode(message.achievement, writer.uint32(34).fork()).ldelim();
            if (message.shilian != null && Object.hasOwnProperty.call(message, "shilian"))
                $root.lq.AccountShiLian.encode(message.shilian, writer.uint32(42).fork()).ldelim();
            if (message.daily_task != null && Object.hasOwnProperty.call(message, "daily_task"))
                $root.lq.AccountUpdate.DailyTaskUpdate.encode(message.daily_task, writer.uint32(50).fork()).ldelim();
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                $root.lq.AccountUpdate.TitleUpdate.encode(message.title, writer.uint32(58).fork()).ldelim();
            if (message.new_recharged_list != null && message.new_recharged_list.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.new_recharged_list.length; ++i)
                    writer.uint32(message.new_recharged_list[i]);
                writer.ldelim();
            }
            if (message.activity_task != null && Object.hasOwnProperty.call(message, "activity_task"))
                $root.lq.AccountUpdate.TaskUpdate.encode(message.activity_task, writer.uint32(74).fork()).ldelim();
            if (message.activity_flip_task != null && Object.hasOwnProperty.call(message, "activity_flip_task"))
                $root.lq.AccountUpdate.TaskUpdate.encode(message.activity_flip_task, writer.uint32(82).fork()).ldelim();
            if (message.activity_period_task != null && Object.hasOwnProperty.call(message, "activity_period_task"))
                $root.lq.AccountUpdate.TaskUpdate.encode(message.activity_period_task, writer.uint32(90).fork()).ldelim();
            if (message.activity_random_task != null && Object.hasOwnProperty.call(message, "activity_random_task"))
                $root.lq.AccountUpdate.TaskUpdate.encode(message.activity_random_task, writer.uint32(98).fork()).ldelim();
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                $root.lq.AccountUpdate.AccountChallengeUpdate.encode(message.challenge, writer.uint32(106).fork()).ldelim();
            if (message.ab_match != null && Object.hasOwnProperty.call(message, "ab_match"))
                $root.lq.AccountUpdate.AccountABMatchUpdate.encode(message.ab_match, writer.uint32(114).fork()).ldelim();
            if (message.activity != null && Object.hasOwnProperty.call(message, "activity"))
                $root.lq.AccountActivityUpdate.encode(message.activity, writer.uint32(122).fork()).ldelim();
            if (message.activity_segment_task != null && Object.hasOwnProperty.call(message, "activity_segment_task"))
                $root.lq.AccountUpdate.SegmentTaskUpdate.encode(message.activity_segment_task, writer.uint32(130).fork()).ldelim();
            if (message.month_ticket != null && Object.hasOwnProperty.call(message, "month_ticket"))
                $root.lq.AccountUpdate.MonthTicketUpdate.encode(message.month_ticket, writer.uint32(138).fork()).ldelim();
            if (message.main_character != null && Object.hasOwnProperty.call(message, "main_character"))
                $root.lq.AccountUpdate.MainCharacterUpdate.encode(message.main_character, writer.uint32(146).fork()).ldelim();
            if (message.badge != null && Object.hasOwnProperty.call(message, "badge"))
                $root.lq.AccountUpdate.BadgeUpdate.encode(message.badge, writer.uint32(154).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.numerical && message.numerical.length))
                            message.numerical = [];
                        message.numerical.push($root.lq.AccountUpdate.NumericalUpdate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.AccountUpdate.CharacterUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.bag = $root.lq.BagUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.bag);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.achievement = $root.lq.AccountUpdate.AchievementUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.achievement);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.shilian = $root.lq.AccountShiLian.decode(reader, reader.uint32(), undefined, _depth + 1, message.shilian);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.daily_task = $root.lq.AccountUpdate.DailyTaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.daily_task);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.title = $root.lq.AccountUpdate.TitleUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.title);
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.new_recharged_list && message.new_recharged_list.length))
                                message.new_recharged_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.new_recharged_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.new_recharged_list && message.new_recharged_list.length))
                            message.new_recharged_list = [];
                        message.new_recharged_list.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.activity_task = $root.lq.AccountUpdate.TaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_task);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.activity_flip_task = $root.lq.AccountUpdate.TaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_flip_task);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.activity_period_task = $root.lq.AccountUpdate.TaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_period_task);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.activity_random_task = $root.lq.AccountUpdate.TaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_random_task);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.challenge = $root.lq.AccountUpdate.AccountChallengeUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.challenge);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.ab_match = $root.lq.AccountUpdate.AccountABMatchUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.ab_match);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.activity = $root.lq.AccountActivityUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.activity_segment_task = $root.lq.AccountUpdate.SegmentTaskUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_segment_task);
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.month_ticket = $root.lq.AccountUpdate.MonthTicketUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_ticket);
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.main_character = $root.lq.AccountUpdate.MainCharacterUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.main_character);
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.badge = $root.lq.AccountUpdate.BadgeUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.badge);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.numerical != null && message.hasOwnProperty("numerical")) {
                if (!Array.isArray(message.numerical))
                    return "numerical: array expected";
                for (let i = 0; i < message.numerical.length; ++i) {
                    let error = $root.lq.AccountUpdate.NumericalUpdate.verify(message.numerical[i], _depth + 1);
                    if (error)
                        return "numerical." + error;
                }
            }
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.AccountUpdate.CharacterUpdate.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            if (message.bag != null && message.hasOwnProperty("bag")) {
                let error = $root.lq.BagUpdate.verify(message.bag, _depth + 1);
                if (error)
                    return "bag." + error;
            }
            if (message.achievement != null && message.hasOwnProperty("achievement")) {
                let error = $root.lq.AccountUpdate.AchievementUpdate.verify(message.achievement, _depth + 1);
                if (error)
                    return "achievement." + error;
            }
            if (message.shilian != null && message.hasOwnProperty("shilian")) {
                let error = $root.lq.AccountShiLian.verify(message.shilian, _depth + 1);
                if (error)
                    return "shilian." + error;
            }
            if (message.daily_task != null && message.hasOwnProperty("daily_task")) {
                let error = $root.lq.AccountUpdate.DailyTaskUpdate.verify(message.daily_task, _depth + 1);
                if (error)
                    return "daily_task." + error;
            }
            if (message.title != null && message.hasOwnProperty("title")) {
                let error = $root.lq.AccountUpdate.TitleUpdate.verify(message.title, _depth + 1);
                if (error)
                    return "title." + error;
            }
            if (message.new_recharged_list != null && message.hasOwnProperty("new_recharged_list")) {
                if (!Array.isArray(message.new_recharged_list))
                    return "new_recharged_list: array expected";
                for (let i = 0; i < message.new_recharged_list.length; ++i)
                    if (!$util.isInteger(message.new_recharged_list[i]))
                        return "new_recharged_list: integer[] expected";
            }
            if (message.activity_task != null && message.hasOwnProperty("activity_task")) {
                let error = $root.lq.AccountUpdate.TaskUpdate.verify(message.activity_task, _depth + 1);
                if (error)
                    return "activity_task." + error;
            }
            if (message.activity_flip_task != null && message.hasOwnProperty("activity_flip_task")) {
                let error = $root.lq.AccountUpdate.TaskUpdate.verify(message.activity_flip_task, _depth + 1);
                if (error)
                    return "activity_flip_task." + error;
            }
            if (message.activity_period_task != null && message.hasOwnProperty("activity_period_task")) {
                let error = $root.lq.AccountUpdate.TaskUpdate.verify(message.activity_period_task, _depth + 1);
                if (error)
                    return "activity_period_task." + error;
            }
            if (message.activity_random_task != null && message.hasOwnProperty("activity_random_task")) {
                let error = $root.lq.AccountUpdate.TaskUpdate.verify(message.activity_random_task, _depth + 1);
                if (error)
                    return "activity_random_task." + error;
            }
            if (message.challenge != null && message.hasOwnProperty("challenge")) {
                let error = $root.lq.AccountUpdate.AccountChallengeUpdate.verify(message.challenge, _depth + 1);
                if (error)
                    return "challenge." + error;
            }
            if (message.ab_match != null && message.hasOwnProperty("ab_match")) {
                let error = $root.lq.AccountUpdate.AccountABMatchUpdate.verify(message.ab_match, _depth + 1);
                if (error)
                    return "ab_match." + error;
            }
            if (message.activity != null && message.hasOwnProperty("activity")) {
                let error = $root.lq.AccountActivityUpdate.verify(message.activity, _depth + 1);
                if (error)
                    return "activity." + error;
            }
            if (message.activity_segment_task != null && message.hasOwnProperty("activity_segment_task")) {
                let error = $root.lq.AccountUpdate.SegmentTaskUpdate.verify(message.activity_segment_task, _depth + 1);
                if (error)
                    return "activity_segment_task." + error;
            }
            if (message.month_ticket != null && message.hasOwnProperty("month_ticket")) {
                let error = $root.lq.AccountUpdate.MonthTicketUpdate.verify(message.month_ticket, _depth + 1);
                if (error)
                    return "month_ticket." + error;
            }
            if (message.main_character != null && message.hasOwnProperty("main_character")) {
                let error = $root.lq.AccountUpdate.MainCharacterUpdate.verify(message.main_character, _depth + 1);
                if (error)
                    return "main_character." + error;
            }
            if (message.badge != null && message.hasOwnProperty("badge")) {
                let error = $root.lq.AccountUpdate.BadgeUpdate.verify(message.badge, _depth + 1);
                if (error)
                    return "badge." + error;
            }
            return null;
        };

        AccountUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountUpdate();
            if (object.numerical) {
                if (!Array.isArray(object.numerical))
                    throw TypeError(".lq.AccountUpdate.numerical: array expected");
                message.numerical = Array(object.numerical.length);
                for (let i = 0; i < object.numerical.length; ++i) {
                    if (typeof object.numerical[i] !== "object")
                        throw TypeError(".lq.AccountUpdate.numerical: object expected");
                    message.numerical[i] = $root.lq.AccountUpdate.NumericalUpdate.fromObject(object.numerical[i], _depth + 1);
                }
            }
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.AccountUpdate.character: object expected");
                message.character = $root.lq.AccountUpdate.CharacterUpdate.fromObject(object.character, _depth + 1);
            }
            if (object.bag != null) {
                if (typeof object.bag !== "object")
                    throw TypeError(".lq.AccountUpdate.bag: object expected");
                message.bag = $root.lq.BagUpdate.fromObject(object.bag, _depth + 1);
            }
            if (object.achievement != null) {
                if (typeof object.achievement !== "object")
                    throw TypeError(".lq.AccountUpdate.achievement: object expected");
                message.achievement = $root.lq.AccountUpdate.AchievementUpdate.fromObject(object.achievement, _depth + 1);
            }
            if (object.shilian != null) {
                if (typeof object.shilian !== "object")
                    throw TypeError(".lq.AccountUpdate.shilian: object expected");
                message.shilian = $root.lq.AccountShiLian.fromObject(object.shilian, _depth + 1);
            }
            if (object.daily_task != null) {
                if (typeof object.daily_task !== "object")
                    throw TypeError(".lq.AccountUpdate.daily_task: object expected");
                message.daily_task = $root.lq.AccountUpdate.DailyTaskUpdate.fromObject(object.daily_task, _depth + 1);
            }
            if (object.title != null) {
                if (typeof object.title !== "object")
                    throw TypeError(".lq.AccountUpdate.title: object expected");
                message.title = $root.lq.AccountUpdate.TitleUpdate.fromObject(object.title, _depth + 1);
            }
            if (object.new_recharged_list) {
                if (!Array.isArray(object.new_recharged_list))
                    throw TypeError(".lq.AccountUpdate.new_recharged_list: array expected");
                message.new_recharged_list = Array(object.new_recharged_list.length);
                for (let i = 0; i < object.new_recharged_list.length; ++i)
                    message.new_recharged_list[i] = object.new_recharged_list[i] >>> 0;
            }
            if (object.activity_task != null) {
                if (typeof object.activity_task !== "object")
                    throw TypeError(".lq.AccountUpdate.activity_task: object expected");
                message.activity_task = $root.lq.AccountUpdate.TaskUpdate.fromObject(object.activity_task, _depth + 1);
            }
            if (object.activity_flip_task != null) {
                if (typeof object.activity_flip_task !== "object")
                    throw TypeError(".lq.AccountUpdate.activity_flip_task: object expected");
                message.activity_flip_task = $root.lq.AccountUpdate.TaskUpdate.fromObject(object.activity_flip_task, _depth + 1);
            }
            if (object.activity_period_task != null) {
                if (typeof object.activity_period_task !== "object")
                    throw TypeError(".lq.AccountUpdate.activity_period_task: object expected");
                message.activity_period_task = $root.lq.AccountUpdate.TaskUpdate.fromObject(object.activity_period_task, _depth + 1);
            }
            if (object.activity_random_task != null) {
                if (typeof object.activity_random_task !== "object")
                    throw TypeError(".lq.AccountUpdate.activity_random_task: object expected");
                message.activity_random_task = $root.lq.AccountUpdate.TaskUpdate.fromObject(object.activity_random_task, _depth + 1);
            }
            if (object.challenge != null) {
                if (typeof object.challenge !== "object")
                    throw TypeError(".lq.AccountUpdate.challenge: object expected");
                message.challenge = $root.lq.AccountUpdate.AccountChallengeUpdate.fromObject(object.challenge, _depth + 1);
            }
            if (object.ab_match != null) {
                if (typeof object.ab_match !== "object")
                    throw TypeError(".lq.AccountUpdate.ab_match: object expected");
                message.ab_match = $root.lq.AccountUpdate.AccountABMatchUpdate.fromObject(object.ab_match, _depth + 1);
            }
            if (object.activity != null) {
                if (typeof object.activity !== "object")
                    throw TypeError(".lq.AccountUpdate.activity: object expected");
                message.activity = $root.lq.AccountActivityUpdate.fromObject(object.activity, _depth + 1);
            }
            if (object.activity_segment_task != null) {
                if (typeof object.activity_segment_task !== "object")
                    throw TypeError(".lq.AccountUpdate.activity_segment_task: object expected");
                message.activity_segment_task = $root.lq.AccountUpdate.SegmentTaskUpdate.fromObject(object.activity_segment_task, _depth + 1);
            }
            if (object.month_ticket != null) {
                if (typeof object.month_ticket !== "object")
                    throw TypeError(".lq.AccountUpdate.month_ticket: object expected");
                message.month_ticket = $root.lq.AccountUpdate.MonthTicketUpdate.fromObject(object.month_ticket, _depth + 1);
            }
            if (object.main_character != null) {
                if (typeof object.main_character !== "object")
                    throw TypeError(".lq.AccountUpdate.main_character: object expected");
                message.main_character = $root.lq.AccountUpdate.MainCharacterUpdate.fromObject(object.main_character, _depth + 1);
            }
            if (object.badge != null) {
                if (typeof object.badge !== "object")
                    throw TypeError(".lq.AccountUpdate.badge: object expected");
                message.badge = $root.lq.AccountUpdate.BadgeUpdate.fromObject(object.badge, _depth + 1);
            }
            return message;
        };

        AccountUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.numerical = [];
                object.new_recharged_list = [];
            }
            if (options.defaults) {
                object.character = null;
                object.bag = null;
                object.achievement = null;
                object.shilian = null;
                object.daily_task = null;
                object.title = null;
                object.activity_task = null;
                object.activity_flip_task = null;
                object.activity_period_task = null;
                object.activity_random_task = null;
                object.challenge = null;
                object.ab_match = null;
                object.activity = null;
                object.activity_segment_task = null;
                object.month_ticket = null;
                object.main_character = null;
                object.badge = null;
            }
            if (message.numerical && message.numerical.length) {
                object.numerical = Array(message.numerical.length);
                for (let j = 0; j < message.numerical.length; ++j)
                    object.numerical[j] = $root.lq.AccountUpdate.NumericalUpdate.toObject(message.numerical[j], options);
            }
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.AccountUpdate.CharacterUpdate.toObject(message.character, options);
            if (message.bag != null && message.hasOwnProperty("bag"))
                object.bag = $root.lq.BagUpdate.toObject(message.bag, options);
            if (message.achievement != null && message.hasOwnProperty("achievement"))
                object.achievement = $root.lq.AccountUpdate.AchievementUpdate.toObject(message.achievement, options);
            if (message.shilian != null && message.hasOwnProperty("shilian"))
                object.shilian = $root.lq.AccountShiLian.toObject(message.shilian, options);
            if (message.daily_task != null && message.hasOwnProperty("daily_task"))
                object.daily_task = $root.lq.AccountUpdate.DailyTaskUpdate.toObject(message.daily_task, options);
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = $root.lq.AccountUpdate.TitleUpdate.toObject(message.title, options);
            if (message.new_recharged_list && message.new_recharged_list.length) {
                object.new_recharged_list = Array(message.new_recharged_list.length);
                for (let j = 0; j < message.new_recharged_list.length; ++j)
                    object.new_recharged_list[j] = message.new_recharged_list[j];
            }
            if (message.activity_task != null && message.hasOwnProperty("activity_task"))
                object.activity_task = $root.lq.AccountUpdate.TaskUpdate.toObject(message.activity_task, options);
            if (message.activity_flip_task != null && message.hasOwnProperty("activity_flip_task"))
                object.activity_flip_task = $root.lq.AccountUpdate.TaskUpdate.toObject(message.activity_flip_task, options);
            if (message.activity_period_task != null && message.hasOwnProperty("activity_period_task"))
                object.activity_period_task = $root.lq.AccountUpdate.TaskUpdate.toObject(message.activity_period_task, options);
            if (message.activity_random_task != null && message.hasOwnProperty("activity_random_task"))
                object.activity_random_task = $root.lq.AccountUpdate.TaskUpdate.toObject(message.activity_random_task, options);
            if (message.challenge != null && message.hasOwnProperty("challenge"))
                object.challenge = $root.lq.AccountUpdate.AccountChallengeUpdate.toObject(message.challenge, options);
            if (message.ab_match != null && message.hasOwnProperty("ab_match"))
                object.ab_match = $root.lq.AccountUpdate.AccountABMatchUpdate.toObject(message.ab_match, options);
            if (message.activity != null && message.hasOwnProperty("activity"))
                object.activity = $root.lq.AccountActivityUpdate.toObject(message.activity, options);
            if (message.activity_segment_task != null && message.hasOwnProperty("activity_segment_task"))
                object.activity_segment_task = $root.lq.AccountUpdate.SegmentTaskUpdate.toObject(message.activity_segment_task, options);
            if (message.month_ticket != null && message.hasOwnProperty("month_ticket"))
                object.month_ticket = $root.lq.AccountUpdate.MonthTicketUpdate.toObject(message.month_ticket, options);
            if (message.main_character != null && message.hasOwnProperty("main_character"))
                object.main_character = $root.lq.AccountUpdate.MainCharacterUpdate.toObject(message.main_character, options);
            if (message.badge != null && message.hasOwnProperty("badge"))
                object.badge = $root.lq.AccountUpdate.BadgeUpdate.toObject(message.badge, options);
            return object;
        };

        AccountUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountUpdate";
        };

        AccountUpdate.NumericalUpdate = (function() {

            function NumericalUpdate(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            NumericalUpdate.prototype.id = 0;
            NumericalUpdate.prototype.final = 0;

            NumericalUpdate.create = function create(properties) {
                return new NumericalUpdate(properties);
            };

            NumericalUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(24).uint32(message.final);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            NumericalUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            NumericalUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.NumericalUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            NumericalUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            NumericalUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                return null;
            };

            NumericalUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.NumericalUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.NumericalUpdate();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                return message;
            };

            NumericalUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.final = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                return object;
            };

            NumericalUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            NumericalUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.NumericalUpdate";
            };

            return NumericalUpdate;
        })();

        AccountUpdate.CharacterUpdate = (function() {

            function CharacterUpdate(properties) {
                this.characters = [];
                this.skins = [];
                this.finished_endings = [];
                this.rewarded_endings = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CharacterUpdate.prototype.characters = $util.emptyArray;
            CharacterUpdate.prototype.skins = $util.emptyArray;
            CharacterUpdate.prototype.finished_endings = $util.emptyArray;
            CharacterUpdate.prototype.rewarded_endings = $util.emptyArray;

            CharacterUpdate.create = function create(properties) {
                return new CharacterUpdate(properties);
            };

            CharacterUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.characters != null && message.characters.length)
                    for (let i = 0; i < message.characters.length; ++i)
                        $root.lq.Character.encode(message.characters[i], writer.uint32(18).fork()).ldelim();
                if (message.skins != null && message.skins.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.skins.length; ++i)
                        writer.uint32(message.skins[i]);
                    writer.ldelim();
                }
                if (message.finished_endings != null && message.finished_endings.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.finished_endings.length; ++i)
                        writer.uint32(message.finished_endings[i]);
                    writer.ldelim();
                }
                if (message.rewarded_endings != null && message.rewarded_endings.length) {
                    writer.uint32(42).fork();
                    for (let i = 0; i < message.rewarded_endings.length; ++i)
                        writer.uint32(message.rewarded_endings[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CharacterUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CharacterUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.CharacterUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.characters && message.characters.length))
                                message.characters = [];
                            message.characters.push($root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.skins && message.skins.length))
                                    message.skins = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.skins.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.skins && message.skins.length))
                                message.skins = [];
                            message.skins.push(reader.uint32());
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.finished_endings && message.finished_endings.length))
                                    message.finished_endings = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.finished_endings.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.finished_endings && message.finished_endings.length))
                                message.finished_endings = [];
                            message.finished_endings.push(reader.uint32());
                            continue;
                        }
                    case 5: {
                            if (wireType === 2) {
                                if (!(message.rewarded_endings && message.rewarded_endings.length))
                                    message.rewarded_endings = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.rewarded_endings.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.rewarded_endings && message.rewarded_endings.length))
                                message.rewarded_endings = [];
                            message.rewarded_endings.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CharacterUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CharacterUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.characters != null && message.hasOwnProperty("characters")) {
                    if (!Array.isArray(message.characters))
                        return "characters: array expected";
                    for (let i = 0; i < message.characters.length; ++i) {
                        let error = $root.lq.Character.verify(message.characters[i], _depth + 1);
                        if (error)
                            return "characters." + error;
                    }
                }
                if (message.skins != null && message.hasOwnProperty("skins")) {
                    if (!Array.isArray(message.skins))
                        return "skins: array expected";
                    for (let i = 0; i < message.skins.length; ++i)
                        if (!$util.isInteger(message.skins[i]))
                            return "skins: integer[] expected";
                }
                if (message.finished_endings != null && message.hasOwnProperty("finished_endings")) {
                    if (!Array.isArray(message.finished_endings))
                        return "finished_endings: array expected";
                    for (let i = 0; i < message.finished_endings.length; ++i)
                        if (!$util.isInteger(message.finished_endings[i]))
                            return "finished_endings: integer[] expected";
                }
                if (message.rewarded_endings != null && message.hasOwnProperty("rewarded_endings")) {
                    if (!Array.isArray(message.rewarded_endings))
                        return "rewarded_endings: array expected";
                    for (let i = 0; i < message.rewarded_endings.length; ++i)
                        if (!$util.isInteger(message.rewarded_endings[i]))
                            return "rewarded_endings: integer[] expected";
                }
                return null;
            };

            CharacterUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.CharacterUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.CharacterUpdate();
                if (object.characters) {
                    if (!Array.isArray(object.characters))
                        throw TypeError(".lq.AccountUpdate.CharacterUpdate.characters: array expected");
                    message.characters = Array(object.characters.length);
                    for (let i = 0; i < object.characters.length; ++i) {
                        if (typeof object.characters[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.CharacterUpdate.characters: object expected");
                        message.characters[i] = $root.lq.Character.fromObject(object.characters[i], _depth + 1);
                    }
                }
                if (object.skins) {
                    if (!Array.isArray(object.skins))
                        throw TypeError(".lq.AccountUpdate.CharacterUpdate.skins: array expected");
                    message.skins = Array(object.skins.length);
                    for (let i = 0; i < object.skins.length; ++i)
                        message.skins[i] = object.skins[i] >>> 0;
                }
                if (object.finished_endings) {
                    if (!Array.isArray(object.finished_endings))
                        throw TypeError(".lq.AccountUpdate.CharacterUpdate.finished_endings: array expected");
                    message.finished_endings = Array(object.finished_endings.length);
                    for (let i = 0; i < object.finished_endings.length; ++i)
                        message.finished_endings[i] = object.finished_endings[i] >>> 0;
                }
                if (object.rewarded_endings) {
                    if (!Array.isArray(object.rewarded_endings))
                        throw TypeError(".lq.AccountUpdate.CharacterUpdate.rewarded_endings: array expected");
                    message.rewarded_endings = Array(object.rewarded_endings.length);
                    for (let i = 0; i < object.rewarded_endings.length; ++i)
                        message.rewarded_endings[i] = object.rewarded_endings[i] >>> 0;
                }
                return message;
            };

            CharacterUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.characters = [];
                    object.skins = [];
                    object.finished_endings = [];
                    object.rewarded_endings = [];
                }
                if (message.characters && message.characters.length) {
                    object.characters = Array(message.characters.length);
                    for (let j = 0; j < message.characters.length; ++j)
                        object.characters[j] = $root.lq.Character.toObject(message.characters[j], options);
                }
                if (message.skins && message.skins.length) {
                    object.skins = Array(message.skins.length);
                    for (let j = 0; j < message.skins.length; ++j)
                        object.skins[j] = message.skins[j];
                }
                if (message.finished_endings && message.finished_endings.length) {
                    object.finished_endings = Array(message.finished_endings.length);
                    for (let j = 0; j < message.finished_endings.length; ++j)
                        object.finished_endings[j] = message.finished_endings[j];
                }
                if (message.rewarded_endings && message.rewarded_endings.length) {
                    object.rewarded_endings = Array(message.rewarded_endings.length);
                    for (let j = 0; j < message.rewarded_endings.length; ++j)
                        object.rewarded_endings[j] = message.rewarded_endings[j];
                }
                return object;
            };

            CharacterUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CharacterUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.CharacterUpdate";
            };

            return CharacterUpdate;
        })();

        AccountUpdate.AchievementUpdate = (function() {

            function AchievementUpdate(properties) {
                this.progresses = [];
                this.rewarded_group = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AchievementUpdate.prototype.progresses = $util.emptyArray;
            AchievementUpdate.prototype.rewarded_group = $util.emptyArray;

            AchievementUpdate.create = function create(properties) {
                return new AchievementUpdate(properties);
            };

            AchievementUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.AchievementProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.rewarded_group != null && message.rewarded_group.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.rewarded_group.length; ++i)
                        writer.uint32(message.rewarded_group[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AchievementUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AchievementUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.AchievementUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.AchievementProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.rewarded_group && message.rewarded_group.length))
                                    message.rewarded_group = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.rewarded_group.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.rewarded_group && message.rewarded_group.length))
                                message.rewarded_group = [];
                            message.rewarded_group.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AchievementUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AchievementUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.AchievementProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                if (message.rewarded_group != null && message.hasOwnProperty("rewarded_group")) {
                    if (!Array.isArray(message.rewarded_group))
                        return "rewarded_group: array expected";
                    for (let i = 0; i < message.rewarded_group.length; ++i)
                        if (!$util.isInteger(message.rewarded_group[i]))
                            return "rewarded_group: integer[] expected";
                }
                return null;
            };

            AchievementUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.AchievementUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.AchievementUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.AchievementUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.AchievementUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.AchievementProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                if (object.rewarded_group) {
                    if (!Array.isArray(object.rewarded_group))
                        throw TypeError(".lq.AccountUpdate.AchievementUpdate.rewarded_group: array expected");
                    message.rewarded_group = Array(object.rewarded_group.length);
                    for (let i = 0; i < object.rewarded_group.length; ++i)
                        message.rewarded_group[i] = object.rewarded_group[i] >>> 0;
                }
                return message;
            };

            AchievementUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.progresses = [];
                    object.rewarded_group = [];
                }
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.AchievementProgress.toObject(message.progresses[j], options);
                }
                if (message.rewarded_group && message.rewarded_group.length) {
                    object.rewarded_group = Array(message.rewarded_group.length);
                    for (let j = 0; j < message.rewarded_group.length; ++j)
                        object.rewarded_group[j] = message.rewarded_group[j];
                }
                return object;
            };

            AchievementUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AchievementUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.AchievementUpdate";
            };

            return AchievementUpdate;
        })();

        AccountUpdate.DailyTaskUpdate = (function() {

            function DailyTaskUpdate(properties) {
                this.progresses = [];
                this.task_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            DailyTaskUpdate.prototype.progresses = $util.emptyArray;
            DailyTaskUpdate.prototype.task_list = $util.emptyArray;

            DailyTaskUpdate.create = function create(properties) {
                return new DailyTaskUpdate(properties);
            };

            DailyTaskUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.task_list != null && message.task_list.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.task_list.length; ++i)
                        writer.uint32(message.task_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            DailyTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            DailyTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.DailyTaskUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.task_list && message.task_list.length))
                                    message.task_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.task_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            message.task_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            DailyTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            DailyTaskUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                if (message.task_list != null && message.hasOwnProperty("task_list")) {
                    if (!Array.isArray(message.task_list))
                        return "task_list: array expected";
                    for (let i = 0; i < message.task_list.length; ++i)
                        if (!$util.isInteger(message.task_list[i]))
                            return "task_list: integer[] expected";
                }
                return null;
            };

            DailyTaskUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.DailyTaskUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.DailyTaskUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.DailyTaskUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.DailyTaskUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                if (object.task_list) {
                    if (!Array.isArray(object.task_list))
                        throw TypeError(".lq.AccountUpdate.DailyTaskUpdate.task_list: array expected");
                    message.task_list = Array(object.task_list.length);
                    for (let i = 0; i < object.task_list.length; ++i)
                        message.task_list[i] = object.task_list[i] >>> 0;
                }
                return message;
            };

            DailyTaskUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.progresses = [];
                    object.task_list = [];
                }
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
                }
                if (message.task_list && message.task_list.length) {
                    object.task_list = Array(message.task_list.length);
                    for (let j = 0; j < message.task_list.length; ++j)
                        object.task_list[j] = message.task_list[j];
                }
                return object;
            };

            DailyTaskUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            DailyTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.DailyTaskUpdate";
            };

            return DailyTaskUpdate;
        })();

        AccountUpdate.TitleUpdate = (function() {

            function TitleUpdate(properties) {
                this.new_titles = [];
                this.remove_titles = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TitleUpdate.prototype.new_titles = $util.emptyArray;
            TitleUpdate.prototype.remove_titles = $util.emptyArray;

            TitleUpdate.create = function create(properties) {
                return new TitleUpdate(properties);
            };

            TitleUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.new_titles != null && message.new_titles.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.new_titles.length; ++i)
                        writer.uint32(message.new_titles[i]);
                    writer.ldelim();
                }
                if (message.remove_titles != null && message.remove_titles.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.remove_titles.length; ++i)
                        writer.uint32(message.remove_titles[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TitleUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TitleUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.TitleUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.new_titles && message.new_titles.length))
                                    message.new_titles = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.new_titles.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.new_titles && message.new_titles.length))
                                message.new_titles = [];
                            message.new_titles.push(reader.uint32());
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.remove_titles && message.remove_titles.length))
                                    message.remove_titles = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.remove_titles.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.remove_titles && message.remove_titles.length))
                                message.remove_titles = [];
                            message.remove_titles.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TitleUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TitleUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.new_titles != null && message.hasOwnProperty("new_titles")) {
                    if (!Array.isArray(message.new_titles))
                        return "new_titles: array expected";
                    for (let i = 0; i < message.new_titles.length; ++i)
                        if (!$util.isInteger(message.new_titles[i]))
                            return "new_titles: integer[] expected";
                }
                if (message.remove_titles != null && message.hasOwnProperty("remove_titles")) {
                    if (!Array.isArray(message.remove_titles))
                        return "remove_titles: array expected";
                    for (let i = 0; i < message.remove_titles.length; ++i)
                        if (!$util.isInteger(message.remove_titles[i]))
                            return "remove_titles: integer[] expected";
                }
                return null;
            };

            TitleUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.TitleUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.TitleUpdate();
                if (object.new_titles) {
                    if (!Array.isArray(object.new_titles))
                        throw TypeError(".lq.AccountUpdate.TitleUpdate.new_titles: array expected");
                    message.new_titles = Array(object.new_titles.length);
                    for (let i = 0; i < object.new_titles.length; ++i)
                        message.new_titles[i] = object.new_titles[i] >>> 0;
                }
                if (object.remove_titles) {
                    if (!Array.isArray(object.remove_titles))
                        throw TypeError(".lq.AccountUpdate.TitleUpdate.remove_titles: array expected");
                    message.remove_titles = Array(object.remove_titles.length);
                    for (let i = 0; i < object.remove_titles.length; ++i)
                        message.remove_titles[i] = object.remove_titles[i] >>> 0;
                }
                return message;
            };

            TitleUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.new_titles = [];
                    object.remove_titles = [];
                }
                if (message.new_titles && message.new_titles.length) {
                    object.new_titles = Array(message.new_titles.length);
                    for (let j = 0; j < message.new_titles.length; ++j)
                        object.new_titles[j] = message.new_titles[j];
                }
                if (message.remove_titles && message.remove_titles.length) {
                    object.remove_titles = Array(message.remove_titles.length);
                    for (let j = 0; j < message.remove_titles.length; ++j)
                        object.remove_titles[j] = message.remove_titles[j];
                }
                return object;
            };

            TitleUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TitleUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.TitleUpdate";
            };

            return TitleUpdate;
        })();

        AccountUpdate.TaskUpdate = (function() {

            function TaskUpdate(properties) {
                this.progresses = [];
                this.task_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TaskUpdate.prototype.progresses = $util.emptyArray;
            TaskUpdate.prototype.task_list = $util.emptyArray;

            TaskUpdate.create = function create(properties) {
                return new TaskUpdate(properties);
            };

            TaskUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.task_list != null && message.task_list.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.task_list.length; ++i)
                        writer.uint32(message.task_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.TaskUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.task_list && message.task_list.length))
                                    message.task_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.task_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            message.task_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TaskUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TaskUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                if (message.task_list != null && message.hasOwnProperty("task_list")) {
                    if (!Array.isArray(message.task_list))
                        return "task_list: array expected";
                    for (let i = 0; i < message.task_list.length; ++i)
                        if (!$util.isInteger(message.task_list[i]))
                            return "task_list: integer[] expected";
                }
                return null;
            };

            TaskUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.TaskUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.TaskUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.TaskUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.TaskUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                if (object.task_list) {
                    if (!Array.isArray(object.task_list))
                        throw TypeError(".lq.AccountUpdate.TaskUpdate.task_list: array expected");
                    message.task_list = Array(object.task_list.length);
                    for (let i = 0; i < object.task_list.length; ++i)
                        message.task_list[i] = object.task_list[i] >>> 0;
                }
                return message;
            };

            TaskUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.progresses = [];
                    object.task_list = [];
                }
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
                }
                if (message.task_list && message.task_list.length) {
                    object.task_list = Array(message.task_list.length);
                    for (let j = 0; j < message.task_list.length; ++j)
                        object.task_list[j] = message.task_list[j];
                }
                return object;
            };

            TaskUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.TaskUpdate";
            };

            return TaskUpdate;
        })();

        AccountUpdate.AccountChallengeUpdate = (function() {

            function AccountChallengeUpdate(properties) {
                this.progresses = [];
                this.task_list = [];
                this.rewarded_season = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountChallengeUpdate.prototype.progresses = $util.emptyArray;
            AccountChallengeUpdate.prototype.level = 0;
            AccountChallengeUpdate.prototype.refresh_count = 0;
            AccountChallengeUpdate.prototype.match_count = 0;
            AccountChallengeUpdate.prototype.ticket_id = 0;
            AccountChallengeUpdate.prototype.task_list = $util.emptyArray;
            AccountChallengeUpdate.prototype.rewarded_season = $util.emptyArray;

            AccountChallengeUpdate.create = function create(properties) {
                return new AccountChallengeUpdate(properties);
            };

            AccountChallengeUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                    writer.uint32(24).uint32(message.refresh_count);
                if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                    writer.uint32(32).uint32(message.match_count);
                if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                    writer.uint32(40).uint32(message.ticket_id);
                if (message.task_list != null && message.task_list.length) {
                    writer.uint32(50).fork();
                    for (let i = 0; i < message.task_list.length; ++i)
                        writer.uint32(message.task_list[i]);
                    writer.ldelim();
                }
                if (message.rewarded_season != null && message.rewarded_season.length) {
                    writer.uint32(58).fork();
                    for (let i = 0; i < message.rewarded_season.length; ++i)
                        writer.uint32(message.rewarded_season[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountChallengeUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountChallengeUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.AccountChallengeUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.refresh_count = value;
                            else
                                delete message.refresh_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.match_count = value;
                            else
                                delete message.match_count;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ticket_id = value;
                            else
                                delete message.ticket_id;
                            continue;
                        }
                    case 6: {
                            if (wireType === 2) {
                                if (!(message.task_list && message.task_list.length))
                                    message.task_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.task_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            message.task_list.push(reader.uint32());
                            continue;
                        }
                    case 7: {
                            if (wireType === 2) {
                                if (!(message.rewarded_season && message.rewarded_season.length))
                                    message.rewarded_season = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.rewarded_season.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.rewarded_season && message.rewarded_season.length))
                                message.rewarded_season = [];
                            message.rewarded_season.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountChallengeUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountChallengeUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                    if (!$util.isInteger(message.refresh_count))
                        return "refresh_count: integer expected";
                if (message.match_count != null && message.hasOwnProperty("match_count"))
                    if (!$util.isInteger(message.match_count))
                        return "match_count: integer expected";
                if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                    if (!$util.isInteger(message.ticket_id))
                        return "ticket_id: integer expected";
                if (message.task_list != null && message.hasOwnProperty("task_list")) {
                    if (!Array.isArray(message.task_list))
                        return "task_list: array expected";
                    for (let i = 0; i < message.task_list.length; ++i)
                        if (!$util.isInteger(message.task_list[i]))
                            return "task_list: integer[] expected";
                }
                if (message.rewarded_season != null && message.hasOwnProperty("rewarded_season")) {
                    if (!Array.isArray(message.rewarded_season))
                        return "rewarded_season: array expected";
                    for (let i = 0; i < message.rewarded_season.length; ++i)
                        if (!$util.isInteger(message.rewarded_season[i]))
                            return "rewarded_season: integer[] expected";
                }
                return null;
            };

            AccountChallengeUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.AccountChallengeUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.AccountChallengeUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.AccountChallengeUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.AccountChallengeUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.refresh_count != null)
                    if (Number(object.refresh_count) !== 0)
                        message.refresh_count = object.refresh_count >>> 0;
                if (object.match_count != null)
                    if (Number(object.match_count) !== 0)
                        message.match_count = object.match_count >>> 0;
                if (object.ticket_id != null)
                    if (Number(object.ticket_id) !== 0)
                        message.ticket_id = object.ticket_id >>> 0;
                if (object.task_list) {
                    if (!Array.isArray(object.task_list))
                        throw TypeError(".lq.AccountUpdate.AccountChallengeUpdate.task_list: array expected");
                    message.task_list = Array(object.task_list.length);
                    for (let i = 0; i < object.task_list.length; ++i)
                        message.task_list[i] = object.task_list[i] >>> 0;
                }
                if (object.rewarded_season) {
                    if (!Array.isArray(object.rewarded_season))
                        throw TypeError(".lq.AccountUpdate.AccountChallengeUpdate.rewarded_season: array expected");
                    message.rewarded_season = Array(object.rewarded_season.length);
                    for (let i = 0; i < object.rewarded_season.length; ++i)
                        message.rewarded_season[i] = object.rewarded_season[i] >>> 0;
                }
                return message;
            };

            AccountChallengeUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.progresses = [];
                    object.task_list = [];
                    object.rewarded_season = [];
                }
                if (options.defaults) {
                    object.level = 0;
                    object.refresh_count = 0;
                    object.match_count = 0;
                    object.ticket_id = 0;
                }
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                    object.refresh_count = message.refresh_count;
                if (message.match_count != null && message.hasOwnProperty("match_count"))
                    object.match_count = message.match_count;
                if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                    object.ticket_id = message.ticket_id;
                if (message.task_list && message.task_list.length) {
                    object.task_list = Array(message.task_list.length);
                    for (let j = 0; j < message.task_list.length; ++j)
                        object.task_list[j] = message.task_list[j];
                }
                if (message.rewarded_season && message.rewarded_season.length) {
                    object.rewarded_season = Array(message.rewarded_season.length);
                    for (let j = 0; j < message.rewarded_season.length; ++j)
                        object.rewarded_season[j] = message.rewarded_season[j];
                }
                return object;
            };

            AccountChallengeUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountChallengeUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.AccountChallengeUpdate";
            };

            return AccountChallengeUpdate;
        })();

        AccountUpdate.AccountABMatchUpdate = (function() {

            function AccountABMatchUpdate(properties) {
                this.match_max_point = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountABMatchUpdate.prototype.match_id = 0;
            AccountABMatchUpdate.prototype.match_count = 0;
            AccountABMatchUpdate.prototype.buy_in_count = 0;
            AccountABMatchUpdate.prototype.point = 0;
            AccountABMatchUpdate.prototype.rewarded = false;
            AccountABMatchUpdate.prototype.match_max_point = $util.emptyArray;
            AccountABMatchUpdate.prototype.quit = false;

            AccountABMatchUpdate.create = function create(properties) {
                return new AccountABMatchUpdate(properties);
            };

            AccountABMatchUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.match_id != null && Object.hasOwnProperty.call(message, "match_id"))
                    writer.uint32(8).uint32(message.match_id);
                if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                    writer.uint32(16).uint32(message.match_count);
                if (message.buy_in_count != null && Object.hasOwnProperty.call(message, "buy_in_count"))
                    writer.uint32(24).uint32(message.buy_in_count);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(32).uint32(message.point);
                if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                    writer.uint32(40).bool(message.rewarded);
                if (message.match_max_point != null && message.match_max_point.length)
                    for (let i = 0; i < message.match_max_point.length; ++i)
                        $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint.encode(message.match_max_point[i], writer.uint32(50).fork()).ldelim();
                if (message.quit != null && Object.hasOwnProperty.call(message, "quit"))
                    writer.uint32(56).bool(message.quit);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountABMatchUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountABMatchUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.AccountABMatchUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.match_id = value;
                            else
                                delete message.match_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.match_count = value;
                            else
                                delete message.match_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.buy_in_count = value;
                            else
                                delete message.buy_in_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.rewarded = value;
                            else
                                delete message.rewarded;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if (!(message.match_max_point && message.match_max_point.length))
                                message.match_max_point = [];
                            message.match_max_point.push($root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.quit = value;
                            else
                                delete message.quit;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountABMatchUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountABMatchUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.match_id != null && message.hasOwnProperty("match_id"))
                    if (!$util.isInteger(message.match_id))
                        return "match_id: integer expected";
                if (message.match_count != null && message.hasOwnProperty("match_count"))
                    if (!$util.isInteger(message.match_count))
                        return "match_count: integer expected";
                if (message.buy_in_count != null && message.hasOwnProperty("buy_in_count"))
                    if (!$util.isInteger(message.buy_in_count))
                        return "buy_in_count: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                    if (typeof message.rewarded !== "boolean")
                        return "rewarded: boolean expected";
                if (message.match_max_point != null && message.hasOwnProperty("match_max_point")) {
                    if (!Array.isArray(message.match_max_point))
                        return "match_max_point: array expected";
                    for (let i = 0; i < message.match_max_point.length; ++i) {
                        let error = $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint.verify(message.match_max_point[i], _depth + 1);
                        if (error)
                            return "match_max_point." + error;
                    }
                }
                if (message.quit != null && message.hasOwnProperty("quit"))
                    if (typeof message.quit !== "boolean")
                        return "quit: boolean expected";
                return null;
            };

            AccountABMatchUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.AccountABMatchUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.AccountABMatchUpdate();
                if (object.match_id != null)
                    if (Number(object.match_id) !== 0)
                        message.match_id = object.match_id >>> 0;
                if (object.match_count != null)
                    if (Number(object.match_count) !== 0)
                        message.match_count = object.match_count >>> 0;
                if (object.buy_in_count != null)
                    if (Number(object.buy_in_count) !== 0)
                        message.buy_in_count = object.buy_in_count >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                if (object.rewarded != null)
                    if (object.rewarded)
                        message.rewarded = Boolean(object.rewarded);
                if (object.match_max_point) {
                    if (!Array.isArray(object.match_max_point))
                        throw TypeError(".lq.AccountUpdate.AccountABMatchUpdate.match_max_point: array expected");
                    message.match_max_point = Array(object.match_max_point.length);
                    for (let i = 0; i < object.match_max_point.length; ++i) {
                        if (typeof object.match_max_point[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.AccountABMatchUpdate.match_max_point: object expected");
                        message.match_max_point[i] = $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint.fromObject(object.match_max_point[i], _depth + 1);
                    }
                }
                if (object.quit != null)
                    if (object.quit)
                        message.quit = Boolean(object.quit);
                return message;
            };

            AccountABMatchUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.match_max_point = [];
                if (options.defaults) {
                    object.match_id = 0;
                    object.match_count = 0;
                    object.buy_in_count = 0;
                    object.point = 0;
                    object.rewarded = false;
                    object.quit = false;
                }
                if (message.match_id != null && message.hasOwnProperty("match_id"))
                    object.match_id = message.match_id;
                if (message.match_count != null && message.hasOwnProperty("match_count"))
                    object.match_count = message.match_count;
                if (message.buy_in_count != null && message.hasOwnProperty("buy_in_count"))
                    object.buy_in_count = message.buy_in_count;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                    object.rewarded = message.rewarded;
                if (message.match_max_point && message.match_max_point.length) {
                    object.match_max_point = Array(message.match_max_point.length);
                    for (let j = 0; j < message.match_max_point.length; ++j)
                        object.match_max_point[j] = $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint.toObject(message.match_max_point[j], options);
                }
                if (message.quit != null && message.hasOwnProperty("quit"))
                    object.quit = message.quit;
                return object;
            };

            AccountABMatchUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountABMatchUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.AccountABMatchUpdate";
            };

            AccountABMatchUpdate.MatchPoint = (function() {

                function MatchPoint(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                MatchPoint.prototype.match_id = 0;
                MatchPoint.prototype.point = 0;

                MatchPoint.create = function create(properties) {
                    return new MatchPoint(properties);
                };

                MatchPoint.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.match_id != null && Object.hasOwnProperty.call(message, "match_id"))
                        writer.uint32(8).uint32(message.match_id);
                    if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                        writer.uint32(16).uint32(message.point);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                MatchPoint.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                MatchPoint.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.match_id = value;
                                else
                                    delete message.match_id;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.point = value;
                                else
                                    delete message.point;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                MatchPoint.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                MatchPoint.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.match_id != null && message.hasOwnProperty("match_id"))
                        if (!$util.isInteger(message.match_id))
                            return "match_id: integer expected";
                    if (message.point != null && message.hasOwnProperty("point"))
                        if (!$util.isInteger(message.point))
                            return "point: integer expected";
                    return null;
                };

                MatchPoint.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.AccountUpdate.AccountABMatchUpdate.MatchPoint();
                    if (object.match_id != null)
                        if (Number(object.match_id) !== 0)
                            message.match_id = object.match_id >>> 0;
                    if (object.point != null)
                        if (Number(object.point) !== 0)
                            message.point = object.point >>> 0;
                    return message;
                };

                MatchPoint.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.match_id = 0;
                        object.point = 0;
                    }
                    if (message.match_id != null && message.hasOwnProperty("match_id"))
                        object.match_id = message.match_id;
                    if (message.point != null && message.hasOwnProperty("point"))
                        object.point = message.point;
                    return object;
                };

                MatchPoint.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                MatchPoint.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.AccountUpdate.AccountABMatchUpdate.MatchPoint";
                };

                return MatchPoint;
            })();

            return AccountABMatchUpdate;
        })();

        AccountUpdate.SegmentTaskUpdate = (function() {

            function SegmentTaskUpdate(properties) {
                this.progresses = [];
                this.task_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SegmentTaskUpdate.prototype.progresses = $util.emptyArray;
            SegmentTaskUpdate.prototype.task_list = $util.emptyArray;

            SegmentTaskUpdate.create = function create(properties) {
                return new SegmentTaskUpdate(properties);
            };

            SegmentTaskUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.SegmentTaskProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.task_list != null && message.task_list.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.task_list.length; ++i)
                        writer.uint32(message.task_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SegmentTaskUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SegmentTaskUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.SegmentTaskUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.SegmentTaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.task_list && message.task_list.length))
                                    message.task_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.task_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            message.task_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SegmentTaskUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SegmentTaskUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.SegmentTaskProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                if (message.task_list != null && message.hasOwnProperty("task_list")) {
                    if (!Array.isArray(message.task_list))
                        return "task_list: array expected";
                    for (let i = 0; i < message.task_list.length; ++i)
                        if (!$util.isInteger(message.task_list[i]))
                            return "task_list: integer[] expected";
                }
                return null;
            };

            SegmentTaskUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.SegmentTaskUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.SegmentTaskUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.SegmentTaskUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.SegmentTaskUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.SegmentTaskProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                if (object.task_list) {
                    if (!Array.isArray(object.task_list))
                        throw TypeError(".lq.AccountUpdate.SegmentTaskUpdate.task_list: array expected");
                    message.task_list = Array(object.task_list.length);
                    for (let i = 0; i < object.task_list.length; ++i)
                        message.task_list[i] = object.task_list[i] >>> 0;
                }
                return message;
            };

            SegmentTaskUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.progresses = [];
                    object.task_list = [];
                }
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.SegmentTaskProgress.toObject(message.progresses[j], options);
                }
                if (message.task_list && message.task_list.length) {
                    object.task_list = Array(message.task_list.length);
                    for (let j = 0; j < message.task_list.length; ++j)
                        object.task_list[j] = message.task_list[j];
                }
                return object;
            };

            SegmentTaskUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SegmentTaskUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.SegmentTaskUpdate";
            };

            return SegmentTaskUpdate;
        })();

        AccountUpdate.MonthTicketUpdate = (function() {

            function MonthTicketUpdate(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MonthTicketUpdate.prototype.end_time = 0;
            MonthTicketUpdate.prototype.last_pay_time = 0;

            MonthTicketUpdate.create = function create(properties) {
                return new MonthTicketUpdate(properties);
            };

            MonthTicketUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(8).uint32(message.end_time);
                if (message.last_pay_time != null && Object.hasOwnProperty.call(message, "last_pay_time"))
                    writer.uint32(16).uint32(message.last_pay_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MonthTicketUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MonthTicketUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.MonthTicketUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_pay_time = value;
                            else
                                delete message.last_pay_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MonthTicketUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MonthTicketUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    if (!$util.isInteger(message.last_pay_time))
                        return "last_pay_time: integer expected";
                return null;
            };

            MonthTicketUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.MonthTicketUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.MonthTicketUpdate();
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.last_pay_time != null)
                    if (Number(object.last_pay_time) !== 0)
                        message.last_pay_time = object.last_pay_time >>> 0;
                return message;
            };

            MonthTicketUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.end_time = 0;
                    object.last_pay_time = 0;
                }
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    object.last_pay_time = message.last_pay_time;
                return object;
            };

            MonthTicketUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MonthTicketUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.MonthTicketUpdate";
            };

            return MonthTicketUpdate;
        })();

        AccountUpdate.MainCharacterUpdate = (function() {

            function MainCharacterUpdate(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MainCharacterUpdate.prototype.character_id = 0;
            MainCharacterUpdate.prototype.skin_id = 0;

            MainCharacterUpdate.create = function create(properties) {
                return new MainCharacterUpdate(properties);
            };

            MainCharacterUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                    writer.uint32(8).uint32(message.character_id);
                if (message.skin_id != null && Object.hasOwnProperty.call(message, "skin_id"))
                    writer.uint32(16).uint32(message.skin_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MainCharacterUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MainCharacterUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.MainCharacterUpdate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.character_id = value;
                            else
                                delete message.character_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.skin_id = value;
                            else
                                delete message.skin_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MainCharacterUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MainCharacterUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    if (!$util.isInteger(message.character_id))
                        return "character_id: integer expected";
                if (message.skin_id != null && message.hasOwnProperty("skin_id"))
                    if (!$util.isInteger(message.skin_id))
                        return "skin_id: integer expected";
                return null;
            };

            MainCharacterUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.MainCharacterUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.MainCharacterUpdate();
                if (object.character_id != null)
                    if (Number(object.character_id) !== 0)
                        message.character_id = object.character_id >>> 0;
                if (object.skin_id != null)
                    if (Number(object.skin_id) !== 0)
                        message.skin_id = object.skin_id >>> 0;
                return message;
            };

            MainCharacterUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.character_id = 0;
                    object.skin_id = 0;
                }
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    object.character_id = message.character_id;
                if (message.skin_id != null && message.hasOwnProperty("skin_id"))
                    object.skin_id = message.skin_id;
                return object;
            };

            MainCharacterUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MainCharacterUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.MainCharacterUpdate";
            };

            return MainCharacterUpdate;
        })();

        AccountUpdate.BadgeUpdate = (function() {

            function BadgeUpdate(properties) {
                this.progresses = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BadgeUpdate.prototype.progresses = $util.emptyArray;

            BadgeUpdate.create = function create(properties) {
                return new BadgeUpdate(properties);
            };

            BadgeUpdate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.progresses != null && message.progresses.length)
                    for (let i = 0; i < message.progresses.length; ++i)
                        $root.lq.BadgeAchieveProgress.encode(message.progresses[i], writer.uint32(10).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BadgeUpdate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BadgeUpdate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountUpdate.BadgeUpdate();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            message.progresses.push($root.lq.BadgeAchieveProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BadgeUpdate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BadgeUpdate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.progresses != null && message.hasOwnProperty("progresses")) {
                    if (!Array.isArray(message.progresses))
                        return "progresses: array expected";
                    for (let i = 0; i < message.progresses.length; ++i) {
                        let error = $root.lq.BadgeAchieveProgress.verify(message.progresses[i], _depth + 1);
                        if (error)
                            return "progresses." + error;
                    }
                }
                return null;
            };

            BadgeUpdate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountUpdate.BadgeUpdate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountUpdate.BadgeUpdate();
                if (object.progresses) {
                    if (!Array.isArray(object.progresses))
                        throw TypeError(".lq.AccountUpdate.BadgeUpdate.progresses: array expected");
                    message.progresses = Array(object.progresses.length);
                    for (let i = 0; i < object.progresses.length; ++i) {
                        if (typeof object.progresses[i] !== "object")
                            throw TypeError(".lq.AccountUpdate.BadgeUpdate.progresses: object expected");
                        message.progresses[i] = $root.lq.BadgeAchieveProgress.fromObject(object.progresses[i], _depth + 1);
                    }
                }
                return message;
            };

            BadgeUpdate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.progresses = [];
                if (message.progresses && message.progresses.length) {
                    object.progresses = Array(message.progresses.length);
                    for (let j = 0; j < message.progresses.length; ++j)
                        object.progresses[j] = $root.lq.BadgeAchieveProgress.toObject(message.progresses[j], options);
                }
                return object;
            };

            BadgeUpdate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BadgeUpdate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountUpdate.BadgeUpdate";
            };

            return BadgeUpdate;
        })();

        return AccountUpdate;
    })();

    lq.ContestGameMetaData = (function() {

        function ContestGameMetaData(properties) {
            this.type_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ContestGameMetaData.prototype.type_list = $util.emptyArray;
        ContestGameMetaData.prototype.rank_type = 0;

        ContestGameMetaData.create = function create(properties) {
            return new ContestGameMetaData(properties);
        };

        ContestGameMetaData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type_list != null && message.type_list.length)
                for (let i = 0; i < message.type_list.length; ++i)
                    $root.lq.ContestGameMetaData.ContestTypeZoneData.encode(message.type_list[i], writer.uint32(10).fork()).ldelim();
            if (message.rank_type != null && Object.hasOwnProperty.call(message, "rank_type"))
                writer.uint32(16).uint32(message.rank_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ContestGameMetaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ContestGameMetaData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestGameMetaData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.type_list && message.type_list.length))
                            message.type_list = [];
                        message.type_list.push($root.lq.ContestGameMetaData.ContestTypeZoneData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_type = value;
                        else
                            delete message.rank_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ContestGameMetaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ContestGameMetaData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type_list != null && message.hasOwnProperty("type_list")) {
                if (!Array.isArray(message.type_list))
                    return "type_list: array expected";
                for (let i = 0; i < message.type_list.length; ++i) {
                    let error = $root.lq.ContestGameMetaData.ContestTypeZoneData.verify(message.type_list[i], _depth + 1);
                    if (error)
                        return "type_list." + error;
                }
            }
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                if (!$util.isInteger(message.rank_type))
                    return "rank_type: integer expected";
            return null;
        };

        ContestGameMetaData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ContestGameMetaData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ContestGameMetaData();
            if (object.type_list) {
                if (!Array.isArray(object.type_list))
                    throw TypeError(".lq.ContestGameMetaData.type_list: array expected");
                message.type_list = Array(object.type_list.length);
                for (let i = 0; i < object.type_list.length; ++i) {
                    if (typeof object.type_list[i] !== "object")
                        throw TypeError(".lq.ContestGameMetaData.type_list: object expected");
                    message.type_list[i] = $root.lq.ContestGameMetaData.ContestTypeZoneData.fromObject(object.type_list[i], _depth + 1);
                }
            }
            if (object.rank_type != null)
                if (Number(object.rank_type) !== 0)
                    message.rank_type = object.rank_type >>> 0;
            return message;
        };

        ContestGameMetaData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.type_list = [];
            if (options.defaults)
                object.rank_type = 0;
            if (message.type_list && message.type_list.length) {
                object.type_list = Array(message.type_list.length);
                for (let j = 0; j < message.type_list.length; ++j)
                    object.type_list[j] = $root.lq.ContestGameMetaData.ContestTypeZoneData.toObject(message.type_list[j], options);
            }
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                object.rank_type = message.rank_type;
            return object;
        };

        ContestGameMetaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContestGameMetaData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ContestGameMetaData";
        };

        ContestGameMetaData.ContestTypeZoneData = (function() {

            function ContestTypeZoneData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ContestTypeZoneData.prototype.zone = 0;
            ContestTypeZoneData.prototype.contest_type = 0;

            ContestTypeZoneData.create = function create(properties) {
                return new ContestTypeZoneData(properties);
            };

            ContestTypeZoneData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.zone != null && Object.hasOwnProperty.call(message, "zone"))
                    writer.uint32(8).uint32(message.zone);
                if (message.contest_type != null && Object.hasOwnProperty.call(message, "contest_type"))
                    writer.uint32(16).uint32(message.contest_type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ContestTypeZoneData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ContestTypeZoneData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestGameMetaData.ContestTypeZoneData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.zone = value;
                            else
                                delete message.zone;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.contest_type = value;
                            else
                                delete message.contest_type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ContestTypeZoneData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ContestTypeZoneData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.zone != null && message.hasOwnProperty("zone"))
                    if (!$util.isInteger(message.zone))
                        return "zone: integer expected";
                if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                    if (!$util.isInteger(message.contest_type))
                        return "contest_type: integer expected";
                return null;
            };

            ContestTypeZoneData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ContestGameMetaData.ContestTypeZoneData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ContestGameMetaData.ContestTypeZoneData();
                if (object.zone != null)
                    if (Number(object.zone) !== 0)
                        message.zone = object.zone >>> 0;
                if (object.contest_type != null)
                    if (Number(object.contest_type) !== 0)
                        message.contest_type = object.contest_type >>> 0;
                return message;
            };

            ContestTypeZoneData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.zone = 0;
                    object.contest_type = 0;
                }
                if (message.zone != null && message.hasOwnProperty("zone"))
                    object.zone = message.zone;
                if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                    object.contest_type = message.contest_type;
                return object;
            };

            ContestTypeZoneData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContestTypeZoneData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ContestGameMetaData.ContestTypeZoneData";
            };

            return ContestTypeZoneData;
        })();

        return ContestGameMetaData;
    })();

    lq.GameMetaData = (function() {

        function GameMetaData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameMetaData.prototype.room_id = 0;
        GameMetaData.prototype.mode_id = 0;
        GameMetaData.prototype.contest_uid = 0;
        GameMetaData.prototype.contest_info = null;

        GameMetaData.create = function create(properties) {
            return new GameMetaData(properties);
        };

        GameMetaData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.room_id != null && Object.hasOwnProperty.call(message, "room_id"))
                writer.uint32(8).uint32(message.room_id);
            if (message.mode_id != null && Object.hasOwnProperty.call(message, "mode_id"))
                writer.uint32(16).uint32(message.mode_id);
            if (message.contest_uid != null && Object.hasOwnProperty.call(message, "contest_uid"))
                writer.uint32(24).uint32(message.contest_uid);
            if (message.contest_info != null && Object.hasOwnProperty.call(message, "contest_info"))
                $root.lq.ContestGameMetaData.encode(message.contest_info, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameMetaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameMetaData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameMetaData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.room_id = value;
                        else
                            delete message.room_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode_id = value;
                        else
                            delete message.mode_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_uid = value;
                        else
                            delete message.contest_uid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.contest_info = $root.lq.ContestGameMetaData.decode(reader, reader.uint32(), undefined, _depth + 1, message.contest_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameMetaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameMetaData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                if (!$util.isInteger(message.room_id))
                    return "room_id: integer expected";
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                if (!$util.isInteger(message.mode_id))
                    return "mode_id: integer expected";
            if (message.contest_uid != null && message.hasOwnProperty("contest_uid"))
                if (!$util.isInteger(message.contest_uid))
                    return "contest_uid: integer expected";
            if (message.contest_info != null && message.hasOwnProperty("contest_info")) {
                let error = $root.lq.ContestGameMetaData.verify(message.contest_info, _depth + 1);
                if (error)
                    return "contest_info." + error;
            }
            return null;
        };

        GameMetaData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameMetaData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameMetaData();
            if (object.room_id != null)
                if (Number(object.room_id) !== 0)
                    message.room_id = object.room_id >>> 0;
            if (object.mode_id != null)
                if (Number(object.mode_id) !== 0)
                    message.mode_id = object.mode_id >>> 0;
            if (object.contest_uid != null)
                if (Number(object.contest_uid) !== 0)
                    message.contest_uid = object.contest_uid >>> 0;
            if (object.contest_info != null) {
                if (typeof object.contest_info !== "object")
                    throw TypeError(".lq.GameMetaData.contest_info: object expected");
                message.contest_info = $root.lq.ContestGameMetaData.fromObject(object.contest_info, _depth + 1);
            }
            return message;
        };

        GameMetaData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.room_id = 0;
                object.mode_id = 0;
                object.contest_uid = 0;
                object.contest_info = null;
            }
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                object.room_id = message.room_id;
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                object.mode_id = message.mode_id;
            if (message.contest_uid != null && message.hasOwnProperty("contest_uid"))
                object.contest_uid = message.contest_uid;
            if (message.contest_info != null && message.hasOwnProperty("contest_info"))
                object.contest_info = $root.lq.ContestGameMetaData.toObject(message.contest_info, options);
            return object;
        };

        GameMetaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameMetaData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameMetaData";
        };

        return GameMetaData;
    })();

    lq.AccountPlayingGame = (function() {

        function AccountPlayingGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountPlayingGame.prototype.game_uuid = "";
        AccountPlayingGame.prototype.category = 0;
        AccountPlayingGame.prototype.meta = null;

        AccountPlayingGame.create = function create(properties) {
            return new AccountPlayingGame(properties);
        };

        AccountPlayingGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(16).uint32(message.category);
            if (message.meta != null && Object.hasOwnProperty.call(message, "meta"))
                $root.lq.GameMetaData.encode(message.meta, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountPlayingGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountPlayingGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountPlayingGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.meta = $root.lq.GameMetaData.decode(reader, reader.uint32(), undefined, _depth + 1, message.meta);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountPlayingGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountPlayingGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.meta != null && message.hasOwnProperty("meta")) {
                let error = $root.lq.GameMetaData.verify(message.meta, _depth + 1);
                if (error)
                    return "meta." + error;
            }
            return null;
        };

        AccountPlayingGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountPlayingGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountPlayingGame();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.meta != null) {
                if (typeof object.meta !== "object")
                    throw TypeError(".lq.AccountPlayingGame.meta: object expected");
                message.meta = $root.lq.GameMetaData.fromObject(object.meta, _depth + 1);
            }
            return message;
        };

        AccountPlayingGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_uuid = "";
                object.category = 0;
                object.meta = null;
            }
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.meta != null && message.hasOwnProperty("meta"))
                object.meta = $root.lq.GameMetaData.toObject(message.meta, options);
            return object;
        };

        AccountPlayingGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountPlayingGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountPlayingGame";
        };

        return AccountPlayingGame;
    })();

    lq.RandomCharacter = (function() {

        function RandomCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RandomCharacter.prototype.character_id = 0;
        RandomCharacter.prototype.skin_id = 0;

        RandomCharacter.create = function create(properties) {
            return new RandomCharacter(properties);
        };

        RandomCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.skin_id != null && Object.hasOwnProperty.call(message, "skin_id"))
                writer.uint32(16).uint32(message.skin_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RandomCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RandomCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RandomCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.skin_id = value;
                        else
                            delete message.skin_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RandomCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RandomCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.skin_id != null && message.hasOwnProperty("skin_id"))
                if (!$util.isInteger(message.skin_id))
                    return "skin_id: integer expected";
            return null;
        };

        RandomCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RandomCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RandomCharacter();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.skin_id != null)
                if (Number(object.skin_id) !== 0)
                    message.skin_id = object.skin_id >>> 0;
            return message;
        };

        RandomCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.character_id = 0;
                object.skin_id = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.skin_id != null && message.hasOwnProperty("skin_id"))
                object.skin_id = message.skin_id;
            return object;
        };

        RandomCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RandomCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RandomCharacter";
        };

        return RandomCharacter;
    })();

    lq.AccountCacheView = (function() {

        function AccountCacheView(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountCacheView.prototype.cache_version = 0;
        AccountCacheView.prototype.account_id = 0;
        AccountCacheView.prototype.nickname = "";
        AccountCacheView.prototype.login_time = 0;
        AccountCacheView.prototype.logout_time = 0;
        AccountCacheView.prototype.is_online = false;
        AccountCacheView.prototype.room_id = 0;
        AccountCacheView.prototype.title = 0;
        AccountCacheView.prototype.avatar_id = 0;
        AccountCacheView.prototype.vip = 0;
        AccountCacheView.prototype.level = null;
        AccountCacheView.prototype.playing_game = null;
        AccountCacheView.prototype.level3 = null;
        AccountCacheView.prototype.avatar_frame = 0;
        AccountCacheView.prototype.verified = 0;
        AccountCacheView.prototype.ban_deadline = 0;
        AccountCacheView.prototype.comment_ban = 0;
        AccountCacheView.prototype.ban_state = 0;

        AccountCacheView.create = function create(properties) {
            return new AccountCacheView(properties);
        };

        AccountCacheView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.cache_version != null && Object.hasOwnProperty.call(message, "cache_version"))
                writer.uint32(8).uint32(message.cache_version);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(26).string(message.nickname);
            if (message.login_time != null && Object.hasOwnProperty.call(message, "login_time"))
                writer.uint32(32).uint32(message.login_time);
            if (message.logout_time != null && Object.hasOwnProperty.call(message, "logout_time"))
                writer.uint32(40).uint32(message.logout_time);
            if (message.is_online != null && Object.hasOwnProperty.call(message, "is_online"))
                writer.uint32(48).bool(message.is_online);
            if (message.room_id != null && Object.hasOwnProperty.call(message, "room_id"))
                writer.uint32(56).uint32(message.room_id);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(64).uint32(message.title);
            if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                writer.uint32(72).uint32(message.avatar_id);
            if (message.vip != null && Object.hasOwnProperty.call(message, "vip"))
                writer.uint32(80).uint32(message.vip);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.AccountLevel.encode(message.level, writer.uint32(90).fork()).ldelim();
            if (message.playing_game != null && Object.hasOwnProperty.call(message, "playing_game"))
                $root.lq.AccountPlayingGame.encode(message.playing_game, writer.uint32(98).fork()).ldelim();
            if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                $root.lq.AccountLevel.encode(message.level3, writer.uint32(106).fork()).ldelim();
            if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                writer.uint32(112).uint32(message.avatar_frame);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(120).uint32(message.verified);
            if (message.ban_deadline != null && Object.hasOwnProperty.call(message, "ban_deadline"))
                writer.uint32(128).uint32(message.ban_deadline);
            if (message.comment_ban != null && Object.hasOwnProperty.call(message, "comment_ban"))
                writer.uint32(136).uint32(message.comment_ban);
            if (message.ban_state != null && Object.hasOwnProperty.call(message, "ban_state"))
                writer.uint32(144).uint32(message.ban_state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountCacheView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountCacheView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountCacheView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cache_version = value;
                        else
                            delete message.cache_version;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.login_time = value;
                        else
                            delete message.login_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.logout_time = value;
                        else
                            delete message.logout_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_online = value;
                        else
                            delete message.is_online;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.room_id = value;
                        else
                            delete message.room_id;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_id = value;
                        else
                            delete message.avatar_id;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vip = value;
                        else
                            delete message.vip;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.playing_game = $root.lq.AccountPlayingGame.decode(reader, reader.uint32(), undefined, _depth + 1, message.playing_game);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_frame = value;
                        else
                            delete message.avatar_frame;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ban_deadline = value;
                        else
                            delete message.ban_deadline;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.comment_ban = value;
                        else
                            delete message.comment_ban;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ban_state = value;
                        else
                            delete message.ban_state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountCacheView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountCacheView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.cache_version != null && message.hasOwnProperty("cache_version"))
                if (!$util.isInteger(message.cache_version))
                    return "cache_version: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                if (!$util.isInteger(message.login_time))
                    return "login_time: integer expected";
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                if (!$util.isInteger(message.logout_time))
                    return "logout_time: integer expected";
            if (message.is_online != null && message.hasOwnProperty("is_online"))
                if (typeof message.is_online !== "boolean")
                    return "is_online: boolean expected";
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                if (!$util.isInteger(message.room_id))
                    return "room_id: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                if (!$util.isInteger(message.avatar_id))
                    return "avatar_id: integer expected";
            if (message.vip != null && message.hasOwnProperty("vip"))
                if (!$util.isInteger(message.vip))
                    return "vip: integer expected";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.playing_game != null && message.hasOwnProperty("playing_game")) {
                let error = $root.lq.AccountPlayingGame.verify(message.playing_game, _depth + 1);
                if (error)
                    return "playing_game." + error;
            }
            if (message.level3 != null && message.hasOwnProperty("level3")) {
                let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                if (error)
                    return "level3." + error;
            }
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                if (!$util.isInteger(message.avatar_frame))
                    return "avatar_frame: integer expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            if (message.ban_deadline != null && message.hasOwnProperty("ban_deadline"))
                if (!$util.isInteger(message.ban_deadline))
                    return "ban_deadline: integer expected";
            if (message.comment_ban != null && message.hasOwnProperty("comment_ban"))
                if (!$util.isInteger(message.comment_ban))
                    return "comment_ban: integer expected";
            if (message.ban_state != null && message.hasOwnProperty("ban_state"))
                if (!$util.isInteger(message.ban_state))
                    return "ban_state: integer expected";
            return null;
        };

        AccountCacheView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountCacheView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountCacheView();
            if (object.cache_version != null)
                if (Number(object.cache_version) !== 0)
                    message.cache_version = object.cache_version >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.login_time != null)
                if (Number(object.login_time) !== 0)
                    message.login_time = object.login_time >>> 0;
            if (object.logout_time != null)
                if (Number(object.logout_time) !== 0)
                    message.logout_time = object.logout_time >>> 0;
            if (object.is_online != null)
                if (object.is_online)
                    message.is_online = Boolean(object.is_online);
            if (object.room_id != null)
                if (Number(object.room_id) !== 0)
                    message.room_id = object.room_id >>> 0;
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            if (object.avatar_id != null)
                if (Number(object.avatar_id) !== 0)
                    message.avatar_id = object.avatar_id >>> 0;
            if (object.vip != null)
                if (Number(object.vip) !== 0)
                    message.vip = object.vip >>> 0;
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.AccountCacheView.level: object expected");
                message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
            }
            if (object.playing_game != null) {
                if (typeof object.playing_game !== "object")
                    throw TypeError(".lq.AccountCacheView.playing_game: object expected");
                message.playing_game = $root.lq.AccountPlayingGame.fromObject(object.playing_game, _depth + 1);
            }
            if (object.level3 != null) {
                if (typeof object.level3 !== "object")
                    throw TypeError(".lq.AccountCacheView.level3: object expected");
                message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
            }
            if (object.avatar_frame != null)
                if (Number(object.avatar_frame) !== 0)
                    message.avatar_frame = object.avatar_frame >>> 0;
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            if (object.ban_deadline != null)
                if (Number(object.ban_deadline) !== 0)
                    message.ban_deadline = object.ban_deadline >>> 0;
            if (object.comment_ban != null)
                if (Number(object.comment_ban) !== 0)
                    message.comment_ban = object.comment_ban >>> 0;
            if (object.ban_state != null)
                if (Number(object.ban_state) !== 0)
                    message.ban_state = object.ban_state >>> 0;
            return message;
        };

        AccountCacheView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.cache_version = 0;
                object.account_id = 0;
                object.nickname = "";
                object.login_time = 0;
                object.logout_time = 0;
                object.is_online = false;
                object.room_id = 0;
                object.title = 0;
                object.avatar_id = 0;
                object.vip = 0;
                object.level = null;
                object.playing_game = null;
                object.level3 = null;
                object.avatar_frame = 0;
                object.verified = 0;
                object.ban_deadline = 0;
                object.comment_ban = 0;
                object.ban_state = 0;
            }
            if (message.cache_version != null && message.hasOwnProperty("cache_version"))
                object.cache_version = message.cache_version;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                object.login_time = message.login_time;
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                object.logout_time = message.logout_time;
            if (message.is_online != null && message.hasOwnProperty("is_online"))
                object.is_online = message.is_online;
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                object.room_id = message.room_id;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                object.avatar_id = message.avatar_id;
            if (message.vip != null && message.hasOwnProperty("vip"))
                object.vip = message.vip;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.AccountLevel.toObject(message.level, options);
            if (message.playing_game != null && message.hasOwnProperty("playing_game"))
                object.playing_game = $root.lq.AccountPlayingGame.toObject(message.playing_game, options);
            if (message.level3 != null && message.hasOwnProperty("level3"))
                object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                object.avatar_frame = message.avatar_frame;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            if (message.ban_deadline != null && message.hasOwnProperty("ban_deadline"))
                object.ban_deadline = message.ban_deadline;
            if (message.comment_ban != null && message.hasOwnProperty("comment_ban"))
                object.comment_ban = message.comment_ban;
            if (message.ban_state != null && message.hasOwnProperty("ban_state"))
                object.ban_state = message.ban_state;
            return object;
        };

        AccountCacheView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountCacheView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountCacheView";
        };

        return AccountCacheView;
    })();

    lq.PlayerBaseView = (function() {

        function PlayerBaseView(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        PlayerBaseView.prototype.account_id = 0;
        PlayerBaseView.prototype.avatar_id = 0;
        PlayerBaseView.prototype.title = 0;
        PlayerBaseView.prototype.nickname = "";
        PlayerBaseView.prototype.level = null;
        PlayerBaseView.prototype.level3 = null;
        PlayerBaseView.prototype.avatar_frame = 0;
        PlayerBaseView.prototype.verified = 0;
        PlayerBaseView.prototype.is_banned = 0;

        PlayerBaseView.create = function create(properties) {
            return new PlayerBaseView(properties);
        };

        PlayerBaseView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                writer.uint32(16).uint32(message.avatar_id);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(24).uint32(message.title);
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(34).string(message.nickname);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.AccountLevel.encode(message.level, writer.uint32(42).fork()).ldelim();
            if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                $root.lq.AccountLevel.encode(message.level3, writer.uint32(50).fork()).ldelim();
            if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                writer.uint32(56).uint32(message.avatar_frame);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(64).uint32(message.verified);
            if (message.is_banned != null && Object.hasOwnProperty.call(message, "is_banned"))
                writer.uint32(72).uint32(message.is_banned);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PlayerBaseView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        PlayerBaseView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PlayerBaseView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_id = value;
                        else
                            delete message.avatar_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_frame = value;
                        else
                            delete message.avatar_frame;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.is_banned = value;
                        else
                            delete message.is_banned;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        PlayerBaseView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        PlayerBaseView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                if (!$util.isInteger(message.avatar_id))
                    return "avatar_id: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.level3 != null && message.hasOwnProperty("level3")) {
                let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                if (error)
                    return "level3." + error;
            }
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                if (!$util.isInteger(message.avatar_frame))
                    return "avatar_frame: integer expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            if (message.is_banned != null && message.hasOwnProperty("is_banned"))
                if (!$util.isInteger(message.is_banned))
                    return "is_banned: integer expected";
            return null;
        };

        PlayerBaseView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.PlayerBaseView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.PlayerBaseView();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.avatar_id != null)
                if (Number(object.avatar_id) !== 0)
                    message.avatar_id = object.avatar_id >>> 0;
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.PlayerBaseView.level: object expected");
                message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
            }
            if (object.level3 != null) {
                if (typeof object.level3 !== "object")
                    throw TypeError(".lq.PlayerBaseView.level3: object expected");
                message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
            }
            if (object.avatar_frame != null)
                if (Number(object.avatar_frame) !== 0)
                    message.avatar_frame = object.avatar_frame >>> 0;
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            if (object.is_banned != null)
                if (Number(object.is_banned) !== 0)
                    message.is_banned = object.is_banned >>> 0;
            return message;
        };

        PlayerBaseView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.avatar_id = 0;
                object.title = 0;
                object.nickname = "";
                object.level = null;
                object.level3 = null;
                object.avatar_frame = 0;
                object.verified = 0;
                object.is_banned = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                object.avatar_id = message.avatar_id;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.AccountLevel.toObject(message.level, options);
            if (message.level3 != null && message.hasOwnProperty("level3"))
                object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                object.avatar_frame = message.avatar_frame;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            if (message.is_banned != null && message.hasOwnProperty("is_banned"))
                object.is_banned = message.is_banned;
            return object;
        };

        PlayerBaseView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PlayerBaseView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.PlayerBaseView";
        };

        return PlayerBaseView;
    })();

    lq.PlayerGameView = (function() {

        function PlayerGameView(properties) {
            this.views = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        PlayerGameView.prototype.account_id = 0;
        PlayerGameView.prototype.avatar_id = 0;
        PlayerGameView.prototype.title = 0;
        PlayerGameView.prototype.nickname = "";
        PlayerGameView.prototype.level = null;
        PlayerGameView.prototype.character = null;
        PlayerGameView.prototype.level3 = null;
        PlayerGameView.prototype.avatar_frame = 0;
        PlayerGameView.prototype.verified = 0;
        PlayerGameView.prototype.views = $util.emptyArray;
        PlayerGameView.prototype.team_id = 0;
        PlayerGameView.prototype.team_name = "";

        PlayerGameView.create = function create(properties) {
            return new PlayerGameView(properties);
        };

        PlayerGameView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                writer.uint32(16).uint32(message.avatar_id);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(24).uint32(message.title);
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(34).string(message.nickname);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.AccountLevel.encode(message.level, writer.uint32(42).fork()).ldelim();
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.Character.encode(message.character, writer.uint32(50).fork()).ldelim();
            if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                $root.lq.AccountLevel.encode(message.level3, writer.uint32(58).fork()).ldelim();
            if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                writer.uint32(64).uint32(message.avatar_frame);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(72).uint32(message.verified);
            if (message.views != null && message.views.length)
                for (let i = 0; i < message.views.length; ++i)
                    $root.lq.ViewSlot.encode(message.views[i], writer.uint32(82).fork()).ldelim();
            if (message.team_id != null && Object.hasOwnProperty.call(message, "team_id"))
                writer.uint32(88).uint32(message.team_id);
            if (message.team_name != null && Object.hasOwnProperty.call(message, "team_name"))
                writer.uint32(98).string(message.team_name);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PlayerGameView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        PlayerGameView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PlayerGameView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_id = value;
                        else
                            delete message.avatar_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_frame = value;
                        else
                            delete message.avatar_frame;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.views && message.views.length))
                            message.views = [];
                        message.views.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.team_id = value;
                        else
                            delete message.team_id;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.team_name = value;
                        else
                            delete message.team_name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        PlayerGameView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        PlayerGameView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                if (!$util.isInteger(message.avatar_id))
                    return "avatar_id: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.Character.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            if (message.level3 != null && message.hasOwnProperty("level3")) {
                let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                if (error)
                    return "level3." + error;
            }
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                if (!$util.isInteger(message.avatar_frame))
                    return "avatar_frame: integer expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            if (message.views != null && message.hasOwnProperty("views")) {
                if (!Array.isArray(message.views))
                    return "views: array expected";
                for (let i = 0; i < message.views.length; ++i) {
                    let error = $root.lq.ViewSlot.verify(message.views[i], _depth + 1);
                    if (error)
                        return "views." + error;
                }
            }
            if (message.team_id != null && message.hasOwnProperty("team_id"))
                if (!$util.isInteger(message.team_id))
                    return "team_id: integer expected";
            if (message.team_name != null && message.hasOwnProperty("team_name"))
                if (!$util.isString(message.team_name))
                    return "team_name: string expected";
            return null;
        };

        PlayerGameView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.PlayerGameView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.PlayerGameView();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.avatar_id != null)
                if (Number(object.avatar_id) !== 0)
                    message.avatar_id = object.avatar_id >>> 0;
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.PlayerGameView.level: object expected");
                message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
            }
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.PlayerGameView.character: object expected");
                message.character = $root.lq.Character.fromObject(object.character, _depth + 1);
            }
            if (object.level3 != null) {
                if (typeof object.level3 !== "object")
                    throw TypeError(".lq.PlayerGameView.level3: object expected");
                message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
            }
            if (object.avatar_frame != null)
                if (Number(object.avatar_frame) !== 0)
                    message.avatar_frame = object.avatar_frame >>> 0;
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            if (object.views) {
                if (!Array.isArray(object.views))
                    throw TypeError(".lq.PlayerGameView.views: array expected");
                message.views = Array(object.views.length);
                for (let i = 0; i < object.views.length; ++i) {
                    if (typeof object.views[i] !== "object")
                        throw TypeError(".lq.PlayerGameView.views: object expected");
                    message.views[i] = $root.lq.ViewSlot.fromObject(object.views[i], _depth + 1);
                }
            }
            if (object.team_id != null)
                if (Number(object.team_id) !== 0)
                    message.team_id = object.team_id >>> 0;
            if (object.team_name != null)
                if (typeof object.team_name !== "string" || object.team_name.length)
                    message.team_name = String(object.team_name);
            return message;
        };

        PlayerGameView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.views = [];
            if (options.defaults) {
                object.account_id = 0;
                object.avatar_id = 0;
                object.title = 0;
                object.nickname = "";
                object.level = null;
                object.character = null;
                object.level3 = null;
                object.avatar_frame = 0;
                object.verified = 0;
                object.team_id = 0;
                object.team_name = "";
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                object.avatar_id = message.avatar_id;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.AccountLevel.toObject(message.level, options);
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.Character.toObject(message.character, options);
            if (message.level3 != null && message.hasOwnProperty("level3"))
                object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                object.avatar_frame = message.avatar_frame;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            if (message.views && message.views.length) {
                object.views = Array(message.views.length);
                for (let j = 0; j < message.views.length; ++j)
                    object.views[j] = $root.lq.ViewSlot.toObject(message.views[j], options);
            }
            if (message.team_id != null && message.hasOwnProperty("team_id"))
                object.team_id = message.team_id;
            if (message.team_name != null && message.hasOwnProperty("team_name"))
                object.team_name = message.team_name;
            return object;
        };

        PlayerGameView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PlayerGameView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.PlayerGameView";
        };

        return PlayerGameView;
    })();

    lq.GameSetting = (function() {

        function GameSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameSetting.prototype.emoji_switch = 0;

        GameSetting.create = function create(properties) {
            return new GameSetting(properties);
        };

        GameSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.emoji_switch != null && Object.hasOwnProperty.call(message, "emoji_switch"))
                writer.uint32(8).uint32(message.emoji_switch);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.emoji_switch = value;
                        else
                            delete message.emoji_switch;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.emoji_switch != null && message.hasOwnProperty("emoji_switch"))
                if (!$util.isInteger(message.emoji_switch))
                    return "emoji_switch: integer expected";
            return null;
        };

        GameSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameSetting();
            if (object.emoji_switch != null)
                if (Number(object.emoji_switch) !== 0)
                    message.emoji_switch = object.emoji_switch >>> 0;
            return message;
        };

        GameSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.emoji_switch = 0;
            if (message.emoji_switch != null && message.hasOwnProperty("emoji_switch"))
                object.emoji_switch = message.emoji_switch;
            return object;
        };

        GameSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameSetting";
        };

        return GameSetting;
    })();

    lq.GameMode = (function() {

        function GameMode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameMode.prototype.mode = 0;
        GameMode.prototype.ai = false;
        GameMode.prototype.extendinfo = "";
        GameMode.prototype.detail_rule = null;
        GameMode.prototype.testing_environment = null;
        GameMode.prototype.game_setting = null;

        GameMode.create = function create(properties) {
            return new GameMode(properties);
        };

        GameMode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(8).uint32(message.mode);
            if (message.ai != null && Object.hasOwnProperty.call(message, "ai"))
                writer.uint32(32).bool(message.ai);
            if (message.extendinfo != null && Object.hasOwnProperty.call(message, "extendinfo"))
                writer.uint32(42).string(message.extendinfo);
            if (message.detail_rule != null && Object.hasOwnProperty.call(message, "detail_rule"))
                $root.lq.GameDetailRule.encode(message.detail_rule, writer.uint32(50).fork()).ldelim();
            if (message.testing_environment != null && Object.hasOwnProperty.call(message, "testing_environment"))
                $root.lq.GameTestingEnvironmentSet.encode(message.testing_environment, writer.uint32(58).fork()).ldelim();
            if (message.game_setting != null && Object.hasOwnProperty.call(message, "game_setting"))
                $root.lq.GameSetting.encode(message.game_setting, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameMode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameMode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ai = value;
                        else
                            delete message.ai;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.extendinfo = value;
                        else
                            delete message.extendinfo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.detail_rule = $root.lq.GameDetailRule.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail_rule);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.testing_environment = $root.lq.GameTestingEnvironmentSet.decode(reader, reader.uint32(), undefined, _depth + 1, message.testing_environment);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.game_setting = $root.lq.GameSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_setting);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameMode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isInteger(message.mode))
                    return "mode: integer expected";
            if (message.ai != null && message.hasOwnProperty("ai"))
                if (typeof message.ai !== "boolean")
                    return "ai: boolean expected";
            if (message.extendinfo != null && message.hasOwnProperty("extendinfo"))
                if (!$util.isString(message.extendinfo))
                    return "extendinfo: string expected";
            if (message.detail_rule != null && message.hasOwnProperty("detail_rule")) {
                let error = $root.lq.GameDetailRule.verify(message.detail_rule, _depth + 1);
                if (error)
                    return "detail_rule." + error;
            }
            if (message.testing_environment != null && message.hasOwnProperty("testing_environment")) {
                let error = $root.lq.GameTestingEnvironmentSet.verify(message.testing_environment, _depth + 1);
                if (error)
                    return "testing_environment." + error;
            }
            if (message.game_setting != null && message.hasOwnProperty("game_setting")) {
                let error = $root.lq.GameSetting.verify(message.game_setting, _depth + 1);
                if (error)
                    return "game_setting." + error;
            }
            return null;
        };

        GameMode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameMode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameMode();
            if (object.mode != null)
                if (Number(object.mode) !== 0)
                    message.mode = object.mode >>> 0;
            if (object.ai != null)
                if (object.ai)
                    message.ai = Boolean(object.ai);
            if (object.extendinfo != null)
                if (typeof object.extendinfo !== "string" || object.extendinfo.length)
                    message.extendinfo = String(object.extendinfo);
            if (object.detail_rule != null) {
                if (typeof object.detail_rule !== "object")
                    throw TypeError(".lq.GameMode.detail_rule: object expected");
                message.detail_rule = $root.lq.GameDetailRule.fromObject(object.detail_rule, _depth + 1);
            }
            if (object.testing_environment != null) {
                if (typeof object.testing_environment !== "object")
                    throw TypeError(".lq.GameMode.testing_environment: object expected");
                message.testing_environment = $root.lq.GameTestingEnvironmentSet.fromObject(object.testing_environment, _depth + 1);
            }
            if (object.game_setting != null) {
                if (typeof object.game_setting !== "object")
                    throw TypeError(".lq.GameMode.game_setting: object expected");
                message.game_setting = $root.lq.GameSetting.fromObject(object.game_setting, _depth + 1);
            }
            return message;
        };

        GameMode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.mode = 0;
                object.ai = false;
                object.extendinfo = "";
                object.detail_rule = null;
                object.testing_environment = null;
                object.game_setting = null;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            if (message.ai != null && message.hasOwnProperty("ai"))
                object.ai = message.ai;
            if (message.extendinfo != null && message.hasOwnProperty("extendinfo"))
                object.extendinfo = message.extendinfo;
            if (message.detail_rule != null && message.hasOwnProperty("detail_rule"))
                object.detail_rule = $root.lq.GameDetailRule.toObject(message.detail_rule, options);
            if (message.testing_environment != null && message.hasOwnProperty("testing_environment"))
                object.testing_environment = $root.lq.GameTestingEnvironmentSet.toObject(message.testing_environment, options);
            if (message.game_setting != null && message.hasOwnProperty("game_setting"))
                object.game_setting = $root.lq.GameSetting.toObject(message.game_setting, options);
            return object;
        };

        GameMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameMode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameMode";
        };

        return GameMode;
    })();

    lq.GameTestingEnvironmentSet = (function() {

        function GameTestingEnvironmentSet(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameTestingEnvironmentSet.prototype.paixing = 0;
        GameTestingEnvironmentSet.prototype.left_count = 0;
        GameTestingEnvironmentSet.prototype.field_spell_var = 0;

        GameTestingEnvironmentSet.create = function create(properties) {
            return new GameTestingEnvironmentSet(properties);
        };

        GameTestingEnvironmentSet.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.paixing != null && Object.hasOwnProperty.call(message, "paixing"))
                writer.uint32(8).uint32(message.paixing);
            if (message.left_count != null && Object.hasOwnProperty.call(message, "left_count"))
                writer.uint32(16).uint32(message.left_count);
            if (message.field_spell_var != null && Object.hasOwnProperty.call(message, "field_spell_var"))
                writer.uint32(24).uint32(message.field_spell_var);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameTestingEnvironmentSet.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameTestingEnvironmentSet.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameTestingEnvironmentSet(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.paixing = value;
                        else
                            delete message.paixing;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_count = value;
                        else
                            delete message.left_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell_var = value;
                        else
                            delete message.field_spell_var;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameTestingEnvironmentSet.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameTestingEnvironmentSet.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.paixing != null && message.hasOwnProperty("paixing"))
                if (!$util.isInteger(message.paixing))
                    return "paixing: integer expected";
            if (message.left_count != null && message.hasOwnProperty("left_count"))
                if (!$util.isInteger(message.left_count))
                    return "left_count: integer expected";
            if (message.field_spell_var != null && message.hasOwnProperty("field_spell_var"))
                if (!$util.isInteger(message.field_spell_var))
                    return "field_spell_var: integer expected";
            return null;
        };

        GameTestingEnvironmentSet.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameTestingEnvironmentSet)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameTestingEnvironmentSet();
            if (object.paixing != null)
                if (Number(object.paixing) !== 0)
                    message.paixing = object.paixing >>> 0;
            if (object.left_count != null)
                if (Number(object.left_count) !== 0)
                    message.left_count = object.left_count >>> 0;
            if (object.field_spell_var != null)
                if (Number(object.field_spell_var) !== 0)
                    message.field_spell_var = object.field_spell_var >>> 0;
            return message;
        };

        GameTestingEnvironmentSet.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.paixing = 0;
                object.left_count = 0;
                object.field_spell_var = 0;
            }
            if (message.paixing != null && message.hasOwnProperty("paixing"))
                object.paixing = message.paixing;
            if (message.left_count != null && message.hasOwnProperty("left_count"))
                object.left_count = message.left_count;
            if (message.field_spell_var != null && message.hasOwnProperty("field_spell_var"))
                object.field_spell_var = message.field_spell_var;
            return object;
        };

        GameTestingEnvironmentSet.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameTestingEnvironmentSet.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameTestingEnvironmentSet";
        };

        return GameTestingEnvironmentSet;
    })();

    lq.GameDetailRule = (function() {

        function GameDetailRule(properties) {
            this.amusement_switches = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameDetailRule.prototype.time_fixed = 0;
        GameDetailRule.prototype.time_add = 0;
        GameDetailRule.prototype.dora_count = 0;
        GameDetailRule.prototype.shiduan = 0;
        GameDetailRule.prototype.init_point = 0;
        GameDetailRule.prototype.fandian = 0;
        GameDetailRule.prototype.can_jifei = false;
        GameDetailRule.prototype.tianbian_value = 0;
        GameDetailRule.prototype.liqibang_value = 0;
        GameDetailRule.prototype.changbang_value = 0;
        GameDetailRule.prototype.noting_fafu_1 = 0;
        GameDetailRule.prototype.noting_fafu_2 = 0;
        GameDetailRule.prototype.noting_fafu_3 = 0;
        GameDetailRule.prototype.have_liujumanguan = false;
        GameDetailRule.prototype.have_qieshangmanguan = false;
        GameDetailRule.prototype.have_biao_dora = false;
        GameDetailRule.prototype.have_gang_biao_dora = false;
        GameDetailRule.prototype.ming_dora_immediately_open = false;
        GameDetailRule.prototype.have_li_dora = false;
        GameDetailRule.prototype.have_gang_li_dora = false;
        GameDetailRule.prototype.have_sifenglianda = false;
        GameDetailRule.prototype.have_sigangsanle = false;
        GameDetailRule.prototype.have_sijializhi = false;
        GameDetailRule.prototype.have_jiuzhongjiupai = false;
        GameDetailRule.prototype.have_sanjiahele = false;
        GameDetailRule.prototype.have_toutiao = false;
        GameDetailRule.prototype.have_helelianzhuang = false;
        GameDetailRule.prototype.have_helezhongju = false;
        GameDetailRule.prototype.have_tingpailianzhuang = false;
        GameDetailRule.prototype.have_tingpaizhongju = false;
        GameDetailRule.prototype.have_yifa = false;
        GameDetailRule.prototype.have_nanruxiru = false;
        GameDetailRule.prototype.jingsuanyuandian = 0;
        GameDetailRule.prototype.shunweima_2 = 0;
        GameDetailRule.prototype.shunweima_3 = 0;
        GameDetailRule.prototype.shunweima_4 = 0;
        GameDetailRule.prototype.bianjietishi = false;
        GameDetailRule.prototype.ai_level = 0;
        GameDetailRule.prototype.have_zimosun = false;
        GameDetailRule.prototype.disable_multi_yukaman = false;
        GameDetailRule.prototype.fanfu = 0;
        GameDetailRule.prototype.guyi_mode = 0;
        GameDetailRule.prototype.dora3_mode = 0;
        GameDetailRule.prototype.begin_open_mode = 0;
        GameDetailRule.prototype.jiuchao_mode = 0;
        GameDetailRule.prototype.muyu_mode = 0;
        GameDetailRule.prototype.open_hand = 0;
        GameDetailRule.prototype.xuezhandaodi = 0;
        GameDetailRule.prototype.huansanzhang = 0;
        GameDetailRule.prototype.chuanma = 0;
        GameDetailRule.prototype.reveal_discard = 0;
        GameDetailRule.prototype.field_spell_mode = 0;
        GameDetailRule.prototype.zhanxing = 0;
        GameDetailRule.prototype.tianming_mode = 0;
        GameDetailRule.prototype.disable_leijiyiman = false;
        GameDetailRule.prototype.disable_double_yakuman = 0;
        GameDetailRule.prototype.disable_composite_yakuman = 0;
        GameDetailRule.prototype.enable_shiti = 0;
        GameDetailRule.prototype.enable_nontsumo_liqi = 0;
        GameDetailRule.prototype.disable_double_wind_four_fu = 0;
        GameDetailRule.prototype.disable_angang_guoshi = 0;
        GameDetailRule.prototype.enable_renhe = 0;
        GameDetailRule.prototype.enable_baopai_extend_settings = 0;
        GameDetailRule.prototype.yongchang_mode = 0;
        GameDetailRule.prototype.hunzhiyiji_mode = 0;
        GameDetailRule.prototype.wanxiangxiuluo_mode = 0;
        GameDetailRule.prototype.beishuizhizhan_mode = 0;
        GameDetailRule.prototype.amusement_switches = $util.emptyArray;

        GameDetailRule.create = function create(properties) {
            return new GameDetailRule(properties);
        };

        GameDetailRule.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.time_fixed != null && Object.hasOwnProperty.call(message, "time_fixed"))
                writer.uint32(8).uint32(message.time_fixed);
            if (message.time_add != null && Object.hasOwnProperty.call(message, "time_add"))
                writer.uint32(16).uint32(message.time_add);
            if (message.dora_count != null && Object.hasOwnProperty.call(message, "dora_count"))
                writer.uint32(24).uint32(message.dora_count);
            if (message.shiduan != null && Object.hasOwnProperty.call(message, "shiduan"))
                writer.uint32(32).uint32(message.shiduan);
            if (message.init_point != null && Object.hasOwnProperty.call(message, "init_point"))
                writer.uint32(40).uint32(message.init_point);
            if (message.fandian != null && Object.hasOwnProperty.call(message, "fandian"))
                writer.uint32(48).uint32(message.fandian);
            if (message.can_jifei != null && Object.hasOwnProperty.call(message, "can_jifei"))
                writer.uint32(56).bool(message.can_jifei);
            if (message.tianbian_value != null && Object.hasOwnProperty.call(message, "tianbian_value"))
                writer.uint32(64).uint32(message.tianbian_value);
            if (message.liqibang_value != null && Object.hasOwnProperty.call(message, "liqibang_value"))
                writer.uint32(72).uint32(message.liqibang_value);
            if (message.changbang_value != null && Object.hasOwnProperty.call(message, "changbang_value"))
                writer.uint32(80).uint32(message.changbang_value);
            if (message.noting_fafu_1 != null && Object.hasOwnProperty.call(message, "noting_fafu_1"))
                writer.uint32(88).uint32(message.noting_fafu_1);
            if (message.noting_fafu_2 != null && Object.hasOwnProperty.call(message, "noting_fafu_2"))
                writer.uint32(96).uint32(message.noting_fafu_2);
            if (message.noting_fafu_3 != null && Object.hasOwnProperty.call(message, "noting_fafu_3"))
                writer.uint32(104).uint32(message.noting_fafu_3);
            if (message.have_liujumanguan != null && Object.hasOwnProperty.call(message, "have_liujumanguan"))
                writer.uint32(112).bool(message.have_liujumanguan);
            if (message.have_qieshangmanguan != null && Object.hasOwnProperty.call(message, "have_qieshangmanguan"))
                writer.uint32(120).bool(message.have_qieshangmanguan);
            if (message.have_biao_dora != null && Object.hasOwnProperty.call(message, "have_biao_dora"))
                writer.uint32(128).bool(message.have_biao_dora);
            if (message.have_gang_biao_dora != null && Object.hasOwnProperty.call(message, "have_gang_biao_dora"))
                writer.uint32(136).bool(message.have_gang_biao_dora);
            if (message.ming_dora_immediately_open != null && Object.hasOwnProperty.call(message, "ming_dora_immediately_open"))
                writer.uint32(144).bool(message.ming_dora_immediately_open);
            if (message.have_li_dora != null && Object.hasOwnProperty.call(message, "have_li_dora"))
                writer.uint32(152).bool(message.have_li_dora);
            if (message.have_gang_li_dora != null && Object.hasOwnProperty.call(message, "have_gang_li_dora"))
                writer.uint32(160).bool(message.have_gang_li_dora);
            if (message.have_sifenglianda != null && Object.hasOwnProperty.call(message, "have_sifenglianda"))
                writer.uint32(168).bool(message.have_sifenglianda);
            if (message.have_sigangsanle != null && Object.hasOwnProperty.call(message, "have_sigangsanle"))
                writer.uint32(176).bool(message.have_sigangsanle);
            if (message.have_sijializhi != null && Object.hasOwnProperty.call(message, "have_sijializhi"))
                writer.uint32(184).bool(message.have_sijializhi);
            if (message.have_jiuzhongjiupai != null && Object.hasOwnProperty.call(message, "have_jiuzhongjiupai"))
                writer.uint32(192).bool(message.have_jiuzhongjiupai);
            if (message.have_sanjiahele != null && Object.hasOwnProperty.call(message, "have_sanjiahele"))
                writer.uint32(200).bool(message.have_sanjiahele);
            if (message.have_toutiao != null && Object.hasOwnProperty.call(message, "have_toutiao"))
                writer.uint32(208).bool(message.have_toutiao);
            if (message.have_helelianzhuang != null && Object.hasOwnProperty.call(message, "have_helelianzhuang"))
                writer.uint32(216).bool(message.have_helelianzhuang);
            if (message.have_helezhongju != null && Object.hasOwnProperty.call(message, "have_helezhongju"))
                writer.uint32(224).bool(message.have_helezhongju);
            if (message.have_tingpailianzhuang != null && Object.hasOwnProperty.call(message, "have_tingpailianzhuang"))
                writer.uint32(232).bool(message.have_tingpailianzhuang);
            if (message.have_tingpaizhongju != null && Object.hasOwnProperty.call(message, "have_tingpaizhongju"))
                writer.uint32(240).bool(message.have_tingpaizhongju);
            if (message.have_yifa != null && Object.hasOwnProperty.call(message, "have_yifa"))
                writer.uint32(248).bool(message.have_yifa);
            if (message.have_nanruxiru != null && Object.hasOwnProperty.call(message, "have_nanruxiru"))
                writer.uint32(256).bool(message.have_nanruxiru);
            if (message.jingsuanyuandian != null && Object.hasOwnProperty.call(message, "jingsuanyuandian"))
                writer.uint32(264).uint32(message.jingsuanyuandian);
            if (message.shunweima_2 != null && Object.hasOwnProperty.call(message, "shunweima_2"))
                writer.uint32(272).int32(message.shunweima_2);
            if (message.shunweima_3 != null && Object.hasOwnProperty.call(message, "shunweima_3"))
                writer.uint32(280).int32(message.shunweima_3);
            if (message.shunweima_4 != null && Object.hasOwnProperty.call(message, "shunweima_4"))
                writer.uint32(288).int32(message.shunweima_4);
            if (message.bianjietishi != null && Object.hasOwnProperty.call(message, "bianjietishi"))
                writer.uint32(296).bool(message.bianjietishi);
            if (message.ai_level != null && Object.hasOwnProperty.call(message, "ai_level"))
                writer.uint32(304).uint32(message.ai_level);
            if (message.have_zimosun != null && Object.hasOwnProperty.call(message, "have_zimosun"))
                writer.uint32(312).bool(message.have_zimosun);
            if (message.disable_multi_yukaman != null && Object.hasOwnProperty.call(message, "disable_multi_yukaman"))
                writer.uint32(320).bool(message.disable_multi_yukaman);
            if (message.fanfu != null && Object.hasOwnProperty.call(message, "fanfu"))
                writer.uint32(328).uint32(message.fanfu);
            if (message.guyi_mode != null && Object.hasOwnProperty.call(message, "guyi_mode"))
                writer.uint32(336).uint32(message.guyi_mode);
            if (message.dora3_mode != null && Object.hasOwnProperty.call(message, "dora3_mode"))
                writer.uint32(344).uint32(message.dora3_mode);
            if (message.begin_open_mode != null && Object.hasOwnProperty.call(message, "begin_open_mode"))
                writer.uint32(352).uint32(message.begin_open_mode);
            if (message.jiuchao_mode != null && Object.hasOwnProperty.call(message, "jiuchao_mode"))
                writer.uint32(360).uint32(message.jiuchao_mode);
            if (message.muyu_mode != null && Object.hasOwnProperty.call(message, "muyu_mode"))
                writer.uint32(368).uint32(message.muyu_mode);
            if (message.open_hand != null && Object.hasOwnProperty.call(message, "open_hand"))
                writer.uint32(376).uint32(message.open_hand);
            if (message.xuezhandaodi != null && Object.hasOwnProperty.call(message, "xuezhandaodi"))
                writer.uint32(384).uint32(message.xuezhandaodi);
            if (message.huansanzhang != null && Object.hasOwnProperty.call(message, "huansanzhang"))
                writer.uint32(392).uint32(message.huansanzhang);
            if (message.chuanma != null && Object.hasOwnProperty.call(message, "chuanma"))
                writer.uint32(400).uint32(message.chuanma);
            if (message.reveal_discard != null && Object.hasOwnProperty.call(message, "reveal_discard"))
                writer.uint32(408).uint32(message.reveal_discard);
            if (message.field_spell_mode != null && Object.hasOwnProperty.call(message, "field_spell_mode"))
                writer.uint32(416).uint32(message.field_spell_mode);
            if (message.zhanxing != null && Object.hasOwnProperty.call(message, "zhanxing"))
                writer.uint32(424).uint32(message.zhanxing);
            if (message.tianming_mode != null && Object.hasOwnProperty.call(message, "tianming_mode"))
                writer.uint32(432).uint32(message.tianming_mode);
            if (message.disable_leijiyiman != null && Object.hasOwnProperty.call(message, "disable_leijiyiman"))
                writer.uint32(480).bool(message.disable_leijiyiman);
            if (message.disable_double_yakuman != null && Object.hasOwnProperty.call(message, "disable_double_yakuman"))
                writer.uint32(496).uint32(message.disable_double_yakuman);
            if (message.disable_composite_yakuman != null && Object.hasOwnProperty.call(message, "disable_composite_yakuman"))
                writer.uint32(504).uint32(message.disable_composite_yakuman);
            if (message.enable_shiti != null && Object.hasOwnProperty.call(message, "enable_shiti"))
                writer.uint32(512).uint32(message.enable_shiti);
            if (message.enable_nontsumo_liqi != null && Object.hasOwnProperty.call(message, "enable_nontsumo_liqi"))
                writer.uint32(520).uint32(message.enable_nontsumo_liqi);
            if (message.disable_double_wind_four_fu != null && Object.hasOwnProperty.call(message, "disable_double_wind_four_fu"))
                writer.uint32(528).uint32(message.disable_double_wind_four_fu);
            if (message.disable_angang_guoshi != null && Object.hasOwnProperty.call(message, "disable_angang_guoshi"))
                writer.uint32(536).uint32(message.disable_angang_guoshi);
            if (message.enable_renhe != null && Object.hasOwnProperty.call(message, "enable_renhe"))
                writer.uint32(544).uint32(message.enable_renhe);
            if (message.enable_baopai_extend_settings != null && Object.hasOwnProperty.call(message, "enable_baopai_extend_settings"))
                writer.uint32(552).uint32(message.enable_baopai_extend_settings);
            if (message.yongchang_mode != null && Object.hasOwnProperty.call(message, "yongchang_mode"))
                writer.uint32(560).uint32(message.yongchang_mode);
            if (message.hunzhiyiji_mode != null && Object.hasOwnProperty.call(message, "hunzhiyiji_mode"))
                writer.uint32(568).uint32(message.hunzhiyiji_mode);
            if (message.wanxiangxiuluo_mode != null && Object.hasOwnProperty.call(message, "wanxiangxiuluo_mode"))
                writer.uint32(576).uint32(message.wanxiangxiuluo_mode);
            if (message.beishuizhizhan_mode != null && Object.hasOwnProperty.call(message, "beishuizhizhan_mode"))
                writer.uint32(584).uint32(message.beishuizhizhan_mode);
            if (message.amusement_switches != null && message.amusement_switches.length) {
                writer.uint32(594).fork();
                for (let i = 0; i < message.amusement_switches.length; ++i)
                    writer.uint32(message.amusement_switches[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameDetailRule.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameDetailRule.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameDetailRule(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_fixed = value;
                        else
                            delete message.time_fixed;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_add = value;
                        else
                            delete message.time_add;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dora_count = value;
                        else
                            delete message.dora_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shiduan = value;
                        else
                            delete message.shiduan;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.init_point = value;
                        else
                            delete message.init_point;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fandian = value;
                        else
                            delete message.fandian;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.can_jifei = value;
                        else
                            delete message.can_jifei;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tianbian_value = value;
                        else
                            delete message.tianbian_value;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang_value = value;
                        else
                            delete message.liqibang_value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.changbang_value = value;
                        else
                            delete message.changbang_value;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_1 = value;
                        else
                            delete message.noting_fafu_1;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_2 = value;
                        else
                            delete message.noting_fafu_2;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_3 = value;
                        else
                            delete message.noting_fafu_3;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_liujumanguan = value;
                        else
                            delete message.have_liujumanguan;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_qieshangmanguan = value;
                        else
                            delete message.have_qieshangmanguan;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_biao_dora = value;
                        else
                            delete message.have_biao_dora;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_gang_biao_dora = value;
                        else
                            delete message.have_gang_biao_dora;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ming_dora_immediately_open = value;
                        else
                            delete message.ming_dora_immediately_open;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_li_dora = value;
                        else
                            delete message.have_li_dora;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_gang_li_dora = value;
                        else
                            delete message.have_gang_li_dora;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sifenglianda = value;
                        else
                            delete message.have_sifenglianda;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sigangsanle = value;
                        else
                            delete message.have_sigangsanle;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sijializhi = value;
                        else
                            delete message.have_sijializhi;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_jiuzhongjiupai = value;
                        else
                            delete message.have_jiuzhongjiupai;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sanjiahele = value;
                        else
                            delete message.have_sanjiahele;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_toutiao = value;
                        else
                            delete message.have_toutiao;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_helelianzhuang = value;
                        else
                            delete message.have_helelianzhuang;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_helezhongju = value;
                        else
                            delete message.have_helezhongju;
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_tingpailianzhuang = value;
                        else
                            delete message.have_tingpailianzhuang;
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_tingpaizhongju = value;
                        else
                            delete message.have_tingpaizhongju;
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_yifa = value;
                        else
                            delete message.have_yifa;
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_nanruxiru = value;
                        else
                            delete message.have_nanruxiru;
                        continue;
                    }
                case 33: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.jingsuanyuandian = value;
                        else
                            delete message.jingsuanyuandian;
                        continue;
                    }
                case 34: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_2 = value;
                        else
                            delete message.shunweima_2;
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_3 = value;
                        else
                            delete message.shunweima_3;
                        continue;
                    }
                case 36: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_4 = value;
                        else
                            delete message.shunweima_4;
                        continue;
                    }
                case 37: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.bianjietishi = value;
                        else
                            delete message.bianjietishi;
                        continue;
                    }
                case 38: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ai_level = value;
                        else
                            delete message.ai_level;
                        continue;
                    }
                case 39: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_zimosun = value;
                        else
                            delete message.have_zimosun;
                        continue;
                    }
                case 40: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.disable_multi_yukaman = value;
                        else
                            delete message.disable_multi_yukaman;
                        continue;
                    }
                case 41: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fanfu = value;
                        else
                            delete message.fanfu;
                        continue;
                    }
                case 42: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.guyi_mode = value;
                        else
                            delete message.guyi_mode;
                        continue;
                    }
                case 43: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dora3_mode = value;
                        else
                            delete message.dora3_mode;
                        continue;
                    }
                case 44: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.begin_open_mode = value;
                        else
                            delete message.begin_open_mode;
                        continue;
                    }
                case 45: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.jiuchao_mode = value;
                        else
                            delete message.jiuchao_mode;
                        continue;
                    }
                case 46: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.muyu_mode = value;
                        else
                            delete message.muyu_mode;
                        continue;
                    }
                case 47: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_hand = value;
                        else
                            delete message.open_hand;
                        continue;
                    }
                case 48: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xuezhandaodi = value;
                        else
                            delete message.xuezhandaodi;
                        continue;
                    }
                case 49: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.huansanzhang = value;
                        else
                            delete message.huansanzhang;
                        continue;
                    }
                case 50: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chuanma = value;
                        else
                            delete message.chuanma;
                        continue;
                    }
                case 51: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reveal_discard = value;
                        else
                            delete message.reveal_discard;
                        continue;
                    }
                case 52: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell_mode = value;
                        else
                            delete message.field_spell_mode;
                        continue;
                    }
                case 53: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.zhanxing = value;
                        else
                            delete message.zhanxing;
                        continue;
                    }
                case 54: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tianming_mode = value;
                        else
                            delete message.tianming_mode;
                        continue;
                    }
                case 60: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.disable_leijiyiman = value;
                        else
                            delete message.disable_leijiyiman;
                        continue;
                    }
                case 62: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_double_yakuman = value;
                        else
                            delete message.disable_double_yakuman;
                        continue;
                    }
                case 63: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_composite_yakuman = value;
                        else
                            delete message.disable_composite_yakuman;
                        continue;
                    }
                case 64: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_shiti = value;
                        else
                            delete message.enable_shiti;
                        continue;
                    }
                case 65: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_nontsumo_liqi = value;
                        else
                            delete message.enable_nontsumo_liqi;
                        continue;
                    }
                case 66: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_double_wind_four_fu = value;
                        else
                            delete message.disable_double_wind_four_fu;
                        continue;
                    }
                case 67: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_angang_guoshi = value;
                        else
                            delete message.disable_angang_guoshi;
                        continue;
                    }
                case 68: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_renhe = value;
                        else
                            delete message.enable_renhe;
                        continue;
                    }
                case 69: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_baopai_extend_settings = value;
                        else
                            delete message.enable_baopai_extend_settings;
                        continue;
                    }
                case 70: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.yongchang_mode = value;
                        else
                            delete message.yongchang_mode;
                        continue;
                    }
                case 71: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.hunzhiyiji_mode = value;
                        else
                            delete message.hunzhiyiji_mode;
                        continue;
                    }
                case 72: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.wanxiangxiuluo_mode = value;
                        else
                            delete message.wanxiangxiuluo_mode;
                        continue;
                    }
                case 73: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.beishuizhizhan_mode = value;
                        else
                            delete message.beishuizhizhan_mode;
                        continue;
                    }
                case 74: {
                        if (wireType === 2) {
                            if (!(message.amusement_switches && message.amusement_switches.length))
                                message.amusement_switches = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.amusement_switches.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.amusement_switches && message.amusement_switches.length))
                            message.amusement_switches = [];
                        message.amusement_switches.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameDetailRule.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameDetailRule.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.time_fixed != null && message.hasOwnProperty("time_fixed"))
                if (!$util.isInteger(message.time_fixed))
                    return "time_fixed: integer expected";
            if (message.time_add != null && message.hasOwnProperty("time_add"))
                if (!$util.isInteger(message.time_add))
                    return "time_add: integer expected";
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                if (!$util.isInteger(message.dora_count))
                    return "dora_count: integer expected";
            if (message.shiduan != null && message.hasOwnProperty("shiduan"))
                if (!$util.isInteger(message.shiduan))
                    return "shiduan: integer expected";
            if (message.init_point != null && message.hasOwnProperty("init_point"))
                if (!$util.isInteger(message.init_point))
                    return "init_point: integer expected";
            if (message.fandian != null && message.hasOwnProperty("fandian"))
                if (!$util.isInteger(message.fandian))
                    return "fandian: integer expected";
            if (message.can_jifei != null && message.hasOwnProperty("can_jifei"))
                if (typeof message.can_jifei !== "boolean")
                    return "can_jifei: boolean expected";
            if (message.tianbian_value != null && message.hasOwnProperty("tianbian_value"))
                if (!$util.isInteger(message.tianbian_value))
                    return "tianbian_value: integer expected";
            if (message.liqibang_value != null && message.hasOwnProperty("liqibang_value"))
                if (!$util.isInteger(message.liqibang_value))
                    return "liqibang_value: integer expected";
            if (message.changbang_value != null && message.hasOwnProperty("changbang_value"))
                if (!$util.isInteger(message.changbang_value))
                    return "changbang_value: integer expected";
            if (message.noting_fafu_1 != null && message.hasOwnProperty("noting_fafu_1"))
                if (!$util.isInteger(message.noting_fafu_1))
                    return "noting_fafu_1: integer expected";
            if (message.noting_fafu_2 != null && message.hasOwnProperty("noting_fafu_2"))
                if (!$util.isInteger(message.noting_fafu_2))
                    return "noting_fafu_2: integer expected";
            if (message.noting_fafu_3 != null && message.hasOwnProperty("noting_fafu_3"))
                if (!$util.isInteger(message.noting_fafu_3))
                    return "noting_fafu_3: integer expected";
            if (message.have_liujumanguan != null && message.hasOwnProperty("have_liujumanguan"))
                if (typeof message.have_liujumanguan !== "boolean")
                    return "have_liujumanguan: boolean expected";
            if (message.have_qieshangmanguan != null && message.hasOwnProperty("have_qieshangmanguan"))
                if (typeof message.have_qieshangmanguan !== "boolean")
                    return "have_qieshangmanguan: boolean expected";
            if (message.have_biao_dora != null && message.hasOwnProperty("have_biao_dora"))
                if (typeof message.have_biao_dora !== "boolean")
                    return "have_biao_dora: boolean expected";
            if (message.have_gang_biao_dora != null && message.hasOwnProperty("have_gang_biao_dora"))
                if (typeof message.have_gang_biao_dora !== "boolean")
                    return "have_gang_biao_dora: boolean expected";
            if (message.ming_dora_immediately_open != null && message.hasOwnProperty("ming_dora_immediately_open"))
                if (typeof message.ming_dora_immediately_open !== "boolean")
                    return "ming_dora_immediately_open: boolean expected";
            if (message.have_li_dora != null && message.hasOwnProperty("have_li_dora"))
                if (typeof message.have_li_dora !== "boolean")
                    return "have_li_dora: boolean expected";
            if (message.have_gang_li_dora != null && message.hasOwnProperty("have_gang_li_dora"))
                if (typeof message.have_gang_li_dora !== "boolean")
                    return "have_gang_li_dora: boolean expected";
            if (message.have_sifenglianda != null && message.hasOwnProperty("have_sifenglianda"))
                if (typeof message.have_sifenglianda !== "boolean")
                    return "have_sifenglianda: boolean expected";
            if (message.have_sigangsanle != null && message.hasOwnProperty("have_sigangsanle"))
                if (typeof message.have_sigangsanle !== "boolean")
                    return "have_sigangsanle: boolean expected";
            if (message.have_sijializhi != null && message.hasOwnProperty("have_sijializhi"))
                if (typeof message.have_sijializhi !== "boolean")
                    return "have_sijializhi: boolean expected";
            if (message.have_jiuzhongjiupai != null && message.hasOwnProperty("have_jiuzhongjiupai"))
                if (typeof message.have_jiuzhongjiupai !== "boolean")
                    return "have_jiuzhongjiupai: boolean expected";
            if (message.have_sanjiahele != null && message.hasOwnProperty("have_sanjiahele"))
                if (typeof message.have_sanjiahele !== "boolean")
                    return "have_sanjiahele: boolean expected";
            if (message.have_toutiao != null && message.hasOwnProperty("have_toutiao"))
                if (typeof message.have_toutiao !== "boolean")
                    return "have_toutiao: boolean expected";
            if (message.have_helelianzhuang != null && message.hasOwnProperty("have_helelianzhuang"))
                if (typeof message.have_helelianzhuang !== "boolean")
                    return "have_helelianzhuang: boolean expected";
            if (message.have_helezhongju != null && message.hasOwnProperty("have_helezhongju"))
                if (typeof message.have_helezhongju !== "boolean")
                    return "have_helezhongju: boolean expected";
            if (message.have_tingpailianzhuang != null && message.hasOwnProperty("have_tingpailianzhuang"))
                if (typeof message.have_tingpailianzhuang !== "boolean")
                    return "have_tingpailianzhuang: boolean expected";
            if (message.have_tingpaizhongju != null && message.hasOwnProperty("have_tingpaizhongju"))
                if (typeof message.have_tingpaizhongju !== "boolean")
                    return "have_tingpaizhongju: boolean expected";
            if (message.have_yifa != null && message.hasOwnProperty("have_yifa"))
                if (typeof message.have_yifa !== "boolean")
                    return "have_yifa: boolean expected";
            if (message.have_nanruxiru != null && message.hasOwnProperty("have_nanruxiru"))
                if (typeof message.have_nanruxiru !== "boolean")
                    return "have_nanruxiru: boolean expected";
            if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                if (!$util.isInteger(message.jingsuanyuandian))
                    return "jingsuanyuandian: integer expected";
            if (message.shunweima_2 != null && message.hasOwnProperty("shunweima_2"))
                if (!$util.isInteger(message.shunweima_2))
                    return "shunweima_2: integer expected";
            if (message.shunweima_3 != null && message.hasOwnProperty("shunweima_3"))
                if (!$util.isInteger(message.shunweima_3))
                    return "shunweima_3: integer expected";
            if (message.shunweima_4 != null && message.hasOwnProperty("shunweima_4"))
                if (!$util.isInteger(message.shunweima_4))
                    return "shunweima_4: integer expected";
            if (message.bianjietishi != null && message.hasOwnProperty("bianjietishi"))
                if (typeof message.bianjietishi !== "boolean")
                    return "bianjietishi: boolean expected";
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                if (!$util.isInteger(message.ai_level))
                    return "ai_level: integer expected";
            if (message.have_zimosun != null && message.hasOwnProperty("have_zimosun"))
                if (typeof message.have_zimosun !== "boolean")
                    return "have_zimosun: boolean expected";
            if (message.disable_multi_yukaman != null && message.hasOwnProperty("disable_multi_yukaman"))
                if (typeof message.disable_multi_yukaman !== "boolean")
                    return "disable_multi_yukaman: boolean expected";
            if (message.fanfu != null && message.hasOwnProperty("fanfu"))
                if (!$util.isInteger(message.fanfu))
                    return "fanfu: integer expected";
            if (message.guyi_mode != null && message.hasOwnProperty("guyi_mode"))
                if (!$util.isInteger(message.guyi_mode))
                    return "guyi_mode: integer expected";
            if (message.dora3_mode != null && message.hasOwnProperty("dora3_mode"))
                if (!$util.isInteger(message.dora3_mode))
                    return "dora3_mode: integer expected";
            if (message.begin_open_mode != null && message.hasOwnProperty("begin_open_mode"))
                if (!$util.isInteger(message.begin_open_mode))
                    return "begin_open_mode: integer expected";
            if (message.jiuchao_mode != null && message.hasOwnProperty("jiuchao_mode"))
                if (!$util.isInteger(message.jiuchao_mode))
                    return "jiuchao_mode: integer expected";
            if (message.muyu_mode != null && message.hasOwnProperty("muyu_mode"))
                if (!$util.isInteger(message.muyu_mode))
                    return "muyu_mode: integer expected";
            if (message.open_hand != null && message.hasOwnProperty("open_hand"))
                if (!$util.isInteger(message.open_hand))
                    return "open_hand: integer expected";
            if (message.xuezhandaodi != null && message.hasOwnProperty("xuezhandaodi"))
                if (!$util.isInteger(message.xuezhandaodi))
                    return "xuezhandaodi: integer expected";
            if (message.huansanzhang != null && message.hasOwnProperty("huansanzhang"))
                if (!$util.isInteger(message.huansanzhang))
                    return "huansanzhang: integer expected";
            if (message.chuanma != null && message.hasOwnProperty("chuanma"))
                if (!$util.isInteger(message.chuanma))
                    return "chuanma: integer expected";
            if (message.reveal_discard != null && message.hasOwnProperty("reveal_discard"))
                if (!$util.isInteger(message.reveal_discard))
                    return "reveal_discard: integer expected";
            if (message.field_spell_mode != null && message.hasOwnProperty("field_spell_mode"))
                if (!$util.isInteger(message.field_spell_mode))
                    return "field_spell_mode: integer expected";
            if (message.zhanxing != null && message.hasOwnProperty("zhanxing"))
                if (!$util.isInteger(message.zhanxing))
                    return "zhanxing: integer expected";
            if (message.tianming_mode != null && message.hasOwnProperty("tianming_mode"))
                if (!$util.isInteger(message.tianming_mode))
                    return "tianming_mode: integer expected";
            if (message.disable_leijiyiman != null && message.hasOwnProperty("disable_leijiyiman"))
                if (typeof message.disable_leijiyiman !== "boolean")
                    return "disable_leijiyiman: boolean expected";
            if (message.disable_double_yakuman != null && message.hasOwnProperty("disable_double_yakuman"))
                if (!$util.isInteger(message.disable_double_yakuman))
                    return "disable_double_yakuman: integer expected";
            if (message.disable_composite_yakuman != null && message.hasOwnProperty("disable_composite_yakuman"))
                if (!$util.isInteger(message.disable_composite_yakuman))
                    return "disable_composite_yakuman: integer expected";
            if (message.enable_shiti != null && message.hasOwnProperty("enable_shiti"))
                if (!$util.isInteger(message.enable_shiti))
                    return "enable_shiti: integer expected";
            if (message.enable_nontsumo_liqi != null && message.hasOwnProperty("enable_nontsumo_liqi"))
                if (!$util.isInteger(message.enable_nontsumo_liqi))
                    return "enable_nontsumo_liqi: integer expected";
            if (message.disable_double_wind_four_fu != null && message.hasOwnProperty("disable_double_wind_four_fu"))
                if (!$util.isInteger(message.disable_double_wind_four_fu))
                    return "disable_double_wind_four_fu: integer expected";
            if (message.disable_angang_guoshi != null && message.hasOwnProperty("disable_angang_guoshi"))
                if (!$util.isInteger(message.disable_angang_guoshi))
                    return "disable_angang_guoshi: integer expected";
            if (message.enable_renhe != null && message.hasOwnProperty("enable_renhe"))
                if (!$util.isInteger(message.enable_renhe))
                    return "enable_renhe: integer expected";
            if (message.enable_baopai_extend_settings != null && message.hasOwnProperty("enable_baopai_extend_settings"))
                if (!$util.isInteger(message.enable_baopai_extend_settings))
                    return "enable_baopai_extend_settings: integer expected";
            if (message.yongchang_mode != null && message.hasOwnProperty("yongchang_mode"))
                if (!$util.isInteger(message.yongchang_mode))
                    return "yongchang_mode: integer expected";
            if (message.hunzhiyiji_mode != null && message.hasOwnProperty("hunzhiyiji_mode"))
                if (!$util.isInteger(message.hunzhiyiji_mode))
                    return "hunzhiyiji_mode: integer expected";
            if (message.wanxiangxiuluo_mode != null && message.hasOwnProperty("wanxiangxiuluo_mode"))
                if (!$util.isInteger(message.wanxiangxiuluo_mode))
                    return "wanxiangxiuluo_mode: integer expected";
            if (message.beishuizhizhan_mode != null && message.hasOwnProperty("beishuizhizhan_mode"))
                if (!$util.isInteger(message.beishuizhizhan_mode))
                    return "beishuizhizhan_mode: integer expected";
            if (message.amusement_switches != null && message.hasOwnProperty("amusement_switches")) {
                if (!Array.isArray(message.amusement_switches))
                    return "amusement_switches: array expected";
                for (let i = 0; i < message.amusement_switches.length; ++i)
                    if (!$util.isInteger(message.amusement_switches[i]))
                        return "amusement_switches: integer[] expected";
            }
            return null;
        };

        GameDetailRule.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameDetailRule)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameDetailRule();
            if (object.time_fixed != null)
                if (Number(object.time_fixed) !== 0)
                    message.time_fixed = object.time_fixed >>> 0;
            if (object.time_add != null)
                if (Number(object.time_add) !== 0)
                    message.time_add = object.time_add >>> 0;
            if (object.dora_count != null)
                if (Number(object.dora_count) !== 0)
                    message.dora_count = object.dora_count >>> 0;
            if (object.shiduan != null)
                if (Number(object.shiduan) !== 0)
                    message.shiduan = object.shiduan >>> 0;
            if (object.init_point != null)
                if (Number(object.init_point) !== 0)
                    message.init_point = object.init_point >>> 0;
            if (object.fandian != null)
                if (Number(object.fandian) !== 0)
                    message.fandian = object.fandian >>> 0;
            if (object.can_jifei != null)
                if (object.can_jifei)
                    message.can_jifei = Boolean(object.can_jifei);
            if (object.tianbian_value != null)
                if (Number(object.tianbian_value) !== 0)
                    message.tianbian_value = object.tianbian_value >>> 0;
            if (object.liqibang_value != null)
                if (Number(object.liqibang_value) !== 0)
                    message.liqibang_value = object.liqibang_value >>> 0;
            if (object.changbang_value != null)
                if (Number(object.changbang_value) !== 0)
                    message.changbang_value = object.changbang_value >>> 0;
            if (object.noting_fafu_1 != null)
                if (Number(object.noting_fafu_1) !== 0)
                    message.noting_fafu_1 = object.noting_fafu_1 >>> 0;
            if (object.noting_fafu_2 != null)
                if (Number(object.noting_fafu_2) !== 0)
                    message.noting_fafu_2 = object.noting_fafu_2 >>> 0;
            if (object.noting_fafu_3 != null)
                if (Number(object.noting_fafu_3) !== 0)
                    message.noting_fafu_3 = object.noting_fafu_3 >>> 0;
            if (object.have_liujumanguan != null)
                if (object.have_liujumanguan)
                    message.have_liujumanguan = Boolean(object.have_liujumanguan);
            if (object.have_qieshangmanguan != null)
                if (object.have_qieshangmanguan)
                    message.have_qieshangmanguan = Boolean(object.have_qieshangmanguan);
            if (object.have_biao_dora != null)
                if (object.have_biao_dora)
                    message.have_biao_dora = Boolean(object.have_biao_dora);
            if (object.have_gang_biao_dora != null)
                if (object.have_gang_biao_dora)
                    message.have_gang_biao_dora = Boolean(object.have_gang_biao_dora);
            if (object.ming_dora_immediately_open != null)
                if (object.ming_dora_immediately_open)
                    message.ming_dora_immediately_open = Boolean(object.ming_dora_immediately_open);
            if (object.have_li_dora != null)
                if (object.have_li_dora)
                    message.have_li_dora = Boolean(object.have_li_dora);
            if (object.have_gang_li_dora != null)
                if (object.have_gang_li_dora)
                    message.have_gang_li_dora = Boolean(object.have_gang_li_dora);
            if (object.have_sifenglianda != null)
                if (object.have_sifenglianda)
                    message.have_sifenglianda = Boolean(object.have_sifenglianda);
            if (object.have_sigangsanle != null)
                if (object.have_sigangsanle)
                    message.have_sigangsanle = Boolean(object.have_sigangsanle);
            if (object.have_sijializhi != null)
                if (object.have_sijializhi)
                    message.have_sijializhi = Boolean(object.have_sijializhi);
            if (object.have_jiuzhongjiupai != null)
                if (object.have_jiuzhongjiupai)
                    message.have_jiuzhongjiupai = Boolean(object.have_jiuzhongjiupai);
            if (object.have_sanjiahele != null)
                if (object.have_sanjiahele)
                    message.have_sanjiahele = Boolean(object.have_sanjiahele);
            if (object.have_toutiao != null)
                if (object.have_toutiao)
                    message.have_toutiao = Boolean(object.have_toutiao);
            if (object.have_helelianzhuang != null)
                if (object.have_helelianzhuang)
                    message.have_helelianzhuang = Boolean(object.have_helelianzhuang);
            if (object.have_helezhongju != null)
                if (object.have_helezhongju)
                    message.have_helezhongju = Boolean(object.have_helezhongju);
            if (object.have_tingpailianzhuang != null)
                if (object.have_tingpailianzhuang)
                    message.have_tingpailianzhuang = Boolean(object.have_tingpailianzhuang);
            if (object.have_tingpaizhongju != null)
                if (object.have_tingpaizhongju)
                    message.have_tingpaizhongju = Boolean(object.have_tingpaizhongju);
            if (object.have_yifa != null)
                if (object.have_yifa)
                    message.have_yifa = Boolean(object.have_yifa);
            if (object.have_nanruxiru != null)
                if (object.have_nanruxiru)
                    message.have_nanruxiru = Boolean(object.have_nanruxiru);
            if (object.jingsuanyuandian != null)
                if (Number(object.jingsuanyuandian) !== 0)
                    message.jingsuanyuandian = object.jingsuanyuandian >>> 0;
            if (object.shunweima_2 != null)
                if (Number(object.shunweima_2) !== 0)
                    message.shunweima_2 = object.shunweima_2 | 0;
            if (object.shunweima_3 != null)
                if (Number(object.shunweima_3) !== 0)
                    message.shunweima_3 = object.shunweima_3 | 0;
            if (object.shunweima_4 != null)
                if (Number(object.shunweima_4) !== 0)
                    message.shunweima_4 = object.shunweima_4 | 0;
            if (object.bianjietishi != null)
                if (object.bianjietishi)
                    message.bianjietishi = Boolean(object.bianjietishi);
            if (object.ai_level != null)
                if (Number(object.ai_level) !== 0)
                    message.ai_level = object.ai_level >>> 0;
            if (object.have_zimosun != null)
                if (object.have_zimosun)
                    message.have_zimosun = Boolean(object.have_zimosun);
            if (object.disable_multi_yukaman != null)
                if (object.disable_multi_yukaman)
                    message.disable_multi_yukaman = Boolean(object.disable_multi_yukaman);
            if (object.fanfu != null)
                if (Number(object.fanfu) !== 0)
                    message.fanfu = object.fanfu >>> 0;
            if (object.guyi_mode != null)
                if (Number(object.guyi_mode) !== 0)
                    message.guyi_mode = object.guyi_mode >>> 0;
            if (object.dora3_mode != null)
                if (Number(object.dora3_mode) !== 0)
                    message.dora3_mode = object.dora3_mode >>> 0;
            if (object.begin_open_mode != null)
                if (Number(object.begin_open_mode) !== 0)
                    message.begin_open_mode = object.begin_open_mode >>> 0;
            if (object.jiuchao_mode != null)
                if (Number(object.jiuchao_mode) !== 0)
                    message.jiuchao_mode = object.jiuchao_mode >>> 0;
            if (object.muyu_mode != null)
                if (Number(object.muyu_mode) !== 0)
                    message.muyu_mode = object.muyu_mode >>> 0;
            if (object.open_hand != null)
                if (Number(object.open_hand) !== 0)
                    message.open_hand = object.open_hand >>> 0;
            if (object.xuezhandaodi != null)
                if (Number(object.xuezhandaodi) !== 0)
                    message.xuezhandaodi = object.xuezhandaodi >>> 0;
            if (object.huansanzhang != null)
                if (Number(object.huansanzhang) !== 0)
                    message.huansanzhang = object.huansanzhang >>> 0;
            if (object.chuanma != null)
                if (Number(object.chuanma) !== 0)
                    message.chuanma = object.chuanma >>> 0;
            if (object.reveal_discard != null)
                if (Number(object.reveal_discard) !== 0)
                    message.reveal_discard = object.reveal_discard >>> 0;
            if (object.field_spell_mode != null)
                if (Number(object.field_spell_mode) !== 0)
                    message.field_spell_mode = object.field_spell_mode >>> 0;
            if (object.zhanxing != null)
                if (Number(object.zhanxing) !== 0)
                    message.zhanxing = object.zhanxing >>> 0;
            if (object.tianming_mode != null)
                if (Number(object.tianming_mode) !== 0)
                    message.tianming_mode = object.tianming_mode >>> 0;
            if (object.disable_leijiyiman != null)
                if (object.disable_leijiyiman)
                    message.disable_leijiyiman = Boolean(object.disable_leijiyiman);
            if (object.disable_double_yakuman != null)
                if (Number(object.disable_double_yakuman) !== 0)
                    message.disable_double_yakuman = object.disable_double_yakuman >>> 0;
            if (object.disable_composite_yakuman != null)
                if (Number(object.disable_composite_yakuman) !== 0)
                    message.disable_composite_yakuman = object.disable_composite_yakuman >>> 0;
            if (object.enable_shiti != null)
                if (Number(object.enable_shiti) !== 0)
                    message.enable_shiti = object.enable_shiti >>> 0;
            if (object.enable_nontsumo_liqi != null)
                if (Number(object.enable_nontsumo_liqi) !== 0)
                    message.enable_nontsumo_liqi = object.enable_nontsumo_liqi >>> 0;
            if (object.disable_double_wind_four_fu != null)
                if (Number(object.disable_double_wind_four_fu) !== 0)
                    message.disable_double_wind_four_fu = object.disable_double_wind_four_fu >>> 0;
            if (object.disable_angang_guoshi != null)
                if (Number(object.disable_angang_guoshi) !== 0)
                    message.disable_angang_guoshi = object.disable_angang_guoshi >>> 0;
            if (object.enable_renhe != null)
                if (Number(object.enable_renhe) !== 0)
                    message.enable_renhe = object.enable_renhe >>> 0;
            if (object.enable_baopai_extend_settings != null)
                if (Number(object.enable_baopai_extend_settings) !== 0)
                    message.enable_baopai_extend_settings = object.enable_baopai_extend_settings >>> 0;
            if (object.yongchang_mode != null)
                if (Number(object.yongchang_mode) !== 0)
                    message.yongchang_mode = object.yongchang_mode >>> 0;
            if (object.hunzhiyiji_mode != null)
                if (Number(object.hunzhiyiji_mode) !== 0)
                    message.hunzhiyiji_mode = object.hunzhiyiji_mode >>> 0;
            if (object.wanxiangxiuluo_mode != null)
                if (Number(object.wanxiangxiuluo_mode) !== 0)
                    message.wanxiangxiuluo_mode = object.wanxiangxiuluo_mode >>> 0;
            if (object.beishuizhizhan_mode != null)
                if (Number(object.beishuizhizhan_mode) !== 0)
                    message.beishuizhizhan_mode = object.beishuizhizhan_mode >>> 0;
            if (object.amusement_switches) {
                if (!Array.isArray(object.amusement_switches))
                    throw TypeError(".lq.GameDetailRule.amusement_switches: array expected");
                message.amusement_switches = Array(object.amusement_switches.length);
                for (let i = 0; i < object.amusement_switches.length; ++i)
                    message.amusement_switches[i] = object.amusement_switches[i] >>> 0;
            }
            return message;
        };

        GameDetailRule.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.amusement_switches = [];
            if (options.defaults) {
                object.time_fixed = 0;
                object.time_add = 0;
                object.dora_count = 0;
                object.shiduan = 0;
                object.init_point = 0;
                object.fandian = 0;
                object.can_jifei = false;
                object.tianbian_value = 0;
                object.liqibang_value = 0;
                object.changbang_value = 0;
                object.noting_fafu_1 = 0;
                object.noting_fafu_2 = 0;
                object.noting_fafu_3 = 0;
                object.have_liujumanguan = false;
                object.have_qieshangmanguan = false;
                object.have_biao_dora = false;
                object.have_gang_biao_dora = false;
                object.ming_dora_immediately_open = false;
                object.have_li_dora = false;
                object.have_gang_li_dora = false;
                object.have_sifenglianda = false;
                object.have_sigangsanle = false;
                object.have_sijializhi = false;
                object.have_jiuzhongjiupai = false;
                object.have_sanjiahele = false;
                object.have_toutiao = false;
                object.have_helelianzhuang = false;
                object.have_helezhongju = false;
                object.have_tingpailianzhuang = false;
                object.have_tingpaizhongju = false;
                object.have_yifa = false;
                object.have_nanruxiru = false;
                object.jingsuanyuandian = 0;
                object.shunweima_2 = 0;
                object.shunweima_3 = 0;
                object.shunweima_4 = 0;
                object.bianjietishi = false;
                object.ai_level = 0;
                object.have_zimosun = false;
                object.disable_multi_yukaman = false;
                object.fanfu = 0;
                object.guyi_mode = 0;
                object.dora3_mode = 0;
                object.begin_open_mode = 0;
                object.jiuchao_mode = 0;
                object.muyu_mode = 0;
                object.open_hand = 0;
                object.xuezhandaodi = 0;
                object.huansanzhang = 0;
                object.chuanma = 0;
                object.reveal_discard = 0;
                object.field_spell_mode = 0;
                object.zhanxing = 0;
                object.tianming_mode = 0;
                object.disable_leijiyiman = false;
                object.disable_double_yakuman = 0;
                object.disable_composite_yakuman = 0;
                object.enable_shiti = 0;
                object.enable_nontsumo_liqi = 0;
                object.disable_double_wind_four_fu = 0;
                object.disable_angang_guoshi = 0;
                object.enable_renhe = 0;
                object.enable_baopai_extend_settings = 0;
                object.yongchang_mode = 0;
                object.hunzhiyiji_mode = 0;
                object.wanxiangxiuluo_mode = 0;
                object.beishuizhizhan_mode = 0;
            }
            if (message.time_fixed != null && message.hasOwnProperty("time_fixed"))
                object.time_fixed = message.time_fixed;
            if (message.time_add != null && message.hasOwnProperty("time_add"))
                object.time_add = message.time_add;
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                object.dora_count = message.dora_count;
            if (message.shiduan != null && message.hasOwnProperty("shiduan"))
                object.shiduan = message.shiduan;
            if (message.init_point != null && message.hasOwnProperty("init_point"))
                object.init_point = message.init_point;
            if (message.fandian != null && message.hasOwnProperty("fandian"))
                object.fandian = message.fandian;
            if (message.can_jifei != null && message.hasOwnProperty("can_jifei"))
                object.can_jifei = message.can_jifei;
            if (message.tianbian_value != null && message.hasOwnProperty("tianbian_value"))
                object.tianbian_value = message.tianbian_value;
            if (message.liqibang_value != null && message.hasOwnProperty("liqibang_value"))
                object.liqibang_value = message.liqibang_value;
            if (message.changbang_value != null && message.hasOwnProperty("changbang_value"))
                object.changbang_value = message.changbang_value;
            if (message.noting_fafu_1 != null && message.hasOwnProperty("noting_fafu_1"))
                object.noting_fafu_1 = message.noting_fafu_1;
            if (message.noting_fafu_2 != null && message.hasOwnProperty("noting_fafu_2"))
                object.noting_fafu_2 = message.noting_fafu_2;
            if (message.noting_fafu_3 != null && message.hasOwnProperty("noting_fafu_3"))
                object.noting_fafu_3 = message.noting_fafu_3;
            if (message.have_liujumanguan != null && message.hasOwnProperty("have_liujumanguan"))
                object.have_liujumanguan = message.have_liujumanguan;
            if (message.have_qieshangmanguan != null && message.hasOwnProperty("have_qieshangmanguan"))
                object.have_qieshangmanguan = message.have_qieshangmanguan;
            if (message.have_biao_dora != null && message.hasOwnProperty("have_biao_dora"))
                object.have_biao_dora = message.have_biao_dora;
            if (message.have_gang_biao_dora != null && message.hasOwnProperty("have_gang_biao_dora"))
                object.have_gang_biao_dora = message.have_gang_biao_dora;
            if (message.ming_dora_immediately_open != null && message.hasOwnProperty("ming_dora_immediately_open"))
                object.ming_dora_immediately_open = message.ming_dora_immediately_open;
            if (message.have_li_dora != null && message.hasOwnProperty("have_li_dora"))
                object.have_li_dora = message.have_li_dora;
            if (message.have_gang_li_dora != null && message.hasOwnProperty("have_gang_li_dora"))
                object.have_gang_li_dora = message.have_gang_li_dora;
            if (message.have_sifenglianda != null && message.hasOwnProperty("have_sifenglianda"))
                object.have_sifenglianda = message.have_sifenglianda;
            if (message.have_sigangsanle != null && message.hasOwnProperty("have_sigangsanle"))
                object.have_sigangsanle = message.have_sigangsanle;
            if (message.have_sijializhi != null && message.hasOwnProperty("have_sijializhi"))
                object.have_sijializhi = message.have_sijializhi;
            if (message.have_jiuzhongjiupai != null && message.hasOwnProperty("have_jiuzhongjiupai"))
                object.have_jiuzhongjiupai = message.have_jiuzhongjiupai;
            if (message.have_sanjiahele != null && message.hasOwnProperty("have_sanjiahele"))
                object.have_sanjiahele = message.have_sanjiahele;
            if (message.have_toutiao != null && message.hasOwnProperty("have_toutiao"))
                object.have_toutiao = message.have_toutiao;
            if (message.have_helelianzhuang != null && message.hasOwnProperty("have_helelianzhuang"))
                object.have_helelianzhuang = message.have_helelianzhuang;
            if (message.have_helezhongju != null && message.hasOwnProperty("have_helezhongju"))
                object.have_helezhongju = message.have_helezhongju;
            if (message.have_tingpailianzhuang != null && message.hasOwnProperty("have_tingpailianzhuang"))
                object.have_tingpailianzhuang = message.have_tingpailianzhuang;
            if (message.have_tingpaizhongju != null && message.hasOwnProperty("have_tingpaizhongju"))
                object.have_tingpaizhongju = message.have_tingpaizhongju;
            if (message.have_yifa != null && message.hasOwnProperty("have_yifa"))
                object.have_yifa = message.have_yifa;
            if (message.have_nanruxiru != null && message.hasOwnProperty("have_nanruxiru"))
                object.have_nanruxiru = message.have_nanruxiru;
            if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                object.jingsuanyuandian = message.jingsuanyuandian;
            if (message.shunweima_2 != null && message.hasOwnProperty("shunweima_2"))
                object.shunweima_2 = message.shunweima_2;
            if (message.shunweima_3 != null && message.hasOwnProperty("shunweima_3"))
                object.shunweima_3 = message.shunweima_3;
            if (message.shunweima_4 != null && message.hasOwnProperty("shunweima_4"))
                object.shunweima_4 = message.shunweima_4;
            if (message.bianjietishi != null && message.hasOwnProperty("bianjietishi"))
                object.bianjietishi = message.bianjietishi;
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                object.ai_level = message.ai_level;
            if (message.have_zimosun != null && message.hasOwnProperty("have_zimosun"))
                object.have_zimosun = message.have_zimosun;
            if (message.disable_multi_yukaman != null && message.hasOwnProperty("disable_multi_yukaman"))
                object.disable_multi_yukaman = message.disable_multi_yukaman;
            if (message.fanfu != null && message.hasOwnProperty("fanfu"))
                object.fanfu = message.fanfu;
            if (message.guyi_mode != null && message.hasOwnProperty("guyi_mode"))
                object.guyi_mode = message.guyi_mode;
            if (message.dora3_mode != null && message.hasOwnProperty("dora3_mode"))
                object.dora3_mode = message.dora3_mode;
            if (message.begin_open_mode != null && message.hasOwnProperty("begin_open_mode"))
                object.begin_open_mode = message.begin_open_mode;
            if (message.jiuchao_mode != null && message.hasOwnProperty("jiuchao_mode"))
                object.jiuchao_mode = message.jiuchao_mode;
            if (message.muyu_mode != null && message.hasOwnProperty("muyu_mode"))
                object.muyu_mode = message.muyu_mode;
            if (message.open_hand != null && message.hasOwnProperty("open_hand"))
                object.open_hand = message.open_hand;
            if (message.xuezhandaodi != null && message.hasOwnProperty("xuezhandaodi"))
                object.xuezhandaodi = message.xuezhandaodi;
            if (message.huansanzhang != null && message.hasOwnProperty("huansanzhang"))
                object.huansanzhang = message.huansanzhang;
            if (message.chuanma != null && message.hasOwnProperty("chuanma"))
                object.chuanma = message.chuanma;
            if (message.reveal_discard != null && message.hasOwnProperty("reveal_discard"))
                object.reveal_discard = message.reveal_discard;
            if (message.field_spell_mode != null && message.hasOwnProperty("field_spell_mode"))
                object.field_spell_mode = message.field_spell_mode;
            if (message.zhanxing != null && message.hasOwnProperty("zhanxing"))
                object.zhanxing = message.zhanxing;
            if (message.tianming_mode != null && message.hasOwnProperty("tianming_mode"))
                object.tianming_mode = message.tianming_mode;
            if (message.disable_leijiyiman != null && message.hasOwnProperty("disable_leijiyiman"))
                object.disable_leijiyiman = message.disable_leijiyiman;
            if (message.disable_double_yakuman != null && message.hasOwnProperty("disable_double_yakuman"))
                object.disable_double_yakuman = message.disable_double_yakuman;
            if (message.disable_composite_yakuman != null && message.hasOwnProperty("disable_composite_yakuman"))
                object.disable_composite_yakuman = message.disable_composite_yakuman;
            if (message.enable_shiti != null && message.hasOwnProperty("enable_shiti"))
                object.enable_shiti = message.enable_shiti;
            if (message.enable_nontsumo_liqi != null && message.hasOwnProperty("enable_nontsumo_liqi"))
                object.enable_nontsumo_liqi = message.enable_nontsumo_liqi;
            if (message.disable_double_wind_four_fu != null && message.hasOwnProperty("disable_double_wind_four_fu"))
                object.disable_double_wind_four_fu = message.disable_double_wind_four_fu;
            if (message.disable_angang_guoshi != null && message.hasOwnProperty("disable_angang_guoshi"))
                object.disable_angang_guoshi = message.disable_angang_guoshi;
            if (message.enable_renhe != null && message.hasOwnProperty("enable_renhe"))
                object.enable_renhe = message.enable_renhe;
            if (message.enable_baopai_extend_settings != null && message.hasOwnProperty("enable_baopai_extend_settings"))
                object.enable_baopai_extend_settings = message.enable_baopai_extend_settings;
            if (message.yongchang_mode != null && message.hasOwnProperty("yongchang_mode"))
                object.yongchang_mode = message.yongchang_mode;
            if (message.hunzhiyiji_mode != null && message.hasOwnProperty("hunzhiyiji_mode"))
                object.hunzhiyiji_mode = message.hunzhiyiji_mode;
            if (message.wanxiangxiuluo_mode != null && message.hasOwnProperty("wanxiangxiuluo_mode"))
                object.wanxiangxiuluo_mode = message.wanxiangxiuluo_mode;
            if (message.beishuizhizhan_mode != null && message.hasOwnProperty("beishuizhizhan_mode"))
                object.beishuizhizhan_mode = message.beishuizhizhan_mode;
            if (message.amusement_switches && message.amusement_switches.length) {
                object.amusement_switches = Array(message.amusement_switches.length);
                for (let j = 0; j < message.amusement_switches.length; ++j)
                    object.amusement_switches[j] = message.amusement_switches[j];
            }
            return object;
        };

        GameDetailRule.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameDetailRule.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameDetailRule";
        };

        return GameDetailRule;
    })();

    lq.Room = (function() {

        function Room(properties) {
            this.persons = [];
            this.ready_list = [];
            this.robots = [];
            this.positions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Room.prototype.room_id = 0;
        Room.prototype.owner_id = 0;
        Room.prototype.mode = null;
        Room.prototype.max_player_count = 0;
        Room.prototype.persons = $util.emptyArray;
        Room.prototype.ready_list = $util.emptyArray;
        Room.prototype.is_playing = false;
        Room.prototype.public_live = false;
        Room.prototype.robot_count = 0;
        Room.prototype.tournament_id = 0;
        Room.prototype.seq = 0;
        Room.prototype.pre_rule = "";
        Room.prototype.robots = $util.emptyArray;
        Room.prototype.positions = $util.emptyArray;

        Room.create = function create(properties) {
            return new Room(properties);
        };

        Room.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.room_id != null && Object.hasOwnProperty.call(message, "room_id"))
                writer.uint32(8).uint32(message.room_id);
            if (message.owner_id != null && Object.hasOwnProperty.call(message, "owner_id"))
                writer.uint32(16).uint32(message.owner_id);
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                $root.lq.GameMode.encode(message.mode, writer.uint32(26).fork()).ldelim();
            if (message.max_player_count != null && Object.hasOwnProperty.call(message, "max_player_count"))
                writer.uint32(32).uint32(message.max_player_count);
            if (message.persons != null && message.persons.length)
                for (let i = 0; i < message.persons.length; ++i)
                    $root.lq.PlayerGameView.encode(message.persons[i], writer.uint32(42).fork()).ldelim();
            if (message.ready_list != null && message.ready_list.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.ready_list.length; ++i)
                    writer.uint32(message.ready_list[i]);
                writer.ldelim();
            }
            if (message.is_playing != null && Object.hasOwnProperty.call(message, "is_playing"))
                writer.uint32(56).bool(message.is_playing);
            if (message.public_live != null && Object.hasOwnProperty.call(message, "public_live"))
                writer.uint32(64).bool(message.public_live);
            if (message.robot_count != null && Object.hasOwnProperty.call(message, "robot_count"))
                writer.uint32(72).uint32(message.robot_count);
            if (message.tournament_id != null && Object.hasOwnProperty.call(message, "tournament_id"))
                writer.uint32(80).uint32(message.tournament_id);
            if (message.seq != null && Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(88).uint32(message.seq);
            if (message.pre_rule != null && Object.hasOwnProperty.call(message, "pre_rule"))
                writer.uint32(98).string(message.pre_rule);
            if (message.robots != null && message.robots.length)
                for (let i = 0; i < message.robots.length; ++i)
                    $root.lq.PlayerGameView.encode(message.robots[i], writer.uint32(106).fork()).ldelim();
            if (message.positions != null && message.positions.length) {
                writer.uint32(114).fork();
                for (let i = 0; i < message.positions.length; ++i)
                    writer.uint32(message.positions[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Room.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Room.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Room(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.room_id = value;
                        else
                            delete message.room_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.owner_id = value;
                        else
                            delete message.owner_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.mode = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.mode);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_player_count = value;
                        else
                            delete message.max_player_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.persons && message.persons.length))
                            message.persons = [];
                        message.persons.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.ready_list && message.ready_list.length))
                                message.ready_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ready_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.ready_list && message.ready_list.length))
                            message.ready_list = [];
                        message.ready_list.push(reader.uint32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_playing = value;
                        else
                            delete message.is_playing;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.public_live = value;
                        else
                            delete message.public_live;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.robot_count = value;
                        else
                            delete message.robot_count;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tournament_id = value;
                        else
                            delete message.tournament_id;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seq = value;
                        else
                            delete message.seq;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.pre_rule = value;
                        else
                            delete message.pre_rule;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.robots && message.robots.length))
                            message.robots = [];
                        message.robots.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType === 2) {
                            if (!(message.positions && message.positions.length))
                                message.positions = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.positions.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.positions && message.positions.length))
                            message.positions = [];
                        message.positions.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Room.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Room.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                if (!$util.isInteger(message.room_id))
                    return "room_id: integer expected";
            if (message.owner_id != null && message.hasOwnProperty("owner_id"))
                if (!$util.isInteger(message.owner_id))
                    return "owner_id: integer expected";
            if (message.mode != null && message.hasOwnProperty("mode")) {
                let error = $root.lq.GameMode.verify(message.mode, _depth + 1);
                if (error)
                    return "mode." + error;
            }
            if (message.max_player_count != null && message.hasOwnProperty("max_player_count"))
                if (!$util.isInteger(message.max_player_count))
                    return "max_player_count: integer expected";
            if (message.persons != null && message.hasOwnProperty("persons")) {
                if (!Array.isArray(message.persons))
                    return "persons: array expected";
                for (let i = 0; i < message.persons.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.persons[i], _depth + 1);
                    if (error)
                        return "persons." + error;
                }
            }
            if (message.ready_list != null && message.hasOwnProperty("ready_list")) {
                if (!Array.isArray(message.ready_list))
                    return "ready_list: array expected";
                for (let i = 0; i < message.ready_list.length; ++i)
                    if (!$util.isInteger(message.ready_list[i]))
                        return "ready_list: integer[] expected";
            }
            if (message.is_playing != null && message.hasOwnProperty("is_playing"))
                if (typeof message.is_playing !== "boolean")
                    return "is_playing: boolean expected";
            if (message.public_live != null && message.hasOwnProperty("public_live"))
                if (typeof message.public_live !== "boolean")
                    return "public_live: boolean expected";
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                if (!$util.isInteger(message.robot_count))
                    return "robot_count: integer expected";
            if (message.tournament_id != null && message.hasOwnProperty("tournament_id"))
                if (!$util.isInteger(message.tournament_id))
                    return "tournament_id: integer expected";
            if (message.seq != null && message.hasOwnProperty("seq"))
                if (!$util.isInteger(message.seq))
                    return "seq: integer expected";
            if (message.pre_rule != null && message.hasOwnProperty("pre_rule"))
                if (!$util.isString(message.pre_rule))
                    return "pre_rule: string expected";
            if (message.robots != null && message.hasOwnProperty("robots")) {
                if (!Array.isArray(message.robots))
                    return "robots: array expected";
                for (let i = 0; i < message.robots.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.robots[i], _depth + 1);
                    if (error)
                        return "robots." + error;
                }
            }
            if (message.positions != null && message.hasOwnProperty("positions")) {
                if (!Array.isArray(message.positions))
                    return "positions: array expected";
                for (let i = 0; i < message.positions.length; ++i)
                    if (!$util.isInteger(message.positions[i]))
                        return "positions: integer[] expected";
            }
            return null;
        };

        Room.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Room)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Room();
            if (object.room_id != null)
                if (Number(object.room_id) !== 0)
                    message.room_id = object.room_id >>> 0;
            if (object.owner_id != null)
                if (Number(object.owner_id) !== 0)
                    message.owner_id = object.owner_id >>> 0;
            if (object.mode != null) {
                if (typeof object.mode !== "object")
                    throw TypeError(".lq.Room.mode: object expected");
                message.mode = $root.lq.GameMode.fromObject(object.mode, _depth + 1);
            }
            if (object.max_player_count != null)
                if (Number(object.max_player_count) !== 0)
                    message.max_player_count = object.max_player_count >>> 0;
            if (object.persons) {
                if (!Array.isArray(object.persons))
                    throw TypeError(".lq.Room.persons: array expected");
                message.persons = Array(object.persons.length);
                for (let i = 0; i < object.persons.length; ++i) {
                    if (typeof object.persons[i] !== "object")
                        throw TypeError(".lq.Room.persons: object expected");
                    message.persons[i] = $root.lq.PlayerGameView.fromObject(object.persons[i], _depth + 1);
                }
            }
            if (object.ready_list) {
                if (!Array.isArray(object.ready_list))
                    throw TypeError(".lq.Room.ready_list: array expected");
                message.ready_list = Array(object.ready_list.length);
                for (let i = 0; i < object.ready_list.length; ++i)
                    message.ready_list[i] = object.ready_list[i] >>> 0;
            }
            if (object.is_playing != null)
                if (object.is_playing)
                    message.is_playing = Boolean(object.is_playing);
            if (object.public_live != null)
                if (object.public_live)
                    message.public_live = Boolean(object.public_live);
            if (object.robot_count != null)
                if (Number(object.robot_count) !== 0)
                    message.robot_count = object.robot_count >>> 0;
            if (object.tournament_id != null)
                if (Number(object.tournament_id) !== 0)
                    message.tournament_id = object.tournament_id >>> 0;
            if (object.seq != null)
                if (Number(object.seq) !== 0)
                    message.seq = object.seq >>> 0;
            if (object.pre_rule != null)
                if (typeof object.pre_rule !== "string" || object.pre_rule.length)
                    message.pre_rule = String(object.pre_rule);
            if (object.robots) {
                if (!Array.isArray(object.robots))
                    throw TypeError(".lq.Room.robots: array expected");
                message.robots = Array(object.robots.length);
                for (let i = 0; i < object.robots.length; ++i) {
                    if (typeof object.robots[i] !== "object")
                        throw TypeError(".lq.Room.robots: object expected");
                    message.robots[i] = $root.lq.PlayerGameView.fromObject(object.robots[i], _depth + 1);
                }
            }
            if (object.positions) {
                if (!Array.isArray(object.positions))
                    throw TypeError(".lq.Room.positions: array expected");
                message.positions = Array(object.positions.length);
                for (let i = 0; i < object.positions.length; ++i)
                    message.positions[i] = object.positions[i] >>> 0;
            }
            return message;
        };

        Room.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.persons = [];
                object.ready_list = [];
                object.robots = [];
                object.positions = [];
            }
            if (options.defaults) {
                object.room_id = 0;
                object.owner_id = 0;
                object.mode = null;
                object.max_player_count = 0;
                object.is_playing = false;
                object.public_live = false;
                object.robot_count = 0;
                object.tournament_id = 0;
                object.seq = 0;
                object.pre_rule = "";
            }
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                object.room_id = message.room_id;
            if (message.owner_id != null && message.hasOwnProperty("owner_id"))
                object.owner_id = message.owner_id;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = $root.lq.GameMode.toObject(message.mode, options);
            if (message.max_player_count != null && message.hasOwnProperty("max_player_count"))
                object.max_player_count = message.max_player_count;
            if (message.persons && message.persons.length) {
                object.persons = Array(message.persons.length);
                for (let j = 0; j < message.persons.length; ++j)
                    object.persons[j] = $root.lq.PlayerGameView.toObject(message.persons[j], options);
            }
            if (message.ready_list && message.ready_list.length) {
                object.ready_list = Array(message.ready_list.length);
                for (let j = 0; j < message.ready_list.length; ++j)
                    object.ready_list[j] = message.ready_list[j];
            }
            if (message.is_playing != null && message.hasOwnProperty("is_playing"))
                object.is_playing = message.is_playing;
            if (message.public_live != null && message.hasOwnProperty("public_live"))
                object.public_live = message.public_live;
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                object.robot_count = message.robot_count;
            if (message.tournament_id != null && message.hasOwnProperty("tournament_id"))
                object.tournament_id = message.tournament_id;
            if (message.seq != null && message.hasOwnProperty("seq"))
                object.seq = message.seq;
            if (message.pre_rule != null && message.hasOwnProperty("pre_rule"))
                object.pre_rule = message.pre_rule;
            if (message.robots && message.robots.length) {
                object.robots = Array(message.robots.length);
                for (let j = 0; j < message.robots.length; ++j)
                    object.robots[j] = $root.lq.PlayerGameView.toObject(message.robots[j], options);
            }
            if (message.positions && message.positions.length) {
                object.positions = Array(message.positions.length);
                for (let j = 0; j < message.positions.length; ++j)
                    object.positions[j] = message.positions[j];
            }
            return object;
        };

        Room.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Room.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Room";
        };

        return Room;
    })();

    lq.GameEndResult = (function() {

        function GameEndResult(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameEndResult.prototype.players = $util.emptyArray;

        GameEndResult.create = function create(properties) {
            return new GameEndResult(properties);
        };

        GameEndResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.GameEndResult.PlayerItem.encode(message.players[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameEndResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameEndResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameEndResult();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.GameEndResult.PlayerItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameEndResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameEndResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.GameEndResult.PlayerItem.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        GameEndResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameEndResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameEndResult();
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.GameEndResult.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.GameEndResult.players: object expected");
                    message.players[i] = $root.lq.GameEndResult.PlayerItem.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        GameEndResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.GameEndResult.PlayerItem.toObject(message.players[j], options);
            }
            return object;
        };

        GameEndResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameEndResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameEndResult";
        };

        GameEndResult.PlayerItem = (function() {

            function PlayerItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PlayerItem.prototype.seat = 0;
            PlayerItem.prototype.total_point = 0;
            PlayerItem.prototype.part_point_1 = 0;
            PlayerItem.prototype.part_point_2 = 0;
            PlayerItem.prototype.grading_score = 0;
            PlayerItem.prototype.gold = 0;

            PlayerItem.create = function create(properties) {
                return new PlayerItem(properties);
            };

            PlayerItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.total_point != null && Object.hasOwnProperty.call(message, "total_point"))
                    writer.uint32(16).int32(message.total_point);
                if (message.part_point_1 != null && Object.hasOwnProperty.call(message, "part_point_1"))
                    writer.uint32(24).int32(message.part_point_1);
                if (message.part_point_2 != null && Object.hasOwnProperty.call(message, "part_point_2"))
                    writer.uint32(32).int32(message.part_point_2);
                if (message.grading_score != null && Object.hasOwnProperty.call(message, "grading_score"))
                    writer.uint32(40).int32(message.grading_score);
                if (message.gold != null && Object.hasOwnProperty.call(message, "gold"))
                    writer.uint32(48).int32(message.gold);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PlayerItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PlayerItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameEndResult.PlayerItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.total_point = value;
                            else
                                delete message.total_point;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.part_point_1 = value;
                            else
                                delete message.part_point_1;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.part_point_2 = value;
                            else
                                delete message.part_point_2;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.grading_score = value;
                            else
                                delete message.grading_score;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.gold = value;
                            else
                                delete message.gold;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PlayerItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PlayerItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    if (!$util.isInteger(message.total_point))
                        return "total_point: integer expected";
                if (message.part_point_1 != null && message.hasOwnProperty("part_point_1"))
                    if (!$util.isInteger(message.part_point_1))
                        return "part_point_1: integer expected";
                if (message.part_point_2 != null && message.hasOwnProperty("part_point_2"))
                    if (!$util.isInteger(message.part_point_2))
                        return "part_point_2: integer expected";
                if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                    if (!$util.isInteger(message.grading_score))
                        return "grading_score: integer expected";
                if (message.gold != null && message.hasOwnProperty("gold"))
                    if (!$util.isInteger(message.gold))
                        return "gold: integer expected";
                return null;
            };

            PlayerItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameEndResult.PlayerItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameEndResult.PlayerItem();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.total_point != null)
                    if (Number(object.total_point) !== 0)
                        message.total_point = object.total_point | 0;
                if (object.part_point_1 != null)
                    if (Number(object.part_point_1) !== 0)
                        message.part_point_1 = object.part_point_1 | 0;
                if (object.part_point_2 != null)
                    if (Number(object.part_point_2) !== 0)
                        message.part_point_2 = object.part_point_2 | 0;
                if (object.grading_score != null)
                    if (Number(object.grading_score) !== 0)
                        message.grading_score = object.grading_score | 0;
                if (object.gold != null)
                    if (Number(object.gold) !== 0)
                        message.gold = object.gold | 0;
                return message;
            };

            PlayerItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.total_point = 0;
                    object.part_point_1 = 0;
                    object.part_point_2 = 0;
                    object.grading_score = 0;
                    object.gold = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    object.total_point = message.total_point;
                if (message.part_point_1 != null && message.hasOwnProperty("part_point_1"))
                    object.part_point_1 = message.part_point_1;
                if (message.part_point_2 != null && message.hasOwnProperty("part_point_2"))
                    object.part_point_2 = message.part_point_2;
                if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                    object.grading_score = message.grading_score;
                if (message.gold != null && message.hasOwnProperty("gold"))
                    object.gold = message.gold;
                return object;
            };

            PlayerItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlayerItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameEndResult.PlayerItem";
            };

            return PlayerItem;
        })();

        return GameEndResult;
    })();

    lq.GameConnectInfo = (function() {

        function GameConnectInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameConnectInfo.prototype.connect_token = "";
        GameConnectInfo.prototype.game_uuid = "";
        GameConnectInfo.prototype.location = "";

        GameConnectInfo.create = function create(properties) {
            return new GameConnectInfo(properties);
        };

        GameConnectInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.connect_token != null && Object.hasOwnProperty.call(message, "connect_token"))
                writer.uint32(18).string(message.connect_token);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(26).string(message.game_uuid);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(34).string(message.location);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameConnectInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameConnectInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameConnectInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.connect_token = value;
                        else
                            delete message.connect_token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.location = value;
                        else
                            delete message.location;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameConnectInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameConnectInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                if (!$util.isString(message.connect_token))
                    return "connect_token: string expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        GameConnectInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameConnectInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameConnectInfo();
            if (object.connect_token != null)
                if (typeof object.connect_token !== "string" || object.connect_token.length)
                    message.connect_token = String(object.connect_token);
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.location != null)
                if (typeof object.location !== "string" || object.location.length)
                    message.location = String(object.location);
            return message;
        };

        GameConnectInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.connect_token = "";
                object.game_uuid = "";
                object.location = "";
            }
            if (message.connect_token != null && message.hasOwnProperty("connect_token"))
                object.connect_token = message.connect_token;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            return object;
        };

        GameConnectInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameConnectInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameConnectInfo";
        };

        return GameConnectInfo;
    })();

    lq.ItemGainRecord = (function() {

        function ItemGainRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ItemGainRecord.prototype.item_id = 0;
        ItemGainRecord.prototype.count = 0;

        ItemGainRecord.create = function create(properties) {
            return new ItemGainRecord(properties);
        };

        ItemGainRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ItemGainRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ItemGainRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ItemGainRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ItemGainRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ItemGainRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ItemGainRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ItemGainRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ItemGainRecord();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ItemGainRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.item_id = 0;
                object.count = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ItemGainRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ItemGainRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ItemGainRecord";
        };

        return ItemGainRecord;
    })();

    lq.ItemGainRecords = (function() {

        function ItemGainRecords(properties) {
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ItemGainRecords.prototype.record_time = 0;
        ItemGainRecords.prototype.limit_source_id = 0;
        ItemGainRecords.prototype.records = $util.emptyArray;

        ItemGainRecords.create = function create(properties) {
            return new ItemGainRecords(properties);
        };

        ItemGainRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.record_time != null && Object.hasOwnProperty.call(message, "record_time"))
                writer.uint32(8).uint32(message.record_time);
            if (message.limit_source_id != null && Object.hasOwnProperty.call(message, "limit_source_id"))
                writer.uint32(16).uint32(message.limit_source_id);
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.lq.ItemGainRecord.encode(message.records[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ItemGainRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ItemGainRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ItemGainRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.record_time = value;
                        else
                            delete message.record_time;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.limit_source_id = value;
                        else
                            delete message.limit_source_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.lq.ItemGainRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ItemGainRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ItemGainRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.record_time != null && message.hasOwnProperty("record_time"))
                if (!$util.isInteger(message.record_time))
                    return "record_time: integer expected";
            if (message.limit_source_id != null && message.hasOwnProperty("limit_source_id"))
                if (!$util.isInteger(message.limit_source_id))
                    return "limit_source_id: integer expected";
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.lq.ItemGainRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        ItemGainRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ItemGainRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ItemGainRecords();
            if (object.record_time != null)
                if (Number(object.record_time) !== 0)
                    message.record_time = object.record_time >>> 0;
            if (object.limit_source_id != null)
                if (Number(object.limit_source_id) !== 0)
                    message.limit_source_id = object.limit_source_id >>> 0;
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.ItemGainRecords.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".lq.ItemGainRecords.records: object expected");
                    message.records[i] = $root.lq.ItemGainRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            return message;
        };

        ItemGainRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults) {
                object.record_time = 0;
                object.limit_source_id = 0;
            }
            if (message.record_time != null && message.hasOwnProperty("record_time"))
                object.record_time = message.record_time;
            if (message.limit_source_id != null && message.hasOwnProperty("limit_source_id"))
                object.limit_source_id = message.limit_source_id;
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.lq.ItemGainRecord.toObject(message.records[j], options);
            }
            return object;
        };

        ItemGainRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ItemGainRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ItemGainRecords";
        };

        return ItemGainRecords;
    })();

    lq.FakeRandomRecords = (function() {

        function FakeRandomRecords(properties) {
            this.gain_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FakeRandomRecords.prototype.item_id = 0;
        FakeRandomRecords.prototype.special_item_id = 0;
        FakeRandomRecords.prototype.gain_count = 0;
        FakeRandomRecords.prototype.gain_history = $util.emptyArray;

        FakeRandomRecords.create = function create(properties) {
            return new FakeRandomRecords(properties);
        };

        FakeRandomRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.special_item_id != null && Object.hasOwnProperty.call(message, "special_item_id"))
                writer.uint32(16).uint32(message.special_item_id);
            if (message.gain_count != null && Object.hasOwnProperty.call(message, "gain_count"))
                writer.uint32(24).uint32(message.gain_count);
            if (message.gain_history != null && message.gain_history.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.gain_history.length; ++i)
                    writer.uint32(message.gain_history[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FakeRandomRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FakeRandomRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FakeRandomRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.special_item_id = value;
                        else
                            delete message.special_item_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gain_count = value;
                        else
                            delete message.gain_count;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.gain_history && message.gain_history.length))
                                message.gain_history = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gain_history.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.gain_history && message.gain_history.length))
                            message.gain_history = [];
                        message.gain_history.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FakeRandomRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FakeRandomRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.special_item_id != null && message.hasOwnProperty("special_item_id"))
                if (!$util.isInteger(message.special_item_id))
                    return "special_item_id: integer expected";
            if (message.gain_count != null && message.hasOwnProperty("gain_count"))
                if (!$util.isInteger(message.gain_count))
                    return "gain_count: integer expected";
            if (message.gain_history != null && message.hasOwnProperty("gain_history")) {
                if (!Array.isArray(message.gain_history))
                    return "gain_history: array expected";
                for (let i = 0; i < message.gain_history.length; ++i)
                    if (!$util.isInteger(message.gain_history[i]))
                        return "gain_history: integer[] expected";
            }
            return null;
        };

        FakeRandomRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FakeRandomRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FakeRandomRecords();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.special_item_id != null)
                if (Number(object.special_item_id) !== 0)
                    message.special_item_id = object.special_item_id >>> 0;
            if (object.gain_count != null)
                if (Number(object.gain_count) !== 0)
                    message.gain_count = object.gain_count >>> 0;
            if (object.gain_history) {
                if (!Array.isArray(object.gain_history))
                    throw TypeError(".lq.FakeRandomRecords.gain_history: array expected");
                message.gain_history = Array(object.gain_history.length);
                for (let i = 0; i < object.gain_history.length; ++i)
                    message.gain_history[i] = object.gain_history[i] >>> 0;
            }
            return message;
        };

        FakeRandomRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gain_history = [];
            if (options.defaults) {
                object.item_id = 0;
                object.special_item_id = 0;
                object.gain_count = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.special_item_id != null && message.hasOwnProperty("special_item_id"))
                object.special_item_id = message.special_item_id;
            if (message.gain_count != null && message.hasOwnProperty("gain_count"))
                object.gain_count = message.gain_count;
            if (message.gain_history && message.gain_history.length) {
                object.gain_history = Array(message.gain_history.length);
                for (let j = 0; j < message.gain_history.length; ++j)
                    object.gain_history[j] = message.gain_history[j];
            }
            return object;
        };

        FakeRandomRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FakeRandomRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FakeRandomRecords";
        };

        return FakeRandomRecords;
    })();

    lq.Item = (function() {

        function Item(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Item.prototype.item_id = 0;
        Item.prototype.stack = 0;

        Item.create = function create(properties) {
            return new Item(properties);
        };

        Item.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.stack != null && Object.hasOwnProperty.call(message, "stack"))
                writer.uint32(16).uint32(message.stack);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Item.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Item.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Item(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stack = value;
                        else
                            delete message.stack;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Item.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Item.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.stack != null && message.hasOwnProperty("stack"))
                if (!$util.isInteger(message.stack))
                    return "stack: integer expected";
            return null;
        };

        Item.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Item)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Item();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.stack != null)
                if (Number(object.stack) !== 0)
                    message.stack = object.stack >>> 0;
            return message;
        };

        Item.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.item_id = 0;
                object.stack = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.stack != null && message.hasOwnProperty("stack"))
                object.stack = message.stack;
            return object;
        };

        Item.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Item.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Item";
        };

        return Item;
    })();

    lq.Bag = (function() {

        function Bag(properties) {
            this.items = [];
            this.daily_gain_record = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Bag.prototype.items = $util.emptyArray;
        Bag.prototype.daily_gain_record = $util.emptyArray;

        Bag.create = function create(properties) {
            return new Bag(properties);
        };

        Bag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.Item.encode(message.items[i], writer.uint32(10).fork()).ldelim();
            if (message.daily_gain_record != null && message.daily_gain_record.length)
                for (let i = 0; i < message.daily_gain_record.length; ++i)
                    $root.lq.ItemGainRecords.encode(message.daily_gain_record[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Bag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Bag.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Bag();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.daily_gain_record && message.daily_gain_record.length))
                            message.daily_gain_record = [];
                        message.daily_gain_record.push($root.lq.ItemGainRecords.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Bag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Bag.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.daily_gain_record != null && message.hasOwnProperty("daily_gain_record")) {
                if (!Array.isArray(message.daily_gain_record))
                    return "daily_gain_record: array expected";
                for (let i = 0; i < message.daily_gain_record.length; ++i) {
                    let error = $root.lq.ItemGainRecords.verify(message.daily_gain_record[i], _depth + 1);
                    if (error)
                        return "daily_gain_record." + error;
                }
            }
            return null;
        };

        Bag.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Bag)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Bag();
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.Bag.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.Bag.items: object expected");
                    message.items[i] = $root.lq.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.daily_gain_record) {
                if (!Array.isArray(object.daily_gain_record))
                    throw TypeError(".lq.Bag.daily_gain_record: array expected");
                message.daily_gain_record = Array(object.daily_gain_record.length);
                for (let i = 0; i < object.daily_gain_record.length; ++i) {
                    if (typeof object.daily_gain_record[i] !== "object")
                        throw TypeError(".lq.Bag.daily_gain_record: object expected");
                    message.daily_gain_record[i] = $root.lq.ItemGainRecords.fromObject(object.daily_gain_record[i], _depth + 1);
                }
            }
            return message;
        };

        Bag.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.items = [];
                object.daily_gain_record = [];
            }
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.Item.toObject(message.items[j], options);
            }
            if (message.daily_gain_record && message.daily_gain_record.length) {
                object.daily_gain_record = Array(message.daily_gain_record.length);
                for (let j = 0; j < message.daily_gain_record.length; ++j)
                    object.daily_gain_record[j] = $root.lq.ItemGainRecords.toObject(message.daily_gain_record[j], options);
            }
            return object;
        };

        Bag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Bag.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Bag";
        };

        return Bag;
    })();

    lq.BagUpdate = (function() {

        function BagUpdate(properties) {
            this.update_items = [];
            this.update_daily_gain_record = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BagUpdate.prototype.update_items = $util.emptyArray;
        BagUpdate.prototype.update_daily_gain_record = $util.emptyArray;

        BagUpdate.create = function create(properties) {
            return new BagUpdate(properties);
        };

        BagUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.update_items != null && message.update_items.length)
                for (let i = 0; i < message.update_items.length; ++i)
                    $root.lq.Item.encode(message.update_items[i], writer.uint32(10).fork()).ldelim();
            if (message.update_daily_gain_record != null && message.update_daily_gain_record.length)
                for (let i = 0; i < message.update_daily_gain_record.length; ++i)
                    $root.lq.ItemGainRecords.encode(message.update_daily_gain_record[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BagUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BagUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BagUpdate();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.update_items && message.update_items.length))
                            message.update_items = [];
                        message.update_items.push($root.lq.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.update_daily_gain_record && message.update_daily_gain_record.length))
                            message.update_daily_gain_record = [];
                        message.update_daily_gain_record.push($root.lq.ItemGainRecords.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BagUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BagUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.update_items != null && message.hasOwnProperty("update_items")) {
                if (!Array.isArray(message.update_items))
                    return "update_items: array expected";
                for (let i = 0; i < message.update_items.length; ++i) {
                    let error = $root.lq.Item.verify(message.update_items[i], _depth + 1);
                    if (error)
                        return "update_items." + error;
                }
            }
            if (message.update_daily_gain_record != null && message.hasOwnProperty("update_daily_gain_record")) {
                if (!Array.isArray(message.update_daily_gain_record))
                    return "update_daily_gain_record: array expected";
                for (let i = 0; i < message.update_daily_gain_record.length; ++i) {
                    let error = $root.lq.ItemGainRecords.verify(message.update_daily_gain_record[i], _depth + 1);
                    if (error)
                        return "update_daily_gain_record." + error;
                }
            }
            return null;
        };

        BagUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BagUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BagUpdate();
            if (object.update_items) {
                if (!Array.isArray(object.update_items))
                    throw TypeError(".lq.BagUpdate.update_items: array expected");
                message.update_items = Array(object.update_items.length);
                for (let i = 0; i < object.update_items.length; ++i) {
                    if (typeof object.update_items[i] !== "object")
                        throw TypeError(".lq.BagUpdate.update_items: object expected");
                    message.update_items[i] = $root.lq.Item.fromObject(object.update_items[i], _depth + 1);
                }
            }
            if (object.update_daily_gain_record) {
                if (!Array.isArray(object.update_daily_gain_record))
                    throw TypeError(".lq.BagUpdate.update_daily_gain_record: array expected");
                message.update_daily_gain_record = Array(object.update_daily_gain_record.length);
                for (let i = 0; i < object.update_daily_gain_record.length; ++i) {
                    if (typeof object.update_daily_gain_record[i] !== "object")
                        throw TypeError(".lq.BagUpdate.update_daily_gain_record: object expected");
                    message.update_daily_gain_record[i] = $root.lq.ItemGainRecords.fromObject(object.update_daily_gain_record[i], _depth + 1);
                }
            }
            return message;
        };

        BagUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.update_items = [];
                object.update_daily_gain_record = [];
            }
            if (message.update_items && message.update_items.length) {
                object.update_items = Array(message.update_items.length);
                for (let j = 0; j < message.update_items.length; ++j)
                    object.update_items[j] = $root.lq.Item.toObject(message.update_items[j], options);
            }
            if (message.update_daily_gain_record && message.update_daily_gain_record.length) {
                object.update_daily_gain_record = Array(message.update_daily_gain_record.length);
                for (let j = 0; j < message.update_daily_gain_record.length; ++j)
                    object.update_daily_gain_record[j] = $root.lq.ItemGainRecords.toObject(message.update_daily_gain_record[j], options);
            }
            return object;
        };

        BagUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BagUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BagUpdate";
        };

        return BagUpdate;
    })();

    lq.RewardSlot = (function() {

        function RewardSlot(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RewardSlot.prototype.id = 0;
        RewardSlot.prototype.count = 0;

        RewardSlot.create = function create(properties) {
            return new RewardSlot(properties);
        };

        RewardSlot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RewardSlot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RewardSlot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RewardSlot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RewardSlot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RewardSlot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        RewardSlot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RewardSlot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RewardSlot();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        RewardSlot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        RewardSlot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RewardSlot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RewardSlot";
        };

        return RewardSlot;
    })();

    lq.OpenResult = (function() {

        function OpenResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        OpenResult.prototype.reward = null;
        OpenResult.prototype.replace = null;

        OpenResult.create = function create(properties) {
            return new OpenResult(properties);
        };

        OpenResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reward != null && Object.hasOwnProperty.call(message, "reward"))
                $root.lq.RewardSlot.encode(message.reward, writer.uint32(10).fork()).ldelim();
            if (message.replace != null && Object.hasOwnProperty.call(message, "replace"))
                $root.lq.RewardSlot.encode(message.replace, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OpenResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        OpenResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.OpenResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.reward = $root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1, message.reward);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.replace = $root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1, message.replace);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        OpenResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        OpenResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.reward != null && message.hasOwnProperty("reward")) {
                let error = $root.lq.RewardSlot.verify(message.reward, _depth + 1);
                if (error)
                    return "reward." + error;
            }
            if (message.replace != null && message.hasOwnProperty("replace")) {
                let error = $root.lq.RewardSlot.verify(message.replace, _depth + 1);
                if (error)
                    return "replace." + error;
            }
            return null;
        };

        OpenResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.OpenResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.OpenResult();
            if (object.reward != null) {
                if (typeof object.reward !== "object")
                    throw TypeError(".lq.OpenResult.reward: object expected");
                message.reward = $root.lq.RewardSlot.fromObject(object.reward, _depth + 1);
            }
            if (object.replace != null) {
                if (typeof object.replace !== "object")
                    throw TypeError(".lq.OpenResult.replace: object expected");
                message.replace = $root.lq.RewardSlot.fromObject(object.replace, _depth + 1);
            }
            return message;
        };

        OpenResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.reward = null;
                object.replace = null;
            }
            if (message.reward != null && message.hasOwnProperty("reward"))
                object.reward = $root.lq.RewardSlot.toObject(message.reward, options);
            if (message.replace != null && message.hasOwnProperty("replace"))
                object.replace = $root.lq.RewardSlot.toObject(message.replace, options);
            return object;
        };

        OpenResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        OpenResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.OpenResult";
        };

        return OpenResult;
    })();

    lq.RewardPlusResult = (function() {

        function RewardPlusResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RewardPlusResult.prototype.id = 0;
        RewardPlusResult.prototype.count = 0;
        RewardPlusResult.prototype.exchange = null;

        RewardPlusResult.create = function create(properties) {
            return new RewardPlusResult(properties);
        };

        RewardPlusResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.exchange != null && Object.hasOwnProperty.call(message, "exchange"))
                $root.lq.RewardPlusResult.Exchange.encode(message.exchange, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RewardPlusResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RewardPlusResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RewardPlusResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.exchange = $root.lq.RewardPlusResult.Exchange.decode(reader, reader.uint32(), undefined, _depth + 1, message.exchange);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RewardPlusResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RewardPlusResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.exchange != null && message.hasOwnProperty("exchange")) {
                let error = $root.lq.RewardPlusResult.Exchange.verify(message.exchange, _depth + 1);
                if (error)
                    return "exchange." + error;
            }
            return null;
        };

        RewardPlusResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RewardPlusResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RewardPlusResult();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.exchange != null) {
                if (typeof object.exchange !== "object")
                    throw TypeError(".lq.RewardPlusResult.exchange: object expected");
                message.exchange = $root.lq.RewardPlusResult.Exchange.fromObject(object.exchange, _depth + 1);
            }
            return message;
        };

        RewardPlusResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
                object.exchange = null;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.exchange != null && message.hasOwnProperty("exchange"))
                object.exchange = $root.lq.RewardPlusResult.Exchange.toObject(message.exchange, options);
            return object;
        };

        RewardPlusResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RewardPlusResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RewardPlusResult";
        };

        RewardPlusResult.Exchange = (function() {

            function Exchange(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Exchange.prototype.id = 0;
            Exchange.prototype.count = 0;
            Exchange.prototype.exchange = 0;

            Exchange.create = function create(properties) {
                return new Exchange(properties);
            };

            Exchange.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.exchange != null && Object.hasOwnProperty.call(message, "exchange"))
                    writer.uint32(24).uint32(message.exchange);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Exchange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Exchange.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RewardPlusResult.Exchange(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.exchange = value;
                            else
                                delete message.exchange;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Exchange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Exchange.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.exchange != null && message.hasOwnProperty("exchange"))
                    if (!$util.isInteger(message.exchange))
                        return "exchange: integer expected";
                return null;
            };

            Exchange.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RewardPlusResult.Exchange)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RewardPlusResult.Exchange();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.exchange != null)
                    if (Number(object.exchange) !== 0)
                        message.exchange = object.exchange >>> 0;
                return message;
            };

            Exchange.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                    object.exchange = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.exchange != null && message.hasOwnProperty("exchange"))
                    object.exchange = message.exchange;
                return object;
            };

            Exchange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Exchange.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RewardPlusResult.Exchange";
            };

            return Exchange;
        })();

        return RewardPlusResult;
    })();

    lq.ExecuteReward = (function() {

        function ExecuteReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ExecuteReward.prototype.reward = null;
        ExecuteReward.prototype.replace = null;
        ExecuteReward.prototype.replace_count = 0;

        ExecuteReward.create = function create(properties) {
            return new ExecuteReward(properties);
        };

        ExecuteReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reward != null && Object.hasOwnProperty.call(message, "reward"))
                $root.lq.RewardSlot.encode(message.reward, writer.uint32(10).fork()).ldelim();
            if (message.replace != null && Object.hasOwnProperty.call(message, "replace"))
                $root.lq.RewardSlot.encode(message.replace, writer.uint32(18).fork()).ldelim();
            if (message.replace_count != null && Object.hasOwnProperty.call(message, "replace_count"))
                writer.uint32(24).uint32(message.replace_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ExecuteReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ExecuteReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ExecuteReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.reward = $root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1, message.reward);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.replace = $root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1, message.replace);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.replace_count = value;
                        else
                            delete message.replace_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ExecuteReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ExecuteReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.reward != null && message.hasOwnProperty("reward")) {
                let error = $root.lq.RewardSlot.verify(message.reward, _depth + 1);
                if (error)
                    return "reward." + error;
            }
            if (message.replace != null && message.hasOwnProperty("replace")) {
                let error = $root.lq.RewardSlot.verify(message.replace, _depth + 1);
                if (error)
                    return "replace." + error;
            }
            if (message.replace_count != null && message.hasOwnProperty("replace_count"))
                if (!$util.isInteger(message.replace_count))
                    return "replace_count: integer expected";
            return null;
        };

        ExecuteReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ExecuteReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ExecuteReward();
            if (object.reward != null) {
                if (typeof object.reward !== "object")
                    throw TypeError(".lq.ExecuteReward.reward: object expected");
                message.reward = $root.lq.RewardSlot.fromObject(object.reward, _depth + 1);
            }
            if (object.replace != null) {
                if (typeof object.replace !== "object")
                    throw TypeError(".lq.ExecuteReward.replace: object expected");
                message.replace = $root.lq.RewardSlot.fromObject(object.replace, _depth + 1);
            }
            if (object.replace_count != null)
                if (Number(object.replace_count) !== 0)
                    message.replace_count = object.replace_count >>> 0;
            return message;
        };

        ExecuteReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.reward = null;
                object.replace = null;
                object.replace_count = 0;
            }
            if (message.reward != null && message.hasOwnProperty("reward"))
                object.reward = $root.lq.RewardSlot.toObject(message.reward, options);
            if (message.replace != null && message.hasOwnProperty("replace"))
                object.replace = $root.lq.RewardSlot.toObject(message.replace, options);
            if (message.replace_count != null && message.hasOwnProperty("replace_count"))
                object.replace_count = message.replace_count;
            return object;
        };

        ExecuteReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExecuteReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ExecuteReward";
        };

        return ExecuteReward;
    })();

    lq.ExecuteResult = (function() {

        function ExecuteResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ExecuteResult.prototype.id = 0;
        ExecuteResult.prototype.count = 0;

        ExecuteResult.create = function create(properties) {
            return new ExecuteResult(properties);
        };

        ExecuteResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).int32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ExecuteResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ExecuteResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ExecuteResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ExecuteResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ExecuteResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ExecuteResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ExecuteResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ExecuteResult();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count | 0;
            return message;
        };

        ExecuteResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ExecuteResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExecuteResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ExecuteResult";
        };

        return ExecuteResult;
    })();

    lq.I18nContext = (function() {

        function I18nContext(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        I18nContext.prototype.lang = "";
        I18nContext.prototype.context = "";

        I18nContext.create = function create(properties) {
            return new I18nContext(properties);
        };

        I18nContext.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.context != null && Object.hasOwnProperty.call(message, "context"))
                writer.uint32(18).string(message.context);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        I18nContext.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        I18nContext.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.I18nContext(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.context = value;
                        else
                            delete message.context;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        I18nContext.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        I18nContext.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            if (message.context != null && message.hasOwnProperty("context"))
                if (!$util.isString(message.context))
                    return "context: string expected";
            return null;
        };

        I18nContext.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.I18nContext)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.I18nContext();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            if (object.context != null)
                if (typeof object.context !== "string" || object.context.length)
                    message.context = String(object.context);
            return message;
        };

        I18nContext.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.lang = "";
                object.context = "";
            }
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            if (message.context != null && message.hasOwnProperty("context"))
                object.context = message.context;
            return object;
        };

        I18nContext.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        I18nContext.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.I18nContext";
        };

        return I18nContext;
    })();

    lq.Mail = (function() {

        function Mail(properties) {
            this.attachments = [];
            this.title_i18n = [];
            this.content_i18n = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Mail.prototype.mail_id = 0;
        Mail.prototype.state = 0;
        Mail.prototype.take_attachment = false;
        Mail.prototype.title = "";
        Mail.prototype.content = "";
        Mail.prototype.attachments = $util.emptyArray;
        Mail.prototype.create_time = 0;
        Mail.prototype.expire_time = 0;
        Mail.prototype.reference_id = 0;
        Mail.prototype.title_i18n = $util.emptyArray;
        Mail.prototype.content_i18n = $util.emptyArray;
        Mail.prototype.template_id = 0;

        Mail.create = function create(properties) {
            return new Mail(properties);
        };

        Mail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail_id != null && Object.hasOwnProperty.call(message, "mail_id"))
                writer.uint32(8).uint32(message.mail_id);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.take_attachment != null && Object.hasOwnProperty.call(message, "take_attachment"))
                writer.uint32(24).bool(message.take_attachment);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(34).string(message.title);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(42).string(message.content);
            if (message.attachments != null && message.attachments.length)
                for (let i = 0; i < message.attachments.length; ++i)
                    $root.lq.RewardSlot.encode(message.attachments[i], writer.uint32(50).fork()).ldelim();
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(56).uint32(message.create_time);
            if (message.expire_time != null && Object.hasOwnProperty.call(message, "expire_time"))
                writer.uint32(64).uint32(message.expire_time);
            if (message.reference_id != null && Object.hasOwnProperty.call(message, "reference_id"))
                writer.uint32(72).uint32(message.reference_id);
            if (message.title_i18n != null && message.title_i18n.length)
                for (let i = 0; i < message.title_i18n.length; ++i)
                    $root.lq.I18nContext.encode(message.title_i18n[i], writer.uint32(82).fork()).ldelim();
            if (message.content_i18n != null && message.content_i18n.length)
                for (let i = 0; i < message.content_i18n.length; ++i)
                    $root.lq.I18nContext.encode(message.content_i18n[i], writer.uint32(90).fork()).ldelim();
            if (message.template_id != null && Object.hasOwnProperty.call(message, "template_id"))
                writer.uint32(96).uint32(message.template_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Mail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Mail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Mail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mail_id = value;
                        else
                            delete message.mail_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.take_attachment = value;
                        else
                            delete message.take_attachment;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.attachments && message.attachments.length))
                            message.attachments = [];
                        message.attachments.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expire_time = value;
                        else
                            delete message.expire_time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reference_id = value;
                        else
                            delete message.reference_id;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.title_i18n && message.title_i18n.length))
                            message.title_i18n = [];
                        message.title_i18n.push($root.lq.I18nContext.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.content_i18n && message.content_i18n.length))
                            message.content_i18n = [];
                        message.content_i18n.push($root.lq.I18nContext.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.template_id = value;
                        else
                            delete message.template_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Mail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Mail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                if (!$util.isInteger(message.mail_id))
                    return "mail_id: integer expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.take_attachment != null && message.hasOwnProperty("take_attachment"))
                if (typeof message.take_attachment !== "boolean")
                    return "take_attachment: boolean expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.attachments != null && message.hasOwnProperty("attachments")) {
                if (!Array.isArray(message.attachments))
                    return "attachments: array expected";
                for (let i = 0; i < message.attachments.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.attachments[i], _depth + 1);
                    if (error)
                        return "attachments." + error;
                }
            }
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                if (!$util.isInteger(message.expire_time))
                    return "expire_time: integer expected";
            if (message.reference_id != null && message.hasOwnProperty("reference_id"))
                if (!$util.isInteger(message.reference_id))
                    return "reference_id: integer expected";
            if (message.title_i18n != null && message.hasOwnProperty("title_i18n")) {
                if (!Array.isArray(message.title_i18n))
                    return "title_i18n: array expected";
                for (let i = 0; i < message.title_i18n.length; ++i) {
                    let error = $root.lq.I18nContext.verify(message.title_i18n[i], _depth + 1);
                    if (error)
                        return "title_i18n." + error;
                }
            }
            if (message.content_i18n != null && message.hasOwnProperty("content_i18n")) {
                if (!Array.isArray(message.content_i18n))
                    return "content_i18n: array expected";
                for (let i = 0; i < message.content_i18n.length; ++i) {
                    let error = $root.lq.I18nContext.verify(message.content_i18n[i], _depth + 1);
                    if (error)
                        return "content_i18n." + error;
                }
            }
            if (message.template_id != null && message.hasOwnProperty("template_id"))
                if (!$util.isInteger(message.template_id))
                    return "template_id: integer expected";
            return null;
        };

        Mail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Mail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Mail();
            if (object.mail_id != null)
                if (Number(object.mail_id) !== 0)
                    message.mail_id = object.mail_id >>> 0;
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.take_attachment != null)
                if (object.take_attachment)
                    message.take_attachment = Boolean(object.take_attachment);
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.attachments) {
                if (!Array.isArray(object.attachments))
                    throw TypeError(".lq.Mail.attachments: array expected");
                message.attachments = Array(object.attachments.length);
                for (let i = 0; i < object.attachments.length; ++i) {
                    if (typeof object.attachments[i] !== "object")
                        throw TypeError(".lq.Mail.attachments: object expected");
                    message.attachments[i] = $root.lq.RewardSlot.fromObject(object.attachments[i], _depth + 1);
                }
            }
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.expire_time != null)
                if (Number(object.expire_time) !== 0)
                    message.expire_time = object.expire_time >>> 0;
            if (object.reference_id != null)
                if (Number(object.reference_id) !== 0)
                    message.reference_id = object.reference_id >>> 0;
            if (object.title_i18n) {
                if (!Array.isArray(object.title_i18n))
                    throw TypeError(".lq.Mail.title_i18n: array expected");
                message.title_i18n = Array(object.title_i18n.length);
                for (let i = 0; i < object.title_i18n.length; ++i) {
                    if (typeof object.title_i18n[i] !== "object")
                        throw TypeError(".lq.Mail.title_i18n: object expected");
                    message.title_i18n[i] = $root.lq.I18nContext.fromObject(object.title_i18n[i], _depth + 1);
                }
            }
            if (object.content_i18n) {
                if (!Array.isArray(object.content_i18n))
                    throw TypeError(".lq.Mail.content_i18n: array expected");
                message.content_i18n = Array(object.content_i18n.length);
                for (let i = 0; i < object.content_i18n.length; ++i) {
                    if (typeof object.content_i18n[i] !== "object")
                        throw TypeError(".lq.Mail.content_i18n: object expected");
                    message.content_i18n[i] = $root.lq.I18nContext.fromObject(object.content_i18n[i], _depth + 1);
                }
            }
            if (object.template_id != null)
                if (Number(object.template_id) !== 0)
                    message.template_id = object.template_id >>> 0;
            return message;
        };

        Mail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.attachments = [];
                object.title_i18n = [];
                object.content_i18n = [];
            }
            if (options.defaults) {
                object.mail_id = 0;
                object.state = 0;
                object.take_attachment = false;
                object.title = "";
                object.content = "";
                object.create_time = 0;
                object.expire_time = 0;
                object.reference_id = 0;
                object.template_id = 0;
            }
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                object.mail_id = message.mail_id;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.take_attachment != null && message.hasOwnProperty("take_attachment"))
                object.take_attachment = message.take_attachment;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.attachments && message.attachments.length) {
                object.attachments = Array(message.attachments.length);
                for (let j = 0; j < message.attachments.length; ++j)
                    object.attachments[j] = $root.lq.RewardSlot.toObject(message.attachments[j], options);
            }
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                object.expire_time = message.expire_time;
            if (message.reference_id != null && message.hasOwnProperty("reference_id"))
                object.reference_id = message.reference_id;
            if (message.title_i18n && message.title_i18n.length) {
                object.title_i18n = Array(message.title_i18n.length);
                for (let j = 0; j < message.title_i18n.length; ++j)
                    object.title_i18n[j] = $root.lq.I18nContext.toObject(message.title_i18n[j], options);
            }
            if (message.content_i18n && message.content_i18n.length) {
                object.content_i18n = Array(message.content_i18n.length);
                for (let j = 0; j < message.content_i18n.length; ++j)
                    object.content_i18n[j] = $root.lq.I18nContext.toObject(message.content_i18n[j], options);
            }
            if (message.template_id != null && message.hasOwnProperty("template_id"))
                object.template_id = message.template_id;
            return object;
        };

        Mail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Mail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Mail";
        };

        return Mail;
    })();

    lq.AchievementProgress = (function() {

        function AchievementProgress(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AchievementProgress.prototype.id = 0;
        AchievementProgress.prototype.counter = 0;
        AchievementProgress.prototype.achieved = false;
        AchievementProgress.prototype.rewarded = false;
        AchievementProgress.prototype.achieved_time = 0;

        AchievementProgress.create = function create(properties) {
            return new AchievementProgress(properties);
        };

        AchievementProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                writer.uint32(16).uint32(message.counter);
            if (message.achieved != null && Object.hasOwnProperty.call(message, "achieved"))
                writer.uint32(24).bool(message.achieved);
            if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                writer.uint32(32).bool(message.rewarded);
            if (message.achieved_time != null && Object.hasOwnProperty.call(message, "achieved_time"))
                writer.uint32(40).uint32(message.achieved_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AchievementProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AchievementProgress.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AchievementProgress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.counter = value;
                        else
                            delete message.counter;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.achieved = value;
                        else
                            delete message.achieved;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rewarded = value;
                        else
                            delete message.rewarded;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.achieved_time = value;
                        else
                            delete message.achieved_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AchievementProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AchievementProgress.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.counter != null && message.hasOwnProperty("counter"))
                if (!$util.isInteger(message.counter))
                    return "counter: integer expected";
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                if (typeof message.achieved !== "boolean")
                    return "achieved: boolean expected";
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                if (typeof message.rewarded !== "boolean")
                    return "rewarded: boolean expected";
            if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                if (!$util.isInteger(message.achieved_time))
                    return "achieved_time: integer expected";
            return null;
        };

        AchievementProgress.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AchievementProgress)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AchievementProgress();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.counter != null)
                if (Number(object.counter) !== 0)
                    message.counter = object.counter >>> 0;
            if (object.achieved != null)
                if (object.achieved)
                    message.achieved = Boolean(object.achieved);
            if (object.rewarded != null)
                if (object.rewarded)
                    message.rewarded = Boolean(object.rewarded);
            if (object.achieved_time != null)
                if (Number(object.achieved_time) !== 0)
                    message.achieved_time = object.achieved_time >>> 0;
            return message;
        };

        AchievementProgress.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.counter = 0;
                object.achieved = false;
                object.rewarded = false;
                object.achieved_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.counter != null && message.hasOwnProperty("counter"))
                object.counter = message.counter;
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                object.achieved = message.achieved;
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                object.rewarded = message.rewarded;
            if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                object.achieved_time = message.achieved_time;
            return object;
        };

        AchievementProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AchievementProgress.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AchievementProgress";
        };

        return AchievementProgress;
    })();

    lq.BadgeAchieveProgress = (function() {

        function BadgeAchieveProgress(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BadgeAchieveProgress.prototype.id = 0;
        BadgeAchieveProgress.prototype.counter = 0;
        BadgeAchieveProgress.prototype.achieved_counter = 0;
        BadgeAchieveProgress.prototype.achieved_time = 0;

        BadgeAchieveProgress.create = function create(properties) {
            return new BadgeAchieveProgress(properties);
        };

        BadgeAchieveProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                writer.uint32(16).uint32(message.counter);
            if (message.achieved_counter != null && Object.hasOwnProperty.call(message, "achieved_counter"))
                writer.uint32(24).uint32(message.achieved_counter);
            if (message.achieved_time != null && Object.hasOwnProperty.call(message, "achieved_time"))
                writer.uint32(32).uint32(message.achieved_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BadgeAchieveProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BadgeAchieveProgress.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BadgeAchieveProgress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.counter = value;
                        else
                            delete message.counter;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.achieved_counter = value;
                        else
                            delete message.achieved_counter;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.achieved_time = value;
                        else
                            delete message.achieved_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BadgeAchieveProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BadgeAchieveProgress.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.counter != null && message.hasOwnProperty("counter"))
                if (!$util.isInteger(message.counter))
                    return "counter: integer expected";
            if (message.achieved_counter != null && message.hasOwnProperty("achieved_counter"))
                if (!$util.isInteger(message.achieved_counter))
                    return "achieved_counter: integer expected";
            if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                if (!$util.isInteger(message.achieved_time))
                    return "achieved_time: integer expected";
            return null;
        };

        BadgeAchieveProgress.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BadgeAchieveProgress)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BadgeAchieveProgress();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.counter != null)
                if (Number(object.counter) !== 0)
                    message.counter = object.counter >>> 0;
            if (object.achieved_counter != null)
                if (Number(object.achieved_counter) !== 0)
                    message.achieved_counter = object.achieved_counter >>> 0;
            if (object.achieved_time != null)
                if (Number(object.achieved_time) !== 0)
                    message.achieved_time = object.achieved_time >>> 0;
            return message;
        };

        BadgeAchieveProgress.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.counter = 0;
                object.achieved_counter = 0;
                object.achieved_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.counter != null && message.hasOwnProperty("counter"))
                object.counter = message.counter;
            if (message.achieved_counter != null && message.hasOwnProperty("achieved_counter"))
                object.achieved_counter = message.achieved_counter;
            if (message.achieved_time != null && message.hasOwnProperty("achieved_time"))
                object.achieved_time = message.achieved_time;
            return object;
        };

        BadgeAchieveProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BadgeAchieveProgress.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BadgeAchieveProgress";
        };

        return BadgeAchieveProgress;
    })();

    lq.AccountStatisticByGameMode = (function() {

        function AccountStatisticByGameMode(properties) {
            this.game_final_position = [];
            this.round_end = [];
            this.rank_score = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountStatisticByGameMode.prototype.mode = 0;
        AccountStatisticByGameMode.prototype.game_count_sum = 0;
        AccountStatisticByGameMode.prototype.game_final_position = $util.emptyArray;
        AccountStatisticByGameMode.prototype.fly_count = 0;
        AccountStatisticByGameMode.prototype.gold_earn_sum = 0;
        AccountStatisticByGameMode.prototype.round_count_sum = 0;
        AccountStatisticByGameMode.prototype.dadian_sum = 0;
        AccountStatisticByGameMode.prototype.round_end = $util.emptyArray;
        AccountStatisticByGameMode.prototype.ming_count_sum = 0;
        AccountStatisticByGameMode.prototype.liqi_count_sum = 0;
        AccountStatisticByGameMode.prototype.xun_count_sum = 0;
        AccountStatisticByGameMode.prototype.highest_lianzhuang = 0;
        AccountStatisticByGameMode.prototype.score_earn_sum = 0;
        AccountStatisticByGameMode.prototype.rank_score = $util.emptyArray;

        AccountStatisticByGameMode.create = function create(properties) {
            return new AccountStatisticByGameMode(properties);
        };

        AccountStatisticByGameMode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(8).uint32(message.mode);
            if (message.game_count_sum != null && Object.hasOwnProperty.call(message, "game_count_sum"))
                writer.uint32(16).uint32(message.game_count_sum);
            if (message.game_final_position != null && message.game_final_position.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.game_final_position.length; ++i)
                    writer.uint32(message.game_final_position[i]);
                writer.ldelim();
            }
            if (message.fly_count != null && Object.hasOwnProperty.call(message, "fly_count"))
                writer.uint32(32).uint32(message.fly_count);
            if (message.gold_earn_sum != null && Object.hasOwnProperty.call(message, "gold_earn_sum"))
                writer.uint32(45).float(message.gold_earn_sum);
            if (message.round_count_sum != null && Object.hasOwnProperty.call(message, "round_count_sum"))
                writer.uint32(48).uint32(message.round_count_sum);
            if (message.dadian_sum != null && Object.hasOwnProperty.call(message, "dadian_sum"))
                writer.uint32(61).float(message.dadian_sum);
            if (message.round_end != null && message.round_end.length)
                for (let i = 0; i < message.round_end.length; ++i)
                    $root.lq.AccountStatisticByGameMode.RoundEndData.encode(message.round_end[i], writer.uint32(66).fork()).ldelim();
            if (message.ming_count_sum != null && Object.hasOwnProperty.call(message, "ming_count_sum"))
                writer.uint32(72).uint32(message.ming_count_sum);
            if (message.liqi_count_sum != null && Object.hasOwnProperty.call(message, "liqi_count_sum"))
                writer.uint32(80).uint32(message.liqi_count_sum);
            if (message.xun_count_sum != null && Object.hasOwnProperty.call(message, "xun_count_sum"))
                writer.uint32(88).uint32(message.xun_count_sum);
            if (message.highest_lianzhuang != null && Object.hasOwnProperty.call(message, "highest_lianzhuang"))
                writer.uint32(96).uint32(message.highest_lianzhuang);
            if (message.score_earn_sum != null && Object.hasOwnProperty.call(message, "score_earn_sum"))
                writer.uint32(104).uint32(message.score_earn_sum);
            if (message.rank_score != null && message.rank_score.length)
                for (let i = 0; i < message.rank_score.length; ++i)
                    $root.lq.AccountStatisticByGameMode.RankScore.encode(message.rank_score[i], writer.uint32(114).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountStatisticByGameMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountStatisticByGameMode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountStatisticByGameMode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_count_sum = value;
                        else
                            delete message.game_count_sum;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.game_final_position && message.game_final_position.length))
                                message.game_final_position = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.game_final_position.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.game_final_position && message.game_final_position.length))
                            message.game_final_position = [];
                        message.game_final_position.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fly_count = value;
                        else
                            delete message.fly_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 5)
                            break;
                        if ((value = reader.float()) !== 0)
                            message.gold_earn_sum = value;
                        else
                            delete message.gold_earn_sum;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round_count_sum = value;
                        else
                            delete message.round_count_sum;
                        continue;
                    }
                case 7: {
                        if (wireType !== 5)
                            break;
                        if ((value = reader.float()) !== 0)
                            message.dadian_sum = value;
                        else
                            delete message.dadian_sum;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.round_end && message.round_end.length))
                            message.round_end = [];
                        message.round_end.push($root.lq.AccountStatisticByGameMode.RoundEndData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ming_count_sum = value;
                        else
                            delete message.ming_count_sum;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqi_count_sum = value;
                        else
                            delete message.liqi_count_sum;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun_count_sum = value;
                        else
                            delete message.xun_count_sum;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.highest_lianzhuang = value;
                        else
                            delete message.highest_lianzhuang;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.score_earn_sum = value;
                        else
                            delete message.score_earn_sum;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank_score && message.rank_score.length))
                            message.rank_score = [];
                        message.rank_score.push($root.lq.AccountStatisticByGameMode.RankScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountStatisticByGameMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountStatisticByGameMode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isInteger(message.mode))
                    return "mode: integer expected";
            if (message.game_count_sum != null && message.hasOwnProperty("game_count_sum"))
                if (!$util.isInteger(message.game_count_sum))
                    return "game_count_sum: integer expected";
            if (message.game_final_position != null && message.hasOwnProperty("game_final_position")) {
                if (!Array.isArray(message.game_final_position))
                    return "game_final_position: array expected";
                for (let i = 0; i < message.game_final_position.length; ++i)
                    if (!$util.isInteger(message.game_final_position[i]))
                        return "game_final_position: integer[] expected";
            }
            if (message.fly_count != null && message.hasOwnProperty("fly_count"))
                if (!$util.isInteger(message.fly_count))
                    return "fly_count: integer expected";
            if (message.gold_earn_sum != null && message.hasOwnProperty("gold_earn_sum"))
                if (typeof message.gold_earn_sum !== "number")
                    return "gold_earn_sum: number expected";
            if (message.round_count_sum != null && message.hasOwnProperty("round_count_sum"))
                if (!$util.isInteger(message.round_count_sum))
                    return "round_count_sum: integer expected";
            if (message.dadian_sum != null && message.hasOwnProperty("dadian_sum"))
                if (typeof message.dadian_sum !== "number")
                    return "dadian_sum: number expected";
            if (message.round_end != null && message.hasOwnProperty("round_end")) {
                if (!Array.isArray(message.round_end))
                    return "round_end: array expected";
                for (let i = 0; i < message.round_end.length; ++i) {
                    let error = $root.lq.AccountStatisticByGameMode.RoundEndData.verify(message.round_end[i], _depth + 1);
                    if (error)
                        return "round_end." + error;
                }
            }
            if (message.ming_count_sum != null && message.hasOwnProperty("ming_count_sum"))
                if (!$util.isInteger(message.ming_count_sum))
                    return "ming_count_sum: integer expected";
            if (message.liqi_count_sum != null && message.hasOwnProperty("liqi_count_sum"))
                if (!$util.isInteger(message.liqi_count_sum))
                    return "liqi_count_sum: integer expected";
            if (message.xun_count_sum != null && message.hasOwnProperty("xun_count_sum"))
                if (!$util.isInteger(message.xun_count_sum))
                    return "xun_count_sum: integer expected";
            if (message.highest_lianzhuang != null && message.hasOwnProperty("highest_lianzhuang"))
                if (!$util.isInteger(message.highest_lianzhuang))
                    return "highest_lianzhuang: integer expected";
            if (message.score_earn_sum != null && message.hasOwnProperty("score_earn_sum"))
                if (!$util.isInteger(message.score_earn_sum))
                    return "score_earn_sum: integer expected";
            if (message.rank_score != null && message.hasOwnProperty("rank_score")) {
                if (!Array.isArray(message.rank_score))
                    return "rank_score: array expected";
                for (let i = 0; i < message.rank_score.length; ++i) {
                    let error = $root.lq.AccountStatisticByGameMode.RankScore.verify(message.rank_score[i], _depth + 1);
                    if (error)
                        return "rank_score." + error;
                }
            }
            return null;
        };

        AccountStatisticByGameMode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountStatisticByGameMode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountStatisticByGameMode();
            if (object.mode != null)
                if (Number(object.mode) !== 0)
                    message.mode = object.mode >>> 0;
            if (object.game_count_sum != null)
                if (Number(object.game_count_sum) !== 0)
                    message.game_count_sum = object.game_count_sum >>> 0;
            if (object.game_final_position) {
                if (!Array.isArray(object.game_final_position))
                    throw TypeError(".lq.AccountStatisticByGameMode.game_final_position: array expected");
                message.game_final_position = Array(object.game_final_position.length);
                for (let i = 0; i < object.game_final_position.length; ++i)
                    message.game_final_position[i] = object.game_final_position[i] >>> 0;
            }
            if (object.fly_count != null)
                if (Number(object.fly_count) !== 0)
                    message.fly_count = object.fly_count >>> 0;
            if (object.gold_earn_sum != null)
                if (Number(object.gold_earn_sum) !== 0)
                    message.gold_earn_sum = Number(object.gold_earn_sum);
            if (object.round_count_sum != null)
                if (Number(object.round_count_sum) !== 0)
                    message.round_count_sum = object.round_count_sum >>> 0;
            if (object.dadian_sum != null)
                if (Number(object.dadian_sum) !== 0)
                    message.dadian_sum = Number(object.dadian_sum);
            if (object.round_end) {
                if (!Array.isArray(object.round_end))
                    throw TypeError(".lq.AccountStatisticByGameMode.round_end: array expected");
                message.round_end = Array(object.round_end.length);
                for (let i = 0; i < object.round_end.length; ++i) {
                    if (typeof object.round_end[i] !== "object")
                        throw TypeError(".lq.AccountStatisticByGameMode.round_end: object expected");
                    message.round_end[i] = $root.lq.AccountStatisticByGameMode.RoundEndData.fromObject(object.round_end[i], _depth + 1);
                }
            }
            if (object.ming_count_sum != null)
                if (Number(object.ming_count_sum) !== 0)
                    message.ming_count_sum = object.ming_count_sum >>> 0;
            if (object.liqi_count_sum != null)
                if (Number(object.liqi_count_sum) !== 0)
                    message.liqi_count_sum = object.liqi_count_sum >>> 0;
            if (object.xun_count_sum != null)
                if (Number(object.xun_count_sum) !== 0)
                    message.xun_count_sum = object.xun_count_sum >>> 0;
            if (object.highest_lianzhuang != null)
                if (Number(object.highest_lianzhuang) !== 0)
                    message.highest_lianzhuang = object.highest_lianzhuang >>> 0;
            if (object.score_earn_sum != null)
                if (Number(object.score_earn_sum) !== 0)
                    message.score_earn_sum = object.score_earn_sum >>> 0;
            if (object.rank_score) {
                if (!Array.isArray(object.rank_score))
                    throw TypeError(".lq.AccountStatisticByGameMode.rank_score: array expected");
                message.rank_score = Array(object.rank_score.length);
                for (let i = 0; i < object.rank_score.length; ++i) {
                    if (typeof object.rank_score[i] !== "object")
                        throw TypeError(".lq.AccountStatisticByGameMode.rank_score: object expected");
                    message.rank_score[i] = $root.lq.AccountStatisticByGameMode.RankScore.fromObject(object.rank_score[i], _depth + 1);
                }
            }
            return message;
        };

        AccountStatisticByGameMode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.game_final_position = [];
                object.round_end = [];
                object.rank_score = [];
            }
            if (options.defaults) {
                object.mode = 0;
                object.game_count_sum = 0;
                object.fly_count = 0;
                object.gold_earn_sum = 0;
                object.round_count_sum = 0;
                object.dadian_sum = 0;
                object.ming_count_sum = 0;
                object.liqi_count_sum = 0;
                object.xun_count_sum = 0;
                object.highest_lianzhuang = 0;
                object.score_earn_sum = 0;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            if (message.game_count_sum != null && message.hasOwnProperty("game_count_sum"))
                object.game_count_sum = message.game_count_sum;
            if (message.game_final_position && message.game_final_position.length) {
                object.game_final_position = Array(message.game_final_position.length);
                for (let j = 0; j < message.game_final_position.length; ++j)
                    object.game_final_position[j] = message.game_final_position[j];
            }
            if (message.fly_count != null && message.hasOwnProperty("fly_count"))
                object.fly_count = message.fly_count;
            if (message.gold_earn_sum != null && message.hasOwnProperty("gold_earn_sum"))
                object.gold_earn_sum = options.json && !isFinite(message.gold_earn_sum) ? String(message.gold_earn_sum) : message.gold_earn_sum;
            if (message.round_count_sum != null && message.hasOwnProperty("round_count_sum"))
                object.round_count_sum = message.round_count_sum;
            if (message.dadian_sum != null && message.hasOwnProperty("dadian_sum"))
                object.dadian_sum = options.json && !isFinite(message.dadian_sum) ? String(message.dadian_sum) : message.dadian_sum;
            if (message.round_end && message.round_end.length) {
                object.round_end = Array(message.round_end.length);
                for (let j = 0; j < message.round_end.length; ++j)
                    object.round_end[j] = $root.lq.AccountStatisticByGameMode.RoundEndData.toObject(message.round_end[j], options);
            }
            if (message.ming_count_sum != null && message.hasOwnProperty("ming_count_sum"))
                object.ming_count_sum = message.ming_count_sum;
            if (message.liqi_count_sum != null && message.hasOwnProperty("liqi_count_sum"))
                object.liqi_count_sum = message.liqi_count_sum;
            if (message.xun_count_sum != null && message.hasOwnProperty("xun_count_sum"))
                object.xun_count_sum = message.xun_count_sum;
            if (message.highest_lianzhuang != null && message.hasOwnProperty("highest_lianzhuang"))
                object.highest_lianzhuang = message.highest_lianzhuang;
            if (message.score_earn_sum != null && message.hasOwnProperty("score_earn_sum"))
                object.score_earn_sum = message.score_earn_sum;
            if (message.rank_score && message.rank_score.length) {
                object.rank_score = Array(message.rank_score.length);
                for (let j = 0; j < message.rank_score.length; ++j)
                    object.rank_score[j] = $root.lq.AccountStatisticByGameMode.RankScore.toObject(message.rank_score[j], options);
            }
            return object;
        };

        AccountStatisticByGameMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountStatisticByGameMode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountStatisticByGameMode";
        };

        AccountStatisticByGameMode.RoundEndData = (function() {

            function RoundEndData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RoundEndData.prototype.type = 0;
            RoundEndData.prototype.sum = 0;

            RoundEndData.create = function create(properties) {
                return new RoundEndData(properties);
            };

            RoundEndData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.sum != null && Object.hasOwnProperty.call(message, "sum"))
                    writer.uint32(16).uint32(message.sum);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RoundEndData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RoundEndData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountStatisticByGameMode.RoundEndData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.sum = value;
                            else
                                delete message.sum;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RoundEndData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RoundEndData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.sum != null && message.hasOwnProperty("sum"))
                    if (!$util.isInteger(message.sum))
                        return "sum: integer expected";
                return null;
            };

            RoundEndData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountStatisticByGameMode.RoundEndData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountStatisticByGameMode.RoundEndData();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.sum != null)
                    if (Number(object.sum) !== 0)
                        message.sum = object.sum >>> 0;
                return message;
            };

            RoundEndData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.sum = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.sum != null && message.hasOwnProperty("sum"))
                    object.sum = message.sum;
                return object;
            };

            RoundEndData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RoundEndData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountStatisticByGameMode.RoundEndData";
            };

            return RoundEndData;
        })();

        AccountStatisticByGameMode.RankScore = (function() {

            function RankScore(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RankScore.prototype.rank = 0;
            RankScore.prototype.score_sum = 0;
            RankScore.prototype.count = 0;

            RankScore.create = function create(properties) {
                return new RankScore(properties);
            };

            RankScore.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(8).uint32(message.rank);
                if (message.score_sum != null && Object.hasOwnProperty.call(message, "score_sum"))
                    writer.uint32(16).int32(message.score_sum);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(24).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RankScore.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RankScore.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountStatisticByGameMode.RankScore(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.score_sum = value;
                            else
                                delete message.score_sum;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RankScore.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RankScore.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.score_sum != null && message.hasOwnProperty("score_sum"))
                    if (!$util.isInteger(message.score_sum))
                        return "score_sum: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RankScore.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountStatisticByGameMode.RankScore)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountStatisticByGameMode.RankScore();
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.score_sum != null)
                    if (Number(object.score_sum) !== 0)
                        message.score_sum = object.score_sum | 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RankScore.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.rank = 0;
                    object.score_sum = 0;
                    object.count = 0;
                }
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.score_sum != null && message.hasOwnProperty("score_sum"))
                    object.score_sum = message.score_sum;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RankScore.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RankScore.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountStatisticByGameMode.RankScore";
            };

            return RankScore;
        })();

        return AccountStatisticByGameMode;
    })();

    lq.AccountStatisticByFan = (function() {

        function AccountStatisticByFan(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountStatisticByFan.prototype.fan_id = 0;
        AccountStatisticByFan.prototype.sum = 0;

        AccountStatisticByFan.create = function create(properties) {
            return new AccountStatisticByFan(properties);
        };

        AccountStatisticByFan.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.fan_id != null && Object.hasOwnProperty.call(message, "fan_id"))
                writer.uint32(8).uint32(message.fan_id);
            if (message.sum != null && Object.hasOwnProperty.call(message, "sum"))
                writer.uint32(16).uint32(message.sum);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountStatisticByFan.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountStatisticByFan.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountStatisticByFan(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fan_id = value;
                        else
                            delete message.fan_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sum = value;
                        else
                            delete message.sum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountStatisticByFan.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountStatisticByFan.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fan_id != null && message.hasOwnProperty("fan_id"))
                if (!$util.isInteger(message.fan_id))
                    return "fan_id: integer expected";
            if (message.sum != null && message.hasOwnProperty("sum"))
                if (!$util.isInteger(message.sum))
                    return "sum: integer expected";
            return null;
        };

        AccountStatisticByFan.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountStatisticByFan)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountStatisticByFan();
            if (object.fan_id != null)
                if (Number(object.fan_id) !== 0)
                    message.fan_id = object.fan_id >>> 0;
            if (object.sum != null)
                if (Number(object.sum) !== 0)
                    message.sum = object.sum >>> 0;
            return message;
        };

        AccountStatisticByFan.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.fan_id = 0;
                object.sum = 0;
            }
            if (message.fan_id != null && message.hasOwnProperty("fan_id"))
                object.fan_id = message.fan_id;
            if (message.sum != null && message.hasOwnProperty("sum"))
                object.sum = message.sum;
            return object;
        };

        AccountStatisticByFan.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountStatisticByFan.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountStatisticByFan";
        };

        return AccountStatisticByFan;
    })();

    lq.AccountFanAchieved = (function() {

        function AccountFanAchieved(properties) {
            this.fan = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountFanAchieved.prototype.mahjong_category = 0;
        AccountFanAchieved.prototype.fan = $util.emptyArray;
        AccountFanAchieved.prototype.liujumanguan = 0;

        AccountFanAchieved.create = function create(properties) {
            return new AccountFanAchieved(properties);
        };

        AccountFanAchieved.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mahjong_category != null && Object.hasOwnProperty.call(message, "mahjong_category"))
                writer.uint32(8).uint32(message.mahjong_category);
            if (message.fan != null && message.fan.length)
                for (let i = 0; i < message.fan.length; ++i)
                    $root.lq.AccountStatisticByFan.encode(message.fan[i], writer.uint32(18).fork()).ldelim();
            if (message.liujumanguan != null && Object.hasOwnProperty.call(message, "liujumanguan"))
                writer.uint32(24).uint32(message.liujumanguan);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountFanAchieved.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountFanAchieved.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountFanAchieved(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mahjong_category = value;
                        else
                            delete message.mahjong_category;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fan && message.fan.length))
                            message.fan = [];
                        message.fan.push($root.lq.AccountStatisticByFan.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liujumanguan = value;
                        else
                            delete message.liujumanguan;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountFanAchieved.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountFanAchieved.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mahjong_category != null && message.hasOwnProperty("mahjong_category"))
                if (!$util.isInteger(message.mahjong_category))
                    return "mahjong_category: integer expected";
            if (message.fan != null && message.hasOwnProperty("fan")) {
                if (!Array.isArray(message.fan))
                    return "fan: array expected";
                for (let i = 0; i < message.fan.length; ++i) {
                    let error = $root.lq.AccountStatisticByFan.verify(message.fan[i], _depth + 1);
                    if (error)
                        return "fan." + error;
                }
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                if (!$util.isInteger(message.liujumanguan))
                    return "liujumanguan: integer expected";
            return null;
        };

        AccountFanAchieved.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountFanAchieved)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountFanAchieved();
            if (object.mahjong_category != null)
                if (Number(object.mahjong_category) !== 0)
                    message.mahjong_category = object.mahjong_category >>> 0;
            if (object.fan) {
                if (!Array.isArray(object.fan))
                    throw TypeError(".lq.AccountFanAchieved.fan: array expected");
                message.fan = Array(object.fan.length);
                for (let i = 0; i < object.fan.length; ++i) {
                    if (typeof object.fan[i] !== "object")
                        throw TypeError(".lq.AccountFanAchieved.fan: object expected");
                    message.fan[i] = $root.lq.AccountStatisticByFan.fromObject(object.fan[i], _depth + 1);
                }
            }
            if (object.liujumanguan != null)
                if (Number(object.liujumanguan) !== 0)
                    message.liujumanguan = object.liujumanguan >>> 0;
            return message;
        };

        AccountFanAchieved.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.fan = [];
            if (options.defaults) {
                object.mahjong_category = 0;
                object.liujumanguan = 0;
            }
            if (message.mahjong_category != null && message.hasOwnProperty("mahjong_category"))
                object.mahjong_category = message.mahjong_category;
            if (message.fan && message.fan.length) {
                object.fan = Array(message.fan.length);
                for (let j = 0; j < message.fan.length; ++j)
                    object.fan[j] = $root.lq.AccountStatisticByFan.toObject(message.fan[j], options);
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                object.liujumanguan = message.liujumanguan;
            return object;
        };

        AccountFanAchieved.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountFanAchieved.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountFanAchieved";
        };

        return AccountFanAchieved;
    })();

    lq.AccountDetailStatistic = (function() {

        function AccountDetailStatistic(properties) {
            this.game_mode = [];
            this.fan = [];
            this.fan_achieved = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountDetailStatistic.prototype.game_mode = $util.emptyArray;
        AccountDetailStatistic.prototype.fan = $util.emptyArray;
        AccountDetailStatistic.prototype.liujumanguan = 0;
        AccountDetailStatistic.prototype.fan_achieved = $util.emptyArray;

        AccountDetailStatistic.create = function create(properties) {
            return new AccountDetailStatistic(properties);
        };

        AccountDetailStatistic.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_mode != null && message.game_mode.length)
                for (let i = 0; i < message.game_mode.length; ++i)
                    $root.lq.AccountStatisticByGameMode.encode(message.game_mode[i], writer.uint32(10).fork()).ldelim();
            if (message.fan != null && message.fan.length)
                for (let i = 0; i < message.fan.length; ++i)
                    $root.lq.AccountStatisticByFan.encode(message.fan[i], writer.uint32(18).fork()).ldelim();
            if (message.liujumanguan != null && Object.hasOwnProperty.call(message, "liujumanguan"))
                writer.uint32(24).uint32(message.liujumanguan);
            if (message.fan_achieved != null && message.fan_achieved.length)
                for (let i = 0; i < message.fan_achieved.length; ++i)
                    $root.lq.AccountFanAchieved.encode(message.fan_achieved[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountDetailStatistic.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountDetailStatistic.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatistic(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.game_mode && message.game_mode.length))
                            message.game_mode = [];
                        message.game_mode.push($root.lq.AccountStatisticByGameMode.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fan && message.fan.length))
                            message.fan = [];
                        message.fan.push($root.lq.AccountStatisticByFan.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liujumanguan = value;
                        else
                            delete message.liujumanguan;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fan_achieved && message.fan_achieved.length))
                            message.fan_achieved = [];
                        message.fan_achieved.push($root.lq.AccountFanAchieved.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountDetailStatistic.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountDetailStatistic.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_mode != null && message.hasOwnProperty("game_mode")) {
                if (!Array.isArray(message.game_mode))
                    return "game_mode: array expected";
                for (let i = 0; i < message.game_mode.length; ++i) {
                    let error = $root.lq.AccountStatisticByGameMode.verify(message.game_mode[i], _depth + 1);
                    if (error)
                        return "game_mode." + error;
                }
            }
            if (message.fan != null && message.hasOwnProperty("fan")) {
                if (!Array.isArray(message.fan))
                    return "fan: array expected";
                for (let i = 0; i < message.fan.length; ++i) {
                    let error = $root.lq.AccountStatisticByFan.verify(message.fan[i], _depth + 1);
                    if (error)
                        return "fan." + error;
                }
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                if (!$util.isInteger(message.liujumanguan))
                    return "liujumanguan: integer expected";
            if (message.fan_achieved != null && message.hasOwnProperty("fan_achieved")) {
                if (!Array.isArray(message.fan_achieved))
                    return "fan_achieved: array expected";
                for (let i = 0; i < message.fan_achieved.length; ++i) {
                    let error = $root.lq.AccountFanAchieved.verify(message.fan_achieved[i], _depth + 1);
                    if (error)
                        return "fan_achieved." + error;
                }
            }
            return null;
        };

        AccountDetailStatistic.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountDetailStatistic)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountDetailStatistic();
            if (object.game_mode) {
                if (!Array.isArray(object.game_mode))
                    throw TypeError(".lq.AccountDetailStatistic.game_mode: array expected");
                message.game_mode = Array(object.game_mode.length);
                for (let i = 0; i < object.game_mode.length; ++i) {
                    if (typeof object.game_mode[i] !== "object")
                        throw TypeError(".lq.AccountDetailStatistic.game_mode: object expected");
                    message.game_mode[i] = $root.lq.AccountStatisticByGameMode.fromObject(object.game_mode[i], _depth + 1);
                }
            }
            if (object.fan) {
                if (!Array.isArray(object.fan))
                    throw TypeError(".lq.AccountDetailStatistic.fan: array expected");
                message.fan = Array(object.fan.length);
                for (let i = 0; i < object.fan.length; ++i) {
                    if (typeof object.fan[i] !== "object")
                        throw TypeError(".lq.AccountDetailStatistic.fan: object expected");
                    message.fan[i] = $root.lq.AccountStatisticByFan.fromObject(object.fan[i], _depth + 1);
                }
            }
            if (object.liujumanguan != null)
                if (Number(object.liujumanguan) !== 0)
                    message.liujumanguan = object.liujumanguan >>> 0;
            if (object.fan_achieved) {
                if (!Array.isArray(object.fan_achieved))
                    throw TypeError(".lq.AccountDetailStatistic.fan_achieved: array expected");
                message.fan_achieved = Array(object.fan_achieved.length);
                for (let i = 0; i < object.fan_achieved.length; ++i) {
                    if (typeof object.fan_achieved[i] !== "object")
                        throw TypeError(".lq.AccountDetailStatistic.fan_achieved: object expected");
                    message.fan_achieved[i] = $root.lq.AccountFanAchieved.fromObject(object.fan_achieved[i], _depth + 1);
                }
            }
            return message;
        };

        AccountDetailStatistic.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.game_mode = [];
                object.fan = [];
                object.fan_achieved = [];
            }
            if (options.defaults)
                object.liujumanguan = 0;
            if (message.game_mode && message.game_mode.length) {
                object.game_mode = Array(message.game_mode.length);
                for (let j = 0; j < message.game_mode.length; ++j)
                    object.game_mode[j] = $root.lq.AccountStatisticByGameMode.toObject(message.game_mode[j], options);
            }
            if (message.fan && message.fan.length) {
                object.fan = Array(message.fan.length);
                for (let j = 0; j < message.fan.length; ++j)
                    object.fan[j] = $root.lq.AccountStatisticByFan.toObject(message.fan[j], options);
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                object.liujumanguan = message.liujumanguan;
            if (message.fan_achieved && message.fan_achieved.length) {
                object.fan_achieved = Array(message.fan_achieved.length);
                for (let j = 0; j < message.fan_achieved.length; ++j)
                    object.fan_achieved[j] = $root.lq.AccountFanAchieved.toObject(message.fan_achieved[j], options);
            }
            return object;
        };

        AccountDetailStatistic.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountDetailStatistic.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountDetailStatistic";
        };

        return AccountDetailStatistic;
    })();

    lq.AccountDetailStatisticByCategory = (function() {

        function AccountDetailStatisticByCategory(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountDetailStatisticByCategory.prototype.category = 0;
        AccountDetailStatisticByCategory.prototype.detail_statistic = null;

        AccountDetailStatisticByCategory.create = function create(properties) {
            return new AccountDetailStatisticByCategory(properties);
        };

        AccountDetailStatisticByCategory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(8).uint32(message.category);
            if (message.detail_statistic != null && Object.hasOwnProperty.call(message, "detail_statistic"))
                $root.lq.AccountDetailStatistic.encode(message.detail_statistic, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountDetailStatisticByCategory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountDetailStatisticByCategory.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticByCategory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.detail_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail_statistic);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountDetailStatisticByCategory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountDetailStatisticByCategory.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.detail_statistic != null && message.hasOwnProperty("detail_statistic")) {
                let error = $root.lq.AccountDetailStatistic.verify(message.detail_statistic, _depth + 1);
                if (error)
                    return "detail_statistic." + error;
            }
            return null;
        };

        AccountDetailStatisticByCategory.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountDetailStatisticByCategory)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountDetailStatisticByCategory();
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.detail_statistic != null) {
                if (typeof object.detail_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticByCategory.detail_statistic: object expected");
                message.detail_statistic = $root.lq.AccountDetailStatistic.fromObject(object.detail_statistic, _depth + 1);
            }
            return message;
        };

        AccountDetailStatisticByCategory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.category = 0;
                object.detail_statistic = null;
            }
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.detail_statistic != null && message.hasOwnProperty("detail_statistic"))
                object.detail_statistic = $root.lq.AccountDetailStatistic.toObject(message.detail_statistic, options);
            return object;
        };

        AccountDetailStatisticByCategory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountDetailStatisticByCategory.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountDetailStatisticByCategory";
        };

        return AccountDetailStatisticByCategory;
    })();

    lq.AccountDetailStatisticV2 = (function() {

        function AccountDetailStatisticV2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountDetailStatisticV2.prototype.friend_room_statistic = null;
        AccountDetailStatisticV2.prototype.rank_statistic = null;
        AccountDetailStatisticV2.prototype.customized_contest_statistic = null;
        AccountDetailStatisticV2.prototype.leisure_match_statistic = null;
        AccountDetailStatisticV2.prototype.challenge_match_statistic = null;
        AccountDetailStatisticV2.prototype.activity_match_statistic = null;
        AccountDetailStatisticV2.prototype.ab_match_statistic = null;

        AccountDetailStatisticV2.create = function create(properties) {
            return new AccountDetailStatisticV2(properties);
        };

        AccountDetailStatisticV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.friend_room_statistic != null && Object.hasOwnProperty.call(message, "friend_room_statistic"))
                $root.lq.AccountDetailStatistic.encode(message.friend_room_statistic, writer.uint32(10).fork()).ldelim();
            if (message.rank_statistic != null && Object.hasOwnProperty.call(message, "rank_statistic"))
                $root.lq.AccountDetailStatisticV2.RankStatistic.encode(message.rank_statistic, writer.uint32(18).fork()).ldelim();
            if (message.customized_contest_statistic != null && Object.hasOwnProperty.call(message, "customized_contest_statistic"))
                $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic.encode(message.customized_contest_statistic, writer.uint32(26).fork()).ldelim();
            if (message.leisure_match_statistic != null && Object.hasOwnProperty.call(message, "leisure_match_statistic"))
                $root.lq.AccountDetailStatistic.encode(message.leisure_match_statistic, writer.uint32(34).fork()).ldelim();
            if (message.challenge_match_statistic != null && Object.hasOwnProperty.call(message, "challenge_match_statistic"))
                $root.lq.AccountDetailStatisticV2.ChallengeStatistic.encode(message.challenge_match_statistic, writer.uint32(42).fork()).ldelim();
            if (message.activity_match_statistic != null && Object.hasOwnProperty.call(message, "activity_match_statistic"))
                $root.lq.AccountDetailStatistic.encode(message.activity_match_statistic, writer.uint32(50).fork()).ldelim();
            if (message.ab_match_statistic != null && Object.hasOwnProperty.call(message, "ab_match_statistic"))
                $root.lq.AccountDetailStatistic.encode(message.ab_match_statistic, writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountDetailStatisticV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountDetailStatisticV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.friend_room_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_room_statistic);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.rank_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.rank_statistic);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.customized_contest_statistic = $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.customized_contest_statistic);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.leisure_match_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.leisure_match_statistic);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.challenge_match_statistic = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.challenge_match_statistic);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.activity_match_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_match_statistic);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.ab_match_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.ab_match_statistic);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountDetailStatisticV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountDetailStatisticV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.friend_room_statistic != null && message.hasOwnProperty("friend_room_statistic")) {
                let error = $root.lq.AccountDetailStatistic.verify(message.friend_room_statistic, _depth + 1);
                if (error)
                    return "friend_room_statistic." + error;
            }
            if (message.rank_statistic != null && message.hasOwnProperty("rank_statistic")) {
                let error = $root.lq.AccountDetailStatisticV2.RankStatistic.verify(message.rank_statistic, _depth + 1);
                if (error)
                    return "rank_statistic." + error;
            }
            if (message.customized_contest_statistic != null && message.hasOwnProperty("customized_contest_statistic")) {
                let error = $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic.verify(message.customized_contest_statistic, _depth + 1);
                if (error)
                    return "customized_contest_statistic." + error;
            }
            if (message.leisure_match_statistic != null && message.hasOwnProperty("leisure_match_statistic")) {
                let error = $root.lq.AccountDetailStatistic.verify(message.leisure_match_statistic, _depth + 1);
                if (error)
                    return "leisure_match_statistic." + error;
            }
            if (message.challenge_match_statistic != null && message.hasOwnProperty("challenge_match_statistic")) {
                let error = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.verify(message.challenge_match_statistic, _depth + 1);
                if (error)
                    return "challenge_match_statistic." + error;
            }
            if (message.activity_match_statistic != null && message.hasOwnProperty("activity_match_statistic")) {
                let error = $root.lq.AccountDetailStatistic.verify(message.activity_match_statistic, _depth + 1);
                if (error)
                    return "activity_match_statistic." + error;
            }
            if (message.ab_match_statistic != null && message.hasOwnProperty("ab_match_statistic")) {
                let error = $root.lq.AccountDetailStatistic.verify(message.ab_match_statistic, _depth + 1);
                if (error)
                    return "ab_match_statistic." + error;
            }
            return null;
        };

        AccountDetailStatisticV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountDetailStatisticV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountDetailStatisticV2();
            if (object.friend_room_statistic != null) {
                if (typeof object.friend_room_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.friend_room_statistic: object expected");
                message.friend_room_statistic = $root.lq.AccountDetailStatistic.fromObject(object.friend_room_statistic, _depth + 1);
            }
            if (object.rank_statistic != null) {
                if (typeof object.rank_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.rank_statistic: object expected");
                message.rank_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.fromObject(object.rank_statistic, _depth + 1);
            }
            if (object.customized_contest_statistic != null) {
                if (typeof object.customized_contest_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.customized_contest_statistic: object expected");
                message.customized_contest_statistic = $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic.fromObject(object.customized_contest_statistic, _depth + 1);
            }
            if (object.leisure_match_statistic != null) {
                if (typeof object.leisure_match_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.leisure_match_statistic: object expected");
                message.leisure_match_statistic = $root.lq.AccountDetailStatistic.fromObject(object.leisure_match_statistic, _depth + 1);
            }
            if (object.challenge_match_statistic != null) {
                if (typeof object.challenge_match_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.challenge_match_statistic: object expected");
                message.challenge_match_statistic = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.fromObject(object.challenge_match_statistic, _depth + 1);
            }
            if (object.activity_match_statistic != null) {
                if (typeof object.activity_match_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.activity_match_statistic: object expected");
                message.activity_match_statistic = $root.lq.AccountDetailStatistic.fromObject(object.activity_match_statistic, _depth + 1);
            }
            if (object.ab_match_statistic != null) {
                if (typeof object.ab_match_statistic !== "object")
                    throw TypeError(".lq.AccountDetailStatisticV2.ab_match_statistic: object expected");
                message.ab_match_statistic = $root.lq.AccountDetailStatistic.fromObject(object.ab_match_statistic, _depth + 1);
            }
            return message;
        };

        AccountDetailStatisticV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.friend_room_statistic = null;
                object.rank_statistic = null;
                object.customized_contest_statistic = null;
                object.leisure_match_statistic = null;
                object.challenge_match_statistic = null;
                object.activity_match_statistic = null;
                object.ab_match_statistic = null;
            }
            if (message.friend_room_statistic != null && message.hasOwnProperty("friend_room_statistic"))
                object.friend_room_statistic = $root.lq.AccountDetailStatistic.toObject(message.friend_room_statistic, options);
            if (message.rank_statistic != null && message.hasOwnProperty("rank_statistic"))
                object.rank_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.toObject(message.rank_statistic, options);
            if (message.customized_contest_statistic != null && message.hasOwnProperty("customized_contest_statistic"))
                object.customized_contest_statistic = $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic.toObject(message.customized_contest_statistic, options);
            if (message.leisure_match_statistic != null && message.hasOwnProperty("leisure_match_statistic"))
                object.leisure_match_statistic = $root.lq.AccountDetailStatistic.toObject(message.leisure_match_statistic, options);
            if (message.challenge_match_statistic != null && message.hasOwnProperty("challenge_match_statistic"))
                object.challenge_match_statistic = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.toObject(message.challenge_match_statistic, options);
            if (message.activity_match_statistic != null && message.hasOwnProperty("activity_match_statistic"))
                object.activity_match_statistic = $root.lq.AccountDetailStatistic.toObject(message.activity_match_statistic, options);
            if (message.ab_match_statistic != null && message.hasOwnProperty("ab_match_statistic"))
                object.ab_match_statistic = $root.lq.AccountDetailStatistic.toObject(message.ab_match_statistic, options);
            return object;
        };

        AccountDetailStatisticV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountDetailStatisticV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountDetailStatisticV2";
        };

        AccountDetailStatisticV2.RankStatistic = (function() {

            function RankStatistic(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RankStatistic.prototype.total_statistic = null;
            RankStatistic.prototype.month_statistic = null;
            RankStatistic.prototype.month_refresh_time = 0;

            RankStatistic.create = function create(properties) {
                return new RankStatistic(properties);
            };

            RankStatistic.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_statistic != null && Object.hasOwnProperty.call(message, "total_statistic"))
                    $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.encode(message.total_statistic, writer.uint32(10).fork()).ldelim();
                if (message.month_statistic != null && Object.hasOwnProperty.call(message, "month_statistic"))
                    $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.encode(message.month_statistic, writer.uint32(18).fork()).ldelim();
                if (message.month_refresh_time != null && Object.hasOwnProperty.call(message, "month_refresh_time"))
                    writer.uint32(24).uint32(message.month_refresh_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RankStatistic.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RankStatistic.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.RankStatistic(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.total_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.decode(reader, reader.uint32(), undefined, _depth + 1, message.total_statistic);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.month_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_statistic);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.month_refresh_time = value;
                            else
                                delete message.month_refresh_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RankStatistic.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RankStatistic.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_statistic != null && message.hasOwnProperty("total_statistic")) {
                    let error = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.verify(message.total_statistic, _depth + 1);
                    if (error)
                        return "total_statistic." + error;
                }
                if (message.month_statistic != null && message.hasOwnProperty("month_statistic")) {
                    let error = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.verify(message.month_statistic, _depth + 1);
                    if (error)
                        return "month_statistic." + error;
                }
                if (message.month_refresh_time != null && message.hasOwnProperty("month_refresh_time"))
                    if (!$util.isInteger(message.month_refresh_time))
                        return "month_refresh_time: integer expected";
                return null;
            };

            RankStatistic.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountDetailStatisticV2.RankStatistic)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountDetailStatisticV2.RankStatistic();
                if (object.total_statistic != null) {
                    if (typeof object.total_statistic !== "object")
                        throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.total_statistic: object expected");
                    message.total_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.fromObject(object.total_statistic, _depth + 1);
                }
                if (object.month_statistic != null) {
                    if (typeof object.month_statistic !== "object")
                        throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.month_statistic: object expected");
                    message.month_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.fromObject(object.month_statistic, _depth + 1);
                }
                if (object.month_refresh_time != null)
                    if (Number(object.month_refresh_time) !== 0)
                        message.month_refresh_time = object.month_refresh_time >>> 0;
                return message;
            };

            RankStatistic.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_statistic = null;
                    object.month_statistic = null;
                    object.month_refresh_time = 0;
                }
                if (message.total_statistic != null && message.hasOwnProperty("total_statistic"))
                    object.total_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.toObject(message.total_statistic, options);
                if (message.month_statistic != null && message.hasOwnProperty("month_statistic"))
                    object.month_statistic = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.toObject(message.month_statistic, options);
                if (message.month_refresh_time != null && message.hasOwnProperty("month_refresh_time"))
                    object.month_refresh_time = message.month_refresh_time;
                return object;
            };

            RankStatistic.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RankStatistic.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountDetailStatisticV2.RankStatistic";
            };

            RankStatistic.RankData = (function() {

                function RankData(properties) {
                    this.level_data_list = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                RankData.prototype.all_level_statistic = null;
                RankData.prototype.level_data_list = $util.emptyArray;

                RankData.create = function create(properties) {
                    return new RankData(properties);
                };

                RankData.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.all_level_statistic != null && Object.hasOwnProperty.call(message, "all_level_statistic"))
                        $root.lq.AccountDetailStatistic.encode(message.all_level_statistic, writer.uint32(10).fork()).ldelim();
                    if (message.level_data_list != null && message.level_data_list.length)
                        for (let i = 0; i < message.level_data_list.length; ++i)
                            $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.encode(message.level_data_list[i], writer.uint32(18).fork()).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                RankData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                RankData.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.RankStatistic.RankData(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                message.all_level_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.all_level_statistic);
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.level_data_list && message.level_data_list.length))
                                    message.level_data_list = [];
                                message.level_data_list.push($root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.decode(reader, reader.uint32(), undefined, _depth + 1));
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                RankData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                RankData.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.all_level_statistic != null && message.hasOwnProperty("all_level_statistic")) {
                        let error = $root.lq.AccountDetailStatistic.verify(message.all_level_statistic, _depth + 1);
                        if (error)
                            return "all_level_statistic." + error;
                    }
                    if (message.level_data_list != null && message.hasOwnProperty("level_data_list")) {
                        if (!Array.isArray(message.level_data_list))
                            return "level_data_list: array expected";
                        for (let i = 0; i < message.level_data_list.length; ++i) {
                            let error = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.verify(message.level_data_list[i], _depth + 1);
                            if (error)
                                return "level_data_list." + error;
                        }
                    }
                    return null;
                };

                RankData.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.AccountDetailStatisticV2.RankStatistic.RankData)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.AccountDetailStatisticV2.RankStatistic.RankData();
                    if (object.all_level_statistic != null) {
                        if (typeof object.all_level_statistic !== "object")
                            throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.RankData.all_level_statistic: object expected");
                        message.all_level_statistic = $root.lq.AccountDetailStatistic.fromObject(object.all_level_statistic, _depth + 1);
                    }
                    if (object.level_data_list) {
                        if (!Array.isArray(object.level_data_list))
                            throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.RankData.level_data_list: array expected");
                        message.level_data_list = Array(object.level_data_list.length);
                        for (let i = 0; i < object.level_data_list.length; ++i) {
                            if (typeof object.level_data_list[i] !== "object")
                                throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.RankData.level_data_list: object expected");
                            message.level_data_list[i] = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.fromObject(object.level_data_list[i], _depth + 1);
                        }
                    }
                    return message;
                };

                RankData.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.level_data_list = [];
                    if (options.defaults)
                        object.all_level_statistic = null;
                    if (message.all_level_statistic != null && message.hasOwnProperty("all_level_statistic"))
                        object.all_level_statistic = $root.lq.AccountDetailStatistic.toObject(message.all_level_statistic, options);
                    if (message.level_data_list && message.level_data_list.length) {
                        object.level_data_list = Array(message.level_data_list.length);
                        for (let j = 0; j < message.level_data_list.length; ++j)
                            object.level_data_list[j] = $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.toObject(message.level_data_list[j], options);
                    }
                    return object;
                };

                RankData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                RankData.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.AccountDetailStatisticV2.RankStatistic.RankData";
                };

                RankData.RankLevelData = (function() {

                    function RankLevelData(properties) {
                        if (properties)
                            for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    RankLevelData.prototype.rank_level = 0;
                    RankLevelData.prototype.statistic = null;

                    RankLevelData.create = function create(properties) {
                        return new RankLevelData(properties);
                    };

                    RankLevelData.encode = function encode(message, writer) {
                        if (!writer)
                            writer = $Writer.create();
                        if (message.rank_level != null && Object.hasOwnProperty.call(message, "rank_level"))
                            writer.uint32(8).uint32(message.rank_level);
                        if (message.statistic != null && Object.hasOwnProperty.call(message, "statistic"))
                            $root.lq.AccountDetailStatistic.encode(message.statistic, writer.uint32(18).fork()).ldelim();
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (let i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    RankLevelData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer).ldelim();
                    };

                    RankLevelData.decode = function decode(reader, length, _end, _depth, _target) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (_depth === undefined)
                            _depth = 0;
                        if (_depth > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData(), value;
                        while (reader.pos < end) {
                            let start = reader.pos;
                            let tag = reader.tag();
                            if (tag === _end) {
                                _end = undefined;
                                break;
                            }
                            let wireType = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (wireType !== 0)
                                        break;
                                    if (value = reader.uint32())
                                        message.rank_level = value;
                                    else
                                        delete message.rank_level;
                                    continue;
                                }
                            case 2: {
                                    if (wireType !== 2)
                                        break;
                                    message.statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.statistic);
                                    continue;
                                }
                            }
                            reader.skipType(wireType, _depth, tag);
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                        }
                        if (_end !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    RankLevelData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    RankLevelData.verify = function verify(message, _depth) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (_depth === undefined)
                            _depth = 0;
                        if (_depth > $util.recursionLimit)
                            return "max depth exceeded";
                        if (message.rank_level != null && message.hasOwnProperty("rank_level"))
                            if (!$util.isInteger(message.rank_level))
                                return "rank_level: integer expected";
                        if (message.statistic != null && message.hasOwnProperty("statistic")) {
                            let error = $root.lq.AccountDetailStatistic.verify(message.statistic, _depth + 1);
                            if (error)
                                return "statistic." + error;
                        }
                        return null;
                    };

                    RankLevelData.fromObject = function fromObject(object, _depth) {
                        if (object instanceof $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData)
                            return object;
                        if (_depth === undefined)
                            _depth = 0;
                        if (_depth > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        let message = new $root.lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData();
                        if (object.rank_level != null)
                            if (Number(object.rank_level) !== 0)
                                message.rank_level = object.rank_level >>> 0;
                        if (object.statistic != null) {
                            if (typeof object.statistic !== "object")
                                throw TypeError(".lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData.statistic: object expected");
                            message.statistic = $root.lq.AccountDetailStatistic.fromObject(object.statistic, _depth + 1);
                        }
                        return message;
                    };

                    RankLevelData.toObject = function toObject(message, options) {
                        if (!options)
                            options = {};
                        let object = {};
                        if (options.defaults) {
                            object.rank_level = 0;
                            object.statistic = null;
                        }
                        if (message.rank_level != null && message.hasOwnProperty("rank_level"))
                            object.rank_level = message.rank_level;
                        if (message.statistic != null && message.hasOwnProperty("statistic"))
                            object.statistic = $root.lq.AccountDetailStatistic.toObject(message.statistic, options);
                        return object;
                    };

                    RankLevelData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    RankLevelData.getTypeUrl = function getTypeUrl(prefix) {
                        if (prefix === undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/lq.AccountDetailStatisticV2.RankStatistic.RankData.RankLevelData";
                    };

                    return RankLevelData;
                })();

                return RankData;
            })();

            return RankStatistic;
        })();

        AccountDetailStatisticV2.CustomizedContestStatistic = (function() {

            function CustomizedContestStatistic(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CustomizedContestStatistic.prototype.total_statistic = null;
            CustomizedContestStatistic.prototype.month_statistic = null;
            CustomizedContestStatistic.prototype.month_refresh_time = 0;

            CustomizedContestStatistic.create = function create(properties) {
                return new CustomizedContestStatistic(properties);
            };

            CustomizedContestStatistic.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_statistic != null && Object.hasOwnProperty.call(message, "total_statistic"))
                    $root.lq.AccountDetailStatistic.encode(message.total_statistic, writer.uint32(10).fork()).ldelim();
                if (message.month_statistic != null && Object.hasOwnProperty.call(message, "month_statistic"))
                    $root.lq.AccountDetailStatistic.encode(message.month_statistic, writer.uint32(18).fork()).ldelim();
                if (message.month_refresh_time != null && Object.hasOwnProperty.call(message, "month_refresh_time"))
                    writer.uint32(24).uint32(message.month_refresh_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CustomizedContestStatistic.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CustomizedContestStatistic.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.total_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.total_statistic);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.month_statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_statistic);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.month_refresh_time = value;
                            else
                                delete message.month_refresh_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CustomizedContestStatistic.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CustomizedContestStatistic.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_statistic != null && message.hasOwnProperty("total_statistic")) {
                    let error = $root.lq.AccountDetailStatistic.verify(message.total_statistic, _depth + 1);
                    if (error)
                        return "total_statistic." + error;
                }
                if (message.month_statistic != null && message.hasOwnProperty("month_statistic")) {
                    let error = $root.lq.AccountDetailStatistic.verify(message.month_statistic, _depth + 1);
                    if (error)
                        return "month_statistic." + error;
                }
                if (message.month_refresh_time != null && message.hasOwnProperty("month_refresh_time"))
                    if (!$util.isInteger(message.month_refresh_time))
                        return "month_refresh_time: integer expected";
                return null;
            };

            CustomizedContestStatistic.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountDetailStatisticV2.CustomizedContestStatistic();
                if (object.total_statistic != null) {
                    if (typeof object.total_statistic !== "object")
                        throw TypeError(".lq.AccountDetailStatisticV2.CustomizedContestStatistic.total_statistic: object expected");
                    message.total_statistic = $root.lq.AccountDetailStatistic.fromObject(object.total_statistic, _depth + 1);
                }
                if (object.month_statistic != null) {
                    if (typeof object.month_statistic !== "object")
                        throw TypeError(".lq.AccountDetailStatisticV2.CustomizedContestStatistic.month_statistic: object expected");
                    message.month_statistic = $root.lq.AccountDetailStatistic.fromObject(object.month_statistic, _depth + 1);
                }
                if (object.month_refresh_time != null)
                    if (Number(object.month_refresh_time) !== 0)
                        message.month_refresh_time = object.month_refresh_time >>> 0;
                return message;
            };

            CustomizedContestStatistic.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_statistic = null;
                    object.month_statistic = null;
                    object.month_refresh_time = 0;
                }
                if (message.total_statistic != null && message.hasOwnProperty("total_statistic"))
                    object.total_statistic = $root.lq.AccountDetailStatistic.toObject(message.total_statistic, options);
                if (message.month_statistic != null && message.hasOwnProperty("month_statistic"))
                    object.month_statistic = $root.lq.AccountDetailStatistic.toObject(message.month_statistic, options);
                if (message.month_refresh_time != null && message.hasOwnProperty("month_refresh_time"))
                    object.month_refresh_time = message.month_refresh_time;
                return object;
            };

            CustomizedContestStatistic.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CustomizedContestStatistic.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountDetailStatisticV2.CustomizedContestStatistic";
            };

            return CustomizedContestStatistic;
        })();

        AccountDetailStatisticV2.ChallengeStatistic = (function() {

            function ChallengeStatistic(properties) {
                this.season_data_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChallengeStatistic.prototype.all_season = null;
            ChallengeStatistic.prototype.season_data_list = $util.emptyArray;

            ChallengeStatistic.create = function create(properties) {
                return new ChallengeStatistic(properties);
            };

            ChallengeStatistic.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.all_season != null && Object.hasOwnProperty.call(message, "all_season"))
                    $root.lq.AccountDetailStatistic.encode(message.all_season, writer.uint32(10).fork()).ldelim();
                if (message.season_data_list != null && message.season_data_list.length)
                    for (let i = 0; i < message.season_data_list.length; ++i)
                        $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.encode(message.season_data_list[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChallengeStatistic.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChallengeStatistic.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.ChallengeStatistic(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.all_season = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.all_season);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.season_data_list && message.season_data_list.length))
                                message.season_data_list = [];
                            message.season_data_list.push($root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChallengeStatistic.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChallengeStatistic.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.all_season != null && message.hasOwnProperty("all_season")) {
                    let error = $root.lq.AccountDetailStatistic.verify(message.all_season, _depth + 1);
                    if (error)
                        return "all_season." + error;
                }
                if (message.season_data_list != null && message.hasOwnProperty("season_data_list")) {
                    if (!Array.isArray(message.season_data_list))
                        return "season_data_list: array expected";
                    for (let i = 0; i < message.season_data_list.length; ++i) {
                        let error = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.verify(message.season_data_list[i], _depth + 1);
                        if (error)
                            return "season_data_list." + error;
                    }
                }
                return null;
            };

            ChallengeStatistic.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountDetailStatisticV2.ChallengeStatistic)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountDetailStatisticV2.ChallengeStatistic();
                if (object.all_season != null) {
                    if (typeof object.all_season !== "object")
                        throw TypeError(".lq.AccountDetailStatisticV2.ChallengeStatistic.all_season: object expected");
                    message.all_season = $root.lq.AccountDetailStatistic.fromObject(object.all_season, _depth + 1);
                }
                if (object.season_data_list) {
                    if (!Array.isArray(object.season_data_list))
                        throw TypeError(".lq.AccountDetailStatisticV2.ChallengeStatistic.season_data_list: array expected");
                    message.season_data_list = Array(object.season_data_list.length);
                    for (let i = 0; i < object.season_data_list.length; ++i) {
                        if (typeof object.season_data_list[i] !== "object")
                            throw TypeError(".lq.AccountDetailStatisticV2.ChallengeStatistic.season_data_list: object expected");
                        message.season_data_list[i] = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.fromObject(object.season_data_list[i], _depth + 1);
                    }
                }
                return message;
            };

            ChallengeStatistic.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.season_data_list = [];
                if (options.defaults)
                    object.all_season = null;
                if (message.all_season != null && message.hasOwnProperty("all_season"))
                    object.all_season = $root.lq.AccountDetailStatistic.toObject(message.all_season, options);
                if (message.season_data_list && message.season_data_list.length) {
                    object.season_data_list = Array(message.season_data_list.length);
                    for (let j = 0; j < message.season_data_list.length; ++j)
                        object.season_data_list[j] = $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.toObject(message.season_data_list[j], options);
                }
                return object;
            };

            ChallengeStatistic.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChallengeStatistic.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountDetailStatisticV2.ChallengeStatistic";
            };

            ChallengeStatistic.SeasonData = (function() {

                function SeasonData(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                SeasonData.prototype.season_id = 0;
                SeasonData.prototype.statistic = null;

                SeasonData.create = function create(properties) {
                    return new SeasonData(properties);
                };

                SeasonData.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.season_id != null && Object.hasOwnProperty.call(message, "season_id"))
                        writer.uint32(8).uint32(message.season_id);
                    if (message.statistic != null && Object.hasOwnProperty.call(message, "statistic"))
                        $root.lq.AccountDetailStatistic.encode(message.statistic, writer.uint32(18).fork()).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                SeasonData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                SeasonData.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.season_id = value;
                                else
                                    delete message.season_id;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                message.statistic = $root.lq.AccountDetailStatistic.decode(reader, reader.uint32(), undefined, _depth + 1, message.statistic);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                SeasonData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                SeasonData.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.season_id != null && message.hasOwnProperty("season_id"))
                        if (!$util.isInteger(message.season_id))
                            return "season_id: integer expected";
                    if (message.statistic != null && message.hasOwnProperty("statistic")) {
                        let error = $root.lq.AccountDetailStatistic.verify(message.statistic, _depth + 1);
                        if (error)
                            return "statistic." + error;
                    }
                    return null;
                };

                SeasonData.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData();
                    if (object.season_id != null)
                        if (Number(object.season_id) !== 0)
                            message.season_id = object.season_id >>> 0;
                    if (object.statistic != null) {
                        if (typeof object.statistic !== "object")
                            throw TypeError(".lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData.statistic: object expected");
                        message.statistic = $root.lq.AccountDetailStatistic.fromObject(object.statistic, _depth + 1);
                    }
                    return message;
                };

                SeasonData.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.season_id = 0;
                        object.statistic = null;
                    }
                    if (message.season_id != null && message.hasOwnProperty("season_id"))
                        object.season_id = message.season_id;
                    if (message.statistic != null && message.hasOwnProperty("statistic"))
                        object.statistic = $root.lq.AccountDetailStatistic.toObject(message.statistic, options);
                    return object;
                };

                SeasonData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                SeasonData.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.AccountDetailStatisticV2.ChallengeStatistic.SeasonData";
                };

                return SeasonData;
            })();

            return ChallengeStatistic;
        })();

        return AccountDetailStatisticV2;
    })();

    lq.AccountShiLian = (function() {

        function AccountShiLian(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountShiLian.prototype.step = 0;
        AccountShiLian.prototype.state = 0;

        AccountShiLian.create = function create(properties) {
            return new AccountShiLian(properties);
        };

        AccountShiLian.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(8).uint32(message.step);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountShiLian.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountShiLian.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountShiLian(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountShiLian.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountShiLian.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            return null;
        };

        AccountShiLian.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountShiLian)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountShiLian();
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            return message;
        };

        AccountShiLian.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.step = 0;
                object.state = 0;
            }
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            return object;
        };

        AccountShiLian.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountShiLian.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountShiLian";
        };

        return AccountShiLian;
    })();

    lq.ClientDeviceInfo = (function() {

        function ClientDeviceInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ClientDeviceInfo.prototype.platform = "";
        ClientDeviceInfo.prototype.hardware = "";
        ClientDeviceInfo.prototype.os = "";
        ClientDeviceInfo.prototype.os_version = "";
        ClientDeviceInfo.prototype.is_browser = false;
        ClientDeviceInfo.prototype.software = "";
        ClientDeviceInfo.prototype.sale_platform = "";
        ClientDeviceInfo.prototype.hardware_vendor = "";
        ClientDeviceInfo.prototype.model_number = "";
        ClientDeviceInfo.prototype.screen_width = 0;
        ClientDeviceInfo.prototype.screen_height = 0;
        ClientDeviceInfo.prototype.user_agent = "";
        ClientDeviceInfo.prototype.screen_type = 0;

        ClientDeviceInfo.create = function create(properties) {
            return new ClientDeviceInfo(properties);
        };

        ClientDeviceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                writer.uint32(10).string(message.platform);
            if (message.hardware != null && Object.hasOwnProperty.call(message, "hardware"))
                writer.uint32(18).string(message.hardware);
            if (message.os != null && Object.hasOwnProperty.call(message, "os"))
                writer.uint32(26).string(message.os);
            if (message.os_version != null && Object.hasOwnProperty.call(message, "os_version"))
                writer.uint32(34).string(message.os_version);
            if (message.is_browser != null && Object.hasOwnProperty.call(message, "is_browser"))
                writer.uint32(40).bool(message.is_browser);
            if (message.software != null && Object.hasOwnProperty.call(message, "software"))
                writer.uint32(50).string(message.software);
            if (message.sale_platform != null && Object.hasOwnProperty.call(message, "sale_platform"))
                writer.uint32(58).string(message.sale_platform);
            if (message.hardware_vendor != null && Object.hasOwnProperty.call(message, "hardware_vendor"))
                writer.uint32(66).string(message.hardware_vendor);
            if (message.model_number != null && Object.hasOwnProperty.call(message, "model_number"))
                writer.uint32(74).string(message.model_number);
            if (message.screen_width != null && Object.hasOwnProperty.call(message, "screen_width"))
                writer.uint32(80).uint32(message.screen_width);
            if (message.screen_height != null && Object.hasOwnProperty.call(message, "screen_height"))
                writer.uint32(88).uint32(message.screen_height);
            if (message.user_agent != null && Object.hasOwnProperty.call(message, "user_agent"))
                writer.uint32(98).string(message.user_agent);
            if (message.screen_type != null && Object.hasOwnProperty.call(message, "screen_type"))
                writer.uint32(104).uint32(message.screen_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ClientDeviceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ClientDeviceInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ClientDeviceInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.platform = value;
                        else
                            delete message.platform;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hardware = value;
                        else
                            delete message.hardware;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.os = value;
                        else
                            delete message.os;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.os_version = value;
                        else
                            delete message.os_version;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_browser = value;
                        else
                            delete message.is_browser;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.software = value;
                        else
                            delete message.software;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sale_platform = value;
                        else
                            delete message.sale_platform;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hardware_vendor = value;
                        else
                            delete message.hardware_vendor;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.model_number = value;
                        else
                            delete message.model_number;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.screen_width = value;
                        else
                            delete message.screen_width;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.screen_height = value;
                        else
                            delete message.screen_height;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.user_agent = value;
                        else
                            delete message.user_agent;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.screen_type = value;
                        else
                            delete message.screen_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ClientDeviceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ClientDeviceInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.platform != null && message.hasOwnProperty("platform"))
                if (!$util.isString(message.platform))
                    return "platform: string expected";
            if (message.hardware != null && message.hasOwnProperty("hardware"))
                if (!$util.isString(message.hardware))
                    return "hardware: string expected";
            if (message.os != null && message.hasOwnProperty("os"))
                if (!$util.isString(message.os))
                    return "os: string expected";
            if (message.os_version != null && message.hasOwnProperty("os_version"))
                if (!$util.isString(message.os_version))
                    return "os_version: string expected";
            if (message.is_browser != null && message.hasOwnProperty("is_browser"))
                if (typeof message.is_browser !== "boolean")
                    return "is_browser: boolean expected";
            if (message.software != null && message.hasOwnProperty("software"))
                if (!$util.isString(message.software))
                    return "software: string expected";
            if (message.sale_platform != null && message.hasOwnProperty("sale_platform"))
                if (!$util.isString(message.sale_platform))
                    return "sale_platform: string expected";
            if (message.hardware_vendor != null && message.hasOwnProperty("hardware_vendor"))
                if (!$util.isString(message.hardware_vendor))
                    return "hardware_vendor: string expected";
            if (message.model_number != null && message.hasOwnProperty("model_number"))
                if (!$util.isString(message.model_number))
                    return "model_number: string expected";
            if (message.screen_width != null && message.hasOwnProperty("screen_width"))
                if (!$util.isInteger(message.screen_width))
                    return "screen_width: integer expected";
            if (message.screen_height != null && message.hasOwnProperty("screen_height"))
                if (!$util.isInteger(message.screen_height))
                    return "screen_height: integer expected";
            if (message.user_agent != null && message.hasOwnProperty("user_agent"))
                if (!$util.isString(message.user_agent))
                    return "user_agent: string expected";
            if (message.screen_type != null && message.hasOwnProperty("screen_type"))
                if (!$util.isInteger(message.screen_type))
                    return "screen_type: integer expected";
            return null;
        };

        ClientDeviceInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ClientDeviceInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ClientDeviceInfo();
            if (object.platform != null)
                if (typeof object.platform !== "string" || object.platform.length)
                    message.platform = String(object.platform);
            if (object.hardware != null)
                if (typeof object.hardware !== "string" || object.hardware.length)
                    message.hardware = String(object.hardware);
            if (object.os != null)
                if (typeof object.os !== "string" || object.os.length)
                    message.os = String(object.os);
            if (object.os_version != null)
                if (typeof object.os_version !== "string" || object.os_version.length)
                    message.os_version = String(object.os_version);
            if (object.is_browser != null)
                if (object.is_browser)
                    message.is_browser = Boolean(object.is_browser);
            if (object.software != null)
                if (typeof object.software !== "string" || object.software.length)
                    message.software = String(object.software);
            if (object.sale_platform != null)
                if (typeof object.sale_platform !== "string" || object.sale_platform.length)
                    message.sale_platform = String(object.sale_platform);
            if (object.hardware_vendor != null)
                if (typeof object.hardware_vendor !== "string" || object.hardware_vendor.length)
                    message.hardware_vendor = String(object.hardware_vendor);
            if (object.model_number != null)
                if (typeof object.model_number !== "string" || object.model_number.length)
                    message.model_number = String(object.model_number);
            if (object.screen_width != null)
                if (Number(object.screen_width) !== 0)
                    message.screen_width = object.screen_width >>> 0;
            if (object.screen_height != null)
                if (Number(object.screen_height) !== 0)
                    message.screen_height = object.screen_height >>> 0;
            if (object.user_agent != null)
                if (typeof object.user_agent !== "string" || object.user_agent.length)
                    message.user_agent = String(object.user_agent);
            if (object.screen_type != null)
                if (Number(object.screen_type) !== 0)
                    message.screen_type = object.screen_type >>> 0;
            return message;
        };

        ClientDeviceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.platform = "";
                object.hardware = "";
                object.os = "";
                object.os_version = "";
                object.is_browser = false;
                object.software = "";
                object.sale_platform = "";
                object.hardware_vendor = "";
                object.model_number = "";
                object.screen_width = 0;
                object.screen_height = 0;
                object.user_agent = "";
                object.screen_type = 0;
            }
            if (message.platform != null && message.hasOwnProperty("platform"))
                object.platform = message.platform;
            if (message.hardware != null && message.hasOwnProperty("hardware"))
                object.hardware = message.hardware;
            if (message.os != null && message.hasOwnProperty("os"))
                object.os = message.os;
            if (message.os_version != null && message.hasOwnProperty("os_version"))
                object.os_version = message.os_version;
            if (message.is_browser != null && message.hasOwnProperty("is_browser"))
                object.is_browser = message.is_browser;
            if (message.software != null && message.hasOwnProperty("software"))
                object.software = message.software;
            if (message.sale_platform != null && message.hasOwnProperty("sale_platform"))
                object.sale_platform = message.sale_platform;
            if (message.hardware_vendor != null && message.hasOwnProperty("hardware_vendor"))
                object.hardware_vendor = message.hardware_vendor;
            if (message.model_number != null && message.hasOwnProperty("model_number"))
                object.model_number = message.model_number;
            if (message.screen_width != null && message.hasOwnProperty("screen_width"))
                object.screen_width = message.screen_width;
            if (message.screen_height != null && message.hasOwnProperty("screen_height"))
                object.screen_height = message.screen_height;
            if (message.user_agent != null && message.hasOwnProperty("user_agent"))
                object.user_agent = message.user_agent;
            if (message.screen_type != null && message.hasOwnProperty("screen_type"))
                object.screen_type = message.screen_type;
            return object;
        };

        ClientDeviceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientDeviceInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ClientDeviceInfo";
        };

        return ClientDeviceInfo;
    })();

    lq.ClientVersionInfo = (function() {

        function ClientVersionInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ClientVersionInfo.prototype.resource = "";
        ClientVersionInfo.prototype["package"] = "";

        ClientVersionInfo.create = function create(properties) {
            return new ClientVersionInfo(properties);
        };

        ClientVersionInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.resource != null && Object.hasOwnProperty.call(message, "resource"))
                writer.uint32(10).string(message.resource);
            if (message["package"] != null && Object.hasOwnProperty.call(message, "package"))
                writer.uint32(18).string(message["package"]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ClientVersionInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ClientVersionInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ClientVersionInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.resource = value;
                        else
                            delete message.resource;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message["package"] = value;
                        else
                            delete message["package"];
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ClientVersionInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ClientVersionInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.resource != null && message.hasOwnProperty("resource"))
                if (!$util.isString(message.resource))
                    return "resource: string expected";
            if (message["package"] != null && message.hasOwnProperty("package"))
                if (!$util.isString(message["package"]))
                    return "package: string expected";
            return null;
        };

        ClientVersionInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ClientVersionInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ClientVersionInfo();
            if (object.resource != null)
                if (typeof object.resource !== "string" || object.resource.length)
                    message.resource = String(object.resource);
            if (object["package"] != null)
                if (typeof object["package"] !== "string" || object["package"].length)
                    message["package"] = String(object["package"]);
            return message;
        };

        ClientVersionInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.resource = "";
                object["package"] = "";
            }
            if (message.resource != null && message.hasOwnProperty("resource"))
                object.resource = message.resource;
            if (message["package"] != null && message.hasOwnProperty("package"))
                object["package"] = message["package"];
            return object;
        };

        ClientVersionInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientVersionInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ClientVersionInfo";
        };

        return ClientVersionInfo;
    })();

    lq.GamePlayerState = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NULL"] = 0;
        values[valuesById[1] = "AUTH"] = 1;
        values[valuesById[2] = "SYNCING"] = 2;
        values[valuesById[3] = "READY"] = 3;
        return values;
    })();

    lq.Announcement = (function() {

        function Announcement(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Announcement.prototype.id = 0;
        Announcement.prototype.title = "";
        Announcement.prototype.content = "";
        Announcement.prototype.header_image = "";

        Announcement.create = function create(properties) {
            return new Announcement(properties);
        };

        Announcement.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(18).string(message.title);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(26).string(message.content);
            if (message.header_image != null && Object.hasOwnProperty.call(message, "header_image"))
                writer.uint32(34).string(message.header_image);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Announcement.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Announcement.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Announcement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.header_image = value;
                        else
                            delete message.header_image;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Announcement.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Announcement.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.header_image != null && message.hasOwnProperty("header_image"))
                if (!$util.isString(message.header_image))
                    return "header_image: string expected";
            return null;
        };

        Announcement.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Announcement)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Announcement();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.header_image != null)
                if (typeof object.header_image !== "string" || object.header_image.length)
                    message.header_image = String(object.header_image);
            return message;
        };

        Announcement.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.title = "";
                object.content = "";
                object.header_image = "";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.header_image != null && message.hasOwnProperty("header_image"))
                object.header_image = message.header_image;
            return object;
        };

        Announcement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Announcement.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Announcement";
        };

        return Announcement;
    })();

    lq.TaskProgress = (function() {

        function TaskProgress(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TaskProgress.prototype.id = 0;
        TaskProgress.prototype.counter = 0;
        TaskProgress.prototype.achieved = false;
        TaskProgress.prototype.rewarded = false;
        TaskProgress.prototype.failed = false;
        TaskProgress.prototype.rewarded_time = 0;

        TaskProgress.create = function create(properties) {
            return new TaskProgress(properties);
        };

        TaskProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                writer.uint32(16).uint32(message.counter);
            if (message.achieved != null && Object.hasOwnProperty.call(message, "achieved"))
                writer.uint32(24).bool(message.achieved);
            if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                writer.uint32(32).bool(message.rewarded);
            if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                writer.uint32(40).bool(message.failed);
            if (message.rewarded_time != null && Object.hasOwnProperty.call(message, "rewarded_time"))
                writer.uint32(48).uint32(message.rewarded_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TaskProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TaskProgress.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TaskProgress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.counter = value;
                        else
                            delete message.counter;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.achieved = value;
                        else
                            delete message.achieved;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rewarded = value;
                        else
                            delete message.rewarded;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed = value;
                        else
                            delete message.failed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rewarded_time = value;
                        else
                            delete message.rewarded_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TaskProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TaskProgress.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.counter != null && message.hasOwnProperty("counter"))
                if (!$util.isInteger(message.counter))
                    return "counter: integer expected";
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                if (typeof message.achieved !== "boolean")
                    return "achieved: boolean expected";
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                if (typeof message.rewarded !== "boolean")
                    return "rewarded: boolean expected";
            if (message.failed != null && message.hasOwnProperty("failed"))
                if (typeof message.failed !== "boolean")
                    return "failed: boolean expected";
            if (message.rewarded_time != null && message.hasOwnProperty("rewarded_time"))
                if (!$util.isInteger(message.rewarded_time))
                    return "rewarded_time: integer expected";
            return null;
        };

        TaskProgress.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TaskProgress)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TaskProgress();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.counter != null)
                if (Number(object.counter) !== 0)
                    message.counter = object.counter >>> 0;
            if (object.achieved != null)
                if (object.achieved)
                    message.achieved = Boolean(object.achieved);
            if (object.rewarded != null)
                if (object.rewarded)
                    message.rewarded = Boolean(object.rewarded);
            if (object.failed != null)
                if (object.failed)
                    message.failed = Boolean(object.failed);
            if (object.rewarded_time != null)
                if (Number(object.rewarded_time) !== 0)
                    message.rewarded_time = object.rewarded_time >>> 0;
            return message;
        };

        TaskProgress.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.counter = 0;
                object.achieved = false;
                object.rewarded = false;
                object.failed = false;
                object.rewarded_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.counter != null && message.hasOwnProperty("counter"))
                object.counter = message.counter;
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                object.achieved = message.achieved;
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                object.rewarded = message.rewarded;
            if (message.failed != null && message.hasOwnProperty("failed"))
                object.failed = message.failed;
            if (message.rewarded_time != null && message.hasOwnProperty("rewarded_time"))
                object.rewarded_time = message.rewarded_time;
            return object;
        };

        TaskProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TaskProgress.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TaskProgress";
        };

        return TaskProgress;
    })();

    lq.GameConfig = (function() {

        function GameConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameConfig.prototype.category = 0;
        GameConfig.prototype.mode = null;
        GameConfig.prototype.meta = null;

        GameConfig.create = function create(properties) {
            return new GameConfig(properties);
        };

        GameConfig.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(8).uint32(message.category);
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                $root.lq.GameMode.encode(message.mode, writer.uint32(18).fork()).ldelim();
            if (message.meta != null && Object.hasOwnProperty.call(message, "meta"))
                $root.lq.GameMetaData.encode(message.meta, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameConfig.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameConfig(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.mode = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.mode);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.meta = $root.lq.GameMetaData.decode(reader, reader.uint32(), undefined, _depth + 1, message.meta);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameConfig.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.mode != null && message.hasOwnProperty("mode")) {
                let error = $root.lq.GameMode.verify(message.mode, _depth + 1);
                if (error)
                    return "mode." + error;
            }
            if (message.meta != null && message.hasOwnProperty("meta")) {
                let error = $root.lq.GameMetaData.verify(message.meta, _depth + 1);
                if (error)
                    return "meta." + error;
            }
            return null;
        };

        GameConfig.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameConfig)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameConfig();
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.mode != null) {
                if (typeof object.mode !== "object")
                    throw TypeError(".lq.GameConfig.mode: object expected");
                message.mode = $root.lq.GameMode.fromObject(object.mode, _depth + 1);
            }
            if (object.meta != null) {
                if (typeof object.meta !== "object")
                    throw TypeError(".lq.GameConfig.meta: object expected");
                message.meta = $root.lq.GameMetaData.fromObject(object.meta, _depth + 1);
            }
            return message;
        };

        GameConfig.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.category = 0;
                object.mode = null;
                object.meta = null;
            }
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = $root.lq.GameMode.toObject(message.mode, options);
            if (message.meta != null && message.hasOwnProperty("meta"))
                object.meta = $root.lq.GameMetaData.toObject(message.meta, options);
            return object;
        };

        GameConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameConfig.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameConfig";
        };

        return GameConfig;
    })();

    lq.RPGState = (function() {

        function RPGState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RPGState.prototype.player_damaged = 0;
        RPGState.prototype.monster_damaged = 0;
        RPGState.prototype.monster_seq = 0;

        RPGState.create = function create(properties) {
            return new RPGState(properties);
        };

        RPGState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.player_damaged != null && Object.hasOwnProperty.call(message, "player_damaged"))
                writer.uint32(8).uint32(message.player_damaged);
            if (message.monster_damaged != null && Object.hasOwnProperty.call(message, "monster_damaged"))
                writer.uint32(16).uint32(message.monster_damaged);
            if (message.monster_seq != null && Object.hasOwnProperty.call(message, "monster_seq"))
                writer.uint32(24).uint32(message.monster_seq);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RPGState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RPGState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RPGState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.player_damaged = value;
                        else
                            delete message.player_damaged;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.monster_damaged = value;
                        else
                            delete message.monster_damaged;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.monster_seq = value;
                        else
                            delete message.monster_seq;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RPGState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RPGState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_damaged != null && message.hasOwnProperty("player_damaged"))
                if (!$util.isInteger(message.player_damaged))
                    return "player_damaged: integer expected";
            if (message.monster_damaged != null && message.hasOwnProperty("monster_damaged"))
                if (!$util.isInteger(message.monster_damaged))
                    return "monster_damaged: integer expected";
            if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                if (!$util.isInteger(message.monster_seq))
                    return "monster_seq: integer expected";
            return null;
        };

        RPGState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RPGState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RPGState();
            if (object.player_damaged != null)
                if (Number(object.player_damaged) !== 0)
                    message.player_damaged = object.player_damaged >>> 0;
            if (object.monster_damaged != null)
                if (Number(object.monster_damaged) !== 0)
                    message.monster_damaged = object.monster_damaged >>> 0;
            if (object.monster_seq != null)
                if (Number(object.monster_seq) !== 0)
                    message.monster_seq = object.monster_seq >>> 0;
            return message;
        };

        RPGState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.player_damaged = 0;
                object.monster_damaged = 0;
                object.monster_seq = 0;
            }
            if (message.player_damaged != null && message.hasOwnProperty("player_damaged"))
                object.player_damaged = message.player_damaged;
            if (message.monster_damaged != null && message.hasOwnProperty("monster_damaged"))
                object.monster_damaged = message.monster_damaged;
            if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                object.monster_seq = message.monster_seq;
            return object;
        };

        RPGState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RPGState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RPGState";
        };

        return RPGState;
    })();

    lq.RPGActivity = (function() {

        function RPGActivity(properties) {
            this.received_rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RPGActivity.prototype.activity_id = 0;
        RPGActivity.prototype.last_show_uuid = "";
        RPGActivity.prototype.last_played_uuid = "";
        RPGActivity.prototype.current_state = null;
        RPGActivity.prototype.last_show_state = null;
        RPGActivity.prototype.received_rewards = $util.emptyArray;
        RPGActivity.prototype.last_show_id = 0;

        RPGActivity.create = function create(properties) {
            return new RPGActivity(properties);
        };

        RPGActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.last_show_uuid != null && Object.hasOwnProperty.call(message, "last_show_uuid"))
                writer.uint32(42).string(message.last_show_uuid);
            if (message.last_played_uuid != null && Object.hasOwnProperty.call(message, "last_played_uuid"))
                writer.uint32(50).string(message.last_played_uuid);
            if (message.current_state != null && Object.hasOwnProperty.call(message, "current_state"))
                $root.lq.RPGState.encode(message.current_state, writer.uint32(58).fork()).ldelim();
            if (message.last_show_state != null && Object.hasOwnProperty.call(message, "last_show_state"))
                $root.lq.RPGState.encode(message.last_show_state, writer.uint32(66).fork()).ldelim();
            if (message.received_rewards != null && message.received_rewards.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.received_rewards.length; ++i)
                    writer.uint32(message.received_rewards[i]);
                writer.ldelim();
            }
            if (message.last_show_id != null && Object.hasOwnProperty.call(message, "last_show_id"))
                writer.uint32(80).uint32(message.last_show_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RPGActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RPGActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RPGActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.last_show_uuid = value;
                        else
                            delete message.last_show_uuid;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.last_played_uuid = value;
                        else
                            delete message.last_played_uuid;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.current_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.current_state);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.last_show_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.last_show_state);
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.received_rewards && message.received_rewards.length))
                                message.received_rewards = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.received_rewards.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.received_rewards && message.received_rewards.length))
                            message.received_rewards = [];
                        message.received_rewards.push(reader.uint32());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_show_id = value;
                        else
                            delete message.last_show_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RPGActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RPGActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.last_show_uuid != null && message.hasOwnProperty("last_show_uuid"))
                if (!$util.isString(message.last_show_uuid))
                    return "last_show_uuid: string expected";
            if (message.last_played_uuid != null && message.hasOwnProperty("last_played_uuid"))
                if (!$util.isString(message.last_played_uuid))
                    return "last_played_uuid: string expected";
            if (message.current_state != null && message.hasOwnProperty("current_state")) {
                let error = $root.lq.RPGState.verify(message.current_state, _depth + 1);
                if (error)
                    return "current_state." + error;
            }
            if (message.last_show_state != null && message.hasOwnProperty("last_show_state")) {
                let error = $root.lq.RPGState.verify(message.last_show_state, _depth + 1);
                if (error)
                    return "last_show_state." + error;
            }
            if (message.received_rewards != null && message.hasOwnProperty("received_rewards")) {
                if (!Array.isArray(message.received_rewards))
                    return "received_rewards: array expected";
                for (let i = 0; i < message.received_rewards.length; ++i)
                    if (!$util.isInteger(message.received_rewards[i]))
                        return "received_rewards: integer[] expected";
            }
            if (message.last_show_id != null && message.hasOwnProperty("last_show_id"))
                if (!$util.isInteger(message.last_show_id))
                    return "last_show_id: integer expected";
            return null;
        };

        RPGActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RPGActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RPGActivity();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.last_show_uuid != null)
                if (typeof object.last_show_uuid !== "string" || object.last_show_uuid.length)
                    message.last_show_uuid = String(object.last_show_uuid);
            if (object.last_played_uuid != null)
                if (typeof object.last_played_uuid !== "string" || object.last_played_uuid.length)
                    message.last_played_uuid = String(object.last_played_uuid);
            if (object.current_state != null) {
                if (typeof object.current_state !== "object")
                    throw TypeError(".lq.RPGActivity.current_state: object expected");
                message.current_state = $root.lq.RPGState.fromObject(object.current_state, _depth + 1);
            }
            if (object.last_show_state != null) {
                if (typeof object.last_show_state !== "object")
                    throw TypeError(".lq.RPGActivity.last_show_state: object expected");
                message.last_show_state = $root.lq.RPGState.fromObject(object.last_show_state, _depth + 1);
            }
            if (object.received_rewards) {
                if (!Array.isArray(object.received_rewards))
                    throw TypeError(".lq.RPGActivity.received_rewards: array expected");
                message.received_rewards = Array(object.received_rewards.length);
                for (let i = 0; i < object.received_rewards.length; ++i)
                    message.received_rewards[i] = object.received_rewards[i] >>> 0;
            }
            if (object.last_show_id != null)
                if (Number(object.last_show_id) !== 0)
                    message.last_show_id = object.last_show_id >>> 0;
            return message;
        };

        RPGActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.received_rewards = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.last_show_uuid = "";
                object.last_played_uuid = "";
                object.current_state = null;
                object.last_show_state = null;
                object.last_show_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.last_show_uuid != null && message.hasOwnProperty("last_show_uuid"))
                object.last_show_uuid = message.last_show_uuid;
            if (message.last_played_uuid != null && message.hasOwnProperty("last_played_uuid"))
                object.last_played_uuid = message.last_played_uuid;
            if (message.current_state != null && message.hasOwnProperty("current_state"))
                object.current_state = $root.lq.RPGState.toObject(message.current_state, options);
            if (message.last_show_state != null && message.hasOwnProperty("last_show_state"))
                object.last_show_state = $root.lq.RPGState.toObject(message.last_show_state, options);
            if (message.received_rewards && message.received_rewards.length) {
                object.received_rewards = Array(message.received_rewards.length);
                for (let j = 0; j < message.received_rewards.length; ++j)
                    object.received_rewards[j] = message.received_rewards[j];
            }
            if (message.last_show_id != null && message.hasOwnProperty("last_show_id"))
                object.last_show_id = message.last_show_id;
            return object;
        };

        RPGActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RPGActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RPGActivity";
        };

        return RPGActivity;
    })();

    lq.ActivityArenaData = (function() {

        function ActivityArenaData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityArenaData.prototype.win_count = 0;
        ActivityArenaData.prototype.lose_count = 0;
        ActivityArenaData.prototype.activity_id = 0;
        ActivityArenaData.prototype.enter_time = 0;
        ActivityArenaData.prototype.daily_enter_count = 0;
        ActivityArenaData.prototype.daily_enter_time = 0;
        ActivityArenaData.prototype.max_win_count = 0;
        ActivityArenaData.prototype.total_win_count = 0;

        ActivityArenaData.create = function create(properties) {
            return new ActivityArenaData(properties);
        };

        ActivityArenaData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.win_count != null && Object.hasOwnProperty.call(message, "win_count"))
                writer.uint32(8).uint32(message.win_count);
            if (message.lose_count != null && Object.hasOwnProperty.call(message, "lose_count"))
                writer.uint32(16).uint32(message.lose_count);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(24).uint32(message.activity_id);
            if (message.enter_time != null && Object.hasOwnProperty.call(message, "enter_time"))
                writer.uint32(32).uint32(message.enter_time);
            if (message.daily_enter_count != null && Object.hasOwnProperty.call(message, "daily_enter_count"))
                writer.uint32(40).uint32(message.daily_enter_count);
            if (message.daily_enter_time != null && Object.hasOwnProperty.call(message, "daily_enter_time"))
                writer.uint32(48).uint32(message.daily_enter_time);
            if (message.max_win_count != null && Object.hasOwnProperty.call(message, "max_win_count"))
                writer.uint32(56).uint32(message.max_win_count);
            if (message.total_win_count != null && Object.hasOwnProperty.call(message, "total_win_count"))
                writer.uint32(64).uint32(message.total_win_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityArenaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityArenaData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityArenaData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.win_count = value;
                        else
                            delete message.win_count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.lose_count = value;
                        else
                            delete message.lose_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enter_time = value;
                        else
                            delete message.enter_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.daily_enter_count = value;
                        else
                            delete message.daily_enter_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.daily_enter_time = value;
                        else
                            delete message.daily_enter_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_win_count = value;
                        else
                            delete message.max_win_count;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_win_count = value;
                        else
                            delete message.total_win_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityArenaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityArenaData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.win_count != null && message.hasOwnProperty("win_count"))
                if (!$util.isInteger(message.win_count))
                    return "win_count: integer expected";
            if (message.lose_count != null && message.hasOwnProperty("lose_count"))
                if (!$util.isInteger(message.lose_count))
                    return "lose_count: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.enter_time != null && message.hasOwnProperty("enter_time"))
                if (!$util.isInteger(message.enter_time))
                    return "enter_time: integer expected";
            if (message.daily_enter_count != null && message.hasOwnProperty("daily_enter_count"))
                if (!$util.isInteger(message.daily_enter_count))
                    return "daily_enter_count: integer expected";
            if (message.daily_enter_time != null && message.hasOwnProperty("daily_enter_time"))
                if (!$util.isInteger(message.daily_enter_time))
                    return "daily_enter_time: integer expected";
            if (message.max_win_count != null && message.hasOwnProperty("max_win_count"))
                if (!$util.isInteger(message.max_win_count))
                    return "max_win_count: integer expected";
            if (message.total_win_count != null && message.hasOwnProperty("total_win_count"))
                if (!$util.isInteger(message.total_win_count))
                    return "total_win_count: integer expected";
            return null;
        };

        ActivityArenaData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityArenaData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityArenaData();
            if (object.win_count != null)
                if (Number(object.win_count) !== 0)
                    message.win_count = object.win_count >>> 0;
            if (object.lose_count != null)
                if (Number(object.lose_count) !== 0)
                    message.lose_count = object.lose_count >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.enter_time != null)
                if (Number(object.enter_time) !== 0)
                    message.enter_time = object.enter_time >>> 0;
            if (object.daily_enter_count != null)
                if (Number(object.daily_enter_count) !== 0)
                    message.daily_enter_count = object.daily_enter_count >>> 0;
            if (object.daily_enter_time != null)
                if (Number(object.daily_enter_time) !== 0)
                    message.daily_enter_time = object.daily_enter_time >>> 0;
            if (object.max_win_count != null)
                if (Number(object.max_win_count) !== 0)
                    message.max_win_count = object.max_win_count >>> 0;
            if (object.total_win_count != null)
                if (Number(object.total_win_count) !== 0)
                    message.total_win_count = object.total_win_count >>> 0;
            return message;
        };

        ActivityArenaData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.win_count = 0;
                object.lose_count = 0;
                object.activity_id = 0;
                object.enter_time = 0;
                object.daily_enter_count = 0;
                object.daily_enter_time = 0;
                object.max_win_count = 0;
                object.total_win_count = 0;
            }
            if (message.win_count != null && message.hasOwnProperty("win_count"))
                object.win_count = message.win_count;
            if (message.lose_count != null && message.hasOwnProperty("lose_count"))
                object.lose_count = message.lose_count;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.enter_time != null && message.hasOwnProperty("enter_time"))
                object.enter_time = message.enter_time;
            if (message.daily_enter_count != null && message.hasOwnProperty("daily_enter_count"))
                object.daily_enter_count = message.daily_enter_count;
            if (message.daily_enter_time != null && message.hasOwnProperty("daily_enter_time"))
                object.daily_enter_time = message.daily_enter_time;
            if (message.max_win_count != null && message.hasOwnProperty("max_win_count"))
                object.max_win_count = message.max_win_count;
            if (message.total_win_count != null && message.hasOwnProperty("total_win_count"))
                object.total_win_count = message.total_win_count;
            return object;
        };

        ActivityArenaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityArenaData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityArenaData";
        };

        return ActivityArenaData;
    })();

    lq.FeedActivityData = (function() {

        function FeedActivityData(properties) {
            this.gift_inbox = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FeedActivityData.prototype.activity_id = 0;
        FeedActivityData.prototype.feed_count = 0;
        FeedActivityData.prototype.friend_receive_data = null;
        FeedActivityData.prototype.friend_send_data = null;
        FeedActivityData.prototype.gift_inbox = $util.emptyArray;

        FeedActivityData.create = function create(properties) {
            return new FeedActivityData(properties);
        };

        FeedActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.feed_count != null && Object.hasOwnProperty.call(message, "feed_count"))
                writer.uint32(16).uint32(message.feed_count);
            if (message.friend_receive_data != null && Object.hasOwnProperty.call(message, "friend_receive_data"))
                $root.lq.FeedActivityData.CountWithTimeData.encode(message.friend_receive_data, writer.uint32(26).fork()).ldelim();
            if (message.friend_send_data != null && Object.hasOwnProperty.call(message, "friend_send_data"))
                $root.lq.FeedActivityData.CountWithTimeData.encode(message.friend_send_data, writer.uint32(34).fork()).ldelim();
            if (message.gift_inbox != null && message.gift_inbox.length)
                for (let i = 0; i < message.gift_inbox.length; ++i)
                    $root.lq.FeedActivityData.GiftBoxData.encode(message.gift_inbox[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FeedActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FeedActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FeedActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.feed_count = value;
                        else
                            delete message.feed_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.friend_receive_data = $root.lq.FeedActivityData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_receive_data);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.friend_send_data = $root.lq.FeedActivityData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_send_data);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gift_inbox && message.gift_inbox.length))
                            message.gift_inbox = [];
                        message.gift_inbox.push($root.lq.FeedActivityData.GiftBoxData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FeedActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FeedActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                if (!$util.isInteger(message.feed_count))
                    return "feed_count: integer expected";
            if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data")) {
                let error = $root.lq.FeedActivityData.CountWithTimeData.verify(message.friend_receive_data, _depth + 1);
                if (error)
                    return "friend_receive_data." + error;
            }
            if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data")) {
                let error = $root.lq.FeedActivityData.CountWithTimeData.verify(message.friend_send_data, _depth + 1);
                if (error)
                    return "friend_send_data." + error;
            }
            if (message.gift_inbox != null && message.hasOwnProperty("gift_inbox")) {
                if (!Array.isArray(message.gift_inbox))
                    return "gift_inbox: array expected";
                for (let i = 0; i < message.gift_inbox.length; ++i) {
                    let error = $root.lq.FeedActivityData.GiftBoxData.verify(message.gift_inbox[i], _depth + 1);
                    if (error)
                        return "gift_inbox." + error;
                }
            }
            return null;
        };

        FeedActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FeedActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FeedActivityData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.feed_count != null)
                if (Number(object.feed_count) !== 0)
                    message.feed_count = object.feed_count >>> 0;
            if (object.friend_receive_data != null) {
                if (typeof object.friend_receive_data !== "object")
                    throw TypeError(".lq.FeedActivityData.friend_receive_data: object expected");
                message.friend_receive_data = $root.lq.FeedActivityData.CountWithTimeData.fromObject(object.friend_receive_data, _depth + 1);
            }
            if (object.friend_send_data != null) {
                if (typeof object.friend_send_data !== "object")
                    throw TypeError(".lq.FeedActivityData.friend_send_data: object expected");
                message.friend_send_data = $root.lq.FeedActivityData.CountWithTimeData.fromObject(object.friend_send_data, _depth + 1);
            }
            if (object.gift_inbox) {
                if (!Array.isArray(object.gift_inbox))
                    throw TypeError(".lq.FeedActivityData.gift_inbox: array expected");
                message.gift_inbox = Array(object.gift_inbox.length);
                for (let i = 0; i < object.gift_inbox.length; ++i) {
                    if (typeof object.gift_inbox[i] !== "object")
                        throw TypeError(".lq.FeedActivityData.gift_inbox: object expected");
                    message.gift_inbox[i] = $root.lq.FeedActivityData.GiftBoxData.fromObject(object.gift_inbox[i], _depth + 1);
                }
            }
            return message;
        };

        FeedActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gift_inbox = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.feed_count = 0;
                object.friend_receive_data = null;
                object.friend_send_data = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                object.feed_count = message.feed_count;
            if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data"))
                object.friend_receive_data = $root.lq.FeedActivityData.CountWithTimeData.toObject(message.friend_receive_data, options);
            if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data"))
                object.friend_send_data = $root.lq.FeedActivityData.CountWithTimeData.toObject(message.friend_send_data, options);
            if (message.gift_inbox && message.gift_inbox.length) {
                object.gift_inbox = Array(message.gift_inbox.length);
                for (let j = 0; j < message.gift_inbox.length; ++j)
                    object.gift_inbox[j] = $root.lq.FeedActivityData.GiftBoxData.toObject(message.gift_inbox[j], options);
            }
            return object;
        };

        FeedActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FeedActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FeedActivityData";
        };

        FeedActivityData.CountWithTimeData = (function() {

            function CountWithTimeData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CountWithTimeData.prototype.count = 0;
            CountWithTimeData.prototype.last_update_time = 0;

            CountWithTimeData.create = function create(properties) {
                return new CountWithTimeData(properties);
            };

            CountWithTimeData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.last_update_time != null && Object.hasOwnProperty.call(message, "last_update_time"))
                    writer.uint32(16).uint32(message.last_update_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CountWithTimeData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CountWithTimeData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FeedActivityData.CountWithTimeData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_update_time = value;
                            else
                                delete message.last_update_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CountWithTimeData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CountWithTimeData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    if (!$util.isInteger(message.last_update_time))
                        return "last_update_time: integer expected";
                return null;
            };

            CountWithTimeData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.FeedActivityData.CountWithTimeData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.FeedActivityData.CountWithTimeData();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.last_update_time != null)
                    if (Number(object.last_update_time) !== 0)
                        message.last_update_time = object.last_update_time >>> 0;
                return message;
            };

            CountWithTimeData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.count = 0;
                    object.last_update_time = 0;
                }
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    object.last_update_time = message.last_update_time;
                return object;
            };

            CountWithTimeData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CountWithTimeData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.FeedActivityData.CountWithTimeData";
            };

            return CountWithTimeData;
        })();

        FeedActivityData.GiftBoxData = (function() {

            function GiftBoxData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GiftBoxData.prototype.id = 0;
            GiftBoxData.prototype.item_id = 0;
            GiftBoxData.prototype.count = 0;
            GiftBoxData.prototype.from_account_id = 0;
            GiftBoxData.prototype.time = 0;
            GiftBoxData.prototype.received = 0;

            GiftBoxData.create = function create(properties) {
                return new GiftBoxData(properties);
            };

            GiftBoxData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(16).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(24).uint32(message.count);
                if (message.from_account_id != null && Object.hasOwnProperty.call(message, "from_account_id"))
                    writer.uint32(32).uint32(message.from_account_id);
                if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                    writer.uint32(40).uint32(message.time);
                if (message.received != null && Object.hasOwnProperty.call(message, "received"))
                    writer.uint32(48).uint32(message.received);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GiftBoxData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GiftBoxData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FeedActivityData.GiftBoxData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from_account_id = value;
                            else
                                delete message.from_account_id;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.received = value;
                            else
                                delete message.received;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GiftBoxData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GiftBoxData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    if (!$util.isInteger(message.from_account_id))
                        return "from_account_id: integer expected";
                if (message.time != null && message.hasOwnProperty("time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                if (message.received != null && message.hasOwnProperty("received"))
                    if (!$util.isInteger(message.received))
                        return "received: integer expected";
                return null;
            };

            GiftBoxData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.FeedActivityData.GiftBoxData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.FeedActivityData.GiftBoxData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.from_account_id != null)
                    if (Number(object.from_account_id) !== 0)
                        message.from_account_id = object.from_account_id >>> 0;
                if (object.time != null)
                    if (Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                if (object.received != null)
                    if (Number(object.received) !== 0)
                        message.received = object.received >>> 0;
                return message;
            };

            GiftBoxData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.item_id = 0;
                    object.count = 0;
                    object.from_account_id = 0;
                    object.time = 0;
                    object.received = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    object.from_account_id = message.from_account_id;
                if (message.time != null && message.hasOwnProperty("time"))
                    object.time = message.time;
                if (message.received != null && message.hasOwnProperty("received"))
                    object.received = message.received;
                return object;
            };

            GiftBoxData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GiftBoxData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.FeedActivityData.GiftBoxData";
            };

            return GiftBoxData;
        })();

        return FeedActivityData;
    })();

    lq.SegmentTaskProgress = (function() {

        function SegmentTaskProgress(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SegmentTaskProgress.prototype.id = 0;
        SegmentTaskProgress.prototype.counter = 0;
        SegmentTaskProgress.prototype.achieved = false;
        SegmentTaskProgress.prototype.rewarded = false;
        SegmentTaskProgress.prototype.failed = false;
        SegmentTaskProgress.prototype.reward_count = 0;
        SegmentTaskProgress.prototype.achieved_count = 0;

        SegmentTaskProgress.create = function create(properties) {
            return new SegmentTaskProgress(properties);
        };

        SegmentTaskProgress.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                writer.uint32(16).uint32(message.counter);
            if (message.achieved != null && Object.hasOwnProperty.call(message, "achieved"))
                writer.uint32(24).bool(message.achieved);
            if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                writer.uint32(32).bool(message.rewarded);
            if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                writer.uint32(40).bool(message.failed);
            if (message.reward_count != null && Object.hasOwnProperty.call(message, "reward_count"))
                writer.uint32(48).uint32(message.reward_count);
            if (message.achieved_count != null && Object.hasOwnProperty.call(message, "achieved_count"))
                writer.uint32(56).uint32(message.achieved_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SegmentTaskProgress.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SegmentTaskProgress.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SegmentTaskProgress(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.counter = value;
                        else
                            delete message.counter;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.achieved = value;
                        else
                            delete message.achieved;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rewarded = value;
                        else
                            delete message.rewarded;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed = value;
                        else
                            delete message.failed;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reward_count = value;
                        else
                            delete message.reward_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.achieved_count = value;
                        else
                            delete message.achieved_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SegmentTaskProgress.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SegmentTaskProgress.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.counter != null && message.hasOwnProperty("counter"))
                if (!$util.isInteger(message.counter))
                    return "counter: integer expected";
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                if (typeof message.achieved !== "boolean")
                    return "achieved: boolean expected";
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                if (typeof message.rewarded !== "boolean")
                    return "rewarded: boolean expected";
            if (message.failed != null && message.hasOwnProperty("failed"))
                if (typeof message.failed !== "boolean")
                    return "failed: boolean expected";
            if (message.reward_count != null && message.hasOwnProperty("reward_count"))
                if (!$util.isInteger(message.reward_count))
                    return "reward_count: integer expected";
            if (message.achieved_count != null && message.hasOwnProperty("achieved_count"))
                if (!$util.isInteger(message.achieved_count))
                    return "achieved_count: integer expected";
            return null;
        };

        SegmentTaskProgress.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SegmentTaskProgress)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SegmentTaskProgress();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.counter != null)
                if (Number(object.counter) !== 0)
                    message.counter = object.counter >>> 0;
            if (object.achieved != null)
                if (object.achieved)
                    message.achieved = Boolean(object.achieved);
            if (object.rewarded != null)
                if (object.rewarded)
                    message.rewarded = Boolean(object.rewarded);
            if (object.failed != null)
                if (object.failed)
                    message.failed = Boolean(object.failed);
            if (object.reward_count != null)
                if (Number(object.reward_count) !== 0)
                    message.reward_count = object.reward_count >>> 0;
            if (object.achieved_count != null)
                if (Number(object.achieved_count) !== 0)
                    message.achieved_count = object.achieved_count >>> 0;
            return message;
        };

        SegmentTaskProgress.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.counter = 0;
                object.achieved = false;
                object.rewarded = false;
                object.failed = false;
                object.reward_count = 0;
                object.achieved_count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.counter != null && message.hasOwnProperty("counter"))
                object.counter = message.counter;
            if (message.achieved != null && message.hasOwnProperty("achieved"))
                object.achieved = message.achieved;
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                object.rewarded = message.rewarded;
            if (message.failed != null && message.hasOwnProperty("failed"))
                object.failed = message.failed;
            if (message.reward_count != null && message.hasOwnProperty("reward_count"))
                object.reward_count = message.reward_count;
            if (message.achieved_count != null && message.hasOwnProperty("achieved_count"))
                object.achieved_count = message.achieved_count;
            return object;
        };

        SegmentTaskProgress.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SegmentTaskProgress.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SegmentTaskProgress";
        };

        return SegmentTaskProgress;
    })();

    lq.MineActivityData = (function() {

        function MineActivityData(properties) {
            this.dig_point = [];
            this.map = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MineActivityData.prototype.dig_point = $util.emptyArray;
        MineActivityData.prototype.map = $util.emptyArray;
        MineActivityData.prototype.id = 0;

        MineActivityData.create = function create(properties) {
            return new MineActivityData(properties);
        };

        MineActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dig_point != null && message.dig_point.length)
                for (let i = 0; i < message.dig_point.length; ++i)
                    $root.lq.Point.encode(message.dig_point[i], writer.uint32(10).fork()).ldelim();
            if (message.map != null && message.map.length)
                for (let i = 0; i < message.map.length; ++i)
                    $root.lq.MineReward.encode(message.map[i], writer.uint32(18).fork()).ldelim();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(24).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MineActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MineActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MineActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.dig_point && message.dig_point.length))
                            message.dig_point = [];
                        message.dig_point.push($root.lq.Point.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.map && message.map.length))
                            message.map = [];
                        message.map.push($root.lq.MineReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MineActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MineActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dig_point != null && message.hasOwnProperty("dig_point")) {
                if (!Array.isArray(message.dig_point))
                    return "dig_point: array expected";
                for (let i = 0; i < message.dig_point.length; ++i) {
                    let error = $root.lq.Point.verify(message.dig_point[i], _depth + 1);
                    if (error)
                        return "dig_point." + error;
                }
            }
            if (message.map != null && message.hasOwnProperty("map")) {
                if (!Array.isArray(message.map))
                    return "map: array expected";
                for (let i = 0; i < message.map.length; ++i) {
                    let error = $root.lq.MineReward.verify(message.map[i], _depth + 1);
                    if (error)
                        return "map." + error;
                }
            }
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        MineActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MineActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MineActivityData();
            if (object.dig_point) {
                if (!Array.isArray(object.dig_point))
                    throw TypeError(".lq.MineActivityData.dig_point: array expected");
                message.dig_point = Array(object.dig_point.length);
                for (let i = 0; i < object.dig_point.length; ++i) {
                    if (typeof object.dig_point[i] !== "object")
                        throw TypeError(".lq.MineActivityData.dig_point: object expected");
                    message.dig_point[i] = $root.lq.Point.fromObject(object.dig_point[i], _depth + 1);
                }
            }
            if (object.map) {
                if (!Array.isArray(object.map))
                    throw TypeError(".lq.MineActivityData.map: array expected");
                message.map = Array(object.map.length);
                for (let i = 0; i < object.map.length; ++i) {
                    if (typeof object.map[i] !== "object")
                        throw TypeError(".lq.MineActivityData.map: object expected");
                    message.map[i] = $root.lq.MineReward.fromObject(object.map[i], _depth + 1);
                }
            }
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        MineActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.dig_point = [];
                object.map = [];
            }
            if (options.defaults)
                object.id = 0;
            if (message.dig_point && message.dig_point.length) {
                object.dig_point = Array(message.dig_point.length);
                for (let j = 0; j < message.dig_point.length; ++j)
                    object.dig_point[j] = $root.lq.Point.toObject(message.dig_point[j], options);
            }
            if (message.map && message.map.length) {
                object.map = Array(message.map.length);
                for (let j = 0; j < message.map.length; ++j)
                    object.map[j] = $root.lq.MineReward.toObject(message.map[j], options);
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        MineActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MineActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MineActivityData";
        };

        return MineActivityData;
    })();

    lq.AccountActivityUpdate = (function() {

        function AccountActivityUpdate(properties) {
            this.mine_data = [];
            this.rpg_data = [];
            this.feed_data = [];
            this.spot_data = [];
            this.friend_gift_data = [];
            this.upgrade_data = [];
            this.gacha_data = [];
            this.simulation_data = [];
            this.combining_data = [];
            this.village_data = [];
            this.festival_data = [];
            this.island_data = [];
            this.story_data = [];
            this.choose_up_data = [];
            this.simulation_v2_data = [];
            this.quest_crew_data = [];
            this.shoot_data = [];
            this.bingo_data = [];
            this.snowball_data = [];
            this.choose_group_up_data = [];
            this.mmo_data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountActivityUpdate.prototype.mine_data = $util.emptyArray;
        AccountActivityUpdate.prototype.rpg_data = $util.emptyArray;
        AccountActivityUpdate.prototype.feed_data = $util.emptyArray;
        AccountActivityUpdate.prototype.spot_data = $util.emptyArray;
        AccountActivityUpdate.prototype.friend_gift_data = $util.emptyArray;
        AccountActivityUpdate.prototype.upgrade_data = $util.emptyArray;
        AccountActivityUpdate.prototype.gacha_data = $util.emptyArray;
        AccountActivityUpdate.prototype.simulation_data = $util.emptyArray;
        AccountActivityUpdate.prototype.combining_data = $util.emptyArray;
        AccountActivityUpdate.prototype.village_data = $util.emptyArray;
        AccountActivityUpdate.prototype.festival_data = $util.emptyArray;
        AccountActivityUpdate.prototype.island_data = $util.emptyArray;
        AccountActivityUpdate.prototype.story_data = $util.emptyArray;
        AccountActivityUpdate.prototype.choose_up_data = $util.emptyArray;
        AccountActivityUpdate.prototype.simulation_v2_data = $util.emptyArray;
        AccountActivityUpdate.prototype.quest_crew_data = $util.emptyArray;
        AccountActivityUpdate.prototype.shoot_data = $util.emptyArray;
        AccountActivityUpdate.prototype.bingo_data = $util.emptyArray;
        AccountActivityUpdate.prototype.snowball_data = $util.emptyArray;
        AccountActivityUpdate.prototype.choose_group_up_data = $util.emptyArray;
        AccountActivityUpdate.prototype.mmo_data = $util.emptyArray;
        AccountActivityUpdate.prototype.activity_buff = null;

        AccountActivityUpdate.create = function create(properties) {
            return new AccountActivityUpdate(properties);
        };

        AccountActivityUpdate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mine_data != null && message.mine_data.length)
                for (let i = 0; i < message.mine_data.length; ++i)
                    $root.lq.MineActivityData.encode(message.mine_data[i], writer.uint32(10).fork()).ldelim();
            if (message.rpg_data != null && message.rpg_data.length)
                for (let i = 0; i < message.rpg_data.length; ++i)
                    $root.lq.RPGActivity.encode(message.rpg_data[i], writer.uint32(18).fork()).ldelim();
            if (message.feed_data != null && message.feed_data.length)
                for (let i = 0; i < message.feed_data.length; ++i)
                    $root.lq.ActivityFeedData.encode(message.feed_data[i], writer.uint32(26).fork()).ldelim();
            if (message.spot_data != null && message.spot_data.length)
                for (let i = 0; i < message.spot_data.length; ++i)
                    $root.lq.ActivitySpotData.encode(message.spot_data[i], writer.uint32(34).fork()).ldelim();
            if (message.friend_gift_data != null && message.friend_gift_data.length)
                for (let i = 0; i < message.friend_gift_data.length; ++i)
                    $root.lq.ActivityFriendGiftData.encode(message.friend_gift_data[i], writer.uint32(42).fork()).ldelim();
            if (message.upgrade_data != null && message.upgrade_data.length)
                for (let i = 0; i < message.upgrade_data.length; ++i)
                    $root.lq.ActivityUpgradeData.encode(message.upgrade_data[i], writer.uint32(50).fork()).ldelim();
            if (message.gacha_data != null && message.gacha_data.length)
                for (let i = 0; i < message.gacha_data.length; ++i)
                    $root.lq.ActivityGachaUpdateData.encode(message.gacha_data[i], writer.uint32(58).fork()).ldelim();
            if (message.simulation_data != null && message.simulation_data.length)
                for (let i = 0; i < message.simulation_data.length; ++i)
                    $root.lq.ActivitySimulationData.encode(message.simulation_data[i], writer.uint32(66).fork()).ldelim();
            if (message.combining_data != null && message.combining_data.length)
                for (let i = 0; i < message.combining_data.length; ++i)
                    $root.lq.ActivityCombiningLQData.encode(message.combining_data[i], writer.uint32(74).fork()).ldelim();
            if (message.village_data != null && message.village_data.length)
                for (let i = 0; i < message.village_data.length; ++i)
                    $root.lq.ActivityVillageData.encode(message.village_data[i], writer.uint32(82).fork()).ldelim();
            if (message.festival_data != null && message.festival_data.length)
                for (let i = 0; i < message.festival_data.length; ++i)
                    $root.lq.ActivityFestivalData.encode(message.festival_data[i], writer.uint32(90).fork()).ldelim();
            if (message.island_data != null && message.island_data.length)
                for (let i = 0; i < message.island_data.length; ++i)
                    $root.lq.ActivityIslandData.encode(message.island_data[i], writer.uint32(98).fork()).ldelim();
            if (message.story_data != null && message.story_data.length)
                for (let i = 0; i < message.story_data.length; ++i)
                    $root.lq.ActivityStoryData.encode(message.story_data[i], writer.uint32(114).fork()).ldelim();
            if (message.choose_up_data != null && message.choose_up_data.length)
                for (let i = 0; i < message.choose_up_data.length; ++i)
                    $root.lq.ActivityChooseUpData.encode(message.choose_up_data[i], writer.uint32(122).fork()).ldelim();
            if (message.simulation_v2_data != null && message.simulation_v2_data.length)
                for (let i = 0; i < message.simulation_v2_data.length; ++i)
                    $root.lq.SimulationV2Data.encode(message.simulation_v2_data[i], writer.uint32(130).fork()).ldelim();
            if (message.quest_crew_data != null && message.quest_crew_data.length)
                for (let i = 0; i < message.quest_crew_data.length; ++i)
                    $root.lq.ActivityQuestCrewChanges.encode(message.quest_crew_data[i], writer.uint32(138).fork()).ldelim();
            if (message.shoot_data != null && message.shoot_data.length)
                for (let i = 0; i < message.shoot_data.length; ++i)
                    $root.lq.ActivityShootData.encode(message.shoot_data[i], writer.uint32(146).fork()).ldelim();
            if (message.bingo_data != null && message.bingo_data.length)
                for (let i = 0; i < message.bingo_data.length; ++i)
                    $root.lq.ActivityBingoData.encode(message.bingo_data[i], writer.uint32(154).fork()).ldelim();
            if (message.snowball_data != null && message.snowball_data.length)
                for (let i = 0; i < message.snowball_data.length; ++i)
                    $root.lq.ActivitySnowballValueChanges.encode(message.snowball_data[i], writer.uint32(162).fork()).ldelim();
            if (message.choose_group_up_data != null && message.choose_group_up_data.length)
                for (let i = 0; i < message.choose_group_up_data.length; ++i)
                    $root.lq.ActivityChooseGroupData.encode(message.choose_group_up_data[i], writer.uint32(170).fork()).ldelim();
            if (message.mmo_data != null && message.mmo_data.length)
                for (let i = 0; i < message.mmo_data.length; ++i)
                    $root.lq.ActivityMMODataChanges.encode(message.mmo_data[i], writer.uint32(178).fork()).ldelim();
            if (message.activity_buff != null && Object.hasOwnProperty.call(message, "activity_buff"))
                $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.encode(message.activity_buff, writer.uint32(186).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountActivityUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountActivityUpdate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountActivityUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mine_data && message.mine_data.length))
                            message.mine_data = [];
                        message.mine_data.push($root.lq.MineActivityData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rpg_data && message.rpg_data.length))
                            message.rpg_data = [];
                        message.rpg_data.push($root.lq.RPGActivity.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.feed_data && message.feed_data.length))
                            message.feed_data = [];
                        message.feed_data.push($root.lq.ActivityFeedData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.spot_data && message.spot_data.length))
                            message.spot_data = [];
                        message.spot_data.push($root.lq.ActivitySpotData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.friend_gift_data && message.friend_gift_data.length))
                            message.friend_gift_data = [];
                        message.friend_gift_data.push($root.lq.ActivityFriendGiftData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.upgrade_data && message.upgrade_data.length))
                            message.upgrade_data = [];
                        message.upgrade_data.push($root.lq.ActivityUpgradeData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gacha_data && message.gacha_data.length))
                            message.gacha_data = [];
                        message.gacha_data.push($root.lq.ActivityGachaUpdateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.simulation_data && message.simulation_data.length))
                            message.simulation_data = [];
                        message.simulation_data.push($root.lq.ActivitySimulationData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.combining_data && message.combining_data.length))
                            message.combining_data = [];
                        message.combining_data.push($root.lq.ActivityCombiningLQData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.village_data && message.village_data.length))
                            message.village_data = [];
                        message.village_data.push($root.lq.ActivityVillageData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.festival_data && message.festival_data.length))
                            message.festival_data = [];
                        message.festival_data.push($root.lq.ActivityFestivalData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.island_data && message.island_data.length))
                            message.island_data = [];
                        message.island_data.push($root.lq.ActivityIslandData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.story_data && message.story_data.length))
                            message.story_data = [];
                        message.story_data.push($root.lq.ActivityStoryData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.choose_up_data && message.choose_up_data.length))
                            message.choose_up_data = [];
                        message.choose_up_data.push($root.lq.ActivityChooseUpData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.simulation_v2_data && message.simulation_v2_data.length))
                            message.simulation_v2_data = [];
                        message.simulation_v2_data.push($root.lq.SimulationV2Data.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (!(message.quest_crew_data && message.quest_crew_data.length))
                            message.quest_crew_data = [];
                        message.quest_crew_data.push($root.lq.ActivityQuestCrewChanges.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if (!(message.shoot_data && message.shoot_data.length))
                            message.shoot_data = [];
                        message.shoot_data.push($root.lq.ActivityShootData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bingo_data && message.bingo_data.length))
                            message.bingo_data = [];
                        message.bingo_data.push($root.lq.ActivityBingoData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if (!(message.snowball_data && message.snowball_data.length))
                            message.snowball_data = [];
                        message.snowball_data.push($root.lq.ActivitySnowballValueChanges.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (!(message.choose_group_up_data && message.choose_group_up_data.length))
                            message.choose_group_up_data = [];
                        message.choose_group_up_data.push($root.lq.ActivityChooseGroupData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mmo_data && message.mmo_data.length))
                            message.mmo_data = [];
                        message.mmo_data.push($root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.activity_buff = $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_buff);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountActivityUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountActivityUpdate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mine_data != null && message.hasOwnProperty("mine_data")) {
                if (!Array.isArray(message.mine_data))
                    return "mine_data: array expected";
                for (let i = 0; i < message.mine_data.length; ++i) {
                    let error = $root.lq.MineActivityData.verify(message.mine_data[i], _depth + 1);
                    if (error)
                        return "mine_data." + error;
                }
            }
            if (message.rpg_data != null && message.hasOwnProperty("rpg_data")) {
                if (!Array.isArray(message.rpg_data))
                    return "rpg_data: array expected";
                for (let i = 0; i < message.rpg_data.length; ++i) {
                    let error = $root.lq.RPGActivity.verify(message.rpg_data[i], _depth + 1);
                    if (error)
                        return "rpg_data." + error;
                }
            }
            if (message.feed_data != null && message.hasOwnProperty("feed_data")) {
                if (!Array.isArray(message.feed_data))
                    return "feed_data: array expected";
                for (let i = 0; i < message.feed_data.length; ++i) {
                    let error = $root.lq.ActivityFeedData.verify(message.feed_data[i], _depth + 1);
                    if (error)
                        return "feed_data." + error;
                }
            }
            if (message.spot_data != null && message.hasOwnProperty("spot_data")) {
                if (!Array.isArray(message.spot_data))
                    return "spot_data: array expected";
                for (let i = 0; i < message.spot_data.length; ++i) {
                    let error = $root.lq.ActivitySpotData.verify(message.spot_data[i], _depth + 1);
                    if (error)
                        return "spot_data." + error;
                }
            }
            if (message.friend_gift_data != null && message.hasOwnProperty("friend_gift_data")) {
                if (!Array.isArray(message.friend_gift_data))
                    return "friend_gift_data: array expected";
                for (let i = 0; i < message.friend_gift_data.length; ++i) {
                    let error = $root.lq.ActivityFriendGiftData.verify(message.friend_gift_data[i], _depth + 1);
                    if (error)
                        return "friend_gift_data." + error;
                }
            }
            if (message.upgrade_data != null && message.hasOwnProperty("upgrade_data")) {
                if (!Array.isArray(message.upgrade_data))
                    return "upgrade_data: array expected";
                for (let i = 0; i < message.upgrade_data.length; ++i) {
                    let error = $root.lq.ActivityUpgradeData.verify(message.upgrade_data[i], _depth + 1);
                    if (error)
                        return "upgrade_data." + error;
                }
            }
            if (message.gacha_data != null && message.hasOwnProperty("gacha_data")) {
                if (!Array.isArray(message.gacha_data))
                    return "gacha_data: array expected";
                for (let i = 0; i < message.gacha_data.length; ++i) {
                    let error = $root.lq.ActivityGachaUpdateData.verify(message.gacha_data[i], _depth + 1);
                    if (error)
                        return "gacha_data." + error;
                }
            }
            if (message.simulation_data != null && message.hasOwnProperty("simulation_data")) {
                if (!Array.isArray(message.simulation_data))
                    return "simulation_data: array expected";
                for (let i = 0; i < message.simulation_data.length; ++i) {
                    let error = $root.lq.ActivitySimulationData.verify(message.simulation_data[i], _depth + 1);
                    if (error)
                        return "simulation_data." + error;
                }
            }
            if (message.combining_data != null && message.hasOwnProperty("combining_data")) {
                if (!Array.isArray(message.combining_data))
                    return "combining_data: array expected";
                for (let i = 0; i < message.combining_data.length; ++i) {
                    let error = $root.lq.ActivityCombiningLQData.verify(message.combining_data[i], _depth + 1);
                    if (error)
                        return "combining_data." + error;
                }
            }
            if (message.village_data != null && message.hasOwnProperty("village_data")) {
                if (!Array.isArray(message.village_data))
                    return "village_data: array expected";
                for (let i = 0; i < message.village_data.length; ++i) {
                    let error = $root.lq.ActivityVillageData.verify(message.village_data[i], _depth + 1);
                    if (error)
                        return "village_data." + error;
                }
            }
            if (message.festival_data != null && message.hasOwnProperty("festival_data")) {
                if (!Array.isArray(message.festival_data))
                    return "festival_data: array expected";
                for (let i = 0; i < message.festival_data.length; ++i) {
                    let error = $root.lq.ActivityFestivalData.verify(message.festival_data[i], _depth + 1);
                    if (error)
                        return "festival_data." + error;
                }
            }
            if (message.island_data != null && message.hasOwnProperty("island_data")) {
                if (!Array.isArray(message.island_data))
                    return "island_data: array expected";
                for (let i = 0; i < message.island_data.length; ++i) {
                    let error = $root.lq.ActivityIslandData.verify(message.island_data[i], _depth + 1);
                    if (error)
                        return "island_data." + error;
                }
            }
            if (message.story_data != null && message.hasOwnProperty("story_data")) {
                if (!Array.isArray(message.story_data))
                    return "story_data: array expected";
                for (let i = 0; i < message.story_data.length; ++i) {
                    let error = $root.lq.ActivityStoryData.verify(message.story_data[i], _depth + 1);
                    if (error)
                        return "story_data." + error;
                }
            }
            if (message.choose_up_data != null && message.hasOwnProperty("choose_up_data")) {
                if (!Array.isArray(message.choose_up_data))
                    return "choose_up_data: array expected";
                for (let i = 0; i < message.choose_up_data.length; ++i) {
                    let error = $root.lq.ActivityChooseUpData.verify(message.choose_up_data[i], _depth + 1);
                    if (error)
                        return "choose_up_data." + error;
                }
            }
            if (message.simulation_v2_data != null && message.hasOwnProperty("simulation_v2_data")) {
                if (!Array.isArray(message.simulation_v2_data))
                    return "simulation_v2_data: array expected";
                for (let i = 0; i < message.simulation_v2_data.length; ++i) {
                    let error = $root.lq.SimulationV2Data.verify(message.simulation_v2_data[i], _depth + 1);
                    if (error)
                        return "simulation_v2_data." + error;
                }
            }
            if (message.quest_crew_data != null && message.hasOwnProperty("quest_crew_data")) {
                if (!Array.isArray(message.quest_crew_data))
                    return "quest_crew_data: array expected";
                for (let i = 0; i < message.quest_crew_data.length; ++i) {
                    let error = $root.lq.ActivityQuestCrewChanges.verify(message.quest_crew_data[i], _depth + 1);
                    if (error)
                        return "quest_crew_data." + error;
                }
            }
            if (message.shoot_data != null && message.hasOwnProperty("shoot_data")) {
                if (!Array.isArray(message.shoot_data))
                    return "shoot_data: array expected";
                for (let i = 0; i < message.shoot_data.length; ++i) {
                    let error = $root.lq.ActivityShootData.verify(message.shoot_data[i], _depth + 1);
                    if (error)
                        return "shoot_data." + error;
                }
            }
            if (message.bingo_data != null && message.hasOwnProperty("bingo_data")) {
                if (!Array.isArray(message.bingo_data))
                    return "bingo_data: array expected";
                for (let i = 0; i < message.bingo_data.length; ++i) {
                    let error = $root.lq.ActivityBingoData.verify(message.bingo_data[i], _depth + 1);
                    if (error)
                        return "bingo_data." + error;
                }
            }
            if (message.snowball_data != null && message.hasOwnProperty("snowball_data")) {
                if (!Array.isArray(message.snowball_data))
                    return "snowball_data: array expected";
                for (let i = 0; i < message.snowball_data.length; ++i) {
                    let error = $root.lq.ActivitySnowballValueChanges.verify(message.snowball_data[i], _depth + 1);
                    if (error)
                        return "snowball_data." + error;
                }
            }
            if (message.choose_group_up_data != null && message.hasOwnProperty("choose_group_up_data")) {
                if (!Array.isArray(message.choose_group_up_data))
                    return "choose_group_up_data: array expected";
                for (let i = 0; i < message.choose_group_up_data.length; ++i) {
                    let error = $root.lq.ActivityChooseGroupData.verify(message.choose_group_up_data[i], _depth + 1);
                    if (error)
                        return "choose_group_up_data." + error;
                }
            }
            if (message.mmo_data != null && message.hasOwnProperty("mmo_data")) {
                if (!Array.isArray(message.mmo_data))
                    return "mmo_data: array expected";
                for (let i = 0; i < message.mmo_data.length; ++i) {
                    let error = $root.lq.ActivityMMODataChanges.verify(message.mmo_data[i], _depth + 1);
                    if (error)
                        return "mmo_data." + error;
                }
            }
            if (message.activity_buff != null && message.hasOwnProperty("activity_buff")) {
                let error = $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.verify(message.activity_buff, _depth + 1);
                if (error)
                    return "activity_buff." + error;
            }
            return null;
        };

        AccountActivityUpdate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountActivityUpdate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountActivityUpdate();
            if (object.mine_data) {
                if (!Array.isArray(object.mine_data))
                    throw TypeError(".lq.AccountActivityUpdate.mine_data: array expected");
                message.mine_data = Array(object.mine_data.length);
                for (let i = 0; i < object.mine_data.length; ++i) {
                    if (typeof object.mine_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.mine_data: object expected");
                    message.mine_data[i] = $root.lq.MineActivityData.fromObject(object.mine_data[i], _depth + 1);
                }
            }
            if (object.rpg_data) {
                if (!Array.isArray(object.rpg_data))
                    throw TypeError(".lq.AccountActivityUpdate.rpg_data: array expected");
                message.rpg_data = Array(object.rpg_data.length);
                for (let i = 0; i < object.rpg_data.length; ++i) {
                    if (typeof object.rpg_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.rpg_data: object expected");
                    message.rpg_data[i] = $root.lq.RPGActivity.fromObject(object.rpg_data[i], _depth + 1);
                }
            }
            if (object.feed_data) {
                if (!Array.isArray(object.feed_data))
                    throw TypeError(".lq.AccountActivityUpdate.feed_data: array expected");
                message.feed_data = Array(object.feed_data.length);
                for (let i = 0; i < object.feed_data.length; ++i) {
                    if (typeof object.feed_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.feed_data: object expected");
                    message.feed_data[i] = $root.lq.ActivityFeedData.fromObject(object.feed_data[i], _depth + 1);
                }
            }
            if (object.spot_data) {
                if (!Array.isArray(object.spot_data))
                    throw TypeError(".lq.AccountActivityUpdate.spot_data: array expected");
                message.spot_data = Array(object.spot_data.length);
                for (let i = 0; i < object.spot_data.length; ++i) {
                    if (typeof object.spot_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.spot_data: object expected");
                    message.spot_data[i] = $root.lq.ActivitySpotData.fromObject(object.spot_data[i], _depth + 1);
                }
            }
            if (object.friend_gift_data) {
                if (!Array.isArray(object.friend_gift_data))
                    throw TypeError(".lq.AccountActivityUpdate.friend_gift_data: array expected");
                message.friend_gift_data = Array(object.friend_gift_data.length);
                for (let i = 0; i < object.friend_gift_data.length; ++i) {
                    if (typeof object.friend_gift_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.friend_gift_data: object expected");
                    message.friend_gift_data[i] = $root.lq.ActivityFriendGiftData.fromObject(object.friend_gift_data[i], _depth + 1);
                }
            }
            if (object.upgrade_data) {
                if (!Array.isArray(object.upgrade_data))
                    throw TypeError(".lq.AccountActivityUpdate.upgrade_data: array expected");
                message.upgrade_data = Array(object.upgrade_data.length);
                for (let i = 0; i < object.upgrade_data.length; ++i) {
                    if (typeof object.upgrade_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.upgrade_data: object expected");
                    message.upgrade_data[i] = $root.lq.ActivityUpgradeData.fromObject(object.upgrade_data[i], _depth + 1);
                }
            }
            if (object.gacha_data) {
                if (!Array.isArray(object.gacha_data))
                    throw TypeError(".lq.AccountActivityUpdate.gacha_data: array expected");
                message.gacha_data = Array(object.gacha_data.length);
                for (let i = 0; i < object.gacha_data.length; ++i) {
                    if (typeof object.gacha_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.gacha_data: object expected");
                    message.gacha_data[i] = $root.lq.ActivityGachaUpdateData.fromObject(object.gacha_data[i], _depth + 1);
                }
            }
            if (object.simulation_data) {
                if (!Array.isArray(object.simulation_data))
                    throw TypeError(".lq.AccountActivityUpdate.simulation_data: array expected");
                message.simulation_data = Array(object.simulation_data.length);
                for (let i = 0; i < object.simulation_data.length; ++i) {
                    if (typeof object.simulation_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.simulation_data: object expected");
                    message.simulation_data[i] = $root.lq.ActivitySimulationData.fromObject(object.simulation_data[i], _depth + 1);
                }
            }
            if (object.combining_data) {
                if (!Array.isArray(object.combining_data))
                    throw TypeError(".lq.AccountActivityUpdate.combining_data: array expected");
                message.combining_data = Array(object.combining_data.length);
                for (let i = 0; i < object.combining_data.length; ++i) {
                    if (typeof object.combining_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.combining_data: object expected");
                    message.combining_data[i] = $root.lq.ActivityCombiningLQData.fromObject(object.combining_data[i], _depth + 1);
                }
            }
            if (object.village_data) {
                if (!Array.isArray(object.village_data))
                    throw TypeError(".lq.AccountActivityUpdate.village_data: array expected");
                message.village_data = Array(object.village_data.length);
                for (let i = 0; i < object.village_data.length; ++i) {
                    if (typeof object.village_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.village_data: object expected");
                    message.village_data[i] = $root.lq.ActivityVillageData.fromObject(object.village_data[i], _depth + 1);
                }
            }
            if (object.festival_data) {
                if (!Array.isArray(object.festival_data))
                    throw TypeError(".lq.AccountActivityUpdate.festival_data: array expected");
                message.festival_data = Array(object.festival_data.length);
                for (let i = 0; i < object.festival_data.length; ++i) {
                    if (typeof object.festival_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.festival_data: object expected");
                    message.festival_data[i] = $root.lq.ActivityFestivalData.fromObject(object.festival_data[i], _depth + 1);
                }
            }
            if (object.island_data) {
                if (!Array.isArray(object.island_data))
                    throw TypeError(".lq.AccountActivityUpdate.island_data: array expected");
                message.island_data = Array(object.island_data.length);
                for (let i = 0; i < object.island_data.length; ++i) {
                    if (typeof object.island_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.island_data: object expected");
                    message.island_data[i] = $root.lq.ActivityIslandData.fromObject(object.island_data[i], _depth + 1);
                }
            }
            if (object.story_data) {
                if (!Array.isArray(object.story_data))
                    throw TypeError(".lq.AccountActivityUpdate.story_data: array expected");
                message.story_data = Array(object.story_data.length);
                for (let i = 0; i < object.story_data.length; ++i) {
                    if (typeof object.story_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.story_data: object expected");
                    message.story_data[i] = $root.lq.ActivityStoryData.fromObject(object.story_data[i], _depth + 1);
                }
            }
            if (object.choose_up_data) {
                if (!Array.isArray(object.choose_up_data))
                    throw TypeError(".lq.AccountActivityUpdate.choose_up_data: array expected");
                message.choose_up_data = Array(object.choose_up_data.length);
                for (let i = 0; i < object.choose_up_data.length; ++i) {
                    if (typeof object.choose_up_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.choose_up_data: object expected");
                    message.choose_up_data[i] = $root.lq.ActivityChooseUpData.fromObject(object.choose_up_data[i], _depth + 1);
                }
            }
            if (object.simulation_v2_data) {
                if (!Array.isArray(object.simulation_v2_data))
                    throw TypeError(".lq.AccountActivityUpdate.simulation_v2_data: array expected");
                message.simulation_v2_data = Array(object.simulation_v2_data.length);
                for (let i = 0; i < object.simulation_v2_data.length; ++i) {
                    if (typeof object.simulation_v2_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.simulation_v2_data: object expected");
                    message.simulation_v2_data[i] = $root.lq.SimulationV2Data.fromObject(object.simulation_v2_data[i], _depth + 1);
                }
            }
            if (object.quest_crew_data) {
                if (!Array.isArray(object.quest_crew_data))
                    throw TypeError(".lq.AccountActivityUpdate.quest_crew_data: array expected");
                message.quest_crew_data = Array(object.quest_crew_data.length);
                for (let i = 0; i < object.quest_crew_data.length; ++i) {
                    if (typeof object.quest_crew_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.quest_crew_data: object expected");
                    message.quest_crew_data[i] = $root.lq.ActivityQuestCrewChanges.fromObject(object.quest_crew_data[i], _depth + 1);
                }
            }
            if (object.shoot_data) {
                if (!Array.isArray(object.shoot_data))
                    throw TypeError(".lq.AccountActivityUpdate.shoot_data: array expected");
                message.shoot_data = Array(object.shoot_data.length);
                for (let i = 0; i < object.shoot_data.length; ++i) {
                    if (typeof object.shoot_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.shoot_data: object expected");
                    message.shoot_data[i] = $root.lq.ActivityShootData.fromObject(object.shoot_data[i], _depth + 1);
                }
            }
            if (object.bingo_data) {
                if (!Array.isArray(object.bingo_data))
                    throw TypeError(".lq.AccountActivityUpdate.bingo_data: array expected");
                message.bingo_data = Array(object.bingo_data.length);
                for (let i = 0; i < object.bingo_data.length; ++i) {
                    if (typeof object.bingo_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.bingo_data: object expected");
                    message.bingo_data[i] = $root.lq.ActivityBingoData.fromObject(object.bingo_data[i], _depth + 1);
                }
            }
            if (object.snowball_data) {
                if (!Array.isArray(object.snowball_data))
                    throw TypeError(".lq.AccountActivityUpdate.snowball_data: array expected");
                message.snowball_data = Array(object.snowball_data.length);
                for (let i = 0; i < object.snowball_data.length; ++i) {
                    if (typeof object.snowball_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.snowball_data: object expected");
                    message.snowball_data[i] = $root.lq.ActivitySnowballValueChanges.fromObject(object.snowball_data[i], _depth + 1);
                }
            }
            if (object.choose_group_up_data) {
                if (!Array.isArray(object.choose_group_up_data))
                    throw TypeError(".lq.AccountActivityUpdate.choose_group_up_data: array expected");
                message.choose_group_up_data = Array(object.choose_group_up_data.length);
                for (let i = 0; i < object.choose_group_up_data.length; ++i) {
                    if (typeof object.choose_group_up_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.choose_group_up_data: object expected");
                    message.choose_group_up_data[i] = $root.lq.ActivityChooseGroupData.fromObject(object.choose_group_up_data[i], _depth + 1);
                }
            }
            if (object.mmo_data) {
                if (!Array.isArray(object.mmo_data))
                    throw TypeError(".lq.AccountActivityUpdate.mmo_data: array expected");
                message.mmo_data = Array(object.mmo_data.length);
                for (let i = 0; i < object.mmo_data.length; ++i) {
                    if (typeof object.mmo_data[i] !== "object")
                        throw TypeError(".lq.AccountActivityUpdate.mmo_data: object expected");
                    message.mmo_data[i] = $root.lq.ActivityMMODataChanges.fromObject(object.mmo_data[i], _depth + 1);
                }
            }
            if (object.activity_buff != null) {
                if (typeof object.activity_buff !== "object")
                    throw TypeError(".lq.AccountActivityUpdate.activity_buff: object expected");
                message.activity_buff = $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.fromObject(object.activity_buff, _depth + 1);
            }
            return message;
        };

        AccountActivityUpdate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.mine_data = [];
                object.rpg_data = [];
                object.feed_data = [];
                object.spot_data = [];
                object.friend_gift_data = [];
                object.upgrade_data = [];
                object.gacha_data = [];
                object.simulation_data = [];
                object.combining_data = [];
                object.village_data = [];
                object.festival_data = [];
                object.island_data = [];
                object.story_data = [];
                object.choose_up_data = [];
                object.simulation_v2_data = [];
                object.quest_crew_data = [];
                object.shoot_data = [];
                object.bingo_data = [];
                object.snowball_data = [];
                object.choose_group_up_data = [];
                object.mmo_data = [];
            }
            if (options.defaults)
                object.activity_buff = null;
            if (message.mine_data && message.mine_data.length) {
                object.mine_data = Array(message.mine_data.length);
                for (let j = 0; j < message.mine_data.length; ++j)
                    object.mine_data[j] = $root.lq.MineActivityData.toObject(message.mine_data[j], options);
            }
            if (message.rpg_data && message.rpg_data.length) {
                object.rpg_data = Array(message.rpg_data.length);
                for (let j = 0; j < message.rpg_data.length; ++j)
                    object.rpg_data[j] = $root.lq.RPGActivity.toObject(message.rpg_data[j], options);
            }
            if (message.feed_data && message.feed_data.length) {
                object.feed_data = Array(message.feed_data.length);
                for (let j = 0; j < message.feed_data.length; ++j)
                    object.feed_data[j] = $root.lq.ActivityFeedData.toObject(message.feed_data[j], options);
            }
            if (message.spot_data && message.spot_data.length) {
                object.spot_data = Array(message.spot_data.length);
                for (let j = 0; j < message.spot_data.length; ++j)
                    object.spot_data[j] = $root.lq.ActivitySpotData.toObject(message.spot_data[j], options);
            }
            if (message.friend_gift_data && message.friend_gift_data.length) {
                object.friend_gift_data = Array(message.friend_gift_data.length);
                for (let j = 0; j < message.friend_gift_data.length; ++j)
                    object.friend_gift_data[j] = $root.lq.ActivityFriendGiftData.toObject(message.friend_gift_data[j], options);
            }
            if (message.upgrade_data && message.upgrade_data.length) {
                object.upgrade_data = Array(message.upgrade_data.length);
                for (let j = 0; j < message.upgrade_data.length; ++j)
                    object.upgrade_data[j] = $root.lq.ActivityUpgradeData.toObject(message.upgrade_data[j], options);
            }
            if (message.gacha_data && message.gacha_data.length) {
                object.gacha_data = Array(message.gacha_data.length);
                for (let j = 0; j < message.gacha_data.length; ++j)
                    object.gacha_data[j] = $root.lq.ActivityGachaUpdateData.toObject(message.gacha_data[j], options);
            }
            if (message.simulation_data && message.simulation_data.length) {
                object.simulation_data = Array(message.simulation_data.length);
                for (let j = 0; j < message.simulation_data.length; ++j)
                    object.simulation_data[j] = $root.lq.ActivitySimulationData.toObject(message.simulation_data[j], options);
            }
            if (message.combining_data && message.combining_data.length) {
                object.combining_data = Array(message.combining_data.length);
                for (let j = 0; j < message.combining_data.length; ++j)
                    object.combining_data[j] = $root.lq.ActivityCombiningLQData.toObject(message.combining_data[j], options);
            }
            if (message.village_data && message.village_data.length) {
                object.village_data = Array(message.village_data.length);
                for (let j = 0; j < message.village_data.length; ++j)
                    object.village_data[j] = $root.lq.ActivityVillageData.toObject(message.village_data[j], options);
            }
            if (message.festival_data && message.festival_data.length) {
                object.festival_data = Array(message.festival_data.length);
                for (let j = 0; j < message.festival_data.length; ++j)
                    object.festival_data[j] = $root.lq.ActivityFestivalData.toObject(message.festival_data[j], options);
            }
            if (message.island_data && message.island_data.length) {
                object.island_data = Array(message.island_data.length);
                for (let j = 0; j < message.island_data.length; ++j)
                    object.island_data[j] = $root.lq.ActivityIslandData.toObject(message.island_data[j], options);
            }
            if (message.story_data && message.story_data.length) {
                object.story_data = Array(message.story_data.length);
                for (let j = 0; j < message.story_data.length; ++j)
                    object.story_data[j] = $root.lq.ActivityStoryData.toObject(message.story_data[j], options);
            }
            if (message.choose_up_data && message.choose_up_data.length) {
                object.choose_up_data = Array(message.choose_up_data.length);
                for (let j = 0; j < message.choose_up_data.length; ++j)
                    object.choose_up_data[j] = $root.lq.ActivityChooseUpData.toObject(message.choose_up_data[j], options);
            }
            if (message.simulation_v2_data && message.simulation_v2_data.length) {
                object.simulation_v2_data = Array(message.simulation_v2_data.length);
                for (let j = 0; j < message.simulation_v2_data.length; ++j)
                    object.simulation_v2_data[j] = $root.lq.SimulationV2Data.toObject(message.simulation_v2_data[j], options);
            }
            if (message.quest_crew_data && message.quest_crew_data.length) {
                object.quest_crew_data = Array(message.quest_crew_data.length);
                for (let j = 0; j < message.quest_crew_data.length; ++j)
                    object.quest_crew_data[j] = $root.lq.ActivityQuestCrewChanges.toObject(message.quest_crew_data[j], options);
            }
            if (message.shoot_data && message.shoot_data.length) {
                object.shoot_data = Array(message.shoot_data.length);
                for (let j = 0; j < message.shoot_data.length; ++j)
                    object.shoot_data[j] = $root.lq.ActivityShootData.toObject(message.shoot_data[j], options);
            }
            if (message.bingo_data && message.bingo_data.length) {
                object.bingo_data = Array(message.bingo_data.length);
                for (let j = 0; j < message.bingo_data.length; ++j)
                    object.bingo_data[j] = $root.lq.ActivityBingoData.toObject(message.bingo_data[j], options);
            }
            if (message.snowball_data && message.snowball_data.length) {
                object.snowball_data = Array(message.snowball_data.length);
                for (let j = 0; j < message.snowball_data.length; ++j)
                    object.snowball_data[j] = $root.lq.ActivitySnowballValueChanges.toObject(message.snowball_data[j], options);
            }
            if (message.choose_group_up_data && message.choose_group_up_data.length) {
                object.choose_group_up_data = Array(message.choose_group_up_data.length);
                for (let j = 0; j < message.choose_group_up_data.length; ++j)
                    object.choose_group_up_data[j] = $root.lq.ActivityChooseGroupData.toObject(message.choose_group_up_data[j], options);
            }
            if (message.mmo_data && message.mmo_data.length) {
                object.mmo_data = Array(message.mmo_data.length);
                for (let j = 0; j < message.mmo_data.length; ++j)
                    object.mmo_data[j] = $root.lq.ActivityMMODataChanges.toObject(message.mmo_data[j], options);
            }
            if (message.activity_buff != null && message.hasOwnProperty("activity_buff"))
                object.activity_buff = $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.toObject(message.activity_buff, options);
            return object;
        };

        AccountActivityUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountActivityUpdate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountActivityUpdate";
        };

        AccountActivityUpdate.ActivityBuffDataArrayDirty = (function() {

            function ActivityBuffDataArrayDirty(properties) {
                this.value = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityBuffDataArrayDirty.prototype.dirty = false;
            ActivityBuffDataArrayDirty.prototype.value = $util.emptyArray;

            ActivityBuffDataArrayDirty.create = function create(properties) {
                return new ActivityBuffDataArrayDirty(properties);
            };

            ActivityBuffDataArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(8).bool(message.dirty);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.lq.ActivityBuffData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityBuffDataArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityBuffDataArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.lq.ActivityBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityBuffDataArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityBuffDataArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (typeof message.dirty !== "boolean")
                        return "dirty: boolean expected";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.lq.ActivityBuffData.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            ActivityBuffDataArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountActivityUpdate.ActivityBuffDataArrayDirty();
                if (object.dirty != null)
                    if (object.dirty)
                        message.dirty = Boolean(object.dirty);
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.value: array expected");
                    message.value = Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".lq.AccountActivityUpdate.ActivityBuffDataArrayDirty.value: object expected");
                        message.value[i] = $root.lq.ActivityBuffData.fromObject(object.value[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityBuffDataArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults)
                    object.dirty = false;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                if (message.value && message.value.length) {
                    object.value = Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.lq.ActivityBuffData.toObject(message.value[j], options);
                }
                return object;
            };

            ActivityBuffDataArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityBuffDataArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountActivityUpdate.ActivityBuffDataArrayDirty";
            };

            return ActivityBuffDataArrayDirty;
        })();

        return AccountActivityUpdate;
    })();

    lq.ActivityCombiningWorkbench = (function() {

        function ActivityCombiningWorkbench(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningWorkbench.prototype.craft_id = 0;
        ActivityCombiningWorkbench.prototype.pos = 0;

        ActivityCombiningWorkbench.create = function create(properties) {
            return new ActivityCombiningWorkbench(properties);
        };

        ActivityCombiningWorkbench.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.craft_id != null && Object.hasOwnProperty.call(message, "craft_id"))
                writer.uint32(8).uint32(message.craft_id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningWorkbench.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningWorkbench.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningWorkbench(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.craft_id = value;
                        else
                            delete message.craft_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningWorkbench.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningWorkbench.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                if (!$util.isInteger(message.craft_id))
                    return "craft_id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            return null;
        };

        ActivityCombiningWorkbench.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningWorkbench)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningWorkbench();
            if (object.craft_id != null)
                if (Number(object.craft_id) !== 0)
                    message.craft_id = object.craft_id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            return message;
        };

        ActivityCombiningWorkbench.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.craft_id = 0;
                object.pos = 0;
            }
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                object.craft_id = message.craft_id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            return object;
        };

        ActivityCombiningWorkbench.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningWorkbench.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningWorkbench";
        };

        return ActivityCombiningWorkbench;
    })();

    lq.ActivityCombiningMenuData = (function() {

        function ActivityCombiningMenuData(properties) {
            this.generated = [];
            this.multi_generated = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningMenuData.prototype.menu_group = 0;
        ActivityCombiningMenuData.prototype.generated = $util.emptyArray;
        ActivityCombiningMenuData.prototype.multi_generated = $util.emptyArray;

        ActivityCombiningMenuData.create = function create(properties) {
            return new ActivityCombiningMenuData(properties);
        };

        ActivityCombiningMenuData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.menu_group != null && Object.hasOwnProperty.call(message, "menu_group"))
                writer.uint32(8).uint32(message.menu_group);
            if (message.generated != null && message.generated.length)
                for (let i = 0; i < message.generated.length; ++i)
                    $root.lq.ActivityCombiningMenuData.MenuRequire.encode(message.generated[i], writer.uint32(18).fork()).ldelim();
            if (message.multi_generated != null && message.multi_generated.length)
                for (let i = 0; i < message.multi_generated.length; ++i)
                    $root.lq.ActivityCombiningMenuData.MenuRequire.encode(message.multi_generated[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningMenuData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningMenuData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningMenuData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.menu_group = value;
                        else
                            delete message.menu_group;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.generated && message.generated.length))
                            message.generated = [];
                        message.generated.push($root.lq.ActivityCombiningMenuData.MenuRequire.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.multi_generated && message.multi_generated.length))
                            message.multi_generated = [];
                        message.multi_generated.push($root.lq.ActivityCombiningMenuData.MenuRequire.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningMenuData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningMenuData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.menu_group != null && message.hasOwnProperty("menu_group"))
                if (!$util.isInteger(message.menu_group))
                    return "menu_group: integer expected";
            if (message.generated != null && message.hasOwnProperty("generated")) {
                if (!Array.isArray(message.generated))
                    return "generated: array expected";
                for (let i = 0; i < message.generated.length; ++i) {
                    let error = $root.lq.ActivityCombiningMenuData.MenuRequire.verify(message.generated[i], _depth + 1);
                    if (error)
                        return "generated." + error;
                }
            }
            if (message.multi_generated != null && message.hasOwnProperty("multi_generated")) {
                if (!Array.isArray(message.multi_generated))
                    return "multi_generated: array expected";
                for (let i = 0; i < message.multi_generated.length; ++i) {
                    let error = $root.lq.ActivityCombiningMenuData.MenuRequire.verify(message.multi_generated[i], _depth + 1);
                    if (error)
                        return "multi_generated." + error;
                }
            }
            return null;
        };

        ActivityCombiningMenuData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningMenuData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningMenuData();
            if (object.menu_group != null)
                if (Number(object.menu_group) !== 0)
                    message.menu_group = object.menu_group >>> 0;
            if (object.generated) {
                if (!Array.isArray(object.generated))
                    throw TypeError(".lq.ActivityCombiningMenuData.generated: array expected");
                message.generated = Array(object.generated.length);
                for (let i = 0; i < object.generated.length; ++i) {
                    if (typeof object.generated[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningMenuData.generated: object expected");
                    message.generated[i] = $root.lq.ActivityCombiningMenuData.MenuRequire.fromObject(object.generated[i], _depth + 1);
                }
            }
            if (object.multi_generated) {
                if (!Array.isArray(object.multi_generated))
                    throw TypeError(".lq.ActivityCombiningMenuData.multi_generated: array expected");
                message.multi_generated = Array(object.multi_generated.length);
                for (let i = 0; i < object.multi_generated.length; ++i) {
                    if (typeof object.multi_generated[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningMenuData.multi_generated: object expected");
                    message.multi_generated[i] = $root.lq.ActivityCombiningMenuData.MenuRequire.fromObject(object.multi_generated[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityCombiningMenuData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.generated = [];
                object.multi_generated = [];
            }
            if (options.defaults)
                object.menu_group = 0;
            if (message.menu_group != null && message.hasOwnProperty("menu_group"))
                object.menu_group = message.menu_group;
            if (message.generated && message.generated.length) {
                object.generated = Array(message.generated.length);
                for (let j = 0; j < message.generated.length; ++j)
                    object.generated[j] = $root.lq.ActivityCombiningMenuData.MenuRequire.toObject(message.generated[j], options);
            }
            if (message.multi_generated && message.multi_generated.length) {
                object.multi_generated = Array(message.multi_generated.length);
                for (let j = 0; j < message.multi_generated.length; ++j)
                    object.multi_generated[j] = $root.lq.ActivityCombiningMenuData.MenuRequire.toObject(message.multi_generated[j], options);
            }
            return object;
        };

        ActivityCombiningMenuData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningMenuData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningMenuData";
        };

        ActivityCombiningMenuData.MenuRequire = (function() {

            function MenuRequire(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MenuRequire.prototype.level = 0;
            MenuRequire.prototype.count = 0;

            MenuRequire.create = function create(properties) {
                return new MenuRequire(properties);
            };

            MenuRequire.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(8).uint32(message.level);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MenuRequire.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MenuRequire.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningMenuData.MenuRequire(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MenuRequire.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MenuRequire.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            MenuRequire.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityCombiningMenuData.MenuRequire)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityCombiningMenuData.MenuRequire();
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            MenuRequire.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level = 0;
                    object.count = 0;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            MenuRequire.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MenuRequire.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityCombiningMenuData.MenuRequire";
            };

            return MenuRequire;
        })();

        return ActivityCombiningMenuData;
    })();

    lq.ActivityCombiningOrderData = (function() {

        function ActivityCombiningOrderData(properties) {
            this.finished_craft_id = [];
            this.craft_id = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningOrderData.prototype.id = 0;
        ActivityCombiningOrderData.prototype.pos = 0;
        ActivityCombiningOrderData.prototype.unlock_day = 0;
        ActivityCombiningOrderData.prototype.char_id = 0;
        ActivityCombiningOrderData.prototype.finished_craft_id = $util.emptyArray;
        ActivityCombiningOrderData.prototype.craft_id = $util.emptyArray;

        ActivityCombiningOrderData.create = function create(properties) {
            return new ActivityCombiningOrderData(properties);
        };

        ActivityCombiningOrderData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.unlock_day != null && Object.hasOwnProperty.call(message, "unlock_day"))
                writer.uint32(32).uint32(message.unlock_day);
            if (message.char_id != null && Object.hasOwnProperty.call(message, "char_id"))
                writer.uint32(40).uint32(message.char_id);
            if (message.finished_craft_id != null && message.finished_craft_id.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.finished_craft_id.length; ++i)
                    writer.uint32(message.finished_craft_id[i]);
                writer.ldelim();
            }
            if (message.craft_id != null && message.craft_id.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.craft_id.length; ++i)
                    writer.uint32(message.craft_id[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningOrderData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningOrderData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningOrderData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unlock_day = value;
                        else
                            delete message.unlock_day;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.char_id = value;
                        else
                            delete message.char_id;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.finished_craft_id && message.finished_craft_id.length))
                                message.finished_craft_id = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finished_craft_id.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.finished_craft_id && message.finished_craft_id.length))
                            message.finished_craft_id = [];
                        message.finished_craft_id.push(reader.uint32());
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.craft_id && message.craft_id.length))
                                message.craft_id = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.craft_id.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.craft_id && message.craft_id.length))
                            message.craft_id = [];
                        message.craft_id.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningOrderData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningOrderData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            if (message.unlock_day != null && message.hasOwnProperty("unlock_day"))
                if (!$util.isInteger(message.unlock_day))
                    return "unlock_day: integer expected";
            if (message.char_id != null && message.hasOwnProperty("char_id"))
                if (!$util.isInteger(message.char_id))
                    return "char_id: integer expected";
            if (message.finished_craft_id != null && message.hasOwnProperty("finished_craft_id")) {
                if (!Array.isArray(message.finished_craft_id))
                    return "finished_craft_id: array expected";
                for (let i = 0; i < message.finished_craft_id.length; ++i)
                    if (!$util.isInteger(message.finished_craft_id[i]))
                        return "finished_craft_id: integer[] expected";
            }
            if (message.craft_id != null && message.hasOwnProperty("craft_id")) {
                if (!Array.isArray(message.craft_id))
                    return "craft_id: array expected";
                for (let i = 0; i < message.craft_id.length; ++i)
                    if (!$util.isInteger(message.craft_id[i]))
                        return "craft_id: integer[] expected";
            }
            return null;
        };

        ActivityCombiningOrderData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningOrderData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningOrderData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            if (object.unlock_day != null)
                if (Number(object.unlock_day) !== 0)
                    message.unlock_day = object.unlock_day >>> 0;
            if (object.char_id != null)
                if (Number(object.char_id) !== 0)
                    message.char_id = object.char_id >>> 0;
            if (object.finished_craft_id) {
                if (!Array.isArray(object.finished_craft_id))
                    throw TypeError(".lq.ActivityCombiningOrderData.finished_craft_id: array expected");
                message.finished_craft_id = Array(object.finished_craft_id.length);
                for (let i = 0; i < object.finished_craft_id.length; ++i)
                    message.finished_craft_id[i] = object.finished_craft_id[i] >>> 0;
            }
            if (object.craft_id) {
                if (!Array.isArray(object.craft_id))
                    throw TypeError(".lq.ActivityCombiningOrderData.craft_id: array expected");
                message.craft_id = Array(object.craft_id.length);
                for (let i = 0; i < object.craft_id.length; ++i)
                    message.craft_id[i] = object.craft_id[i] >>> 0;
            }
            return message;
        };

        ActivityCombiningOrderData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.finished_craft_id = [];
                object.craft_id = [];
            }
            if (options.defaults) {
                object.id = 0;
                object.pos = 0;
                object.unlock_day = 0;
                object.char_id = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            if (message.unlock_day != null && message.hasOwnProperty("unlock_day"))
                object.unlock_day = message.unlock_day;
            if (message.char_id != null && message.hasOwnProperty("char_id"))
                object.char_id = message.char_id;
            if (message.finished_craft_id && message.finished_craft_id.length) {
                object.finished_craft_id = Array(message.finished_craft_id.length);
                for (let j = 0; j < message.finished_craft_id.length; ++j)
                    object.finished_craft_id[j] = message.finished_craft_id[j];
            }
            if (message.craft_id && message.craft_id.length) {
                object.craft_id = Array(message.craft_id.length);
                for (let j = 0; j < message.craft_id.length; ++j)
                    object.craft_id[j] = message.craft_id[j];
            }
            return object;
        };

        ActivityCombiningOrderData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningOrderData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningOrderData";
        };

        return ActivityCombiningOrderData;
    })();

    lq.ActivityCombiningLQData = (function() {

        function ActivityCombiningLQData(properties) {
            this.workbench = [];
            this.orders = [];
            this.unlocked_craft = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningLQData.prototype.activity_id = 0;
        ActivityCombiningLQData.prototype.workbench = $util.emptyArray;
        ActivityCombiningLQData.prototype.orders = $util.emptyArray;
        ActivityCombiningLQData.prototype.recycle_bin = null;
        ActivityCombiningLQData.prototype.unlocked_craft = $util.emptyArray;
        ActivityCombiningLQData.prototype.daily_bonus_count = 0;

        ActivityCombiningLQData.create = function create(properties) {
            return new ActivityCombiningLQData(properties);
        };

        ActivityCombiningLQData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.workbench != null && message.workbench.length)
                for (let i = 0; i < message.workbench.length; ++i)
                    $root.lq.ActivityCombiningWorkbench.encode(message.workbench[i], writer.uint32(18).fork()).ldelim();
            if (message.orders != null && message.orders.length)
                for (let i = 0; i < message.orders.length; ++i)
                    $root.lq.ActivityCombiningOrderData.encode(message.orders[i], writer.uint32(26).fork()).ldelim();
            if (message.recycle_bin != null && Object.hasOwnProperty.call(message, "recycle_bin"))
                $root.lq.ActivityCombiningWorkbench.encode(message.recycle_bin, writer.uint32(34).fork()).ldelim();
            if (message.unlocked_craft != null && message.unlocked_craft.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.unlocked_craft.length; ++i)
                    writer.uint32(message.unlocked_craft[i]);
                writer.ldelim();
            }
            if (message.daily_bonus_count != null && Object.hasOwnProperty.call(message, "daily_bonus_count"))
                writer.uint32(48).uint32(message.daily_bonus_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningLQData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningLQData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningLQData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.workbench && message.workbench.length))
                            message.workbench = [];
                        message.workbench.push($root.lq.ActivityCombiningWorkbench.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.orders && message.orders.length))
                            message.orders = [];
                        message.orders.push($root.lq.ActivityCombiningOrderData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.recycle_bin = $root.lq.ActivityCombiningWorkbench.decode(reader, reader.uint32(), undefined, _depth + 1, message.recycle_bin);
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.unlocked_craft && message.unlocked_craft.length))
                                message.unlocked_craft = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.unlocked_craft.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.unlocked_craft && message.unlocked_craft.length))
                            message.unlocked_craft = [];
                        message.unlocked_craft.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.daily_bonus_count = value;
                        else
                            delete message.daily_bonus_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningLQData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningLQData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.workbench != null && message.hasOwnProperty("workbench")) {
                if (!Array.isArray(message.workbench))
                    return "workbench: array expected";
                for (let i = 0; i < message.workbench.length; ++i) {
                    let error = $root.lq.ActivityCombiningWorkbench.verify(message.workbench[i], _depth + 1);
                    if (error)
                        return "workbench." + error;
                }
            }
            if (message.orders != null && message.hasOwnProperty("orders")) {
                if (!Array.isArray(message.orders))
                    return "orders: array expected";
                for (let i = 0; i < message.orders.length; ++i) {
                    let error = $root.lq.ActivityCombiningOrderData.verify(message.orders[i], _depth + 1);
                    if (error)
                        return "orders." + error;
                }
            }
            if (message.recycle_bin != null && message.hasOwnProperty("recycle_bin")) {
                let error = $root.lq.ActivityCombiningWorkbench.verify(message.recycle_bin, _depth + 1);
                if (error)
                    return "recycle_bin." + error;
            }
            if (message.unlocked_craft != null && message.hasOwnProperty("unlocked_craft")) {
                if (!Array.isArray(message.unlocked_craft))
                    return "unlocked_craft: array expected";
                for (let i = 0; i < message.unlocked_craft.length; ++i)
                    if (!$util.isInteger(message.unlocked_craft[i]))
                        return "unlocked_craft: integer[] expected";
            }
            if (message.daily_bonus_count != null && message.hasOwnProperty("daily_bonus_count"))
                if (!$util.isInteger(message.daily_bonus_count))
                    return "daily_bonus_count: integer expected";
            return null;
        };

        ActivityCombiningLQData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningLQData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningLQData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.workbench) {
                if (!Array.isArray(object.workbench))
                    throw TypeError(".lq.ActivityCombiningLQData.workbench: array expected");
                message.workbench = Array(object.workbench.length);
                for (let i = 0; i < object.workbench.length; ++i) {
                    if (typeof object.workbench[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningLQData.workbench: object expected");
                    message.workbench[i] = $root.lq.ActivityCombiningWorkbench.fromObject(object.workbench[i], _depth + 1);
                }
            }
            if (object.orders) {
                if (!Array.isArray(object.orders))
                    throw TypeError(".lq.ActivityCombiningLQData.orders: array expected");
                message.orders = Array(object.orders.length);
                for (let i = 0; i < object.orders.length; ++i) {
                    if (typeof object.orders[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningLQData.orders: object expected");
                    message.orders[i] = $root.lq.ActivityCombiningOrderData.fromObject(object.orders[i], _depth + 1);
                }
            }
            if (object.recycle_bin != null) {
                if (typeof object.recycle_bin !== "object")
                    throw TypeError(".lq.ActivityCombiningLQData.recycle_bin: object expected");
                message.recycle_bin = $root.lq.ActivityCombiningWorkbench.fromObject(object.recycle_bin, _depth + 1);
            }
            if (object.unlocked_craft) {
                if (!Array.isArray(object.unlocked_craft))
                    throw TypeError(".lq.ActivityCombiningLQData.unlocked_craft: array expected");
                message.unlocked_craft = Array(object.unlocked_craft.length);
                for (let i = 0; i < object.unlocked_craft.length; ++i)
                    message.unlocked_craft[i] = object.unlocked_craft[i] >>> 0;
            }
            if (object.daily_bonus_count != null)
                if (Number(object.daily_bonus_count) !== 0)
                    message.daily_bonus_count = object.daily_bonus_count >>> 0;
            return message;
        };

        ActivityCombiningLQData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.workbench = [];
                object.orders = [];
                object.unlocked_craft = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.recycle_bin = null;
                object.daily_bonus_count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.workbench && message.workbench.length) {
                object.workbench = Array(message.workbench.length);
                for (let j = 0; j < message.workbench.length; ++j)
                    object.workbench[j] = $root.lq.ActivityCombiningWorkbench.toObject(message.workbench[j], options);
            }
            if (message.orders && message.orders.length) {
                object.orders = Array(message.orders.length);
                for (let j = 0; j < message.orders.length; ++j)
                    object.orders[j] = $root.lq.ActivityCombiningOrderData.toObject(message.orders[j], options);
            }
            if (message.recycle_bin != null && message.hasOwnProperty("recycle_bin"))
                object.recycle_bin = $root.lq.ActivityCombiningWorkbench.toObject(message.recycle_bin, options);
            if (message.unlocked_craft && message.unlocked_craft.length) {
                object.unlocked_craft = Array(message.unlocked_craft.length);
                for (let j = 0; j < message.unlocked_craft.length; ++j)
                    object.unlocked_craft[j] = message.unlocked_craft[j];
            }
            if (message.daily_bonus_count != null && message.hasOwnProperty("daily_bonus_count"))
                object.daily_bonus_count = message.daily_bonus_count;
            return object;
        };

        ActivityCombiningLQData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningLQData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningLQData";
        };

        return ActivityCombiningLQData;
    })();

    lq.ActivityCombiningPoolData = (function() {

        function ActivityCombiningPoolData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningPoolData.prototype.group = 0;
        ActivityCombiningPoolData.prototype.count = 0;

        ActivityCombiningPoolData.create = function create(properties) {
            return new ActivityCombiningPoolData(properties);
        };

        ActivityCombiningPoolData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.group != null && Object.hasOwnProperty.call(message, "group"))
                writer.uint32(8).uint32(message.group);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningPoolData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningPoolData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningPoolData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.group = value;
                        else
                            delete message.group;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningPoolData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningPoolData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.group != null && message.hasOwnProperty("group"))
                if (!$util.isInteger(message.group))
                    return "group: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ActivityCombiningPoolData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningPoolData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningPoolData();
            if (object.group != null)
                if (Number(object.group) !== 0)
                    message.group = object.group >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ActivityCombiningPoolData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.group = 0;
                object.count = 0;
            }
            if (message.group != null && message.hasOwnProperty("group"))
                object.group = message.group;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ActivityCombiningPoolData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningPoolData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningPoolData";
        };

        return ActivityCombiningPoolData;
    })();

    lq.ActivityCombiningData = (function() {

        function ActivityCombiningData(properties) {
            this.workbench = [];
            this.orders = [];
            this.unlocked_craft = [];
            this.craft_pool = [];
            this.order_pool = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityCombiningData.prototype.activity_id = 0;
        ActivityCombiningData.prototype.workbench = $util.emptyArray;
        ActivityCombiningData.prototype.orders = $util.emptyArray;
        ActivityCombiningData.prototype.recycle_bin = null;
        ActivityCombiningData.prototype.menu = null;
        ActivityCombiningData.prototype.current_order_id = 0;
        ActivityCombiningData.prototype.bonus = null;
        ActivityCombiningData.prototype.unlocked_craft = $util.emptyArray;
        ActivityCombiningData.prototype.craft_pool = $util.emptyArray;
        ActivityCombiningData.prototype.order_pool = $util.emptyArray;

        ActivityCombiningData.create = function create(properties) {
            return new ActivityCombiningData(properties);
        };

        ActivityCombiningData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.workbench != null && message.workbench.length)
                for (let i = 0; i < message.workbench.length; ++i)
                    $root.lq.ActivityCombiningWorkbench.encode(message.workbench[i], writer.uint32(18).fork()).ldelim();
            if (message.orders != null && message.orders.length)
                for (let i = 0; i < message.orders.length; ++i)
                    $root.lq.ActivityCombiningOrderData.encode(message.orders[i], writer.uint32(26).fork()).ldelim();
            if (message.recycle_bin != null && Object.hasOwnProperty.call(message, "recycle_bin"))
                $root.lq.ActivityCombiningWorkbench.encode(message.recycle_bin, writer.uint32(34).fork()).ldelim();
            if (message.menu != null && Object.hasOwnProperty.call(message, "menu"))
                $root.lq.ActivityCombiningMenuData.encode(message.menu, writer.uint32(42).fork()).ldelim();
            if (message.current_order_id != null && Object.hasOwnProperty.call(message, "current_order_id"))
                writer.uint32(48).uint32(message.current_order_id);
            if (message.bonus != null && Object.hasOwnProperty.call(message, "bonus"))
                $root.lq.ActivityCombiningData.BonusData.encode(message.bonus, writer.uint32(58).fork()).ldelim();
            if (message.unlocked_craft != null && message.unlocked_craft.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.unlocked_craft.length; ++i)
                    writer.uint32(message.unlocked_craft[i]);
                writer.ldelim();
            }
            if (message.craft_pool != null && message.craft_pool.length)
                for (let i = 0; i < message.craft_pool.length; ++i)
                    $root.lq.ActivityCombiningPoolData.encode(message.craft_pool[i], writer.uint32(74).fork()).ldelim();
            if (message.order_pool != null && message.order_pool.length)
                for (let i = 0; i < message.order_pool.length; ++i)
                    $root.lq.ActivityCombiningPoolData.encode(message.order_pool[i], writer.uint32(82).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityCombiningData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityCombiningData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.workbench && message.workbench.length))
                            message.workbench = [];
                        message.workbench.push($root.lq.ActivityCombiningWorkbench.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.orders && message.orders.length))
                            message.orders = [];
                        message.orders.push($root.lq.ActivityCombiningOrderData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.recycle_bin = $root.lq.ActivityCombiningWorkbench.decode(reader, reader.uint32(), undefined, _depth + 1, message.recycle_bin);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.menu = $root.lq.ActivityCombiningMenuData.decode(reader, reader.uint32(), undefined, _depth + 1, message.menu);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.current_order_id = value;
                        else
                            delete message.current_order_id;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.bonus = $root.lq.ActivityCombiningData.BonusData.decode(reader, reader.uint32(), undefined, _depth + 1, message.bonus);
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.unlocked_craft && message.unlocked_craft.length))
                                message.unlocked_craft = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.unlocked_craft.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.unlocked_craft && message.unlocked_craft.length))
                            message.unlocked_craft = [];
                        message.unlocked_craft.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.craft_pool && message.craft_pool.length))
                            message.craft_pool = [];
                        message.craft_pool.push($root.lq.ActivityCombiningPoolData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.order_pool && message.order_pool.length))
                            message.order_pool = [];
                        message.order_pool.push($root.lq.ActivityCombiningPoolData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityCombiningData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityCombiningData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.workbench != null && message.hasOwnProperty("workbench")) {
                if (!Array.isArray(message.workbench))
                    return "workbench: array expected";
                for (let i = 0; i < message.workbench.length; ++i) {
                    let error = $root.lq.ActivityCombiningWorkbench.verify(message.workbench[i], _depth + 1);
                    if (error)
                        return "workbench." + error;
                }
            }
            if (message.orders != null && message.hasOwnProperty("orders")) {
                if (!Array.isArray(message.orders))
                    return "orders: array expected";
                for (let i = 0; i < message.orders.length; ++i) {
                    let error = $root.lq.ActivityCombiningOrderData.verify(message.orders[i], _depth + 1);
                    if (error)
                        return "orders." + error;
                }
            }
            if (message.recycle_bin != null && message.hasOwnProperty("recycle_bin")) {
                let error = $root.lq.ActivityCombiningWorkbench.verify(message.recycle_bin, _depth + 1);
                if (error)
                    return "recycle_bin." + error;
            }
            if (message.menu != null && message.hasOwnProperty("menu")) {
                let error = $root.lq.ActivityCombiningMenuData.verify(message.menu, _depth + 1);
                if (error)
                    return "menu." + error;
            }
            if (message.current_order_id != null && message.hasOwnProperty("current_order_id"))
                if (!$util.isInteger(message.current_order_id))
                    return "current_order_id: integer expected";
            if (message.bonus != null && message.hasOwnProperty("bonus")) {
                let error = $root.lq.ActivityCombiningData.BonusData.verify(message.bonus, _depth + 1);
                if (error)
                    return "bonus." + error;
            }
            if (message.unlocked_craft != null && message.hasOwnProperty("unlocked_craft")) {
                if (!Array.isArray(message.unlocked_craft))
                    return "unlocked_craft: array expected";
                for (let i = 0; i < message.unlocked_craft.length; ++i)
                    if (!$util.isInteger(message.unlocked_craft[i]))
                        return "unlocked_craft: integer[] expected";
            }
            if (message.craft_pool != null && message.hasOwnProperty("craft_pool")) {
                if (!Array.isArray(message.craft_pool))
                    return "craft_pool: array expected";
                for (let i = 0; i < message.craft_pool.length; ++i) {
                    let error = $root.lq.ActivityCombiningPoolData.verify(message.craft_pool[i], _depth + 1);
                    if (error)
                        return "craft_pool." + error;
                }
            }
            if (message.order_pool != null && message.hasOwnProperty("order_pool")) {
                if (!Array.isArray(message.order_pool))
                    return "order_pool: array expected";
                for (let i = 0; i < message.order_pool.length; ++i) {
                    let error = $root.lq.ActivityCombiningPoolData.verify(message.order_pool[i], _depth + 1);
                    if (error)
                        return "order_pool." + error;
                }
            }
            return null;
        };

        ActivityCombiningData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityCombiningData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityCombiningData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.workbench) {
                if (!Array.isArray(object.workbench))
                    throw TypeError(".lq.ActivityCombiningData.workbench: array expected");
                message.workbench = Array(object.workbench.length);
                for (let i = 0; i < object.workbench.length; ++i) {
                    if (typeof object.workbench[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningData.workbench: object expected");
                    message.workbench[i] = $root.lq.ActivityCombiningWorkbench.fromObject(object.workbench[i], _depth + 1);
                }
            }
            if (object.orders) {
                if (!Array.isArray(object.orders))
                    throw TypeError(".lq.ActivityCombiningData.orders: array expected");
                message.orders = Array(object.orders.length);
                for (let i = 0; i < object.orders.length; ++i) {
                    if (typeof object.orders[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningData.orders: object expected");
                    message.orders[i] = $root.lq.ActivityCombiningOrderData.fromObject(object.orders[i], _depth + 1);
                }
            }
            if (object.recycle_bin != null) {
                if (typeof object.recycle_bin !== "object")
                    throw TypeError(".lq.ActivityCombiningData.recycle_bin: object expected");
                message.recycle_bin = $root.lq.ActivityCombiningWorkbench.fromObject(object.recycle_bin, _depth + 1);
            }
            if (object.menu != null) {
                if (typeof object.menu !== "object")
                    throw TypeError(".lq.ActivityCombiningData.menu: object expected");
                message.menu = $root.lq.ActivityCombiningMenuData.fromObject(object.menu, _depth + 1);
            }
            if (object.current_order_id != null)
                if (Number(object.current_order_id) !== 0)
                    message.current_order_id = object.current_order_id >>> 0;
            if (object.bonus != null) {
                if (typeof object.bonus !== "object")
                    throw TypeError(".lq.ActivityCombiningData.bonus: object expected");
                message.bonus = $root.lq.ActivityCombiningData.BonusData.fromObject(object.bonus, _depth + 1);
            }
            if (object.unlocked_craft) {
                if (!Array.isArray(object.unlocked_craft))
                    throw TypeError(".lq.ActivityCombiningData.unlocked_craft: array expected");
                message.unlocked_craft = Array(object.unlocked_craft.length);
                for (let i = 0; i < object.unlocked_craft.length; ++i)
                    message.unlocked_craft[i] = object.unlocked_craft[i] >>> 0;
            }
            if (object.craft_pool) {
                if (!Array.isArray(object.craft_pool))
                    throw TypeError(".lq.ActivityCombiningData.craft_pool: array expected");
                message.craft_pool = Array(object.craft_pool.length);
                for (let i = 0; i < object.craft_pool.length; ++i) {
                    if (typeof object.craft_pool[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningData.craft_pool: object expected");
                    message.craft_pool[i] = $root.lq.ActivityCombiningPoolData.fromObject(object.craft_pool[i], _depth + 1);
                }
            }
            if (object.order_pool) {
                if (!Array.isArray(object.order_pool))
                    throw TypeError(".lq.ActivityCombiningData.order_pool: array expected");
                message.order_pool = Array(object.order_pool.length);
                for (let i = 0; i < object.order_pool.length; ++i) {
                    if (typeof object.order_pool[i] !== "object")
                        throw TypeError(".lq.ActivityCombiningData.order_pool: object expected");
                    message.order_pool[i] = $root.lq.ActivityCombiningPoolData.fromObject(object.order_pool[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityCombiningData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.workbench = [];
                object.orders = [];
                object.unlocked_craft = [];
                object.craft_pool = [];
                object.order_pool = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.recycle_bin = null;
                object.menu = null;
                object.current_order_id = 0;
                object.bonus = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.workbench && message.workbench.length) {
                object.workbench = Array(message.workbench.length);
                for (let j = 0; j < message.workbench.length; ++j)
                    object.workbench[j] = $root.lq.ActivityCombiningWorkbench.toObject(message.workbench[j], options);
            }
            if (message.orders && message.orders.length) {
                object.orders = Array(message.orders.length);
                for (let j = 0; j < message.orders.length; ++j)
                    object.orders[j] = $root.lq.ActivityCombiningOrderData.toObject(message.orders[j], options);
            }
            if (message.recycle_bin != null && message.hasOwnProperty("recycle_bin"))
                object.recycle_bin = $root.lq.ActivityCombiningWorkbench.toObject(message.recycle_bin, options);
            if (message.menu != null && message.hasOwnProperty("menu"))
                object.menu = $root.lq.ActivityCombiningMenuData.toObject(message.menu, options);
            if (message.current_order_id != null && message.hasOwnProperty("current_order_id"))
                object.current_order_id = message.current_order_id;
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                object.bonus = $root.lq.ActivityCombiningData.BonusData.toObject(message.bonus, options);
            if (message.unlocked_craft && message.unlocked_craft.length) {
                object.unlocked_craft = Array(message.unlocked_craft.length);
                for (let j = 0; j < message.unlocked_craft.length; ++j)
                    object.unlocked_craft[j] = message.unlocked_craft[j];
            }
            if (message.craft_pool && message.craft_pool.length) {
                object.craft_pool = Array(message.craft_pool.length);
                for (let j = 0; j < message.craft_pool.length; ++j)
                    object.craft_pool[j] = $root.lq.ActivityCombiningPoolData.toObject(message.craft_pool[j], options);
            }
            if (message.order_pool && message.order_pool.length) {
                object.order_pool = Array(message.order_pool.length);
                for (let j = 0; j < message.order_pool.length; ++j)
                    object.order_pool[j] = $root.lq.ActivityCombiningPoolData.toObject(message.order_pool[j], options);
            }
            return object;
        };

        ActivityCombiningData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityCombiningData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityCombiningData";
        };

        ActivityCombiningData.BonusData = (function() {

            function BonusData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BonusData.prototype.count = 0;
            BonusData.prototype.update_time = 0;

            BonusData.create = function create(properties) {
                return new BonusData(properties);
            };

            BonusData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                    writer.uint32(16).uint32(message.update_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BonusData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BonusData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityCombiningData.BonusData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.update_time = value;
                            else
                                delete message.update_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BonusData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BonusData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.update_time != null && message.hasOwnProperty("update_time"))
                    if (!$util.isInteger(message.update_time))
                        return "update_time: integer expected";
                return null;
            };

            BonusData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityCombiningData.BonusData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityCombiningData.BonusData();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.update_time != null)
                    if (Number(object.update_time) !== 0)
                        message.update_time = object.update_time >>> 0;
                return message;
            };

            BonusData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.count = 0;
                    object.update_time = 0;
                }
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.update_time != null && message.hasOwnProperty("update_time"))
                    object.update_time = message.update_time;
                return object;
            };

            BonusData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BonusData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityCombiningData.BonusData";
            };

            return BonusData;
        })();

        return ActivityCombiningData;
    })();

    lq.VillageReward = (function() {

        function VillageReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VillageReward.prototype.id = 0;
        VillageReward.prototype.count = 0;

        VillageReward.create = function create(properties) {
            return new VillageReward(properties);
        };

        VillageReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VillageReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VillageReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VillageReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VillageReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VillageReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        VillageReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VillageReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VillageReward();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        VillageReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        VillageReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VillageReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VillageReward";
        };

        return VillageReward;
    })();

    lq.VillageBuildingData = (function() {

        function VillageBuildingData(properties) {
            this.reward = [];
            this.workers = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VillageBuildingData.prototype.id = 0;
        VillageBuildingData.prototype.reward = $util.emptyArray;
        VillageBuildingData.prototype.workers = $util.emptyArray;

        VillageBuildingData.create = function create(properties) {
            return new VillageBuildingData(properties);
        };

        VillageBuildingData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.reward != null && message.reward.length)
                for (let i = 0; i < message.reward.length; ++i)
                    $root.lq.VillageReward.encode(message.reward[i], writer.uint32(26).fork()).ldelim();
            if (message.workers != null && message.workers.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.workers.length; ++i)
                    writer.uint32(message.workers[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VillageBuildingData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VillageBuildingData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VillageBuildingData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward && message.reward.length))
                            message.reward = [];
                        message.reward.push($root.lq.VillageReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.workers && message.workers.length))
                                message.workers = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.workers.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.workers && message.workers.length))
                            message.workers = [];
                        message.workers.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VillageBuildingData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VillageBuildingData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.reward != null && message.hasOwnProperty("reward")) {
                if (!Array.isArray(message.reward))
                    return "reward: array expected";
                for (let i = 0; i < message.reward.length; ++i) {
                    let error = $root.lq.VillageReward.verify(message.reward[i], _depth + 1);
                    if (error)
                        return "reward." + error;
                }
            }
            if (message.workers != null && message.hasOwnProperty("workers")) {
                if (!Array.isArray(message.workers))
                    return "workers: array expected";
                for (let i = 0; i < message.workers.length; ++i)
                    if (!$util.isInteger(message.workers[i]))
                        return "workers: integer[] expected";
            }
            return null;
        };

        VillageBuildingData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VillageBuildingData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VillageBuildingData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.reward) {
                if (!Array.isArray(object.reward))
                    throw TypeError(".lq.VillageBuildingData.reward: array expected");
                message.reward = Array(object.reward.length);
                for (let i = 0; i < object.reward.length; ++i) {
                    if (typeof object.reward[i] !== "object")
                        throw TypeError(".lq.VillageBuildingData.reward: object expected");
                    message.reward[i] = $root.lq.VillageReward.fromObject(object.reward[i], _depth + 1);
                }
            }
            if (object.workers) {
                if (!Array.isArray(object.workers))
                    throw TypeError(".lq.VillageBuildingData.workers: array expected");
                message.workers = Array(object.workers.length);
                for (let i = 0; i < object.workers.length; ++i)
                    message.workers[i] = object.workers[i] >>> 0;
            }
            return message;
        };

        VillageBuildingData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.reward = [];
                object.workers = [];
            }
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.reward && message.reward.length) {
                object.reward = Array(message.reward.length);
                for (let j = 0; j < message.reward.length; ++j)
                    object.reward[j] = $root.lq.VillageReward.toObject(message.reward[j], options);
            }
            if (message.workers && message.workers.length) {
                object.workers = Array(message.workers.length);
                for (let j = 0; j < message.workers.length; ++j)
                    object.workers[j] = message.workers[j];
            }
            return object;
        };

        VillageBuildingData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VillageBuildingData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VillageBuildingData";
        };

        return VillageBuildingData;
    })();

    lq.VillageTripData = (function() {

        function VillageTripData(properties) {
            this.reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VillageTripData.prototype.start_round = 0;
        VillageTripData.prototype.dest_id = 0;
        VillageTripData.prototype.reward = $util.emptyArray;
        VillageTripData.prototype.level = 0;
        VillageTripData.prototype.info = null;

        VillageTripData.create = function create(properties) {
            return new VillageTripData(properties);
        };

        VillageTripData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.start_round != null && Object.hasOwnProperty.call(message, "start_round"))
                writer.uint32(8).uint32(message.start_round);
            if (message.dest_id != null && Object.hasOwnProperty.call(message, "dest_id"))
                writer.uint32(16).uint32(message.dest_id);
            if (message.reward != null && message.reward.length)
                for (let i = 0; i < message.reward.length; ++i)
                    $root.lq.VillageReward.encode(message.reward[i], writer.uint32(26).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(32).uint32(message.level);
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.lq.VillageTargetInfo.encode(message.info, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VillageTripData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VillageTripData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VillageTripData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_round = value;
                        else
                            delete message.start_round;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dest_id = value;
                        else
                            delete message.dest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward && message.reward.length))
                            message.reward = [];
                        message.reward.push($root.lq.VillageReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.info = $root.lq.VillageTargetInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VillageTripData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VillageTripData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start_round != null && message.hasOwnProperty("start_round"))
                if (!$util.isInteger(message.start_round))
                    return "start_round: integer expected";
            if (message.dest_id != null && message.hasOwnProperty("dest_id"))
                if (!$util.isInteger(message.dest_id))
                    return "dest_id: integer expected";
            if (message.reward != null && message.hasOwnProperty("reward")) {
                if (!Array.isArray(message.reward))
                    return "reward: array expected";
                for (let i = 0; i < message.reward.length; ++i) {
                    let error = $root.lq.VillageReward.verify(message.reward[i], _depth + 1);
                    if (error)
                        return "reward." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.lq.VillageTargetInfo.verify(message.info, _depth + 1);
                if (error)
                    return "info." + error;
            }
            return null;
        };

        VillageTripData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VillageTripData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VillageTripData();
            if (object.start_round != null)
                if (Number(object.start_round) !== 0)
                    message.start_round = object.start_round >>> 0;
            if (object.dest_id != null)
                if (Number(object.dest_id) !== 0)
                    message.dest_id = object.dest_id >>> 0;
            if (object.reward) {
                if (!Array.isArray(object.reward))
                    throw TypeError(".lq.VillageTripData.reward: array expected");
                message.reward = Array(object.reward.length);
                for (let i = 0; i < object.reward.length; ++i) {
                    if (typeof object.reward[i] !== "object")
                        throw TypeError(".lq.VillageTripData.reward: object expected");
                    message.reward[i] = $root.lq.VillageReward.fromObject(object.reward[i], _depth + 1);
                }
            }
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".lq.VillageTripData.info: object expected");
                message.info = $root.lq.VillageTargetInfo.fromObject(object.info, _depth + 1);
            }
            return message;
        };

        VillageTripData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward = [];
            if (options.defaults) {
                object.start_round = 0;
                object.dest_id = 0;
                object.level = 0;
                object.info = null;
            }
            if (message.start_round != null && message.hasOwnProperty("start_round"))
                object.start_round = message.start_round;
            if (message.dest_id != null && message.hasOwnProperty("dest_id"))
                object.dest_id = message.dest_id;
            if (message.reward && message.reward.length) {
                object.reward = Array(message.reward.length);
                for (let j = 0; j < message.reward.length; ++j)
                    object.reward[j] = $root.lq.VillageReward.toObject(message.reward[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.lq.VillageTargetInfo.toObject(message.info, options);
            return object;
        };

        VillageTripData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VillageTripData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VillageTripData";
        };

        return VillageTripData;
    })();

    lq.VillageTaskData = (function() {

        function VillageTaskData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VillageTaskData.prototype.id = 0;
        VillageTaskData.prototype.completed_count = 0;

        VillageTaskData.create = function create(properties) {
            return new VillageTaskData(properties);
        };

        VillageTaskData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.completed_count != null && Object.hasOwnProperty.call(message, "completed_count"))
                writer.uint32(16).uint32(message.completed_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VillageTaskData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VillageTaskData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VillageTaskData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.completed_count = value;
                        else
                            delete message.completed_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VillageTaskData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VillageTaskData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.completed_count != null && message.hasOwnProperty("completed_count"))
                if (!$util.isInteger(message.completed_count))
                    return "completed_count: integer expected";
            return null;
        };

        VillageTaskData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VillageTaskData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VillageTaskData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.completed_count != null)
                if (Number(object.completed_count) !== 0)
                    message.completed_count = object.completed_count >>> 0;
            return message;
        };

        VillageTaskData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.completed_count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.completed_count != null && message.hasOwnProperty("completed_count"))
                object.completed_count = message.completed_count;
            return object;
        };

        VillageTaskData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VillageTaskData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VillageTaskData";
        };

        return VillageTaskData;
    })();

    lq.VillageTargetInfo = (function() {

        function VillageTargetInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VillageTargetInfo.prototype.nickname = "";
        VillageTargetInfo.prototype.avatar = 0;
        VillageTargetInfo.prototype.avatar_frame = 0;
        VillageTargetInfo.prototype.title = 0;
        VillageTargetInfo.prototype.verified = 0;

        VillageTargetInfo.create = function create(properties) {
            return new VillageTargetInfo(properties);
        };

        VillageTargetInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(10).string(message.nickname);
            if (message.avatar != null && Object.hasOwnProperty.call(message, "avatar"))
                writer.uint32(16).uint32(message.avatar);
            if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                writer.uint32(24).uint32(message.avatar_frame);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(32).uint32(message.title);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(40).uint32(message.verified);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VillageTargetInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VillageTargetInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VillageTargetInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar = value;
                        else
                            delete message.avatar;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_frame = value;
                        else
                            delete message.avatar_frame;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VillageTargetInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VillageTargetInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                if (!$util.isInteger(message.avatar))
                    return "avatar: integer expected";
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                if (!$util.isInteger(message.avatar_frame))
                    return "avatar_frame: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            return null;
        };

        VillageTargetInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VillageTargetInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VillageTargetInfo();
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.avatar != null)
                if (Number(object.avatar) !== 0)
                    message.avatar = object.avatar >>> 0;
            if (object.avatar_frame != null)
                if (Number(object.avatar_frame) !== 0)
                    message.avatar_frame = object.avatar_frame >>> 0;
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            return message;
        };

        VillageTargetInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.nickname = "";
                object.avatar = 0;
                object.avatar_frame = 0;
                object.title = 0;
                object.verified = 0;
            }
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.avatar != null && message.hasOwnProperty("avatar"))
                object.avatar = message.avatar;
            if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                object.avatar_frame = message.avatar_frame;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            return object;
        };

        VillageTargetInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VillageTargetInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VillageTargetInfo";
        };

        return VillageTargetInfo;
    })();

    lq.ActivityVillageData = (function() {

        function ActivityVillageData(properties) {
            this.buildings = [];
            this.trip = [];
            this.tasks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityVillageData.prototype.activity_id = 0;
        ActivityVillageData.prototype.buildings = $util.emptyArray;
        ActivityVillageData.prototype.trip = $util.emptyArray;
        ActivityVillageData.prototype.tasks = $util.emptyArray;
        ActivityVillageData.prototype.round = 0;

        ActivityVillageData.create = function create(properties) {
            return new ActivityVillageData(properties);
        };

        ActivityVillageData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.buildings != null && message.buildings.length)
                for (let i = 0; i < message.buildings.length; ++i)
                    $root.lq.VillageBuildingData.encode(message.buildings[i], writer.uint32(18).fork()).ldelim();
            if (message.trip != null && message.trip.length)
                for (let i = 0; i < message.trip.length; ++i)
                    $root.lq.VillageTripData.encode(message.trip[i], writer.uint32(26).fork()).ldelim();
            if (message.tasks != null && message.tasks.length)
                for (let i = 0; i < message.tasks.length; ++i)
                    $root.lq.VillageTaskData.encode(message.tasks[i], writer.uint32(50).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(56).uint32(message.round);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityVillageData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityVillageData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityVillageData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buildings && message.buildings.length))
                            message.buildings = [];
                        message.buildings.push($root.lq.VillageBuildingData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.trip && message.trip.length))
                            message.trip = [];
                        message.trip.push($root.lq.VillageTripData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tasks && message.tasks.length))
                            message.tasks = [];
                        message.tasks.push($root.lq.VillageTaskData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityVillageData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityVillageData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.buildings != null && message.hasOwnProperty("buildings")) {
                if (!Array.isArray(message.buildings))
                    return "buildings: array expected";
                for (let i = 0; i < message.buildings.length; ++i) {
                    let error = $root.lq.VillageBuildingData.verify(message.buildings[i], _depth + 1);
                    if (error)
                        return "buildings." + error;
                }
            }
            if (message.trip != null && message.hasOwnProperty("trip")) {
                if (!Array.isArray(message.trip))
                    return "trip: array expected";
                for (let i = 0; i < message.trip.length; ++i) {
                    let error = $root.lq.VillageTripData.verify(message.trip[i], _depth + 1);
                    if (error)
                        return "trip." + error;
                }
            }
            if (message.tasks != null && message.hasOwnProperty("tasks")) {
                if (!Array.isArray(message.tasks))
                    return "tasks: array expected";
                for (let i = 0; i < message.tasks.length; ++i) {
                    let error = $root.lq.VillageTaskData.verify(message.tasks[i], _depth + 1);
                    if (error)
                        return "tasks." + error;
                }
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            return null;
        };

        ActivityVillageData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityVillageData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityVillageData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.buildings) {
                if (!Array.isArray(object.buildings))
                    throw TypeError(".lq.ActivityVillageData.buildings: array expected");
                message.buildings = Array(object.buildings.length);
                for (let i = 0; i < object.buildings.length; ++i) {
                    if (typeof object.buildings[i] !== "object")
                        throw TypeError(".lq.ActivityVillageData.buildings: object expected");
                    message.buildings[i] = $root.lq.VillageBuildingData.fromObject(object.buildings[i], _depth + 1);
                }
            }
            if (object.trip) {
                if (!Array.isArray(object.trip))
                    throw TypeError(".lq.ActivityVillageData.trip: array expected");
                message.trip = Array(object.trip.length);
                for (let i = 0; i < object.trip.length; ++i) {
                    if (typeof object.trip[i] !== "object")
                        throw TypeError(".lq.ActivityVillageData.trip: object expected");
                    message.trip[i] = $root.lq.VillageTripData.fromObject(object.trip[i], _depth + 1);
                }
            }
            if (object.tasks) {
                if (!Array.isArray(object.tasks))
                    throw TypeError(".lq.ActivityVillageData.tasks: array expected");
                message.tasks = Array(object.tasks.length);
                for (let i = 0; i < object.tasks.length; ++i) {
                    if (typeof object.tasks[i] !== "object")
                        throw TypeError(".lq.ActivityVillageData.tasks: object expected");
                    message.tasks[i] = $root.lq.VillageTaskData.fromObject(object.tasks[i], _depth + 1);
                }
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            return message;
        };

        ActivityVillageData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.buildings = [];
                object.trip = [];
                object.tasks = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.round = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.buildings && message.buildings.length) {
                object.buildings = Array(message.buildings.length);
                for (let j = 0; j < message.buildings.length; ++j)
                    object.buildings[j] = $root.lq.VillageBuildingData.toObject(message.buildings[j], options);
            }
            if (message.trip && message.trip.length) {
                object.trip = Array(message.trip.length);
                for (let j = 0; j < message.trip.length; ++j)
                    object.trip[j] = $root.lq.VillageTripData.toObject(message.trip[j], options);
            }
            if (message.tasks && message.tasks.length) {
                object.tasks = Array(message.tasks.length);
                for (let j = 0; j < message.tasks.length; ++j)
                    object.tasks[j] = $root.lq.VillageTaskData.toObject(message.tasks[j], options);
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            return object;
        };

        ActivityVillageData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityVillageData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityVillageData";
        };

        return ActivityVillageData;
    })();

    lq.TimeCounterData = (function() {

        function TimeCounterData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TimeCounterData.prototype.count = 0;
        TimeCounterData.prototype.update_time = 0;

        TimeCounterData.create = function create(properties) {
            return new TimeCounterData(properties);
        };

        TimeCounterData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(8).uint32(message.count);
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(16).uint32(message.update_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TimeCounterData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TimeCounterData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TimeCounterData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TimeCounterData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TimeCounterData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            return null;
        };

        TimeCounterData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TimeCounterData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TimeCounterData();
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            return message;
        };

        TimeCounterData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.count = 0;
                object.update_time = 0;
            }
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            return object;
        };

        TimeCounterData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TimeCounterData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TimeCounterData";
        };

        return TimeCounterData;
    })();

    lq.SignedTimeCounterData = (function() {

        function SignedTimeCounterData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SignedTimeCounterData.prototype.count = 0;
        SignedTimeCounterData.prototype.update_time = 0;

        SignedTimeCounterData.create = function create(properties) {
            return new SignedTimeCounterData(properties);
        };

        SignedTimeCounterData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(8).int32(message.count);
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(16).uint32(message.update_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SignedTimeCounterData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SignedTimeCounterData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SignedTimeCounterData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SignedTimeCounterData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SignedTimeCounterData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            return null;
        };

        SignedTimeCounterData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SignedTimeCounterData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SignedTimeCounterData();
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count | 0;
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            return message;
        };

        SignedTimeCounterData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.count = 0;
                object.update_time = 0;
            }
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            return object;
        };

        SignedTimeCounterData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SignedTimeCounterData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SignedTimeCounterData";
        };

        return SignedTimeCounterData;
    })();

    lq.FestivalProposalData = (function() {

        function FestivalProposalData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FestivalProposalData.prototype.id = 0;
        FestivalProposalData.prototype.proposal_id = 0;
        FestivalProposalData.prototype.pos = 0;

        FestivalProposalData.create = function create(properties) {
            return new FestivalProposalData(properties);
        };

        FestivalProposalData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.proposal_id != null && Object.hasOwnProperty.call(message, "proposal_id"))
                writer.uint32(16).uint32(message.proposal_id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(24).uint32(message.pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FestivalProposalData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FestivalProposalData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FestivalProposalData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.proposal_id = value;
                        else
                            delete message.proposal_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FestivalProposalData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FestivalProposalData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.proposal_id != null && message.hasOwnProperty("proposal_id"))
                if (!$util.isInteger(message.proposal_id))
                    return "proposal_id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            return null;
        };

        FestivalProposalData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FestivalProposalData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FestivalProposalData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.proposal_id != null)
                if (Number(object.proposal_id) !== 0)
                    message.proposal_id = object.proposal_id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            return message;
        };

        FestivalProposalData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.proposal_id = 0;
                object.pos = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.proposal_id != null && message.hasOwnProperty("proposal_id"))
                object.proposal_id = message.proposal_id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            return object;
        };

        FestivalProposalData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FestivalProposalData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FestivalProposalData";
        };

        return FestivalProposalData;
    })();

    lq.ActivityFestivalData = (function() {

        function ActivityFestivalData(properties) {
            this.proposal_list = [];
            this.event_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityFestivalData.prototype.activity_id = 0;
        ActivityFestivalData.prototype.level = 0;
        ActivityFestivalData.prototype.proposal_list = $util.emptyArray;
        ActivityFestivalData.prototype.event_list = $util.emptyArray;
        ActivityFestivalData.prototype.buy_record = null;

        ActivityFestivalData.create = function create(properties) {
            return new ActivityFestivalData(properties);
        };

        ActivityFestivalData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.proposal_list != null && message.proposal_list.length)
                for (let i = 0; i < message.proposal_list.length; ++i)
                    $root.lq.FestivalProposalData.encode(message.proposal_list[i], writer.uint32(26).fork()).ldelim();
            if (message.event_list != null && message.event_list.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.event_list.length; ++i)
                    writer.uint32(message.event_list[i]);
                writer.ldelim();
            }
            if (message.buy_record != null && Object.hasOwnProperty.call(message, "buy_record"))
                $root.lq.SignedTimeCounterData.encode(message.buy_record, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityFestivalData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityFestivalData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFestivalData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.proposal_list && message.proposal_list.length))
                            message.proposal_list = [];
                        message.proposal_list.push($root.lq.FestivalProposalData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.event_list && message.event_list.length))
                                message.event_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.event_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.event_list && message.event_list.length))
                            message.event_list = [];
                        message.event_list.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.buy_record = $root.lq.SignedTimeCounterData.decode(reader, reader.uint32(), undefined, _depth + 1, message.buy_record);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityFestivalData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityFestivalData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.proposal_list != null && message.hasOwnProperty("proposal_list")) {
                if (!Array.isArray(message.proposal_list))
                    return "proposal_list: array expected";
                for (let i = 0; i < message.proposal_list.length; ++i) {
                    let error = $root.lq.FestivalProposalData.verify(message.proposal_list[i], _depth + 1);
                    if (error)
                        return "proposal_list." + error;
                }
            }
            if (message.event_list != null && message.hasOwnProperty("event_list")) {
                if (!Array.isArray(message.event_list))
                    return "event_list: array expected";
                for (let i = 0; i < message.event_list.length; ++i)
                    if (!$util.isInteger(message.event_list[i]))
                        return "event_list: integer[] expected";
            }
            if (message.buy_record != null && message.hasOwnProperty("buy_record")) {
                let error = $root.lq.SignedTimeCounterData.verify(message.buy_record, _depth + 1);
                if (error)
                    return "buy_record." + error;
            }
            return null;
        };

        ActivityFestivalData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityFestivalData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityFestivalData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.proposal_list) {
                if (!Array.isArray(object.proposal_list))
                    throw TypeError(".lq.ActivityFestivalData.proposal_list: array expected");
                message.proposal_list = Array(object.proposal_list.length);
                for (let i = 0; i < object.proposal_list.length; ++i) {
                    if (typeof object.proposal_list[i] !== "object")
                        throw TypeError(".lq.ActivityFestivalData.proposal_list: object expected");
                    message.proposal_list[i] = $root.lq.FestivalProposalData.fromObject(object.proposal_list[i], _depth + 1);
                }
            }
            if (object.event_list) {
                if (!Array.isArray(object.event_list))
                    throw TypeError(".lq.ActivityFestivalData.event_list: array expected");
                message.event_list = Array(object.event_list.length);
                for (let i = 0; i < object.event_list.length; ++i)
                    message.event_list[i] = object.event_list[i] >>> 0;
            }
            if (object.buy_record != null) {
                if (typeof object.buy_record !== "object")
                    throw TypeError(".lq.ActivityFestivalData.buy_record: object expected");
                message.buy_record = $root.lq.SignedTimeCounterData.fromObject(object.buy_record, _depth + 1);
            }
            return message;
        };

        ActivityFestivalData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.proposal_list = [];
                object.event_list = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.level = 0;
                object.buy_record = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.proposal_list && message.proposal_list.length) {
                object.proposal_list = Array(message.proposal_list.length);
                for (let j = 0; j < message.proposal_list.length; ++j)
                    object.proposal_list[j] = $root.lq.FestivalProposalData.toObject(message.proposal_list[j], options);
            }
            if (message.event_list && message.event_list.length) {
                object.event_list = Array(message.event_list.length);
                for (let j = 0; j < message.event_list.length; ++j)
                    object.event_list[j] = message.event_list[j];
            }
            if (message.buy_record != null && message.hasOwnProperty("buy_record"))
                object.buy_record = $root.lq.SignedTimeCounterData.toObject(message.buy_record, options);
            return object;
        };

        ActivityFestivalData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityFestivalData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityFestivalData";
        };

        return ActivityFestivalData;
    })();

    lq.SimulationV2Data = (function() {

        function SimulationV2Data(properties) {
            this.event_pool = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Data.prototype.activity_id = 0;
        SimulationV2Data.prototype.season = null;
        SimulationV2Data.prototype.highest_score = 0;
        SimulationV2Data.prototype.upgrade = null;
        SimulationV2Data.prototype.event_pool = $util.emptyArray;
        SimulationV2Data.prototype.season_count = 0;

        SimulationV2Data.create = function create(properties) {
            return new SimulationV2Data(properties);
        };

        SimulationV2Data.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                $root.lq.SimulationV2SeasonData.encode(message.season, writer.uint32(18).fork()).ldelim();
            if (message.highest_score != null && Object.hasOwnProperty.call(message, "highest_score"))
                writer.uint32(24).int32(message.highest_score);
            if (message.upgrade != null && Object.hasOwnProperty.call(message, "upgrade"))
                $root.lq.SimulationV2Ability.encode(message.upgrade, writer.uint32(34).fork()).ldelim();
            if (message.event_pool != null && message.event_pool.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.event_pool.length; ++i)
                    writer.uint32(message.event_pool[i]);
                writer.ldelim();
            }
            if (message.season_count != null && Object.hasOwnProperty.call(message, "season_count"))
                writer.uint32(48).uint32(message.season_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Data.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Data.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Data(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.season = $root.lq.SimulationV2SeasonData.decode(reader, reader.uint32(), undefined, _depth + 1, message.season);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.highest_score = value;
                        else
                            delete message.highest_score;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.upgrade = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade);
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.event_pool && message.event_pool.length))
                                message.event_pool = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.event_pool.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.event_pool && message.event_pool.length))
                            message.event_pool = [];
                        message.event_pool.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.season_count = value;
                        else
                            delete message.season_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Data.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Data.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.season != null && message.hasOwnProperty("season")) {
                let error = $root.lq.SimulationV2SeasonData.verify(message.season, _depth + 1);
                if (error)
                    return "season." + error;
            }
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                if (!$util.isInteger(message.highest_score))
                    return "highest_score: integer expected";
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                let error = $root.lq.SimulationV2Ability.verify(message.upgrade, _depth + 1);
                if (error)
                    return "upgrade." + error;
            }
            if (message.event_pool != null && message.hasOwnProperty("event_pool")) {
                if (!Array.isArray(message.event_pool))
                    return "event_pool: array expected";
                for (let i = 0; i < message.event_pool.length; ++i)
                    if (!$util.isInteger(message.event_pool[i]))
                        return "event_pool: integer[] expected";
            }
            if (message.season_count != null && message.hasOwnProperty("season_count"))
                if (!$util.isInteger(message.season_count))
                    return "season_count: integer expected";
            return null;
        };

        SimulationV2Data.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Data)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Data();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.season != null) {
                if (typeof object.season !== "object")
                    throw TypeError(".lq.SimulationV2Data.season: object expected");
                message.season = $root.lq.SimulationV2SeasonData.fromObject(object.season, _depth + 1);
            }
            if (object.highest_score != null)
                if (Number(object.highest_score) !== 0)
                    message.highest_score = object.highest_score | 0;
            if (object.upgrade != null) {
                if (typeof object.upgrade !== "object")
                    throw TypeError(".lq.SimulationV2Data.upgrade: object expected");
                message.upgrade = $root.lq.SimulationV2Ability.fromObject(object.upgrade, _depth + 1);
            }
            if (object.event_pool) {
                if (!Array.isArray(object.event_pool))
                    throw TypeError(".lq.SimulationV2Data.event_pool: array expected");
                message.event_pool = Array(object.event_pool.length);
                for (let i = 0; i < object.event_pool.length; ++i)
                    message.event_pool[i] = object.event_pool[i] >>> 0;
            }
            if (object.season_count != null)
                if (Number(object.season_count) !== 0)
                    message.season_count = object.season_count >>> 0;
            return message;
        };

        SimulationV2Data.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.event_pool = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.season = null;
                object.highest_score = 0;
                object.upgrade = null;
                object.season_count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.season != null && message.hasOwnProperty("season"))
                object.season = $root.lq.SimulationV2SeasonData.toObject(message.season, options);
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                object.highest_score = message.highest_score;
            if (message.upgrade != null && message.hasOwnProperty("upgrade"))
                object.upgrade = $root.lq.SimulationV2Ability.toObject(message.upgrade, options);
            if (message.event_pool && message.event_pool.length) {
                object.event_pool = Array(message.event_pool.length);
                for (let j = 0; j < message.event_pool.length; ++j)
                    object.event_pool[j] = message.event_pool[j];
            }
            if (message.season_count != null && message.hasOwnProperty("season_count"))
                object.season_count = message.season_count;
            return object;
        };

        SimulationV2Data.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Data.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Data";
        };

        return SimulationV2Data;
    })();

    lq.IslandBagItemData = (function() {

        function IslandBagItemData(properties) {
            this.pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        IslandBagItemData.prototype.id = 0;
        IslandBagItemData.prototype.pos = $util.emptyArray;
        IslandBagItemData.prototype.rotate = 0;
        IslandBagItemData.prototype.goods_id = 0;
        IslandBagItemData.prototype.price = 0;

        IslandBagItemData.create = function create(properties) {
            return new IslandBagItemData(properties);
        };

        IslandBagItemData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.pos != null && message.pos.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.pos.length; ++i)
                    writer.uint32(message.pos[i]);
                writer.ldelim();
            }
            if (message.rotate != null && Object.hasOwnProperty.call(message, "rotate"))
                writer.uint32(24).uint32(message.rotate);
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(32).uint32(message.goods_id);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(40).uint32(message.price);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        IslandBagItemData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        IslandBagItemData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.IslandBagItemData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.pos && message.pos.length))
                                message.pos = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.pos.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.pos && message.pos.length))
                            message.pos = [];
                        message.pos.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rotate = value;
                        else
                            delete message.rotate;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.price = value;
                        else
                            delete message.price;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        IslandBagItemData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        IslandBagItemData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos")) {
                if (!Array.isArray(message.pos))
                    return "pos: array expected";
                for (let i = 0; i < message.pos.length; ++i)
                    if (!$util.isInteger(message.pos[i]))
                        return "pos: integer[] expected";
            }
            if (message.rotate != null && message.hasOwnProperty("rotate"))
                if (!$util.isInteger(message.rotate))
                    return "rotate: integer expected";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isInteger(message.price))
                    return "price: integer expected";
            return null;
        };

        IslandBagItemData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.IslandBagItemData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.IslandBagItemData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.pos) {
                if (!Array.isArray(object.pos))
                    throw TypeError(".lq.IslandBagItemData.pos: array expected");
                message.pos = Array(object.pos.length);
                for (let i = 0; i < object.pos.length; ++i)
                    message.pos[i] = object.pos[i] >>> 0;
            }
            if (object.rotate != null)
                if (Number(object.rotate) !== 0)
                    message.rotate = object.rotate >>> 0;
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.price != null)
                if (Number(object.price) !== 0)
                    message.price = object.price >>> 0;
            return message;
        };

        IslandBagItemData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.pos = [];
            if (options.defaults) {
                object.id = 0;
                object.rotate = 0;
                object.goods_id = 0;
                object.price = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.pos && message.pos.length) {
                object.pos = Array(message.pos.length);
                for (let j = 0; j < message.pos.length; ++j)
                    object.pos[j] = message.pos[j];
            }
            if (message.rotate != null && message.hasOwnProperty("rotate"))
                object.rotate = message.rotate;
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            return object;
        };

        IslandBagItemData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        IslandBagItemData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.IslandBagItemData";
        };

        return IslandBagItemData;
    })();

    lq.IslandBagData = (function() {

        function IslandBagData(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        IslandBagData.prototype.id = 0;
        IslandBagData.prototype.matrix = "";
        IslandBagData.prototype.items = $util.emptyArray;

        IslandBagData.create = function create(properties) {
            return new IslandBagData(properties);
        };

        IslandBagData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.matrix != null && Object.hasOwnProperty.call(message, "matrix"))
                writer.uint32(18).string(message.matrix);
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.IslandBagItemData.encode(message.items[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        IslandBagData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        IslandBagData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.IslandBagData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.matrix = value;
                        else
                            delete message.matrix;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.IslandBagItemData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        IslandBagData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        IslandBagData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.matrix != null && message.hasOwnProperty("matrix"))
                if (!$util.isString(message.matrix))
                    return "matrix: string expected";
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.IslandBagItemData.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        IslandBagData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.IslandBagData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.IslandBagData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.matrix != null)
                if (typeof object.matrix !== "string" || object.matrix.length)
                    message.matrix = String(object.matrix);
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.IslandBagData.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.IslandBagData.items: object expected");
                    message.items[i] = $root.lq.IslandBagItemData.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        IslandBagData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.id = 0;
                object.matrix = "";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.matrix != null && message.hasOwnProperty("matrix"))
                object.matrix = message.matrix;
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.IslandBagItemData.toObject(message.items[j], options);
            }
            return object;
        };

        IslandBagData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        IslandBagData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.IslandBagData";
        };

        return IslandBagData;
    })();

    lq.IslandGoodsData = (function() {

        function IslandGoodsData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        IslandGoodsData.prototype.goods_id = 0;
        IslandGoodsData.prototype.count = 0;
        IslandGoodsData.prototype.update_time = 0;

        IslandGoodsData.create = function create(properties) {
            return new IslandGoodsData(properties);
        };

        IslandGoodsData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).int32(message.count);
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(24).uint32(message.update_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        IslandGoodsData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        IslandGoodsData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.IslandGoodsData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        IslandGoodsData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        IslandGoodsData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            return null;
        };

        IslandGoodsData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.IslandGoodsData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.IslandGoodsData();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count | 0;
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            return message;
        };

        IslandGoodsData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.count = 0;
                object.update_time = 0;
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            return object;
        };

        IslandGoodsData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        IslandGoodsData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.IslandGoodsData";
        };

        return IslandGoodsData;
    })();

    lq.IslandZoneData = (function() {

        function IslandZoneData(properties) {
            this.goods_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        IslandZoneData.prototype.id = 0;
        IslandZoneData.prototype.currency_used = null;
        IslandZoneData.prototype.goods_records = $util.emptyArray;

        IslandZoneData.create = function create(properties) {
            return new IslandZoneData(properties);
        };

        IslandZoneData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.currency_used != null && Object.hasOwnProperty.call(message, "currency_used"))
                $root.lq.SignedTimeCounterData.encode(message.currency_used, writer.uint32(18).fork()).ldelim();
            if (message.goods_records != null && message.goods_records.length)
                for (let i = 0; i < message.goods_records.length; ++i)
                    $root.lq.IslandGoodsData.encode(message.goods_records[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        IslandZoneData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        IslandZoneData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.IslandZoneData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.currency_used = $root.lq.SignedTimeCounterData.decode(reader, reader.uint32(), undefined, _depth + 1, message.currency_used);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.goods_records && message.goods_records.length))
                            message.goods_records = [];
                        message.goods_records.push($root.lq.IslandGoodsData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        IslandZoneData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        IslandZoneData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.currency_used != null && message.hasOwnProperty("currency_used")) {
                let error = $root.lq.SignedTimeCounterData.verify(message.currency_used, _depth + 1);
                if (error)
                    return "currency_used." + error;
            }
            if (message.goods_records != null && message.hasOwnProperty("goods_records")) {
                if (!Array.isArray(message.goods_records))
                    return "goods_records: array expected";
                for (let i = 0; i < message.goods_records.length; ++i) {
                    let error = $root.lq.IslandGoodsData.verify(message.goods_records[i], _depth + 1);
                    if (error)
                        return "goods_records." + error;
                }
            }
            return null;
        };

        IslandZoneData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.IslandZoneData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.IslandZoneData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.currency_used != null) {
                if (typeof object.currency_used !== "object")
                    throw TypeError(".lq.IslandZoneData.currency_used: object expected");
                message.currency_used = $root.lq.SignedTimeCounterData.fromObject(object.currency_used, _depth + 1);
            }
            if (object.goods_records) {
                if (!Array.isArray(object.goods_records))
                    throw TypeError(".lq.IslandZoneData.goods_records: array expected");
                message.goods_records = Array(object.goods_records.length);
                for (let i = 0; i < object.goods_records.length; ++i) {
                    if (typeof object.goods_records[i] !== "object")
                        throw TypeError(".lq.IslandZoneData.goods_records: object expected");
                    message.goods_records[i] = $root.lq.IslandGoodsData.fromObject(object.goods_records[i], _depth + 1);
                }
            }
            return message;
        };

        IslandZoneData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.goods_records = [];
            if (options.defaults) {
                object.id = 0;
                object.currency_used = null;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.currency_used != null && message.hasOwnProperty("currency_used"))
                object.currency_used = $root.lq.SignedTimeCounterData.toObject(message.currency_used, options);
            if (message.goods_records && message.goods_records.length) {
                object.goods_records = Array(message.goods_records.length);
                for (let j = 0; j < message.goods_records.length; ++j)
                    object.goods_records[j] = $root.lq.IslandGoodsData.toObject(message.goods_records[j], options);
            }
            return object;
        };

        IslandZoneData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        IslandZoneData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.IslandZoneData";
        };

        return IslandZoneData;
    })();

    lq.ActivityIslandData = (function() {

        function ActivityIslandData(properties) {
            this.bags = [];
            this.zones = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityIslandData.prototype.activity_id = 0;
        ActivityIslandData.prototype.zone = 0;
        ActivityIslandData.prototype.bags = $util.emptyArray;
        ActivityIslandData.prototype.zones = $util.emptyArray;

        ActivityIslandData.create = function create(properties) {
            return new ActivityIslandData(properties);
        };

        ActivityIslandData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.zone != null && Object.hasOwnProperty.call(message, "zone"))
                writer.uint32(16).uint32(message.zone);
            if (message.bags != null && message.bags.length)
                for (let i = 0; i < message.bags.length; ++i)
                    $root.lq.IslandBagData.encode(message.bags[i], writer.uint32(26).fork()).ldelim();
            if (message.zones != null && message.zones.length)
                for (let i = 0; i < message.zones.length; ++i)
                    $root.lq.IslandZoneData.encode(message.zones[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityIslandData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityIslandData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityIslandData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.zone = value;
                        else
                            delete message.zone;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bags && message.bags.length))
                            message.bags = [];
                        message.bags.push($root.lq.IslandBagData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.zones && message.zones.length))
                            message.zones = [];
                        message.zones.push($root.lq.IslandZoneData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityIslandData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityIslandData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.zone != null && message.hasOwnProperty("zone"))
                if (!$util.isInteger(message.zone))
                    return "zone: integer expected";
            if (message.bags != null && message.hasOwnProperty("bags")) {
                if (!Array.isArray(message.bags))
                    return "bags: array expected";
                for (let i = 0; i < message.bags.length; ++i) {
                    let error = $root.lq.IslandBagData.verify(message.bags[i], _depth + 1);
                    if (error)
                        return "bags." + error;
                }
            }
            if (message.zones != null && message.hasOwnProperty("zones")) {
                if (!Array.isArray(message.zones))
                    return "zones: array expected";
                for (let i = 0; i < message.zones.length; ++i) {
                    let error = $root.lq.IslandZoneData.verify(message.zones[i], _depth + 1);
                    if (error)
                        return "zones." + error;
                }
            }
            return null;
        };

        ActivityIslandData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityIslandData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityIslandData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.zone != null)
                if (Number(object.zone) !== 0)
                    message.zone = object.zone >>> 0;
            if (object.bags) {
                if (!Array.isArray(object.bags))
                    throw TypeError(".lq.ActivityIslandData.bags: array expected");
                message.bags = Array(object.bags.length);
                for (let i = 0; i < object.bags.length; ++i) {
                    if (typeof object.bags[i] !== "object")
                        throw TypeError(".lq.ActivityIslandData.bags: object expected");
                    message.bags[i] = $root.lq.IslandBagData.fromObject(object.bags[i], _depth + 1);
                }
            }
            if (object.zones) {
                if (!Array.isArray(object.zones))
                    throw TypeError(".lq.ActivityIslandData.zones: array expected");
                message.zones = Array(object.zones.length);
                for (let i = 0; i < object.zones.length; ++i) {
                    if (typeof object.zones[i] !== "object")
                        throw TypeError(".lq.ActivityIslandData.zones: object expected");
                    message.zones[i] = $root.lq.IslandZoneData.fromObject(object.zones[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityIslandData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.bags = [];
                object.zones = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.zone = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.zone != null && message.hasOwnProperty("zone"))
                object.zone = message.zone;
            if (message.bags && message.bags.length) {
                object.bags = Array(message.bags.length);
                for (let j = 0; j < message.bags.length; ++j)
                    object.bags[j] = $root.lq.IslandBagData.toObject(message.bags[j], options);
            }
            if (message.zones && message.zones.length) {
                object.zones = Array(message.zones.length);
                for (let j = 0; j < message.zones.length; ++j)
                    object.zones[j] = $root.lq.IslandZoneData.toObject(message.zones[j], options);
            }
            return object;
        };

        ActivityIslandData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityIslandData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityIslandData";
        };

        return ActivityIslandData;
    })();

    lq.ActivityFeedData = (function() {

        function ActivityFeedData(properties) {
            this.gift_inbox = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityFeedData.prototype.activity_id = 0;
        ActivityFeedData.prototype.feed_count = 0;
        ActivityFeedData.prototype.friend_receive_data = null;
        ActivityFeedData.prototype.friend_send_data = null;
        ActivityFeedData.prototype.gift_inbox = $util.emptyArray;
        ActivityFeedData.prototype.max_inbox_id = 0;

        ActivityFeedData.create = function create(properties) {
            return new ActivityFeedData(properties);
        };

        ActivityFeedData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.feed_count != null && Object.hasOwnProperty.call(message, "feed_count"))
                writer.uint32(16).uint32(message.feed_count);
            if (message.friend_receive_data != null && Object.hasOwnProperty.call(message, "friend_receive_data"))
                $root.lq.ActivityFeedData.CountWithTimeData.encode(message.friend_receive_data, writer.uint32(26).fork()).ldelim();
            if (message.friend_send_data != null && Object.hasOwnProperty.call(message, "friend_send_data"))
                $root.lq.ActivityFeedData.CountWithTimeData.encode(message.friend_send_data, writer.uint32(34).fork()).ldelim();
            if (message.gift_inbox != null && message.gift_inbox.length)
                for (let i = 0; i < message.gift_inbox.length; ++i)
                    $root.lq.ActivityFeedData.GiftBoxData.encode(message.gift_inbox[i], writer.uint32(42).fork()).ldelim();
            if (message.max_inbox_id != null && Object.hasOwnProperty.call(message, "max_inbox_id"))
                writer.uint32(48).uint32(message.max_inbox_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityFeedData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityFeedData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFeedData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.feed_count = value;
                        else
                            delete message.feed_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.friend_receive_data = $root.lq.ActivityFeedData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_receive_data);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.friend_send_data = $root.lq.ActivityFeedData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_send_data);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gift_inbox && message.gift_inbox.length))
                            message.gift_inbox = [];
                        message.gift_inbox.push($root.lq.ActivityFeedData.GiftBoxData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_inbox_id = value;
                        else
                            delete message.max_inbox_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityFeedData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityFeedData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                if (!$util.isInteger(message.feed_count))
                    return "feed_count: integer expected";
            if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data")) {
                let error = $root.lq.ActivityFeedData.CountWithTimeData.verify(message.friend_receive_data, _depth + 1);
                if (error)
                    return "friend_receive_data." + error;
            }
            if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data")) {
                let error = $root.lq.ActivityFeedData.CountWithTimeData.verify(message.friend_send_data, _depth + 1);
                if (error)
                    return "friend_send_data." + error;
            }
            if (message.gift_inbox != null && message.hasOwnProperty("gift_inbox")) {
                if (!Array.isArray(message.gift_inbox))
                    return "gift_inbox: array expected";
                for (let i = 0; i < message.gift_inbox.length; ++i) {
                    let error = $root.lq.ActivityFeedData.GiftBoxData.verify(message.gift_inbox[i], _depth + 1);
                    if (error)
                        return "gift_inbox." + error;
                }
            }
            if (message.max_inbox_id != null && message.hasOwnProperty("max_inbox_id"))
                if (!$util.isInteger(message.max_inbox_id))
                    return "max_inbox_id: integer expected";
            return null;
        };

        ActivityFeedData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityFeedData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityFeedData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.feed_count != null)
                if (Number(object.feed_count) !== 0)
                    message.feed_count = object.feed_count >>> 0;
            if (object.friend_receive_data != null) {
                if (typeof object.friend_receive_data !== "object")
                    throw TypeError(".lq.ActivityFeedData.friend_receive_data: object expected");
                message.friend_receive_data = $root.lq.ActivityFeedData.CountWithTimeData.fromObject(object.friend_receive_data, _depth + 1);
            }
            if (object.friend_send_data != null) {
                if (typeof object.friend_send_data !== "object")
                    throw TypeError(".lq.ActivityFeedData.friend_send_data: object expected");
                message.friend_send_data = $root.lq.ActivityFeedData.CountWithTimeData.fromObject(object.friend_send_data, _depth + 1);
            }
            if (object.gift_inbox) {
                if (!Array.isArray(object.gift_inbox))
                    throw TypeError(".lq.ActivityFeedData.gift_inbox: array expected");
                message.gift_inbox = Array(object.gift_inbox.length);
                for (let i = 0; i < object.gift_inbox.length; ++i) {
                    if (typeof object.gift_inbox[i] !== "object")
                        throw TypeError(".lq.ActivityFeedData.gift_inbox: object expected");
                    message.gift_inbox[i] = $root.lq.ActivityFeedData.GiftBoxData.fromObject(object.gift_inbox[i], _depth + 1);
                }
            }
            if (object.max_inbox_id != null)
                if (Number(object.max_inbox_id) !== 0)
                    message.max_inbox_id = object.max_inbox_id >>> 0;
            return message;
        };

        ActivityFeedData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gift_inbox = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.feed_count = 0;
                object.friend_receive_data = null;
                object.friend_send_data = null;
                object.max_inbox_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                object.feed_count = message.feed_count;
            if (message.friend_receive_data != null && message.hasOwnProperty("friend_receive_data"))
                object.friend_receive_data = $root.lq.ActivityFeedData.CountWithTimeData.toObject(message.friend_receive_data, options);
            if (message.friend_send_data != null && message.hasOwnProperty("friend_send_data"))
                object.friend_send_data = $root.lq.ActivityFeedData.CountWithTimeData.toObject(message.friend_send_data, options);
            if (message.gift_inbox && message.gift_inbox.length) {
                object.gift_inbox = Array(message.gift_inbox.length);
                for (let j = 0; j < message.gift_inbox.length; ++j)
                    object.gift_inbox[j] = $root.lq.ActivityFeedData.GiftBoxData.toObject(message.gift_inbox[j], options);
            }
            if (message.max_inbox_id != null && message.hasOwnProperty("max_inbox_id"))
                object.max_inbox_id = message.max_inbox_id;
            return object;
        };

        ActivityFeedData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityFeedData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityFeedData";
        };

        ActivityFeedData.CountWithTimeData = (function() {

            function CountWithTimeData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CountWithTimeData.prototype.count = 0;
            CountWithTimeData.prototype.last_update_time = 0;

            CountWithTimeData.create = function create(properties) {
                return new CountWithTimeData(properties);
            };

            CountWithTimeData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.last_update_time != null && Object.hasOwnProperty.call(message, "last_update_time"))
                    writer.uint32(16).uint32(message.last_update_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CountWithTimeData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CountWithTimeData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFeedData.CountWithTimeData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_update_time = value;
                            else
                                delete message.last_update_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CountWithTimeData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CountWithTimeData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    if (!$util.isInteger(message.last_update_time))
                        return "last_update_time: integer expected";
                return null;
            };

            CountWithTimeData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityFeedData.CountWithTimeData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityFeedData.CountWithTimeData();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.last_update_time != null)
                    if (Number(object.last_update_time) !== 0)
                        message.last_update_time = object.last_update_time >>> 0;
                return message;
            };

            CountWithTimeData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.count = 0;
                    object.last_update_time = 0;
                }
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    object.last_update_time = message.last_update_time;
                return object;
            };

            CountWithTimeData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CountWithTimeData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityFeedData.CountWithTimeData";
            };

            return CountWithTimeData;
        })();

        ActivityFeedData.GiftBoxData = (function() {

            function GiftBoxData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GiftBoxData.prototype.id = 0;
            GiftBoxData.prototype.item_id = 0;
            GiftBoxData.prototype.count = 0;
            GiftBoxData.prototype.from_account_id = 0;
            GiftBoxData.prototype.time = 0;
            GiftBoxData.prototype.received = 0;

            GiftBoxData.create = function create(properties) {
                return new GiftBoxData(properties);
            };

            GiftBoxData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(16).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(24).uint32(message.count);
                if (message.from_account_id != null && Object.hasOwnProperty.call(message, "from_account_id"))
                    writer.uint32(32).uint32(message.from_account_id);
                if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                    writer.uint32(40).uint32(message.time);
                if (message.received != null && Object.hasOwnProperty.call(message, "received"))
                    writer.uint32(48).uint32(message.received);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GiftBoxData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GiftBoxData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFeedData.GiftBoxData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from_account_id = value;
                            else
                                delete message.from_account_id;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.received = value;
                            else
                                delete message.received;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GiftBoxData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GiftBoxData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    if (!$util.isInteger(message.from_account_id))
                        return "from_account_id: integer expected";
                if (message.time != null && message.hasOwnProperty("time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                if (message.received != null && message.hasOwnProperty("received"))
                    if (!$util.isInteger(message.received))
                        return "received: integer expected";
                return null;
            };

            GiftBoxData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityFeedData.GiftBoxData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityFeedData.GiftBoxData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.from_account_id != null)
                    if (Number(object.from_account_id) !== 0)
                        message.from_account_id = object.from_account_id >>> 0;
                if (object.time != null)
                    if (Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                if (object.received != null)
                    if (Number(object.received) !== 0)
                        message.received = object.received >>> 0;
                return message;
            };

            GiftBoxData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.item_id = 0;
                    object.count = 0;
                    object.from_account_id = 0;
                    object.time = 0;
                    object.received = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    object.from_account_id = message.from_account_id;
                if (message.time != null && message.hasOwnProperty("time"))
                    object.time = message.time;
                if (message.received != null && message.hasOwnProperty("received"))
                    object.received = message.received;
                return object;
            };

            GiftBoxData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GiftBoxData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityFeedData.GiftBoxData";
            };

            return GiftBoxData;
        })();

        return ActivityFeedData;
    })();

    lq.UnlockedStoryData = (function() {

        function UnlockedStoryData(properties) {
            this.finished_ending = [];
            this.rewarded_ending = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        UnlockedStoryData.prototype.story_id = 0;
        UnlockedStoryData.prototype.finished_ending = $util.emptyArray;
        UnlockedStoryData.prototype.rewarded_ending = $util.emptyArray;
        UnlockedStoryData.prototype.finish_rewarded = 0;
        UnlockedStoryData.prototype.all_finish_rewarded = 0;

        UnlockedStoryData.create = function create(properties) {
            return new UnlockedStoryData(properties);
        };

        UnlockedStoryData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(8).uint32(message.story_id);
            if (message.finished_ending != null && message.finished_ending.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.finished_ending.length; ++i)
                    writer.uint32(message.finished_ending[i]);
                writer.ldelim();
            }
            if (message.rewarded_ending != null && message.rewarded_ending.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.rewarded_ending.length; ++i)
                    writer.uint32(message.rewarded_ending[i]);
                writer.ldelim();
            }
            if (message.finish_rewarded != null && Object.hasOwnProperty.call(message, "finish_rewarded"))
                writer.uint32(32).uint32(message.finish_rewarded);
            if (message.all_finish_rewarded != null && Object.hasOwnProperty.call(message, "all_finish_rewarded"))
                writer.uint32(40).uint32(message.all_finish_rewarded);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        UnlockedStoryData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        UnlockedStoryData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.UnlockedStoryData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.finished_ending && message.finished_ending.length))
                                message.finished_ending = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finished_ending.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.finished_ending && message.finished_ending.length))
                            message.finished_ending = [];
                        message.finished_ending.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.rewarded_ending && message.rewarded_ending.length))
                                message.rewarded_ending = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_ending.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_ending && message.rewarded_ending.length))
                            message.rewarded_ending = [];
                        message.rewarded_ending.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_rewarded = value;
                        else
                            delete message.finish_rewarded;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.all_finish_rewarded = value;
                        else
                            delete message.all_finish_rewarded;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        UnlockedStoryData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        UnlockedStoryData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            if (message.finished_ending != null && message.hasOwnProperty("finished_ending")) {
                if (!Array.isArray(message.finished_ending))
                    return "finished_ending: array expected";
                for (let i = 0; i < message.finished_ending.length; ++i)
                    if (!$util.isInteger(message.finished_ending[i]))
                        return "finished_ending: integer[] expected";
            }
            if (message.rewarded_ending != null && message.hasOwnProperty("rewarded_ending")) {
                if (!Array.isArray(message.rewarded_ending))
                    return "rewarded_ending: array expected";
                for (let i = 0; i < message.rewarded_ending.length; ++i)
                    if (!$util.isInteger(message.rewarded_ending[i]))
                        return "rewarded_ending: integer[] expected";
            }
            if (message.finish_rewarded != null && message.hasOwnProperty("finish_rewarded"))
                if (!$util.isInteger(message.finish_rewarded))
                    return "finish_rewarded: integer expected";
            if (message.all_finish_rewarded != null && message.hasOwnProperty("all_finish_rewarded"))
                if (!$util.isInteger(message.all_finish_rewarded))
                    return "all_finish_rewarded: integer expected";
            return null;
        };

        UnlockedStoryData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.UnlockedStoryData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.UnlockedStoryData();
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            if (object.finished_ending) {
                if (!Array.isArray(object.finished_ending))
                    throw TypeError(".lq.UnlockedStoryData.finished_ending: array expected");
                message.finished_ending = Array(object.finished_ending.length);
                for (let i = 0; i < object.finished_ending.length; ++i)
                    message.finished_ending[i] = object.finished_ending[i] >>> 0;
            }
            if (object.rewarded_ending) {
                if (!Array.isArray(object.rewarded_ending))
                    throw TypeError(".lq.UnlockedStoryData.rewarded_ending: array expected");
                message.rewarded_ending = Array(object.rewarded_ending.length);
                for (let i = 0; i < object.rewarded_ending.length; ++i)
                    message.rewarded_ending[i] = object.rewarded_ending[i] >>> 0;
            }
            if (object.finish_rewarded != null)
                if (Number(object.finish_rewarded) !== 0)
                    message.finish_rewarded = object.finish_rewarded >>> 0;
            if (object.all_finish_rewarded != null)
                if (Number(object.all_finish_rewarded) !== 0)
                    message.all_finish_rewarded = object.all_finish_rewarded >>> 0;
            return message;
        };

        UnlockedStoryData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.finished_ending = [];
                object.rewarded_ending = [];
            }
            if (options.defaults) {
                object.story_id = 0;
                object.finish_rewarded = 0;
                object.all_finish_rewarded = 0;
            }
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            if (message.finished_ending && message.finished_ending.length) {
                object.finished_ending = Array(message.finished_ending.length);
                for (let j = 0; j < message.finished_ending.length; ++j)
                    object.finished_ending[j] = message.finished_ending[j];
            }
            if (message.rewarded_ending && message.rewarded_ending.length) {
                object.rewarded_ending = Array(message.rewarded_ending.length);
                for (let j = 0; j < message.rewarded_ending.length; ++j)
                    object.rewarded_ending[j] = message.rewarded_ending[j];
            }
            if (message.finish_rewarded != null && message.hasOwnProperty("finish_rewarded"))
                object.finish_rewarded = message.finish_rewarded;
            if (message.all_finish_rewarded != null && message.hasOwnProperty("all_finish_rewarded"))
                object.all_finish_rewarded = message.all_finish_rewarded;
            return object;
        };

        UnlockedStoryData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        UnlockedStoryData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.UnlockedStoryData";
        };

        return UnlockedStoryData;
    })();

    lq.ActivityStoryData = (function() {

        function ActivityStoryData(properties) {
            this.unlocked_story = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityStoryData.prototype.activity_id = 0;
        ActivityStoryData.prototype.unlocked_story = $util.emptyArray;

        ActivityStoryData.create = function create(properties) {
            return new ActivityStoryData(properties);
        };

        ActivityStoryData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.unlocked_story != null && message.unlocked_story.length)
                for (let i = 0; i < message.unlocked_story.length; ++i)
                    $root.lq.UnlockedStoryData.encode(message.unlocked_story[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityStoryData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityStoryData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityStoryData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.unlocked_story && message.unlocked_story.length))
                            message.unlocked_story = [];
                        message.unlocked_story.push($root.lq.UnlockedStoryData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityStoryData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityStoryData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.unlocked_story != null && message.hasOwnProperty("unlocked_story")) {
                if (!Array.isArray(message.unlocked_story))
                    return "unlocked_story: array expected";
                for (let i = 0; i < message.unlocked_story.length; ++i) {
                    let error = $root.lq.UnlockedStoryData.verify(message.unlocked_story[i], _depth + 1);
                    if (error)
                        return "unlocked_story." + error;
                }
            }
            return null;
        };

        ActivityStoryData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityStoryData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityStoryData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.unlocked_story) {
                if (!Array.isArray(object.unlocked_story))
                    throw TypeError(".lq.ActivityStoryData.unlocked_story: array expected");
                message.unlocked_story = Array(object.unlocked_story.length);
                for (let i = 0; i < object.unlocked_story.length; ++i) {
                    if (typeof object.unlocked_story[i] !== "object")
                        throw TypeError(".lq.ActivityStoryData.unlocked_story: object expected");
                    message.unlocked_story[i] = $root.lq.UnlockedStoryData.fromObject(object.unlocked_story[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityStoryData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.unlocked_story = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.unlocked_story && message.unlocked_story.length) {
                object.unlocked_story = Array(message.unlocked_story.length);
                for (let j = 0; j < message.unlocked_story.length; ++j)
                    object.unlocked_story[j] = $root.lq.UnlockedStoryData.toObject(message.unlocked_story[j], options);
            }
            return object;
        };

        ActivityStoryData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityStoryData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityStoryData";
        };

        return ActivityStoryData;
    })();

    lq.ActivityProgressRewardData = (function() {

        function ActivityProgressRewardData(properties) {
            this.rewarded_progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityProgressRewardData.prototype.activity_id = 0;
        ActivityProgressRewardData.prototype.rewarded_progresses = $util.emptyArray;

        ActivityProgressRewardData.create = function create(properties) {
            return new ActivityProgressRewardData(properties);
        };

        ActivityProgressRewardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.rewarded_progresses != null && message.rewarded_progresses.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.rewarded_progresses.length; ++i)
                    writer.uint32(message.rewarded_progresses[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityProgressRewardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityProgressRewardData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityProgressRewardData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.rewarded_progresses && message.rewarded_progresses.length))
                                message.rewarded_progresses = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_progresses.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_progresses && message.rewarded_progresses.length))
                            message.rewarded_progresses = [];
                        message.rewarded_progresses.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityProgressRewardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityProgressRewardData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.rewarded_progresses != null && message.hasOwnProperty("rewarded_progresses")) {
                if (!Array.isArray(message.rewarded_progresses))
                    return "rewarded_progresses: array expected";
                for (let i = 0; i < message.rewarded_progresses.length; ++i)
                    if (!$util.isInteger(message.rewarded_progresses[i]))
                        return "rewarded_progresses: integer[] expected";
            }
            return null;
        };

        ActivityProgressRewardData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityProgressRewardData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityProgressRewardData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.rewarded_progresses) {
                if (!Array.isArray(object.rewarded_progresses))
                    throw TypeError(".lq.ActivityProgressRewardData.rewarded_progresses: array expected");
                message.rewarded_progresses = Array(object.rewarded_progresses.length);
                for (let i = 0; i < object.rewarded_progresses.length; ++i)
                    message.rewarded_progresses[i] = object.rewarded_progresses[i] >>> 0;
            }
            return message;
        };

        ActivityProgressRewardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewarded_progresses = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.rewarded_progresses && message.rewarded_progresses.length) {
                object.rewarded_progresses = Array(message.rewarded_progresses.length);
                for (let j = 0; j < message.rewarded_progresses.length; ++j)
                    object.rewarded_progresses[j] = message.rewarded_progresses[j];
            }
            return object;
        };

        ActivityProgressRewardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityProgressRewardData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityProgressRewardData";
        };

        return ActivityProgressRewardData;
    })();

    lq.QCMember = (function() {

        function QCMember(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QCMember.prototype.member_id = 0;
        QCMember.prototype.consumed_sta = null;

        QCMember.create = function create(properties) {
            return new QCMember(properties);
        };

        QCMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.member_id != null && Object.hasOwnProperty.call(message, "member_id"))
                writer.uint32(8).uint32(message.member_id);
            if (message.consumed_sta != null && Object.hasOwnProperty.call(message, "consumed_sta"))
                $root.lq.TimeCounterData.encode(message.consumed_sta, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QCMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QCMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QCMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.member_id = value;
                        else
                            delete message.member_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.consumed_sta = $root.lq.TimeCounterData.decode(reader, reader.uint32(), undefined, _depth + 1, message.consumed_sta);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QCMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QCMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.member_id != null && message.hasOwnProperty("member_id"))
                if (!$util.isInteger(message.member_id))
                    return "member_id: integer expected";
            if (message.consumed_sta != null && message.hasOwnProperty("consumed_sta")) {
                let error = $root.lq.TimeCounterData.verify(message.consumed_sta, _depth + 1);
                if (error)
                    return "consumed_sta." + error;
            }
            return null;
        };

        QCMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QCMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QCMember();
            if (object.member_id != null)
                if (Number(object.member_id) !== 0)
                    message.member_id = object.member_id >>> 0;
            if (object.consumed_sta != null) {
                if (typeof object.consumed_sta !== "object")
                    throw TypeError(".lq.QCMember.consumed_sta: object expected");
                message.consumed_sta = $root.lq.TimeCounterData.fromObject(object.consumed_sta, _depth + 1);
            }
            return message;
        };

        QCMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.member_id = 0;
                object.consumed_sta = null;
            }
            if (message.member_id != null && message.hasOwnProperty("member_id"))
                object.member_id = message.member_id;
            if (message.consumed_sta != null && message.hasOwnProperty("consumed_sta"))
                object.consumed_sta = $root.lq.TimeCounterData.toObject(message.consumed_sta, options);
            return object;
        };

        QCMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QCMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QCMember";
        };

        return QCMember;
    })();

    lq.ActivityQuestCrewEffectResult = (function() {

        function ActivityQuestCrewEffectResult(properties) {
            this.consumed_change = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityQuestCrewEffectResult.prototype.result_change = null;
        ActivityQuestCrewEffectResult.prototype.consumed_change = $util.emptyArray;
        ActivityQuestCrewEffectResult.prototype.reward = null;

        ActivityQuestCrewEffectResult.create = function create(properties) {
            return new ActivityQuestCrewEffectResult(properties);
        };

        ActivityQuestCrewEffectResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result_change != null && Object.hasOwnProperty.call(message, "result_change"))
                $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange.encode(message.result_change, writer.uint32(10).fork()).ldelim();
            if (message.consumed_change != null && message.consumed_change.length)
                for (let i = 0; i < message.consumed_change.length; ++i)
                    $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange.encode(message.consumed_change[i], writer.uint32(18).fork()).ldelim();
            if (message.reward != null && Object.hasOwnProperty.call(message, "reward"))
                $root.lq.ActivityQuestCrewEffectResult.QCItemReward.encode(message.reward, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityQuestCrewEffectResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityQuestCrewEffectResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewEffectResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.result_change = $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange.decode(reader, reader.uint32(), undefined, _depth + 1, message.result_change);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.consumed_change && message.consumed_change.length))
                            message.consumed_change = [];
                        message.consumed_change.push($root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.reward = $root.lq.ActivityQuestCrewEffectResult.QCItemReward.decode(reader, reader.uint32(), undefined, _depth + 1, message.reward);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityQuestCrewEffectResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityQuestCrewEffectResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.result_change != null && message.hasOwnProperty("result_change")) {
                let error = $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange.verify(message.result_change, _depth + 1);
                if (error)
                    return "result_change." + error;
            }
            if (message.consumed_change != null && message.hasOwnProperty("consumed_change")) {
                if (!Array.isArray(message.consumed_change))
                    return "consumed_change: array expected";
                for (let i = 0; i < message.consumed_change.length; ++i) {
                    let error = $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange.verify(message.consumed_change[i], _depth + 1);
                    if (error)
                        return "consumed_change." + error;
                }
            }
            if (message.reward != null && message.hasOwnProperty("reward")) {
                let error = $root.lq.ActivityQuestCrewEffectResult.QCItemReward.verify(message.reward, _depth + 1);
                if (error)
                    return "reward." + error;
            }
            return null;
        };

        ActivityQuestCrewEffectResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityQuestCrewEffectResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityQuestCrewEffectResult();
            if (object.result_change != null) {
                if (typeof object.result_change !== "object")
                    throw TypeError(".lq.ActivityQuestCrewEffectResult.result_change: object expected");
                message.result_change = $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange.fromObject(object.result_change, _depth + 1);
            }
            if (object.consumed_change) {
                if (!Array.isArray(object.consumed_change))
                    throw TypeError(".lq.ActivityQuestCrewEffectResult.consumed_change: array expected");
                message.consumed_change = Array(object.consumed_change.length);
                for (let i = 0; i < object.consumed_change.length; ++i) {
                    if (typeof object.consumed_change[i] !== "object")
                        throw TypeError(".lq.ActivityQuestCrewEffectResult.consumed_change: object expected");
                    message.consumed_change[i] = $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange.fromObject(object.consumed_change[i], _depth + 1);
                }
            }
            if (object.reward != null) {
                if (typeof object.reward !== "object")
                    throw TypeError(".lq.ActivityQuestCrewEffectResult.reward: object expected");
                message.reward = $root.lq.ActivityQuestCrewEffectResult.QCItemReward.fromObject(object.reward, _depth + 1);
            }
            return message;
        };

        ActivityQuestCrewEffectResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.consumed_change = [];
            if (options.defaults) {
                object.result_change = null;
                object.reward = null;
            }
            if (message.result_change != null && message.hasOwnProperty("result_change"))
                object.result_change = $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange.toObject(message.result_change, options);
            if (message.consumed_change && message.consumed_change.length) {
                object.consumed_change = Array(message.consumed_change.length);
                for (let j = 0; j < message.consumed_change.length; ++j)
                    object.consumed_change[j] = $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange.toObject(message.consumed_change[j], options);
            }
            if (message.reward != null && message.hasOwnProperty("reward"))
                object.reward = $root.lq.ActivityQuestCrewEffectResult.QCItemReward.toObject(message.reward, options);
            return object;
        };

        ActivityQuestCrewEffectResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityQuestCrewEffectResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityQuestCrewEffectResult";
        };

        ActivityQuestCrewEffectResult.QCQuestResultChange = (function() {

            function QCQuestResultChange(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QCQuestResultChange.prototype.from = 0;
            QCQuestResultChange.prototype.to = 0;

            QCQuestResultChange.create = function create(properties) {
                return new QCQuestResultChange(properties);
            };

            QCQuestResultChange.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                    writer.uint32(8).uint32(message.from);
                if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                    writer.uint32(16).uint32(message.to);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QCQuestResultChange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QCQuestResultChange.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from = value;
                            else
                                delete message.from;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.to = value;
                            else
                                delete message.to;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QCQuestResultChange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QCQuestResultChange.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.from != null && message.hasOwnProperty("from"))
                    if (!$util.isInteger(message.from))
                        return "from: integer expected";
                if (message.to != null && message.hasOwnProperty("to"))
                    if (!$util.isInteger(message.to))
                        return "to: integer expected";
                return null;
            };

            QCQuestResultChange.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityQuestCrewEffectResult.QCQuestResultChange();
                if (object.from != null)
                    if (Number(object.from) !== 0)
                        message.from = object.from >>> 0;
                if (object.to != null)
                    if (Number(object.to) !== 0)
                        message.to = object.to >>> 0;
                return message;
            };

            QCQuestResultChange.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.from = 0;
                    object.to = 0;
                }
                if (message.from != null && message.hasOwnProperty("from"))
                    object.from = message.from;
                if (message.to != null && message.hasOwnProperty("to"))
                    object.to = message.to;
                return object;
            };

            QCQuestResultChange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QCQuestResultChange.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityQuestCrewEffectResult.QCQuestResultChange";
            };

            return QCQuestResultChange;
        })();

        ActivityQuestCrewEffectResult.QCQuestConsumeChange = (function() {

            function QCQuestConsumeChange(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QCQuestConsumeChange.prototype.member_id = 0;
            QCQuestConsumeChange.prototype.from = 0;
            QCQuestConsumeChange.prototype.to = 0;

            QCQuestConsumeChange.create = function create(properties) {
                return new QCQuestConsumeChange(properties);
            };

            QCQuestConsumeChange.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.member_id != null && Object.hasOwnProperty.call(message, "member_id"))
                    writer.uint32(8).uint32(message.member_id);
                if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                    writer.uint32(16).uint32(message.from);
                if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                    writer.uint32(24).uint32(message.to);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QCQuestConsumeChange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QCQuestConsumeChange.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.member_id = value;
                            else
                                delete message.member_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from = value;
                            else
                                delete message.from;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.to = value;
                            else
                                delete message.to;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QCQuestConsumeChange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QCQuestConsumeChange.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.member_id != null && message.hasOwnProperty("member_id"))
                    if (!$util.isInteger(message.member_id))
                        return "member_id: integer expected";
                if (message.from != null && message.hasOwnProperty("from"))
                    if (!$util.isInteger(message.from))
                        return "from: integer expected";
                if (message.to != null && message.hasOwnProperty("to"))
                    if (!$util.isInteger(message.to))
                        return "to: integer expected";
                return null;
            };

            QCQuestConsumeChange.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange();
                if (object.member_id != null)
                    if (Number(object.member_id) !== 0)
                        message.member_id = object.member_id >>> 0;
                if (object.from != null)
                    if (Number(object.from) !== 0)
                        message.from = object.from >>> 0;
                if (object.to != null)
                    if (Number(object.to) !== 0)
                        message.to = object.to >>> 0;
                return message;
            };

            QCQuestConsumeChange.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.member_id = 0;
                    object.from = 0;
                    object.to = 0;
                }
                if (message.member_id != null && message.hasOwnProperty("member_id"))
                    object.member_id = message.member_id;
                if (message.from != null && message.hasOwnProperty("from"))
                    object.from = message.from;
                if (message.to != null && message.hasOwnProperty("to"))
                    object.to = message.to;
                return object;
            };

            QCQuestConsumeChange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QCQuestConsumeChange.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityQuestCrewEffectResult.QCQuestConsumeChange";
            };

            return QCQuestConsumeChange;
        })();

        ActivityQuestCrewEffectResult.QCItemReward = (function() {

            function QCItemReward(properties) {
                this.execute_reward = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QCItemReward.prototype.execute_reward = $util.emptyArray;

            QCItemReward.create = function create(properties) {
                return new QCItemReward(properties);
            };

            QCItemReward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.execute_reward != null && message.execute_reward.length)
                    for (let i = 0; i < message.execute_reward.length; ++i)
                        $root.lq.ExecuteReward.encode(message.execute_reward[i], writer.uint32(10).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QCItemReward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QCItemReward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewEffectResult.QCItemReward();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.execute_reward && message.execute_reward.length))
                                message.execute_reward = [];
                            message.execute_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QCItemReward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QCItemReward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.execute_reward != null && message.hasOwnProperty("execute_reward")) {
                    if (!Array.isArray(message.execute_reward))
                        return "execute_reward: array expected";
                    for (let i = 0; i < message.execute_reward.length; ++i) {
                        let error = $root.lq.ExecuteReward.verify(message.execute_reward[i], _depth + 1);
                        if (error)
                            return "execute_reward." + error;
                    }
                }
                return null;
            };

            QCItemReward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityQuestCrewEffectResult.QCItemReward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityQuestCrewEffectResult.QCItemReward();
                if (object.execute_reward) {
                    if (!Array.isArray(object.execute_reward))
                        throw TypeError(".lq.ActivityQuestCrewEffectResult.QCItemReward.execute_reward: array expected");
                    message.execute_reward = Array(object.execute_reward.length);
                    for (let i = 0; i < object.execute_reward.length; ++i) {
                        if (typeof object.execute_reward[i] !== "object")
                            throw TypeError(".lq.ActivityQuestCrewEffectResult.QCItemReward.execute_reward: object expected");
                        message.execute_reward[i] = $root.lq.ExecuteReward.fromObject(object.execute_reward[i], _depth + 1);
                    }
                }
                return message;
            };

            QCItemReward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.execute_reward = [];
                if (message.execute_reward && message.execute_reward.length) {
                    object.execute_reward = Array(message.execute_reward.length);
                    for (let j = 0; j < message.execute_reward.length; ++j)
                        object.execute_reward[j] = $root.lq.ExecuteReward.toObject(message.execute_reward[j], options);
                }
                return object;
            };

            QCItemReward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QCItemReward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityQuestCrewEffectResult.QCItemReward";
            };

            return QCItemReward;
        })();

        return ActivityQuestCrewEffectResult;
    })();

    lq.QCQuest = (function() {

        function QCQuest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QCQuest.prototype.quest_id = 0;
        QCQuest.prototype.finished = 0;
        QCQuest.prototype.finished_time = 0;

        QCQuest.create = function create(properties) {
            return new QCQuest(properties);
        };

        QCQuest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.quest_id != null && Object.hasOwnProperty.call(message, "quest_id"))
                writer.uint32(8).uint32(message.quest_id);
            if (message.finished != null && Object.hasOwnProperty.call(message, "finished"))
                writer.uint32(16).uint32(message.finished);
            if (message.finished_time != null && Object.hasOwnProperty.call(message, "finished_time"))
                writer.uint32(24).uint32(message.finished_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QCQuest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QCQuest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QCQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.quest_id = value;
                        else
                            delete message.quest_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finished = value;
                        else
                            delete message.finished;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finished_time = value;
                        else
                            delete message.finished_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QCQuest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QCQuest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.quest_id != null && message.hasOwnProperty("quest_id"))
                if (!$util.isInteger(message.quest_id))
                    return "quest_id: integer expected";
            if (message.finished != null && message.hasOwnProperty("finished"))
                if (!$util.isInteger(message.finished))
                    return "finished: integer expected";
            if (message.finished_time != null && message.hasOwnProperty("finished_time"))
                if (!$util.isInteger(message.finished_time))
                    return "finished_time: integer expected";
            return null;
        };

        QCQuest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QCQuest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QCQuest();
            if (object.quest_id != null)
                if (Number(object.quest_id) !== 0)
                    message.quest_id = object.quest_id >>> 0;
            if (object.finished != null)
                if (Number(object.finished) !== 0)
                    message.finished = object.finished >>> 0;
            if (object.finished_time != null)
                if (Number(object.finished_time) !== 0)
                    message.finished_time = object.finished_time >>> 0;
            return message;
        };

        QCQuest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.quest_id = 0;
                object.finished = 0;
                object.finished_time = 0;
            }
            if (message.quest_id != null && message.hasOwnProperty("quest_id"))
                object.quest_id = message.quest_id;
            if (message.finished != null && message.hasOwnProperty("finished"))
                object.finished = message.finished;
            if (message.finished_time != null && message.hasOwnProperty("finished_time"))
                object.finished_time = message.finished_time;
            return object;
        };

        QCQuest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QCQuest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QCQuest";
        };

        return QCQuest;
    })();

    lq.ActivityQuestCrewData = (function() {

        function ActivityQuestCrewData(properties) {
            this.members = [];
            this.quest_board = [];
            this.market_board = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityQuestCrewData.prototype.activity_id = 0;
        ActivityQuestCrewData.prototype.members = $util.emptyArray;
        ActivityQuestCrewData.prototype.quest_board = $util.emptyArray;
        ActivityQuestCrewData.prototype.market_board = $util.emptyArray;

        ActivityQuestCrewData.create = function create(properties) {
            return new ActivityQuestCrewData(properties);
        };

        ActivityQuestCrewData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.members != null && message.members.length)
                for (let i = 0; i < message.members.length; ++i)
                    $root.lq.QCMember.encode(message.members[i], writer.uint32(18).fork()).ldelim();
            if (message.quest_board != null && message.quest_board.length)
                for (let i = 0; i < message.quest_board.length; ++i)
                    $root.lq.QCQuest.encode(message.quest_board[i], writer.uint32(26).fork()).ldelim();
            if (message.market_board != null && message.market_board.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.market_board.length; ++i)
                    writer.uint32(message.market_board[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityQuestCrewData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityQuestCrewData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.members && message.members.length))
                            message.members = [];
                        message.members.push($root.lq.QCMember.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.quest_board && message.quest_board.length))
                            message.quest_board = [];
                        message.quest_board.push($root.lq.QCQuest.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.market_board && message.market_board.length))
                                message.market_board = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.market_board.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.market_board && message.market_board.length))
                            message.market_board = [];
                        message.market_board.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityQuestCrewData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityQuestCrewData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.members != null && message.hasOwnProperty("members")) {
                if (!Array.isArray(message.members))
                    return "members: array expected";
                for (let i = 0; i < message.members.length; ++i) {
                    let error = $root.lq.QCMember.verify(message.members[i], _depth + 1);
                    if (error)
                        return "members." + error;
                }
            }
            if (message.quest_board != null && message.hasOwnProperty("quest_board")) {
                if (!Array.isArray(message.quest_board))
                    return "quest_board: array expected";
                for (let i = 0; i < message.quest_board.length; ++i) {
                    let error = $root.lq.QCQuest.verify(message.quest_board[i], _depth + 1);
                    if (error)
                        return "quest_board." + error;
                }
            }
            if (message.market_board != null && message.hasOwnProperty("market_board")) {
                if (!Array.isArray(message.market_board))
                    return "market_board: array expected";
                for (let i = 0; i < message.market_board.length; ++i)
                    if (!$util.isInteger(message.market_board[i]))
                        return "market_board: integer[] expected";
            }
            return null;
        };

        ActivityQuestCrewData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityQuestCrewData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityQuestCrewData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.members) {
                if (!Array.isArray(object.members))
                    throw TypeError(".lq.ActivityQuestCrewData.members: array expected");
                message.members = Array(object.members.length);
                for (let i = 0; i < object.members.length; ++i) {
                    if (typeof object.members[i] !== "object")
                        throw TypeError(".lq.ActivityQuestCrewData.members: object expected");
                    message.members[i] = $root.lq.QCMember.fromObject(object.members[i], _depth + 1);
                }
            }
            if (object.quest_board) {
                if (!Array.isArray(object.quest_board))
                    throw TypeError(".lq.ActivityQuestCrewData.quest_board: array expected");
                message.quest_board = Array(object.quest_board.length);
                for (let i = 0; i < object.quest_board.length; ++i) {
                    if (typeof object.quest_board[i] !== "object")
                        throw TypeError(".lq.ActivityQuestCrewData.quest_board: object expected");
                    message.quest_board[i] = $root.lq.QCQuest.fromObject(object.quest_board[i], _depth + 1);
                }
            }
            if (object.market_board) {
                if (!Array.isArray(object.market_board))
                    throw TypeError(".lq.ActivityQuestCrewData.market_board: array expected");
                message.market_board = Array(object.market_board.length);
                for (let i = 0; i < object.market_board.length; ++i)
                    message.market_board[i] = object.market_board[i] >>> 0;
            }
            return message;
        };

        ActivityQuestCrewData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.members = [];
                object.quest_board = [];
                object.market_board = [];
            }
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.members && message.members.length) {
                object.members = Array(message.members.length);
                for (let j = 0; j < message.members.length; ++j)
                    object.members[j] = $root.lq.QCMember.toObject(message.members[j], options);
            }
            if (message.quest_board && message.quest_board.length) {
                object.quest_board = Array(message.quest_board.length);
                for (let j = 0; j < message.quest_board.length; ++j)
                    object.quest_board[j] = $root.lq.QCQuest.toObject(message.quest_board[j], options);
            }
            if (message.market_board && message.market_board.length) {
                object.market_board = Array(message.market_board.length);
                for (let j = 0; j < message.market_board.length; ++j)
                    object.market_board[j] = message.market_board[j];
            }
            return object;
        };

        ActivityQuestCrewData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityQuestCrewData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityQuestCrewData";
        };

        return ActivityQuestCrewData;
    })();

    lq.ActivityQuestCrewChanges = (function() {

        function ActivityQuestCrewChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityQuestCrewChanges.prototype.activity_id = 0;
        ActivityQuestCrewChanges.prototype.members = null;
        ActivityQuestCrewChanges.prototype.quest_board = null;
        ActivityQuestCrewChanges.prototype.market_board = null;

        ActivityQuestCrewChanges.create = function create(properties) {
            return new ActivityQuestCrewChanges(properties);
        };

        ActivityQuestCrewChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.members != null && Object.hasOwnProperty.call(message, "members"))
                $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty.encode(message.members, writer.uint32(18).fork()).ldelim();
            if (message.quest_board != null && Object.hasOwnProperty.call(message, "quest_board"))
                $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty.encode(message.quest_board, writer.uint32(26).fork()).ldelim();
            if (message.market_board != null && Object.hasOwnProperty.call(message, "market_board"))
                $root.lq.UInt32ArrayDirty.encode(message.market_board, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityQuestCrewChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityQuestCrewChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.members = $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.members);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.quest_board = $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.quest_board);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.market_board = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.market_board);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityQuestCrewChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityQuestCrewChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.members != null && message.hasOwnProperty("members")) {
                let error = $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty.verify(message.members, _depth + 1);
                if (error)
                    return "members." + error;
            }
            if (message.quest_board != null && message.hasOwnProperty("quest_board")) {
                let error = $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty.verify(message.quest_board, _depth + 1);
                if (error)
                    return "quest_board." + error;
            }
            if (message.market_board != null && message.hasOwnProperty("market_board")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.market_board, _depth + 1);
                if (error)
                    return "market_board." + error;
            }
            return null;
        };

        ActivityQuestCrewChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityQuestCrewChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityQuestCrewChanges();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.members != null) {
                if (typeof object.members !== "object")
                    throw TypeError(".lq.ActivityQuestCrewChanges.members: object expected");
                message.members = $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty.fromObject(object.members, _depth + 1);
            }
            if (object.quest_board != null) {
                if (typeof object.quest_board !== "object")
                    throw TypeError(".lq.ActivityQuestCrewChanges.quest_board: object expected");
                message.quest_board = $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty.fromObject(object.quest_board, _depth + 1);
            }
            if (object.market_board != null) {
                if (typeof object.market_board !== "object")
                    throw TypeError(".lq.ActivityQuestCrewChanges.market_board: object expected");
                message.market_board = $root.lq.UInt32ArrayDirty.fromObject(object.market_board, _depth + 1);
            }
            return message;
        };

        ActivityQuestCrewChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.members = null;
                object.quest_board = null;
                object.market_board = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.members != null && message.hasOwnProperty("members"))
                object.members = $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty.toObject(message.members, options);
            if (message.quest_board != null && message.hasOwnProperty("quest_board"))
                object.quest_board = $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty.toObject(message.quest_board, options);
            if (message.market_board != null && message.hasOwnProperty("market_board"))
                object.market_board = $root.lq.UInt32ArrayDirty.toObject(message.market_board, options);
            return object;
        };

        ActivityQuestCrewChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityQuestCrewChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityQuestCrewChanges";
        };

        ActivityQuestCrewChanges.QCMemberArrayDirty = (function() {

            function QCMemberArrayDirty(properties) {
                this.value = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QCMemberArrayDirty.prototype.dirty = false;
            QCMemberArrayDirty.prototype.value = $util.emptyArray;

            QCMemberArrayDirty.create = function create(properties) {
                return new QCMemberArrayDirty(properties);
            };

            QCMemberArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(8).bool(message.dirty);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.lq.QCMember.encode(message.value[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QCMemberArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QCMemberArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.lq.QCMember.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QCMemberArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QCMemberArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (typeof message.dirty !== "boolean")
                        return "dirty: boolean expected";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.lq.QCMember.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            QCMemberArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityQuestCrewChanges.QCMemberArrayDirty();
                if (object.dirty != null)
                    if (object.dirty)
                        message.dirty = Boolean(object.dirty);
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".lq.ActivityQuestCrewChanges.QCMemberArrayDirty.value: array expected");
                    message.value = Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".lq.ActivityQuestCrewChanges.QCMemberArrayDirty.value: object expected");
                        message.value[i] = $root.lq.QCMember.fromObject(object.value[i], _depth + 1);
                    }
                }
                return message;
            };

            QCMemberArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults)
                    object.dirty = false;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                if (message.value && message.value.length) {
                    object.value = Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.lq.QCMember.toObject(message.value[j], options);
                }
                return object;
            };

            QCMemberArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QCMemberArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityQuestCrewChanges.QCMemberArrayDirty";
            };

            return QCMemberArrayDirty;
        })();

        ActivityQuestCrewChanges.QCQuestArrayDirty = (function() {

            function QCQuestArrayDirty(properties) {
                this.value = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QCQuestArrayDirty.prototype.dirty = false;
            QCQuestArrayDirty.prototype.value = $util.emptyArray;

            QCQuestArrayDirty.create = function create(properties) {
                return new QCQuestArrayDirty(properties);
            };

            QCQuestArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(8).bool(message.dirty);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.lq.QCQuest.encode(message.value[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QCQuestArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QCQuestArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.lq.QCQuest.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QCQuestArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QCQuestArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (typeof message.dirty !== "boolean")
                        return "dirty: boolean expected";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.lq.QCQuest.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            QCQuestArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityQuestCrewChanges.QCQuestArrayDirty();
                if (object.dirty != null)
                    if (object.dirty)
                        message.dirty = Boolean(object.dirty);
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".lq.ActivityQuestCrewChanges.QCQuestArrayDirty.value: array expected");
                    message.value = Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".lq.ActivityQuestCrewChanges.QCQuestArrayDirty.value: object expected");
                        message.value[i] = $root.lq.QCQuest.fromObject(object.value[i], _depth + 1);
                    }
                }
                return message;
            };

            QCQuestArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults)
                    object.dirty = false;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                if (message.value && message.value.length) {
                    object.value = Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.lq.QCQuest.toObject(message.value[j], options);
                }
                return object;
            };

            QCQuestArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QCQuestArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityQuestCrewChanges.QCQuestArrayDirty";
            };

            return QCQuestArrayDirty;
        })();

        return ActivityQuestCrewChanges;
    })();

    lq.ActivityChooseUpData = (function() {

        function ActivityChooseUpData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityChooseUpData.prototype.activity_id = 0;
        ActivityChooseUpData.prototype.chest_id = 0;
        ActivityChooseUpData.prototype.selection = 0;
        ActivityChooseUpData.prototype.is_end = 0;

        ActivityChooseUpData.create = function create(properties) {
            return new ActivityChooseUpData(properties);
        };

        ActivityChooseUpData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(16).uint32(message.chest_id);
            if (message.selection != null && Object.hasOwnProperty.call(message, "selection"))
                writer.uint32(24).uint32(message.selection);
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(32).uint32(message.is_end);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityChooseUpData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityChooseUpData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityChooseUpData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selection = value;
                        else
                            delete message.selection;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityChooseUpData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityChooseUpData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.selection != null && message.hasOwnProperty("selection"))
                if (!$util.isInteger(message.selection))
                    return "selection: integer expected";
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (!$util.isInteger(message.is_end))
                    return "is_end: integer expected";
            return null;
        };

        ActivityChooseUpData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityChooseUpData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityChooseUpData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.selection != null)
                if (Number(object.selection) !== 0)
                    message.selection = object.selection >>> 0;
            if (object.is_end != null)
                if (Number(object.is_end) !== 0)
                    message.is_end = object.is_end >>> 0;
            return message;
        };

        ActivityChooseUpData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.chest_id = 0;
                object.selection = 0;
                object.is_end = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.selection != null && message.hasOwnProperty("selection"))
                object.selection = message.selection;
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            return object;
        };

        ActivityChooseUpData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityChooseUpData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityChooseUpData";
        };

        return ActivityChooseUpData;
    })();

    lq.ActivityChooseGroupData = (function() {

        function ActivityChooseGroupData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityChooseGroupData.prototype.activity_id = 0;
        ActivityChooseGroupData.prototype.chest_id = 0;
        ActivityChooseGroupData.prototype.selection_id = 0;

        ActivityChooseGroupData.create = function create(properties) {
            return new ActivityChooseGroupData(properties);
        };

        ActivityChooseGroupData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(16).uint32(message.chest_id);
            if (message.selection_id != null && Object.hasOwnProperty.call(message, "selection_id"))
                writer.uint32(24).uint32(message.selection_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityChooseGroupData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityChooseGroupData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityChooseGroupData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selection_id = value;
                        else
                            delete message.selection_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityChooseGroupData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityChooseGroupData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.selection_id != null && message.hasOwnProperty("selection_id"))
                if (!$util.isInteger(message.selection_id))
                    return "selection_id: integer expected";
            return null;
        };

        ActivityChooseGroupData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityChooseGroupData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityChooseGroupData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.selection_id != null)
                if (Number(object.selection_id) !== 0)
                    message.selection_id = object.selection_id >>> 0;
            return message;
        };

        ActivityChooseGroupData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.chest_id = 0;
                object.selection_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.selection_id != null && message.hasOwnProperty("selection_id"))
                object.selection_id = message.selection_id;
            return object;
        };

        ActivityChooseGroupData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityChooseGroupData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityChooseGroupData";
        };

        return ActivityChooseGroupData;
    })();

    lq.ActivityMMODataChanges = (function() {

        function ActivityMMODataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMODataChanges.prototype.activity_id = 0;
        ActivityMMODataChanges.prototype.character = null;
        ActivityMMODataChanges.prototype.team = null;
        ActivityMMODataChanges.prototype.bag = null;
        ActivityMMODataChanges.prototype.map = null;
        ActivityMMODataChanges.prototype.support = null;

        ActivityMMODataChanges.create = function create(properties) {
            return new ActivityMMODataChanges(properties);
        };

        ActivityMMODataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.encode(message.character, writer.uint32(18).fork()).ldelim();
            if (message.team != null && Object.hasOwnProperty.call(message, "team"))
                $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.encode(message.team, writer.uint32(26).fork()).ldelim();
            if (message.bag != null && Object.hasOwnProperty.call(message, "bag"))
                $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.encode(message.bag, writer.uint32(34).fork()).ldelim();
            if (message.map != null && Object.hasOwnProperty.call(message, "map"))
                $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.encode(message.map, writer.uint32(42).fork()).ldelim();
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.encode(message.support, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMODataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMODataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.team = $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.team);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.bag = $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.bag);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.map = $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.map);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.support = $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.support);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMODataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMODataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            if (message.team != null && message.hasOwnProperty("team")) {
                let error = $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.verify(message.team, _depth + 1);
                if (error)
                    return "team." + error;
            }
            if (message.bag != null && message.hasOwnProperty("bag")) {
                let error = $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.verify(message.bag, _depth + 1);
                if (error)
                    return "bag." + error;
            }
            if (message.map != null && message.hasOwnProperty("map")) {
                let error = $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.verify(message.map, _depth + 1);
                if (error)
                    return "map." + error;
            }
            if (message.support != null && message.hasOwnProperty("support")) {
                let error = $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.verify(message.support, _depth + 1);
                if (error)
                    return "support." + error;
            }
            return null;
        };

        ActivityMMODataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMODataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMODataChanges();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.ActivityMMODataChanges.character: object expected");
                message.character = $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.fromObject(object.character, _depth + 1);
            }
            if (object.team != null) {
                if (typeof object.team !== "object")
                    throw TypeError(".lq.ActivityMMODataChanges.team: object expected");
                message.team = $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.fromObject(object.team, _depth + 1);
            }
            if (object.bag != null) {
                if (typeof object.bag !== "object")
                    throw TypeError(".lq.ActivityMMODataChanges.bag: object expected");
                message.bag = $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.fromObject(object.bag, _depth + 1);
            }
            if (object.map != null) {
                if (typeof object.map !== "object")
                    throw TypeError(".lq.ActivityMMODataChanges.map: object expected");
                message.map = $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.fromObject(object.map, _depth + 1);
            }
            if (object.support != null) {
                if (typeof object.support !== "object")
                    throw TypeError(".lq.ActivityMMODataChanges.support: object expected");
                message.support = $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.fromObject(object.support, _depth + 1);
            }
            return message;
        };

        ActivityMMODataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.character = null;
                object.team = null;
                object.bag = null;
                object.map = null;
                object.support = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.toObject(message.character, options);
            if (message.team != null && message.hasOwnProperty("team"))
                object.team = $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.toObject(message.team, options);
            if (message.bag != null && message.hasOwnProperty("bag"))
                object.bag = $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.toObject(message.bag, options);
            if (message.map != null && message.hasOwnProperty("map"))
                object.map = $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.toObject(message.map, options);
            if (message.support != null && message.hasOwnProperty("support"))
                object.support = $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.toObject(message.support, options);
            return object;
        };

        ActivityMMODataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMODataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMODataChanges";
        };

        ActivityMMODataChanges.ActivityMMOCharacterDataChanges = (function() {

            function ActivityMMOCharacterDataChanges(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOCharacterDataChanges.prototype.character_id = null;
            ActivityMMOCharacterDataChanges.prototype.equipments = null;

            ActivityMMOCharacterDataChanges.create = function create(properties) {
                return new ActivityMMOCharacterDataChanges(properties);
            };

            ActivityMMOCharacterDataChanges.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                    $root.lq.UInt32Dirty.encode(message.character_id, writer.uint32(10).fork()).ldelim();
                if (message.equipments != null && Object.hasOwnProperty.call(message, "equipments"))
                    $root.lq.UInt32ArrayDirty.encode(message.equipments, writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOCharacterDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOCharacterDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.character_id = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.character_id);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.equipments = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.equipments);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOCharacterDataChanges.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOCharacterDataChanges.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character_id != null && message.hasOwnProperty("character_id")) {
                    let error = $root.lq.UInt32Dirty.verify(message.character_id, _depth + 1);
                    if (error)
                        return "character_id." + error;
                }
                if (message.equipments != null && message.hasOwnProperty("equipments")) {
                    let error = $root.lq.UInt32ArrayDirty.verify(message.equipments, _depth + 1);
                    if (error)
                        return "equipments." + error;
                }
                return null;
            };

            ActivityMMOCharacterDataChanges.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges();
                if (object.character_id != null) {
                    if (typeof object.character_id !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.character_id: object expected");
                    message.character_id = $root.lq.UInt32Dirty.fromObject(object.character_id, _depth + 1);
                }
                if (object.equipments != null) {
                    if (typeof object.equipments !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges.equipments: object expected");
                    message.equipments = $root.lq.UInt32ArrayDirty.fromObject(object.equipments, _depth + 1);
                }
                return message;
            };

            ActivityMMOCharacterDataChanges.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.character_id = null;
                    object.equipments = null;
                }
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    object.character_id = $root.lq.UInt32Dirty.toObject(message.character_id, options);
                if (message.equipments != null && message.hasOwnProperty("equipments"))
                    object.equipments = $root.lq.UInt32ArrayDirty.toObject(message.equipments, options);
                return object;
            };

            ActivityMMOCharacterDataChanges.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOCharacterDataChanges.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOCharacterDataChanges";
            };

            return ActivityMMOCharacterDataChanges;
        })();

        ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty = (function() {

            function ActivityMMOTeamMemberArrayDirty(properties) {
                this.value = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOTeamMemberArrayDirty.prototype.dirty = false;
            ActivityMMOTeamMemberArrayDirty.prototype.value = $util.emptyArray;

            ActivityMMOTeamMemberArrayDirty.create = function create(properties) {
                return new ActivityMMOTeamMemberArrayDirty(properties);
            };

            ActivityMMOTeamMemberArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(8).bool(message.dirty);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.lq.ActivityMMOTeamMember.encode(message.value[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOTeamMemberArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOTeamMemberArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.lq.ActivityMMOTeamMember.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOTeamMemberArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOTeamMemberArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (typeof message.dirty !== "boolean")
                        return "dirty: boolean expected";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.lq.ActivityMMOTeamMember.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            ActivityMMOTeamMemberArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty();
                if (object.dirty != null)
                    if (object.dirty)
                        message.dirty = Boolean(object.dirty);
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.value: array expected");
                    message.value = Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.value: object expected");
                        message.value[i] = $root.lq.ActivityMMOTeamMember.fromObject(object.value[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityMMOTeamMemberArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults)
                    object.dirty = false;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                if (message.value && message.value.length) {
                    object.value = Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.lq.ActivityMMOTeamMember.toObject(message.value[j], options);
                }
                return object;
            };

            ActivityMMOTeamMemberArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOTeamMemberArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty";
            };

            return ActivityMMOTeamMemberArrayDirty;
        })();

        ActivityMMODataChanges.ActivityMMOTeamDataChanges = (function() {

            function ActivityMMOTeamDataChanges(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOTeamDataChanges.prototype.members = null;

            ActivityMMOTeamDataChanges.create = function create(properties) {
                return new ActivityMMOTeamDataChanges(properties);
            };

            ActivityMMOTeamDataChanges.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.members != null && Object.hasOwnProperty.call(message, "members"))
                    $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.encode(message.members, writer.uint32(10).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOTeamDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOTeamDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.members = $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.members);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOTeamDataChanges.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOTeamDataChanges.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.members != null && message.hasOwnProperty("members")) {
                    let error = $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.verify(message.members, _depth + 1);
                    if (error)
                        return "members." + error;
                }
                return null;
            };

            ActivityMMOTeamDataChanges.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges();
                if (object.members != null) {
                    if (typeof object.members !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges.members: object expected");
                    message.members = $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.fromObject(object.members, _depth + 1);
                }
                return message;
            };

            ActivityMMOTeamDataChanges.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.members = null;
                if (message.members != null && message.hasOwnProperty("members"))
                    object.members = $root.lq.ActivityMMODataChanges.ActivityMMOTeamMemberArrayDirty.toObject(message.members, options);
                return object;
            };

            ActivityMMOTeamDataChanges.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOTeamDataChanges.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOTeamDataChanges";
            };

            return ActivityMMOTeamDataChanges;
        })();

        ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty = (function() {

            function ActivityMMOEquipmentsArrayDirty(properties) {
                this.value = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOEquipmentsArrayDirty.prototype.dirty = false;
            ActivityMMOEquipmentsArrayDirty.prototype.value = $util.emptyArray;

            ActivityMMOEquipmentsArrayDirty.create = function create(properties) {
                return new ActivityMMOEquipmentsArrayDirty(properties);
            };

            ActivityMMOEquipmentsArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(8).bool(message.dirty);
                if (message.value != null && message.value.length)
                    for (let i = 0; i < message.value.length; ++i)
                        $root.lq.ActivityMMOEquipmentData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOEquipmentsArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOEquipmentsArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.value && message.value.length))
                                message.value = [];
                            message.value.push($root.lq.ActivityMMOEquipmentData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOEquipmentsArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOEquipmentsArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (typeof message.dirty !== "boolean")
                        return "dirty: boolean expected";
                if (message.value != null && message.hasOwnProperty("value")) {
                    if (!Array.isArray(message.value))
                        return "value: array expected";
                    for (let i = 0; i < message.value.length; ++i) {
                        let error = $root.lq.ActivityMMOEquipmentData.verify(message.value[i], _depth + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            ActivityMMOEquipmentsArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty();
                if (object.dirty != null)
                    if (object.dirty)
                        message.dirty = Boolean(object.dirty);
                if (object.value) {
                    if (!Array.isArray(object.value))
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.value: array expected");
                    message.value = Array(object.value.length);
                    for (let i = 0; i < object.value.length; ++i) {
                        if (typeof object.value[i] !== "object")
                            throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.value: object expected");
                        message.value[i] = $root.lq.ActivityMMOEquipmentData.fromObject(object.value[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityMMOEquipmentsArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.value = [];
                if (options.defaults)
                    object.dirty = false;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                if (message.value && message.value.length) {
                    object.value = Array(message.value.length);
                    for (let j = 0; j < message.value.length; ++j)
                        object.value[j] = $root.lq.ActivityMMOEquipmentData.toObject(message.value[j], options);
                }
                return object;
            };

            ActivityMMOEquipmentsArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOEquipmentsArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty";
            };

            return ActivityMMOEquipmentsArrayDirty;
        })();

        ActivityMMODataChanges.ActivityMMOBagDataChanges = (function() {

            function ActivityMMOBagDataChanges(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOBagDataChanges.prototype.equipments = null;
            ActivityMMOBagDataChanges.prototype.total_gain_count = null;
            ActivityMMOBagDataChanges.prototype.total_fusion_count = null;

            ActivityMMOBagDataChanges.create = function create(properties) {
                return new ActivityMMOBagDataChanges(properties);
            };

            ActivityMMOBagDataChanges.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.equipments != null && Object.hasOwnProperty.call(message, "equipments"))
                    $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.encode(message.equipments, writer.uint32(10).fork()).ldelim();
                if (message.total_gain_count != null && Object.hasOwnProperty.call(message, "total_gain_count"))
                    $root.lq.UInt32Dirty.encode(message.total_gain_count, writer.uint32(18).fork()).ldelim();
                if (message.total_fusion_count != null && Object.hasOwnProperty.call(message, "total_fusion_count"))
                    $root.lq.UInt32Dirty.encode(message.total_fusion_count, writer.uint32(26).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOBagDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOBagDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.equipments = $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.equipments);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.total_gain_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.total_gain_count);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.total_fusion_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.total_fusion_count);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOBagDataChanges.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOBagDataChanges.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.equipments != null && message.hasOwnProperty("equipments")) {
                    let error = $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.verify(message.equipments, _depth + 1);
                    if (error)
                        return "equipments." + error;
                }
                if (message.total_gain_count != null && message.hasOwnProperty("total_gain_count")) {
                    let error = $root.lq.UInt32Dirty.verify(message.total_gain_count, _depth + 1);
                    if (error)
                        return "total_gain_count." + error;
                }
                if (message.total_fusion_count != null && message.hasOwnProperty("total_fusion_count")) {
                    let error = $root.lq.UInt32Dirty.verify(message.total_fusion_count, _depth + 1);
                    if (error)
                        return "total_fusion_count." + error;
                }
                return null;
            };

            ActivityMMOBagDataChanges.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOBagDataChanges();
                if (object.equipments != null) {
                    if (typeof object.equipments !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.equipments: object expected");
                    message.equipments = $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.fromObject(object.equipments, _depth + 1);
                }
                if (object.total_gain_count != null) {
                    if (typeof object.total_gain_count !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.total_gain_count: object expected");
                    message.total_gain_count = $root.lq.UInt32Dirty.fromObject(object.total_gain_count, _depth + 1);
                }
                if (object.total_fusion_count != null) {
                    if (typeof object.total_fusion_count !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOBagDataChanges.total_fusion_count: object expected");
                    message.total_fusion_count = $root.lq.UInt32Dirty.fromObject(object.total_fusion_count, _depth + 1);
                }
                return message;
            };

            ActivityMMOBagDataChanges.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.equipments = null;
                    object.total_gain_count = null;
                    object.total_fusion_count = null;
                }
                if (message.equipments != null && message.hasOwnProperty("equipments"))
                    object.equipments = $root.lq.ActivityMMODataChanges.ActivityMMOEquipmentsArrayDirty.toObject(message.equipments, options);
                if (message.total_gain_count != null && message.hasOwnProperty("total_gain_count"))
                    object.total_gain_count = $root.lq.UInt32Dirty.toObject(message.total_gain_count, options);
                if (message.total_fusion_count != null && message.hasOwnProperty("total_fusion_count"))
                    object.total_fusion_count = $root.lq.UInt32Dirty.toObject(message.total_fusion_count, options);
                return object;
            };

            ActivityMMOBagDataChanges.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOBagDataChanges.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOBagDataChanges";
            };

            return ActivityMMOBagDataChanges;
        })();

        ActivityMMODataChanges.ActivityMMOMapDataChanges = (function() {

            function ActivityMMOMapDataChanges(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOMapDataChanges.prototype.level = null;
            ActivityMMOMapDataChanges.prototype.random_seed = null;
            ActivityMMOMapDataChanges.prototype.won = null;
            ActivityMMOMapDataChanges.prototype.failed = null;

            ActivityMMOMapDataChanges.create = function create(properties) {
                return new ActivityMMOMapDataChanges(properties);
            };

            ActivityMMOMapDataChanges.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    $root.lq.UInt32Dirty.encode(message.level, writer.uint32(10).fork()).ldelim();
                if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                    $root.lq.UInt32Dirty.encode(message.random_seed, writer.uint32(18).fork()).ldelim();
                if (message.won != null && Object.hasOwnProperty.call(message, "won"))
                    $root.lq.UInt32Dirty.encode(message.won, writer.uint32(26).fork()).ldelim();
                if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                    $root.lq.UInt32Dirty.encode(message.failed, writer.uint32(34).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOMapDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOMapDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.level = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.random_seed = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.random_seed);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.won = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.won);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            message.failed = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.failed);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOMapDataChanges.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOMapDataChanges.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && message.hasOwnProperty("level")) {
                    let error = $root.lq.UInt32Dirty.verify(message.level, _depth + 1);
                    if (error)
                        return "level." + error;
                }
                if (message.random_seed != null && message.hasOwnProperty("random_seed")) {
                    let error = $root.lq.UInt32Dirty.verify(message.random_seed, _depth + 1);
                    if (error)
                        return "random_seed." + error;
                }
                if (message.won != null && message.hasOwnProperty("won")) {
                    let error = $root.lq.UInt32Dirty.verify(message.won, _depth + 1);
                    if (error)
                        return "won." + error;
                }
                if (message.failed != null && message.hasOwnProperty("failed")) {
                    let error = $root.lq.UInt32Dirty.verify(message.failed, _depth + 1);
                    if (error)
                        return "failed." + error;
                }
                return null;
            };

            ActivityMMOMapDataChanges.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOMapDataChanges();
                if (object.level != null) {
                    if (typeof object.level !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.level: object expected");
                    message.level = $root.lq.UInt32Dirty.fromObject(object.level, _depth + 1);
                }
                if (object.random_seed != null) {
                    if (typeof object.random_seed !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.random_seed: object expected");
                    message.random_seed = $root.lq.UInt32Dirty.fromObject(object.random_seed, _depth + 1);
                }
                if (object.won != null) {
                    if (typeof object.won !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.won: object expected");
                    message.won = $root.lq.UInt32Dirty.fromObject(object.won, _depth + 1);
                }
                if (object.failed != null) {
                    if (typeof object.failed !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOMapDataChanges.failed: object expected");
                    message.failed = $root.lq.UInt32Dirty.fromObject(object.failed, _depth + 1);
                }
                return message;
            };

            ActivityMMOMapDataChanges.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level = null;
                    object.random_seed = null;
                    object.won = null;
                    object.failed = null;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = $root.lq.UInt32Dirty.toObject(message.level, options);
                if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                    object.random_seed = $root.lq.UInt32Dirty.toObject(message.random_seed, options);
                if (message.won != null && message.hasOwnProperty("won"))
                    object.won = $root.lq.UInt32Dirty.toObject(message.won, options);
                if (message.failed != null && message.hasOwnProperty("failed"))
                    object.failed = $root.lq.UInt32Dirty.toObject(message.failed, options);
                return object;
            };

            ActivityMMOMapDataChanges.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOMapDataChanges.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOMapDataChanges";
            };

            return ActivityMMOMapDataChanges;
        })();

        ActivityMMODataChanges.ActivityMMOSupportDataChanges = (function() {

            function ActivityMMOSupportDataChanges(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOSupportDataChanges.prototype.last_receive_time = null;
            ActivityMMOSupportDataChanges.prototype.most_supporter = null;
            ActivityMMOSupportDataChanges.prototype.receive_support_count = null;

            ActivityMMOSupportDataChanges.create = function create(properties) {
                return new ActivityMMOSupportDataChanges(properties);
            };

            ActivityMMOSupportDataChanges.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.last_receive_time != null && Object.hasOwnProperty.call(message, "last_receive_time"))
                    $root.lq.UInt32Dirty.encode(message.last_receive_time, writer.uint32(10).fork()).ldelim();
                if (message.most_supporter != null && Object.hasOwnProperty.call(message, "most_supporter"))
                    $root.lq.ActivityMMOSupportRecordDirty.encode(message.most_supporter, writer.uint32(18).fork()).ldelim();
                if (message.receive_support_count != null && Object.hasOwnProperty.call(message, "receive_support_count"))
                    $root.lq.UInt32Dirty.encode(message.receive_support_count, writer.uint32(26).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOSupportDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOSupportDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.last_receive_time = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.last_receive_time);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.most_supporter = $root.lq.ActivityMMOSupportRecordDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.most_supporter);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.receive_support_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.receive_support_count);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOSupportDataChanges.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOSupportDataChanges.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.last_receive_time != null && message.hasOwnProperty("last_receive_time")) {
                    let error = $root.lq.UInt32Dirty.verify(message.last_receive_time, _depth + 1);
                    if (error)
                        return "last_receive_time." + error;
                }
                if (message.most_supporter != null && message.hasOwnProperty("most_supporter")) {
                    let error = $root.lq.ActivityMMOSupportRecordDirty.verify(message.most_supporter, _depth + 1);
                    if (error)
                        return "most_supporter." + error;
                }
                if (message.receive_support_count != null && message.hasOwnProperty("receive_support_count")) {
                    let error = $root.lq.UInt32Dirty.verify(message.receive_support_count, _depth + 1);
                    if (error)
                        return "receive_support_count." + error;
                }
                return null;
            };

            ActivityMMOSupportDataChanges.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges();
                if (object.last_receive_time != null) {
                    if (typeof object.last_receive_time !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.last_receive_time: object expected");
                    message.last_receive_time = $root.lq.UInt32Dirty.fromObject(object.last_receive_time, _depth + 1);
                }
                if (object.most_supporter != null) {
                    if (typeof object.most_supporter !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.most_supporter: object expected");
                    message.most_supporter = $root.lq.ActivityMMOSupportRecordDirty.fromObject(object.most_supporter, _depth + 1);
                }
                if (object.receive_support_count != null) {
                    if (typeof object.receive_support_count !== "object")
                        throw TypeError(".lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges.receive_support_count: object expected");
                    message.receive_support_count = $root.lq.UInt32Dirty.fromObject(object.receive_support_count, _depth + 1);
                }
                return message;
            };

            ActivityMMOSupportDataChanges.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.last_receive_time = null;
                    object.most_supporter = null;
                    object.receive_support_count = null;
                }
                if (message.last_receive_time != null && message.hasOwnProperty("last_receive_time"))
                    object.last_receive_time = $root.lq.UInt32Dirty.toObject(message.last_receive_time, options);
                if (message.most_supporter != null && message.hasOwnProperty("most_supporter"))
                    object.most_supporter = $root.lq.ActivityMMOSupportRecordDirty.toObject(message.most_supporter, options);
                if (message.receive_support_count != null && message.hasOwnProperty("receive_support_count"))
                    object.receive_support_count = $root.lq.UInt32Dirty.toObject(message.receive_support_count, options);
                return object;
            };

            ActivityMMOSupportDataChanges.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOSupportDataChanges.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMODataChanges.ActivityMMOSupportDataChanges";
            };

            return ActivityMMOSupportDataChanges;
        })();

        return ActivityMMODataChanges;
    })();

    lq.ActivityMMOTeamMember = (function() {

        function ActivityMMOTeamMember(properties) {
            this.equipments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMOTeamMember.prototype.character_id = 0;
        ActivityMMOTeamMember.prototype.equipments = $util.emptyArray;
        ActivityMMOTeamMember.prototype.id = 0;
        ActivityMMOTeamMember.prototype.type = 0;

        ActivityMMOTeamMember.create = function create(properties) {
            return new ActivityMMOTeamMember(properties);
        };

        ActivityMMOTeamMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.equipments != null && message.equipments.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.equipments.length; ++i)
                    writer.uint32(message.equipments[i]);
                writer.ldelim();
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(24).uint32(message.id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(32).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMOTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMOTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOTeamMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equipments.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.equipments && message.equipments.length))
                            message.equipments = [];
                        message.equipments.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMOTeamMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMOTeamMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.equipments != null && message.hasOwnProperty("equipments")) {
                if (!Array.isArray(message.equipments))
                    return "equipments: array expected";
                for (let i = 0; i < message.equipments.length; ++i)
                    if (!$util.isInteger(message.equipments[i]))
                        return "equipments: integer[] expected";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ActivityMMOTeamMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMOTeamMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMOTeamMember();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.equipments) {
                if (!Array.isArray(object.equipments))
                    throw TypeError(".lq.ActivityMMOTeamMember.equipments: array expected");
                message.equipments = Array(object.equipments.length);
                for (let i = 0; i < object.equipments.length; ++i)
                    message.equipments[i] = object.equipments[i] >>> 0;
            }
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ActivityMMOTeamMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.equipments = [];
            if (options.defaults) {
                object.character_id = 0;
                object.id = 0;
                object.type = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.equipments && message.equipments.length) {
                object.equipments = Array(message.equipments.length);
                for (let j = 0; j < message.equipments.length; ++j)
                    object.equipments[j] = message.equipments[j];
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ActivityMMOTeamMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMOTeamMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMOTeamMember";
        };

        return ActivityMMOTeamMember;
    })();

    lq.ActivityMMOEquipmentData = (function() {

        function ActivityMMOEquipmentData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMOEquipmentData.prototype.id = 0;
        ActivityMMOEquipmentData.prototype.stack = 0;

        ActivityMMOEquipmentData.create = function create(properties) {
            return new ActivityMMOEquipmentData(properties);
        };

        ActivityMMOEquipmentData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.stack != null && Object.hasOwnProperty.call(message, "stack"))
                writer.uint32(16).uint32(message.stack);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMOEquipmentData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMOEquipmentData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOEquipmentData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stack = value;
                        else
                            delete message.stack;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMOEquipmentData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMOEquipmentData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.stack != null && message.hasOwnProperty("stack"))
                if (!$util.isInteger(message.stack))
                    return "stack: integer expected";
            return null;
        };

        ActivityMMOEquipmentData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMOEquipmentData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMOEquipmentData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.stack != null)
                if (Number(object.stack) !== 0)
                    message.stack = object.stack >>> 0;
            return message;
        };

        ActivityMMOEquipmentData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.stack = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.stack != null && message.hasOwnProperty("stack"))
                object.stack = message.stack;
            return object;
        };

        ActivityMMOEquipmentData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMOEquipmentData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMOEquipmentData";
        };

        return ActivityMMOEquipmentData;
    })();

    lq.MMOActivityTeamCandidateData = (function() {

        function MMOActivityTeamCandidateData(properties) {
            this.equipments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MMOActivityTeamCandidateData.prototype.character_id = 0;
        MMOActivityTeamCandidateData.prototype.equipments = $util.emptyArray;
        MMOActivityTeamCandidateData.prototype.id = 0;

        MMOActivityTeamCandidateData.create = function create(properties) {
            return new MMOActivityTeamCandidateData(properties);
        };

        MMOActivityTeamCandidateData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.equipments != null && message.equipments.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.equipments.length; ++i)
                    writer.uint32(message.equipments[i]);
                writer.ldelim();
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(24).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MMOActivityTeamCandidateData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MMOActivityTeamCandidateData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MMOActivityTeamCandidateData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equipments.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.equipments && message.equipments.length))
                            message.equipments = [];
                        message.equipments.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MMOActivityTeamCandidateData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MMOActivityTeamCandidateData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.equipments != null && message.hasOwnProperty("equipments")) {
                if (!Array.isArray(message.equipments))
                    return "equipments: array expected";
                for (let i = 0; i < message.equipments.length; ++i)
                    if (!$util.isInteger(message.equipments[i]))
                        return "equipments: integer[] expected";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        MMOActivityTeamCandidateData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MMOActivityTeamCandidateData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MMOActivityTeamCandidateData();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.equipments) {
                if (!Array.isArray(object.equipments))
                    throw TypeError(".lq.MMOActivityTeamCandidateData.equipments: array expected");
                message.equipments = Array(object.equipments.length);
                for (let i = 0; i < object.equipments.length; ++i)
                    message.equipments[i] = object.equipments[i] >>> 0;
            }
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        MMOActivityTeamCandidateData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.equipments = [];
            if (options.defaults) {
                object.character_id = 0;
                object.id = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.equipments && message.equipments.length) {
                object.equipments = Array(message.equipments.length);
                for (let j = 0; j < message.equipments.length; ++j)
                    object.equipments[j] = message.equipments[j];
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        MMOActivityTeamCandidateData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MMOActivityTeamCandidateData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MMOActivityTeamCandidateData";
        };

        return MMOActivityTeamCandidateData;
    })();

    lq.ActivityMMOSupportRecord = (function() {

        function ActivityMMOSupportRecord(properties) {
            this.equipments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMOSupportRecord.prototype.id = 0;
        ActivityMMOSupportRecord.prototype.count = 0;
        ActivityMMOSupportRecord.prototype.type = 0;
        ActivityMMOSupportRecord.prototype.character_id = 0;
        ActivityMMOSupportRecord.prototype.equipments = $util.emptyArray;

        ActivityMMOSupportRecord.create = function create(properties) {
            return new ActivityMMOSupportRecord(properties);
        };

        ActivityMMOSupportRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(32).uint32(message.character_id);
            if (message.equipments != null && message.equipments.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.equipments.length; ++i)
                    writer.uint32(message.equipments[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMOSupportRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMOSupportRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOSupportRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equipments.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.equipments && message.equipments.length))
                            message.equipments = [];
                        message.equipments.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMOSupportRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMOSupportRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.equipments != null && message.hasOwnProperty("equipments")) {
                if (!Array.isArray(message.equipments))
                    return "equipments: array expected";
                for (let i = 0; i < message.equipments.length; ++i)
                    if (!$util.isInteger(message.equipments[i]))
                        return "equipments: integer[] expected";
            }
            return null;
        };

        ActivityMMOSupportRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMOSupportRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMOSupportRecord();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.equipments) {
                if (!Array.isArray(object.equipments))
                    throw TypeError(".lq.ActivityMMOSupportRecord.equipments: array expected");
                message.equipments = Array(object.equipments.length);
                for (let i = 0; i < object.equipments.length; ++i)
                    message.equipments[i] = object.equipments[i] >>> 0;
            }
            return message;
        };

        ActivityMMOSupportRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.equipments = [];
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
                object.type = 0;
                object.character_id = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.equipments && message.equipments.length) {
                object.equipments = Array(message.equipments.length);
                for (let j = 0; j < message.equipments.length; ++j)
                    object.equipments[j] = message.equipments[j];
            }
            return object;
        };

        ActivityMMOSupportRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMOSupportRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMOSupportRecord";
        };

        return ActivityMMOSupportRecord;
    })();

    lq.ActivityMMOSupportRecordDirty = (function() {

        function ActivityMMOSupportRecordDirty(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMOSupportRecordDirty.prototype.dirty = false;
        ActivityMMOSupportRecordDirty.prototype.value = null;

        ActivityMMOSupportRecordDirty.create = function create(properties) {
            return new ActivityMMOSupportRecordDirty(properties);
        };

        ActivityMMOSupportRecordDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                $root.lq.ActivityMMOSupportRecord.encode(message.value, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMOSupportRecordDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMOSupportRecordDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOSupportRecordDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.lq.ActivityMMOSupportRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMOSupportRecordDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMOSupportRecordDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                let error = $root.lq.ActivityMMOSupportRecord.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        ActivityMMOSupportRecordDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMOSupportRecordDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMOSupportRecordDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value != null) {
                if (typeof object.value !== "object")
                    throw TypeError(".lq.ActivityMMOSupportRecordDirty.value: object expected");
                message.value = $root.lq.ActivityMMOSupportRecord.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        ActivityMMOSupportRecordDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dirty = false;
                object.value = null;
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = $root.lq.ActivityMMOSupportRecord.toObject(message.value, options);
            return object;
        };

        ActivityMMOSupportRecordDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMOSupportRecordDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMOSupportRecordDirty";
        };

        return ActivityMMOSupportRecordDirty;
    })();

    lq.ActivityMMOData = (function() {

        function ActivityMMOData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMMOData.prototype.activity_id = 0;
        ActivityMMOData.prototype.character = null;
        ActivityMMOData.prototype.team = null;
        ActivityMMOData.prototype.bag = null;
        ActivityMMOData.prototype.map = null;
        ActivityMMOData.prototype.support = null;

        ActivityMMOData.create = function create(properties) {
            return new ActivityMMOData(properties);
        };

        ActivityMMOData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.ActivityMMOData.ActivityMMOCharacterData.encode(message.character, writer.uint32(18).fork()).ldelim();
            if (message.team != null && Object.hasOwnProperty.call(message, "team"))
                $root.lq.ActivityMMOData.ActivityMMOTeamData.encode(message.team, writer.uint32(26).fork()).ldelim();
            if (message.bag != null && Object.hasOwnProperty.call(message, "bag"))
                $root.lq.ActivityMMOData.ActivityMMOBagData.encode(message.bag, writer.uint32(34).fork()).ldelim();
            if (message.map != null && Object.hasOwnProperty.call(message, "map"))
                $root.lq.ActivityMMOData.ActivityMMOMapData.encode(message.map, writer.uint32(42).fork()).ldelim();
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                $root.lq.ActivityMMOData.ActivityMMOSupportData.encode(message.support, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMMOData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMMOData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.ActivityMMOData.ActivityMMOCharacterData.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.team = $root.lq.ActivityMMOData.ActivityMMOTeamData.decode(reader, reader.uint32(), undefined, _depth + 1, message.team);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.bag = $root.lq.ActivityMMOData.ActivityMMOBagData.decode(reader, reader.uint32(), undefined, _depth + 1, message.bag);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.map = $root.lq.ActivityMMOData.ActivityMMOMapData.decode(reader, reader.uint32(), undefined, _depth + 1, message.map);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.support = $root.lq.ActivityMMOData.ActivityMMOSupportData.decode(reader, reader.uint32(), undefined, _depth + 1, message.support);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMMOData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMMOData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.ActivityMMOData.ActivityMMOCharacterData.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            if (message.team != null && message.hasOwnProperty("team")) {
                let error = $root.lq.ActivityMMOData.ActivityMMOTeamData.verify(message.team, _depth + 1);
                if (error)
                    return "team." + error;
            }
            if (message.bag != null && message.hasOwnProperty("bag")) {
                let error = $root.lq.ActivityMMOData.ActivityMMOBagData.verify(message.bag, _depth + 1);
                if (error)
                    return "bag." + error;
            }
            if (message.map != null && message.hasOwnProperty("map")) {
                let error = $root.lq.ActivityMMOData.ActivityMMOMapData.verify(message.map, _depth + 1);
                if (error)
                    return "map." + error;
            }
            if (message.support != null && message.hasOwnProperty("support")) {
                let error = $root.lq.ActivityMMOData.ActivityMMOSupportData.verify(message.support, _depth + 1);
                if (error)
                    return "support." + error;
            }
            return null;
        };

        ActivityMMOData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMMOData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMMOData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.ActivityMMOData.character: object expected");
                message.character = $root.lq.ActivityMMOData.ActivityMMOCharacterData.fromObject(object.character, _depth + 1);
            }
            if (object.team != null) {
                if (typeof object.team !== "object")
                    throw TypeError(".lq.ActivityMMOData.team: object expected");
                message.team = $root.lq.ActivityMMOData.ActivityMMOTeamData.fromObject(object.team, _depth + 1);
            }
            if (object.bag != null) {
                if (typeof object.bag !== "object")
                    throw TypeError(".lq.ActivityMMOData.bag: object expected");
                message.bag = $root.lq.ActivityMMOData.ActivityMMOBagData.fromObject(object.bag, _depth + 1);
            }
            if (object.map != null) {
                if (typeof object.map !== "object")
                    throw TypeError(".lq.ActivityMMOData.map: object expected");
                message.map = $root.lq.ActivityMMOData.ActivityMMOMapData.fromObject(object.map, _depth + 1);
            }
            if (object.support != null) {
                if (typeof object.support !== "object")
                    throw TypeError(".lq.ActivityMMOData.support: object expected");
                message.support = $root.lq.ActivityMMOData.ActivityMMOSupportData.fromObject(object.support, _depth + 1);
            }
            return message;
        };

        ActivityMMOData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.character = null;
                object.team = null;
                object.bag = null;
                object.map = null;
                object.support = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.ActivityMMOData.ActivityMMOCharacterData.toObject(message.character, options);
            if (message.team != null && message.hasOwnProperty("team"))
                object.team = $root.lq.ActivityMMOData.ActivityMMOTeamData.toObject(message.team, options);
            if (message.bag != null && message.hasOwnProperty("bag"))
                object.bag = $root.lq.ActivityMMOData.ActivityMMOBagData.toObject(message.bag, options);
            if (message.map != null && message.hasOwnProperty("map"))
                object.map = $root.lq.ActivityMMOData.ActivityMMOMapData.toObject(message.map, options);
            if (message.support != null && message.hasOwnProperty("support"))
                object.support = $root.lq.ActivityMMOData.ActivityMMOSupportData.toObject(message.support, options);
            return object;
        };

        ActivityMMOData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMMOData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMMOData";
        };

        ActivityMMOData.ActivityMMOCharacterData = (function() {

            function ActivityMMOCharacterData(properties) {
                this.equipments = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOCharacterData.prototype.character_id = 0;
            ActivityMMOCharacterData.prototype.equipments = $util.emptyArray;

            ActivityMMOCharacterData.create = function create(properties) {
                return new ActivityMMOCharacterData(properties);
            };

            ActivityMMOCharacterData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                    writer.uint32(8).uint32(message.character_id);
                if (message.equipments != null && message.equipments.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.equipments.length; ++i)
                        writer.uint32(message.equipments[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOCharacterData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOCharacterData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOCharacterData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.character_id = value;
                            else
                                delete message.character_id;
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.equipments && message.equipments.length))
                                    message.equipments = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.equipments.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            message.equipments.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOCharacterData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOCharacterData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    if (!$util.isInteger(message.character_id))
                        return "character_id: integer expected";
                if (message.equipments != null && message.hasOwnProperty("equipments")) {
                    if (!Array.isArray(message.equipments))
                        return "equipments: array expected";
                    for (let i = 0; i < message.equipments.length; ++i)
                        if (!$util.isInteger(message.equipments[i]))
                            return "equipments: integer[] expected";
                }
                return null;
            };

            ActivityMMOCharacterData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMOData.ActivityMMOCharacterData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMOData.ActivityMMOCharacterData();
                if (object.character_id != null)
                    if (Number(object.character_id) !== 0)
                        message.character_id = object.character_id >>> 0;
                if (object.equipments) {
                    if (!Array.isArray(object.equipments))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOCharacterData.equipments: array expected");
                    message.equipments = Array(object.equipments.length);
                    for (let i = 0; i < object.equipments.length; ++i)
                        message.equipments[i] = object.equipments[i] >>> 0;
                }
                return message;
            };

            ActivityMMOCharacterData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.equipments = [];
                if (options.defaults)
                    object.character_id = 0;
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    object.character_id = message.character_id;
                if (message.equipments && message.equipments.length) {
                    object.equipments = Array(message.equipments.length);
                    for (let j = 0; j < message.equipments.length; ++j)
                        object.equipments[j] = message.equipments[j];
                }
                return object;
            };

            ActivityMMOCharacterData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOCharacterData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMOData.ActivityMMOCharacterData";
            };

            return ActivityMMOCharacterData;
        })();

        ActivityMMOData.ActivityMMOTeamData = (function() {

            function ActivityMMOTeamData(properties) {
                this.members = [];
                this.friend_list = [];
                this.random_friends = [];
                this.npc_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOTeamData.prototype.members = $util.emptyArray;
            ActivityMMOTeamData.prototype.friend_list = $util.emptyArray;
            ActivityMMOTeamData.prototype.friend_list_expire = 0;
            ActivityMMOTeamData.prototype.random_friends = $util.emptyArray;
            ActivityMMOTeamData.prototype.daily_update_time = 0;
            ActivityMMOTeamData.prototype.npc_list = $util.emptyArray;

            ActivityMMOTeamData.create = function create(properties) {
                return new ActivityMMOTeamData(properties);
            };

            ActivityMMOTeamData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.members != null && message.members.length)
                    for (let i = 0; i < message.members.length; ++i)
                        $root.lq.ActivityMMOTeamMember.encode(message.members[i], writer.uint32(10).fork()).ldelim();
                if (message.friend_list != null && message.friend_list.length)
                    for (let i = 0; i < message.friend_list.length; ++i)
                        $root.lq.MMOActivityTeamCandidateData.encode(message.friend_list[i], writer.uint32(802).fork()).ldelim();
                if (message.friend_list_expire != null && Object.hasOwnProperty.call(message, "friend_list_expire"))
                    writer.uint32(808).uint32(message.friend_list_expire);
                if (message.random_friends != null && message.random_friends.length)
                    for (let i = 0; i < message.random_friends.length; ++i)
                        $root.lq.MMOActivityTeamCandidateData.encode(message.random_friends[i], writer.uint32(818).fork()).ldelim();
                if (message.daily_update_time != null && Object.hasOwnProperty.call(message, "daily_update_time"))
                    writer.uint32(824).uint32(message.daily_update_time);
                if (message.npc_list != null && message.npc_list.length)
                    for (let i = 0; i < message.npc_list.length; ++i)
                        $root.lq.MMOActivityTeamCandidateData.encode(message.npc_list[i], writer.uint32(834).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOTeamData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOTeamData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOTeamData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.members && message.members.length))
                                message.members = [];
                            message.members.push($root.lq.ActivityMMOTeamMember.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 100: {
                            if (wireType !== 2)
                                break;
                            if (!(message.friend_list && message.friend_list.length))
                                message.friend_list = [];
                            message.friend_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 101: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.friend_list_expire = value;
                            else
                                delete message.friend_list_expire;
                            continue;
                        }
                    case 102: {
                            if (wireType !== 2)
                                break;
                            if (!(message.random_friends && message.random_friends.length))
                                message.random_friends = [];
                            message.random_friends.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 103: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.daily_update_time = value;
                            else
                                delete message.daily_update_time;
                            continue;
                        }
                    case 104: {
                            if (wireType !== 2)
                                break;
                            if (!(message.npc_list && message.npc_list.length))
                                message.npc_list = [];
                            message.npc_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOTeamData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOTeamData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.members != null && message.hasOwnProperty("members")) {
                    if (!Array.isArray(message.members))
                        return "members: array expected";
                    for (let i = 0; i < message.members.length; ++i) {
                        let error = $root.lq.ActivityMMOTeamMember.verify(message.members[i], _depth + 1);
                        if (error)
                            return "members." + error;
                    }
                }
                if (message.friend_list != null && message.hasOwnProperty("friend_list")) {
                    if (!Array.isArray(message.friend_list))
                        return "friend_list: array expected";
                    for (let i = 0; i < message.friend_list.length; ++i) {
                        let error = $root.lq.MMOActivityTeamCandidateData.verify(message.friend_list[i], _depth + 1);
                        if (error)
                            return "friend_list." + error;
                    }
                }
                if (message.friend_list_expire != null && message.hasOwnProperty("friend_list_expire"))
                    if (!$util.isInteger(message.friend_list_expire))
                        return "friend_list_expire: integer expected";
                if (message.random_friends != null && message.hasOwnProperty("random_friends")) {
                    if (!Array.isArray(message.random_friends))
                        return "random_friends: array expected";
                    for (let i = 0; i < message.random_friends.length; ++i) {
                        let error = $root.lq.MMOActivityTeamCandidateData.verify(message.random_friends[i], _depth + 1);
                        if (error)
                            return "random_friends." + error;
                    }
                }
                if (message.daily_update_time != null && message.hasOwnProperty("daily_update_time"))
                    if (!$util.isInteger(message.daily_update_time))
                        return "daily_update_time: integer expected";
                if (message.npc_list != null && message.hasOwnProperty("npc_list")) {
                    if (!Array.isArray(message.npc_list))
                        return "npc_list: array expected";
                    for (let i = 0; i < message.npc_list.length; ++i) {
                        let error = $root.lq.MMOActivityTeamCandidateData.verify(message.npc_list[i], _depth + 1);
                        if (error)
                            return "npc_list." + error;
                    }
                }
                return null;
            };

            ActivityMMOTeamData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMOData.ActivityMMOTeamData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMOData.ActivityMMOTeamData();
                if (object.members) {
                    if (!Array.isArray(object.members))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.members: array expected");
                    message.members = Array(object.members.length);
                    for (let i = 0; i < object.members.length; ++i) {
                        if (typeof object.members[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.members: object expected");
                        message.members[i] = $root.lq.ActivityMMOTeamMember.fromObject(object.members[i], _depth + 1);
                    }
                }
                if (object.friend_list) {
                    if (!Array.isArray(object.friend_list))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.friend_list: array expected");
                    message.friend_list = Array(object.friend_list.length);
                    for (let i = 0; i < object.friend_list.length; ++i) {
                        if (typeof object.friend_list[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.friend_list: object expected");
                        message.friend_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.friend_list[i], _depth + 1);
                    }
                }
                if (object.friend_list_expire != null)
                    if (Number(object.friend_list_expire) !== 0)
                        message.friend_list_expire = object.friend_list_expire >>> 0;
                if (object.random_friends) {
                    if (!Array.isArray(object.random_friends))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.random_friends: array expected");
                    message.random_friends = Array(object.random_friends.length);
                    for (let i = 0; i < object.random_friends.length; ++i) {
                        if (typeof object.random_friends[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.random_friends: object expected");
                        message.random_friends[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.random_friends[i], _depth + 1);
                    }
                }
                if (object.daily_update_time != null)
                    if (Number(object.daily_update_time) !== 0)
                        message.daily_update_time = object.daily_update_time >>> 0;
                if (object.npc_list) {
                    if (!Array.isArray(object.npc_list))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.npc_list: array expected");
                    message.npc_list = Array(object.npc_list.length);
                    for (let i = 0; i < object.npc_list.length; ++i) {
                        if (typeof object.npc_list[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOTeamData.npc_list: object expected");
                        message.npc_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.npc_list[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityMMOTeamData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.members = [];
                    object.friend_list = [];
                    object.random_friends = [];
                    object.npc_list = [];
                }
                if (options.defaults) {
                    object.friend_list_expire = 0;
                    object.daily_update_time = 0;
                }
                if (message.members && message.members.length) {
                    object.members = Array(message.members.length);
                    for (let j = 0; j < message.members.length; ++j)
                        object.members[j] = $root.lq.ActivityMMOTeamMember.toObject(message.members[j], options);
                }
                if (message.friend_list && message.friend_list.length) {
                    object.friend_list = Array(message.friend_list.length);
                    for (let j = 0; j < message.friend_list.length; ++j)
                        object.friend_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.friend_list[j], options);
                }
                if (message.friend_list_expire != null && message.hasOwnProperty("friend_list_expire"))
                    object.friend_list_expire = message.friend_list_expire;
                if (message.random_friends && message.random_friends.length) {
                    object.random_friends = Array(message.random_friends.length);
                    for (let j = 0; j < message.random_friends.length; ++j)
                        object.random_friends[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.random_friends[j], options);
                }
                if (message.daily_update_time != null && message.hasOwnProperty("daily_update_time"))
                    object.daily_update_time = message.daily_update_time;
                if (message.npc_list && message.npc_list.length) {
                    object.npc_list = Array(message.npc_list.length);
                    for (let j = 0; j < message.npc_list.length; ++j)
                        object.npc_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.npc_list[j], options);
                }
                return object;
            };

            ActivityMMOTeamData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOTeamData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMOData.ActivityMMOTeamData";
            };

            return ActivityMMOTeamData;
        })();

        ActivityMMOData.ActivityMMOBagData = (function() {

            function ActivityMMOBagData(properties) {
                this.equipments = [];
                this.random_record = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOBagData.prototype.equipments = $util.emptyArray;
            ActivityMMOBagData.prototype.total_gain_count = 0;
            ActivityMMOBagData.prototype.total_fusion_count = 0;
            ActivityMMOBagData.prototype.random_record = $util.emptyArray;

            ActivityMMOBagData.create = function create(properties) {
                return new ActivityMMOBagData(properties);
            };

            ActivityMMOBagData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.equipments != null && message.equipments.length)
                    for (let i = 0; i < message.equipments.length; ++i)
                        $root.lq.ActivityMMOEquipmentData.encode(message.equipments[i], writer.uint32(10).fork()).ldelim();
                if (message.total_gain_count != null && Object.hasOwnProperty.call(message, "total_gain_count"))
                    writer.uint32(16).uint32(message.total_gain_count);
                if (message.total_fusion_count != null && Object.hasOwnProperty.call(message, "total_fusion_count"))
                    writer.uint32(24).uint32(message.total_fusion_count);
                if (message.random_record != null && message.random_record.length)
                    for (let i = 0; i < message.random_record.length; ++i)
                        $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord.encode(message.random_record[i], writer.uint32(802).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOBagData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOBagData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOBagData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            message.equipments.push($root.lq.ActivityMMOEquipmentData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_gain_count = value;
                            else
                                delete message.total_gain_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_fusion_count = value;
                            else
                                delete message.total_fusion_count;
                            continue;
                        }
                    case 100: {
                            if (wireType !== 2)
                                break;
                            if (!(message.random_record && message.random_record.length))
                                message.random_record = [];
                            message.random_record.push($root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOBagData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOBagData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.equipments != null && message.hasOwnProperty("equipments")) {
                    if (!Array.isArray(message.equipments))
                        return "equipments: array expected";
                    for (let i = 0; i < message.equipments.length; ++i) {
                        let error = $root.lq.ActivityMMOEquipmentData.verify(message.equipments[i], _depth + 1);
                        if (error)
                            return "equipments." + error;
                    }
                }
                if (message.total_gain_count != null && message.hasOwnProperty("total_gain_count"))
                    if (!$util.isInteger(message.total_gain_count))
                        return "total_gain_count: integer expected";
                if (message.total_fusion_count != null && message.hasOwnProperty("total_fusion_count"))
                    if (!$util.isInteger(message.total_fusion_count))
                        return "total_fusion_count: integer expected";
                if (message.random_record != null && message.hasOwnProperty("random_record")) {
                    if (!Array.isArray(message.random_record))
                        return "random_record: array expected";
                    for (let i = 0; i < message.random_record.length; ++i) {
                        let error = $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord.verify(message.random_record[i], _depth + 1);
                        if (error)
                            return "random_record." + error;
                    }
                }
                return null;
            };

            ActivityMMOBagData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMOData.ActivityMMOBagData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMOData.ActivityMMOBagData();
                if (object.equipments) {
                    if (!Array.isArray(object.equipments))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOBagData.equipments: array expected");
                    message.equipments = Array(object.equipments.length);
                    for (let i = 0; i < object.equipments.length; ++i) {
                        if (typeof object.equipments[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOBagData.equipments: object expected");
                        message.equipments[i] = $root.lq.ActivityMMOEquipmentData.fromObject(object.equipments[i], _depth + 1);
                    }
                }
                if (object.total_gain_count != null)
                    if (Number(object.total_gain_count) !== 0)
                        message.total_gain_count = object.total_gain_count >>> 0;
                if (object.total_fusion_count != null)
                    if (Number(object.total_fusion_count) !== 0)
                        message.total_fusion_count = object.total_fusion_count >>> 0;
                if (object.random_record) {
                    if (!Array.isArray(object.random_record))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOBagData.random_record: array expected");
                    message.random_record = Array(object.random_record.length);
                    for (let i = 0; i < object.random_record.length; ++i) {
                        if (typeof object.random_record[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOBagData.random_record: object expected");
                        message.random_record[i] = $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord.fromObject(object.random_record[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityMMOBagData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.equipments = [];
                    object.random_record = [];
                }
                if (options.defaults) {
                    object.total_gain_count = 0;
                    object.total_fusion_count = 0;
                }
                if (message.equipments && message.equipments.length) {
                    object.equipments = Array(message.equipments.length);
                    for (let j = 0; j < message.equipments.length; ++j)
                        object.equipments[j] = $root.lq.ActivityMMOEquipmentData.toObject(message.equipments[j], options);
                }
                if (message.total_gain_count != null && message.hasOwnProperty("total_gain_count"))
                    object.total_gain_count = message.total_gain_count;
                if (message.total_fusion_count != null && message.hasOwnProperty("total_fusion_count"))
                    object.total_fusion_count = message.total_fusion_count;
                if (message.random_record && message.random_record.length) {
                    object.random_record = Array(message.random_record.length);
                    for (let j = 0; j < message.random_record.length; ++j)
                        object.random_record[j] = $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord.toObject(message.random_record[j], options);
                }
                return object;
            };

            ActivityMMOBagData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOBagData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMOData.ActivityMMOBagData";
            };

            ActivityMMOBagData.ActivityMMORandomRecord = (function() {

                function ActivityMMORandomRecord(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                ActivityMMORandomRecord.prototype.rarity = 0;
                ActivityMMORandomRecord.prototype.count = 0;

                ActivityMMORandomRecord.create = function create(properties) {
                    return new ActivityMMORandomRecord(properties);
                };

                ActivityMMORandomRecord.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.rarity != null && Object.hasOwnProperty.call(message, "rarity"))
                        writer.uint32(8).uint32(message.rarity);
                    if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                        writer.uint32(16).uint32(message.count);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                ActivityMMORandomRecord.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                ActivityMMORandomRecord.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.rarity = value;
                                else
                                    delete message.rarity;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.count = value;
                                else
                                    delete message.count;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                ActivityMMORandomRecord.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                ActivityMMORandomRecord.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.rarity != null && message.hasOwnProperty("rarity"))
                        if (!$util.isInteger(message.rarity))
                            return "rarity: integer expected";
                    if (message.count != null && message.hasOwnProperty("count"))
                        if (!$util.isInteger(message.count))
                            return "count: integer expected";
                    return null;
                };

                ActivityMMORandomRecord.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord();
                    if (object.rarity != null)
                        if (Number(object.rarity) !== 0)
                            message.rarity = object.rarity >>> 0;
                    if (object.count != null)
                        if (Number(object.count) !== 0)
                            message.count = object.count >>> 0;
                    return message;
                };

                ActivityMMORandomRecord.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.rarity = 0;
                        object.count = 0;
                    }
                    if (message.rarity != null && message.hasOwnProperty("rarity"))
                        object.rarity = message.rarity;
                    if (message.count != null && message.hasOwnProperty("count"))
                        object.count = message.count;
                    return object;
                };

                ActivityMMORandomRecord.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                ActivityMMORandomRecord.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ActivityMMOData.ActivityMMOBagData.ActivityMMORandomRecord";
                };

                return ActivityMMORandomRecord;
            })();

            return ActivityMMOBagData;
        })();

        ActivityMMOData.ActivityMMOMapData = (function() {

            function ActivityMMOMapData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOMapData.prototype.level = 0;
            ActivityMMOMapData.prototype.random_seed = 0;
            ActivityMMOMapData.prototype.won = 0;
            ActivityMMOMapData.prototype.failed = 0;

            ActivityMMOMapData.create = function create(properties) {
                return new ActivityMMOMapData(properties);
            };

            ActivityMMOMapData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(8).uint32(message.level);
                if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                    writer.uint32(16).uint32(message.random_seed);
                if (message.won != null && Object.hasOwnProperty.call(message, "won"))
                    writer.uint32(24).uint32(message.won);
                if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                    writer.uint32(32).uint32(message.failed);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOMapData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOMapData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOMapData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.random_seed = value;
                            else
                                delete message.random_seed;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.won = value;
                            else
                                delete message.won;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.failed = value;
                            else
                                delete message.failed;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOMapData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOMapData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                    if (!$util.isInteger(message.random_seed))
                        return "random_seed: integer expected";
                if (message.won != null && message.hasOwnProperty("won"))
                    if (!$util.isInteger(message.won))
                        return "won: integer expected";
                if (message.failed != null && message.hasOwnProperty("failed"))
                    if (!$util.isInteger(message.failed))
                        return "failed: integer expected";
                return null;
            };

            ActivityMMOMapData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMOData.ActivityMMOMapData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMOData.ActivityMMOMapData();
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.random_seed != null)
                    if (Number(object.random_seed) !== 0)
                        message.random_seed = object.random_seed >>> 0;
                if (object.won != null)
                    if (Number(object.won) !== 0)
                        message.won = object.won >>> 0;
                if (object.failed != null)
                    if (Number(object.failed) !== 0)
                        message.failed = object.failed >>> 0;
                return message;
            };

            ActivityMMOMapData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level = 0;
                    object.random_seed = 0;
                    object.won = 0;
                    object.failed = 0;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                    object.random_seed = message.random_seed;
                if (message.won != null && message.hasOwnProperty("won"))
                    object.won = message.won;
                if (message.failed != null && message.hasOwnProperty("failed"))
                    object.failed = message.failed;
                return object;
            };

            ActivityMMOMapData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOMapData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMOData.ActivityMMOMapData";
            };

            return ActivityMMOMapData;
        })();

        ActivityMMOData.ActivityMMOSupportData = (function() {

            function ActivityMMOSupportData(properties) {
                this.support_record = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityMMOSupportData.prototype.last_receive_time = 0;
            ActivityMMOSupportData.prototype.most_supporter = null;
            ActivityMMOSupportData.prototype.receive_support_count = 0;
            ActivityMMOSupportData.prototype.support_record = $util.emptyArray;

            ActivityMMOSupportData.create = function create(properties) {
                return new ActivityMMOSupportData(properties);
            };

            ActivityMMOSupportData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.last_receive_time != null && Object.hasOwnProperty.call(message, "last_receive_time"))
                    writer.uint32(8).uint32(message.last_receive_time);
                if (message.most_supporter != null && Object.hasOwnProperty.call(message, "most_supporter"))
                    $root.lq.ActivityMMOSupportRecord.encode(message.most_supporter, writer.uint32(18).fork()).ldelim();
                if (message.receive_support_count != null && Object.hasOwnProperty.call(message, "receive_support_count"))
                    writer.uint32(24).uint32(message.receive_support_count);
                if (message.support_record != null && message.support_record.length)
                    for (let i = 0; i < message.support_record.length; ++i)
                        $root.lq.ActivityMMOSupportRecord.encode(message.support_record[i], writer.uint32(802).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityMMOSupportData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityMMOSupportData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMMOData.ActivityMMOSupportData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_receive_time = value;
                            else
                                delete message.last_receive_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.most_supporter = $root.lq.ActivityMMOSupportRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.most_supporter);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.receive_support_count = value;
                            else
                                delete message.receive_support_count;
                            continue;
                        }
                    case 100: {
                            if (wireType !== 2)
                                break;
                            if (!(message.support_record && message.support_record.length))
                                message.support_record = [];
                            message.support_record.push($root.lq.ActivityMMOSupportRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityMMOSupportData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityMMOSupportData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.last_receive_time != null && message.hasOwnProperty("last_receive_time"))
                    if (!$util.isInteger(message.last_receive_time))
                        return "last_receive_time: integer expected";
                if (message.most_supporter != null && message.hasOwnProperty("most_supporter")) {
                    let error = $root.lq.ActivityMMOSupportRecord.verify(message.most_supporter, _depth + 1);
                    if (error)
                        return "most_supporter." + error;
                }
                if (message.receive_support_count != null && message.hasOwnProperty("receive_support_count"))
                    if (!$util.isInteger(message.receive_support_count))
                        return "receive_support_count: integer expected";
                if (message.support_record != null && message.hasOwnProperty("support_record")) {
                    if (!Array.isArray(message.support_record))
                        return "support_record: array expected";
                    for (let i = 0; i < message.support_record.length; ++i) {
                        let error = $root.lq.ActivityMMOSupportRecord.verify(message.support_record[i], _depth + 1);
                        if (error)
                            return "support_record." + error;
                    }
                }
                return null;
            };

            ActivityMMOSupportData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMMOData.ActivityMMOSupportData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMMOData.ActivityMMOSupportData();
                if (object.last_receive_time != null)
                    if (Number(object.last_receive_time) !== 0)
                        message.last_receive_time = object.last_receive_time >>> 0;
                if (object.most_supporter != null) {
                    if (typeof object.most_supporter !== "object")
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOSupportData.most_supporter: object expected");
                    message.most_supporter = $root.lq.ActivityMMOSupportRecord.fromObject(object.most_supporter, _depth + 1);
                }
                if (object.receive_support_count != null)
                    if (Number(object.receive_support_count) !== 0)
                        message.receive_support_count = object.receive_support_count >>> 0;
                if (object.support_record) {
                    if (!Array.isArray(object.support_record))
                        throw TypeError(".lq.ActivityMMOData.ActivityMMOSupportData.support_record: array expected");
                    message.support_record = Array(object.support_record.length);
                    for (let i = 0; i < object.support_record.length; ++i) {
                        if (typeof object.support_record[i] !== "object")
                            throw TypeError(".lq.ActivityMMOData.ActivityMMOSupportData.support_record: object expected");
                        message.support_record[i] = $root.lq.ActivityMMOSupportRecord.fromObject(object.support_record[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityMMOSupportData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.support_record = [];
                if (options.defaults) {
                    object.last_receive_time = 0;
                    object.most_supporter = null;
                    object.receive_support_count = 0;
                }
                if (message.last_receive_time != null && message.hasOwnProperty("last_receive_time"))
                    object.last_receive_time = message.last_receive_time;
                if (message.most_supporter != null && message.hasOwnProperty("most_supporter"))
                    object.most_supporter = $root.lq.ActivityMMOSupportRecord.toObject(message.most_supporter, options);
                if (message.receive_support_count != null && message.hasOwnProperty("receive_support_count"))
                    object.receive_support_count = message.receive_support_count;
                if (message.support_record && message.support_record.length) {
                    object.support_record = Array(message.support_record.length);
                    for (let j = 0; j < message.support_record.length; ++j)
                        object.support_record[j] = $root.lq.ActivityMMOSupportRecord.toObject(message.support_record[j], options);
                }
                return object;
            };

            ActivityMMOSupportData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityMMOSupportData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMMOData.ActivityMMOSupportData";
            };

            return ActivityMMOSupportData;
        })();

        return ActivityMMOData;
    })();

    lq.ActivityFriendGiftData = (function() {

        function ActivityFriendGiftData(properties) {
            this.gift_inbox = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityFriendGiftData.prototype.activity_id = 0;
        ActivityFriendGiftData.prototype.max_inbox_id = 0;
        ActivityFriendGiftData.prototype.receive_data = null;
        ActivityFriendGiftData.prototype.send_data = null;
        ActivityFriendGiftData.prototype.gift_inbox = $util.emptyArray;

        ActivityFriendGiftData.create = function create(properties) {
            return new ActivityFriendGiftData(properties);
        };

        ActivityFriendGiftData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.max_inbox_id != null && Object.hasOwnProperty.call(message, "max_inbox_id"))
                writer.uint32(16).uint32(message.max_inbox_id);
            if (message.receive_data != null && Object.hasOwnProperty.call(message, "receive_data"))
                $root.lq.ActivityFriendGiftData.CountWithTimeData.encode(message.receive_data, writer.uint32(26).fork()).ldelim();
            if (message.send_data != null && Object.hasOwnProperty.call(message, "send_data"))
                $root.lq.ActivityFriendGiftData.CountWithTimeData.encode(message.send_data, writer.uint32(34).fork()).ldelim();
            if (message.gift_inbox != null && message.gift_inbox.length)
                for (let i = 0; i < message.gift_inbox.length; ++i)
                    $root.lq.ActivityFriendGiftData.GiftBoxData.encode(message.gift_inbox[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityFriendGiftData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityFriendGiftData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFriendGiftData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_inbox_id = value;
                        else
                            delete message.max_inbox_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.receive_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.receive_data);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.send_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.send_data);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gift_inbox && message.gift_inbox.length))
                            message.gift_inbox = [];
                        message.gift_inbox.push($root.lq.ActivityFriendGiftData.GiftBoxData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityFriendGiftData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityFriendGiftData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.max_inbox_id != null && message.hasOwnProperty("max_inbox_id"))
                if (!$util.isInteger(message.max_inbox_id))
                    return "max_inbox_id: integer expected";
            if (message.receive_data != null && message.hasOwnProperty("receive_data")) {
                let error = $root.lq.ActivityFriendGiftData.CountWithTimeData.verify(message.receive_data, _depth + 1);
                if (error)
                    return "receive_data." + error;
            }
            if (message.send_data != null && message.hasOwnProperty("send_data")) {
                let error = $root.lq.ActivityFriendGiftData.CountWithTimeData.verify(message.send_data, _depth + 1);
                if (error)
                    return "send_data." + error;
            }
            if (message.gift_inbox != null && message.hasOwnProperty("gift_inbox")) {
                if (!Array.isArray(message.gift_inbox))
                    return "gift_inbox: array expected";
                for (let i = 0; i < message.gift_inbox.length; ++i) {
                    let error = $root.lq.ActivityFriendGiftData.GiftBoxData.verify(message.gift_inbox[i], _depth + 1);
                    if (error)
                        return "gift_inbox." + error;
                }
            }
            return null;
        };

        ActivityFriendGiftData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityFriendGiftData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityFriendGiftData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.max_inbox_id != null)
                if (Number(object.max_inbox_id) !== 0)
                    message.max_inbox_id = object.max_inbox_id >>> 0;
            if (object.receive_data != null) {
                if (typeof object.receive_data !== "object")
                    throw TypeError(".lq.ActivityFriendGiftData.receive_data: object expected");
                message.receive_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.fromObject(object.receive_data, _depth + 1);
            }
            if (object.send_data != null) {
                if (typeof object.send_data !== "object")
                    throw TypeError(".lq.ActivityFriendGiftData.send_data: object expected");
                message.send_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.fromObject(object.send_data, _depth + 1);
            }
            if (object.gift_inbox) {
                if (!Array.isArray(object.gift_inbox))
                    throw TypeError(".lq.ActivityFriendGiftData.gift_inbox: array expected");
                message.gift_inbox = Array(object.gift_inbox.length);
                for (let i = 0; i < object.gift_inbox.length; ++i) {
                    if (typeof object.gift_inbox[i] !== "object")
                        throw TypeError(".lq.ActivityFriendGiftData.gift_inbox: object expected");
                    message.gift_inbox[i] = $root.lq.ActivityFriendGiftData.GiftBoxData.fromObject(object.gift_inbox[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityFriendGiftData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gift_inbox = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.max_inbox_id = 0;
                object.receive_data = null;
                object.send_data = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.max_inbox_id != null && message.hasOwnProperty("max_inbox_id"))
                object.max_inbox_id = message.max_inbox_id;
            if (message.receive_data != null && message.hasOwnProperty("receive_data"))
                object.receive_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.toObject(message.receive_data, options);
            if (message.send_data != null && message.hasOwnProperty("send_data"))
                object.send_data = $root.lq.ActivityFriendGiftData.CountWithTimeData.toObject(message.send_data, options);
            if (message.gift_inbox && message.gift_inbox.length) {
                object.gift_inbox = Array(message.gift_inbox.length);
                for (let j = 0; j < message.gift_inbox.length; ++j)
                    object.gift_inbox[j] = $root.lq.ActivityFriendGiftData.GiftBoxData.toObject(message.gift_inbox[j], options);
            }
            return object;
        };

        ActivityFriendGiftData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityFriendGiftData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityFriendGiftData";
        };

        ActivityFriendGiftData.CountWithTimeData = (function() {

            function CountWithTimeData(properties) {
                this.send_friend_id = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CountWithTimeData.prototype.count = 0;
            CountWithTimeData.prototype.last_update_time = 0;
            CountWithTimeData.prototype.send_friend_id = $util.emptyArray;

            CountWithTimeData.create = function create(properties) {
                return new CountWithTimeData(properties);
            };

            CountWithTimeData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.last_update_time != null && Object.hasOwnProperty.call(message, "last_update_time"))
                    writer.uint32(16).uint32(message.last_update_time);
                if (message.send_friend_id != null && message.send_friend_id.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.send_friend_id.length; ++i)
                        writer.uint32(message.send_friend_id[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CountWithTimeData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CountWithTimeData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFriendGiftData.CountWithTimeData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_update_time = value;
                            else
                                delete message.last_update_time;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.send_friend_id && message.send_friend_id.length))
                                    message.send_friend_id = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.send_friend_id.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.send_friend_id && message.send_friend_id.length))
                                message.send_friend_id = [];
                            message.send_friend_id.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CountWithTimeData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CountWithTimeData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    if (!$util.isInteger(message.last_update_time))
                        return "last_update_time: integer expected";
                if (message.send_friend_id != null && message.hasOwnProperty("send_friend_id")) {
                    if (!Array.isArray(message.send_friend_id))
                        return "send_friend_id: array expected";
                    for (let i = 0; i < message.send_friend_id.length; ++i)
                        if (!$util.isInteger(message.send_friend_id[i]))
                            return "send_friend_id: integer[] expected";
                }
                return null;
            };

            CountWithTimeData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityFriendGiftData.CountWithTimeData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityFriendGiftData.CountWithTimeData();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.last_update_time != null)
                    if (Number(object.last_update_time) !== 0)
                        message.last_update_time = object.last_update_time >>> 0;
                if (object.send_friend_id) {
                    if (!Array.isArray(object.send_friend_id))
                        throw TypeError(".lq.ActivityFriendGiftData.CountWithTimeData.send_friend_id: array expected");
                    message.send_friend_id = Array(object.send_friend_id.length);
                    for (let i = 0; i < object.send_friend_id.length; ++i)
                        message.send_friend_id[i] = object.send_friend_id[i] >>> 0;
                }
                return message;
            };

            CountWithTimeData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.send_friend_id = [];
                if (options.defaults) {
                    object.count = 0;
                    object.last_update_time = 0;
                }
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.last_update_time != null && message.hasOwnProperty("last_update_time"))
                    object.last_update_time = message.last_update_time;
                if (message.send_friend_id && message.send_friend_id.length) {
                    object.send_friend_id = Array(message.send_friend_id.length);
                    for (let j = 0; j < message.send_friend_id.length; ++j)
                        object.send_friend_id[j] = message.send_friend_id[j];
                }
                return object;
            };

            CountWithTimeData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CountWithTimeData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityFriendGiftData.CountWithTimeData";
            };

            return CountWithTimeData;
        })();

        ActivityFriendGiftData.GiftBoxData = (function() {

            function GiftBoxData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GiftBoxData.prototype.id = 0;
            GiftBoxData.prototype.item_id = 0;
            GiftBoxData.prototype.count = 0;
            GiftBoxData.prototype.from_account_id = 0;
            GiftBoxData.prototype.time = 0;
            GiftBoxData.prototype.received = 0;

            GiftBoxData.create = function create(properties) {
                return new GiftBoxData(properties);
            };

            GiftBoxData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(16).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(24).uint32(message.count);
                if (message.from_account_id != null && Object.hasOwnProperty.call(message, "from_account_id"))
                    writer.uint32(32).uint32(message.from_account_id);
                if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                    writer.uint32(40).uint32(message.time);
                if (message.received != null && Object.hasOwnProperty.call(message, "received"))
                    writer.uint32(48).uint32(message.received);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GiftBoxData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GiftBoxData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityFriendGiftData.GiftBoxData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from_account_id = value;
                            else
                                delete message.from_account_id;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.received = value;
                            else
                                delete message.received;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GiftBoxData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GiftBoxData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    if (!$util.isInteger(message.from_account_id))
                        return "from_account_id: integer expected";
                if (message.time != null && message.hasOwnProperty("time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                if (message.received != null && message.hasOwnProperty("received"))
                    if (!$util.isInteger(message.received))
                        return "received: integer expected";
                return null;
            };

            GiftBoxData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityFriendGiftData.GiftBoxData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityFriendGiftData.GiftBoxData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.from_account_id != null)
                    if (Number(object.from_account_id) !== 0)
                        message.from_account_id = object.from_account_id >>> 0;
                if (object.time != null)
                    if (Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                if (object.received != null)
                    if (Number(object.received) !== 0)
                        message.received = object.received >>> 0;
                return message;
            };

            GiftBoxData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.item_id = 0;
                    object.count = 0;
                    object.from_account_id = 0;
                    object.time = 0;
                    object.received = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    object.from_account_id = message.from_account_id;
                if (message.time != null && message.hasOwnProperty("time"))
                    object.time = message.time;
                if (message.received != null && message.hasOwnProperty("received"))
                    object.received = message.received;
                return object;
            };

            GiftBoxData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GiftBoxData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityFriendGiftData.GiftBoxData";
            };

            return GiftBoxData;
        })();

        return ActivityFriendGiftData;
    })();

    lq.ActivityUpgradeData = (function() {

        function ActivityUpgradeData(properties) {
            this.groups = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityUpgradeData.prototype.activity_id = 0;
        ActivityUpgradeData.prototype.groups = $util.emptyArray;
        ActivityUpgradeData.prototype.received_level = 0;

        ActivityUpgradeData.create = function create(properties) {
            return new ActivityUpgradeData(properties);
        };

        ActivityUpgradeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.groups != null && message.groups.length)
                for (let i = 0; i < message.groups.length; ++i)
                    $root.lq.ActivityUpgradeData.LevelGroup.encode(message.groups[i], writer.uint32(18).fork()).ldelim();
            if (message.received_level != null && Object.hasOwnProperty.call(message, "received_level"))
                writer.uint32(24).uint32(message.received_level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityUpgradeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityUpgradeData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityUpgradeData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.groups && message.groups.length))
                            message.groups = [];
                        message.groups.push($root.lq.ActivityUpgradeData.LevelGroup.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.received_level = value;
                        else
                            delete message.received_level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityUpgradeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityUpgradeData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.groups != null && message.hasOwnProperty("groups")) {
                if (!Array.isArray(message.groups))
                    return "groups: array expected";
                for (let i = 0; i < message.groups.length; ++i) {
                    let error = $root.lq.ActivityUpgradeData.LevelGroup.verify(message.groups[i], _depth + 1);
                    if (error)
                        return "groups." + error;
                }
            }
            if (message.received_level != null && message.hasOwnProperty("received_level"))
                if (!$util.isInteger(message.received_level))
                    return "received_level: integer expected";
            return null;
        };

        ActivityUpgradeData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityUpgradeData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityUpgradeData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.groups) {
                if (!Array.isArray(object.groups))
                    throw TypeError(".lq.ActivityUpgradeData.groups: array expected");
                message.groups = Array(object.groups.length);
                for (let i = 0; i < object.groups.length; ++i) {
                    if (typeof object.groups[i] !== "object")
                        throw TypeError(".lq.ActivityUpgradeData.groups: object expected");
                    message.groups[i] = $root.lq.ActivityUpgradeData.LevelGroup.fromObject(object.groups[i], _depth + 1);
                }
            }
            if (object.received_level != null)
                if (Number(object.received_level) !== 0)
                    message.received_level = object.received_level >>> 0;
            return message;
        };

        ActivityUpgradeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.groups = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.received_level = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.groups && message.groups.length) {
                object.groups = Array(message.groups.length);
                for (let j = 0; j < message.groups.length; ++j)
                    object.groups[j] = $root.lq.ActivityUpgradeData.LevelGroup.toObject(message.groups[j], options);
            }
            if (message.received_level != null && message.hasOwnProperty("received_level"))
                object.received_level = message.received_level;
            return object;
        };

        ActivityUpgradeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityUpgradeData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityUpgradeData";
        };

        ActivityUpgradeData.LevelGroup = (function() {

            function LevelGroup(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LevelGroup.prototype.group_id = 0;
            LevelGroup.prototype.level = 0;

            LevelGroup.create = function create(properties) {
                return new LevelGroup(properties);
            };

            LevelGroup.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.group_id != null && Object.hasOwnProperty.call(message, "group_id"))
                    writer.uint32(8).uint32(message.group_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LevelGroup.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LevelGroup.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityUpgradeData.LevelGroup(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.group_id = value;
                            else
                                delete message.group_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LevelGroup.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LevelGroup.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.group_id != null && message.hasOwnProperty("group_id"))
                    if (!$util.isInteger(message.group_id))
                        return "group_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                return null;
            };

            LevelGroup.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityUpgradeData.LevelGroup)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityUpgradeData.LevelGroup();
                if (object.group_id != null)
                    if (Number(object.group_id) !== 0)
                        message.group_id = object.group_id >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                return message;
            };

            LevelGroup.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.group_id = 0;
                    object.level = 0;
                }
                if (message.group_id != null && message.hasOwnProperty("group_id"))
                    object.group_id = message.group_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                return object;
            };

            LevelGroup.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LevelGroup.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityUpgradeData.LevelGroup";
            };

            return LevelGroup;
        })();

        return ActivityUpgradeData;
    })();

    lq.GachaRecord = (function() {

        function GachaRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GachaRecord.prototype.id = 0;
        GachaRecord.prototype.count = 0;

        GachaRecord.create = function create(properties) {
            return new GachaRecord(properties);
        };

        GachaRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GachaRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GachaRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GachaRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GachaRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GachaRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        GachaRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GachaRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GachaRecord();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        GachaRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        GachaRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GachaRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GachaRecord";
        };

        return GachaRecord;
    })();

    lq.ActivityGachaData = (function() {

        function ActivityGachaData(properties) {
            this.gained = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityGachaData.prototype.activity_id = 0;
        ActivityGachaData.prototype.gained = $util.emptyArray;

        ActivityGachaData.create = function create(properties) {
            return new ActivityGachaData(properties);
        };

        ActivityGachaData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.gained != null && message.gained.length)
                for (let i = 0; i < message.gained.length; ++i)
                    $root.lq.GachaRecord.encode(message.gained[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityGachaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityGachaData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityGachaData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gained && message.gained.length))
                            message.gained = [];
                        message.gained.push($root.lq.GachaRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityGachaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityGachaData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.gained != null && message.hasOwnProperty("gained")) {
                if (!Array.isArray(message.gained))
                    return "gained: array expected";
                for (let i = 0; i < message.gained.length; ++i) {
                    let error = $root.lq.GachaRecord.verify(message.gained[i], _depth + 1);
                    if (error)
                        return "gained." + error;
                }
            }
            return null;
        };

        ActivityGachaData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityGachaData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityGachaData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.gained) {
                if (!Array.isArray(object.gained))
                    throw TypeError(".lq.ActivityGachaData.gained: array expected");
                message.gained = Array(object.gained.length);
                for (let i = 0; i < object.gained.length; ++i) {
                    if (typeof object.gained[i] !== "object")
                        throw TypeError(".lq.ActivityGachaData.gained: object expected");
                    message.gained[i] = $root.lq.GachaRecord.fromObject(object.gained[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityGachaData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gained = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.gained && message.gained.length) {
                object.gained = Array(message.gained.length);
                for (let j = 0; j < message.gained.length; ++j)
                    object.gained[j] = $root.lq.GachaRecord.toObject(message.gained[j], options);
            }
            return object;
        };

        ActivityGachaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityGachaData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityGachaData";
        };

        return ActivityGachaData;
    })();

    lq.ActivityGachaUpdateData = (function() {

        function ActivityGachaUpdateData(properties) {
            this.gained = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityGachaUpdateData.prototype.activity_id = 0;
        ActivityGachaUpdateData.prototype.gained = $util.emptyArray;
        ActivityGachaUpdateData.prototype.remain_count = 0;

        ActivityGachaUpdateData.create = function create(properties) {
            return new ActivityGachaUpdateData(properties);
        };

        ActivityGachaUpdateData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.gained != null && message.gained.length)
                for (let i = 0; i < message.gained.length; ++i)
                    $root.lq.GachaRecord.encode(message.gained[i], writer.uint32(18).fork()).ldelim();
            if (message.remain_count != null && Object.hasOwnProperty.call(message, "remain_count"))
                writer.uint32(24).uint32(message.remain_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityGachaUpdateData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityGachaUpdateData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityGachaUpdateData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gained && message.gained.length))
                            message.gained = [];
                        message.gained.push($root.lq.GachaRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain_count = value;
                        else
                            delete message.remain_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityGachaUpdateData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityGachaUpdateData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.gained != null && message.hasOwnProperty("gained")) {
                if (!Array.isArray(message.gained))
                    return "gained: array expected";
                for (let i = 0; i < message.gained.length; ++i) {
                    let error = $root.lq.GachaRecord.verify(message.gained[i], _depth + 1);
                    if (error)
                        return "gained." + error;
                }
            }
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                if (!$util.isInteger(message.remain_count))
                    return "remain_count: integer expected";
            return null;
        };

        ActivityGachaUpdateData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityGachaUpdateData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityGachaUpdateData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.gained) {
                if (!Array.isArray(object.gained))
                    throw TypeError(".lq.ActivityGachaUpdateData.gained: array expected");
                message.gained = Array(object.gained.length);
                for (let i = 0; i < object.gained.length; ++i) {
                    if (typeof object.gained[i] !== "object")
                        throw TypeError(".lq.ActivityGachaUpdateData.gained: object expected");
                    message.gained[i] = $root.lq.GachaRecord.fromObject(object.gained[i], _depth + 1);
                }
            }
            if (object.remain_count != null)
                if (Number(object.remain_count) !== 0)
                    message.remain_count = object.remain_count >>> 0;
            return message;
        };

        ActivityGachaUpdateData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gained = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.remain_count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.gained && message.gained.length) {
                object.gained = Array(message.gained.length);
                for (let j = 0; j < message.gained.length; ++j)
                    object.gained[j] = $root.lq.GachaRecord.toObject(message.gained[j], options);
            }
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                object.remain_count = message.remain_count;
            return object;
        };

        ActivityGachaUpdateData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityGachaUpdateData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityGachaUpdateData";
        };

        return ActivityGachaUpdateData;
    })();

    lq.ActivitySimulationGameRecordMessage = (function() {

        function ActivitySimulationGameRecordMessage(properties) {
            this.args = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySimulationGameRecordMessage.prototype.type = 0;
        ActivitySimulationGameRecordMessage.prototype.args = $util.emptyArray;
        ActivitySimulationGameRecordMessage.prototype.xun = 0;

        ActivitySimulationGameRecordMessage.create = function create(properties) {
            return new ActivitySimulationGameRecordMessage(properties);
        };

        ActivitySimulationGameRecordMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.args != null && message.args.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.args.length; ++i)
                    writer.uint32(message.args[i]);
                writer.ldelim();
            }
            if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                writer.uint32(24).uint32(message.xun);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySimulationGameRecordMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySimulationGameRecordMessage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySimulationGameRecordMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.args && message.args.length))
                                message.args = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.args.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.args && message.args.length))
                            message.args = [];
                        message.args.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun = value;
                        else
                            delete message.xun;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySimulationGameRecordMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySimulationGameRecordMessage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.args != null && message.hasOwnProperty("args")) {
                if (!Array.isArray(message.args))
                    return "args: array expected";
                for (let i = 0; i < message.args.length; ++i)
                    if (!$util.isInteger(message.args[i]))
                        return "args: integer[] expected";
            }
            if (message.xun != null && message.hasOwnProperty("xun"))
                if (!$util.isInteger(message.xun))
                    return "xun: integer expected";
            return null;
        };

        ActivitySimulationGameRecordMessage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySimulationGameRecordMessage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySimulationGameRecordMessage();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.args) {
                if (!Array.isArray(object.args))
                    throw TypeError(".lq.ActivitySimulationGameRecordMessage.args: array expected");
                message.args = Array(object.args.length);
                for (let i = 0; i < object.args.length; ++i)
                    message.args[i] = object.args[i] >>> 0;
            }
            if (object.xun != null)
                if (Number(object.xun) !== 0)
                    message.xun = object.xun >>> 0;
            return message;
        };

        ActivitySimulationGameRecordMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.args = [];
            if (options.defaults) {
                object.type = 0;
                object.xun = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.args && message.args.length) {
                object.args = Array(message.args.length);
                for (let j = 0; j < message.args.length; ++j)
                    object.args[j] = message.args[j];
            }
            if (message.xun != null && message.hasOwnProperty("xun"))
                object.xun = message.xun;
            return object;
        };

        ActivitySimulationGameRecordMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySimulationGameRecordMessage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySimulationGameRecordMessage";
        };

        return ActivitySimulationGameRecordMessage;
    })();

    lq.ActivitySimulationGameRecord = (function() {

        function ActivitySimulationGameRecord(properties) {
            this.seats = [];
            this.scores = [];
            this.messages = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySimulationGameRecord.prototype.round = 0;
        ActivitySimulationGameRecord.prototype.seats = $util.emptyArray;
        ActivitySimulationGameRecord.prototype.uuid = "";
        ActivitySimulationGameRecord.prototype.start_time = 0;
        ActivitySimulationGameRecord.prototype.scores = $util.emptyArray;
        ActivitySimulationGameRecord.prototype.messages = $util.emptyArray;

        ActivitySimulationGameRecord.create = function create(properties) {
            return new ActivitySimulationGameRecord(properties);
        };

        ActivitySimulationGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(8).uint32(message.round);
            if (message.seats != null && message.seats.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.seats.length; ++i)
                    writer.uint32(message.seats[i]);
                writer.ldelim();
            }
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(26).string(message.uuid);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(32).uint32(message.start_time);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.messages != null && message.messages.length)
                for (let i = 0; i < message.messages.length; ++i)
                    $root.lq.ActivitySimulationGameRecordMessage.encode(message.messages[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySimulationGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySimulationGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySimulationGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.seats && message.seats.length))
                                message.seats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.seats.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.seats && message.seats.length))
                            message.seats = [];
                        message.seats.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.messages && message.messages.length))
                            message.messages = [];
                        message.messages.push($root.lq.ActivitySimulationGameRecordMessage.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySimulationGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySimulationGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.seats != null && message.hasOwnProperty("seats")) {
                if (!Array.isArray(message.seats))
                    return "seats: array expected";
                for (let i = 0; i < message.seats.length; ++i)
                    if (!$util.isInteger(message.seats[i]))
                        return "seats: integer[] expected";
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.messages != null && message.hasOwnProperty("messages")) {
                if (!Array.isArray(message.messages))
                    return "messages: array expected";
                for (let i = 0; i < message.messages.length; ++i) {
                    let error = $root.lq.ActivitySimulationGameRecordMessage.verify(message.messages[i], _depth + 1);
                    if (error)
                        return "messages." + error;
                }
            }
            return null;
        };

        ActivitySimulationGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySimulationGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySimulationGameRecord();
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.seats) {
                if (!Array.isArray(object.seats))
                    throw TypeError(".lq.ActivitySimulationGameRecord.seats: array expected");
                message.seats = Array(object.seats.length);
                for (let i = 0; i < object.seats.length; ++i)
                    message.seats[i] = object.seats[i] >>> 0;
            }
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActivitySimulationGameRecord.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.messages) {
                if (!Array.isArray(object.messages))
                    throw TypeError(".lq.ActivitySimulationGameRecord.messages: array expected");
                message.messages = Array(object.messages.length);
                for (let i = 0; i < object.messages.length; ++i) {
                    if (typeof object.messages[i] !== "object")
                        throw TypeError(".lq.ActivitySimulationGameRecord.messages: object expected");
                    message.messages[i] = $root.lq.ActivitySimulationGameRecordMessage.fromObject(object.messages[i], _depth + 1);
                }
            }
            return message;
        };

        ActivitySimulationGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.seats = [];
                object.scores = [];
                object.messages = [];
            }
            if (options.defaults) {
                object.round = 0;
                object.uuid = "";
                object.start_time = 0;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.seats && message.seats.length) {
                object.seats = Array(message.seats.length);
                for (let j = 0; j < message.seats.length; ++j)
                    object.seats[j] = message.seats[j];
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.messages && message.messages.length) {
                object.messages = Array(message.messages.length);
                for (let j = 0; j < message.messages.length; ++j)
                    object.messages[j] = $root.lq.ActivitySimulationGameRecordMessage.toObject(message.messages[j], options);
            }
            return object;
        };

        ActivitySimulationGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySimulationGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySimulationGameRecord";
        };

        return ActivitySimulationGameRecord;
    })();

    lq.ActivitySimulationDailyContest = (function() {

        function ActivitySimulationDailyContest(properties) {
            this.characters = [];
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySimulationDailyContest.prototype.day = 0;
        ActivitySimulationDailyContest.prototype.characters = $util.emptyArray;
        ActivitySimulationDailyContest.prototype.records = $util.emptyArray;
        ActivitySimulationDailyContest.prototype.round = 0;

        ActivitySimulationDailyContest.create = function create(properties) {
            return new ActivitySimulationDailyContest(properties);
        };

        ActivitySimulationDailyContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.day != null && Object.hasOwnProperty.call(message, "day"))
                writer.uint32(8).uint32(message.day);
            if (message.characters != null && message.characters.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.characters.length; ++i)
                    writer.uint32(message.characters[i]);
                writer.ldelim();
            }
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.lq.ActivitySimulationGameRecord.encode(message.records[i], writer.uint32(26).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(32).uint32(message.round);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySimulationDailyContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySimulationDailyContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySimulationDailyContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.day = value;
                        else
                            delete message.day;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.characters && message.characters.length))
                                message.characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.characters && message.characters.length))
                            message.characters = [];
                        message.characters.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.lq.ActivitySimulationGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySimulationDailyContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySimulationDailyContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.day != null && message.hasOwnProperty("day"))
                if (!$util.isInteger(message.day))
                    return "day: integer expected";
            if (message.characters != null && message.hasOwnProperty("characters")) {
                if (!Array.isArray(message.characters))
                    return "characters: array expected";
                for (let i = 0; i < message.characters.length; ++i)
                    if (!$util.isInteger(message.characters[i]))
                        return "characters: integer[] expected";
            }
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.lq.ActivitySimulationGameRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            return null;
        };

        ActivitySimulationDailyContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySimulationDailyContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySimulationDailyContest();
            if (object.day != null)
                if (Number(object.day) !== 0)
                    message.day = object.day >>> 0;
            if (object.characters) {
                if (!Array.isArray(object.characters))
                    throw TypeError(".lq.ActivitySimulationDailyContest.characters: array expected");
                message.characters = Array(object.characters.length);
                for (let i = 0; i < object.characters.length; ++i)
                    message.characters[i] = object.characters[i] >>> 0;
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.ActivitySimulationDailyContest.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".lq.ActivitySimulationDailyContest.records: object expected");
                    message.records[i] = $root.lq.ActivitySimulationGameRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            return message;
        };

        ActivitySimulationDailyContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.characters = [];
                object.records = [];
            }
            if (options.defaults) {
                object.day = 0;
                object.round = 0;
            }
            if (message.day != null && message.hasOwnProperty("day"))
                object.day = message.day;
            if (message.characters && message.characters.length) {
                object.characters = Array(message.characters.length);
                for (let j = 0; j < message.characters.length; ++j)
                    object.characters[j] = message.characters[j];
            }
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.lq.ActivitySimulationGameRecord.toObject(message.records[j], options);
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            return object;
        };

        ActivitySimulationDailyContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySimulationDailyContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySimulationDailyContest";
        };

        return ActivitySimulationDailyContest;
    })();

    lq.ActivitySimulationTrainRecord = (function() {

        function ActivitySimulationTrainRecord(properties) {
            this.modify_stats = [];
            this.final_stats = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySimulationTrainRecord.prototype.time = 0;
        ActivitySimulationTrainRecord.prototype.modify_stats = $util.emptyArray;
        ActivitySimulationTrainRecord.prototype.final_stats = $util.emptyArray;
        ActivitySimulationTrainRecord.prototype.type = 0;

        ActivitySimulationTrainRecord.create = function create(properties) {
            return new ActivitySimulationTrainRecord(properties);
        };

        ActivitySimulationTrainRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(8).uint32(message.time);
            if (message.modify_stats != null && message.modify_stats.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.modify_stats.length; ++i)
                    writer.int32(message.modify_stats[i]);
                writer.ldelim();
            }
            if (message.final_stats != null && message.final_stats.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.final_stats.length; ++i)
                    writer.uint32(message.final_stats[i]);
                writer.ldelim();
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(32).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySimulationTrainRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySimulationTrainRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySimulationTrainRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.modify_stats && message.modify_stats.length))
                                message.modify_stats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.modify_stats.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modify_stats && message.modify_stats.length))
                            message.modify_stats = [];
                        message.modify_stats.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.final_stats && message.final_stats.length))
                                message.final_stats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.final_stats.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.final_stats && message.final_stats.length))
                            message.final_stats = [];
                        message.final_stats.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySimulationTrainRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySimulationTrainRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            if (message.modify_stats != null && message.hasOwnProperty("modify_stats")) {
                if (!Array.isArray(message.modify_stats))
                    return "modify_stats: array expected";
                for (let i = 0; i < message.modify_stats.length; ++i)
                    if (!$util.isInteger(message.modify_stats[i]))
                        return "modify_stats: integer[] expected";
            }
            if (message.final_stats != null && message.hasOwnProperty("final_stats")) {
                if (!Array.isArray(message.final_stats))
                    return "final_stats: array expected";
                for (let i = 0; i < message.final_stats.length; ++i)
                    if (!$util.isInteger(message.final_stats[i]))
                        return "final_stats: integer[] expected";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ActivitySimulationTrainRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySimulationTrainRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySimulationTrainRecord();
            if (object.time != null)
                if (Number(object.time) !== 0)
                    message.time = object.time >>> 0;
            if (object.modify_stats) {
                if (!Array.isArray(object.modify_stats))
                    throw TypeError(".lq.ActivitySimulationTrainRecord.modify_stats: array expected");
                message.modify_stats = Array(object.modify_stats.length);
                for (let i = 0; i < object.modify_stats.length; ++i)
                    message.modify_stats[i] = object.modify_stats[i] | 0;
            }
            if (object.final_stats) {
                if (!Array.isArray(object.final_stats))
                    throw TypeError(".lq.ActivitySimulationTrainRecord.final_stats: array expected");
                message.final_stats = Array(object.final_stats.length);
                for (let i = 0; i < object.final_stats.length; ++i)
                    message.final_stats[i] = object.final_stats[i] >>> 0;
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ActivitySimulationTrainRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.modify_stats = [];
                object.final_stats = [];
            }
            if (options.defaults) {
                object.time = 0;
                object.type = 0;
            }
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            if (message.modify_stats && message.modify_stats.length) {
                object.modify_stats = Array(message.modify_stats.length);
                for (let j = 0; j < message.modify_stats.length; ++j)
                    object.modify_stats[j] = message.modify_stats[j];
            }
            if (message.final_stats && message.final_stats.length) {
                object.final_stats = Array(message.final_stats.length);
                for (let j = 0; j < message.final_stats.length; ++j)
                    object.final_stats[j] = message.final_stats[j];
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ActivitySimulationTrainRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySimulationTrainRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySimulationTrainRecord";
        };

        return ActivitySimulationTrainRecord;
    })();

    lq.ActivitySimulationData = (function() {

        function ActivitySimulationData(properties) {
            this.stats = [];
            this.daily_contest = [];
            this.train_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySimulationData.prototype.activity_id = 0;
        ActivitySimulationData.prototype.stats = $util.emptyArray;
        ActivitySimulationData.prototype.stamina_update_time = 0;
        ActivitySimulationData.prototype.daily_contest = $util.emptyArray;
        ActivitySimulationData.prototype.train_records = $util.emptyArray;

        ActivitySimulationData.create = function create(properties) {
            return new ActivitySimulationData(properties);
        };

        ActivitySimulationData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.stats != null && message.stats.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.stats.length; ++i)
                    writer.uint32(message.stats[i]);
                writer.ldelim();
            }
            if (message.stamina_update_time != null && Object.hasOwnProperty.call(message, "stamina_update_time"))
                writer.uint32(24).uint32(message.stamina_update_time);
            if (message.daily_contest != null && message.daily_contest.length)
                for (let i = 0; i < message.daily_contest.length; ++i)
                    $root.lq.ActivitySimulationDailyContest.encode(message.daily_contest[i], writer.uint32(34).fork()).ldelim();
            if (message.train_records != null && message.train_records.length)
                for (let i = 0; i < message.train_records.length; ++i)
                    $root.lq.ActivitySimulationTrainRecord.encode(message.train_records[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySimulationData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySimulationData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySimulationData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.stats && message.stats.length))
                                message.stats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.stats.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.stats && message.stats.length))
                            message.stats = [];
                        message.stats.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stamina_update_time = value;
                        else
                            delete message.stamina_update_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.daily_contest && message.daily_contest.length))
                            message.daily_contest = [];
                        message.daily_contest.push($root.lq.ActivitySimulationDailyContest.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.train_records && message.train_records.length))
                            message.train_records = [];
                        message.train_records.push($root.lq.ActivitySimulationTrainRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySimulationData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySimulationData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.stats != null && message.hasOwnProperty("stats")) {
                if (!Array.isArray(message.stats))
                    return "stats: array expected";
                for (let i = 0; i < message.stats.length; ++i)
                    if (!$util.isInteger(message.stats[i]))
                        return "stats: integer[] expected";
            }
            if (message.stamina_update_time != null && message.hasOwnProperty("stamina_update_time"))
                if (!$util.isInteger(message.stamina_update_time))
                    return "stamina_update_time: integer expected";
            if (message.daily_contest != null && message.hasOwnProperty("daily_contest")) {
                if (!Array.isArray(message.daily_contest))
                    return "daily_contest: array expected";
                for (let i = 0; i < message.daily_contest.length; ++i) {
                    let error = $root.lq.ActivitySimulationDailyContest.verify(message.daily_contest[i], _depth + 1);
                    if (error)
                        return "daily_contest." + error;
                }
            }
            if (message.train_records != null && message.hasOwnProperty("train_records")) {
                if (!Array.isArray(message.train_records))
                    return "train_records: array expected";
                for (let i = 0; i < message.train_records.length; ++i) {
                    let error = $root.lq.ActivitySimulationTrainRecord.verify(message.train_records[i], _depth + 1);
                    if (error)
                        return "train_records." + error;
                }
            }
            return null;
        };

        ActivitySimulationData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySimulationData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySimulationData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.stats) {
                if (!Array.isArray(object.stats))
                    throw TypeError(".lq.ActivitySimulationData.stats: array expected");
                message.stats = Array(object.stats.length);
                for (let i = 0; i < object.stats.length; ++i)
                    message.stats[i] = object.stats[i] >>> 0;
            }
            if (object.stamina_update_time != null)
                if (Number(object.stamina_update_time) !== 0)
                    message.stamina_update_time = object.stamina_update_time >>> 0;
            if (object.daily_contest) {
                if (!Array.isArray(object.daily_contest))
                    throw TypeError(".lq.ActivitySimulationData.daily_contest: array expected");
                message.daily_contest = Array(object.daily_contest.length);
                for (let i = 0; i < object.daily_contest.length; ++i) {
                    if (typeof object.daily_contest[i] !== "object")
                        throw TypeError(".lq.ActivitySimulationData.daily_contest: object expected");
                    message.daily_contest[i] = $root.lq.ActivitySimulationDailyContest.fromObject(object.daily_contest[i], _depth + 1);
                }
            }
            if (object.train_records) {
                if (!Array.isArray(object.train_records))
                    throw TypeError(".lq.ActivitySimulationData.train_records: array expected");
                message.train_records = Array(object.train_records.length);
                for (let i = 0; i < object.train_records.length; ++i) {
                    if (typeof object.train_records[i] !== "object")
                        throw TypeError(".lq.ActivitySimulationData.train_records: object expected");
                    message.train_records[i] = $root.lq.ActivitySimulationTrainRecord.fromObject(object.train_records[i], _depth + 1);
                }
            }
            return message;
        };

        ActivitySimulationData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.stats = [];
                object.daily_contest = [];
                object.train_records = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.stamina_update_time = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.stats && message.stats.length) {
                object.stats = Array(message.stats.length);
                for (let j = 0; j < message.stats.length; ++j)
                    object.stats[j] = message.stats[j];
            }
            if (message.stamina_update_time != null && message.hasOwnProperty("stamina_update_time"))
                object.stamina_update_time = message.stamina_update_time;
            if (message.daily_contest && message.daily_contest.length) {
                object.daily_contest = Array(message.daily_contest.length);
                for (let j = 0; j < message.daily_contest.length; ++j)
                    object.daily_contest[j] = $root.lq.ActivitySimulationDailyContest.toObject(message.daily_contest[j], options);
            }
            if (message.train_records && message.train_records.length) {
                object.train_records = Array(message.train_records.length);
                for (let j = 0; j < message.train_records.length; ++j)
                    object.train_records[j] = $root.lq.ActivitySimulationTrainRecord.toObject(message.train_records[j], options);
            }
            return object;
        };

        ActivitySimulationData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySimulationData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySimulationData";
        };

        return ActivitySimulationData;
    })();

    lq.ActivitySpotData = (function() {

        function ActivitySpotData(properties) {
            this.spots = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySpotData.prototype.activity_id = 0;
        ActivitySpotData.prototype.spots = $util.emptyArray;

        ActivitySpotData.create = function create(properties) {
            return new ActivitySpotData(properties);
        };

        ActivitySpotData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.spots != null && message.spots.length)
                for (let i = 0; i < message.spots.length; ++i)
                    $root.lq.ActivitySpotData.SpotData.encode(message.spots[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySpotData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySpotData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySpotData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.spots && message.spots.length))
                            message.spots = [];
                        message.spots.push($root.lq.ActivitySpotData.SpotData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySpotData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySpotData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.spots != null && message.hasOwnProperty("spots")) {
                if (!Array.isArray(message.spots))
                    return "spots: array expected";
                for (let i = 0; i < message.spots.length; ++i) {
                    let error = $root.lq.ActivitySpotData.SpotData.verify(message.spots[i], _depth + 1);
                    if (error)
                        return "spots." + error;
                }
            }
            return null;
        };

        ActivitySpotData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySpotData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySpotData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.spots) {
                if (!Array.isArray(object.spots))
                    throw TypeError(".lq.ActivitySpotData.spots: array expected");
                message.spots = Array(object.spots.length);
                for (let i = 0; i < object.spots.length; ++i) {
                    if (typeof object.spots[i] !== "object")
                        throw TypeError(".lq.ActivitySpotData.spots: object expected");
                    message.spots[i] = $root.lq.ActivitySpotData.SpotData.fromObject(object.spots[i], _depth + 1);
                }
            }
            return message;
        };

        ActivitySpotData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.spots = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.spots && message.spots.length) {
                object.spots = Array(message.spots.length);
                for (let j = 0; j < message.spots.length; ++j)
                    object.spots[j] = $root.lq.ActivitySpotData.SpotData.toObject(message.spots[j], options);
            }
            return object;
        };

        ActivitySpotData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySpotData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySpotData";
        };

        ActivitySpotData.SpotData = (function() {

            function SpotData(properties) {
                this.unlocked_ending = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SpotData.prototype.unique_id = 0;
            SpotData.prototype.rewarded = 0;
            SpotData.prototype.unlocked_ending = $util.emptyArray;
            SpotData.prototype.unlocked = 0;

            SpotData.create = function create(properties) {
                return new SpotData(properties);
            };

            SpotData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                    writer.uint32(8).uint32(message.unique_id);
                if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                    writer.uint32(16).uint32(message.rewarded);
                if (message.unlocked_ending != null && message.unlocked_ending.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.unlocked_ending.length; ++i)
                        writer.uint32(message.unlocked_ending[i]);
                    writer.ldelim();
                }
                if (message.unlocked != null && Object.hasOwnProperty.call(message, "unlocked"))
                    writer.uint32(32).uint32(message.unlocked);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SpotData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SpotData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySpotData.SpotData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.unique_id = value;
                            else
                                delete message.unique_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rewarded = value;
                            else
                                delete message.rewarded;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.unlocked_ending && message.unlocked_ending.length))
                                    message.unlocked_ending = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.unlocked_ending.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.unlocked_ending && message.unlocked_ending.length))
                                message.unlocked_ending = [];
                            message.unlocked_ending.push(reader.uint32());
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.unlocked = value;
                            else
                                delete message.unlocked;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SpotData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SpotData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                    if (!$util.isInteger(message.unique_id))
                        return "unique_id: integer expected";
                if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                    if (!$util.isInteger(message.rewarded))
                        return "rewarded: integer expected";
                if (message.unlocked_ending != null && message.hasOwnProperty("unlocked_ending")) {
                    if (!Array.isArray(message.unlocked_ending))
                        return "unlocked_ending: array expected";
                    for (let i = 0; i < message.unlocked_ending.length; ++i)
                        if (!$util.isInteger(message.unlocked_ending[i]))
                            return "unlocked_ending: integer[] expected";
                }
                if (message.unlocked != null && message.hasOwnProperty("unlocked"))
                    if (!$util.isInteger(message.unlocked))
                        return "unlocked: integer expected";
                return null;
            };

            SpotData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivitySpotData.SpotData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivitySpotData.SpotData();
                if (object.unique_id != null)
                    if (Number(object.unique_id) !== 0)
                        message.unique_id = object.unique_id >>> 0;
                if (object.rewarded != null)
                    if (Number(object.rewarded) !== 0)
                        message.rewarded = object.rewarded >>> 0;
                if (object.unlocked_ending) {
                    if (!Array.isArray(object.unlocked_ending))
                        throw TypeError(".lq.ActivitySpotData.SpotData.unlocked_ending: array expected");
                    message.unlocked_ending = Array(object.unlocked_ending.length);
                    for (let i = 0; i < object.unlocked_ending.length; ++i)
                        message.unlocked_ending[i] = object.unlocked_ending[i] >>> 0;
                }
                if (object.unlocked != null)
                    if (Number(object.unlocked) !== 0)
                        message.unlocked = object.unlocked >>> 0;
                return message;
            };

            SpotData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.unlocked_ending = [];
                if (options.defaults) {
                    object.unique_id = 0;
                    object.rewarded = 0;
                    object.unlocked = 0;
                }
                if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                    object.unique_id = message.unique_id;
                if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                    object.rewarded = message.rewarded;
                if (message.unlocked_ending && message.unlocked_ending.length) {
                    object.unlocked_ending = Array(message.unlocked_ending.length);
                    for (let j = 0; j < message.unlocked_ending.length; ++j)
                        object.unlocked_ending[j] = message.unlocked_ending[j];
                }
                if (message.unlocked != null && message.hasOwnProperty("unlocked"))
                    object.unlocked = message.unlocked;
                return object;
            };

            SpotData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SpotData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivitySpotData.SpotData";
            };

            return SpotData;
        })();

        return ActivitySpotData;
    })();

    lq.AccountActiveState = (function() {

        function AccountActiveState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountActiveState.prototype.account_id = 0;
        AccountActiveState.prototype.login_time = 0;
        AccountActiveState.prototype.logout_time = 0;
        AccountActiveState.prototype.is_online = false;
        AccountActiveState.prototype.playing = null;

        AccountActiveState.create = function create(properties) {
            return new AccountActiveState(properties);
        };

        AccountActiveState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.login_time != null && Object.hasOwnProperty.call(message, "login_time"))
                writer.uint32(16).uint32(message.login_time);
            if (message.logout_time != null && Object.hasOwnProperty.call(message, "logout_time"))
                writer.uint32(24).uint32(message.logout_time);
            if (message.is_online != null && Object.hasOwnProperty.call(message, "is_online"))
                writer.uint32(32).bool(message.is_online);
            if (message.playing != null && Object.hasOwnProperty.call(message, "playing"))
                $root.lq.AccountPlayingGame.encode(message.playing, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountActiveState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountActiveState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountActiveState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.login_time = value;
                        else
                            delete message.login_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.logout_time = value;
                        else
                            delete message.logout_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_online = value;
                        else
                            delete message.is_online;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.playing = $root.lq.AccountPlayingGame.decode(reader, reader.uint32(), undefined, _depth + 1, message.playing);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountActiveState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountActiveState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                if (!$util.isInteger(message.login_time))
                    return "login_time: integer expected";
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                if (!$util.isInteger(message.logout_time))
                    return "logout_time: integer expected";
            if (message.is_online != null && message.hasOwnProperty("is_online"))
                if (typeof message.is_online !== "boolean")
                    return "is_online: boolean expected";
            if (message.playing != null && message.hasOwnProperty("playing")) {
                let error = $root.lq.AccountPlayingGame.verify(message.playing, _depth + 1);
                if (error)
                    return "playing." + error;
            }
            return null;
        };

        AccountActiveState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountActiveState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountActiveState();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.login_time != null)
                if (Number(object.login_time) !== 0)
                    message.login_time = object.login_time >>> 0;
            if (object.logout_time != null)
                if (Number(object.logout_time) !== 0)
                    message.logout_time = object.logout_time >>> 0;
            if (object.is_online != null)
                if (object.is_online)
                    message.is_online = Boolean(object.is_online);
            if (object.playing != null) {
                if (typeof object.playing !== "object")
                    throw TypeError(".lq.AccountActiveState.playing: object expected");
                message.playing = $root.lq.AccountPlayingGame.fromObject(object.playing, _depth + 1);
            }
            return message;
        };

        AccountActiveState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.login_time = 0;
                object.logout_time = 0;
                object.is_online = false;
                object.playing = null;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                object.login_time = message.login_time;
            if (message.logout_time != null && message.hasOwnProperty("logout_time"))
                object.logout_time = message.logout_time;
            if (message.is_online != null && message.hasOwnProperty("is_online"))
                object.is_online = message.is_online;
            if (message.playing != null && message.hasOwnProperty("playing"))
                object.playing = $root.lq.AccountPlayingGame.toObject(message.playing, options);
            return object;
        };

        AccountActiveState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountActiveState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountActiveState";
        };

        return AccountActiveState;
    })();

    lq.Friend = (function() {

        function Friend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Friend.prototype.base = null;
        Friend.prototype.state = null;
        Friend.prototype.remark = "";

        Friend.create = function create(properties) {
            return new Friend(properties);
        };

        Friend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                $root.lq.PlayerBaseView.encode(message.base, writer.uint32(10).fork()).ldelim();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                $root.lq.AccountActiveState.encode(message.state, writer.uint32(18).fork()).ldelim();
            if (message.remark != null && Object.hasOwnProperty.call(message, "remark"))
                writer.uint32(26).string(message.remark);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Friend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Friend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Friend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.base = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.base);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.state = $root.lq.AccountActiveState.decode(reader, reader.uint32(), undefined, _depth + 1, message.state);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.remark = value;
                        else
                            delete message.remark;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Friend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Friend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.base != null && message.hasOwnProperty("base")) {
                let error = $root.lq.PlayerBaseView.verify(message.base, _depth + 1);
                if (error)
                    return "base." + error;
            }
            if (message.state != null && message.hasOwnProperty("state")) {
                let error = $root.lq.AccountActiveState.verify(message.state, _depth + 1);
                if (error)
                    return "state." + error;
            }
            if (message.remark != null && message.hasOwnProperty("remark"))
                if (!$util.isString(message.remark))
                    return "remark: string expected";
            return null;
        };

        Friend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Friend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Friend();
            if (object.base != null) {
                if (typeof object.base !== "object")
                    throw TypeError(".lq.Friend.base: object expected");
                message.base = $root.lq.PlayerBaseView.fromObject(object.base, _depth + 1);
            }
            if (object.state != null) {
                if (typeof object.state !== "object")
                    throw TypeError(".lq.Friend.state: object expected");
                message.state = $root.lq.AccountActiveState.fromObject(object.state, _depth + 1);
            }
            if (object.remark != null)
                if (typeof object.remark !== "string" || object.remark.length)
                    message.remark = String(object.remark);
            return message;
        };

        Friend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.base = null;
                object.state = null;
                object.remark = "";
            }
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = $root.lq.PlayerBaseView.toObject(message.base, options);
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = $root.lq.AccountActiveState.toObject(message.state, options);
            if (message.remark != null && message.hasOwnProperty("remark"))
                object.remark = message.remark;
            return object;
        };

        Friend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Friend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Friend";
        };

        return Friend;
    })();

    lq.Point = (function() {

        function Point(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Point.prototype.x = 0;
        Point.prototype.y = 0;

        Point.create = function create(properties) {
            return new Point(properties);
        };

        Point.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                writer.uint32(8).uint32(message.x);
            if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                writer.uint32(16).uint32(message.y);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Point.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Point.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Point(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Point.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Point.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.x != null && message.hasOwnProperty("x"))
                if (!$util.isInteger(message.x))
                    return "x: integer expected";
            if (message.y != null && message.hasOwnProperty("y"))
                if (!$util.isInteger(message.y))
                    return "y: integer expected";
            return null;
        };

        Point.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Point)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Point();
            if (object.x != null)
                if (Number(object.x) !== 0)
                    message.x = object.x >>> 0;
            if (object.y != null)
                if (Number(object.y) !== 0)
                    message.y = object.y >>> 0;
            return message;
        };

        Point.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
            }
            if (message.x != null && message.hasOwnProperty("x"))
                object.x = message.x;
            if (message.y != null && message.hasOwnProperty("y"))
                object.y = message.y;
            return object;
        };

        Point.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Point.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Point";
        };

        return Point;
    })();

    lq.MineReward = (function() {

        function MineReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MineReward.prototype.point = null;
        MineReward.prototype.reward_id = 0;
        MineReward.prototype.received = false;

        MineReward.create = function create(properties) {
            return new MineReward(properties);
        };

        MineReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                $root.lq.Point.encode(message.point, writer.uint32(10).fork()).ldelim();
            if (message.reward_id != null && Object.hasOwnProperty.call(message, "reward_id"))
                writer.uint32(16).uint32(message.reward_id);
            if (message.received != null && Object.hasOwnProperty.call(message, "received"))
                writer.uint32(24).bool(message.received);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MineReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MineReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MineReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.point = $root.lq.Point.decode(reader, reader.uint32(), undefined, _depth + 1, message.point);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reward_id = value;
                        else
                            delete message.reward_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.received = value;
                        else
                            delete message.received;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MineReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MineReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.point != null && message.hasOwnProperty("point")) {
                let error = $root.lq.Point.verify(message.point, _depth + 1);
                if (error)
                    return "point." + error;
            }
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                if (!$util.isInteger(message.reward_id))
                    return "reward_id: integer expected";
            if (message.received != null && message.hasOwnProperty("received"))
                if (typeof message.received !== "boolean")
                    return "received: boolean expected";
            return null;
        };

        MineReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MineReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MineReward();
            if (object.point != null) {
                if (typeof object.point !== "object")
                    throw TypeError(".lq.MineReward.point: object expected");
                message.point = $root.lq.Point.fromObject(object.point, _depth + 1);
            }
            if (object.reward_id != null)
                if (Number(object.reward_id) !== 0)
                    message.reward_id = object.reward_id >>> 0;
            if (object.received != null)
                if (object.received)
                    message.received = Boolean(object.received);
            return message;
        };

        MineReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.point = null;
                object.reward_id = 0;
                object.received = false;
            }
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = $root.lq.Point.toObject(message.point, options);
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                object.reward_id = message.reward_id;
            if (message.received != null && message.hasOwnProperty("received"))
                object.received = message.received;
            return object;
        };

        MineReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MineReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MineReward";
        };

        return MineReward;
    })();

    lq.GameLiveUnit = (function() {

        function GameLiveUnit(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameLiveUnit.prototype.timestamp = 0;
        GameLiveUnit.prototype.action_category = 0;
        GameLiveUnit.prototype.action_data = $util.newBuffer([]);

        GameLiveUnit.create = function create(properties) {
            return new GameLiveUnit(properties);
        };

        GameLiveUnit.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(8).uint32(message.timestamp);
            if (message.action_category != null && Object.hasOwnProperty.call(message, "action_category"))
                writer.uint32(16).uint32(message.action_category);
            if (message.action_data != null && Object.hasOwnProperty.call(message, "action_data"))
                writer.uint32(26).bytes(message.action_data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameLiveUnit.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameLiveUnit.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameLiveUnit(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.action_category = value;
                        else
                            delete message.action_category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.action_data = value;
                        else
                            delete message.action_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameLiveUnit.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameLiveUnit.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp))
                    return "timestamp: integer expected";
            if (message.action_category != null && message.hasOwnProperty("action_category"))
                if (!$util.isInteger(message.action_category))
                    return "action_category: integer expected";
            if (message.action_data != null && message.hasOwnProperty("action_data"))
                if (!(message.action_data && typeof message.action_data.length === "number" || $util.isString(message.action_data)))
                    return "action_data: buffer expected";
            return null;
        };

        GameLiveUnit.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameLiveUnit)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameLiveUnit();
            if (object.timestamp != null)
                if (Number(object.timestamp) !== 0)
                    message.timestamp = object.timestamp >>> 0;
            if (object.action_category != null)
                if (Number(object.action_category) !== 0)
                    message.action_category = object.action_category >>> 0;
            if (object.action_data != null)
                if (object.action_data.length)
                    if (typeof object.action_data === "string")
                        $util.base64.decode(object.action_data, message.action_data = $util.newBuffer($util.base64.length(object.action_data)), 0);
                    else if (object.action_data.length >= 0)
                        message.action_data = object.action_data;
            return message;
        };

        GameLiveUnit.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.timestamp = 0;
                object.action_category = 0;
                if (options.bytes === String)
                    object.action_data = "";
                else {
                    object.action_data = [];
                    if (options.bytes !== Array)
                        object.action_data = $util.newBuffer(object.action_data);
                }
            }
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                object.timestamp = message.timestamp;
            if (message.action_category != null && message.hasOwnProperty("action_category"))
                object.action_category = message.action_category;
            if (message.action_data != null && message.hasOwnProperty("action_data"))
                object.action_data = options.bytes === String ? $util.base64.encode(message.action_data, 0, message.action_data.length) : options.bytes === Array ? Array.prototype.slice.call(message.action_data) : message.action_data;
            return object;
        };

        GameLiveUnit.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameLiveUnit.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameLiveUnit";
        };

        return GameLiveUnit;
    })();

    lq.GameLiveSegment = (function() {

        function GameLiveSegment(properties) {
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameLiveSegment.prototype.actions = $util.emptyArray;

        GameLiveSegment.create = function create(properties) {
            return new GameLiveSegment(properties);
        };

        GameLiveSegment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.lq.GameLiveUnit.encode(message.actions[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameLiveSegment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameLiveSegment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameLiveSegment();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.lq.GameLiveUnit.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameLiveSegment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameLiveSegment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.actions != null && message.hasOwnProperty("actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.lq.GameLiveUnit.verify(message.actions[i], _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            return null;
        };

        GameLiveSegment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameLiveSegment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameLiveSegment();
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".lq.GameLiveSegment.actions: array expected");
                message.actions = Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i) {
                    if (typeof object.actions[i] !== "object")
                        throw TypeError(".lq.GameLiveSegment.actions: object expected");
                    message.actions[i] = $root.lq.GameLiveUnit.fromObject(object.actions[i], _depth + 1);
                }
            }
            return message;
        };

        GameLiveSegment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.actions = [];
            if (message.actions && message.actions.length) {
                object.actions = Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.lq.GameLiveUnit.toObject(message.actions[j], options);
            }
            return object;
        };

        GameLiveSegment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameLiveSegment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameLiveSegment";
        };

        return GameLiveSegment;
    })();

    lq.GameLiveSegmentUri = (function() {

        function GameLiveSegmentUri(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameLiveSegmentUri.prototype.segment_id = 0;
        GameLiveSegmentUri.prototype.segment_uri = "";

        GameLiveSegmentUri.create = function create(properties) {
            return new GameLiveSegmentUri(properties);
        };

        GameLiveSegmentUri.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.segment_id != null && Object.hasOwnProperty.call(message, "segment_id"))
                writer.uint32(8).uint32(message.segment_id);
            if (message.segment_uri != null && Object.hasOwnProperty.call(message, "segment_uri"))
                writer.uint32(18).string(message.segment_uri);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameLiveSegmentUri.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameLiveSegmentUri.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameLiveSegmentUri(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.segment_id = value;
                        else
                            delete message.segment_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.segment_uri = value;
                        else
                            delete message.segment_uri;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameLiveSegmentUri.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameLiveSegmentUri.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.segment_id != null && message.hasOwnProperty("segment_id"))
                if (!$util.isInteger(message.segment_id))
                    return "segment_id: integer expected";
            if (message.segment_uri != null && message.hasOwnProperty("segment_uri"))
                if (!$util.isString(message.segment_uri))
                    return "segment_uri: string expected";
            return null;
        };

        GameLiveSegmentUri.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameLiveSegmentUri)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameLiveSegmentUri();
            if (object.segment_id != null)
                if (Number(object.segment_id) !== 0)
                    message.segment_id = object.segment_id >>> 0;
            if (object.segment_uri != null)
                if (typeof object.segment_uri !== "string" || object.segment_uri.length)
                    message.segment_uri = String(object.segment_uri);
            return message;
        };

        GameLiveSegmentUri.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.segment_id = 0;
                object.segment_uri = "";
            }
            if (message.segment_id != null && message.hasOwnProperty("segment_id"))
                object.segment_id = message.segment_id;
            if (message.segment_uri != null && message.hasOwnProperty("segment_uri"))
                object.segment_uri = message.segment_uri;
            return object;
        };

        GameLiveSegmentUri.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameLiveSegmentUri.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameLiveSegmentUri";
        };

        return GameLiveSegmentUri;
    })();

    lq.GameLiveHead = (function() {

        function GameLiveHead(properties) {
            this.players = [];
            this.seat_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameLiveHead.prototype.uuid = "";
        GameLiveHead.prototype.start_time = 0;
        GameLiveHead.prototype.game_config = null;
        GameLiveHead.prototype.players = $util.emptyArray;
        GameLiveHead.prototype.seat_list = $util.emptyArray;

        GameLiveHead.create = function create(properties) {
            return new GameLiveHead(properties);
        };

        GameLiveHead.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(16).uint32(message.start_time);
            if (message.game_config != null && Object.hasOwnProperty.call(message, "game_config"))
                $root.lq.GameConfig.encode(message.game_config, writer.uint32(26).fork()).ldelim();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.PlayerGameView.encode(message.players[i], writer.uint32(34).fork()).ldelim();
            if (message.seat_list != null && message.seat_list.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.seat_list.length; ++i)
                    writer.uint32(message.seat_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameLiveHead.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameLiveHead.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameLiveHead(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game_config = $root.lq.GameConfig.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_config);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.seat_list && message.seat_list.length))
                                message.seat_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.seat_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.seat_list && message.seat_list.length))
                            message.seat_list = [];
                        message.seat_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameLiveHead.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameLiveHead.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.game_config != null && message.hasOwnProperty("game_config")) {
                let error = $root.lq.GameConfig.verify(message.game_config, _depth + 1);
                if (error)
                    return "game_config." + error;
            }
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.seat_list != null && message.hasOwnProperty("seat_list")) {
                if (!Array.isArray(message.seat_list))
                    return "seat_list: array expected";
                for (let i = 0; i < message.seat_list.length; ++i)
                    if (!$util.isInteger(message.seat_list[i]))
                        return "seat_list: integer[] expected";
            }
            return null;
        };

        GameLiveHead.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameLiveHead)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameLiveHead();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.game_config != null) {
                if (typeof object.game_config !== "object")
                    throw TypeError(".lq.GameLiveHead.game_config: object expected");
                message.game_config = $root.lq.GameConfig.fromObject(object.game_config, _depth + 1);
            }
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.GameLiveHead.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.GameLiveHead.players: object expected");
                    message.players[i] = $root.lq.PlayerGameView.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.seat_list) {
                if (!Array.isArray(object.seat_list))
                    throw TypeError(".lq.GameLiveHead.seat_list: array expected");
                message.seat_list = Array(object.seat_list.length);
                for (let i = 0; i < object.seat_list.length; ++i)
                    message.seat_list[i] = object.seat_list[i] >>> 0;
            }
            return message;
        };

        GameLiveHead.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.seat_list = [];
            }
            if (options.defaults) {
                object.uuid = "";
                object.start_time = 0;
                object.game_config = null;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.game_config != null && message.hasOwnProperty("game_config"))
                object.game_config = $root.lq.GameConfig.toObject(message.game_config, options);
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.PlayerGameView.toObject(message.players[j], options);
            }
            if (message.seat_list && message.seat_list.length) {
                object.seat_list = Array(message.seat_list.length);
                for (let j = 0; j < message.seat_list.length; ++j)
                    object.seat_list[j] = message.seat_list[j];
            }
            return object;
        };

        GameLiveHead.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameLiveHead.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameLiveHead";
        };

        return GameLiveHead;
    })();

    lq.GameNewRoundState = (function() {

        function GameNewRoundState(properties) {
            this.seat_states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameNewRoundState.prototype.seat_states = $util.emptyArray;

        GameNewRoundState.create = function create(properties) {
            return new GameNewRoundState(properties);
        };

        GameNewRoundState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat_states != null && message.seat_states.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.seat_states.length; ++i)
                    writer.uint32(message.seat_states[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameNewRoundState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameNewRoundState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameNewRoundState();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.seat_states && message.seat_states.length))
                                message.seat_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.seat_states.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.seat_states && message.seat_states.length))
                            message.seat_states = [];
                        message.seat_states.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameNewRoundState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameNewRoundState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat_states != null && message.hasOwnProperty("seat_states")) {
                if (!Array.isArray(message.seat_states))
                    return "seat_states: array expected";
                for (let i = 0; i < message.seat_states.length; ++i)
                    if (!$util.isInteger(message.seat_states[i]))
                        return "seat_states: integer[] expected";
            }
            return null;
        };

        GameNewRoundState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameNewRoundState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameNewRoundState();
            if (object.seat_states) {
                if (!Array.isArray(object.seat_states))
                    throw TypeError(".lq.GameNewRoundState.seat_states: array expected");
                message.seat_states = Array(object.seat_states.length);
                for (let i = 0; i < object.seat_states.length; ++i)
                    message.seat_states[i] = object.seat_states[i] >>> 0;
            }
            return message;
        };

        GameNewRoundState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.seat_states = [];
            if (message.seat_states && message.seat_states.length) {
                object.seat_states = Array(message.seat_states.length);
                for (let j = 0; j < message.seat_states.length; ++j)
                    object.seat_states[j] = message.seat_states[j];
            }
            return object;
        };

        GameNewRoundState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameNewRoundState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameNewRoundState";
        };

        return GameNewRoundState;
    })();

    lq.GameEndAction = (function() {

        function GameEndAction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameEndAction.prototype.state = 0;

        GameEndAction.create = function create(properties) {
            return new GameEndAction(properties);
        };

        GameEndAction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(8).uint32(message.state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameEndAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameEndAction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameEndAction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameEndAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameEndAction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            return null;
        };

        GameEndAction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameEndAction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameEndAction();
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            return message;
        };

        GameEndAction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.state = 0;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            return object;
        };

        GameEndAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameEndAction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameEndAction";
        };

        return GameEndAction;
    })();

    lq.GameNoopAction = (function() {

        function GameNoopAction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameNoopAction.create = function create(properties) {
            return new GameNoopAction(properties);
        };

        GameNoopAction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameNoopAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameNoopAction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameNoopAction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameNoopAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameNoopAction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        GameNoopAction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameNoopAction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.GameNoopAction();
        };

        GameNoopAction.toObject = function toObject() {
            return {};
        };

        GameNoopAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameNoopAction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameNoopAction";
        };

        return GameNoopAction;
    })();

    lq.CommentItem = (function() {

        function CommentItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CommentItem.prototype.comment_id = 0;
        CommentItem.prototype.timestamp = 0;
        CommentItem.prototype.commenter = null;
        CommentItem.prototype.content = "";
        CommentItem.prototype.is_banned = 0;

        CommentItem.create = function create(properties) {
            return new CommentItem(properties);
        };

        CommentItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.comment_id != null && Object.hasOwnProperty.call(message, "comment_id"))
                writer.uint32(8).uint32(message.comment_id);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(16).uint32(message.timestamp);
            if (message.commenter != null && Object.hasOwnProperty.call(message, "commenter"))
                $root.lq.PlayerBaseView.encode(message.commenter, writer.uint32(26).fork()).ldelim();
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(34).string(message.content);
            if (message.is_banned != null && Object.hasOwnProperty.call(message, "is_banned"))
                writer.uint32(40).uint32(message.is_banned);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CommentItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CommentItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CommentItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.comment_id = value;
                        else
                            delete message.comment_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.commenter = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.commenter);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.is_banned = value;
                        else
                            delete message.is_banned;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CommentItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CommentItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.comment_id != null && message.hasOwnProperty("comment_id"))
                if (!$util.isInteger(message.comment_id))
                    return "comment_id: integer expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp))
                    return "timestamp: integer expected";
            if (message.commenter != null && message.hasOwnProperty("commenter")) {
                let error = $root.lq.PlayerBaseView.verify(message.commenter, _depth + 1);
                if (error)
                    return "commenter." + error;
            }
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.is_banned != null && message.hasOwnProperty("is_banned"))
                if (!$util.isInteger(message.is_banned))
                    return "is_banned: integer expected";
            return null;
        };

        CommentItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CommentItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CommentItem();
            if (object.comment_id != null)
                if (Number(object.comment_id) !== 0)
                    message.comment_id = object.comment_id >>> 0;
            if (object.timestamp != null)
                if (Number(object.timestamp) !== 0)
                    message.timestamp = object.timestamp >>> 0;
            if (object.commenter != null) {
                if (typeof object.commenter !== "object")
                    throw TypeError(".lq.CommentItem.commenter: object expected");
                message.commenter = $root.lq.PlayerBaseView.fromObject(object.commenter, _depth + 1);
            }
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.is_banned != null)
                if (Number(object.is_banned) !== 0)
                    message.is_banned = object.is_banned >>> 0;
            return message;
        };

        CommentItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.comment_id = 0;
                object.timestamp = 0;
                object.commenter = null;
                object.content = "";
                object.is_banned = 0;
            }
            if (message.comment_id != null && message.hasOwnProperty("comment_id"))
                object.comment_id = message.comment_id;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                object.timestamp = message.timestamp;
            if (message.commenter != null && message.hasOwnProperty("commenter"))
                object.commenter = $root.lq.PlayerBaseView.toObject(message.commenter, options);
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.is_banned != null && message.hasOwnProperty("is_banned"))
                object.is_banned = message.is_banned;
            return object;
        };

        CommentItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CommentItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CommentItem";
        };

        return CommentItem;
    })();

    lq.RollingNotice = (function() {

        function RollingNotice(properties) {
            this.repeat_time = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RollingNotice.prototype.content = "";
        RollingNotice.prototype.start_time = 0;
        RollingNotice.prototype.end_time = 0;
        RollingNotice.prototype.repeat_interval = 0;
        RollingNotice.prototype.repeat_time = $util.emptyArray;
        RollingNotice.prototype.repeat_type = 0;

        RollingNotice.create = function create(properties) {
            return new RollingNotice(properties);
        };

        RollingNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(18).string(message.content);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(24).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(32).uint32(message.end_time);
            if (message.repeat_interval != null && Object.hasOwnProperty.call(message, "repeat_interval"))
                writer.uint32(40).uint32(message.repeat_interval);
            if (message.repeat_time != null && message.repeat_time.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.repeat_time.length; ++i)
                    writer.uint32(message.repeat_time[i]);
                writer.ldelim();
            }
            if (message.repeat_type != null && Object.hasOwnProperty.call(message, "repeat_type"))
                writer.uint32(64).uint32(message.repeat_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RollingNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RollingNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RollingNotice(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.repeat_interval = value;
                        else
                            delete message.repeat_interval;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.repeat_time && message.repeat_time.length))
                                message.repeat_time = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.repeat_time.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.repeat_time && message.repeat_time.length))
                            message.repeat_time = [];
                        message.repeat_time.push(reader.uint32());
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.repeat_type = value;
                        else
                            delete message.repeat_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RollingNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RollingNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.repeat_interval != null && message.hasOwnProperty("repeat_interval"))
                if (!$util.isInteger(message.repeat_interval))
                    return "repeat_interval: integer expected";
            if (message.repeat_time != null && message.hasOwnProperty("repeat_time")) {
                if (!Array.isArray(message.repeat_time))
                    return "repeat_time: array expected";
                for (let i = 0; i < message.repeat_time.length; ++i)
                    if (!$util.isInteger(message.repeat_time[i]))
                        return "repeat_time: integer[] expected";
            }
            if (message.repeat_type != null && message.hasOwnProperty("repeat_type"))
                if (!$util.isInteger(message.repeat_type))
                    return "repeat_type: integer expected";
            return null;
        };

        RollingNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RollingNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RollingNotice();
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.repeat_interval != null)
                if (Number(object.repeat_interval) !== 0)
                    message.repeat_interval = object.repeat_interval >>> 0;
            if (object.repeat_time) {
                if (!Array.isArray(object.repeat_time))
                    throw TypeError(".lq.RollingNotice.repeat_time: array expected");
                message.repeat_time = Array(object.repeat_time.length);
                for (let i = 0; i < object.repeat_time.length; ++i)
                    message.repeat_time[i] = object.repeat_time[i] >>> 0;
            }
            if (object.repeat_type != null)
                if (Number(object.repeat_type) !== 0)
                    message.repeat_type = object.repeat_type >>> 0;
            return message;
        };

        RollingNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.repeat_time = [];
            if (options.defaults) {
                object.content = "";
                object.start_time = 0;
                object.end_time = 0;
                object.repeat_interval = 0;
                object.repeat_type = 0;
            }
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.repeat_interval != null && message.hasOwnProperty("repeat_interval"))
                object.repeat_interval = message.repeat_interval;
            if (message.repeat_time && message.repeat_time.length) {
                object.repeat_time = Array(message.repeat_time.length);
                for (let j = 0; j < message.repeat_time.length; ++j)
                    object.repeat_time[j] = message.repeat_time[j];
            }
            if (message.repeat_type != null && message.hasOwnProperty("repeat_type"))
                object.repeat_type = message.repeat_type;
            return object;
        };

        RollingNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RollingNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RollingNotice";
        };

        return RollingNotice;
    })();

    lq.MaintainNotice = (function() {

        function MaintainNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MaintainNotice.prototype.maintain_time = 0;

        MaintainNotice.create = function create(properties) {
            return new MaintainNotice(properties);
        };

        MaintainNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.maintain_time != null && Object.hasOwnProperty.call(message, "maintain_time"))
                writer.uint32(8).uint32(message.maintain_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MaintainNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MaintainNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MaintainNotice(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.maintain_time = value;
                        else
                            delete message.maintain_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MaintainNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MaintainNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.maintain_time != null && message.hasOwnProperty("maintain_time"))
                if (!$util.isInteger(message.maintain_time))
                    return "maintain_time: integer expected";
            return null;
        };

        MaintainNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MaintainNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MaintainNotice();
            if (object.maintain_time != null)
                if (Number(object.maintain_time) !== 0)
                    message.maintain_time = object.maintain_time >>> 0;
            return message;
        };

        MaintainNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.maintain_time = 0;
            if (message.maintain_time != null && message.hasOwnProperty("maintain_time"))
                object.maintain_time = message.maintain_time;
            return object;
        };

        MaintainNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MaintainNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MaintainNotice";
        };

        return MaintainNotice;
    })();

    lq.BillingGoods = (function() {

        function BillingGoods(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BillingGoods.prototype.id = "";
        BillingGoods.prototype.name = "";
        BillingGoods.prototype.desc = "";
        BillingGoods.prototype.icon = "";
        BillingGoods.prototype.resource_id = 0;
        BillingGoods.prototype.resource_count = 0;

        BillingGoods.create = function create(properties) {
            return new BillingGoods(properties);
        };

        BillingGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(10).string(message.id);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(18).string(message.name);
            if (message.desc != null && Object.hasOwnProperty.call(message, "desc"))
                writer.uint32(26).string(message.desc);
            if (message.icon != null && Object.hasOwnProperty.call(message, "icon"))
                writer.uint32(34).string(message.icon);
            if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                writer.uint32(40).uint32(message.resource_id);
            if (message.resource_count != null && Object.hasOwnProperty.call(message, "resource_count"))
                writer.uint32(48).uint32(message.resource_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BillingGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BillingGoods.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BillingGoods(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.desc = value;
                        else
                            delete message.desc;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.icon = value;
                        else
                            delete message.icon;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.resource_id = value;
                        else
                            delete message.resource_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.resource_count = value;
                        else
                            delete message.resource_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BillingGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BillingGoods.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isString(message.id))
                    return "id: string expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.desc != null && message.hasOwnProperty("desc"))
                if (!$util.isString(message.desc))
                    return "desc: string expected";
            if (message.icon != null && message.hasOwnProperty("icon"))
                if (!$util.isString(message.icon))
                    return "icon: string expected";
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                if (!$util.isInteger(message.resource_id))
                    return "resource_id: integer expected";
            if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                if (!$util.isInteger(message.resource_count))
                    return "resource_count: integer expected";
            return null;
        };

        BillingGoods.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BillingGoods)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BillingGoods();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.desc != null)
                if (typeof object.desc !== "string" || object.desc.length)
                    message.desc = String(object.desc);
            if (object.icon != null)
                if (typeof object.icon !== "string" || object.icon.length)
                    message.icon = String(object.icon);
            if (object.resource_id != null)
                if (Number(object.resource_id) !== 0)
                    message.resource_id = object.resource_id >>> 0;
            if (object.resource_count != null)
                if (Number(object.resource_count) !== 0)
                    message.resource_count = object.resource_count >>> 0;
            return message;
        };

        BillingGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.desc = "";
                object.icon = "";
                object.resource_id = 0;
                object.resource_count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.desc != null && message.hasOwnProperty("desc"))
                object.desc = message.desc;
            if (message.icon != null && message.hasOwnProperty("icon"))
                object.icon = message.icon;
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                object.resource_id = message.resource_id;
            if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                object.resource_count = message.resource_count;
            return object;
        };

        BillingGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BillingGoods.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BillingGoods";
        };

        return BillingGoods;
    })();

    lq.BillShortcut = (function() {

        function BillShortcut(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BillShortcut.prototype.id = 0;
        BillShortcut.prototype.count = 0;
        BillShortcut.prototype.dealPrice = 0;

        BillShortcut.create = function create(properties) {
            return new BillShortcut(properties);
        };

        BillShortcut.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.dealPrice != null && Object.hasOwnProperty.call(message, "dealPrice"))
                writer.uint32(24).uint32(message.dealPrice);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BillShortcut.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BillShortcut.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BillShortcut(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dealPrice = value;
                        else
                            delete message.dealPrice;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BillShortcut.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BillShortcut.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.dealPrice != null && message.hasOwnProperty("dealPrice"))
                if (!$util.isInteger(message.dealPrice))
                    return "dealPrice: integer expected";
            return null;
        };

        BillShortcut.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BillShortcut)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BillShortcut();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.dealPrice != null)
                if (Number(object.dealPrice) !== 0)
                    message.dealPrice = object.dealPrice >>> 0;
            return message;
        };

        BillShortcut.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
                object.dealPrice = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.dealPrice != null && message.hasOwnProperty("dealPrice"))
                object.dealPrice = message.dealPrice;
            return object;
        };

        BillShortcut.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BillShortcut.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BillShortcut";
        };

        return BillShortcut;
    })();

    lq.BillingProduct = (function() {

        function BillingProduct(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BillingProduct.prototype.goods = null;
        BillingProduct.prototype.currency_code = "";
        BillingProduct.prototype.currency_price = 0;
        BillingProduct.prototype.sort_weight = 0;

        BillingProduct.create = function create(properties) {
            return new BillingProduct(properties);
        };

        BillingProduct.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods != null && Object.hasOwnProperty.call(message, "goods"))
                $root.lq.BillingGoods.encode(message.goods, writer.uint32(10).fork()).ldelim();
            if (message.currency_code != null && Object.hasOwnProperty.call(message, "currency_code"))
                writer.uint32(18).string(message.currency_code);
            if (message.currency_price != null && Object.hasOwnProperty.call(message, "currency_price"))
                writer.uint32(24).uint32(message.currency_price);
            if (message.sort_weight != null && Object.hasOwnProperty.call(message, "sort_weight"))
                writer.uint32(32).uint32(message.sort_weight);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BillingProduct.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BillingProduct.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BillingProduct(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.goods = $root.lq.BillingGoods.decode(reader, reader.uint32(), undefined, _depth + 1, message.goods);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.currency_code = value;
                        else
                            delete message.currency_code;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.currency_price = value;
                        else
                            delete message.currency_price;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sort_weight = value;
                        else
                            delete message.sort_weight;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BillingProduct.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BillingProduct.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods != null && message.hasOwnProperty("goods")) {
                let error = $root.lq.BillingGoods.verify(message.goods, _depth + 1);
                if (error)
                    return "goods." + error;
            }
            if (message.currency_code != null && message.hasOwnProperty("currency_code"))
                if (!$util.isString(message.currency_code))
                    return "currency_code: string expected";
            if (message.currency_price != null && message.hasOwnProperty("currency_price"))
                if (!$util.isInteger(message.currency_price))
                    return "currency_price: integer expected";
            if (message.sort_weight != null && message.hasOwnProperty("sort_weight"))
                if (!$util.isInteger(message.sort_weight))
                    return "sort_weight: integer expected";
            return null;
        };

        BillingProduct.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BillingProduct)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BillingProduct();
            if (object.goods != null) {
                if (typeof object.goods !== "object")
                    throw TypeError(".lq.BillingProduct.goods: object expected");
                message.goods = $root.lq.BillingGoods.fromObject(object.goods, _depth + 1);
            }
            if (object.currency_code != null)
                if (typeof object.currency_code !== "string" || object.currency_code.length)
                    message.currency_code = String(object.currency_code);
            if (object.currency_price != null)
                if (Number(object.currency_price) !== 0)
                    message.currency_price = object.currency_price >>> 0;
            if (object.sort_weight != null)
                if (Number(object.sort_weight) !== 0)
                    message.sort_weight = object.sort_weight >>> 0;
            return message;
        };

        BillingProduct.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods = null;
                object.currency_code = "";
                object.currency_price = 0;
                object.sort_weight = 0;
            }
            if (message.goods != null && message.hasOwnProperty("goods"))
                object.goods = $root.lq.BillingGoods.toObject(message.goods, options);
            if (message.currency_code != null && message.hasOwnProperty("currency_code"))
                object.currency_code = message.currency_code;
            if (message.currency_price != null && message.hasOwnProperty("currency_price"))
                object.currency_price = message.currency_price;
            if (message.sort_weight != null && message.hasOwnProperty("sort_weight"))
                object.sort_weight = message.sort_weight;
            return object;
        };

        BillingProduct.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BillingProduct.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BillingProduct";
        };

        return BillingProduct;
    })();

    lq.Character = (function() {

        function Character(properties) {
            this.views = [];
            this.extra_emoji = [];
            this.rewarded_level = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Character.prototype.charid = 0;
        Character.prototype.level = 0;
        Character.prototype.exp = 0;
        Character.prototype.views = $util.emptyArray;
        Character.prototype.skin = 0;
        Character.prototype.is_upgraded = false;
        Character.prototype.extra_emoji = $util.emptyArray;
        Character.prototype.rewarded_level = $util.emptyArray;

        Character.create = function create(properties) {
            return new Character(properties);
        };

        Character.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.charid != null && Object.hasOwnProperty.call(message, "charid"))
                writer.uint32(8).uint32(message.charid);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                writer.uint32(24).uint32(message.exp);
            if (message.views != null && message.views.length)
                for (let i = 0; i < message.views.length; ++i)
                    $root.lq.ViewSlot.encode(message.views[i], writer.uint32(34).fork()).ldelim();
            if (message.skin != null && Object.hasOwnProperty.call(message, "skin"))
                writer.uint32(40).uint32(message.skin);
            if (message.is_upgraded != null && Object.hasOwnProperty.call(message, "is_upgraded"))
                writer.uint32(48).bool(message.is_upgraded);
            if (message.extra_emoji != null && message.extra_emoji.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.extra_emoji.length; ++i)
                    writer.uint32(message.extra_emoji[i]);
                writer.ldelim();
            }
            if (message.rewarded_level != null && message.rewarded_level.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.rewarded_level.length; ++i)
                    writer.uint32(message.rewarded_level[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Character.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Character.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Character(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.charid = value;
                        else
                            delete message.charid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.views && message.views.length))
                            message.views = [];
                        message.views.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.skin = value;
                        else
                            delete message.skin;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_upgraded = value;
                        else
                            delete message.is_upgraded;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.extra_emoji && message.extra_emoji.length))
                                message.extra_emoji = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.extra_emoji.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.extra_emoji && message.extra_emoji.length))
                            message.extra_emoji = [];
                        message.extra_emoji.push(reader.uint32());
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.rewarded_level && message.rewarded_level.length))
                                message.rewarded_level = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_level.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_level && message.rewarded_level.length))
                            message.rewarded_level = [];
                        message.rewarded_level.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Character.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Character.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.charid != null && message.hasOwnProperty("charid"))
                if (!$util.isInteger(message.charid))
                    return "charid: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (!$util.isInteger(message.exp))
                    return "exp: integer expected";
            if (message.views != null && message.hasOwnProperty("views")) {
                if (!Array.isArray(message.views))
                    return "views: array expected";
                for (let i = 0; i < message.views.length; ++i) {
                    let error = $root.lq.ViewSlot.verify(message.views[i], _depth + 1);
                    if (error)
                        return "views." + error;
                }
            }
            if (message.skin != null && message.hasOwnProperty("skin"))
                if (!$util.isInteger(message.skin))
                    return "skin: integer expected";
            if (message.is_upgraded != null && message.hasOwnProperty("is_upgraded"))
                if (typeof message.is_upgraded !== "boolean")
                    return "is_upgraded: boolean expected";
            if (message.extra_emoji != null && message.hasOwnProperty("extra_emoji")) {
                if (!Array.isArray(message.extra_emoji))
                    return "extra_emoji: array expected";
                for (let i = 0; i < message.extra_emoji.length; ++i)
                    if (!$util.isInteger(message.extra_emoji[i]))
                        return "extra_emoji: integer[] expected";
            }
            if (message.rewarded_level != null && message.hasOwnProperty("rewarded_level")) {
                if (!Array.isArray(message.rewarded_level))
                    return "rewarded_level: array expected";
                for (let i = 0; i < message.rewarded_level.length; ++i)
                    if (!$util.isInteger(message.rewarded_level[i]))
                        return "rewarded_level: integer[] expected";
            }
            return null;
        };

        Character.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Character)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Character();
            if (object.charid != null)
                if (Number(object.charid) !== 0)
                    message.charid = object.charid >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.exp != null)
                if (Number(object.exp) !== 0)
                    message.exp = object.exp >>> 0;
            if (object.views) {
                if (!Array.isArray(object.views))
                    throw TypeError(".lq.Character.views: array expected");
                message.views = Array(object.views.length);
                for (let i = 0; i < object.views.length; ++i) {
                    if (typeof object.views[i] !== "object")
                        throw TypeError(".lq.Character.views: object expected");
                    message.views[i] = $root.lq.ViewSlot.fromObject(object.views[i], _depth + 1);
                }
            }
            if (object.skin != null)
                if (Number(object.skin) !== 0)
                    message.skin = object.skin >>> 0;
            if (object.is_upgraded != null)
                if (object.is_upgraded)
                    message.is_upgraded = Boolean(object.is_upgraded);
            if (object.extra_emoji) {
                if (!Array.isArray(object.extra_emoji))
                    throw TypeError(".lq.Character.extra_emoji: array expected");
                message.extra_emoji = Array(object.extra_emoji.length);
                for (let i = 0; i < object.extra_emoji.length; ++i)
                    message.extra_emoji[i] = object.extra_emoji[i] >>> 0;
            }
            if (object.rewarded_level) {
                if (!Array.isArray(object.rewarded_level))
                    throw TypeError(".lq.Character.rewarded_level: array expected");
                message.rewarded_level = Array(object.rewarded_level.length);
                for (let i = 0; i < object.rewarded_level.length; ++i)
                    message.rewarded_level[i] = object.rewarded_level[i] >>> 0;
            }
            return message;
        };

        Character.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.views = [];
                object.extra_emoji = [];
                object.rewarded_level = [];
            }
            if (options.defaults) {
                object.charid = 0;
                object.level = 0;
                object.exp = 0;
                object.skin = 0;
                object.is_upgraded = false;
            }
            if (message.charid != null && message.hasOwnProperty("charid"))
                object.charid = message.charid;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.exp != null && message.hasOwnProperty("exp"))
                object.exp = message.exp;
            if (message.views && message.views.length) {
                object.views = Array(message.views.length);
                for (let j = 0; j < message.views.length; ++j)
                    object.views[j] = $root.lq.ViewSlot.toObject(message.views[j], options);
            }
            if (message.skin != null && message.hasOwnProperty("skin"))
                object.skin = message.skin;
            if (message.is_upgraded != null && message.hasOwnProperty("is_upgraded"))
                object.is_upgraded = message.is_upgraded;
            if (message.extra_emoji && message.extra_emoji.length) {
                object.extra_emoji = Array(message.extra_emoji.length);
                for (let j = 0; j < message.extra_emoji.length; ++j)
                    object.extra_emoji[j] = message.extra_emoji[j];
            }
            if (message.rewarded_level && message.rewarded_level.length) {
                object.rewarded_level = Array(message.rewarded_level.length);
                for (let j = 0; j < message.rewarded_level.length; ++j)
                    object.rewarded_level[j] = message.rewarded_level[j];
            }
            return object;
        };

        Character.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Character.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Character";
        };

        return Character;
    })();

    lq.BuyRecord = (function() {

        function BuyRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BuyRecord.prototype.id = 0;
        BuyRecord.prototype.count = 0;

        BuyRecord.create = function create(properties) {
            return new BuyRecord(properties);
        };

        BuyRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BuyRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BuyRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BuyRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BuyRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BuyRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        BuyRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BuyRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BuyRecord();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        BuyRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        BuyRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BuyRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BuyRecord";
        };

        return BuyRecord;
    })();

    lq.ZHPShop = (function() {

        function ZHPShop(properties) {
            this.goods = [];
            this.buy_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ZHPShop.prototype.goods = $util.emptyArray;
        ZHPShop.prototype.buy_records = $util.emptyArray;
        ZHPShop.prototype.free_refresh = null;
        ZHPShop.prototype.cost_refresh = null;

        ZHPShop.create = function create(properties) {
            return new ZHPShop(properties);
        };

        ZHPShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods != null && message.goods.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.goods.length; ++i)
                    writer.uint32(message.goods[i]);
                writer.ldelim();
            }
            if (message.buy_records != null && message.buy_records.length)
                for (let i = 0; i < message.buy_records.length; ++i)
                    $root.lq.BuyRecord.encode(message.buy_records[i], writer.uint32(18).fork()).ldelim();
            if (message.free_refresh != null && Object.hasOwnProperty.call(message, "free_refresh"))
                $root.lq.ZHPShop.RefreshCount.encode(message.free_refresh, writer.uint32(26).fork()).ldelim();
            if (message.cost_refresh != null && Object.hasOwnProperty.call(message, "cost_refresh"))
                $root.lq.ZHPShop.RefreshCount.encode(message.cost_refresh, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ZHPShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ZHPShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ZHPShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.goods && message.goods.length))
                                message.goods = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.goods.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.goods && message.goods.length))
                            message.goods = [];
                        message.goods.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buy_records && message.buy_records.length))
                            message.buy_records = [];
                        message.buy_records.push($root.lq.BuyRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.free_refresh = $root.lq.ZHPShop.RefreshCount.decode(reader, reader.uint32(), undefined, _depth + 1, message.free_refresh);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.cost_refresh = $root.lq.ZHPShop.RefreshCount.decode(reader, reader.uint32(), undefined, _depth + 1, message.cost_refresh);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ZHPShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ZHPShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods != null && message.hasOwnProperty("goods")) {
                if (!Array.isArray(message.goods))
                    return "goods: array expected";
                for (let i = 0; i < message.goods.length; ++i)
                    if (!$util.isInteger(message.goods[i]))
                        return "goods: integer[] expected";
            }
            if (message.buy_records != null && message.hasOwnProperty("buy_records")) {
                if (!Array.isArray(message.buy_records))
                    return "buy_records: array expected";
                for (let i = 0; i < message.buy_records.length; ++i) {
                    let error = $root.lq.BuyRecord.verify(message.buy_records[i], _depth + 1);
                    if (error)
                        return "buy_records." + error;
                }
            }
            if (message.free_refresh != null && message.hasOwnProperty("free_refresh")) {
                let error = $root.lq.ZHPShop.RefreshCount.verify(message.free_refresh, _depth + 1);
                if (error)
                    return "free_refresh." + error;
            }
            if (message.cost_refresh != null && message.hasOwnProperty("cost_refresh")) {
                let error = $root.lq.ZHPShop.RefreshCount.verify(message.cost_refresh, _depth + 1);
                if (error)
                    return "cost_refresh." + error;
            }
            return null;
        };

        ZHPShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ZHPShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ZHPShop();
            if (object.goods) {
                if (!Array.isArray(object.goods))
                    throw TypeError(".lq.ZHPShop.goods: array expected");
                message.goods = Array(object.goods.length);
                for (let i = 0; i < object.goods.length; ++i)
                    message.goods[i] = object.goods[i] >>> 0;
            }
            if (object.buy_records) {
                if (!Array.isArray(object.buy_records))
                    throw TypeError(".lq.ZHPShop.buy_records: array expected");
                message.buy_records = Array(object.buy_records.length);
                for (let i = 0; i < object.buy_records.length; ++i) {
                    if (typeof object.buy_records[i] !== "object")
                        throw TypeError(".lq.ZHPShop.buy_records: object expected");
                    message.buy_records[i] = $root.lq.BuyRecord.fromObject(object.buy_records[i], _depth + 1);
                }
            }
            if (object.free_refresh != null) {
                if (typeof object.free_refresh !== "object")
                    throw TypeError(".lq.ZHPShop.free_refresh: object expected");
                message.free_refresh = $root.lq.ZHPShop.RefreshCount.fromObject(object.free_refresh, _depth + 1);
            }
            if (object.cost_refresh != null) {
                if (typeof object.cost_refresh !== "object")
                    throw TypeError(".lq.ZHPShop.cost_refresh: object expected");
                message.cost_refresh = $root.lq.ZHPShop.RefreshCount.fromObject(object.cost_refresh, _depth + 1);
            }
            return message;
        };

        ZHPShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.goods = [];
                object.buy_records = [];
            }
            if (options.defaults) {
                object.free_refresh = null;
                object.cost_refresh = null;
            }
            if (message.goods && message.goods.length) {
                object.goods = Array(message.goods.length);
                for (let j = 0; j < message.goods.length; ++j)
                    object.goods[j] = message.goods[j];
            }
            if (message.buy_records && message.buy_records.length) {
                object.buy_records = Array(message.buy_records.length);
                for (let j = 0; j < message.buy_records.length; ++j)
                    object.buy_records[j] = $root.lq.BuyRecord.toObject(message.buy_records[j], options);
            }
            if (message.free_refresh != null && message.hasOwnProperty("free_refresh"))
                object.free_refresh = $root.lq.ZHPShop.RefreshCount.toObject(message.free_refresh, options);
            if (message.cost_refresh != null && message.hasOwnProperty("cost_refresh"))
                object.cost_refresh = $root.lq.ZHPShop.RefreshCount.toObject(message.cost_refresh, options);
            return object;
        };

        ZHPShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ZHPShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ZHPShop";
        };

        ZHPShop.RefreshCount = (function() {

            function RefreshCount(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RefreshCount.prototype.count = 0;
            RefreshCount.prototype.limit = 0;

            RefreshCount.create = function create(properties) {
                return new RefreshCount(properties);
            };

            RefreshCount.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                    writer.uint32(16).uint32(message.limit);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RefreshCount.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RefreshCount.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ZHPShop.RefreshCount(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.limit = value;
                            else
                                delete message.limit;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RefreshCount.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RefreshCount.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.limit != null && message.hasOwnProperty("limit"))
                    if (!$util.isInteger(message.limit))
                        return "limit: integer expected";
                return null;
            };

            RefreshCount.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ZHPShop.RefreshCount)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ZHPShop.RefreshCount();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.limit != null)
                    if (Number(object.limit) !== 0)
                        message.limit = object.limit >>> 0;
                return message;
            };

            RefreshCount.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.count = 0;
                    object.limit = 0;
                }
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.limit != null && message.hasOwnProperty("limit"))
                    object.limit = message.limit;
                return object;
            };

            RefreshCount.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RefreshCount.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ZHPShop.RefreshCount";
            };

            return RefreshCount;
        })();

        return ZHPShop;
    })();

    lq.MonthTicketInfo = (function() {

        function MonthTicketInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MonthTicketInfo.prototype.id = 0;
        MonthTicketInfo.prototype.end_time = 0;
        MonthTicketInfo.prototype.last_pay_time = 0;

        MonthTicketInfo.create = function create(properties) {
            return new MonthTicketInfo(properties);
        };

        MonthTicketInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(16).uint32(message.end_time);
            if (message.last_pay_time != null && Object.hasOwnProperty.call(message, "last_pay_time"))
                writer.uint32(24).uint32(message.last_pay_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MonthTicketInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MonthTicketInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MonthTicketInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_pay_time = value;
                        else
                            delete message.last_pay_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MonthTicketInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MonthTicketInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                if (!$util.isInteger(message.last_pay_time))
                    return "last_pay_time: integer expected";
            return null;
        };

        MonthTicketInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MonthTicketInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MonthTicketInfo();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.last_pay_time != null)
                if (Number(object.last_pay_time) !== 0)
                    message.last_pay_time = object.last_pay_time >>> 0;
            return message;
        };

        MonthTicketInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.end_time = 0;
                object.last_pay_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                object.last_pay_time = message.last_pay_time;
            return object;
        };

        MonthTicketInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MonthTicketInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MonthTicketInfo";
        };

        return MonthTicketInfo;
    })();

    lq.ShopInfo = (function() {

        function ShopInfo(properties) {
            this.buy_records = [];
            this.selected_package_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ShopInfo.prototype.zhp = null;
        ShopInfo.prototype.buy_records = $util.emptyArray;
        ShopInfo.prototype.last_refresh_time = 0;
        ShopInfo.prototype.selected_package_records = $util.emptyArray;

        ShopInfo.create = function create(properties) {
            return new ShopInfo(properties);
        };

        ShopInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.zhp != null && Object.hasOwnProperty.call(message, "zhp"))
                $root.lq.ZHPShop.encode(message.zhp, writer.uint32(10).fork()).ldelim();
            if (message.buy_records != null && message.buy_records.length)
                for (let i = 0; i < message.buy_records.length; ++i)
                    $root.lq.BuyRecord.encode(message.buy_records[i], writer.uint32(18).fork()).ldelim();
            if (message.last_refresh_time != null && Object.hasOwnProperty.call(message, "last_refresh_time"))
                writer.uint32(24).uint32(message.last_refresh_time);
            if (message.selected_package_records != null && message.selected_package_records.length)
                for (let i = 0; i < message.selected_package_records.length; ++i)
                    $root.lq.ShopInfo.SelectedPackageBuyRecord.encode(message.selected_package_records[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ShopInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ShopInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ShopInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.zhp = $root.lq.ZHPShop.decode(reader, reader.uint32(), undefined, _depth + 1, message.zhp);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buy_records && message.buy_records.length))
                            message.buy_records = [];
                        message.buy_records.push($root.lq.BuyRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_refresh_time = value;
                        else
                            delete message.last_refresh_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.selected_package_records && message.selected_package_records.length))
                            message.selected_package_records = [];
                        message.selected_package_records.push($root.lq.ShopInfo.SelectedPackageBuyRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ShopInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ShopInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.zhp != null && message.hasOwnProperty("zhp")) {
                let error = $root.lq.ZHPShop.verify(message.zhp, _depth + 1);
                if (error)
                    return "zhp." + error;
            }
            if (message.buy_records != null && message.hasOwnProperty("buy_records")) {
                if (!Array.isArray(message.buy_records))
                    return "buy_records: array expected";
                for (let i = 0; i < message.buy_records.length; ++i) {
                    let error = $root.lq.BuyRecord.verify(message.buy_records[i], _depth + 1);
                    if (error)
                        return "buy_records." + error;
                }
            }
            if (message.last_refresh_time != null && message.hasOwnProperty("last_refresh_time"))
                if (!$util.isInteger(message.last_refresh_time))
                    return "last_refresh_time: integer expected";
            if (message.selected_package_records != null && message.hasOwnProperty("selected_package_records")) {
                if (!Array.isArray(message.selected_package_records))
                    return "selected_package_records: array expected";
                for (let i = 0; i < message.selected_package_records.length; ++i) {
                    let error = $root.lq.ShopInfo.SelectedPackageBuyRecord.verify(message.selected_package_records[i], _depth + 1);
                    if (error)
                        return "selected_package_records." + error;
                }
            }
            return null;
        };

        ShopInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ShopInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ShopInfo();
            if (object.zhp != null) {
                if (typeof object.zhp !== "object")
                    throw TypeError(".lq.ShopInfo.zhp: object expected");
                message.zhp = $root.lq.ZHPShop.fromObject(object.zhp, _depth + 1);
            }
            if (object.buy_records) {
                if (!Array.isArray(object.buy_records))
                    throw TypeError(".lq.ShopInfo.buy_records: array expected");
                message.buy_records = Array(object.buy_records.length);
                for (let i = 0; i < object.buy_records.length; ++i) {
                    if (typeof object.buy_records[i] !== "object")
                        throw TypeError(".lq.ShopInfo.buy_records: object expected");
                    message.buy_records[i] = $root.lq.BuyRecord.fromObject(object.buy_records[i], _depth + 1);
                }
            }
            if (object.last_refresh_time != null)
                if (Number(object.last_refresh_time) !== 0)
                    message.last_refresh_time = object.last_refresh_time >>> 0;
            if (object.selected_package_records) {
                if (!Array.isArray(object.selected_package_records))
                    throw TypeError(".lq.ShopInfo.selected_package_records: array expected");
                message.selected_package_records = Array(object.selected_package_records.length);
                for (let i = 0; i < object.selected_package_records.length; ++i) {
                    if (typeof object.selected_package_records[i] !== "object")
                        throw TypeError(".lq.ShopInfo.selected_package_records: object expected");
                    message.selected_package_records[i] = $root.lq.ShopInfo.SelectedPackageBuyRecord.fromObject(object.selected_package_records[i], _depth + 1);
                }
            }
            return message;
        };

        ShopInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.buy_records = [];
                object.selected_package_records = [];
            }
            if (options.defaults) {
                object.zhp = null;
                object.last_refresh_time = 0;
            }
            if (message.zhp != null && message.hasOwnProperty("zhp"))
                object.zhp = $root.lq.ZHPShop.toObject(message.zhp, options);
            if (message.buy_records && message.buy_records.length) {
                object.buy_records = Array(message.buy_records.length);
                for (let j = 0; j < message.buy_records.length; ++j)
                    object.buy_records[j] = $root.lq.BuyRecord.toObject(message.buy_records[j], options);
            }
            if (message.last_refresh_time != null && message.hasOwnProperty("last_refresh_time"))
                object.last_refresh_time = message.last_refresh_time;
            if (message.selected_package_records && message.selected_package_records.length) {
                object.selected_package_records = Array(message.selected_package_records.length);
                for (let j = 0; j < message.selected_package_records.length; ++j)
                    object.selected_package_records[j] = $root.lq.ShopInfo.SelectedPackageBuyRecord.toObject(message.selected_package_records[j], options);
            }
            return object;
        };

        ShopInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ShopInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ShopInfo";
        };

        ShopInfo.SelectedPackageBuyRecord = (function() {

            function SelectedPackageBuyRecord(properties) {
                this.buy_records = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SelectedPackageBuyRecord.prototype.package_id = 0;
            SelectedPackageBuyRecord.prototype.buy_records = $util.emptyArray;

            SelectedPackageBuyRecord.create = function create(properties) {
                return new SelectedPackageBuyRecord(properties);
            };

            SelectedPackageBuyRecord.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.package_id != null && Object.hasOwnProperty.call(message, "package_id"))
                    writer.uint32(8).uint32(message.package_id);
                if (message.buy_records != null && message.buy_records.length)
                    for (let i = 0; i < message.buy_records.length; ++i)
                        $root.lq.BuyRecord.encode(message.buy_records[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SelectedPackageBuyRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SelectedPackageBuyRecord.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ShopInfo.SelectedPackageBuyRecord(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.package_id = value;
                            else
                                delete message.package_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.buy_records && message.buy_records.length))
                                message.buy_records = [];
                            message.buy_records.push($root.lq.BuyRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SelectedPackageBuyRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SelectedPackageBuyRecord.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.package_id != null && message.hasOwnProperty("package_id"))
                    if (!$util.isInteger(message.package_id))
                        return "package_id: integer expected";
                if (message.buy_records != null && message.hasOwnProperty("buy_records")) {
                    if (!Array.isArray(message.buy_records))
                        return "buy_records: array expected";
                    for (let i = 0; i < message.buy_records.length; ++i) {
                        let error = $root.lq.BuyRecord.verify(message.buy_records[i], _depth + 1);
                        if (error)
                            return "buy_records." + error;
                    }
                }
                return null;
            };

            SelectedPackageBuyRecord.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ShopInfo.SelectedPackageBuyRecord)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ShopInfo.SelectedPackageBuyRecord();
                if (object.package_id != null)
                    if (Number(object.package_id) !== 0)
                        message.package_id = object.package_id >>> 0;
                if (object.buy_records) {
                    if (!Array.isArray(object.buy_records))
                        throw TypeError(".lq.ShopInfo.SelectedPackageBuyRecord.buy_records: array expected");
                    message.buy_records = Array(object.buy_records.length);
                    for (let i = 0; i < object.buy_records.length; ++i) {
                        if (typeof object.buy_records[i] !== "object")
                            throw TypeError(".lq.ShopInfo.SelectedPackageBuyRecord.buy_records: object expected");
                        message.buy_records[i] = $root.lq.BuyRecord.fromObject(object.buy_records[i], _depth + 1);
                    }
                }
                return message;
            };

            SelectedPackageBuyRecord.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.buy_records = [];
                if (options.defaults)
                    object.package_id = 0;
                if (message.package_id != null && message.hasOwnProperty("package_id"))
                    object.package_id = message.package_id;
                if (message.buy_records && message.buy_records.length) {
                    object.buy_records = Array(message.buy_records.length);
                    for (let j = 0; j < message.buy_records.length; ++j)
                        object.buy_records[j] = $root.lq.BuyRecord.toObject(message.buy_records[j], options);
                }
                return object;
            };

            SelectedPackageBuyRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SelectedPackageBuyRecord.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ShopInfo.SelectedPackageBuyRecord";
            };

            return SelectedPackageBuyRecord;
        })();

        return ShopInfo;
    })();

    lq.ChangeNicknameRecord = (function() {

        function ChangeNicknameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ChangeNicknameRecord.prototype.from = "";
        ChangeNicknameRecord.prototype.to = "";
        ChangeNicknameRecord.prototype.time = 0;

        ChangeNicknameRecord.create = function create(properties) {
            return new ChangeNicknameRecord(properties);
        };

        ChangeNicknameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                writer.uint32(10).string(message.from);
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(18).string(message.to);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(24).uint32(message.time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ChangeNicknameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ChangeNicknameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ChangeNicknameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.from = value;
                        else
                            delete message.from;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.to = value;
                        else
                            delete message.to;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ChangeNicknameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ChangeNicknameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.from != null && message.hasOwnProperty("from"))
                if (!$util.isString(message.from))
                    return "from: string expected";
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isString(message.to))
                    return "to: string expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            return null;
        };

        ChangeNicknameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ChangeNicknameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ChangeNicknameRecord();
            if (object.from != null)
                if (typeof object.from !== "string" || object.from.length)
                    message.from = String(object.from);
            if (object.to != null)
                if (typeof object.to !== "string" || object.to.length)
                    message.to = String(object.to);
            if (object.time != null)
                if (Number(object.time) !== 0)
                    message.time = object.time >>> 0;
            return message;
        };

        ChangeNicknameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.from = "";
                object.to = "";
                object.time = 0;
            }
            if (message.from != null && message.hasOwnProperty("from"))
                object.from = message.from;
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            return object;
        };

        ChangeNicknameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChangeNicknameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ChangeNicknameRecord";
        };

        return ChangeNicknameRecord;
    })();

    lq.ServerSettings = (function() {

        function ServerSettings(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ServerSettings.prototype.payment_setting = null;
        ServerSettings.prototype.payment_setting_v2 = null;
        ServerSettings.prototype.nickname_setting = null;

        ServerSettings.create = function create(properties) {
            return new ServerSettings(properties);
        };

        ServerSettings.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.payment_setting != null && Object.hasOwnProperty.call(message, "payment_setting"))
                $root.lq.PaymentSetting.encode(message.payment_setting, writer.uint32(26).fork()).ldelim();
            if (message.payment_setting_v2 != null && Object.hasOwnProperty.call(message, "payment_setting_v2"))
                $root.lq.PaymentSettingV2.encode(message.payment_setting_v2, writer.uint32(34).fork()).ldelim();
            if (message.nickname_setting != null && Object.hasOwnProperty.call(message, "nickname_setting"))
                $root.lq.NicknameSetting.encode(message.nickname_setting, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ServerSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ServerSettings.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ServerSettings(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.payment_setting = $root.lq.PaymentSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.payment_setting);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.payment_setting_v2 = $root.lq.PaymentSettingV2.decode(reader, reader.uint32(), undefined, _depth + 1, message.payment_setting_v2);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.nickname_setting = $root.lq.NicknameSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.nickname_setting);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ServerSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ServerSettings.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.payment_setting != null && message.hasOwnProperty("payment_setting")) {
                let error = $root.lq.PaymentSetting.verify(message.payment_setting, _depth + 1);
                if (error)
                    return "payment_setting." + error;
            }
            if (message.payment_setting_v2 != null && message.hasOwnProperty("payment_setting_v2")) {
                let error = $root.lq.PaymentSettingV2.verify(message.payment_setting_v2, _depth + 1);
                if (error)
                    return "payment_setting_v2." + error;
            }
            if (message.nickname_setting != null && message.hasOwnProperty("nickname_setting")) {
                let error = $root.lq.NicknameSetting.verify(message.nickname_setting, _depth + 1);
                if (error)
                    return "nickname_setting." + error;
            }
            return null;
        };

        ServerSettings.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ServerSettings)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ServerSettings();
            if (object.payment_setting != null) {
                if (typeof object.payment_setting !== "object")
                    throw TypeError(".lq.ServerSettings.payment_setting: object expected");
                message.payment_setting = $root.lq.PaymentSetting.fromObject(object.payment_setting, _depth + 1);
            }
            if (object.payment_setting_v2 != null) {
                if (typeof object.payment_setting_v2 !== "object")
                    throw TypeError(".lq.ServerSettings.payment_setting_v2: object expected");
                message.payment_setting_v2 = $root.lq.PaymentSettingV2.fromObject(object.payment_setting_v2, _depth + 1);
            }
            if (object.nickname_setting != null) {
                if (typeof object.nickname_setting !== "object")
                    throw TypeError(".lq.ServerSettings.nickname_setting: object expected");
                message.nickname_setting = $root.lq.NicknameSetting.fromObject(object.nickname_setting, _depth + 1);
            }
            return message;
        };

        ServerSettings.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.payment_setting = null;
                object.payment_setting_v2 = null;
                object.nickname_setting = null;
            }
            if (message.payment_setting != null && message.hasOwnProperty("payment_setting"))
                object.payment_setting = $root.lq.PaymentSetting.toObject(message.payment_setting, options);
            if (message.payment_setting_v2 != null && message.hasOwnProperty("payment_setting_v2"))
                object.payment_setting_v2 = $root.lq.PaymentSettingV2.toObject(message.payment_setting_v2, options);
            if (message.nickname_setting != null && message.hasOwnProperty("nickname_setting"))
                object.nickname_setting = $root.lq.NicknameSetting.toObject(message.nickname_setting, options);
            return object;
        };

        ServerSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ServerSettings.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ServerSettings";
        };

        return ServerSettings;
    })();

    lq.NicknameSetting = (function() {

        function NicknameSetting(properties) {
            this.nicknames = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NicknameSetting.prototype.enable = 0;
        NicknameSetting.prototype.nicknames = $util.emptyArray;

        NicknameSetting.create = function create(properties) {
            return new NicknameSetting(properties);
        };

        NicknameSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.enable != null && Object.hasOwnProperty.call(message, "enable"))
                writer.uint32(8).uint32(message.enable);
            if (message.nicknames != null && message.nicknames.length)
                for (let i = 0; i < message.nicknames.length; ++i)
                    writer.uint32(18).string(message.nicknames[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NicknameSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NicknameSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NicknameSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable = value;
                        else
                            delete message.enable;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.nicknames && message.nicknames.length))
                            message.nicknames = [];
                        message.nicknames.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NicknameSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NicknameSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enable != null && message.hasOwnProperty("enable"))
                if (!$util.isInteger(message.enable))
                    return "enable: integer expected";
            if (message.nicknames != null && message.hasOwnProperty("nicknames")) {
                if (!Array.isArray(message.nicknames))
                    return "nicknames: array expected";
                for (let i = 0; i < message.nicknames.length; ++i)
                    if (!$util.isString(message.nicknames[i]))
                        return "nicknames: string[] expected";
            }
            return null;
        };

        NicknameSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NicknameSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NicknameSetting();
            if (object.enable != null)
                if (Number(object.enable) !== 0)
                    message.enable = object.enable >>> 0;
            if (object.nicknames) {
                if (!Array.isArray(object.nicknames))
                    throw TypeError(".lq.NicknameSetting.nicknames: array expected");
                message.nicknames = Array(object.nicknames.length);
                for (let i = 0; i < object.nicknames.length; ++i)
                    message.nicknames[i] = String(object.nicknames[i]);
            }
            return message;
        };

        NicknameSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.nicknames = [];
            if (options.defaults)
                object.enable = 0;
            if (message.enable != null && message.hasOwnProperty("enable"))
                object.enable = message.enable;
            if (message.nicknames && message.nicknames.length) {
                object.nicknames = Array(message.nicknames.length);
                for (let j = 0; j < message.nicknames.length; ++j)
                    object.nicknames[j] = message.nicknames[j];
            }
            return object;
        };

        NicknameSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NicknameSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NicknameSetting";
        };

        return NicknameSetting;
    })();

    lq.PaymentSettingV2 = (function() {

        function PaymentSettingV2(properties) {
            this.payment_platforms = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        PaymentSettingV2.prototype.open_payment = 0;
        PaymentSettingV2.prototype.payment_platforms = $util.emptyArray;

        PaymentSettingV2.create = function create(properties) {
            return new PaymentSettingV2(properties);
        };

        PaymentSettingV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.open_payment != null && Object.hasOwnProperty.call(message, "open_payment"))
                writer.uint32(8).uint32(message.open_payment);
            if (message.payment_platforms != null && message.payment_platforms.length)
                for (let i = 0; i < message.payment_platforms.length; ++i)
                    $root.lq.PaymentSettingV2.PaymentSettingUnit.encode(message.payment_platforms[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PaymentSettingV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        PaymentSettingV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSettingV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_payment = value;
                        else
                            delete message.open_payment;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.payment_platforms && message.payment_platforms.length))
                            message.payment_platforms = [];
                        message.payment_platforms.push($root.lq.PaymentSettingV2.PaymentSettingUnit.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        PaymentSettingV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        PaymentSettingV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.open_payment != null && message.hasOwnProperty("open_payment"))
                if (!$util.isInteger(message.open_payment))
                    return "open_payment: integer expected";
            if (message.payment_platforms != null && message.hasOwnProperty("payment_platforms")) {
                if (!Array.isArray(message.payment_platforms))
                    return "payment_platforms: array expected";
                for (let i = 0; i < message.payment_platforms.length; ++i) {
                    let error = $root.lq.PaymentSettingV2.PaymentSettingUnit.verify(message.payment_platforms[i], _depth + 1);
                    if (error)
                        return "payment_platforms." + error;
                }
            }
            return null;
        };

        PaymentSettingV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.PaymentSettingV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.PaymentSettingV2();
            if (object.open_payment != null)
                if (Number(object.open_payment) !== 0)
                    message.open_payment = object.open_payment >>> 0;
            if (object.payment_platforms) {
                if (!Array.isArray(object.payment_platforms))
                    throw TypeError(".lq.PaymentSettingV2.payment_platforms: array expected");
                message.payment_platforms = Array(object.payment_platforms.length);
                for (let i = 0; i < object.payment_platforms.length; ++i) {
                    if (typeof object.payment_platforms[i] !== "object")
                        throw TypeError(".lq.PaymentSettingV2.payment_platforms: object expected");
                    message.payment_platforms[i] = $root.lq.PaymentSettingV2.PaymentSettingUnit.fromObject(object.payment_platforms[i], _depth + 1);
                }
            }
            return message;
        };

        PaymentSettingV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.payment_platforms = [];
            if (options.defaults)
                object.open_payment = 0;
            if (message.open_payment != null && message.hasOwnProperty("open_payment"))
                object.open_payment = message.open_payment;
            if (message.payment_platforms && message.payment_platforms.length) {
                object.payment_platforms = Array(message.payment_platforms.length);
                for (let j = 0; j < message.payment_platforms.length; ++j)
                    object.payment_platforms[j] = $root.lq.PaymentSettingV2.PaymentSettingUnit.toObject(message.payment_platforms[j], options);
            }
            return object;
        };

        PaymentSettingV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentSettingV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.PaymentSettingV2";
        };

        PaymentSettingV2.PaymentMaintain = (function() {

            function PaymentMaintain(properties) {
                this.enabled_channel = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PaymentMaintain.prototype.start_time = 0;
            PaymentMaintain.prototype.end_time = 0;
            PaymentMaintain.prototype.goods_click_action = 0;
            PaymentMaintain.prototype.goods_click_text = "";
            PaymentMaintain.prototype.enabled_channel = $util.emptyArray;

            PaymentMaintain.create = function create(properties) {
                return new PaymentMaintain(properties);
            };

            PaymentMaintain.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                    writer.uint32(8).uint32(message.start_time);
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(16).uint32(message.end_time);
                if (message.goods_click_action != null && Object.hasOwnProperty.call(message, "goods_click_action"))
                    writer.uint32(24).uint32(message.goods_click_action);
                if (message.goods_click_text != null && Object.hasOwnProperty.call(message, "goods_click_text"))
                    writer.uint32(34).string(message.goods_click_text);
                if (message.enabled_channel != null && message.enabled_channel.length)
                    for (let i = 0; i < message.enabled_channel.length; ++i)
                        writer.uint32(42).string(message.enabled_channel[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PaymentMaintain.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PaymentMaintain.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSettingV2.PaymentMaintain(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.start_time = value;
                            else
                                delete message.start_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.goods_click_action = value;
                            else
                                delete message.goods_click_action;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.goods_click_text = value;
                            else
                                delete message.goods_click_text;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.enabled_channel && message.enabled_channel.length))
                                message.enabled_channel = [];
                            message.enabled_channel.push(reader.string());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PaymentMaintain.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PaymentMaintain.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    if (!$util.isInteger(message.start_time))
                        return "start_time: integer expected";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.goods_click_action != null && message.hasOwnProperty("goods_click_action"))
                    if (!$util.isInteger(message.goods_click_action))
                        return "goods_click_action: integer expected";
                if (message.goods_click_text != null && message.hasOwnProperty("goods_click_text"))
                    if (!$util.isString(message.goods_click_text))
                        return "goods_click_text: string expected";
                if (message.enabled_channel != null && message.hasOwnProperty("enabled_channel")) {
                    if (!Array.isArray(message.enabled_channel))
                        return "enabled_channel: array expected";
                    for (let i = 0; i < message.enabled_channel.length; ++i)
                        if (!$util.isString(message.enabled_channel[i]))
                            return "enabled_channel: string[] expected";
                }
                return null;
            };

            PaymentMaintain.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.PaymentSettingV2.PaymentMaintain)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.PaymentSettingV2.PaymentMaintain();
                if (object.start_time != null)
                    if (Number(object.start_time) !== 0)
                        message.start_time = object.start_time >>> 0;
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.goods_click_action != null)
                    if (Number(object.goods_click_action) !== 0)
                        message.goods_click_action = object.goods_click_action >>> 0;
                if (object.goods_click_text != null)
                    if (typeof object.goods_click_text !== "string" || object.goods_click_text.length)
                        message.goods_click_text = String(object.goods_click_text);
                if (object.enabled_channel) {
                    if (!Array.isArray(object.enabled_channel))
                        throw TypeError(".lq.PaymentSettingV2.PaymentMaintain.enabled_channel: array expected");
                    message.enabled_channel = Array(object.enabled_channel.length);
                    for (let i = 0; i < object.enabled_channel.length; ++i)
                        message.enabled_channel[i] = String(object.enabled_channel[i]);
                }
                return message;
            };

            PaymentMaintain.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.enabled_channel = [];
                if (options.defaults) {
                    object.start_time = 0;
                    object.end_time = 0;
                    object.goods_click_action = 0;
                    object.goods_click_text = "";
                }
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    object.start_time = message.start_time;
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.goods_click_action != null && message.hasOwnProperty("goods_click_action"))
                    object.goods_click_action = message.goods_click_action;
                if (message.goods_click_text != null && message.hasOwnProperty("goods_click_text"))
                    object.goods_click_text = message.goods_click_text;
                if (message.enabled_channel && message.enabled_channel.length) {
                    object.enabled_channel = Array(message.enabled_channel.length);
                    for (let j = 0; j < message.enabled_channel.length; ++j)
                        object.enabled_channel[j] = message.enabled_channel[j];
                }
                return object;
            };

            PaymentMaintain.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentMaintain.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.PaymentSettingV2.PaymentMaintain";
            };

            return PaymentMaintain;
        })();

        PaymentSettingV2.PaymentSettingUnit = (function() {

            function PaymentSettingUnit(properties) {
                this.enabled_channel = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PaymentSettingUnit.prototype.platform = "";
            PaymentSettingUnit.prototype.is_show = false;
            PaymentSettingUnit.prototype.goods_click_action = 0;
            PaymentSettingUnit.prototype.goods_click_text = "";
            PaymentSettingUnit.prototype.maintain = null;
            PaymentSettingUnit.prototype.enable_for_frozen_account = false;
            PaymentSettingUnit.prototype.extra_data = "";
            PaymentSettingUnit.prototype.enabled_channel = $util.emptyArray;

            PaymentSettingUnit.create = function create(properties) {
                return new PaymentSettingUnit(properties);
            };

            PaymentSettingUnit.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    writer.uint32(10).string(message.platform);
                if (message.is_show != null && Object.hasOwnProperty.call(message, "is_show"))
                    writer.uint32(16).bool(message.is_show);
                if (message.goods_click_action != null && Object.hasOwnProperty.call(message, "goods_click_action"))
                    writer.uint32(24).uint32(message.goods_click_action);
                if (message.goods_click_text != null && Object.hasOwnProperty.call(message, "goods_click_text"))
                    writer.uint32(34).string(message.goods_click_text);
                if (message.maintain != null && Object.hasOwnProperty.call(message, "maintain"))
                    $root.lq.PaymentSettingV2.PaymentMaintain.encode(message.maintain, writer.uint32(42).fork()).ldelim();
                if (message.enable_for_frozen_account != null && Object.hasOwnProperty.call(message, "enable_for_frozen_account"))
                    writer.uint32(48).bool(message.enable_for_frozen_account);
                if (message.extra_data != null && Object.hasOwnProperty.call(message, "extra_data"))
                    writer.uint32(58).string(message.extra_data);
                if (message.enabled_channel != null && message.enabled_channel.length)
                    for (let i = 0; i < message.enabled_channel.length; ++i)
                        writer.uint32(66).string(message.enabled_channel[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PaymentSettingUnit.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PaymentSettingUnit.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSettingV2.PaymentSettingUnit(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.platform = value;
                            else
                                delete message.platform;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_show = value;
                            else
                                delete message.is_show;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.goods_click_action = value;
                            else
                                delete message.goods_click_action;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.goods_click_text = value;
                            else
                                delete message.goods_click_text;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.maintain = $root.lq.PaymentSettingV2.PaymentMaintain.decode(reader, reader.uint32(), undefined, _depth + 1, message.maintain);
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.enable_for_frozen_account = value;
                            else
                                delete message.enable_for_frozen_account;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.extra_data = value;
                            else
                                delete message.extra_data;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if (!(message.enabled_channel && message.enabled_channel.length))
                                message.enabled_channel = [];
                            message.enabled_channel.push(reader.string());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PaymentSettingUnit.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PaymentSettingUnit.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.platform != null && message.hasOwnProperty("platform"))
                    if (!$util.isString(message.platform))
                        return "platform: string expected";
                if (message.is_show != null && message.hasOwnProperty("is_show"))
                    if (typeof message.is_show !== "boolean")
                        return "is_show: boolean expected";
                if (message.goods_click_action != null && message.hasOwnProperty("goods_click_action"))
                    if (!$util.isInteger(message.goods_click_action))
                        return "goods_click_action: integer expected";
                if (message.goods_click_text != null && message.hasOwnProperty("goods_click_text"))
                    if (!$util.isString(message.goods_click_text))
                        return "goods_click_text: string expected";
                if (message.maintain != null && message.hasOwnProperty("maintain")) {
                    let error = $root.lq.PaymentSettingV2.PaymentMaintain.verify(message.maintain, _depth + 1);
                    if (error)
                        return "maintain." + error;
                }
                if (message.enable_for_frozen_account != null && message.hasOwnProperty("enable_for_frozen_account"))
                    if (typeof message.enable_for_frozen_account !== "boolean")
                        return "enable_for_frozen_account: boolean expected";
                if (message.extra_data != null && message.hasOwnProperty("extra_data"))
                    if (!$util.isString(message.extra_data))
                        return "extra_data: string expected";
                if (message.enabled_channel != null && message.hasOwnProperty("enabled_channel")) {
                    if (!Array.isArray(message.enabled_channel))
                        return "enabled_channel: array expected";
                    for (let i = 0; i < message.enabled_channel.length; ++i)
                        if (!$util.isString(message.enabled_channel[i]))
                            return "enabled_channel: string[] expected";
                }
                return null;
            };

            PaymentSettingUnit.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.PaymentSettingV2.PaymentSettingUnit)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.PaymentSettingV2.PaymentSettingUnit();
                if (object.platform != null)
                    if (typeof object.platform !== "string" || object.platform.length)
                        message.platform = String(object.platform);
                if (object.is_show != null)
                    if (object.is_show)
                        message.is_show = Boolean(object.is_show);
                if (object.goods_click_action != null)
                    if (Number(object.goods_click_action) !== 0)
                        message.goods_click_action = object.goods_click_action >>> 0;
                if (object.goods_click_text != null)
                    if (typeof object.goods_click_text !== "string" || object.goods_click_text.length)
                        message.goods_click_text = String(object.goods_click_text);
                if (object.maintain != null) {
                    if (typeof object.maintain !== "object")
                        throw TypeError(".lq.PaymentSettingV2.PaymentSettingUnit.maintain: object expected");
                    message.maintain = $root.lq.PaymentSettingV2.PaymentMaintain.fromObject(object.maintain, _depth + 1);
                }
                if (object.enable_for_frozen_account != null)
                    if (object.enable_for_frozen_account)
                        message.enable_for_frozen_account = Boolean(object.enable_for_frozen_account);
                if (object.extra_data != null)
                    if (typeof object.extra_data !== "string" || object.extra_data.length)
                        message.extra_data = String(object.extra_data);
                if (object.enabled_channel) {
                    if (!Array.isArray(object.enabled_channel))
                        throw TypeError(".lq.PaymentSettingV2.PaymentSettingUnit.enabled_channel: array expected");
                    message.enabled_channel = Array(object.enabled_channel.length);
                    for (let i = 0; i < object.enabled_channel.length; ++i)
                        message.enabled_channel[i] = String(object.enabled_channel[i]);
                }
                return message;
            };

            PaymentSettingUnit.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.enabled_channel = [];
                if (options.defaults) {
                    object.platform = "";
                    object.is_show = false;
                    object.goods_click_action = 0;
                    object.goods_click_text = "";
                    object.maintain = null;
                    object.enable_for_frozen_account = false;
                    object.extra_data = "";
                }
                if (message.platform != null && message.hasOwnProperty("platform"))
                    object.platform = message.platform;
                if (message.is_show != null && message.hasOwnProperty("is_show"))
                    object.is_show = message.is_show;
                if (message.goods_click_action != null && message.hasOwnProperty("goods_click_action"))
                    object.goods_click_action = message.goods_click_action;
                if (message.goods_click_text != null && message.hasOwnProperty("goods_click_text"))
                    object.goods_click_text = message.goods_click_text;
                if (message.maintain != null && message.hasOwnProperty("maintain"))
                    object.maintain = $root.lq.PaymentSettingV2.PaymentMaintain.toObject(message.maintain, options);
                if (message.enable_for_frozen_account != null && message.hasOwnProperty("enable_for_frozen_account"))
                    object.enable_for_frozen_account = message.enable_for_frozen_account;
                if (message.extra_data != null && message.hasOwnProperty("extra_data"))
                    object.extra_data = message.extra_data;
                if (message.enabled_channel && message.enabled_channel.length) {
                    object.enabled_channel = Array(message.enabled_channel.length);
                    for (let j = 0; j < message.enabled_channel.length; ++j)
                        object.enabled_channel[j] = message.enabled_channel[j];
                }
                return object;
            };

            PaymentSettingUnit.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentSettingUnit.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.PaymentSettingV2.PaymentSettingUnit";
            };

            return PaymentSettingUnit;
        })();

        return PaymentSettingV2;
    })();

    lq.PaymentSetting = (function() {

        function PaymentSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        PaymentSetting.prototype.open_payment = 0;
        PaymentSetting.prototype.payment_info_show_type = 0;
        PaymentSetting.prototype.payment_info = "";
        PaymentSetting.prototype.wechat = null;
        PaymentSetting.prototype.alipay = null;

        PaymentSetting.create = function create(properties) {
            return new PaymentSetting(properties);
        };

        PaymentSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.open_payment != null && Object.hasOwnProperty.call(message, "open_payment"))
                writer.uint32(8).uint32(message.open_payment);
            if (message.payment_info_show_type != null && Object.hasOwnProperty.call(message, "payment_info_show_type"))
                writer.uint32(16).uint32(message.payment_info_show_type);
            if (message.payment_info != null && Object.hasOwnProperty.call(message, "payment_info"))
                writer.uint32(26).string(message.payment_info);
            if (message.wechat != null && Object.hasOwnProperty.call(message, "wechat"))
                $root.lq.PaymentSetting.WechatData.encode(message.wechat, writer.uint32(34).fork()).ldelim();
            if (message.alipay != null && Object.hasOwnProperty.call(message, "alipay"))
                $root.lq.PaymentSetting.AlipayData.encode(message.alipay, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PaymentSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        PaymentSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_payment = value;
                        else
                            delete message.open_payment;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.payment_info_show_type = value;
                        else
                            delete message.payment_info_show_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.payment_info = value;
                        else
                            delete message.payment_info;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.wechat = $root.lq.PaymentSetting.WechatData.decode(reader, reader.uint32(), undefined, _depth + 1, message.wechat);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.alipay = $root.lq.PaymentSetting.AlipayData.decode(reader, reader.uint32(), undefined, _depth + 1, message.alipay);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        PaymentSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        PaymentSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.open_payment != null && message.hasOwnProperty("open_payment"))
                if (!$util.isInteger(message.open_payment))
                    return "open_payment: integer expected";
            if (message.payment_info_show_type != null && message.hasOwnProperty("payment_info_show_type"))
                if (!$util.isInteger(message.payment_info_show_type))
                    return "payment_info_show_type: integer expected";
            if (message.payment_info != null && message.hasOwnProperty("payment_info"))
                if (!$util.isString(message.payment_info))
                    return "payment_info: string expected";
            if (message.wechat != null && message.hasOwnProperty("wechat")) {
                let error = $root.lq.PaymentSetting.WechatData.verify(message.wechat, _depth + 1);
                if (error)
                    return "wechat." + error;
            }
            if (message.alipay != null && message.hasOwnProperty("alipay")) {
                let error = $root.lq.PaymentSetting.AlipayData.verify(message.alipay, _depth + 1);
                if (error)
                    return "alipay." + error;
            }
            return null;
        };

        PaymentSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.PaymentSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.PaymentSetting();
            if (object.open_payment != null)
                if (Number(object.open_payment) !== 0)
                    message.open_payment = object.open_payment >>> 0;
            if (object.payment_info_show_type != null)
                if (Number(object.payment_info_show_type) !== 0)
                    message.payment_info_show_type = object.payment_info_show_type >>> 0;
            if (object.payment_info != null)
                if (typeof object.payment_info !== "string" || object.payment_info.length)
                    message.payment_info = String(object.payment_info);
            if (object.wechat != null) {
                if (typeof object.wechat !== "object")
                    throw TypeError(".lq.PaymentSetting.wechat: object expected");
                message.wechat = $root.lq.PaymentSetting.WechatData.fromObject(object.wechat, _depth + 1);
            }
            if (object.alipay != null) {
                if (typeof object.alipay !== "object")
                    throw TypeError(".lq.PaymentSetting.alipay: object expected");
                message.alipay = $root.lq.PaymentSetting.AlipayData.fromObject(object.alipay, _depth + 1);
            }
            return message;
        };

        PaymentSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.open_payment = 0;
                object.payment_info_show_type = 0;
                object.payment_info = "";
                object.wechat = null;
                object.alipay = null;
            }
            if (message.open_payment != null && message.hasOwnProperty("open_payment"))
                object.open_payment = message.open_payment;
            if (message.payment_info_show_type != null && message.hasOwnProperty("payment_info_show_type"))
                object.payment_info_show_type = message.payment_info_show_type;
            if (message.payment_info != null && message.hasOwnProperty("payment_info"))
                object.payment_info = message.payment_info;
            if (message.wechat != null && message.hasOwnProperty("wechat"))
                object.wechat = $root.lq.PaymentSetting.WechatData.toObject(message.wechat, options);
            if (message.alipay != null && message.hasOwnProperty("alipay"))
                object.alipay = $root.lq.PaymentSetting.AlipayData.toObject(message.alipay, options);
            return object;
        };

        PaymentSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.PaymentSetting";
        };

        PaymentSetting.WechatData = (function() {

            function WechatData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            WechatData.prototype.disable_create = false;
            WechatData.prototype.payment_source_platform = 0;
            WechatData.prototype.enable_credit = false;

            WechatData.create = function create(properties) {
                return new WechatData(properties);
            };

            WechatData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.disable_create != null && Object.hasOwnProperty.call(message, "disable_create"))
                    writer.uint32(8).bool(message.disable_create);
                if (message.payment_source_platform != null && Object.hasOwnProperty.call(message, "payment_source_platform"))
                    writer.uint32(16).uint32(message.payment_source_platform);
                if (message.enable_credit != null && Object.hasOwnProperty.call(message, "enable_credit"))
                    writer.uint32(24).bool(message.enable_credit);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            WechatData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            WechatData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSetting.WechatData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.disable_create = value;
                            else
                                delete message.disable_create;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.payment_source_platform = value;
                            else
                                delete message.payment_source_platform;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.enable_credit = value;
                            else
                                delete message.enable_credit;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            WechatData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            WechatData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.disable_create != null && message.hasOwnProperty("disable_create"))
                    if (typeof message.disable_create !== "boolean")
                        return "disable_create: boolean expected";
                if (message.payment_source_platform != null && message.hasOwnProperty("payment_source_platform"))
                    if (!$util.isInteger(message.payment_source_platform))
                        return "payment_source_platform: integer expected";
                if (message.enable_credit != null && message.hasOwnProperty("enable_credit"))
                    if (typeof message.enable_credit !== "boolean")
                        return "enable_credit: boolean expected";
                return null;
            };

            WechatData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.PaymentSetting.WechatData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.PaymentSetting.WechatData();
                if (object.disable_create != null)
                    if (object.disable_create)
                        message.disable_create = Boolean(object.disable_create);
                if (object.payment_source_platform != null)
                    if (Number(object.payment_source_platform) !== 0)
                        message.payment_source_platform = object.payment_source_platform >>> 0;
                if (object.enable_credit != null)
                    if (object.enable_credit)
                        message.enable_credit = Boolean(object.enable_credit);
                return message;
            };

            WechatData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.disable_create = false;
                    object.payment_source_platform = 0;
                    object.enable_credit = false;
                }
                if (message.disable_create != null && message.hasOwnProperty("disable_create"))
                    object.disable_create = message.disable_create;
                if (message.payment_source_platform != null && message.hasOwnProperty("payment_source_platform"))
                    object.payment_source_platform = message.payment_source_platform;
                if (message.enable_credit != null && message.hasOwnProperty("enable_credit"))
                    object.enable_credit = message.enable_credit;
                return object;
            };

            WechatData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            WechatData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.PaymentSetting.WechatData";
            };

            return WechatData;
        })();

        PaymentSetting.AlipayData = (function() {

            function AlipayData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AlipayData.prototype.disable_create = false;
            AlipayData.prototype.payment_source_platform = 0;

            AlipayData.create = function create(properties) {
                return new AlipayData(properties);
            };

            AlipayData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.disable_create != null && Object.hasOwnProperty.call(message, "disable_create"))
                    writer.uint32(8).bool(message.disable_create);
                if (message.payment_source_platform != null && Object.hasOwnProperty.call(message, "payment_source_platform"))
                    writer.uint32(16).uint32(message.payment_source_platform);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AlipayData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AlipayData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PaymentSetting.AlipayData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.disable_create = value;
                            else
                                delete message.disable_create;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.payment_source_platform = value;
                            else
                                delete message.payment_source_platform;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AlipayData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AlipayData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.disable_create != null && message.hasOwnProperty("disable_create"))
                    if (typeof message.disable_create !== "boolean")
                        return "disable_create: boolean expected";
                if (message.payment_source_platform != null && message.hasOwnProperty("payment_source_platform"))
                    if (!$util.isInteger(message.payment_source_platform))
                        return "payment_source_platform: integer expected";
                return null;
            };

            AlipayData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.PaymentSetting.AlipayData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.PaymentSetting.AlipayData();
                if (object.disable_create != null)
                    if (object.disable_create)
                        message.disable_create = Boolean(object.disable_create);
                if (object.payment_source_platform != null)
                    if (Number(object.payment_source_platform) !== 0)
                        message.payment_source_platform = object.payment_source_platform >>> 0;
                return message;
            };

            AlipayData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.disable_create = false;
                    object.payment_source_platform = 0;
                }
                if (message.disable_create != null && message.hasOwnProperty("disable_create"))
                    object.disable_create = message.disable_create;
                if (message.payment_source_platform != null && message.hasOwnProperty("payment_source_platform"))
                    object.payment_source_platform = message.payment_source_platform;
                return object;
            };

            AlipayData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AlipayData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.PaymentSetting.AlipayData";
            };

            return AlipayData;
        })();

        return PaymentSetting;
    })();

    lq.AccountSetting = (function() {

        function AccountSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountSetting.prototype.key = 0;
        AccountSetting.prototype.value = 0;

        AccountSetting.create = function create(properties) {
            return new AccountSetting(properties);
        };

        AccountSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(8).uint32(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(16).uint32(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isInteger(message.key))
                    return "key: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        AccountSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountSetting();
            if (object.key != null)
                if (Number(object.key) !== 0)
                    message.key = object.key >>> 0;
            if (object.value != null)
                if (Number(object.value) !== 0)
                    message.value = object.value >>> 0;
            return message;
        };

        AccountSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.key = 0;
                object.value = 0;
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        AccountSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountSetting";
        };

        return AccountSetting;
    })();

    lq.ChestData = (function() {

        function ChestData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ChestData.prototype.chest_id = 0;
        ChestData.prototype.total_open_count = 0;
        ChestData.prototype.consume_count = 0;
        ChestData.prototype.face_black_count = 0;

        ChestData.create = function create(properties) {
            return new ChestData(properties);
        };

        ChestData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(8).uint32(message.chest_id);
            if (message.total_open_count != null && Object.hasOwnProperty.call(message, "total_open_count"))
                writer.uint32(16).uint32(message.total_open_count);
            if (message.consume_count != null && Object.hasOwnProperty.call(message, "consume_count"))
                writer.uint32(24).uint32(message.consume_count);
            if (message.face_black_count != null && Object.hasOwnProperty.call(message, "face_black_count"))
                writer.uint32(32).uint32(message.face_black_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ChestData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ChestData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ChestData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_open_count = value;
                        else
                            delete message.total_open_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.consume_count = value;
                        else
                            delete message.consume_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.face_black_count = value;
                        else
                            delete message.face_black_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ChestData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ChestData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                if (!$util.isInteger(message.total_open_count))
                    return "total_open_count: integer expected";
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                if (!$util.isInteger(message.consume_count))
                    return "consume_count: integer expected";
            if (message.face_black_count != null && message.hasOwnProperty("face_black_count"))
                if (!$util.isInteger(message.face_black_count))
                    return "face_black_count: integer expected";
            return null;
        };

        ChestData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ChestData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ChestData();
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.total_open_count != null)
                if (Number(object.total_open_count) !== 0)
                    message.total_open_count = object.total_open_count >>> 0;
            if (object.consume_count != null)
                if (Number(object.consume_count) !== 0)
                    message.consume_count = object.consume_count >>> 0;
            if (object.face_black_count != null)
                if (Number(object.face_black_count) !== 0)
                    message.face_black_count = object.face_black_count >>> 0;
            return message;
        };

        ChestData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chest_id = 0;
                object.total_open_count = 0;
                object.consume_count = 0;
                object.face_black_count = 0;
            }
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                object.total_open_count = message.total_open_count;
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                object.consume_count = message.consume_count;
            if (message.face_black_count != null && message.hasOwnProperty("face_black_count"))
                object.face_black_count = message.face_black_count;
            return object;
        };

        ChestData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChestData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ChestData";
        };

        return ChestData;
    })();

    lq.ChestDataV2 = (function() {

        function ChestDataV2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ChestDataV2.prototype.chest_id = 0;
        ChestDataV2.prototype.total_open_count = 0;
        ChestDataV2.prototype.face_black_count = 0;
        ChestDataV2.prototype.ticket_face_black_count = 0;

        ChestDataV2.create = function create(properties) {
            return new ChestDataV2(properties);
        };

        ChestDataV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(8).uint32(message.chest_id);
            if (message.total_open_count != null && Object.hasOwnProperty.call(message, "total_open_count"))
                writer.uint32(16).uint32(message.total_open_count);
            if (message.face_black_count != null && Object.hasOwnProperty.call(message, "face_black_count"))
                writer.uint32(24).uint32(message.face_black_count);
            if (message.ticket_face_black_count != null && Object.hasOwnProperty.call(message, "ticket_face_black_count"))
                writer.uint32(32).uint32(message.ticket_face_black_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ChestDataV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ChestDataV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ChestDataV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_open_count = value;
                        else
                            delete message.total_open_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.face_black_count = value;
                        else
                            delete message.face_black_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_face_black_count = value;
                        else
                            delete message.ticket_face_black_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ChestDataV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ChestDataV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                if (!$util.isInteger(message.total_open_count))
                    return "total_open_count: integer expected";
            if (message.face_black_count != null && message.hasOwnProperty("face_black_count"))
                if (!$util.isInteger(message.face_black_count))
                    return "face_black_count: integer expected";
            if (message.ticket_face_black_count != null && message.hasOwnProperty("ticket_face_black_count"))
                if (!$util.isInteger(message.ticket_face_black_count))
                    return "ticket_face_black_count: integer expected";
            return null;
        };

        ChestDataV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ChestDataV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ChestDataV2();
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.total_open_count != null)
                if (Number(object.total_open_count) !== 0)
                    message.total_open_count = object.total_open_count >>> 0;
            if (object.face_black_count != null)
                if (Number(object.face_black_count) !== 0)
                    message.face_black_count = object.face_black_count >>> 0;
            if (object.ticket_face_black_count != null)
                if (Number(object.ticket_face_black_count) !== 0)
                    message.ticket_face_black_count = object.ticket_face_black_count >>> 0;
            return message;
        };

        ChestDataV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chest_id = 0;
                object.total_open_count = 0;
                object.face_black_count = 0;
                object.ticket_face_black_count = 0;
            }
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                object.total_open_count = message.total_open_count;
            if (message.face_black_count != null && message.hasOwnProperty("face_black_count"))
                object.face_black_count = message.face_black_count;
            if (message.ticket_face_black_count != null && message.hasOwnProperty("ticket_face_black_count"))
                object.ticket_face_black_count = message.ticket_face_black_count;
            return object;
        };

        ChestDataV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChestDataV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ChestDataV2";
        };

        return ChestDataV2;
    })();

    lq.FaithData = (function() {

        function FaithData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FaithData.prototype.faith_id = 0;
        FaithData.prototype.total_open_count = 0;
        FaithData.prototype.consume_count = 0;
        FaithData.prototype.modify_count = 0;

        FaithData.create = function create(properties) {
            return new FaithData(properties);
        };

        FaithData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.faith_id != null && Object.hasOwnProperty.call(message, "faith_id"))
                writer.uint32(8).uint32(message.faith_id);
            if (message.total_open_count != null && Object.hasOwnProperty.call(message, "total_open_count"))
                writer.uint32(16).uint32(message.total_open_count);
            if (message.consume_count != null && Object.hasOwnProperty.call(message, "consume_count"))
                writer.uint32(24).uint32(message.consume_count);
            if (message.modify_count != null && Object.hasOwnProperty.call(message, "modify_count"))
                writer.uint32(32).int32(message.modify_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FaithData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FaithData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FaithData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.faith_id = value;
                        else
                            delete message.faith_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_open_count = value;
                        else
                            delete message.total_open_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.consume_count = value;
                        else
                            delete message.consume_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_count = value;
                        else
                            delete message.modify_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FaithData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FaithData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.faith_id != null && message.hasOwnProperty("faith_id"))
                if (!$util.isInteger(message.faith_id))
                    return "faith_id: integer expected";
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                if (!$util.isInteger(message.total_open_count))
                    return "total_open_count: integer expected";
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                if (!$util.isInteger(message.consume_count))
                    return "consume_count: integer expected";
            if (message.modify_count != null && message.hasOwnProperty("modify_count"))
                if (!$util.isInteger(message.modify_count))
                    return "modify_count: integer expected";
            return null;
        };

        FaithData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FaithData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FaithData();
            if (object.faith_id != null)
                if (Number(object.faith_id) !== 0)
                    message.faith_id = object.faith_id >>> 0;
            if (object.total_open_count != null)
                if (Number(object.total_open_count) !== 0)
                    message.total_open_count = object.total_open_count >>> 0;
            if (object.consume_count != null)
                if (Number(object.consume_count) !== 0)
                    message.consume_count = object.consume_count >>> 0;
            if (object.modify_count != null)
                if (Number(object.modify_count) !== 0)
                    message.modify_count = object.modify_count | 0;
            return message;
        };

        FaithData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.faith_id = 0;
                object.total_open_count = 0;
                object.consume_count = 0;
                object.modify_count = 0;
            }
            if (message.faith_id != null && message.hasOwnProperty("faith_id"))
                object.faith_id = message.faith_id;
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                object.total_open_count = message.total_open_count;
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                object.consume_count = message.consume_count;
            if (message.modify_count != null && message.hasOwnProperty("modify_count"))
                object.modify_count = message.modify_count;
            return object;
        };

        FaithData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FaithData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FaithData";
        };

        return FaithData;
    })();

    lq.CustomizedContestBase = (function() {

        function CustomizedContestBase(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestBase.prototype.unique_id = 0;
        CustomizedContestBase.prototype.contest_id = 0;
        CustomizedContestBase.prototype.contest_name = "";
        CustomizedContestBase.prototype.state = 0;
        CustomizedContestBase.prototype.creator_id = 0;
        CustomizedContestBase.prototype.create_time = 0;
        CustomizedContestBase.prototype.start_time = 0;
        CustomizedContestBase.prototype.finish_time = 0;
        CustomizedContestBase.prototype.open = false;
        CustomizedContestBase.prototype.contest_type = 0;
        CustomizedContestBase.prototype.public_notice = "";
        CustomizedContestBase.prototype.check_state = 0;
        CustomizedContestBase.prototype.checking_name = "";
        CustomizedContestBase.prototype.rank_type = 0;
        CustomizedContestBase.prototype.show_team_rank = false;

        CustomizedContestBase.create = function create(properties) {
            return new CustomizedContestBase(properties);
        };

        CustomizedContestBase.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.contest_id != null && Object.hasOwnProperty.call(message, "contest_id"))
                writer.uint32(16).uint32(message.contest_id);
            if (message.contest_name != null && Object.hasOwnProperty.call(message, "contest_name"))
                writer.uint32(26).string(message.contest_name);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(32).uint32(message.state);
            if (message.creator_id != null && Object.hasOwnProperty.call(message, "creator_id"))
                writer.uint32(40).uint32(message.creator_id);
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(48).uint32(message.create_time);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(56).uint32(message.start_time);
            if (message.finish_time != null && Object.hasOwnProperty.call(message, "finish_time"))
                writer.uint32(64).uint32(message.finish_time);
            if (message.open != null && Object.hasOwnProperty.call(message, "open"))
                writer.uint32(72).bool(message.open);
            if (message.contest_type != null && Object.hasOwnProperty.call(message, "contest_type"))
                writer.uint32(80).uint32(message.contest_type);
            if (message.public_notice != null && Object.hasOwnProperty.call(message, "public_notice"))
                writer.uint32(90).string(message.public_notice);
            if (message.check_state != null && Object.hasOwnProperty.call(message, "check_state"))
                writer.uint32(96).uint32(message.check_state);
            if (message.checking_name != null && Object.hasOwnProperty.call(message, "checking_name"))
                writer.uint32(106).string(message.checking_name);
            if (message.rank_type != null && Object.hasOwnProperty.call(message, "rank_type"))
                writer.uint32(112).uint32(message.rank_type);
            if (message.show_team_rank != null && Object.hasOwnProperty.call(message, "show_team_rank"))
                writer.uint32(120).bool(message.show_team_rank);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestBase.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestBase.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestBase(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_id = value;
                        else
                            delete message.contest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.contest_name = value;
                        else
                            delete message.contest_name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.creator_id = value;
                        else
                            delete message.creator_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_time = value;
                        else
                            delete message.finish_time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.open = value;
                        else
                            delete message.open;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_type = value;
                        else
                            delete message.contest_type;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.public_notice = value;
                        else
                            delete message.public_notice;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.check_state = value;
                        else
                            delete message.check_state;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.checking_name = value;
                        else
                            delete message.checking_name;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_type = value;
                        else
                            delete message.rank_type;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.show_team_rank = value;
                        else
                            delete message.show_team_rank;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestBase.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestBase.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                if (!$util.isInteger(message.contest_id))
                    return "contest_id: integer expected";
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                if (!$util.isString(message.contest_name))
                    return "contest_name: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.creator_id != null && message.hasOwnProperty("creator_id"))
                if (!$util.isInteger(message.creator_id))
                    return "creator_id: integer expected";
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                if (!$util.isInteger(message.finish_time))
                    return "finish_time: integer expected";
            if (message.open != null && message.hasOwnProperty("open"))
                if (typeof message.open !== "boolean")
                    return "open: boolean expected";
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                if (!$util.isInteger(message.contest_type))
                    return "contest_type: integer expected";
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                if (!$util.isString(message.public_notice))
                    return "public_notice: string expected";
            if (message.check_state != null && message.hasOwnProperty("check_state"))
                if (!$util.isInteger(message.check_state))
                    return "check_state: integer expected";
            if (message.checking_name != null && message.hasOwnProperty("checking_name"))
                if (!$util.isString(message.checking_name))
                    return "checking_name: string expected";
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                if (!$util.isInteger(message.rank_type))
                    return "rank_type: integer expected";
            if (message.show_team_rank != null && message.hasOwnProperty("show_team_rank"))
                if (typeof message.show_team_rank !== "boolean")
                    return "show_team_rank: boolean expected";
            return null;
        };

        CustomizedContestBase.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestBase)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestBase();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.contest_id != null)
                if (Number(object.contest_id) !== 0)
                    message.contest_id = object.contest_id >>> 0;
            if (object.contest_name != null)
                if (typeof object.contest_name !== "string" || object.contest_name.length)
                    message.contest_name = String(object.contest_name);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.creator_id != null)
                if (Number(object.creator_id) !== 0)
                    message.creator_id = object.creator_id >>> 0;
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.finish_time != null)
                if (Number(object.finish_time) !== 0)
                    message.finish_time = object.finish_time >>> 0;
            if (object.open != null)
                if (object.open)
                    message.open = Boolean(object.open);
            if (object.contest_type != null)
                if (Number(object.contest_type) !== 0)
                    message.contest_type = object.contest_type >>> 0;
            if (object.public_notice != null)
                if (typeof object.public_notice !== "string" || object.public_notice.length)
                    message.public_notice = String(object.public_notice);
            if (object.check_state != null)
                if (Number(object.check_state) !== 0)
                    message.check_state = object.check_state >>> 0;
            if (object.checking_name != null)
                if (typeof object.checking_name !== "string" || object.checking_name.length)
                    message.checking_name = String(object.checking_name);
            if (object.rank_type != null)
                if (Number(object.rank_type) !== 0)
                    message.rank_type = object.rank_type >>> 0;
            if (object.show_team_rank != null)
                if (object.show_team_rank)
                    message.show_team_rank = Boolean(object.show_team_rank);
            return message;
        };

        CustomizedContestBase.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.contest_id = 0;
                object.contest_name = "";
                object.state = 0;
                object.creator_id = 0;
                object.create_time = 0;
                object.start_time = 0;
                object.finish_time = 0;
                object.open = false;
                object.contest_type = 0;
                object.public_notice = "";
                object.check_state = 0;
                object.checking_name = "";
                object.rank_type = 0;
                object.show_team_rank = false;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                object.contest_id = message.contest_id;
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                object.contest_name = message.contest_name;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.creator_id != null && message.hasOwnProperty("creator_id"))
                object.creator_id = message.creator_id;
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                object.finish_time = message.finish_time;
            if (message.open != null && message.hasOwnProperty("open"))
                object.open = message.open;
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                object.contest_type = message.contest_type;
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                object.public_notice = message.public_notice;
            if (message.check_state != null && message.hasOwnProperty("check_state"))
                object.check_state = message.check_state;
            if (message.checking_name != null && message.hasOwnProperty("checking_name"))
                object.checking_name = message.checking_name;
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                object.rank_type = message.rank_type;
            if (message.show_team_rank != null && message.hasOwnProperty("show_team_rank"))
                object.show_team_rank = message.show_team_rank;
            return object;
        };

        CustomizedContestBase.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestBase.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestBase";
        };

        return CustomizedContestBase;
    })();

    lq.CustomizedContestExtend = (function() {

        function CustomizedContestExtend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestExtend.prototype.unique_id = 0;
        CustomizedContestExtend.prototype.public_notice = "";

        CustomizedContestExtend.create = function create(properties) {
            return new CustomizedContestExtend(properties);
        };

        CustomizedContestExtend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.public_notice != null && Object.hasOwnProperty.call(message, "public_notice"))
                writer.uint32(18).string(message.public_notice);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestExtend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestExtend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestExtend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.public_notice = value;
                        else
                            delete message.public_notice;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestExtend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestExtend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                if (!$util.isString(message.public_notice))
                    return "public_notice: string expected";
            return null;
        };

        CustomizedContestExtend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestExtend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestExtend();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.public_notice != null)
                if (typeof object.public_notice !== "string" || object.public_notice.length)
                    message.public_notice = String(object.public_notice);
            return message;
        };

        CustomizedContestExtend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.public_notice = "";
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                object.public_notice = message.public_notice;
            return object;
        };

        CustomizedContestExtend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestExtend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestExtend";
        };

        return CustomizedContestExtend;
    })();

    lq.CustomizedContestAbstract = (function() {

        function CustomizedContestAbstract(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestAbstract.prototype.unique_id = 0;
        CustomizedContestAbstract.prototype.contest_id = 0;
        CustomizedContestAbstract.prototype.contest_name = "";
        CustomizedContestAbstract.prototype.state = 0;
        CustomizedContestAbstract.prototype.creator_id = 0;
        CustomizedContestAbstract.prototype.create_time = 0;
        CustomizedContestAbstract.prototype.start_time = 0;
        CustomizedContestAbstract.prototype.finish_time = 0;
        CustomizedContestAbstract.prototype.open = false;
        CustomizedContestAbstract.prototype.public_notice = "";
        CustomizedContestAbstract.prototype.contest_type = 0;

        CustomizedContestAbstract.create = function create(properties) {
            return new CustomizedContestAbstract(properties);
        };

        CustomizedContestAbstract.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.contest_id != null && Object.hasOwnProperty.call(message, "contest_id"))
                writer.uint32(16).uint32(message.contest_id);
            if (message.contest_name != null && Object.hasOwnProperty.call(message, "contest_name"))
                writer.uint32(26).string(message.contest_name);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(32).uint32(message.state);
            if (message.creator_id != null && Object.hasOwnProperty.call(message, "creator_id"))
                writer.uint32(40).uint32(message.creator_id);
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(48).uint32(message.create_time);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(56).uint32(message.start_time);
            if (message.finish_time != null && Object.hasOwnProperty.call(message, "finish_time"))
                writer.uint32(64).uint32(message.finish_time);
            if (message.open != null && Object.hasOwnProperty.call(message, "open"))
                writer.uint32(72).bool(message.open);
            if (message.public_notice != null && Object.hasOwnProperty.call(message, "public_notice"))
                writer.uint32(82).string(message.public_notice);
            if (message.contest_type != null && Object.hasOwnProperty.call(message, "contest_type"))
                writer.uint32(88).uint32(message.contest_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestAbstract.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestAbstract.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestAbstract(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_id = value;
                        else
                            delete message.contest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.contest_name = value;
                        else
                            delete message.contest_name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.creator_id = value;
                        else
                            delete message.creator_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_time = value;
                        else
                            delete message.finish_time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.open = value;
                        else
                            delete message.open;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.public_notice = value;
                        else
                            delete message.public_notice;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_type = value;
                        else
                            delete message.contest_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestAbstract.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestAbstract.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                if (!$util.isInteger(message.contest_id))
                    return "contest_id: integer expected";
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                if (!$util.isString(message.contest_name))
                    return "contest_name: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.creator_id != null && message.hasOwnProperty("creator_id"))
                if (!$util.isInteger(message.creator_id))
                    return "creator_id: integer expected";
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                if (!$util.isInteger(message.finish_time))
                    return "finish_time: integer expected";
            if (message.open != null && message.hasOwnProperty("open"))
                if (typeof message.open !== "boolean")
                    return "open: boolean expected";
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                if (!$util.isString(message.public_notice))
                    return "public_notice: string expected";
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                if (!$util.isInteger(message.contest_type))
                    return "contest_type: integer expected";
            return null;
        };

        CustomizedContestAbstract.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestAbstract)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestAbstract();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.contest_id != null)
                if (Number(object.contest_id) !== 0)
                    message.contest_id = object.contest_id >>> 0;
            if (object.contest_name != null)
                if (typeof object.contest_name !== "string" || object.contest_name.length)
                    message.contest_name = String(object.contest_name);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.creator_id != null)
                if (Number(object.creator_id) !== 0)
                    message.creator_id = object.creator_id >>> 0;
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.finish_time != null)
                if (Number(object.finish_time) !== 0)
                    message.finish_time = object.finish_time >>> 0;
            if (object.open != null)
                if (object.open)
                    message.open = Boolean(object.open);
            if (object.public_notice != null)
                if (typeof object.public_notice !== "string" || object.public_notice.length)
                    message.public_notice = String(object.public_notice);
            if (object.contest_type != null)
                if (Number(object.contest_type) !== 0)
                    message.contest_type = object.contest_type >>> 0;
            return message;
        };

        CustomizedContestAbstract.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.contest_id = 0;
                object.contest_name = "";
                object.state = 0;
                object.creator_id = 0;
                object.create_time = 0;
                object.start_time = 0;
                object.finish_time = 0;
                object.open = false;
                object.public_notice = "";
                object.contest_type = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                object.contest_id = message.contest_id;
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                object.contest_name = message.contest_name;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.creator_id != null && message.hasOwnProperty("creator_id"))
                object.creator_id = message.creator_id;
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                object.finish_time = message.finish_time;
            if (message.open != null && message.hasOwnProperty("open"))
                object.open = message.open;
            if (message.public_notice != null && message.hasOwnProperty("public_notice"))
                object.public_notice = message.public_notice;
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                object.contest_type = message.contest_type;
            return object;
        };

        CustomizedContestAbstract.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestAbstract.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestAbstract";
        };

        return CustomizedContestAbstract;
    })();

    lq.CustomizedContestDetail = (function() {

        function CustomizedContestDetail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestDetail.prototype.unique_id = 0;
        CustomizedContestDetail.prototype.contest_id = 0;
        CustomizedContestDetail.prototype.contest_name = "";
        CustomizedContestDetail.prototype.state = 0;
        CustomizedContestDetail.prototype.create_time = 0;
        CustomizedContestDetail.prototype.start_time = 0;
        CustomizedContestDetail.prototype.finish_time = 0;
        CustomizedContestDetail.prototype.open = false;
        CustomizedContestDetail.prototype.rank_rule = 0;
        CustomizedContestDetail.prototype.game_mode = null;
        CustomizedContestDetail.prototype.private_notice = "";
        CustomizedContestDetail.prototype.observer_switch = 0;
        CustomizedContestDetail.prototype.emoji_switch = 0;
        CustomizedContestDetail.prototype.contest_type = 0;
        CustomizedContestDetail.prototype.disable_broadcast = 0;
        CustomizedContestDetail.prototype.signup_start_time = 0;
        CustomizedContestDetail.prototype.signup_end_time = 0;
        CustomizedContestDetail.prototype.signup_type = 0;
        CustomizedContestDetail.prototype.auto_match = 0;
        CustomizedContestDetail.prototype.rank_type = 0;
        CustomizedContestDetail.prototype.show_team_rank = false;
        CustomizedContestDetail.prototype.tied_rank = 0;
        CustomizedContestDetail.prototype.match_start_time = 0;
        CustomizedContestDetail.prototype.match_end_time = 0;
        CustomizedContestDetail.prototype.open_rank_percent = 0;

        CustomizedContestDetail.create = function create(properties) {
            return new CustomizedContestDetail(properties);
        };

        CustomizedContestDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.contest_id != null && Object.hasOwnProperty.call(message, "contest_id"))
                writer.uint32(16).uint32(message.contest_id);
            if (message.contest_name != null && Object.hasOwnProperty.call(message, "contest_name"))
                writer.uint32(26).string(message.contest_name);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(32).uint32(message.state);
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(48).uint32(message.create_time);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(56).uint32(message.start_time);
            if (message.finish_time != null && Object.hasOwnProperty.call(message, "finish_time"))
                writer.uint32(64).uint32(message.finish_time);
            if (message.open != null && Object.hasOwnProperty.call(message, "open"))
                writer.uint32(72).bool(message.open);
            if (message.rank_rule != null && Object.hasOwnProperty.call(message, "rank_rule"))
                writer.uint32(80).uint32(message.rank_rule);
            if (message.game_mode != null && Object.hasOwnProperty.call(message, "game_mode"))
                $root.lq.GameMode.encode(message.game_mode, writer.uint32(90).fork()).ldelim();
            if (message.private_notice != null && Object.hasOwnProperty.call(message, "private_notice"))
                writer.uint32(98).string(message.private_notice);
            if (message.observer_switch != null && Object.hasOwnProperty.call(message, "observer_switch"))
                writer.uint32(104).uint32(message.observer_switch);
            if (message.emoji_switch != null && Object.hasOwnProperty.call(message, "emoji_switch"))
                writer.uint32(112).uint32(message.emoji_switch);
            if (message.contest_type != null && Object.hasOwnProperty.call(message, "contest_type"))
                writer.uint32(120).uint32(message.contest_type);
            if (message.disable_broadcast != null && Object.hasOwnProperty.call(message, "disable_broadcast"))
                writer.uint32(128).uint32(message.disable_broadcast);
            if (message.signup_start_time != null && Object.hasOwnProperty.call(message, "signup_start_time"))
                writer.uint32(136).uint32(message.signup_start_time);
            if (message.signup_end_time != null && Object.hasOwnProperty.call(message, "signup_end_time"))
                writer.uint32(144).uint32(message.signup_end_time);
            if (message.signup_type != null && Object.hasOwnProperty.call(message, "signup_type"))
                writer.uint32(152).uint32(message.signup_type);
            if (message.auto_match != null && Object.hasOwnProperty.call(message, "auto_match"))
                writer.uint32(160).uint32(message.auto_match);
            if (message.rank_type != null && Object.hasOwnProperty.call(message, "rank_type"))
                writer.uint32(168).uint32(message.rank_type);
            if (message.show_team_rank != null && Object.hasOwnProperty.call(message, "show_team_rank"))
                writer.uint32(176).bool(message.show_team_rank);
            if (message.tied_rank != null && Object.hasOwnProperty.call(message, "tied_rank"))
                writer.uint32(184).uint32(message.tied_rank);
            if (message.match_start_time != null && Object.hasOwnProperty.call(message, "match_start_time"))
                writer.uint32(192).uint32(message.match_start_time);
            if (message.match_end_time != null && Object.hasOwnProperty.call(message, "match_end_time"))
                writer.uint32(200).uint32(message.match_end_time);
            if (message.open_rank_percent != null && Object.hasOwnProperty.call(message, "open_rank_percent"))
                writer.uint32(208).uint32(message.open_rank_percent);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_id = value;
                        else
                            delete message.contest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.contest_name = value;
                        else
                            delete message.contest_name;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_time = value;
                        else
                            delete message.finish_time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.open = value;
                        else
                            delete message.open;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_rule = value;
                        else
                            delete message.rank_rule;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.game_mode = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_mode);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.private_notice = value;
                        else
                            delete message.private_notice;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.observer_switch = value;
                        else
                            delete message.observer_switch;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.emoji_switch = value;
                        else
                            delete message.emoji_switch;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_type = value;
                        else
                            delete message.contest_type;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_broadcast = value;
                        else
                            delete message.disable_broadcast;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.signup_start_time = value;
                        else
                            delete message.signup_start_time;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.signup_end_time = value;
                        else
                            delete message.signup_end_time;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.signup_type = value;
                        else
                            delete message.signup_type;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.auto_match = value;
                        else
                            delete message.auto_match;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_type = value;
                        else
                            delete message.rank_type;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.show_team_rank = value;
                        else
                            delete message.show_team_rank;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tied_rank = value;
                        else
                            delete message.tied_rank;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_start_time = value;
                        else
                            delete message.match_start_time;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_end_time = value;
                        else
                            delete message.match_end_time;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_rank_percent = value;
                        else
                            delete message.open_rank_percent;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                if (!$util.isInteger(message.contest_id))
                    return "contest_id: integer expected";
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                if (!$util.isString(message.contest_name))
                    return "contest_name: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                if (!$util.isInteger(message.finish_time))
                    return "finish_time: integer expected";
            if (message.open != null && message.hasOwnProperty("open"))
                if (typeof message.open !== "boolean")
                    return "open: boolean expected";
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                if (!$util.isInteger(message.rank_rule))
                    return "rank_rule: integer expected";
            if (message.game_mode != null && message.hasOwnProperty("game_mode")) {
                let error = $root.lq.GameMode.verify(message.game_mode, _depth + 1);
                if (error)
                    return "game_mode." + error;
            }
            if (message.private_notice != null && message.hasOwnProperty("private_notice"))
                if (!$util.isString(message.private_notice))
                    return "private_notice: string expected";
            if (message.observer_switch != null && message.hasOwnProperty("observer_switch"))
                if (!$util.isInteger(message.observer_switch))
                    return "observer_switch: integer expected";
            if (message.emoji_switch != null && message.hasOwnProperty("emoji_switch"))
                if (!$util.isInteger(message.emoji_switch))
                    return "emoji_switch: integer expected";
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                if (!$util.isInteger(message.contest_type))
                    return "contest_type: integer expected";
            if (message.disable_broadcast != null && message.hasOwnProperty("disable_broadcast"))
                if (!$util.isInteger(message.disable_broadcast))
                    return "disable_broadcast: integer expected";
            if (message.signup_start_time != null && message.hasOwnProperty("signup_start_time"))
                if (!$util.isInteger(message.signup_start_time))
                    return "signup_start_time: integer expected";
            if (message.signup_end_time != null && message.hasOwnProperty("signup_end_time"))
                if (!$util.isInteger(message.signup_end_time))
                    return "signup_end_time: integer expected";
            if (message.signup_type != null && message.hasOwnProperty("signup_type"))
                if (!$util.isInteger(message.signup_type))
                    return "signup_type: integer expected";
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                if (!$util.isInteger(message.auto_match))
                    return "auto_match: integer expected";
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                if (!$util.isInteger(message.rank_type))
                    return "rank_type: integer expected";
            if (message.show_team_rank != null && message.hasOwnProperty("show_team_rank"))
                if (typeof message.show_team_rank !== "boolean")
                    return "show_team_rank: boolean expected";
            if (message.tied_rank != null && message.hasOwnProperty("tied_rank"))
                if (!$util.isInteger(message.tied_rank))
                    return "tied_rank: integer expected";
            if (message.match_start_time != null && message.hasOwnProperty("match_start_time"))
                if (!$util.isInteger(message.match_start_time))
                    return "match_start_time: integer expected";
            if (message.match_end_time != null && message.hasOwnProperty("match_end_time"))
                if (!$util.isInteger(message.match_end_time))
                    return "match_end_time: integer expected";
            if (message.open_rank_percent != null && message.hasOwnProperty("open_rank_percent"))
                if (!$util.isInteger(message.open_rank_percent))
                    return "open_rank_percent: integer expected";
            return null;
        };

        CustomizedContestDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestDetail();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.contest_id != null)
                if (Number(object.contest_id) !== 0)
                    message.contest_id = object.contest_id >>> 0;
            if (object.contest_name != null)
                if (typeof object.contest_name !== "string" || object.contest_name.length)
                    message.contest_name = String(object.contest_name);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.finish_time != null)
                if (Number(object.finish_time) !== 0)
                    message.finish_time = object.finish_time >>> 0;
            if (object.open != null)
                if (object.open)
                    message.open = Boolean(object.open);
            if (object.rank_rule != null)
                if (Number(object.rank_rule) !== 0)
                    message.rank_rule = object.rank_rule >>> 0;
            if (object.game_mode != null) {
                if (typeof object.game_mode !== "object")
                    throw TypeError(".lq.CustomizedContestDetail.game_mode: object expected");
                message.game_mode = $root.lq.GameMode.fromObject(object.game_mode, _depth + 1);
            }
            if (object.private_notice != null)
                if (typeof object.private_notice !== "string" || object.private_notice.length)
                    message.private_notice = String(object.private_notice);
            if (object.observer_switch != null)
                if (Number(object.observer_switch) !== 0)
                    message.observer_switch = object.observer_switch >>> 0;
            if (object.emoji_switch != null)
                if (Number(object.emoji_switch) !== 0)
                    message.emoji_switch = object.emoji_switch >>> 0;
            if (object.contest_type != null)
                if (Number(object.contest_type) !== 0)
                    message.contest_type = object.contest_type >>> 0;
            if (object.disable_broadcast != null)
                if (Number(object.disable_broadcast) !== 0)
                    message.disable_broadcast = object.disable_broadcast >>> 0;
            if (object.signup_start_time != null)
                if (Number(object.signup_start_time) !== 0)
                    message.signup_start_time = object.signup_start_time >>> 0;
            if (object.signup_end_time != null)
                if (Number(object.signup_end_time) !== 0)
                    message.signup_end_time = object.signup_end_time >>> 0;
            if (object.signup_type != null)
                if (Number(object.signup_type) !== 0)
                    message.signup_type = object.signup_type >>> 0;
            if (object.auto_match != null)
                if (Number(object.auto_match) !== 0)
                    message.auto_match = object.auto_match >>> 0;
            if (object.rank_type != null)
                if (Number(object.rank_type) !== 0)
                    message.rank_type = object.rank_type >>> 0;
            if (object.show_team_rank != null)
                if (object.show_team_rank)
                    message.show_team_rank = Boolean(object.show_team_rank);
            if (object.tied_rank != null)
                if (Number(object.tied_rank) !== 0)
                    message.tied_rank = object.tied_rank >>> 0;
            if (object.match_start_time != null)
                if (Number(object.match_start_time) !== 0)
                    message.match_start_time = object.match_start_time >>> 0;
            if (object.match_end_time != null)
                if (Number(object.match_end_time) !== 0)
                    message.match_end_time = object.match_end_time >>> 0;
            if (object.open_rank_percent != null)
                if (Number(object.open_rank_percent) !== 0)
                    message.open_rank_percent = object.open_rank_percent >>> 0;
            return message;
        };

        CustomizedContestDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.contest_id = 0;
                object.contest_name = "";
                object.state = 0;
                object.create_time = 0;
                object.start_time = 0;
                object.finish_time = 0;
                object.open = false;
                object.rank_rule = 0;
                object.game_mode = null;
                object.private_notice = "";
                object.observer_switch = 0;
                object.emoji_switch = 0;
                object.contest_type = 0;
                object.disable_broadcast = 0;
                object.signup_start_time = 0;
                object.signup_end_time = 0;
                object.signup_type = 0;
                object.auto_match = 0;
                object.rank_type = 0;
                object.show_team_rank = false;
                object.tied_rank = 0;
                object.match_start_time = 0;
                object.match_end_time = 0;
                object.open_rank_percent = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                object.contest_id = message.contest_id;
            if (message.contest_name != null && message.hasOwnProperty("contest_name"))
                object.contest_name = message.contest_name;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                object.finish_time = message.finish_time;
            if (message.open != null && message.hasOwnProperty("open"))
                object.open = message.open;
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                object.rank_rule = message.rank_rule;
            if (message.game_mode != null && message.hasOwnProperty("game_mode"))
                object.game_mode = $root.lq.GameMode.toObject(message.game_mode, options);
            if (message.private_notice != null && message.hasOwnProperty("private_notice"))
                object.private_notice = message.private_notice;
            if (message.observer_switch != null && message.hasOwnProperty("observer_switch"))
                object.observer_switch = message.observer_switch;
            if (message.emoji_switch != null && message.hasOwnProperty("emoji_switch"))
                object.emoji_switch = message.emoji_switch;
            if (message.contest_type != null && message.hasOwnProperty("contest_type"))
                object.contest_type = message.contest_type;
            if (message.disable_broadcast != null && message.hasOwnProperty("disable_broadcast"))
                object.disable_broadcast = message.disable_broadcast;
            if (message.signup_start_time != null && message.hasOwnProperty("signup_start_time"))
                object.signup_start_time = message.signup_start_time;
            if (message.signup_end_time != null && message.hasOwnProperty("signup_end_time"))
                object.signup_end_time = message.signup_end_time;
            if (message.signup_type != null && message.hasOwnProperty("signup_type"))
                object.signup_type = message.signup_type;
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                object.auto_match = message.auto_match;
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                object.rank_type = message.rank_type;
            if (message.show_team_rank != null && message.hasOwnProperty("show_team_rank"))
                object.show_team_rank = message.show_team_rank;
            if (message.tied_rank != null && message.hasOwnProperty("tied_rank"))
                object.tied_rank = message.tied_rank;
            if (message.match_start_time != null && message.hasOwnProperty("match_start_time"))
                object.match_start_time = message.match_start_time;
            if (message.match_end_time != null && message.hasOwnProperty("match_end_time"))
                object.match_end_time = message.match_end_time;
            if (message.open_rank_percent != null && message.hasOwnProperty("open_rank_percent"))
                object.open_rank_percent = message.open_rank_percent;
            return object;
        };

        CustomizedContestDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestDetail";
        };

        return CustomizedContestDetail;
    })();

    lq.CustomizedContestGamePlan = (function() {

        function CustomizedContestGamePlan(properties) {
            this.ready_players = [];
            this.account_ids = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestGamePlan.prototype.unique_id = 0;
        CustomizedContestGamePlan.prototype.uuid = "";
        CustomizedContestGamePlan.prototype.ready_players = $util.emptyArray;
        CustomizedContestGamePlan.prototype.account_ids = $util.emptyArray;
        CustomizedContestGamePlan.prototype.game_start_time = 0;
        CustomizedContestGamePlan.prototype.expired_time = 0;

        CustomizedContestGamePlan.create = function create(properties) {
            return new CustomizedContestGamePlan(properties);
        };

        CustomizedContestGamePlan.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(18).string(message.uuid);
            if (message.ready_players != null && message.ready_players.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.ready_players.length; ++i)
                    writer.uint32(message.ready_players[i]);
                writer.ldelim();
            }
            if (message.account_ids != null && message.account_ids.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.account_ids.length; ++i)
                    writer.uint32(message.account_ids[i]);
                writer.ldelim();
            }
            if (message.game_start_time != null && Object.hasOwnProperty.call(message, "game_start_time"))
                writer.uint32(40).uint32(message.game_start_time);
            if (message.expired_time != null && Object.hasOwnProperty.call(message, "expired_time"))
                writer.uint32(48).uint32(message.expired_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestGamePlan.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestGamePlan.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestGamePlan(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.ready_players && message.ready_players.length))
                                message.ready_players = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ready_players.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.ready_players && message.ready_players.length))
                            message.ready_players = [];
                        message.ready_players.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.account_ids && message.account_ids.length))
                                message.account_ids = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_ids.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_ids && message.account_ids.length))
                            message.account_ids = [];
                        message.account_ids.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_start_time = value;
                        else
                            delete message.game_start_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expired_time = value;
                        else
                            delete message.expired_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestGamePlan.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestGamePlan.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.ready_players != null && message.hasOwnProperty("ready_players")) {
                if (!Array.isArray(message.ready_players))
                    return "ready_players: array expected";
                for (let i = 0; i < message.ready_players.length; ++i)
                    if (!$util.isInteger(message.ready_players[i]))
                        return "ready_players: integer[] expected";
            }
            if (message.account_ids != null && message.hasOwnProperty("account_ids")) {
                if (!Array.isArray(message.account_ids))
                    return "account_ids: array expected";
                for (let i = 0; i < message.account_ids.length; ++i)
                    if (!$util.isInteger(message.account_ids[i]))
                        return "account_ids: integer[] expected";
            }
            if (message.game_start_time != null && message.hasOwnProperty("game_start_time"))
                if (!$util.isInteger(message.game_start_time))
                    return "game_start_time: integer expected";
            if (message.expired_time != null && message.hasOwnProperty("expired_time"))
                if (!$util.isInteger(message.expired_time))
                    return "expired_time: integer expected";
            return null;
        };

        CustomizedContestGamePlan.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestGamePlan)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestGamePlan();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.ready_players) {
                if (!Array.isArray(object.ready_players))
                    throw TypeError(".lq.CustomizedContestGamePlan.ready_players: array expected");
                message.ready_players = Array(object.ready_players.length);
                for (let i = 0; i < object.ready_players.length; ++i)
                    message.ready_players[i] = object.ready_players[i] >>> 0;
            }
            if (object.account_ids) {
                if (!Array.isArray(object.account_ids))
                    throw TypeError(".lq.CustomizedContestGamePlan.account_ids: array expected");
                message.account_ids = Array(object.account_ids.length);
                for (let i = 0; i < object.account_ids.length; ++i)
                    message.account_ids[i] = object.account_ids[i] >>> 0;
            }
            if (object.game_start_time != null)
                if (Number(object.game_start_time) !== 0)
                    message.game_start_time = object.game_start_time >>> 0;
            if (object.expired_time != null)
                if (Number(object.expired_time) !== 0)
                    message.expired_time = object.expired_time >>> 0;
            return message;
        };

        CustomizedContestGamePlan.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ready_players = [];
                object.account_ids = [];
            }
            if (options.defaults) {
                object.unique_id = 0;
                object.uuid = "";
                object.game_start_time = 0;
                object.expired_time = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.ready_players && message.ready_players.length) {
                object.ready_players = Array(message.ready_players.length);
                for (let j = 0; j < message.ready_players.length; ++j)
                    object.ready_players[j] = message.ready_players[j];
            }
            if (message.account_ids && message.account_ids.length) {
                object.account_ids = Array(message.account_ids.length);
                for (let j = 0; j < message.account_ids.length; ++j)
                    object.account_ids[j] = message.account_ids[j];
            }
            if (message.game_start_time != null && message.hasOwnProperty("game_start_time"))
                object.game_start_time = message.game_start_time;
            if (message.expired_time != null && message.hasOwnProperty("expired_time"))
                object.expired_time = message.expired_time;
            return object;
        };

        CustomizedContestGamePlan.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestGamePlan.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestGamePlan";
        };

        return CustomizedContestGamePlan;
    })();

    lq.CustomizedContestPlayerReport = (function() {

        function CustomizedContestPlayerReport(properties) {
            this.game_ranks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestPlayerReport.prototype.rank_rule = 0;
        CustomizedContestPlayerReport.prototype.rank = 0;
        CustomizedContestPlayerReport.prototype.point = 0;
        CustomizedContestPlayerReport.prototype.game_ranks = $util.emptyArray;
        CustomizedContestPlayerReport.prototype.total_game_count = 0;
        CustomizedContestPlayerReport.prototype.rank_percent = 0;

        CustomizedContestPlayerReport.create = function create(properties) {
            return new CustomizedContestPlayerReport(properties);
        };

        CustomizedContestPlayerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rank_rule != null && Object.hasOwnProperty.call(message, "rank_rule"))
                writer.uint32(8).uint32(message.rank_rule);
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(16).uint32(message.rank);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(24).int32(message.point);
            if (message.game_ranks != null && message.game_ranks.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.game_ranks.length; ++i)
                    writer.uint32(message.game_ranks[i]);
                writer.ldelim();
            }
            if (message.total_game_count != null && Object.hasOwnProperty.call(message, "total_game_count"))
                writer.uint32(40).uint32(message.total_game_count);
            if (message.rank_percent != null && Object.hasOwnProperty.call(message, "rank_percent"))
                writer.uint32(48).uint32(message.rank_percent);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestPlayerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestPlayerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestPlayerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_rule = value;
                        else
                            delete message.rank_rule;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.game_ranks && message.game_ranks.length))
                                message.game_ranks = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.game_ranks.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.game_ranks && message.game_ranks.length))
                            message.game_ranks = [];
                        message.game_ranks.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_game_count = value;
                        else
                            delete message.total_game_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_percent = value;
                        else
                            delete message.rank_percent;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestPlayerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestPlayerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                if (!$util.isInteger(message.rank_rule))
                    return "rank_rule: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank"))
                if (!$util.isInteger(message.rank))
                    return "rank: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.game_ranks != null && message.hasOwnProperty("game_ranks")) {
                if (!Array.isArray(message.game_ranks))
                    return "game_ranks: array expected";
                for (let i = 0; i < message.game_ranks.length; ++i)
                    if (!$util.isInteger(message.game_ranks[i]))
                        return "game_ranks: integer[] expected";
            }
            if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                if (!$util.isInteger(message.total_game_count))
                    return "total_game_count: integer expected";
            if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                if (!$util.isInteger(message.rank_percent))
                    return "rank_percent: integer expected";
            return null;
        };

        CustomizedContestPlayerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestPlayerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestPlayerReport();
            if (object.rank_rule != null)
                if (Number(object.rank_rule) !== 0)
                    message.rank_rule = object.rank_rule >>> 0;
            if (object.rank != null)
                if (Number(object.rank) !== 0)
                    message.rank = object.rank >>> 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point | 0;
            if (object.game_ranks) {
                if (!Array.isArray(object.game_ranks))
                    throw TypeError(".lq.CustomizedContestPlayerReport.game_ranks: array expected");
                message.game_ranks = Array(object.game_ranks.length);
                for (let i = 0; i < object.game_ranks.length; ++i)
                    message.game_ranks[i] = object.game_ranks[i] >>> 0;
            }
            if (object.total_game_count != null)
                if (Number(object.total_game_count) !== 0)
                    message.total_game_count = object.total_game_count >>> 0;
            if (object.rank_percent != null)
                if (Number(object.rank_percent) !== 0)
                    message.rank_percent = object.rank_percent >>> 0;
            return message;
        };

        CustomizedContestPlayerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.game_ranks = [];
            if (options.defaults) {
                object.rank_rule = 0;
                object.rank = 0;
                object.point = 0;
                object.total_game_count = 0;
                object.rank_percent = 0;
            }
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                object.rank_rule = message.rank_rule;
            if (message.rank != null && message.hasOwnProperty("rank"))
                object.rank = message.rank;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.game_ranks && message.game_ranks.length) {
                object.game_ranks = Array(message.game_ranks.length);
                for (let j = 0; j < message.game_ranks.length; ++j)
                    object.game_ranks[j] = message.game_ranks[j];
            }
            if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                object.total_game_count = message.total_game_count;
            if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                object.rank_percent = message.rank_percent;
            return object;
        };

        CustomizedContestPlayerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestPlayerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestPlayerReport";
        };

        return CustomizedContestPlayerReport;
    })();

    lq.RecordGame = (function() {

        function RecordGame(properties) {
            this.accounts = [];
            this.robots = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordGame.prototype.uuid = "";
        RecordGame.prototype.start_time = 0;
        RecordGame.prototype.end_time = 0;
        RecordGame.prototype.config = null;
        RecordGame.prototype.accounts = $util.emptyArray;
        RecordGame.prototype.result = null;
        RecordGame.prototype.robots = $util.emptyArray;
        RecordGame.prototype.standard_rule = 0;

        RecordGame.create = function create(properties) {
            return new RecordGame(properties);
        };

        RecordGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(16).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(24).uint32(message.end_time);
            if (message.config != null && Object.hasOwnProperty.call(message, "config"))
                $root.lq.GameConfig.encode(message.config, writer.uint32(42).fork()).ldelim();
            if (message.accounts != null && message.accounts.length)
                for (let i = 0; i < message.accounts.length; ++i)
                    $root.lq.RecordGame.AccountInfo.encode(message.accounts[i], writer.uint32(90).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.lq.GameEndResult.encode(message.result, writer.uint32(98).fork()).ldelim();
            if (message.robots != null && message.robots.length)
                for (let i = 0; i < message.robots.length; ++i)
                    $root.lq.RecordGame.AccountInfo.encode(message.robots[i], writer.uint32(106).fork()).ldelim();
            if (message.standard_rule != null && Object.hasOwnProperty.call(message, "standard_rule"))
                writer.uint32(112).uint32(message.standard_rule);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.config = $root.lq.GameConfig.decode(reader, reader.uint32(), undefined, _depth + 1, message.config);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.accounts && message.accounts.length))
                            message.accounts = [];
                        message.accounts.push($root.lq.RecordGame.AccountInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.result = $root.lq.GameEndResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.robots && message.robots.length))
                            message.robots = [];
                        message.robots.push($root.lq.RecordGame.AccountInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.standard_rule = value;
                        else
                            delete message.standard_rule;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.config != null && message.hasOwnProperty("config")) {
                let error = $root.lq.GameConfig.verify(message.config, _depth + 1);
                if (error)
                    return "config." + error;
            }
            if (message.accounts != null && message.hasOwnProperty("accounts")) {
                if (!Array.isArray(message.accounts))
                    return "accounts: array expected";
                for (let i = 0; i < message.accounts.length; ++i) {
                    let error = $root.lq.RecordGame.AccountInfo.verify(message.accounts[i], _depth + 1);
                    if (error)
                        return "accounts." + error;
                }
            }
            if (message.result != null && message.hasOwnProperty("result")) {
                let error = $root.lq.GameEndResult.verify(message.result, _depth + 1);
                if (error)
                    return "result." + error;
            }
            if (message.robots != null && message.hasOwnProperty("robots")) {
                if (!Array.isArray(message.robots))
                    return "robots: array expected";
                for (let i = 0; i < message.robots.length; ++i) {
                    let error = $root.lq.RecordGame.AccountInfo.verify(message.robots[i], _depth + 1);
                    if (error)
                        return "robots." + error;
                }
            }
            if (message.standard_rule != null && message.hasOwnProperty("standard_rule"))
                if (!$util.isInteger(message.standard_rule))
                    return "standard_rule: integer expected";
            return null;
        };

        RecordGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordGame();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.config != null) {
                if (typeof object.config !== "object")
                    throw TypeError(".lq.RecordGame.config: object expected");
                message.config = $root.lq.GameConfig.fromObject(object.config, _depth + 1);
            }
            if (object.accounts) {
                if (!Array.isArray(object.accounts))
                    throw TypeError(".lq.RecordGame.accounts: array expected");
                message.accounts = Array(object.accounts.length);
                for (let i = 0; i < object.accounts.length; ++i) {
                    if (typeof object.accounts[i] !== "object")
                        throw TypeError(".lq.RecordGame.accounts: object expected");
                    message.accounts[i] = $root.lq.RecordGame.AccountInfo.fromObject(object.accounts[i], _depth + 1);
                }
            }
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".lq.RecordGame.result: object expected");
                message.result = $root.lq.GameEndResult.fromObject(object.result, _depth + 1);
            }
            if (object.robots) {
                if (!Array.isArray(object.robots))
                    throw TypeError(".lq.RecordGame.robots: array expected");
                message.robots = Array(object.robots.length);
                for (let i = 0; i < object.robots.length; ++i) {
                    if (typeof object.robots[i] !== "object")
                        throw TypeError(".lq.RecordGame.robots: object expected");
                    message.robots[i] = $root.lq.RecordGame.AccountInfo.fromObject(object.robots[i], _depth + 1);
                }
            }
            if (object.standard_rule != null)
                if (Number(object.standard_rule) !== 0)
                    message.standard_rule = object.standard_rule >>> 0;
            return message;
        };

        RecordGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.accounts = [];
                object.robots = [];
            }
            if (options.defaults) {
                object.uuid = "";
                object.start_time = 0;
                object.end_time = 0;
                object.config = null;
                object.result = null;
                object.standard_rule = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.config != null && message.hasOwnProperty("config"))
                object.config = $root.lq.GameConfig.toObject(message.config, options);
            if (message.accounts && message.accounts.length) {
                object.accounts = Array(message.accounts.length);
                for (let j = 0; j < message.accounts.length; ++j)
                    object.accounts[j] = $root.lq.RecordGame.AccountInfo.toObject(message.accounts[j], options);
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.lq.GameEndResult.toObject(message.result, options);
            if (message.robots && message.robots.length) {
                object.robots = Array(message.robots.length);
                for (let j = 0; j < message.robots.length; ++j)
                    object.robots[j] = $root.lq.RecordGame.AccountInfo.toObject(message.robots[j], options);
            }
            if (message.standard_rule != null && message.hasOwnProperty("standard_rule"))
                object.standard_rule = message.standard_rule;
            return object;
        };

        RecordGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordGame";
        };

        RecordGame.AccountInfo = (function() {

            function AccountInfo(properties) {
                this.views = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountInfo.prototype.account_id = 0;
            AccountInfo.prototype.seat = 0;
            AccountInfo.prototype.nickname = "";
            AccountInfo.prototype.avatar_id = 0;
            AccountInfo.prototype.character = null;
            AccountInfo.prototype.title = 0;
            AccountInfo.prototype.level = null;
            AccountInfo.prototype.level3 = null;
            AccountInfo.prototype.avatar_frame = 0;
            AccountInfo.prototype.verified = 0;
            AccountInfo.prototype.views = $util.emptyArray;

            AccountInfo.create = function create(properties) {
                return new AccountInfo(properties);
            };

            AccountInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(16).uint32(message.seat);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(26).string(message.nickname);
                if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                    writer.uint32(32).uint32(message.avatar_id);
                if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                    $root.lq.Character.encode(message.character, writer.uint32(42).fork()).ldelim();
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(48).uint32(message.title);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    $root.lq.AccountLevel.encode(message.level, writer.uint32(58).fork()).ldelim();
                if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                    $root.lq.AccountLevel.encode(message.level3, writer.uint32(66).fork()).ldelim();
                if (message.avatar_frame != null && Object.hasOwnProperty.call(message, "avatar_frame"))
                    writer.uint32(72).uint32(message.avatar_frame);
                if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                    writer.uint32(80).uint32(message.verified);
                if (message.views != null && message.views.length)
                    for (let i = 0; i < message.views.length; ++i)
                        $root.lq.ViewSlot.encode(message.views[i], writer.uint32(90).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordGame.AccountInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.avatar_id = value;
                            else
                                delete message.avatar_id;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            message.character = $root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.title = value;
                            else
                                delete message.title;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.avatar_frame = value;
                            else
                                delete message.avatar_frame;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.verified = value;
                            else
                                delete message.verified;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 2)
                                break;
                            if (!(message.views && message.views.length))
                                message.views = [];
                            message.views.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                    if (!$util.isInteger(message.avatar_id))
                        return "avatar_id: integer expected";
                if (message.character != null && message.hasOwnProperty("character")) {
                    let error = $root.lq.Character.verify(message.character, _depth + 1);
                    if (error)
                        return "character." + error;
                }
                if (message.title != null && message.hasOwnProperty("title"))
                    if (!$util.isInteger(message.title))
                        return "title: integer expected";
                if (message.level != null && message.hasOwnProperty("level")) {
                    let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                    if (error)
                        return "level." + error;
                }
                if (message.level3 != null && message.hasOwnProperty("level3")) {
                    let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                    if (error)
                        return "level3." + error;
                }
                if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                    if (!$util.isInteger(message.avatar_frame))
                        return "avatar_frame: integer expected";
                if (message.verified != null && message.hasOwnProperty("verified"))
                    if (!$util.isInteger(message.verified))
                        return "verified: integer expected";
                if (message.views != null && message.hasOwnProperty("views")) {
                    if (!Array.isArray(message.views))
                        return "views: array expected";
                    for (let i = 0; i < message.views.length; ++i) {
                        let error = $root.lq.ViewSlot.verify(message.views[i], _depth + 1);
                        if (error)
                            return "views." + error;
                    }
                }
                return null;
            };

            AccountInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordGame.AccountInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordGame.AccountInfo();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                if (object.avatar_id != null)
                    if (Number(object.avatar_id) !== 0)
                        message.avatar_id = object.avatar_id >>> 0;
                if (object.character != null) {
                    if (typeof object.character !== "object")
                        throw TypeError(".lq.RecordGame.AccountInfo.character: object expected");
                    message.character = $root.lq.Character.fromObject(object.character, _depth + 1);
                }
                if (object.title != null)
                    if (Number(object.title) !== 0)
                        message.title = object.title >>> 0;
                if (object.level != null) {
                    if (typeof object.level !== "object")
                        throw TypeError(".lq.RecordGame.AccountInfo.level: object expected");
                    message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
                }
                if (object.level3 != null) {
                    if (typeof object.level3 !== "object")
                        throw TypeError(".lq.RecordGame.AccountInfo.level3: object expected");
                    message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
                }
                if (object.avatar_frame != null)
                    if (Number(object.avatar_frame) !== 0)
                        message.avatar_frame = object.avatar_frame >>> 0;
                if (object.verified != null)
                    if (Number(object.verified) !== 0)
                        message.verified = object.verified >>> 0;
                if (object.views) {
                    if (!Array.isArray(object.views))
                        throw TypeError(".lq.RecordGame.AccountInfo.views: array expected");
                    message.views = Array(object.views.length);
                    for (let i = 0; i < object.views.length; ++i) {
                        if (typeof object.views[i] !== "object")
                            throw TypeError(".lq.RecordGame.AccountInfo.views: object expected");
                        message.views[i] = $root.lq.ViewSlot.fromObject(object.views[i], _depth + 1);
                    }
                }
                return message;
            };

            AccountInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.views = [];
                if (options.defaults) {
                    object.account_id = 0;
                    object.seat = 0;
                    object.nickname = "";
                    object.avatar_id = 0;
                    object.character = null;
                    object.title = 0;
                    object.level = null;
                    object.level3 = null;
                    object.avatar_frame = 0;
                    object.verified = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                    object.avatar_id = message.avatar_id;
                if (message.character != null && message.hasOwnProperty("character"))
                    object.character = $root.lq.Character.toObject(message.character, options);
                if (message.title != null && message.hasOwnProperty("title"))
                    object.title = message.title;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = $root.lq.AccountLevel.toObject(message.level, options);
                if (message.level3 != null && message.hasOwnProperty("level3"))
                    object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
                if (message.avatar_frame != null && message.hasOwnProperty("avatar_frame"))
                    object.avatar_frame = message.avatar_frame;
                if (message.verified != null && message.hasOwnProperty("verified"))
                    object.verified = message.verified;
                if (message.views && message.views.length) {
                    object.views = Array(message.views.length);
                    for (let j = 0; j < message.views.length; ++j)
                        object.views[j] = $root.lq.ViewSlot.toObject(message.views[j], options);
                }
                return object;
            };

            AccountInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordGame.AccountInfo";
            };

            return AccountInfo;
        })();

        return RecordGame;
    })();

    lq.RecordListEntry = (function() {

        function RecordListEntry(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordListEntry.prototype.version = 0;
        RecordListEntry.prototype.uuid = "";
        RecordListEntry.prototype.start_time = 0;
        RecordListEntry.prototype.end_time = 0;
        RecordListEntry.prototype.tag = 0;
        RecordListEntry.prototype.subtag = 0;
        RecordListEntry.prototype.players = $util.emptyArray;
        RecordListEntry.prototype.standard_rule = 0;

        RecordListEntry.create = function create(properties) {
            return new RecordListEntry(properties);
        };

        RecordListEntry.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(8).uint32(message.version);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(18).string(message.uuid);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(24).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(32).uint32(message.end_time);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(40).uint32(message.tag);
            if (message.subtag != null && Object.hasOwnProperty.call(message, "subtag"))
                writer.uint32(48).uint32(message.subtag);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.RecordPlayerResult.encode(message.players[i], writer.uint32(58).fork()).ldelim();
            if (message.standard_rule != null && Object.hasOwnProperty.call(message, "standard_rule"))
                writer.uint32(64).uint32(message.standard_rule);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordListEntry.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordListEntry.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordListEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.subtag = value;
                        else
                            delete message.subtag;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.RecordPlayerResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.standard_rule = value;
                        else
                            delete message.standard_rule;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordListEntry.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordListEntry.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isInteger(message.tag))
                    return "tag: integer expected";
            if (message.subtag != null && message.hasOwnProperty("subtag"))
                if (!$util.isInteger(message.subtag))
                    return "subtag: integer expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.RecordPlayerResult.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.standard_rule != null && message.hasOwnProperty("standard_rule"))
                if (!$util.isInteger(message.standard_rule))
                    return "standard_rule: integer expected";
            return null;
        };

        RecordListEntry.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordListEntry)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordListEntry();
            if (object.version != null)
                if (Number(object.version) !== 0)
                    message.version = object.version >>> 0;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.tag != null)
                if (Number(object.tag) !== 0)
                    message.tag = object.tag >>> 0;
            if (object.subtag != null)
                if (Number(object.subtag) !== 0)
                    message.subtag = object.subtag >>> 0;
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.RecordListEntry.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.RecordListEntry.players: object expected");
                    message.players[i] = $root.lq.RecordPlayerResult.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.standard_rule != null)
                if (Number(object.standard_rule) !== 0)
                    message.standard_rule = object.standard_rule >>> 0;
            return message;
        };

        RecordListEntry.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (options.defaults) {
                object.version = 0;
                object.uuid = "";
                object.start_time = 0;
                object.end_time = 0;
                object.tag = 0;
                object.subtag = 0;
                object.standard_rule = 0;
            }
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            if (message.subtag != null && message.hasOwnProperty("subtag"))
                object.subtag = message.subtag;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.RecordPlayerResult.toObject(message.players[j], options);
            }
            if (message.standard_rule != null && message.hasOwnProperty("standard_rule"))
                object.standard_rule = message.standard_rule;
            return object;
        };

        RecordListEntry.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordListEntry.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordListEntry";
        };

        return RecordListEntry;
    })();

    lq.RecordPlayerResult = (function() {

        function RecordPlayerResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordPlayerResult.prototype.rank = 0;
        RecordPlayerResult.prototype.account_id = 0;
        RecordPlayerResult.prototype.nickname = "";
        RecordPlayerResult.prototype.level = null;
        RecordPlayerResult.prototype.level3 = null;
        RecordPlayerResult.prototype.seat = 0;
        RecordPlayerResult.prototype.pt = 0;
        RecordPlayerResult.prototype.point = 0;
        RecordPlayerResult.prototype.max_hu_type = 0;
        RecordPlayerResult.prototype.action_liqi = 0;
        RecordPlayerResult.prototype.action_rong = 0;
        RecordPlayerResult.prototype.action_zimo = 0;
        RecordPlayerResult.prototype.action_chong = 0;
        RecordPlayerResult.prototype.verified = 0;

        RecordPlayerResult.create = function create(properties) {
            return new RecordPlayerResult(properties);
        };

        RecordPlayerResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(8).uint32(message.rank);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(26).string(message.nickname);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.AccountLevel.encode(message.level, writer.uint32(34).fork()).ldelim();
            if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                $root.lq.AccountLevel.encode(message.level3, writer.uint32(42).fork()).ldelim();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(48).uint32(message.seat);
            if (message.pt != null && Object.hasOwnProperty.call(message, "pt"))
                writer.uint32(56).int32(message.pt);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(64).int32(message.point);
            if (message.max_hu_type != null && Object.hasOwnProperty.call(message, "max_hu_type"))
                writer.uint32(72).uint32(message.max_hu_type);
            if (message.action_liqi != null && Object.hasOwnProperty.call(message, "action_liqi"))
                writer.uint32(80).uint32(message.action_liqi);
            if (message.action_rong != null && Object.hasOwnProperty.call(message, "action_rong"))
                writer.uint32(88).uint32(message.action_rong);
            if (message.action_zimo != null && Object.hasOwnProperty.call(message, "action_zimo"))
                writer.uint32(96).uint32(message.action_zimo);
            if (message.action_chong != null && Object.hasOwnProperty.call(message, "action_chong"))
                writer.uint32(104).uint32(message.action_chong);
            if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                writer.uint32(112).uint32(message.verified);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordPlayerResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordPlayerResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordPlayerResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.pt = value;
                        else
                            delete message.pt;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_hu_type = value;
                        else
                            delete message.max_hu_type;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.action_liqi = value;
                        else
                            delete message.action_liqi;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.action_rong = value;
                        else
                            delete message.action_rong;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.action_zimo = value;
                        else
                            delete message.action_zimo;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.action_chong = value;
                        else
                            delete message.action_chong;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified = value;
                        else
                            delete message.verified;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordPlayerResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordPlayerResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rank != null && message.hasOwnProperty("rank"))
                if (!$util.isInteger(message.rank))
                    return "rank: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.level3 != null && message.hasOwnProperty("level3")) {
                let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                if (error)
                    return "level3." + error;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.pt != null && message.hasOwnProperty("pt"))
                if (!$util.isInteger(message.pt))
                    return "pt: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.max_hu_type != null && message.hasOwnProperty("max_hu_type"))
                if (!$util.isInteger(message.max_hu_type))
                    return "max_hu_type: integer expected";
            if (message.action_liqi != null && message.hasOwnProperty("action_liqi"))
                if (!$util.isInteger(message.action_liqi))
                    return "action_liqi: integer expected";
            if (message.action_rong != null && message.hasOwnProperty("action_rong"))
                if (!$util.isInteger(message.action_rong))
                    return "action_rong: integer expected";
            if (message.action_zimo != null && message.hasOwnProperty("action_zimo"))
                if (!$util.isInteger(message.action_zimo))
                    return "action_zimo: integer expected";
            if (message.action_chong != null && message.hasOwnProperty("action_chong"))
                if (!$util.isInteger(message.action_chong))
                    return "action_chong: integer expected";
            if (message.verified != null && message.hasOwnProperty("verified"))
                if (!$util.isInteger(message.verified))
                    return "verified: integer expected";
            return null;
        };

        RecordPlayerResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordPlayerResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordPlayerResult();
            if (object.rank != null)
                if (Number(object.rank) !== 0)
                    message.rank = object.rank >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.RecordPlayerResult.level: object expected");
                message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
            }
            if (object.level3 != null) {
                if (typeof object.level3 !== "object")
                    throw TypeError(".lq.RecordPlayerResult.level3: object expected");
                message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
            }
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.pt != null)
                if (Number(object.pt) !== 0)
                    message.pt = object.pt | 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point | 0;
            if (object.max_hu_type != null)
                if (Number(object.max_hu_type) !== 0)
                    message.max_hu_type = object.max_hu_type >>> 0;
            if (object.action_liqi != null)
                if (Number(object.action_liqi) !== 0)
                    message.action_liqi = object.action_liqi >>> 0;
            if (object.action_rong != null)
                if (Number(object.action_rong) !== 0)
                    message.action_rong = object.action_rong >>> 0;
            if (object.action_zimo != null)
                if (Number(object.action_zimo) !== 0)
                    message.action_zimo = object.action_zimo >>> 0;
            if (object.action_chong != null)
                if (Number(object.action_chong) !== 0)
                    message.action_chong = object.action_chong >>> 0;
            if (object.verified != null)
                if (Number(object.verified) !== 0)
                    message.verified = object.verified >>> 0;
            return message;
        };

        RecordPlayerResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.rank = 0;
                object.account_id = 0;
                object.nickname = "";
                object.level = null;
                object.level3 = null;
                object.seat = 0;
                object.pt = 0;
                object.point = 0;
                object.max_hu_type = 0;
                object.action_liqi = 0;
                object.action_rong = 0;
                object.action_zimo = 0;
                object.action_chong = 0;
                object.verified = 0;
            }
            if (message.rank != null && message.hasOwnProperty("rank"))
                object.rank = message.rank;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.AccountLevel.toObject(message.level, options);
            if (message.level3 != null && message.hasOwnProperty("level3"))
                object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.pt != null && message.hasOwnProperty("pt"))
                object.pt = message.pt;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.max_hu_type != null && message.hasOwnProperty("max_hu_type"))
                object.max_hu_type = message.max_hu_type;
            if (message.action_liqi != null && message.hasOwnProperty("action_liqi"))
                object.action_liqi = message.action_liqi;
            if (message.action_rong != null && message.hasOwnProperty("action_rong"))
                object.action_rong = message.action_rong;
            if (message.action_zimo != null && message.hasOwnProperty("action_zimo"))
                object.action_zimo = message.action_zimo;
            if (message.action_chong != null && message.hasOwnProperty("action_chong"))
                object.action_chong = message.action_chong;
            if (message.verified != null && message.hasOwnProperty("verified"))
                object.verified = message.verified;
            return object;
        };

        RecordPlayerResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordPlayerResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordPlayerResult";
        };

        return RecordPlayerResult;
    })();

    lq.CustomizedContestGameStart = (function() {

        function CustomizedContestGameStart(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestGameStart.prototype.players = $util.emptyArray;

        CustomizedContestGameStart.create = function create(properties) {
            return new CustomizedContestGameStart(properties);
        };

        CustomizedContestGameStart.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.CustomizedContestGameStart.Item.encode(message.players[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestGameStart.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestGameStart.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestGameStart();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.CustomizedContestGameStart.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestGameStart.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestGameStart.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.CustomizedContestGameStart.Item.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        CustomizedContestGameStart.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestGameStart)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestGameStart();
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.CustomizedContestGameStart.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.CustomizedContestGameStart.players: object expected");
                    message.players[i] = $root.lq.CustomizedContestGameStart.Item.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        CustomizedContestGameStart.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.CustomizedContestGameStart.Item.toObject(message.players[j], options);
            }
            return object;
        };

        CustomizedContestGameStart.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestGameStart.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestGameStart";
        };

        CustomizedContestGameStart.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.nickname = "";

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(18).string(message.nickname);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestGameStart.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.CustomizedContestGameStart.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.CustomizedContestGameStart.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.nickname = "";
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.CustomizedContestGameStart.Item";
            };

            return Item;
        })();

        return CustomizedContestGameStart;
    })();

    lq.CustomizedContestGameEnd = (function() {

        function CustomizedContestGameEnd(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        CustomizedContestGameEnd.prototype.players = $util.emptyArray;

        CustomizedContestGameEnd.create = function create(properties) {
            return new CustomizedContestGameEnd(properties);
        };

        CustomizedContestGameEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.CustomizedContestGameEnd.Item.encode(message.players[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CustomizedContestGameEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        CustomizedContestGameEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestGameEnd();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.CustomizedContestGameEnd.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        CustomizedContestGameEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        CustomizedContestGameEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.CustomizedContestGameEnd.Item.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        CustomizedContestGameEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.CustomizedContestGameEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.CustomizedContestGameEnd();
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.CustomizedContestGameEnd.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.CustomizedContestGameEnd.players: object expected");
                    message.players[i] = $root.lq.CustomizedContestGameEnd.Item.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        CustomizedContestGameEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.CustomizedContestGameEnd.Item.toObject(message.players[j], options);
            }
            return object;
        };

        CustomizedContestGameEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CustomizedContestGameEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.CustomizedContestGameEnd";
        };

        CustomizedContestGameEnd.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.nickname = "";
            Item.prototype.total_point = 0;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(18).string(message.nickname);
                if (message.total_point != null && Object.hasOwnProperty.call(message, "total_point"))
                    writer.uint32(24).int32(message.total_point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.CustomizedContestGameEnd.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.total_point = value;
                            else
                                delete message.total_point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    if (!$util.isInteger(message.total_point))
                        return "total_point: integer expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.CustomizedContestGameEnd.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.CustomizedContestGameEnd.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                if (object.total_point != null)
                    if (Number(object.total_point) !== 0)
                        message.total_point = object.total_point | 0;
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.nickname = "";
                    object.total_point = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    object.total_point = message.total_point;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.CustomizedContestGameEnd.Item";
            };

            return Item;
        })();

        return CustomizedContestGameEnd;
    })();

    lq.Activity = (function() {

        function Activity(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        Activity.prototype.activity_id = 0;
        Activity.prototype.start_time = 0;
        Activity.prototype.end_time = 0;
        Activity.prototype.type = "";

        Activity.create = function create(properties) {
            return new Activity(properties);
        };

        Activity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(16).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(24).uint32(message.end_time);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(34).string(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Activity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        Activity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.Activity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        Activity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        Activity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            return null;
        };

        Activity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.Activity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.Activity();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = String(object.type);
            return message;
        };

        Activity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.start_time = 0;
                object.end_time = 0;
                object.type = "";
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        Activity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Activity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.Activity";
        };

        return Activity;
    })();

    lq.ExchangeRecord = (function() {

        function ExchangeRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ExchangeRecord.prototype.exchange_id = 0;
        ExchangeRecord.prototype.count = 0;

        ExchangeRecord.create = function create(properties) {
            return new ExchangeRecord(properties);
        };

        ExchangeRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.exchange_id != null && Object.hasOwnProperty.call(message, "exchange_id"))
                writer.uint32(8).uint32(message.exchange_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ExchangeRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ExchangeRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ExchangeRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exchange_id = value;
                        else
                            delete message.exchange_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ExchangeRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ExchangeRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.exchange_id != null && message.hasOwnProperty("exchange_id"))
                if (!$util.isInteger(message.exchange_id))
                    return "exchange_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ExchangeRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ExchangeRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ExchangeRecord();
            if (object.exchange_id != null)
                if (Number(object.exchange_id) !== 0)
                    message.exchange_id = object.exchange_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ExchangeRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.exchange_id = 0;
                object.count = 0;
            }
            if (message.exchange_id != null && message.hasOwnProperty("exchange_id"))
                object.exchange_id = message.exchange_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ExchangeRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExchangeRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ExchangeRecord";
        };

        return ExchangeRecord;
    })();

    lq.ActivityAccumulatedPointData = (function() {

        function ActivityAccumulatedPointData(properties) {
            this.gained_reward_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAccumulatedPointData.prototype.activity_id = 0;
        ActivityAccumulatedPointData.prototype.point = 0;
        ActivityAccumulatedPointData.prototype.gained_reward_list = $util.emptyArray;

        ActivityAccumulatedPointData.create = function create(properties) {
            return new ActivityAccumulatedPointData(properties);
        };

        ActivityAccumulatedPointData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(16).int32(message.point);
            if (message.gained_reward_list != null && message.gained_reward_list.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.gained_reward_list.length; ++i)
                    writer.uint32(message.gained_reward_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAccumulatedPointData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAccumulatedPointData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAccumulatedPointData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.gained_reward_list && message.gained_reward_list.length))
                                message.gained_reward_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gained_reward_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.gained_reward_list && message.gained_reward_list.length))
                            message.gained_reward_list = [];
                        message.gained_reward_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAccumulatedPointData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAccumulatedPointData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.gained_reward_list != null && message.hasOwnProperty("gained_reward_list")) {
                if (!Array.isArray(message.gained_reward_list))
                    return "gained_reward_list: array expected";
                for (let i = 0; i < message.gained_reward_list.length; ++i)
                    if (!$util.isInteger(message.gained_reward_list[i]))
                        return "gained_reward_list: integer[] expected";
            }
            return null;
        };

        ActivityAccumulatedPointData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAccumulatedPointData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAccumulatedPointData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point | 0;
            if (object.gained_reward_list) {
                if (!Array.isArray(object.gained_reward_list))
                    throw TypeError(".lq.ActivityAccumulatedPointData.gained_reward_list: array expected");
                message.gained_reward_list = Array(object.gained_reward_list.length);
                for (let i = 0; i < object.gained_reward_list.length; ++i)
                    message.gained_reward_list[i] = object.gained_reward_list[i] >>> 0;
            }
            return message;
        };

        ActivityAccumulatedPointData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gained_reward_list = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.point = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.gained_reward_list && message.gained_reward_list.length) {
                object.gained_reward_list = Array(message.gained_reward_list.length);
                for (let j = 0; j < message.gained_reward_list.length; ++j)
                    object.gained_reward_list[j] = message.gained_reward_list[j];
            }
            return object;
        };

        ActivityAccumulatedPointData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAccumulatedPointData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAccumulatedPointData";
        };

        return ActivityAccumulatedPointData;
    })();

    lq.ActivityRankPointData = (function() {

        function ActivityRankPointData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityRankPointData.prototype.leaderboard_id = 0;
        ActivityRankPointData.prototype.point = 0;
        ActivityRankPointData.prototype.gained_reward = false;
        ActivityRankPointData.prototype.gainable_time = 0;

        ActivityRankPointData.create = function create(properties) {
            return new ActivityRankPointData(properties);
        };

        ActivityRankPointData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.leaderboard_id != null && Object.hasOwnProperty.call(message, "leaderboard_id"))
                writer.uint32(8).uint32(message.leaderboard_id);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(16).int32(message.point);
            if (message.gained_reward != null && Object.hasOwnProperty.call(message, "gained_reward"))
                writer.uint32(24).bool(message.gained_reward);
            if (message.gainable_time != null && Object.hasOwnProperty.call(message, "gainable_time"))
                writer.uint32(32).uint32(message.gainable_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityRankPointData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityRankPointData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityRankPointData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.leaderboard_id = value;
                        else
                            delete message.leaderboard_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gained_reward = value;
                        else
                            delete message.gained_reward;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gainable_time = value;
                        else
                            delete message.gainable_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityRankPointData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityRankPointData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                if (!$util.isInteger(message.leaderboard_id))
                    return "leaderboard_id: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.gained_reward != null && message.hasOwnProperty("gained_reward"))
                if (typeof message.gained_reward !== "boolean")
                    return "gained_reward: boolean expected";
            if (message.gainable_time != null && message.hasOwnProperty("gainable_time"))
                if (!$util.isInteger(message.gainable_time))
                    return "gainable_time: integer expected";
            return null;
        };

        ActivityRankPointData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityRankPointData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityRankPointData();
            if (object.leaderboard_id != null)
                if (Number(object.leaderboard_id) !== 0)
                    message.leaderboard_id = object.leaderboard_id >>> 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point | 0;
            if (object.gained_reward != null)
                if (object.gained_reward)
                    message.gained_reward = Boolean(object.gained_reward);
            if (object.gainable_time != null)
                if (Number(object.gainable_time) !== 0)
                    message.gainable_time = object.gainable_time >>> 0;
            return message;
        };

        ActivityRankPointData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.leaderboard_id = 0;
                object.point = 0;
                object.gained_reward = false;
                object.gainable_time = 0;
            }
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                object.leaderboard_id = message.leaderboard_id;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.gained_reward != null && message.hasOwnProperty("gained_reward"))
                object.gained_reward = message.gained_reward;
            if (message.gainable_time != null && message.hasOwnProperty("gainable_time"))
                object.gainable_time = message.gainable_time;
            return object;
        };

        ActivityRankPointData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityRankPointData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityRankPointData";
        };

        return ActivityRankPointData;
    })();

    lq.GameRoundHuData = (function() {

        function GameRoundHuData(properties) {
            this.fans = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRoundHuData.prototype.hupai = null;
        GameRoundHuData.prototype.fans = $util.emptyArray;
        GameRoundHuData.prototype.score = 0;
        GameRoundHuData.prototype.xun = 0;
        GameRoundHuData.prototype.title_id = 0;
        GameRoundHuData.prototype.fan_sum = 0;
        GameRoundHuData.prototype.fu_sum = 0;
        GameRoundHuData.prototype.yakuman_count = 0;
        GameRoundHuData.prototype.biao_dora_count = 0;
        GameRoundHuData.prototype.red_dora_count = 0;
        GameRoundHuData.prototype.li_dora_count = 0;
        GameRoundHuData.prototype.babei_count = 0;
        GameRoundHuData.prototype.xuan_shang_count = 0;
        GameRoundHuData.prototype.pai_left_count = 0;

        GameRoundHuData.create = function create(properties) {
            return new GameRoundHuData(properties);
        };

        GameRoundHuData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hupai != null && Object.hasOwnProperty.call(message, "hupai"))
                $root.lq.GameRoundHuData.HuPai.encode(message.hupai, writer.uint32(10).fork()).ldelim();
            if (message.fans != null && message.fans.length)
                for (let i = 0; i < message.fans.length; ++i)
                    $root.lq.GameRoundHuData.Fan.encode(message.fans[i], writer.uint32(18).fork()).ldelim();
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(24).uint32(message.score);
            if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                writer.uint32(32).uint32(message.xun);
            if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                writer.uint32(40).uint32(message.title_id);
            if (message.fan_sum != null && Object.hasOwnProperty.call(message, "fan_sum"))
                writer.uint32(48).uint32(message.fan_sum);
            if (message.fu_sum != null && Object.hasOwnProperty.call(message, "fu_sum"))
                writer.uint32(56).uint32(message.fu_sum);
            if (message.yakuman_count != null && Object.hasOwnProperty.call(message, "yakuman_count"))
                writer.uint32(64).uint32(message.yakuman_count);
            if (message.biao_dora_count != null && Object.hasOwnProperty.call(message, "biao_dora_count"))
                writer.uint32(72).uint32(message.biao_dora_count);
            if (message.red_dora_count != null && Object.hasOwnProperty.call(message, "red_dora_count"))
                writer.uint32(80).uint32(message.red_dora_count);
            if (message.li_dora_count != null && Object.hasOwnProperty.call(message, "li_dora_count"))
                writer.uint32(88).uint32(message.li_dora_count);
            if (message.babei_count != null && Object.hasOwnProperty.call(message, "babei_count"))
                writer.uint32(96).uint32(message.babei_count);
            if (message.xuan_shang_count != null && Object.hasOwnProperty.call(message, "xuan_shang_count"))
                writer.uint32(104).uint32(message.xuan_shang_count);
            if (message.pai_left_count != null && Object.hasOwnProperty.call(message, "pai_left_count"))
                writer.uint32(112).uint32(message.pai_left_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRoundHuData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRoundHuData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundHuData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.hupai = $root.lq.GameRoundHuData.HuPai.decode(reader, reader.uint32(), undefined, _depth + 1, message.hupai);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fans && message.fans.length))
                            message.fans = [];
                        message.fans.push($root.lq.GameRoundHuData.Fan.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun = value;
                        else
                            delete message.xun;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title_id = value;
                        else
                            delete message.title_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fan_sum = value;
                        else
                            delete message.fan_sum;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu_sum = value;
                        else
                            delete message.fu_sum;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.yakuman_count = value;
                        else
                            delete message.yakuman_count;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.biao_dora_count = value;
                        else
                            delete message.biao_dora_count;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.red_dora_count = value;
                        else
                            delete message.red_dora_count;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.li_dora_count = value;
                        else
                            delete message.li_dora_count;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.babei_count = value;
                        else
                            delete message.babei_count;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xuan_shang_count = value;
                        else
                            delete message.xuan_shang_count;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pai_left_count = value;
                        else
                            delete message.pai_left_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRoundHuData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRoundHuData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hupai != null && message.hasOwnProperty("hupai")) {
                let error = $root.lq.GameRoundHuData.HuPai.verify(message.hupai, _depth + 1);
                if (error)
                    return "hupai." + error;
            }
            if (message.fans != null && message.hasOwnProperty("fans")) {
                if (!Array.isArray(message.fans))
                    return "fans: array expected";
                for (let i = 0; i < message.fans.length; ++i) {
                    let error = $root.lq.GameRoundHuData.Fan.verify(message.fans[i], _depth + 1);
                    if (error)
                        return "fans." + error;
                }
            }
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.xun != null && message.hasOwnProperty("xun"))
                if (!$util.isInteger(message.xun))
                    return "xun: integer expected";
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                if (!$util.isInteger(message.title_id))
                    return "title_id: integer expected";
            if (message.fan_sum != null && message.hasOwnProperty("fan_sum"))
                if (!$util.isInteger(message.fan_sum))
                    return "fan_sum: integer expected";
            if (message.fu_sum != null && message.hasOwnProperty("fu_sum"))
                if (!$util.isInteger(message.fu_sum))
                    return "fu_sum: integer expected";
            if (message.yakuman_count != null && message.hasOwnProperty("yakuman_count"))
                if (!$util.isInteger(message.yakuman_count))
                    return "yakuman_count: integer expected";
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                if (!$util.isInteger(message.biao_dora_count))
                    return "biao_dora_count: integer expected";
            if (message.red_dora_count != null && message.hasOwnProperty("red_dora_count"))
                if (!$util.isInteger(message.red_dora_count))
                    return "red_dora_count: integer expected";
            if (message.li_dora_count != null && message.hasOwnProperty("li_dora_count"))
                if (!$util.isInteger(message.li_dora_count))
                    return "li_dora_count: integer expected";
            if (message.babei_count != null && message.hasOwnProperty("babei_count"))
                if (!$util.isInteger(message.babei_count))
                    return "babei_count: integer expected";
            if (message.xuan_shang_count != null && message.hasOwnProperty("xuan_shang_count"))
                if (!$util.isInteger(message.xuan_shang_count))
                    return "xuan_shang_count: integer expected";
            if (message.pai_left_count != null && message.hasOwnProperty("pai_left_count"))
                if (!$util.isInteger(message.pai_left_count))
                    return "pai_left_count: integer expected";
            return null;
        };

        GameRoundHuData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRoundHuData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRoundHuData();
            if (object.hupai != null) {
                if (typeof object.hupai !== "object")
                    throw TypeError(".lq.GameRoundHuData.hupai: object expected");
                message.hupai = $root.lq.GameRoundHuData.HuPai.fromObject(object.hupai, _depth + 1);
            }
            if (object.fans) {
                if (!Array.isArray(object.fans))
                    throw TypeError(".lq.GameRoundHuData.fans: array expected");
                message.fans = Array(object.fans.length);
                for (let i = 0; i < object.fans.length; ++i) {
                    if (typeof object.fans[i] !== "object")
                        throw TypeError(".lq.GameRoundHuData.fans: object expected");
                    message.fans[i] = $root.lq.GameRoundHuData.Fan.fromObject(object.fans[i], _depth + 1);
                }
            }
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score >>> 0;
            if (object.xun != null)
                if (Number(object.xun) !== 0)
                    message.xun = object.xun >>> 0;
            if (object.title_id != null)
                if (Number(object.title_id) !== 0)
                    message.title_id = object.title_id >>> 0;
            if (object.fan_sum != null)
                if (Number(object.fan_sum) !== 0)
                    message.fan_sum = object.fan_sum >>> 0;
            if (object.fu_sum != null)
                if (Number(object.fu_sum) !== 0)
                    message.fu_sum = object.fu_sum >>> 0;
            if (object.yakuman_count != null)
                if (Number(object.yakuman_count) !== 0)
                    message.yakuman_count = object.yakuman_count >>> 0;
            if (object.biao_dora_count != null)
                if (Number(object.biao_dora_count) !== 0)
                    message.biao_dora_count = object.biao_dora_count >>> 0;
            if (object.red_dora_count != null)
                if (Number(object.red_dora_count) !== 0)
                    message.red_dora_count = object.red_dora_count >>> 0;
            if (object.li_dora_count != null)
                if (Number(object.li_dora_count) !== 0)
                    message.li_dora_count = object.li_dora_count >>> 0;
            if (object.babei_count != null)
                if (Number(object.babei_count) !== 0)
                    message.babei_count = object.babei_count >>> 0;
            if (object.xuan_shang_count != null)
                if (Number(object.xuan_shang_count) !== 0)
                    message.xuan_shang_count = object.xuan_shang_count >>> 0;
            if (object.pai_left_count != null)
                if (Number(object.pai_left_count) !== 0)
                    message.pai_left_count = object.pai_left_count >>> 0;
            return message;
        };

        GameRoundHuData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.fans = [];
            if (options.defaults) {
                object.hupai = null;
                object.score = 0;
                object.xun = 0;
                object.title_id = 0;
                object.fan_sum = 0;
                object.fu_sum = 0;
                object.yakuman_count = 0;
                object.biao_dora_count = 0;
                object.red_dora_count = 0;
                object.li_dora_count = 0;
                object.babei_count = 0;
                object.xuan_shang_count = 0;
                object.pai_left_count = 0;
            }
            if (message.hupai != null && message.hasOwnProperty("hupai"))
                object.hupai = $root.lq.GameRoundHuData.HuPai.toObject(message.hupai, options);
            if (message.fans && message.fans.length) {
                object.fans = Array(message.fans.length);
                for (let j = 0; j < message.fans.length; ++j)
                    object.fans[j] = $root.lq.GameRoundHuData.Fan.toObject(message.fans[j], options);
            }
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.xun != null && message.hasOwnProperty("xun"))
                object.xun = message.xun;
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                object.title_id = message.title_id;
            if (message.fan_sum != null && message.hasOwnProperty("fan_sum"))
                object.fan_sum = message.fan_sum;
            if (message.fu_sum != null && message.hasOwnProperty("fu_sum"))
                object.fu_sum = message.fu_sum;
            if (message.yakuman_count != null && message.hasOwnProperty("yakuman_count"))
                object.yakuman_count = message.yakuman_count;
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                object.biao_dora_count = message.biao_dora_count;
            if (message.red_dora_count != null && message.hasOwnProperty("red_dora_count"))
                object.red_dora_count = message.red_dora_count;
            if (message.li_dora_count != null && message.hasOwnProperty("li_dora_count"))
                object.li_dora_count = message.li_dora_count;
            if (message.babei_count != null && message.hasOwnProperty("babei_count"))
                object.babei_count = message.babei_count;
            if (message.xuan_shang_count != null && message.hasOwnProperty("xuan_shang_count"))
                object.xuan_shang_count = message.xuan_shang_count;
            if (message.pai_left_count != null && message.hasOwnProperty("pai_left_count"))
                object.pai_left_count = message.pai_left_count;
            return object;
        };

        GameRoundHuData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRoundHuData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRoundHuData";
        };

        GameRoundHuData.HuPai = (function() {

            function HuPai(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            HuPai.prototype.tile = "";
            HuPai.prototype.seat = 0;
            HuPai.prototype.liqi = 0;

            HuPai.create = function create(properties) {
                return new HuPai(properties);
            };

            HuPai.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                    writer.uint32(10).string(message.tile);
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(16).uint32(message.seat);
                if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                    writer.uint32(24).uint32(message.liqi);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            HuPai.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            HuPai.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundHuData.HuPai(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.tile = value;
                            else
                                delete message.tile;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.liqi = value;
                            else
                                delete message.liqi;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            HuPai.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            HuPai.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tile != null && message.hasOwnProperty("tile"))
                    if (!$util.isString(message.tile))
                        return "tile: string expected";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.liqi != null && message.hasOwnProperty("liqi"))
                    if (!$util.isInteger(message.liqi))
                        return "liqi: integer expected";
                return null;
            };

            HuPai.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameRoundHuData.HuPai)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameRoundHuData.HuPai();
                if (object.tile != null)
                    if (typeof object.tile !== "string" || object.tile.length)
                        message.tile = String(object.tile);
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.liqi != null)
                    if (Number(object.liqi) !== 0)
                        message.liqi = object.liqi >>> 0;
                return message;
            };

            HuPai.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.tile = "";
                    object.seat = 0;
                    object.liqi = 0;
                }
                if (message.tile != null && message.hasOwnProperty("tile"))
                    object.tile = message.tile;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.liqi != null && message.hasOwnProperty("liqi"))
                    object.liqi = message.liqi;
                return object;
            };

            HuPai.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HuPai.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameRoundHuData.HuPai";
            };

            return HuPai;
        })();

        GameRoundHuData.Fan = (function() {

            function Fan(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Fan.prototype.id = 0;
            Fan.prototype.count = 0;
            Fan.prototype.fan = 0;

            Fan.create = function create(properties) {
                return new Fan(properties);
            };

            Fan.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                    writer.uint32(24).uint32(message.fan);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Fan.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Fan.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundHuData.Fan(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.fan = value;
                            else
                                delete message.fan;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Fan.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Fan.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.fan != null && message.hasOwnProperty("fan"))
                    if (!$util.isInteger(message.fan))
                        return "fan: integer expected";
                return null;
            };

            Fan.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameRoundHuData.Fan)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameRoundHuData.Fan();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.fan != null)
                    if (Number(object.fan) !== 0)
                        message.fan = object.fan >>> 0;
                return message;
            };

            Fan.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                    object.fan = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.fan != null && message.hasOwnProperty("fan"))
                    object.fan = message.fan;
                return object;
            };

            Fan.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Fan.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameRoundHuData.Fan";
            };

            return Fan;
        })();

        return GameRoundHuData;
    })();

    lq.GameRoundPlayerFangChongInfo = (function() {

        function GameRoundPlayerFangChongInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRoundPlayerFangChongInfo.prototype.seat = 0;
        GameRoundPlayerFangChongInfo.prototype.tile = "";
        GameRoundPlayerFangChongInfo.prototype.pai_left_count = 0;

        GameRoundPlayerFangChongInfo.create = function create(properties) {
            return new GameRoundPlayerFangChongInfo(properties);
        };

        GameRoundPlayerFangChongInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.pai_left_count != null && Object.hasOwnProperty.call(message, "pai_left_count"))
                writer.uint32(24).uint32(message.pai_left_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRoundPlayerFangChongInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRoundPlayerFangChongInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundPlayerFangChongInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pai_left_count = value;
                        else
                            delete message.pai_left_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRoundPlayerFangChongInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRoundPlayerFangChongInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.pai_left_count != null && message.hasOwnProperty("pai_left_count"))
                if (!$util.isInteger(message.pai_left_count))
                    return "pai_left_count: integer expected";
            return null;
        };

        GameRoundPlayerFangChongInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRoundPlayerFangChongInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRoundPlayerFangChongInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.pai_left_count != null)
                if (Number(object.pai_left_count) !== 0)
                    message.pai_left_count = object.pai_left_count >>> 0;
            return message;
        };

        GameRoundPlayerFangChongInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.tile = "";
                object.pai_left_count = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.pai_left_count != null && message.hasOwnProperty("pai_left_count"))
                object.pai_left_count = message.pai_left_count;
            return object;
        };

        GameRoundPlayerFangChongInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRoundPlayerFangChongInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRoundPlayerFangChongInfo";
        };

        return GameRoundPlayerFangChongInfo;
    })();

    lq.GameRoundPlayerResult = (function() {

        function GameRoundPlayerResult(properties) {
            this.hands = [];
            this.ming = [];
            this.fangchongs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRoundPlayerResult.prototype.type = 0;
        GameRoundPlayerResult.prototype.hands = $util.emptyArray;
        GameRoundPlayerResult.prototype.ming = $util.emptyArray;
        GameRoundPlayerResult.prototype.liqi_type = 0;
        GameRoundPlayerResult.prototype.is_fulu = false;
        GameRoundPlayerResult.prototype.is_liujumanguan = false;
        GameRoundPlayerResult.prototype.lian_zhuang = 0;
        GameRoundPlayerResult.prototype.hu = null;
        GameRoundPlayerResult.prototype.fangchongs = $util.emptyArray;
        GameRoundPlayerResult.prototype.liqi_fangchong = false;
        GameRoundPlayerResult.prototype.liqi_failed = false;

        GameRoundPlayerResult.create = function create(properties) {
            return new GameRoundPlayerResult(properties);
        };

        GameRoundPlayerResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.hands != null && message.hands.length)
                for (let i = 0; i < message.hands.length; ++i)
                    writer.uint32(18).string(message.hands[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(26).string(message.ming[i]);
            if (message.liqi_type != null && Object.hasOwnProperty.call(message, "liqi_type"))
                writer.uint32(32).uint32(message.liqi_type);
            if (message.is_fulu != null && Object.hasOwnProperty.call(message, "is_fulu"))
                writer.uint32(40).bool(message.is_fulu);
            if (message.is_liujumanguan != null && Object.hasOwnProperty.call(message, "is_liujumanguan"))
                writer.uint32(48).bool(message.is_liujumanguan);
            if (message.lian_zhuang != null && Object.hasOwnProperty.call(message, "lian_zhuang"))
                writer.uint32(56).uint32(message.lian_zhuang);
            if (message.hu != null && Object.hasOwnProperty.call(message, "hu"))
                $root.lq.GameRoundHuData.encode(message.hu, writer.uint32(66).fork()).ldelim();
            if (message.fangchongs != null && message.fangchongs.length)
                for (let i = 0; i < message.fangchongs.length; ++i)
                    $root.lq.GameRoundPlayerFangChongInfo.encode(message.fangchongs[i], writer.uint32(74).fork()).ldelim();
            if (message.liqi_fangchong != null && Object.hasOwnProperty.call(message, "liqi_fangchong"))
                writer.uint32(80).bool(message.liqi_fangchong);
            if (message.liqi_failed != null && Object.hasOwnProperty.call(message, "liqi_failed"))
                writer.uint32(88).bool(message.liqi_failed);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRoundPlayerResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRoundPlayerResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundPlayerResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hands && message.hands.length))
                            message.hands = [];
                        message.hands.push(reader.string());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqi_type = value;
                        else
                            delete message.liqi_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_fulu = value;
                        else
                            delete message.is_fulu;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_liujumanguan = value;
                        else
                            delete message.is_liujumanguan;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.lian_zhuang = value;
                        else
                            delete message.lian_zhuang;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.hu = $root.lq.GameRoundHuData.decode(reader, reader.uint32(), undefined, _depth + 1, message.hu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fangchongs && message.fangchongs.length))
                            message.fangchongs = [];
                        message.fangchongs.push($root.lq.GameRoundPlayerFangChongInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liqi_fangchong = value;
                        else
                            delete message.liqi_fangchong;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liqi_failed = value;
                        else
                            delete message.liqi_failed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRoundPlayerResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRoundPlayerResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.hands != null && message.hasOwnProperty("hands")) {
                if (!Array.isArray(message.hands))
                    return "hands: array expected";
                for (let i = 0; i < message.hands.length; ++i)
                    if (!$util.isString(message.hands[i]))
                        return "hands: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.liqi_type != null && message.hasOwnProperty("liqi_type"))
                if (!$util.isInteger(message.liqi_type))
                    return "liqi_type: integer expected";
            if (message.is_fulu != null && message.hasOwnProperty("is_fulu"))
                if (typeof message.is_fulu !== "boolean")
                    return "is_fulu: boolean expected";
            if (message.is_liujumanguan != null && message.hasOwnProperty("is_liujumanguan"))
                if (typeof message.is_liujumanguan !== "boolean")
                    return "is_liujumanguan: boolean expected";
            if (message.lian_zhuang != null && message.hasOwnProperty("lian_zhuang"))
                if (!$util.isInteger(message.lian_zhuang))
                    return "lian_zhuang: integer expected";
            if (message.hu != null && message.hasOwnProperty("hu")) {
                let error = $root.lq.GameRoundHuData.verify(message.hu, _depth + 1);
                if (error)
                    return "hu." + error;
            }
            if (message.fangchongs != null && message.hasOwnProperty("fangchongs")) {
                if (!Array.isArray(message.fangchongs))
                    return "fangchongs: array expected";
                for (let i = 0; i < message.fangchongs.length; ++i) {
                    let error = $root.lq.GameRoundPlayerFangChongInfo.verify(message.fangchongs[i], _depth + 1);
                    if (error)
                        return "fangchongs." + error;
                }
            }
            if (message.liqi_fangchong != null && message.hasOwnProperty("liqi_fangchong"))
                if (typeof message.liqi_fangchong !== "boolean")
                    return "liqi_fangchong: boolean expected";
            if (message.liqi_failed != null && message.hasOwnProperty("liqi_failed"))
                if (typeof message.liqi_failed !== "boolean")
                    return "liqi_failed: boolean expected";
            return null;
        };

        GameRoundPlayerResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRoundPlayerResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRoundPlayerResult();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.hands) {
                if (!Array.isArray(object.hands))
                    throw TypeError(".lq.GameRoundPlayerResult.hands: array expected");
                message.hands = Array(object.hands.length);
                for (let i = 0; i < object.hands.length; ++i)
                    message.hands[i] = String(object.hands[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.GameRoundPlayerResult.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.liqi_type != null)
                if (Number(object.liqi_type) !== 0)
                    message.liqi_type = object.liqi_type >>> 0;
            if (object.is_fulu != null)
                if (object.is_fulu)
                    message.is_fulu = Boolean(object.is_fulu);
            if (object.is_liujumanguan != null)
                if (object.is_liujumanguan)
                    message.is_liujumanguan = Boolean(object.is_liujumanguan);
            if (object.lian_zhuang != null)
                if (Number(object.lian_zhuang) !== 0)
                    message.lian_zhuang = object.lian_zhuang >>> 0;
            if (object.hu != null) {
                if (typeof object.hu !== "object")
                    throw TypeError(".lq.GameRoundPlayerResult.hu: object expected");
                message.hu = $root.lq.GameRoundHuData.fromObject(object.hu, _depth + 1);
            }
            if (object.fangchongs) {
                if (!Array.isArray(object.fangchongs))
                    throw TypeError(".lq.GameRoundPlayerResult.fangchongs: array expected");
                message.fangchongs = Array(object.fangchongs.length);
                for (let i = 0; i < object.fangchongs.length; ++i) {
                    if (typeof object.fangchongs[i] !== "object")
                        throw TypeError(".lq.GameRoundPlayerResult.fangchongs: object expected");
                    message.fangchongs[i] = $root.lq.GameRoundPlayerFangChongInfo.fromObject(object.fangchongs[i], _depth + 1);
                }
            }
            if (object.liqi_fangchong != null)
                if (object.liqi_fangchong)
                    message.liqi_fangchong = Boolean(object.liqi_fangchong);
            if (object.liqi_failed != null)
                if (object.liqi_failed)
                    message.liqi_failed = Boolean(object.liqi_failed);
            return message;
        };

        GameRoundPlayerResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hands = [];
                object.ming = [];
                object.fangchongs = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.liqi_type = 0;
                object.is_fulu = false;
                object.is_liujumanguan = false;
                object.lian_zhuang = 0;
                object.hu = null;
                object.liqi_fangchong = false;
                object.liqi_failed = false;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.hands && message.hands.length) {
                object.hands = Array(message.hands.length);
                for (let j = 0; j < message.hands.length; ++j)
                    object.hands[j] = message.hands[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.liqi_type != null && message.hasOwnProperty("liqi_type"))
                object.liqi_type = message.liqi_type;
            if (message.is_fulu != null && message.hasOwnProperty("is_fulu"))
                object.is_fulu = message.is_fulu;
            if (message.is_liujumanguan != null && message.hasOwnProperty("is_liujumanguan"))
                object.is_liujumanguan = message.is_liujumanguan;
            if (message.lian_zhuang != null && message.hasOwnProperty("lian_zhuang"))
                object.lian_zhuang = message.lian_zhuang;
            if (message.hu != null && message.hasOwnProperty("hu"))
                object.hu = $root.lq.GameRoundHuData.toObject(message.hu, options);
            if (message.fangchongs && message.fangchongs.length) {
                object.fangchongs = Array(message.fangchongs.length);
                for (let j = 0; j < message.fangchongs.length; ++j)
                    object.fangchongs[j] = $root.lq.GameRoundPlayerFangChongInfo.toObject(message.fangchongs[j], options);
            }
            if (message.liqi_fangchong != null && message.hasOwnProperty("liqi_fangchong"))
                object.liqi_fangchong = message.liqi_fangchong;
            if (message.liqi_failed != null && message.hasOwnProperty("liqi_failed"))
                object.liqi_failed = message.liqi_failed;
            return object;
        };

        GameRoundPlayerResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRoundPlayerResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRoundPlayerResult";
        };

        return GameRoundPlayerResult;
    })();

    lq.GameRoundPlayer = (function() {

        function GameRoundPlayer(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRoundPlayer.prototype.score = 0;
        GameRoundPlayer.prototype.rank = 0;
        GameRoundPlayer.prototype.result = null;

        GameRoundPlayer.create = function create(properties) {
            return new GameRoundPlayer(properties);
        };

        GameRoundPlayer.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(8).int32(message.score);
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(16).uint32(message.rank);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.lq.GameRoundPlayerResult.encode(message.result, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRoundPlayer.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRoundPlayer.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundPlayer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.result = $root.lq.GameRoundPlayerResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRoundPlayer.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRoundPlayer.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank"))
                if (!$util.isInteger(message.rank))
                    return "rank: integer expected";
            if (message.result != null && message.hasOwnProperty("result")) {
                let error = $root.lq.GameRoundPlayerResult.verify(message.result, _depth + 1);
                if (error)
                    return "result." + error;
            }
            return null;
        };

        GameRoundPlayer.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRoundPlayer)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRoundPlayer();
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score | 0;
            if (object.rank != null)
                if (Number(object.rank) !== 0)
                    message.rank = object.rank >>> 0;
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".lq.GameRoundPlayer.result: object expected");
                message.result = $root.lq.GameRoundPlayerResult.fromObject(object.result, _depth + 1);
            }
            return message;
        };

        GameRoundPlayer.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.score = 0;
                object.rank = 0;
                object.result = null;
            }
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.rank != null && message.hasOwnProperty("rank"))
                object.rank = message.rank;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.lq.GameRoundPlayerResult.toObject(message.result, options);
            return object;
        };

        GameRoundPlayer.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRoundPlayer.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRoundPlayer";
        };

        return GameRoundPlayer;
    })();

    lq.GameRoundSnapshot = (function() {

        function GameRoundSnapshot(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRoundSnapshot.prototype.ju = 0;
        GameRoundSnapshot.prototype.ben = 0;
        GameRoundSnapshot.prototype.players = $util.emptyArray;

        GameRoundSnapshot.create = function create(properties) {
            return new GameRoundSnapshot(properties);
        };

        GameRoundSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(8).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(16).uint32(message.ben);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.GameRoundPlayer.encode(message.players[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRoundSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRoundSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRoundSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.GameRoundPlayer.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRoundSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRoundSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.GameRoundPlayer.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        GameRoundSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRoundSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRoundSnapshot();
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.GameRoundSnapshot.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.GameRoundSnapshot.players: object expected");
                    message.players[i] = $root.lq.GameRoundPlayer.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        GameRoundSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (options.defaults) {
                object.ju = 0;
                object.ben = 0;
            }
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.GameRoundPlayer.toObject(message.players[j], options);
            }
            return object;
        };

        GameRoundSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRoundSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRoundSnapshot";
        };

        return GameRoundSnapshot;
    })();

    lq.GameFinalSnapshot = (function() {

        function GameFinalSnapshot(properties) {
            this.seats = [];
            this.rounds = [];
            this.account_views = [];
            this.final_players = [];
            this.afk_info = [];
            this.robot_views = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameFinalSnapshot.prototype.uuid = "";
        GameFinalSnapshot.prototype.state = 0;
        GameFinalSnapshot.prototype.category = 0;
        GameFinalSnapshot.prototype.mode = null;
        GameFinalSnapshot.prototype.meta = null;
        GameFinalSnapshot.prototype.calculate_param = null;
        GameFinalSnapshot.prototype.create_time = 0;
        GameFinalSnapshot.prototype.start_time = 0;
        GameFinalSnapshot.prototype.finish_time = 0;
        GameFinalSnapshot.prototype.seats = $util.emptyArray;
        GameFinalSnapshot.prototype.rounds = $util.emptyArray;
        GameFinalSnapshot.prototype.account_views = $util.emptyArray;
        GameFinalSnapshot.prototype.final_players = $util.emptyArray;
        GameFinalSnapshot.prototype.afk_info = $util.emptyArray;
        GameFinalSnapshot.prototype.robot_views = $util.emptyArray;

        GameFinalSnapshot.create = function create(properties) {
            return new GameFinalSnapshot(properties);
        };

        GameFinalSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(24).uint32(message.category);
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                $root.lq.GameMode.encode(message.mode, writer.uint32(34).fork()).ldelim();
            if (message.meta != null && Object.hasOwnProperty.call(message, "meta"))
                $root.lq.GameMetaData.encode(message.meta, writer.uint32(42).fork()).ldelim();
            if (message.calculate_param != null && Object.hasOwnProperty.call(message, "calculate_param"))
                $root.lq.GameFinalSnapshot.CalculateParam.encode(message.calculate_param, writer.uint32(50).fork()).ldelim();
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(56).uint32(message.create_time);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(64).uint32(message.start_time);
            if (message.finish_time != null && Object.hasOwnProperty.call(message, "finish_time"))
                writer.uint32(72).uint32(message.finish_time);
            if (message.seats != null && message.seats.length)
                for (let i = 0; i < message.seats.length; ++i)
                    $root.lq.GameFinalSnapshot.GameSeat.encode(message.seats[i], writer.uint32(82).fork()).ldelim();
            if (message.rounds != null && message.rounds.length)
                for (let i = 0; i < message.rounds.length; ++i)
                    $root.lq.GameRoundSnapshot.encode(message.rounds[i], writer.uint32(90).fork()).ldelim();
            if (message.account_views != null && message.account_views.length)
                for (let i = 0; i < message.account_views.length; ++i)
                    $root.lq.PlayerGameView.encode(message.account_views[i], writer.uint32(98).fork()).ldelim();
            if (message.final_players != null && message.final_players.length)
                for (let i = 0; i < message.final_players.length; ++i)
                    $root.lq.GameFinalSnapshot.FinalPlayer.encode(message.final_players[i], writer.uint32(106).fork()).ldelim();
            if (message.afk_info != null && message.afk_info.length)
                for (let i = 0; i < message.afk_info.length; ++i)
                    $root.lq.GameFinalSnapshot.AFKInfo.encode(message.afk_info[i], writer.uint32(114).fork()).ldelim();
            if (message.robot_views != null && message.robot_views.length)
                for (let i = 0; i < message.robot_views.length; ++i)
                    $root.lq.PlayerGameView.encode(message.robot_views[i], writer.uint32(122).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameFinalSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameFinalSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameFinalSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.mode = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.mode);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.meta = $root.lq.GameMetaData.decode(reader, reader.uint32(), undefined, _depth + 1, message.meta);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.calculate_param = $root.lq.GameFinalSnapshot.CalculateParam.decode(reader, reader.uint32(), undefined, _depth + 1, message.calculate_param);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_time = value;
                        else
                            delete message.finish_time;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.seats && message.seats.length))
                            message.seats = [];
                        message.seats.push($root.lq.GameFinalSnapshot.GameSeat.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rounds && message.rounds.length))
                            message.rounds = [];
                        message.rounds.push($root.lq.GameRoundSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.account_views && message.account_views.length))
                            message.account_views = [];
                        message.account_views.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.final_players && message.final_players.length))
                            message.final_players = [];
                        message.final_players.push($root.lq.GameFinalSnapshot.FinalPlayer.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.afk_info && message.afk_info.length))
                            message.afk_info = [];
                        message.afk_info.push($root.lq.GameFinalSnapshot.AFKInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.robot_views && message.robot_views.length))
                            message.robot_views = [];
                        message.robot_views.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameFinalSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameFinalSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.mode != null && message.hasOwnProperty("mode")) {
                let error = $root.lq.GameMode.verify(message.mode, _depth + 1);
                if (error)
                    return "mode." + error;
            }
            if (message.meta != null && message.hasOwnProperty("meta")) {
                let error = $root.lq.GameMetaData.verify(message.meta, _depth + 1);
                if (error)
                    return "meta." + error;
            }
            if (message.calculate_param != null && message.hasOwnProperty("calculate_param")) {
                let error = $root.lq.GameFinalSnapshot.CalculateParam.verify(message.calculate_param, _depth + 1);
                if (error)
                    return "calculate_param." + error;
            }
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                if (!$util.isInteger(message.finish_time))
                    return "finish_time: integer expected";
            if (message.seats != null && message.hasOwnProperty("seats")) {
                if (!Array.isArray(message.seats))
                    return "seats: array expected";
                for (let i = 0; i < message.seats.length; ++i) {
                    let error = $root.lq.GameFinalSnapshot.GameSeat.verify(message.seats[i], _depth + 1);
                    if (error)
                        return "seats." + error;
                }
            }
            if (message.rounds != null && message.hasOwnProperty("rounds")) {
                if (!Array.isArray(message.rounds))
                    return "rounds: array expected";
                for (let i = 0; i < message.rounds.length; ++i) {
                    let error = $root.lq.GameRoundSnapshot.verify(message.rounds[i], _depth + 1);
                    if (error)
                        return "rounds." + error;
                }
            }
            if (message.account_views != null && message.hasOwnProperty("account_views")) {
                if (!Array.isArray(message.account_views))
                    return "account_views: array expected";
                for (let i = 0; i < message.account_views.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.account_views[i], _depth + 1);
                    if (error)
                        return "account_views." + error;
                }
            }
            if (message.final_players != null && message.hasOwnProperty("final_players")) {
                if (!Array.isArray(message.final_players))
                    return "final_players: array expected";
                for (let i = 0; i < message.final_players.length; ++i) {
                    let error = $root.lq.GameFinalSnapshot.FinalPlayer.verify(message.final_players[i], _depth + 1);
                    if (error)
                        return "final_players." + error;
                }
            }
            if (message.afk_info != null && message.hasOwnProperty("afk_info")) {
                if (!Array.isArray(message.afk_info))
                    return "afk_info: array expected";
                for (let i = 0; i < message.afk_info.length; ++i) {
                    let error = $root.lq.GameFinalSnapshot.AFKInfo.verify(message.afk_info[i], _depth + 1);
                    if (error)
                        return "afk_info." + error;
                }
            }
            if (message.robot_views != null && message.hasOwnProperty("robot_views")) {
                if (!Array.isArray(message.robot_views))
                    return "robot_views: array expected";
                for (let i = 0; i < message.robot_views.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.robot_views[i], _depth + 1);
                    if (error)
                        return "robot_views." + error;
                }
            }
            return null;
        };

        GameFinalSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameFinalSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameFinalSnapshot();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.mode != null) {
                if (typeof object.mode !== "object")
                    throw TypeError(".lq.GameFinalSnapshot.mode: object expected");
                message.mode = $root.lq.GameMode.fromObject(object.mode, _depth + 1);
            }
            if (object.meta != null) {
                if (typeof object.meta !== "object")
                    throw TypeError(".lq.GameFinalSnapshot.meta: object expected");
                message.meta = $root.lq.GameMetaData.fromObject(object.meta, _depth + 1);
            }
            if (object.calculate_param != null) {
                if (typeof object.calculate_param !== "object")
                    throw TypeError(".lq.GameFinalSnapshot.calculate_param: object expected");
                message.calculate_param = $root.lq.GameFinalSnapshot.CalculateParam.fromObject(object.calculate_param, _depth + 1);
            }
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.finish_time != null)
                if (Number(object.finish_time) !== 0)
                    message.finish_time = object.finish_time >>> 0;
            if (object.seats) {
                if (!Array.isArray(object.seats))
                    throw TypeError(".lq.GameFinalSnapshot.seats: array expected");
                message.seats = Array(object.seats.length);
                for (let i = 0; i < object.seats.length; ++i) {
                    if (typeof object.seats[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.seats: object expected");
                    message.seats[i] = $root.lq.GameFinalSnapshot.GameSeat.fromObject(object.seats[i], _depth + 1);
                }
            }
            if (object.rounds) {
                if (!Array.isArray(object.rounds))
                    throw TypeError(".lq.GameFinalSnapshot.rounds: array expected");
                message.rounds = Array(object.rounds.length);
                for (let i = 0; i < object.rounds.length; ++i) {
                    if (typeof object.rounds[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.rounds: object expected");
                    message.rounds[i] = $root.lq.GameRoundSnapshot.fromObject(object.rounds[i], _depth + 1);
                }
            }
            if (object.account_views) {
                if (!Array.isArray(object.account_views))
                    throw TypeError(".lq.GameFinalSnapshot.account_views: array expected");
                message.account_views = Array(object.account_views.length);
                for (let i = 0; i < object.account_views.length; ++i) {
                    if (typeof object.account_views[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.account_views: object expected");
                    message.account_views[i] = $root.lq.PlayerGameView.fromObject(object.account_views[i], _depth + 1);
                }
            }
            if (object.final_players) {
                if (!Array.isArray(object.final_players))
                    throw TypeError(".lq.GameFinalSnapshot.final_players: array expected");
                message.final_players = Array(object.final_players.length);
                for (let i = 0; i < object.final_players.length; ++i) {
                    if (typeof object.final_players[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.final_players: object expected");
                    message.final_players[i] = $root.lq.GameFinalSnapshot.FinalPlayer.fromObject(object.final_players[i], _depth + 1);
                }
            }
            if (object.afk_info) {
                if (!Array.isArray(object.afk_info))
                    throw TypeError(".lq.GameFinalSnapshot.afk_info: array expected");
                message.afk_info = Array(object.afk_info.length);
                for (let i = 0; i < object.afk_info.length; ++i) {
                    if (typeof object.afk_info[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.afk_info: object expected");
                    message.afk_info[i] = $root.lq.GameFinalSnapshot.AFKInfo.fromObject(object.afk_info[i], _depth + 1);
                }
            }
            if (object.robot_views) {
                if (!Array.isArray(object.robot_views))
                    throw TypeError(".lq.GameFinalSnapshot.robot_views: array expected");
                message.robot_views = Array(object.robot_views.length);
                for (let i = 0; i < object.robot_views.length; ++i) {
                    if (typeof object.robot_views[i] !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.robot_views: object expected");
                    message.robot_views[i] = $root.lq.PlayerGameView.fromObject(object.robot_views[i], _depth + 1);
                }
            }
            return message;
        };

        GameFinalSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.seats = [];
                object.rounds = [];
                object.account_views = [];
                object.final_players = [];
                object.afk_info = [];
                object.robot_views = [];
            }
            if (options.defaults) {
                object.uuid = "";
                object.state = 0;
                object.category = 0;
                object.mode = null;
                object.meta = null;
                object.calculate_param = null;
                object.create_time = 0;
                object.start_time = 0;
                object.finish_time = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = $root.lq.GameMode.toObject(message.mode, options);
            if (message.meta != null && message.hasOwnProperty("meta"))
                object.meta = $root.lq.GameMetaData.toObject(message.meta, options);
            if (message.calculate_param != null && message.hasOwnProperty("calculate_param"))
                object.calculate_param = $root.lq.GameFinalSnapshot.CalculateParam.toObject(message.calculate_param, options);
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                object.finish_time = message.finish_time;
            if (message.seats && message.seats.length) {
                object.seats = Array(message.seats.length);
                for (let j = 0; j < message.seats.length; ++j)
                    object.seats[j] = $root.lq.GameFinalSnapshot.GameSeat.toObject(message.seats[j], options);
            }
            if (message.rounds && message.rounds.length) {
                object.rounds = Array(message.rounds.length);
                for (let j = 0; j < message.rounds.length; ++j)
                    object.rounds[j] = $root.lq.GameRoundSnapshot.toObject(message.rounds[j], options);
            }
            if (message.account_views && message.account_views.length) {
                object.account_views = Array(message.account_views.length);
                for (let j = 0; j < message.account_views.length; ++j)
                    object.account_views[j] = $root.lq.PlayerGameView.toObject(message.account_views[j], options);
            }
            if (message.final_players && message.final_players.length) {
                object.final_players = Array(message.final_players.length);
                for (let j = 0; j < message.final_players.length; ++j)
                    object.final_players[j] = $root.lq.GameFinalSnapshot.FinalPlayer.toObject(message.final_players[j], options);
            }
            if (message.afk_info && message.afk_info.length) {
                object.afk_info = Array(message.afk_info.length);
                for (let j = 0; j < message.afk_info.length; ++j)
                    object.afk_info[j] = $root.lq.GameFinalSnapshot.AFKInfo.toObject(message.afk_info[j], options);
            }
            if (message.robot_views && message.robot_views.length) {
                object.robot_views = Array(message.robot_views.length);
                for (let j = 0; j < message.robot_views.length; ++j)
                    object.robot_views[j] = $root.lq.PlayerGameView.toObject(message.robot_views[j], options);
            }
            return object;
        };

        GameFinalSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameFinalSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameFinalSnapshot";
        };

        GameFinalSnapshot.CalculateParam = (function() {

            function CalculateParam(properties) {
                this.rank_points = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CalculateParam.prototype.init_point = 0;
            CalculateParam.prototype.jingsuanyuandian = 0;
            CalculateParam.prototype.rank_points = $util.emptyArray;

            CalculateParam.create = function create(properties) {
                return new CalculateParam(properties);
            };

            CalculateParam.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.init_point != null && Object.hasOwnProperty.call(message, "init_point"))
                    writer.uint32(8).uint32(message.init_point);
                if (message.jingsuanyuandian != null && Object.hasOwnProperty.call(message, "jingsuanyuandian"))
                    writer.uint32(16).uint32(message.jingsuanyuandian);
                if (message.rank_points != null && message.rank_points.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.rank_points.length; ++i)
                        writer.int32(message.rank_points[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CalculateParam.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CalculateParam.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameFinalSnapshot.CalculateParam(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.init_point = value;
                            else
                                delete message.init_point;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.jingsuanyuandian = value;
                            else
                                delete message.jingsuanyuandian;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.rank_points && message.rank_points.length))
                                    message.rank_points = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.rank_points.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.rank_points && message.rank_points.length))
                                message.rank_points = [];
                            message.rank_points.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CalculateParam.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CalculateParam.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.init_point != null && message.hasOwnProperty("init_point"))
                    if (!$util.isInteger(message.init_point))
                        return "init_point: integer expected";
                if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                    if (!$util.isInteger(message.jingsuanyuandian))
                        return "jingsuanyuandian: integer expected";
                if (message.rank_points != null && message.hasOwnProperty("rank_points")) {
                    if (!Array.isArray(message.rank_points))
                        return "rank_points: array expected";
                    for (let i = 0; i < message.rank_points.length; ++i)
                        if (!$util.isInteger(message.rank_points[i]))
                            return "rank_points: integer[] expected";
                }
                return null;
            };

            CalculateParam.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameFinalSnapshot.CalculateParam)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameFinalSnapshot.CalculateParam();
                if (object.init_point != null)
                    if (Number(object.init_point) !== 0)
                        message.init_point = object.init_point >>> 0;
                if (object.jingsuanyuandian != null)
                    if (Number(object.jingsuanyuandian) !== 0)
                        message.jingsuanyuandian = object.jingsuanyuandian >>> 0;
                if (object.rank_points) {
                    if (!Array.isArray(object.rank_points))
                        throw TypeError(".lq.GameFinalSnapshot.CalculateParam.rank_points: array expected");
                    message.rank_points = Array(object.rank_points.length);
                    for (let i = 0; i < object.rank_points.length; ++i)
                        message.rank_points[i] = object.rank_points[i] | 0;
                }
                return message;
            };

            CalculateParam.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rank_points = [];
                if (options.defaults) {
                    object.init_point = 0;
                    object.jingsuanyuandian = 0;
                }
                if (message.init_point != null && message.hasOwnProperty("init_point"))
                    object.init_point = message.init_point;
                if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                    object.jingsuanyuandian = message.jingsuanyuandian;
                if (message.rank_points && message.rank_points.length) {
                    object.rank_points = Array(message.rank_points.length);
                    for (let j = 0; j < message.rank_points.length; ++j)
                        object.rank_points[j] = message.rank_points[j];
                }
                return object;
            };

            CalculateParam.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CalculateParam.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameFinalSnapshot.CalculateParam";
            };

            return CalculateParam;
        })();

        GameFinalSnapshot.GameSeat = (function() {

            function GameSeat(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameSeat.prototype.type = 0;
            GameSeat.prototype.account_id = 0;
            GameSeat.prototype.notify_endpoint = null;
            GameSeat.prototype.client_address = "";
            GameSeat.prototype.is_connected = false;

            GameSeat.create = function create(properties) {
                return new GameSeat(properties);
            };

            GameSeat.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(16).uint32(message.account_id);
                if (message.notify_endpoint != null && Object.hasOwnProperty.call(message, "notify_endpoint"))
                    $root.lq.NetworkEndpoint.encode(message.notify_endpoint, writer.uint32(26).fork()).ldelim();
                if (message.client_address != null && Object.hasOwnProperty.call(message, "client_address"))
                    writer.uint32(34).string(message.client_address);
                if (message.is_connected != null && Object.hasOwnProperty.call(message, "is_connected"))
                    writer.uint32(40).bool(message.is_connected);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameSeat.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameSeat.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameFinalSnapshot.GameSeat(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.notify_endpoint = $root.lq.NetworkEndpoint.decode(reader, reader.uint32(), undefined, _depth + 1, message.notify_endpoint);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.client_address = value;
                            else
                                delete message.client_address;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_connected = value;
                            else
                                delete message.is_connected;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameSeat.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameSeat.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.notify_endpoint != null && message.hasOwnProperty("notify_endpoint")) {
                    let error = $root.lq.NetworkEndpoint.verify(message.notify_endpoint, _depth + 1);
                    if (error)
                        return "notify_endpoint." + error;
                }
                if (message.client_address != null && message.hasOwnProperty("client_address"))
                    if (!$util.isString(message.client_address))
                        return "client_address: string expected";
                if (message.is_connected != null && message.hasOwnProperty("is_connected"))
                    if (typeof message.is_connected !== "boolean")
                        return "is_connected: boolean expected";
                return null;
            };

            GameSeat.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameFinalSnapshot.GameSeat)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameFinalSnapshot.GameSeat();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.notify_endpoint != null) {
                    if (typeof object.notify_endpoint !== "object")
                        throw TypeError(".lq.GameFinalSnapshot.GameSeat.notify_endpoint: object expected");
                    message.notify_endpoint = $root.lq.NetworkEndpoint.fromObject(object.notify_endpoint, _depth + 1);
                }
                if (object.client_address != null)
                    if (typeof object.client_address !== "string" || object.client_address.length)
                        message.client_address = String(object.client_address);
                if (object.is_connected != null)
                    if (object.is_connected)
                        message.is_connected = Boolean(object.is_connected);
                return message;
            };

            GameSeat.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.account_id = 0;
                    object.notify_endpoint = null;
                    object.client_address = "";
                    object.is_connected = false;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.notify_endpoint != null && message.hasOwnProperty("notify_endpoint"))
                    object.notify_endpoint = $root.lq.NetworkEndpoint.toObject(message.notify_endpoint, options);
                if (message.client_address != null && message.hasOwnProperty("client_address"))
                    object.client_address = message.client_address;
                if (message.is_connected != null && message.hasOwnProperty("is_connected"))
                    object.is_connected = message.is_connected;
                return object;
            };

            GameSeat.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameSeat.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameFinalSnapshot.GameSeat";
            };

            return GameSeat;
        })();

        GameFinalSnapshot.FinalPlayer = (function() {

            function FinalPlayer(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FinalPlayer.prototype.seat = 0;
            FinalPlayer.prototype.total_point = 0;
            FinalPlayer.prototype.part_point_1 = 0;
            FinalPlayer.prototype.part_point_2 = 0;
            FinalPlayer.prototype.grading_score = 0;
            FinalPlayer.prototype.gold = 0;

            FinalPlayer.create = function create(properties) {
                return new FinalPlayer(properties);
            };

            FinalPlayer.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.total_point != null && Object.hasOwnProperty.call(message, "total_point"))
                    writer.uint32(16).int32(message.total_point);
                if (message.part_point_1 != null && Object.hasOwnProperty.call(message, "part_point_1"))
                    writer.uint32(24).int32(message.part_point_1);
                if (message.part_point_2 != null && Object.hasOwnProperty.call(message, "part_point_2"))
                    writer.uint32(32).int32(message.part_point_2);
                if (message.grading_score != null && Object.hasOwnProperty.call(message, "grading_score"))
                    writer.uint32(40).int32(message.grading_score);
                if (message.gold != null && Object.hasOwnProperty.call(message, "gold"))
                    writer.uint32(48).int32(message.gold);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FinalPlayer.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FinalPlayer.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameFinalSnapshot.FinalPlayer(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.total_point = value;
                            else
                                delete message.total_point;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.part_point_1 = value;
                            else
                                delete message.part_point_1;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.part_point_2 = value;
                            else
                                delete message.part_point_2;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.grading_score = value;
                            else
                                delete message.grading_score;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.gold = value;
                            else
                                delete message.gold;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FinalPlayer.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FinalPlayer.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    if (!$util.isInteger(message.total_point))
                        return "total_point: integer expected";
                if (message.part_point_1 != null && message.hasOwnProperty("part_point_1"))
                    if (!$util.isInteger(message.part_point_1))
                        return "part_point_1: integer expected";
                if (message.part_point_2 != null && message.hasOwnProperty("part_point_2"))
                    if (!$util.isInteger(message.part_point_2))
                        return "part_point_2: integer expected";
                if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                    if (!$util.isInteger(message.grading_score))
                        return "grading_score: integer expected";
                if (message.gold != null && message.hasOwnProperty("gold"))
                    if (!$util.isInteger(message.gold))
                        return "gold: integer expected";
                return null;
            };

            FinalPlayer.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameFinalSnapshot.FinalPlayer)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameFinalSnapshot.FinalPlayer();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.total_point != null)
                    if (Number(object.total_point) !== 0)
                        message.total_point = object.total_point | 0;
                if (object.part_point_1 != null)
                    if (Number(object.part_point_1) !== 0)
                        message.part_point_1 = object.part_point_1 | 0;
                if (object.part_point_2 != null)
                    if (Number(object.part_point_2) !== 0)
                        message.part_point_2 = object.part_point_2 | 0;
                if (object.grading_score != null)
                    if (Number(object.grading_score) !== 0)
                        message.grading_score = object.grading_score | 0;
                if (object.gold != null)
                    if (Number(object.gold) !== 0)
                        message.gold = object.gold | 0;
                return message;
            };

            FinalPlayer.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.total_point = 0;
                    object.part_point_1 = 0;
                    object.part_point_2 = 0;
                    object.grading_score = 0;
                    object.gold = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    object.total_point = message.total_point;
                if (message.part_point_1 != null && message.hasOwnProperty("part_point_1"))
                    object.part_point_1 = message.part_point_1;
                if (message.part_point_2 != null && message.hasOwnProperty("part_point_2"))
                    object.part_point_2 = message.part_point_2;
                if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                    object.grading_score = message.grading_score;
                if (message.gold != null && message.hasOwnProperty("gold"))
                    object.gold = message.gold;
                return object;
            };

            FinalPlayer.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FinalPlayer.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameFinalSnapshot.FinalPlayer";
            };

            return FinalPlayer;
        })();

        GameFinalSnapshot.AFKInfo = (function() {

            function AFKInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AFKInfo.prototype.deal_tile_count = 0;
            AFKInfo.prototype.moqie_count = 0;
            AFKInfo.prototype.seat = 0;

            AFKInfo.create = function create(properties) {
                return new AFKInfo(properties);
            };

            AFKInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.deal_tile_count != null && Object.hasOwnProperty.call(message, "deal_tile_count"))
                    writer.uint32(8).uint32(message.deal_tile_count);
                if (message.moqie_count != null && Object.hasOwnProperty.call(message, "moqie_count"))
                    writer.uint32(16).uint32(message.moqie_count);
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(24).uint32(message.seat);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AFKInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AFKInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameFinalSnapshot.AFKInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.deal_tile_count = value;
                            else
                                delete message.deal_tile_count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.moqie_count = value;
                            else
                                delete message.moqie_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AFKInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AFKInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.deal_tile_count != null && message.hasOwnProperty("deal_tile_count"))
                    if (!$util.isInteger(message.deal_tile_count))
                        return "deal_tile_count: integer expected";
                if (message.moqie_count != null && message.hasOwnProperty("moqie_count"))
                    if (!$util.isInteger(message.moqie_count))
                        return "moqie_count: integer expected";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                return null;
            };

            AFKInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameFinalSnapshot.AFKInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameFinalSnapshot.AFKInfo();
                if (object.deal_tile_count != null)
                    if (Number(object.deal_tile_count) !== 0)
                        message.deal_tile_count = object.deal_tile_count >>> 0;
                if (object.moqie_count != null)
                    if (Number(object.moqie_count) !== 0)
                        message.moqie_count = object.moqie_count >>> 0;
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                return message;
            };

            AFKInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.deal_tile_count = 0;
                    object.moqie_count = 0;
                    object.seat = 0;
                }
                if (message.deal_tile_count != null && message.hasOwnProperty("deal_tile_count"))
                    object.deal_tile_count = message.deal_tile_count;
                if (message.moqie_count != null && message.hasOwnProperty("moqie_count"))
                    object.moqie_count = message.moqie_count;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                return object;
            };

            AFKInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AFKInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameFinalSnapshot.AFKInfo";
            };

            return AFKInfo;
        })();

        return GameFinalSnapshot;
    })();

    lq.RecordCollectedData = (function() {

        function RecordCollectedData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordCollectedData.prototype.uuid = "";
        RecordCollectedData.prototype.remarks = "";
        RecordCollectedData.prototype.start_time = 0;
        RecordCollectedData.prototype.end_time = 0;

        RecordCollectedData.create = function create(properties) {
            return new RecordCollectedData(properties);
        };

        RecordCollectedData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.remarks != null && Object.hasOwnProperty.call(message, "remarks"))
                writer.uint32(18).string(message.remarks);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(24).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(32).uint32(message.end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordCollectedData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordCollectedData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordCollectedData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.remarks = value;
                        else
                            delete message.remarks;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordCollectedData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordCollectedData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                if (!$util.isString(message.remarks))
                    return "remarks: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            return null;
        };

        RecordCollectedData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordCollectedData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordCollectedData();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.remarks != null)
                if (typeof object.remarks !== "string" || object.remarks.length)
                    message.remarks = String(object.remarks);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            return message;
        };

        RecordCollectedData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.uuid = "";
                object.remarks = "";
                object.start_time = 0;
                object.end_time = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                object.remarks = message.remarks;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            return object;
        };

        RecordCollectedData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordCollectedData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordCollectedData";
        };

        return RecordCollectedData;
    })();

    lq.ContestDetailRule = (function() {

        function ContestDetailRule(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ContestDetailRule.prototype.init_point = 0;
        ContestDetailRule.prototype.fandian = 0;
        ContestDetailRule.prototype.can_jifei = false;
        ContestDetailRule.prototype.tianbian_value = 0;
        ContestDetailRule.prototype.liqibang_value = 0;
        ContestDetailRule.prototype.changbang_value = 0;
        ContestDetailRule.prototype.noting_fafu_1 = 0;
        ContestDetailRule.prototype.noting_fafu_2 = 0;
        ContestDetailRule.prototype.noting_fafu_3 = 0;
        ContestDetailRule.prototype.have_liujumanguan = false;
        ContestDetailRule.prototype.have_qieshangmanguan = false;
        ContestDetailRule.prototype.have_biao_dora = false;
        ContestDetailRule.prototype.have_gang_biao_dora = false;
        ContestDetailRule.prototype.ming_dora_immediately_open = false;
        ContestDetailRule.prototype.have_li_dora = false;
        ContestDetailRule.prototype.have_gang_li_dora = false;
        ContestDetailRule.prototype.have_sifenglianda = false;
        ContestDetailRule.prototype.have_sigangsanle = false;
        ContestDetailRule.prototype.have_sijializhi = false;
        ContestDetailRule.prototype.have_jiuzhongjiupai = false;
        ContestDetailRule.prototype.have_sanjiahele = false;
        ContestDetailRule.prototype.have_toutiao = false;
        ContestDetailRule.prototype.have_helelianzhuang = false;
        ContestDetailRule.prototype.have_helezhongju = false;
        ContestDetailRule.prototype.have_tingpailianzhuang = false;
        ContestDetailRule.prototype.have_tingpaizhongju = false;
        ContestDetailRule.prototype.have_yifa = false;
        ContestDetailRule.prototype.have_nanruxiru = false;
        ContestDetailRule.prototype.jingsuanyuandian = 0;
        ContestDetailRule.prototype.shunweima_2 = 0;
        ContestDetailRule.prototype.shunweima_3 = 0;
        ContestDetailRule.prototype.shunweima_4 = 0;
        ContestDetailRule.prototype.bianjietishi = false;
        ContestDetailRule.prototype.ai_level = 0;
        ContestDetailRule.prototype.have_zimosun = false;
        ContestDetailRule.prototype.disable_multi_yukaman = false;
        ContestDetailRule.prototype.guyi_mode = 0;
        ContestDetailRule.prototype.disable_leijiyiman = false;
        ContestDetailRule.prototype.dora3_mode = 0;
        ContestDetailRule.prototype.xuezhandaodi = 0;
        ContestDetailRule.prototype.huansanzhang = 0;
        ContestDetailRule.prototype.chuanma = 0;
        ContestDetailRule.prototype.disable_double_yakuman = 0;
        ContestDetailRule.prototype.disable_composite_yakuman = 0;
        ContestDetailRule.prototype.enable_shiti = 0;
        ContestDetailRule.prototype.enable_nontsumo_liqi = 0;
        ContestDetailRule.prototype.disable_double_wind_four_fu = 0;
        ContestDetailRule.prototype.disable_angang_guoshi = 0;
        ContestDetailRule.prototype.enable_renhe = 0;
        ContestDetailRule.prototype.enable_baopai_extend_settings = 0;
        ContestDetailRule.prototype.fanfu = 0;

        ContestDetailRule.create = function create(properties) {
            return new ContestDetailRule(properties);
        };

        ContestDetailRule.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.init_point != null && Object.hasOwnProperty.call(message, "init_point"))
                writer.uint32(40).uint32(message.init_point);
            if (message.fandian != null && Object.hasOwnProperty.call(message, "fandian"))
                writer.uint32(48).uint32(message.fandian);
            if (message.can_jifei != null && Object.hasOwnProperty.call(message, "can_jifei"))
                writer.uint32(56).bool(message.can_jifei);
            if (message.tianbian_value != null && Object.hasOwnProperty.call(message, "tianbian_value"))
                writer.uint32(64).uint32(message.tianbian_value);
            if (message.liqibang_value != null && Object.hasOwnProperty.call(message, "liqibang_value"))
                writer.uint32(72).uint32(message.liqibang_value);
            if (message.changbang_value != null && Object.hasOwnProperty.call(message, "changbang_value"))
                writer.uint32(80).uint32(message.changbang_value);
            if (message.noting_fafu_1 != null && Object.hasOwnProperty.call(message, "noting_fafu_1"))
                writer.uint32(88).uint32(message.noting_fafu_1);
            if (message.noting_fafu_2 != null && Object.hasOwnProperty.call(message, "noting_fafu_2"))
                writer.uint32(96).uint32(message.noting_fafu_2);
            if (message.noting_fafu_3 != null && Object.hasOwnProperty.call(message, "noting_fafu_3"))
                writer.uint32(104).uint32(message.noting_fafu_3);
            if (message.have_liujumanguan != null && Object.hasOwnProperty.call(message, "have_liujumanguan"))
                writer.uint32(112).bool(message.have_liujumanguan);
            if (message.have_qieshangmanguan != null && Object.hasOwnProperty.call(message, "have_qieshangmanguan"))
                writer.uint32(120).bool(message.have_qieshangmanguan);
            if (message.have_biao_dora != null && Object.hasOwnProperty.call(message, "have_biao_dora"))
                writer.uint32(128).bool(message.have_biao_dora);
            if (message.have_gang_biao_dora != null && Object.hasOwnProperty.call(message, "have_gang_biao_dora"))
                writer.uint32(136).bool(message.have_gang_biao_dora);
            if (message.ming_dora_immediately_open != null && Object.hasOwnProperty.call(message, "ming_dora_immediately_open"))
                writer.uint32(144).bool(message.ming_dora_immediately_open);
            if (message.have_li_dora != null && Object.hasOwnProperty.call(message, "have_li_dora"))
                writer.uint32(152).bool(message.have_li_dora);
            if (message.have_gang_li_dora != null && Object.hasOwnProperty.call(message, "have_gang_li_dora"))
                writer.uint32(160).bool(message.have_gang_li_dora);
            if (message.have_sifenglianda != null && Object.hasOwnProperty.call(message, "have_sifenglianda"))
                writer.uint32(168).bool(message.have_sifenglianda);
            if (message.have_sigangsanle != null && Object.hasOwnProperty.call(message, "have_sigangsanle"))
                writer.uint32(176).bool(message.have_sigangsanle);
            if (message.have_sijializhi != null && Object.hasOwnProperty.call(message, "have_sijializhi"))
                writer.uint32(184).bool(message.have_sijializhi);
            if (message.have_jiuzhongjiupai != null && Object.hasOwnProperty.call(message, "have_jiuzhongjiupai"))
                writer.uint32(192).bool(message.have_jiuzhongjiupai);
            if (message.have_sanjiahele != null && Object.hasOwnProperty.call(message, "have_sanjiahele"))
                writer.uint32(200).bool(message.have_sanjiahele);
            if (message.have_toutiao != null && Object.hasOwnProperty.call(message, "have_toutiao"))
                writer.uint32(208).bool(message.have_toutiao);
            if (message.have_helelianzhuang != null && Object.hasOwnProperty.call(message, "have_helelianzhuang"))
                writer.uint32(216).bool(message.have_helelianzhuang);
            if (message.have_helezhongju != null && Object.hasOwnProperty.call(message, "have_helezhongju"))
                writer.uint32(224).bool(message.have_helezhongju);
            if (message.have_tingpailianzhuang != null && Object.hasOwnProperty.call(message, "have_tingpailianzhuang"))
                writer.uint32(232).bool(message.have_tingpailianzhuang);
            if (message.have_tingpaizhongju != null && Object.hasOwnProperty.call(message, "have_tingpaizhongju"))
                writer.uint32(240).bool(message.have_tingpaizhongju);
            if (message.have_yifa != null && Object.hasOwnProperty.call(message, "have_yifa"))
                writer.uint32(248).bool(message.have_yifa);
            if (message.have_nanruxiru != null && Object.hasOwnProperty.call(message, "have_nanruxiru"))
                writer.uint32(256).bool(message.have_nanruxiru);
            if (message.jingsuanyuandian != null && Object.hasOwnProperty.call(message, "jingsuanyuandian"))
                writer.uint32(264).uint32(message.jingsuanyuandian);
            if (message.shunweima_2 != null && Object.hasOwnProperty.call(message, "shunweima_2"))
                writer.uint32(272).int32(message.shunweima_2);
            if (message.shunweima_3 != null && Object.hasOwnProperty.call(message, "shunweima_3"))
                writer.uint32(280).int32(message.shunweima_3);
            if (message.shunweima_4 != null && Object.hasOwnProperty.call(message, "shunweima_4"))
                writer.uint32(288).int32(message.shunweima_4);
            if (message.bianjietishi != null && Object.hasOwnProperty.call(message, "bianjietishi"))
                writer.uint32(296).bool(message.bianjietishi);
            if (message.ai_level != null && Object.hasOwnProperty.call(message, "ai_level"))
                writer.uint32(304).uint32(message.ai_level);
            if (message.have_zimosun != null && Object.hasOwnProperty.call(message, "have_zimosun"))
                writer.uint32(312).bool(message.have_zimosun);
            if (message.disable_multi_yukaman != null && Object.hasOwnProperty.call(message, "disable_multi_yukaman"))
                writer.uint32(320).bool(message.disable_multi_yukaman);
            if (message.guyi_mode != null && Object.hasOwnProperty.call(message, "guyi_mode"))
                writer.uint32(328).uint32(message.guyi_mode);
            if (message.disable_leijiyiman != null && Object.hasOwnProperty.call(message, "disable_leijiyiman"))
                writer.uint32(336).bool(message.disable_leijiyiman);
            if (message.dora3_mode != null && Object.hasOwnProperty.call(message, "dora3_mode"))
                writer.uint32(344).uint32(message.dora3_mode);
            if (message.xuezhandaodi != null && Object.hasOwnProperty.call(message, "xuezhandaodi"))
                writer.uint32(352).uint32(message.xuezhandaodi);
            if (message.huansanzhang != null && Object.hasOwnProperty.call(message, "huansanzhang"))
                writer.uint32(360).uint32(message.huansanzhang);
            if (message.chuanma != null && Object.hasOwnProperty.call(message, "chuanma"))
                writer.uint32(368).uint32(message.chuanma);
            if (message.disable_double_yakuman != null && Object.hasOwnProperty.call(message, "disable_double_yakuman"))
                writer.uint32(496).uint32(message.disable_double_yakuman);
            if (message.disable_composite_yakuman != null && Object.hasOwnProperty.call(message, "disable_composite_yakuman"))
                writer.uint32(504).uint32(message.disable_composite_yakuman);
            if (message.enable_shiti != null && Object.hasOwnProperty.call(message, "enable_shiti"))
                writer.uint32(512).uint32(message.enable_shiti);
            if (message.enable_nontsumo_liqi != null && Object.hasOwnProperty.call(message, "enable_nontsumo_liqi"))
                writer.uint32(520).uint32(message.enable_nontsumo_liqi);
            if (message.disable_double_wind_four_fu != null && Object.hasOwnProperty.call(message, "disable_double_wind_four_fu"))
                writer.uint32(528).uint32(message.disable_double_wind_four_fu);
            if (message.disable_angang_guoshi != null && Object.hasOwnProperty.call(message, "disable_angang_guoshi"))
                writer.uint32(536).uint32(message.disable_angang_guoshi);
            if (message.enable_renhe != null && Object.hasOwnProperty.call(message, "enable_renhe"))
                writer.uint32(544).uint32(message.enable_renhe);
            if (message.enable_baopai_extend_settings != null && Object.hasOwnProperty.call(message, "enable_baopai_extend_settings"))
                writer.uint32(552).uint32(message.enable_baopai_extend_settings);
            if (message.fanfu != null && Object.hasOwnProperty.call(message, "fanfu"))
                writer.uint32(560).uint32(message.fanfu);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ContestDetailRule.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ContestDetailRule.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestDetailRule(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.init_point = value;
                        else
                            delete message.init_point;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fandian = value;
                        else
                            delete message.fandian;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.can_jifei = value;
                        else
                            delete message.can_jifei;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tianbian_value = value;
                        else
                            delete message.tianbian_value;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang_value = value;
                        else
                            delete message.liqibang_value;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.changbang_value = value;
                        else
                            delete message.changbang_value;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_1 = value;
                        else
                            delete message.noting_fafu_1;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_2 = value;
                        else
                            delete message.noting_fafu_2;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.noting_fafu_3 = value;
                        else
                            delete message.noting_fafu_3;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_liujumanguan = value;
                        else
                            delete message.have_liujumanguan;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_qieshangmanguan = value;
                        else
                            delete message.have_qieshangmanguan;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_biao_dora = value;
                        else
                            delete message.have_biao_dora;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_gang_biao_dora = value;
                        else
                            delete message.have_gang_biao_dora;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ming_dora_immediately_open = value;
                        else
                            delete message.ming_dora_immediately_open;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_li_dora = value;
                        else
                            delete message.have_li_dora;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_gang_li_dora = value;
                        else
                            delete message.have_gang_li_dora;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sifenglianda = value;
                        else
                            delete message.have_sifenglianda;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sigangsanle = value;
                        else
                            delete message.have_sigangsanle;
                        continue;
                    }
                case 23: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sijializhi = value;
                        else
                            delete message.have_sijializhi;
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_jiuzhongjiupai = value;
                        else
                            delete message.have_jiuzhongjiupai;
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_sanjiahele = value;
                        else
                            delete message.have_sanjiahele;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_toutiao = value;
                        else
                            delete message.have_toutiao;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_helelianzhuang = value;
                        else
                            delete message.have_helelianzhuang;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_helezhongju = value;
                        else
                            delete message.have_helezhongju;
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_tingpailianzhuang = value;
                        else
                            delete message.have_tingpailianzhuang;
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_tingpaizhongju = value;
                        else
                            delete message.have_tingpaizhongju;
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_yifa = value;
                        else
                            delete message.have_yifa;
                        continue;
                    }
                case 32: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_nanruxiru = value;
                        else
                            delete message.have_nanruxiru;
                        continue;
                    }
                case 33: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.jingsuanyuandian = value;
                        else
                            delete message.jingsuanyuandian;
                        continue;
                    }
                case 34: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_2 = value;
                        else
                            delete message.shunweima_2;
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_3 = value;
                        else
                            delete message.shunweima_3;
                        continue;
                    }
                case 36: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.shunweima_4 = value;
                        else
                            delete message.shunweima_4;
                        continue;
                    }
                case 37: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.bianjietishi = value;
                        else
                            delete message.bianjietishi;
                        continue;
                    }
                case 38: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ai_level = value;
                        else
                            delete message.ai_level;
                        continue;
                    }
                case 39: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.have_zimosun = value;
                        else
                            delete message.have_zimosun;
                        continue;
                    }
                case 40: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.disable_multi_yukaman = value;
                        else
                            delete message.disable_multi_yukaman;
                        continue;
                    }
                case 41: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.guyi_mode = value;
                        else
                            delete message.guyi_mode;
                        continue;
                    }
                case 42: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.disable_leijiyiman = value;
                        else
                            delete message.disable_leijiyiman;
                        continue;
                    }
                case 43: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dora3_mode = value;
                        else
                            delete message.dora3_mode;
                        continue;
                    }
                case 44: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xuezhandaodi = value;
                        else
                            delete message.xuezhandaodi;
                        continue;
                    }
                case 45: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.huansanzhang = value;
                        else
                            delete message.huansanzhang;
                        continue;
                    }
                case 46: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chuanma = value;
                        else
                            delete message.chuanma;
                        continue;
                    }
                case 62: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_double_yakuman = value;
                        else
                            delete message.disable_double_yakuman;
                        continue;
                    }
                case 63: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_composite_yakuman = value;
                        else
                            delete message.disable_composite_yakuman;
                        continue;
                    }
                case 64: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_shiti = value;
                        else
                            delete message.enable_shiti;
                        continue;
                    }
                case 65: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_nontsumo_liqi = value;
                        else
                            delete message.enable_nontsumo_liqi;
                        continue;
                    }
                case 66: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_double_wind_four_fu = value;
                        else
                            delete message.disable_double_wind_four_fu;
                        continue;
                    }
                case 67: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_angang_guoshi = value;
                        else
                            delete message.disable_angang_guoshi;
                        continue;
                    }
                case 68: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_renhe = value;
                        else
                            delete message.enable_renhe;
                        continue;
                    }
                case 69: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enable_baopai_extend_settings = value;
                        else
                            delete message.enable_baopai_extend_settings;
                        continue;
                    }
                case 70: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fanfu = value;
                        else
                            delete message.fanfu;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ContestDetailRule.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ContestDetailRule.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.init_point != null && message.hasOwnProperty("init_point"))
                if (!$util.isInteger(message.init_point))
                    return "init_point: integer expected";
            if (message.fandian != null && message.hasOwnProperty("fandian"))
                if (!$util.isInteger(message.fandian))
                    return "fandian: integer expected";
            if (message.can_jifei != null && message.hasOwnProperty("can_jifei"))
                if (typeof message.can_jifei !== "boolean")
                    return "can_jifei: boolean expected";
            if (message.tianbian_value != null && message.hasOwnProperty("tianbian_value"))
                if (!$util.isInteger(message.tianbian_value))
                    return "tianbian_value: integer expected";
            if (message.liqibang_value != null && message.hasOwnProperty("liqibang_value"))
                if (!$util.isInteger(message.liqibang_value))
                    return "liqibang_value: integer expected";
            if (message.changbang_value != null && message.hasOwnProperty("changbang_value"))
                if (!$util.isInteger(message.changbang_value))
                    return "changbang_value: integer expected";
            if (message.noting_fafu_1 != null && message.hasOwnProperty("noting_fafu_1"))
                if (!$util.isInteger(message.noting_fafu_1))
                    return "noting_fafu_1: integer expected";
            if (message.noting_fafu_2 != null && message.hasOwnProperty("noting_fafu_2"))
                if (!$util.isInteger(message.noting_fafu_2))
                    return "noting_fafu_2: integer expected";
            if (message.noting_fafu_3 != null && message.hasOwnProperty("noting_fafu_3"))
                if (!$util.isInteger(message.noting_fafu_3))
                    return "noting_fafu_3: integer expected";
            if (message.have_liujumanguan != null && message.hasOwnProperty("have_liujumanguan"))
                if (typeof message.have_liujumanguan !== "boolean")
                    return "have_liujumanguan: boolean expected";
            if (message.have_qieshangmanguan != null && message.hasOwnProperty("have_qieshangmanguan"))
                if (typeof message.have_qieshangmanguan !== "boolean")
                    return "have_qieshangmanguan: boolean expected";
            if (message.have_biao_dora != null && message.hasOwnProperty("have_biao_dora"))
                if (typeof message.have_biao_dora !== "boolean")
                    return "have_biao_dora: boolean expected";
            if (message.have_gang_biao_dora != null && message.hasOwnProperty("have_gang_biao_dora"))
                if (typeof message.have_gang_biao_dora !== "boolean")
                    return "have_gang_biao_dora: boolean expected";
            if (message.ming_dora_immediately_open != null && message.hasOwnProperty("ming_dora_immediately_open"))
                if (typeof message.ming_dora_immediately_open !== "boolean")
                    return "ming_dora_immediately_open: boolean expected";
            if (message.have_li_dora != null && message.hasOwnProperty("have_li_dora"))
                if (typeof message.have_li_dora !== "boolean")
                    return "have_li_dora: boolean expected";
            if (message.have_gang_li_dora != null && message.hasOwnProperty("have_gang_li_dora"))
                if (typeof message.have_gang_li_dora !== "boolean")
                    return "have_gang_li_dora: boolean expected";
            if (message.have_sifenglianda != null && message.hasOwnProperty("have_sifenglianda"))
                if (typeof message.have_sifenglianda !== "boolean")
                    return "have_sifenglianda: boolean expected";
            if (message.have_sigangsanle != null && message.hasOwnProperty("have_sigangsanle"))
                if (typeof message.have_sigangsanle !== "boolean")
                    return "have_sigangsanle: boolean expected";
            if (message.have_sijializhi != null && message.hasOwnProperty("have_sijializhi"))
                if (typeof message.have_sijializhi !== "boolean")
                    return "have_sijializhi: boolean expected";
            if (message.have_jiuzhongjiupai != null && message.hasOwnProperty("have_jiuzhongjiupai"))
                if (typeof message.have_jiuzhongjiupai !== "boolean")
                    return "have_jiuzhongjiupai: boolean expected";
            if (message.have_sanjiahele != null && message.hasOwnProperty("have_sanjiahele"))
                if (typeof message.have_sanjiahele !== "boolean")
                    return "have_sanjiahele: boolean expected";
            if (message.have_toutiao != null && message.hasOwnProperty("have_toutiao"))
                if (typeof message.have_toutiao !== "boolean")
                    return "have_toutiao: boolean expected";
            if (message.have_helelianzhuang != null && message.hasOwnProperty("have_helelianzhuang"))
                if (typeof message.have_helelianzhuang !== "boolean")
                    return "have_helelianzhuang: boolean expected";
            if (message.have_helezhongju != null && message.hasOwnProperty("have_helezhongju"))
                if (typeof message.have_helezhongju !== "boolean")
                    return "have_helezhongju: boolean expected";
            if (message.have_tingpailianzhuang != null && message.hasOwnProperty("have_tingpailianzhuang"))
                if (typeof message.have_tingpailianzhuang !== "boolean")
                    return "have_tingpailianzhuang: boolean expected";
            if (message.have_tingpaizhongju != null && message.hasOwnProperty("have_tingpaizhongju"))
                if (typeof message.have_tingpaizhongju !== "boolean")
                    return "have_tingpaizhongju: boolean expected";
            if (message.have_yifa != null && message.hasOwnProperty("have_yifa"))
                if (typeof message.have_yifa !== "boolean")
                    return "have_yifa: boolean expected";
            if (message.have_nanruxiru != null && message.hasOwnProperty("have_nanruxiru"))
                if (typeof message.have_nanruxiru !== "boolean")
                    return "have_nanruxiru: boolean expected";
            if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                if (!$util.isInteger(message.jingsuanyuandian))
                    return "jingsuanyuandian: integer expected";
            if (message.shunweima_2 != null && message.hasOwnProperty("shunweima_2"))
                if (!$util.isInteger(message.shunweima_2))
                    return "shunweima_2: integer expected";
            if (message.shunweima_3 != null && message.hasOwnProperty("shunweima_3"))
                if (!$util.isInteger(message.shunweima_3))
                    return "shunweima_3: integer expected";
            if (message.shunweima_4 != null && message.hasOwnProperty("shunweima_4"))
                if (!$util.isInteger(message.shunweima_4))
                    return "shunweima_4: integer expected";
            if (message.bianjietishi != null && message.hasOwnProperty("bianjietishi"))
                if (typeof message.bianjietishi !== "boolean")
                    return "bianjietishi: boolean expected";
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                if (!$util.isInteger(message.ai_level))
                    return "ai_level: integer expected";
            if (message.have_zimosun != null && message.hasOwnProperty("have_zimosun"))
                if (typeof message.have_zimosun !== "boolean")
                    return "have_zimosun: boolean expected";
            if (message.disable_multi_yukaman != null && message.hasOwnProperty("disable_multi_yukaman"))
                if (typeof message.disable_multi_yukaman !== "boolean")
                    return "disable_multi_yukaman: boolean expected";
            if (message.guyi_mode != null && message.hasOwnProperty("guyi_mode"))
                if (!$util.isInteger(message.guyi_mode))
                    return "guyi_mode: integer expected";
            if (message.disable_leijiyiman != null && message.hasOwnProperty("disable_leijiyiman"))
                if (typeof message.disable_leijiyiman !== "boolean")
                    return "disable_leijiyiman: boolean expected";
            if (message.dora3_mode != null && message.hasOwnProperty("dora3_mode"))
                if (!$util.isInteger(message.dora3_mode))
                    return "dora3_mode: integer expected";
            if (message.xuezhandaodi != null && message.hasOwnProperty("xuezhandaodi"))
                if (!$util.isInteger(message.xuezhandaodi))
                    return "xuezhandaodi: integer expected";
            if (message.huansanzhang != null && message.hasOwnProperty("huansanzhang"))
                if (!$util.isInteger(message.huansanzhang))
                    return "huansanzhang: integer expected";
            if (message.chuanma != null && message.hasOwnProperty("chuanma"))
                if (!$util.isInteger(message.chuanma))
                    return "chuanma: integer expected";
            if (message.disable_double_yakuman != null && message.hasOwnProperty("disable_double_yakuman"))
                if (!$util.isInteger(message.disable_double_yakuman))
                    return "disable_double_yakuman: integer expected";
            if (message.disable_composite_yakuman != null && message.hasOwnProperty("disable_composite_yakuman"))
                if (!$util.isInteger(message.disable_composite_yakuman))
                    return "disable_composite_yakuman: integer expected";
            if (message.enable_shiti != null && message.hasOwnProperty("enable_shiti"))
                if (!$util.isInteger(message.enable_shiti))
                    return "enable_shiti: integer expected";
            if (message.enable_nontsumo_liqi != null && message.hasOwnProperty("enable_nontsumo_liqi"))
                if (!$util.isInteger(message.enable_nontsumo_liqi))
                    return "enable_nontsumo_liqi: integer expected";
            if (message.disable_double_wind_four_fu != null && message.hasOwnProperty("disable_double_wind_four_fu"))
                if (!$util.isInteger(message.disable_double_wind_four_fu))
                    return "disable_double_wind_four_fu: integer expected";
            if (message.disable_angang_guoshi != null && message.hasOwnProperty("disable_angang_guoshi"))
                if (!$util.isInteger(message.disable_angang_guoshi))
                    return "disable_angang_guoshi: integer expected";
            if (message.enable_renhe != null && message.hasOwnProperty("enable_renhe"))
                if (!$util.isInteger(message.enable_renhe))
                    return "enable_renhe: integer expected";
            if (message.enable_baopai_extend_settings != null && message.hasOwnProperty("enable_baopai_extend_settings"))
                if (!$util.isInteger(message.enable_baopai_extend_settings))
                    return "enable_baopai_extend_settings: integer expected";
            if (message.fanfu != null && message.hasOwnProperty("fanfu"))
                if (!$util.isInteger(message.fanfu))
                    return "fanfu: integer expected";
            return null;
        };

        ContestDetailRule.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ContestDetailRule)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ContestDetailRule();
            if (object.init_point != null)
                if (Number(object.init_point) !== 0)
                    message.init_point = object.init_point >>> 0;
            if (object.fandian != null)
                if (Number(object.fandian) !== 0)
                    message.fandian = object.fandian >>> 0;
            if (object.can_jifei != null)
                if (object.can_jifei)
                    message.can_jifei = Boolean(object.can_jifei);
            if (object.tianbian_value != null)
                if (Number(object.tianbian_value) !== 0)
                    message.tianbian_value = object.tianbian_value >>> 0;
            if (object.liqibang_value != null)
                if (Number(object.liqibang_value) !== 0)
                    message.liqibang_value = object.liqibang_value >>> 0;
            if (object.changbang_value != null)
                if (Number(object.changbang_value) !== 0)
                    message.changbang_value = object.changbang_value >>> 0;
            if (object.noting_fafu_1 != null)
                if (Number(object.noting_fafu_1) !== 0)
                    message.noting_fafu_1 = object.noting_fafu_1 >>> 0;
            if (object.noting_fafu_2 != null)
                if (Number(object.noting_fafu_2) !== 0)
                    message.noting_fafu_2 = object.noting_fafu_2 >>> 0;
            if (object.noting_fafu_3 != null)
                if (Number(object.noting_fafu_3) !== 0)
                    message.noting_fafu_3 = object.noting_fafu_3 >>> 0;
            if (object.have_liujumanguan != null)
                if (object.have_liujumanguan)
                    message.have_liujumanguan = Boolean(object.have_liujumanguan);
            if (object.have_qieshangmanguan != null)
                if (object.have_qieshangmanguan)
                    message.have_qieshangmanguan = Boolean(object.have_qieshangmanguan);
            if (object.have_biao_dora != null)
                if (object.have_biao_dora)
                    message.have_biao_dora = Boolean(object.have_biao_dora);
            if (object.have_gang_biao_dora != null)
                if (object.have_gang_biao_dora)
                    message.have_gang_biao_dora = Boolean(object.have_gang_biao_dora);
            if (object.ming_dora_immediately_open != null)
                if (object.ming_dora_immediately_open)
                    message.ming_dora_immediately_open = Boolean(object.ming_dora_immediately_open);
            if (object.have_li_dora != null)
                if (object.have_li_dora)
                    message.have_li_dora = Boolean(object.have_li_dora);
            if (object.have_gang_li_dora != null)
                if (object.have_gang_li_dora)
                    message.have_gang_li_dora = Boolean(object.have_gang_li_dora);
            if (object.have_sifenglianda != null)
                if (object.have_sifenglianda)
                    message.have_sifenglianda = Boolean(object.have_sifenglianda);
            if (object.have_sigangsanle != null)
                if (object.have_sigangsanle)
                    message.have_sigangsanle = Boolean(object.have_sigangsanle);
            if (object.have_sijializhi != null)
                if (object.have_sijializhi)
                    message.have_sijializhi = Boolean(object.have_sijializhi);
            if (object.have_jiuzhongjiupai != null)
                if (object.have_jiuzhongjiupai)
                    message.have_jiuzhongjiupai = Boolean(object.have_jiuzhongjiupai);
            if (object.have_sanjiahele != null)
                if (object.have_sanjiahele)
                    message.have_sanjiahele = Boolean(object.have_sanjiahele);
            if (object.have_toutiao != null)
                if (object.have_toutiao)
                    message.have_toutiao = Boolean(object.have_toutiao);
            if (object.have_helelianzhuang != null)
                if (object.have_helelianzhuang)
                    message.have_helelianzhuang = Boolean(object.have_helelianzhuang);
            if (object.have_helezhongju != null)
                if (object.have_helezhongju)
                    message.have_helezhongju = Boolean(object.have_helezhongju);
            if (object.have_tingpailianzhuang != null)
                if (object.have_tingpailianzhuang)
                    message.have_tingpailianzhuang = Boolean(object.have_tingpailianzhuang);
            if (object.have_tingpaizhongju != null)
                if (object.have_tingpaizhongju)
                    message.have_tingpaizhongju = Boolean(object.have_tingpaizhongju);
            if (object.have_yifa != null)
                if (object.have_yifa)
                    message.have_yifa = Boolean(object.have_yifa);
            if (object.have_nanruxiru != null)
                if (object.have_nanruxiru)
                    message.have_nanruxiru = Boolean(object.have_nanruxiru);
            if (object.jingsuanyuandian != null)
                if (Number(object.jingsuanyuandian) !== 0)
                    message.jingsuanyuandian = object.jingsuanyuandian >>> 0;
            if (object.shunweima_2 != null)
                if (Number(object.shunweima_2) !== 0)
                    message.shunweima_2 = object.shunweima_2 | 0;
            if (object.shunweima_3 != null)
                if (Number(object.shunweima_3) !== 0)
                    message.shunweima_3 = object.shunweima_3 | 0;
            if (object.shunweima_4 != null)
                if (Number(object.shunweima_4) !== 0)
                    message.shunweima_4 = object.shunweima_4 | 0;
            if (object.bianjietishi != null)
                if (object.bianjietishi)
                    message.bianjietishi = Boolean(object.bianjietishi);
            if (object.ai_level != null)
                if (Number(object.ai_level) !== 0)
                    message.ai_level = object.ai_level >>> 0;
            if (object.have_zimosun != null)
                if (object.have_zimosun)
                    message.have_zimosun = Boolean(object.have_zimosun);
            if (object.disable_multi_yukaman != null)
                if (object.disable_multi_yukaman)
                    message.disable_multi_yukaman = Boolean(object.disable_multi_yukaman);
            if (object.guyi_mode != null)
                if (Number(object.guyi_mode) !== 0)
                    message.guyi_mode = object.guyi_mode >>> 0;
            if (object.disable_leijiyiman != null)
                if (object.disable_leijiyiman)
                    message.disable_leijiyiman = Boolean(object.disable_leijiyiman);
            if (object.dora3_mode != null)
                if (Number(object.dora3_mode) !== 0)
                    message.dora3_mode = object.dora3_mode >>> 0;
            if (object.xuezhandaodi != null)
                if (Number(object.xuezhandaodi) !== 0)
                    message.xuezhandaodi = object.xuezhandaodi >>> 0;
            if (object.huansanzhang != null)
                if (Number(object.huansanzhang) !== 0)
                    message.huansanzhang = object.huansanzhang >>> 0;
            if (object.chuanma != null)
                if (Number(object.chuanma) !== 0)
                    message.chuanma = object.chuanma >>> 0;
            if (object.disable_double_yakuman != null)
                if (Number(object.disable_double_yakuman) !== 0)
                    message.disable_double_yakuman = object.disable_double_yakuman >>> 0;
            if (object.disable_composite_yakuman != null)
                if (Number(object.disable_composite_yakuman) !== 0)
                    message.disable_composite_yakuman = object.disable_composite_yakuman >>> 0;
            if (object.enable_shiti != null)
                if (Number(object.enable_shiti) !== 0)
                    message.enable_shiti = object.enable_shiti >>> 0;
            if (object.enable_nontsumo_liqi != null)
                if (Number(object.enable_nontsumo_liqi) !== 0)
                    message.enable_nontsumo_liqi = object.enable_nontsumo_liqi >>> 0;
            if (object.disable_double_wind_four_fu != null)
                if (Number(object.disable_double_wind_four_fu) !== 0)
                    message.disable_double_wind_four_fu = object.disable_double_wind_four_fu >>> 0;
            if (object.disable_angang_guoshi != null)
                if (Number(object.disable_angang_guoshi) !== 0)
                    message.disable_angang_guoshi = object.disable_angang_guoshi >>> 0;
            if (object.enable_renhe != null)
                if (Number(object.enable_renhe) !== 0)
                    message.enable_renhe = object.enable_renhe >>> 0;
            if (object.enable_baopai_extend_settings != null)
                if (Number(object.enable_baopai_extend_settings) !== 0)
                    message.enable_baopai_extend_settings = object.enable_baopai_extend_settings >>> 0;
            if (object.fanfu != null)
                if (Number(object.fanfu) !== 0)
                    message.fanfu = object.fanfu >>> 0;
            return message;
        };

        ContestDetailRule.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.init_point = 0;
                object.fandian = 0;
                object.can_jifei = false;
                object.tianbian_value = 0;
                object.liqibang_value = 0;
                object.changbang_value = 0;
                object.noting_fafu_1 = 0;
                object.noting_fafu_2 = 0;
                object.noting_fafu_3 = 0;
                object.have_liujumanguan = false;
                object.have_qieshangmanguan = false;
                object.have_biao_dora = false;
                object.have_gang_biao_dora = false;
                object.ming_dora_immediately_open = false;
                object.have_li_dora = false;
                object.have_gang_li_dora = false;
                object.have_sifenglianda = false;
                object.have_sigangsanle = false;
                object.have_sijializhi = false;
                object.have_jiuzhongjiupai = false;
                object.have_sanjiahele = false;
                object.have_toutiao = false;
                object.have_helelianzhuang = false;
                object.have_helezhongju = false;
                object.have_tingpailianzhuang = false;
                object.have_tingpaizhongju = false;
                object.have_yifa = false;
                object.have_nanruxiru = false;
                object.jingsuanyuandian = 0;
                object.shunweima_2 = 0;
                object.shunweima_3 = 0;
                object.shunweima_4 = 0;
                object.bianjietishi = false;
                object.ai_level = 0;
                object.have_zimosun = false;
                object.disable_multi_yukaman = false;
                object.guyi_mode = 0;
                object.disable_leijiyiman = false;
                object.dora3_mode = 0;
                object.xuezhandaodi = 0;
                object.huansanzhang = 0;
                object.chuanma = 0;
                object.disable_double_yakuman = 0;
                object.disable_composite_yakuman = 0;
                object.enable_shiti = 0;
                object.enable_nontsumo_liqi = 0;
                object.disable_double_wind_four_fu = 0;
                object.disable_angang_guoshi = 0;
                object.enable_renhe = 0;
                object.enable_baopai_extend_settings = 0;
                object.fanfu = 0;
            }
            if (message.init_point != null && message.hasOwnProperty("init_point"))
                object.init_point = message.init_point;
            if (message.fandian != null && message.hasOwnProperty("fandian"))
                object.fandian = message.fandian;
            if (message.can_jifei != null && message.hasOwnProperty("can_jifei"))
                object.can_jifei = message.can_jifei;
            if (message.tianbian_value != null && message.hasOwnProperty("tianbian_value"))
                object.tianbian_value = message.tianbian_value;
            if (message.liqibang_value != null && message.hasOwnProperty("liqibang_value"))
                object.liqibang_value = message.liqibang_value;
            if (message.changbang_value != null && message.hasOwnProperty("changbang_value"))
                object.changbang_value = message.changbang_value;
            if (message.noting_fafu_1 != null && message.hasOwnProperty("noting_fafu_1"))
                object.noting_fafu_1 = message.noting_fafu_1;
            if (message.noting_fafu_2 != null && message.hasOwnProperty("noting_fafu_2"))
                object.noting_fafu_2 = message.noting_fafu_2;
            if (message.noting_fafu_3 != null && message.hasOwnProperty("noting_fafu_3"))
                object.noting_fafu_3 = message.noting_fafu_3;
            if (message.have_liujumanguan != null && message.hasOwnProperty("have_liujumanguan"))
                object.have_liujumanguan = message.have_liujumanguan;
            if (message.have_qieshangmanguan != null && message.hasOwnProperty("have_qieshangmanguan"))
                object.have_qieshangmanguan = message.have_qieshangmanguan;
            if (message.have_biao_dora != null && message.hasOwnProperty("have_biao_dora"))
                object.have_biao_dora = message.have_biao_dora;
            if (message.have_gang_biao_dora != null && message.hasOwnProperty("have_gang_biao_dora"))
                object.have_gang_biao_dora = message.have_gang_biao_dora;
            if (message.ming_dora_immediately_open != null && message.hasOwnProperty("ming_dora_immediately_open"))
                object.ming_dora_immediately_open = message.ming_dora_immediately_open;
            if (message.have_li_dora != null && message.hasOwnProperty("have_li_dora"))
                object.have_li_dora = message.have_li_dora;
            if (message.have_gang_li_dora != null && message.hasOwnProperty("have_gang_li_dora"))
                object.have_gang_li_dora = message.have_gang_li_dora;
            if (message.have_sifenglianda != null && message.hasOwnProperty("have_sifenglianda"))
                object.have_sifenglianda = message.have_sifenglianda;
            if (message.have_sigangsanle != null && message.hasOwnProperty("have_sigangsanle"))
                object.have_sigangsanle = message.have_sigangsanle;
            if (message.have_sijializhi != null && message.hasOwnProperty("have_sijializhi"))
                object.have_sijializhi = message.have_sijializhi;
            if (message.have_jiuzhongjiupai != null && message.hasOwnProperty("have_jiuzhongjiupai"))
                object.have_jiuzhongjiupai = message.have_jiuzhongjiupai;
            if (message.have_sanjiahele != null && message.hasOwnProperty("have_sanjiahele"))
                object.have_sanjiahele = message.have_sanjiahele;
            if (message.have_toutiao != null && message.hasOwnProperty("have_toutiao"))
                object.have_toutiao = message.have_toutiao;
            if (message.have_helelianzhuang != null && message.hasOwnProperty("have_helelianzhuang"))
                object.have_helelianzhuang = message.have_helelianzhuang;
            if (message.have_helezhongju != null && message.hasOwnProperty("have_helezhongju"))
                object.have_helezhongju = message.have_helezhongju;
            if (message.have_tingpailianzhuang != null && message.hasOwnProperty("have_tingpailianzhuang"))
                object.have_tingpailianzhuang = message.have_tingpailianzhuang;
            if (message.have_tingpaizhongju != null && message.hasOwnProperty("have_tingpaizhongju"))
                object.have_tingpaizhongju = message.have_tingpaizhongju;
            if (message.have_yifa != null && message.hasOwnProperty("have_yifa"))
                object.have_yifa = message.have_yifa;
            if (message.have_nanruxiru != null && message.hasOwnProperty("have_nanruxiru"))
                object.have_nanruxiru = message.have_nanruxiru;
            if (message.jingsuanyuandian != null && message.hasOwnProperty("jingsuanyuandian"))
                object.jingsuanyuandian = message.jingsuanyuandian;
            if (message.shunweima_2 != null && message.hasOwnProperty("shunweima_2"))
                object.shunweima_2 = message.shunweima_2;
            if (message.shunweima_3 != null && message.hasOwnProperty("shunweima_3"))
                object.shunweima_3 = message.shunweima_3;
            if (message.shunweima_4 != null && message.hasOwnProperty("shunweima_4"))
                object.shunweima_4 = message.shunweima_4;
            if (message.bianjietishi != null && message.hasOwnProperty("bianjietishi"))
                object.bianjietishi = message.bianjietishi;
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                object.ai_level = message.ai_level;
            if (message.have_zimosun != null && message.hasOwnProperty("have_zimosun"))
                object.have_zimosun = message.have_zimosun;
            if (message.disable_multi_yukaman != null && message.hasOwnProperty("disable_multi_yukaman"))
                object.disable_multi_yukaman = message.disable_multi_yukaman;
            if (message.guyi_mode != null && message.hasOwnProperty("guyi_mode"))
                object.guyi_mode = message.guyi_mode;
            if (message.disable_leijiyiman != null && message.hasOwnProperty("disable_leijiyiman"))
                object.disable_leijiyiman = message.disable_leijiyiman;
            if (message.dora3_mode != null && message.hasOwnProperty("dora3_mode"))
                object.dora3_mode = message.dora3_mode;
            if (message.xuezhandaodi != null && message.hasOwnProperty("xuezhandaodi"))
                object.xuezhandaodi = message.xuezhandaodi;
            if (message.huansanzhang != null && message.hasOwnProperty("huansanzhang"))
                object.huansanzhang = message.huansanzhang;
            if (message.chuanma != null && message.hasOwnProperty("chuanma"))
                object.chuanma = message.chuanma;
            if (message.disable_double_yakuman != null && message.hasOwnProperty("disable_double_yakuman"))
                object.disable_double_yakuman = message.disable_double_yakuman;
            if (message.disable_composite_yakuman != null && message.hasOwnProperty("disable_composite_yakuman"))
                object.disable_composite_yakuman = message.disable_composite_yakuman;
            if (message.enable_shiti != null && message.hasOwnProperty("enable_shiti"))
                object.enable_shiti = message.enable_shiti;
            if (message.enable_nontsumo_liqi != null && message.hasOwnProperty("enable_nontsumo_liqi"))
                object.enable_nontsumo_liqi = message.enable_nontsumo_liqi;
            if (message.disable_double_wind_four_fu != null && message.hasOwnProperty("disable_double_wind_four_fu"))
                object.disable_double_wind_four_fu = message.disable_double_wind_four_fu;
            if (message.disable_angang_guoshi != null && message.hasOwnProperty("disable_angang_guoshi"))
                object.disable_angang_guoshi = message.disable_angang_guoshi;
            if (message.enable_renhe != null && message.hasOwnProperty("enable_renhe"))
                object.enable_renhe = message.enable_renhe;
            if (message.enable_baopai_extend_settings != null && message.hasOwnProperty("enable_baopai_extend_settings"))
                object.enable_baopai_extend_settings = message.enable_baopai_extend_settings;
            if (message.fanfu != null && message.hasOwnProperty("fanfu"))
                object.fanfu = message.fanfu;
            return object;
        };

        ContestDetailRule.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContestDetailRule.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ContestDetailRule";
        };

        return ContestDetailRule;
    })();

    lq.ContestDetailRuleV2 = (function() {

        function ContestDetailRuleV2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ContestDetailRuleV2.prototype.game_rule = null;
        ContestDetailRuleV2.prototype.extra_rule = null;

        ContestDetailRuleV2.create = function create(properties) {
            return new ContestDetailRuleV2(properties);
        };

        ContestDetailRuleV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_rule != null && Object.hasOwnProperty.call(message, "game_rule"))
                $root.lq.ContestDetailRule.encode(message.game_rule, writer.uint32(10).fork()).ldelim();
            if (message.extra_rule != null && Object.hasOwnProperty.call(message, "extra_rule"))
                $root.lq.ContestDetailRuleV2.ExtraRule.encode(message.extra_rule, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ContestDetailRuleV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ContestDetailRuleV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestDetailRuleV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.game_rule = $root.lq.ContestDetailRule.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_rule);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.extra_rule = $root.lq.ContestDetailRuleV2.ExtraRule.decode(reader, reader.uint32(), undefined, _depth + 1, message.extra_rule);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ContestDetailRuleV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ContestDetailRuleV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_rule != null && message.hasOwnProperty("game_rule")) {
                let error = $root.lq.ContestDetailRule.verify(message.game_rule, _depth + 1);
                if (error)
                    return "game_rule." + error;
            }
            if (message.extra_rule != null && message.hasOwnProperty("extra_rule")) {
                let error = $root.lq.ContestDetailRuleV2.ExtraRule.verify(message.extra_rule, _depth + 1);
                if (error)
                    return "extra_rule." + error;
            }
            return null;
        };

        ContestDetailRuleV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ContestDetailRuleV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ContestDetailRuleV2();
            if (object.game_rule != null) {
                if (typeof object.game_rule !== "object")
                    throw TypeError(".lq.ContestDetailRuleV2.game_rule: object expected");
                message.game_rule = $root.lq.ContestDetailRule.fromObject(object.game_rule, _depth + 1);
            }
            if (object.extra_rule != null) {
                if (typeof object.extra_rule !== "object")
                    throw TypeError(".lq.ContestDetailRuleV2.extra_rule: object expected");
                message.extra_rule = $root.lq.ContestDetailRuleV2.ExtraRule.fromObject(object.extra_rule, _depth + 1);
            }
            return message;
        };

        ContestDetailRuleV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_rule = null;
                object.extra_rule = null;
            }
            if (message.game_rule != null && message.hasOwnProperty("game_rule"))
                object.game_rule = $root.lq.ContestDetailRule.toObject(message.game_rule, options);
            if (message.extra_rule != null && message.hasOwnProperty("extra_rule"))
                object.extra_rule = $root.lq.ContestDetailRuleV2.ExtraRule.toObject(message.extra_rule, options);
            return object;
        };

        ContestDetailRuleV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContestDetailRuleV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ContestDetailRuleV2";
        };

        ContestDetailRuleV2.ExtraRule = (function() {

            function ExtraRule(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ExtraRule.prototype.required_level = 0;
            ExtraRule.prototype.max_game_count = 0;

            ExtraRule.create = function create(properties) {
                return new ExtraRule(properties);
            };

            ExtraRule.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.required_level != null && Object.hasOwnProperty.call(message, "required_level"))
                    writer.uint32(8).uint32(message.required_level);
                if (message.max_game_count != null && Object.hasOwnProperty.call(message, "max_game_count"))
                    writer.uint32(16).uint32(message.max_game_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ExtraRule.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ExtraRule.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestDetailRuleV2.ExtraRule(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.required_level = value;
                            else
                                delete message.required_level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.max_game_count = value;
                            else
                                delete message.max_game_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ExtraRule.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ExtraRule.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.required_level != null && message.hasOwnProperty("required_level"))
                    if (!$util.isInteger(message.required_level))
                        return "required_level: integer expected";
                if (message.max_game_count != null && message.hasOwnProperty("max_game_count"))
                    if (!$util.isInteger(message.max_game_count))
                        return "max_game_count: integer expected";
                return null;
            };

            ExtraRule.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ContestDetailRuleV2.ExtraRule)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ContestDetailRuleV2.ExtraRule();
                if (object.required_level != null)
                    if (Number(object.required_level) !== 0)
                        message.required_level = object.required_level >>> 0;
                if (object.max_game_count != null)
                    if (Number(object.max_game_count) !== 0)
                        message.max_game_count = object.max_game_count >>> 0;
                return message;
            };

            ExtraRule.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.required_level = 0;
                    object.max_game_count = 0;
                }
                if (message.required_level != null && message.hasOwnProperty("required_level"))
                    object.required_level = message.required_level;
                if (message.max_game_count != null && message.hasOwnProperty("max_game_count"))
                    object.max_game_count = message.max_game_count;
                return object;
            };

            ExtraRule.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExtraRule.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ContestDetailRuleV2.ExtraRule";
            };

            return ExtraRule;
        })();

        return ContestDetailRuleV2;
    })();

    lq.GameRuleSetting = (function() {

        function GameRuleSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRuleSetting.prototype.round_type = 0;
        GameRuleSetting.prototype.shiduan = false;
        GameRuleSetting.prototype.dora_count = 0;
        GameRuleSetting.prototype.thinking_type = 0;
        GameRuleSetting.prototype.use_detail_rule = false;
        GameRuleSetting.prototype.detail_rule_v2 = null;

        GameRuleSetting.create = function create(properties) {
            return new GameRuleSetting(properties);
        };

        GameRuleSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round_type != null && Object.hasOwnProperty.call(message, "round_type"))
                writer.uint32(8).uint32(message.round_type);
            if (message.shiduan != null && Object.hasOwnProperty.call(message, "shiduan"))
                writer.uint32(16).bool(message.shiduan);
            if (message.dora_count != null && Object.hasOwnProperty.call(message, "dora_count"))
                writer.uint32(24).uint32(message.dora_count);
            if (message.thinking_type != null && Object.hasOwnProperty.call(message, "thinking_type"))
                writer.uint32(32).uint32(message.thinking_type);
            if (message.use_detail_rule != null && Object.hasOwnProperty.call(message, "use_detail_rule"))
                writer.uint32(40).bool(message.use_detail_rule);
            if (message.detail_rule_v2 != null && Object.hasOwnProperty.call(message, "detail_rule_v2"))
                $root.lq.ContestDetailRuleV2.encode(message.detail_rule_v2, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRuleSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRuleSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRuleSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round_type = value;
                        else
                            delete message.round_type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.shiduan = value;
                        else
                            delete message.shiduan;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dora_count = value;
                        else
                            delete message.dora_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.thinking_type = value;
                        else
                            delete message.thinking_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_detail_rule = value;
                        else
                            delete message.use_detail_rule;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.detail_rule_v2 = $root.lq.ContestDetailRuleV2.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail_rule_v2);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRuleSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRuleSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round_type != null && message.hasOwnProperty("round_type"))
                if (!$util.isInteger(message.round_type))
                    return "round_type: integer expected";
            if (message.shiduan != null && message.hasOwnProperty("shiduan"))
                if (typeof message.shiduan !== "boolean")
                    return "shiduan: boolean expected";
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                if (!$util.isInteger(message.dora_count))
                    return "dora_count: integer expected";
            if (message.thinking_type != null && message.hasOwnProperty("thinking_type"))
                if (!$util.isInteger(message.thinking_type))
                    return "thinking_type: integer expected";
            if (message.use_detail_rule != null && message.hasOwnProperty("use_detail_rule"))
                if (typeof message.use_detail_rule !== "boolean")
                    return "use_detail_rule: boolean expected";
            if (message.detail_rule_v2 != null && message.hasOwnProperty("detail_rule_v2")) {
                let error = $root.lq.ContestDetailRuleV2.verify(message.detail_rule_v2, _depth + 1);
                if (error)
                    return "detail_rule_v2." + error;
            }
            return null;
        };

        GameRuleSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRuleSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRuleSetting();
            if (object.round_type != null)
                if (Number(object.round_type) !== 0)
                    message.round_type = object.round_type >>> 0;
            if (object.shiduan != null)
                if (object.shiduan)
                    message.shiduan = Boolean(object.shiduan);
            if (object.dora_count != null)
                if (Number(object.dora_count) !== 0)
                    message.dora_count = object.dora_count >>> 0;
            if (object.thinking_type != null)
                if (Number(object.thinking_type) !== 0)
                    message.thinking_type = object.thinking_type >>> 0;
            if (object.use_detail_rule != null)
                if (object.use_detail_rule)
                    message.use_detail_rule = Boolean(object.use_detail_rule);
            if (object.detail_rule_v2 != null) {
                if (typeof object.detail_rule_v2 !== "object")
                    throw TypeError(".lq.GameRuleSetting.detail_rule_v2: object expected");
                message.detail_rule_v2 = $root.lq.ContestDetailRuleV2.fromObject(object.detail_rule_v2, _depth + 1);
            }
            return message;
        };

        GameRuleSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.round_type = 0;
                object.shiduan = false;
                object.dora_count = 0;
                object.thinking_type = 0;
                object.use_detail_rule = false;
                object.detail_rule_v2 = null;
            }
            if (message.round_type != null && message.hasOwnProperty("round_type"))
                object.round_type = message.round_type;
            if (message.shiduan != null && message.hasOwnProperty("shiduan"))
                object.shiduan = message.shiduan;
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                object.dora_count = message.dora_count;
            if (message.thinking_type != null && message.hasOwnProperty("thinking_type"))
                object.thinking_type = message.thinking_type;
            if (message.use_detail_rule != null && message.hasOwnProperty("use_detail_rule"))
                object.use_detail_rule = message.use_detail_rule;
            if (message.detail_rule_v2 != null && message.hasOwnProperty("detail_rule_v2"))
                object.detail_rule_v2 = $root.lq.ContestDetailRuleV2.toObject(message.detail_rule_v2, options);
            return object;
        };

        GameRuleSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRuleSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRuleSetting";
        };

        return GameRuleSetting;
    })();

    lq.RecordTingPaiInfo = (function() {

        function RecordTingPaiInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordTingPaiInfo.prototype.tile = "";
        RecordTingPaiInfo.prototype.haveyi = false;
        RecordTingPaiInfo.prototype.yiman = false;
        RecordTingPaiInfo.prototype.count = 0;
        RecordTingPaiInfo.prototype.fu = 0;
        RecordTingPaiInfo.prototype.biao_dora_count = 0;
        RecordTingPaiInfo.prototype.yiman_zimo = false;
        RecordTingPaiInfo.prototype.count_zimo = 0;
        RecordTingPaiInfo.prototype.fu_zimo = 0;

        RecordTingPaiInfo.create = function create(properties) {
            return new RecordTingPaiInfo(properties);
        };

        RecordTingPaiInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(10).string(message.tile);
            if (message.haveyi != null && Object.hasOwnProperty.call(message, "haveyi"))
                writer.uint32(16).bool(message.haveyi);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(24).bool(message.yiman);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(32).uint32(message.count);
            if (message.fu != null && Object.hasOwnProperty.call(message, "fu"))
                writer.uint32(40).uint32(message.fu);
            if (message.biao_dora_count != null && Object.hasOwnProperty.call(message, "biao_dora_count"))
                writer.uint32(48).uint32(message.biao_dora_count);
            if (message.yiman_zimo != null && Object.hasOwnProperty.call(message, "yiman_zimo"))
                writer.uint32(56).bool(message.yiman_zimo);
            if (message.count_zimo != null && Object.hasOwnProperty.call(message, "count_zimo"))
                writer.uint32(64).uint32(message.count_zimo);
            if (message.fu_zimo != null && Object.hasOwnProperty.call(message, "fu_zimo"))
                writer.uint32(72).uint32(message.fu_zimo);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordTingPaiInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordTingPaiInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordTingPaiInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.haveyi = value;
                        else
                            delete message.haveyi;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu = value;
                        else
                            delete message.fu;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.biao_dora_count = value;
                        else
                            delete message.biao_dora_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman_zimo = value;
                        else
                            delete message.yiman_zimo;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count_zimo = value;
                        else
                            delete message.count_zimo;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu_zimo = value;
                        else
                            delete message.fu_zimo;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordTingPaiInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordTingPaiInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.haveyi != null && message.hasOwnProperty("haveyi"))
                if (typeof message.haveyi !== "boolean")
                    return "haveyi: boolean expected";
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.fu != null && message.hasOwnProperty("fu"))
                if (!$util.isInteger(message.fu))
                    return "fu: integer expected";
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                if (!$util.isInteger(message.biao_dora_count))
                    return "biao_dora_count: integer expected";
            if (message.yiman_zimo != null && message.hasOwnProperty("yiman_zimo"))
                if (typeof message.yiman_zimo !== "boolean")
                    return "yiman_zimo: boolean expected";
            if (message.count_zimo != null && message.hasOwnProperty("count_zimo"))
                if (!$util.isInteger(message.count_zimo))
                    return "count_zimo: integer expected";
            if (message.fu_zimo != null && message.hasOwnProperty("fu_zimo"))
                if (!$util.isInteger(message.fu_zimo))
                    return "fu_zimo: integer expected";
            return null;
        };

        RecordTingPaiInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordTingPaiInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordTingPaiInfo();
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.haveyi != null)
                if (object.haveyi)
                    message.haveyi = Boolean(object.haveyi);
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.fu != null)
                if (Number(object.fu) !== 0)
                    message.fu = object.fu >>> 0;
            if (object.biao_dora_count != null)
                if (Number(object.biao_dora_count) !== 0)
                    message.biao_dora_count = object.biao_dora_count >>> 0;
            if (object.yiman_zimo != null)
                if (object.yiman_zimo)
                    message.yiman_zimo = Boolean(object.yiman_zimo);
            if (object.count_zimo != null)
                if (Number(object.count_zimo) !== 0)
                    message.count_zimo = object.count_zimo >>> 0;
            if (object.fu_zimo != null)
                if (Number(object.fu_zimo) !== 0)
                    message.fu_zimo = object.fu_zimo >>> 0;
            return message;
        };

        RecordTingPaiInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tile = "";
                object.haveyi = false;
                object.yiman = false;
                object.count = 0;
                object.fu = 0;
                object.biao_dora_count = 0;
                object.yiman_zimo = false;
                object.count_zimo = 0;
                object.fu_zimo = 0;
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.haveyi != null && message.hasOwnProperty("haveyi"))
                object.haveyi = message.haveyi;
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.fu != null && message.hasOwnProperty("fu"))
                object.fu = message.fu;
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                object.biao_dora_count = message.biao_dora_count;
            if (message.yiman_zimo != null && message.hasOwnProperty("yiman_zimo"))
                object.yiman_zimo = message.yiman_zimo;
            if (message.count_zimo != null && message.hasOwnProperty("count_zimo"))
                object.count_zimo = message.count_zimo;
            if (message.fu_zimo != null && message.hasOwnProperty("fu_zimo"))
                object.fu_zimo = message.fu_zimo;
            return object;
        };

        RecordTingPaiInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordTingPaiInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordTingPaiInfo";
        };

        return RecordTingPaiInfo;
    })();

    lq.RecordNoTilePlayerInfo = (function() {

        function RecordNoTilePlayerInfo(properties) {
            this.hand = [];
            this.tings = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordNoTilePlayerInfo.prototype.tingpai = false;
        RecordNoTilePlayerInfo.prototype.hand = $util.emptyArray;
        RecordNoTilePlayerInfo.prototype.tings = $util.emptyArray;
        RecordNoTilePlayerInfo.prototype.liuman = false;

        RecordNoTilePlayerInfo.create = function create(properties) {
            return new RecordNoTilePlayerInfo(properties);
        };

        RecordNoTilePlayerInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tingpai != null && Object.hasOwnProperty.call(message, "tingpai"))
                writer.uint32(24).bool(message.tingpai);
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(34).string(message.hand[i]);
            if (message.tings != null && message.tings.length)
                for (let i = 0; i < message.tings.length; ++i)
                    $root.lq.RecordTingPaiInfo.encode(message.tings[i], writer.uint32(42).fork()).ldelim();
            if (message.liuman != null && Object.hasOwnProperty.call(message, "liuman"))
                writer.uint32(48).bool(message.liuman);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordNoTilePlayerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordNoTilePlayerInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNoTilePlayerInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.tingpai = value;
                        else
                            delete message.tingpai;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tings && message.tings.length))
                            message.tings = [];
                        message.tings.push($root.lq.RecordTingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liuman = value;
                        else
                            delete message.liuman;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordNoTilePlayerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordNoTilePlayerInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tingpai != null && message.hasOwnProperty("tingpai"))
                if (typeof message.tingpai !== "boolean")
                    return "tingpai: boolean expected";
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.tings != null && message.hasOwnProperty("tings")) {
                if (!Array.isArray(message.tings))
                    return "tings: array expected";
                for (let i = 0; i < message.tings.length; ++i) {
                    let error = $root.lq.RecordTingPaiInfo.verify(message.tings[i], _depth + 1);
                    if (error)
                        return "tings." + error;
                }
            }
            if (message.liuman != null && message.hasOwnProperty("liuman"))
                if (typeof message.liuman !== "boolean")
                    return "liuman: boolean expected";
            return null;
        };

        RecordNoTilePlayerInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordNoTilePlayerInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordNoTilePlayerInfo();
            if (object.tingpai != null)
                if (object.tingpai)
                    message.tingpai = Boolean(object.tingpai);
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.RecordNoTilePlayerInfo.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.tings) {
                if (!Array.isArray(object.tings))
                    throw TypeError(".lq.RecordNoTilePlayerInfo.tings: array expected");
                message.tings = Array(object.tings.length);
                for (let i = 0; i < object.tings.length; ++i) {
                    if (typeof object.tings[i] !== "object")
                        throw TypeError(".lq.RecordNoTilePlayerInfo.tings: object expected");
                    message.tings[i] = $root.lq.RecordTingPaiInfo.fromObject(object.tings[i], _depth + 1);
                }
            }
            if (object.liuman != null)
                if (object.liuman)
                    message.liuman = Boolean(object.liuman);
            return message;
        };

        RecordNoTilePlayerInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hand = [];
                object.tings = [];
            }
            if (options.defaults) {
                object.tingpai = false;
                object.liuman = false;
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai"))
                object.tingpai = message.tingpai;
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.tings && message.tings.length) {
                object.tings = Array(message.tings.length);
                for (let j = 0; j < message.tings.length; ++j)
                    object.tings[j] = $root.lq.RecordTingPaiInfo.toObject(message.tings[j], options);
            }
            if (message.liuman != null && message.hasOwnProperty("liuman"))
                object.liuman = message.liuman;
            return object;
        };

        RecordNoTilePlayerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordNoTilePlayerInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordNoTilePlayerInfo";
        };

        return RecordNoTilePlayerInfo;
    })();

    lq.RecordHuleInfo = (function() {

        function RecordHuleInfo(properties) {
            this.hand = [];
            this.ming = [];
            this.doras = [];
            this.li_doras = [];
            this.fans = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordHuleInfo.prototype.hand = $util.emptyArray;
        RecordHuleInfo.prototype.ming = $util.emptyArray;
        RecordHuleInfo.prototype.hu_tile = "";
        RecordHuleInfo.prototype.seat = 0;
        RecordHuleInfo.prototype.zimo = false;
        RecordHuleInfo.prototype.qinjia = false;
        RecordHuleInfo.prototype.liqi = false;
        RecordHuleInfo.prototype.doras = $util.emptyArray;
        RecordHuleInfo.prototype.li_doras = $util.emptyArray;
        RecordHuleInfo.prototype.yiman = false;
        RecordHuleInfo.prototype.count = 0;
        RecordHuleInfo.prototype.fans = $util.emptyArray;
        RecordHuleInfo.prototype.fu = 0;
        RecordHuleInfo.prototype.point_zimo_qin = 0;
        RecordHuleInfo.prototype.point_zimo_xian = 0;
        RecordHuleInfo.prototype.title_id = 0;
        RecordHuleInfo.prototype.point_sum = 0;
        RecordHuleInfo.prototype.dadian = 0;
        RecordHuleInfo.prototype.is_jue_zhang = false;
        RecordHuleInfo.prototype.xun = 0;
        RecordHuleInfo.prototype.ting_type = 0;
        RecordHuleInfo.prototype.ting_mian = 0;

        RecordHuleInfo.create = function create(properties) {
            return new RecordHuleInfo(properties);
        };

        RecordHuleInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(10).string(message.hand[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(18).string(message.ming[i]);
            if (message.hu_tile != null && Object.hasOwnProperty.call(message, "hu_tile"))
                writer.uint32(26).string(message.hu_tile);
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(32).uint32(message.seat);
            if (message.zimo != null && Object.hasOwnProperty.call(message, "zimo"))
                writer.uint32(40).bool(message.zimo);
            if (message.qinjia != null && Object.hasOwnProperty.call(message, "qinjia"))
                writer.uint32(48).bool(message.qinjia);
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                writer.uint32(56).bool(message.liqi);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.li_doras != null && message.li_doras.length)
                for (let i = 0; i < message.li_doras.length; ++i)
                    writer.uint32(74).string(message.li_doras[i]);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(80).bool(message.yiman);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(88).uint32(message.count);
            if (message.fans != null && message.fans.length)
                for (let i = 0; i < message.fans.length; ++i)
                    $root.lq.RecordHuleInfo.RecordFanInfo.encode(message.fans[i], writer.uint32(98).fork()).ldelim();
            if (message.fu != null && Object.hasOwnProperty.call(message, "fu"))
                writer.uint32(104).uint32(message.fu);
            if (message.point_zimo_qin != null && Object.hasOwnProperty.call(message, "point_zimo_qin"))
                writer.uint32(112).uint32(message.point_zimo_qin);
            if (message.point_zimo_xian != null && Object.hasOwnProperty.call(message, "point_zimo_xian"))
                writer.uint32(120).uint32(message.point_zimo_xian);
            if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                writer.uint32(128).uint32(message.title_id);
            if (message.point_sum != null && Object.hasOwnProperty.call(message, "point_sum"))
                writer.uint32(136).uint32(message.point_sum);
            if (message.dadian != null && Object.hasOwnProperty.call(message, "dadian"))
                writer.uint32(144).uint32(message.dadian);
            if (message.is_jue_zhang != null && Object.hasOwnProperty.call(message, "is_jue_zhang"))
                writer.uint32(152).bool(message.is_jue_zhang);
            if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                writer.uint32(160).uint32(message.xun);
            if (message.ting_type != null && Object.hasOwnProperty.call(message, "ting_type"))
                writer.uint32(168).uint32(message.ting_type);
            if (message.ting_mian != null && Object.hasOwnProperty.call(message, "ting_mian"))
                writer.uint32(176).uint32(message.ting_mian);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordHuleInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordHuleInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHuleInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hu_tile = value;
                        else
                            delete message.hu_tile;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zimo = value;
                        else
                            delete message.zimo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.qinjia = value;
                        else
                            delete message.qinjia;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liqi = value;
                        else
                            delete message.liqi;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.li_doras && message.li_doras.length))
                            message.li_doras = [];
                        message.li_doras.push(reader.string());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fans && message.fans.length))
                            message.fans = [];
                        message.fans.push($root.lq.RecordHuleInfo.RecordFanInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu = value;
                        else
                            delete message.fu;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_zimo_qin = value;
                        else
                            delete message.point_zimo_qin;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_zimo_xian = value;
                        else
                            delete message.point_zimo_xian;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title_id = value;
                        else
                            delete message.title_id;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_sum = value;
                        else
                            delete message.point_sum;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dadian = value;
                        else
                            delete message.dadian;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_jue_zhang = value;
                        else
                            delete message.is_jue_zhang;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun = value;
                        else
                            delete message.xun;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ting_type = value;
                        else
                            delete message.ting_type;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ting_mian = value;
                        else
                            delete message.ting_mian;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordHuleInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordHuleInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                if (!$util.isString(message.hu_tile))
                    return "hu_tile: string expected";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                if (typeof message.zimo !== "boolean")
                    return "zimo: boolean expected";
            if (message.qinjia != null && message.hasOwnProperty("qinjia"))
                if (typeof message.qinjia !== "boolean")
                    return "qinjia: boolean expected";
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                if (typeof message.liqi !== "boolean")
                    return "liqi: boolean expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.li_doras != null && message.hasOwnProperty("li_doras")) {
                if (!Array.isArray(message.li_doras))
                    return "li_doras: array expected";
                for (let i = 0; i < message.li_doras.length; ++i)
                    if (!$util.isString(message.li_doras[i]))
                        return "li_doras: string[] expected";
            }
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.fans != null && message.hasOwnProperty("fans")) {
                if (!Array.isArray(message.fans))
                    return "fans: array expected";
                for (let i = 0; i < message.fans.length; ++i) {
                    let error = $root.lq.RecordHuleInfo.RecordFanInfo.verify(message.fans[i], _depth + 1);
                    if (error)
                        return "fans." + error;
                }
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                if (!$util.isInteger(message.fu))
                    return "fu: integer expected";
            if (message.point_zimo_qin != null && message.hasOwnProperty("point_zimo_qin"))
                if (!$util.isInteger(message.point_zimo_qin))
                    return "point_zimo_qin: integer expected";
            if (message.point_zimo_xian != null && message.hasOwnProperty("point_zimo_xian"))
                if (!$util.isInteger(message.point_zimo_xian))
                    return "point_zimo_xian: integer expected";
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                if (!$util.isInteger(message.title_id))
                    return "title_id: integer expected";
            if (message.point_sum != null && message.hasOwnProperty("point_sum"))
                if (!$util.isInteger(message.point_sum))
                    return "point_sum: integer expected";
            if (message.dadian != null && message.hasOwnProperty("dadian"))
                if (!$util.isInteger(message.dadian))
                    return "dadian: integer expected";
            if (message.is_jue_zhang != null && message.hasOwnProperty("is_jue_zhang"))
                if (typeof message.is_jue_zhang !== "boolean")
                    return "is_jue_zhang: boolean expected";
            if (message.xun != null && message.hasOwnProperty("xun"))
                if (!$util.isInteger(message.xun))
                    return "xun: integer expected";
            if (message.ting_type != null && message.hasOwnProperty("ting_type"))
                if (!$util.isInteger(message.ting_type))
                    return "ting_type: integer expected";
            if (message.ting_mian != null && message.hasOwnProperty("ting_mian"))
                if (!$util.isInteger(message.ting_mian))
                    return "ting_mian: integer expected";
            return null;
        };

        RecordHuleInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordHuleInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordHuleInfo();
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.RecordHuleInfo.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.RecordHuleInfo.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.hu_tile != null)
                if (typeof object.hu_tile !== "string" || object.hu_tile.length)
                    message.hu_tile = String(object.hu_tile);
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.zimo != null)
                if (object.zimo)
                    message.zimo = Boolean(object.zimo);
            if (object.qinjia != null)
                if (object.qinjia)
                    message.qinjia = Boolean(object.qinjia);
            if (object.liqi != null)
                if (object.liqi)
                    message.liqi = Boolean(object.liqi);
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordHuleInfo.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.li_doras) {
                if (!Array.isArray(object.li_doras))
                    throw TypeError(".lq.RecordHuleInfo.li_doras: array expected");
                message.li_doras = Array(object.li_doras.length);
                for (let i = 0; i < object.li_doras.length; ++i)
                    message.li_doras[i] = String(object.li_doras[i]);
            }
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.fans) {
                if (!Array.isArray(object.fans))
                    throw TypeError(".lq.RecordHuleInfo.fans: array expected");
                message.fans = Array(object.fans.length);
                for (let i = 0; i < object.fans.length; ++i) {
                    if (typeof object.fans[i] !== "object")
                        throw TypeError(".lq.RecordHuleInfo.fans: object expected");
                    message.fans[i] = $root.lq.RecordHuleInfo.RecordFanInfo.fromObject(object.fans[i], _depth + 1);
                }
            }
            if (object.fu != null)
                if (Number(object.fu) !== 0)
                    message.fu = object.fu >>> 0;
            if (object.point_zimo_qin != null)
                if (Number(object.point_zimo_qin) !== 0)
                    message.point_zimo_qin = object.point_zimo_qin >>> 0;
            if (object.point_zimo_xian != null)
                if (Number(object.point_zimo_xian) !== 0)
                    message.point_zimo_xian = object.point_zimo_xian >>> 0;
            if (object.title_id != null)
                if (Number(object.title_id) !== 0)
                    message.title_id = object.title_id >>> 0;
            if (object.point_sum != null)
                if (Number(object.point_sum) !== 0)
                    message.point_sum = object.point_sum >>> 0;
            if (object.dadian != null)
                if (Number(object.dadian) !== 0)
                    message.dadian = object.dadian >>> 0;
            if (object.is_jue_zhang != null)
                if (object.is_jue_zhang)
                    message.is_jue_zhang = Boolean(object.is_jue_zhang);
            if (object.xun != null)
                if (Number(object.xun) !== 0)
                    message.xun = object.xun >>> 0;
            if (object.ting_type != null)
                if (Number(object.ting_type) !== 0)
                    message.ting_type = object.ting_type >>> 0;
            if (object.ting_mian != null)
                if (Number(object.ting_mian) !== 0)
                    message.ting_mian = object.ting_mian >>> 0;
            return message;
        };

        RecordHuleInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hand = [];
                object.ming = [];
                object.doras = [];
                object.li_doras = [];
                object.fans = [];
            }
            if (options.defaults) {
                object.hu_tile = "";
                object.seat = 0;
                object.zimo = false;
                object.qinjia = false;
                object.liqi = false;
                object.yiman = false;
                object.count = 0;
                object.fu = 0;
                object.point_zimo_qin = 0;
                object.point_zimo_xian = 0;
                object.title_id = 0;
                object.point_sum = 0;
                object.dadian = 0;
                object.is_jue_zhang = false;
                object.xun = 0;
                object.ting_type = 0;
                object.ting_mian = 0;
            }
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                object.hu_tile = message.hu_tile;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                object.zimo = message.zimo;
            if (message.qinjia != null && message.hasOwnProperty("qinjia"))
                object.qinjia = message.qinjia;
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = message.liqi;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.li_doras && message.li_doras.length) {
                object.li_doras = Array(message.li_doras.length);
                for (let j = 0; j < message.li_doras.length; ++j)
                    object.li_doras[j] = message.li_doras[j];
            }
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.fans && message.fans.length) {
                object.fans = Array(message.fans.length);
                for (let j = 0; j < message.fans.length; ++j)
                    object.fans[j] = $root.lq.RecordHuleInfo.RecordFanInfo.toObject(message.fans[j], options);
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                object.fu = message.fu;
            if (message.point_zimo_qin != null && message.hasOwnProperty("point_zimo_qin"))
                object.point_zimo_qin = message.point_zimo_qin;
            if (message.point_zimo_xian != null && message.hasOwnProperty("point_zimo_xian"))
                object.point_zimo_xian = message.point_zimo_xian;
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                object.title_id = message.title_id;
            if (message.point_sum != null && message.hasOwnProperty("point_sum"))
                object.point_sum = message.point_sum;
            if (message.dadian != null && message.hasOwnProperty("dadian"))
                object.dadian = message.dadian;
            if (message.is_jue_zhang != null && message.hasOwnProperty("is_jue_zhang"))
                object.is_jue_zhang = message.is_jue_zhang;
            if (message.xun != null && message.hasOwnProperty("xun"))
                object.xun = message.xun;
            if (message.ting_type != null && message.hasOwnProperty("ting_type"))
                object.ting_type = message.ting_type;
            if (message.ting_mian != null && message.hasOwnProperty("ting_mian"))
                object.ting_mian = message.ting_mian;
            return object;
        };

        RecordHuleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordHuleInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordHuleInfo";
        };

        RecordHuleInfo.RecordFanInfo = (function() {

            function RecordFanInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RecordFanInfo.prototype.val = 0;
            RecordFanInfo.prototype.id = 0;

            RecordFanInfo.create = function create(properties) {
                return new RecordFanInfo(properties);
            };

            RecordFanInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                    writer.uint32(8).uint32(message.val);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(16).uint32(message.id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RecordFanInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RecordFanInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHuleInfo.RecordFanInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.val = value;
                            else
                                delete message.val;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RecordFanInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RecordFanInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.val != null && message.hasOwnProperty("val"))
                    if (!$util.isInteger(message.val))
                        return "val: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                return null;
            };

            RecordFanInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordHuleInfo.RecordFanInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordHuleInfo.RecordFanInfo();
                if (object.val != null)
                    if (Number(object.val) !== 0)
                        message.val = object.val >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                return message;
            };

            RecordFanInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.val = 0;
                    object.id = 0;
                }
                if (message.val != null && message.hasOwnProperty("val"))
                    object.val = message.val;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                return object;
            };

            RecordFanInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RecordFanInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordHuleInfo.RecordFanInfo";
            };

            return RecordFanInfo;
        })();

        return RecordHuleInfo;
    })();

    lq.RecordHulesInfo = (function() {

        function RecordHulesInfo(properties) {
            this.hules = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordHulesInfo.prototype.seat = 0;
        RecordHulesInfo.prototype.hules = $util.emptyArray;

        RecordHulesInfo.create = function create(properties) {
            return new RecordHulesInfo(properties);
        };

        RecordHulesInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).int32(message.seat);
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.RecordHuleInfo.encode(message.hules[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordHulesInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordHulesInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHulesInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.RecordHuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordHulesInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordHulesInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.RecordHuleInfo.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            return null;
        };

        RecordHulesInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordHulesInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordHulesInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat | 0;
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.RecordHulesInfo.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.RecordHulesInfo.hules: object expected");
                    message.hules[i] = $root.lq.RecordHuleInfo.fromObject(object.hules[i], _depth + 1);
                }
            }
            return message;
        };

        RecordHulesInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.hules = [];
            if (options.defaults)
                object.seat = 0;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.RecordHuleInfo.toObject(message.hules[j], options);
            }
            return object;
        };

        RecordHulesInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordHulesInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordHulesInfo";
        };

        return RecordHulesInfo;
    })();

    lq.RecordLiujuInfo = (function() {

        function RecordLiujuInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordLiujuInfo.prototype.seat = 0;
        RecordLiujuInfo.prototype.type = 0;

        RecordLiujuInfo.create = function create(properties) {
            return new RecordLiujuInfo(properties);
        };

        RecordLiujuInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordLiujuInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordLiujuInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordLiujuInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordLiujuInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordLiujuInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        RecordLiujuInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordLiujuInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordLiujuInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        RecordLiujuInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        RecordLiujuInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordLiujuInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordLiujuInfo";
        };

        return RecordLiujuInfo;
    })();

    lq.RecordNoTileInfo = (function() {

        function RecordNoTileInfo(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordNoTileInfo.prototype.liujumanguan = false;
        RecordNoTileInfo.prototype.players = $util.emptyArray;

        RecordNoTileInfo.create = function create(properties) {
            return new RecordNoTileInfo(properties);
        };

        RecordNoTileInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.liujumanguan != null && Object.hasOwnProperty.call(message, "liujumanguan"))
                writer.uint32(8).bool(message.liujumanguan);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.RecordNoTilePlayerInfo.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordNoTileInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordNoTileInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNoTileInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liujumanguan = value;
                        else
                            delete message.liujumanguan;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.RecordNoTilePlayerInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordNoTileInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordNoTileInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                if (typeof message.liujumanguan !== "boolean")
                    return "liujumanguan: boolean expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.RecordNoTilePlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        RecordNoTileInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordNoTileInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordNoTileInfo();
            if (object.liujumanguan != null)
                if (object.liujumanguan)
                    message.liujumanguan = Boolean(object.liujumanguan);
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.RecordNoTileInfo.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.RecordNoTileInfo.players: object expected");
                    message.players[i] = $root.lq.RecordNoTilePlayerInfo.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        RecordNoTileInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (options.defaults)
                object.liujumanguan = false;
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                object.liujumanguan = message.liujumanguan;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.RecordNoTilePlayerInfo.toObject(message.players[j], options);
            }
            return object;
        };

        RecordNoTileInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordNoTileInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordNoTileInfo";
        };

        return RecordNoTileInfo;
    })();

    lq.RecordLiqiInfo = (function() {

        function RecordLiqiInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordLiqiInfo.prototype.seat = 0;
        RecordLiqiInfo.prototype.score = 0;
        RecordLiqiInfo.prototype.is_w = false;
        RecordLiqiInfo.prototype.is_zhen_ting = false;
        RecordLiqiInfo.prototype.xun = 0;
        RecordLiqiInfo.prototype.is_success = false;

        RecordLiqiInfo.create = function create(properties) {
            return new RecordLiqiInfo(properties);
        };

        RecordLiqiInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(16).uint32(message.score);
            if (message.is_w != null && Object.hasOwnProperty.call(message, "is_w"))
                writer.uint32(24).bool(message.is_w);
            if (message.is_zhen_ting != null && Object.hasOwnProperty.call(message, "is_zhen_ting"))
                writer.uint32(32).bool(message.is_zhen_ting);
            if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                writer.uint32(40).uint32(message.xun);
            if (message.is_success != null && Object.hasOwnProperty.call(message, "is_success"))
                writer.uint32(48).bool(message.is_success);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordLiqiInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordLiqiInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordLiqiInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_w = value;
                        else
                            delete message.is_w;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_zhen_ting = value;
                        else
                            delete message.is_zhen_ting;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun = value;
                        else
                            delete message.xun;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_success = value;
                        else
                            delete message.is_success;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordLiqiInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordLiqiInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.is_w != null && message.hasOwnProperty("is_w"))
                if (typeof message.is_w !== "boolean")
                    return "is_w: boolean expected";
            if (message.is_zhen_ting != null && message.hasOwnProperty("is_zhen_ting"))
                if (typeof message.is_zhen_ting !== "boolean")
                    return "is_zhen_ting: boolean expected";
            if (message.xun != null && message.hasOwnProperty("xun"))
                if (!$util.isInteger(message.xun))
                    return "xun: integer expected";
            if (message.is_success != null && message.hasOwnProperty("is_success"))
                if (typeof message.is_success !== "boolean")
                    return "is_success: boolean expected";
            return null;
        };

        RecordLiqiInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordLiqiInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordLiqiInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score >>> 0;
            if (object.is_w != null)
                if (object.is_w)
                    message.is_w = Boolean(object.is_w);
            if (object.is_zhen_ting != null)
                if (object.is_zhen_ting)
                    message.is_zhen_ting = Boolean(object.is_zhen_ting);
            if (object.xun != null)
                if (Number(object.xun) !== 0)
                    message.xun = object.xun >>> 0;
            if (object.is_success != null)
                if (object.is_success)
                    message.is_success = Boolean(object.is_success);
            return message;
        };

        RecordLiqiInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.score = 0;
                object.is_w = false;
                object.is_zhen_ting = false;
                object.xun = 0;
                object.is_success = false;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.is_w != null && message.hasOwnProperty("is_w"))
                object.is_w = message.is_w;
            if (message.is_zhen_ting != null && message.hasOwnProperty("is_zhen_ting"))
                object.is_zhen_ting = message.is_zhen_ting;
            if (message.xun != null && message.hasOwnProperty("xun"))
                object.xun = message.xun;
            if (message.is_success != null && message.hasOwnProperty("is_success"))
                object.is_success = message.is_success;
            return object;
        };

        RecordLiqiInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordLiqiInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordLiqiInfo";
        };

        return RecordLiqiInfo;
    })();

    lq.RecordGangInfo = (function() {

        function RecordGangInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordGangInfo.prototype.seat = 0;
        RecordGangInfo.prototype.type = 0;
        RecordGangInfo.prototype.pai = "";
        RecordGangInfo.prototype.is_dora = false;
        RecordGangInfo.prototype.xun = 0;

        RecordGangInfo.create = function create(properties) {
            return new RecordGangInfo(properties);
        };

        RecordGangInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.pai != null && Object.hasOwnProperty.call(message, "pai"))
                writer.uint32(26).string(message.pai);
            if (message.is_dora != null && Object.hasOwnProperty.call(message, "is_dora"))
                writer.uint32(32).bool(message.is_dora);
            if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                writer.uint32(40).uint32(message.xun);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordGangInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordGangInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordGangInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.pai = value;
                        else
                            delete message.pai;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_dora = value;
                        else
                            delete message.is_dora;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xun = value;
                        else
                            delete message.xun;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordGangInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordGangInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.pai != null && message.hasOwnProperty("pai"))
                if (!$util.isString(message.pai))
                    return "pai: string expected";
            if (message.is_dora != null && message.hasOwnProperty("is_dora"))
                if (typeof message.is_dora !== "boolean")
                    return "is_dora: boolean expected";
            if (message.xun != null && message.hasOwnProperty("xun"))
                if (!$util.isInteger(message.xun))
                    return "xun: integer expected";
            return null;
        };

        RecordGangInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordGangInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordGangInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.pai != null)
                if (typeof object.pai !== "string" || object.pai.length)
                    message.pai = String(object.pai);
            if (object.is_dora != null)
                if (object.is_dora)
                    message.is_dora = Boolean(object.is_dora);
            if (object.xun != null)
                if (Number(object.xun) !== 0)
                    message.xun = object.xun >>> 0;
            return message;
        };

        RecordGangInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.pai = "";
                object.is_dora = false;
                object.xun = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.pai != null && message.hasOwnProperty("pai"))
                object.pai = message.pai;
            if (message.is_dora != null && message.hasOwnProperty("is_dora"))
                object.is_dora = message.is_dora;
            if (message.xun != null && message.hasOwnProperty("xun"))
                object.xun = message.xun;
            return object;
        };

        RecordGangInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordGangInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordGangInfo";
        };

        return RecordGangInfo;
    })();

    lq.RecordBaBeiInfo = (function() {

        function RecordBaBeiInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordBaBeiInfo.prototype.seat = 0;
        RecordBaBeiInfo.prototype.is_zi_mo = false;
        RecordBaBeiInfo.prototype.is_chong = false;
        RecordBaBeiInfo.prototype.is_bei = false;

        RecordBaBeiInfo.create = function create(properties) {
            return new RecordBaBeiInfo(properties);
        };

        RecordBaBeiInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.is_zi_mo != null && Object.hasOwnProperty.call(message, "is_zi_mo"))
                writer.uint32(16).bool(message.is_zi_mo);
            if (message.is_chong != null && Object.hasOwnProperty.call(message, "is_chong"))
                writer.uint32(24).bool(message.is_chong);
            if (message.is_bei != null && Object.hasOwnProperty.call(message, "is_bei"))
                writer.uint32(32).bool(message.is_bei);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordBaBeiInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordBaBeiInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordBaBeiInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_zi_mo = value;
                        else
                            delete message.is_zi_mo;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_chong = value;
                        else
                            delete message.is_chong;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_bei = value;
                        else
                            delete message.is_bei;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordBaBeiInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordBaBeiInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.is_zi_mo != null && message.hasOwnProperty("is_zi_mo"))
                if (typeof message.is_zi_mo !== "boolean")
                    return "is_zi_mo: boolean expected";
            if (message.is_chong != null && message.hasOwnProperty("is_chong"))
                if (typeof message.is_chong !== "boolean")
                    return "is_chong: boolean expected";
            if (message.is_bei != null && message.hasOwnProperty("is_bei"))
                if (typeof message.is_bei !== "boolean")
                    return "is_bei: boolean expected";
            return null;
        };

        RecordBaBeiInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordBaBeiInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordBaBeiInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.is_zi_mo != null)
                if (object.is_zi_mo)
                    message.is_zi_mo = Boolean(object.is_zi_mo);
            if (object.is_chong != null)
                if (object.is_chong)
                    message.is_chong = Boolean(object.is_chong);
            if (object.is_bei != null)
                if (object.is_bei)
                    message.is_bei = Boolean(object.is_bei);
            return message;
        };

        RecordBaBeiInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.is_zi_mo = false;
                object.is_chong = false;
                object.is_bei = false;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.is_zi_mo != null && message.hasOwnProperty("is_zi_mo"))
                object.is_zi_mo = message.is_zi_mo;
            if (message.is_chong != null && message.hasOwnProperty("is_chong"))
                object.is_chong = message.is_chong;
            if (message.is_bei != null && message.hasOwnProperty("is_bei"))
                object.is_bei = message.is_bei;
            return object;
        };

        RecordBaBeiInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordBaBeiInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordBaBeiInfo";
        };

        return RecordBaBeiInfo;
    })();

    lq.RecordPeiPaiInfo = (function() {

        function RecordPeiPaiInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordPeiPaiInfo.prototype.dora_count = 0;
        RecordPeiPaiInfo.prototype.r_dora_count = 0;
        RecordPeiPaiInfo.prototype.bei_count = 0;

        RecordPeiPaiInfo.create = function create(properties) {
            return new RecordPeiPaiInfo(properties);
        };

        RecordPeiPaiInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dora_count != null && Object.hasOwnProperty.call(message, "dora_count"))
                writer.uint32(8).uint32(message.dora_count);
            if (message.r_dora_count != null && Object.hasOwnProperty.call(message, "r_dora_count"))
                writer.uint32(16).uint32(message.r_dora_count);
            if (message.bei_count != null && Object.hasOwnProperty.call(message, "bei_count"))
                writer.uint32(24).uint32(message.bei_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordPeiPaiInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordPeiPaiInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordPeiPaiInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dora_count = value;
                        else
                            delete message.dora_count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.r_dora_count = value;
                        else
                            delete message.r_dora_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bei_count = value;
                        else
                            delete message.bei_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordPeiPaiInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordPeiPaiInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                if (!$util.isInteger(message.dora_count))
                    return "dora_count: integer expected";
            if (message.r_dora_count != null && message.hasOwnProperty("r_dora_count"))
                if (!$util.isInteger(message.r_dora_count))
                    return "r_dora_count: integer expected";
            if (message.bei_count != null && message.hasOwnProperty("bei_count"))
                if (!$util.isInteger(message.bei_count))
                    return "bei_count: integer expected";
            return null;
        };

        RecordPeiPaiInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordPeiPaiInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordPeiPaiInfo();
            if (object.dora_count != null)
                if (Number(object.dora_count) !== 0)
                    message.dora_count = object.dora_count >>> 0;
            if (object.r_dora_count != null)
                if (Number(object.r_dora_count) !== 0)
                    message.r_dora_count = object.r_dora_count >>> 0;
            if (object.bei_count != null)
                if (Number(object.bei_count) !== 0)
                    message.bei_count = object.bei_count >>> 0;
            return message;
        };

        RecordPeiPaiInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dora_count = 0;
                object.r_dora_count = 0;
                object.bei_count = 0;
            }
            if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                object.dora_count = message.dora_count;
            if (message.r_dora_count != null && message.hasOwnProperty("r_dora_count"))
                object.r_dora_count = message.r_dora_count;
            if (message.bei_count != null && message.hasOwnProperty("bei_count"))
                object.bei_count = message.bei_count;
            return object;
        };

        RecordPeiPaiInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordPeiPaiInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordPeiPaiInfo";
        };

        return RecordPeiPaiInfo;
    })();

    lq.RecordRoundInfo = (function() {

        function RecordRoundInfo(properties) {
            this.scores = [];
            this.liqi_infos = [];
            this.gang_infos = [];
            this.peipai_infos = [];
            this.babai_infos = [];
            this.xiuluo_hules_info = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordRoundInfo.prototype.name = "";
        RecordRoundInfo.prototype.chang = 0;
        RecordRoundInfo.prototype.ju = 0;
        RecordRoundInfo.prototype.ben = 0;
        RecordRoundInfo.prototype.scores = $util.emptyArray;
        RecordRoundInfo.prototype.liqi_infos = $util.emptyArray;
        RecordRoundInfo.prototype.gang_infos = $util.emptyArray;
        RecordRoundInfo.prototype.peipai_infos = $util.emptyArray;
        RecordRoundInfo.prototype.babai_infos = $util.emptyArray;
        RecordRoundInfo.prototype.hules_info = null;
        RecordRoundInfo.prototype.liuju_info = null;
        RecordRoundInfo.prototype.no_tile_info = null;
        RecordRoundInfo.prototype.xiuluo_hules_info = $util.emptyArray;

        RecordRoundInfo.create = function create(properties) {
            return new RecordRoundInfo(properties);
        };

        RecordRoundInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(10).string(message.name);
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(16).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(24).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(32).uint32(message.ben);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.uint32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqi_infos != null && message.liqi_infos.length)
                for (let i = 0; i < message.liqi_infos.length; ++i)
                    $root.lq.RecordLiqiInfo.encode(message.liqi_infos[i], writer.uint32(58).fork()).ldelim();
            if (message.gang_infos != null && message.gang_infos.length)
                for (let i = 0; i < message.gang_infos.length; ++i)
                    $root.lq.RecordGangInfo.encode(message.gang_infos[i], writer.uint32(66).fork()).ldelim();
            if (message.peipai_infos != null && message.peipai_infos.length)
                for (let i = 0; i < message.peipai_infos.length; ++i)
                    $root.lq.RecordPeiPaiInfo.encode(message.peipai_infos[i], writer.uint32(74).fork()).ldelim();
            if (message.babai_infos != null && message.babai_infos.length)
                for (let i = 0; i < message.babai_infos.length; ++i)
                    $root.lq.RecordBaBeiInfo.encode(message.babai_infos[i], writer.uint32(82).fork()).ldelim();
            if (message.hules_info != null && Object.hasOwnProperty.call(message, "hules_info"))
                $root.lq.RecordHulesInfo.encode(message.hules_info, writer.uint32(90).fork()).ldelim();
            if (message.liuju_info != null && Object.hasOwnProperty.call(message, "liuju_info"))
                $root.lq.RecordLiujuInfo.encode(message.liuju_info, writer.uint32(98).fork()).ldelim();
            if (message.no_tile_info != null && Object.hasOwnProperty.call(message, "no_tile_info"))
                $root.lq.RecordNoTileInfo.encode(message.no_tile_info, writer.uint32(106).fork()).ldelim();
            if (message.xiuluo_hules_info != null && message.xiuluo_hules_info.length)
                for (let i = 0; i < message.xiuluo_hules_info.length; ++i)
                    $root.lq.RecordHulesInfo.encode(message.xiuluo_hules_info[i], writer.uint32(114).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordRoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordRoundInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordRoundInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.uint32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.liqi_infos && message.liqi_infos.length))
                            message.liqi_infos = [];
                        message.liqi_infos.push($root.lq.RecordLiqiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gang_infos && message.gang_infos.length))
                            message.gang_infos = [];
                        message.gang_infos.push($root.lq.RecordGangInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.peipai_infos && message.peipai_infos.length))
                            message.peipai_infos = [];
                        message.peipai_infos.push($root.lq.RecordPeiPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.babai_infos && message.babai_infos.length))
                            message.babai_infos = [];
                        message.babai_infos.push($root.lq.RecordBaBeiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.hules_info = $root.lq.RecordHulesInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hules_info);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.liuju_info = $root.lq.RecordLiujuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.liuju_info);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.no_tile_info = $root.lq.RecordNoTileInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.no_tile_info);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.xiuluo_hules_info && message.xiuluo_hules_info.length))
                            message.xiuluo_hules_info = [];
                        message.xiuluo_hules_info.push($root.lq.RecordHulesInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordRoundInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordRoundInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqi_infos != null && message.hasOwnProperty("liqi_infos")) {
                if (!Array.isArray(message.liqi_infos))
                    return "liqi_infos: array expected";
                for (let i = 0; i < message.liqi_infos.length; ++i) {
                    let error = $root.lq.RecordLiqiInfo.verify(message.liqi_infos[i], _depth + 1);
                    if (error)
                        return "liqi_infos." + error;
                }
            }
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos")) {
                if (!Array.isArray(message.gang_infos))
                    return "gang_infos: array expected";
                for (let i = 0; i < message.gang_infos.length; ++i) {
                    let error = $root.lq.RecordGangInfo.verify(message.gang_infos[i], _depth + 1);
                    if (error)
                        return "gang_infos." + error;
                }
            }
            if (message.peipai_infos != null && message.hasOwnProperty("peipai_infos")) {
                if (!Array.isArray(message.peipai_infos))
                    return "peipai_infos: array expected";
                for (let i = 0; i < message.peipai_infos.length; ++i) {
                    let error = $root.lq.RecordPeiPaiInfo.verify(message.peipai_infos[i], _depth + 1);
                    if (error)
                        return "peipai_infos." + error;
                }
            }
            if (message.babai_infos != null && message.hasOwnProperty("babai_infos")) {
                if (!Array.isArray(message.babai_infos))
                    return "babai_infos: array expected";
                for (let i = 0; i < message.babai_infos.length; ++i) {
                    let error = $root.lq.RecordBaBeiInfo.verify(message.babai_infos[i], _depth + 1);
                    if (error)
                        return "babai_infos." + error;
                }
            }
            if (message.hules_info != null && message.hasOwnProperty("hules_info")) {
                let error = $root.lq.RecordHulesInfo.verify(message.hules_info, _depth + 1);
                if (error)
                    return "hules_info." + error;
            }
            if (message.liuju_info != null && message.hasOwnProperty("liuju_info")) {
                let error = $root.lq.RecordLiujuInfo.verify(message.liuju_info, _depth + 1);
                if (error)
                    return "liuju_info." + error;
            }
            if (message.no_tile_info != null && message.hasOwnProperty("no_tile_info")) {
                let error = $root.lq.RecordNoTileInfo.verify(message.no_tile_info, _depth + 1);
                if (error)
                    return "no_tile_info." + error;
            }
            if (message.xiuluo_hules_info != null && message.hasOwnProperty("xiuluo_hules_info")) {
                if (!Array.isArray(message.xiuluo_hules_info))
                    return "xiuluo_hules_info: array expected";
                for (let i = 0; i < message.xiuluo_hules_info.length; ++i) {
                    let error = $root.lq.RecordHulesInfo.verify(message.xiuluo_hules_info[i], _depth + 1);
                    if (error)
                        return "xiuluo_hules_info." + error;
                }
            }
            return null;
        };

        RecordRoundInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordRoundInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordRoundInfo();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordRoundInfo.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] >>> 0;
            }
            if (object.liqi_infos) {
                if (!Array.isArray(object.liqi_infos))
                    throw TypeError(".lq.RecordRoundInfo.liqi_infos: array expected");
                message.liqi_infos = Array(object.liqi_infos.length);
                for (let i = 0; i < object.liqi_infos.length; ++i) {
                    if (typeof object.liqi_infos[i] !== "object")
                        throw TypeError(".lq.RecordRoundInfo.liqi_infos: object expected");
                    message.liqi_infos[i] = $root.lq.RecordLiqiInfo.fromObject(object.liqi_infos[i], _depth + 1);
                }
            }
            if (object.gang_infos) {
                if (!Array.isArray(object.gang_infos))
                    throw TypeError(".lq.RecordRoundInfo.gang_infos: array expected");
                message.gang_infos = Array(object.gang_infos.length);
                for (let i = 0; i < object.gang_infos.length; ++i) {
                    if (typeof object.gang_infos[i] !== "object")
                        throw TypeError(".lq.RecordRoundInfo.gang_infos: object expected");
                    message.gang_infos[i] = $root.lq.RecordGangInfo.fromObject(object.gang_infos[i], _depth + 1);
                }
            }
            if (object.peipai_infos) {
                if (!Array.isArray(object.peipai_infos))
                    throw TypeError(".lq.RecordRoundInfo.peipai_infos: array expected");
                message.peipai_infos = Array(object.peipai_infos.length);
                for (let i = 0; i < object.peipai_infos.length; ++i) {
                    if (typeof object.peipai_infos[i] !== "object")
                        throw TypeError(".lq.RecordRoundInfo.peipai_infos: object expected");
                    message.peipai_infos[i] = $root.lq.RecordPeiPaiInfo.fromObject(object.peipai_infos[i], _depth + 1);
                }
            }
            if (object.babai_infos) {
                if (!Array.isArray(object.babai_infos))
                    throw TypeError(".lq.RecordRoundInfo.babai_infos: array expected");
                message.babai_infos = Array(object.babai_infos.length);
                for (let i = 0; i < object.babai_infos.length; ++i) {
                    if (typeof object.babai_infos[i] !== "object")
                        throw TypeError(".lq.RecordRoundInfo.babai_infos: object expected");
                    message.babai_infos[i] = $root.lq.RecordBaBeiInfo.fromObject(object.babai_infos[i], _depth + 1);
                }
            }
            if (object.hules_info != null) {
                if (typeof object.hules_info !== "object")
                    throw TypeError(".lq.RecordRoundInfo.hules_info: object expected");
                message.hules_info = $root.lq.RecordHulesInfo.fromObject(object.hules_info, _depth + 1);
            }
            if (object.liuju_info != null) {
                if (typeof object.liuju_info !== "object")
                    throw TypeError(".lq.RecordRoundInfo.liuju_info: object expected");
                message.liuju_info = $root.lq.RecordLiujuInfo.fromObject(object.liuju_info, _depth + 1);
            }
            if (object.no_tile_info != null) {
                if (typeof object.no_tile_info !== "object")
                    throw TypeError(".lq.RecordRoundInfo.no_tile_info: object expected");
                message.no_tile_info = $root.lq.RecordNoTileInfo.fromObject(object.no_tile_info, _depth + 1);
            }
            if (object.xiuluo_hules_info) {
                if (!Array.isArray(object.xiuluo_hules_info))
                    throw TypeError(".lq.RecordRoundInfo.xiuluo_hules_info: array expected");
                message.xiuluo_hules_info = Array(object.xiuluo_hules_info.length);
                for (let i = 0; i < object.xiuluo_hules_info.length; ++i) {
                    if (typeof object.xiuluo_hules_info[i] !== "object")
                        throw TypeError(".lq.RecordRoundInfo.xiuluo_hules_info: object expected");
                    message.xiuluo_hules_info[i] = $root.lq.RecordHulesInfo.fromObject(object.xiuluo_hules_info[i], _depth + 1);
                }
            }
            return message;
        };

        RecordRoundInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.liqi_infos = [];
                object.gang_infos = [];
                object.peipai_infos = [];
                object.babai_infos = [];
                object.xiuluo_hules_info = [];
            }
            if (options.defaults) {
                object.name = "";
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
                object.hules_info = null;
                object.liuju_info = null;
                object.no_tile_info = null;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqi_infos && message.liqi_infos.length) {
                object.liqi_infos = Array(message.liqi_infos.length);
                for (let j = 0; j < message.liqi_infos.length; ++j)
                    object.liqi_infos[j] = $root.lq.RecordLiqiInfo.toObject(message.liqi_infos[j], options);
            }
            if (message.gang_infos && message.gang_infos.length) {
                object.gang_infos = Array(message.gang_infos.length);
                for (let j = 0; j < message.gang_infos.length; ++j)
                    object.gang_infos[j] = $root.lq.RecordGangInfo.toObject(message.gang_infos[j], options);
            }
            if (message.peipai_infos && message.peipai_infos.length) {
                object.peipai_infos = Array(message.peipai_infos.length);
                for (let j = 0; j < message.peipai_infos.length; ++j)
                    object.peipai_infos[j] = $root.lq.RecordPeiPaiInfo.toObject(message.peipai_infos[j], options);
            }
            if (message.babai_infos && message.babai_infos.length) {
                object.babai_infos = Array(message.babai_infos.length);
                for (let j = 0; j < message.babai_infos.length; ++j)
                    object.babai_infos[j] = $root.lq.RecordBaBeiInfo.toObject(message.babai_infos[j], options);
            }
            if (message.hules_info != null && message.hasOwnProperty("hules_info"))
                object.hules_info = $root.lq.RecordHulesInfo.toObject(message.hules_info, options);
            if (message.liuju_info != null && message.hasOwnProperty("liuju_info"))
                object.liuju_info = $root.lq.RecordLiujuInfo.toObject(message.liuju_info, options);
            if (message.no_tile_info != null && message.hasOwnProperty("no_tile_info"))
                object.no_tile_info = $root.lq.RecordNoTileInfo.toObject(message.no_tile_info, options);
            if (message.xiuluo_hules_info && message.xiuluo_hules_info.length) {
                object.xiuluo_hules_info = Array(message.xiuluo_hules_info.length);
                for (let j = 0; j < message.xiuluo_hules_info.length; ++j)
                    object.xiuluo_hules_info[j] = $root.lq.RecordHulesInfo.toObject(message.xiuluo_hules_info[j], options);
            }
            return object;
        };

        RecordRoundInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordRoundInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordRoundInfo";
        };

        return RecordRoundInfo;
    })();

    lq.RecordAnalysisedData = (function() {

        function RecordAnalysisedData(properties) {
            this.round_infos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordAnalysisedData.prototype.round_infos = $util.emptyArray;

        RecordAnalysisedData.create = function create(properties) {
            return new RecordAnalysisedData(properties);
        };

        RecordAnalysisedData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round_infos != null && message.round_infos.length)
                for (let i = 0; i < message.round_infos.length; ++i)
                    $root.lq.RecordRoundInfo.encode(message.round_infos[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordAnalysisedData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordAnalysisedData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordAnalysisedData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.round_infos && message.round_infos.length))
                            message.round_infos = [];
                        message.round_infos.push($root.lq.RecordRoundInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordAnalysisedData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordAnalysisedData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round_infos != null && message.hasOwnProperty("round_infos")) {
                if (!Array.isArray(message.round_infos))
                    return "round_infos: array expected";
                for (let i = 0; i < message.round_infos.length; ++i) {
                    let error = $root.lq.RecordRoundInfo.verify(message.round_infos[i], _depth + 1);
                    if (error)
                        return "round_infos." + error;
                }
            }
            return null;
        };

        RecordAnalysisedData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordAnalysisedData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordAnalysisedData();
            if (object.round_infos) {
                if (!Array.isArray(object.round_infos))
                    throw TypeError(".lq.RecordAnalysisedData.round_infos: array expected");
                message.round_infos = Array(object.round_infos.length);
                for (let i = 0; i < object.round_infos.length; ++i) {
                    if (typeof object.round_infos[i] !== "object")
                        throw TypeError(".lq.RecordAnalysisedData.round_infos: object expected");
                    message.round_infos[i] = $root.lq.RecordRoundInfo.fromObject(object.round_infos[i], _depth + 1);
                }
            }
            return message;
        };

        RecordAnalysisedData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.round_infos = [];
            if (message.round_infos && message.round_infos.length) {
                object.round_infos = Array(message.round_infos.length);
                for (let j = 0; j < message.round_infos.length; ++j)
                    object.round_infos[j] = $root.lq.RecordRoundInfo.toObject(message.round_infos[j], options);
            }
            return object;
        };

        RecordAnalysisedData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordAnalysisedData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordAnalysisedData";
        };

        return RecordAnalysisedData;
    })();

    lq.VoteData = (function() {

        function VoteData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        VoteData.prototype.activity_id = 0;
        VoteData.prototype.vote = 0;
        VoteData.prototype.count = 0;

        VoteData.create = function create(properties) {
            return new VoteData(properties);
        };

        VoteData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                writer.uint32(16).uint32(message.vote);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(24).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VoteData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        VoteData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.VoteData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vote = value;
                        else
                            delete message.vote;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        VoteData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        VoteData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.vote != null && message.hasOwnProperty("vote"))
                if (!$util.isInteger(message.vote))
                    return "vote: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        VoteData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.VoteData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.VoteData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.vote != null)
                if (Number(object.vote) !== 0)
                    message.vote = object.vote >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        VoteData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.vote = 0;
                object.count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.vote != null && message.hasOwnProperty("vote"))
                object.vote = message.vote;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        VoteData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VoteData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.VoteData";
        };

        return VoteData;
    })();

    lq.ActivityBuffData = (function() {

        function ActivityBuffData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityBuffData.prototype.buff_id = 0;
        ActivityBuffData.prototype.level = 0;
        ActivityBuffData.prototype.count = 0;
        ActivityBuffData.prototype.update_time = 0;

        ActivityBuffData.create = function create(properties) {
            return new ActivityBuffData(properties);
        };

        ActivityBuffData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.buff_id != null && Object.hasOwnProperty.call(message, "buff_id"))
                writer.uint32(8).uint32(message.buff_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(40).uint32(message.level);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(48).uint32(message.count);
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(56).uint32(message.update_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityBuffData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityBuffData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityBuffData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.buff_id = value;
                        else
                            delete message.buff_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityBuffData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityBuffData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                if (!$util.isInteger(message.buff_id))
                    return "buff_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            return null;
        };

        ActivityBuffData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityBuffData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityBuffData();
            if (object.buff_id != null)
                if (Number(object.buff_id) !== 0)
                    message.buff_id = object.buff_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            return message;
        };

        ActivityBuffData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.buff_id = 0;
                object.level = 0;
                object.count = 0;
                object.update_time = 0;
            }
            if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                object.buff_id = message.buff_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            return object;
        };

        ActivityBuffData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityBuffData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityBuffData";
        };

        return ActivityBuffData;
    })();

    lq.AccountResourceSnapshot = (function() {

        function AccountResourceSnapshot(properties) {
            this.bag_item = [];
            this.currency = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountResourceSnapshot.prototype.bag_item = $util.emptyArray;
        AccountResourceSnapshot.prototype.currency = $util.emptyArray;
        AccountResourceSnapshot.prototype.title = null;
        AccountResourceSnapshot.prototype.used_title = null;
        AccountResourceSnapshot.prototype.currency_convert = 0;

        AccountResourceSnapshot.create = function create(properties) {
            return new AccountResourceSnapshot(properties);
        };

        AccountResourceSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.bag_item != null && message.bag_item.length)
                for (let i = 0; i < message.bag_item.length; ++i)
                    $root.lq.AccountResourceSnapshot.BagItemSnapshot.encode(message.bag_item[i], writer.uint32(10).fork()).ldelim();
            if (message.currency != null && message.currency.length)
                for (let i = 0; i < message.currency.length; ++i)
                    $root.lq.AccountResourceSnapshot.CurrencySnapshot.encode(message.currency[i], writer.uint32(18).fork()).ldelim();
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                $root.lq.AccountResourceSnapshot.TitleSnapshot.encode(message.title, writer.uint32(26).fork()).ldelim();
            if (message.used_title != null && Object.hasOwnProperty.call(message, "used_title"))
                $root.lq.AccountResourceSnapshot.UsedTitleSnapshot.encode(message.used_title, writer.uint32(34).fork()).ldelim();
            if (message.currency_convert != null && Object.hasOwnProperty.call(message, "currency_convert"))
                writer.uint32(40).uint32(message.currency_convert);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountResourceSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountResourceSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountResourceSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bag_item && message.bag_item.length))
                            message.bag_item = [];
                        message.bag_item.push($root.lq.AccountResourceSnapshot.BagItemSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.currency && message.currency.length))
                            message.currency = [];
                        message.currency.push($root.lq.AccountResourceSnapshot.CurrencySnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.title = $root.lq.AccountResourceSnapshot.TitleSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.title);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.used_title = $root.lq.AccountResourceSnapshot.UsedTitleSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.used_title);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.currency_convert = value;
                        else
                            delete message.currency_convert;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountResourceSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountResourceSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.bag_item != null && message.hasOwnProperty("bag_item")) {
                if (!Array.isArray(message.bag_item))
                    return "bag_item: array expected";
                for (let i = 0; i < message.bag_item.length; ++i) {
                    let error = $root.lq.AccountResourceSnapshot.BagItemSnapshot.verify(message.bag_item[i], _depth + 1);
                    if (error)
                        return "bag_item." + error;
                }
            }
            if (message.currency != null && message.hasOwnProperty("currency")) {
                if (!Array.isArray(message.currency))
                    return "currency: array expected";
                for (let i = 0; i < message.currency.length; ++i) {
                    let error = $root.lq.AccountResourceSnapshot.CurrencySnapshot.verify(message.currency[i], _depth + 1);
                    if (error)
                        return "currency." + error;
                }
            }
            if (message.title != null && message.hasOwnProperty("title")) {
                let error = $root.lq.AccountResourceSnapshot.TitleSnapshot.verify(message.title, _depth + 1);
                if (error)
                    return "title." + error;
            }
            if (message.used_title != null && message.hasOwnProperty("used_title")) {
                let error = $root.lq.AccountResourceSnapshot.UsedTitleSnapshot.verify(message.used_title, _depth + 1);
                if (error)
                    return "used_title." + error;
            }
            if (message.currency_convert != null && message.hasOwnProperty("currency_convert"))
                if (!$util.isInteger(message.currency_convert))
                    return "currency_convert: integer expected";
            return null;
        };

        AccountResourceSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountResourceSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountResourceSnapshot();
            if (object.bag_item) {
                if (!Array.isArray(object.bag_item))
                    throw TypeError(".lq.AccountResourceSnapshot.bag_item: array expected");
                message.bag_item = Array(object.bag_item.length);
                for (let i = 0; i < object.bag_item.length; ++i) {
                    if (typeof object.bag_item[i] !== "object")
                        throw TypeError(".lq.AccountResourceSnapshot.bag_item: object expected");
                    message.bag_item[i] = $root.lq.AccountResourceSnapshot.BagItemSnapshot.fromObject(object.bag_item[i], _depth + 1);
                }
            }
            if (object.currency) {
                if (!Array.isArray(object.currency))
                    throw TypeError(".lq.AccountResourceSnapshot.currency: array expected");
                message.currency = Array(object.currency.length);
                for (let i = 0; i < object.currency.length; ++i) {
                    if (typeof object.currency[i] !== "object")
                        throw TypeError(".lq.AccountResourceSnapshot.currency: object expected");
                    message.currency[i] = $root.lq.AccountResourceSnapshot.CurrencySnapshot.fromObject(object.currency[i], _depth + 1);
                }
            }
            if (object.title != null) {
                if (typeof object.title !== "object")
                    throw TypeError(".lq.AccountResourceSnapshot.title: object expected");
                message.title = $root.lq.AccountResourceSnapshot.TitleSnapshot.fromObject(object.title, _depth + 1);
            }
            if (object.used_title != null) {
                if (typeof object.used_title !== "object")
                    throw TypeError(".lq.AccountResourceSnapshot.used_title: object expected");
                message.used_title = $root.lq.AccountResourceSnapshot.UsedTitleSnapshot.fromObject(object.used_title, _depth + 1);
            }
            if (object.currency_convert != null)
                if (Number(object.currency_convert) !== 0)
                    message.currency_convert = object.currency_convert >>> 0;
            return message;
        };

        AccountResourceSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.bag_item = [];
                object.currency = [];
            }
            if (options.defaults) {
                object.title = null;
                object.used_title = null;
                object.currency_convert = 0;
            }
            if (message.bag_item && message.bag_item.length) {
                object.bag_item = Array(message.bag_item.length);
                for (let j = 0; j < message.bag_item.length; ++j)
                    object.bag_item[j] = $root.lq.AccountResourceSnapshot.BagItemSnapshot.toObject(message.bag_item[j], options);
            }
            if (message.currency && message.currency.length) {
                object.currency = Array(message.currency.length);
                for (let j = 0; j < message.currency.length; ++j)
                    object.currency[j] = $root.lq.AccountResourceSnapshot.CurrencySnapshot.toObject(message.currency[j], options);
            }
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = $root.lq.AccountResourceSnapshot.TitleSnapshot.toObject(message.title, options);
            if (message.used_title != null && message.hasOwnProperty("used_title"))
                object.used_title = $root.lq.AccountResourceSnapshot.UsedTitleSnapshot.toObject(message.used_title, options);
            if (message.currency_convert != null && message.hasOwnProperty("currency_convert"))
                object.currency_convert = message.currency_convert;
            return object;
        };

        AccountResourceSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountResourceSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountResourceSnapshot";
        };

        AccountResourceSnapshot.BagItemSnapshot = (function() {

            function BagItemSnapshot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BagItemSnapshot.prototype.resource_id = 0;
            BagItemSnapshot.prototype.resource_count = 0;
            BagItemSnapshot.prototype.resource_version = 0;

            BagItemSnapshot.create = function create(properties) {
                return new BagItemSnapshot(properties);
            };

            BagItemSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                    writer.uint32(8).uint32(message.resource_id);
                if (message.resource_count != null && Object.hasOwnProperty.call(message, "resource_count"))
                    writer.uint32(16).uint32(message.resource_count);
                if (message.resource_version != null && Object.hasOwnProperty.call(message, "resource_version"))
                    writer.uint32(24).uint32(message.resource_version);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BagItemSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BagItemSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountResourceSnapshot.BagItemSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_id = value;
                            else
                                delete message.resource_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_count = value;
                            else
                                delete message.resource_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_version = value;
                            else
                                delete message.resource_version;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BagItemSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BagItemSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    if (!$util.isInteger(message.resource_id))
                        return "resource_id: integer expected";
                if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                    if (!$util.isInteger(message.resource_count))
                        return "resource_count: integer expected";
                if (message.resource_version != null && message.hasOwnProperty("resource_version"))
                    if (!$util.isInteger(message.resource_version))
                        return "resource_version: integer expected";
                return null;
            };

            BagItemSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountResourceSnapshot.BagItemSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountResourceSnapshot.BagItemSnapshot();
                if (object.resource_id != null)
                    if (Number(object.resource_id) !== 0)
                        message.resource_id = object.resource_id >>> 0;
                if (object.resource_count != null)
                    if (Number(object.resource_count) !== 0)
                        message.resource_count = object.resource_count >>> 0;
                if (object.resource_version != null)
                    if (Number(object.resource_version) !== 0)
                        message.resource_version = object.resource_version >>> 0;
                return message;
            };

            BagItemSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.resource_id = 0;
                    object.resource_count = 0;
                    object.resource_version = 0;
                }
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    object.resource_id = message.resource_id;
                if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                    object.resource_count = message.resource_count;
                if (message.resource_version != null && message.hasOwnProperty("resource_version"))
                    object.resource_version = message.resource_version;
                return object;
            };

            BagItemSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BagItemSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountResourceSnapshot.BagItemSnapshot";
            };

            return BagItemSnapshot;
        })();

        AccountResourceSnapshot.CurrencySnapshot = (function() {

            function CurrencySnapshot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CurrencySnapshot.prototype.currency_id = 0;
            CurrencySnapshot.prototype.currency_count = 0;

            CurrencySnapshot.create = function create(properties) {
                return new CurrencySnapshot(properties);
            };

            CurrencySnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.currency_id != null && Object.hasOwnProperty.call(message, "currency_id"))
                    writer.uint32(8).uint32(message.currency_id);
                if (message.currency_count != null && Object.hasOwnProperty.call(message, "currency_count"))
                    writer.uint32(16).uint32(message.currency_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CurrencySnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CurrencySnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountResourceSnapshot.CurrencySnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.currency_id = value;
                            else
                                delete message.currency_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.currency_count = value;
                            else
                                delete message.currency_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CurrencySnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CurrencySnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.currency_id != null && message.hasOwnProperty("currency_id"))
                    if (!$util.isInteger(message.currency_id))
                        return "currency_id: integer expected";
                if (message.currency_count != null && message.hasOwnProperty("currency_count"))
                    if (!$util.isInteger(message.currency_count))
                        return "currency_count: integer expected";
                return null;
            };

            CurrencySnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountResourceSnapshot.CurrencySnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountResourceSnapshot.CurrencySnapshot();
                if (object.currency_id != null)
                    if (Number(object.currency_id) !== 0)
                        message.currency_id = object.currency_id >>> 0;
                if (object.currency_count != null)
                    if (Number(object.currency_count) !== 0)
                        message.currency_count = object.currency_count >>> 0;
                return message;
            };

            CurrencySnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.currency_id = 0;
                    object.currency_count = 0;
                }
                if (message.currency_id != null && message.hasOwnProperty("currency_id"))
                    object.currency_id = message.currency_id;
                if (message.currency_count != null && message.hasOwnProperty("currency_count"))
                    object.currency_count = message.currency_count;
                return object;
            };

            CurrencySnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CurrencySnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountResourceSnapshot.CurrencySnapshot";
            };

            return CurrencySnapshot;
        })();

        AccountResourceSnapshot.TitleSnapshot = (function() {

            function TitleSnapshot(properties) {
                this.title_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TitleSnapshot.prototype.title_list = $util.emptyArray;

            TitleSnapshot.create = function create(properties) {
                return new TitleSnapshot(properties);
            };

            TitleSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.title_list != null && message.title_list.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.title_list.length; ++i)
                        writer.uint32(message.title_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TitleSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TitleSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountResourceSnapshot.TitleSnapshot();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.title_list && message.title_list.length))
                                    message.title_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.title_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.title_list && message.title_list.length))
                                message.title_list = [];
                            message.title_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TitleSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TitleSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.title_list != null && message.hasOwnProperty("title_list")) {
                    if (!Array.isArray(message.title_list))
                        return "title_list: array expected";
                    for (let i = 0; i < message.title_list.length; ++i)
                        if (!$util.isInteger(message.title_list[i]))
                            return "title_list: integer[] expected";
                }
                return null;
            };

            TitleSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountResourceSnapshot.TitleSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountResourceSnapshot.TitleSnapshot();
                if (object.title_list) {
                    if (!Array.isArray(object.title_list))
                        throw TypeError(".lq.AccountResourceSnapshot.TitleSnapshot.title_list: array expected");
                    message.title_list = Array(object.title_list.length);
                    for (let i = 0; i < object.title_list.length; ++i)
                        message.title_list[i] = object.title_list[i] >>> 0;
                }
                return message;
            };

            TitleSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.title_list = [];
                if (message.title_list && message.title_list.length) {
                    object.title_list = Array(message.title_list.length);
                    for (let j = 0; j < message.title_list.length; ++j)
                        object.title_list[j] = message.title_list[j];
                }
                return object;
            };

            TitleSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TitleSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountResourceSnapshot.TitleSnapshot";
            };

            return TitleSnapshot;
        })();

        AccountResourceSnapshot.UsedTitleSnapshot = (function() {

            function UsedTitleSnapshot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            UsedTitleSnapshot.prototype.title_id = 0;

            UsedTitleSnapshot.create = function create(properties) {
                return new UsedTitleSnapshot(properties);
            };

            UsedTitleSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                    writer.uint32(8).uint32(message.title_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            UsedTitleSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            UsedTitleSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountResourceSnapshot.UsedTitleSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.title_id = value;
                            else
                                delete message.title_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            UsedTitleSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            UsedTitleSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.title_id != null && message.hasOwnProperty("title_id"))
                    if (!$util.isInteger(message.title_id))
                        return "title_id: integer expected";
                return null;
            };

            UsedTitleSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountResourceSnapshot.UsedTitleSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountResourceSnapshot.UsedTitleSnapshot();
                if (object.title_id != null)
                    if (Number(object.title_id) !== 0)
                        message.title_id = object.title_id >>> 0;
                return message;
            };

            UsedTitleSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.title_id = 0;
                if (message.title_id != null && message.hasOwnProperty("title_id"))
                    object.title_id = message.title_id;
                return object;
            };

            UsedTitleSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            UsedTitleSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountResourceSnapshot.UsedTitleSnapshot";
            };

            return UsedTitleSnapshot;
        })();

        return AccountResourceSnapshot;
    })();

    lq.AccountCharacterSnapshot = (function() {

        function AccountCharacterSnapshot(properties) {
            this.created_characters = [];
            this.removed_characters = [];
            this.modified_characters = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountCharacterSnapshot.prototype.created_characters = $util.emptyArray;
        AccountCharacterSnapshot.prototype.removed_characters = $util.emptyArray;
        AccountCharacterSnapshot.prototype.modified_characters = $util.emptyArray;
        AccountCharacterSnapshot.prototype.main_character = null;
        AccountCharacterSnapshot.prototype.skins = null;
        AccountCharacterSnapshot.prototype.hidden_characters = null;

        AccountCharacterSnapshot.create = function create(properties) {
            return new AccountCharacterSnapshot(properties);
        };

        AccountCharacterSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.created_characters != null && message.created_characters.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.created_characters.length; ++i)
                    writer.uint32(message.created_characters[i]);
                writer.ldelim();
            }
            if (message.removed_characters != null && message.removed_characters.length)
                for (let i = 0; i < message.removed_characters.length; ++i)
                    $root.lq.Character.encode(message.removed_characters[i], writer.uint32(18).fork()).ldelim();
            if (message.modified_characters != null && message.modified_characters.length)
                for (let i = 0; i < message.modified_characters.length; ++i)
                    $root.lq.Character.encode(message.modified_characters[i], writer.uint32(26).fork()).ldelim();
            if (message.main_character != null && Object.hasOwnProperty.call(message, "main_character"))
                $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot.encode(message.main_character, writer.uint32(34).fork()).ldelim();
            if (message.skins != null && Object.hasOwnProperty.call(message, "skins"))
                $root.lq.AccountCharacterSnapshot.SkinsSnapshot.encode(message.skins, writer.uint32(42).fork()).ldelim();
            if (message.hidden_characters != null && Object.hasOwnProperty.call(message, "hidden_characters"))
                $root.lq.AccountCharacterSnapshot.HiddenCharacter.encode(message.hidden_characters, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountCharacterSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountCharacterSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountCharacterSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.created_characters && message.created_characters.length))
                                message.created_characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.created_characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.created_characters && message.created_characters.length))
                            message.created_characters = [];
                        message.created_characters.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.removed_characters && message.removed_characters.length))
                            message.removed_characters = [];
                        message.removed_characters.push($root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modified_characters && message.modified_characters.length))
                            message.modified_characters = [];
                        message.modified_characters.push($root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.main_character = $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.main_character);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.skins = $root.lq.AccountCharacterSnapshot.SkinsSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.skins);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.hidden_characters = $root.lq.AccountCharacterSnapshot.HiddenCharacter.decode(reader, reader.uint32(), undefined, _depth + 1, message.hidden_characters);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountCharacterSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountCharacterSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.created_characters != null && message.hasOwnProperty("created_characters")) {
                if (!Array.isArray(message.created_characters))
                    return "created_characters: array expected";
                for (let i = 0; i < message.created_characters.length; ++i)
                    if (!$util.isInteger(message.created_characters[i]))
                        return "created_characters: integer[] expected";
            }
            if (message.removed_characters != null && message.hasOwnProperty("removed_characters")) {
                if (!Array.isArray(message.removed_characters))
                    return "removed_characters: array expected";
                for (let i = 0; i < message.removed_characters.length; ++i) {
                    let error = $root.lq.Character.verify(message.removed_characters[i], _depth + 1);
                    if (error)
                        return "removed_characters." + error;
                }
            }
            if (message.modified_characters != null && message.hasOwnProperty("modified_characters")) {
                if (!Array.isArray(message.modified_characters))
                    return "modified_characters: array expected";
                for (let i = 0; i < message.modified_characters.length; ++i) {
                    let error = $root.lq.Character.verify(message.modified_characters[i], _depth + 1);
                    if (error)
                        return "modified_characters." + error;
                }
            }
            if (message.main_character != null && message.hasOwnProperty("main_character")) {
                let error = $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot.verify(message.main_character, _depth + 1);
                if (error)
                    return "main_character." + error;
            }
            if (message.skins != null && message.hasOwnProperty("skins")) {
                let error = $root.lq.AccountCharacterSnapshot.SkinsSnapshot.verify(message.skins, _depth + 1);
                if (error)
                    return "skins." + error;
            }
            if (message.hidden_characters != null && message.hasOwnProperty("hidden_characters")) {
                let error = $root.lq.AccountCharacterSnapshot.HiddenCharacter.verify(message.hidden_characters, _depth + 1);
                if (error)
                    return "hidden_characters." + error;
            }
            return null;
        };

        AccountCharacterSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountCharacterSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountCharacterSnapshot();
            if (object.created_characters) {
                if (!Array.isArray(object.created_characters))
                    throw TypeError(".lq.AccountCharacterSnapshot.created_characters: array expected");
                message.created_characters = Array(object.created_characters.length);
                for (let i = 0; i < object.created_characters.length; ++i)
                    message.created_characters[i] = object.created_characters[i] >>> 0;
            }
            if (object.removed_characters) {
                if (!Array.isArray(object.removed_characters))
                    throw TypeError(".lq.AccountCharacterSnapshot.removed_characters: array expected");
                message.removed_characters = Array(object.removed_characters.length);
                for (let i = 0; i < object.removed_characters.length; ++i) {
                    if (typeof object.removed_characters[i] !== "object")
                        throw TypeError(".lq.AccountCharacterSnapshot.removed_characters: object expected");
                    message.removed_characters[i] = $root.lq.Character.fromObject(object.removed_characters[i], _depth + 1);
                }
            }
            if (object.modified_characters) {
                if (!Array.isArray(object.modified_characters))
                    throw TypeError(".lq.AccountCharacterSnapshot.modified_characters: array expected");
                message.modified_characters = Array(object.modified_characters.length);
                for (let i = 0; i < object.modified_characters.length; ++i) {
                    if (typeof object.modified_characters[i] !== "object")
                        throw TypeError(".lq.AccountCharacterSnapshot.modified_characters: object expected");
                    message.modified_characters[i] = $root.lq.Character.fromObject(object.modified_characters[i], _depth + 1);
                }
            }
            if (object.main_character != null) {
                if (typeof object.main_character !== "object")
                    throw TypeError(".lq.AccountCharacterSnapshot.main_character: object expected");
                message.main_character = $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot.fromObject(object.main_character, _depth + 1);
            }
            if (object.skins != null) {
                if (typeof object.skins !== "object")
                    throw TypeError(".lq.AccountCharacterSnapshot.skins: object expected");
                message.skins = $root.lq.AccountCharacterSnapshot.SkinsSnapshot.fromObject(object.skins, _depth + 1);
            }
            if (object.hidden_characters != null) {
                if (typeof object.hidden_characters !== "object")
                    throw TypeError(".lq.AccountCharacterSnapshot.hidden_characters: object expected");
                message.hidden_characters = $root.lq.AccountCharacterSnapshot.HiddenCharacter.fromObject(object.hidden_characters, _depth + 1);
            }
            return message;
        };

        AccountCharacterSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.created_characters = [];
                object.removed_characters = [];
                object.modified_characters = [];
            }
            if (options.defaults) {
                object.main_character = null;
                object.skins = null;
                object.hidden_characters = null;
            }
            if (message.created_characters && message.created_characters.length) {
                object.created_characters = Array(message.created_characters.length);
                for (let j = 0; j < message.created_characters.length; ++j)
                    object.created_characters[j] = message.created_characters[j];
            }
            if (message.removed_characters && message.removed_characters.length) {
                object.removed_characters = Array(message.removed_characters.length);
                for (let j = 0; j < message.removed_characters.length; ++j)
                    object.removed_characters[j] = $root.lq.Character.toObject(message.removed_characters[j], options);
            }
            if (message.modified_characters && message.modified_characters.length) {
                object.modified_characters = Array(message.modified_characters.length);
                for (let j = 0; j < message.modified_characters.length; ++j)
                    object.modified_characters[j] = $root.lq.Character.toObject(message.modified_characters[j], options);
            }
            if (message.main_character != null && message.hasOwnProperty("main_character"))
                object.main_character = $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot.toObject(message.main_character, options);
            if (message.skins != null && message.hasOwnProperty("skins"))
                object.skins = $root.lq.AccountCharacterSnapshot.SkinsSnapshot.toObject(message.skins, options);
            if (message.hidden_characters != null && message.hasOwnProperty("hidden_characters"))
                object.hidden_characters = $root.lq.AccountCharacterSnapshot.HiddenCharacter.toObject(message.hidden_characters, options);
            return object;
        };

        AccountCharacterSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountCharacterSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountCharacterSnapshot";
        };

        AccountCharacterSnapshot.MainCharacterSnapshot = (function() {

            function MainCharacterSnapshot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MainCharacterSnapshot.prototype.character_id = 0;

            MainCharacterSnapshot.create = function create(properties) {
                return new MainCharacterSnapshot(properties);
            };

            MainCharacterSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                    writer.uint32(8).uint32(message.character_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MainCharacterSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MainCharacterSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.character_id = value;
                            else
                                delete message.character_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MainCharacterSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MainCharacterSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    if (!$util.isInteger(message.character_id))
                        return "character_id: integer expected";
                return null;
            };

            MainCharacterSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountCharacterSnapshot.MainCharacterSnapshot();
                if (object.character_id != null)
                    if (Number(object.character_id) !== 0)
                        message.character_id = object.character_id >>> 0;
                return message;
            };

            MainCharacterSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.character_id = 0;
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    object.character_id = message.character_id;
                return object;
            };

            MainCharacterSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MainCharacterSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountCharacterSnapshot.MainCharacterSnapshot";
            };

            return MainCharacterSnapshot;
        })();

        AccountCharacterSnapshot.SkinsSnapshot = (function() {

            function SkinsSnapshot(properties) {
                this.skin_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SkinsSnapshot.prototype.skin_list = $util.emptyArray;

            SkinsSnapshot.create = function create(properties) {
                return new SkinsSnapshot(properties);
            };

            SkinsSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.skin_list != null && message.skin_list.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.skin_list.length; ++i)
                        writer.uint32(message.skin_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SkinsSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SkinsSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountCharacterSnapshot.SkinsSnapshot();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.skin_list && message.skin_list.length))
                                    message.skin_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.skin_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.skin_list && message.skin_list.length))
                                message.skin_list = [];
                            message.skin_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SkinsSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SkinsSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.skin_list != null && message.hasOwnProperty("skin_list")) {
                    if (!Array.isArray(message.skin_list))
                        return "skin_list: array expected";
                    for (let i = 0; i < message.skin_list.length; ++i)
                        if (!$util.isInteger(message.skin_list[i]))
                            return "skin_list: integer[] expected";
                }
                return null;
            };

            SkinsSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountCharacterSnapshot.SkinsSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountCharacterSnapshot.SkinsSnapshot();
                if (object.skin_list) {
                    if (!Array.isArray(object.skin_list))
                        throw TypeError(".lq.AccountCharacterSnapshot.SkinsSnapshot.skin_list: array expected");
                    message.skin_list = Array(object.skin_list.length);
                    for (let i = 0; i < object.skin_list.length; ++i)
                        message.skin_list[i] = object.skin_list[i] >>> 0;
                }
                return message;
            };

            SkinsSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.skin_list = [];
                if (message.skin_list && message.skin_list.length) {
                    object.skin_list = Array(message.skin_list.length);
                    for (let j = 0; j < message.skin_list.length; ++j)
                        object.skin_list[j] = message.skin_list[j];
                }
                return object;
            };

            SkinsSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SkinsSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountCharacterSnapshot.SkinsSnapshot";
            };

            return SkinsSnapshot;
        })();

        AccountCharacterSnapshot.HiddenCharacter = (function() {

            function HiddenCharacter(properties) {
                this.hidden_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            HiddenCharacter.prototype.hidden_list = $util.emptyArray;

            HiddenCharacter.create = function create(properties) {
                return new HiddenCharacter(properties);
            };

            HiddenCharacter.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.hidden_list != null && message.hidden_list.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.hidden_list.length; ++i)
                        writer.uint32(message.hidden_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            HiddenCharacter.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            HiddenCharacter.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountCharacterSnapshot.HiddenCharacter();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.hidden_list && message.hidden_list.length))
                                    message.hidden_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.hidden_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.hidden_list && message.hidden_list.length))
                                message.hidden_list = [];
                            message.hidden_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            HiddenCharacter.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            HiddenCharacter.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.hidden_list != null && message.hasOwnProperty("hidden_list")) {
                    if (!Array.isArray(message.hidden_list))
                        return "hidden_list: array expected";
                    for (let i = 0; i < message.hidden_list.length; ++i)
                        if (!$util.isInteger(message.hidden_list[i]))
                            return "hidden_list: integer[] expected";
                }
                return null;
            };

            HiddenCharacter.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountCharacterSnapshot.HiddenCharacter)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountCharacterSnapshot.HiddenCharacter();
                if (object.hidden_list) {
                    if (!Array.isArray(object.hidden_list))
                        throw TypeError(".lq.AccountCharacterSnapshot.HiddenCharacter.hidden_list: array expected");
                    message.hidden_list = Array(object.hidden_list.length);
                    for (let i = 0; i < object.hidden_list.length; ++i)
                        message.hidden_list[i] = object.hidden_list[i] >>> 0;
                }
                return message;
            };

            HiddenCharacter.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.hidden_list = [];
                if (message.hidden_list && message.hidden_list.length) {
                    object.hidden_list = Array(message.hidden_list.length);
                    for (let j = 0; j < message.hidden_list.length; ++j)
                        object.hidden_list[j] = message.hidden_list[j];
                }
                return object;
            };

            HiddenCharacter.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HiddenCharacter.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountCharacterSnapshot.HiddenCharacter";
            };

            return HiddenCharacter;
        })();

        return AccountCharacterSnapshot;
    })();

    lq.AccountMailRecord = (function() {

        function AccountMailRecord(properties) {
            this.created_mails = [];
            this.removed_mails = [];
            this.modified_mails = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountMailRecord.prototype.created_mails = $util.emptyArray;
        AccountMailRecord.prototype.removed_mails = $util.emptyArray;
        AccountMailRecord.prototype.modified_mails = $util.emptyArray;

        AccountMailRecord.create = function create(properties) {
            return new AccountMailRecord(properties);
        };

        AccountMailRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.created_mails != null && message.created_mails.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.created_mails.length; ++i)
                    writer.uint32(message.created_mails[i]);
                writer.ldelim();
            }
            if (message.removed_mails != null && message.removed_mails.length)
                for (let i = 0; i < message.removed_mails.length; ++i)
                    $root.lq.AccountMailRecord.MailSnapshot.encode(message.removed_mails[i], writer.uint32(18).fork()).ldelim();
            if (message.modified_mails != null && message.modified_mails.length)
                for (let i = 0; i < message.modified_mails.length; ++i)
                    $root.lq.AccountMailRecord.MailSnapshot.encode(message.modified_mails[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountMailRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountMailRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMailRecord();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.created_mails && message.created_mails.length))
                                message.created_mails = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.created_mails.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.created_mails && message.created_mails.length))
                            message.created_mails = [];
                        message.created_mails.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.removed_mails && message.removed_mails.length))
                            message.removed_mails = [];
                        message.removed_mails.push($root.lq.AccountMailRecord.MailSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modified_mails && message.modified_mails.length))
                            message.modified_mails = [];
                        message.modified_mails.push($root.lq.AccountMailRecord.MailSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountMailRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountMailRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.created_mails != null && message.hasOwnProperty("created_mails")) {
                if (!Array.isArray(message.created_mails))
                    return "created_mails: array expected";
                for (let i = 0; i < message.created_mails.length; ++i)
                    if (!$util.isInteger(message.created_mails[i]))
                        return "created_mails: integer[] expected";
            }
            if (message.removed_mails != null && message.hasOwnProperty("removed_mails")) {
                if (!Array.isArray(message.removed_mails))
                    return "removed_mails: array expected";
                for (let i = 0; i < message.removed_mails.length; ++i) {
                    let error = $root.lq.AccountMailRecord.MailSnapshot.verify(message.removed_mails[i], _depth + 1);
                    if (error)
                        return "removed_mails." + error;
                }
            }
            if (message.modified_mails != null && message.hasOwnProperty("modified_mails")) {
                if (!Array.isArray(message.modified_mails))
                    return "modified_mails: array expected";
                for (let i = 0; i < message.modified_mails.length; ++i) {
                    let error = $root.lq.AccountMailRecord.MailSnapshot.verify(message.modified_mails[i], _depth + 1);
                    if (error)
                        return "modified_mails." + error;
                }
            }
            return null;
        };

        AccountMailRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountMailRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountMailRecord();
            if (object.created_mails) {
                if (!Array.isArray(object.created_mails))
                    throw TypeError(".lq.AccountMailRecord.created_mails: array expected");
                message.created_mails = Array(object.created_mails.length);
                for (let i = 0; i < object.created_mails.length; ++i)
                    message.created_mails[i] = object.created_mails[i] >>> 0;
            }
            if (object.removed_mails) {
                if (!Array.isArray(object.removed_mails))
                    throw TypeError(".lq.AccountMailRecord.removed_mails: array expected");
                message.removed_mails = Array(object.removed_mails.length);
                for (let i = 0; i < object.removed_mails.length; ++i) {
                    if (typeof object.removed_mails[i] !== "object")
                        throw TypeError(".lq.AccountMailRecord.removed_mails: object expected");
                    message.removed_mails[i] = $root.lq.AccountMailRecord.MailSnapshot.fromObject(object.removed_mails[i], _depth + 1);
                }
            }
            if (object.modified_mails) {
                if (!Array.isArray(object.modified_mails))
                    throw TypeError(".lq.AccountMailRecord.modified_mails: array expected");
                message.modified_mails = Array(object.modified_mails.length);
                for (let i = 0; i < object.modified_mails.length; ++i) {
                    if (typeof object.modified_mails[i] !== "object")
                        throw TypeError(".lq.AccountMailRecord.modified_mails: object expected");
                    message.modified_mails[i] = $root.lq.AccountMailRecord.MailSnapshot.fromObject(object.modified_mails[i], _depth + 1);
                }
            }
            return message;
        };

        AccountMailRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.created_mails = [];
                object.removed_mails = [];
                object.modified_mails = [];
            }
            if (message.created_mails && message.created_mails.length) {
                object.created_mails = Array(message.created_mails.length);
                for (let j = 0; j < message.created_mails.length; ++j)
                    object.created_mails[j] = message.created_mails[j];
            }
            if (message.removed_mails && message.removed_mails.length) {
                object.removed_mails = Array(message.removed_mails.length);
                for (let j = 0; j < message.removed_mails.length; ++j)
                    object.removed_mails[j] = $root.lq.AccountMailRecord.MailSnapshot.toObject(message.removed_mails[j], options);
            }
            if (message.modified_mails && message.modified_mails.length) {
                object.modified_mails = Array(message.modified_mails.length);
                for (let j = 0; j < message.modified_mails.length; ++j)
                    object.modified_mails[j] = $root.lq.AccountMailRecord.MailSnapshot.toObject(message.modified_mails[j], options);
            }
            return object;
        };

        AccountMailRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountMailRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountMailRecord";
        };

        AccountMailRecord.MailSnapshot = (function() {

            function MailSnapshot(properties) {
                this.attachments = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MailSnapshot.prototype.mail_id = 0;
            MailSnapshot.prototype.reference_id = 0;
            MailSnapshot.prototype.create_time = 0;
            MailSnapshot.prototype.expire_time = 0;
            MailSnapshot.prototype.take_attachment = 0;
            MailSnapshot.prototype.attachments = $util.emptyArray;

            MailSnapshot.create = function create(properties) {
                return new MailSnapshot(properties);
            };

            MailSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.mail_id != null && Object.hasOwnProperty.call(message, "mail_id"))
                    writer.uint32(8).uint32(message.mail_id);
                if (message.reference_id != null && Object.hasOwnProperty.call(message, "reference_id"))
                    writer.uint32(16).uint32(message.reference_id);
                if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                    writer.uint32(24).uint32(message.create_time);
                if (message.expire_time != null && Object.hasOwnProperty.call(message, "expire_time"))
                    writer.uint32(32).uint32(message.expire_time);
                if (message.take_attachment != null && Object.hasOwnProperty.call(message, "take_attachment"))
                    writer.uint32(40).uint32(message.take_attachment);
                if (message.attachments != null && message.attachments.length)
                    for (let i = 0; i < message.attachments.length; ++i)
                        $root.lq.RewardSlot.encode(message.attachments[i], writer.uint32(50).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MailSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MailSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMailRecord.MailSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.mail_id = value;
                            else
                                delete message.mail_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.reference_id = value;
                            else
                                delete message.reference_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.create_time = value;
                            else
                                delete message.create_time;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.expire_time = value;
                            else
                                delete message.expire_time;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.take_attachment = value;
                            else
                                delete message.take_attachment;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if (!(message.attachments && message.attachments.length))
                                message.attachments = [];
                            message.attachments.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MailSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MailSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                    if (!$util.isInteger(message.mail_id))
                        return "mail_id: integer expected";
                if (message.reference_id != null && message.hasOwnProperty("reference_id"))
                    if (!$util.isInteger(message.reference_id))
                        return "reference_id: integer expected";
                if (message.create_time != null && message.hasOwnProperty("create_time"))
                    if (!$util.isInteger(message.create_time))
                        return "create_time: integer expected";
                if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                    if (!$util.isInteger(message.expire_time))
                        return "expire_time: integer expected";
                if (message.take_attachment != null && message.hasOwnProperty("take_attachment"))
                    if (!$util.isInteger(message.take_attachment))
                        return "take_attachment: integer expected";
                if (message.attachments != null && message.hasOwnProperty("attachments")) {
                    if (!Array.isArray(message.attachments))
                        return "attachments: array expected";
                    for (let i = 0; i < message.attachments.length; ++i) {
                        let error = $root.lq.RewardSlot.verify(message.attachments[i], _depth + 1);
                        if (error)
                            return "attachments." + error;
                    }
                }
                return null;
            };

            MailSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMailRecord.MailSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMailRecord.MailSnapshot();
                if (object.mail_id != null)
                    if (Number(object.mail_id) !== 0)
                        message.mail_id = object.mail_id >>> 0;
                if (object.reference_id != null)
                    if (Number(object.reference_id) !== 0)
                        message.reference_id = object.reference_id >>> 0;
                if (object.create_time != null)
                    if (Number(object.create_time) !== 0)
                        message.create_time = object.create_time >>> 0;
                if (object.expire_time != null)
                    if (Number(object.expire_time) !== 0)
                        message.expire_time = object.expire_time >>> 0;
                if (object.take_attachment != null)
                    if (Number(object.take_attachment) !== 0)
                        message.take_attachment = object.take_attachment >>> 0;
                if (object.attachments) {
                    if (!Array.isArray(object.attachments))
                        throw TypeError(".lq.AccountMailRecord.MailSnapshot.attachments: array expected");
                    message.attachments = Array(object.attachments.length);
                    for (let i = 0; i < object.attachments.length; ++i) {
                        if (typeof object.attachments[i] !== "object")
                            throw TypeError(".lq.AccountMailRecord.MailSnapshot.attachments: object expected");
                        message.attachments[i] = $root.lq.RewardSlot.fromObject(object.attachments[i], _depth + 1);
                    }
                }
                return message;
            };

            MailSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.attachments = [];
                if (options.defaults) {
                    object.mail_id = 0;
                    object.reference_id = 0;
                    object.create_time = 0;
                    object.expire_time = 0;
                    object.take_attachment = 0;
                }
                if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                    object.mail_id = message.mail_id;
                if (message.reference_id != null && message.hasOwnProperty("reference_id"))
                    object.reference_id = message.reference_id;
                if (message.create_time != null && message.hasOwnProperty("create_time"))
                    object.create_time = message.create_time;
                if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                    object.expire_time = message.expire_time;
                if (message.take_attachment != null && message.hasOwnProperty("take_attachment"))
                    object.take_attachment = message.take_attachment;
                if (message.attachments && message.attachments.length) {
                    object.attachments = Array(message.attachments.length);
                    for (let j = 0; j < message.attachments.length; ++j)
                        object.attachments[j] = $root.lq.RewardSlot.toObject(message.attachments[j], options);
                }
                return object;
            };

            MailSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MailSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMailRecord.MailSnapshot";
            };

            return MailSnapshot;
        })();

        return AccountMailRecord;
    })();

    lq.AccountAchievementSnapshot = (function() {

        function AccountAchievementSnapshot(properties) {
            this.achievements = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountAchievementSnapshot.prototype.achievements = $util.emptyArray;
        AccountAchievementSnapshot.prototype.rewarded_group = null;
        AccountAchievementSnapshot.prototype.version = null;

        AccountAchievementSnapshot.create = function create(properties) {
            return new AccountAchievementSnapshot(properties);
        };

        AccountAchievementSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.achievements != null && message.achievements.length)
                for (let i = 0; i < message.achievements.length; ++i)
                    $root.lq.AchievementProgress.encode(message.achievements[i], writer.uint32(10).fork()).ldelim();
            if (message.rewarded_group != null && Object.hasOwnProperty.call(message, "rewarded_group"))
                $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot.encode(message.rewarded_group, writer.uint32(18).fork()).ldelim();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                $root.lq.AccountAchievementSnapshot.AchievementVersion.encode(message.version, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountAchievementSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountAchievementSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountAchievementSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.achievements && message.achievements.length))
                            message.achievements = [];
                        message.achievements.push($root.lq.AchievementProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.rewarded_group = $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.rewarded_group);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.version = $root.lq.AccountAchievementSnapshot.AchievementVersion.decode(reader, reader.uint32(), undefined, _depth + 1, message.version);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountAchievementSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountAchievementSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.achievements != null && message.hasOwnProperty("achievements")) {
                if (!Array.isArray(message.achievements))
                    return "achievements: array expected";
                for (let i = 0; i < message.achievements.length; ++i) {
                    let error = $root.lq.AchievementProgress.verify(message.achievements[i], _depth + 1);
                    if (error)
                        return "achievements." + error;
                }
            }
            if (message.rewarded_group != null && message.hasOwnProperty("rewarded_group")) {
                let error = $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot.verify(message.rewarded_group, _depth + 1);
                if (error)
                    return "rewarded_group." + error;
            }
            if (message.version != null && message.hasOwnProperty("version")) {
                let error = $root.lq.AccountAchievementSnapshot.AchievementVersion.verify(message.version, _depth + 1);
                if (error)
                    return "version." + error;
            }
            return null;
        };

        AccountAchievementSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountAchievementSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountAchievementSnapshot();
            if (object.achievements) {
                if (!Array.isArray(object.achievements))
                    throw TypeError(".lq.AccountAchievementSnapshot.achievements: array expected");
                message.achievements = Array(object.achievements.length);
                for (let i = 0; i < object.achievements.length; ++i) {
                    if (typeof object.achievements[i] !== "object")
                        throw TypeError(".lq.AccountAchievementSnapshot.achievements: object expected");
                    message.achievements[i] = $root.lq.AchievementProgress.fromObject(object.achievements[i], _depth + 1);
                }
            }
            if (object.rewarded_group != null) {
                if (typeof object.rewarded_group !== "object")
                    throw TypeError(".lq.AccountAchievementSnapshot.rewarded_group: object expected");
                message.rewarded_group = $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot.fromObject(object.rewarded_group, _depth + 1);
            }
            if (object.version != null) {
                if (typeof object.version !== "object")
                    throw TypeError(".lq.AccountAchievementSnapshot.version: object expected");
                message.version = $root.lq.AccountAchievementSnapshot.AchievementVersion.fromObject(object.version, _depth + 1);
            }
            return message;
        };

        AccountAchievementSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.achievements = [];
            if (options.defaults) {
                object.rewarded_group = null;
                object.version = null;
            }
            if (message.achievements && message.achievements.length) {
                object.achievements = Array(message.achievements.length);
                for (let j = 0; j < message.achievements.length; ++j)
                    object.achievements[j] = $root.lq.AchievementProgress.toObject(message.achievements[j], options);
            }
            if (message.rewarded_group != null && message.hasOwnProperty("rewarded_group"))
                object.rewarded_group = $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot.toObject(message.rewarded_group, options);
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = $root.lq.AccountAchievementSnapshot.AchievementVersion.toObject(message.version, options);
            return object;
        };

        AccountAchievementSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountAchievementSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountAchievementSnapshot";
        };

        AccountAchievementSnapshot.RewardedGroupSnapshot = (function() {

            function RewardedGroupSnapshot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardedGroupSnapshot.prototype.rewarded_id = 0;

            RewardedGroupSnapshot.create = function create(properties) {
                return new RewardedGroupSnapshot(properties);
            };

            RewardedGroupSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rewarded_id != null && Object.hasOwnProperty.call(message, "rewarded_id"))
                    writer.uint32(8).uint32(message.rewarded_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardedGroupSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardedGroupSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rewarded_id = value;
                            else
                                delete message.rewarded_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardedGroupSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardedGroupSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rewarded_id != null && message.hasOwnProperty("rewarded_id"))
                    if (!$util.isInteger(message.rewarded_id))
                        return "rewarded_id: integer expected";
                return null;
            };

            RewardedGroupSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountAchievementSnapshot.RewardedGroupSnapshot();
                if (object.rewarded_id != null)
                    if (Number(object.rewarded_id) !== 0)
                        message.rewarded_id = object.rewarded_id >>> 0;
                return message;
            };

            RewardedGroupSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.rewarded_id = 0;
                if (message.rewarded_id != null && message.hasOwnProperty("rewarded_id"))
                    object.rewarded_id = message.rewarded_id;
                return object;
            };

            RewardedGroupSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardedGroupSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountAchievementSnapshot.RewardedGroupSnapshot";
            };

            return RewardedGroupSnapshot;
        })();

        AccountAchievementSnapshot.AchievementVersion = (function() {

            function AchievementVersion(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AchievementVersion.prototype.version = 0;

            AchievementVersion.create = function create(properties) {
                return new AchievementVersion(properties);
            };

            AchievementVersion.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(8).uint32(message.version);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AchievementVersion.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AchievementVersion.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountAchievementSnapshot.AchievementVersion(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AchievementVersion.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AchievementVersion.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.version != null && message.hasOwnProperty("version"))
                    if (!$util.isInteger(message.version))
                        return "version: integer expected";
                return null;
            };

            AchievementVersion.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountAchievementSnapshot.AchievementVersion)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountAchievementSnapshot.AchievementVersion();
                if (object.version != null)
                    if (Number(object.version) !== 0)
                        message.version = object.version >>> 0;
                return message;
            };

            AchievementVersion.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.version = 0;
                if (message.version != null && message.hasOwnProperty("version"))
                    object.version = message.version;
                return object;
            };

            AchievementVersion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AchievementVersion.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountAchievementSnapshot.AchievementVersion";
            };

            return AchievementVersion;
        })();

        return AccountAchievementSnapshot;
    })();

    lq.AccountMiscSnapshot = (function() {

        function AccountMiscSnapshot(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountMiscSnapshot.prototype.faith_data = null;
        AccountMiscSnapshot.prototype.vip_reward_gained = null;
        AccountMiscSnapshot.prototype.vip = null;
        AccountMiscSnapshot.prototype.shop_info = null;
        AccountMiscSnapshot.prototype.month_ticket = null;
        AccountMiscSnapshot.prototype.recharged = null;
        AccountMiscSnapshot.prototype.month_ticket_v2 = null;

        AccountMiscSnapshot.create = function create(properties) {
            return new AccountMiscSnapshot(properties);
        };

        AccountMiscSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.faith_data != null && Object.hasOwnProperty.call(message, "faith_data"))
                $root.lq.FaithData.encode(message.faith_data, writer.uint32(10).fork()).ldelim();
            if (message.vip_reward_gained != null && Object.hasOwnProperty.call(message, "vip_reward_gained"))
                $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.encode(message.vip_reward_gained, writer.uint32(18).fork()).ldelim();
            if (message.vip != null && Object.hasOwnProperty.call(message, "vip"))
                $root.lq.AccountMiscSnapshot.AccountVIP.encode(message.vip, writer.uint32(26).fork()).ldelim();
            if (message.shop_info != null && Object.hasOwnProperty.call(message, "shop_info"))
                $root.lq.ShopInfo.encode(message.shop_info, writer.uint32(34).fork()).ldelim();
            if (message.month_ticket != null && Object.hasOwnProperty.call(message, "month_ticket"))
                $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.encode(message.month_ticket, writer.uint32(42).fork()).ldelim();
            if (message.recharged != null && Object.hasOwnProperty.call(message, "recharged"))
                $root.lq.AccountMiscSnapshot.AccountRechargeInfo.encode(message.recharged, writer.uint32(50).fork()).ldelim();
            if (message.month_ticket_v2 != null && Object.hasOwnProperty.call(message, "month_ticket_v2"))
                $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.encode(message.month_ticket_v2, writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountMiscSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountMiscSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.faith_data = $root.lq.FaithData.decode(reader, reader.uint32(), undefined, _depth + 1, message.faith_data);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.vip_reward_gained = $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.vip_reward_gained);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.vip = $root.lq.AccountMiscSnapshot.AccountVIP.decode(reader, reader.uint32(), undefined, _depth + 1, message.vip);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.shop_info = $root.lq.ShopInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_info);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.month_ticket = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_ticket);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.recharged = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.recharged);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.month_ticket_v2 = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_ticket_v2);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountMiscSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountMiscSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.faith_data != null && message.hasOwnProperty("faith_data")) {
                let error = $root.lq.FaithData.verify(message.faith_data, _depth + 1);
                if (error)
                    return "faith_data." + error;
            }
            if (message.vip_reward_gained != null && message.hasOwnProperty("vip_reward_gained")) {
                let error = $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.verify(message.vip_reward_gained, _depth + 1);
                if (error)
                    return "vip_reward_gained." + error;
            }
            if (message.vip != null && message.hasOwnProperty("vip")) {
                let error = $root.lq.AccountMiscSnapshot.AccountVIP.verify(message.vip, _depth + 1);
                if (error)
                    return "vip." + error;
            }
            if (message.shop_info != null && message.hasOwnProperty("shop_info")) {
                let error = $root.lq.ShopInfo.verify(message.shop_info, _depth + 1);
                if (error)
                    return "shop_info." + error;
            }
            if (message.month_ticket != null && message.hasOwnProperty("month_ticket")) {
                let error = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.verify(message.month_ticket, _depth + 1);
                if (error)
                    return "month_ticket." + error;
            }
            if (message.recharged != null && message.hasOwnProperty("recharged")) {
                let error = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.verify(message.recharged, _depth + 1);
                if (error)
                    return "recharged." + error;
            }
            if (message.month_ticket_v2 != null && message.hasOwnProperty("month_ticket_v2")) {
                let error = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.verify(message.month_ticket_v2, _depth + 1);
                if (error)
                    return "month_ticket_v2." + error;
            }
            return null;
        };

        AccountMiscSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountMiscSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountMiscSnapshot();
            if (object.faith_data != null) {
                if (typeof object.faith_data !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.faith_data: object expected");
                message.faith_data = $root.lq.FaithData.fromObject(object.faith_data, _depth + 1);
            }
            if (object.vip_reward_gained != null) {
                if (typeof object.vip_reward_gained !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.vip_reward_gained: object expected");
                message.vip_reward_gained = $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.fromObject(object.vip_reward_gained, _depth + 1);
            }
            if (object.vip != null) {
                if (typeof object.vip !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.vip: object expected");
                message.vip = $root.lq.AccountMiscSnapshot.AccountVIP.fromObject(object.vip, _depth + 1);
            }
            if (object.shop_info != null) {
                if (typeof object.shop_info !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.shop_info: object expected");
                message.shop_info = $root.lq.ShopInfo.fromObject(object.shop_info, _depth + 1);
            }
            if (object.month_ticket != null) {
                if (typeof object.month_ticket !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.month_ticket: object expected");
                message.month_ticket = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.fromObject(object.month_ticket, _depth + 1);
            }
            if (object.recharged != null) {
                if (typeof object.recharged !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.recharged: object expected");
                message.recharged = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.fromObject(object.recharged, _depth + 1);
            }
            if (object.month_ticket_v2 != null) {
                if (typeof object.month_ticket_v2 !== "object")
                    throw TypeError(".lq.AccountMiscSnapshot.month_ticket_v2: object expected");
                message.month_ticket_v2 = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.fromObject(object.month_ticket_v2, _depth + 1);
            }
            return message;
        };

        AccountMiscSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.faith_data = null;
                object.vip_reward_gained = null;
                object.vip = null;
                object.shop_info = null;
                object.month_ticket = null;
                object.recharged = null;
                object.month_ticket_v2 = null;
            }
            if (message.faith_data != null && message.hasOwnProperty("faith_data"))
                object.faith_data = $root.lq.FaithData.toObject(message.faith_data, options);
            if (message.vip_reward_gained != null && message.hasOwnProperty("vip_reward_gained"))
                object.vip_reward_gained = $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.toObject(message.vip_reward_gained, options);
            if (message.vip != null && message.hasOwnProperty("vip"))
                object.vip = $root.lq.AccountMiscSnapshot.AccountVIP.toObject(message.vip, options);
            if (message.shop_info != null && message.hasOwnProperty("shop_info"))
                object.shop_info = $root.lq.ShopInfo.toObject(message.shop_info, options);
            if (message.month_ticket != null && message.hasOwnProperty("month_ticket"))
                object.month_ticket = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.toObject(message.month_ticket, options);
            if (message.recharged != null && message.hasOwnProperty("recharged"))
                object.recharged = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.toObject(message.recharged, options);
            if (message.month_ticket_v2 != null && message.hasOwnProperty("month_ticket_v2"))
                object.month_ticket_v2 = $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.toObject(message.month_ticket_v2, options);
            return object;
        };

        AccountMiscSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountMiscSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountMiscSnapshot";
        };

        AccountMiscSnapshot.AccountVIPRewardSnapshot = (function() {

            function AccountVIPRewardSnapshot(properties) {
                this.rewarded = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountVIPRewardSnapshot.prototype.rewarded = $util.emptyArray;

            AccountVIPRewardSnapshot.create = function create(properties) {
                return new AccountVIPRewardSnapshot(properties);
            };

            AccountVIPRewardSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rewarded != null && message.rewarded.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.rewarded.length; ++i)
                        writer.uint32(message.rewarded[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountVIPRewardSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountVIPRewardSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.rewarded && message.rewarded.length))
                                    message.rewarded = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.rewarded.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.rewarded && message.rewarded.length))
                                message.rewarded = [];
                            message.rewarded.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountVIPRewardSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountVIPRewardSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rewarded != null && message.hasOwnProperty("rewarded")) {
                    if (!Array.isArray(message.rewarded))
                        return "rewarded: array expected";
                    for (let i = 0; i < message.rewarded.length; ++i)
                        if (!$util.isInteger(message.rewarded[i]))
                            return "rewarded: integer[] expected";
                }
                return null;
            };

            AccountVIPRewardSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.AccountVIPRewardSnapshot();
                if (object.rewarded) {
                    if (!Array.isArray(object.rewarded))
                        throw TypeError(".lq.AccountMiscSnapshot.AccountVIPRewardSnapshot.rewarded: array expected");
                    message.rewarded = Array(object.rewarded.length);
                    for (let i = 0; i < object.rewarded.length; ++i)
                        message.rewarded[i] = object.rewarded[i] >>> 0;
                }
                return message;
            };

            AccountVIPRewardSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rewarded = [];
                if (message.rewarded && message.rewarded.length) {
                    object.rewarded = Array(message.rewarded.length);
                    for (let j = 0; j < message.rewarded.length; ++j)
                        object.rewarded[j] = message.rewarded[j];
                }
                return object;
            };

            AccountVIPRewardSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountVIPRewardSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.AccountVIPRewardSnapshot";
            };

            return AccountVIPRewardSnapshot;
        })();

        AccountMiscSnapshot.MonthTicketInfo = (function() {

            function MonthTicketInfo(properties) {
                this.history = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MonthTicketInfo.prototype.id = 0;
            MonthTicketInfo.prototype.end_time = 0;
            MonthTicketInfo.prototype.last_pay_time = 0;
            MonthTicketInfo.prototype.record_start_time = 0;
            MonthTicketInfo.prototype.history = $util.emptyArray;

            MonthTicketInfo.create = function create(properties) {
                return new MonthTicketInfo(properties);
            };

            MonthTicketInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(16).uint32(message.end_time);
                if (message.last_pay_time != null && Object.hasOwnProperty.call(message, "last_pay_time"))
                    writer.uint32(24).uint32(message.last_pay_time);
                if (message.record_start_time != null && Object.hasOwnProperty.call(message, "record_start_time"))
                    writer.uint32(32).uint32(message.record_start_time);
                if (message.history != null && message.history.length) {
                    writer.uint32(42).fork();
                    for (let i = 0; i < message.history.length; ++i)
                        writer.uint32(message.history[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MonthTicketInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MonthTicketInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.MonthTicketInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_pay_time = value;
                            else
                                delete message.last_pay_time;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.record_start_time = value;
                            else
                                delete message.record_start_time;
                            continue;
                        }
                    case 5: {
                            if (wireType === 2) {
                                if (!(message.history && message.history.length))
                                    message.history = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.history.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.history && message.history.length))
                                message.history = [];
                            message.history.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MonthTicketInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MonthTicketInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    if (!$util.isInteger(message.last_pay_time))
                        return "last_pay_time: integer expected";
                if (message.record_start_time != null && message.hasOwnProperty("record_start_time"))
                    if (!$util.isInteger(message.record_start_time))
                        return "record_start_time: integer expected";
                if (message.history != null && message.hasOwnProperty("history")) {
                    if (!Array.isArray(message.history))
                        return "history: array expected";
                    for (let i = 0; i < message.history.length; ++i)
                        if (!$util.isInteger(message.history[i]))
                            return "history: integer[] expected";
                }
                return null;
            };

            MonthTicketInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.MonthTicketInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.MonthTicketInfo();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.last_pay_time != null)
                    if (Number(object.last_pay_time) !== 0)
                        message.last_pay_time = object.last_pay_time >>> 0;
                if (object.record_start_time != null)
                    if (Number(object.record_start_time) !== 0)
                        message.record_start_time = object.record_start_time >>> 0;
                if (object.history) {
                    if (!Array.isArray(object.history))
                        throw TypeError(".lq.AccountMiscSnapshot.MonthTicketInfo.history: array expected");
                    message.history = Array(object.history.length);
                    for (let i = 0; i < object.history.length; ++i)
                        message.history[i] = object.history[i] >>> 0;
                }
                return message;
            };

            MonthTicketInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.history = [];
                if (options.defaults) {
                    object.id = 0;
                    object.end_time = 0;
                    object.last_pay_time = 0;
                    object.record_start_time = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    object.last_pay_time = message.last_pay_time;
                if (message.record_start_time != null && message.hasOwnProperty("record_start_time"))
                    object.record_start_time = message.record_start_time;
                if (message.history && message.history.length) {
                    object.history = Array(message.history.length);
                    for (let j = 0; j < message.history.length; ++j)
                        object.history[j] = message.history[j];
                }
                return object;
            };

            MonthTicketInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MonthTicketInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.MonthTicketInfo";
            };

            return MonthTicketInfo;
        })();

        AccountMiscSnapshot.AccountMonthTicketSnapshot = (function() {

            function AccountMonthTicketSnapshot(properties) {
                this.tickets = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountMonthTicketSnapshot.prototype.tickets = $util.emptyArray;

            AccountMonthTicketSnapshot.create = function create(properties) {
                return new AccountMonthTicketSnapshot(properties);
            };

            AccountMonthTicketSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.tickets != null && message.tickets.length)
                    for (let i = 0; i < message.tickets.length; ++i)
                        $root.lq.AccountMiscSnapshot.MonthTicketInfo.encode(message.tickets[i], writer.uint32(10).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountMonthTicketSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountMonthTicketSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tickets && message.tickets.length))
                                message.tickets = [];
                            message.tickets.push($root.lq.AccountMiscSnapshot.MonthTicketInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountMonthTicketSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountMonthTicketSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tickets != null && message.hasOwnProperty("tickets")) {
                    if (!Array.isArray(message.tickets))
                        return "tickets: array expected";
                    for (let i = 0; i < message.tickets.length; ++i) {
                        let error = $root.lq.AccountMiscSnapshot.MonthTicketInfo.verify(message.tickets[i], _depth + 1);
                        if (error)
                            return "tickets." + error;
                    }
                }
                return null;
            };

            AccountMonthTicketSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshot();
                if (object.tickets) {
                    if (!Array.isArray(object.tickets))
                        throw TypeError(".lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.tickets: array expected");
                    message.tickets = Array(object.tickets.length);
                    for (let i = 0; i < object.tickets.length; ++i) {
                        if (typeof object.tickets[i] !== "object")
                            throw TypeError(".lq.AccountMiscSnapshot.AccountMonthTicketSnapshot.tickets: object expected");
                        message.tickets[i] = $root.lq.AccountMiscSnapshot.MonthTicketInfo.fromObject(object.tickets[i], _depth + 1);
                    }
                }
                return message;
            };

            AccountMonthTicketSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tickets = [];
                if (message.tickets && message.tickets.length) {
                    object.tickets = Array(message.tickets.length);
                    for (let j = 0; j < message.tickets.length; ++j)
                        object.tickets[j] = $root.lq.AccountMiscSnapshot.MonthTicketInfo.toObject(message.tickets[j], options);
                }
                return object;
            };

            AccountMonthTicketSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountMonthTicketSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.AccountMonthTicketSnapshot";
            };

            return AccountMonthTicketSnapshot;
        })();

        AccountMiscSnapshot.AccountVIP = (function() {

            function AccountVIP(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountVIP.prototype.vip = 0;

            AccountVIP.create = function create(properties) {
                return new AccountVIP(properties);
            };

            AccountVIP.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.vip != null && Object.hasOwnProperty.call(message, "vip"))
                    writer.uint32(8).uint32(message.vip);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountVIP.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountVIP.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountVIP(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.vip = value;
                            else
                                delete message.vip;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountVIP.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountVIP.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.vip != null && message.hasOwnProperty("vip"))
                    if (!$util.isInteger(message.vip))
                        return "vip: integer expected";
                return null;
            };

            AccountVIP.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.AccountVIP)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.AccountVIP();
                if (object.vip != null)
                    if (Number(object.vip) !== 0)
                        message.vip = object.vip >>> 0;
                return message;
            };

            AccountVIP.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.vip = 0;
                if (message.vip != null && message.hasOwnProperty("vip"))
                    object.vip = message.vip;
                return object;
            };

            AccountVIP.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountVIP.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.AccountVIP";
            };

            return AccountVIP;
        })();

        AccountMiscSnapshot.AccountRechargeInfo = (function() {

            function AccountRechargeInfo(properties) {
                this.records = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountRechargeInfo.prototype.records = $util.emptyArray;
            AccountRechargeInfo.prototype.has_data = 0;

            AccountRechargeInfo.create = function create(properties) {
                return new AccountRechargeInfo(properties);
            };

            AccountRechargeInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.records != null && message.records.length)
                    for (let i = 0; i < message.records.length; ++i)
                        $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord.encode(message.records[i], writer.uint32(10).fork()).ldelim();
                if (message.has_data != null && Object.hasOwnProperty.call(message, "has_data"))
                    writer.uint32(16).uint32(message.has_data);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountRechargeInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountRechargeInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountRechargeInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.records && message.records.length))
                                message.records = [];
                            message.records.push($root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.has_data = value;
                            else
                                delete message.has_data;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountRechargeInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountRechargeInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.records != null && message.hasOwnProperty("records")) {
                    if (!Array.isArray(message.records))
                        return "records: array expected";
                    for (let i = 0; i < message.records.length; ++i) {
                        let error = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord.verify(message.records[i], _depth + 1);
                        if (error)
                            return "records." + error;
                    }
                }
                if (message.has_data != null && message.hasOwnProperty("has_data"))
                    if (!$util.isInteger(message.has_data))
                        return "has_data: integer expected";
                return null;
            };

            AccountRechargeInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.AccountRechargeInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.AccountRechargeInfo();
                if (object.records) {
                    if (!Array.isArray(object.records))
                        throw TypeError(".lq.AccountMiscSnapshot.AccountRechargeInfo.records: array expected");
                    message.records = Array(object.records.length);
                    for (let i = 0; i < object.records.length; ++i) {
                        if (typeof object.records[i] !== "object")
                            throw TypeError(".lq.AccountMiscSnapshot.AccountRechargeInfo.records: object expected");
                        message.records[i] = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord.fromObject(object.records[i], _depth + 1);
                    }
                }
                if (object.has_data != null)
                    if (Number(object.has_data) !== 0)
                        message.has_data = object.has_data >>> 0;
                return message;
            };

            AccountRechargeInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.records = [];
                if (options.defaults)
                    object.has_data = 0;
                if (message.records && message.records.length) {
                    object.records = Array(message.records.length);
                    for (let j = 0; j < message.records.length; ++j)
                        object.records[j] = $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord.toObject(message.records[j], options);
                }
                if (message.has_data != null && message.hasOwnProperty("has_data"))
                    object.has_data = message.has_data;
                return object;
            };

            AccountRechargeInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountRechargeInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.AccountRechargeInfo";
            };

            AccountRechargeInfo.RechargeRecord = (function() {

                function RechargeRecord(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                RechargeRecord.prototype.level = 0;
                RechargeRecord.prototype.recharge_time = 0;

                RechargeRecord.create = function create(properties) {
                    return new RechargeRecord(properties);
                };

                RechargeRecord.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                        writer.uint32(8).uint32(message.level);
                    if (message.recharge_time != null && Object.hasOwnProperty.call(message, "recharge_time"))
                        writer.uint32(16).uint32(message.recharge_time);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                RechargeRecord.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                RechargeRecord.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.level = value;
                                else
                                    delete message.level;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.recharge_time = value;
                                else
                                    delete message.recharge_time;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                RechargeRecord.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                RechargeRecord.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.level != null && message.hasOwnProperty("level"))
                        if (!$util.isInteger(message.level))
                            return "level: integer expected";
                    if (message.recharge_time != null && message.hasOwnProperty("recharge_time"))
                        if (!$util.isInteger(message.recharge_time))
                            return "recharge_time: integer expected";
                    return null;
                };

                RechargeRecord.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord();
                    if (object.level != null)
                        if (Number(object.level) !== 0)
                            message.level = object.level >>> 0;
                    if (object.recharge_time != null)
                        if (Number(object.recharge_time) !== 0)
                            message.recharge_time = object.recharge_time >>> 0;
                    return message;
                };

                RechargeRecord.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.level = 0;
                        object.recharge_time = 0;
                    }
                    if (message.level != null && message.hasOwnProperty("level"))
                        object.level = message.level;
                    if (message.recharge_time != null && message.hasOwnProperty("recharge_time"))
                        object.recharge_time = message.recharge_time;
                    return object;
                };

                RechargeRecord.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                RechargeRecord.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.AccountMiscSnapshot.AccountRechargeInfo.RechargeRecord";
                };

                return RechargeRecord;
            })();

            return AccountRechargeInfo;
        })();

        AccountMiscSnapshot.AccountMonthTicketSnapshotV2 = (function() {

            function AccountMonthTicketSnapshotV2(properties) {
                this.history = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountMonthTicketSnapshotV2.prototype.end_time = 0;
            AccountMonthTicketSnapshotV2.prototype.last_pay_time = 0;
            AccountMonthTicketSnapshotV2.prototype.record_start_time = 0;
            AccountMonthTicketSnapshotV2.prototype.history = $util.emptyArray;

            AccountMonthTicketSnapshotV2.create = function create(properties) {
                return new AccountMonthTicketSnapshotV2(properties);
            };

            AccountMonthTicketSnapshotV2.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(8).uint32(message.end_time);
                if (message.last_pay_time != null && Object.hasOwnProperty.call(message, "last_pay_time"))
                    writer.uint32(16).uint32(message.last_pay_time);
                if (message.record_start_time != null && Object.hasOwnProperty.call(message, "record_start_time"))
                    writer.uint32(24).uint32(message.record_start_time);
                if (message.history != null && message.history.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.history.length; ++i)
                        writer.uint32(message.history[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountMonthTicketSnapshotV2.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountMonthTicketSnapshotV2.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_pay_time = value;
                            else
                                delete message.last_pay_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.record_start_time = value;
                            else
                                delete message.record_start_time;
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.history && message.history.length))
                                    message.history = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.history.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.history && message.history.length))
                                message.history = [];
                            message.history.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountMonthTicketSnapshotV2.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountMonthTicketSnapshotV2.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    if (!$util.isInteger(message.last_pay_time))
                        return "last_pay_time: integer expected";
                if (message.record_start_time != null && message.hasOwnProperty("record_start_time"))
                    if (!$util.isInteger(message.record_start_time))
                        return "record_start_time: integer expected";
                if (message.history != null && message.hasOwnProperty("history")) {
                    if (!Array.isArray(message.history))
                        return "history: array expected";
                    for (let i = 0; i < message.history.length; ++i)
                        if (!$util.isInteger(message.history[i]))
                            return "history: integer[] expected";
                }
                return null;
            };

            AccountMonthTicketSnapshotV2.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2();
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.last_pay_time != null)
                    if (Number(object.last_pay_time) !== 0)
                        message.last_pay_time = object.last_pay_time >>> 0;
                if (object.record_start_time != null)
                    if (Number(object.record_start_time) !== 0)
                        message.record_start_time = object.record_start_time >>> 0;
                if (object.history) {
                    if (!Array.isArray(object.history))
                        throw TypeError(".lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2.history: array expected");
                    message.history = Array(object.history.length);
                    for (let i = 0; i < object.history.length; ++i)
                        message.history[i] = object.history[i] >>> 0;
                }
                return message;
            };

            AccountMonthTicketSnapshotV2.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.history = [];
                if (options.defaults) {
                    object.end_time = 0;
                    object.last_pay_time = 0;
                    object.record_start_time = 0;
                }
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.last_pay_time != null && message.hasOwnProperty("last_pay_time"))
                    object.last_pay_time = message.last_pay_time;
                if (message.record_start_time != null && message.hasOwnProperty("record_start_time"))
                    object.record_start_time = message.record_start_time;
                if (message.history && message.history.length) {
                    object.history = Array(message.history.length);
                    for (let j = 0; j < message.history.length; ++j)
                        object.history[j] = message.history[j];
                }
                return object;
            };

            AccountMonthTicketSnapshotV2.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountMonthTicketSnapshotV2.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AccountMiscSnapshot.AccountMonthTicketSnapshotV2";
            };

            return AccountMonthTicketSnapshotV2;
        })();

        return AccountMiscSnapshot;
    })();

    lq.AccountGiftCodeRecord = (function() {

        function AccountGiftCodeRecord(properties) {
            this.used_gift_code = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccountGiftCodeRecord.prototype.used_gift_code = $util.emptyArray;

        AccountGiftCodeRecord.create = function create(properties) {
            return new AccountGiftCodeRecord(properties);
        };

        AccountGiftCodeRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.used_gift_code != null && message.used_gift_code.length)
                for (let i = 0; i < message.used_gift_code.length; ++i)
                    writer.uint32(10).string(message.used_gift_code[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccountGiftCodeRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccountGiftCodeRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccountGiftCodeRecord();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.used_gift_code && message.used_gift_code.length))
                            message.used_gift_code = [];
                        message.used_gift_code.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccountGiftCodeRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccountGiftCodeRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.used_gift_code != null && message.hasOwnProperty("used_gift_code")) {
                if (!Array.isArray(message.used_gift_code))
                    return "used_gift_code: array expected";
                for (let i = 0; i < message.used_gift_code.length; ++i)
                    if (!$util.isString(message.used_gift_code[i]))
                        return "used_gift_code: string[] expected";
            }
            return null;
        };

        AccountGiftCodeRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccountGiftCodeRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccountGiftCodeRecord();
            if (object.used_gift_code) {
                if (!Array.isArray(object.used_gift_code))
                    throw TypeError(".lq.AccountGiftCodeRecord.used_gift_code: array expected");
                message.used_gift_code = Array(object.used_gift_code.length);
                for (let i = 0; i < object.used_gift_code.length; ++i)
                    message.used_gift_code[i] = String(object.used_gift_code[i]);
            }
            return message;
        };

        AccountGiftCodeRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.used_gift_code = [];
            if (message.used_gift_code && message.used_gift_code.length) {
                object.used_gift_code = Array(message.used_gift_code.length);
                for (let j = 0; j < message.used_gift_code.length; ++j)
                    object.used_gift_code[j] = message.used_gift_code[j];
            }
            return object;
        };

        AccountGiftCodeRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountGiftCodeRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccountGiftCodeRecord";
        };

        return AccountGiftCodeRecord;
    })();

    lq.AccSn = (function() {

        function AccSn(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccSn.prototype.resource = null;
        AccSn.prototype.character = null;
        AccSn.prototype.mail = null;
        AccSn.prototype.achievement = null;
        AccSn.prototype.misc = null;
        AccSn.prototype.gift_code = null;

        AccSn.create = function create(properties) {
            return new AccSn(properties);
        };

        AccSn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.resource != null && Object.hasOwnProperty.call(message, "resource"))
                $root.lq.AccountResourceSnapshot.encode(message.resource, writer.uint32(10).fork()).ldelim();
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.AccountCharacterSnapshot.encode(message.character, writer.uint32(18).fork()).ldelim();
            if (message.mail != null && Object.hasOwnProperty.call(message, "mail"))
                $root.lq.AccountMailRecord.encode(message.mail, writer.uint32(26).fork()).ldelim();
            if (message.achievement != null && Object.hasOwnProperty.call(message, "achievement"))
                $root.lq.AccountAchievementSnapshot.encode(message.achievement, writer.uint32(34).fork()).ldelim();
            if (message.misc != null && Object.hasOwnProperty.call(message, "misc"))
                $root.lq.AccountMiscSnapshot.encode(message.misc, writer.uint32(42).fork()).ldelim();
            if (message.gift_code != null && Object.hasOwnProperty.call(message, "gift_code"))
                $root.lq.AccountGiftCodeRecord.encode(message.gift_code, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccSn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccSn.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccSn(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.resource = $root.lq.AccountResourceSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.resource);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.AccountCharacterSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.mail = $root.lq.AccountMailRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.mail);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.achievement = $root.lq.AccountAchievementSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.achievement);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.misc = $root.lq.AccountMiscSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.misc);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.gift_code = $root.lq.AccountGiftCodeRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.gift_code);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccSn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccSn.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.resource != null && message.hasOwnProperty("resource")) {
                let error = $root.lq.AccountResourceSnapshot.verify(message.resource, _depth + 1);
                if (error)
                    return "resource." + error;
            }
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.AccountCharacterSnapshot.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            if (message.mail != null && message.hasOwnProperty("mail")) {
                let error = $root.lq.AccountMailRecord.verify(message.mail, _depth + 1);
                if (error)
                    return "mail." + error;
            }
            if (message.achievement != null && message.hasOwnProperty("achievement")) {
                let error = $root.lq.AccountAchievementSnapshot.verify(message.achievement, _depth + 1);
                if (error)
                    return "achievement." + error;
            }
            if (message.misc != null && message.hasOwnProperty("misc")) {
                let error = $root.lq.AccountMiscSnapshot.verify(message.misc, _depth + 1);
                if (error)
                    return "misc." + error;
            }
            if (message.gift_code != null && message.hasOwnProperty("gift_code")) {
                let error = $root.lq.AccountGiftCodeRecord.verify(message.gift_code, _depth + 1);
                if (error)
                    return "gift_code." + error;
            }
            return null;
        };

        AccSn.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccSn)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccSn();
            if (object.resource != null) {
                if (typeof object.resource !== "object")
                    throw TypeError(".lq.AccSn.resource: object expected");
                message.resource = $root.lq.AccountResourceSnapshot.fromObject(object.resource, _depth + 1);
            }
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.AccSn.character: object expected");
                message.character = $root.lq.AccountCharacterSnapshot.fromObject(object.character, _depth + 1);
            }
            if (object.mail != null) {
                if (typeof object.mail !== "object")
                    throw TypeError(".lq.AccSn.mail: object expected");
                message.mail = $root.lq.AccountMailRecord.fromObject(object.mail, _depth + 1);
            }
            if (object.achievement != null) {
                if (typeof object.achievement !== "object")
                    throw TypeError(".lq.AccSn.achievement: object expected");
                message.achievement = $root.lq.AccountAchievementSnapshot.fromObject(object.achievement, _depth + 1);
            }
            if (object.misc != null) {
                if (typeof object.misc !== "object")
                    throw TypeError(".lq.AccSn.misc: object expected");
                message.misc = $root.lq.AccountMiscSnapshot.fromObject(object.misc, _depth + 1);
            }
            if (object.gift_code != null) {
                if (typeof object.gift_code !== "object")
                    throw TypeError(".lq.AccSn.gift_code: object expected");
                message.gift_code = $root.lq.AccountGiftCodeRecord.fromObject(object.gift_code, _depth + 1);
            }
            return message;
        };

        AccSn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.resource = null;
                object.character = null;
                object.mail = null;
                object.achievement = null;
                object.misc = null;
                object.gift_code = null;
            }
            if (message.resource != null && message.hasOwnProperty("resource"))
                object.resource = $root.lq.AccountResourceSnapshot.toObject(message.resource, options);
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.AccountCharacterSnapshot.toObject(message.character, options);
            if (message.mail != null && message.hasOwnProperty("mail"))
                object.mail = $root.lq.AccountMailRecord.toObject(message.mail, options);
            if (message.achievement != null && message.hasOwnProperty("achievement"))
                object.achievement = $root.lq.AccountAchievementSnapshot.toObject(message.achievement, options);
            if (message.misc != null && message.hasOwnProperty("misc"))
                object.misc = $root.lq.AccountMiscSnapshot.toObject(message.misc, options);
            if (message.gift_code != null && message.hasOwnProperty("gift_code"))
                object.gift_code = $root.lq.AccountGiftCodeRecord.toObject(message.gift_code, options);
            return object;
        };

        AccSn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccSn.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccSn";
        };

        return AccSn;
    })();

    lq.AccSnDa = (function() {

        function AccSnDa(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AccSnDa.prototype.account_id = 0;
        AccSnDa.prototype.time = 0;
        AccSnDa.prototype.snapshot = $util.newBuffer([]);

        AccSnDa.create = function create(properties) {
            return new AccSnDa(properties);
        };

        AccSnDa.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(16).uint32(message.time);
            if (message.snapshot != null && Object.hasOwnProperty.call(message, "snapshot"))
                writer.uint32(26).bytes(message.snapshot);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AccSnDa.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AccSnDa.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AccSnDa(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.snapshot = value;
                        else
                            delete message.snapshot;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AccSnDa.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AccSnDa.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            if (message.snapshot != null && message.hasOwnProperty("snapshot"))
                if (!(message.snapshot && typeof message.snapshot.length === "number" || $util.isString(message.snapshot)))
                    return "snapshot: buffer expected";
            return null;
        };

        AccSnDa.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AccSnDa)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AccSnDa();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.time != null)
                if (Number(object.time) !== 0)
                    message.time = object.time >>> 0;
            if (object.snapshot != null)
                if (object.snapshot.length)
                    if (typeof object.snapshot === "string")
                        $util.base64.decode(object.snapshot, message.snapshot = $util.newBuffer($util.base64.length(object.snapshot)), 0);
                    else if (object.snapshot.length >= 0)
                        message.snapshot = object.snapshot;
            return message;
        };

        AccSnDa.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.time = 0;
                if (options.bytes === String)
                    object.snapshot = "";
                else {
                    object.snapshot = [];
                    if (options.bytes !== Array)
                        object.snapshot = $util.newBuffer(object.snapshot);
                }
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            if (message.snapshot != null && message.hasOwnProperty("snapshot"))
                object.snapshot = options.bytes === String ? $util.base64.encode(message.snapshot, 0, message.snapshot.length) : options.bytes === Array ? Array.prototype.slice.call(message.snapshot) : message.snapshot;
            return object;
        };

        AccSnDa.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccSnDa.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AccSnDa";
        };

        return AccSnDa;
    })();

    lq.TransparentData = (function() {

        function TransparentData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TransparentData.prototype.method = "";
        TransparentData.prototype.data = $util.newBuffer([]);
        TransparentData.prototype.session = "";
        TransparentData.prototype.remote = null;

        TransparentData.create = function create(properties) {
            return new TransparentData(properties);
        };

        TransparentData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.method != null && Object.hasOwnProperty.call(message, "method"))
                writer.uint32(10).string(message.method);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(18).bytes(message.data);
            if (message.session != null && Object.hasOwnProperty.call(message, "session"))
                writer.uint32(26).string(message.session);
            if (message.remote != null && Object.hasOwnProperty.call(message, "remote"))
                $root.lq.NetworkEndpoint.encode(message.remote, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TransparentData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TransparentData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TransparentData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.method = value;
                        else
                            delete message.method;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.session = value;
                        else
                            delete message.session;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.remote = $root.lq.NetworkEndpoint.decode(reader, reader.uint32(), undefined, _depth + 1, message.remote);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TransparentData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TransparentData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.method != null && message.hasOwnProperty("method"))
                if (!$util.isString(message.method))
                    return "method: string expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.session != null && message.hasOwnProperty("session"))
                if (!$util.isString(message.session))
                    return "session: string expected";
            if (message.remote != null && message.hasOwnProperty("remote")) {
                let error = $root.lq.NetworkEndpoint.verify(message.remote, _depth + 1);
                if (error)
                    return "remote." + error;
            }
            return null;
        };

        TransparentData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TransparentData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TransparentData();
            if (object.method != null)
                if (typeof object.method !== "string" || object.method.length)
                    message.method = String(object.method);
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.session != null)
                if (typeof object.session !== "string" || object.session.length)
                    message.session = String(object.session);
            if (object.remote != null) {
                if (typeof object.remote !== "object")
                    throw TypeError(".lq.TransparentData.remote: object expected");
                message.remote = $root.lq.NetworkEndpoint.fromObject(object.remote, _depth + 1);
            }
            return message;
        };

        TransparentData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.method = "";
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.session = "";
                object.remote = null;
            }
            if (message.method != null && message.hasOwnProperty("method"))
                object.method = message.method;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.session != null && message.hasOwnProperty("session"))
                object.session = message.session;
            if (message.remote != null && message.hasOwnProperty("remote"))
                object.remote = $root.lq.NetworkEndpoint.toObject(message.remote, options);
            return object;
        };

        TransparentData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TransparentData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TransparentData";
        };

        return TransparentData;
    })();

    lq.QuestionnaireReward = (function() {

        function QuestionnaireReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QuestionnaireReward.prototype.resource_id = 0;
        QuestionnaireReward.prototype.count = 0;

        QuestionnaireReward.create = function create(properties) {
            return new QuestionnaireReward(properties);
        };

        QuestionnaireReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                writer.uint32(8).uint32(message.resource_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QuestionnaireReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QuestionnaireReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.resource_id = value;
                        else
                            delete message.resource_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QuestionnaireReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QuestionnaireReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                if (!$util.isInteger(message.resource_id))
                    return "resource_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        QuestionnaireReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QuestionnaireReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QuestionnaireReward();
            if (object.resource_id != null)
                if (Number(object.resource_id) !== 0)
                    message.resource_id = object.resource_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        QuestionnaireReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.resource_id = 0;
                object.count = 0;
            }
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                object.resource_id = message.resource_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        QuestionnaireReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuestionnaireReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QuestionnaireReward";
        };

        return QuestionnaireReward;
    })();

    lq.QuestionnaireDetail = (function() {

        function QuestionnaireDetail(properties) {
            this.rewards = [];
            this.questions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QuestionnaireDetail.prototype.id = 0;
        QuestionnaireDetail.prototype.version_id = 0;
        QuestionnaireDetail.prototype.effective_time_start = 0;
        QuestionnaireDetail.prototype.effective_time_end = 0;
        QuestionnaireDetail.prototype.rewards = $util.emptyArray;
        QuestionnaireDetail.prototype.banner_title = "";
        QuestionnaireDetail.prototype.title = "";
        QuestionnaireDetail.prototype.announcement_title = "";
        QuestionnaireDetail.prototype.announcement_content = "";
        QuestionnaireDetail.prototype.final_text = "";
        QuestionnaireDetail.prototype.questions = $util.emptyArray;
        QuestionnaireDetail.prototype.type = 0;

        QuestionnaireDetail.create = function create(properties) {
            return new QuestionnaireDetail(properties);
        };

        QuestionnaireDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.version_id != null && Object.hasOwnProperty.call(message, "version_id"))
                writer.uint32(16).uint32(message.version_id);
            if (message.effective_time_start != null && Object.hasOwnProperty.call(message, "effective_time_start"))
                writer.uint32(24).uint32(message.effective_time_start);
            if (message.effective_time_end != null && Object.hasOwnProperty.call(message, "effective_time_end"))
                writer.uint32(32).uint32(message.effective_time_end);
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.QuestionnaireReward.encode(message.rewards[i], writer.uint32(42).fork()).ldelim();
            if (message.banner_title != null && Object.hasOwnProperty.call(message, "banner_title"))
                writer.uint32(50).string(message.banner_title);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(58).string(message.title);
            if (message.announcement_title != null && Object.hasOwnProperty.call(message, "announcement_title"))
                writer.uint32(66).string(message.announcement_title);
            if (message.announcement_content != null && Object.hasOwnProperty.call(message, "announcement_content"))
                writer.uint32(74).string(message.announcement_content);
            if (message.final_text != null && Object.hasOwnProperty.call(message, "final_text"))
                writer.uint32(82).string(message.final_text);
            if (message.questions != null && message.questions.length)
                for (let i = 0; i < message.questions.length; ++i)
                    $root.lq.QuestionnaireQuestion.encode(message.questions[i], writer.uint32(90).fork()).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(96).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QuestionnaireDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QuestionnaireDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version_id = value;
                        else
                            delete message.version_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effective_time_start = value;
                        else
                            delete message.effective_time_start;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effective_time_end = value;
                        else
                            delete message.effective_time_end;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.QuestionnaireReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.banner_title = value;
                        else
                            delete message.banner_title;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.announcement_title = value;
                        else
                            delete message.announcement_title;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.announcement_content = value;
                        else
                            delete message.announcement_content;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.final_text = value;
                        else
                            delete message.final_text;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.questions && message.questions.length))
                            message.questions = [];
                        message.questions.push($root.lq.QuestionnaireQuestion.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QuestionnaireDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QuestionnaireDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.version_id != null && message.hasOwnProperty("version_id"))
                if (!$util.isInteger(message.version_id))
                    return "version_id: integer expected";
            if (message.effective_time_start != null && message.hasOwnProperty("effective_time_start"))
                if (!$util.isInteger(message.effective_time_start))
                    return "effective_time_start: integer expected";
            if (message.effective_time_end != null && message.hasOwnProperty("effective_time_end"))
                if (!$util.isInteger(message.effective_time_end))
                    return "effective_time_end: integer expected";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.QuestionnaireReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.banner_title != null && message.hasOwnProperty("banner_title"))
                if (!$util.isString(message.banner_title))
                    return "banner_title: string expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.announcement_title != null && message.hasOwnProperty("announcement_title"))
                if (!$util.isString(message.announcement_title))
                    return "announcement_title: string expected";
            if (message.announcement_content != null && message.hasOwnProperty("announcement_content"))
                if (!$util.isString(message.announcement_content))
                    return "announcement_content: string expected";
            if (message.final_text != null && message.hasOwnProperty("final_text"))
                if (!$util.isString(message.final_text))
                    return "final_text: string expected";
            if (message.questions != null && message.hasOwnProperty("questions")) {
                if (!Array.isArray(message.questions))
                    return "questions: array expected";
                for (let i = 0; i < message.questions.length; ++i) {
                    let error = $root.lq.QuestionnaireQuestion.verify(message.questions[i], _depth + 1);
                    if (error)
                        return "questions." + error;
                }
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        QuestionnaireDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QuestionnaireDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QuestionnaireDetail();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.version_id != null)
                if (Number(object.version_id) !== 0)
                    message.version_id = object.version_id >>> 0;
            if (object.effective_time_start != null)
                if (Number(object.effective_time_start) !== 0)
                    message.effective_time_start = object.effective_time_start >>> 0;
            if (object.effective_time_end != null)
                if (Number(object.effective_time_end) !== 0)
                    message.effective_time_end = object.effective_time_end >>> 0;
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.QuestionnaireDetail.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.QuestionnaireDetail.rewards: object expected");
                    message.rewards[i] = $root.lq.QuestionnaireReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.banner_title != null)
                if (typeof object.banner_title !== "string" || object.banner_title.length)
                    message.banner_title = String(object.banner_title);
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.announcement_title != null)
                if (typeof object.announcement_title !== "string" || object.announcement_title.length)
                    message.announcement_title = String(object.announcement_title);
            if (object.announcement_content != null)
                if (typeof object.announcement_content !== "string" || object.announcement_content.length)
                    message.announcement_content = String(object.announcement_content);
            if (object.final_text != null)
                if (typeof object.final_text !== "string" || object.final_text.length)
                    message.final_text = String(object.final_text);
            if (object.questions) {
                if (!Array.isArray(object.questions))
                    throw TypeError(".lq.QuestionnaireDetail.questions: array expected");
                message.questions = Array(object.questions.length);
                for (let i = 0; i < object.questions.length; ++i) {
                    if (typeof object.questions[i] !== "object")
                        throw TypeError(".lq.QuestionnaireDetail.questions: object expected");
                    message.questions[i] = $root.lq.QuestionnaireQuestion.fromObject(object.questions[i], _depth + 1);
                }
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        QuestionnaireDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.rewards = [];
                object.questions = [];
            }
            if (options.defaults) {
                object.id = 0;
                object.version_id = 0;
                object.effective_time_start = 0;
                object.effective_time_end = 0;
                object.banner_title = "";
                object.title = "";
                object.announcement_title = "";
                object.announcement_content = "";
                object.final_text = "";
                object.type = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.version_id != null && message.hasOwnProperty("version_id"))
                object.version_id = message.version_id;
            if (message.effective_time_start != null && message.hasOwnProperty("effective_time_start"))
                object.effective_time_start = message.effective_time_start;
            if (message.effective_time_end != null && message.hasOwnProperty("effective_time_end"))
                object.effective_time_end = message.effective_time_end;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.QuestionnaireReward.toObject(message.rewards[j], options);
            }
            if (message.banner_title != null && message.hasOwnProperty("banner_title"))
                object.banner_title = message.banner_title;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.announcement_title != null && message.hasOwnProperty("announcement_title"))
                object.announcement_title = message.announcement_title;
            if (message.announcement_content != null && message.hasOwnProperty("announcement_content"))
                object.announcement_content = message.announcement_content;
            if (message.final_text != null && message.hasOwnProperty("final_text"))
                object.final_text = message.final_text;
            if (message.questions && message.questions.length) {
                object.questions = Array(message.questions.length);
                for (let j = 0; j < message.questions.length; ++j)
                    object.questions[j] = $root.lq.QuestionnaireQuestion.toObject(message.questions[j], options);
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        QuestionnaireDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuestionnaireDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QuestionnaireDetail";
        };

        return QuestionnaireDetail;
    })();

    lq.QuestionnaireQuestion = (function() {

        function QuestionnaireQuestion(properties) {
            this.options = [];
            this.next_question = [];
            this.matrix_row = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QuestionnaireQuestion.prototype.id = 0;
        QuestionnaireQuestion.prototype.title = "";
        QuestionnaireQuestion.prototype.describe = "";
        QuestionnaireQuestion.prototype.type = "";
        QuestionnaireQuestion.prototype.sub_type = "";
        QuestionnaireQuestion.prototype.options = $util.emptyArray;
        QuestionnaireQuestion.prototype.option_random_sort = false;
        QuestionnaireQuestion.prototype.require = false;
        QuestionnaireQuestion.prototype.max_choice = 0;
        QuestionnaireQuestion.prototype.next_question = $util.emptyArray;
        QuestionnaireQuestion.prototype.matrix_row = $util.emptyArray;
        QuestionnaireQuestion.prototype.option_random_sort_index = 0;

        QuestionnaireQuestion.create = function create(properties) {
            return new QuestionnaireQuestion(properties);
        };

        QuestionnaireQuestion.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(18).string(message.title);
            if (message.describe != null && Object.hasOwnProperty.call(message, "describe"))
                writer.uint32(26).string(message.describe);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(34).string(message.type);
            if (message.sub_type != null && Object.hasOwnProperty.call(message, "sub_type"))
                writer.uint32(42).string(message.sub_type);
            if (message.options != null && message.options.length)
                for (let i = 0; i < message.options.length; ++i)
                    $root.lq.QuestionnaireQuestion.QuestionOption.encode(message.options[i], writer.uint32(50).fork()).ldelim();
            if (message.option_random_sort != null && Object.hasOwnProperty.call(message, "option_random_sort"))
                writer.uint32(56).bool(message.option_random_sort);
            if (message.require != null && Object.hasOwnProperty.call(message, "require"))
                writer.uint32(64).bool(message.require);
            if (message.max_choice != null && Object.hasOwnProperty.call(message, "max_choice"))
                writer.uint32(72).uint32(message.max_choice);
            if (message.next_question != null && message.next_question.length)
                for (let i = 0; i < message.next_question.length; ++i)
                    $root.lq.QuestionnaireQuestion.NextQuestionData.encode(message.next_question[i], writer.uint32(82).fork()).ldelim();
            if (message.matrix_row != null && message.matrix_row.length)
                for (let i = 0; i < message.matrix_row.length; ++i)
                    writer.uint32(90).string(message.matrix_row[i]);
            if (message.option_random_sort_index != null && Object.hasOwnProperty.call(message, "option_random_sort_index"))
                writer.uint32(96).int32(message.option_random_sort_index);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QuestionnaireQuestion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QuestionnaireQuestion.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireQuestion(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.describe = value;
                        else
                            delete message.describe;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sub_type = value;
                        else
                            delete message.sub_type;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.options && message.options.length))
                            message.options = [];
                        message.options.push($root.lq.QuestionnaireQuestion.QuestionOption.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.option_random_sort = value;
                        else
                            delete message.option_random_sort;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.require = value;
                        else
                            delete message.require;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_choice = value;
                        else
                            delete message.max_choice;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.next_question && message.next_question.length))
                            message.next_question = [];
                        message.next_question.push($root.lq.QuestionnaireQuestion.NextQuestionData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.matrix_row && message.matrix_row.length))
                            message.matrix_row = [];
                        message.matrix_row.push(reader.string());
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.option_random_sort_index = value;
                        else
                            delete message.option_random_sort_index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QuestionnaireQuestion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QuestionnaireQuestion.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.describe != null && message.hasOwnProperty("describe"))
                if (!$util.isString(message.describe))
                    return "describe: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.sub_type != null && message.hasOwnProperty("sub_type"))
                if (!$util.isString(message.sub_type))
                    return "sub_type: string expected";
            if (message.options != null && message.hasOwnProperty("options")) {
                if (!Array.isArray(message.options))
                    return "options: array expected";
                for (let i = 0; i < message.options.length; ++i) {
                    let error = $root.lq.QuestionnaireQuestion.QuestionOption.verify(message.options[i], _depth + 1);
                    if (error)
                        return "options." + error;
                }
            }
            if (message.option_random_sort != null && message.hasOwnProperty("option_random_sort"))
                if (typeof message.option_random_sort !== "boolean")
                    return "option_random_sort: boolean expected";
            if (message.require != null && message.hasOwnProperty("require"))
                if (typeof message.require !== "boolean")
                    return "require: boolean expected";
            if (message.max_choice != null && message.hasOwnProperty("max_choice"))
                if (!$util.isInteger(message.max_choice))
                    return "max_choice: integer expected";
            if (message.next_question != null && message.hasOwnProperty("next_question")) {
                if (!Array.isArray(message.next_question))
                    return "next_question: array expected";
                for (let i = 0; i < message.next_question.length; ++i) {
                    let error = $root.lq.QuestionnaireQuestion.NextQuestionData.verify(message.next_question[i], _depth + 1);
                    if (error)
                        return "next_question." + error;
                }
            }
            if (message.matrix_row != null && message.hasOwnProperty("matrix_row")) {
                if (!Array.isArray(message.matrix_row))
                    return "matrix_row: array expected";
                for (let i = 0; i < message.matrix_row.length; ++i)
                    if (!$util.isString(message.matrix_row[i]))
                        return "matrix_row: string[] expected";
            }
            if (message.option_random_sort_index != null && message.hasOwnProperty("option_random_sort_index"))
                if (!$util.isInteger(message.option_random_sort_index))
                    return "option_random_sort_index: integer expected";
            return null;
        };

        QuestionnaireQuestion.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QuestionnaireQuestion)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QuestionnaireQuestion();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.describe != null)
                if (typeof object.describe !== "string" || object.describe.length)
                    message.describe = String(object.describe);
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = String(object.type);
            if (object.sub_type != null)
                if (typeof object.sub_type !== "string" || object.sub_type.length)
                    message.sub_type = String(object.sub_type);
            if (object.options) {
                if (!Array.isArray(object.options))
                    throw TypeError(".lq.QuestionnaireQuestion.options: array expected");
                message.options = Array(object.options.length);
                for (let i = 0; i < object.options.length; ++i) {
                    if (typeof object.options[i] !== "object")
                        throw TypeError(".lq.QuestionnaireQuestion.options: object expected");
                    message.options[i] = $root.lq.QuestionnaireQuestion.QuestionOption.fromObject(object.options[i], _depth + 1);
                }
            }
            if (object.option_random_sort != null)
                if (object.option_random_sort)
                    message.option_random_sort = Boolean(object.option_random_sort);
            if (object.require != null)
                if (object.require)
                    message.require = Boolean(object.require);
            if (object.max_choice != null)
                if (Number(object.max_choice) !== 0)
                    message.max_choice = object.max_choice >>> 0;
            if (object.next_question) {
                if (!Array.isArray(object.next_question))
                    throw TypeError(".lq.QuestionnaireQuestion.next_question: array expected");
                message.next_question = Array(object.next_question.length);
                for (let i = 0; i < object.next_question.length; ++i) {
                    if (typeof object.next_question[i] !== "object")
                        throw TypeError(".lq.QuestionnaireQuestion.next_question: object expected");
                    message.next_question[i] = $root.lq.QuestionnaireQuestion.NextQuestionData.fromObject(object.next_question[i], _depth + 1);
                }
            }
            if (object.matrix_row) {
                if (!Array.isArray(object.matrix_row))
                    throw TypeError(".lq.QuestionnaireQuestion.matrix_row: array expected");
                message.matrix_row = Array(object.matrix_row.length);
                for (let i = 0; i < object.matrix_row.length; ++i)
                    message.matrix_row[i] = String(object.matrix_row[i]);
            }
            if (object.option_random_sort_index != null)
                if (Number(object.option_random_sort_index) !== 0)
                    message.option_random_sort_index = object.option_random_sort_index | 0;
            return message;
        };

        QuestionnaireQuestion.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.options = [];
                object.next_question = [];
                object.matrix_row = [];
            }
            if (options.defaults) {
                object.id = 0;
                object.title = "";
                object.describe = "";
                object.type = "";
                object.sub_type = "";
                object.option_random_sort = false;
                object.require = false;
                object.max_choice = 0;
                object.option_random_sort_index = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.describe != null && message.hasOwnProperty("describe"))
                object.describe = message.describe;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.sub_type != null && message.hasOwnProperty("sub_type"))
                object.sub_type = message.sub_type;
            if (message.options && message.options.length) {
                object.options = Array(message.options.length);
                for (let j = 0; j < message.options.length; ++j)
                    object.options[j] = $root.lq.QuestionnaireQuestion.QuestionOption.toObject(message.options[j], options);
            }
            if (message.option_random_sort != null && message.hasOwnProperty("option_random_sort"))
                object.option_random_sort = message.option_random_sort;
            if (message.require != null && message.hasOwnProperty("require"))
                object.require = message.require;
            if (message.max_choice != null && message.hasOwnProperty("max_choice"))
                object.max_choice = message.max_choice;
            if (message.next_question && message.next_question.length) {
                object.next_question = Array(message.next_question.length);
                for (let j = 0; j < message.next_question.length; ++j)
                    object.next_question[j] = $root.lq.QuestionnaireQuestion.NextQuestionData.toObject(message.next_question[j], options);
            }
            if (message.matrix_row && message.matrix_row.length) {
                object.matrix_row = Array(message.matrix_row.length);
                for (let j = 0; j < message.matrix_row.length; ++j)
                    object.matrix_row[j] = message.matrix_row[j];
            }
            if (message.option_random_sort_index != null && message.hasOwnProperty("option_random_sort_index"))
                object.option_random_sort_index = message.option_random_sort_index;
            return object;
        };

        QuestionnaireQuestion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuestionnaireQuestion.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QuestionnaireQuestion";
        };

        QuestionnaireQuestion.QuestionOption = (function() {

            function QuestionOption(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QuestionOption.prototype.label = "";
            QuestionOption.prototype.value = "";
            QuestionOption.prototype.allow_input = false;

            QuestionOption.create = function create(properties) {
                return new QuestionOption(properties);
            };

            QuestionOption.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.label != null && Object.hasOwnProperty.call(message, "label"))
                    writer.uint32(10).string(message.label);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(18).string(message.value);
                if (message.allow_input != null && Object.hasOwnProperty.call(message, "allow_input"))
                    writer.uint32(24).bool(message.allow_input);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QuestionOption.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QuestionOption.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireQuestion.QuestionOption(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.label = value;
                            else
                                delete message.label;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.allow_input = value;
                            else
                                delete message.allow_input;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QuestionOption.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QuestionOption.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.label != null && message.hasOwnProperty("label"))
                    if (!$util.isString(message.label))
                        return "label: string expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isString(message.value))
                        return "value: string expected";
                if (message.allow_input != null && message.hasOwnProperty("allow_input"))
                    if (typeof message.allow_input !== "boolean")
                        return "allow_input: boolean expected";
                return null;
            };

            QuestionOption.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.QuestionnaireQuestion.QuestionOption)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.QuestionnaireQuestion.QuestionOption();
                if (object.label != null)
                    if (typeof object.label !== "string" || object.label.length)
                        message.label = String(object.label);
                if (object.value != null)
                    if (typeof object.value !== "string" || object.value.length)
                        message.value = String(object.value);
                if (object.allow_input != null)
                    if (object.allow_input)
                        message.allow_input = Boolean(object.allow_input);
                return message;
            };

            QuestionOption.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.label = "";
                    object.value = "";
                    object.allow_input = false;
                }
                if (message.label != null && message.hasOwnProperty("label"))
                    object.label = message.label;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                if (message.allow_input != null && message.hasOwnProperty("allow_input"))
                    object.allow_input = message.allow_input;
                return object;
            };

            QuestionOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuestionOption.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.QuestionnaireQuestion.QuestionOption";
            };

            return QuestionOption;
        })();

        QuestionnaireQuestion.NextQuestionData = (function() {

            function NextQuestionData(properties) {
                this.conditions = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            NextQuestionData.prototype.target_question_id = 0;
            NextQuestionData.prototype.conditions = $util.emptyArray;

            NextQuestionData.create = function create(properties) {
                return new NextQuestionData(properties);
            };

            NextQuestionData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.target_question_id != null && Object.hasOwnProperty.call(message, "target_question_id"))
                    writer.uint32(8).uint32(message.target_question_id);
                if (message.conditions != null && message.conditions.length)
                    for (let i = 0; i < message.conditions.length; ++i)
                        $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.encode(message.conditions[i], writer.uint32(82).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            NextQuestionData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            NextQuestionData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireQuestion.NextQuestionData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.target_question_id = value;
                            else
                                delete message.target_question_id;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if (!(message.conditions && message.conditions.length))
                                message.conditions = [];
                            message.conditions.push($root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            NextQuestionData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            NextQuestionData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.target_question_id != null && message.hasOwnProperty("target_question_id"))
                    if (!$util.isInteger(message.target_question_id))
                        return "target_question_id: integer expected";
                if (message.conditions != null && message.hasOwnProperty("conditions")) {
                    if (!Array.isArray(message.conditions))
                        return "conditions: array expected";
                    for (let i = 0; i < message.conditions.length; ++i) {
                        let error = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.verify(message.conditions[i], _depth + 1);
                        if (error)
                            return "conditions." + error;
                    }
                }
                return null;
            };

            NextQuestionData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.QuestionnaireQuestion.NextQuestionData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.QuestionnaireQuestion.NextQuestionData();
                if (object.target_question_id != null)
                    if (Number(object.target_question_id) !== 0)
                        message.target_question_id = object.target_question_id >>> 0;
                if (object.conditions) {
                    if (!Array.isArray(object.conditions))
                        throw TypeError(".lq.QuestionnaireQuestion.NextQuestionData.conditions: array expected");
                    message.conditions = Array(object.conditions.length);
                    for (let i = 0; i < object.conditions.length; ++i) {
                        if (typeof object.conditions[i] !== "object")
                            throw TypeError(".lq.QuestionnaireQuestion.NextQuestionData.conditions: object expected");
                        message.conditions[i] = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.fromObject(object.conditions[i], _depth + 1);
                    }
                }
                return message;
            };

            NextQuestionData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.conditions = [];
                if (options.defaults)
                    object.target_question_id = 0;
                if (message.target_question_id != null && message.hasOwnProperty("target_question_id"))
                    object.target_question_id = message.target_question_id;
                if (message.conditions && message.conditions.length) {
                    object.conditions = Array(message.conditions.length);
                    for (let j = 0; j < message.conditions.length; ++j)
                        object.conditions[j] = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.toObject(message.conditions[j], options);
                }
                return object;
            };

            NextQuestionData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            NextQuestionData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.QuestionnaireQuestion.NextQuestionData";
            };

            NextQuestionData.QuestionCondition = (function() {

                function QuestionCondition(properties) {
                    this.values = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                QuestionCondition.prototype.question_id = 0;
                QuestionCondition.prototype.op = "";
                QuestionCondition.prototype.values = $util.emptyArray;

                QuestionCondition.create = function create(properties) {
                    return new QuestionCondition(properties);
                };

                QuestionCondition.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.question_id != null && Object.hasOwnProperty.call(message, "question_id"))
                        writer.uint32(8).uint32(message.question_id);
                    if (message.op != null && Object.hasOwnProperty.call(message, "op"))
                        writer.uint32(18).string(message.op);
                    if (message.values != null && message.values.length)
                        for (let i = 0; i < message.values.length; ++i)
                            writer.uint32(26).string(message.values[i]);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                QuestionCondition.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                QuestionCondition.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.question_id = value;
                                else
                                    delete message.question_id;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.op = value;
                                else
                                    delete message.op;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.values && message.values.length))
                                    message.values = [];
                                message.values.push(reader.string());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                QuestionCondition.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                QuestionCondition.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.question_id != null && message.hasOwnProperty("question_id"))
                        if (!$util.isInteger(message.question_id))
                            return "question_id: integer expected";
                    if (message.op != null && message.hasOwnProperty("op"))
                        if (!$util.isString(message.op))
                            return "op: string expected";
                    if (message.values != null && message.hasOwnProperty("values")) {
                        if (!Array.isArray(message.values))
                            return "values: array expected";
                        for (let i = 0; i < message.values.length; ++i)
                            if (!$util.isString(message.values[i]))
                                return "values: string[] expected";
                    }
                    return null;
                };

                QuestionCondition.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition();
                    if (object.question_id != null)
                        if (Number(object.question_id) !== 0)
                            message.question_id = object.question_id >>> 0;
                    if (object.op != null)
                        if (typeof object.op !== "string" || object.op.length)
                            message.op = String(object.op);
                    if (object.values) {
                        if (!Array.isArray(object.values))
                            throw TypeError(".lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.values: array expected");
                        message.values = Array(object.values.length);
                        for (let i = 0; i < object.values.length; ++i)
                            message.values[i] = String(object.values[i]);
                    }
                    return message;
                };

                QuestionCondition.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.values = [];
                    if (options.defaults) {
                        object.question_id = 0;
                        object.op = "";
                    }
                    if (message.question_id != null && message.hasOwnProperty("question_id"))
                        object.question_id = message.question_id;
                    if (message.op != null && message.hasOwnProperty("op"))
                        object.op = message.op;
                    if (message.values && message.values.length) {
                        object.values = Array(message.values.length);
                        for (let j = 0; j < message.values.length; ++j)
                            object.values[j] = message.values[j];
                    }
                    return object;
                };

                QuestionCondition.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                QuestionCondition.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition";
                };

                return QuestionCondition;
            })();

            NextQuestionData.QuestionconditionWrapper = (function() {

                function QuestionconditionWrapper(properties) {
                    this.conditions = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                QuestionconditionWrapper.prototype.conditions = $util.emptyArray;

                QuestionconditionWrapper.create = function create(properties) {
                    return new QuestionconditionWrapper(properties);
                };

                QuestionconditionWrapper.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.conditions != null && message.conditions.length)
                        for (let i = 0; i < message.conditions.length; ++i)
                            $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.encode(message.conditions[i], writer.uint32(10).fork()).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                QuestionconditionWrapper.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                QuestionconditionWrapper.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper();
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.conditions && message.conditions.length))
                                    message.conditions = [];
                                message.conditions.push($root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.decode(reader, reader.uint32(), undefined, _depth + 1));
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                QuestionconditionWrapper.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                QuestionconditionWrapper.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.conditions != null && message.hasOwnProperty("conditions")) {
                        if (!Array.isArray(message.conditions))
                            return "conditions: array expected";
                        for (let i = 0; i < message.conditions.length; ++i) {
                            let error = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.verify(message.conditions[i], _depth + 1);
                            if (error)
                                return "conditions." + error;
                        }
                    }
                    return null;
                };

                QuestionconditionWrapper.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper();
                    if (object.conditions) {
                        if (!Array.isArray(object.conditions))
                            throw TypeError(".lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.conditions: array expected");
                        message.conditions = Array(object.conditions.length);
                        for (let i = 0; i < object.conditions.length; ++i) {
                            if (typeof object.conditions[i] !== "object")
                                throw TypeError(".lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper.conditions: object expected");
                            message.conditions[i] = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.fromObject(object.conditions[i], _depth + 1);
                        }
                    }
                    return message;
                };

                QuestionconditionWrapper.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.conditions = [];
                    if (message.conditions && message.conditions.length) {
                        object.conditions = Array(message.conditions.length);
                        for (let j = 0; j < message.conditions.length; ++j)
                            object.conditions[j] = $root.lq.QuestionnaireQuestion.NextQuestionData.QuestionCondition.toObject(message.conditions[j], options);
                    }
                    return object;
                };

                QuestionconditionWrapper.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                QuestionconditionWrapper.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.QuestionnaireQuestion.NextQuestionData.QuestionconditionWrapper";
                };

                return QuestionconditionWrapper;
            })();

            return NextQuestionData;
        })();

        return QuestionnaireQuestion;
    })();

    lq.QuestionnaireBrief = (function() {

        function QuestionnaireBrief(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        QuestionnaireBrief.prototype.id = 0;
        QuestionnaireBrief.prototype.version_id = 0;
        QuestionnaireBrief.prototype.effective_time_start = 0;
        QuestionnaireBrief.prototype.effective_time_end = 0;
        QuestionnaireBrief.prototype.rewards = $util.emptyArray;
        QuestionnaireBrief.prototype.banner_title = "";
        QuestionnaireBrief.prototype.title = "";
        QuestionnaireBrief.prototype.type = 0;

        QuestionnaireBrief.create = function create(properties) {
            return new QuestionnaireBrief(properties);
        };

        QuestionnaireBrief.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.version_id != null && Object.hasOwnProperty.call(message, "version_id"))
                writer.uint32(16).uint32(message.version_id);
            if (message.effective_time_start != null && Object.hasOwnProperty.call(message, "effective_time_start"))
                writer.uint32(24).uint32(message.effective_time_start);
            if (message.effective_time_end != null && Object.hasOwnProperty.call(message, "effective_time_end"))
                writer.uint32(32).uint32(message.effective_time_end);
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.QuestionnaireReward.encode(message.rewards[i], writer.uint32(42).fork()).ldelim();
            if (message.banner_title != null && Object.hasOwnProperty.call(message, "banner_title"))
                writer.uint32(50).string(message.banner_title);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(58).string(message.title);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(64).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        QuestionnaireBrief.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        QuestionnaireBrief.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.QuestionnaireBrief(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version_id = value;
                        else
                            delete message.version_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effective_time_start = value;
                        else
                            delete message.effective_time_start;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effective_time_end = value;
                        else
                            delete message.effective_time_end;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.QuestionnaireReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.banner_title = value;
                        else
                            delete message.banner_title;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        QuestionnaireBrief.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        QuestionnaireBrief.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.version_id != null && message.hasOwnProperty("version_id"))
                if (!$util.isInteger(message.version_id))
                    return "version_id: integer expected";
            if (message.effective_time_start != null && message.hasOwnProperty("effective_time_start"))
                if (!$util.isInteger(message.effective_time_start))
                    return "effective_time_start: integer expected";
            if (message.effective_time_end != null && message.hasOwnProperty("effective_time_end"))
                if (!$util.isInteger(message.effective_time_end))
                    return "effective_time_end: integer expected";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.QuestionnaireReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.banner_title != null && message.hasOwnProperty("banner_title"))
                if (!$util.isString(message.banner_title))
                    return "banner_title: string expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        QuestionnaireBrief.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.QuestionnaireBrief)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.QuestionnaireBrief();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.version_id != null)
                if (Number(object.version_id) !== 0)
                    message.version_id = object.version_id >>> 0;
            if (object.effective_time_start != null)
                if (Number(object.effective_time_start) !== 0)
                    message.effective_time_start = object.effective_time_start >>> 0;
            if (object.effective_time_end != null)
                if (Number(object.effective_time_end) !== 0)
                    message.effective_time_end = object.effective_time_end >>> 0;
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.QuestionnaireBrief.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.QuestionnaireBrief.rewards: object expected");
                    message.rewards[i] = $root.lq.QuestionnaireReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.banner_title != null)
                if (typeof object.banner_title !== "string" || object.banner_title.length)
                    message.banner_title = String(object.banner_title);
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        QuestionnaireBrief.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.id = 0;
                object.version_id = 0;
                object.effective_time_start = 0;
                object.effective_time_end = 0;
                object.banner_title = "";
                object.title = "";
                object.type = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.version_id != null && message.hasOwnProperty("version_id"))
                object.version_id = message.version_id;
            if (message.effective_time_start != null && message.hasOwnProperty("effective_time_start"))
                object.effective_time_start = message.effective_time_start;
            if (message.effective_time_end != null && message.hasOwnProperty("effective_time_end"))
                object.effective_time_end = message.effective_time_end;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.QuestionnaireReward.toObject(message.rewards[j], options);
            }
            if (message.banner_title != null && message.hasOwnProperty("banner_title"))
                object.banner_title = message.banner_title;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        QuestionnaireBrief.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuestionnaireBrief.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.QuestionnaireBrief";
        };

        return QuestionnaireBrief;
    })();

    lq.SeerReport = (function() {

        function SeerReport(properties) {
            this.events = [];
            this.rounds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerReport.prototype.uuid = "";
        SeerReport.prototype.events = $util.emptyArray;
        SeerReport.prototype.rounds = $util.emptyArray;

        SeerReport.create = function create(properties) {
            return new SeerReport(properties);
        };

        SeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.events != null && message.events.length)
                for (let i = 0; i < message.events.length; ++i)
                    $root.lq.SeerEvent.encode(message.events[i], writer.uint32(18).fork()).ldelim();
            if (message.rounds != null && message.rounds.length)
                for (let i = 0; i < message.rounds.length; ++i)
                    $root.lq.SeerRound.encode(message.rounds[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.events && message.events.length))
                            message.events = [];
                        message.events.push($root.lq.SeerEvent.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rounds && message.rounds.length))
                            message.rounds = [];
                        message.rounds.push($root.lq.SeerRound.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.events != null && message.hasOwnProperty("events")) {
                if (!Array.isArray(message.events))
                    return "events: array expected";
                for (let i = 0; i < message.events.length; ++i) {
                    let error = $root.lq.SeerEvent.verify(message.events[i], _depth + 1);
                    if (error)
                        return "events." + error;
                }
            }
            if (message.rounds != null && message.hasOwnProperty("rounds")) {
                if (!Array.isArray(message.rounds))
                    return "rounds: array expected";
                for (let i = 0; i < message.rounds.length; ++i) {
                    let error = $root.lq.SeerRound.verify(message.rounds[i], _depth + 1);
                    if (error)
                        return "rounds." + error;
                }
            }
            return null;
        };

        SeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerReport();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.events) {
                if (!Array.isArray(object.events))
                    throw TypeError(".lq.SeerReport.events: array expected");
                message.events = Array(object.events.length);
                for (let i = 0; i < object.events.length; ++i) {
                    if (typeof object.events[i] !== "object")
                        throw TypeError(".lq.SeerReport.events: object expected");
                    message.events[i] = $root.lq.SeerEvent.fromObject(object.events[i], _depth + 1);
                }
            }
            if (object.rounds) {
                if (!Array.isArray(object.rounds))
                    throw TypeError(".lq.SeerReport.rounds: array expected");
                message.rounds = Array(object.rounds.length);
                for (let i = 0; i < object.rounds.length; ++i) {
                    if (typeof object.rounds[i] !== "object")
                        throw TypeError(".lq.SeerReport.rounds: object expected");
                    message.rounds[i] = $root.lq.SeerRound.fromObject(object.rounds[i], _depth + 1);
                }
            }
            return message;
        };

        SeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.events = [];
                object.rounds = [];
            }
            if (options.defaults)
                object.uuid = "";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.events && message.events.length) {
                object.events = Array(message.events.length);
                for (let j = 0; j < message.events.length; ++j)
                    object.events[j] = $root.lq.SeerEvent.toObject(message.events[j], options);
            }
            if (message.rounds && message.rounds.length) {
                object.rounds = Array(message.rounds.length);
                for (let j = 0; j < message.rounds.length; ++j)
                    object.rounds[j] = $root.lq.SeerRound.toObject(message.rounds[j], options);
            }
            return object;
        };

        SeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerReport";
        };

        return SeerReport;
    })();

    lq.SeerEvent = (function() {

        function SeerEvent(properties) {
            this.recommends = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerEvent.prototype.record_index = 0;
        SeerEvent.prototype.seer_index = 0;
        SeerEvent.prototype.recommends = $util.emptyArray;

        SeerEvent.create = function create(properties) {
            return new SeerEvent(properties);
        };

        SeerEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.record_index != null && Object.hasOwnProperty.call(message, "record_index"))
                writer.uint32(8).int32(message.record_index);
            if (message.seer_index != null && Object.hasOwnProperty.call(message, "seer_index"))
                writer.uint32(16).int32(message.seer_index);
            if (message.recommends != null && message.recommends.length)
                for (let i = 0; i < message.recommends.length; ++i)
                    $root.lq.SeerRecommend.encode(message.recommends[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.record_index = value;
                        else
                            delete message.record_index;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.seer_index = value;
                        else
                            delete message.seer_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recommends && message.recommends.length))
                            message.recommends = [];
                        message.recommends.push($root.lq.SeerRecommend.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.record_index != null && message.hasOwnProperty("record_index"))
                if (!$util.isInteger(message.record_index))
                    return "record_index: integer expected";
            if (message.seer_index != null && message.hasOwnProperty("seer_index"))
                if (!$util.isInteger(message.seer_index))
                    return "seer_index: integer expected";
            if (message.recommends != null && message.hasOwnProperty("recommends")) {
                if (!Array.isArray(message.recommends))
                    return "recommends: array expected";
                for (let i = 0; i < message.recommends.length; ++i) {
                    let error = $root.lq.SeerRecommend.verify(message.recommends[i], _depth + 1);
                    if (error)
                        return "recommends." + error;
                }
            }
            return null;
        };

        SeerEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerEvent();
            if (object.record_index != null)
                if (Number(object.record_index) !== 0)
                    message.record_index = object.record_index | 0;
            if (object.seer_index != null)
                if (Number(object.seer_index) !== 0)
                    message.seer_index = object.seer_index | 0;
            if (object.recommends) {
                if (!Array.isArray(object.recommends))
                    throw TypeError(".lq.SeerEvent.recommends: array expected");
                message.recommends = Array(object.recommends.length);
                for (let i = 0; i < object.recommends.length; ++i) {
                    if (typeof object.recommends[i] !== "object")
                        throw TypeError(".lq.SeerEvent.recommends: object expected");
                    message.recommends[i] = $root.lq.SeerRecommend.fromObject(object.recommends[i], _depth + 1);
                }
            }
            return message;
        };

        SeerEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.recommends = [];
            if (options.defaults) {
                object.record_index = 0;
                object.seer_index = 0;
            }
            if (message.record_index != null && message.hasOwnProperty("record_index"))
                object.record_index = message.record_index;
            if (message.seer_index != null && message.hasOwnProperty("seer_index"))
                object.seer_index = message.seer_index;
            if (message.recommends && message.recommends.length) {
                object.recommends = Array(message.recommends.length);
                for (let j = 0; j < message.recommends.length; ++j)
                    object.recommends[j] = $root.lq.SeerRecommend.toObject(message.recommends[j], options);
            }
            return object;
        };

        SeerEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerEvent";
        };

        return SeerEvent;
    })();

    lq.SeerRecommend = (function() {

        function SeerRecommend(properties) {
            this.predictions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerRecommend.prototype.seat = 0;
        SeerRecommend.prototype.predictions = $util.emptyArray;

        SeerRecommend.create = function create(properties) {
            return new SeerRecommend(properties);
        };

        SeerRecommend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).int32(message.seat);
            if (message.predictions != null && message.predictions.length)
                for (let i = 0; i < message.predictions.length; ++i)
                    $root.lq.SeerPrediction.encode(message.predictions[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerRecommend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerRecommend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerRecommend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.predictions && message.predictions.length))
                            message.predictions = [];
                        message.predictions.push($root.lq.SeerPrediction.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerRecommend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerRecommend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.predictions != null && message.hasOwnProperty("predictions")) {
                if (!Array.isArray(message.predictions))
                    return "predictions: array expected";
                for (let i = 0; i < message.predictions.length; ++i) {
                    let error = $root.lq.SeerPrediction.verify(message.predictions[i], _depth + 1);
                    if (error)
                        return "predictions." + error;
                }
            }
            return null;
        };

        SeerRecommend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerRecommend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerRecommend();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat | 0;
            if (object.predictions) {
                if (!Array.isArray(object.predictions))
                    throw TypeError(".lq.SeerRecommend.predictions: array expected");
                message.predictions = Array(object.predictions.length);
                for (let i = 0; i < object.predictions.length; ++i) {
                    if (typeof object.predictions[i] !== "object")
                        throw TypeError(".lq.SeerRecommend.predictions: object expected");
                    message.predictions[i] = $root.lq.SeerPrediction.fromObject(object.predictions[i], _depth + 1);
                }
            }
            return message;
        };

        SeerRecommend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.predictions = [];
            if (options.defaults)
                object.seat = 0;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.predictions && message.predictions.length) {
                object.predictions = Array(message.predictions.length);
                for (let j = 0; j < message.predictions.length; ++j)
                    object.predictions[j] = $root.lq.SeerPrediction.toObject(message.predictions[j], options);
            }
            return object;
        };

        SeerRecommend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerRecommend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerRecommend";
        };

        return SeerRecommend;
    })();

    lq.SeerPrediction = (function() {

        function SeerPrediction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerPrediction.prototype.action = 0;
        SeerPrediction.prototype.score = 0;

        SeerPrediction.create = function create(properties) {
            return new SeerPrediction(properties);
        };

        SeerPrediction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                writer.uint32(8).int32(message.action);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(16).int32(message.score);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerPrediction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerPrediction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerPrediction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.action = value;
                        else
                            delete message.action;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerPrediction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerPrediction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.action != null && message.hasOwnProperty("action"))
                if (!$util.isInteger(message.action))
                    return "action: integer expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            return null;
        };

        SeerPrediction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerPrediction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerPrediction();
            if (object.action != null)
                if (Number(object.action) !== 0)
                    message.action = object.action | 0;
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score | 0;
            return message;
        };

        SeerPrediction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.action = 0;
                object.score = 0;
            }
            if (message.action != null && message.hasOwnProperty("action"))
                object.action = message.action;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            return object;
        };

        SeerPrediction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerPrediction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerPrediction";
        };

        return SeerPrediction;
    })();

    lq.SeerRound = (function() {

        function SeerRound(properties) {
            this.player_scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerRound.prototype.chang = 0;
        SeerRound.prototype.ju = 0;
        SeerRound.prototype.ben = 0;
        SeerRound.prototype.player_scores = $util.emptyArray;

        SeerRound.create = function create(properties) {
            return new SeerRound(properties);
        };

        SeerRound.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(8).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(16).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(24).uint32(message.ben);
            if (message.player_scores != null && message.player_scores.length)
                for (let i = 0; i < message.player_scores.length; ++i)
                    $root.lq.SeerScore.encode(message.player_scores[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerRound.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerRound.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerRound(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_scores && message.player_scores.length))
                            message.player_scores = [];
                        message.player_scores.push($root.lq.SeerScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerRound.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerRound.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.player_scores != null && message.hasOwnProperty("player_scores")) {
                if (!Array.isArray(message.player_scores))
                    return "player_scores: array expected";
                for (let i = 0; i < message.player_scores.length; ++i) {
                    let error = $root.lq.SeerScore.verify(message.player_scores[i], _depth + 1);
                    if (error)
                        return "player_scores." + error;
                }
            }
            return null;
        };

        SeerRound.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerRound)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerRound();
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.player_scores) {
                if (!Array.isArray(object.player_scores))
                    throw TypeError(".lq.SeerRound.player_scores: array expected");
                message.player_scores = Array(object.player_scores.length);
                for (let i = 0; i < object.player_scores.length; ++i) {
                    if (typeof object.player_scores[i] !== "object")
                        throw TypeError(".lq.SeerRound.player_scores: object expected");
                    message.player_scores[i] = $root.lq.SeerScore.fromObject(object.player_scores[i], _depth + 1);
                }
            }
            return message;
        };

        SeerRound.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.player_scores = [];
            if (options.defaults) {
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
            }
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.player_scores && message.player_scores.length) {
                object.player_scores = Array(message.player_scores.length);
                for (let j = 0; j < message.player_scores.length; ++j)
                    object.player_scores[j] = $root.lq.SeerScore.toObject(message.player_scores[j], options);
            }
            return object;
        };

        SeerRound.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerRound.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerRound";
        };

        return SeerRound;
    })();

    lq.SeerScore = (function() {

        function SeerScore(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerScore.prototype.seat = 0;
        SeerScore.prototype.rating = 0;

        SeerScore.create = function create(properties) {
            return new SeerScore(properties);
        };

        SeerScore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.rating != null && Object.hasOwnProperty.call(message, "rating"))
                writer.uint32(16).uint32(message.rating);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerScore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerScore.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerScore(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rating = value;
                        else
                            delete message.rating;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerScore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerScore.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.rating != null && message.hasOwnProperty("rating"))
                if (!$util.isInteger(message.rating))
                    return "rating: integer expected";
            return null;
        };

        SeerScore.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerScore)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerScore();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.rating != null)
                if (Number(object.rating) !== 0)
                    message.rating = object.rating >>> 0;
            return message;
        };

        SeerScore.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.rating = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.rating != null && message.hasOwnProperty("rating"))
                object.rating = message.rating;
            return object;
        };

        SeerScore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerScore.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerScore";
        };

        return SeerScore;
    })();

    lq.SeerBrief = (function() {

        function SeerBrief(properties) {
            this.player_scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SeerBrief.prototype.uuid = "";
        SeerBrief.prototype.state = 0;
        SeerBrief.prototype.expire_time = 0;
        SeerBrief.prototype.player_scores = $util.emptyArray;
        SeerBrief.prototype.create_time = 0;

        SeerBrief.create = function create(properties) {
            return new SeerBrief(properties);
        };

        SeerBrief.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.expire_time != null && Object.hasOwnProperty.call(message, "expire_time"))
                writer.uint32(24).uint32(message.expire_time);
            if (message.player_scores != null && message.player_scores.length)
                for (let i = 0; i < message.player_scores.length; ++i)
                    $root.lq.SeerScore.encode(message.player_scores[i], writer.uint32(34).fork()).ldelim();
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(40).uint32(message.create_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SeerBrief.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SeerBrief.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SeerBrief(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expire_time = value;
                        else
                            delete message.expire_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_scores && message.player_scores.length))
                            message.player_scores = [];
                        message.player_scores.push($root.lq.SeerScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SeerBrief.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SeerBrief.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                if (!$util.isInteger(message.expire_time))
                    return "expire_time: integer expected";
            if (message.player_scores != null && message.hasOwnProperty("player_scores")) {
                if (!Array.isArray(message.player_scores))
                    return "player_scores: array expected";
                for (let i = 0; i < message.player_scores.length; ++i) {
                    let error = $root.lq.SeerScore.verify(message.player_scores[i], _depth + 1);
                    if (error)
                        return "player_scores." + error;
                }
            }
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            return null;
        };

        SeerBrief.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SeerBrief)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SeerBrief();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.expire_time != null)
                if (Number(object.expire_time) !== 0)
                    message.expire_time = object.expire_time >>> 0;
            if (object.player_scores) {
                if (!Array.isArray(object.player_scores))
                    throw TypeError(".lq.SeerBrief.player_scores: array expected");
                message.player_scores = Array(object.player_scores.length);
                for (let i = 0; i < object.player_scores.length; ++i) {
                    if (typeof object.player_scores[i] !== "object")
                        throw TypeError(".lq.SeerBrief.player_scores: object expected");
                    message.player_scores[i] = $root.lq.SeerScore.fromObject(object.player_scores[i], _depth + 1);
                }
            }
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            return message;
        };

        SeerBrief.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.player_scores = [];
            if (options.defaults) {
                object.uuid = "";
                object.state = 0;
                object.expire_time = 0;
                object.create_time = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                object.expire_time = message.expire_time;
            if (message.player_scores && message.player_scores.length) {
                object.player_scores = Array(message.player_scores.length);
                for (let j = 0; j < message.player_scores.length; ++j)
                    object.player_scores[j] = $root.lq.SeerScore.toObject(message.player_scores[j], options);
            }
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            return object;
        };

        SeerBrief.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SeerBrief.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SeerBrief";
        };

        return SeerBrief;
    })();

    lq.SimulationV2SeasonData = (function() {

        function SimulationV2SeasonData(properties) {
            this.effect_list = [];
            this.event_history = [];
            this.match_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2SeasonData.prototype.round = 0;
        SimulationV2SeasonData.prototype.ability = null;
        SimulationV2SeasonData.prototype.effect_list = $util.emptyArray;
        SimulationV2SeasonData.prototype.match = null;
        SimulationV2SeasonData.prototype.event = null;
        SimulationV2SeasonData.prototype.event_history = $util.emptyArray;
        SimulationV2SeasonData.prototype.record = null;
        SimulationV2SeasonData.prototype.total_score = 0;
        SimulationV2SeasonData.prototype.match_history = $util.emptyArray;

        SimulationV2SeasonData.create = function create(properties) {
            return new SimulationV2SeasonData(properties);
        };

        SimulationV2SeasonData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(8).uint32(message.round);
            if (message.ability != null && Object.hasOwnProperty.call(message, "ability"))
                $root.lq.SimulationV2Ability.encode(message.ability, writer.uint32(18).fork()).ldelim();
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.SimulationV2Effect.encode(message.effect_list[i], writer.uint32(26).fork()).ldelim();
            if (message.match != null && Object.hasOwnProperty.call(message, "match"))
                $root.lq.SimulationV2Match.encode(message.match, writer.uint32(34).fork()).ldelim();
            if (message.event != null && Object.hasOwnProperty.call(message, "event"))
                $root.lq.SimulationV2Event.encode(message.event, writer.uint32(42).fork()).ldelim();
            if (message.event_history != null && message.event_history.length)
                for (let i = 0; i < message.event_history.length; ++i)
                    $root.lq.SimulationV2EventHistory.encode(message.event_history[i], writer.uint32(50).fork()).ldelim();
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.SimulationV2Record.encode(message.record, writer.uint32(58).fork()).ldelim();
            if (message.total_score != null && Object.hasOwnProperty.call(message, "total_score"))
                writer.uint32(64).int32(message.total_score);
            if (message.match_history != null && message.match_history.length)
                for (let i = 0; i < message.match_history.length; ++i)
                    $root.lq.SimulationV2MatchRecord.encode(message.match_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2SeasonData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2SeasonData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2SeasonData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.ability = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.ability);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.SimulationV2Effect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.match = $root.lq.SimulationV2Match.decode(reader, reader.uint32(), undefined, _depth + 1, message.match);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.event = $root.lq.SimulationV2Event.decode(reader, reader.uint32(), undefined, _depth + 1, message.event);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.event_history && message.event_history.length))
                            message.event_history = [];
                        message.event_history.push($root.lq.SimulationV2EventHistory.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.SimulationV2Record.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.total_score = value;
                        else
                            delete message.total_score;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.match_history && message.match_history.length))
                            message.match_history = [];
                        message.match_history.push($root.lq.SimulationV2MatchRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2SeasonData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2SeasonData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.ability != null && message.hasOwnProperty("ability")) {
                let error = $root.lq.SimulationV2Ability.verify(message.ability, _depth + 1);
                if (error)
                    return "ability." + error;
            }
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.SimulationV2Effect.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.match != null && message.hasOwnProperty("match")) {
                let error = $root.lq.SimulationV2Match.verify(message.match, _depth + 1);
                if (error)
                    return "match." + error;
            }
            if (message.event != null && message.hasOwnProperty("event")) {
                let error = $root.lq.SimulationV2Event.verify(message.event, _depth + 1);
                if (error)
                    return "event." + error;
            }
            if (message.event_history != null && message.hasOwnProperty("event_history")) {
                if (!Array.isArray(message.event_history))
                    return "event_history: array expected";
                for (let i = 0; i < message.event_history.length; ++i) {
                    let error = $root.lq.SimulationV2EventHistory.verify(message.event_history[i], _depth + 1);
                    if (error)
                        return "event_history." + error;
                }
            }
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.SimulationV2Record.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.total_score != null && message.hasOwnProperty("total_score"))
                if (!$util.isInteger(message.total_score))
                    return "total_score: integer expected";
            if (message.match_history != null && message.hasOwnProperty("match_history")) {
                if (!Array.isArray(message.match_history))
                    return "match_history: array expected";
                for (let i = 0; i < message.match_history.length; ++i) {
                    let error = $root.lq.SimulationV2MatchRecord.verify(message.match_history[i], _depth + 1);
                    if (error)
                        return "match_history." + error;
                }
            }
            return null;
        };

        SimulationV2SeasonData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2SeasonData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2SeasonData();
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.ability != null) {
                if (typeof object.ability !== "object")
                    throw TypeError(".lq.SimulationV2SeasonData.ability: object expected");
                message.ability = $root.lq.SimulationV2Ability.fromObject(object.ability, _depth + 1);
            }
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.SimulationV2SeasonData.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.SimulationV2SeasonData.effect_list: object expected");
                    message.effect_list[i] = $root.lq.SimulationV2Effect.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.match != null) {
                if (typeof object.match !== "object")
                    throw TypeError(".lq.SimulationV2SeasonData.match: object expected");
                message.match = $root.lq.SimulationV2Match.fromObject(object.match, _depth + 1);
            }
            if (object.event != null) {
                if (typeof object.event !== "object")
                    throw TypeError(".lq.SimulationV2SeasonData.event: object expected");
                message.event = $root.lq.SimulationV2Event.fromObject(object.event, _depth + 1);
            }
            if (object.event_history) {
                if (!Array.isArray(object.event_history))
                    throw TypeError(".lq.SimulationV2SeasonData.event_history: array expected");
                message.event_history = Array(object.event_history.length);
                for (let i = 0; i < object.event_history.length; ++i) {
                    if (typeof object.event_history[i] !== "object")
                        throw TypeError(".lq.SimulationV2SeasonData.event_history: object expected");
                    message.event_history[i] = $root.lq.SimulationV2EventHistory.fromObject(object.event_history[i], _depth + 1);
                }
            }
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.SimulationV2SeasonData.record: object expected");
                message.record = $root.lq.SimulationV2Record.fromObject(object.record, _depth + 1);
            }
            if (object.total_score != null)
                if (Number(object.total_score) !== 0)
                    message.total_score = object.total_score | 0;
            if (object.match_history) {
                if (!Array.isArray(object.match_history))
                    throw TypeError(".lq.SimulationV2SeasonData.match_history: array expected");
                message.match_history = Array(object.match_history.length);
                for (let i = 0; i < object.match_history.length; ++i) {
                    if (typeof object.match_history[i] !== "object")
                        throw TypeError(".lq.SimulationV2SeasonData.match_history: object expected");
                    message.match_history[i] = $root.lq.SimulationV2MatchRecord.fromObject(object.match_history[i], _depth + 1);
                }
            }
            return message;
        };

        SimulationV2SeasonData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effect_list = [];
                object.event_history = [];
                object.match_history = [];
            }
            if (options.defaults) {
                object.round = 0;
                object.ability = null;
                object.match = null;
                object.event = null;
                object.record = null;
                object.total_score = 0;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.ability != null && message.hasOwnProperty("ability"))
                object.ability = $root.lq.SimulationV2Ability.toObject(message.ability, options);
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.SimulationV2Effect.toObject(message.effect_list[j], options);
            }
            if (message.match != null && message.hasOwnProperty("match"))
                object.match = $root.lq.SimulationV2Match.toObject(message.match, options);
            if (message.event != null && message.hasOwnProperty("event"))
                object.event = $root.lq.SimulationV2Event.toObject(message.event, options);
            if (message.event_history && message.event_history.length) {
                object.event_history = Array(message.event_history.length);
                for (let j = 0; j < message.event_history.length; ++j)
                    object.event_history[j] = $root.lq.SimulationV2EventHistory.toObject(message.event_history[j], options);
            }
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.SimulationV2Record.toObject(message.record, options);
            if (message.total_score != null && message.hasOwnProperty("total_score"))
                object.total_score = message.total_score;
            if (message.match_history && message.match_history.length) {
                object.match_history = Array(message.match_history.length);
                for (let j = 0; j < message.match_history.length; ++j)
                    object.match_history[j] = $root.lq.SimulationV2MatchRecord.toObject(message.match_history[j], options);
            }
            return object;
        };

        SimulationV2SeasonData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2SeasonData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2SeasonData";
        };

        return SimulationV2SeasonData;
    })();

    lq.SimulationV2PlayerRecord = (function() {

        function SimulationV2PlayerRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2PlayerRecord.prototype.id = 0;
        SimulationV2PlayerRecord.prototype.main = false;
        SimulationV2PlayerRecord.prototype.score = 0;
        SimulationV2PlayerRecord.prototype.rank = 0;
        SimulationV2PlayerRecord.prototype.seat = 0;

        SimulationV2PlayerRecord.create = function create(properties) {
            return new SimulationV2PlayerRecord(properties);
        };

        SimulationV2PlayerRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.main != null && Object.hasOwnProperty.call(message, "main"))
                writer.uint32(16).bool(message.main);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(24).int32(message.score);
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(32).uint32(message.rank);
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(40).uint32(message.seat);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2PlayerRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2PlayerRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2PlayerRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.main = value;
                        else
                            delete message.main;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2PlayerRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2PlayerRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.main != null && message.hasOwnProperty("main"))
                if (typeof message.main !== "boolean")
                    return "main: boolean expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank"))
                if (!$util.isInteger(message.rank))
                    return "rank: integer expected";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            return null;
        };

        SimulationV2PlayerRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2PlayerRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2PlayerRecord();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.main != null)
                if (object.main)
                    message.main = Boolean(object.main);
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score | 0;
            if (object.rank != null)
                if (Number(object.rank) !== 0)
                    message.rank = object.rank >>> 0;
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            return message;
        };

        SimulationV2PlayerRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.main = false;
                object.score = 0;
                object.rank = 0;
                object.seat = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.main != null && message.hasOwnProperty("main"))
                object.main = message.main;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.rank != null && message.hasOwnProperty("rank"))
                object.rank = message.rank;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            return object;
        };

        SimulationV2PlayerRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2PlayerRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2PlayerRecord";
        };

        return SimulationV2PlayerRecord;
    })();

    lq.SimulationV2MatchRecord = (function() {

        function SimulationV2MatchRecord(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2MatchRecord.prototype.players = $util.emptyArray;
        SimulationV2MatchRecord.prototype.round = 0;

        SimulationV2MatchRecord.create = function create(properties) {
            return new SimulationV2MatchRecord(properties);
        };

        SimulationV2MatchRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.SimulationV2PlayerRecord.encode(message.players[i], writer.uint32(10).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(16).uint32(message.round);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2MatchRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2MatchRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.SimulationV2PlayerRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2MatchRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2MatchRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.SimulationV2PlayerRecord.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            return null;
        };

        SimulationV2MatchRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2MatchRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2MatchRecord();
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.SimulationV2MatchRecord.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.SimulationV2MatchRecord.players: object expected");
                    message.players[i] = $root.lq.SimulationV2PlayerRecord.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            return message;
        };

        SimulationV2MatchRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (options.defaults)
                object.round = 0;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.SimulationV2PlayerRecord.toObject(message.players[j], options);
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            return object;
        };

        SimulationV2MatchRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2MatchRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2MatchRecord";
        };

        return SimulationV2MatchRecord;
    })();

    lq.SimulationV2EventHistory = (function() {

        function SimulationV2EventHistory(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2EventHistory.prototype.id = 0;
        SimulationV2EventHistory.prototype.round = 0;

        SimulationV2EventHistory.create = function create(properties) {
            return new SimulationV2EventHistory(properties);
        };

        SimulationV2EventHistory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(16).uint32(message.round);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2EventHistory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2EventHistory.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2EventHistory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2EventHistory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2EventHistory.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            return null;
        };

        SimulationV2EventHistory.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2EventHistory)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2EventHistory();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            return message;
        };

        SimulationV2EventHistory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.round = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            return object;
        };

        SimulationV2EventHistory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2EventHistory.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2EventHistory";
        };

        return SimulationV2EventHistory;
    })();

    lq.SimulationV2Event = (function() {

        function SimulationV2Event(properties) {
            this.selections = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Event.prototype.id = 0;
        SimulationV2Event.prototype.selections = $util.emptyArray;
        SimulationV2Event.prototype.next_round = 0;

        SimulationV2Event.create = function create(properties) {
            return new SimulationV2Event(properties);
        };

        SimulationV2Event.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.selections != null && message.selections.length)
                for (let i = 0; i < message.selections.length; ++i)
                    $root.lq.SimulationV2Event.SimulationV2EventSelection.encode(message.selections[i], writer.uint32(18).fork()).ldelim();
            if (message.next_round != null && Object.hasOwnProperty.call(message, "next_round"))
                writer.uint32(24).uint32(message.next_round);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Event.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Event.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Event(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.selections && message.selections.length))
                            message.selections = [];
                        message.selections.push($root.lq.SimulationV2Event.SimulationV2EventSelection.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.next_round = value;
                        else
                            delete message.next_round;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Event.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Event.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.selections != null && message.hasOwnProperty("selections")) {
                if (!Array.isArray(message.selections))
                    return "selections: array expected";
                for (let i = 0; i < message.selections.length; ++i) {
                    let error = $root.lq.SimulationV2Event.SimulationV2EventSelection.verify(message.selections[i], _depth + 1);
                    if (error)
                        return "selections." + error;
                }
            }
            if (message.next_round != null && message.hasOwnProperty("next_round"))
                if (!$util.isInteger(message.next_round))
                    return "next_round: integer expected";
            return null;
        };

        SimulationV2Event.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Event)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Event();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.selections) {
                if (!Array.isArray(object.selections))
                    throw TypeError(".lq.SimulationV2Event.selections: array expected");
                message.selections = Array(object.selections.length);
                for (let i = 0; i < object.selections.length; ++i) {
                    if (typeof object.selections[i] !== "object")
                        throw TypeError(".lq.SimulationV2Event.selections: object expected");
                    message.selections[i] = $root.lq.SimulationV2Event.SimulationV2EventSelection.fromObject(object.selections[i], _depth + 1);
                }
            }
            if (object.next_round != null)
                if (Number(object.next_round) !== 0)
                    message.next_round = object.next_round >>> 0;
            return message;
        };

        SimulationV2Event.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.selections = [];
            if (options.defaults) {
                object.id = 0;
                object.next_round = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.selections && message.selections.length) {
                object.selections = Array(message.selections.length);
                for (let j = 0; j < message.selections.length; ++j)
                    object.selections[j] = $root.lq.SimulationV2Event.SimulationV2EventSelection.toObject(message.selections[j], options);
            }
            if (message.next_round != null && message.hasOwnProperty("next_round"))
                object.next_round = message.next_round;
            return object;
        };

        SimulationV2Event.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Event.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Event";
        };

        SimulationV2Event.SimulationV2EventSelection = (function() {

            function SimulationV2EventSelection(properties) {
                this.results = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SimulationV2EventSelection.prototype.id = 0;
            SimulationV2EventSelection.prototype.results = $util.emptyArray;

            SimulationV2EventSelection.create = function create(properties) {
                return new SimulationV2EventSelection(properties);
            };

            SimulationV2EventSelection.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.results != null && message.results.length)
                    for (let i = 0; i < message.results.length; ++i)
                        $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SimulationV2EventSelection.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SimulationV2EventSelection.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Event.SimulationV2EventSelection(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.results && message.results.length))
                                message.results = [];
                            message.results.push($root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SimulationV2EventSelection.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SimulationV2EventSelection.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.results != null && message.hasOwnProperty("results")) {
                    if (!Array.isArray(message.results))
                        return "results: array expected";
                    for (let i = 0; i < message.results.length; ++i) {
                        let error = $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult.verify(message.results[i], _depth + 1);
                        if (error)
                            return "results." + error;
                    }
                }
                return null;
            };

            SimulationV2EventSelection.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2Event.SimulationV2EventSelection)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2Event.SimulationV2EventSelection();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.results) {
                    if (!Array.isArray(object.results))
                        throw TypeError(".lq.SimulationV2Event.SimulationV2EventSelection.results: array expected");
                    message.results = Array(object.results.length);
                    for (let i = 0; i < object.results.length; ++i) {
                        if (typeof object.results[i] !== "object")
                            throw TypeError(".lq.SimulationV2Event.SimulationV2EventSelection.results: object expected");
                        message.results[i] = $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult.fromObject(object.results[i], _depth + 1);
                    }
                }
                return message;
            };

            SimulationV2EventSelection.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.results = [];
                if (options.defaults)
                    object.id = 0;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.results && message.results.length) {
                    object.results = Array(message.results.length);
                    for (let j = 0; j < message.results.length; ++j)
                        object.results[j] = $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult.toObject(message.results[j], options);
                }
                return object;
            };

            SimulationV2EventSelection.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SimulationV2EventSelection.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2Event.SimulationV2EventSelection";
            };

            SimulationV2EventSelection.SimulationV2EventResult = (function() {

                function SimulationV2EventResult(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                SimulationV2EventResult.prototype.id = 0;
                SimulationV2EventResult.prototype.weight = 0;

                SimulationV2EventResult.create = function create(properties) {
                    return new SimulationV2EventResult(properties);
                };

                SimulationV2EventResult.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(8).uint32(message.id);
                    if (message.weight != null && Object.hasOwnProperty.call(message, "weight"))
                        writer.uint32(16).uint32(message.weight);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                SimulationV2EventResult.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                SimulationV2EventResult.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.id = value;
                                else
                                    delete message.id;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.weight = value;
                                else
                                    delete message.weight;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                SimulationV2EventResult.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                SimulationV2EventResult.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.id != null && message.hasOwnProperty("id"))
                        if (!$util.isInteger(message.id))
                            return "id: integer expected";
                    if (message.weight != null && message.hasOwnProperty("weight"))
                        if (!$util.isInteger(message.weight))
                            return "weight: integer expected";
                    return null;
                };

                SimulationV2EventResult.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult();
                    if (object.id != null)
                        if (Number(object.id) !== 0)
                            message.id = object.id >>> 0;
                    if (object.weight != null)
                        if (Number(object.weight) !== 0)
                            message.weight = object.weight >>> 0;
                    return message;
                };

                SimulationV2EventResult.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.id = 0;
                        object.weight = 0;
                    }
                    if (message.id != null && message.hasOwnProperty("id"))
                        object.id = message.id;
                    if (message.weight != null && message.hasOwnProperty("weight"))
                        object.weight = message.weight;
                    return object;
                };

                SimulationV2EventResult.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                SimulationV2EventResult.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.SimulationV2Event.SimulationV2EventSelection.SimulationV2EventResult";
                };

                return SimulationV2EventResult;
            })();

            return SimulationV2EventSelection;
        })();

        return SimulationV2Event;
    })();

    lq.SimulationV2Ability = (function() {

        function SimulationV2Ability(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Ability.prototype.luk = 0;
        SimulationV2Ability.prototype.tec = 0;
        SimulationV2Ability.prototype.ins = 0;
        SimulationV2Ability.prototype.int = 0;
        SimulationV2Ability.prototype.res = 0;

        SimulationV2Ability.create = function create(properties) {
            return new SimulationV2Ability(properties);
        };

        SimulationV2Ability.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.luk != null && Object.hasOwnProperty.call(message, "luk"))
                writer.uint32(8).uint32(message.luk);
            if (message.tec != null && Object.hasOwnProperty.call(message, "tec"))
                writer.uint32(16).uint32(message.tec);
            if (message.ins != null && Object.hasOwnProperty.call(message, "ins"))
                writer.uint32(24).uint32(message.ins);
            if (message.int != null && Object.hasOwnProperty.call(message, "int"))
                writer.uint32(32).uint32(message.int);
            if (message.res != null && Object.hasOwnProperty.call(message, "res"))
                writer.uint32(40).uint32(message.res);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Ability.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Ability.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Ability(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.luk = value;
                        else
                            delete message.luk;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tec = value;
                        else
                            delete message.tec;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ins = value;
                        else
                            delete message.ins;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.int = value;
                        else
                            delete message.int;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.res = value;
                        else
                            delete message.res;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Ability.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Ability.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.luk != null && message.hasOwnProperty("luk"))
                if (!$util.isInteger(message.luk))
                    return "luk: integer expected";
            if (message.tec != null && message.hasOwnProperty("tec"))
                if (!$util.isInteger(message.tec))
                    return "tec: integer expected";
            if (message.ins != null && message.hasOwnProperty("ins"))
                if (!$util.isInteger(message.ins))
                    return "ins: integer expected";
            if (message.int != null && message.hasOwnProperty("int"))
                if (!$util.isInteger(message.int))
                    return "int: integer expected";
            if (message.res != null && message.hasOwnProperty("res"))
                if (!$util.isInteger(message.res))
                    return "res: integer expected";
            return null;
        };

        SimulationV2Ability.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Ability)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Ability();
            if (object.luk != null)
                if (Number(object.luk) !== 0)
                    message.luk = object.luk >>> 0;
            if (object.tec != null)
                if (Number(object.tec) !== 0)
                    message.tec = object.tec >>> 0;
            if (object.ins != null)
                if (Number(object.ins) !== 0)
                    message.ins = object.ins >>> 0;
            if (object.int != null)
                if (Number(object.int) !== 0)
                    message.int = object.int >>> 0;
            if (object.res != null)
                if (Number(object.res) !== 0)
                    message.res = object.res >>> 0;
            return message;
        };

        SimulationV2Ability.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.luk = 0;
                object.tec = 0;
                object.ins = 0;
                object.int = 0;
                object.res = 0;
            }
            if (message.luk != null && message.hasOwnProperty("luk"))
                object.luk = message.luk;
            if (message.tec != null && message.hasOwnProperty("tec"))
                object.tec = message.tec;
            if (message.ins != null && message.hasOwnProperty("ins"))
                object.ins = message.ins;
            if (message.int != null && message.hasOwnProperty("int"))
                object.int = message.int;
            if (message.res != null && message.hasOwnProperty("res"))
                object.res = message.res;
            return object;
        };

        SimulationV2Ability.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Ability.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Ability";
        };

        return SimulationV2Ability;
    })();

    lq.SimulationV2Buff = (function() {

        function SimulationV2Buff(properties) {
            this.store = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Buff.prototype.id = 0;
        SimulationV2Buff.prototype.round = 0;
        SimulationV2Buff.prototype.store = $util.emptyArray;

        SimulationV2Buff.create = function create(properties) {
            return new SimulationV2Buff(properties);
        };

        SimulationV2Buff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(16).uint32(message.round);
            if (message.store != null && message.store.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.store.length; ++i)
                    writer.uint32(message.store[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Buff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Buff.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Buff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.store && message.store.length))
                                message.store = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.store.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.store && message.store.length))
                            message.store = [];
                        message.store.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Buff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Buff.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.store != null && message.hasOwnProperty("store")) {
                if (!Array.isArray(message.store))
                    return "store: array expected";
                for (let i = 0; i < message.store.length; ++i)
                    if (!$util.isInteger(message.store[i]))
                        return "store: integer[] expected";
            }
            return null;
        };

        SimulationV2Buff.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Buff)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Buff();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.store) {
                if (!Array.isArray(object.store))
                    throw TypeError(".lq.SimulationV2Buff.store: array expected");
                message.store = Array(object.store.length);
                for (let i = 0; i < object.store.length; ++i)
                    message.store[i] = object.store[i] >>> 0;
            }
            return message;
        };

        SimulationV2Buff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.store = [];
            if (options.defaults) {
                object.id = 0;
                object.round = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.store && message.store.length) {
                object.store = Array(message.store.length);
                for (let j = 0; j < message.store.length; ++j)
                    object.store[j] = message.store[j];
            }
            return object;
        };

        SimulationV2Buff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Buff.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Buff";
        };

        return SimulationV2Buff;
    })();

    lq.SimulationV2Effect = (function() {

        function SimulationV2Effect(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Effect.prototype.id = 0;

        SimulationV2Effect.create = function create(properties) {
            return new SimulationV2Effect(properties);
        };

        SimulationV2Effect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Effect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Effect.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Effect(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Effect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Effect.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        SimulationV2Effect.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Effect)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Effect();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        SimulationV2Effect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        SimulationV2Effect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Effect.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Effect";
        };

        return SimulationV2Effect;
    })();

    lq.SimulationV2MatchInfo = (function() {

        function SimulationV2MatchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2MatchInfo.prototype.chang = 0;
        SimulationV2MatchInfo.prototype.ju = 0;
        SimulationV2MatchInfo.prototype.ben = 0;
        SimulationV2MatchInfo.prototype.gong = 0;
        SimulationV2MatchInfo.prototype.remain = 0;

        SimulationV2MatchInfo.create = function create(properties) {
            return new SimulationV2MatchInfo(properties);
        };

        SimulationV2MatchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(8).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(16).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(24).uint32(message.ben);
            if (message.gong != null && Object.hasOwnProperty.call(message, "gong"))
                writer.uint32(32).uint32(message.gong);
            if (message.remain != null && Object.hasOwnProperty.call(message, "remain"))
                writer.uint32(40).uint32(message.remain);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2MatchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2MatchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gong = value;
                        else
                            delete message.gong;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain = value;
                        else
                            delete message.remain;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2MatchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2MatchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.gong != null && message.hasOwnProperty("gong"))
                if (!$util.isInteger(message.gong))
                    return "gong: integer expected";
            if (message.remain != null && message.hasOwnProperty("remain"))
                if (!$util.isInteger(message.remain))
                    return "remain: integer expected";
            return null;
        };

        SimulationV2MatchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2MatchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2MatchInfo();
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.gong != null)
                if (Number(object.gong) !== 0)
                    message.gong = object.gong >>> 0;
            if (object.remain != null)
                if (Number(object.remain) !== 0)
                    message.remain = object.remain >>> 0;
            return message;
        };

        SimulationV2MatchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
                object.gong = 0;
                object.remain = 0;
            }
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.gong != null && message.hasOwnProperty("gong"))
                object.gong = message.gong;
            if (message.remain != null && message.hasOwnProperty("remain"))
                object.remain = message.remain;
            return object;
        };

        SimulationV2MatchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2MatchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2MatchInfo";
        };

        return SimulationV2MatchInfo;
    })();

    lq.SimulationV2Record = (function() {

        function SimulationV2Record(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Record.prototype.hu_count = 0;
        SimulationV2Record.prototype.chong_count = 0;
        SimulationV2Record.prototype.highest_hu = 0;
        SimulationV2Record.prototype.rank = $util.emptyArray;
        SimulationV2Record.prototype.round_count = 0;

        SimulationV2Record.create = function create(properties) {
            return new SimulationV2Record(properties);
        };

        SimulationV2Record.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hu_count != null && Object.hasOwnProperty.call(message, "hu_count"))
                writer.uint32(8).uint32(message.hu_count);
            if (message.chong_count != null && Object.hasOwnProperty.call(message, "chong_count"))
                writer.uint32(16).uint32(message.chong_count);
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                writer.uint32(24).uint32(message.highest_hu);
            if (message.rank != null && message.rank.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.rank.length; ++i)
                    writer.uint32(message.rank[i]);
                writer.ldelim();
            }
            if (message.round_count != null && Object.hasOwnProperty.call(message, "round_count"))
                writer.uint32(40).uint32(message.round_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Record.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Record.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Record(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.hu_count = value;
                        else
                            delete message.hu_count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chong_count = value;
                        else
                            delete message.chong_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.highest_hu = value;
                        else
                            delete message.highest_hu;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.rank && message.rank.length))
                                message.rank = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rank.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round_count = value;
                        else
                            delete message.round_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Record.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Record.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hu_count != null && message.hasOwnProperty("hu_count"))
                if (!$util.isInteger(message.hu_count))
                    return "hu_count: integer expected";
            if (message.chong_count != null && message.hasOwnProperty("chong_count"))
                if (!$util.isInteger(message.chong_count))
                    return "chong_count: integer expected";
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                if (!$util.isInteger(message.highest_hu))
                    return "highest_hu: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i)
                    if (!$util.isInteger(message.rank[i]))
                        return "rank: integer[] expected";
            }
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                if (!$util.isInteger(message.round_count))
                    return "round_count: integer expected";
            return null;
        };

        SimulationV2Record.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Record)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Record();
            if (object.hu_count != null)
                if (Number(object.hu_count) !== 0)
                    message.hu_count = object.hu_count >>> 0;
            if (object.chong_count != null)
                if (Number(object.chong_count) !== 0)
                    message.chong_count = object.chong_count >>> 0;
            if (object.highest_hu != null)
                if (Number(object.highest_hu) !== 0)
                    message.highest_hu = object.highest_hu >>> 0;
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.SimulationV2Record.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i)
                    message.rank[i] = object.rank[i] >>> 0;
            }
            if (object.round_count != null)
                if (Number(object.round_count) !== 0)
                    message.round_count = object.round_count >>> 0;
            return message;
        };

        SimulationV2Record.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults) {
                object.hu_count = 0;
                object.chong_count = 0;
                object.highest_hu = 0;
                object.round_count = 0;
            }
            if (message.hu_count != null && message.hasOwnProperty("hu_count"))
                object.hu_count = message.hu_count;
            if (message.chong_count != null && message.hasOwnProperty("chong_count"))
                object.chong_count = message.chong_count;
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = message.highest_hu;
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = message.rank[j];
            }
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                object.round_count = message.round_count;
            return object;
        };

        SimulationV2Record.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Record.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Record";
        };

        return SimulationV2Record;
    })();

    lq.SimulationV2MatchHistory = (function() {

        function SimulationV2MatchHistory(properties) {
            this.score_modify = [];
            this.hule = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2MatchHistory.prototype.type = 0;
        SimulationV2MatchHistory.prototype.remain = 0;
        SimulationV2MatchHistory.prototype.score_modify = $util.emptyArray;
        SimulationV2MatchHistory.prototype.round_start = null;
        SimulationV2MatchHistory.prototype.riichi = null;
        SimulationV2MatchHistory.prototype.fulu = null;
        SimulationV2MatchHistory.prototype.hule = $util.emptyArray;
        SimulationV2MatchHistory.prototype.push_ting = null;
        SimulationV2MatchHistory.prototype.find_ting = null;
        SimulationV2MatchHistory.prototype.liuju = null;
        SimulationV2MatchHistory.prototype.story = null;

        SimulationV2MatchHistory.create = function create(properties) {
            return new SimulationV2MatchHistory(properties);
        };

        SimulationV2MatchHistory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.remain != null && Object.hasOwnProperty.call(message, "remain"))
                writer.uint32(16).uint32(message.remain);
            if (message.score_modify != null && message.score_modify.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.score_modify.length; ++i)
                    writer.int32(message.score_modify[i]);
                writer.ldelim();
            }
            if (message.round_start != null && Object.hasOwnProperty.call(message, "round_start"))
                $root.lq.SimulationV2MatchHistory.RoundStartArgs.encode(message.round_start, writer.uint32(34).fork()).ldelim();
            if (message.riichi != null && Object.hasOwnProperty.call(message, "riichi"))
                $root.lq.SimulationV2MatchHistory.RiichiArgs.encode(message.riichi, writer.uint32(42).fork()).ldelim();
            if (message.fulu != null && Object.hasOwnProperty.call(message, "fulu"))
                $root.lq.SimulationV2MatchHistory.FuluArgs.encode(message.fulu, writer.uint32(50).fork()).ldelim();
            if (message.hule != null && message.hule.length)
                for (let i = 0; i < message.hule.length; ++i)
                    $root.lq.SimulationV2MatchHistory.HuleArgs.encode(message.hule[i], writer.uint32(58).fork()).ldelim();
            if (message.push_ting != null && Object.hasOwnProperty.call(message, "push_ting"))
                $root.lq.SimulationV2MatchHistory.PushTingArgs.encode(message.push_ting, writer.uint32(66).fork()).ldelim();
            if (message.find_ting != null && Object.hasOwnProperty.call(message, "find_ting"))
                $root.lq.SimulationV2MatchHistory.FindTingArgs.encode(message.find_ting, writer.uint32(74).fork()).ldelim();
            if (message.liuju != null && Object.hasOwnProperty.call(message, "liuju"))
                $root.lq.SimulationV2MatchHistory.LiujuArgs.encode(message.liuju, writer.uint32(82).fork()).ldelim();
            if (message.story != null && Object.hasOwnProperty.call(message, "story"))
                $root.lq.SimulationV2MatchHistory.StoryArgs.encode(message.story, writer.uint32(90).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2MatchHistory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2MatchHistory.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain = value;
                        else
                            delete message.remain;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.score_modify && message.score_modify.length))
                                message.score_modify = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.score_modify.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.score_modify && message.score_modify.length))
                            message.score_modify = [];
                        message.score_modify.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.round_start = $root.lq.SimulationV2MatchHistory.RoundStartArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.round_start);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.riichi = $root.lq.SimulationV2MatchHistory.RiichiArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.riichi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.fulu = $root.lq.SimulationV2MatchHistory.FuluArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.fulu);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hule && message.hule.length))
                            message.hule = [];
                        message.hule.push($root.lq.SimulationV2MatchHistory.HuleArgs.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.push_ting = $root.lq.SimulationV2MatchHistory.PushTingArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.push_ting);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.find_ting = $root.lq.SimulationV2MatchHistory.FindTingArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.find_ting);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.liuju = $root.lq.SimulationV2MatchHistory.LiujuArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.liuju);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.story = $root.lq.SimulationV2MatchHistory.StoryArgs.decode(reader, reader.uint32(), undefined, _depth + 1, message.story);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2MatchHistory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2MatchHistory.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.remain != null && message.hasOwnProperty("remain"))
                if (!$util.isInteger(message.remain))
                    return "remain: integer expected";
            if (message.score_modify != null && message.hasOwnProperty("score_modify")) {
                if (!Array.isArray(message.score_modify))
                    return "score_modify: array expected";
                for (let i = 0; i < message.score_modify.length; ++i)
                    if (!$util.isInteger(message.score_modify[i]))
                        return "score_modify: integer[] expected";
            }
            if (message.round_start != null && message.hasOwnProperty("round_start")) {
                let error = $root.lq.SimulationV2MatchHistory.RoundStartArgs.verify(message.round_start, _depth + 1);
                if (error)
                    return "round_start." + error;
            }
            if (message.riichi != null && message.hasOwnProperty("riichi")) {
                let error = $root.lq.SimulationV2MatchHistory.RiichiArgs.verify(message.riichi, _depth + 1);
                if (error)
                    return "riichi." + error;
            }
            if (message.fulu != null && message.hasOwnProperty("fulu")) {
                let error = $root.lq.SimulationV2MatchHistory.FuluArgs.verify(message.fulu, _depth + 1);
                if (error)
                    return "fulu." + error;
            }
            if (message.hule != null && message.hasOwnProperty("hule")) {
                if (!Array.isArray(message.hule))
                    return "hule: array expected";
                for (let i = 0; i < message.hule.length; ++i) {
                    let error = $root.lq.SimulationV2MatchHistory.HuleArgs.verify(message.hule[i], _depth + 1);
                    if (error)
                        return "hule." + error;
                }
            }
            if (message.push_ting != null && message.hasOwnProperty("push_ting")) {
                let error = $root.lq.SimulationV2MatchHistory.PushTingArgs.verify(message.push_ting, _depth + 1);
                if (error)
                    return "push_ting." + error;
            }
            if (message.find_ting != null && message.hasOwnProperty("find_ting")) {
                let error = $root.lq.SimulationV2MatchHistory.FindTingArgs.verify(message.find_ting, _depth + 1);
                if (error)
                    return "find_ting." + error;
            }
            if (message.liuju != null && message.hasOwnProperty("liuju")) {
                let error = $root.lq.SimulationV2MatchHistory.LiujuArgs.verify(message.liuju, _depth + 1);
                if (error)
                    return "liuju." + error;
            }
            if (message.story != null && message.hasOwnProperty("story")) {
                let error = $root.lq.SimulationV2MatchHistory.StoryArgs.verify(message.story, _depth + 1);
                if (error)
                    return "story." + error;
            }
            return null;
        };

        SimulationV2MatchHistory.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2MatchHistory)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2MatchHistory();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.remain != null)
                if (Number(object.remain) !== 0)
                    message.remain = object.remain >>> 0;
            if (object.score_modify) {
                if (!Array.isArray(object.score_modify))
                    throw TypeError(".lq.SimulationV2MatchHistory.score_modify: array expected");
                message.score_modify = Array(object.score_modify.length);
                for (let i = 0; i < object.score_modify.length; ++i)
                    message.score_modify[i] = object.score_modify[i] | 0;
            }
            if (object.round_start != null) {
                if (typeof object.round_start !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.round_start: object expected");
                message.round_start = $root.lq.SimulationV2MatchHistory.RoundStartArgs.fromObject(object.round_start, _depth + 1);
            }
            if (object.riichi != null) {
                if (typeof object.riichi !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.riichi: object expected");
                message.riichi = $root.lq.SimulationV2MatchHistory.RiichiArgs.fromObject(object.riichi, _depth + 1);
            }
            if (object.fulu != null) {
                if (typeof object.fulu !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.fulu: object expected");
                message.fulu = $root.lq.SimulationV2MatchHistory.FuluArgs.fromObject(object.fulu, _depth + 1);
            }
            if (object.hule) {
                if (!Array.isArray(object.hule))
                    throw TypeError(".lq.SimulationV2MatchHistory.hule: array expected");
                message.hule = Array(object.hule.length);
                for (let i = 0; i < object.hule.length; ++i) {
                    if (typeof object.hule[i] !== "object")
                        throw TypeError(".lq.SimulationV2MatchHistory.hule: object expected");
                    message.hule[i] = $root.lq.SimulationV2MatchHistory.HuleArgs.fromObject(object.hule[i], _depth + 1);
                }
            }
            if (object.push_ting != null) {
                if (typeof object.push_ting !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.push_ting: object expected");
                message.push_ting = $root.lq.SimulationV2MatchHistory.PushTingArgs.fromObject(object.push_ting, _depth + 1);
            }
            if (object.find_ting != null) {
                if (typeof object.find_ting !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.find_ting: object expected");
                message.find_ting = $root.lq.SimulationV2MatchHistory.FindTingArgs.fromObject(object.find_ting, _depth + 1);
            }
            if (object.liuju != null) {
                if (typeof object.liuju !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.liuju: object expected");
                message.liuju = $root.lq.SimulationV2MatchHistory.LiujuArgs.fromObject(object.liuju, _depth + 1);
            }
            if (object.story != null) {
                if (typeof object.story !== "object")
                    throw TypeError(".lq.SimulationV2MatchHistory.story: object expected");
                message.story = $root.lq.SimulationV2MatchHistory.StoryArgs.fromObject(object.story, _depth + 1);
            }
            return message;
        };

        SimulationV2MatchHistory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.score_modify = [];
                object.hule = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.remain = 0;
                object.round_start = null;
                object.riichi = null;
                object.fulu = null;
                object.push_ting = null;
                object.find_ting = null;
                object.liuju = null;
                object.story = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.remain != null && message.hasOwnProperty("remain"))
                object.remain = message.remain;
            if (message.score_modify && message.score_modify.length) {
                object.score_modify = Array(message.score_modify.length);
                for (let j = 0; j < message.score_modify.length; ++j)
                    object.score_modify[j] = message.score_modify[j];
            }
            if (message.round_start != null && message.hasOwnProperty("round_start"))
                object.round_start = $root.lq.SimulationV2MatchHistory.RoundStartArgs.toObject(message.round_start, options);
            if (message.riichi != null && message.hasOwnProperty("riichi"))
                object.riichi = $root.lq.SimulationV2MatchHistory.RiichiArgs.toObject(message.riichi, options);
            if (message.fulu != null && message.hasOwnProperty("fulu"))
                object.fulu = $root.lq.SimulationV2MatchHistory.FuluArgs.toObject(message.fulu, options);
            if (message.hule && message.hule.length) {
                object.hule = Array(message.hule.length);
                for (let j = 0; j < message.hule.length; ++j)
                    object.hule[j] = $root.lq.SimulationV2MatchHistory.HuleArgs.toObject(message.hule[j], options);
            }
            if (message.push_ting != null && message.hasOwnProperty("push_ting"))
                object.push_ting = $root.lq.SimulationV2MatchHistory.PushTingArgs.toObject(message.push_ting, options);
            if (message.find_ting != null && message.hasOwnProperty("find_ting"))
                object.find_ting = $root.lq.SimulationV2MatchHistory.FindTingArgs.toObject(message.find_ting, options);
            if (message.liuju != null && message.hasOwnProperty("liuju"))
                object.liuju = $root.lq.SimulationV2MatchHistory.LiujuArgs.toObject(message.liuju, options);
            if (message.story != null && message.hasOwnProperty("story"))
                object.story = $root.lq.SimulationV2MatchHistory.StoryArgs.toObject(message.story, options);
            return object;
        };

        SimulationV2MatchHistory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2MatchHistory.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2MatchHistory";
        };

        SimulationV2MatchHistory.RoundStartArgs = (function() {

            function RoundStartArgs(properties) {
                this.scores = [];
                this.effected_buff_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RoundStartArgs.prototype.info = null;
            RoundStartArgs.prototype.scores = $util.emptyArray;
            RoundStartArgs.prototype.ting = 0;
            RoundStartArgs.prototype.effected_buff_list = $util.emptyArray;

            RoundStartArgs.create = function create(properties) {
                return new RoundStartArgs(properties);
            };

            RoundStartArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                    $root.lq.SimulationV2MatchInfo.encode(message.info, writer.uint32(10).fork()).ldelim();
                if (message.scores != null && message.scores.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.scores.length; ++i)
                        writer.int32(message.scores[i]);
                    writer.ldelim();
                }
                if (message.ting != null && Object.hasOwnProperty.call(message, "ting"))
                    writer.uint32(24).uint32(message.ting);
                if (message.effected_buff_list != null && message.effected_buff_list.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.effected_buff_list.length; ++i)
                        writer.uint32(message.effected_buff_list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RoundStartArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RoundStartArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.RoundStartArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.info = $root.lq.SimulationV2MatchInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.info);
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.scores && message.scores.length))
                                    message.scores = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.scores.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            message.scores.push(reader.int32());
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ting = value;
                            else
                                delete message.ting;
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.effected_buff_list && message.effected_buff_list.length))
                                    message.effected_buff_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.effected_buff_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.effected_buff_list && message.effected_buff_list.length))
                                message.effected_buff_list = [];
                            message.effected_buff_list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RoundStartArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RoundStartArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.info != null && message.hasOwnProperty("info")) {
                    let error = $root.lq.SimulationV2MatchInfo.verify(message.info, _depth + 1);
                    if (error)
                        return "info." + error;
                }
                if (message.scores != null && message.hasOwnProperty("scores")) {
                    if (!Array.isArray(message.scores))
                        return "scores: array expected";
                    for (let i = 0; i < message.scores.length; ++i)
                        if (!$util.isInteger(message.scores[i]))
                            return "scores: integer[] expected";
                }
                if (message.ting != null && message.hasOwnProperty("ting"))
                    if (!$util.isInteger(message.ting))
                        return "ting: integer expected";
                if (message.effected_buff_list != null && message.hasOwnProperty("effected_buff_list")) {
                    if (!Array.isArray(message.effected_buff_list))
                        return "effected_buff_list: array expected";
                    for (let i = 0; i < message.effected_buff_list.length; ++i)
                        if (!$util.isInteger(message.effected_buff_list[i]))
                            return "effected_buff_list: integer[] expected";
                }
                return null;
            };

            RoundStartArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.RoundStartArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.RoundStartArgs();
                if (object.info != null) {
                    if (typeof object.info !== "object")
                        throw TypeError(".lq.SimulationV2MatchHistory.RoundStartArgs.info: object expected");
                    message.info = $root.lq.SimulationV2MatchInfo.fromObject(object.info, _depth + 1);
                }
                if (object.scores) {
                    if (!Array.isArray(object.scores))
                        throw TypeError(".lq.SimulationV2MatchHistory.RoundStartArgs.scores: array expected");
                    message.scores = Array(object.scores.length);
                    for (let i = 0; i < object.scores.length; ++i)
                        message.scores[i] = object.scores[i] | 0;
                }
                if (object.ting != null)
                    if (Number(object.ting) !== 0)
                        message.ting = object.ting >>> 0;
                if (object.effected_buff_list) {
                    if (!Array.isArray(object.effected_buff_list))
                        throw TypeError(".lq.SimulationV2MatchHistory.RoundStartArgs.effected_buff_list: array expected");
                    message.effected_buff_list = Array(object.effected_buff_list.length);
                    for (let i = 0; i < object.effected_buff_list.length; ++i)
                        message.effected_buff_list[i] = object.effected_buff_list[i] >>> 0;
                }
                return message;
            };

            RoundStartArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.scores = [];
                    object.effected_buff_list = [];
                }
                if (options.defaults) {
                    object.info = null;
                    object.ting = 0;
                }
                if (message.info != null && message.hasOwnProperty("info"))
                    object.info = $root.lq.SimulationV2MatchInfo.toObject(message.info, options);
                if (message.scores && message.scores.length) {
                    object.scores = Array(message.scores.length);
                    for (let j = 0; j < message.scores.length; ++j)
                        object.scores[j] = message.scores[j];
                }
                if (message.ting != null && message.hasOwnProperty("ting"))
                    object.ting = message.ting;
                if (message.effected_buff_list && message.effected_buff_list.length) {
                    object.effected_buff_list = Array(message.effected_buff_list.length);
                    for (let j = 0; j < message.effected_buff_list.length; ++j)
                        object.effected_buff_list[j] = message.effected_buff_list[j];
                }
                return object;
            };

            RoundStartArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RoundStartArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.RoundStartArgs";
            };

            return RoundStartArgs;
        })();

        SimulationV2MatchHistory.RiichiArgs = (function() {

            function RiichiArgs(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RiichiArgs.prototype.seat = 0;

            RiichiArgs.create = function create(properties) {
                return new RiichiArgs(properties);
            };

            RiichiArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RiichiArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RiichiArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.RiichiArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RiichiArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RiichiArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                return null;
            };

            RiichiArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.RiichiArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.RiichiArgs();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                return message;
            };

            RiichiArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                return object;
            };

            RiichiArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RiichiArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.RiichiArgs";
            };

            return RiichiArgs;
        })();

        SimulationV2MatchHistory.FuluArgs = (function() {

            function FuluArgs(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FuluArgs.prototype.seat = 0;
            FuluArgs.prototype.ting = 0;
            FuluArgs.prototype.fulu = 0;

            FuluArgs.create = function create(properties) {
                return new FuluArgs(properties);
            };

            FuluArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.ting != null && Object.hasOwnProperty.call(message, "ting"))
                    writer.uint32(16).uint32(message.ting);
                if (message.fulu != null && Object.hasOwnProperty.call(message, "fulu"))
                    writer.uint32(24).uint32(message.fulu);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FuluArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FuluArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.FuluArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ting = value;
                            else
                                delete message.ting;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.fulu = value;
                            else
                                delete message.fulu;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FuluArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FuluArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.ting != null && message.hasOwnProperty("ting"))
                    if (!$util.isInteger(message.ting))
                        return "ting: integer expected";
                if (message.fulu != null && message.hasOwnProperty("fulu"))
                    if (!$util.isInteger(message.fulu))
                        return "fulu: integer expected";
                return null;
            };

            FuluArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.FuluArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.FuluArgs();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.ting != null)
                    if (Number(object.ting) !== 0)
                        message.ting = object.ting >>> 0;
                if (object.fulu != null)
                    if (Number(object.fulu) !== 0)
                        message.fulu = object.fulu >>> 0;
                return message;
            };

            FuluArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.ting = 0;
                    object.fulu = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.ting != null && message.hasOwnProperty("ting"))
                    object.ting = message.ting;
                if (message.fulu != null && message.hasOwnProperty("fulu"))
                    object.fulu = message.fulu;
                return object;
            };

            FuluArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FuluArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.FuluArgs";
            };

            return FuluArgs;
        })();

        SimulationV2MatchHistory.HuleArgs = (function() {

            function HuleArgs(properties) {
                this.score_modify = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            HuleArgs.prototype.seat = 0;
            HuleArgs.prototype.zimo = false;
            HuleArgs.prototype.chong_seat = 0;
            HuleArgs.prototype.point = 0;
            HuleArgs.prototype.fan = 0;
            HuleArgs.prototype.score_modify = $util.emptyArray;

            HuleArgs.create = function create(properties) {
                return new HuleArgs(properties);
            };

            HuleArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.zimo != null && Object.hasOwnProperty.call(message, "zimo"))
                    writer.uint32(16).bool(message.zimo);
                if (message.chong_seat != null && Object.hasOwnProperty.call(message, "chong_seat"))
                    writer.uint32(24).uint32(message.chong_seat);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(32).int32(message.point);
                if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                    writer.uint32(40).int32(message.fan);
                if (message.score_modify != null && message.score_modify.length) {
                    writer.uint32(50).fork();
                    for (let i = 0; i < message.score_modify.length; ++i)
                        writer.int32(message.score_modify[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            HuleArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            HuleArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.HuleArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.zimo = value;
                            else
                                delete message.zimo;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chong_seat = value;
                            else
                                delete message.chong_seat;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.fan = value;
                            else
                                delete message.fan;
                            continue;
                        }
                    case 6: {
                            if (wireType === 2) {
                                if (!(message.score_modify && message.score_modify.length))
                                    message.score_modify = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.score_modify.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.score_modify && message.score_modify.length))
                                message.score_modify = [];
                            message.score_modify.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            HuleArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            HuleArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.zimo != null && message.hasOwnProperty("zimo"))
                    if (typeof message.zimo !== "boolean")
                        return "zimo: boolean expected";
                if (message.chong_seat != null && message.hasOwnProperty("chong_seat"))
                    if (!$util.isInteger(message.chong_seat))
                        return "chong_seat: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                if (message.fan != null && message.hasOwnProperty("fan"))
                    if (!$util.isInteger(message.fan))
                        return "fan: integer expected";
                if (message.score_modify != null && message.hasOwnProperty("score_modify")) {
                    if (!Array.isArray(message.score_modify))
                        return "score_modify: array expected";
                    for (let i = 0; i < message.score_modify.length; ++i)
                        if (!$util.isInteger(message.score_modify[i]))
                            return "score_modify: integer[] expected";
                }
                return null;
            };

            HuleArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.HuleArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.HuleArgs();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.zimo != null)
                    if (object.zimo)
                        message.zimo = Boolean(object.zimo);
                if (object.chong_seat != null)
                    if (Number(object.chong_seat) !== 0)
                        message.chong_seat = object.chong_seat >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point | 0;
                if (object.fan != null)
                    if (Number(object.fan) !== 0)
                        message.fan = object.fan | 0;
                if (object.score_modify) {
                    if (!Array.isArray(object.score_modify))
                        throw TypeError(".lq.SimulationV2MatchHistory.HuleArgs.score_modify: array expected");
                    message.score_modify = Array(object.score_modify.length);
                    for (let i = 0; i < object.score_modify.length; ++i)
                        message.score_modify[i] = object.score_modify[i] | 0;
                }
                return message;
            };

            HuleArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.score_modify = [];
                if (options.defaults) {
                    object.seat = 0;
                    object.zimo = false;
                    object.chong_seat = 0;
                    object.point = 0;
                    object.fan = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.zimo != null && message.hasOwnProperty("zimo"))
                    object.zimo = message.zimo;
                if (message.chong_seat != null && message.hasOwnProperty("chong_seat"))
                    object.chong_seat = message.chong_seat;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                if (message.fan != null && message.hasOwnProperty("fan"))
                    object.fan = message.fan;
                if (message.score_modify && message.score_modify.length) {
                    object.score_modify = Array(message.score_modify.length);
                    for (let j = 0; j < message.score_modify.length; ++j)
                        object.score_modify[j] = message.score_modify[j];
                }
                return object;
            };

            HuleArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HuleArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.HuleArgs";
            };

            return HuleArgs;
        })();

        SimulationV2MatchHistory.PushTingArgs = (function() {

            function PushTingArgs(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PushTingArgs.prototype.seat = 0;
            PushTingArgs.prototype.ting = 0;

            PushTingArgs.create = function create(properties) {
                return new PushTingArgs(properties);
            };

            PushTingArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.ting != null && Object.hasOwnProperty.call(message, "ting"))
                    writer.uint32(16).uint32(message.ting);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PushTingArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PushTingArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.PushTingArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ting = value;
                            else
                                delete message.ting;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PushTingArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PushTingArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.ting != null && message.hasOwnProperty("ting"))
                    if (!$util.isInteger(message.ting))
                        return "ting: integer expected";
                return null;
            };

            PushTingArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.PushTingArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.PushTingArgs();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.ting != null)
                    if (Number(object.ting) !== 0)
                        message.ting = object.ting >>> 0;
                return message;
            };

            PushTingArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.ting = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.ting != null && message.hasOwnProperty("ting"))
                    object.ting = message.ting;
                return object;
            };

            PushTingArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PushTingArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.PushTingArgs";
            };

            return PushTingArgs;
        })();

        SimulationV2MatchHistory.FindTingArgs = (function() {

            function FindTingArgs(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FindTingArgs.prototype.seat = 0;
            FindTingArgs.prototype.target = 0;

            FindTingArgs.create = function create(properties) {
                return new FindTingArgs(properties);
            };

            FindTingArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                    writer.uint32(16).uint32(message.target);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FindTingArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FindTingArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.FindTingArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.target = value;
                            else
                                delete message.target;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FindTingArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FindTingArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.target != null && message.hasOwnProperty("target"))
                    if (!$util.isInteger(message.target))
                        return "target: integer expected";
                return null;
            };

            FindTingArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.FindTingArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.FindTingArgs();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.target != null)
                    if (Number(object.target) !== 0)
                        message.target = object.target >>> 0;
                return message;
            };

            FindTingArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.target = 0;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.target != null && message.hasOwnProperty("target"))
                    object.target = message.target;
                return object;
            };

            FindTingArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FindTingArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.FindTingArgs";
            };

            return FindTingArgs;
        })();

        SimulationV2MatchHistory.LiujuArgs = (function() {

            function LiujuArgs(properties) {
                this.ting = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LiujuArgs.prototype.ting = $util.emptyArray;

            LiujuArgs.create = function create(properties) {
                return new LiujuArgs(properties);
            };

            LiujuArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.ting != null && message.ting.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.ting.length; ++i)
                        writer.uint32(message.ting[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LiujuArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LiujuArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.LiujuArgs();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.ting && message.ting.length))
                                    message.ting = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.ting.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.ting && message.ting.length))
                                message.ting = [];
                            message.ting.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LiujuArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LiujuArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.ting != null && message.hasOwnProperty("ting")) {
                    if (!Array.isArray(message.ting))
                        return "ting: array expected";
                    for (let i = 0; i < message.ting.length; ++i)
                        if (!$util.isInteger(message.ting[i]))
                            return "ting: integer[] expected";
                }
                return null;
            };

            LiujuArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.LiujuArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.LiujuArgs();
                if (object.ting) {
                    if (!Array.isArray(object.ting))
                        throw TypeError(".lq.SimulationV2MatchHistory.LiujuArgs.ting: array expected");
                    message.ting = Array(object.ting.length);
                    for (let i = 0; i < object.ting.length; ++i)
                        message.ting[i] = object.ting[i] >>> 0;
                }
                return message;
            };

            LiujuArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.ting = [];
                if (message.ting && message.ting.length) {
                    object.ting = Array(message.ting.length);
                    for (let j = 0; j < message.ting.length; ++j)
                        object.ting[j] = message.ting[j];
                }
                return object;
            };

            LiujuArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LiujuArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.LiujuArgs";
            };

            return LiujuArgs;
        })();

        SimulationV2MatchHistory.StoryArgs = (function() {

            function StoryArgs(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            StoryArgs.prototype.story_id = 0;

            StoryArgs.create = function create(properties) {
                return new StoryArgs(properties);
            };

            StoryArgs.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                    writer.uint32(8).uint32(message.story_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            StoryArgs.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            StoryArgs.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2MatchHistory.StoryArgs(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.story_id = value;
                            else
                                delete message.story_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            StoryArgs.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            StoryArgs.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.story_id != null && message.hasOwnProperty("story_id"))
                    if (!$util.isInteger(message.story_id))
                        return "story_id: integer expected";
                return null;
            };

            StoryArgs.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2MatchHistory.StoryArgs)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2MatchHistory.StoryArgs();
                if (object.story_id != null)
                    if (Number(object.story_id) !== 0)
                        message.story_id = object.story_id >>> 0;
                return message;
            };

            StoryArgs.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.story_id = 0;
                if (message.story_id != null && message.hasOwnProperty("story_id"))
                    object.story_id = message.story_id;
                return object;
            };

            StoryArgs.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            StoryArgs.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2MatchHistory.StoryArgs";
            };

            return StoryArgs;
        })();

        return SimulationV2MatchHistory;
    })();

    lq.SimulationV2Match = (function() {

        function SimulationV2Match(properties) {
            this.players = [];
            this.history = [];
            this.rank = [];
            this.actions = [];
            this.buff_list = [];
            this.effected_buff_list = [];
            this.triggered_story = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationV2Match.prototype.info = null;
        SimulationV2Match.prototype.players = $util.emptyArray;
        SimulationV2Match.prototype.history = $util.emptyArray;
        SimulationV2Match.prototype.rank = $util.emptyArray;
        SimulationV2Match.prototype.is_match_end = false;
        SimulationV2Match.prototype.actions = $util.emptyArray;
        SimulationV2Match.prototype.buff_list = $util.emptyArray;
        SimulationV2Match.prototype.is_first_round = false;
        SimulationV2Match.prototype.last_event_remain = 0;
        SimulationV2Match.prototype.effected_buff_list = $util.emptyArray;
        SimulationV2Match.prototype.triggered_story = $util.emptyArray;

        SimulationV2Match.create = function create(properties) {
            return new SimulationV2Match(properties);
        };

        SimulationV2Match.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.info != null && Object.hasOwnProperty.call(message, "info"))
                $root.lq.SimulationV2MatchInfo.encode(message.info, writer.uint32(10).fork()).ldelim();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.SimulationV2Match.SimulationV2Player.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.history != null && message.history.length)
                for (let i = 0; i < message.history.length; ++i)
                    $root.lq.SimulationV2MatchHistory.encode(message.history[i], writer.uint32(26).fork()).ldelim();
            if (message.rank != null && message.rank.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.rank.length; ++i)
                    writer.uint32(message.rank[i]);
                writer.ldelim();
            }
            if (message.is_match_end != null && Object.hasOwnProperty.call(message, "is_match_end"))
                writer.uint32(40).bool(message.is_match_end);
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.lq.SimulationActionData.encode(message.actions[i], writer.uint32(50).fork()).ldelim();
            if (message.buff_list != null && message.buff_list.length)
                for (let i = 0; i < message.buff_list.length; ++i)
                    $root.lq.SimulationV2Buff.encode(message.buff_list[i], writer.uint32(74).fork()).ldelim();
            if (message.is_first_round != null && Object.hasOwnProperty.call(message, "is_first_round"))
                writer.uint32(80).bool(message.is_first_round);
            if (message.last_event_remain != null && Object.hasOwnProperty.call(message, "last_event_remain"))
                writer.uint32(88).uint32(message.last_event_remain);
            if (message.effected_buff_list != null && message.effected_buff_list.length) {
                writer.uint32(98).fork();
                for (let i = 0; i < message.effected_buff_list.length; ++i)
                    writer.uint32(message.effected_buff_list[i]);
                writer.ldelim();
            }
            if (message.triggered_story != null && message.triggered_story.length) {
                writer.uint32(106).fork();
                for (let i = 0; i < message.triggered_story.length; ++i)
                    writer.uint32(message.triggered_story[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationV2Match.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationV2Match.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Match(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.info = $root.lq.SimulationV2MatchInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.info);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.SimulationV2Match.SimulationV2Player.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.history && message.history.length))
                            message.history = [];
                        message.history.push($root.lq.SimulationV2MatchHistory.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.rank && message.rank.length))
                                message.rank = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rank.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_match_end = value;
                        else
                            delete message.is_match_end;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.lq.SimulationActionData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buff_list && message.buff_list.length))
                            message.buff_list = [];
                        message.buff_list.push($root.lq.SimulationV2Buff.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_first_round = value;
                        else
                            delete message.is_first_round;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_event_remain = value;
                        else
                            delete message.last_event_remain;
                        continue;
                    }
                case 12: {
                        if (wireType === 2) {
                            if (!(message.effected_buff_list && message.effected_buff_list.length))
                                message.effected_buff_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.effected_buff_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.effected_buff_list && message.effected_buff_list.length))
                            message.effected_buff_list = [];
                        message.effected_buff_list.push(reader.uint32());
                        continue;
                    }
                case 13: {
                        if (wireType === 2) {
                            if (!(message.triggered_story && message.triggered_story.length))
                                message.triggered_story = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.triggered_story.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.triggered_story && message.triggered_story.length))
                            message.triggered_story = [];
                        message.triggered_story.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationV2Match.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationV2Match.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.info != null && message.hasOwnProperty("info")) {
                let error = $root.lq.SimulationV2MatchInfo.verify(message.info, _depth + 1);
                if (error)
                    return "info." + error;
            }
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.SimulationV2Match.SimulationV2Player.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.history != null && message.hasOwnProperty("history")) {
                if (!Array.isArray(message.history))
                    return "history: array expected";
                for (let i = 0; i < message.history.length; ++i) {
                    let error = $root.lq.SimulationV2MatchHistory.verify(message.history[i], _depth + 1);
                    if (error)
                        return "history." + error;
                }
            }
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i)
                    if (!$util.isInteger(message.rank[i]))
                        return "rank: integer[] expected";
            }
            if (message.is_match_end != null && message.hasOwnProperty("is_match_end"))
                if (typeof message.is_match_end !== "boolean")
                    return "is_match_end: boolean expected";
            if (message.actions != null && message.hasOwnProperty("actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.lq.SimulationActionData.verify(message.actions[i], _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.buff_list != null && message.hasOwnProperty("buff_list")) {
                if (!Array.isArray(message.buff_list))
                    return "buff_list: array expected";
                for (let i = 0; i < message.buff_list.length; ++i) {
                    let error = $root.lq.SimulationV2Buff.verify(message.buff_list[i], _depth + 1);
                    if (error)
                        return "buff_list." + error;
                }
            }
            if (message.is_first_round != null && message.hasOwnProperty("is_first_round"))
                if (typeof message.is_first_round !== "boolean")
                    return "is_first_round: boolean expected";
            if (message.last_event_remain != null && message.hasOwnProperty("last_event_remain"))
                if (!$util.isInteger(message.last_event_remain))
                    return "last_event_remain: integer expected";
            if (message.effected_buff_list != null && message.hasOwnProperty("effected_buff_list")) {
                if (!Array.isArray(message.effected_buff_list))
                    return "effected_buff_list: array expected";
                for (let i = 0; i < message.effected_buff_list.length; ++i)
                    if (!$util.isInteger(message.effected_buff_list[i]))
                        return "effected_buff_list: integer[] expected";
            }
            if (message.triggered_story != null && message.hasOwnProperty("triggered_story")) {
                if (!Array.isArray(message.triggered_story))
                    return "triggered_story: array expected";
                for (let i = 0; i < message.triggered_story.length; ++i)
                    if (!$util.isInteger(message.triggered_story[i]))
                        return "triggered_story: integer[] expected";
            }
            return null;
        };

        SimulationV2Match.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationV2Match)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationV2Match();
            if (object.info != null) {
                if (typeof object.info !== "object")
                    throw TypeError(".lq.SimulationV2Match.info: object expected");
                message.info = $root.lq.SimulationV2MatchInfo.fromObject(object.info, _depth + 1);
            }
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.SimulationV2Match.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.SimulationV2Match.players: object expected");
                    message.players[i] = $root.lq.SimulationV2Match.SimulationV2Player.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.history) {
                if (!Array.isArray(object.history))
                    throw TypeError(".lq.SimulationV2Match.history: array expected");
                message.history = Array(object.history.length);
                for (let i = 0; i < object.history.length; ++i) {
                    if (typeof object.history[i] !== "object")
                        throw TypeError(".lq.SimulationV2Match.history: object expected");
                    message.history[i] = $root.lq.SimulationV2MatchHistory.fromObject(object.history[i], _depth + 1);
                }
            }
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.SimulationV2Match.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i)
                    message.rank[i] = object.rank[i] >>> 0;
            }
            if (object.is_match_end != null)
                if (object.is_match_end)
                    message.is_match_end = Boolean(object.is_match_end);
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".lq.SimulationV2Match.actions: array expected");
                message.actions = Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i) {
                    if (typeof object.actions[i] !== "object")
                        throw TypeError(".lq.SimulationV2Match.actions: object expected");
                    message.actions[i] = $root.lq.SimulationActionData.fromObject(object.actions[i], _depth + 1);
                }
            }
            if (object.buff_list) {
                if (!Array.isArray(object.buff_list))
                    throw TypeError(".lq.SimulationV2Match.buff_list: array expected");
                message.buff_list = Array(object.buff_list.length);
                for (let i = 0; i < object.buff_list.length; ++i) {
                    if (typeof object.buff_list[i] !== "object")
                        throw TypeError(".lq.SimulationV2Match.buff_list: object expected");
                    message.buff_list[i] = $root.lq.SimulationV2Buff.fromObject(object.buff_list[i], _depth + 1);
                }
            }
            if (object.is_first_round != null)
                if (object.is_first_round)
                    message.is_first_round = Boolean(object.is_first_round);
            if (object.last_event_remain != null)
                if (Number(object.last_event_remain) !== 0)
                    message.last_event_remain = object.last_event_remain >>> 0;
            if (object.effected_buff_list) {
                if (!Array.isArray(object.effected_buff_list))
                    throw TypeError(".lq.SimulationV2Match.effected_buff_list: array expected");
                message.effected_buff_list = Array(object.effected_buff_list.length);
                for (let i = 0; i < object.effected_buff_list.length; ++i)
                    message.effected_buff_list[i] = object.effected_buff_list[i] >>> 0;
            }
            if (object.triggered_story) {
                if (!Array.isArray(object.triggered_story))
                    throw TypeError(".lq.SimulationV2Match.triggered_story: array expected");
                message.triggered_story = Array(object.triggered_story.length);
                for (let i = 0; i < object.triggered_story.length; ++i)
                    message.triggered_story[i] = object.triggered_story[i] >>> 0;
            }
            return message;
        };

        SimulationV2Match.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.history = [];
                object.rank = [];
                object.actions = [];
                object.buff_list = [];
                object.effected_buff_list = [];
                object.triggered_story = [];
            }
            if (options.defaults) {
                object.info = null;
                object.is_match_end = false;
                object.is_first_round = false;
                object.last_event_remain = 0;
            }
            if (message.info != null && message.hasOwnProperty("info"))
                object.info = $root.lq.SimulationV2MatchInfo.toObject(message.info, options);
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.SimulationV2Match.SimulationV2Player.toObject(message.players[j], options);
            }
            if (message.history && message.history.length) {
                object.history = Array(message.history.length);
                for (let j = 0; j < message.history.length; ++j)
                    object.history[j] = $root.lq.SimulationV2MatchHistory.toObject(message.history[j], options);
            }
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = message.rank[j];
            }
            if (message.is_match_end != null && message.hasOwnProperty("is_match_end"))
                object.is_match_end = message.is_match_end;
            if (message.actions && message.actions.length) {
                object.actions = Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.lq.SimulationActionData.toObject(message.actions[j], options);
            }
            if (message.buff_list && message.buff_list.length) {
                object.buff_list = Array(message.buff_list.length);
                for (let j = 0; j < message.buff_list.length; ++j)
                    object.buff_list[j] = $root.lq.SimulationV2Buff.toObject(message.buff_list[j], options);
            }
            if (message.is_first_round != null && message.hasOwnProperty("is_first_round"))
                object.is_first_round = message.is_first_round;
            if (message.last_event_remain != null && message.hasOwnProperty("last_event_remain"))
                object.last_event_remain = message.last_event_remain;
            if (message.effected_buff_list && message.effected_buff_list.length) {
                object.effected_buff_list = Array(message.effected_buff_list.length);
                for (let j = 0; j < message.effected_buff_list.length; ++j)
                    object.effected_buff_list[j] = message.effected_buff_list[j];
            }
            if (message.triggered_story && message.triggered_story.length) {
                object.triggered_story = Array(message.triggered_story.length);
                for (let j = 0; j < message.triggered_story.length; ++j)
                    object.triggered_story[j] = message.triggered_story[j];
            }
            return object;
        };

        SimulationV2Match.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationV2Match.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationV2Match";
        };

        SimulationV2Match.SimulationV2Player = (function() {

            function SimulationV2Player(properties) {
                this.find_ting = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SimulationV2Player.prototype.id = 0;
            SimulationV2Player.prototype.main = false;
            SimulationV2Player.prototype.ting = 0;
            SimulationV2Player.prototype.score = 0;
            SimulationV2Player.prototype.fulu = 0;
            SimulationV2Player.prototype.riichi = false;
            SimulationV2Player.prototype.find_ting = $util.emptyArray;
            SimulationV2Player.prototype.seat = 0;
            SimulationV2Player.prototype.con_push_ting = 0;
            SimulationV2Player.prototype.con_keep_ting = 0;
            SimulationV2Player.prototype.ippatsu = false;

            SimulationV2Player.create = function create(properties) {
                return new SimulationV2Player(properties);
            };

            SimulationV2Player.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.main != null && Object.hasOwnProperty.call(message, "main"))
                    writer.uint32(16).bool(message.main);
                if (message.ting != null && Object.hasOwnProperty.call(message, "ting"))
                    writer.uint32(32).uint32(message.ting);
                if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                    writer.uint32(40).int32(message.score);
                if (message.fulu != null && Object.hasOwnProperty.call(message, "fulu"))
                    writer.uint32(48).uint32(message.fulu);
                if (message.riichi != null && Object.hasOwnProperty.call(message, "riichi"))
                    writer.uint32(56).bool(message.riichi);
                if (message.find_ting != null && message.find_ting.length) {
                    writer.uint32(66).fork();
                    for (let i = 0; i < message.find_ting.length; ++i)
                        writer.uint32(message.find_ting[i]);
                    writer.ldelim();
                }
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(72).uint32(message.seat);
                if (message.con_push_ting != null && Object.hasOwnProperty.call(message, "con_push_ting"))
                    writer.uint32(80).uint32(message.con_push_ting);
                if (message.con_keep_ting != null && Object.hasOwnProperty.call(message, "con_keep_ting"))
                    writer.uint32(88).uint32(message.con_keep_ting);
                if (message.ippatsu != null && Object.hasOwnProperty.call(message, "ippatsu"))
                    writer.uint32(96).bool(message.ippatsu);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SimulationV2Player.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SimulationV2Player.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationV2Match.SimulationV2Player(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.main = value;
                            else
                                delete message.main;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ting = value;
                            else
                                delete message.ting;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.score = value;
                            else
                                delete message.score;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.fulu = value;
                            else
                                delete message.fulu;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.riichi = value;
                            else
                                delete message.riichi;
                            continue;
                        }
                    case 8: {
                            if (wireType === 2) {
                                if (!(message.find_ting && message.find_ting.length))
                                    message.find_ting = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.find_ting.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.find_ting && message.find_ting.length))
                                message.find_ting = [];
                            message.find_ting.push(reader.uint32());
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.con_push_ting = value;
                            else
                                delete message.con_push_ting;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.con_keep_ting = value;
                            else
                                delete message.con_keep_ting;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.ippatsu = value;
                            else
                                delete message.ippatsu;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SimulationV2Player.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SimulationV2Player.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.main != null && message.hasOwnProperty("main"))
                    if (typeof message.main !== "boolean")
                        return "main: boolean expected";
                if (message.ting != null && message.hasOwnProperty("ting"))
                    if (!$util.isInteger(message.ting))
                        return "ting: integer expected";
                if (message.score != null && message.hasOwnProperty("score"))
                    if (!$util.isInteger(message.score))
                        return "score: integer expected";
                if (message.fulu != null && message.hasOwnProperty("fulu"))
                    if (!$util.isInteger(message.fulu))
                        return "fulu: integer expected";
                if (message.riichi != null && message.hasOwnProperty("riichi"))
                    if (typeof message.riichi !== "boolean")
                        return "riichi: boolean expected";
                if (message.find_ting != null && message.hasOwnProperty("find_ting")) {
                    if (!Array.isArray(message.find_ting))
                        return "find_ting: array expected";
                    for (let i = 0; i < message.find_ting.length; ++i)
                        if (!$util.isInteger(message.find_ting[i]))
                            return "find_ting: integer[] expected";
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.con_push_ting != null && message.hasOwnProperty("con_push_ting"))
                    if (!$util.isInteger(message.con_push_ting))
                        return "con_push_ting: integer expected";
                if (message.con_keep_ting != null && message.hasOwnProperty("con_keep_ting"))
                    if (!$util.isInteger(message.con_keep_ting))
                        return "con_keep_ting: integer expected";
                if (message.ippatsu != null && message.hasOwnProperty("ippatsu"))
                    if (typeof message.ippatsu !== "boolean")
                        return "ippatsu: boolean expected";
                return null;
            };

            SimulationV2Player.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationV2Match.SimulationV2Player)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationV2Match.SimulationV2Player();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.main != null)
                    if (object.main)
                        message.main = Boolean(object.main);
                if (object.ting != null)
                    if (Number(object.ting) !== 0)
                        message.ting = object.ting >>> 0;
                if (object.score != null)
                    if (Number(object.score) !== 0)
                        message.score = object.score | 0;
                if (object.fulu != null)
                    if (Number(object.fulu) !== 0)
                        message.fulu = object.fulu >>> 0;
                if (object.riichi != null)
                    if (object.riichi)
                        message.riichi = Boolean(object.riichi);
                if (object.find_ting) {
                    if (!Array.isArray(object.find_ting))
                        throw TypeError(".lq.SimulationV2Match.SimulationV2Player.find_ting: array expected");
                    message.find_ting = Array(object.find_ting.length);
                    for (let i = 0; i < object.find_ting.length; ++i)
                        message.find_ting[i] = object.find_ting[i] >>> 0;
                }
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.con_push_ting != null)
                    if (Number(object.con_push_ting) !== 0)
                        message.con_push_ting = object.con_push_ting >>> 0;
                if (object.con_keep_ting != null)
                    if (Number(object.con_keep_ting) !== 0)
                        message.con_keep_ting = object.con_keep_ting >>> 0;
                if (object.ippatsu != null)
                    if (object.ippatsu)
                        message.ippatsu = Boolean(object.ippatsu);
                return message;
            };

            SimulationV2Player.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.find_ting = [];
                if (options.defaults) {
                    object.id = 0;
                    object.main = false;
                    object.ting = 0;
                    object.score = 0;
                    object.fulu = 0;
                    object.riichi = false;
                    object.seat = 0;
                    object.con_push_ting = 0;
                    object.con_keep_ting = 0;
                    object.ippatsu = false;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.main != null && message.hasOwnProperty("main"))
                    object.main = message.main;
                if (message.ting != null && message.hasOwnProperty("ting"))
                    object.ting = message.ting;
                if (message.score != null && message.hasOwnProperty("score"))
                    object.score = message.score;
                if (message.fulu != null && message.hasOwnProperty("fulu"))
                    object.fulu = message.fulu;
                if (message.riichi != null && message.hasOwnProperty("riichi"))
                    object.riichi = message.riichi;
                if (message.find_ting && message.find_ting.length) {
                    object.find_ting = Array(message.find_ting.length);
                    for (let j = 0; j < message.find_ting.length; ++j)
                        object.find_ting[j] = message.find_ting[j];
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.con_push_ting != null && message.hasOwnProperty("con_push_ting"))
                    object.con_push_ting = message.con_push_ting;
                if (message.con_keep_ting != null && message.hasOwnProperty("con_keep_ting"))
                    object.con_keep_ting = message.con_keep_ting;
                if (message.ippatsu != null && message.hasOwnProperty("ippatsu"))
                    object.ippatsu = message.ippatsu;
                return object;
            };

            SimulationV2Player.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SimulationV2Player.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationV2Match.SimulationV2Player";
            };

            return SimulationV2Player;
        })();

        return SimulationV2Match;
    })();

    lq.SimulationActionData = (function() {

        function SimulationActionData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SimulationActionData.prototype.type = 0;
        SimulationActionData.prototype.riichi = null;
        SimulationActionData.prototype.hule = null;
        SimulationActionData.prototype.fulu = null;
        SimulationActionData.prototype.discard_tile = null;
        SimulationActionData.prototype.deal_tile = null;

        SimulationActionData.create = function create(properties) {
            return new SimulationActionData(properties);
        };

        SimulationActionData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.riichi != null && Object.hasOwnProperty.call(message, "riichi"))
                $root.lq.SimulationActionData.ActionRiichiData.encode(message.riichi, writer.uint32(18).fork()).ldelim();
            if (message.hule != null && Object.hasOwnProperty.call(message, "hule"))
                $root.lq.SimulationActionData.ActionHuleData.encode(message.hule, writer.uint32(26).fork()).ldelim();
            if (message.fulu != null && Object.hasOwnProperty.call(message, "fulu"))
                $root.lq.SimulationActionData.ActionFuluData.encode(message.fulu, writer.uint32(34).fork()).ldelim();
            if (message.discard_tile != null && Object.hasOwnProperty.call(message, "discard_tile"))
                $root.lq.SimulationActionData.ActionDiscardData.encode(message.discard_tile, writer.uint32(42).fork()).ldelim();
            if (message.deal_tile != null && Object.hasOwnProperty.call(message, "deal_tile"))
                $root.lq.SimulationActionData.ActionDealTileData.encode(message.deal_tile, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SimulationActionData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SimulationActionData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.riichi = $root.lq.SimulationActionData.ActionRiichiData.decode(reader, reader.uint32(), undefined, _depth + 1, message.riichi);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.hule = $root.lq.SimulationActionData.ActionHuleData.decode(reader, reader.uint32(), undefined, _depth + 1, message.hule);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.fulu = $root.lq.SimulationActionData.ActionFuluData.decode(reader, reader.uint32(), undefined, _depth + 1, message.fulu);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.discard_tile = $root.lq.SimulationActionData.ActionDiscardData.decode(reader, reader.uint32(), undefined, _depth + 1, message.discard_tile);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.deal_tile = $root.lq.SimulationActionData.ActionDealTileData.decode(reader, reader.uint32(), undefined, _depth + 1, message.deal_tile);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SimulationActionData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SimulationActionData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.riichi != null && message.hasOwnProperty("riichi")) {
                let error = $root.lq.SimulationActionData.ActionRiichiData.verify(message.riichi, _depth + 1);
                if (error)
                    return "riichi." + error;
            }
            if (message.hule != null && message.hasOwnProperty("hule")) {
                let error = $root.lq.SimulationActionData.ActionHuleData.verify(message.hule, _depth + 1);
                if (error)
                    return "hule." + error;
            }
            if (message.fulu != null && message.hasOwnProperty("fulu")) {
                let error = $root.lq.SimulationActionData.ActionFuluData.verify(message.fulu, _depth + 1);
                if (error)
                    return "fulu." + error;
            }
            if (message.discard_tile != null && message.hasOwnProperty("discard_tile")) {
                let error = $root.lq.SimulationActionData.ActionDiscardData.verify(message.discard_tile, _depth + 1);
                if (error)
                    return "discard_tile." + error;
            }
            if (message.deal_tile != null && message.hasOwnProperty("deal_tile")) {
                let error = $root.lq.SimulationActionData.ActionDealTileData.verify(message.deal_tile, _depth + 1);
                if (error)
                    return "deal_tile." + error;
            }
            return null;
        };

        SimulationActionData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SimulationActionData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SimulationActionData();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.riichi != null) {
                if (typeof object.riichi !== "object")
                    throw TypeError(".lq.SimulationActionData.riichi: object expected");
                message.riichi = $root.lq.SimulationActionData.ActionRiichiData.fromObject(object.riichi, _depth + 1);
            }
            if (object.hule != null) {
                if (typeof object.hule !== "object")
                    throw TypeError(".lq.SimulationActionData.hule: object expected");
                message.hule = $root.lq.SimulationActionData.ActionHuleData.fromObject(object.hule, _depth + 1);
            }
            if (object.fulu != null) {
                if (typeof object.fulu !== "object")
                    throw TypeError(".lq.SimulationActionData.fulu: object expected");
                message.fulu = $root.lq.SimulationActionData.ActionFuluData.fromObject(object.fulu, _depth + 1);
            }
            if (object.discard_tile != null) {
                if (typeof object.discard_tile !== "object")
                    throw TypeError(".lq.SimulationActionData.discard_tile: object expected");
                message.discard_tile = $root.lq.SimulationActionData.ActionDiscardData.fromObject(object.discard_tile, _depth + 1);
            }
            if (object.deal_tile != null) {
                if (typeof object.deal_tile !== "object")
                    throw TypeError(".lq.SimulationActionData.deal_tile: object expected");
                message.deal_tile = $root.lq.SimulationActionData.ActionDealTileData.fromObject(object.deal_tile, _depth + 1);
            }
            return message;
        };

        SimulationActionData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.riichi = null;
                object.hule = null;
                object.fulu = null;
                object.discard_tile = null;
                object.deal_tile = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.riichi != null && message.hasOwnProperty("riichi"))
                object.riichi = $root.lq.SimulationActionData.ActionRiichiData.toObject(message.riichi, options);
            if (message.hule != null && message.hasOwnProperty("hule"))
                object.hule = $root.lq.SimulationActionData.ActionHuleData.toObject(message.hule, options);
            if (message.fulu != null && message.hasOwnProperty("fulu"))
                object.fulu = $root.lq.SimulationActionData.ActionFuluData.toObject(message.fulu, options);
            if (message.discard_tile != null && message.hasOwnProperty("discard_tile"))
                object.discard_tile = $root.lq.SimulationActionData.ActionDiscardData.toObject(message.discard_tile, options);
            if (message.deal_tile != null && message.hasOwnProperty("deal_tile"))
                object.deal_tile = $root.lq.SimulationActionData.ActionDealTileData.toObject(message.deal_tile, options);
            return object;
        };

        SimulationActionData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SimulationActionData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SimulationActionData";
        };

        SimulationActionData.ActionRiichiData = (function() {

            function ActionRiichiData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActionRiichiData.prototype.seat = 0;

            ActionRiichiData.create = function create(properties) {
                return new ActionRiichiData(properties);
            };

            ActionRiichiData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActionRiichiData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActionRiichiData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionRiichiData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActionRiichiData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActionRiichiData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                return null;
            };

            ActionRiichiData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationActionData.ActionRiichiData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationActionData.ActionRiichiData();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                return message;
            };

            ActionRiichiData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                return object;
            };

            ActionRiichiData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActionRiichiData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationActionData.ActionRiichiData";
            };

            return ActionRiichiData;
        })();

        SimulationActionData.ActionHuleData = (function() {

            function ActionHuleData(properties) {
                this.hule = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActionHuleData.prototype.hule = $util.emptyArray;

            ActionHuleData.create = function create(properties) {
                return new ActionHuleData(properties);
            };

            ActionHuleData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.hule != null && message.hule.length)
                    for (let i = 0; i < message.hule.length; ++i)
                        $root.lq.SimulationActionData.ActionHuleData.HuleInfo.encode(message.hule[i], writer.uint32(10).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActionHuleData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActionHuleData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionHuleData();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.hule && message.hule.length))
                                message.hule = [];
                            message.hule.push($root.lq.SimulationActionData.ActionHuleData.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActionHuleData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActionHuleData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.hule != null && message.hasOwnProperty("hule")) {
                    if (!Array.isArray(message.hule))
                        return "hule: array expected";
                    for (let i = 0; i < message.hule.length; ++i) {
                        let error = $root.lq.SimulationActionData.ActionHuleData.HuleInfo.verify(message.hule[i], _depth + 1);
                        if (error)
                            return "hule." + error;
                    }
                }
                return null;
            };

            ActionHuleData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationActionData.ActionHuleData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationActionData.ActionHuleData();
                if (object.hule) {
                    if (!Array.isArray(object.hule))
                        throw TypeError(".lq.SimulationActionData.ActionHuleData.hule: array expected");
                    message.hule = Array(object.hule.length);
                    for (let i = 0; i < object.hule.length; ++i) {
                        if (typeof object.hule[i] !== "object")
                            throw TypeError(".lq.SimulationActionData.ActionHuleData.hule: object expected");
                        message.hule[i] = $root.lq.SimulationActionData.ActionHuleData.HuleInfo.fromObject(object.hule[i], _depth + 1);
                    }
                }
                return message;
            };

            ActionHuleData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.hule = [];
                if (message.hule && message.hule.length) {
                    object.hule = Array(message.hule.length);
                    for (let j = 0; j < message.hule.length; ++j)
                        object.hule[j] = $root.lq.SimulationActionData.ActionHuleData.HuleInfo.toObject(message.hule[j], options);
                }
                return object;
            };

            ActionHuleData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActionHuleData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationActionData.ActionHuleData";
            };

            ActionHuleData.HuleInfo = (function() {

                function HuleInfo(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                HuleInfo.prototype.fan = 0;
                HuleInfo.prototype.zimo = false;
                HuleInfo.prototype.point = 0;
                HuleInfo.prototype.oya = false;
                HuleInfo.prototype.player = 0;
                HuleInfo.prototype.chong = 0;
                HuleInfo.prototype.toutiao = false;

                HuleInfo.create = function create(properties) {
                    return new HuleInfo(properties);
                };

                HuleInfo.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                        writer.uint32(8).uint32(message.fan);
                    if (message.zimo != null && Object.hasOwnProperty.call(message, "zimo"))
                        writer.uint32(16).bool(message.zimo);
                    if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                        writer.uint32(24).uint32(message.point);
                    if (message.oya != null && Object.hasOwnProperty.call(message, "oya"))
                        writer.uint32(32).bool(message.oya);
                    if (message.player != null && Object.hasOwnProperty.call(message, "player"))
                        writer.uint32(40).uint32(message.player);
                    if (message.chong != null && Object.hasOwnProperty.call(message, "chong"))
                        writer.uint32(48).uint32(message.chong);
                    if (message.toutiao != null && Object.hasOwnProperty.call(message, "toutiao"))
                        writer.uint32(56).bool(message.toutiao);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                HuleInfo.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                HuleInfo.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionHuleData.HuleInfo(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.fan = value;
                                else
                                    delete message.fan;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.zimo = value;
                                else
                                    delete message.zimo;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.point = value;
                                else
                                    delete message.point;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.oya = value;
                                else
                                    delete message.oya;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.player = value;
                                else
                                    delete message.player;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.chong = value;
                                else
                                    delete message.chong;
                                continue;
                            }
                        case 7: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.bool())
                                    message.toutiao = value;
                                else
                                    delete message.toutiao;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                HuleInfo.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                HuleInfo.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.fan != null && message.hasOwnProperty("fan"))
                        if (!$util.isInteger(message.fan))
                            return "fan: integer expected";
                    if (message.zimo != null && message.hasOwnProperty("zimo"))
                        if (typeof message.zimo !== "boolean")
                            return "zimo: boolean expected";
                    if (message.point != null && message.hasOwnProperty("point"))
                        if (!$util.isInteger(message.point))
                            return "point: integer expected";
                    if (message.oya != null && message.hasOwnProperty("oya"))
                        if (typeof message.oya !== "boolean")
                            return "oya: boolean expected";
                    if (message.player != null && message.hasOwnProperty("player"))
                        if (!$util.isInteger(message.player))
                            return "player: integer expected";
                    if (message.chong != null && message.hasOwnProperty("chong"))
                        if (!$util.isInteger(message.chong))
                            return "chong: integer expected";
                    if (message.toutiao != null && message.hasOwnProperty("toutiao"))
                        if (typeof message.toutiao !== "boolean")
                            return "toutiao: boolean expected";
                    return null;
                };

                HuleInfo.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.SimulationActionData.ActionHuleData.HuleInfo)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.SimulationActionData.ActionHuleData.HuleInfo();
                    if (object.fan != null)
                        if (Number(object.fan) !== 0)
                            message.fan = object.fan >>> 0;
                    if (object.zimo != null)
                        if (object.zimo)
                            message.zimo = Boolean(object.zimo);
                    if (object.point != null)
                        if (Number(object.point) !== 0)
                            message.point = object.point >>> 0;
                    if (object.oya != null)
                        if (object.oya)
                            message.oya = Boolean(object.oya);
                    if (object.player != null)
                        if (Number(object.player) !== 0)
                            message.player = object.player >>> 0;
                    if (object.chong != null)
                        if (Number(object.chong) !== 0)
                            message.chong = object.chong >>> 0;
                    if (object.toutiao != null)
                        if (object.toutiao)
                            message.toutiao = Boolean(object.toutiao);
                    return message;
                };

                HuleInfo.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.fan = 0;
                        object.zimo = false;
                        object.point = 0;
                        object.oya = false;
                        object.player = 0;
                        object.chong = 0;
                        object.toutiao = false;
                    }
                    if (message.fan != null && message.hasOwnProperty("fan"))
                        object.fan = message.fan;
                    if (message.zimo != null && message.hasOwnProperty("zimo"))
                        object.zimo = message.zimo;
                    if (message.point != null && message.hasOwnProperty("point"))
                        object.point = message.point;
                    if (message.oya != null && message.hasOwnProperty("oya"))
                        object.oya = message.oya;
                    if (message.player != null && message.hasOwnProperty("player"))
                        object.player = message.player;
                    if (message.chong != null && message.hasOwnProperty("chong"))
                        object.chong = message.chong;
                    if (message.toutiao != null && message.hasOwnProperty("toutiao"))
                        object.toutiao = message.toutiao;
                    return object;
                };

                HuleInfo.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                HuleInfo.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.SimulationActionData.ActionHuleData.HuleInfo";
                };

                return HuleInfo;
            })();

            return ActionHuleData;
        })();

        SimulationActionData.ActionFuluData = (function() {

            function ActionFuluData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActionFuluData.prototype.seat = 0;

            ActionFuluData.create = function create(properties) {
                return new ActionFuluData(properties);
            };

            ActionFuluData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActionFuluData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActionFuluData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionFuluData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActionFuluData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActionFuluData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                return null;
            };

            ActionFuluData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationActionData.ActionFuluData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationActionData.ActionFuluData();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                return message;
            };

            ActionFuluData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                return object;
            };

            ActionFuluData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActionFuluData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationActionData.ActionFuluData";
            };

            return ActionFuluData;
        })();

        SimulationActionData.ActionDiscardData = (function() {

            function ActionDiscardData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActionDiscardData.prototype.seat = 0;
            ActionDiscardData.prototype.riichi = false;

            ActionDiscardData.create = function create(properties) {
                return new ActionDiscardData(properties);
            };

            ActionDiscardData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.riichi != null && Object.hasOwnProperty.call(message, "riichi"))
                    writer.uint32(16).bool(message.riichi);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActionDiscardData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActionDiscardData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionDiscardData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.riichi = value;
                            else
                                delete message.riichi;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActionDiscardData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActionDiscardData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.riichi != null && message.hasOwnProperty("riichi"))
                    if (typeof message.riichi !== "boolean")
                        return "riichi: boolean expected";
                return null;
            };

            ActionDiscardData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationActionData.ActionDiscardData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationActionData.ActionDiscardData();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.riichi != null)
                    if (object.riichi)
                        message.riichi = Boolean(object.riichi);
                return message;
            };

            ActionDiscardData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.seat = 0;
                    object.riichi = false;
                }
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.riichi != null && message.hasOwnProperty("riichi"))
                    object.riichi = message.riichi;
                return object;
            };

            ActionDiscardData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActionDiscardData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationActionData.ActionDiscardData";
            };

            return ActionDiscardData;
        })();

        SimulationActionData.ActionDealTileData = (function() {

            function ActionDealTileData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActionDealTileData.prototype.seat = 0;

            ActionDealTileData.create = function create(properties) {
                return new ActionDealTileData(properties);
            };

            ActionDealTileData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActionDealTileData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActionDealTileData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SimulationActionData.ActionDealTileData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActionDealTileData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActionDealTileData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                return null;
            };

            ActionDealTileData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SimulationActionData.ActionDealTileData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SimulationActionData.ActionDealTileData();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                return message;
            };

            ActionDealTileData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                return object;
            };

            ActionDealTileData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActionDealTileData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SimulationActionData.ActionDealTileData";
            };

            return ActionDealTileData;
        })();

        return SimulationActionData;
    })();

    lq.ActivityShootValueChange = (function() {

        function ActivityShootValueChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityShootValueChange.prototype.level = null;
        ActivityShootValueChange.prototype.enemies = null;
        ActivityShootValueChange.prototype.rewarded_ids = null;

        ActivityShootValueChange.create = function create(properties) {
            return new ActivityShootValueChange(properties);
        };

        ActivityShootValueChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.ActivityShootValueChange.Uint32ValueDirty.encode(message.level, writer.uint32(10).fork()).ldelim();
            if (message.enemies != null && Object.hasOwnProperty.call(message, "enemies"))
                $root.lq.ActivityShootEnemyInfoDirty.encode(message.enemies, writer.uint32(18).fork()).ldelim();
            if (message.rewarded_ids != null && Object.hasOwnProperty.call(message, "rewarded_ids"))
                $root.lq.ActivityShootValueChange.RewardArrayDirty.encode(message.rewarded_ids, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityShootValueChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityShootValueChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootValueChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.ActivityShootValueChange.Uint32ValueDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.enemies = $root.lq.ActivityShootEnemyInfoDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.enemies);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.rewarded_ids = $root.lq.ActivityShootValueChange.RewardArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.rewarded_ids);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityShootValueChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityShootValueChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.ActivityShootValueChange.Uint32ValueDirty.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.enemies != null && message.hasOwnProperty("enemies")) {
                let error = $root.lq.ActivityShootEnemyInfoDirty.verify(message.enemies, _depth + 1);
                if (error)
                    return "enemies." + error;
            }
            if (message.rewarded_ids != null && message.hasOwnProperty("rewarded_ids")) {
                let error = $root.lq.ActivityShootValueChange.RewardArrayDirty.verify(message.rewarded_ids, _depth + 1);
                if (error)
                    return "rewarded_ids." + error;
            }
            return null;
        };

        ActivityShootValueChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityShootValueChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityShootValueChange();
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.ActivityShootValueChange.level: object expected");
                message.level = $root.lq.ActivityShootValueChange.Uint32ValueDirty.fromObject(object.level, _depth + 1);
            }
            if (object.enemies != null) {
                if (typeof object.enemies !== "object")
                    throw TypeError(".lq.ActivityShootValueChange.enemies: object expected");
                message.enemies = $root.lq.ActivityShootEnemyInfoDirty.fromObject(object.enemies, _depth + 1);
            }
            if (object.rewarded_ids != null) {
                if (typeof object.rewarded_ids !== "object")
                    throw TypeError(".lq.ActivityShootValueChange.rewarded_ids: object expected");
                message.rewarded_ids = $root.lq.ActivityShootValueChange.RewardArrayDirty.fromObject(object.rewarded_ids, _depth + 1);
            }
            return message;
        };

        ActivityShootValueChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.level = null;
                object.enemies = null;
                object.rewarded_ids = null;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.ActivityShootValueChange.Uint32ValueDirty.toObject(message.level, options);
            if (message.enemies != null && message.hasOwnProperty("enemies"))
                object.enemies = $root.lq.ActivityShootEnemyInfoDirty.toObject(message.enemies, options);
            if (message.rewarded_ids != null && message.hasOwnProperty("rewarded_ids"))
                object.rewarded_ids = $root.lq.ActivityShootValueChange.RewardArrayDirty.toObject(message.rewarded_ids, options);
            return object;
        };

        ActivityShootValueChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityShootValueChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityShootValueChange";
        };

        ActivityShootValueChange.Uint32ValueDirty = (function() {

            function Uint32ValueDirty(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Uint32ValueDirty.prototype.value = 0;
            Uint32ValueDirty.prototype.dirty = 0;

            Uint32ValueDirty.create = function create(properties) {
                return new Uint32ValueDirty(properties);
            };

            Uint32ValueDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(8).uint32(message.value);
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(16).uint32(message.dirty);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Uint32ValueDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Uint32ValueDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootValueChange.Uint32ValueDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Uint32ValueDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Uint32ValueDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (!$util.isInteger(message.dirty))
                        return "dirty: integer expected";
                return null;
            };

            Uint32ValueDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityShootValueChange.Uint32ValueDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityShootValueChange.Uint32ValueDirty();
                if (object.value != null)
                    if (Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                if (object.dirty != null)
                    if (Number(object.dirty) !== 0)
                        message.dirty = object.dirty >>> 0;
                return message;
            };

            Uint32ValueDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.value = 0;
                    object.dirty = 0;
                }
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                return object;
            };

            Uint32ValueDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Uint32ValueDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityShootValueChange.Uint32ValueDirty";
            };

            return Uint32ValueDirty;
        })();

        ActivityShootValueChange.RewardArrayDirty = (function() {

            function RewardArrayDirty(properties) {
                this.reward_ids = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardArrayDirty.prototype.reward_ids = $util.emptyArray;
            RewardArrayDirty.prototype.dirty = 0;

            RewardArrayDirty.create = function create(properties) {
                return new RewardArrayDirty(properties);
            };

            RewardArrayDirty.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.reward_ids != null && message.reward_ids.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.reward_ids.length; ++i)
                        writer.uint32(message.reward_ids[i]);
                    writer.ldelim();
                }
                if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                    writer.uint32(16).uint32(message.dirty);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootValueChange.RewardArrayDirty(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.reward_ids && message.reward_ids.length))
                                    message.reward_ids = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.reward_ids.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.reward_ids && message.reward_ids.length))
                                message.reward_ids = [];
                            message.reward_ids.push(reader.uint32());
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dirty = value;
                            else
                                delete message.dirty;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardArrayDirty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardArrayDirty.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.reward_ids != null && message.hasOwnProperty("reward_ids")) {
                    if (!Array.isArray(message.reward_ids))
                        return "reward_ids: array expected";
                    for (let i = 0; i < message.reward_ids.length; ++i)
                        if (!$util.isInteger(message.reward_ids[i]))
                            return "reward_ids: integer[] expected";
                }
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    if (!$util.isInteger(message.dirty))
                        return "dirty: integer expected";
                return null;
            };

            RewardArrayDirty.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityShootValueChange.RewardArrayDirty)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityShootValueChange.RewardArrayDirty();
                if (object.reward_ids) {
                    if (!Array.isArray(object.reward_ids))
                        throw TypeError(".lq.ActivityShootValueChange.RewardArrayDirty.reward_ids: array expected");
                    message.reward_ids = Array(object.reward_ids.length);
                    for (let i = 0; i < object.reward_ids.length; ++i)
                        message.reward_ids[i] = object.reward_ids[i] >>> 0;
                }
                if (object.dirty != null)
                    if (Number(object.dirty) !== 0)
                        message.dirty = object.dirty >>> 0;
                return message;
            };

            RewardArrayDirty.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.reward_ids = [];
                if (options.defaults)
                    object.dirty = 0;
                if (message.reward_ids && message.reward_ids.length) {
                    object.reward_ids = Array(message.reward_ids.length);
                    for (let j = 0; j < message.reward_ids.length; ++j)
                        object.reward_ids[j] = message.reward_ids[j];
                }
                if (message.dirty != null && message.hasOwnProperty("dirty"))
                    object.dirty = message.dirty;
                return object;
            };

            RewardArrayDirty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityShootValueChange.RewardArrayDirty";
            };

            return RewardArrayDirty;
        })();

        return ActivityShootValueChange;
    })();

    lq.ActivityShootEnemyInfoDirty = (function() {

        function ActivityShootEnemyInfoDirty(properties) {
            this.enemies = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityShootEnemyInfoDirty.prototype.dirty = 0;
        ActivityShootEnemyInfoDirty.prototype.enemies = $util.emptyArray;

        ActivityShootEnemyInfoDirty.create = function create(properties) {
            return new ActivityShootEnemyInfoDirty(properties);
        };

        ActivityShootEnemyInfoDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).uint32(message.dirty);
            if (message.enemies != null && message.enemies.length)
                for (let i = 0; i < message.enemies.length; ++i)
                    $root.lq.ActivityShootEnemyInfo.encode(message.enemies[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityShootEnemyInfoDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityShootEnemyInfoDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootEnemyInfoDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.enemies && message.enemies.length))
                            message.enemies = [];
                        message.enemies.push($root.lq.ActivityShootEnemyInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityShootEnemyInfoDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityShootEnemyInfoDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (!$util.isInteger(message.dirty))
                    return "dirty: integer expected";
            if (message.enemies != null && message.hasOwnProperty("enemies")) {
                if (!Array.isArray(message.enemies))
                    return "enemies: array expected";
                for (let i = 0; i < message.enemies.length; ++i) {
                    let error = $root.lq.ActivityShootEnemyInfo.verify(message.enemies[i], _depth + 1);
                    if (error)
                        return "enemies." + error;
                }
            }
            return null;
        };

        ActivityShootEnemyInfoDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityShootEnemyInfoDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityShootEnemyInfoDirty();
            if (object.dirty != null)
                if (Number(object.dirty) !== 0)
                    message.dirty = object.dirty >>> 0;
            if (object.enemies) {
                if (!Array.isArray(object.enemies))
                    throw TypeError(".lq.ActivityShootEnemyInfoDirty.enemies: array expected");
                message.enemies = Array(object.enemies.length);
                for (let i = 0; i < object.enemies.length; ++i) {
                    if (typeof object.enemies[i] !== "object")
                        throw TypeError(".lq.ActivityShootEnemyInfoDirty.enemies: object expected");
                    message.enemies[i] = $root.lq.ActivityShootEnemyInfo.fromObject(object.enemies[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityShootEnemyInfoDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.enemies = [];
            if (options.defaults)
                object.dirty = 0;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.enemies && message.enemies.length) {
                object.enemies = Array(message.enemies.length);
                for (let j = 0; j < message.enemies.length; ++j)
                    object.enemies[j] = $root.lq.ActivityShootEnemyInfo.toObject(message.enemies[j], options);
            }
            return object;
        };

        ActivityShootEnemyInfoDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityShootEnemyInfoDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityShootEnemyInfoDirty";
        };

        return ActivityShootEnemyInfoDirty;
    })();

    lq.ActivityShootData = (function() {

        function ActivityShootData(properties) {
            this.enemies = [];
            this.rewarded_ids = [];
            this.rewarded_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityShootData.prototype.activity_id = 0;
        ActivityShootData.prototype.level = 0;
        ActivityShootData.prototype.enemies = $util.emptyArray;
        ActivityShootData.prototype.rewarded_ids = $util.emptyArray;
        ActivityShootData.prototype.ended = false;
        ActivityShootData.prototype.rewarded_records = $util.emptyArray;

        ActivityShootData.create = function create(properties) {
            return new ActivityShootData(properties);
        };

        ActivityShootData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.enemies != null && message.enemies.length)
                for (let i = 0; i < message.enemies.length; ++i)
                    $root.lq.ActivityShootEnemyInfo.encode(message.enemies[i], writer.uint32(26).fork()).ldelim();
            if (message.rewarded_ids != null && message.rewarded_ids.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.rewarded_ids.length; ++i)
                    writer.uint32(message.rewarded_ids[i]);
                writer.ldelim();
            }
            if (message.ended != null && Object.hasOwnProperty.call(message, "ended"))
                writer.uint32(40).bool(message.ended);
            if (message.rewarded_records != null && message.rewarded_records.length)
                for (let i = 0; i < message.rewarded_records.length; ++i)
                    $root.lq.ActivityShootRewardRecord.encode(message.rewarded_records[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityShootData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityShootData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.enemies && message.enemies.length))
                            message.enemies = [];
                        message.enemies.push($root.lq.ActivityShootEnemyInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.rewarded_ids && message.rewarded_ids.length))
                                message.rewarded_ids = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_ids.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_ids && message.rewarded_ids.length))
                            message.rewarded_ids = [];
                        message.rewarded_ids.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewarded_records && message.rewarded_records.length))
                            message.rewarded_records = [];
                        message.rewarded_records.push($root.lq.ActivityShootRewardRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityShootData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityShootData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.enemies != null && message.hasOwnProperty("enemies")) {
                if (!Array.isArray(message.enemies))
                    return "enemies: array expected";
                for (let i = 0; i < message.enemies.length; ++i) {
                    let error = $root.lq.ActivityShootEnemyInfo.verify(message.enemies[i], _depth + 1);
                    if (error)
                        return "enemies." + error;
                }
            }
            if (message.rewarded_ids != null && message.hasOwnProperty("rewarded_ids")) {
                if (!Array.isArray(message.rewarded_ids))
                    return "rewarded_ids: array expected";
                for (let i = 0; i < message.rewarded_ids.length; ++i)
                    if (!$util.isInteger(message.rewarded_ids[i]))
                        return "rewarded_ids: integer[] expected";
            }
            if (message.ended != null && message.hasOwnProperty("ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            if (message.rewarded_records != null && message.hasOwnProperty("rewarded_records")) {
                if (!Array.isArray(message.rewarded_records))
                    return "rewarded_records: array expected";
                for (let i = 0; i < message.rewarded_records.length; ++i) {
                    let error = $root.lq.ActivityShootRewardRecord.verify(message.rewarded_records[i], _depth + 1);
                    if (error)
                        return "rewarded_records." + error;
                }
            }
            return null;
        };

        ActivityShootData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityShootData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityShootData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.enemies) {
                if (!Array.isArray(object.enemies))
                    throw TypeError(".lq.ActivityShootData.enemies: array expected");
                message.enemies = Array(object.enemies.length);
                for (let i = 0; i < object.enemies.length; ++i) {
                    if (typeof object.enemies[i] !== "object")
                        throw TypeError(".lq.ActivityShootData.enemies: object expected");
                    message.enemies[i] = $root.lq.ActivityShootEnemyInfo.fromObject(object.enemies[i], _depth + 1);
                }
            }
            if (object.rewarded_ids) {
                if (!Array.isArray(object.rewarded_ids))
                    throw TypeError(".lq.ActivityShootData.rewarded_ids: array expected");
                message.rewarded_ids = Array(object.rewarded_ids.length);
                for (let i = 0; i < object.rewarded_ids.length; ++i)
                    message.rewarded_ids[i] = object.rewarded_ids[i] >>> 0;
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = Boolean(object.ended);
            if (object.rewarded_records) {
                if (!Array.isArray(object.rewarded_records))
                    throw TypeError(".lq.ActivityShootData.rewarded_records: array expected");
                message.rewarded_records = Array(object.rewarded_records.length);
                for (let i = 0; i < object.rewarded_records.length; ++i) {
                    if (typeof object.rewarded_records[i] !== "object")
                        throw TypeError(".lq.ActivityShootData.rewarded_records: object expected");
                    message.rewarded_records[i] = $root.lq.ActivityShootRewardRecord.fromObject(object.rewarded_records[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityShootData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.enemies = [];
                object.rewarded_ids = [];
                object.rewarded_records = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.level = 0;
                object.ended = false;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.enemies && message.enemies.length) {
                object.enemies = Array(message.enemies.length);
                for (let j = 0; j < message.enemies.length; ++j)
                    object.enemies[j] = $root.lq.ActivityShootEnemyInfo.toObject(message.enemies[j], options);
            }
            if (message.rewarded_ids && message.rewarded_ids.length) {
                object.rewarded_ids = Array(message.rewarded_ids.length);
                for (let j = 0; j < message.rewarded_ids.length; ++j)
                    object.rewarded_ids[j] = message.rewarded_ids[j];
            }
            if (message.ended != null && message.hasOwnProperty("ended"))
                object.ended = message.ended;
            if (message.rewarded_records && message.rewarded_records.length) {
                object.rewarded_records = Array(message.rewarded_records.length);
                for (let j = 0; j < message.rewarded_records.length; ++j)
                    object.rewarded_records[j] = $root.lq.ActivityShootRewardRecord.toObject(message.rewarded_records[j], options);
            }
            return object;
        };

        ActivityShootData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityShootData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityShootData";
        };

        return ActivityShootData;
    })();

    lq.ActivityShootEnemyInfo = (function() {

        function ActivityShootEnemyInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityShootEnemyInfo.prototype.group_id = 0;
        ActivityShootEnemyInfo.prototype.enemy_id = 0;
        ActivityShootEnemyInfo.prototype.hp = 0;

        ActivityShootEnemyInfo.create = function create(properties) {
            return new ActivityShootEnemyInfo(properties);
        };

        ActivityShootEnemyInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.group_id != null && Object.hasOwnProperty.call(message, "group_id"))
                writer.uint32(8).uint32(message.group_id);
            if (message.enemy_id != null && Object.hasOwnProperty.call(message, "enemy_id"))
                writer.uint32(16).uint32(message.enemy_id);
            if (message.hp != null && Object.hasOwnProperty.call(message, "hp"))
                writer.uint32(24).uint32(message.hp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityShootEnemyInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityShootEnemyInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootEnemyInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.group_id = value;
                        else
                            delete message.group_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enemy_id = value;
                        else
                            delete message.enemy_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.hp = value;
                        else
                            delete message.hp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityShootEnemyInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityShootEnemyInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.group_id != null && message.hasOwnProperty("group_id"))
                if (!$util.isInteger(message.group_id))
                    return "group_id: integer expected";
            if (message.enemy_id != null && message.hasOwnProperty("enemy_id"))
                if (!$util.isInteger(message.enemy_id))
                    return "enemy_id: integer expected";
            if (message.hp != null && message.hasOwnProperty("hp"))
                if (!$util.isInteger(message.hp))
                    return "hp: integer expected";
            return null;
        };

        ActivityShootEnemyInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityShootEnemyInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityShootEnemyInfo();
            if (object.group_id != null)
                if (Number(object.group_id) !== 0)
                    message.group_id = object.group_id >>> 0;
            if (object.enemy_id != null)
                if (Number(object.enemy_id) !== 0)
                    message.enemy_id = object.enemy_id >>> 0;
            if (object.hp != null)
                if (Number(object.hp) !== 0)
                    message.hp = object.hp >>> 0;
            return message;
        };

        ActivityShootEnemyInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.group_id = 0;
                object.enemy_id = 0;
                object.hp = 0;
            }
            if (message.group_id != null && message.hasOwnProperty("group_id"))
                object.group_id = message.group_id;
            if (message.enemy_id != null && message.hasOwnProperty("enemy_id"))
                object.enemy_id = message.enemy_id;
            if (message.hp != null && message.hasOwnProperty("hp"))
                object.hp = message.hp;
            return object;
        };

        ActivityShootEnemyInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityShootEnemyInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityShootEnemyInfo";
        };

        return ActivityShootEnemyInfo;
    })();

    lq.ActivityShootRewardRecord = (function() {

        function ActivityShootRewardRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityShootRewardRecord.prototype.enemy_id = 0;
        ActivityShootRewardRecord.prototype.reward_id = 0;
        ActivityShootRewardRecord.prototype.rewarded_time = 0;

        ActivityShootRewardRecord.create = function create(properties) {
            return new ActivityShootRewardRecord(properties);
        };

        ActivityShootRewardRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.enemy_id != null && Object.hasOwnProperty.call(message, "enemy_id"))
                writer.uint32(8).uint32(message.enemy_id);
            if (message.reward_id != null && Object.hasOwnProperty.call(message, "reward_id"))
                writer.uint32(16).uint32(message.reward_id);
            if (message.rewarded_time != null && Object.hasOwnProperty.call(message, "rewarded_time"))
                writer.uint32(24).uint32(message.rewarded_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityShootRewardRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityShootRewardRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityShootRewardRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.enemy_id = value;
                        else
                            delete message.enemy_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reward_id = value;
                        else
                            delete message.reward_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rewarded_time = value;
                        else
                            delete message.rewarded_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityShootRewardRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityShootRewardRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enemy_id != null && message.hasOwnProperty("enemy_id"))
                if (!$util.isInteger(message.enemy_id))
                    return "enemy_id: integer expected";
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                if (!$util.isInteger(message.reward_id))
                    return "reward_id: integer expected";
            if (message.rewarded_time != null && message.hasOwnProperty("rewarded_time"))
                if (!$util.isInteger(message.rewarded_time))
                    return "rewarded_time: integer expected";
            return null;
        };

        ActivityShootRewardRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityShootRewardRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityShootRewardRecord();
            if (object.enemy_id != null)
                if (Number(object.enemy_id) !== 0)
                    message.enemy_id = object.enemy_id >>> 0;
            if (object.reward_id != null)
                if (Number(object.reward_id) !== 0)
                    message.reward_id = object.reward_id >>> 0;
            if (object.rewarded_time != null)
                if (Number(object.rewarded_time) !== 0)
                    message.rewarded_time = object.rewarded_time >>> 0;
            return message;
        };

        ActivityShootRewardRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.enemy_id = 0;
                object.reward_id = 0;
                object.rewarded_time = 0;
            }
            if (message.enemy_id != null && message.hasOwnProperty("enemy_id"))
                object.enemy_id = message.enemy_id;
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                object.reward_id = message.reward_id;
            if (message.rewarded_time != null && message.hasOwnProperty("rewarded_time"))
                object.rewarded_time = message.rewarded_time;
            return object;
        };

        ActivityShootRewardRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityShootRewardRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityShootRewardRecord";
        };

        return ActivityShootRewardRecord;
    })();

    lq.ActivityBingoCardData = (function() {

        function ActivityBingoCardData(properties) {
            this.achieved_pos = [];
            this.rewarded_ids = [];
            this.achieved_records = [];
            this.reward_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityBingoCardData.prototype.card_id = 0;
        ActivityBingoCardData.prototype.achieved_pos = $util.emptyArray;
        ActivityBingoCardData.prototype.rewarded_ids = $util.emptyArray;
        ActivityBingoCardData.prototype.state = 0;
        ActivityBingoCardData.prototype.achieved_records = $util.emptyArray;
        ActivityBingoCardData.prototype.reward_records = $util.emptyArray;

        ActivityBingoCardData.create = function create(properties) {
            return new ActivityBingoCardData(properties);
        };

        ActivityBingoCardData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.card_id != null && Object.hasOwnProperty.call(message, "card_id"))
                writer.uint32(8).uint32(message.card_id);
            if (message.achieved_pos != null && message.achieved_pos.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.achieved_pos.length; ++i)
                    writer.uint32(message.achieved_pos[i]);
                writer.ldelim();
            }
            if (message.rewarded_ids != null && message.rewarded_ids.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.rewarded_ids.length; ++i)
                    writer.uint32(message.rewarded_ids[i]);
                writer.ldelim();
            }
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(32).uint32(message.state);
            if (message.achieved_records != null && message.achieved_records.length)
                for (let i = 0; i < message.achieved_records.length; ++i)
                    $root.lq.ActivityBingoCardData.BingoAchievedRecord.encode(message.achieved_records[i], writer.uint32(42).fork()).ldelim();
            if (message.reward_records != null && message.reward_records.length)
                for (let i = 0; i < message.reward_records.length; ++i)
                    $root.lq.ActivityBingoCardData.BingoRewardRecord.encode(message.reward_records[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityBingoCardData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityBingoCardData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityBingoCardData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.card_id = value;
                        else
                            delete message.card_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.achieved_pos && message.achieved_pos.length))
                                message.achieved_pos = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.achieved_pos.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.achieved_pos && message.achieved_pos.length))
                            message.achieved_pos = [];
                        message.achieved_pos.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.rewarded_ids && message.rewarded_ids.length))
                                message.rewarded_ids = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_ids.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_ids && message.rewarded_ids.length))
                            message.rewarded_ids = [];
                        message.rewarded_ids.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.achieved_records && message.achieved_records.length))
                            message.achieved_records = [];
                        message.achieved_records.push($root.lq.ActivityBingoCardData.BingoAchievedRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_records && message.reward_records.length))
                            message.reward_records = [];
                        message.reward_records.push($root.lq.ActivityBingoCardData.BingoRewardRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityBingoCardData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityBingoCardData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.card_id != null && message.hasOwnProperty("card_id"))
                if (!$util.isInteger(message.card_id))
                    return "card_id: integer expected";
            if (message.achieved_pos != null && message.hasOwnProperty("achieved_pos")) {
                if (!Array.isArray(message.achieved_pos))
                    return "achieved_pos: array expected";
                for (let i = 0; i < message.achieved_pos.length; ++i)
                    if (!$util.isInteger(message.achieved_pos[i]))
                        return "achieved_pos: integer[] expected";
            }
            if (message.rewarded_ids != null && message.hasOwnProperty("rewarded_ids")) {
                if (!Array.isArray(message.rewarded_ids))
                    return "rewarded_ids: array expected";
                for (let i = 0; i < message.rewarded_ids.length; ++i)
                    if (!$util.isInteger(message.rewarded_ids[i]))
                        return "rewarded_ids: integer[] expected";
            }
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.achieved_records != null && message.hasOwnProperty("achieved_records")) {
                if (!Array.isArray(message.achieved_records))
                    return "achieved_records: array expected";
                for (let i = 0; i < message.achieved_records.length; ++i) {
                    let error = $root.lq.ActivityBingoCardData.BingoAchievedRecord.verify(message.achieved_records[i], _depth + 1);
                    if (error)
                        return "achieved_records." + error;
                }
            }
            if (message.reward_records != null && message.hasOwnProperty("reward_records")) {
                if (!Array.isArray(message.reward_records))
                    return "reward_records: array expected";
                for (let i = 0; i < message.reward_records.length; ++i) {
                    let error = $root.lq.ActivityBingoCardData.BingoRewardRecord.verify(message.reward_records[i], _depth + 1);
                    if (error)
                        return "reward_records." + error;
                }
            }
            return null;
        };

        ActivityBingoCardData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityBingoCardData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityBingoCardData();
            if (object.card_id != null)
                if (Number(object.card_id) !== 0)
                    message.card_id = object.card_id >>> 0;
            if (object.achieved_pos) {
                if (!Array.isArray(object.achieved_pos))
                    throw TypeError(".lq.ActivityBingoCardData.achieved_pos: array expected");
                message.achieved_pos = Array(object.achieved_pos.length);
                for (let i = 0; i < object.achieved_pos.length; ++i)
                    message.achieved_pos[i] = object.achieved_pos[i] >>> 0;
            }
            if (object.rewarded_ids) {
                if (!Array.isArray(object.rewarded_ids))
                    throw TypeError(".lq.ActivityBingoCardData.rewarded_ids: array expected");
                message.rewarded_ids = Array(object.rewarded_ids.length);
                for (let i = 0; i < object.rewarded_ids.length; ++i)
                    message.rewarded_ids[i] = object.rewarded_ids[i] >>> 0;
            }
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.achieved_records) {
                if (!Array.isArray(object.achieved_records))
                    throw TypeError(".lq.ActivityBingoCardData.achieved_records: array expected");
                message.achieved_records = Array(object.achieved_records.length);
                for (let i = 0; i < object.achieved_records.length; ++i) {
                    if (typeof object.achieved_records[i] !== "object")
                        throw TypeError(".lq.ActivityBingoCardData.achieved_records: object expected");
                    message.achieved_records[i] = $root.lq.ActivityBingoCardData.BingoAchievedRecord.fromObject(object.achieved_records[i], _depth + 1);
                }
            }
            if (object.reward_records) {
                if (!Array.isArray(object.reward_records))
                    throw TypeError(".lq.ActivityBingoCardData.reward_records: array expected");
                message.reward_records = Array(object.reward_records.length);
                for (let i = 0; i < object.reward_records.length; ++i) {
                    if (typeof object.reward_records[i] !== "object")
                        throw TypeError(".lq.ActivityBingoCardData.reward_records: object expected");
                    message.reward_records[i] = $root.lq.ActivityBingoCardData.BingoRewardRecord.fromObject(object.reward_records[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityBingoCardData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.achieved_pos = [];
                object.rewarded_ids = [];
                object.achieved_records = [];
                object.reward_records = [];
            }
            if (options.defaults) {
                object.card_id = 0;
                object.state = 0;
            }
            if (message.card_id != null && message.hasOwnProperty("card_id"))
                object.card_id = message.card_id;
            if (message.achieved_pos && message.achieved_pos.length) {
                object.achieved_pos = Array(message.achieved_pos.length);
                for (let j = 0; j < message.achieved_pos.length; ++j)
                    object.achieved_pos[j] = message.achieved_pos[j];
            }
            if (message.rewarded_ids && message.rewarded_ids.length) {
                object.rewarded_ids = Array(message.rewarded_ids.length);
                for (let j = 0; j < message.rewarded_ids.length; ++j)
                    object.rewarded_ids[j] = message.rewarded_ids[j];
            }
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.achieved_records && message.achieved_records.length) {
                object.achieved_records = Array(message.achieved_records.length);
                for (let j = 0; j < message.achieved_records.length; ++j)
                    object.achieved_records[j] = $root.lq.ActivityBingoCardData.BingoAchievedRecord.toObject(message.achieved_records[j], options);
            }
            if (message.reward_records && message.reward_records.length) {
                object.reward_records = Array(message.reward_records.length);
                for (let j = 0; j < message.reward_records.length; ++j)
                    object.reward_records[j] = $root.lq.ActivityBingoCardData.BingoRewardRecord.toObject(message.reward_records[j], options);
            }
            return object;
        };

        ActivityBingoCardData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityBingoCardData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityBingoCardData";
        };

        ActivityBingoCardData.BingoAchievedRecord = (function() {

            function BingoAchievedRecord(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BingoAchievedRecord.prototype.pos = 0;
            BingoAchievedRecord.prototype.time = 0;

            BingoAchievedRecord.create = function create(properties) {
                return new BingoAchievedRecord(properties);
            };

            BingoAchievedRecord.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                    writer.uint32(8).uint32(message.pos);
                if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                    writer.uint32(16).uint32(message.time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BingoAchievedRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BingoAchievedRecord.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityBingoCardData.BingoAchievedRecord(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pos = value;
                            else
                                delete message.pos;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BingoAchievedRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BingoAchievedRecord.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pos != null && message.hasOwnProperty("pos"))
                    if (!$util.isInteger(message.pos))
                        return "pos: integer expected";
                if (message.time != null && message.hasOwnProperty("time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                return null;
            };

            BingoAchievedRecord.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityBingoCardData.BingoAchievedRecord)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityBingoCardData.BingoAchievedRecord();
                if (object.pos != null)
                    if (Number(object.pos) !== 0)
                        message.pos = object.pos >>> 0;
                if (object.time != null)
                    if (Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                return message;
            };

            BingoAchievedRecord.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.pos = 0;
                    object.time = 0;
                }
                if (message.pos != null && message.hasOwnProperty("pos"))
                    object.pos = message.pos;
                if (message.time != null && message.hasOwnProperty("time"))
                    object.time = message.time;
                return object;
            };

            BingoAchievedRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BingoAchievedRecord.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityBingoCardData.BingoAchievedRecord";
            };

            return BingoAchievedRecord;
        })();

        ActivityBingoCardData.BingoRewardRecord = (function() {

            function BingoRewardRecord(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BingoRewardRecord.prototype.id = 0;
            BingoRewardRecord.prototype.time = 0;

            BingoRewardRecord.create = function create(properties) {
                return new BingoRewardRecord(properties);
            };

            BingoRewardRecord.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                    writer.uint32(16).uint32(message.time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BingoRewardRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BingoRewardRecord.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityBingoCardData.BingoRewardRecord(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.time = value;
                            else
                                delete message.time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BingoRewardRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BingoRewardRecord.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.time != null && message.hasOwnProperty("time"))
                    if (!$util.isInteger(message.time))
                        return "time: integer expected";
                return null;
            };

            BingoRewardRecord.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityBingoCardData.BingoRewardRecord)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityBingoCardData.BingoRewardRecord();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.time != null)
                    if (Number(object.time) !== 0)
                        message.time = object.time >>> 0;
                return message;
            };

            BingoRewardRecord.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.time = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.time != null && message.hasOwnProperty("time"))
                    object.time = message.time;
                return object;
            };

            BingoRewardRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BingoRewardRecord.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityBingoCardData.BingoRewardRecord";
            };

            return BingoRewardRecord;
        })();

        return ActivityBingoCardData;
    })();

    lq.ActivityBingoData = (function() {

        function ActivityBingoData(properties) {
            this.cards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityBingoData.prototype.activity_id = 0;
        ActivityBingoData.prototype.cards = $util.emptyArray;

        ActivityBingoData.create = function create(properties) {
            return new ActivityBingoData(properties);
        };

        ActivityBingoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.lq.ActivityBingoCardData.encode(message.cards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityBingoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityBingoData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityBingoData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.lq.ActivityBingoCardData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityBingoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityBingoData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.cards != null && message.hasOwnProperty("cards")) {
                if (!Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.lq.ActivityBingoCardData.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        ActivityBingoData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityBingoData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityBingoData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.cards) {
                if (!Array.isArray(object.cards))
                    throw TypeError(".lq.ActivityBingoData.cards: array expected");
                message.cards = Array(object.cards.length);
                for (let i = 0; i < object.cards.length; ++i) {
                    if (typeof object.cards[i] !== "object")
                        throw TypeError(".lq.ActivityBingoData.cards: object expected");
                    message.cards[i] = $root.lq.ActivityBingoCardData.fromObject(object.cards[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityBingoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.cards = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.cards && message.cards.length) {
                object.cards = Array(message.cards.length);
                for (let j = 0; j < message.cards.length; ++j)
                    object.cards[j] = $root.lq.ActivityBingoCardData.toObject(message.cards[j], options);
            }
            return object;
        };

        ActivityBingoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityBingoData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityBingoData";
        };

        return ActivityBingoData;
    })();

    lq.ActivityIdTimeRecord = (function() {

        function ActivityIdTimeRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityIdTimeRecord.prototype.id = 0;
        ActivityIdTimeRecord.prototype.time = 0;

        ActivityIdTimeRecord.create = function create(properties) {
            return new ActivityIdTimeRecord(properties);
        };

        ActivityIdTimeRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(16).uint32(message.time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityIdTimeRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityIdTimeRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityIdTimeRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityIdTimeRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityIdTimeRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            return null;
        };

        ActivityIdTimeRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityIdTimeRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityIdTimeRecord();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.time != null)
                if (Number(object.time) !== 0)
                    message.time = object.time >>> 0;
            return message;
        };

        ActivityIdTimeRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            return object;
        };

        ActivityIdTimeRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityIdTimeRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityIdTimeRecord";
        };

        return ActivityIdTimeRecord;
    })();

    lq.ActivitySnowballData = (function() {

        function ActivitySnowballData(properties) {
            this.upgrade = [];
            this.rewarded_level = [];
            this.finished_level = [];
            this.reward_records = [];
            this.finish_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballData.prototype.activity_id = 0;
        ActivitySnowballData.prototype.level = 0;
        ActivitySnowballData.prototype.upgrade = $util.emptyArray;
        ActivitySnowballData.prototype.rewarded_level = $util.emptyArray;
        ActivitySnowballData.prototype.finished_level = $util.emptyArray;
        ActivitySnowballData.prototype.random_seed = 0;
        ActivitySnowballData.prototype.battle_id = "";
        ActivitySnowballData.prototype.level_finished_count = 0;
        ActivitySnowballData.prototype.player_combo = 0;
        ActivitySnowballData.prototype.reward_records = $util.emptyArray;
        ActivitySnowballData.prototype.finish_records = $util.emptyArray;

        ActivitySnowballData.create = function create(properties) {
            return new ActivitySnowballData(properties);
        };

        ActivitySnowballData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.upgrade != null && message.upgrade.length)
                for (let i = 0; i < message.upgrade.length; ++i)
                    $root.lq.ActivitySnowballUpgrade.encode(message.upgrade[i], writer.uint32(26).fork()).ldelim();
            if (message.rewarded_level != null && message.rewarded_level.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.rewarded_level.length; ++i)
                    writer.uint32(message.rewarded_level[i]);
                writer.ldelim();
            }
            if (message.finished_level != null && message.finished_level.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.finished_level.length; ++i)
                    writer.uint32(message.finished_level[i]);
                writer.ldelim();
            }
            if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                writer.uint32(48).uint32(message.random_seed);
            if (message.battle_id != null && Object.hasOwnProperty.call(message, "battle_id"))
                writer.uint32(58).string(message.battle_id);
            if (message.level_finished_count != null && Object.hasOwnProperty.call(message, "level_finished_count"))
                writer.uint32(64).uint32(message.level_finished_count);
            if (message.player_combo != null && Object.hasOwnProperty.call(message, "player_combo"))
                writer.uint32(72).uint32(message.player_combo);
            if (message.reward_records != null && message.reward_records.length)
                for (let i = 0; i < message.reward_records.length; ++i)
                    $root.lq.ActivityIdTimeRecord.encode(message.reward_records[i], writer.uint32(82).fork()).ldelim();
            if (message.finish_records != null && message.finish_records.length)
                for (let i = 0; i < message.finish_records.length; ++i)
                    $root.lq.ActivityIdTimeRecord.encode(message.finish_records[i], writer.uint32(90).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.upgrade && message.upgrade.length))
                            message.upgrade = [];
                        message.upgrade.push($root.lq.ActivitySnowballUpgrade.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.rewarded_level && message.rewarded_level.length))
                                message.rewarded_level = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_level.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_level && message.rewarded_level.length))
                            message.rewarded_level = [];
                        message.rewarded_level.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.finished_level && message.finished_level.length))
                                message.finished_level = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finished_level.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.finished_level && message.finished_level.length))
                            message.finished_level = [];
                        message.finished_level.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.random_seed = value;
                        else
                            delete message.random_seed;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.battle_id = value;
                        else
                            delete message.battle_id;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level_finished_count = value;
                        else
                            delete message.level_finished_count;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.player_combo = value;
                        else
                            delete message.player_combo;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_records && message.reward_records.length))
                            message.reward_records = [];
                        message.reward_records.push($root.lq.ActivityIdTimeRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.finish_records && message.finish_records.length))
                            message.finish_records = [];
                        message.finish_records.push($root.lq.ActivityIdTimeRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                if (!Array.isArray(message.upgrade))
                    return "upgrade: array expected";
                for (let i = 0; i < message.upgrade.length; ++i) {
                    let error = $root.lq.ActivitySnowballUpgrade.verify(message.upgrade[i], _depth + 1);
                    if (error)
                        return "upgrade." + error;
                }
            }
            if (message.rewarded_level != null && message.hasOwnProperty("rewarded_level")) {
                if (!Array.isArray(message.rewarded_level))
                    return "rewarded_level: array expected";
                for (let i = 0; i < message.rewarded_level.length; ++i)
                    if (!$util.isInteger(message.rewarded_level[i]))
                        return "rewarded_level: integer[] expected";
            }
            if (message.finished_level != null && message.hasOwnProperty("finished_level")) {
                if (!Array.isArray(message.finished_level))
                    return "finished_level: array expected";
                for (let i = 0; i < message.finished_level.length; ++i)
                    if (!$util.isInteger(message.finished_level[i]))
                        return "finished_level: integer[] expected";
            }
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                if (!$util.isInteger(message.random_seed))
                    return "random_seed: integer expected";
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                if (!$util.isString(message.battle_id))
                    return "battle_id: string expected";
            if (message.level_finished_count != null && message.hasOwnProperty("level_finished_count"))
                if (!$util.isInteger(message.level_finished_count))
                    return "level_finished_count: integer expected";
            if (message.player_combo != null && message.hasOwnProperty("player_combo"))
                if (!$util.isInteger(message.player_combo))
                    return "player_combo: integer expected";
            if (message.reward_records != null && message.hasOwnProperty("reward_records")) {
                if (!Array.isArray(message.reward_records))
                    return "reward_records: array expected";
                for (let i = 0; i < message.reward_records.length; ++i) {
                    let error = $root.lq.ActivityIdTimeRecord.verify(message.reward_records[i], _depth + 1);
                    if (error)
                        return "reward_records." + error;
                }
            }
            if (message.finish_records != null && message.hasOwnProperty("finish_records")) {
                if (!Array.isArray(message.finish_records))
                    return "finish_records: array expected";
                for (let i = 0; i < message.finish_records.length; ++i) {
                    let error = $root.lq.ActivityIdTimeRecord.verify(message.finish_records[i], _depth + 1);
                    if (error)
                        return "finish_records." + error;
                }
            }
            return null;
        };

        ActivitySnowballData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.upgrade) {
                if (!Array.isArray(object.upgrade))
                    throw TypeError(".lq.ActivitySnowballData.upgrade: array expected");
                message.upgrade = Array(object.upgrade.length);
                for (let i = 0; i < object.upgrade.length; ++i) {
                    if (typeof object.upgrade[i] !== "object")
                        throw TypeError(".lq.ActivitySnowballData.upgrade: object expected");
                    message.upgrade[i] = $root.lq.ActivitySnowballUpgrade.fromObject(object.upgrade[i], _depth + 1);
                }
            }
            if (object.rewarded_level) {
                if (!Array.isArray(object.rewarded_level))
                    throw TypeError(".lq.ActivitySnowballData.rewarded_level: array expected");
                message.rewarded_level = Array(object.rewarded_level.length);
                for (let i = 0; i < object.rewarded_level.length; ++i)
                    message.rewarded_level[i] = object.rewarded_level[i] >>> 0;
            }
            if (object.finished_level) {
                if (!Array.isArray(object.finished_level))
                    throw TypeError(".lq.ActivitySnowballData.finished_level: array expected");
                message.finished_level = Array(object.finished_level.length);
                for (let i = 0; i < object.finished_level.length; ++i)
                    message.finished_level[i] = object.finished_level[i] >>> 0;
            }
            if (object.random_seed != null)
                if (Number(object.random_seed) !== 0)
                    message.random_seed = object.random_seed >>> 0;
            if (object.battle_id != null)
                if (typeof object.battle_id !== "string" || object.battle_id.length)
                    message.battle_id = String(object.battle_id);
            if (object.level_finished_count != null)
                if (Number(object.level_finished_count) !== 0)
                    message.level_finished_count = object.level_finished_count >>> 0;
            if (object.player_combo != null)
                if (Number(object.player_combo) !== 0)
                    message.player_combo = object.player_combo >>> 0;
            if (object.reward_records) {
                if (!Array.isArray(object.reward_records))
                    throw TypeError(".lq.ActivitySnowballData.reward_records: array expected");
                message.reward_records = Array(object.reward_records.length);
                for (let i = 0; i < object.reward_records.length; ++i) {
                    if (typeof object.reward_records[i] !== "object")
                        throw TypeError(".lq.ActivitySnowballData.reward_records: object expected");
                    message.reward_records[i] = $root.lq.ActivityIdTimeRecord.fromObject(object.reward_records[i], _depth + 1);
                }
            }
            if (object.finish_records) {
                if (!Array.isArray(object.finish_records))
                    throw TypeError(".lq.ActivitySnowballData.finish_records: array expected");
                message.finish_records = Array(object.finish_records.length);
                for (let i = 0; i < object.finish_records.length; ++i) {
                    if (typeof object.finish_records[i] !== "object")
                        throw TypeError(".lq.ActivitySnowballData.finish_records: object expected");
                    message.finish_records[i] = $root.lq.ActivityIdTimeRecord.fromObject(object.finish_records[i], _depth + 1);
                }
            }
            return message;
        };

        ActivitySnowballData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.upgrade = [];
                object.rewarded_level = [];
                object.finished_level = [];
                object.reward_records = [];
                object.finish_records = [];
            }
            if (options.defaults) {
                object.activity_id = 0;
                object.level = 0;
                object.random_seed = 0;
                object.battle_id = "";
                object.level_finished_count = 0;
                object.player_combo = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.upgrade && message.upgrade.length) {
                object.upgrade = Array(message.upgrade.length);
                for (let j = 0; j < message.upgrade.length; ++j)
                    object.upgrade[j] = $root.lq.ActivitySnowballUpgrade.toObject(message.upgrade[j], options);
            }
            if (message.rewarded_level && message.rewarded_level.length) {
                object.rewarded_level = Array(message.rewarded_level.length);
                for (let j = 0; j < message.rewarded_level.length; ++j)
                    object.rewarded_level[j] = message.rewarded_level[j];
            }
            if (message.finished_level && message.finished_level.length) {
                object.finished_level = Array(message.finished_level.length);
                for (let j = 0; j < message.finished_level.length; ++j)
                    object.finished_level[j] = message.finished_level[j];
            }
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                object.random_seed = message.random_seed;
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                object.battle_id = message.battle_id;
            if (message.level_finished_count != null && message.hasOwnProperty("level_finished_count"))
                object.level_finished_count = message.level_finished_count;
            if (message.player_combo != null && message.hasOwnProperty("player_combo"))
                object.player_combo = message.player_combo;
            if (message.reward_records && message.reward_records.length) {
                object.reward_records = Array(message.reward_records.length);
                for (let j = 0; j < message.reward_records.length; ++j)
                    object.reward_records[j] = $root.lq.ActivityIdTimeRecord.toObject(message.reward_records[j], options);
            }
            if (message.finish_records && message.finish_records.length) {
                object.finish_records = Array(message.finish_records.length);
                for (let j = 0; j < message.finish_records.length; ++j)
                    object.finish_records[j] = $root.lq.ActivityIdTimeRecord.toObject(message.finish_records[j], options);
            }
            return object;
        };

        ActivitySnowballData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballData";
        };

        return ActivitySnowballData;
    })();

    lq.ActivitySnowballUpgrade = (function() {

        function ActivitySnowballUpgrade(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballUpgrade.prototype.id = 0;

        ActivitySnowballUpgrade.create = function create(properties) {
            return new ActivitySnowballUpgrade(properties);
        };

        ActivitySnowballUpgrade.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballUpgrade.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballUpgrade.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballUpgrade(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballUpgrade.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballUpgrade.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ActivitySnowballUpgrade.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballUpgrade)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballUpgrade();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ActivitySnowballUpgrade.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ActivitySnowballUpgrade.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballUpgrade.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballUpgrade";
        };

        return ActivitySnowballUpgrade;
    })();

    lq.ActivitySnowballUpgradeDirty = (function() {

        function ActivitySnowballUpgradeDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballUpgradeDirty.prototype.value = $util.emptyArray;
        ActivitySnowballUpgradeDirty.prototype.dirty = false;

        ActivitySnowballUpgradeDirty.create = function create(properties) {
            return new ActivitySnowballUpgradeDirty(properties);
        };

        ActivitySnowballUpgradeDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.ActivitySnowballUpgrade.encode(message.value[i], writer.uint32(10).fork()).ldelim();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(16).bool(message.dirty);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballUpgradeDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballUpgradeDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballUpgradeDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.ActivitySnowballUpgrade.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballUpgradeDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballUpgradeDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.ActivitySnowballUpgrade.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            return null;
        };

        ActivitySnowballUpgradeDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballUpgradeDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballUpgradeDirty();
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.ActivitySnowballUpgradeDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.ActivitySnowballUpgradeDirty.value: object expected");
                    message.value[i] = $root.lq.ActivitySnowballUpgrade.fromObject(object.value[i], _depth + 1);
                }
            }
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            return message;
        };

        ActivitySnowballUpgradeDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.ActivitySnowballUpgrade.toObject(message.value[j], options);
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            return object;
        };

        ActivitySnowballUpgradeDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballUpgradeDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballUpgradeDirty";
        };

        return ActivitySnowballUpgradeDirty;
    })();

    lq.ActivitySnowballValueChanges = (function() {

        function ActivitySnowballValueChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballValueChanges.prototype.level = null;
        ActivitySnowballValueChanges.prototype.upgrade = null;
        ActivitySnowballValueChanges.prototype.rewarded_level = null;
        ActivitySnowballValueChanges.prototype.finished_level = null;
        ActivitySnowballValueChanges.prototype.level_finished_count = null;
        ActivitySnowballValueChanges.prototype.player_combo = null;

        ActivitySnowballValueChanges.create = function create(properties) {
            return new ActivitySnowballValueChanges(properties);
        };

        ActivitySnowballValueChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.UInt32Dirty.encode(message.level, writer.uint32(10).fork()).ldelim();
            if (message.upgrade != null && Object.hasOwnProperty.call(message, "upgrade"))
                $root.lq.ActivitySnowballUpgradeDirty.encode(message.upgrade, writer.uint32(18).fork()).ldelim();
            if (message.rewarded_level != null && Object.hasOwnProperty.call(message, "rewarded_level"))
                $root.lq.UInt32ArrayDirty.encode(message.rewarded_level, writer.uint32(26).fork()).ldelim();
            if (message.finished_level != null && Object.hasOwnProperty.call(message, "finished_level"))
                $root.lq.UInt32ArrayDirty.encode(message.finished_level, writer.uint32(34).fork()).ldelim();
            if (message.level_finished_count != null && Object.hasOwnProperty.call(message, "level_finished_count"))
                $root.lq.UInt32Dirty.encode(message.level_finished_count, writer.uint32(42).fork()).ldelim();
            if (message.player_combo != null && Object.hasOwnProperty.call(message, "player_combo"))
                $root.lq.UInt32Dirty.encode(message.player_combo, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballValueChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballValueChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballValueChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.upgrade = $root.lq.ActivitySnowballUpgradeDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.rewarded_level = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.rewarded_level);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.finished_level = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.finished_level);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.level_finished_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level_finished_count);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.player_combo = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.player_combo);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballValueChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballValueChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.UInt32Dirty.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                let error = $root.lq.ActivitySnowballUpgradeDirty.verify(message.upgrade, _depth + 1);
                if (error)
                    return "upgrade." + error;
            }
            if (message.rewarded_level != null && message.hasOwnProperty("rewarded_level")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.rewarded_level, _depth + 1);
                if (error)
                    return "rewarded_level." + error;
            }
            if (message.finished_level != null && message.hasOwnProperty("finished_level")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.finished_level, _depth + 1);
                if (error)
                    return "finished_level." + error;
            }
            if (message.level_finished_count != null && message.hasOwnProperty("level_finished_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.level_finished_count, _depth + 1);
                if (error)
                    return "level_finished_count." + error;
            }
            if (message.player_combo != null && message.hasOwnProperty("player_combo")) {
                let error = $root.lq.UInt32Dirty.verify(message.player_combo, _depth + 1);
                if (error)
                    return "player_combo." + error;
            }
            return null;
        };

        ActivitySnowballValueChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballValueChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballValueChanges();
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.level: object expected");
                message.level = $root.lq.UInt32Dirty.fromObject(object.level, _depth + 1);
            }
            if (object.upgrade != null) {
                if (typeof object.upgrade !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.upgrade: object expected");
                message.upgrade = $root.lq.ActivitySnowballUpgradeDirty.fromObject(object.upgrade, _depth + 1);
            }
            if (object.rewarded_level != null) {
                if (typeof object.rewarded_level !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.rewarded_level: object expected");
                message.rewarded_level = $root.lq.UInt32ArrayDirty.fromObject(object.rewarded_level, _depth + 1);
            }
            if (object.finished_level != null) {
                if (typeof object.finished_level !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.finished_level: object expected");
                message.finished_level = $root.lq.UInt32ArrayDirty.fromObject(object.finished_level, _depth + 1);
            }
            if (object.level_finished_count != null) {
                if (typeof object.level_finished_count !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.level_finished_count: object expected");
                message.level_finished_count = $root.lq.UInt32Dirty.fromObject(object.level_finished_count, _depth + 1);
            }
            if (object.player_combo != null) {
                if (typeof object.player_combo !== "object")
                    throw TypeError(".lq.ActivitySnowballValueChanges.player_combo: object expected");
                message.player_combo = $root.lq.UInt32Dirty.fromObject(object.player_combo, _depth + 1);
            }
            return message;
        };

        ActivitySnowballValueChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.level = null;
                object.upgrade = null;
                object.rewarded_level = null;
                object.finished_level = null;
                object.level_finished_count = null;
                object.player_combo = null;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.UInt32Dirty.toObject(message.level, options);
            if (message.upgrade != null && message.hasOwnProperty("upgrade"))
                object.upgrade = $root.lq.ActivitySnowballUpgradeDirty.toObject(message.upgrade, options);
            if (message.rewarded_level != null && message.hasOwnProperty("rewarded_level"))
                object.rewarded_level = $root.lq.UInt32ArrayDirty.toObject(message.rewarded_level, options);
            if (message.finished_level != null && message.hasOwnProperty("finished_level"))
                object.finished_level = $root.lq.UInt32ArrayDirty.toObject(message.finished_level, options);
            if (message.level_finished_count != null && message.hasOwnProperty("level_finished_count"))
                object.level_finished_count = $root.lq.UInt32Dirty.toObject(message.level_finished_count, options);
            if (message.player_combo != null && message.hasOwnProperty("player_combo"))
                object.player_combo = $root.lq.UInt32Dirty.toObject(message.player_combo, options);
            return object;
        };

        ActivitySnowballValueChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballValueChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballValueChanges";
        };

        return ActivitySnowballValueChanges;
    })();

    lq.ActivitySnowballPlayerAttackedInfo = (function() {

        function ActivitySnowballPlayerAttackedInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballPlayerAttackedInfo.prototype.target = 0;
        ActivitySnowballPlayerAttackedInfo.prototype.track = 0;
        ActivitySnowballPlayerAttackedInfo.prototype.damage = 0;
        ActivitySnowballPlayerAttackedInfo.prototype.critia = 0;

        ActivitySnowballPlayerAttackedInfo.create = function create(properties) {
            return new ActivitySnowballPlayerAttackedInfo(properties);
        };

        ActivitySnowballPlayerAttackedInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                writer.uint32(8).uint32(message.target);
            if (message.track != null && Object.hasOwnProperty.call(message, "track"))
                writer.uint32(16).uint32(message.track);
            if (message.damage != null && Object.hasOwnProperty.call(message, "damage"))
                writer.uint32(24).uint32(message.damage);
            if (message.critia != null && Object.hasOwnProperty.call(message, "critia"))
                writer.uint32(32).uint32(message.critia);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballPlayerAttackedInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballPlayerAttackedInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballPlayerAttackedInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target = value;
                        else
                            delete message.target;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.track = value;
                        else
                            delete message.track;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.damage = value;
                        else
                            delete message.damage;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.critia = value;
                        else
                            delete message.critia;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballPlayerAttackedInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballPlayerAttackedInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target != null && message.hasOwnProperty("target"))
                if (!$util.isInteger(message.target))
                    return "target: integer expected";
            if (message.track != null && message.hasOwnProperty("track"))
                if (!$util.isInteger(message.track))
                    return "track: integer expected";
            if (message.damage != null && message.hasOwnProperty("damage"))
                if (!$util.isInteger(message.damage))
                    return "damage: integer expected";
            if (message.critia != null && message.hasOwnProperty("critia"))
                if (!$util.isInteger(message.critia))
                    return "critia: integer expected";
            return null;
        };

        ActivitySnowballPlayerAttackedInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballPlayerAttackedInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballPlayerAttackedInfo();
            if (object.target != null)
                if (Number(object.target) !== 0)
                    message.target = object.target >>> 0;
            if (object.track != null)
                if (Number(object.track) !== 0)
                    message.track = object.track >>> 0;
            if (object.damage != null)
                if (Number(object.damage) !== 0)
                    message.damage = object.damage >>> 0;
            if (object.critia != null)
                if (Number(object.critia) !== 0)
                    message.critia = object.critia >>> 0;
            return message;
        };

        ActivitySnowballPlayerAttackedInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target = 0;
                object.track = 0;
                object.damage = 0;
                object.critia = 0;
            }
            if (message.target != null && message.hasOwnProperty("target"))
                object.target = message.target;
            if (message.track != null && message.hasOwnProperty("track"))
                object.track = message.track;
            if (message.damage != null && message.hasOwnProperty("damage"))
                object.damage = message.damage;
            if (message.critia != null && message.hasOwnProperty("critia"))
                object.critia = message.critia;
            return object;
        };

        ActivitySnowballPlayerAttackedInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballPlayerAttackedInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballPlayerAttackedInfo";
        };

        return ActivitySnowballPlayerAttackedInfo;
    })();

    lq.ActivitySnowballPlayerAction = (function() {

        function ActivitySnowballPlayerAction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballPlayerAction.prototype.tick = 0;
        ActivitySnowballPlayerAction.prototype.track = 0;

        ActivitySnowballPlayerAction.create = function create(properties) {
            return new ActivitySnowballPlayerAction(properties);
        };

        ActivitySnowballPlayerAction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tick != null && Object.hasOwnProperty.call(message, "tick"))
                writer.uint32(8).uint32(message.tick);
            if (message.track != null && Object.hasOwnProperty.call(message, "track"))
                writer.uint32(16).uint32(message.track);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballPlayerAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballPlayerAction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballPlayerAction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tick = value;
                        else
                            delete message.tick;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.track = value;
                        else
                            delete message.track;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballPlayerAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballPlayerAction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tick != null && message.hasOwnProperty("tick"))
                if (!$util.isInteger(message.tick))
                    return "tick: integer expected";
            if (message.track != null && message.hasOwnProperty("track"))
                if (!$util.isInteger(message.track))
                    return "track: integer expected";
            return null;
        };

        ActivitySnowballPlayerAction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballPlayerAction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballPlayerAction();
            if (object.tick != null)
                if (Number(object.tick) !== 0)
                    message.tick = object.tick >>> 0;
            if (object.track != null)
                if (Number(object.track) !== 0)
                    message.track = object.track >>> 0;
            return message;
        };

        ActivitySnowballPlayerAction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tick = 0;
                object.track = 0;
            }
            if (message.tick != null && message.hasOwnProperty("tick"))
                object.tick = message.tick;
            if (message.track != null && message.hasOwnProperty("track"))
                object.track = message.track;
            return object;
        };

        ActivitySnowballPlayerAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballPlayerAction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballPlayerAction";
        };

        return ActivitySnowballPlayerAction;
    })();

    lq.ActivitySnowballPlayerState = (function() {

        function ActivitySnowballPlayerState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivitySnowballPlayerState.prototype.player = 0;
        ActivitySnowballPlayerState.prototype.hp = 0;
        ActivitySnowballPlayerState.prototype.mp = 0;

        ActivitySnowballPlayerState.create = function create(properties) {
            return new ActivitySnowballPlayerState(properties);
        };

        ActivitySnowballPlayerState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.player != null && Object.hasOwnProperty.call(message, "player"))
                writer.uint32(8).uint32(message.player);
            if (message.hp != null && Object.hasOwnProperty.call(message, "hp"))
                writer.uint32(16).uint32(message.hp);
            if (message.mp != null && Object.hasOwnProperty.call(message, "mp"))
                writer.uint32(24).uint32(message.mp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivitySnowballPlayerState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivitySnowballPlayerState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivitySnowballPlayerState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.player = value;
                        else
                            delete message.player;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.hp = value;
                        else
                            delete message.hp;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mp = value;
                        else
                            delete message.mp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivitySnowballPlayerState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivitySnowballPlayerState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player != null && message.hasOwnProperty("player"))
                if (!$util.isInteger(message.player))
                    return "player: integer expected";
            if (message.hp != null && message.hasOwnProperty("hp"))
                if (!$util.isInteger(message.hp))
                    return "hp: integer expected";
            if (message.mp != null && message.hasOwnProperty("mp"))
                if (!$util.isInteger(message.mp))
                    return "mp: integer expected";
            return null;
        };

        ActivitySnowballPlayerState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivitySnowballPlayerState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivitySnowballPlayerState();
            if (object.player != null)
                if (Number(object.player) !== 0)
                    message.player = object.player >>> 0;
            if (object.hp != null)
                if (Number(object.hp) !== 0)
                    message.hp = object.hp >>> 0;
            if (object.mp != null)
                if (Number(object.mp) !== 0)
                    message.mp = object.mp >>> 0;
            return message;
        };

        ActivitySnowballPlayerState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.player = 0;
                object.hp = 0;
                object.mp = 0;
            }
            if (message.player != null && message.hasOwnProperty("player"))
                object.player = message.player;
            if (message.hp != null && message.hasOwnProperty("hp"))
                object.hp = message.hp;
            if (message.mp != null && message.hasOwnProperty("mp"))
                object.mp = message.mp;
            return object;
        };

        ActivitySnowballPlayerState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivitySnowballPlayerState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivitySnowballPlayerState";
        };

        return ActivitySnowballPlayerState;
    })();

    lq.SnowballActivityBossAction = (function() {

        function SnowballActivityBossAction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SnowballActivityBossAction.prototype.tick = 0;
        SnowballActivityBossAction.prototype.type = 0;
        SnowballActivityBossAction.prototype.track = 0;
        SnowballActivityBossAction.prototype.attack_info = null;
        SnowballActivityBossAction.prototype.mp_consume_info = null;

        SnowballActivityBossAction.create = function create(properties) {
            return new SnowballActivityBossAction(properties);
        };

        SnowballActivityBossAction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tick != null && Object.hasOwnProperty.call(message, "tick"))
                writer.uint32(8).uint32(message.tick);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.track != null && Object.hasOwnProperty.call(message, "track"))
                writer.uint32(24).uint32(message.track);
            if (message.attack_info != null && Object.hasOwnProperty.call(message, "attack_info"))
                $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo.encode(message.attack_info, writer.uint32(34).fork()).ldelim();
            if (message.mp_consume_info != null && Object.hasOwnProperty.call(message, "mp_consume_info"))
                $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo.encode(message.mp_consume_info, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SnowballActivityBossAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SnowballActivityBossAction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SnowballActivityBossAction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tick = value;
                        else
                            delete message.tick;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.track = value;
                        else
                            delete message.track;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.attack_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.attack_info);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.mp_consume_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.mp_consume_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SnowballActivityBossAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SnowballActivityBossAction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tick != null && message.hasOwnProperty("tick"))
                if (!$util.isInteger(message.tick))
                    return "tick: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.track != null && message.hasOwnProperty("track"))
                if (!$util.isInteger(message.track))
                    return "track: integer expected";
            if (message.attack_info != null && message.hasOwnProperty("attack_info")) {
                let error = $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo.verify(message.attack_info, _depth + 1);
                if (error)
                    return "attack_info." + error;
            }
            if (message.mp_consume_info != null && message.hasOwnProperty("mp_consume_info")) {
                let error = $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo.verify(message.mp_consume_info, _depth + 1);
                if (error)
                    return "mp_consume_info." + error;
            }
            return null;
        };

        SnowballActivityBossAction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SnowballActivityBossAction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SnowballActivityBossAction();
            if (object.tick != null)
                if (Number(object.tick) !== 0)
                    message.tick = object.tick >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.track != null)
                if (Number(object.track) !== 0)
                    message.track = object.track >>> 0;
            if (object.attack_info != null) {
                if (typeof object.attack_info !== "object")
                    throw TypeError(".lq.SnowballActivityBossAction.attack_info: object expected");
                message.attack_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo.fromObject(object.attack_info, _depth + 1);
            }
            if (object.mp_consume_info != null) {
                if (typeof object.mp_consume_info !== "object")
                    throw TypeError(".lq.SnowballActivityBossAction.mp_consume_info: object expected");
                message.mp_consume_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo.fromObject(object.mp_consume_info, _depth + 1);
            }
            return message;
        };

        SnowballActivityBossAction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tick = 0;
                object.type = 0;
                object.track = 0;
                object.attack_info = null;
                object.mp_consume_info = null;
            }
            if (message.tick != null && message.hasOwnProperty("tick"))
                object.tick = message.tick;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.track != null && message.hasOwnProperty("track"))
                object.track = message.track;
            if (message.attack_info != null && message.hasOwnProperty("attack_info"))
                object.attack_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo.toObject(message.attack_info, options);
            if (message.mp_consume_info != null && message.hasOwnProperty("mp_consume_info"))
                object.mp_consume_info = $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo.toObject(message.mp_consume_info, options);
            return object;
        };

        SnowballActivityBossAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SnowballActivityBossAction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SnowballActivityBossAction";
        };

        SnowballActivityBossAction.SnowballActivityBossAttackInfo = (function() {

            function SnowballActivityBossAttackInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SnowballActivityBossAttackInfo.prototype.damage = 0;
            SnowballActivityBossAttackInfo.prototype.is_con_attack = 0;

            SnowballActivityBossAttackInfo.create = function create(properties) {
                return new SnowballActivityBossAttackInfo(properties);
            };

            SnowballActivityBossAttackInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.damage != null && Object.hasOwnProperty.call(message, "damage"))
                    writer.uint32(8).uint32(message.damage);
                if (message.is_con_attack != null && Object.hasOwnProperty.call(message, "is_con_attack"))
                    writer.uint32(16).uint32(message.is_con_attack);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SnowballActivityBossAttackInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SnowballActivityBossAttackInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.damage = value;
                            else
                                delete message.damage;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.is_con_attack = value;
                            else
                                delete message.is_con_attack;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SnowballActivityBossAttackInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SnowballActivityBossAttackInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.damage != null && message.hasOwnProperty("damage"))
                    if (!$util.isInteger(message.damage))
                        return "damage: integer expected";
                if (message.is_con_attack != null && message.hasOwnProperty("is_con_attack"))
                    if (!$util.isInteger(message.is_con_attack))
                        return "is_con_attack: integer expected";
                return null;
            };

            SnowballActivityBossAttackInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo();
                if (object.damage != null)
                    if (Number(object.damage) !== 0)
                        message.damage = object.damage >>> 0;
                if (object.is_con_attack != null)
                    if (Number(object.is_con_attack) !== 0)
                        message.is_con_attack = object.is_con_attack >>> 0;
                return message;
            };

            SnowballActivityBossAttackInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.damage = 0;
                    object.is_con_attack = 0;
                }
                if (message.damage != null && message.hasOwnProperty("damage"))
                    object.damage = message.damage;
                if (message.is_con_attack != null && message.hasOwnProperty("is_con_attack"))
                    object.is_con_attack = message.is_con_attack;
                return object;
            };

            SnowballActivityBossAttackInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SnowballActivityBossAttackInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SnowballActivityBossAction.SnowballActivityBossAttackInfo";
            };

            return SnowballActivityBossAttackInfo;
        })();

        SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo = (function() {

            function SnowballActivityBossMPConsumeInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SnowballActivityBossMPConsumeInfo.prototype.mp_consume = 0;
            SnowballActivityBossMPConsumeInfo.prototype.before_delay = 0;
            SnowballActivityBossMPConsumeInfo.prototype.after_delay = 0;

            SnowballActivityBossMPConsumeInfo.create = function create(properties) {
                return new SnowballActivityBossMPConsumeInfo(properties);
            };

            SnowballActivityBossMPConsumeInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.mp_consume != null && Object.hasOwnProperty.call(message, "mp_consume"))
                    writer.uint32(8).uint32(message.mp_consume);
                if (message.before_delay != null && Object.hasOwnProperty.call(message, "before_delay"))
                    writer.uint32(16).uint32(message.before_delay);
                if (message.after_delay != null && Object.hasOwnProperty.call(message, "after_delay"))
                    writer.uint32(24).uint32(message.after_delay);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SnowballActivityBossMPConsumeInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SnowballActivityBossMPConsumeInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.mp_consume = value;
                            else
                                delete message.mp_consume;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.before_delay = value;
                            else
                                delete message.before_delay;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.after_delay = value;
                            else
                                delete message.after_delay;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SnowballActivityBossMPConsumeInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SnowballActivityBossMPConsumeInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mp_consume != null && message.hasOwnProperty("mp_consume"))
                    if (!$util.isInteger(message.mp_consume))
                        return "mp_consume: integer expected";
                if (message.before_delay != null && message.hasOwnProperty("before_delay"))
                    if (!$util.isInteger(message.before_delay))
                        return "before_delay: integer expected";
                if (message.after_delay != null && message.hasOwnProperty("after_delay"))
                    if (!$util.isInteger(message.after_delay))
                        return "after_delay: integer expected";
                return null;
            };

            SnowballActivityBossMPConsumeInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo();
                if (object.mp_consume != null)
                    if (Number(object.mp_consume) !== 0)
                        message.mp_consume = object.mp_consume >>> 0;
                if (object.before_delay != null)
                    if (Number(object.before_delay) !== 0)
                        message.before_delay = object.before_delay >>> 0;
                if (object.after_delay != null)
                    if (Number(object.after_delay) !== 0)
                        message.after_delay = object.after_delay >>> 0;
                return message;
            };

            SnowballActivityBossMPConsumeInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.mp_consume = 0;
                    object.before_delay = 0;
                    object.after_delay = 0;
                }
                if (message.mp_consume != null && message.hasOwnProperty("mp_consume"))
                    object.mp_consume = message.mp_consume;
                if (message.before_delay != null && message.hasOwnProperty("before_delay"))
                    object.before_delay = message.before_delay;
                if (message.after_delay != null && message.hasOwnProperty("after_delay"))
                    object.after_delay = message.after_delay;
                return object;
            };

            SnowballActivityBossMPConsumeInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SnowballActivityBossMPConsumeInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.SnowballActivityBossAction.SnowballActivityBossMPConsumeInfo";
            };

            return SnowballActivityBossMPConsumeInfo;
        })();

        return SnowballActivityBossAction;
    })();

    lq.ClientDeviceInfoLog = (function() {

        function ClientDeviceInfoLog(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ClientDeviceInfoLog.prototype.device_info = null;
        ClientDeviceInfoLog.prototype.login_time = 0;
        ClientDeviceInfoLog.prototype.account_id = 0;

        ClientDeviceInfoLog.create = function create(properties) {
            return new ClientDeviceInfoLog(properties);
        };

        ClientDeviceInfoLog.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.device_info != null && Object.hasOwnProperty.call(message, "device_info"))
                $root.lq.ClientDeviceInfo.encode(message.device_info, writer.uint32(10).fork()).ldelim();
            if (message.login_time != null && Object.hasOwnProperty.call(message, "login_time"))
                writer.uint32(16).uint32(message.login_time);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ClientDeviceInfoLog.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ClientDeviceInfoLog.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ClientDeviceInfoLog(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.device_info = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device_info);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.login_time = value;
                        else
                            delete message.login_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ClientDeviceInfoLog.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ClientDeviceInfoLog.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.device_info != null && message.hasOwnProperty("device_info")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device_info, _depth + 1);
                if (error)
                    return "device_info." + error;
            }
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                if (!$util.isInteger(message.login_time))
                    return "login_time: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ClientDeviceInfoLog.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ClientDeviceInfoLog)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ClientDeviceInfoLog();
            if (object.device_info != null) {
                if (typeof object.device_info !== "object")
                    throw TypeError(".lq.ClientDeviceInfoLog.device_info: object expected");
                message.device_info = $root.lq.ClientDeviceInfo.fromObject(object.device_info, _depth + 1);
            }
            if (object.login_time != null)
                if (Number(object.login_time) !== 0)
                    message.login_time = object.login_time >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ClientDeviceInfoLog.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.device_info = null;
                object.login_time = 0;
                object.account_id = 0;
            }
            if (message.device_info != null && message.hasOwnProperty("device_info"))
                object.device_info = $root.lq.ClientDeviceInfo.toObject(message.device_info, options);
            if (message.login_time != null && message.hasOwnProperty("login_time"))
                object.login_time = message.login_time;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ClientDeviceInfoLog.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientDeviceInfoLog.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ClientDeviceInfoLog";
        };

        return ClientDeviceInfoLog;
    })();

    lq.MarathonGameRecord = (function() {

        function MarathonGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MarathonGameRecord.prototype.point = 0;
        MarathonGameRecord.prototype.distance = 0;
        MarathonGameRecord.prototype.used_tick = 0;

        MarathonGameRecord.create = function create(properties) {
            return new MarathonGameRecord(properties);
        };

        MarathonGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(8).uint32(message.point);
            if (message.distance != null && Object.hasOwnProperty.call(message, "distance"))
                writer.uint32(16).uint32(message.distance);
            if (message.used_tick != null && Object.hasOwnProperty.call(message, "used_tick"))
                writer.uint32(24).uint32(message.used_tick);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MarathonGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MarathonGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MarathonGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.distance = value;
                        else
                            delete message.distance;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.used_tick = value;
                        else
                            delete message.used_tick;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MarathonGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MarathonGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.distance != null && message.hasOwnProperty("distance"))
                if (!$util.isInteger(message.distance))
                    return "distance: integer expected";
            if (message.used_tick != null && message.hasOwnProperty("used_tick"))
                if (!$util.isInteger(message.used_tick))
                    return "used_tick: integer expected";
            return null;
        };

        MarathonGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MarathonGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MarathonGameRecord();
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point >>> 0;
            if (object.distance != null)
                if (Number(object.distance) !== 0)
                    message.distance = object.distance >>> 0;
            if (object.used_tick != null)
                if (Number(object.used_tick) !== 0)
                    message.used_tick = object.used_tick >>> 0;
            return message;
        };

        MarathonGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.point = 0;
                object.distance = 0;
                object.used_tick = 0;
            }
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.distance != null && message.hasOwnProperty("distance"))
                object.distance = message.distance;
            if (message.used_tick != null && message.hasOwnProperty("used_tick"))
                object.used_tick = message.used_tick;
            return object;
        };

        MarathonGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MarathonGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MarathonGameRecord";
        };

        return MarathonGameRecord;
    })();

    lq.ActivityMarathonData = (function() {

        function ActivityMarathonData(properties) {
            this.history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMarathonData.prototype.activity_id = 0;
        ActivityMarathonData.prototype.highest_record = null;
        ActivityMarathonData.prototype.race_data = null;
        ActivityMarathonData.prototype.history = $util.emptyArray;

        ActivityMarathonData.create = function create(properties) {
            return new ActivityMarathonData(properties);
        };

        ActivityMarathonData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.highest_record != null && Object.hasOwnProperty.call(message, "highest_record"))
                $root.lq.MarathonGameRecord.encode(message.highest_record, writer.uint32(18).fork()).ldelim();
            if (message.race_data != null && Object.hasOwnProperty.call(message, "race_data"))
                $root.lq.ActivityMarathonData.MarathonRaceData.encode(message.race_data, writer.uint32(26).fork()).ldelim();
            if (message.history != null && message.history.length)
                for (let i = 0; i < message.history.length; ++i)
                    $root.lq.ActivityMarathonData.MarathonRaceHistory.encode(message.history[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMarathonData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMarathonData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMarathonData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.highest_record = $root.lq.MarathonGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_record);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.decode(reader, reader.uint32(), undefined, _depth + 1, message.race_data);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.history && message.history.length))
                            message.history = [];
                        message.history.push($root.lq.ActivityMarathonData.MarathonRaceHistory.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMarathonData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMarathonData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.highest_record != null && message.hasOwnProperty("highest_record")) {
                let error = $root.lq.MarathonGameRecord.verify(message.highest_record, _depth + 1);
                if (error)
                    return "highest_record." + error;
            }
            if (message.race_data != null && message.hasOwnProperty("race_data")) {
                let error = $root.lq.ActivityMarathonData.MarathonRaceData.verify(message.race_data, _depth + 1);
                if (error)
                    return "race_data." + error;
            }
            if (message.history != null && message.hasOwnProperty("history")) {
                if (!Array.isArray(message.history))
                    return "history: array expected";
                for (let i = 0; i < message.history.length; ++i) {
                    let error = $root.lq.ActivityMarathonData.MarathonRaceHistory.verify(message.history[i], _depth + 1);
                    if (error)
                        return "history." + error;
                }
            }
            return null;
        };

        ActivityMarathonData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMarathonData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMarathonData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.highest_record != null) {
                if (typeof object.highest_record !== "object")
                    throw TypeError(".lq.ActivityMarathonData.highest_record: object expected");
                message.highest_record = $root.lq.MarathonGameRecord.fromObject(object.highest_record, _depth + 1);
            }
            if (object.race_data != null) {
                if (typeof object.race_data !== "object")
                    throw TypeError(".lq.ActivityMarathonData.race_data: object expected");
                message.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.fromObject(object.race_data, _depth + 1);
            }
            if (object.history) {
                if (!Array.isArray(object.history))
                    throw TypeError(".lq.ActivityMarathonData.history: array expected");
                message.history = Array(object.history.length);
                for (let i = 0; i < object.history.length; ++i) {
                    if (typeof object.history[i] !== "object")
                        throw TypeError(".lq.ActivityMarathonData.history: object expected");
                    message.history[i] = $root.lq.ActivityMarathonData.MarathonRaceHistory.fromObject(object.history[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityMarathonData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.history = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.highest_record = null;
                object.race_data = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.highest_record != null && message.hasOwnProperty("highest_record"))
                object.highest_record = $root.lq.MarathonGameRecord.toObject(message.highest_record, options);
            if (message.race_data != null && message.hasOwnProperty("race_data"))
                object.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.toObject(message.race_data, options);
            if (message.history && message.history.length) {
                object.history = Array(message.history.length);
                for (let j = 0; j < message.history.length; ++j)
                    object.history[j] = $root.lq.ActivityMarathonData.MarathonRaceHistory.toObject(message.history[j], options);
            }
            return object;
        };

        ActivityMarathonData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMarathonData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMarathonData";
        };

        ActivityMarathonData.MarathonRaceData = (function() {

            function MarathonRaceData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MarathonRaceData.prototype.id = "";
            MarathonRaceData.prototype.random_seed = 0;
            MarathonRaceData.prototype.start_time = 0;

            MarathonRaceData.create = function create(properties) {
                return new MarathonRaceData(properties);
            };

            MarathonRaceData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(10).string(message.id);
                if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                    writer.uint32(16).uint32(message.random_seed);
                if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                    writer.uint32(24).uint32(message.start_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MarathonRaceData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MarathonRaceData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMarathonData.MarathonRaceData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.random_seed = value;
                            else
                                delete message.random_seed;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.start_time = value;
                            else
                                delete message.start_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MarathonRaceData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MarathonRaceData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isString(message.id))
                        return "id: string expected";
                if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                    if (!$util.isInteger(message.random_seed))
                        return "random_seed: integer expected";
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    if (!$util.isInteger(message.start_time))
                        return "start_time: integer expected";
                return null;
            };

            MarathonRaceData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMarathonData.MarathonRaceData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMarathonData.MarathonRaceData();
                if (object.id != null)
                    if (typeof object.id !== "string" || object.id.length)
                        message.id = String(object.id);
                if (object.random_seed != null)
                    if (Number(object.random_seed) !== 0)
                        message.random_seed = object.random_seed >>> 0;
                if (object.start_time != null)
                    if (Number(object.start_time) !== 0)
                        message.start_time = object.start_time >>> 0;
                return message;
            };

            MarathonRaceData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = "";
                    object.random_seed = 0;
                    object.start_time = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                    object.random_seed = message.random_seed;
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    object.start_time = message.start_time;
                return object;
            };

            MarathonRaceData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarathonRaceData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMarathonData.MarathonRaceData";
            };

            return MarathonRaceData;
        })();

        ActivityMarathonData.MarathonRaceHistory = (function() {

            function MarathonRaceHistory(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MarathonRaceHistory.prototype.race_data = null;
            MarathonRaceHistory.prototype.record = null;

            MarathonRaceHistory.create = function create(properties) {
                return new MarathonRaceHistory(properties);
            };

            MarathonRaceHistory.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.race_data != null && Object.hasOwnProperty.call(message, "race_data"))
                    $root.lq.ActivityMarathonData.MarathonRaceData.encode(message.race_data, writer.uint32(10).fork()).ldelim();
                if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                    $root.lq.MarathonGameRecord.encode(message.record, writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MarathonRaceHistory.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MarathonRaceHistory.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMarathonData.MarathonRaceHistory(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.decode(reader, reader.uint32(), undefined, _depth + 1, message.race_data);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.record = $root.lq.MarathonGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MarathonRaceHistory.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MarathonRaceHistory.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.race_data != null && message.hasOwnProperty("race_data")) {
                    let error = $root.lq.ActivityMarathonData.MarathonRaceData.verify(message.race_data, _depth + 1);
                    if (error)
                        return "race_data." + error;
                }
                if (message.record != null && message.hasOwnProperty("record")) {
                    let error = $root.lq.MarathonGameRecord.verify(message.record, _depth + 1);
                    if (error)
                        return "record." + error;
                }
                return null;
            };

            MarathonRaceHistory.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ActivityMarathonData.MarathonRaceHistory)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ActivityMarathonData.MarathonRaceHistory();
                if (object.race_data != null) {
                    if (typeof object.race_data !== "object")
                        throw TypeError(".lq.ActivityMarathonData.MarathonRaceHistory.race_data: object expected");
                    message.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.fromObject(object.race_data, _depth + 1);
                }
                if (object.record != null) {
                    if (typeof object.record !== "object")
                        throw TypeError(".lq.ActivityMarathonData.MarathonRaceHistory.record: object expected");
                    message.record = $root.lq.MarathonGameRecord.fromObject(object.record, _depth + 1);
                }
                return message;
            };

            MarathonRaceHistory.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.race_data = null;
                    object.record = null;
                }
                if (message.race_data != null && message.hasOwnProperty("race_data"))
                    object.race_data = $root.lq.ActivityMarathonData.MarathonRaceData.toObject(message.race_data, options);
                if (message.record != null && message.hasOwnProperty("record"))
                    object.record = $root.lq.MarathonGameRecord.toObject(message.record, options);
                return object;
            };

            MarathonRaceHistory.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarathonRaceHistory.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ActivityMarathonData.MarathonRaceHistory";
            };

            return MarathonRaceHistory;
        })();

        return ActivityMarathonData;
    })();

    lq.ActivityMarathonCheckData = (function() {

        function ActivityMarathonCheckData(properties) {
            this.item = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityMarathonCheckData.prototype.round = 0;
        ActivityMarathonCheckData.prototype.item = $util.emptyArray;
        ActivityMarathonCheckData.prototype.tile = "";
        ActivityMarathonCheckData.prototype.tick = 0;
        ActivityMarathonCheckData.prototype.point = 0;
        ActivityMarathonCheckData.prototype.time_end = 0;

        ActivityMarathonCheckData.create = function create(properties) {
            return new ActivityMarathonCheckData(properties);
        };

        ActivityMarathonCheckData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(8).uint32(message.round);
            if (message.item != null && message.item.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.item.length; ++i)
                    writer.uint32(message.item[i]);
                writer.ldelim();
            }
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(26).string(message.tile);
            if (message.tick != null && Object.hasOwnProperty.call(message, "tick"))
                writer.uint32(32).uint32(message.tick);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(40).uint32(message.point);
            if (message.time_end != null && Object.hasOwnProperty.call(message, "time_end"))
                writer.uint32(48).uint32(message.time_end);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityMarathonCheckData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityMarathonCheckData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityMarathonCheckData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.item && message.item.length))
                                message.item = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.item.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.item && message.item.length))
                            message.item = [];
                        message.item.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tick = value;
                        else
                            delete message.tick;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_end = value;
                        else
                            delete message.time_end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityMarathonCheckData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityMarathonCheckData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.item != null && message.hasOwnProperty("item")) {
                if (!Array.isArray(message.item))
                    return "item: array expected";
                for (let i = 0; i < message.item.length; ++i)
                    if (!$util.isInteger(message.item[i]))
                        return "item: integer[] expected";
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.tick != null && message.hasOwnProperty("tick"))
                if (!$util.isInteger(message.tick))
                    return "tick: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.time_end != null && message.hasOwnProperty("time_end"))
                if (!$util.isInteger(message.time_end))
                    return "time_end: integer expected";
            return null;
        };

        ActivityMarathonCheckData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityMarathonCheckData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityMarathonCheckData();
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.item) {
                if (!Array.isArray(object.item))
                    throw TypeError(".lq.ActivityMarathonCheckData.item: array expected");
                message.item = Array(object.item.length);
                for (let i = 0; i < object.item.length; ++i)
                    message.item[i] = object.item[i] >>> 0;
            }
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.tick != null)
                if (Number(object.tick) !== 0)
                    message.tick = object.tick >>> 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point >>> 0;
            if (object.time_end != null)
                if (Number(object.time_end) !== 0)
                    message.time_end = object.time_end >>> 0;
            return message;
        };

        ActivityMarathonCheckData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.item = [];
            if (options.defaults) {
                object.round = 0;
                object.tile = "";
                object.tick = 0;
                object.point = 0;
                object.time_end = 0;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.item && message.item.length) {
                object.item = Array(message.item.length);
                for (let j = 0; j < message.item.length; ++j)
                    object.item[j] = message.item[j];
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.tick != null && message.hasOwnProperty("tick"))
                object.tick = message.tick;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.time_end != null && message.hasOwnProperty("time_end"))
                object.time_end = message.time_end;
            return object;
        };

        ActivityMarathonCheckData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityMarathonCheckData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityMarathonCheckData";
        };

        return ActivityMarathonCheckData;
    })();

    lq.BannerActivityData = (function() {

        function BannerActivityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        BannerActivityData.prototype.activity_id = 0;
        BannerActivityData.prototype.tag_img_url = "";
        BannerActivityData.prototype.banner_background_url = "";
        BannerActivityData.prototype.banner_button_url = "";
        BannerActivityData.prototype.tag_name = "";
        BannerActivityData.prototype.start_time = 0;
        BannerActivityData.prototype.end_time = 0;
        BannerActivityData.prototype.url = "";
        BannerActivityData.prototype.type = 0;
        BannerActivityData.prototype.sort = 0;
        BannerActivityData.prototype.need_popout = 0;

        BannerActivityData.create = function create(properties) {
            return new BannerActivityData(properties);
        };

        BannerActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.tag_img_url != null && Object.hasOwnProperty.call(message, "tag_img_url"))
                writer.uint32(18).string(message.tag_img_url);
            if (message.banner_background_url != null && Object.hasOwnProperty.call(message, "banner_background_url"))
                writer.uint32(26).string(message.banner_background_url);
            if (message.banner_button_url != null && Object.hasOwnProperty.call(message, "banner_button_url"))
                writer.uint32(34).string(message.banner_button_url);
            if (message.tag_name != null && Object.hasOwnProperty.call(message, "tag_name"))
                writer.uint32(42).string(message.tag_name);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(48).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(56).uint32(message.end_time);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(66).string(message.url);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(72).uint32(message.type);
            if (message.sort != null && Object.hasOwnProperty.call(message, "sort"))
                writer.uint32(80).uint32(message.sort);
            if (message.need_popout != null && Object.hasOwnProperty.call(message, "need_popout"))
                writer.uint32(88).uint32(message.need_popout);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BannerActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        BannerActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.BannerActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag_img_url = value;
                        else
                            delete message.tag_img_url;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.banner_background_url = value;
                        else
                            delete message.banner_background_url;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.banner_button_url = value;
                        else
                            delete message.banner_button_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag_name = value;
                        else
                            delete message.tag_name;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.url = value;
                        else
                            delete message.url;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sort = value;
                        else
                            delete message.sort;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.need_popout = value;
                        else
                            delete message.need_popout;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        BannerActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        BannerActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.tag_img_url != null && message.hasOwnProperty("tag_img_url"))
                if (!$util.isString(message.tag_img_url))
                    return "tag_img_url: string expected";
            if (message.banner_background_url != null && message.hasOwnProperty("banner_background_url"))
                if (!$util.isString(message.banner_background_url))
                    return "banner_background_url: string expected";
            if (message.banner_button_url != null && message.hasOwnProperty("banner_button_url"))
                if (!$util.isString(message.banner_button_url))
                    return "banner_button_url: string expected";
            if (message.tag_name != null && message.hasOwnProperty("tag_name"))
                if (!$util.isString(message.tag_name))
                    return "tag_name: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.url != null && message.hasOwnProperty("url"))
                if (!$util.isString(message.url))
                    return "url: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.sort != null && message.hasOwnProperty("sort"))
                if (!$util.isInteger(message.sort))
                    return "sort: integer expected";
            if (message.need_popout != null && message.hasOwnProperty("need_popout"))
                if (!$util.isInteger(message.need_popout))
                    return "need_popout: integer expected";
            return null;
        };

        BannerActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.BannerActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.BannerActivityData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.tag_img_url != null)
                if (typeof object.tag_img_url !== "string" || object.tag_img_url.length)
                    message.tag_img_url = String(object.tag_img_url);
            if (object.banner_background_url != null)
                if (typeof object.banner_background_url !== "string" || object.banner_background_url.length)
                    message.banner_background_url = String(object.banner_background_url);
            if (object.banner_button_url != null)
                if (typeof object.banner_button_url !== "string" || object.banner_button_url.length)
                    message.banner_button_url = String(object.banner_button_url);
            if (object.tag_name != null)
                if (typeof object.tag_name !== "string" || object.tag_name.length)
                    message.tag_name = String(object.tag_name);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.url != null)
                if (typeof object.url !== "string" || object.url.length)
                    message.url = String(object.url);
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.sort != null)
                if (Number(object.sort) !== 0)
                    message.sort = object.sort >>> 0;
            if (object.need_popout != null)
                if (Number(object.need_popout) !== 0)
                    message.need_popout = object.need_popout >>> 0;
            return message;
        };

        BannerActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.tag_img_url = "";
                object.banner_background_url = "";
                object.banner_button_url = "";
                object.tag_name = "";
                object.start_time = 0;
                object.end_time = 0;
                object.url = "";
                object.type = 0;
                object.sort = 0;
                object.need_popout = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.tag_img_url != null && message.hasOwnProperty("tag_img_url"))
                object.tag_img_url = message.tag_img_url;
            if (message.banner_background_url != null && message.hasOwnProperty("banner_background_url"))
                object.banner_background_url = message.banner_background_url;
            if (message.banner_button_url != null && message.hasOwnProperty("banner_button_url"))
                object.banner_button_url = message.banner_button_url;
            if (message.tag_name != null && message.hasOwnProperty("tag_name"))
                object.tag_name = message.tag_name;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.url != null && message.hasOwnProperty("url"))
                object.url = message.url;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.sort != null && message.hasOwnProperty("sort"))
                object.sort = message.sort;
            if (message.need_popout != null && message.hasOwnProperty("need_popout"))
                object.need_popout = message.need_popout;
            return object;
        };

        BannerActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BannerActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.BannerActivityData";
        };

        return BannerActivityData;
    })();

    lq.Lobby = (function() {

        function Lobby(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        }

        (Lobby.prototype = Object.create($protobuf.rpc.Service.prototype)).constructor = Lobby;

        Lobby.create = function create(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };


        Object.defineProperty(Lobby.prototype.fetchConnectionInfo = function fetchConnectionInfo(request, callback) {
            return this.rpcCall(fetchConnectionInfo, $root.lq.ReqCommon, $root.lq.ResConnectionInfo, request, callback);
        }, "name", { value: "fetchConnectionInfo" });


        Object.defineProperty(Lobby.prototype.fetchQueueInfo = function fetchQueueInfo(request, callback) {
            return this.rpcCall(fetchQueueInfo, $root.lq.ReqCommon, $root.lq.ResFetchQueueInfo, request, callback);
        }, "name", { value: "fetchQueueInfo" });


        Object.defineProperty(Lobby.prototype.cancelQueue = function cancelQueue(request, callback) {
            return this.rpcCall(cancelQueue, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "cancelQueue" });


        Object.defineProperty(Lobby.prototype.openidCheck = function openidCheck(request, callback) {
            return this.rpcCall(openidCheck, $root.lq.ReqOpenidCheck, $root.lq.ResOauth2Check, request, callback);
        }, "name", { value: "openidCheck" });


        Object.defineProperty(Lobby.prototype.signup = function signup(request, callback) {
            return this.rpcCall(signup, $root.lq.ReqSignupAccount, $root.lq.ResSignupAccount, request, callback);
        }, "name", { value: "signup" });


        Object.defineProperty(Lobby.prototype.login = function login(request, callback) {
            return this.rpcCall(login, $root.lq.ReqLogin, $root.lq.ResLogin, request, callback);
        }, "name", { value: "login" });


        Object.defineProperty(Lobby.prototype.prepareLogin = function prepareLogin(request, callback) {
            return this.rpcCall(prepareLogin, $root.lq.ReqPrepareLogin, $root.lq.ResCommon, request, callback);
        }, "name", { value: "prepareLogin" });


        Object.defineProperty(Lobby.prototype.fastLogin = function fastLogin(request, callback) {
            return this.rpcCall(fastLogin, $root.lq.ReqFastLogin, $root.lq.ResFastLogin, request, callback);
        }, "name", { value: "fastLogin" });


        Object.defineProperty(Lobby.prototype.fetchInfo = function fetchInfo(request, callback) {
            return this.rpcCall(fetchInfo, $root.lq.ReqCommon, $root.lq.ResFetchInfo, request, callback);
        }, "name", { value: "fetchInfo" });


        Object.defineProperty(Lobby.prototype.loginSuccess = function loginSuccess(request, callback) {
            return this.rpcCall(loginSuccess, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "loginSuccess" });


        Object.defineProperty(Lobby.prototype.fetchServerMaintenanceInfo = function fetchServerMaintenanceInfo(request, callback) {
            return this.rpcCall(fetchServerMaintenanceInfo, $root.lq.ReqCommon, $root.lq.ResFetchServerMaintenanceInfo, request, callback);
        }, "name", { value: "fetchServerMaintenanceInfo" });


        Object.defineProperty(Lobby.prototype.emailLogin = function emailLogin(request, callback) {
            return this.rpcCall(emailLogin, $root.lq.ReqEmailLogin, $root.lq.ResLogin, request, callback);
        }, "name", { value: "emailLogin" });


        Object.defineProperty(Lobby.prototype.oauth2Auth = function oauth2Auth(request, callback) {
            return this.rpcCall(oauth2Auth, $root.lq.ReqOauth2Auth, $root.lq.ResOauth2Auth, request, callback);
        }, "name", { value: "oauth2Auth" });


        Object.defineProperty(Lobby.prototype.oauth2Check = function oauth2Check(request, callback) {
            return this.rpcCall(oauth2Check, $root.lq.ReqOauth2Check, $root.lq.ResOauth2Check, request, callback);
        }, "name", { value: "oauth2Check" });


        Object.defineProperty(Lobby.prototype.oauth2Signup = function oauth2Signup(request, callback) {
            return this.rpcCall(oauth2Signup, $root.lq.ReqOauth2Signup, $root.lq.ResOauth2Signup, request, callback);
        }, "name", { value: "oauth2Signup" });


        Object.defineProperty(Lobby.prototype.oauth2Login = function oauth2Login(request, callback) {
            return this.rpcCall(oauth2Login, $root.lq.ReqOauth2Login, $root.lq.ResLogin, request, callback);
        }, "name", { value: "oauth2Login" });


        Object.defineProperty(Lobby.prototype.dmmPreLogin = function dmmPreLogin(request, callback) {
            return this.rpcCall(dmmPreLogin, $root.lq.ReqDMMPreLogin, $root.lq.ResDMMPreLogin, request, callback);
        }, "name", { value: "dmmPreLogin" });


        Object.defineProperty(Lobby.prototype.createPhoneVerifyCode = function createPhoneVerifyCode(request, callback) {
            return this.rpcCall(createPhoneVerifyCode, $root.lq.ReqCreatePhoneVerifyCode, $root.lq.ResCommon, request, callback);
        }, "name", { value: "createPhoneVerifyCode" });


        Object.defineProperty(Lobby.prototype.createEmailVerifyCode = function createEmailVerifyCode(request, callback) {
            return this.rpcCall(createEmailVerifyCode, $root.lq.ReqCreateEmailVerifyCode, $root.lq.ResCommon, request, callback);
        }, "name", { value: "createEmailVerifyCode" });


        Object.defineProperty(Lobby.prototype.verfifyCodeForSecure = function verfifyCodeForSecure(request, callback) {
            return this.rpcCall(verfifyCodeForSecure, $root.lq.ReqVerifyCodeForSecure, $root.lq.ResVerfiyCodeForSecure, request, callback);
        }, "name", { value: "verfifyCodeForSecure" });


        Object.defineProperty(Lobby.prototype.bindPhoneNumber = function bindPhoneNumber(request, callback) {
            return this.rpcCall(bindPhoneNumber, $root.lq.ReqBindPhoneNumber, $root.lq.ResCommon, request, callback);
        }, "name", { value: "bindPhoneNumber" });


        Object.defineProperty(Lobby.prototype.unbindPhoneNumber = function unbindPhoneNumber(request, callback) {
            return this.rpcCall(unbindPhoneNumber, $root.lq.ReqUnbindPhoneNumber, $root.lq.ResCommon, request, callback);
        }, "name", { value: "unbindPhoneNumber" });


        Object.defineProperty(Lobby.prototype.fetchPhoneLoginBind = function fetchPhoneLoginBind(request, callback) {
            return this.rpcCall(fetchPhoneLoginBind, $root.lq.ReqCommon, $root.lq.ResFetchPhoneLoginBind, request, callback);
        }, "name", { value: "fetchPhoneLoginBind" });


        Object.defineProperty(Lobby.prototype.createPhoneLoginBind = function createPhoneLoginBind(request, callback) {
            return this.rpcCall(createPhoneLoginBind, $root.lq.ReqCreatePhoneLoginBind, $root.lq.ResCommon, request, callback);
        }, "name", { value: "createPhoneLoginBind" });


        Object.defineProperty(Lobby.prototype.bindEmail = function bindEmail(request, callback) {
            return this.rpcCall(bindEmail, $root.lq.ReqBindEmail, $root.lq.ResCommon, request, callback);
        }, "name", { value: "bindEmail" });


        Object.defineProperty(Lobby.prototype.modifyPassword = function modifyPassword(request, callback) {
            return this.rpcCall(modifyPassword, $root.lq.ReqModifyPassword, $root.lq.ResCommon, request, callback);
        }, "name", { value: "modifyPassword" });


        Object.defineProperty(Lobby.prototype.bindAccount = function bindAccount(request, callback) {
            return this.rpcCall(bindAccount, $root.lq.ReqBindAccount, $root.lq.ResCommon, request, callback);
        }, "name", { value: "bindAccount" });


        Object.defineProperty(Lobby.prototype.logout = function logout(request, callback) {
            return this.rpcCall(logout, $root.lq.ReqLogout, $root.lq.ResLogout, request, callback);
        }, "name", { value: "logout" });


        Object.defineProperty(Lobby.prototype.heatbeat = function heatbeat(request, callback) {
            return this.rpcCall(heatbeat, $root.lq.ReqHeatBeat, $root.lq.ResCommon, request, callback);
        }, "name", { value: "heatbeat" });


        Object.defineProperty(Lobby.prototype.searchAccountByEid = function searchAccountByEid(request, callback) {
            return this.rpcCall(searchAccountByEid, $root.lq.ReqSearchAccountByEidLobby, $root.lq.ResSearchAccountbyEidLobby, request, callback);
        }, "name", { value: "searchAccountByEid" });


        Object.defineProperty(Lobby.prototype.loginBeat = function loginBeat(request, callback) {
            return this.rpcCall(loginBeat, $root.lq.ReqLoginBeat, $root.lq.ResCommon, request, callback);
        }, "name", { value: "loginBeat" });


        Object.defineProperty(Lobby.prototype.createNickname = function createNickname(request, callback) {
            return this.rpcCall(createNickname, $root.lq.ReqCreateNickname, $root.lq.ResCommon, request, callback);
        }, "name", { value: "createNickname" });


        Object.defineProperty(Lobby.prototype.modifyNickname = function modifyNickname(request, callback) {
            return this.rpcCall(modifyNickname, $root.lq.ReqModifyNickname, $root.lq.ResCommon, request, callback);
        }, "name", { value: "modifyNickname" });


        Object.defineProperty(Lobby.prototype.modifyBirthday = function modifyBirthday(request, callback) {
            return this.rpcCall(modifyBirthday, $root.lq.ReqModifyBirthday, $root.lq.ResCommon, request, callback);
        }, "name", { value: "modifyBirthday" });


        Object.defineProperty(Lobby.prototype.fetchRoom = function fetchRoom(request, callback) {
            return this.rpcCall(fetchRoom, $root.lq.ReqCommon, $root.lq.ResSelfRoom, request, callback);
        }, "name", { value: "fetchRoom" });


        Object.defineProperty(Lobby.prototype.fetchGamingInfo = function fetchGamingInfo(request, callback) {
            return this.rpcCall(fetchGamingInfo, $root.lq.ReqCommon, $root.lq.ResFetchGamingInfo, request, callback);
        }, "name", { value: "fetchGamingInfo" });


        Object.defineProperty(Lobby.prototype.createRoom = function createRoom(request, callback) {
            return this.rpcCall(createRoom, $root.lq.ReqCreateRoom, $root.lq.ResCreateRoom, request, callback);
        }, "name", { value: "createRoom" });


        Object.defineProperty(Lobby.prototype.joinRoom = function joinRoom(request, callback) {
            return this.rpcCall(joinRoom, $root.lq.ReqJoinRoom, $root.lq.ResJoinRoom, request, callback);
        }, "name", { value: "joinRoom" });


        Object.defineProperty(Lobby.prototype.leaveRoom = function leaveRoom(request, callback) {
            return this.rpcCall(leaveRoom, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "leaveRoom" });


        Object.defineProperty(Lobby.prototype.readyPlay = function readyPlay(request, callback) {
            return this.rpcCall(readyPlay, $root.lq.ReqRoomReady, $root.lq.ResCommon, request, callback);
        }, "name", { value: "readyPlay" });


        Object.defineProperty(Lobby.prototype.dressingStatus = function dressingStatus(request, callback) {
            return this.rpcCall(dressingStatus, $root.lq.ReqRoomDressing, $root.lq.ResCommon, request, callback);
        }, "name", { value: "dressingStatus" });


        Object.defineProperty(Lobby.prototype.startRoom = function startRoom(request, callback) {
            return this.rpcCall(startRoom, $root.lq.ReqRoomStart, $root.lq.ResCommon, request, callback);
        }, "name", { value: "startRoom" });


        Object.defineProperty(Lobby.prototype.roomKickPlayer = function roomKickPlayer(request, callback) {
            return this.rpcCall(roomKickPlayer, $root.lq.ReqRoomKickPlayer, $root.lq.ResCommon, request, callback);
        }, "name", { value: "roomKickPlayer" });


        Object.defineProperty(Lobby.prototype.modifyRoom = function modifyRoom(request, callback) {
            return this.rpcCall(modifyRoom, $root.lq.ReqModifyRoom, $root.lq.ResCommon, request, callback);
        }, "name", { value: "modifyRoom" });


        Object.defineProperty(Lobby.prototype.addRoomRobot = function addRoomRobot(request, callback) {
            return this.rpcCall(addRoomRobot, $root.lq.ReqAddRoomRobot, $root.lq.ResCommon, request, callback);
        }, "name", { value: "addRoomRobot" });


        Object.defineProperty(Lobby.prototype.matchGame = function matchGame(request, callback) {
            return this.rpcCall(matchGame, $root.lq.ReqJoinMatchQueue, $root.lq.ResCommon, request, callback);
        }, "name", { value: "matchGame" });


        Object.defineProperty(Lobby.prototype.cancelMatch = function cancelMatch(request, callback) {
            return this.rpcCall(cancelMatch, $root.lq.ReqCancelMatchQueue, $root.lq.ResCommon, request, callback);
        }, "name", { value: "cancelMatch" });


        Object.defineProperty(Lobby.prototype.fetchAccountInfo = function fetchAccountInfo(request, callback) {
            return this.rpcCall(fetchAccountInfo, $root.lq.ReqAccountInfo, $root.lq.ResAccountInfo, request, callback);
        }, "name", { value: "fetchAccountInfo" });


        Object.defineProperty(Lobby.prototype.changeAvatar = function changeAvatar(request, callback) {
            return this.rpcCall(changeAvatar, $root.lq.ReqChangeAvatar, $root.lq.ResCommon, request, callback);
        }, "name", { value: "changeAvatar" });


        Object.defineProperty(Lobby.prototype.receiveVersionReward = function receiveVersionReward(request, callback) {
            return this.rpcCall(receiveVersionReward, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "receiveVersionReward" });


        Object.defineProperty(Lobby.prototype.fetchAccountStatisticInfo = function fetchAccountStatisticInfo(request, callback) {
            return this.rpcCall(fetchAccountStatisticInfo, $root.lq.ReqAccountStatisticInfo, $root.lq.ResAccountStatisticInfo, request, callback);
        }, "name", { value: "fetchAccountStatisticInfo" });


        Object.defineProperty(Lobby.prototype.fetchAccountChallengeRankInfo = function fetchAccountChallengeRankInfo(request, callback) {
            return this.rpcCall(fetchAccountChallengeRankInfo, $root.lq.ReqAccountInfo, $root.lq.ResAccountChallengeRankInfo, request, callback);
        }, "name", { value: "fetchAccountChallengeRankInfo" });


        Object.defineProperty(Lobby.prototype.fetchAccountCharacterInfo = function fetchAccountCharacterInfo(request, callback) {
            return this.rpcCall(fetchAccountCharacterInfo, $root.lq.ReqCommon, $root.lq.ResAccountCharacterInfo, request, callback);
        }, "name", { value: "fetchAccountCharacterInfo" });


        Object.defineProperty(Lobby.prototype.shopPurchase = function shopPurchase(request, callback) {
            return this.rpcCall(shopPurchase, $root.lq.ReqShopPurchase, $root.lq.ResShopPurchase, request, callback);
        }, "name", { value: "shopPurchase" });


        Object.defineProperty(Lobby.prototype.fetchGameRecord = function fetchGameRecord(request, callback) {
            return this.rpcCall(fetchGameRecord, $root.lq.ReqGameRecord, $root.lq.ResGameRecord, request, callback);
        }, "name", { value: "fetchGameRecord" });


        Object.defineProperty(Lobby.prototype.readGameRecord = function readGameRecord(request, callback) {
            return this.rpcCall(readGameRecord, $root.lq.ReqGameRecord, $root.lq.ResCommon, request, callback);
        }, "name", { value: "readGameRecord" });


        Object.defineProperty(Lobby.prototype.fetchGameRecordList = function fetchGameRecordList(request, callback) {
            return this.rpcCall(fetchGameRecordList, $root.lq.ReqGameRecordList, $root.lq.ResGameRecordList, request, callback);
        }, "name", { value: "fetchGameRecordList" });


        Object.defineProperty(Lobby.prototype.fetchGameRecordListV2 = function fetchGameRecordListV2(request, callback) {
            return this.rpcCall(fetchGameRecordListV2, $root.lq.ReqGameRecordListV2, $root.lq.ResGameRecordListV2, request, callback);
        }, "name", { value: "fetchGameRecordListV2" });


        Object.defineProperty(Lobby.prototype.fetchNextGameRecordList = function fetchNextGameRecordList(request, callback) {
            return this.rpcCall(fetchNextGameRecordList, $root.lq.ReqNextGameRecordList, $root.lq.ResNextGameRecordList, request, callback);
        }, "name", { value: "fetchNextGameRecordList" });


        Object.defineProperty(Lobby.prototype.fetchCollectedGameRecordList = function fetchCollectedGameRecordList(request, callback) {
            return this.rpcCall(fetchCollectedGameRecordList, $root.lq.ReqCommon, $root.lq.ResCollectedGameRecordList, request, callback);
        }, "name", { value: "fetchCollectedGameRecordList" });


        Object.defineProperty(Lobby.prototype.fetchGameRecordsDetail = function fetchGameRecordsDetail(request, callback) {
            return this.rpcCall(fetchGameRecordsDetail, $root.lq.ReqGameRecordsDetail, $root.lq.ResGameRecordsDetail, request, callback);
        }, "name", { value: "fetchGameRecordsDetail" });


        Object.defineProperty(Lobby.prototype.fetchGameRecordsDetailV2 = function fetchGameRecordsDetailV2(request, callback) {
            return this.rpcCall(fetchGameRecordsDetailV2, $root.lq.ReqGameRecordsDetailV2, $root.lq.ResGameRecordsDetailV2, request, callback);
        }, "name", { value: "fetchGameRecordsDetailV2" });


        Object.defineProperty(Lobby.prototype.addCollectedGameRecord = function addCollectedGameRecord(request, callback) {
            return this.rpcCall(addCollectedGameRecord, $root.lq.ReqAddCollectedGameRecord, $root.lq.ResAddCollectedGameRecord, request, callback);
        }, "name", { value: "addCollectedGameRecord" });


        Object.defineProperty(Lobby.prototype.removeCollectedGameRecord = function removeCollectedGameRecord(request, callback) {
            return this.rpcCall(removeCollectedGameRecord, $root.lq.ReqRemoveCollectedGameRecord, $root.lq.ResRemoveCollectedGameRecord, request, callback);
        }, "name", { value: "removeCollectedGameRecord" });


        Object.defineProperty(Lobby.prototype.changeCollectedGameRecordRemarks = function changeCollectedGameRecordRemarks(request, callback) {
            return this.rpcCall(changeCollectedGameRecordRemarks, $root.lq.ReqChangeCollectedGameRecordRemarks, $root.lq.ResChangeCollectedGameRecordRemarks, request, callback);
        }, "name", { value: "changeCollectedGameRecordRemarks" });


        Object.defineProperty(Lobby.prototype.fetchLevelLeaderboard = function fetchLevelLeaderboard(request, callback) {
            return this.rpcCall(fetchLevelLeaderboard, $root.lq.ReqLevelLeaderboard, $root.lq.ResLevelLeaderboard, request, callback);
        }, "name", { value: "fetchLevelLeaderboard" });


        Object.defineProperty(Lobby.prototype.fetchChallengeLeaderboard = function fetchChallengeLeaderboard(request, callback) {
            return this.rpcCall(fetchChallengeLeaderboard, $root.lq.ReqChallangeLeaderboard, $root.lq.ResChallengeLeaderboard, request, callback);
        }, "name", { value: "fetchChallengeLeaderboard" });


        Object.defineProperty(Lobby.prototype.fetchMutiChallengeLevel = function fetchMutiChallengeLevel(request, callback) {
            return this.rpcCall(fetchMutiChallengeLevel, $root.lq.ReqMutiChallengeLevel, $root.lq.ResMutiChallengeLevel, request, callback);
        }, "name", { value: "fetchMutiChallengeLevel" });


        Object.defineProperty(Lobby.prototype.fetchMultiAccountBrief = function fetchMultiAccountBrief(request, callback) {
            return this.rpcCall(fetchMultiAccountBrief, $root.lq.ReqMultiAccountId, $root.lq.ResMultiAccountBrief, request, callback);
        }, "name", { value: "fetchMultiAccountBrief" });


        Object.defineProperty(Lobby.prototype.fetchFriendList = function fetchFriendList(request, callback) {
            return this.rpcCall(fetchFriendList, $root.lq.ReqCommon, $root.lq.ResFriendList, request, callback);
        }, "name", { value: "fetchFriendList" });


        Object.defineProperty(Lobby.prototype.fetchFriendApplyList = function fetchFriendApplyList(request, callback) {
            return this.rpcCall(fetchFriendApplyList, $root.lq.ReqCommon, $root.lq.ResFriendApplyList, request, callback);
        }, "name", { value: "fetchFriendApplyList" });


        Object.defineProperty(Lobby.prototype.applyFriend = function applyFriend(request, callback) {
            return this.rpcCall(applyFriend, $root.lq.ReqApplyFriend, $root.lq.ResCommon, request, callback);
        }, "name", { value: "applyFriend" });


        Object.defineProperty(Lobby.prototype.handleFriendApply = function handleFriendApply(request, callback) {
            return this.rpcCall(handleFriendApply, $root.lq.ReqHandleFriendApply, $root.lq.ResCommon, request, callback);
        }, "name", { value: "handleFriendApply" });


        Object.defineProperty(Lobby.prototype.removeFriend = function removeFriend(request, callback) {
            return this.rpcCall(removeFriend, $root.lq.ReqRemoveFriend, $root.lq.ResCommon, request, callback);
        }, "name", { value: "removeFriend" });


        Object.defineProperty(Lobby.prototype.searchAccountById = function searchAccountById(request, callback) {
            return this.rpcCall(searchAccountById, $root.lq.ReqSearchAccountById, $root.lq.ResSearchAccountById, request, callback);
        }, "name", { value: "searchAccountById" });


        Object.defineProperty(Lobby.prototype.searchAccountByPattern = function searchAccountByPattern(request, callback) {
            return this.rpcCall(searchAccountByPattern, $root.lq.ReqSearchAccountByPattern, $root.lq.ResSearchAccountByPattern, request, callback);
        }, "name", { value: "searchAccountByPattern" });


        Object.defineProperty(Lobby.prototype.fetchAccountState = function fetchAccountState(request, callback) {
            return this.rpcCall(fetchAccountState, $root.lq.ReqAccountList, $root.lq.ResAccountStates, request, callback);
        }, "name", { value: "fetchAccountState" });


        Object.defineProperty(Lobby.prototype.fetchBagInfo = function fetchBagInfo(request, callback) {
            return this.rpcCall(fetchBagInfo, $root.lq.ReqCommon, $root.lq.ResBagInfo, request, callback);
        }, "name", { value: "fetchBagInfo" });


        Object.defineProperty(Lobby.prototype.useBagItem = function useBagItem(request, callback) {
            return this.rpcCall(useBagItem, $root.lq.ReqUseBagItem, $root.lq.ResCommon, request, callback);
        }, "name", { value: "useBagItem" });


        Object.defineProperty(Lobby.prototype.openManualItem = function openManualItem(request, callback) {
            return this.rpcCall(openManualItem, $root.lq.ReqOpenManualItem, $root.lq.ResCommon, request, callback);
        }, "name", { value: "openManualItem" });


        Object.defineProperty(Lobby.prototype.openRandomRewardItem = function openRandomRewardItem(request, callback) {
            return this.rpcCall(openRandomRewardItem, $root.lq.ReqOpenRandomRewardItem, $root.lq.ResOpenRandomRewardItem, request, callback);
        }, "name", { value: "openRandomRewardItem" });


        Object.defineProperty(Lobby.prototype.openAllRewardItem = function openAllRewardItem(request, callback) {
            return this.rpcCall(openAllRewardItem, $root.lq.ReqOpenAllRewardItem, $root.lq.ResOpenAllRewardItem, request, callback);
        }, "name", { value: "openAllRewardItem" });


        Object.defineProperty(Lobby.prototype.composeShard = function composeShard(request, callback) {
            return this.rpcCall(composeShard, $root.lq.ReqComposeShard, $root.lq.ResCommon, request, callback);
        }, "name", { value: "composeShard" });


        Object.defineProperty(Lobby.prototype.fetchAnnouncement = function fetchAnnouncement(request, callback) {
            return this.rpcCall(fetchAnnouncement, $root.lq.ReqFetchAnnouncement, $root.lq.ResAnnouncement, request, callback);
        }, "name", { value: "fetchAnnouncement" });


        Object.defineProperty(Lobby.prototype.readAnnouncement = function readAnnouncement(request, callback) {
            return this.rpcCall(readAnnouncement, $root.lq.ReqReadAnnouncement, $root.lq.ResCommon, request, callback);
        }, "name", { value: "readAnnouncement" });


        Object.defineProperty(Lobby.prototype.fetchMailInfo = function fetchMailInfo(request, callback) {
            return this.rpcCall(fetchMailInfo, $root.lq.ReqCommon, $root.lq.ResMailInfo, request, callback);
        }, "name", { value: "fetchMailInfo" });


        Object.defineProperty(Lobby.prototype.readMail = function readMail(request, callback) {
            return this.rpcCall(readMail, $root.lq.ReqReadMail, $root.lq.ResCommon, request, callback);
        }, "name", { value: "readMail" });


        Object.defineProperty(Lobby.prototype.deleteMail = function deleteMail(request, callback) {
            return this.rpcCall(deleteMail, $root.lq.ReqDeleteMail, $root.lq.ResCommon, request, callback);
        }, "name", { value: "deleteMail" });


        Object.defineProperty(Lobby.prototype.takeAttachmentFromMail = function takeAttachmentFromMail(request, callback) {
            return this.rpcCall(takeAttachmentFromMail, $root.lq.ReqTakeAttachment, $root.lq.ResCommon, request, callback);
        }, "name", { value: "takeAttachmentFromMail" });


        Object.defineProperty(Lobby.prototype.receiveAchievementReward = function receiveAchievementReward(request, callback) {
            return this.rpcCall(receiveAchievementReward, $root.lq.ReqReceiveAchievementReward, $root.lq.ResReceiveAchievementReward, request, callback);
        }, "name", { value: "receiveAchievementReward" });


        Object.defineProperty(Lobby.prototype.receiveAchievementGroupReward = function receiveAchievementGroupReward(request, callback) {
            return this.rpcCall(receiveAchievementGroupReward, $root.lq.ReqReceiveAchievementGroupReward, $root.lq.ResReceiveAchievementGroupReward, request, callback);
        }, "name", { value: "receiveAchievementGroupReward" });


        Object.defineProperty(Lobby.prototype.fetchAchievementRate = function fetchAchievementRate(request, callback) {
            return this.rpcCall(fetchAchievementRate, $root.lq.ReqCommon, $root.lq.ResFetchAchievementRate, request, callback);
        }, "name", { value: "fetchAchievementRate" });


        Object.defineProperty(Lobby.prototype.fetchAchievement = function fetchAchievement(request, callback) {
            return this.rpcCall(fetchAchievement, $root.lq.ReqCommon, $root.lq.ResAchievement, request, callback);
        }, "name", { value: "fetchAchievement" });


        Object.defineProperty(Lobby.prototype.buyShiLian = function buyShiLian(request, callback) {
            return this.rpcCall(buyShiLian, $root.lq.ReqBuyShiLian, $root.lq.ResCommon, request, callback);
        }, "name", { value: "buyShiLian" });


        Object.defineProperty(Lobby.prototype.matchShiLian = function matchShiLian(request, callback) {
            return this.rpcCall(matchShiLian, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "matchShiLian" });


        Object.defineProperty(Lobby.prototype.goNextShiLian = function goNextShiLian(request, callback) {
            return this.rpcCall(goNextShiLian, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "goNextShiLian" });


        Object.defineProperty(Lobby.prototype.updateClientValue = function updateClientValue(request, callback) {
            return this.rpcCall(updateClientValue, $root.lq.ReqUpdateClientValue, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateClientValue" });


        Object.defineProperty(Lobby.prototype.fetchClientValue = function fetchClientValue(request, callback) {
            return this.rpcCall(fetchClientValue, $root.lq.ReqCommon, $root.lq.ResClientValue, request, callback);
        }, "name", { value: "fetchClientValue" });


        Object.defineProperty(Lobby.prototype.clientMessage = function clientMessage(request, callback) {
            return this.rpcCall(clientMessage, $root.lq.ReqClientMessage, $root.lq.ResCommon, request, callback);
        }, "name", { value: "clientMessage" });


        Object.defineProperty(Lobby.prototype.fetchCurrentMatchInfo = function fetchCurrentMatchInfo(request, callback) {
            return this.rpcCall(fetchCurrentMatchInfo, $root.lq.ReqCurrentMatchInfo, $root.lq.ResCurrentMatchInfo, request, callback);
        }, "name", { value: "fetchCurrentMatchInfo" });


        Object.defineProperty(Lobby.prototype.userComplain = function userComplain(request, callback) {
            return this.rpcCall(userComplain, $root.lq.ReqUserComplain, $root.lq.ResCommon, request, callback);
        }, "name", { value: "userComplain" });


        Object.defineProperty(Lobby.prototype.fetchReviveCoinInfo = function fetchReviveCoinInfo(request, callback) {
            return this.rpcCall(fetchReviveCoinInfo, $root.lq.ReqCommon, $root.lq.ResReviveCoinInfo, request, callback);
        }, "name", { value: "fetchReviveCoinInfo" });


        Object.defineProperty(Lobby.prototype.gainReviveCoin = function gainReviveCoin(request, callback) {
            return this.rpcCall(gainReviveCoin, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gainReviveCoin" });


        Object.defineProperty(Lobby.prototype.fetchDailyTask = function fetchDailyTask(request, callback) {
            return this.rpcCall(fetchDailyTask, $root.lq.ReqCommon, $root.lq.ResDailyTask, request, callback);
        }, "name", { value: "fetchDailyTask" });


        Object.defineProperty(Lobby.prototype.refreshDailyTask = function refreshDailyTask(request, callback) {
            return this.rpcCall(refreshDailyTask, $root.lq.ReqRefreshDailyTask, $root.lq.ResRefreshDailyTask, request, callback);
        }, "name", { value: "refreshDailyTask" });


        Object.defineProperty(Lobby.prototype.useGiftCode = function useGiftCode(request, callback) {
            return this.rpcCall(useGiftCode, $root.lq.ReqUseGiftCode, $root.lq.ResUseGiftCode, request, callback);
        }, "name", { value: "useGiftCode" });


        Object.defineProperty(Lobby.prototype.useSpecialGiftCode = function useSpecialGiftCode(request, callback) {
            return this.rpcCall(useSpecialGiftCode, $root.lq.ReqUseGiftCode, $root.lq.ResUseSpecialGiftCode, request, callback);
        }, "name", { value: "useSpecialGiftCode" });


        Object.defineProperty(Lobby.prototype.fetchTitleList = function fetchTitleList(request, callback) {
            return this.rpcCall(fetchTitleList, $root.lq.ReqCommon, $root.lq.ResTitleList, request, callback);
        }, "name", { value: "fetchTitleList" });


        Object.defineProperty(Lobby.prototype.useTitle = function useTitle(request, callback) {
            return this.rpcCall(useTitle, $root.lq.ReqUseTitle, $root.lq.ResCommon, request, callback);
        }, "name", { value: "useTitle" });


        Object.defineProperty(Lobby.prototype.sendClientMessage = function sendClientMessage(request, callback) {
            return this.rpcCall(sendClientMessage, $root.lq.ReqSendClientMessage, $root.lq.ResCommon, request, callback);
        }, "name", { value: "sendClientMessage" });


        Object.defineProperty(Lobby.prototype.fetchGameLiveInfo = function fetchGameLiveInfo(request, callback) {
            return this.rpcCall(fetchGameLiveInfo, $root.lq.ReqGameLiveInfo, $root.lq.ResGameLiveInfo, request, callback);
        }, "name", { value: "fetchGameLiveInfo" });


        Object.defineProperty(Lobby.prototype.fetchGameLiveLeftSegment = function fetchGameLiveLeftSegment(request, callback) {
            return this.rpcCall(fetchGameLiveLeftSegment, $root.lq.ReqGameLiveLeftSegment, $root.lq.ResGameLiveLeftSegment, request, callback);
        }, "name", { value: "fetchGameLiveLeftSegment" });


        Object.defineProperty(Lobby.prototype.fetchGameLiveList = function fetchGameLiveList(request, callback) {
            return this.rpcCall(fetchGameLiveList, $root.lq.ReqGameLiveList, $root.lq.ResGameLiveList, request, callback);
        }, "name", { value: "fetchGameLiveList" });


        Object.defineProperty(Lobby.prototype.fetchCommentSetting = function fetchCommentSetting(request, callback) {
            return this.rpcCall(fetchCommentSetting, $root.lq.ReqCommon, $root.lq.ResCommentSetting, request, callback);
        }, "name", { value: "fetchCommentSetting" });


        Object.defineProperty(Lobby.prototype.updateCommentSetting = function updateCommentSetting(request, callback) {
            return this.rpcCall(updateCommentSetting, $root.lq.ReqUpdateCommentSetting, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateCommentSetting" });


        Object.defineProperty(Lobby.prototype.fetchCommentList = function fetchCommentList(request, callback) {
            return this.rpcCall(fetchCommentList, $root.lq.ReqFetchCommentList, $root.lq.ResFetchCommentList, request, callback);
        }, "name", { value: "fetchCommentList" });


        Object.defineProperty(Lobby.prototype.fetchCommentContent = function fetchCommentContent(request, callback) {
            return this.rpcCall(fetchCommentContent, $root.lq.ReqFetchCommentContent, $root.lq.ResFetchCommentContent, request, callback);
        }, "name", { value: "fetchCommentContent" });


        Object.defineProperty(Lobby.prototype.leaveComment = function leaveComment(request, callback) {
            return this.rpcCall(leaveComment, $root.lq.ReqLeaveComment, $root.lq.ResCommon, request, callback);
        }, "name", { value: "leaveComment" });


        Object.defineProperty(Lobby.prototype.deleteComment = function deleteComment(request, callback) {
            return this.rpcCall(deleteComment, $root.lq.ReqDeleteComment, $root.lq.ResCommon, request, callback);
        }, "name", { value: "deleteComment" });


        Object.defineProperty(Lobby.prototype.updateReadComment = function updateReadComment(request, callback) {
            return this.rpcCall(updateReadComment, $root.lq.ReqUpdateReadComment, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateReadComment" });


        Object.defineProperty(Lobby.prototype.fetchRollingNotice = function fetchRollingNotice(request, callback) {
            return this.rpcCall(fetchRollingNotice, $root.lq.ReqFetchRollingNotice, $root.lq.ResFetchRollingNotice, request, callback);
        }, "name", { value: "fetchRollingNotice" });


        Object.defineProperty(Lobby.prototype.fetchMaintainNotice = function fetchMaintainNotice(request, callback) {
            return this.rpcCall(fetchMaintainNotice, $root.lq.ReqCommon, $root.lq.ResFetchMaintainNotice, request, callback);
        }, "name", { value: "fetchMaintainNotice" });


        Object.defineProperty(Lobby.prototype.fetchServerTime = function fetchServerTime(request, callback) {
            return this.rpcCall(fetchServerTime, $root.lq.ReqCommon, $root.lq.ResServerTime, request, callback);
        }, "name", { value: "fetchServerTime" });


        Object.defineProperty(Lobby.prototype.fetchPlatformProducts = function fetchPlatformProducts(request, callback) {
            return this.rpcCall(fetchPlatformProducts, $root.lq.ReqPlatformBillingProducts, $root.lq.ResPlatformBillingProducts, request, callback);
        }, "name", { value: "fetchPlatformProducts" });


        Object.defineProperty(Lobby.prototype.fetchRandomCharacter = function fetchRandomCharacter(request, callback) {
            return this.rpcCall(fetchRandomCharacter, $root.lq.ReqCommon, $root.lq.ResRandomCharacter, request, callback);
        }, "name", { value: "fetchRandomCharacter" });


        Object.defineProperty(Lobby.prototype.setRandomCharacter = function setRandomCharacter(request, callback) {
            return this.rpcCall(setRandomCharacter, $root.lq.ReqRandomCharacter, $root.lq.ResCommon, request, callback);
        }, "name", { value: "setRandomCharacter" });


        Object.defineProperty(Lobby.prototype.cancelGooglePlayOrder = function cancelGooglePlayOrder(request, callback) {
            return this.rpcCall(cancelGooglePlayOrder, $root.lq.ReqCancelGooglePlayOrder, $root.lq.ResCommon, request, callback);
        }, "name", { value: "cancelGooglePlayOrder" });


        Object.defineProperty(Lobby.prototype.openChest = function openChest(request, callback) {
            return this.rpcCall(openChest, $root.lq.ReqOpenChest, $root.lq.ResOpenChest, request, callback);
        }, "name", { value: "openChest" });


        Object.defineProperty(Lobby.prototype.buyFromChestShop = function buyFromChestShop(request, callback) {
            return this.rpcCall(buyFromChestShop, $root.lq.ReqBuyFromChestShop, $root.lq.ResBuyFromChestShop, request, callback);
        }, "name", { value: "buyFromChestShop" });


        Object.defineProperty(Lobby.prototype.fetchDailySignInInfo = function fetchDailySignInInfo(request, callback) {
            return this.rpcCall(fetchDailySignInInfo, $root.lq.ReqCommon, $root.lq.ResDailySignInInfo, request, callback);
        }, "name", { value: "fetchDailySignInInfo" });


        Object.defineProperty(Lobby.prototype.doDailySignIn = function doDailySignIn(request, callback) {
            return this.rpcCall(doDailySignIn, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "doDailySignIn" });


        Object.defineProperty(Lobby.prototype.doActivitySignIn = function doActivitySignIn(request, callback) {
            return this.rpcCall(doActivitySignIn, $root.lq.ReqDoActivitySignIn, $root.lq.ResDoActivitySignIn, request, callback);
        }, "name", { value: "doActivitySignIn" });


        Object.defineProperty(Lobby.prototype.fetchCharacterInfo = function fetchCharacterInfo(request, callback) {
            return this.rpcCall(fetchCharacterInfo, $root.lq.ReqCommon, $root.lq.ResCharacterInfo, request, callback);
        }, "name", { value: "fetchCharacterInfo" });


        Object.defineProperty(Lobby.prototype.updateCharacterSort = function updateCharacterSort(request, callback) {
            return this.rpcCall(updateCharacterSort, $root.lq.ReqUpdateCharacterSort, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateCharacterSort" });


        Object.defineProperty(Lobby.prototype.changeMainCharacter = function changeMainCharacter(request, callback) {
            return this.rpcCall(changeMainCharacter, $root.lq.ReqChangeMainCharacter, $root.lq.ResCommon, request, callback);
        }, "name", { value: "changeMainCharacter" });


        Object.defineProperty(Lobby.prototype.changeCharacterSkin = function changeCharacterSkin(request, callback) {
            return this.rpcCall(changeCharacterSkin, $root.lq.ReqChangeCharacterSkin, $root.lq.ResCommon, request, callback);
        }, "name", { value: "changeCharacterSkin" });


        Object.defineProperty(Lobby.prototype.changeCharacterView = function changeCharacterView(request, callback) {
            return this.rpcCall(changeCharacterView, $root.lq.ReqChangeCharacterView, $root.lq.ResCommon, request, callback);
        }, "name", { value: "changeCharacterView" });


        Object.defineProperty(Lobby.prototype.setHiddenCharacter = function setHiddenCharacter(request, callback) {
            return this.rpcCall(setHiddenCharacter, $root.lq.ReqSetHiddenCharacter, $root.lq.ResSetHiddenCharacter, request, callback);
        }, "name", { value: "setHiddenCharacter" });


        Object.defineProperty(Lobby.prototype.sendGiftToCharacter = function sendGiftToCharacter(request, callback) {
            return this.rpcCall(sendGiftToCharacter, $root.lq.ReqSendGiftToCharacter, $root.lq.ResSendGiftToCharacter, request, callback);
        }, "name", { value: "sendGiftToCharacter" });


        Object.defineProperty(Lobby.prototype.sellItem = function sellItem(request, callback) {
            return this.rpcCall(sellItem, $root.lq.ReqSellItem, $root.lq.ResCommon, request, callback);
        }, "name", { value: "sellItem" });


        Object.defineProperty(Lobby.prototype.fetchCommonView = function fetchCommonView(request, callback) {
            return this.rpcCall(fetchCommonView, $root.lq.ReqCommon, $root.lq.ResCommonView, request, callback);
        }, "name", { value: "fetchCommonView" });


        Object.defineProperty(Lobby.prototype.changeCommonView = function changeCommonView(request, callback) {
            return this.rpcCall(changeCommonView, $root.lq.ReqChangeCommonView, $root.lq.ResCommon, request, callback);
        }, "name", { value: "changeCommonView" });


        Object.defineProperty(Lobby.prototype.saveCommonViews = function saveCommonViews(request, callback) {
            return this.rpcCall(saveCommonViews, $root.lq.ReqSaveCommonViews, $root.lq.ResCommon, request, callback);
        }, "name", { value: "saveCommonViews" });


        Object.defineProperty(Lobby.prototype.fetchCommonViews = function fetchCommonViews(request, callback) {
            return this.rpcCall(fetchCommonViews, $root.lq.ReqCommonViews, $root.lq.ResCommonViews, request, callback);
        }, "name", { value: "fetchCommonViews" });


        Object.defineProperty(Lobby.prototype.fetchAllCommonViews = function fetchAllCommonViews(request, callback) {
            return this.rpcCall(fetchAllCommonViews, $root.lq.ReqCommon, $root.lq.ResAllcommonViews, request, callback);
        }, "name", { value: "fetchAllCommonViews" });


        Object.defineProperty(Lobby.prototype.useCommonView = function useCommonView(request, callback) {
            return this.rpcCall(useCommonView, $root.lq.ReqUseCommonView, $root.lq.ResCommon, request, callback);
        }, "name", { value: "useCommonView" });


        Object.defineProperty(Lobby.prototype.upgradeCharacter = function upgradeCharacter(request, callback) {
            return this.rpcCall(upgradeCharacter, $root.lq.ReqUpgradeCharacter, $root.lq.ResUpgradeCharacter, request, callback);
        }, "name", { value: "upgradeCharacter" });


        Object.defineProperty(Lobby.prototype.addFinishedEnding = function addFinishedEnding(request, callback) {
            return this.rpcCall(addFinishedEnding, $root.lq.ReqFinishedEnding, $root.lq.ResCommon, request, callback);
        }, "name", { value: "addFinishedEnding" });


        Object.defineProperty(Lobby.prototype.receiveEndingReward = function receiveEndingReward(request, callback) {
            return this.rpcCall(receiveEndingReward, $root.lq.ReqFinishedEnding, $root.lq.ResCommon, request, callback);
        }, "name", { value: "receiveEndingReward" });


        Object.defineProperty(Lobby.prototype.gameMasterCommand = function gameMasterCommand(request, callback) {
            return this.rpcCall(gameMasterCommand, $root.lq.ReqGMCommand, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gameMasterCommand" });


        Object.defineProperty(Lobby.prototype.fetchShopInfo = function fetchShopInfo(request, callback) {
            return this.rpcCall(fetchShopInfo, $root.lq.ReqCommon, $root.lq.ResShopInfo, request, callback);
        }, "name", { value: "fetchShopInfo" });


        Object.defineProperty(Lobby.prototype.buyFromShop = function buyFromShop(request, callback) {
            return this.rpcCall(buyFromShop, $root.lq.ReqBuyFromShop, $root.lq.ResBuyFromShop, request, callback);
        }, "name", { value: "buyFromShop" });


        Object.defineProperty(Lobby.prototype.buyFromZHP = function buyFromZHP(request, callback) {
            return this.rpcCall(buyFromZHP, $root.lq.ReqBuyFromZHP, $root.lq.ResCommon, request, callback);
        }, "name", { value: "buyFromZHP" });


        Object.defineProperty(Lobby.prototype.refreshZHPShop = function refreshZHPShop(request, callback) {
            return this.rpcCall(refreshZHPShop, $root.lq.ReqReshZHPShop, $root.lq.ResRefreshZHPShop, request, callback);
        }, "name", { value: "refreshZHPShop" });


        Object.defineProperty(Lobby.prototype.fetchMonthTicketInfo = function fetchMonthTicketInfo(request, callback) {
            return this.rpcCall(fetchMonthTicketInfo, $root.lq.ReqCommon, $root.lq.ResMonthTicketInfo, request, callback);
        }, "name", { value: "fetchMonthTicketInfo" });


        Object.defineProperty(Lobby.prototype.payMonthTicket = function payMonthTicket(request, callback) {
            return this.rpcCall(payMonthTicket, $root.lq.ReqCommon, $root.lq.ResPayMonthTicket, request, callback);
        }, "name", { value: "payMonthTicket" });


        Object.defineProperty(Lobby.prototype.exchangeCurrency = function exchangeCurrency(request, callback) {
            return this.rpcCall(exchangeCurrency, $root.lq.ReqExchangeCurrency, $root.lq.ResCommon, request, callback);
        }, "name", { value: "exchangeCurrency" });


        Object.defineProperty(Lobby.prototype.exchangeChestStone = function exchangeChestStone(request, callback) {
            return this.rpcCall(exchangeChestStone, $root.lq.ReqExchangeCurrency, $root.lq.ResCommon, request, callback);
        }, "name", { value: "exchangeChestStone" });


        Object.defineProperty(Lobby.prototype.exchangeDiamond = function exchangeDiamond(request, callback) {
            return this.rpcCall(exchangeDiamond, $root.lq.ReqExchangeCurrency, $root.lq.ResCommon, request, callback);
        }, "name", { value: "exchangeDiamond" });


        Object.defineProperty(Lobby.prototype.fetchServerSettings = function fetchServerSettings(request, callback) {
            return this.rpcCall(fetchServerSettings, $root.lq.ReqCommon, $root.lq.ResServerSettings, request, callback);
        }, "name", { value: "fetchServerSettings" });


        Object.defineProperty(Lobby.prototype.fetchAccountSettings = function fetchAccountSettings(request, callback) {
            return this.rpcCall(fetchAccountSettings, $root.lq.ReqCommon, $root.lq.ResAccountSettings, request, callback);
        }, "name", { value: "fetchAccountSettings" });


        Object.defineProperty(Lobby.prototype.updateAccountSettings = function updateAccountSettings(request, callback) {
            return this.rpcCall(updateAccountSettings, $root.lq.ReqUpdateAccountSettings, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateAccountSettings" });


        Object.defineProperty(Lobby.prototype.fetchModNicknameTime = function fetchModNicknameTime(request, callback) {
            return this.rpcCall(fetchModNicknameTime, $root.lq.ReqCommon, $root.lq.ResModNicknameTime, request, callback);
        }, "name", { value: "fetchModNicknameTime" });


        Object.defineProperty(Lobby.prototype.createWechatNativeOrder = function createWechatNativeOrder(request, callback) {
            return this.rpcCall(createWechatNativeOrder, $root.lq.ReqCreateWechatNativeOrder, $root.lq.ResCreateWechatNativeOrder, request, callback);
        }, "name", { value: "createWechatNativeOrder" });


        Object.defineProperty(Lobby.prototype.createWechatAppOrder = function createWechatAppOrder(request, callback) {
            return this.rpcCall(createWechatAppOrder, $root.lq.ReqCreateWechatAppOrder, $root.lq.ResCreateWechatAppOrder, request, callback);
        }, "name", { value: "createWechatAppOrder" });


        Object.defineProperty(Lobby.prototype.createAlipayOrder = function createAlipayOrder(request, callback) {
            return this.rpcCall(createAlipayOrder, $root.lq.ReqCreateAlipayOrder, $root.lq.ResCreateAlipayOrder, request, callback);
        }, "name", { value: "createAlipayOrder" });


        Object.defineProperty(Lobby.prototype.createAlipayScanOrder = function createAlipayScanOrder(request, callback) {
            return this.rpcCall(createAlipayScanOrder, $root.lq.ReqCreateAlipayScanOrder, $root.lq.ResCreateAlipayScanOrder, request, callback);
        }, "name", { value: "createAlipayScanOrder" });


        Object.defineProperty(Lobby.prototype.createAlipayAppOrder = function createAlipayAppOrder(request, callback) {
            return this.rpcCall(createAlipayAppOrder, $root.lq.ReqCreateAlipayAppOrder, $root.lq.ResCreateAlipayAppOrder, request, callback);
        }, "name", { value: "createAlipayAppOrder" });


        Object.defineProperty(Lobby.prototype.createJPCreditCardOrder = function createJPCreditCardOrder(request, callback) {
            return this.rpcCall(createJPCreditCardOrder, $root.lq.ReqCreateJPCreditCardOrder, $root.lq.ResCreateJPCreditCardOrder, request, callback);
        }, "name", { value: "createJPCreditCardOrder" });


        Object.defineProperty(Lobby.prototype.createJPPaypalOrder = function createJPPaypalOrder(request, callback) {
            return this.rpcCall(createJPPaypalOrder, $root.lq.ReqCreateJPPaypalOrder, $root.lq.ResCreateJPPaypalOrder, request, callback);
        }, "name", { value: "createJPPaypalOrder" });


        Object.defineProperty(Lobby.prototype.createJPAuOrder = function createJPAuOrder(request, callback) {
            return this.rpcCall(createJPAuOrder, $root.lq.ReqCreateJPAuOrder, $root.lq.ResCreateJPAuOrder, request, callback);
        }, "name", { value: "createJPAuOrder" });


        Object.defineProperty(Lobby.prototype.createJPDocomoOrder = function createJPDocomoOrder(request, callback) {
            return this.rpcCall(createJPDocomoOrder, $root.lq.ReqCreateJPDocomoOrder, $root.lq.ResCreateJPDocomoOrder, request, callback);
        }, "name", { value: "createJPDocomoOrder" });


        Object.defineProperty(Lobby.prototype.createJPWebMoneyOrder = function createJPWebMoneyOrder(request, callback) {
            return this.rpcCall(createJPWebMoneyOrder, $root.lq.ReqCreateJPWebMoneyOrder, $root.lq.ResCreateJPWebMoneyOrder, request, callback);
        }, "name", { value: "createJPWebMoneyOrder" });


        Object.defineProperty(Lobby.prototype.createJPSoftbankOrder = function createJPSoftbankOrder(request, callback) {
            return this.rpcCall(createJPSoftbankOrder, $root.lq.ReqCreateJPSoftbankOrder, $root.lq.ResCreateJPSoftbankOrder, request, callback);
        }, "name", { value: "createJPSoftbankOrder" });


        Object.defineProperty(Lobby.prototype.createJPPayPayOrder = function createJPPayPayOrder(request, callback) {
            return this.rpcCall(createJPPayPayOrder, $root.lq.ReqCreateJPPayPayOrder, $root.lq.ResCreateJPPayPayOrder, request, callback);
        }, "name", { value: "createJPPayPayOrder" });


        Object.defineProperty(Lobby.prototype.fetchJPCommonCreditCardOrder = function fetchJPCommonCreditCardOrder(request, callback) {
            return this.rpcCall(fetchJPCommonCreditCardOrder, $root.lq.ReqFetchJPCommonCreditCardOrder, $root.lq.ResFetchJPCommonCreditCardOrder, request, callback);
        }, "name", { value: "fetchJPCommonCreditCardOrder" });


        Object.defineProperty(Lobby.prototype.createJPGMOOrder = function createJPGMOOrder(request, callback) {
            return this.rpcCall(createJPGMOOrder, $root.lq.ReqCreateJPGMOOrder, $root.lq.ResCreateJPGMOOrder, request, callback);
        }, "name", { value: "createJPGMOOrder" });


        Object.defineProperty(Lobby.prototype.createENPaypalOrder = function createENPaypalOrder(request, callback) {
            return this.rpcCall(createENPaypalOrder, $root.lq.ReqCreateENPaypalOrder, $root.lq.ResCreateENPaypalOrder, request, callback);
        }, "name", { value: "createENPaypalOrder" });


        Object.defineProperty(Lobby.prototype.createENMasterCardOrder = function createENMasterCardOrder(request, callback) {
            return this.rpcCall(createENMasterCardOrder, $root.lq.ReqCreateENMasterCardOrder, $root.lq.ResCreateENMasterCardOrder, request, callback);
        }, "name", { value: "createENMasterCardOrder" });


        Object.defineProperty(Lobby.prototype.createENVisaOrder = function createENVisaOrder(request, callback) {
            return this.rpcCall(createENVisaOrder, $root.lq.ReqCreateENVisaOrder, $root.lq.ResCreateENVisaOrder, request, callback);
        }, "name", { value: "createENVisaOrder" });


        Object.defineProperty(Lobby.prototype.createENJCBOrder = function createENJCBOrder(request, callback) {
            return this.rpcCall(createENJCBOrder, $root.lq.ReqCreateENJCBOrder, $root.lq.ResCreateENJCBOrder, request, callback);
        }, "name", { value: "createENJCBOrder" });


        Object.defineProperty(Lobby.prototype.createENAlipayOrder = function createENAlipayOrder(request, callback) {
            return this.rpcCall(createENAlipayOrder, $root.lq.ReqCreateENAlipayOrder, $root.lq.ResCreateENAlipayOrder, request, callback);
        }, "name", { value: "createENAlipayOrder" });


        Object.defineProperty(Lobby.prototype.createKRPaypalOrder = function createKRPaypalOrder(request, callback) {
            return this.rpcCall(createKRPaypalOrder, $root.lq.ReqCreateKRPaypalOrder, $root.lq.ResCreateKRPaypalOrder, request, callback);
        }, "name", { value: "createKRPaypalOrder" });


        Object.defineProperty(Lobby.prototype.createKRMasterCardOrder = function createKRMasterCardOrder(request, callback) {
            return this.rpcCall(createKRMasterCardOrder, $root.lq.ReqCreateKRMasterCardOrder, $root.lq.ResCreateKRMasterCardOrder, request, callback);
        }, "name", { value: "createKRMasterCardOrder" });


        Object.defineProperty(Lobby.prototype.createKRVisaOrder = function createKRVisaOrder(request, callback) {
            return this.rpcCall(createKRVisaOrder, $root.lq.ReqCreateKRVisaOrder, $root.lq.ResCreateKRVisaOrder, request, callback);
        }, "name", { value: "createKRVisaOrder" });


        Object.defineProperty(Lobby.prototype.createKRJCBOrder = function createKRJCBOrder(request, callback) {
            return this.rpcCall(createKRJCBOrder, $root.lq.ReqCreateKRJCBOrder, $root.lq.ResCreateKRJCBOrder, request, callback);
        }, "name", { value: "createKRJCBOrder" });


        Object.defineProperty(Lobby.prototype.createKRAlipayOrder = function createKRAlipayOrder(request, callback) {
            return this.rpcCall(createKRAlipayOrder, $root.lq.ReqCreateKRAlipayOrder, $root.lq.ResCreateKRAlipayOrder, request, callback);
        }, "name", { value: "createKRAlipayOrder" });


        Object.defineProperty(Lobby.prototype.createDMMOrder = function createDMMOrder(request, callback) {
            return this.rpcCall(createDMMOrder, $root.lq.ReqCreateDMMOrder, $root.lq.ResCreateDmmOrder, request, callback);
        }, "name", { value: "createDMMOrder" });


        Object.defineProperty(Lobby.prototype.createIAPOrder = function createIAPOrder(request, callback) {
            return this.rpcCall(createIAPOrder, $root.lq.ReqCreateIAPOrder, $root.lq.ResCreateIAPOrder, request, callback);
        }, "name", { value: "createIAPOrder" });


        Object.defineProperty(Lobby.prototype.createSteamOrder = function createSteamOrder(request, callback) {
            return this.rpcCall(createSteamOrder, $root.lq.ReqCreateSteamOrder, $root.lq.ResCreateSteamOrder, request, callback);
        }, "name", { value: "createSteamOrder" });


        Object.defineProperty(Lobby.prototype.verifySteamOrder = function verifySteamOrder(request, callback) {
            return this.rpcCall(verifySteamOrder, $root.lq.ReqVerifySteamOrder, $root.lq.ResCommon, request, callback);
        }, "name", { value: "verifySteamOrder" });


        Object.defineProperty(Lobby.prototype.createMyCardAndroidOrder = function createMyCardAndroidOrder(request, callback) {
            return this.rpcCall(createMyCardAndroidOrder, $root.lq.ReqCreateMyCardOrder, $root.lq.ResCreateMyCardOrder, request, callback);
        }, "name", { value: "createMyCardAndroidOrder" });


        Object.defineProperty(Lobby.prototype.createMyCardWebOrder = function createMyCardWebOrder(request, callback) {
            return this.rpcCall(createMyCardWebOrder, $root.lq.ReqCreateMyCardOrder, $root.lq.ResCreateMyCardOrder, request, callback);
        }, "name", { value: "createMyCardWebOrder" });


        Object.defineProperty(Lobby.prototype.createPaypalOrder = function createPaypalOrder(request, callback) {
            return this.rpcCall(createPaypalOrder, $root.lq.ReqCreatePaypalOrder, $root.lq.ResCreatePaypalOrder, request, callback);
        }, "name", { value: "createPaypalOrder" });


        Object.defineProperty(Lobby.prototype.createXsollaOrder = function createXsollaOrder(request, callback) {
            return this.rpcCall(createXsollaOrder, $root.lq.ReqCreateXsollaOrder, $root.lq.ResCreateXsollaOrder, request, callback);
        }, "name", { value: "createXsollaOrder" });


        Object.defineProperty(Lobby.prototype.createXsollaV4Order = function createXsollaV4Order(request, callback) {
            return this.rpcCall(createXsollaV4Order, $root.lq.ReqCreateXsollaOrder, $root.lq.ResCreateXsollaOrder, request, callback);
        }, "name", { value: "createXsollaV4Order" });


        Object.defineProperty(Lobby.prototype.verifyMyCardOrder = function verifyMyCardOrder(request, callback) {
            return this.rpcCall(verifyMyCardOrder, $root.lq.ReqVerifyMyCardOrder, $root.lq.ResCommon, request, callback);
        }, "name", { value: "verifyMyCardOrder" });


        Object.defineProperty(Lobby.prototype.verificationIAPOrder = function verificationIAPOrder(request, callback) {
            return this.rpcCall(verificationIAPOrder, $root.lq.ReqVerificationIAPOrder, $root.lq.ResVerificationIAPOrder, request, callback);
        }, "name", { value: "verificationIAPOrder" });


        Object.defineProperty(Lobby.prototype.createYostarSDKOrder = function createYostarSDKOrder(request, callback) {
            return this.rpcCall(createYostarSDKOrder, $root.lq.ReqCreateYostarOrder, $root.lq.ResCreateYostarOrder, request, callback);
        }, "name", { value: "createYostarSDKOrder" });


        Object.defineProperty(Lobby.prototype.createYostarV4SDKOrder = function createYostarV4SDKOrder(request, callback) {
            return this.rpcCall(createYostarV4SDKOrder, $root.lq.ReqCreateYostarV4SDKOrder, $root.lq.ResCreateYostarV4SDKOrder, request, callback);
        }, "name", { value: "createYostarV4SDKOrder" });


        Object.defineProperty(Lobby.prototype.createBillingOrder = function createBillingOrder(request, callback) {
            return this.rpcCall(createBillingOrder, $root.lq.ReqCreateBillingOrder, $root.lq.ResCreateBillingOrder, request, callback);
        }, "name", { value: "createBillingOrder" });


        Object.defineProperty(Lobby.prototype.solveGooglePlayOrder = function solveGooglePlayOrder(request, callback) {
            return this.rpcCall(solveGooglePlayOrder, $root.lq.ReqSolveGooglePlayOrder, $root.lq.ResCommon, request, callback);
        }, "name", { value: "solveGooglePlayOrder" });


        Object.defineProperty(Lobby.prototype.solveGooglePayOrderV3 = function solveGooglePayOrderV3(request, callback) {
            return this.rpcCall(solveGooglePayOrderV3, $root.lq.ReqSolveGooglePlayOrderV3, $root.lq.ResCommon, request, callback);
        }, "name", { value: "solveGooglePayOrderV3" });


        Object.defineProperty(Lobby.prototype.deliverAA32Order = function deliverAA32Order(request, callback) {
            return this.rpcCall(deliverAA32Order, $root.lq.ReqDeliverAA32Order, $root.lq.ResCommon, request, callback);
        }, "name", { value: "deliverAA32Order" });


        Object.defineProperty(Lobby.prototype.fetchMisc = function fetchMisc(request, callback) {
            return this.rpcCall(fetchMisc, $root.lq.ReqCommon, $root.lq.ResMisc, request, callback);
        }, "name", { value: "fetchMisc" });


        Object.defineProperty(Lobby.prototype.modifySignature = function modifySignature(request, callback) {
            return this.rpcCall(modifySignature, $root.lq.ReqModifySignature, $root.lq.ResCommon, request, callback);
        }, "name", { value: "modifySignature" });


        Object.defineProperty(Lobby.prototype.fetchIDCardInfo = function fetchIDCardInfo(request, callback) {
            return this.rpcCall(fetchIDCardInfo, $root.lq.ReqCommon, $root.lq.ResIDCardInfo, request, callback);
        }, "name", { value: "fetchIDCardInfo" });


        Object.defineProperty(Lobby.prototype.updateIDCardInfo = function updateIDCardInfo(request, callback) {
            return this.rpcCall(updateIDCardInfo, $root.lq.ReqUpdateIDCardInfo, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateIDCardInfo" });


        Object.defineProperty(Lobby.prototype.fetchVipReward = function fetchVipReward(request, callback) {
            return this.rpcCall(fetchVipReward, $root.lq.ReqCommon, $root.lq.ResVipReward, request, callback);
        }, "name", { value: "fetchVipReward" });


        Object.defineProperty(Lobby.prototype.gainVipReward = function gainVipReward(request, callback) {
            return this.rpcCall(gainVipReward, $root.lq.ReqGainVipReward, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gainVipReward" });


        Object.defineProperty(Lobby.prototype.fetchRefundOrder = function fetchRefundOrder(request, callback) {
            return this.rpcCall(fetchRefundOrder, $root.lq.ReqCommon, $root.lq.ResFetchRefundOrder, request, callback);
        }, "name", { value: "fetchRefundOrder" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestList = function fetchCustomizedContestList(request, callback) {
            return this.rpcCall(fetchCustomizedContestList, $root.lq.ReqFetchCustomizedContestList, $root.lq.ResFetchCustomizedContestList, request, callback);
        }, "name", { value: "fetchCustomizedContestList" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestAuthInfo = function fetchCustomizedContestAuthInfo(request, callback) {
            return this.rpcCall(fetchCustomizedContestAuthInfo, $root.lq.ReqFetchCustomizedContestAuthInfo, $root.lq.ResFetchCustomizedContestAuthInfo, request, callback);
        }, "name", { value: "fetchCustomizedContestAuthInfo" });


        Object.defineProperty(Lobby.prototype.enterCustomizedContest = function enterCustomizedContest(request, callback) {
            return this.rpcCall(enterCustomizedContest, $root.lq.ReqEnterCustomizedContest, $root.lq.ResEnterCustomizedContest, request, callback);
        }, "name", { value: "enterCustomizedContest" });


        Object.defineProperty(Lobby.prototype.leaveCustomizedContest = function leaveCustomizedContest(request, callback) {
            return this.rpcCall(leaveCustomizedContest, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "leaveCustomizedContest" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestOnlineInfo = function fetchCustomizedContestOnlineInfo(request, callback) {
            return this.rpcCall(fetchCustomizedContestOnlineInfo, $root.lq.ReqFetchCustomizedContestOnlineInfo, $root.lq.ResFetchCustomizedContestOnlineInfo, request, callback);
        }, "name", { value: "fetchCustomizedContestOnlineInfo" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestByContestId = function fetchCustomizedContestByContestId(request, callback) {
            return this.rpcCall(fetchCustomizedContestByContestId, $root.lq.ReqFetchCustomizedContestByContestId, $root.lq.ResFetchCustomizedContestByContestId, request, callback);
        }, "name", { value: "fetchCustomizedContestByContestId" });


        Object.defineProperty(Lobby.prototype.signupCustomizedContest = function signupCustomizedContest(request, callback) {
            return this.rpcCall(signupCustomizedContest, $root.lq.ReqSignupCustomizedContest, $root.lq.ResSignupCustomizedContest, request, callback);
        }, "name", { value: "signupCustomizedContest" });


        Object.defineProperty(Lobby.prototype.startCustomizedContest = function startCustomizedContest(request, callback) {
            return this.rpcCall(startCustomizedContest, $root.lq.ReqStartCustomizedContest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "startCustomizedContest" });


        Object.defineProperty(Lobby.prototype.stopCustomizedContest = function stopCustomizedContest(request, callback) {
            return this.rpcCall(stopCustomizedContest, $root.lq.ReqStopCustomizedContest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "stopCustomizedContest" });


        Object.defineProperty(Lobby.prototype.joinCustomizedContestChatRoom = function joinCustomizedContestChatRoom(request, callback) {
            return this.rpcCall(joinCustomizedContestChatRoom, $root.lq.ReqJoinCustomizedContestChatRoom, $root.lq.ResJoinCustomizedContestChatRoom, request, callback);
        }, "name", { value: "joinCustomizedContestChatRoom" });


        Object.defineProperty(Lobby.prototype.leaveCustomizedContestChatRoom = function leaveCustomizedContestChatRoom(request, callback) {
            return this.rpcCall(leaveCustomizedContestChatRoom, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "leaveCustomizedContestChatRoom" });


        Object.defineProperty(Lobby.prototype.sayChatMessage = function sayChatMessage(request, callback) {
            return this.rpcCall(sayChatMessage, $root.lq.ReqSayChatMessage, $root.lq.ResCommon, request, callback);
        }, "name", { value: "sayChatMessage" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestGameRecords = function fetchCustomizedContestGameRecords(request, callback) {
            return this.rpcCall(fetchCustomizedContestGameRecords, $root.lq.ReqFetchCustomizedContestGameRecords, $root.lq.ResFetchCustomizedContestGameRecords, request, callback);
        }, "name", { value: "fetchCustomizedContestGameRecords" });


        Object.defineProperty(Lobby.prototype.fetchCustomizedContestGameLiveList = function fetchCustomizedContestGameLiveList(request, callback) {
            return this.rpcCall(fetchCustomizedContestGameLiveList, $root.lq.ReqFetchCustomizedContestGameLiveList, $root.lq.ResFetchCustomizedContestGameLiveList, request, callback);
        }, "name", { value: "fetchCustomizedContestGameLiveList" });


        Object.defineProperty(Lobby.prototype.followCustomizedContest = function followCustomizedContest(request, callback) {
            return this.rpcCall(followCustomizedContest, $root.lq.ReqTargetCustomizedContest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "followCustomizedContest" });


        Object.defineProperty(Lobby.prototype.unfollowCustomizedContest = function unfollowCustomizedContest(request, callback) {
            return this.rpcCall(unfollowCustomizedContest, $root.lq.ReqTargetCustomizedContest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "unfollowCustomizedContest" });


        Object.defineProperty(Lobby.prototype.fetchContestTeamRank = function fetchContestTeamRank(request, callback) {
            return this.rpcCall(fetchContestTeamRank, $root.lq.ReqFetchContestTeamRank, $root.lq.ResFetchContestTeamRank, request, callback);
        }, "name", { value: "fetchContestTeamRank" });


        Object.defineProperty(Lobby.prototype.fetchContestTeamMember = function fetchContestTeamMember(request, callback) {
            return this.rpcCall(fetchContestTeamMember, $root.lq.ReqFetchContestTeamMember, $root.lq.ResFetchContestTeamMember, request, callback);
        }, "name", { value: "fetchContestTeamMember" });


        Object.defineProperty(Lobby.prototype.fetchContestTeamPlayerRank = function fetchContestTeamPlayerRank(request, callback) {
            return this.rpcCall(fetchContestTeamPlayerRank, $root.lq.ReqFetchContestTeamPlayerRank, $root.lq.ResFetchContestPlayerRank, request, callback);
        }, "name", { value: "fetchContestTeamPlayerRank" });


        Object.defineProperty(Lobby.prototype.fetchContestPlayerRank = function fetchContestPlayerRank(request, callback) {
            return this.rpcCall(fetchContestPlayerRank, $root.lq.ReqFetchContestPlayerRank, $root.lq.ResFetchContestPlayerRank, request, callback);
        }, "name", { value: "fetchContestPlayerRank" });


        Object.defineProperty(Lobby.prototype.fetchActivityList = function fetchActivityList(request, callback) {
            return this.rpcCall(fetchActivityList, $root.lq.ReqCommon, $root.lq.ResActivityList, request, callback);
        }, "name", { value: "fetchActivityList" });


        Object.defineProperty(Lobby.prototype.fetchAccountActivityData = function fetchAccountActivityData(request, callback) {
            return this.rpcCall(fetchAccountActivityData, $root.lq.ReqCommon, $root.lq.ResAccountActivityData, request, callback);
        }, "name", { value: "fetchAccountActivityData" });


        Object.defineProperty(Lobby.prototype.exchangeActivityItem = function exchangeActivityItem(request, callback) {
            return this.rpcCall(exchangeActivityItem, $root.lq.ReqExchangeActivityItem, $root.lq.ResExchangeActivityItem, request, callback);
        }, "name", { value: "exchangeActivityItem" });


        Object.defineProperty(Lobby.prototype.completeActivityTask = function completeActivityTask(request, callback) {
            return this.rpcCall(completeActivityTask, $root.lq.ReqCompleteActivityTask, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completeActivityTask" });


        Object.defineProperty(Lobby.prototype.completeActivityTaskBatch = function completeActivityTaskBatch(request, callback) {
            return this.rpcCall(completeActivityTaskBatch, $root.lq.ReqCompleteActivityTaskBatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completeActivityTaskBatch" });


        Object.defineProperty(Lobby.prototype.completeActivityFlipTask = function completeActivityFlipTask(request, callback) {
            return this.rpcCall(completeActivityFlipTask, $root.lq.ReqCompleteActivityTask, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completeActivityFlipTask" });


        Object.defineProperty(Lobby.prototype.completeActivityFlipTaskBatch = function completeActivityFlipTaskBatch(request, callback) {
            return this.rpcCall(completeActivityFlipTaskBatch, $root.lq.ReqCompleteActivityFlipTaskBatch, $root.lq.ResCompleteActivityFlipTaskBatch, request, callback);
        }, "name", { value: "completeActivityFlipTaskBatch" });


        Object.defineProperty(Lobby.prototype.completePeriodActivityTask = function completePeriodActivityTask(request, callback) {
            return this.rpcCall(completePeriodActivityTask, $root.lq.ReqCompleteActivityTask, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completePeriodActivityTask" });


        Object.defineProperty(Lobby.prototype.completePeriodActivityTaskBatch = function completePeriodActivityTaskBatch(request, callback) {
            return this.rpcCall(completePeriodActivityTaskBatch, $root.lq.ReqCompletePeriodActivityTaskBatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completePeriodActivityTaskBatch" });


        Object.defineProperty(Lobby.prototype.completeRandomActivityTask = function completeRandomActivityTask(request, callback) {
            return this.rpcCall(completeRandomActivityTask, $root.lq.ReqCompleteActivityTask, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completeRandomActivityTask" });


        Object.defineProperty(Lobby.prototype.completeRandomActivityTaskBatch = function completeRandomActivityTaskBatch(request, callback) {
            return this.rpcCall(completeRandomActivityTaskBatch, $root.lq.ReqCompleteActivityTaskBatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "completeRandomActivityTaskBatch" });


        Object.defineProperty(Lobby.prototype.receiveActivityFlipTask = function receiveActivityFlipTask(request, callback) {
            return this.rpcCall(receiveActivityFlipTask, $root.lq.ReqReceiveActivityFlipTask, $root.lq.ResReceiveActivityFlipTask, request, callback);
        }, "name", { value: "receiveActivityFlipTask" });


        Object.defineProperty(Lobby.prototype.receiveActivityFlipTaskBatch = function receiveActivityFlipTaskBatch(request, callback) {
            return this.rpcCall(receiveActivityFlipTaskBatch, $root.lq.ReqReceiveActivityFlipTaskBatch, $root.lq.ResReceiveActivityFlipTaskBatch, request, callback);
        }, "name", { value: "receiveActivityFlipTaskBatch" });


        Object.defineProperty(Lobby.prototype.completeSegmentTaskReward = function completeSegmentTaskReward(request, callback) {
            return this.rpcCall(completeSegmentTaskReward, $root.lq.ReqCompleteSegmentTaskReward, $root.lq.ResCompleteSegmentTaskReward, request, callback);
        }, "name", { value: "completeSegmentTaskReward" });


        Object.defineProperty(Lobby.prototype.fetchActivityFlipInfo = function fetchActivityFlipInfo(request, callback) {
            return this.rpcCall(fetchActivityFlipInfo, $root.lq.ReqFetchActivityFlipInfo, $root.lq.ResFetchActivityFlipInfo, request, callback);
        }, "name", { value: "fetchActivityFlipInfo" });


        Object.defineProperty(Lobby.prototype.gainAccumulatedPointActivityReward = function gainAccumulatedPointActivityReward(request, callback) {
            return this.rpcCall(gainAccumulatedPointActivityReward, $root.lq.ReqGainAccumulatedPointActivityReward, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gainAccumulatedPointActivityReward" });


        Object.defineProperty(Lobby.prototype.gainMultiPointActivityReward = function gainMultiPointActivityReward(request, callback) {
            return this.rpcCall(gainMultiPointActivityReward, $root.lq.ReqGainMultiPointActivityReward, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gainMultiPointActivityReward" });


        Object.defineProperty(Lobby.prototype.fetchRankPointLeaderboard = function fetchRankPointLeaderboard(request, callback) {
            return this.rpcCall(fetchRankPointLeaderboard, $root.lq.ReqFetchRankPointLeaderboard, $root.lq.ResFetchRankPointLeaderboard, request, callback);
        }, "name", { value: "fetchRankPointLeaderboard" });


        Object.defineProperty(Lobby.prototype.gainRankPointReward = function gainRankPointReward(request, callback) {
            return this.rpcCall(gainRankPointReward, $root.lq.ReqGainRankPointReward, $root.lq.ResCommon, request, callback);
        }, "name", { value: "gainRankPointReward" });


        Object.defineProperty(Lobby.prototype.richmanActivityNextMove = function richmanActivityNextMove(request, callback) {
            return this.rpcCall(richmanActivityNextMove, $root.lq.ReqRichmanNextMove, $root.lq.ResRichmanNextMove, request, callback);
        }, "name", { value: "richmanActivityNextMove" });


        Object.defineProperty(Lobby.prototype.richmanAcitivitySpecialMove = function richmanAcitivitySpecialMove(request, callback) {
            return this.rpcCall(richmanAcitivitySpecialMove, $root.lq.ReqRichmanSpecialMove, $root.lq.ResRichmanNextMove, request, callback);
        }, "name", { value: "richmanAcitivitySpecialMove" });


        Object.defineProperty(Lobby.prototype.richmanActivityChestInfo = function richmanActivityChestInfo(request, callback) {
            return this.rpcCall(richmanActivityChestInfo, $root.lq.ReqRichmanChestInfo, $root.lq.ResRichmanChestInfo, request, callback);
        }, "name", { value: "richmanActivityChestInfo" });


        Object.defineProperty(Lobby.prototype.createGameObserveAuth = function createGameObserveAuth(request, callback) {
            return this.rpcCall(createGameObserveAuth, $root.lq.ReqCreateGameObserveAuth, $root.lq.ResCreateGameObserveAuth, request, callback);
        }, "name", { value: "createGameObserveAuth" });


        Object.defineProperty(Lobby.prototype.refreshGameObserveAuth = function refreshGameObserveAuth(request, callback) {
            return this.rpcCall(refreshGameObserveAuth, $root.lq.ReqRefreshGameObserveAuth, $root.lq.ResRefreshGameObserveAuth, request, callback);
        }, "name", { value: "refreshGameObserveAuth" });


        Object.defineProperty(Lobby.prototype.fetchActivityBuff = function fetchActivityBuff(request, callback) {
            return this.rpcCall(fetchActivityBuff, $root.lq.ReqCommon, $root.lq.ResActivityBuff, request, callback);
        }, "name", { value: "fetchActivityBuff" });


        Object.defineProperty(Lobby.prototype.upgradeActivityBuff = function upgradeActivityBuff(request, callback) {
            return this.rpcCall(upgradeActivityBuff, $root.lq.ReqUpgradeActivityBuff, $root.lq.ResActivityBuff, request, callback);
        }, "name", { value: "upgradeActivityBuff" });


        Object.defineProperty(Lobby.prototype.setActivityBuff = function setActivityBuff(request, callback) {
            return this.rpcCall(setActivityBuff, $root.lq.ReqSetActivityBuff, $root.lq.ResActivityBuff, request, callback);
        }, "name", { value: "setActivityBuff" });


        Object.defineProperty(Lobby.prototype.upgradeActivityLevel = function upgradeActivityLevel(request, callback) {
            return this.rpcCall(upgradeActivityLevel, $root.lq.ReqUpgradeActivityLevel, $root.lq.ResUpgradeActivityLevel, request, callback);
        }, "name", { value: "upgradeActivityLevel" });


        Object.defineProperty(Lobby.prototype.receiveUpgradeActivityReward = function receiveUpgradeActivityReward(request, callback) {
            return this.rpcCall(receiveUpgradeActivityReward, $root.lq.ReqReceiveUpgradeActivityReward, $root.lq.ResReceiveUpgradeActivityReward, request, callback);
        }, "name", { value: "receiveUpgradeActivityReward" });


        Object.defineProperty(Lobby.prototype.upgradeChallenge = function upgradeChallenge(request, callback) {
            return this.rpcCall(upgradeChallenge, $root.lq.ReqCommon, $root.lq.ResUpgradeChallenge, request, callback);
        }, "name", { value: "upgradeChallenge" });


        Object.defineProperty(Lobby.prototype.refreshChallenge = function refreshChallenge(request, callback) {
            return this.rpcCall(refreshChallenge, $root.lq.ReqCommon, $root.lq.ResRefreshChallenge, request, callback);
        }, "name", { value: "refreshChallenge" });


        Object.defineProperty(Lobby.prototype.fetchChallengeInfo = function fetchChallengeInfo(request, callback) {
            return this.rpcCall(fetchChallengeInfo, $root.lq.ReqCommon, $root.lq.ResFetchChallengeInfo, request, callback);
        }, "name", { value: "fetchChallengeInfo" });


        Object.defineProperty(Lobby.prototype.forceCompleteChallengeTask = function forceCompleteChallengeTask(request, callback) {
            return this.rpcCall(forceCompleteChallengeTask, $root.lq.ReqForceCompleteChallengeTask, $root.lq.ResCommon, request, callback);
        }, "name", { value: "forceCompleteChallengeTask" });


        Object.defineProperty(Lobby.prototype.fetchChallengeSeason = function fetchChallengeSeason(request, callback) {
            return this.rpcCall(fetchChallengeSeason, $root.lq.ReqCommon, $root.lq.ResChallengeSeasonInfo, request, callback);
        }, "name", { value: "fetchChallengeSeason" });


        Object.defineProperty(Lobby.prototype.receiveChallengeRankReward = function receiveChallengeRankReward(request, callback) {
            return this.rpcCall(receiveChallengeRankReward, $root.lq.ReqReceiveChallengeRankReward, $root.lq.ResReceiveChallengeRankReward, request, callback);
        }, "name", { value: "receiveChallengeRankReward" });


        Object.defineProperty(Lobby.prototype.fetchABMatchInfo = function fetchABMatchInfo(request, callback) {
            return this.rpcCall(fetchABMatchInfo, $root.lq.ReqCommon, $root.lq.ResFetchABMatch, request, callback);
        }, "name", { value: "fetchABMatchInfo" });


        Object.defineProperty(Lobby.prototype.buyInABMatch = function buyInABMatch(request, callback) {
            return this.rpcCall(buyInABMatch, $root.lq.ReqBuyInABMatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "buyInABMatch" });


        Object.defineProperty(Lobby.prototype.receiveABMatchReward = function receiveABMatchReward(request, callback) {
            return this.rpcCall(receiveABMatchReward, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "receiveABMatchReward" });


        Object.defineProperty(Lobby.prototype.quitABMatch = function quitABMatch(request, callback) {
            return this.rpcCall(quitABMatch, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "quitABMatch" });


        Object.defineProperty(Lobby.prototype.startUnifiedMatch = function startUnifiedMatch(request, callback) {
            return this.rpcCall(startUnifiedMatch, $root.lq.ReqStartUnifiedMatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "startUnifiedMatch" });


        Object.defineProperty(Lobby.prototype.cancelUnifiedMatch = function cancelUnifiedMatch(request, callback) {
            return this.rpcCall(cancelUnifiedMatch, $root.lq.ReqCancelUnifiedMatch, $root.lq.ResCommon, request, callback);
        }, "name", { value: "cancelUnifiedMatch" });


        Object.defineProperty(Lobby.prototype.fetchGamePointRank = function fetchGamePointRank(request, callback) {
            return this.rpcCall(fetchGamePointRank, $root.lq.ReqGamePointRank, $root.lq.ResGamePointRank, request, callback);
        }, "name", { value: "fetchGamePointRank" });


        Object.defineProperty(Lobby.prototype.fetchSelfGamePointRank = function fetchSelfGamePointRank(request, callback) {
            return this.rpcCall(fetchSelfGamePointRank, $root.lq.ReqGamePointRank, $root.lq.ResFetchSelfGamePointRank, request, callback);
        }, "name", { value: "fetchSelfGamePointRank" });


        Object.defineProperty(Lobby.prototype.readSNS = function readSNS(request, callback) {
            return this.rpcCall(readSNS, $root.lq.ReqReadSNS, $root.lq.ResReadSNS, request, callback);
        }, "name", { value: "readSNS" });


        Object.defineProperty(Lobby.prototype.replySNS = function replySNS(request, callback) {
            return this.rpcCall(replySNS, $root.lq.ReqReplySNS, $root.lq.ResReplySNS, request, callback);
        }, "name", { value: "replySNS" });


        Object.defineProperty(Lobby.prototype.likeSNS = function likeSNS(request, callback) {
            return this.rpcCall(likeSNS, $root.lq.ReqLikeSNS, $root.lq.ResLikeSNS, request, callback);
        }, "name", { value: "likeSNS" });


        Object.defineProperty(Lobby.prototype.digMine = function digMine(request, callback) {
            return this.rpcCall(digMine, $root.lq.ReqDigMine, $root.lq.ResDigMine, request, callback);
        }, "name", { value: "digMine" });


        Object.defineProperty(Lobby.prototype.fetchLastPrivacy = function fetchLastPrivacy(request, callback) {
            return this.rpcCall(fetchLastPrivacy, $root.lq.ReqFetchLastPrivacy, $root.lq.ResFetchLastPrivacy, request, callback);
        }, "name", { value: "fetchLastPrivacy" });


        Object.defineProperty(Lobby.prototype.checkPrivacy = function checkPrivacy(request, callback) {
            return this.rpcCall(checkPrivacy, $root.lq.ReqCheckPrivacy, $root.lq.ResCommon, request, callback);
        }, "name", { value: "checkPrivacy" });


        Object.defineProperty(Lobby.prototype.fetchRPGBattleHistory = function fetchRPGBattleHistory(request, callback) {
            return this.rpcCall(fetchRPGBattleHistory, $root.lq.ReqFetchRPGBattleHistory, $root.lq.ResFetchRPGBattleHistory, request, callback);
        }, "name", { value: "fetchRPGBattleHistory" });


        Object.defineProperty(Lobby.prototype.fetchRPGBattleHistoryV2 = function fetchRPGBattleHistoryV2(request, callback) {
            return this.rpcCall(fetchRPGBattleHistoryV2, $root.lq.ReqFetchRPGBattleHistory, $root.lq.ResFetchRPGBattleHistoryV2, request, callback);
        }, "name", { value: "fetchRPGBattleHistoryV2" });


        Object.defineProperty(Lobby.prototype.receiveRPGRewards = function receiveRPGRewards(request, callback) {
            return this.rpcCall(receiveRPGRewards, $root.lq.ReqReceiveRPGRewards, $root.lq.ResReceiveRPGRewards, request, callback);
        }, "name", { value: "receiveRPGRewards" });


        Object.defineProperty(Lobby.prototype.receiveRPGReward = function receiveRPGReward(request, callback) {
            return this.rpcCall(receiveRPGReward, $root.lq.ReqReceiveRPGReward, $root.lq.ResReceiveRPGRewards, request, callback);
        }, "name", { value: "receiveRPGReward" });


        Object.defineProperty(Lobby.prototype.buyArenaTicket = function buyArenaTicket(request, callback) {
            return this.rpcCall(buyArenaTicket, $root.lq.ReqBuyArenaTicket, $root.lq.ResCommon, request, callback);
        }, "name", { value: "buyArenaTicket" });


        Object.defineProperty(Lobby.prototype.enterArena = function enterArena(request, callback) {
            return this.rpcCall(enterArena, $root.lq.ReqEnterArena, $root.lq.ResCommon, request, callback);
        }, "name", { value: "enterArena" });


        Object.defineProperty(Lobby.prototype.receiveArenaReward = function receiveArenaReward(request, callback) {
            return this.rpcCall(receiveArenaReward, $root.lq.ReqArenaReward, $root.lq.ResArenaReward, request, callback);
        }, "name", { value: "receiveArenaReward" });


        Object.defineProperty(Lobby.prototype.fetchOBToken = function fetchOBToken(request, callback) {
            return this.rpcCall(fetchOBToken, $root.lq.ReqFetchOBToken, $root.lq.ResFetchOBToken, request, callback);
        }, "name", { value: "fetchOBToken" });


        Object.defineProperty(Lobby.prototype.receiveCharacterRewards = function receiveCharacterRewards(request, callback) {
            return this.rpcCall(receiveCharacterRewards, $root.lq.ReqReceiveCharacterRewards, $root.lq.ResReceiveCharacterRewards, request, callback);
        }, "name", { value: "receiveCharacterRewards" });


        Object.defineProperty(Lobby.prototype.feedActivityFeed = function feedActivityFeed(request, callback) {
            return this.rpcCall(feedActivityFeed, $root.lq.ReqFeedActivityFeed, $root.lq.ResFeedActivityFeed, request, callback);
        }, "name", { value: "feedActivityFeed" });


        Object.defineProperty(Lobby.prototype.sendActivityGiftToFriend = function sendActivityGiftToFriend(request, callback) {
            return this.rpcCall(sendActivityGiftToFriend, $root.lq.ReqSendActivityGiftToFriend, $root.lq.ResSendActivityGiftToFriend, request, callback);
        }, "name", { value: "sendActivityGiftToFriend" });


        Object.defineProperty(Lobby.prototype.receiveActivityGift = function receiveActivityGift(request, callback) {
            return this.rpcCall(receiveActivityGift, $root.lq.ReqReceiveActivityGift, $root.lq.ResCommon, request, callback);
        }, "name", { value: "receiveActivityGift" });


        Object.defineProperty(Lobby.prototype.receiveAllActivityGift = function receiveAllActivityGift(request, callback) {
            return this.rpcCall(receiveAllActivityGift, $root.lq.ReqReceiveAllActivityGift, $root.lq.ResReceiveAllActivityGift, request, callback);
        }, "name", { value: "receiveAllActivityGift" });


        Object.defineProperty(Lobby.prototype.fetchFriendGiftActivityData = function fetchFriendGiftActivityData(request, callback) {
            return this.rpcCall(fetchFriendGiftActivityData, $root.lq.ReqFetchFriendGiftActivityData, $root.lq.ResFetchFriendGiftActivityData, request, callback);
        }, "name", { value: "fetchFriendGiftActivityData" });


        Object.defineProperty(Lobby.prototype.openPreChestItem = function openPreChestItem(request, callback) {
            return this.rpcCall(openPreChestItem, $root.lq.ReqOpenPreChestItem, $root.lq.ResOpenPreChestItem, request, callback);
        }, "name", { value: "openPreChestItem" });


        Object.defineProperty(Lobby.prototype.fetchVoteActivity = function fetchVoteActivity(request, callback) {
            return this.rpcCall(fetchVoteActivity, $root.lq.ReqFetchVoteActivity, $root.lq.ResFetchVoteActivity, request, callback);
        }, "name", { value: "fetchVoteActivity" });


        Object.defineProperty(Lobby.prototype.voteActivity = function voteActivity(request, callback) {
            return this.rpcCall(voteActivity, $root.lq.ReqVoteActivity, $root.lq.ResVoteActivity, request, callback);
        }, "name", { value: "voteActivity" });


        Object.defineProperty(Lobby.prototype.unlockActivitySpot = function unlockActivitySpot(request, callback) {
            return this.rpcCall(unlockActivitySpot, $root.lq.ReqUnlockActivitySpot, $root.lq.ResCommon, request, callback);
        }, "name", { value: "unlockActivitySpot" });


        Object.defineProperty(Lobby.prototype.unlockActivitySpotEnding = function unlockActivitySpotEnding(request, callback) {
            return this.rpcCall(unlockActivitySpotEnding, $root.lq.ReqUnlockActivitySpotEnding, $root.lq.ResCommon, request, callback);
        }, "name", { value: "unlockActivitySpotEnding" });


        Object.defineProperty(Lobby.prototype.receiveActivitySpotReward = function receiveActivitySpotReward(request, callback) {
            return this.rpcCall(receiveActivitySpotReward, $root.lq.ReqReceiveActivitySpotReward, $root.lq.ResReceiveActivitySpotReward, request, callback);
        }, "name", { value: "receiveActivitySpotReward" });


        Object.defineProperty(Lobby.prototype.deleteAccount = function deleteAccount(request, callback) {
            return this.rpcCall(deleteAccount, $root.lq.ReqCommon, $root.lq.ResDeleteAccount, request, callback);
        }, "name", { value: "deleteAccount" });


        Object.defineProperty(Lobby.prototype.cancelDeleteAccount = function cancelDeleteAccount(request, callback) {
            return this.rpcCall(cancelDeleteAccount, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "cancelDeleteAccount" });


        Object.defineProperty(Lobby.prototype.logReport = function logReport(request, callback) {
            return this.rpcCall(logReport, $root.lq.ReqLogReport, $root.lq.ResCommon, request, callback);
        }, "name", { value: "logReport" });


        Object.defineProperty(Lobby.prototype.bindOauth2 = function bindOauth2(request, callback) {
            return this.rpcCall(bindOauth2, $root.lq.ReqBindOauth2, $root.lq.ResCommon, request, callback);
        }, "name", { value: "bindOauth2" });


        Object.defineProperty(Lobby.prototype.fetchOauth2Info = function fetchOauth2Info(request, callback) {
            return this.rpcCall(fetchOauth2Info, $root.lq.ReqFetchOauth2, $root.lq.ResFetchOauth2, request, callback);
        }, "name", { value: "fetchOauth2Info" });


        Object.defineProperty(Lobby.prototype.setLoadingImage = function setLoadingImage(request, callback) {
            return this.rpcCall(setLoadingImage, $root.lq.ReqSetLoadingImage, $root.lq.ResCommon, request, callback);
        }, "name", { value: "setLoadingImage" });


        Object.defineProperty(Lobby.prototype.fetchShopInterval = function fetchShopInterval(request, callback) {
            return this.rpcCall(fetchShopInterval, $root.lq.ReqCommon, $root.lq.ResFetchShopInterval, request, callback);
        }, "name", { value: "fetchShopInterval" });


        Object.defineProperty(Lobby.prototype.fetchActivityInterval = function fetchActivityInterval(request, callback) {
            return this.rpcCall(fetchActivityInterval, $root.lq.ReqCommon, $root.lq.ResFetchActivityInterval, request, callback);
        }, "name", { value: "fetchActivityInterval" });


        Object.defineProperty(Lobby.prototype.fetchRecentFriend = function fetchRecentFriend(request, callback) {
            return this.rpcCall(fetchRecentFriend, $root.lq.ReqCommon, $root.lq.ResFetchrecentFriend, request, callback);
        }, "name", { value: "fetchRecentFriend" });


        Object.defineProperty(Lobby.prototype.openGacha = function openGacha(request, callback) {
            return this.rpcCall(openGacha, $root.lq.ReqOpenGacha, $root.lq.ResOpenGacha, request, callback);
        }, "name", { value: "openGacha" });


        Object.defineProperty(Lobby.prototype.taskRequest = function taskRequest(request, callback) {
            return this.rpcCall(taskRequest, $root.lq.ReqTaskRequest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "taskRequest" });


        Object.defineProperty(Lobby.prototype.yostarDeleteAccount = function yostarDeleteAccount(request, callback) {
            return this.rpcCall(yostarDeleteAccount, $root.lq.ReqYostarDeleteAccount, $root.lq.ResYostarDeleteAccount, request, callback);
        }, "name", { value: "yostarDeleteAccount" });


        Object.defineProperty(Lobby.prototype.simulationActivityTrain = function simulationActivityTrain(request, callback) {
            return this.rpcCall(simulationActivityTrain, $root.lq.ReqSimulationActivityTrain, $root.lq.ResSimulationActivityTrain, request, callback);
        }, "name", { value: "simulationActivityTrain" });


        Object.defineProperty(Lobby.prototype.fetchSimulationGameRecord = function fetchSimulationGameRecord(request, callback) {
            return this.rpcCall(fetchSimulationGameRecord, $root.lq.ReqFetchSimulationGameRecord, $root.lq.ResFetchSimulationGameRecord, request, callback);
        }, "name", { value: "fetchSimulationGameRecord" });


        Object.defineProperty(Lobby.prototype.startSimulationActivityGame = function startSimulationActivityGame(request, callback) {
            return this.rpcCall(startSimulationActivityGame, $root.lq.ReqStartSimulationActivityGame, $root.lq.ResStartSimulationActivityGame, request, callback);
        }, "name", { value: "startSimulationActivityGame" });


        Object.defineProperty(Lobby.prototype.fetchSimulationGameRank = function fetchSimulationGameRank(request, callback) {
            return this.rpcCall(fetchSimulationGameRank, $root.lq.ReqFetchSimulationGameRank, $root.lq.ResFetchSimulationGameRank, request, callback);
        }, "name", { value: "fetchSimulationGameRank" });


        Object.defineProperty(Lobby.prototype.generateCombiningCraft = function generateCombiningCraft(request, callback) {
            return this.rpcCall(generateCombiningCraft, $root.lq.ReqGenerateCombiningCraft, $root.lq.ResGenerateCombiningCraft, request, callback);
        }, "name", { value: "generateCombiningCraft" });


        Object.defineProperty(Lobby.prototype.moveCombiningCraft = function moveCombiningCraft(request, callback) {
            return this.rpcCall(moveCombiningCraft, $root.lq.ReqMoveCombiningCraft, $root.lq.ResMoveCombiningCraft, request, callback);
        }, "name", { value: "moveCombiningCraft" });


        Object.defineProperty(Lobby.prototype.combiningRecycleCraft = function combiningRecycleCraft(request, callback) {
            return this.rpcCall(combiningRecycleCraft, $root.lq.ReqCombiningRecycleCraft, $root.lq.ResCombiningRecycleCraft, request, callback);
        }, "name", { value: "combiningRecycleCraft" });


        Object.defineProperty(Lobby.prototype.recoverCombiningRecycle = function recoverCombiningRecycle(request, callback) {
            return this.rpcCall(recoverCombiningRecycle, $root.lq.ReqRecoverCombiningRecycle, $root.lq.ResRecoverCombiningRecycle, request, callback);
        }, "name", { value: "recoverCombiningRecycle" });


        Object.defineProperty(Lobby.prototype.finishCombiningOrder = function finishCombiningOrder(request, callback) {
            return this.rpcCall(finishCombiningOrder, $root.lq.ReqFinishCombiningOrder, $root.lq.ResFinishCombiningOrder, request, callback);
        }, "name", { value: "finishCombiningOrder" });


        Object.defineProperty(Lobby.prototype.upgradeVillageBuilding = function upgradeVillageBuilding(request, callback) {
            return this.rpcCall(upgradeVillageBuilding, $root.lq.ReqUpgradeVillageBuilding, $root.lq.ResCommon, request, callback);
        }, "name", { value: "upgradeVillageBuilding" });


        Object.defineProperty(Lobby.prototype.receiveVillageBuildingReward = function receiveVillageBuildingReward(request, callback) {
            return this.rpcCall(receiveVillageBuildingReward, $root.lq.ReqReceiveVillageBuildingReward, $root.lq.ResReceiveVillageBuildingReward, request, callback);
        }, "name", { value: "receiveVillageBuildingReward" });


        Object.defineProperty(Lobby.prototype.startVillageTrip = function startVillageTrip(request, callback) {
            return this.rpcCall(startVillageTrip, $root.lq.ReqStartVillageTrip, $root.lq.ResCommon, request, callback);
        }, "name", { value: "startVillageTrip" });


        Object.defineProperty(Lobby.prototype.receiveVillageTripReward = function receiveVillageTripReward(request, callback) {
            return this.rpcCall(receiveVillageTripReward, $root.lq.ReqReceiveVillageTripReward, $root.lq.ResReceiveVillageTripReward, request, callback);
        }, "name", { value: "receiveVillageTripReward" });


        Object.defineProperty(Lobby.prototype.completeVillageTask = function completeVillageTask(request, callback) {
            return this.rpcCall(completeVillageTask, $root.lq.ReqCompleteVillageTask, $root.lq.ResCompleteVillageTask, request, callback);
        }, "name", { value: "completeVillageTask" });


        Object.defineProperty(Lobby.prototype.getFriendVillageData = function getFriendVillageData(request, callback) {
            return this.rpcCall(getFriendVillageData, $root.lq.ReqGetFriendVillageData, $root.lq.ResGetFriendVillageData, request, callback);
        }, "name", { value: "getFriendVillageData" });


        Object.defineProperty(Lobby.prototype.setVillageWorker = function setVillageWorker(request, callback) {
            return this.rpcCall(setVillageWorker, $root.lq.ReqSetVillageWorker, $root.lq.ResSetVillageWorker, request, callback);
        }, "name", { value: "setVillageWorker" });


        Object.defineProperty(Lobby.prototype.nextRoundVillage = function nextRoundVillage(request, callback) {
            return this.rpcCall(nextRoundVillage, $root.lq.ReqNextRoundVillage, $root.lq.ResNextRoundVillage, request, callback);
        }, "name", { value: "nextRoundVillage" });


        Object.defineProperty(Lobby.prototype.shootActivityAttackEnemies = function shootActivityAttackEnemies(request, callback) {
            return this.rpcCall(shootActivityAttackEnemies, $root.lq.ReqShootActivityAttackEnemies, $root.lq.ResShootActivityAttackEnemies, request, callback);
        }, "name", { value: "shootActivityAttackEnemies" });


        Object.defineProperty(Lobby.prototype.resolveFestivalActivityProposal = function resolveFestivalActivityProposal(request, callback) {
            return this.rpcCall(resolveFestivalActivityProposal, $root.lq.ReqResolveFestivalActivityProposal, $root.lq.ResResolveFestivalActivityProposal, request, callback);
        }, "name", { value: "resolveFestivalActivityProposal" });


        Object.defineProperty(Lobby.prototype.resolveFestivalActivityEvent = function resolveFestivalActivityEvent(request, callback) {
            return this.rpcCall(resolveFestivalActivityEvent, $root.lq.ReqResolveFestivalActivityEvent, $root.lq.ResResolveFestivalActivityEvent, request, callback);
        }, "name", { value: "resolveFestivalActivityEvent" });


        Object.defineProperty(Lobby.prototype.buyFestivalProposal = function buyFestivalProposal(request, callback) {
            return this.rpcCall(buyFestivalProposal, $root.lq.ReqBuyFestivalProposal, $root.lq.ResBuyFestivalProposal, request, callback);
        }, "name", { value: "buyFestivalProposal" });


        Object.defineProperty(Lobby.prototype.islandActivityMove = function islandActivityMove(request, callback) {
            return this.rpcCall(islandActivityMove, $root.lq.ReqIslandActivityMove, $root.lq.ResCommon, request, callback);
        }, "name", { value: "islandActivityMove" });


        Object.defineProperty(Lobby.prototype.islandActivityBuy = function islandActivityBuy(request, callback) {
            return this.rpcCall(islandActivityBuy, $root.lq.ReqIslandActivityBuy, $root.lq.ResCommon, request, callback);
        }, "name", { value: "islandActivityBuy" });


        Object.defineProperty(Lobby.prototype.islandActivitySell = function islandActivitySell(request, callback) {
            return this.rpcCall(islandActivitySell, $root.lq.ReqIslandActivitySell, $root.lq.ResCommon, request, callback);
        }, "name", { value: "islandActivitySell" });


        Object.defineProperty(Lobby.prototype.islandActivityTidyBag = function islandActivityTidyBag(request, callback) {
            return this.rpcCall(islandActivityTidyBag, $root.lq.ReqIslandActivityTidyBag, $root.lq.ResCommon, request, callback);
        }, "name", { value: "islandActivityTidyBag" });


        Object.defineProperty(Lobby.prototype.islandActivityUnlockBagGrid = function islandActivityUnlockBagGrid(request, callback) {
            return this.rpcCall(islandActivityUnlockBagGrid, $root.lq.ReqIslandActivityUnlockBagGrid, $root.lq.ResCommon, request, callback);
        }, "name", { value: "islandActivityUnlockBagGrid" });


        Object.defineProperty(Lobby.prototype.createCustomizedContest = function createCustomizedContest(request, callback) {
            return this.rpcCall(createCustomizedContest, $root.lq.ReqCreateCustomizedContest, $root.lq.ResCreateCustomizedContest, request, callback);
        }, "name", { value: "createCustomizedContest" });


        Object.defineProperty(Lobby.prototype.fetchManagerCustomizedContestList = function fetchManagerCustomizedContestList(request, callback) {
            return this.rpcCall(fetchManagerCustomizedContestList, $root.lq.ReqFetchmanagerCustomizedContestList, $root.lq.ResFetchManagerCustomizedContestList, request, callback);
        }, "name", { value: "fetchManagerCustomizedContestList" });


        Object.defineProperty(Lobby.prototype.fetchManagerCustomizedContest = function fetchManagerCustomizedContest(request, callback) {
            return this.rpcCall(fetchManagerCustomizedContest, $root.lq.ReqFetchManagerCustomizedContest, $root.lq.ResFetchManagerCustomizedContest, request, callback);
        }, "name", { value: "fetchManagerCustomizedContest" });


        Object.defineProperty(Lobby.prototype.updateManagerCustomizedContest = function updateManagerCustomizedContest(request, callback) {
            return this.rpcCall(updateManagerCustomizedContest, $root.lq.ReqUpdateManagerCustomizedContest, $root.lq.ResCommon, request, callback);
        }, "name", { value: "updateManagerCustomizedContest" });


        Object.defineProperty(Lobby.prototype.fetchReadyPlayerList = function fetchReadyPlayerList(request, callback) {
            return this.rpcCall(fetchReadyPlayerList, $root.lq.ReqFetchReadyPlayerList, $root.lq.ResFetchReadyPlayerList, request, callback);
        }, "name", { value: "fetchReadyPlayerList" });


        Object.defineProperty(Lobby.prototype.createGamePlan = function createGamePlan(request, callback) {
            return this.rpcCall(createGamePlan, $root.lq.ReqCreateGamePlan, $root.lq.ResCommon, request, callback);
        }, "name", { value: "createGamePlan" });


        Object.defineProperty(Lobby.prototype.generateContestManagerLoginCode = function generateContestManagerLoginCode(request, callback) {
            return this.rpcCall(generateContestManagerLoginCode, $root.lq.ReqCommon, $root.lq.ResGenerateContestManagerLoginCode, request, callback);
        }, "name", { value: "generateContestManagerLoginCode" });


        Object.defineProperty(Lobby.prototype.fetchAmuletActivityData = function fetchAmuletActivityData(request, callback) {
            return this.rpcCall(fetchAmuletActivityData, $root.lq.ReqFetchAmuletActivityData, $root.lq.ResFetchAmuletActivityData, request, callback);
        }, "name", { value: "fetchAmuletActivityData" });


        Object.defineProperty(Lobby.prototype.amuletActivityFetchBrief = function amuletActivityFetchBrief(request, callback) {
            return this.rpcCall(amuletActivityFetchBrief, $root.lq.ReqAmuletActivityFetchBrief, $root.lq.ResAmuletActivityFetchBrief, request, callback);
        }, "name", { value: "amuletActivityFetchBrief" });


        Object.defineProperty(Lobby.prototype.amuletActivityStartGame = function amuletActivityStartGame(request, callback) {
            return this.rpcCall(amuletActivityStartGame, $root.lq.ReqAmuletActivityStartGame, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityStartGame" });


        Object.defineProperty(Lobby.prototype.amuletActivityOperate = function amuletActivityOperate(request, callback) {
            return this.rpcCall(amuletActivityOperate, $root.lq.ReqAmuletActivityOperate, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityOperate" });


        Object.defineProperty(Lobby.prototype.amuletActivityUpgrade = function amuletActivityUpgrade(request, callback) {
            return this.rpcCall(amuletActivityUpgrade, $root.lq.ReqAmuletActivityUpgrade, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityUpgrade" });


        Object.defineProperty(Lobby.prototype.amuletActivityBuy = function amuletActivityBuy(request, callback) {
            return this.rpcCall(amuletActivityBuy, $root.lq.ReqAmuletActivityBuy, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityBuy" });


        Object.defineProperty(Lobby.prototype.amuletActivitySelectPack = function amuletActivitySelectPack(request, callback) {
            return this.rpcCall(amuletActivitySelectPack, $root.lq.ReqAmuletActivitySelectPack, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivitySelectPack" });


        Object.defineProperty(Lobby.prototype.amuletActivitySellEffect = function amuletActivitySellEffect(request, callback) {
            return this.rpcCall(amuletActivitySellEffect, $root.lq.ReqAmuletActivitySellEffect, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivitySellEffect" });


        Object.defineProperty(Lobby.prototype.amuletActivityEffectSort = function amuletActivityEffectSort(request, callback) {
            return this.rpcCall(amuletActivityEffectSort, $root.lq.ReqAmuletActivityEffectSort, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityEffectSort" });


        Object.defineProperty(Lobby.prototype.amuletActivityGiveup = function amuletActivityGiveup(request, callback) {
            return this.rpcCall(amuletActivityGiveup, $root.lq.ReqAmuletActivityGiveup, $root.lq.ResCommon, request, callback);
        }, "name", { value: "amuletActivityGiveup" });


        Object.defineProperty(Lobby.prototype.amuletActivityRefreshShop = function amuletActivityRefreshShop(request, callback) {
            return this.rpcCall(amuletActivityRefreshShop, $root.lq.ReqAmuletActivityRefreshShop, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityRefreshShop" });


        Object.defineProperty(Lobby.prototype.amuletActivitySelectFreeEffect = function amuletActivitySelectFreeEffect(request, callback) {
            return this.rpcCall(amuletActivitySelectFreeEffect, $root.lq.ReqAmuletActivitySelectFreeEffect, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivitySelectFreeEffect" });


        Object.defineProperty(Lobby.prototype.amuletActivityUpgradeShopBuff = function amuletActivityUpgradeShopBuff(request, callback) {
            return this.rpcCall(amuletActivityUpgradeShopBuff, $root.lq.ReqAmuletActivityUpgradeShopBuff, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityUpgradeShopBuff" });


        Object.defineProperty(Lobby.prototype.amuletActivityEndShopping = function amuletActivityEndShopping(request, callback) {
            return this.rpcCall(amuletActivityEndShopping, $root.lq.ReqAmuletActivityEndShopping, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivityEndShopping" });


        Object.defineProperty(Lobby.prototype.amuletActivitySetSkillLevel = function amuletActivitySetSkillLevel(request, callback) {
            return this.rpcCall(amuletActivitySetSkillLevel, $root.lq.ReqAmuletActivitySetSkillLevel, $root.lq.ResCommon, request, callback);
        }, "name", { value: "amuletActivitySetSkillLevel" });


        Object.defineProperty(Lobby.prototype.amuletActivityMaintainInfo = function amuletActivityMaintainInfo(request, callback) {
            return this.rpcCall(amuletActivityMaintainInfo, $root.lq.ReqCommon, $root.lq.ResAmuletActivityMaintainInfo, request, callback);
        }, "name", { value: "amuletActivityMaintainInfo" });


        Object.defineProperty(Lobby.prototype.amuletActivitySelectRewardPack = function amuletActivitySelectRewardPack(request, callback) {
            return this.rpcCall(amuletActivitySelectRewardPack, $root.lq.ReqAmuletActivitySelectRewardPack, $root.lq.ResAmuletEventResponse, request, callback);
        }, "name", { value: "amuletActivitySelectRewardPack" });


        Object.defineProperty(Lobby.prototype.amuletActivitySelectBookEffect = function amuletActivitySelectBookEffect(request, callback) {
            return this.rpcCall(amuletActivitySelectBookEffect, $root.lq.ReqAmuletActivitySelectBookEffect, $root.lq.ResCommon, request, callback);
        }, "name", { value: "amuletActivitySelectBookEffect" });


        Object.defineProperty(Lobby.prototype.storyActivityUnlock = function storyActivityUnlock(request, callback) {
            return this.rpcCall(storyActivityUnlock, $root.lq.ReqStoryActivityUnlock, $root.lq.ResCommon, request, callback);
        }, "name", { value: "storyActivityUnlock" });


        Object.defineProperty(Lobby.prototype.storyActivityUnlockEnding = function storyActivityUnlockEnding(request, callback) {
            return this.rpcCall(storyActivityUnlockEnding, $root.lq.ReqStoryActivityUnlockEnding, $root.lq.ResCommon, request, callback);
        }, "name", { value: "storyActivityUnlockEnding" });


        Object.defineProperty(Lobby.prototype.storyActivityReceiveEndingReward = function storyActivityReceiveEndingReward(request, callback) {
            return this.rpcCall(storyActivityReceiveEndingReward, $root.lq.ReqStoryActivityReceiveEndingReward, $root.lq.ResStoryReward, request, callback);
        }, "name", { value: "storyActivityReceiveEndingReward" });


        Object.defineProperty(Lobby.prototype.storyActivityReceiveFinishReward = function storyActivityReceiveFinishReward(request, callback) {
            return this.rpcCall(storyActivityReceiveFinishReward, $root.lq.ReqStoryActivityReceiveFinishReward, $root.lq.ResStoryReward, request, callback);
        }, "name", { value: "storyActivityReceiveFinishReward" });


        Object.defineProperty(Lobby.prototype.storyActivityReceiveAllFinishReward = function storyActivityReceiveAllFinishReward(request, callback) {
            return this.rpcCall(storyActivityReceiveAllFinishReward, $root.lq.ReqStoryActivityReceiveAllFinishReward, $root.lq.ResStoryReward, request, callback);
        }, "name", { value: "storyActivityReceiveAllFinishReward" });


        Object.defineProperty(Lobby.prototype.storyActivityUnlockEndingAndReceive = function storyActivityUnlockEndingAndReceive(request, callback) {
            return this.rpcCall(storyActivityUnlockEndingAndReceive, $root.lq.ReqStoryActivityUnlockEndingAndReceive, $root.lq.ResStoryActivityUnlockEndingAndReceive, request, callback);
        }, "name", { value: "storyActivityUnlockEndingAndReceive" });


        Object.defineProperty(Lobby.prototype.fetchActivityRank = function fetchActivityRank(request, callback) {
            return this.rpcCall(fetchActivityRank, $root.lq.ReqFetchActivityRank, $root.lq.ResFetchActivityRank, request, callback);
        }, "name", { value: "fetchActivityRank" });


        Object.defineProperty(Lobby.prototype.setVerifiedHidden = function setVerifiedHidden(request, callback) {
            return this.rpcCall(setVerifiedHidden, $root.lq.ReqSetVerifiedHidden, $root.lq.ResCommon, request, callback);
        }, "name", { value: "setVerifiedHidden" });


        Object.defineProperty(Lobby.prototype.fetchQuestionnaireList = function fetchQuestionnaireList(request, callback) {
            return this.rpcCall(fetchQuestionnaireList, $root.lq.ReqFetchQuestionnaireList, $root.lq.ResFetchQuestionnaireList, request, callback);
        }, "name", { value: "fetchQuestionnaireList" });


        Object.defineProperty(Lobby.prototype.fetchQuestionnaireDetail = function fetchQuestionnaireDetail(request, callback) {
            return this.rpcCall(fetchQuestionnaireDetail, $root.lq.ReqFetchQuestionnaireDetail, $root.lq.ResFetchQuestionnaireDetail, request, callback);
        }, "name", { value: "fetchQuestionnaireDetail" });


        Object.defineProperty(Lobby.prototype.submitQuestionnaire = function submitQuestionnaire(request, callback) {
            return this.rpcCall(submitQuestionnaire, $root.lq.ReqSubmitQuestionnaire, $root.lq.ResCommon, request, callback);
        }, "name", { value: "submitQuestionnaire" });


        Object.defineProperty(Lobby.prototype.setFriendRoomRandomBotChar = function setFriendRoomRandomBotChar(request, callback) {
            return this.rpcCall(setFriendRoomRandomBotChar, $root.lq.ReqSetFriendRoomRandomBotChar, $root.lq.ResCommon, request, callback);
        }, "name", { value: "setFriendRoomRandomBotChar" });


        Object.defineProperty(Lobby.prototype.fetchAccountGameHuRecords = function fetchAccountGameHuRecords(request, callback) {
            return this.rpcCall(fetchAccountGameHuRecords, $root.lq.ReqFetchAccountGameHuRecords, $root.lq.ResFetchAccountGameHuRecords, request, callback);
        }, "name", { value: "fetchAccountGameHuRecords" });


        Object.defineProperty(Lobby.prototype.fetchAccountInfoExtra = function fetchAccountInfoExtra(request, callback) {
            return this.rpcCall(fetchAccountInfoExtra, $root.lq.ReqFetchAccountInfoExtra, $root.lq.ResFetchAccountInfoExtra, request, callback);
        }, "name", { value: "fetchAccountInfoExtra" });


        Object.defineProperty(Lobby.prototype.setAccountFavoriteHu = function setAccountFavoriteHu(request, callback) {
            return this.rpcCall(setAccountFavoriteHu, $root.lq.ReqSetAccountFavoriteHu, $root.lq.ResCommon, request, callback);
        }, "name", { value: "setAccountFavoriteHu" });


        Object.defineProperty(Lobby.prototype.fetchSeerReport = function fetchSeerReport(request, callback) {
            return this.rpcCall(fetchSeerReport, $root.lq.ReqFetchSeerReport, $root.lq.ResFetchSeerReport, request, callback);
        }, "name", { value: "fetchSeerReport" });


        Object.defineProperty(Lobby.prototype.createSeerReport = function createSeerReport(request, callback) {
            return this.rpcCall(createSeerReport, $root.lq.ReqCreateSeerReport, $root.lq.ResCreateSeerReport, request, callback);
        }, "name", { value: "createSeerReport" });


        Object.defineProperty(Lobby.prototype.fetchSeerReportList = function fetchSeerReportList(request, callback) {
            return this.rpcCall(fetchSeerReportList, $root.lq.ReqCommon, $root.lq.ResFetchSeerReportList, request, callback);
        }, "name", { value: "fetchSeerReportList" });


        Object.defineProperty(Lobby.prototype.fetchSeerInfo = function fetchSeerInfo(request, callback) {
            return this.rpcCall(fetchSeerInfo, $root.lq.ReqCommon, $root.lq.ResFetchSeerInfo, request, callback);
        }, "name", { value: "fetchSeerInfo" });


        Object.defineProperty(Lobby.prototype.selectChestChooseUpActivity = function selectChestChooseUpActivity(request, callback) {
            return this.rpcCall(selectChestChooseUpActivity, $root.lq.ReqSelectChestChooseUp, $root.lq.ResCommon, request, callback);
        }, "name", { value: "selectChestChooseUpActivity" });


        Object.defineProperty(Lobby.prototype.selectChestChooseGroupActivity = function selectChestChooseGroupActivity(request, callback) {
            return this.rpcCall(selectChestChooseGroupActivity, $root.lq.ReqSelectChestChooseGroupActivity, $root.lq.ResCommon, request, callback);
        }, "name", { value: "selectChestChooseGroupActivity" });


        Object.defineProperty(Lobby.prototype.generateAnnualReportToken = function generateAnnualReportToken(request, callback) {
            return this.rpcCall(generateAnnualReportToken, $root.lq.ReqGenerateAnnualReportToken, $root.lq.ResGenerateAnnualReportToken, request, callback);
        }, "name", { value: "generateAnnualReportToken" });


        Object.defineProperty(Lobby.prototype.fetchAnnualReportInfo = function fetchAnnualReportInfo(request, callback) {
            return this.rpcCall(fetchAnnualReportInfo, $root.lq.ReqCommon, $root.lq.ResFetchAnnualReportInfo, request, callback);
        }, "name", { value: "fetchAnnualReportInfo" });


        Object.defineProperty(Lobby.prototype.remarkFriend = function remarkFriend(request, callback) {
            return this.rpcCall(remarkFriend, $root.lq.ReqRemarkFriend, $root.lq.ResCommon, request, callback);
        }, "name", { value: "remarkFriend" });


        Object.defineProperty(Lobby.prototype.simV2ActivityFetchInfo = function simV2ActivityFetchInfo(request, callback) {
            return this.rpcCall(simV2ActivityFetchInfo, $root.lq.ReqSimV2ActivityFetchInfo, $root.lq.ResSimV2ActivityFetchInfo, request, callback);
        }, "name", { value: "simV2ActivityFetchInfo" });


        Object.defineProperty(Lobby.prototype.simV2ActivityStartSeason = function simV2ActivityStartSeason(request, callback) {
            return this.rpcCall(simV2ActivityStartSeason, $root.lq.ReqSimV2ActivityStartSeason, $root.lq.ResSimV2ActivityStartSeason, request, callback);
        }, "name", { value: "simV2ActivityStartSeason" });


        Object.defineProperty(Lobby.prototype.simV2ActivityTrain = function simV2ActivityTrain(request, callback) {
            return this.rpcCall(simV2ActivityTrain, $root.lq.ReqSimV2ActivityTrain, $root.lq.ResSimV2ActivityTrain, request, callback);
        }, "name", { value: "simV2ActivityTrain" });


        Object.defineProperty(Lobby.prototype.simV2ActivitySelectEvent = function simV2ActivitySelectEvent(request, callback) {
            return this.rpcCall(simV2ActivitySelectEvent, $root.lq.ReqSimV2ActivitySelectEvent, $root.lq.ResSimV2ActivitySelectEvent, request, callback);
        }, "name", { value: "simV2ActivitySelectEvent" });


        Object.defineProperty(Lobby.prototype.simV2ActivityStartMatch = function simV2ActivityStartMatch(request, callback) {
            return this.rpcCall(simV2ActivityStartMatch, $root.lq.ReqSimV2ActivityStartMatch, $root.lq.ResSimV2ActivityStartMatch, request, callback);
        }, "name", { value: "simV2ActivityStartMatch" });


        Object.defineProperty(Lobby.prototype.simV2ActivityEndMatch = function simV2ActivityEndMatch(request, callback) {
            return this.rpcCall(simV2ActivityEndMatch, $root.lq.ReqSimV2ActivityEndMatch, $root.lq.ResSimV2ActivityEndMatch, request, callback);
        }, "name", { value: "simV2ActivityEndMatch" });


        Object.defineProperty(Lobby.prototype.simV2ActivityGiveUp = function simV2ActivityGiveUp(request, callback) {
            return this.rpcCall(simV2ActivityGiveUp, $root.lq.ReqSimV2ActivityGiveUp, $root.lq.ResCommon, request, callback);
        }, "name", { value: "simV2ActivityGiveUp" });


        Object.defineProperty(Lobby.prototype.simV2ActivitySetUpgrade = function simV2ActivitySetUpgrade(request, callback) {
            return this.rpcCall(simV2ActivitySetUpgrade, $root.lq.ReqSimV2ActivitySetUpgrade, $root.lq.ResCommon, request, callback);
        }, "name", { value: "simV2ActivitySetUpgrade" });


        Object.defineProperty(Lobby.prototype.progressRewardActivityReceive = function progressRewardActivityReceive(request, callback) {
            return this.rpcCall(progressRewardActivityReceive, $root.lq.ReqProgressRewardActivityReceive, $root.lq.ResProgressRewardActivityReceive, request, callback);
        }, "name", { value: "progressRewardActivityReceive" });


        Object.defineProperty(Lobby.prototype.fetchProgressRewardActivityInfo = function fetchProgressRewardActivityInfo(request, callback) {
            return this.rpcCall(fetchProgressRewardActivityInfo, $root.lq.ReqFetchProgressRewardActivityInfo, $root.lq.ResFetchProgressRewardActivityInfo, request, callback);
        }, "name", { value: "fetchProgressRewardActivityInfo" });


        Object.defineProperty(Lobby.prototype.questCrewActivityStartQuest = function questCrewActivityStartQuest(request, callback) {
            return this.rpcCall(questCrewActivityStartQuest, $root.lq.ReqQuestCrewActivityStartQuest, $root.lq.ResQuestCrewActivityStartQuest, request, callback);
        }, "name", { value: "questCrewActivityStartQuest" });


        Object.defineProperty(Lobby.prototype.questCrewActivityHire = function questCrewActivityHire(request, callback) {
            return this.rpcCall(questCrewActivityHire, $root.lq.ReqQuestCrewActivityHire, $root.lq.ResQuestCrewActivityHire, request, callback);
        }, "name", { value: "questCrewActivityHire" });


        Object.defineProperty(Lobby.prototype.questCrewActivityFeed = function questCrewActivityFeed(request, callback) {
            return this.rpcCall(questCrewActivityFeed, $root.lq.ReqQuestCrewActivityFeed, $root.lq.ResQuestCrewActivityFeed, request, callback);
        }, "name", { value: "questCrewActivityFeed" });


        Object.defineProperty(Lobby.prototype.questCrewActivityRefreshMarket = function questCrewActivityRefreshMarket(request, callback) {
            return this.rpcCall(questCrewActivityRefreshMarket, $root.lq.ReqQuestCrewActivityRefreshMarket, $root.lq.ResQuestCrewActivityRefreshMarket, request, callback);
        }, "name", { value: "questCrewActivityRefreshMarket" });


        Object.defineProperty(Lobby.prototype.bingoActivityReceiveReward = function bingoActivityReceiveReward(request, callback) {
            return this.rpcCall(bingoActivityReceiveReward, $root.lq.ReqBingoActivityReceiveReward, $root.lq.ResBingoActivityReceiveReward, request, callback);
        }, "name", { value: "bingoActivityReceiveReward" });


        Object.defineProperty(Lobby.prototype.fetchBingoActivityData = function fetchBingoActivityData(request, callback) {
            return this.rpcCall(fetchBingoActivityData, $root.lq.ReqFetchBingoActivityData, $root.lq.ResFetchBingoActivityData, request, callback);
        }, "name", { value: "fetchBingoActivityData" });


        Object.defineProperty(Lobby.prototype.snowballActivityStartBattle = function snowballActivityStartBattle(request, callback) {
            return this.rpcCall(snowballActivityStartBattle, $root.lq.ReqSnowballActivityStartBattle, $root.lq.ResSnowballActivityStartBattle, request, callback);
        }, "name", { value: "snowballActivityStartBattle" });


        Object.defineProperty(Lobby.prototype.snowballActivityFinishBattle = function snowballActivityFinishBattle(request, callback) {
            return this.rpcCall(snowballActivityFinishBattle, $root.lq.ReqSnowballActivityFinishBattle, $root.lq.ResSnowballActivityFinishBattle, request, callback);
        }, "name", { value: "snowballActivityFinishBattle" });


        Object.defineProperty(Lobby.prototype.snowballActivityUpgrade = function snowballActivityUpgrade(request, callback) {
            return this.rpcCall(snowballActivityUpgrade, $root.lq.ReqSnowballActivityUpgrade, $root.lq.ResSnowballActivityUpgrade, request, callback);
        }, "name", { value: "snowballActivityUpgrade" });


        Object.defineProperty(Lobby.prototype.snowballActivityReceiveReward = function snowballActivityReceiveReward(request, callback) {
            return this.rpcCall(snowballActivityReceiveReward, $root.lq.ReqSnowballActivityReceiveReward, $root.lq.ResSnowballActivityReceiveReward, request, callback);
        }, "name", { value: "snowballActivityReceiveReward" });


        Object.defineProperty(Lobby.prototype.fetchBannerActivityData = function fetchBannerActivityData(request, callback) {
            return this.rpcCall(fetchBannerActivityData, $root.lq.ReqFetchBannerActivityData, $root.lq.ResFetchBannerActivityData, request, callback);
        }, "name", { value: "fetchBannerActivityData" });


        Object.defineProperty(Lobby.prototype.marathonActivityStartRace = function marathonActivityStartRace(request, callback) {
            return this.rpcCall(marathonActivityStartRace, $root.lq.ReqMarathonActivityStartRace, $root.lq.ResMarathonActivityStartRace, request, callback);
        }, "name", { value: "marathonActivityStartRace" });


        Object.defineProperty(Lobby.prototype.marathonActivityFinishRace = function marathonActivityFinishRace(request, callback) {
            return this.rpcCall(marathonActivityFinishRace, $root.lq.ReqMarathonActivityFinishRace, $root.lq.ResMarathonActivityFinishRace, request, callback);
        }, "name", { value: "marathonActivityFinishRace" });


        Object.defineProperty(Lobby.prototype.mmoActivityFetchData = function mmoActivityFetchData(request, callback) {
            return this.rpcCall(mmoActivityFetchData, $root.lq.ReqMMOActivityFetchData, $root.lq.ResMMOActivityFetchData, request, callback);
        }, "name", { value: "mmoActivityFetchData" });


        Object.defineProperty(Lobby.prototype.mmoActivityEquipFusion = function mmoActivityEquipFusion(request, callback) {
            return this.rpcCall(mmoActivityEquipFusion, $root.lq.ReqMMOActivityEquipFusion, $root.lq.ResMMOActivityEquipFusion, request, callback);
        }, "name", { value: "mmoActivityEquipFusion" });


        Object.defineProperty(Lobby.prototype.mmoActivitySetCharacter = function mmoActivitySetCharacter(request, callback) {
            return this.rpcCall(mmoActivitySetCharacter, $root.lq.ReqMMOActivitySetCharacter, $root.lq.ResMMOActivitySetCharacter, request, callback);
        }, "name", { value: "mmoActivitySetCharacter" });


        Object.defineProperty(Lobby.prototype.mmoActivitySetTeamMember = function mmoActivitySetTeamMember(request, callback) {
            return this.rpcCall(mmoActivitySetTeamMember, $root.lq.ReqMMOActivitySetTeamMember, $root.lq.ResMMOActivitySetTeamMember, request, callback);
        }, "name", { value: "mmoActivitySetTeamMember" });


        Object.defineProperty(Lobby.prototype.mmoActivityStartBattle = function mmoActivityStartBattle(request, callback) {
            return this.rpcCall(mmoActivityStartBattle, $root.lq.ReqMMOActivityStartBattle, $root.lq.ResMMOActivityStartBattle, request, callback);
        }, "name", { value: "mmoActivityStartBattle" });


        Object.defineProperty(Lobby.prototype.mmoActivityFinishBattle = function mmoActivityFinishBattle(request, callback) {
            return this.rpcCall(mmoActivityFinishBattle, $root.lq.ReqMMOActivityFinishBattle, $root.lq.ResMMOActivityFinishBattle, request, callback);
        }, "name", { value: "mmoActivityFinishBattle" });


        Object.defineProperty(Lobby.prototype.mmoActivitySetEquip = function mmoActivitySetEquip(request, callback) {
            return this.rpcCall(mmoActivitySetEquip, $root.lq.ReqMMOActivitySetEquip, $root.lq.ResMMOActivitySetEquip, request, callback);
        }, "name", { value: "mmoActivitySetEquip" });


        Object.defineProperty(Lobby.prototype.mmoActivityUpdateFriendList = function mmoActivityUpdateFriendList(request, callback) {
            return this.rpcCall(mmoActivityUpdateFriendList, $root.lq.ReqMMOActivityUpdatehFriendList, $root.lq.ResMMOActivityUpdatehFriendList, request, callback);
        }, "name", { value: "mmoActivityUpdateFriendList" });


        Object.defineProperty(Lobby.prototype.mmoActivityReceiveSupportReward = function mmoActivityReceiveSupportReward(request, callback) {
            return this.rpcCall(mmoActivityReceiveSupportReward, $root.lq.ReqMMOActivityReceiveSupportReward, $root.lq.ResMMOActivityReceiveSupportReward, request, callback);
        }, "name", { value: "mmoActivityReceiveSupportReward" });

        return Lobby;
    })();

    lq.ResConnectionInfo = (function() {

        function ResConnectionInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResConnectionInfo.prototype.error = null;
        ResConnectionInfo.prototype.client_endpoint = null;

        ResConnectionInfo.create = function create(properties) {
            return new ResConnectionInfo(properties);
        };

        ResConnectionInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.client_endpoint != null && Object.hasOwnProperty.call(message, "client_endpoint"))
                $root.lq.NetworkEndpoint.encode(message.client_endpoint, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResConnectionInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResConnectionInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResConnectionInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.client_endpoint = $root.lq.NetworkEndpoint.decode(reader, reader.uint32(), undefined, _depth + 1, message.client_endpoint);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResConnectionInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResConnectionInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.client_endpoint != null && message.hasOwnProperty("client_endpoint")) {
                let error = $root.lq.NetworkEndpoint.verify(message.client_endpoint, _depth + 1);
                if (error)
                    return "client_endpoint." + error;
            }
            return null;
        };

        ResConnectionInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResConnectionInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResConnectionInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResConnectionInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.client_endpoint != null) {
                if (typeof object.client_endpoint !== "object")
                    throw TypeError(".lq.ResConnectionInfo.client_endpoint: object expected");
                message.client_endpoint = $root.lq.NetworkEndpoint.fromObject(object.client_endpoint, _depth + 1);
            }
            return message;
        };

        ResConnectionInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.client_endpoint = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.client_endpoint != null && message.hasOwnProperty("client_endpoint"))
                object.client_endpoint = $root.lq.NetworkEndpoint.toObject(message.client_endpoint, options);
            return object;
        };

        ResConnectionInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResConnectionInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResConnectionInfo";
        };

        return ResConnectionInfo;
    })();

    lq.ResFetchQueueInfo = (function() {

        function ResFetchQueueInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchQueueInfo.prototype.error = null;
        ResFetchQueueInfo.prototype.remain = 0;
        ResFetchQueueInfo.prototype.rank = 0;

        ResFetchQueueInfo.create = function create(properties) {
            return new ResFetchQueueInfo(properties);
        };

        ResFetchQueueInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.remain != null && Object.hasOwnProperty.call(message, "remain"))
                writer.uint32(16).uint32(message.remain);
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(24).uint32(message.rank);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchQueueInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchQueueInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchQueueInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain = value;
                        else
                            delete message.remain;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank = value;
                        else
                            delete message.rank;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchQueueInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchQueueInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.remain != null && message.hasOwnProperty("remain"))
                if (!$util.isInteger(message.remain))
                    return "remain: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank"))
                if (!$util.isInteger(message.rank))
                    return "rank: integer expected";
            return null;
        };

        ResFetchQueueInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchQueueInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchQueueInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchQueueInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.remain != null)
                if (Number(object.remain) !== 0)
                    message.remain = object.remain >>> 0;
            if (object.rank != null)
                if (Number(object.rank) !== 0)
                    message.rank = object.rank >>> 0;
            return message;
        };

        ResFetchQueueInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.remain = 0;
                object.rank = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.remain != null && message.hasOwnProperty("remain"))
                object.remain = message.remain;
            if (message.rank != null && message.hasOwnProperty("rank"))
                object.rank = message.rank;
            return object;
        };

        ResFetchQueueInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchQueueInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchQueueInfo";
        };

        return ResFetchQueueInfo;
    })();

    lq.ReqOpenidCheck = (function() {

        function ReqOpenidCheck(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenidCheck.prototype.type = 0;
        ReqOpenidCheck.prototype.token = "";

        ReqOpenidCheck.create = function create(properties) {
            return new ReqOpenidCheck(properties);
        };

        ReqOpenidCheck.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenidCheck.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenidCheck.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenidCheck(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenidCheck.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenidCheck.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        ReqOpenidCheck.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenidCheck)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenidCheck();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            return message;
        };

        ReqOpenidCheck.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.token = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        ReqOpenidCheck.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenidCheck.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenidCheck";
        };

        return ReqOpenidCheck;
    })();

    lq.ReqSignupAccount = (function() {

        function ReqSignupAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSignupAccount.prototype.account = "";
        ReqSignupAccount.prototype.password = "";
        ReqSignupAccount.prototype.code = "";
        ReqSignupAccount.prototype.type = 0;
        ReqSignupAccount.prototype.device = null;
        ReqSignupAccount.prototype.client_version_string = "";
        ReqSignupAccount.prototype.tag = "";

        ReqSignupAccount.create = function create(properties) {
            return new ReqSignupAccount(properties);
        };

        ReqSignupAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                writer.uint32(10).string(message.account);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(18).string(message.password);
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(26).string(message.code);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(32).uint32(message.type);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(42).fork()).ldelim();
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(58).string(message.tag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSignupAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSignupAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSignupAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSignupAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSignupAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && message.hasOwnProperty("account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        ReqSignupAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSignupAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSignupAccount();
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = String(object.account);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqSignupAccount.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.tag != null)
                if (typeof object.tag !== "string" || object.tag.length)
                    message.tag = String(object.tag);
            return message;
        };

        ReqSignupAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account = "";
                object.password = "";
                object.code = "";
                object.type = 0;
                object.device = null;
                object.client_version_string = "";
                object.tag = "";
            }
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = message.account;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        ReqSignupAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSignupAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSignupAccount";
        };

        return ReqSignupAccount;
    })();

    lq.ResSignupAccount = (function() {

        function ResSignupAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSignupAccount.prototype.error = null;

        ResSignupAccount.create = function create(properties) {
            return new ResSignupAccount(properties);
        };

        ResSignupAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSignupAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSignupAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSignupAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSignupAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSignupAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResSignupAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSignupAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSignupAccount();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSignupAccount.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResSignupAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResSignupAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSignupAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSignupAccount";
        };

        return ResSignupAccount;
    })();

    lq.ReqLogin = (function() {

        function ReqLogin(properties) {
            this.currency_platforms = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLogin.prototype.account = "";
        ReqLogin.prototype.password = "";
        ReqLogin.prototype.reconnect = false;
        ReqLogin.prototype.device = null;
        ReqLogin.prototype.random_key = "";
        ReqLogin.prototype.client_version = null;
        ReqLogin.prototype.gen_access_token = false;
        ReqLogin.prototype.currency_platforms = $util.emptyArray;
        ReqLogin.prototype.type = 0;
        ReqLogin.prototype.version = 0;
        ReqLogin.prototype.client_version_string = "";
        ReqLogin.prototype.tag = "";

        ReqLogin.create = function create(properties) {
            return new ReqLogin(properties);
        };

        ReqLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                writer.uint32(10).string(message.account);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(18).string(message.password);
            if (message.reconnect != null && Object.hasOwnProperty.call(message, "reconnect"))
                writer.uint32(24).bool(message.reconnect);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(34).fork()).ldelim();
            if (message.random_key != null && Object.hasOwnProperty.call(message, "random_key"))
                writer.uint32(42).string(message.random_key);
            if (message.client_version != null && Object.hasOwnProperty.call(message, "client_version"))
                $root.lq.ClientVersionInfo.encode(message.client_version, writer.uint32(50).fork()).ldelim();
            if (message.gen_access_token != null && Object.hasOwnProperty.call(message, "gen_access_token"))
                writer.uint32(56).bool(message.gen_access_token);
            if (message.currency_platforms != null && message.currency_platforms.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    writer.uint32(message.currency_platforms[i]);
                writer.ldelim();
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(72).uint32(message.type);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(80).uint32(message.version);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(90).string(message.client_version_string);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(98).string(message.tag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reconnect = value;
                        else
                            delete message.reconnect;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.random_key = value;
                        else
                            delete message.random_key;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.client_version = $root.lq.ClientVersionInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.client_version);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gen_access_token = value;
                        else
                            delete message.gen_access_token;
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.currency_platforms && message.currency_platforms.length))
                                message.currency_platforms = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.currency_platforms.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.currency_platforms && message.currency_platforms.length))
                            message.currency_platforms = [];
                        message.currency_platforms.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && message.hasOwnProperty("account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                if (typeof message.reconnect !== "boolean")
                    return "reconnect: boolean expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                if (!$util.isString(message.random_key))
                    return "random_key: string expected";
            if (message.client_version != null && message.hasOwnProperty("client_version")) {
                let error = $root.lq.ClientVersionInfo.verify(message.client_version, _depth + 1);
                if (error)
                    return "client_version." + error;
            }
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                if (typeof message.gen_access_token !== "boolean")
                    return "gen_access_token: boolean expected";
            if (message.currency_platforms != null && message.hasOwnProperty("currency_platforms")) {
                if (!Array.isArray(message.currency_platforms))
                    return "currency_platforms: array expected";
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    if (!$util.isInteger(message.currency_platforms[i]))
                        return "currency_platforms: integer[] expected";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        ReqLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLogin();
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = String(object.account);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            if (object.reconnect != null)
                if (object.reconnect)
                    message.reconnect = Boolean(object.reconnect);
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqLogin.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            if (object.random_key != null)
                if (typeof object.random_key !== "string" || object.random_key.length)
                    message.random_key = String(object.random_key);
            if (object.client_version != null) {
                if (typeof object.client_version !== "object")
                    throw TypeError(".lq.ReqLogin.client_version: object expected");
                message.client_version = $root.lq.ClientVersionInfo.fromObject(object.client_version, _depth + 1);
            }
            if (object.gen_access_token != null)
                if (object.gen_access_token)
                    message.gen_access_token = Boolean(object.gen_access_token);
            if (object.currency_platforms) {
                if (!Array.isArray(object.currency_platforms))
                    throw TypeError(".lq.ReqLogin.currency_platforms: array expected");
                message.currency_platforms = Array(object.currency_platforms.length);
                for (let i = 0; i < object.currency_platforms.length; ++i)
                    message.currency_platforms[i] = object.currency_platforms[i] >>> 0;
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.version != null)
                if (Number(object.version) !== 0)
                    message.version = object.version >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.tag != null)
                if (typeof object.tag !== "string" || object.tag.length)
                    message.tag = String(object.tag);
            return message;
        };

        ReqLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.currency_platforms = [];
            if (options.defaults) {
                object.account = "";
                object.password = "";
                object.reconnect = false;
                object.device = null;
                object.random_key = "";
                object.client_version = null;
                object.gen_access_token = false;
                object.type = 0;
                object.version = 0;
                object.client_version_string = "";
                object.tag = "";
            }
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = message.account;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                object.reconnect = message.reconnect;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                object.random_key = message.random_key;
            if (message.client_version != null && message.hasOwnProperty("client_version"))
                object.client_version = $root.lq.ClientVersionInfo.toObject(message.client_version, options);
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                object.gen_access_token = message.gen_access_token;
            if (message.currency_platforms && message.currency_platforms.length) {
                object.currency_platforms = Array(message.currency_platforms.length);
                for (let j = 0; j < message.currency_platforms.length; ++j)
                    object.currency_platforms[j] = message.currency_platforms[j];
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        ReqLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLogin";
        };

        return ReqLogin;
    })();

    lq.ResLogin = (function() {

        function ResLogin(properties) {
            this.logined_version = [];
            this.rewarded_version = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResLogin.prototype.error = null;
        ResLogin.prototype.account_id = 0;
        ResLogin.prototype.account = null;
        ResLogin.prototype.game_info = null;
        ResLogin.prototype.has_unread_announcement = false;
        ResLogin.prototype.access_token = "";
        ResLogin.prototype.signup_time = 0;
        ResLogin.prototype.is_id_card_authed = false;
        ResLogin.prototype.country = "";
        ResLogin.prototype.logined_version = $util.emptyArray;
        ResLogin.prototype.rewarded_version = $util.emptyArray;

        ResLogin.create = function create(properties) {
            return new ResLogin(properties);
        };

        ResLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                $root.lq.Account.encode(message.account, writer.uint32(26).fork()).ldelim();
            if (message.game_info != null && Object.hasOwnProperty.call(message, "game_info"))
                $root.lq.GameConnectInfo.encode(message.game_info, writer.uint32(34).fork()).ldelim();
            if (message.has_unread_announcement != null && Object.hasOwnProperty.call(message, "has_unread_announcement"))
                writer.uint32(40).bool(message.has_unread_announcement);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(50).string(message.access_token);
            if (message.signup_time != null && Object.hasOwnProperty.call(message, "signup_time"))
                writer.uint32(56).uint32(message.signup_time);
            if (message.is_id_card_authed != null && Object.hasOwnProperty.call(message, "is_id_card_authed"))
                writer.uint32(64).bool(message.is_id_card_authed);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(74).string(message.country);
            if (message.logined_version != null && message.logined_version.length) {
                writer.uint32(82).fork();
                for (let i = 0; i < message.logined_version.length; ++i)
                    writer.uint32(message.logined_version[i]);
                writer.ldelim();
            }
            if (message.rewarded_version != null && message.rewarded_version.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.rewarded_version.length; ++i)
                    writer.uint32(message.rewarded_version[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.lq.Account.decode(reader, reader.uint32(), undefined, _depth + 1, message.account);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_info = $root.lq.GameConnectInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_info);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.has_unread_announcement = value;
                        else
                            delete message.has_unread_announcement;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.signup_time = value;
                        else
                            delete message.signup_time;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_id_card_authed = value;
                        else
                            delete message.is_id_card_authed;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.country = value;
                        else
                            delete message.country;
                        continue;
                    }
                case 10: {
                        if (wireType === 2) {
                            if (!(message.logined_version && message.logined_version.length))
                                message.logined_version = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.logined_version.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.logined_version && message.logined_version.length))
                            message.logined_version = [];
                        message.logined_version.push(reader.uint32());
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.rewarded_version && message.rewarded_version.length))
                                message.rewarded_version = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_version.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_version && message.rewarded_version.length))
                            message.rewarded_version = [];
                        message.rewarded_version.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.account != null && message.hasOwnProperty("account")) {
                let error = $root.lq.Account.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.game_info != null && message.hasOwnProperty("game_info")) {
                let error = $root.lq.GameConnectInfo.verify(message.game_info, _depth + 1);
                if (error)
                    return "game_info." + error;
            }
            if (message.has_unread_announcement != null && message.hasOwnProperty("has_unread_announcement"))
                if (typeof message.has_unread_announcement !== "boolean")
                    return "has_unread_announcement: boolean expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.signup_time != null && message.hasOwnProperty("signup_time"))
                if (!$util.isInteger(message.signup_time))
                    return "signup_time: integer expected";
            if (message.is_id_card_authed != null && message.hasOwnProperty("is_id_card_authed"))
                if (typeof message.is_id_card_authed !== "boolean")
                    return "is_id_card_authed: boolean expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            if (message.logined_version != null && message.hasOwnProperty("logined_version")) {
                if (!Array.isArray(message.logined_version))
                    return "logined_version: array expected";
                for (let i = 0; i < message.logined_version.length; ++i)
                    if (!$util.isInteger(message.logined_version[i]))
                        return "logined_version: integer[] expected";
            }
            if (message.rewarded_version != null && message.hasOwnProperty("rewarded_version")) {
                if (!Array.isArray(message.rewarded_version))
                    return "rewarded_version: array expected";
                for (let i = 0; i < message.rewarded_version.length; ++i)
                    if (!$util.isInteger(message.rewarded_version[i]))
                        return "rewarded_version: integer[] expected";
            }
            return null;
        };

        ResLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResLogin();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResLogin.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.account != null) {
                if (typeof object.account !== "object")
                    throw TypeError(".lq.ResLogin.account: object expected");
                message.account = $root.lq.Account.fromObject(object.account, _depth + 1);
            }
            if (object.game_info != null) {
                if (typeof object.game_info !== "object")
                    throw TypeError(".lq.ResLogin.game_info: object expected");
                message.game_info = $root.lq.GameConnectInfo.fromObject(object.game_info, _depth + 1);
            }
            if (object.has_unread_announcement != null)
                if (object.has_unread_announcement)
                    message.has_unread_announcement = Boolean(object.has_unread_announcement);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.signup_time != null)
                if (Number(object.signup_time) !== 0)
                    message.signup_time = object.signup_time >>> 0;
            if (object.is_id_card_authed != null)
                if (object.is_id_card_authed)
                    message.is_id_card_authed = Boolean(object.is_id_card_authed);
            if (object.country != null)
                if (typeof object.country !== "string" || object.country.length)
                    message.country = String(object.country);
            if (object.logined_version) {
                if (!Array.isArray(object.logined_version))
                    throw TypeError(".lq.ResLogin.logined_version: array expected");
                message.logined_version = Array(object.logined_version.length);
                for (let i = 0; i < object.logined_version.length; ++i)
                    message.logined_version[i] = object.logined_version[i] >>> 0;
            }
            if (object.rewarded_version) {
                if (!Array.isArray(object.rewarded_version))
                    throw TypeError(".lq.ResLogin.rewarded_version: array expected");
                message.rewarded_version = Array(object.rewarded_version.length);
                for (let i = 0; i < object.rewarded_version.length; ++i)
                    message.rewarded_version[i] = object.rewarded_version[i] >>> 0;
            }
            return message;
        };

        ResLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.logined_version = [];
                object.rewarded_version = [];
            }
            if (options.defaults) {
                object.error = null;
                object.account_id = 0;
                object.account = null;
                object.game_info = null;
                object.has_unread_announcement = false;
                object.access_token = "";
                object.signup_time = 0;
                object.is_id_card_authed = false;
                object.country = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = $root.lq.Account.toObject(message.account, options);
            if (message.game_info != null && message.hasOwnProperty("game_info"))
                object.game_info = $root.lq.GameConnectInfo.toObject(message.game_info, options);
            if (message.has_unread_announcement != null && message.hasOwnProperty("has_unread_announcement"))
                object.has_unread_announcement = message.has_unread_announcement;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.signup_time != null && message.hasOwnProperty("signup_time"))
                object.signup_time = message.signup_time;
            if (message.is_id_card_authed != null && message.hasOwnProperty("is_id_card_authed"))
                object.is_id_card_authed = message.is_id_card_authed;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            if (message.logined_version && message.logined_version.length) {
                object.logined_version = Array(message.logined_version.length);
                for (let j = 0; j < message.logined_version.length; ++j)
                    object.logined_version[j] = message.logined_version[j];
            }
            if (message.rewarded_version && message.rewarded_version.length) {
                object.rewarded_version = Array(message.rewarded_version.length);
                for (let j = 0; j < message.rewarded_version.length; ++j)
                    object.rewarded_version[j] = message.rewarded_version[j];
            }
            return object;
        };

        ResLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResLogin";
        };

        return ResLogin;
    })();

    lq.ReqPrepareLogin = (function() {

        function ReqPrepareLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqPrepareLogin.prototype.access_token = "";
        ReqPrepareLogin.prototype.type = 0;

        ReqPrepareLogin.create = function create(properties) {
            return new ReqPrepareLogin(properties);
        };

        ReqPrepareLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(10).string(message.access_token);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqPrepareLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqPrepareLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqPrepareLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqPrepareLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqPrepareLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqPrepareLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqPrepareLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqPrepareLogin();
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqPrepareLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.access_token = "";
                object.type = 0;
            }
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqPrepareLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqPrepareLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqPrepareLogin";
        };

        return ReqPrepareLogin;
    })();

    lq.ReqFastLogin = (function() {

        function ReqFastLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFastLogin.prototype.client_version_string = "";

        ReqFastLogin.create = function create(properties) {
            return new ReqFastLogin(properties);
        };

        ReqFastLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(10).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFastLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFastLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFastLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFastLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFastLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqFastLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFastLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFastLogin();
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqFastLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.client_version_string = "";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqFastLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFastLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFastLogin";
        };

        return ReqFastLogin;
    })();

    lq.ResFastLogin = (function() {

        function ResFastLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFastLogin.prototype.error = null;
        ResFastLogin.prototype.game_info = null;
        ResFastLogin.prototype.room = null;

        ResFastLogin.create = function create(properties) {
            return new ResFastLogin(properties);
        };

        ResFastLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.game_info != null && Object.hasOwnProperty.call(message, "game_info"))
                $root.lq.GameConnectInfo.encode(message.game_info, writer.uint32(18).fork()).ldelim();
            if (message.room != null && Object.hasOwnProperty.call(message, "room"))
                $root.lq.Room.encode(message.room, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFastLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFastLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFastLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.game_info = $root.lq.GameConnectInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_info);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.lq.Room.decode(reader, reader.uint32(), undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFastLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFastLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.game_info != null && message.hasOwnProperty("game_info")) {
                let error = $root.lq.GameConnectInfo.verify(message.game_info, _depth + 1);
                if (error)
                    return "game_info." + error;
            }
            if (message.room != null && message.hasOwnProperty("room")) {
                let error = $root.lq.Room.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        ResFastLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFastLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFastLogin();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFastLogin.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.game_info != null) {
                if (typeof object.game_info !== "object")
                    throw TypeError(".lq.ResFastLogin.game_info: object expected");
                message.game_info = $root.lq.GameConnectInfo.fromObject(object.game_info, _depth + 1);
            }
            if (object.room != null) {
                if (typeof object.room !== "object")
                    throw TypeError(".lq.ResFastLogin.room: object expected");
                message.room = $root.lq.Room.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        ResFastLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.game_info = null;
                object.room = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.game_info != null && message.hasOwnProperty("game_info"))
                object.game_info = $root.lq.GameConnectInfo.toObject(message.game_info, options);
            if (message.room != null && message.hasOwnProperty("room"))
                object.room = $root.lq.Room.toObject(message.room, options);
            return object;
        };

        ResFastLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFastLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFastLogin";
        };

        return ResFastLogin;
    })();

    lq.ReqEmailLogin = (function() {

        function ReqEmailLogin(properties) {
            this.currency_platforms = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqEmailLogin.prototype.email = "";
        ReqEmailLogin.prototype.password = "";
        ReqEmailLogin.prototype.reconnect = false;
        ReqEmailLogin.prototype.device = null;
        ReqEmailLogin.prototype.random_key = "";
        ReqEmailLogin.prototype.client_version = "";
        ReqEmailLogin.prototype.gen_access_token = false;
        ReqEmailLogin.prototype.currency_platforms = $util.emptyArray;

        ReqEmailLogin.create = function create(properties) {
            return new ReqEmailLogin(properties);
        };

        ReqEmailLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(10).string(message.email);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(18).string(message.password);
            if (message.reconnect != null && Object.hasOwnProperty.call(message, "reconnect"))
                writer.uint32(24).bool(message.reconnect);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(34).fork()).ldelim();
            if (message.random_key != null && Object.hasOwnProperty.call(message, "random_key"))
                writer.uint32(42).string(message.random_key);
            if (message.client_version != null && Object.hasOwnProperty.call(message, "client_version"))
                writer.uint32(50).string(message.client_version);
            if (message.gen_access_token != null && Object.hasOwnProperty.call(message, "gen_access_token"))
                writer.uint32(56).bool(message.gen_access_token);
            if (message.currency_platforms != null && message.currency_platforms.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    writer.uint32(message.currency_platforms[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqEmailLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqEmailLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqEmailLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.email = value;
                        else
                            delete message.email;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reconnect = value;
                        else
                            delete message.reconnect;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.random_key = value;
                        else
                            delete message.random_key;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version = value;
                        else
                            delete message.client_version;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gen_access_token = value;
                        else
                            delete message.gen_access_token;
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.currency_platforms && message.currency_platforms.length))
                                message.currency_platforms = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.currency_platforms.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.currency_platforms && message.currency_platforms.length))
                            message.currency_platforms = [];
                        message.currency_platforms.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqEmailLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqEmailLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                if (typeof message.reconnect !== "boolean")
                    return "reconnect: boolean expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                if (!$util.isString(message.random_key))
                    return "random_key: string expected";
            if (message.client_version != null && message.hasOwnProperty("client_version"))
                if (!$util.isString(message.client_version))
                    return "client_version: string expected";
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                if (typeof message.gen_access_token !== "boolean")
                    return "gen_access_token: boolean expected";
            if (message.currency_platforms != null && message.hasOwnProperty("currency_platforms")) {
                if (!Array.isArray(message.currency_platforms))
                    return "currency_platforms: array expected";
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    if (!$util.isInteger(message.currency_platforms[i]))
                        return "currency_platforms: integer[] expected";
            }
            return null;
        };

        ReqEmailLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqEmailLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqEmailLogin();
            if (object.email != null)
                if (typeof object.email !== "string" || object.email.length)
                    message.email = String(object.email);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            if (object.reconnect != null)
                if (object.reconnect)
                    message.reconnect = Boolean(object.reconnect);
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqEmailLogin.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            if (object.random_key != null)
                if (typeof object.random_key !== "string" || object.random_key.length)
                    message.random_key = String(object.random_key);
            if (object.client_version != null)
                if (typeof object.client_version !== "string" || object.client_version.length)
                    message.client_version = String(object.client_version);
            if (object.gen_access_token != null)
                if (object.gen_access_token)
                    message.gen_access_token = Boolean(object.gen_access_token);
            if (object.currency_platforms) {
                if (!Array.isArray(object.currency_platforms))
                    throw TypeError(".lq.ReqEmailLogin.currency_platforms: array expected");
                message.currency_platforms = Array(object.currency_platforms.length);
                for (let i = 0; i < object.currency_platforms.length; ++i)
                    message.currency_platforms[i] = object.currency_platforms[i] >>> 0;
            }
            return message;
        };

        ReqEmailLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.currency_platforms = [];
            if (options.defaults) {
                object.email = "";
                object.password = "";
                object.reconnect = false;
                object.device = null;
                object.random_key = "";
                object.client_version = "";
                object.gen_access_token = false;
            }
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                object.reconnect = message.reconnect;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                object.random_key = message.random_key;
            if (message.client_version != null && message.hasOwnProperty("client_version"))
                object.client_version = message.client_version;
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                object.gen_access_token = message.gen_access_token;
            if (message.currency_platforms && message.currency_platforms.length) {
                object.currency_platforms = Array(message.currency_platforms.length);
                for (let j = 0; j < message.currency_platforms.length; ++j)
                    object.currency_platforms[j] = message.currency_platforms[j];
            }
            return object;
        };

        ReqEmailLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqEmailLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqEmailLogin";
        };

        return ReqEmailLogin;
    })();

    lq.ReqBindAccount = (function() {

        function ReqBindAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBindAccount.prototype.account = "";
        ReqBindAccount.prototype.password = "";

        ReqBindAccount.create = function create(properties) {
            return new ReqBindAccount(properties);
        };

        ReqBindAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                writer.uint32(10).string(message.account);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(18).string(message.password);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBindAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBindAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBindAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account = value;
                        else
                            delete message.account;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBindAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBindAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account != null && message.hasOwnProperty("account"))
                if (!$util.isString(message.account))
                    return "account: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            return null;
        };

        ReqBindAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBindAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBindAccount();
            if (object.account != null)
                if (typeof object.account !== "string" || object.account.length)
                    message.account = String(object.account);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            return message;
        };

        ReqBindAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account = "";
                object.password = "";
            }
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = message.account;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            return object;
        };

        ReqBindAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBindAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBindAccount";
        };

        return ReqBindAccount;
    })();

    lq.ReqCreatePhoneVerifyCode = (function() {

        function ReqCreatePhoneVerifyCode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreatePhoneVerifyCode.prototype.phone = "";
        ReqCreatePhoneVerifyCode.prototype.usage = 0;

        ReqCreatePhoneVerifyCode.create = function create(properties) {
            return new ReqCreatePhoneVerifyCode(properties);
        };

        ReqCreatePhoneVerifyCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                writer.uint32(10).string(message.phone);
            if (message.usage != null && Object.hasOwnProperty.call(message, "usage"))
                writer.uint32(16).uint32(message.usage);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreatePhoneVerifyCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreatePhoneVerifyCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreatePhoneVerifyCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.phone = value;
                        else
                            delete message.phone;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.usage = value;
                        else
                            delete message.usage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreatePhoneVerifyCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreatePhoneVerifyCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.phone != null && message.hasOwnProperty("phone"))
                if (!$util.isString(message.phone))
                    return "phone: string expected";
            if (message.usage != null && message.hasOwnProperty("usage"))
                if (!$util.isInteger(message.usage))
                    return "usage: integer expected";
            return null;
        };

        ReqCreatePhoneVerifyCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreatePhoneVerifyCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreatePhoneVerifyCode();
            if (object.phone != null)
                if (typeof object.phone !== "string" || object.phone.length)
                    message.phone = String(object.phone);
            if (object.usage != null)
                if (Number(object.usage) !== 0)
                    message.usage = object.usage >>> 0;
            return message;
        };

        ReqCreatePhoneVerifyCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.phone = "";
                object.usage = 0;
            }
            if (message.phone != null && message.hasOwnProperty("phone"))
                object.phone = message.phone;
            if (message.usage != null && message.hasOwnProperty("usage"))
                object.usage = message.usage;
            return object;
        };

        ReqCreatePhoneVerifyCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreatePhoneVerifyCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreatePhoneVerifyCode";
        };

        return ReqCreatePhoneVerifyCode;
    })();

    lq.ReqCreateEmailVerifyCode = (function() {

        function ReqCreateEmailVerifyCode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateEmailVerifyCode.prototype.email = "";
        ReqCreateEmailVerifyCode.prototype.usage = 0;

        ReqCreateEmailVerifyCode.create = function create(properties) {
            return new ReqCreateEmailVerifyCode(properties);
        };

        ReqCreateEmailVerifyCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(10).string(message.email);
            if (message.usage != null && Object.hasOwnProperty.call(message, "usage"))
                writer.uint32(16).uint32(message.usage);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateEmailVerifyCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateEmailVerifyCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateEmailVerifyCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.email = value;
                        else
                            delete message.email;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.usage = value;
                        else
                            delete message.usage;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateEmailVerifyCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateEmailVerifyCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.usage != null && message.hasOwnProperty("usage"))
                if (!$util.isInteger(message.usage))
                    return "usage: integer expected";
            return null;
        };

        ReqCreateEmailVerifyCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateEmailVerifyCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateEmailVerifyCode();
            if (object.email != null)
                if (typeof object.email !== "string" || object.email.length)
                    message.email = String(object.email);
            if (object.usage != null)
                if (Number(object.usage) !== 0)
                    message.usage = object.usage >>> 0;
            return message;
        };

        ReqCreateEmailVerifyCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.email = "";
                object.usage = 0;
            }
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.usage != null && message.hasOwnProperty("usage"))
                object.usage = message.usage;
            return object;
        };

        ReqCreateEmailVerifyCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateEmailVerifyCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateEmailVerifyCode";
        };

        return ReqCreateEmailVerifyCode;
    })();

    lq.ReqVerifyCodeForSecure = (function() {

        function ReqVerifyCodeForSecure(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVerifyCodeForSecure.prototype.code = "";
        ReqVerifyCodeForSecure.prototype.operation = 0;

        ReqVerifyCodeForSecure.create = function create(properties) {
            return new ReqVerifyCodeForSecure(properties);
        };

        ReqVerifyCodeForSecure.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(10).string(message.code);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                writer.uint32(16).uint32(message.operation);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVerifyCodeForSecure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVerifyCodeForSecure.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVerifyCodeForSecure(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.operation = value;
                        else
                            delete message.operation;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVerifyCodeForSecure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVerifyCodeForSecure.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.operation != null && message.hasOwnProperty("operation"))
                if (!$util.isInteger(message.operation))
                    return "operation: integer expected";
            return null;
        };

        ReqVerifyCodeForSecure.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVerifyCodeForSecure)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVerifyCodeForSecure();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.operation != null)
                if (Number(object.operation) !== 0)
                    message.operation = object.operation >>> 0;
            return message;
        };

        ReqVerifyCodeForSecure.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.code = "";
                object.operation = 0;
            }
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = message.operation;
            return object;
        };

        ReqVerifyCodeForSecure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVerifyCodeForSecure.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVerifyCodeForSecure";
        };

        return ReqVerifyCodeForSecure;
    })();

    lq.ResVerfiyCodeForSecure = (function() {

        function ResVerfiyCodeForSecure(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResVerfiyCodeForSecure.prototype.error = null;
        ResVerfiyCodeForSecure.prototype.secure_token = "";

        ResVerfiyCodeForSecure.create = function create(properties) {
            return new ResVerfiyCodeForSecure(properties);
        };

        ResVerfiyCodeForSecure.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.secure_token != null && Object.hasOwnProperty.call(message, "secure_token"))
                writer.uint32(18).string(message.secure_token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResVerfiyCodeForSecure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResVerfiyCodeForSecure.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResVerfiyCodeForSecure(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.secure_token = value;
                        else
                            delete message.secure_token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResVerfiyCodeForSecure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResVerfiyCodeForSecure.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.secure_token != null && message.hasOwnProperty("secure_token"))
                if (!$util.isString(message.secure_token))
                    return "secure_token: string expected";
            return null;
        };

        ResVerfiyCodeForSecure.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResVerfiyCodeForSecure)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResVerfiyCodeForSecure();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResVerfiyCodeForSecure.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.secure_token != null)
                if (typeof object.secure_token !== "string" || object.secure_token.length)
                    message.secure_token = String(object.secure_token);
            return message;
        };

        ResVerfiyCodeForSecure.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.secure_token = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.secure_token != null && message.hasOwnProperty("secure_token"))
                object.secure_token = message.secure_token;
            return object;
        };

        ResVerfiyCodeForSecure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResVerfiyCodeForSecure.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResVerfiyCodeForSecure";
        };

        return ResVerfiyCodeForSecure;
    })();

    lq.ReqBindPhoneNumber = (function() {

        function ReqBindPhoneNumber(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBindPhoneNumber.prototype.code = "";
        ReqBindPhoneNumber.prototype.phone = "";
        ReqBindPhoneNumber.prototype.password = "";
        ReqBindPhoneNumber.prototype.multi_bind_version = false;

        ReqBindPhoneNumber.create = function create(properties) {
            return new ReqBindPhoneNumber(properties);
        };

        ReqBindPhoneNumber.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(10).string(message.code);
            if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                writer.uint32(18).string(message.phone);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(26).string(message.password);
            if (message.multi_bind_version != null && Object.hasOwnProperty.call(message, "multi_bind_version"))
                writer.uint32(32).bool(message.multi_bind_version);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBindPhoneNumber.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBindPhoneNumber.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBindPhoneNumber(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.phone = value;
                        else
                            delete message.phone;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.multi_bind_version = value;
                        else
                            delete message.multi_bind_version;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBindPhoneNumber.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBindPhoneNumber.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.phone != null && message.hasOwnProperty("phone"))
                if (!$util.isString(message.phone))
                    return "phone: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            if (message.multi_bind_version != null && message.hasOwnProperty("multi_bind_version"))
                if (typeof message.multi_bind_version !== "boolean")
                    return "multi_bind_version: boolean expected";
            return null;
        };

        ReqBindPhoneNumber.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBindPhoneNumber)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBindPhoneNumber();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.phone != null)
                if (typeof object.phone !== "string" || object.phone.length)
                    message.phone = String(object.phone);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            if (object.multi_bind_version != null)
                if (object.multi_bind_version)
                    message.multi_bind_version = Boolean(object.multi_bind_version);
            return message;
        };

        ReqBindPhoneNumber.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.code = "";
                object.phone = "";
                object.password = "";
                object.multi_bind_version = false;
            }
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.phone != null && message.hasOwnProperty("phone"))
                object.phone = message.phone;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            if (message.multi_bind_version != null && message.hasOwnProperty("multi_bind_version"))
                object.multi_bind_version = message.multi_bind_version;
            return object;
        };

        ReqBindPhoneNumber.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBindPhoneNumber.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBindPhoneNumber";
        };

        return ReqBindPhoneNumber;
    })();

    lq.ReqUnbindPhoneNumber = (function() {

        function ReqUnbindPhoneNumber(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUnbindPhoneNumber.prototype.code = "";
        ReqUnbindPhoneNumber.prototype.phone = "";
        ReqUnbindPhoneNumber.prototype.password = "";

        ReqUnbindPhoneNumber.create = function create(properties) {
            return new ReqUnbindPhoneNumber(properties);
        };

        ReqUnbindPhoneNumber.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(10).string(message.code);
            if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                writer.uint32(18).string(message.phone);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(26).string(message.password);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUnbindPhoneNumber.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUnbindPhoneNumber.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUnbindPhoneNumber(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.phone = value;
                        else
                            delete message.phone;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUnbindPhoneNumber.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUnbindPhoneNumber.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.phone != null && message.hasOwnProperty("phone"))
                if (!$util.isString(message.phone))
                    return "phone: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            return null;
        };

        ReqUnbindPhoneNumber.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUnbindPhoneNumber)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUnbindPhoneNumber();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.phone != null)
                if (typeof object.phone !== "string" || object.phone.length)
                    message.phone = String(object.phone);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            return message;
        };

        ReqUnbindPhoneNumber.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.code = "";
                object.phone = "";
                object.password = "";
            }
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.phone != null && message.hasOwnProperty("phone"))
                object.phone = message.phone;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            return object;
        };

        ReqUnbindPhoneNumber.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUnbindPhoneNumber.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUnbindPhoneNumber";
        };

        return ReqUnbindPhoneNumber;
    })();

    lq.ResFetchPhoneLoginBind = (function() {

        function ResFetchPhoneLoginBind(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchPhoneLoginBind.prototype.error = null;
        ResFetchPhoneLoginBind.prototype.phone_login = 0;

        ResFetchPhoneLoginBind.create = function create(properties) {
            return new ResFetchPhoneLoginBind(properties);
        };

        ResFetchPhoneLoginBind.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.phone_login != null && Object.hasOwnProperty.call(message, "phone_login"))
                writer.uint32(16).uint32(message.phone_login);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchPhoneLoginBind.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchPhoneLoginBind.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchPhoneLoginBind(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.phone_login = value;
                        else
                            delete message.phone_login;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchPhoneLoginBind.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchPhoneLoginBind.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.phone_login != null && message.hasOwnProperty("phone_login"))
                if (!$util.isInteger(message.phone_login))
                    return "phone_login: integer expected";
            return null;
        };

        ResFetchPhoneLoginBind.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchPhoneLoginBind)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchPhoneLoginBind();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchPhoneLoginBind.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.phone_login != null)
                if (Number(object.phone_login) !== 0)
                    message.phone_login = object.phone_login >>> 0;
            return message;
        };

        ResFetchPhoneLoginBind.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.phone_login = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.phone_login != null && message.hasOwnProperty("phone_login"))
                object.phone_login = message.phone_login;
            return object;
        };

        ResFetchPhoneLoginBind.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchPhoneLoginBind.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchPhoneLoginBind";
        };

        return ResFetchPhoneLoginBind;
    })();

    lq.ReqCreatePhoneLoginBind = (function() {

        function ReqCreatePhoneLoginBind(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreatePhoneLoginBind.prototype.password = "";

        ReqCreatePhoneLoginBind.create = function create(properties) {
            return new ReqCreatePhoneLoginBind(properties);
        };

        ReqCreatePhoneLoginBind.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(10).string(message.password);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreatePhoneLoginBind.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreatePhoneLoginBind.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreatePhoneLoginBind(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreatePhoneLoginBind.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreatePhoneLoginBind.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            return null;
        };

        ReqCreatePhoneLoginBind.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreatePhoneLoginBind)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreatePhoneLoginBind();
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            return message;
        };

        ReqCreatePhoneLoginBind.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.password = "";
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            return object;
        };

        ReqCreatePhoneLoginBind.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreatePhoneLoginBind.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreatePhoneLoginBind";
        };

        return ReqCreatePhoneLoginBind;
    })();

    lq.ReqBindEmail = (function() {

        function ReqBindEmail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBindEmail.prototype.email = "";
        ReqBindEmail.prototype.code = "";
        ReqBindEmail.prototype.password = "";

        ReqBindEmail.create = function create(properties) {
            return new ReqBindEmail(properties);
        };

        ReqBindEmail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(10).string(message.email);
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(18).string(message.code);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(26).string(message.password);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBindEmail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBindEmail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBindEmail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.email = value;
                        else
                            delete message.email;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBindEmail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBindEmail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.password != null && message.hasOwnProperty("password"))
                if (!$util.isString(message.password))
                    return "password: string expected";
            return null;
        };

        ReqBindEmail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBindEmail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBindEmail();
            if (object.email != null)
                if (typeof object.email !== "string" || object.email.length)
                    message.email = String(object.email);
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = String(object.password);
            return message;
        };

        ReqBindEmail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.email = "";
                object.code = "";
                object.password = "";
            }
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.password != null && message.hasOwnProperty("password"))
                object.password = message.password;
            return object;
        };

        ReqBindEmail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBindEmail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBindEmail";
        };

        return ReqBindEmail;
    })();

    lq.ReqModifyPassword = (function() {

        function ReqModifyPassword(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqModifyPassword.prototype.new_password = "";
        ReqModifyPassword.prototype.old_password = "";
        ReqModifyPassword.prototype.secure_token = "";

        ReqModifyPassword.create = function create(properties) {
            return new ReqModifyPassword(properties);
        };

        ReqModifyPassword.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.new_password != null && Object.hasOwnProperty.call(message, "new_password"))
                writer.uint32(10).string(message.new_password);
            if (message.old_password != null && Object.hasOwnProperty.call(message, "old_password"))
                writer.uint32(18).string(message.old_password);
            if (message.secure_token != null && Object.hasOwnProperty.call(message, "secure_token"))
                writer.uint32(26).string(message.secure_token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqModifyPassword.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqModifyPassword.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqModifyPassword(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.new_password = value;
                        else
                            delete message.new_password;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.old_password = value;
                        else
                            delete message.old_password;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.secure_token = value;
                        else
                            delete message.secure_token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqModifyPassword.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqModifyPassword.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.new_password != null && message.hasOwnProperty("new_password"))
                if (!$util.isString(message.new_password))
                    return "new_password: string expected";
            if (message.old_password != null && message.hasOwnProperty("old_password"))
                if (!$util.isString(message.old_password))
                    return "old_password: string expected";
            if (message.secure_token != null && message.hasOwnProperty("secure_token"))
                if (!$util.isString(message.secure_token))
                    return "secure_token: string expected";
            return null;
        };

        ReqModifyPassword.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqModifyPassword)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqModifyPassword();
            if (object.new_password != null)
                if (typeof object.new_password !== "string" || object.new_password.length)
                    message.new_password = String(object.new_password);
            if (object.old_password != null)
                if (typeof object.old_password !== "string" || object.old_password.length)
                    message.old_password = String(object.old_password);
            if (object.secure_token != null)
                if (typeof object.secure_token !== "string" || object.secure_token.length)
                    message.secure_token = String(object.secure_token);
            return message;
        };

        ReqModifyPassword.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.new_password = "";
                object.old_password = "";
                object.secure_token = "";
            }
            if (message.new_password != null && message.hasOwnProperty("new_password"))
                object.new_password = message.new_password;
            if (message.old_password != null && message.hasOwnProperty("old_password"))
                object.old_password = message.old_password;
            if (message.secure_token != null && message.hasOwnProperty("secure_token"))
                object.secure_token = message.secure_token;
            return object;
        };

        ReqModifyPassword.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqModifyPassword.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqModifyPassword";
        };

        return ReqModifyPassword;
    })();

    lq.ReqOauth2Auth = (function() {

        function ReqOauth2Auth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOauth2Auth.prototype.type = 0;
        ReqOauth2Auth.prototype.code = "";
        ReqOauth2Auth.prototype.uid = "";
        ReqOauth2Auth.prototype.client_version_string = "";

        ReqOauth2Auth.create = function create(properties) {
            return new ReqOauth2Auth(properties);
        };

        ReqOauth2Auth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(18).string(message.code);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(26).string(message.uid);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(34).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOauth2Auth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOauth2Auth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOauth2Auth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uid = value;
                        else
                            delete message.uid;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOauth2Auth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOauth2Auth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isString(message.uid))
                    return "uid: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqOauth2Auth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOauth2Auth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOauth2Auth();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            if (object.uid != null)
                if (typeof object.uid !== "string" || object.uid.length)
                    message.uid = String(object.uid);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqOauth2Auth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.code = "";
                object.uid = "";
                object.client_version_string = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqOauth2Auth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOauth2Auth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOauth2Auth";
        };

        return ReqOauth2Auth;
    })();

    lq.ResOauth2Auth = (function() {

        function ResOauth2Auth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOauth2Auth.prototype.error = null;
        ResOauth2Auth.prototype.access_token = "";

        ResOauth2Auth.create = function create(properties) {
            return new ResOauth2Auth(properties);
        };

        ResOauth2Auth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(18).string(message.access_token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOauth2Auth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOauth2Auth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOauth2Auth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOauth2Auth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOauth2Auth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            return null;
        };

        ResOauth2Auth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOauth2Auth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOauth2Auth();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOauth2Auth.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            return message;
        };

        ResOauth2Auth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.access_token = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            return object;
        };

        ResOauth2Auth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOauth2Auth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOauth2Auth";
        };

        return ResOauth2Auth;
    })();

    lq.ReqOauth2Check = (function() {

        function ReqOauth2Check(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOauth2Check.prototype.type = 0;
        ReqOauth2Check.prototype.access_token = "";

        ReqOauth2Check.create = function create(properties) {
            return new ReqOauth2Check(properties);
        };

        ReqOauth2Check.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(18).string(message.access_token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOauth2Check.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOauth2Check.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOauth2Check(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOauth2Check.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOauth2Check.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            return null;
        };

        ReqOauth2Check.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOauth2Check)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOauth2Check();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            return message;
        };

        ReqOauth2Check.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.access_token = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            return object;
        };

        ReqOauth2Check.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOauth2Check.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOauth2Check";
        };

        return ReqOauth2Check;
    })();

    lq.ResOauth2Check = (function() {

        function ResOauth2Check(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOauth2Check.prototype.error = null;
        ResOauth2Check.prototype.has_account = false;

        ResOauth2Check.create = function create(properties) {
            return new ResOauth2Check(properties);
        };

        ResOauth2Check.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.has_account != null && Object.hasOwnProperty.call(message, "has_account"))
                writer.uint32(16).bool(message.has_account);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOauth2Check.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOauth2Check.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOauth2Check(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.has_account = value;
                        else
                            delete message.has_account;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOauth2Check.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOauth2Check.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.has_account != null && message.hasOwnProperty("has_account"))
                if (typeof message.has_account !== "boolean")
                    return "has_account: boolean expected";
            return null;
        };

        ResOauth2Check.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOauth2Check)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOauth2Check();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOauth2Check.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.has_account != null)
                if (object.has_account)
                    message.has_account = Boolean(object.has_account);
            return message;
        };

        ResOauth2Check.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.has_account = false;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.has_account != null && message.hasOwnProperty("has_account"))
                object.has_account = message.has_account;
            return object;
        };

        ResOauth2Check.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOauth2Check.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOauth2Check";
        };

        return ResOauth2Check;
    })();

    lq.ReqOauth2Signup = (function() {

        function ReqOauth2Signup(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOauth2Signup.prototype.type = 0;
        ReqOauth2Signup.prototype.access_token = "";
        ReqOauth2Signup.prototype.email = "";
        ReqOauth2Signup.prototype.advertise_str = "";
        ReqOauth2Signup.prototype.device = null;
        ReqOauth2Signup.prototype.client_version = null;
        ReqOauth2Signup.prototype.client_version_string = "";
        ReqOauth2Signup.prototype.tag = "";

        ReqOauth2Signup.create = function create(properties) {
            return new ReqOauth2Signup(properties);
        };

        ReqOauth2Signup.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(18).string(message.access_token);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(26).string(message.email);
            if (message.advertise_str != null && Object.hasOwnProperty.call(message, "advertise_str"))
                writer.uint32(34).string(message.advertise_str);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(42).fork()).ldelim();
            if (message.client_version != null && Object.hasOwnProperty.call(message, "client_version"))
                $root.lq.ClientVersionInfo.encode(message.client_version, writer.uint32(50).fork()).ldelim();
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(58).string(message.client_version_string);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(66).string(message.tag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOauth2Signup.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOauth2Signup.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOauth2Signup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.email = value;
                        else
                            delete message.email;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.advertise_str = value;
                        else
                            delete message.advertise_str;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.client_version = $root.lq.ClientVersionInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.client_version);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOauth2Signup.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOauth2Signup.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.email != null && message.hasOwnProperty("email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.advertise_str != null && message.hasOwnProperty("advertise_str"))
                if (!$util.isString(message.advertise_str))
                    return "advertise_str: string expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            if (message.client_version != null && message.hasOwnProperty("client_version")) {
                let error = $root.lq.ClientVersionInfo.verify(message.client_version, _depth + 1);
                if (error)
                    return "client_version." + error;
            }
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        ReqOauth2Signup.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOauth2Signup)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOauth2Signup();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.email != null)
                if (typeof object.email !== "string" || object.email.length)
                    message.email = String(object.email);
            if (object.advertise_str != null)
                if (typeof object.advertise_str !== "string" || object.advertise_str.length)
                    message.advertise_str = String(object.advertise_str);
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqOauth2Signup.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            if (object.client_version != null) {
                if (typeof object.client_version !== "object")
                    throw TypeError(".lq.ReqOauth2Signup.client_version: object expected");
                message.client_version = $root.lq.ClientVersionInfo.fromObject(object.client_version, _depth + 1);
            }
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.tag != null)
                if (typeof object.tag !== "string" || object.tag.length)
                    message.tag = String(object.tag);
            return message;
        };

        ReqOauth2Signup.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.access_token = "";
                object.email = "";
                object.advertise_str = "";
                object.device = null;
                object.client_version = null;
                object.client_version_string = "";
                object.tag = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.email != null && message.hasOwnProperty("email"))
                object.email = message.email;
            if (message.advertise_str != null && message.hasOwnProperty("advertise_str"))
                object.advertise_str = message.advertise_str;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            if (message.client_version != null && message.hasOwnProperty("client_version"))
                object.client_version = $root.lq.ClientVersionInfo.toObject(message.client_version, options);
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        ReqOauth2Signup.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOauth2Signup.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOauth2Signup";
        };

        return ReqOauth2Signup;
    })();

    lq.ResOauth2Signup = (function() {

        function ResOauth2Signup(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOauth2Signup.prototype.error = null;

        ResOauth2Signup.create = function create(properties) {
            return new ResOauth2Signup(properties);
        };

        ResOauth2Signup.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOauth2Signup.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOauth2Signup.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOauth2Signup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOauth2Signup.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOauth2Signup.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResOauth2Signup.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOauth2Signup)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOauth2Signup();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOauth2Signup.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResOauth2Signup.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResOauth2Signup.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOauth2Signup.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOauth2Signup";
        };

        return ResOauth2Signup;
    })();

    lq.ReqOauth2Login = (function() {

        function ReqOauth2Login(properties) {
            this.currency_platforms = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOauth2Login.prototype.type = 0;
        ReqOauth2Login.prototype.access_token = "";
        ReqOauth2Login.prototype.reconnect = false;
        ReqOauth2Login.prototype.device = null;
        ReqOauth2Login.prototype.random_key = "";
        ReqOauth2Login.prototype.client_version = null;
        ReqOauth2Login.prototype.gen_access_token = false;
        ReqOauth2Login.prototype.currency_platforms = $util.emptyArray;
        ReqOauth2Login.prototype.version = 0;
        ReqOauth2Login.prototype.client_version_string = "";
        ReqOauth2Login.prototype.tag = "";

        ReqOauth2Login.create = function create(properties) {
            return new ReqOauth2Login(properties);
        };

        ReqOauth2Login.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(18).string(message.access_token);
            if (message.reconnect != null && Object.hasOwnProperty.call(message, "reconnect"))
                writer.uint32(24).bool(message.reconnect);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(34).fork()).ldelim();
            if (message.random_key != null && Object.hasOwnProperty.call(message, "random_key"))
                writer.uint32(42).string(message.random_key);
            if (message.client_version != null && Object.hasOwnProperty.call(message, "client_version"))
                $root.lq.ClientVersionInfo.encode(message.client_version, writer.uint32(50).fork()).ldelim();
            if (message.gen_access_token != null && Object.hasOwnProperty.call(message, "gen_access_token"))
                writer.uint32(56).bool(message.gen_access_token);
            if (message.currency_platforms != null && message.currency_platforms.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    writer.uint32(message.currency_platforms[i]);
                writer.ldelim();
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(72).uint32(message.version);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(82).string(message.client_version_string);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(90).string(message.tag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOauth2Login.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOauth2Login.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOauth2Login(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.reconnect = value;
                        else
                            delete message.reconnect;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.random_key = value;
                        else
                            delete message.random_key;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.client_version = $root.lq.ClientVersionInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.client_version);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gen_access_token = value;
                        else
                            delete message.gen_access_token;
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.currency_platforms && message.currency_platforms.length))
                                message.currency_platforms = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.currency_platforms.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.currency_platforms && message.currency_platforms.length))
                            message.currency_platforms = [];
                        message.currency_platforms.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOauth2Login.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOauth2Login.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                if (typeof message.reconnect !== "boolean")
                    return "reconnect: boolean expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                if (!$util.isString(message.random_key))
                    return "random_key: string expected";
            if (message.client_version != null && message.hasOwnProperty("client_version")) {
                let error = $root.lq.ClientVersionInfo.verify(message.client_version, _depth + 1);
                if (error)
                    return "client_version." + error;
            }
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                if (typeof message.gen_access_token !== "boolean")
                    return "gen_access_token: boolean expected";
            if (message.currency_platforms != null && message.hasOwnProperty("currency_platforms")) {
                if (!Array.isArray(message.currency_platforms))
                    return "currency_platforms: array expected";
                for (let i = 0; i < message.currency_platforms.length; ++i)
                    if (!$util.isInteger(message.currency_platforms[i]))
                        return "currency_platforms: integer[] expected";
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        ReqOauth2Login.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOauth2Login)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOauth2Login();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.reconnect != null)
                if (object.reconnect)
                    message.reconnect = Boolean(object.reconnect);
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqOauth2Login.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            if (object.random_key != null)
                if (typeof object.random_key !== "string" || object.random_key.length)
                    message.random_key = String(object.random_key);
            if (object.client_version != null) {
                if (typeof object.client_version !== "object")
                    throw TypeError(".lq.ReqOauth2Login.client_version: object expected");
                message.client_version = $root.lq.ClientVersionInfo.fromObject(object.client_version, _depth + 1);
            }
            if (object.gen_access_token != null)
                if (object.gen_access_token)
                    message.gen_access_token = Boolean(object.gen_access_token);
            if (object.currency_platforms) {
                if (!Array.isArray(object.currency_platforms))
                    throw TypeError(".lq.ReqOauth2Login.currency_platforms: array expected");
                message.currency_platforms = Array(object.currency_platforms.length);
                for (let i = 0; i < object.currency_platforms.length; ++i)
                    message.currency_platforms[i] = object.currency_platforms[i] >>> 0;
            }
            if (object.version != null)
                if (Number(object.version) !== 0)
                    message.version = object.version >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.tag != null)
                if (typeof object.tag !== "string" || object.tag.length)
                    message.tag = String(object.tag);
            return message;
        };

        ReqOauth2Login.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.currency_platforms = [];
            if (options.defaults) {
                object.type = 0;
                object.access_token = "";
                object.reconnect = false;
                object.device = null;
                object.random_key = "";
                object.client_version = null;
                object.gen_access_token = false;
                object.version = 0;
                object.client_version_string = "";
                object.tag = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.reconnect != null && message.hasOwnProperty("reconnect"))
                object.reconnect = message.reconnect;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            if (message.random_key != null && message.hasOwnProperty("random_key"))
                object.random_key = message.random_key;
            if (message.client_version != null && message.hasOwnProperty("client_version"))
                object.client_version = $root.lq.ClientVersionInfo.toObject(message.client_version, options);
            if (message.gen_access_token != null && message.hasOwnProperty("gen_access_token"))
                object.gen_access_token = message.gen_access_token;
            if (message.currency_platforms && message.currency_platforms.length) {
                object.currency_platforms = Array(message.currency_platforms.length);
                for (let j = 0; j < message.currency_platforms.length; ++j)
                    object.currency_platforms[j] = message.currency_platforms[j];
            }
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        ReqOauth2Login.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOauth2Login.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOauth2Login";
        };

        return ReqOauth2Login;
    })();

    lq.ReqDMMPreLogin = (function() {

        function ReqDMMPreLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDMMPreLogin.prototype.finish_url = "";

        ReqDMMPreLogin.create = function create(properties) {
            return new ReqDMMPreLogin(properties);
        };

        ReqDMMPreLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.finish_url != null && Object.hasOwnProperty.call(message, "finish_url"))
                writer.uint32(10).string(message.finish_url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDMMPreLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDMMPreLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDMMPreLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.finish_url = value;
                        else
                            delete message.finish_url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDMMPreLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDMMPreLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.finish_url != null && message.hasOwnProperty("finish_url"))
                if (!$util.isString(message.finish_url))
                    return "finish_url: string expected";
            return null;
        };

        ReqDMMPreLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDMMPreLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDMMPreLogin();
            if (object.finish_url != null)
                if (typeof object.finish_url !== "string" || object.finish_url.length)
                    message.finish_url = String(object.finish_url);
            return message;
        };

        ReqDMMPreLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.finish_url = "";
            if (message.finish_url != null && message.hasOwnProperty("finish_url"))
                object.finish_url = message.finish_url;
            return object;
        };

        ReqDMMPreLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDMMPreLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDMMPreLogin";
        };

        return ReqDMMPreLogin;
    })();

    lq.ResDMMPreLogin = (function() {

        function ResDMMPreLogin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDMMPreLogin.prototype.error = null;
        ResDMMPreLogin.prototype.parameter = "";

        ResDMMPreLogin.create = function create(properties) {
            return new ResDMMPreLogin(properties);
        };

        ResDMMPreLogin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.parameter != null && Object.hasOwnProperty.call(message, "parameter"))
                writer.uint32(10).string(message.parameter);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDMMPreLogin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDMMPreLogin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDMMPreLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.parameter = value;
                        else
                            delete message.parameter;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDMMPreLogin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDMMPreLogin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.parameter != null && message.hasOwnProperty("parameter"))
                if (!$util.isString(message.parameter))
                    return "parameter: string expected";
            return null;
        };

        ResDMMPreLogin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDMMPreLogin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDMMPreLogin();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDMMPreLogin.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.parameter != null)
                if (typeof object.parameter !== "string" || object.parameter.length)
                    message.parameter = String(object.parameter);
            return message;
        };

        ResDMMPreLogin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.parameter = "";
                object.error = null;
            }
            if (message.parameter != null && message.hasOwnProperty("parameter"))
                object.parameter = message.parameter;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResDMMPreLogin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDMMPreLogin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDMMPreLogin";
        };

        return ResDMMPreLogin;
    })();

    lq.ReqLogout = (function() {

        function ReqLogout(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLogout.create = function create(properties) {
            return new ReqLogout(properties);
        };

        ReqLogout.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLogout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLogout.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLogout();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLogout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLogout.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        ReqLogout.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLogout)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.ReqLogout();
        };

        ReqLogout.toObject = function toObject() {
            return {};
        };

        ReqLogout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLogout.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLogout";
        };

        return ReqLogout;
    })();

    lq.ResLogout = (function() {

        function ResLogout(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResLogout.prototype.error = null;

        ResLogout.create = function create(properties) {
            return new ResLogout(properties);
        };

        ResLogout.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResLogout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResLogout.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResLogout(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResLogout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResLogout.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResLogout.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResLogout)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResLogout();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResLogout.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResLogout.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResLogout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResLogout.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResLogout";
        };

        return ResLogout;
    })();

    lq.ReqHeatBeat = (function() {

        function ReqHeatBeat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqHeatBeat.prototype.no_operation_counter = 0;

        ReqHeatBeat.create = function create(properties) {
            return new ReqHeatBeat(properties);
        };

        ReqHeatBeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.no_operation_counter != null && Object.hasOwnProperty.call(message, "no_operation_counter"))
                writer.uint32(8).uint32(message.no_operation_counter);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqHeatBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqHeatBeat.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqHeatBeat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.no_operation_counter = value;
                        else
                            delete message.no_operation_counter;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqHeatBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqHeatBeat.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.no_operation_counter != null && message.hasOwnProperty("no_operation_counter"))
                if (!$util.isInteger(message.no_operation_counter))
                    return "no_operation_counter: integer expected";
            return null;
        };

        ReqHeatBeat.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqHeatBeat)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqHeatBeat();
            if (object.no_operation_counter != null)
                if (Number(object.no_operation_counter) !== 0)
                    message.no_operation_counter = object.no_operation_counter >>> 0;
            return message;
        };

        ReqHeatBeat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.no_operation_counter = 0;
            if (message.no_operation_counter != null && message.hasOwnProperty("no_operation_counter"))
                object.no_operation_counter = message.no_operation_counter;
            return object;
        };

        ReqHeatBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqHeatBeat.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqHeatBeat";
        };

        return ReqHeatBeat;
    })();

    lq.ReqSearchAccountByEidLobby = (function() {

        function ReqSearchAccountByEidLobby(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSearchAccountByEidLobby.prototype.eid = 0;

        ReqSearchAccountByEidLobby.create = function create(properties) {
            return new ReqSearchAccountByEidLobby(properties);
        };

        ReqSearchAccountByEidLobby.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.eid != null && Object.hasOwnProperty.call(message, "eid"))
                writer.uint32(8).uint32(message.eid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSearchAccountByEidLobby.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSearchAccountByEidLobby.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSearchAccountByEidLobby(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.eid = value;
                        else
                            delete message.eid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSearchAccountByEidLobby.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSearchAccountByEidLobby.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.eid != null && message.hasOwnProperty("eid"))
                if (!$util.isInteger(message.eid))
                    return "eid: integer expected";
            return null;
        };

        ReqSearchAccountByEidLobby.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSearchAccountByEidLobby)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSearchAccountByEidLobby();
            if (object.eid != null)
                if (Number(object.eid) !== 0)
                    message.eid = object.eid >>> 0;
            return message;
        };

        ReqSearchAccountByEidLobby.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.eid = 0;
            if (message.eid != null && message.hasOwnProperty("eid"))
                object.eid = message.eid;
            return object;
        };

        ReqSearchAccountByEidLobby.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSearchAccountByEidLobby.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSearchAccountByEidLobby";
        };

        return ReqSearchAccountByEidLobby;
    })();

    lq.ResSearchAccountbyEidLobby = (function() {

        function ResSearchAccountbyEidLobby(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSearchAccountbyEidLobby.prototype.error = null;
        ResSearchAccountbyEidLobby.prototype.account_id = 0;

        ResSearchAccountbyEidLobby.create = function create(properties) {
            return new ResSearchAccountbyEidLobby(properties);
        };

        ResSearchAccountbyEidLobby.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSearchAccountbyEidLobby.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSearchAccountbyEidLobby.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSearchAccountbyEidLobby(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSearchAccountbyEidLobby.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSearchAccountbyEidLobby.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ResSearchAccountbyEidLobby.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSearchAccountbyEidLobby)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSearchAccountbyEidLobby();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSearchAccountbyEidLobby.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ResSearchAccountbyEidLobby.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.account_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ResSearchAccountbyEidLobby.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSearchAccountbyEidLobby.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSearchAccountbyEidLobby";
        };

        return ResSearchAccountbyEidLobby;
    })();

    lq.ReqLoginBeat = (function() {

        function ReqLoginBeat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLoginBeat.prototype.contract = "";

        ReqLoginBeat.create = function create(properties) {
            return new ReqLoginBeat(properties);
        };

        ReqLoginBeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.contract != null && Object.hasOwnProperty.call(message, "contract"))
                writer.uint32(10).string(message.contract);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLoginBeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLoginBeat.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLoginBeat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.contract = value;
                        else
                            delete message.contract;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLoginBeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLoginBeat.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contract != null && message.hasOwnProperty("contract"))
                if (!$util.isString(message.contract))
                    return "contract: string expected";
            return null;
        };

        ReqLoginBeat.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLoginBeat)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLoginBeat();
            if (object.contract != null)
                if (typeof object.contract !== "string" || object.contract.length)
                    message.contract = String(object.contract);
            return message;
        };

        ReqLoginBeat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.contract = "";
            if (message.contract != null && message.hasOwnProperty("contract"))
                object.contract = message.contract;
            return object;
        };

        ReqLoginBeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLoginBeat.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLoginBeat";
        };

        return ReqLoginBeat;
    })();

    lq.ReqJoinMatchQueue = (function() {

        function ReqJoinMatchQueue(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqJoinMatchQueue.prototype.match_mode = 0;
        ReqJoinMatchQueue.prototype.client_version_string = "";

        ReqJoinMatchQueue.create = function create(properties) {
            return new ReqJoinMatchQueue(properties);
        };

        ReqJoinMatchQueue.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.match_mode != null && Object.hasOwnProperty.call(message, "match_mode"))
                writer.uint32(8).uint32(message.match_mode);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqJoinMatchQueue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqJoinMatchQueue.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqJoinMatchQueue(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_mode = value;
                        else
                            delete message.match_mode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqJoinMatchQueue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqJoinMatchQueue.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.match_mode != null && message.hasOwnProperty("match_mode"))
                if (!$util.isInteger(message.match_mode))
                    return "match_mode: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqJoinMatchQueue.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqJoinMatchQueue)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqJoinMatchQueue();
            if (object.match_mode != null)
                if (Number(object.match_mode) !== 0)
                    message.match_mode = object.match_mode >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqJoinMatchQueue.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.match_mode = 0;
                object.client_version_string = "";
            }
            if (message.match_mode != null && message.hasOwnProperty("match_mode"))
                object.match_mode = message.match_mode;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqJoinMatchQueue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqJoinMatchQueue.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqJoinMatchQueue";
        };

        return ReqJoinMatchQueue;
    })();

    lq.ReqCancelMatchQueue = (function() {

        function ReqCancelMatchQueue(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCancelMatchQueue.prototype.match_mode = 0;

        ReqCancelMatchQueue.create = function create(properties) {
            return new ReqCancelMatchQueue(properties);
        };

        ReqCancelMatchQueue.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.match_mode != null && Object.hasOwnProperty.call(message, "match_mode"))
                writer.uint32(8).uint32(message.match_mode);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCancelMatchQueue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCancelMatchQueue.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCancelMatchQueue(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_mode = value;
                        else
                            delete message.match_mode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCancelMatchQueue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCancelMatchQueue.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.match_mode != null && message.hasOwnProperty("match_mode"))
                if (!$util.isInteger(message.match_mode))
                    return "match_mode: integer expected";
            return null;
        };

        ReqCancelMatchQueue.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCancelMatchQueue)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCancelMatchQueue();
            if (object.match_mode != null)
                if (Number(object.match_mode) !== 0)
                    message.match_mode = object.match_mode >>> 0;
            return message;
        };

        ReqCancelMatchQueue.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.match_mode = 0;
            if (message.match_mode != null && message.hasOwnProperty("match_mode"))
                object.match_mode = message.match_mode;
            return object;
        };

        ReqCancelMatchQueue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCancelMatchQueue.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCancelMatchQueue";
        };

        return ReqCancelMatchQueue;
    })();

    lq.ReqAccountInfo = (function() {

        function ReqAccountInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAccountInfo.prototype.account_id = 0;

        ReqAccountInfo.create = function create(properties) {
            return new ReqAccountInfo(properties);
        };

        ReqAccountInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAccountInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAccountInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAccountInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAccountInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAccountInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqAccountInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAccountInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAccountInfo();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqAccountInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.account_id = 0;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqAccountInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAccountInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAccountInfo";
        };

        return ReqAccountInfo;
    })();

    lq.ResAccountInfo = (function() {

        function ResAccountInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountInfo.prototype.error = null;
        ResAccountInfo.prototype.account = null;
        ResAccountInfo.prototype.room = null;

        ResAccountInfo.create = function create(properties) {
            return new ResAccountInfo(properties);
        };

        ResAccountInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.account != null && Object.hasOwnProperty.call(message, "account"))
                $root.lq.Account.encode(message.account, writer.uint32(18).fork()).ldelim();
            if (message.room != null && Object.hasOwnProperty.call(message, "room"))
                $root.lq.Room.encode(message.room, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.account = $root.lq.Account.decode(reader, reader.uint32(), undefined, _depth + 1, message.account);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.lq.Room.decode(reader, reader.uint32(), undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.account != null && message.hasOwnProperty("account")) {
                let error = $root.lq.Account.verify(message.account, _depth + 1);
                if (error)
                    return "account." + error;
            }
            if (message.room != null && message.hasOwnProperty("room")) {
                let error = $root.lq.Room.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        ResAccountInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.account != null) {
                if (typeof object.account !== "object")
                    throw TypeError(".lq.ResAccountInfo.account: object expected");
                message.account = $root.lq.Account.fromObject(object.account, _depth + 1);
            }
            if (object.room != null) {
                if (typeof object.room !== "object")
                    throw TypeError(".lq.ResAccountInfo.room: object expected");
                message.room = $root.lq.Room.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        ResAccountInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.account = null;
                object.room = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.account != null && message.hasOwnProperty("account"))
                object.account = $root.lq.Account.toObject(message.account, options);
            if (message.room != null && message.hasOwnProperty("room"))
                object.room = $root.lq.Room.toObject(message.room, options);
            return object;
        };

        ResAccountInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountInfo";
        };

        return ResAccountInfo;
    })();

    lq.ReqCreateNickname = (function() {

        function ReqCreateNickname(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateNickname.prototype.nickname = "";
        ReqCreateNickname.prototype.advertise_str = "";
        ReqCreateNickname.prototype.tag = "";

        ReqCreateNickname.create = function create(properties) {
            return new ReqCreateNickname(properties);
        };

        ReqCreateNickname.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(10).string(message.nickname);
            if (message.advertise_str != null && Object.hasOwnProperty.call(message, "advertise_str"))
                writer.uint32(18).string(message.advertise_str);
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(26).string(message.tag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateNickname.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateNickname.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateNickname(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.advertise_str = value;
                        else
                            delete message.advertise_str;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateNickname.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateNickname.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.advertise_str != null && message.hasOwnProperty("advertise_str"))
                if (!$util.isString(message.advertise_str))
                    return "advertise_str: string expected";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isString(message.tag))
                    return "tag: string expected";
            return null;
        };

        ReqCreateNickname.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateNickname)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateNickname();
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.advertise_str != null)
                if (typeof object.advertise_str !== "string" || object.advertise_str.length)
                    message.advertise_str = String(object.advertise_str);
            if (object.tag != null)
                if (typeof object.tag !== "string" || object.tag.length)
                    message.tag = String(object.tag);
            return message;
        };

        ReqCreateNickname.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.nickname = "";
                object.advertise_str = "";
                object.tag = "";
            }
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.advertise_str != null && message.hasOwnProperty("advertise_str"))
                object.advertise_str = message.advertise_str;
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            return object;
        };

        ReqCreateNickname.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateNickname.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateNickname";
        };

        return ReqCreateNickname;
    })();

    lq.ReqModifyNickname = (function() {

        function ReqModifyNickname(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqModifyNickname.prototype.nickname = "";
        ReqModifyNickname.prototype.use_item_id = 0;

        ReqModifyNickname.create = function create(properties) {
            return new ReqModifyNickname(properties);
        };

        ReqModifyNickname.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                writer.uint32(10).string(message.nickname);
            if (message.use_item_id != null && Object.hasOwnProperty.call(message, "use_item_id"))
                writer.uint32(16).uint32(message.use_item_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqModifyNickname.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqModifyNickname.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqModifyNickname(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nickname = value;
                        else
                            delete message.nickname;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.use_item_id = value;
                        else
                            delete message.use_item_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqModifyNickname.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqModifyNickname.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                if (!$util.isString(message.nickname))
                    return "nickname: string expected";
            if (message.use_item_id != null && message.hasOwnProperty("use_item_id"))
                if (!$util.isInteger(message.use_item_id))
                    return "use_item_id: integer expected";
            return null;
        };

        ReqModifyNickname.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqModifyNickname)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqModifyNickname();
            if (object.nickname != null)
                if (typeof object.nickname !== "string" || object.nickname.length)
                    message.nickname = String(object.nickname);
            if (object.use_item_id != null)
                if (Number(object.use_item_id) !== 0)
                    message.use_item_id = object.use_item_id >>> 0;
            return message;
        };

        ReqModifyNickname.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.nickname = "";
                object.use_item_id = 0;
            }
            if (message.nickname != null && message.hasOwnProperty("nickname"))
                object.nickname = message.nickname;
            if (message.use_item_id != null && message.hasOwnProperty("use_item_id"))
                object.use_item_id = message.use_item_id;
            return object;
        };

        ReqModifyNickname.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqModifyNickname.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqModifyNickname";
        };

        return ReqModifyNickname;
    })();

    lq.ReqModifyBirthday = (function() {

        function ReqModifyBirthday(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqModifyBirthday.prototype.birthday = 0;

        ReqModifyBirthday.create = function create(properties) {
            return new ReqModifyBirthday(properties);
        };

        ReqModifyBirthday.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.birthday != null && Object.hasOwnProperty.call(message, "birthday"))
                writer.uint32(8).int32(message.birthday);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqModifyBirthday.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqModifyBirthday.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqModifyBirthday(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.birthday = value;
                        else
                            delete message.birthday;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqModifyBirthday.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqModifyBirthday.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.birthday != null && message.hasOwnProperty("birthday"))
                if (!$util.isInteger(message.birthday))
                    return "birthday: integer expected";
            return null;
        };

        ReqModifyBirthday.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqModifyBirthday)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqModifyBirthday();
            if (object.birthday != null)
                if (Number(object.birthday) !== 0)
                    message.birthday = object.birthday | 0;
            return message;
        };

        ReqModifyBirthday.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.birthday = 0;
            if (message.birthday != null && message.hasOwnProperty("birthday"))
                object.birthday = message.birthday;
            return object;
        };

        ReqModifyBirthday.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqModifyBirthday.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqModifyBirthday";
        };

        return ReqModifyBirthday;
    })();

    lq.ResSelfRoom = (function() {

        function ResSelfRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSelfRoom.prototype.error = null;
        ResSelfRoom.prototype.room = null;

        ResSelfRoom.create = function create(properties) {
            return new ResSelfRoom(properties);
        };

        ResSelfRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.room != null && Object.hasOwnProperty.call(message, "room"))
                $root.lq.Room.encode(message.room, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSelfRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSelfRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSelfRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.lq.Room.decode(reader, reader.uint32(), undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSelfRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSelfRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.room != null && message.hasOwnProperty("room")) {
                let error = $root.lq.Room.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        ResSelfRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSelfRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSelfRoom();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSelfRoom.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.room != null) {
                if (typeof object.room !== "object")
                    throw TypeError(".lq.ResSelfRoom.room: object expected");
                message.room = $root.lq.Room.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        ResSelfRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.room = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.room != null && message.hasOwnProperty("room"))
                object.room = $root.lq.Room.toObject(message.room, options);
            return object;
        };

        ResSelfRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSelfRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSelfRoom";
        };

        return ResSelfRoom;
    })();

    lq.ResFetchGamingInfo = (function() {

        function ResFetchGamingInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchGamingInfo.prototype.error = null;
        ResFetchGamingInfo.prototype.game_info = null;

        ResFetchGamingInfo.create = function create(properties) {
            return new ResFetchGamingInfo(properties);
        };

        ResFetchGamingInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.game_info != null && Object.hasOwnProperty.call(message, "game_info"))
                $root.lq.GameConnectInfo.encode(message.game_info, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchGamingInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchGamingInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchGamingInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.game_info = $root.lq.GameConnectInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchGamingInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchGamingInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.game_info != null && message.hasOwnProperty("game_info")) {
                let error = $root.lq.GameConnectInfo.verify(message.game_info, _depth + 1);
                if (error)
                    return "game_info." + error;
            }
            return null;
        };

        ResFetchGamingInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchGamingInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchGamingInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchGamingInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.game_info != null) {
                if (typeof object.game_info !== "object")
                    throw TypeError(".lq.ResFetchGamingInfo.game_info: object expected");
                message.game_info = $root.lq.GameConnectInfo.fromObject(object.game_info, _depth + 1);
            }
            return message;
        };

        ResFetchGamingInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.game_info = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.game_info != null && message.hasOwnProperty("game_info"))
                object.game_info = $root.lq.GameConnectInfo.toObject(message.game_info, options);
            return object;
        };

        ResFetchGamingInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchGamingInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchGamingInfo";
        };

        return ResFetchGamingInfo;
    })();

    lq.ReqCreateRoom = (function() {

        function ReqCreateRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateRoom.prototype.player_count = 0;
        ReqCreateRoom.prototype.mode = null;
        ReqCreateRoom.prototype.public_live = false;
        ReqCreateRoom.prototype.client_version_string = "";
        ReqCreateRoom.prototype.pre_rule = "";

        ReqCreateRoom.create = function create(properties) {
            return new ReqCreateRoom(properties);
        };

        ReqCreateRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.player_count != null && Object.hasOwnProperty.call(message, "player_count"))
                writer.uint32(8).uint32(message.player_count);
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                $root.lq.GameMode.encode(message.mode, writer.uint32(18).fork()).ldelim();
            if (message.public_live != null && Object.hasOwnProperty.call(message, "public_live"))
                writer.uint32(24).bool(message.public_live);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(34).string(message.client_version_string);
            if (message.pre_rule != null && Object.hasOwnProperty.call(message, "pre_rule"))
                writer.uint32(42).string(message.pre_rule);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.player_count = value;
                        else
                            delete message.player_count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.mode = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.mode);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.public_live = value;
                        else
                            delete message.public_live;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.pre_rule = value;
                        else
                            delete message.pre_rule;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.player_count != null && message.hasOwnProperty("player_count"))
                if (!$util.isInteger(message.player_count))
                    return "player_count: integer expected";
            if (message.mode != null && message.hasOwnProperty("mode")) {
                let error = $root.lq.GameMode.verify(message.mode, _depth + 1);
                if (error)
                    return "mode." + error;
            }
            if (message.public_live != null && message.hasOwnProperty("public_live"))
                if (typeof message.public_live !== "boolean")
                    return "public_live: boolean expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.pre_rule != null && message.hasOwnProperty("pre_rule"))
                if (!$util.isString(message.pre_rule))
                    return "pre_rule: string expected";
            return null;
        };

        ReqCreateRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateRoom();
            if (object.player_count != null)
                if (Number(object.player_count) !== 0)
                    message.player_count = object.player_count >>> 0;
            if (object.mode != null) {
                if (typeof object.mode !== "object")
                    throw TypeError(".lq.ReqCreateRoom.mode: object expected");
                message.mode = $root.lq.GameMode.fromObject(object.mode, _depth + 1);
            }
            if (object.public_live != null)
                if (object.public_live)
                    message.public_live = Boolean(object.public_live);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.pre_rule != null)
                if (typeof object.pre_rule !== "string" || object.pre_rule.length)
                    message.pre_rule = String(object.pre_rule);
            return message;
        };

        ReqCreateRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.player_count = 0;
                object.mode = null;
                object.public_live = false;
                object.client_version_string = "";
                object.pre_rule = "";
            }
            if (message.player_count != null && message.hasOwnProperty("player_count"))
                object.player_count = message.player_count;
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = $root.lq.GameMode.toObject(message.mode, options);
            if (message.public_live != null && message.hasOwnProperty("public_live"))
                object.public_live = message.public_live;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.pre_rule != null && message.hasOwnProperty("pre_rule"))
                object.pre_rule = message.pre_rule;
            return object;
        };

        ReqCreateRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateRoom";
        };

        return ReqCreateRoom;
    })();

    lq.ResCreateRoom = (function() {

        function ResCreateRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateRoom.prototype.error = null;
        ResCreateRoom.prototype.room = null;

        ResCreateRoom.create = function create(properties) {
            return new ResCreateRoom(properties);
        };

        ResCreateRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.room != null && Object.hasOwnProperty.call(message, "room"))
                $root.lq.Room.encode(message.room, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.lq.Room.decode(reader, reader.uint32(), undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.room != null && message.hasOwnProperty("room")) {
                let error = $root.lq.Room.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        ResCreateRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateRoom();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateRoom.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.room != null) {
                if (typeof object.room !== "object")
                    throw TypeError(".lq.ResCreateRoom.room: object expected");
                message.room = $root.lq.Room.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        ResCreateRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.room = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.room != null && message.hasOwnProperty("room"))
                object.room = $root.lq.Room.toObject(message.room, options);
            return object;
        };

        ResCreateRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateRoom";
        };

        return ResCreateRoom;
    })();

    lq.ReqJoinRoom = (function() {

        function ReqJoinRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqJoinRoom.prototype.room_id = 0;
        ReqJoinRoom.prototype.client_version_string = "";

        ReqJoinRoom.create = function create(properties) {
            return new ReqJoinRoom(properties);
        };

        ReqJoinRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.room_id != null && Object.hasOwnProperty.call(message, "room_id"))
                writer.uint32(8).uint32(message.room_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqJoinRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqJoinRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqJoinRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.room_id = value;
                        else
                            delete message.room_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqJoinRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqJoinRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                if (!$util.isInteger(message.room_id))
                    return "room_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqJoinRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqJoinRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqJoinRoom();
            if (object.room_id != null)
                if (Number(object.room_id) !== 0)
                    message.room_id = object.room_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqJoinRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.room_id = 0;
                object.client_version_string = "";
            }
            if (message.room_id != null && message.hasOwnProperty("room_id"))
                object.room_id = message.room_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqJoinRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqJoinRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqJoinRoom";
        };

        return ReqJoinRoom;
    })();

    lq.ResJoinRoom = (function() {

        function ResJoinRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResJoinRoom.prototype.error = null;
        ResJoinRoom.prototype.room = null;

        ResJoinRoom.create = function create(properties) {
            return new ResJoinRoom(properties);
        };

        ResJoinRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.room != null && Object.hasOwnProperty.call(message, "room"))
                $root.lq.Room.encode(message.room, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResJoinRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResJoinRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResJoinRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.room = $root.lq.Room.decode(reader, reader.uint32(), undefined, _depth + 1, message.room);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResJoinRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResJoinRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.room != null && message.hasOwnProperty("room")) {
                let error = $root.lq.Room.verify(message.room, _depth + 1);
                if (error)
                    return "room." + error;
            }
            return null;
        };

        ResJoinRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResJoinRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResJoinRoom();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResJoinRoom.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.room != null) {
                if (typeof object.room !== "object")
                    throw TypeError(".lq.ResJoinRoom.room: object expected");
                message.room = $root.lq.Room.fromObject(object.room, _depth + 1);
            }
            return message;
        };

        ResJoinRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.room = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.room != null && message.hasOwnProperty("room"))
                object.room = $root.lq.Room.toObject(message.room, options);
            return object;
        };

        ResJoinRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResJoinRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResJoinRoom";
        };

        return ResJoinRoom;
    })();

    lq.ReqRoomReady = (function() {

        function ReqRoomReady(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRoomReady.prototype.ready = false;

        ReqRoomReady.create = function create(properties) {
            return new ReqRoomReady(properties);
        };

        ReqRoomReady.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ready != null && Object.hasOwnProperty.call(message, "ready"))
                writer.uint32(8).bool(message.ready);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRoomReady.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRoomReady.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRoomReady(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ready = value;
                        else
                            delete message.ready;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRoomReady.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRoomReady.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ready != null && message.hasOwnProperty("ready"))
                if (typeof message.ready !== "boolean")
                    return "ready: boolean expected";
            return null;
        };

        ReqRoomReady.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRoomReady)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRoomReady();
            if (object.ready != null)
                if (object.ready)
                    message.ready = Boolean(object.ready);
            return message;
        };

        ReqRoomReady.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.ready = false;
            if (message.ready != null && message.hasOwnProperty("ready"))
                object.ready = message.ready;
            return object;
        };

        ReqRoomReady.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRoomReady.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRoomReady";
        };

        return ReqRoomReady;
    })();

    lq.ReqRoomDressing = (function() {

        function ReqRoomDressing(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRoomDressing.prototype.dressing = false;

        ReqRoomDressing.create = function create(properties) {
            return new ReqRoomDressing(properties);
        };

        ReqRoomDressing.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dressing != null && Object.hasOwnProperty.call(message, "dressing"))
                writer.uint32(8).bool(message.dressing);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRoomDressing.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRoomDressing.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRoomDressing(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dressing = value;
                        else
                            delete message.dressing;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRoomDressing.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRoomDressing.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dressing != null && message.hasOwnProperty("dressing"))
                if (typeof message.dressing !== "boolean")
                    return "dressing: boolean expected";
            return null;
        };

        ReqRoomDressing.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRoomDressing)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRoomDressing();
            if (object.dressing != null)
                if (object.dressing)
                    message.dressing = Boolean(object.dressing);
            return message;
        };

        ReqRoomDressing.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.dressing = false;
            if (message.dressing != null && message.hasOwnProperty("dressing"))
                object.dressing = message.dressing;
            return object;
        };

        ReqRoomDressing.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRoomDressing.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRoomDressing";
        };

        return ReqRoomDressing;
    })();

    lq.ReqRoomStart = (function() {

        function ReqRoomStart(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRoomStart.create = function create(properties) {
            return new ReqRoomStart(properties);
        };

        ReqRoomStart.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRoomStart.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRoomStart.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRoomStart();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRoomStart.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRoomStart.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        ReqRoomStart.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRoomStart)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.ReqRoomStart();
        };

        ReqRoomStart.toObject = function toObject() {
            return {};
        };

        ReqRoomStart.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRoomStart.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRoomStart";
        };

        return ReqRoomStart;
    })();

    lq.ReqRoomKickPlayer = (function() {

        function ReqRoomKickPlayer(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRoomKickPlayer.prototype.id = 0;

        ReqRoomKickPlayer.create = function create(properties) {
            return new ReqRoomKickPlayer(properties);
        };

        ReqRoomKickPlayer.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRoomKickPlayer.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRoomKickPlayer.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRoomKickPlayer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRoomKickPlayer.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRoomKickPlayer.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqRoomKickPlayer.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRoomKickPlayer)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRoomKickPlayer();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqRoomKickPlayer.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqRoomKickPlayer.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRoomKickPlayer.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRoomKickPlayer";
        };

        return ReqRoomKickPlayer;
    })();

    lq.ReqModifyRoom = (function() {

        function ReqModifyRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqModifyRoom.prototype.robot_count = 0;

        ReqModifyRoom.create = function create(properties) {
            return new ReqModifyRoom(properties);
        };

        ReqModifyRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.robot_count != null && Object.hasOwnProperty.call(message, "robot_count"))
                writer.uint32(8).uint32(message.robot_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqModifyRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqModifyRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqModifyRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.robot_count = value;
                        else
                            delete message.robot_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqModifyRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqModifyRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                if (!$util.isInteger(message.robot_count))
                    return "robot_count: integer expected";
            return null;
        };

        ReqModifyRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqModifyRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqModifyRoom();
            if (object.robot_count != null)
                if (Number(object.robot_count) !== 0)
                    message.robot_count = object.robot_count >>> 0;
            return message;
        };

        ReqModifyRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.robot_count = 0;
            if (message.robot_count != null && message.hasOwnProperty("robot_count"))
                object.robot_count = message.robot_count;
            return object;
        };

        ReqModifyRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqModifyRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqModifyRoom";
        };

        return ReqModifyRoom;
    })();

    lq.ReqAddRoomRobot = (function() {

        function ReqAddRoomRobot(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAddRoomRobot.prototype.position = 0;

        ReqAddRoomRobot.create = function create(properties) {
            return new ReqAddRoomRobot(properties);
        };

        ReqAddRoomRobot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                writer.uint32(8).uint32(message.position);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAddRoomRobot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAddRoomRobot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAddRoomRobot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.position = value;
                        else
                            delete message.position;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAddRoomRobot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAddRoomRobot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.position != null && message.hasOwnProperty("position"))
                if (!$util.isInteger(message.position))
                    return "position: integer expected";
            return null;
        };

        ReqAddRoomRobot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAddRoomRobot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAddRoomRobot();
            if (object.position != null)
                if (Number(object.position) !== 0)
                    message.position = object.position >>> 0;
            return message;
        };

        ReqAddRoomRobot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.position = 0;
            if (message.position != null && message.hasOwnProperty("position"))
                object.position = message.position;
            return object;
        };

        ReqAddRoomRobot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAddRoomRobot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAddRoomRobot";
        };

        return ReqAddRoomRobot;
    })();

    lq.ReqChangeAvatar = (function() {

        function ReqChangeAvatar(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeAvatar.prototype.avatar_id = 0;

        ReqChangeAvatar.create = function create(properties) {
            return new ReqChangeAvatar(properties);
        };

        ReqChangeAvatar.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.avatar_id != null && Object.hasOwnProperty.call(message, "avatar_id"))
                writer.uint32(8).uint32(message.avatar_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeAvatar.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeAvatar.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeAvatar(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.avatar_id = value;
                        else
                            delete message.avatar_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeAvatar.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeAvatar.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                if (!$util.isInteger(message.avatar_id))
                    return "avatar_id: integer expected";
            return null;
        };

        ReqChangeAvatar.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeAvatar)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeAvatar();
            if (object.avatar_id != null)
                if (Number(object.avatar_id) !== 0)
                    message.avatar_id = object.avatar_id >>> 0;
            return message;
        };

        ReqChangeAvatar.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.avatar_id = 0;
            if (message.avatar_id != null && message.hasOwnProperty("avatar_id"))
                object.avatar_id = message.avatar_id;
            return object;
        };

        ReqChangeAvatar.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeAvatar.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeAvatar";
        };

        return ReqChangeAvatar;
    })();

    lq.ReqAccountStatisticInfo = (function() {

        function ReqAccountStatisticInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAccountStatisticInfo.prototype.account_id = 0;

        ReqAccountStatisticInfo.create = function create(properties) {
            return new ReqAccountStatisticInfo(properties);
        };

        ReqAccountStatisticInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAccountStatisticInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAccountStatisticInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAccountStatisticInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAccountStatisticInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAccountStatisticInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqAccountStatisticInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAccountStatisticInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAccountStatisticInfo();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqAccountStatisticInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.account_id = 0;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqAccountStatisticInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAccountStatisticInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAccountStatisticInfo";
        };

        return ReqAccountStatisticInfo;
    })();

    lq.ResAccountStatisticInfo = (function() {

        function ResAccountStatisticInfo(properties) {
            this.statistic_data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountStatisticInfo.prototype.error = null;
        ResAccountStatisticInfo.prototype.statistic_data = $util.emptyArray;
        ResAccountStatisticInfo.prototype.detail_data = null;

        ResAccountStatisticInfo.create = function create(properties) {
            return new ResAccountStatisticInfo(properties);
        };

        ResAccountStatisticInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.statistic_data != null && message.statistic_data.length)
                for (let i = 0; i < message.statistic_data.length; ++i)
                    $root.lq.AccountStatisticData.encode(message.statistic_data[i], writer.uint32(18).fork()).ldelim();
            if (message.detail_data != null && Object.hasOwnProperty.call(message, "detail_data"))
                $root.lq.AccountDetailStatisticV2.encode(message.detail_data, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountStatisticInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountStatisticInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountStatisticInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.statistic_data && message.statistic_data.length))
                            message.statistic_data = [];
                        message.statistic_data.push($root.lq.AccountStatisticData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.detail_data = $root.lq.AccountDetailStatisticV2.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail_data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountStatisticInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountStatisticInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.statistic_data != null && message.hasOwnProperty("statistic_data")) {
                if (!Array.isArray(message.statistic_data))
                    return "statistic_data: array expected";
                for (let i = 0; i < message.statistic_data.length; ++i) {
                    let error = $root.lq.AccountStatisticData.verify(message.statistic_data[i], _depth + 1);
                    if (error)
                        return "statistic_data." + error;
                }
            }
            if (message.detail_data != null && message.hasOwnProperty("detail_data")) {
                let error = $root.lq.AccountDetailStatisticV2.verify(message.detail_data, _depth + 1);
                if (error)
                    return "detail_data." + error;
            }
            return null;
        };

        ResAccountStatisticInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountStatisticInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountStatisticInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountStatisticInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.statistic_data) {
                if (!Array.isArray(object.statistic_data))
                    throw TypeError(".lq.ResAccountStatisticInfo.statistic_data: array expected");
                message.statistic_data = Array(object.statistic_data.length);
                for (let i = 0; i < object.statistic_data.length; ++i) {
                    if (typeof object.statistic_data[i] !== "object")
                        throw TypeError(".lq.ResAccountStatisticInfo.statistic_data: object expected");
                    message.statistic_data[i] = $root.lq.AccountStatisticData.fromObject(object.statistic_data[i], _depth + 1);
                }
            }
            if (object.detail_data != null) {
                if (typeof object.detail_data !== "object")
                    throw TypeError(".lq.ResAccountStatisticInfo.detail_data: object expected");
                message.detail_data = $root.lq.AccountDetailStatisticV2.fromObject(object.detail_data, _depth + 1);
            }
            return message;
        };

        ResAccountStatisticInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.statistic_data = [];
            if (options.defaults) {
                object.error = null;
                object.detail_data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.statistic_data && message.statistic_data.length) {
                object.statistic_data = Array(message.statistic_data.length);
                for (let j = 0; j < message.statistic_data.length; ++j)
                    object.statistic_data[j] = $root.lq.AccountStatisticData.toObject(message.statistic_data[j], options);
            }
            if (message.detail_data != null && message.hasOwnProperty("detail_data"))
                object.detail_data = $root.lq.AccountDetailStatisticV2.toObject(message.detail_data, options);
            return object;
        };

        ResAccountStatisticInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountStatisticInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountStatisticInfo";
        };

        return ResAccountStatisticInfo;
    })();

    lq.ResAccountChallengeRankInfo = (function() {

        function ResAccountChallengeRankInfo(properties) {
            this.season_info = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountChallengeRankInfo.prototype.error = null;
        ResAccountChallengeRankInfo.prototype.season_info = $util.emptyArray;

        ResAccountChallengeRankInfo.create = function create(properties) {
            return new ResAccountChallengeRankInfo(properties);
        };

        ResAccountChallengeRankInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.season_info != null && message.season_info.length)
                for (let i = 0; i < message.season_info.length; ++i)
                    $root.lq.ResAccountChallengeRankInfo.ChallengeRank.encode(message.season_info[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountChallengeRankInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountChallengeRankInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountChallengeRankInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.season_info && message.season_info.length))
                            message.season_info = [];
                        message.season_info.push($root.lq.ResAccountChallengeRankInfo.ChallengeRank.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountChallengeRankInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountChallengeRankInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.season_info != null && message.hasOwnProperty("season_info")) {
                if (!Array.isArray(message.season_info))
                    return "season_info: array expected";
                for (let i = 0; i < message.season_info.length; ++i) {
                    let error = $root.lq.ResAccountChallengeRankInfo.ChallengeRank.verify(message.season_info[i], _depth + 1);
                    if (error)
                        return "season_info." + error;
                }
            }
            return null;
        };

        ResAccountChallengeRankInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountChallengeRankInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountChallengeRankInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountChallengeRankInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.season_info) {
                if (!Array.isArray(object.season_info))
                    throw TypeError(".lq.ResAccountChallengeRankInfo.season_info: array expected");
                message.season_info = Array(object.season_info.length);
                for (let i = 0; i < object.season_info.length; ++i) {
                    if (typeof object.season_info[i] !== "object")
                        throw TypeError(".lq.ResAccountChallengeRankInfo.season_info: object expected");
                    message.season_info[i] = $root.lq.ResAccountChallengeRankInfo.ChallengeRank.fromObject(object.season_info[i], _depth + 1);
                }
            }
            return message;
        };

        ResAccountChallengeRankInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.season_info = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.season_info && message.season_info.length) {
                object.season_info = Array(message.season_info.length);
                for (let j = 0; j < message.season_info.length; ++j)
                    object.season_info[j] = $root.lq.ResAccountChallengeRankInfo.ChallengeRank.toObject(message.season_info[j], options);
            }
            return object;
        };

        ResAccountChallengeRankInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountChallengeRankInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountChallengeRankInfo";
        };

        ResAccountChallengeRankInfo.ChallengeRank = (function() {

            function ChallengeRank(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChallengeRank.prototype.season = 0;
            ChallengeRank.prototype.rank = 0;
            ChallengeRank.prototype.level = 0;

            ChallengeRank.create = function create(properties) {
                return new ChallengeRank(properties);
            };

            ChallengeRank.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                    writer.uint32(8).uint32(message.season);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(16).uint32(message.rank);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(24).uint32(message.level);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChallengeRank.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChallengeRank.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountChallengeRankInfo.ChallengeRank(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.season = value;
                            else
                                delete message.season;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChallengeRank.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChallengeRank.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.season != null && message.hasOwnProperty("season"))
                    if (!$util.isInteger(message.season))
                        return "season: integer expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                return null;
            };

            ChallengeRank.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountChallengeRankInfo.ChallengeRank)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountChallengeRankInfo.ChallengeRank();
                if (object.season != null)
                    if (Number(object.season) !== 0)
                        message.season = object.season >>> 0;
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                return message;
            };

            ChallengeRank.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.season = 0;
                    object.rank = 0;
                    object.level = 0;
                }
                if (message.season != null && message.hasOwnProperty("season"))
                    object.season = message.season;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                return object;
            };

            ChallengeRank.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChallengeRank.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountChallengeRankInfo.ChallengeRank";
            };

            return ChallengeRank;
        })();

        return ResAccountChallengeRankInfo;
    })();

    lq.ResAccountCharacterInfo = (function() {

        function ResAccountCharacterInfo(properties) {
            this.unlock_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountCharacterInfo.prototype.error = null;
        ResAccountCharacterInfo.prototype.unlock_list = $util.emptyArray;

        ResAccountCharacterInfo.create = function create(properties) {
            return new ResAccountCharacterInfo(properties);
        };

        ResAccountCharacterInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unlock_list != null && message.unlock_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.unlock_list.length; ++i)
                    writer.uint32(message.unlock_list[i]);
                writer.ldelim();
            }
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountCharacterInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountCharacterInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountCharacterInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType === 2) {
                            if (!(message.unlock_list && message.unlock_list.length))
                                message.unlock_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.unlock_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.unlock_list && message.unlock_list.length))
                            message.unlock_list = [];
                        message.unlock_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountCharacterInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountCharacterInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.unlock_list != null && message.hasOwnProperty("unlock_list")) {
                if (!Array.isArray(message.unlock_list))
                    return "unlock_list: array expected";
                for (let i = 0; i < message.unlock_list.length; ++i)
                    if (!$util.isInteger(message.unlock_list[i]))
                        return "unlock_list: integer[] expected";
            }
            return null;
        };

        ResAccountCharacterInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountCharacterInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountCharacterInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountCharacterInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.unlock_list) {
                if (!Array.isArray(object.unlock_list))
                    throw TypeError(".lq.ResAccountCharacterInfo.unlock_list: array expected");
                message.unlock_list = Array(object.unlock_list.length);
                for (let i = 0; i < object.unlock_list.length; ++i)
                    message.unlock_list[i] = object.unlock_list[i] >>> 0;
            }
            return message;
        };

        ResAccountCharacterInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.unlock_list = [];
            if (options.defaults)
                object.error = null;
            if (message.unlock_list && message.unlock_list.length) {
                object.unlock_list = Array(message.unlock_list.length);
                for (let j = 0; j < message.unlock_list.length; ++j)
                    object.unlock_list[j] = message.unlock_list[j];
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResAccountCharacterInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountCharacterInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountCharacterInfo";
        };

        return ResAccountCharacterInfo;
    })();

    lq.ReqShopPurchase = (function() {

        function ReqShopPurchase(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqShopPurchase.prototype.type = "";
        ReqShopPurchase.prototype.id = 0;

        ReqShopPurchase.create = function create(properties) {
            return new ReqShopPurchase(properties);
        };

        ReqShopPurchase.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(10).string(message.type);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqShopPurchase.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqShopPurchase.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqShopPurchase(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqShopPurchase.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqShopPurchase.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqShopPurchase.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqShopPurchase)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqShopPurchase();
            if (object.type != null)
                if (typeof object.type !== "string" || object.type.length)
                    message.type = String(object.type);
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqShopPurchase.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = "";
                object.id = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqShopPurchase.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqShopPurchase.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqShopPurchase";
        };

        return ReqShopPurchase;
    })();

    lq.ResShopPurchase = (function() {

        function ResShopPurchase(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResShopPurchase.prototype.error = null;
        ResShopPurchase.prototype.update = null;

        ResShopPurchase.create = function create(properties) {
            return new ResShopPurchase(properties);
        };

        ResShopPurchase.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.update != null && Object.hasOwnProperty.call(message, "update"))
                $root.lq.AccountUpdate.encode(message.update, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResShopPurchase.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResShopPurchase.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResShopPurchase(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.update = $root.lq.AccountUpdate.decode(reader, reader.uint32(), undefined, _depth + 1, message.update);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResShopPurchase.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResShopPurchase.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.update != null && message.hasOwnProperty("update")) {
                let error = $root.lq.AccountUpdate.verify(message.update, _depth + 1);
                if (error)
                    return "update." + error;
            }
            return null;
        };

        ResShopPurchase.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResShopPurchase)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResShopPurchase();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResShopPurchase.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.update != null) {
                if (typeof object.update !== "object")
                    throw TypeError(".lq.ResShopPurchase.update: object expected");
                message.update = $root.lq.AccountUpdate.fromObject(object.update, _depth + 1);
            }
            return message;
        };

        ResShopPurchase.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.update = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.update != null && message.hasOwnProperty("update"))
                object.update = $root.lq.AccountUpdate.toObject(message.update, options);
            return object;
        };

        ResShopPurchase.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResShopPurchase.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResShopPurchase";
        };

        return ResShopPurchase;
    })();

    lq.ReqGameRecord = (function() {

        function ReqGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameRecord.prototype.game_uuid = "";
        ReqGameRecord.prototype.client_version_string = "";

        ReqGameRecord.create = function create(properties) {
            return new ReqGameRecord(properties);
        };

        ReqGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameRecord();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_uuid = "";
                object.client_version_string = "";
            }
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameRecord";
        };

        return ReqGameRecord;
    })();

    lq.ResGameRecord = (function() {

        function ResGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameRecord.prototype.error = null;
        ResGameRecord.prototype.head = null;
        ResGameRecord.prototype.data = $util.newBuffer([]);
        ResGameRecord.prototype.data_url = "";

        ResGameRecord.create = function create(properties) {
            return new ResGameRecord(properties);
        };

        ResGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.head != null && Object.hasOwnProperty.call(message, "head"))
                $root.lq.RecordGame.encode(message.head, writer.uint32(26).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(34).bytes(message.data);
            if (message.data_url != null && Object.hasOwnProperty.call(message, "data_url"))
                writer.uint32(42).string(message.data_url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.head = $root.lq.RecordGame.decode(reader, reader.uint32(), undefined, _depth + 1, message.head);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.data_url = value;
                        else
                            delete message.data_url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.head != null && message.hasOwnProperty("head")) {
                let error = $root.lq.RecordGame.verify(message.head, _depth + 1);
                if (error)
                    return "head." + error;
            }
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            if (message.data_url != null && message.hasOwnProperty("data_url"))
                if (!$util.isString(message.data_url))
                    return "data_url: string expected";
            return null;
        };

        ResGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameRecord();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameRecord.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.head != null) {
                if (typeof object.head !== "object")
                    throw TypeError(".lq.ResGameRecord.head: object expected");
                message.head = $root.lq.RecordGame.fromObject(object.head, _depth + 1);
            }
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.data_url != null)
                if (typeof object.data_url !== "string" || object.data_url.length)
                    message.data_url = String(object.data_url);
            return message;
        };

        ResGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.head = null;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.data_url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.head != null && message.hasOwnProperty("head"))
                object.head = $root.lq.RecordGame.toObject(message.head, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.data_url != null && message.hasOwnProperty("data_url"))
                object.data_url = message.data_url;
            return object;
        };

        ResGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameRecord";
        };

        return ResGameRecord;
    })();

    lq.ReqGameRecordList = (function() {

        function ReqGameRecordList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameRecordList.prototype.start = 0;
        ReqGameRecordList.prototype.count = 0;
        ReqGameRecordList.prototype.type = 0;

        ReqGameRecordList.create = function create(properties) {
            return new ReqGameRecordList(properties);
        };

        ReqGameRecordList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.start != null && Object.hasOwnProperty.call(message, "start"))
                writer.uint32(8).uint32(message.start);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameRecordList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameRecordList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameRecordList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start = value;
                        else
                            delete message.start;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameRecordList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameRecordList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start != null && message.hasOwnProperty("start"))
                if (!$util.isInteger(message.start))
                    return "start: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqGameRecordList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameRecordList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameRecordList();
            if (object.start != null)
                if (Number(object.start) !== 0)
                    message.start = object.start >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqGameRecordList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.start = 0;
                object.count = 0;
                object.type = 0;
            }
            if (message.start != null && message.hasOwnProperty("start"))
                object.start = message.start;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqGameRecordList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameRecordList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameRecordList";
        };

        return ReqGameRecordList;
    })();

    lq.ResGameRecordList = (function() {

        function ResGameRecordList(properties) {
            this.record_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameRecordList.prototype.error = null;
        ResGameRecordList.prototype.total_count = 0;
        ResGameRecordList.prototype.record_list = $util.emptyArray;

        ResGameRecordList.create = function create(properties) {
            return new ResGameRecordList(properties);
        };

        ResGameRecordList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.total_count != null && Object.hasOwnProperty.call(message, "total_count"))
                writer.uint32(16).uint32(message.total_count);
            if (message.record_list != null && message.record_list.length)
                for (let i = 0; i < message.record_list.length; ++i)
                    $root.lq.RecordGame.encode(message.record_list[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameRecordList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameRecordList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameRecordList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_count = value;
                        else
                            delete message.total_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.record_list && message.record_list.length))
                            message.record_list = [];
                        message.record_list.push($root.lq.RecordGame.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameRecordList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameRecordList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.total_count != null && message.hasOwnProperty("total_count"))
                if (!$util.isInteger(message.total_count))
                    return "total_count: integer expected";
            if (message.record_list != null && message.hasOwnProperty("record_list")) {
                if (!Array.isArray(message.record_list))
                    return "record_list: array expected";
                for (let i = 0; i < message.record_list.length; ++i) {
                    let error = $root.lq.RecordGame.verify(message.record_list[i], _depth + 1);
                    if (error)
                        return "record_list." + error;
                }
            }
            return null;
        };

        ResGameRecordList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameRecordList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameRecordList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameRecordList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.total_count != null)
                if (Number(object.total_count) !== 0)
                    message.total_count = object.total_count >>> 0;
            if (object.record_list) {
                if (!Array.isArray(object.record_list))
                    throw TypeError(".lq.ResGameRecordList.record_list: array expected");
                message.record_list = Array(object.record_list.length);
                for (let i = 0; i < object.record_list.length; ++i) {
                    if (typeof object.record_list[i] !== "object")
                        throw TypeError(".lq.ResGameRecordList.record_list: object expected");
                    message.record_list[i] = $root.lq.RecordGame.fromObject(object.record_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResGameRecordList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.record_list = [];
            if (options.defaults) {
                object.error = null;
                object.total_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.total_count != null && message.hasOwnProperty("total_count"))
                object.total_count = message.total_count;
            if (message.record_list && message.record_list.length) {
                object.record_list = Array(message.record_list.length);
                for (let j = 0; j < message.record_list.length; ++j)
                    object.record_list[j] = $root.lq.RecordGame.toObject(message.record_list[j], options);
            }
            return object;
        };

        ResGameRecordList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameRecordList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameRecordList";
        };

        return ResGameRecordList;
    })();

    lq.ReqGameRecordListV2 = (function() {

        function ReqGameRecordListV2(properties) {
            this.ranks = [];
            this.modes = [];
            this.level_mode = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameRecordListV2.prototype.tag = 0;
        ReqGameRecordListV2.prototype.begin_time = 0;
        ReqGameRecordListV2.prototype.end_time = 0;
        ReqGameRecordListV2.prototype.ranks = $util.emptyArray;
        ReqGameRecordListV2.prototype.modes = $util.emptyArray;
        ReqGameRecordListV2.prototype.max_hu_type = 0;
        ReqGameRecordListV2.prototype.level_mode = $util.emptyArray;

        ReqGameRecordListV2.create = function create(properties) {
            return new ReqGameRecordListV2(properties);
        };

        ReqGameRecordListV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                writer.uint32(8).uint32(message.tag);
            if (message.begin_time != null && Object.hasOwnProperty.call(message, "begin_time"))
                writer.uint32(16).uint32(message.begin_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(24).uint32(message.end_time);
            if (message.ranks != null && message.ranks.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.ranks.length; ++i)
                    writer.uint32(message.ranks[i]);
                writer.ldelim();
            }
            if (message.modes != null && message.modes.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.modes.length; ++i)
                    writer.uint32(message.modes[i]);
                writer.ldelim();
            }
            if (message.max_hu_type != null && Object.hasOwnProperty.call(message, "max_hu_type"))
                writer.uint32(48).uint32(message.max_hu_type);
            if (message.level_mode != null && message.level_mode.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.level_mode.length; ++i)
                    writer.uint32(message.level_mode[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameRecordListV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameRecordListV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameRecordListV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tag = value;
                        else
                            delete message.tag;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.begin_time = value;
                        else
                            delete message.begin_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.ranks && message.ranks.length))
                                message.ranks = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ranks.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.ranks && message.ranks.length))
                            message.ranks = [];
                        message.ranks.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.modes && message.modes.length))
                                message.modes = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.modes.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modes && message.modes.length))
                            message.modes = [];
                        message.modes.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_hu_type = value;
                        else
                            delete message.max_hu_type;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.level_mode && message.level_mode.length))
                                message.level_mode = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.level_mode.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.level_mode && message.level_mode.length))
                            message.level_mode = [];
                        message.level_mode.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameRecordListV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameRecordListV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tag != null && message.hasOwnProperty("tag"))
                if (!$util.isInteger(message.tag))
                    return "tag: integer expected";
            if (message.begin_time != null && message.hasOwnProperty("begin_time"))
                if (!$util.isInteger(message.begin_time))
                    return "begin_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.ranks != null && message.hasOwnProperty("ranks")) {
                if (!Array.isArray(message.ranks))
                    return "ranks: array expected";
                for (let i = 0; i < message.ranks.length; ++i)
                    if (!$util.isInteger(message.ranks[i]))
                        return "ranks: integer[] expected";
            }
            if (message.modes != null && message.hasOwnProperty("modes")) {
                if (!Array.isArray(message.modes))
                    return "modes: array expected";
                for (let i = 0; i < message.modes.length; ++i)
                    if (!$util.isInteger(message.modes[i]))
                        return "modes: integer[] expected";
            }
            if (message.max_hu_type != null && message.hasOwnProperty("max_hu_type"))
                if (!$util.isInteger(message.max_hu_type))
                    return "max_hu_type: integer expected";
            if (message.level_mode != null && message.hasOwnProperty("level_mode")) {
                if (!Array.isArray(message.level_mode))
                    return "level_mode: array expected";
                for (let i = 0; i < message.level_mode.length; ++i)
                    if (!$util.isInteger(message.level_mode[i]))
                        return "level_mode: integer[] expected";
            }
            return null;
        };

        ReqGameRecordListV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameRecordListV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameRecordListV2();
            if (object.tag != null)
                if (Number(object.tag) !== 0)
                    message.tag = object.tag >>> 0;
            if (object.begin_time != null)
                if (Number(object.begin_time) !== 0)
                    message.begin_time = object.begin_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.ranks) {
                if (!Array.isArray(object.ranks))
                    throw TypeError(".lq.ReqGameRecordListV2.ranks: array expected");
                message.ranks = Array(object.ranks.length);
                for (let i = 0; i < object.ranks.length; ++i)
                    message.ranks[i] = object.ranks[i] >>> 0;
            }
            if (object.modes) {
                if (!Array.isArray(object.modes))
                    throw TypeError(".lq.ReqGameRecordListV2.modes: array expected");
                message.modes = Array(object.modes.length);
                for (let i = 0; i < object.modes.length; ++i)
                    message.modes[i] = object.modes[i] >>> 0;
            }
            if (object.max_hu_type != null)
                if (Number(object.max_hu_type) !== 0)
                    message.max_hu_type = object.max_hu_type >>> 0;
            if (object.level_mode) {
                if (!Array.isArray(object.level_mode))
                    throw TypeError(".lq.ReqGameRecordListV2.level_mode: array expected");
                message.level_mode = Array(object.level_mode.length);
                for (let i = 0; i < object.level_mode.length; ++i)
                    message.level_mode[i] = object.level_mode[i] >>> 0;
            }
            return message;
        };

        ReqGameRecordListV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ranks = [];
                object.modes = [];
                object.level_mode = [];
            }
            if (options.defaults) {
                object.tag = 0;
                object.begin_time = 0;
                object.end_time = 0;
                object.max_hu_type = 0;
            }
            if (message.tag != null && message.hasOwnProperty("tag"))
                object.tag = message.tag;
            if (message.begin_time != null && message.hasOwnProperty("begin_time"))
                object.begin_time = message.begin_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.ranks && message.ranks.length) {
                object.ranks = Array(message.ranks.length);
                for (let j = 0; j < message.ranks.length; ++j)
                    object.ranks[j] = message.ranks[j];
            }
            if (message.modes && message.modes.length) {
                object.modes = Array(message.modes.length);
                for (let j = 0; j < message.modes.length; ++j)
                    object.modes[j] = message.modes[j];
            }
            if (message.max_hu_type != null && message.hasOwnProperty("max_hu_type"))
                object.max_hu_type = message.max_hu_type;
            if (message.level_mode && message.level_mode.length) {
                object.level_mode = Array(message.level_mode.length);
                for (let j = 0; j < message.level_mode.length; ++j)
                    object.level_mode[j] = message.level_mode[j];
            }
            return object;
        };

        ReqGameRecordListV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameRecordListV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameRecordListV2";
        };

        return ReqGameRecordListV2;
    })();

    lq.ResGameRecordListV2 = (function() {

        function ResGameRecordListV2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameRecordListV2.prototype.error = null;
        ResGameRecordListV2.prototype.iterator = "";
        ResGameRecordListV2.prototype.iterator_expire = 0;
        ResGameRecordListV2.prototype.actual_begin_time = 0;
        ResGameRecordListV2.prototype.actual_end_time = 0;

        ResGameRecordListV2.create = function create(properties) {
            return new ResGameRecordListV2(properties);
        };

        ResGameRecordListV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.iterator != null && Object.hasOwnProperty.call(message, "iterator"))
                writer.uint32(18).string(message.iterator);
            if (message.iterator_expire != null && Object.hasOwnProperty.call(message, "iterator_expire"))
                writer.uint32(24).uint32(message.iterator_expire);
            if (message.actual_begin_time != null && Object.hasOwnProperty.call(message, "actual_begin_time"))
                writer.uint32(32).uint32(message.actual_begin_time);
            if (message.actual_end_time != null && Object.hasOwnProperty.call(message, "actual_end_time"))
                writer.uint32(40).uint32(message.actual_end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameRecordListV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameRecordListV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameRecordListV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.iterator = value;
                        else
                            delete message.iterator;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.iterator_expire = value;
                        else
                            delete message.iterator_expire;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.actual_begin_time = value;
                        else
                            delete message.actual_begin_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.actual_end_time = value;
                        else
                            delete message.actual_end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameRecordListV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameRecordListV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.iterator != null && message.hasOwnProperty("iterator"))
                if (!$util.isString(message.iterator))
                    return "iterator: string expected";
            if (message.iterator_expire != null && message.hasOwnProperty("iterator_expire"))
                if (!$util.isInteger(message.iterator_expire))
                    return "iterator_expire: integer expected";
            if (message.actual_begin_time != null && message.hasOwnProperty("actual_begin_time"))
                if (!$util.isInteger(message.actual_begin_time))
                    return "actual_begin_time: integer expected";
            if (message.actual_end_time != null && message.hasOwnProperty("actual_end_time"))
                if (!$util.isInteger(message.actual_end_time))
                    return "actual_end_time: integer expected";
            return null;
        };

        ResGameRecordListV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameRecordListV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameRecordListV2();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameRecordListV2.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.iterator != null)
                if (typeof object.iterator !== "string" || object.iterator.length)
                    message.iterator = String(object.iterator);
            if (object.iterator_expire != null)
                if (Number(object.iterator_expire) !== 0)
                    message.iterator_expire = object.iterator_expire >>> 0;
            if (object.actual_begin_time != null)
                if (Number(object.actual_begin_time) !== 0)
                    message.actual_begin_time = object.actual_begin_time >>> 0;
            if (object.actual_end_time != null)
                if (Number(object.actual_end_time) !== 0)
                    message.actual_end_time = object.actual_end_time >>> 0;
            return message;
        };

        ResGameRecordListV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.iterator = "";
                object.iterator_expire = 0;
                object.actual_begin_time = 0;
                object.actual_end_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.iterator != null && message.hasOwnProperty("iterator"))
                object.iterator = message.iterator;
            if (message.iterator_expire != null && message.hasOwnProperty("iterator_expire"))
                object.iterator_expire = message.iterator_expire;
            if (message.actual_begin_time != null && message.hasOwnProperty("actual_begin_time"))
                object.actual_begin_time = message.actual_begin_time;
            if (message.actual_end_time != null && message.hasOwnProperty("actual_end_time"))
                object.actual_end_time = message.actual_end_time;
            return object;
        };

        ResGameRecordListV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameRecordListV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameRecordListV2";
        };

        return ResGameRecordListV2;
    })();

    lq.ReqNextGameRecordList = (function() {

        function ReqNextGameRecordList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqNextGameRecordList.prototype.iterator = "";
        ReqNextGameRecordList.prototype.count = 0;

        ReqNextGameRecordList.create = function create(properties) {
            return new ReqNextGameRecordList(properties);
        };

        ReqNextGameRecordList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.iterator != null && Object.hasOwnProperty.call(message, "iterator"))
                writer.uint32(10).string(message.iterator);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqNextGameRecordList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqNextGameRecordList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqNextGameRecordList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.iterator = value;
                        else
                            delete message.iterator;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqNextGameRecordList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqNextGameRecordList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.iterator != null && message.hasOwnProperty("iterator"))
                if (!$util.isString(message.iterator))
                    return "iterator: string expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqNextGameRecordList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqNextGameRecordList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqNextGameRecordList();
            if (object.iterator != null)
                if (typeof object.iterator !== "string" || object.iterator.length)
                    message.iterator = String(object.iterator);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqNextGameRecordList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.iterator = "";
                object.count = 0;
            }
            if (message.iterator != null && message.hasOwnProperty("iterator"))
                object.iterator = message.iterator;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqNextGameRecordList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqNextGameRecordList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqNextGameRecordList";
        };

        return ReqNextGameRecordList;
    })();

    lq.ResNextGameRecordList = (function() {

        function ResNextGameRecordList(properties) {
            this.entries = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResNextGameRecordList.prototype.error = null;
        ResNextGameRecordList.prototype.next = false;
        ResNextGameRecordList.prototype.entries = $util.emptyArray;
        ResNextGameRecordList.prototype.iterator_expire = 0;
        ResNextGameRecordList.prototype.next_end_time = 0;

        ResNextGameRecordList.create = function create(properties) {
            return new ResNextGameRecordList(properties);
        };

        ResNextGameRecordList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.next != null && Object.hasOwnProperty.call(message, "next"))
                writer.uint32(16).bool(message.next);
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.lq.RecordListEntry.encode(message.entries[i], writer.uint32(26).fork()).ldelim();
            if (message.iterator_expire != null && Object.hasOwnProperty.call(message, "iterator_expire"))
                writer.uint32(32).uint32(message.iterator_expire);
            if (message.next_end_time != null && Object.hasOwnProperty.call(message, "next_end_time"))
                writer.uint32(40).uint32(message.next_end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResNextGameRecordList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResNextGameRecordList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResNextGameRecordList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.next = value;
                        else
                            delete message.next;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.lq.RecordListEntry.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.iterator_expire = value;
                        else
                            delete message.iterator_expire;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.next_end_time = value;
                        else
                            delete message.next_end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResNextGameRecordList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResNextGameRecordList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.next != null && message.hasOwnProperty("next"))
                if (typeof message.next !== "boolean")
                    return "next: boolean expected";
            if (message.entries != null && message.hasOwnProperty("entries")) {
                if (!Array.isArray(message.entries))
                    return "entries: array expected";
                for (let i = 0; i < message.entries.length; ++i) {
                    let error = $root.lq.RecordListEntry.verify(message.entries[i], _depth + 1);
                    if (error)
                        return "entries." + error;
                }
            }
            if (message.iterator_expire != null && message.hasOwnProperty("iterator_expire"))
                if (!$util.isInteger(message.iterator_expire))
                    return "iterator_expire: integer expected";
            if (message.next_end_time != null && message.hasOwnProperty("next_end_time"))
                if (!$util.isInteger(message.next_end_time))
                    return "next_end_time: integer expected";
            return null;
        };

        ResNextGameRecordList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResNextGameRecordList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResNextGameRecordList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResNextGameRecordList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.next != null)
                if (object.next)
                    message.next = Boolean(object.next);
            if (object.entries) {
                if (!Array.isArray(object.entries))
                    throw TypeError(".lq.ResNextGameRecordList.entries: array expected");
                message.entries = Array(object.entries.length);
                for (let i = 0; i < object.entries.length; ++i) {
                    if (typeof object.entries[i] !== "object")
                        throw TypeError(".lq.ResNextGameRecordList.entries: object expected");
                    message.entries[i] = $root.lq.RecordListEntry.fromObject(object.entries[i], _depth + 1);
                }
            }
            if (object.iterator_expire != null)
                if (Number(object.iterator_expire) !== 0)
                    message.iterator_expire = object.iterator_expire >>> 0;
            if (object.next_end_time != null)
                if (Number(object.next_end_time) !== 0)
                    message.next_end_time = object.next_end_time >>> 0;
            return message;
        };

        ResNextGameRecordList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.entries = [];
            if (options.defaults) {
                object.error = null;
                object.next = false;
                object.iterator_expire = 0;
                object.next_end_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.next != null && message.hasOwnProperty("next"))
                object.next = message.next;
            if (message.entries && message.entries.length) {
                object.entries = Array(message.entries.length);
                for (let j = 0; j < message.entries.length; ++j)
                    object.entries[j] = $root.lq.RecordListEntry.toObject(message.entries[j], options);
            }
            if (message.iterator_expire != null && message.hasOwnProperty("iterator_expire"))
                object.iterator_expire = message.iterator_expire;
            if (message.next_end_time != null && message.hasOwnProperty("next_end_time"))
                object.next_end_time = message.next_end_time;
            return object;
        };

        ResNextGameRecordList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResNextGameRecordList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResNextGameRecordList";
        };

        return ResNextGameRecordList;
    })();

    lq.ResCollectedGameRecordList = (function() {

        function ResCollectedGameRecordList(properties) {
            this.record_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCollectedGameRecordList.prototype.error = null;
        ResCollectedGameRecordList.prototype.record_list = $util.emptyArray;
        ResCollectedGameRecordList.prototype.record_collect_limit = 0;

        ResCollectedGameRecordList.create = function create(properties) {
            return new ResCollectedGameRecordList(properties);
        };

        ResCollectedGameRecordList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.record_list != null && message.record_list.length)
                for (let i = 0; i < message.record_list.length; ++i)
                    $root.lq.RecordCollectedData.encode(message.record_list[i], writer.uint32(18).fork()).ldelim();
            if (message.record_collect_limit != null && Object.hasOwnProperty.call(message, "record_collect_limit"))
                writer.uint32(24).uint32(message.record_collect_limit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCollectedGameRecordList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCollectedGameRecordList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCollectedGameRecordList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.record_list && message.record_list.length))
                            message.record_list = [];
                        message.record_list.push($root.lq.RecordCollectedData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.record_collect_limit = value;
                        else
                            delete message.record_collect_limit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCollectedGameRecordList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCollectedGameRecordList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.record_list != null && message.hasOwnProperty("record_list")) {
                if (!Array.isArray(message.record_list))
                    return "record_list: array expected";
                for (let i = 0; i < message.record_list.length; ++i) {
                    let error = $root.lq.RecordCollectedData.verify(message.record_list[i], _depth + 1);
                    if (error)
                        return "record_list." + error;
                }
            }
            if (message.record_collect_limit != null && message.hasOwnProperty("record_collect_limit"))
                if (!$util.isInteger(message.record_collect_limit))
                    return "record_collect_limit: integer expected";
            return null;
        };

        ResCollectedGameRecordList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCollectedGameRecordList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCollectedGameRecordList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCollectedGameRecordList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.record_list) {
                if (!Array.isArray(object.record_list))
                    throw TypeError(".lq.ResCollectedGameRecordList.record_list: array expected");
                message.record_list = Array(object.record_list.length);
                for (let i = 0; i < object.record_list.length; ++i) {
                    if (typeof object.record_list[i] !== "object")
                        throw TypeError(".lq.ResCollectedGameRecordList.record_list: object expected");
                    message.record_list[i] = $root.lq.RecordCollectedData.fromObject(object.record_list[i], _depth + 1);
                }
            }
            if (object.record_collect_limit != null)
                if (Number(object.record_collect_limit) !== 0)
                    message.record_collect_limit = object.record_collect_limit >>> 0;
            return message;
        };

        ResCollectedGameRecordList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.record_list = [];
            if (options.defaults) {
                object.error = null;
                object.record_collect_limit = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.record_list && message.record_list.length) {
                object.record_list = Array(message.record_list.length);
                for (let j = 0; j < message.record_list.length; ++j)
                    object.record_list[j] = $root.lq.RecordCollectedData.toObject(message.record_list[j], options);
            }
            if (message.record_collect_limit != null && message.hasOwnProperty("record_collect_limit"))
                object.record_collect_limit = message.record_collect_limit;
            return object;
        };

        ResCollectedGameRecordList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCollectedGameRecordList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCollectedGameRecordList";
        };

        return ResCollectedGameRecordList;
    })();

    lq.ReqGameRecordsDetail = (function() {

        function ReqGameRecordsDetail(properties) {
            this.uuid_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameRecordsDetail.prototype.uuid_list = $util.emptyArray;

        ReqGameRecordsDetail.create = function create(properties) {
            return new ReqGameRecordsDetail(properties);
        };

        ReqGameRecordsDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid_list != null && message.uuid_list.length)
                for (let i = 0; i < message.uuid_list.length; ++i)
                    writer.uint32(10).string(message.uuid_list[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameRecordsDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameRecordsDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameRecordsDetail();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.uuid_list && message.uuid_list.length))
                            message.uuid_list = [];
                        message.uuid_list.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameRecordsDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameRecordsDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid_list != null && message.hasOwnProperty("uuid_list")) {
                if (!Array.isArray(message.uuid_list))
                    return "uuid_list: array expected";
                for (let i = 0; i < message.uuid_list.length; ++i)
                    if (!$util.isString(message.uuid_list[i]))
                        return "uuid_list: string[] expected";
            }
            return null;
        };

        ReqGameRecordsDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameRecordsDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameRecordsDetail();
            if (object.uuid_list) {
                if (!Array.isArray(object.uuid_list))
                    throw TypeError(".lq.ReqGameRecordsDetail.uuid_list: array expected");
                message.uuid_list = Array(object.uuid_list.length);
                for (let i = 0; i < object.uuid_list.length; ++i)
                    message.uuid_list[i] = String(object.uuid_list[i]);
            }
            return message;
        };

        ReqGameRecordsDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.uuid_list = [];
            if (message.uuid_list && message.uuid_list.length) {
                object.uuid_list = Array(message.uuid_list.length);
                for (let j = 0; j < message.uuid_list.length; ++j)
                    object.uuid_list[j] = message.uuid_list[j];
            }
            return object;
        };

        ReqGameRecordsDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameRecordsDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameRecordsDetail";
        };

        return ReqGameRecordsDetail;
    })();

    lq.ResGameRecordsDetail = (function() {

        function ResGameRecordsDetail(properties) {
            this.record_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameRecordsDetail.prototype.error = null;
        ResGameRecordsDetail.prototype.record_list = $util.emptyArray;

        ResGameRecordsDetail.create = function create(properties) {
            return new ResGameRecordsDetail(properties);
        };

        ResGameRecordsDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.record_list != null && message.record_list.length)
                for (let i = 0; i < message.record_list.length; ++i)
                    $root.lq.RecordGame.encode(message.record_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameRecordsDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameRecordsDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameRecordsDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.record_list && message.record_list.length))
                            message.record_list = [];
                        message.record_list.push($root.lq.RecordGame.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameRecordsDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameRecordsDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.record_list != null && message.hasOwnProperty("record_list")) {
                if (!Array.isArray(message.record_list))
                    return "record_list: array expected";
                for (let i = 0; i < message.record_list.length; ++i) {
                    let error = $root.lq.RecordGame.verify(message.record_list[i], _depth + 1);
                    if (error)
                        return "record_list." + error;
                }
            }
            return null;
        };

        ResGameRecordsDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameRecordsDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameRecordsDetail();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameRecordsDetail.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.record_list) {
                if (!Array.isArray(object.record_list))
                    throw TypeError(".lq.ResGameRecordsDetail.record_list: array expected");
                message.record_list = Array(object.record_list.length);
                for (let i = 0; i < object.record_list.length; ++i) {
                    if (typeof object.record_list[i] !== "object")
                        throw TypeError(".lq.ResGameRecordsDetail.record_list: object expected");
                    message.record_list[i] = $root.lq.RecordGame.fromObject(object.record_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResGameRecordsDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.record_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.record_list && message.record_list.length) {
                object.record_list = Array(message.record_list.length);
                for (let j = 0; j < message.record_list.length; ++j)
                    object.record_list[j] = $root.lq.RecordGame.toObject(message.record_list[j], options);
            }
            return object;
        };

        ResGameRecordsDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameRecordsDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameRecordsDetail";
        };

        return ResGameRecordsDetail;
    })();

    lq.ReqGameRecordsDetailV2 = (function() {

        function ReqGameRecordsDetailV2(properties) {
            this.uuid_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameRecordsDetailV2.prototype.uuid_list = $util.emptyArray;

        ReqGameRecordsDetailV2.create = function create(properties) {
            return new ReqGameRecordsDetailV2(properties);
        };

        ReqGameRecordsDetailV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid_list != null && message.uuid_list.length)
                for (let i = 0; i < message.uuid_list.length; ++i)
                    writer.uint32(10).string(message.uuid_list[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameRecordsDetailV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameRecordsDetailV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameRecordsDetailV2();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.uuid_list && message.uuid_list.length))
                            message.uuid_list = [];
                        message.uuid_list.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameRecordsDetailV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameRecordsDetailV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid_list != null && message.hasOwnProperty("uuid_list")) {
                if (!Array.isArray(message.uuid_list))
                    return "uuid_list: array expected";
                for (let i = 0; i < message.uuid_list.length; ++i)
                    if (!$util.isString(message.uuid_list[i]))
                        return "uuid_list: string[] expected";
            }
            return null;
        };

        ReqGameRecordsDetailV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameRecordsDetailV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameRecordsDetailV2();
            if (object.uuid_list) {
                if (!Array.isArray(object.uuid_list))
                    throw TypeError(".lq.ReqGameRecordsDetailV2.uuid_list: array expected");
                message.uuid_list = Array(object.uuid_list.length);
                for (let i = 0; i < object.uuid_list.length; ++i)
                    message.uuid_list[i] = String(object.uuid_list[i]);
            }
            return message;
        };

        ReqGameRecordsDetailV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.uuid_list = [];
            if (message.uuid_list && message.uuid_list.length) {
                object.uuid_list = Array(message.uuid_list.length);
                for (let j = 0; j < message.uuid_list.length; ++j)
                    object.uuid_list[j] = message.uuid_list[j];
            }
            return object;
        };

        ReqGameRecordsDetailV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameRecordsDetailV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameRecordsDetailV2";
        };

        return ReqGameRecordsDetailV2;
    })();

    lq.ResGameRecordsDetailV2 = (function() {

        function ResGameRecordsDetailV2(properties) {
            this.entries = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameRecordsDetailV2.prototype.error = null;
        ResGameRecordsDetailV2.prototype.entries = $util.emptyArray;

        ResGameRecordsDetailV2.create = function create(properties) {
            return new ResGameRecordsDetailV2(properties);
        };

        ResGameRecordsDetailV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.lq.RecordListEntry.encode(message.entries[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameRecordsDetailV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameRecordsDetailV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameRecordsDetailV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.lq.RecordListEntry.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameRecordsDetailV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameRecordsDetailV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.entries != null && message.hasOwnProperty("entries")) {
                if (!Array.isArray(message.entries))
                    return "entries: array expected";
                for (let i = 0; i < message.entries.length; ++i) {
                    let error = $root.lq.RecordListEntry.verify(message.entries[i], _depth + 1);
                    if (error)
                        return "entries." + error;
                }
            }
            return null;
        };

        ResGameRecordsDetailV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameRecordsDetailV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameRecordsDetailV2();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameRecordsDetailV2.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.entries) {
                if (!Array.isArray(object.entries))
                    throw TypeError(".lq.ResGameRecordsDetailV2.entries: array expected");
                message.entries = Array(object.entries.length);
                for (let i = 0; i < object.entries.length; ++i) {
                    if (typeof object.entries[i] !== "object")
                        throw TypeError(".lq.ResGameRecordsDetailV2.entries: object expected");
                    message.entries[i] = $root.lq.RecordListEntry.fromObject(object.entries[i], _depth + 1);
                }
            }
            return message;
        };

        ResGameRecordsDetailV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.entries = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.entries && message.entries.length) {
                object.entries = Array(message.entries.length);
                for (let j = 0; j < message.entries.length; ++j)
                    object.entries[j] = $root.lq.RecordListEntry.toObject(message.entries[j], options);
            }
            return object;
        };

        ResGameRecordsDetailV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameRecordsDetailV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameRecordsDetailV2";
        };

        return ResGameRecordsDetailV2;
    })();

    lq.ReqAddCollectedGameRecord = (function() {

        function ReqAddCollectedGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAddCollectedGameRecord.prototype.uuid = "";
        ReqAddCollectedGameRecord.prototype.remarks = "";
        ReqAddCollectedGameRecord.prototype.start_time = 0;
        ReqAddCollectedGameRecord.prototype.end_time = 0;

        ReqAddCollectedGameRecord.create = function create(properties) {
            return new ReqAddCollectedGameRecord(properties);
        };

        ReqAddCollectedGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.remarks != null && Object.hasOwnProperty.call(message, "remarks"))
                writer.uint32(18).string(message.remarks);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(24).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(32).uint32(message.end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAddCollectedGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAddCollectedGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAddCollectedGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.remarks = value;
                        else
                            delete message.remarks;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAddCollectedGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAddCollectedGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                if (!$util.isString(message.remarks))
                    return "remarks: string expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            return null;
        };

        ReqAddCollectedGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAddCollectedGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAddCollectedGameRecord();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.remarks != null)
                if (typeof object.remarks !== "string" || object.remarks.length)
                    message.remarks = String(object.remarks);
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            return message;
        };

        ReqAddCollectedGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.uuid = "";
                object.remarks = "";
                object.start_time = 0;
                object.end_time = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                object.remarks = message.remarks;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            return object;
        };

        ReqAddCollectedGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAddCollectedGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAddCollectedGameRecord";
        };

        return ReqAddCollectedGameRecord;
    })();

    lq.ResAddCollectedGameRecord = (function() {

        function ResAddCollectedGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAddCollectedGameRecord.prototype.error = null;

        ResAddCollectedGameRecord.create = function create(properties) {
            return new ResAddCollectedGameRecord(properties);
        };

        ResAddCollectedGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAddCollectedGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAddCollectedGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAddCollectedGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAddCollectedGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAddCollectedGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResAddCollectedGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAddCollectedGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAddCollectedGameRecord();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAddCollectedGameRecord.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResAddCollectedGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResAddCollectedGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAddCollectedGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAddCollectedGameRecord";
        };

        return ResAddCollectedGameRecord;
    })();

    lq.ReqRemoveCollectedGameRecord = (function() {

        function ReqRemoveCollectedGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRemoveCollectedGameRecord.prototype.uuid = "";

        ReqRemoveCollectedGameRecord.create = function create(properties) {
            return new ReqRemoveCollectedGameRecord(properties);
        };

        ReqRemoveCollectedGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRemoveCollectedGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRemoveCollectedGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRemoveCollectedGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRemoveCollectedGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRemoveCollectedGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            return null;
        };

        ReqRemoveCollectedGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRemoveCollectedGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRemoveCollectedGameRecord();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            return message;
        };

        ReqRemoveCollectedGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.uuid = "";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            return object;
        };

        ReqRemoveCollectedGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRemoveCollectedGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRemoveCollectedGameRecord";
        };

        return ReqRemoveCollectedGameRecord;
    })();

    lq.ResRemoveCollectedGameRecord = (function() {

        function ResRemoveCollectedGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRemoveCollectedGameRecord.prototype.error = null;

        ResRemoveCollectedGameRecord.create = function create(properties) {
            return new ResRemoveCollectedGameRecord(properties);
        };

        ResRemoveCollectedGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRemoveCollectedGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRemoveCollectedGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRemoveCollectedGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRemoveCollectedGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRemoveCollectedGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResRemoveCollectedGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRemoveCollectedGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRemoveCollectedGameRecord();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRemoveCollectedGameRecord.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResRemoveCollectedGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResRemoveCollectedGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRemoveCollectedGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRemoveCollectedGameRecord";
        };

        return ResRemoveCollectedGameRecord;
    })();

    lq.ReqChangeCollectedGameRecordRemarks = (function() {

        function ReqChangeCollectedGameRecordRemarks(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeCollectedGameRecordRemarks.prototype.uuid = "";
        ReqChangeCollectedGameRecordRemarks.prototype.remarks = "";

        ReqChangeCollectedGameRecordRemarks.create = function create(properties) {
            return new ReqChangeCollectedGameRecordRemarks(properties);
        };

        ReqChangeCollectedGameRecordRemarks.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.remarks != null && Object.hasOwnProperty.call(message, "remarks"))
                writer.uint32(18).string(message.remarks);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeCollectedGameRecordRemarks.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeCollectedGameRecordRemarks.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeCollectedGameRecordRemarks(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.remarks = value;
                        else
                            delete message.remarks;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeCollectedGameRecordRemarks.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeCollectedGameRecordRemarks.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                if (!$util.isString(message.remarks))
                    return "remarks: string expected";
            return null;
        };

        ReqChangeCollectedGameRecordRemarks.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeCollectedGameRecordRemarks)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeCollectedGameRecordRemarks();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.remarks != null)
                if (typeof object.remarks !== "string" || object.remarks.length)
                    message.remarks = String(object.remarks);
            return message;
        };

        ReqChangeCollectedGameRecordRemarks.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.uuid = "";
                object.remarks = "";
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.remarks != null && message.hasOwnProperty("remarks"))
                object.remarks = message.remarks;
            return object;
        };

        ReqChangeCollectedGameRecordRemarks.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeCollectedGameRecordRemarks.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeCollectedGameRecordRemarks";
        };

        return ReqChangeCollectedGameRecordRemarks;
    })();

    lq.ResChangeCollectedGameRecordRemarks = (function() {

        function ResChangeCollectedGameRecordRemarks(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResChangeCollectedGameRecordRemarks.prototype.error = null;

        ResChangeCollectedGameRecordRemarks.create = function create(properties) {
            return new ResChangeCollectedGameRecordRemarks(properties);
        };

        ResChangeCollectedGameRecordRemarks.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResChangeCollectedGameRecordRemarks.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResChangeCollectedGameRecordRemarks.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResChangeCollectedGameRecordRemarks(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResChangeCollectedGameRecordRemarks.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResChangeCollectedGameRecordRemarks.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResChangeCollectedGameRecordRemarks.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResChangeCollectedGameRecordRemarks)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResChangeCollectedGameRecordRemarks();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResChangeCollectedGameRecordRemarks.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResChangeCollectedGameRecordRemarks.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResChangeCollectedGameRecordRemarks.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResChangeCollectedGameRecordRemarks.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResChangeCollectedGameRecordRemarks";
        };

        return ResChangeCollectedGameRecordRemarks;
    })();

    lq.ReqLevelLeaderboard = (function() {

        function ReqLevelLeaderboard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLevelLeaderboard.prototype.type = 0;

        ReqLevelLeaderboard.create = function create(properties) {
            return new ReqLevelLeaderboard(properties);
        };

        ReqLevelLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLevelLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLevelLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLevelLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLevelLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLevelLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqLevelLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLevelLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLevelLeaderboard();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqLevelLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.type = 0;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqLevelLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLevelLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLevelLeaderboard";
        };

        return ReqLevelLeaderboard;
    })();

    lq.ResLevelLeaderboard = (function() {

        function ResLevelLeaderboard(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResLevelLeaderboard.prototype.error = null;
        ResLevelLeaderboard.prototype.items = $util.emptyArray;
        ResLevelLeaderboard.prototype.self_rank = 0;

        ResLevelLeaderboard.create = function create(properties) {
            return new ResLevelLeaderboard(properties);
        };

        ResLevelLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResLevelLeaderboard.Item.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.self_rank != null && Object.hasOwnProperty.call(message, "self_rank"))
                writer.uint32(24).uint32(message.self_rank);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResLevelLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResLevelLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResLevelLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResLevelLeaderboard.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.self_rank = value;
                        else
                            delete message.self_rank;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResLevelLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResLevelLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResLevelLeaderboard.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                if (!$util.isInteger(message.self_rank))
                    return "self_rank: integer expected";
            return null;
        };

        ResLevelLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResLevelLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResLevelLeaderboard();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResLevelLeaderboard.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResLevelLeaderboard.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResLevelLeaderboard.items: object expected");
                    message.items[i] = $root.lq.ResLevelLeaderboard.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.self_rank != null)
                if (Number(object.self_rank) !== 0)
                    message.self_rank = object.self_rank >>> 0;
            return message;
        };

        ResLevelLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.error = null;
                object.self_rank = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResLevelLeaderboard.Item.toObject(message.items[j], options);
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                object.self_rank = message.self_rank;
            return object;
        };

        ResLevelLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResLevelLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResLevelLeaderboard";
        };

        ResLevelLeaderboard.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.level = null;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    $root.lq.AccountLevel.encode(message.level, writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResLevelLeaderboard.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level")) {
                    let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                    if (error)
                        return "level." + error;
                }
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResLevelLeaderboard.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResLevelLeaderboard.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.level != null) {
                    if (typeof object.level !== "object")
                        throw TypeError(".lq.ResLevelLeaderboard.Item.level: object expected");
                    message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
                }
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.level = null;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = $root.lq.AccountLevel.toObject(message.level, options);
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResLevelLeaderboard.Item";
            };

            return Item;
        })();

        return ResLevelLeaderboard;
    })();

    lq.ReqChallangeLeaderboard = (function() {

        function ReqChallangeLeaderboard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChallangeLeaderboard.prototype.season = 0;

        ReqChallangeLeaderboard.create = function create(properties) {
            return new ReqChallangeLeaderboard(properties);
        };

        ReqChallangeLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                writer.uint32(8).uint32(message.season);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChallangeLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChallangeLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChallangeLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.season = value;
                        else
                            delete message.season;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChallangeLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChallangeLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.season != null && message.hasOwnProperty("season"))
                if (!$util.isInteger(message.season))
                    return "season: integer expected";
            return null;
        };

        ReqChallangeLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChallangeLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChallangeLeaderboard();
            if (object.season != null)
                if (Number(object.season) !== 0)
                    message.season = object.season >>> 0;
            return message;
        };

        ReqChallangeLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.season = 0;
            if (message.season != null && message.hasOwnProperty("season"))
                object.season = message.season;
            return object;
        };

        ReqChallangeLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChallangeLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChallangeLeaderboard";
        };

        return ReqChallangeLeaderboard;
    })();

    lq.ResChallengeLeaderboard = (function() {

        function ResChallengeLeaderboard(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResChallengeLeaderboard.prototype.error = null;
        ResChallengeLeaderboard.prototype.items = $util.emptyArray;
        ResChallengeLeaderboard.prototype.self_rank = 0;

        ResChallengeLeaderboard.create = function create(properties) {
            return new ResChallengeLeaderboard(properties);
        };

        ResChallengeLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResChallengeLeaderboard.Item.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.self_rank != null && Object.hasOwnProperty.call(message, "self_rank"))
                writer.uint32(24).uint32(message.self_rank);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResChallengeLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResChallengeLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResChallengeLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResChallengeLeaderboard.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.self_rank = value;
                        else
                            delete message.self_rank;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResChallengeLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResChallengeLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResChallengeLeaderboard.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                if (!$util.isInteger(message.self_rank))
                    return "self_rank: integer expected";
            return null;
        };

        ResChallengeLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResChallengeLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResChallengeLeaderboard();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResChallengeLeaderboard.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResChallengeLeaderboard.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResChallengeLeaderboard.items: object expected");
                    message.items[i] = $root.lq.ResChallengeLeaderboard.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.self_rank != null)
                if (Number(object.self_rank) !== 0)
                    message.self_rank = object.self_rank >>> 0;
            return message;
        };

        ResChallengeLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.error = null;
                object.self_rank = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResChallengeLeaderboard.Item.toObject(message.items[j], options);
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                object.self_rank = message.self_rank;
            return object;
        };

        ResChallengeLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResChallengeLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResChallengeLeaderboard";
        };

        ResChallengeLeaderboard.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.level = 0;
            Item.prototype.nickname = "";

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(26).string(message.nickname);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResChallengeLeaderboard.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResChallengeLeaderboard.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResChallengeLeaderboard.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.level = 0;
                    object.nickname = "";
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResChallengeLeaderboard.Item";
            };

            return Item;
        })();

        return ResChallengeLeaderboard;
    })();

    lq.ReqMutiChallengeLevel = (function() {

        function ReqMutiChallengeLevel(properties) {
            this.account_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMutiChallengeLevel.prototype.account_id_list = $util.emptyArray;
        ReqMutiChallengeLevel.prototype.season = 0;

        ReqMutiChallengeLevel.create = function create(properties) {
            return new ReqMutiChallengeLevel(properties);
        };

        ReqMutiChallengeLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id_list != null && message.account_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.account_id_list.length; ++i)
                    writer.uint32(message.account_id_list[i]);
                writer.ldelim();
            }
            if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                writer.uint32(16).uint32(message.season);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMutiChallengeLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMutiChallengeLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMutiChallengeLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.account_id_list && message.account_id_list.length))
                                message.account_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_id_list && message.account_id_list.length))
                            message.account_id_list = [];
                        message.account_id_list.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.season = value;
                        else
                            delete message.season;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMutiChallengeLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMutiChallengeLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id_list != null && message.hasOwnProperty("account_id_list")) {
                if (!Array.isArray(message.account_id_list))
                    return "account_id_list: array expected";
                for (let i = 0; i < message.account_id_list.length; ++i)
                    if (!$util.isInteger(message.account_id_list[i]))
                        return "account_id_list: integer[] expected";
            }
            if (message.season != null && message.hasOwnProperty("season"))
                if (!$util.isInteger(message.season))
                    return "season: integer expected";
            return null;
        };

        ReqMutiChallengeLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMutiChallengeLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMutiChallengeLevel();
            if (object.account_id_list) {
                if (!Array.isArray(object.account_id_list))
                    throw TypeError(".lq.ReqMutiChallengeLevel.account_id_list: array expected");
                message.account_id_list = Array(object.account_id_list.length);
                for (let i = 0; i < object.account_id_list.length; ++i)
                    message.account_id_list[i] = object.account_id_list[i] >>> 0;
            }
            if (object.season != null)
                if (Number(object.season) !== 0)
                    message.season = object.season >>> 0;
            return message;
        };

        ReqMutiChallengeLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_id_list = [];
            if (options.defaults)
                object.season = 0;
            if (message.account_id_list && message.account_id_list.length) {
                object.account_id_list = Array(message.account_id_list.length);
                for (let j = 0; j < message.account_id_list.length; ++j)
                    object.account_id_list[j] = message.account_id_list[j];
            }
            if (message.season != null && message.hasOwnProperty("season"))
                object.season = message.season;
            return object;
        };

        ReqMutiChallengeLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMutiChallengeLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMutiChallengeLevel";
        };

        return ReqMutiChallengeLevel;
    })();

    lq.ResMutiChallengeLevel = (function() {

        function ResMutiChallengeLevel(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMutiChallengeLevel.prototype.error = null;
        ResMutiChallengeLevel.prototype.items = $util.emptyArray;

        ResMutiChallengeLevel.create = function create(properties) {
            return new ResMutiChallengeLevel(properties);
        };

        ResMutiChallengeLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResMutiChallengeLevel.Item.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMutiChallengeLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMutiChallengeLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMutiChallengeLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResMutiChallengeLevel.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMutiChallengeLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMutiChallengeLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResMutiChallengeLevel.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ResMutiChallengeLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMutiChallengeLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMutiChallengeLevel();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMutiChallengeLevel.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResMutiChallengeLevel.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResMutiChallengeLevel.items: object expected");
                    message.items[i] = $root.lq.ResMutiChallengeLevel.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ResMutiChallengeLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResMutiChallengeLevel.Item.toObject(message.items[j], options);
            }
            return object;
        };

        ResMutiChallengeLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMutiChallengeLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMutiChallengeLevel";
        };

        ResMutiChallengeLevel.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.level = 0;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMutiChallengeLevel.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResMutiChallengeLevel.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResMutiChallengeLevel.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.level = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResMutiChallengeLevel.Item";
            };

            return Item;
        })();

        return ResMutiChallengeLevel;
    })();

    lq.ReqMultiAccountId = (function() {

        function ReqMultiAccountId(properties) {
            this.account_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMultiAccountId.prototype.account_id_list = $util.emptyArray;

        ReqMultiAccountId.create = function create(properties) {
            return new ReqMultiAccountId(properties);
        };

        ReqMultiAccountId.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id_list != null && message.account_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.account_id_list.length; ++i)
                    writer.uint32(message.account_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMultiAccountId.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMultiAccountId.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMultiAccountId();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.account_id_list && message.account_id_list.length))
                                message.account_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_id_list && message.account_id_list.length))
                            message.account_id_list = [];
                        message.account_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMultiAccountId.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMultiAccountId.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id_list != null && message.hasOwnProperty("account_id_list")) {
                if (!Array.isArray(message.account_id_list))
                    return "account_id_list: array expected";
                for (let i = 0; i < message.account_id_list.length; ++i)
                    if (!$util.isInteger(message.account_id_list[i]))
                        return "account_id_list: integer[] expected";
            }
            return null;
        };

        ReqMultiAccountId.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMultiAccountId)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMultiAccountId();
            if (object.account_id_list) {
                if (!Array.isArray(object.account_id_list))
                    throw TypeError(".lq.ReqMultiAccountId.account_id_list: array expected");
                message.account_id_list = Array(object.account_id_list.length);
                for (let i = 0; i < object.account_id_list.length; ++i)
                    message.account_id_list[i] = object.account_id_list[i] >>> 0;
            }
            return message;
        };

        ReqMultiAccountId.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_id_list = [];
            if (message.account_id_list && message.account_id_list.length) {
                object.account_id_list = Array(message.account_id_list.length);
                for (let j = 0; j < message.account_id_list.length; ++j)
                    object.account_id_list[j] = message.account_id_list[j];
            }
            return object;
        };

        ReqMultiAccountId.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMultiAccountId.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMultiAccountId";
        };

        return ReqMultiAccountId;
    })();

    lq.ResMultiAccountBrief = (function() {

        function ResMultiAccountBrief(properties) {
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMultiAccountBrief.prototype.error = null;
        ResMultiAccountBrief.prototype.players = $util.emptyArray;

        ResMultiAccountBrief.create = function create(properties) {
            return new ResMultiAccountBrief(properties);
        };

        ResMultiAccountBrief.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.PlayerBaseView.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMultiAccountBrief.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMultiAccountBrief.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMultiAccountBrief(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMultiAccountBrief.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMultiAccountBrief.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.PlayerBaseView.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            return null;
        };

        ResMultiAccountBrief.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMultiAccountBrief)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMultiAccountBrief();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMultiAccountBrief.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.ResMultiAccountBrief.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.ResMultiAccountBrief.players: object expected");
                    message.players[i] = $root.lq.PlayerBaseView.fromObject(object.players[i], _depth + 1);
                }
            }
            return message;
        };

        ResMultiAccountBrief.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.players = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.PlayerBaseView.toObject(message.players[j], options);
            }
            return object;
        };

        ResMultiAccountBrief.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMultiAccountBrief.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMultiAccountBrief";
        };

        return ResMultiAccountBrief;
    })();

    lq.ResFriendList = (function() {

        function ResFriendList(properties) {
            this.friends = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFriendList.prototype.error = null;
        ResFriendList.prototype.friends = $util.emptyArray;
        ResFriendList.prototype.friend_max_count = 0;
        ResFriendList.prototype.friend_count = 0;

        ResFriendList.create = function create(properties) {
            return new ResFriendList(properties);
        };

        ResFriendList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.friends != null && message.friends.length)
                for (let i = 0; i < message.friends.length; ++i)
                    $root.lq.Friend.encode(message.friends[i], writer.uint32(18).fork()).ldelim();
            if (message.friend_max_count != null && Object.hasOwnProperty.call(message, "friend_max_count"))
                writer.uint32(24).uint32(message.friend_max_count);
            if (message.friend_count != null && Object.hasOwnProperty.call(message, "friend_count"))
                writer.uint32(32).uint32(message.friend_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFriendList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFriendList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.friends && message.friends.length))
                            message.friends = [];
                        message.friends.push($root.lq.Friend.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.friend_max_count = value;
                        else
                            delete message.friend_max_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.friend_count = value;
                        else
                            delete message.friend_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFriendList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.friends != null && message.hasOwnProperty("friends")) {
                if (!Array.isArray(message.friends))
                    return "friends: array expected";
                for (let i = 0; i < message.friends.length; ++i) {
                    let error = $root.lq.Friend.verify(message.friends[i], _depth + 1);
                    if (error)
                        return "friends." + error;
                }
            }
            if (message.friend_max_count != null && message.hasOwnProperty("friend_max_count"))
                if (!$util.isInteger(message.friend_max_count))
                    return "friend_max_count: integer expected";
            if (message.friend_count != null && message.hasOwnProperty("friend_count"))
                if (!$util.isInteger(message.friend_count))
                    return "friend_count: integer expected";
            return null;
        };

        ResFriendList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFriendList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFriendList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFriendList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.friends) {
                if (!Array.isArray(object.friends))
                    throw TypeError(".lq.ResFriendList.friends: array expected");
                message.friends = Array(object.friends.length);
                for (let i = 0; i < object.friends.length; ++i) {
                    if (typeof object.friends[i] !== "object")
                        throw TypeError(".lq.ResFriendList.friends: object expected");
                    message.friends[i] = $root.lq.Friend.fromObject(object.friends[i], _depth + 1);
                }
            }
            if (object.friend_max_count != null)
                if (Number(object.friend_max_count) !== 0)
                    message.friend_max_count = object.friend_max_count >>> 0;
            if (object.friend_count != null)
                if (Number(object.friend_count) !== 0)
                    message.friend_count = object.friend_count >>> 0;
            return message;
        };

        ResFriendList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.friends = [];
            if (options.defaults) {
                object.error = null;
                object.friend_max_count = 0;
                object.friend_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.friends && message.friends.length) {
                object.friends = Array(message.friends.length);
                for (let j = 0; j < message.friends.length; ++j)
                    object.friends[j] = $root.lq.Friend.toObject(message.friends[j], options);
            }
            if (message.friend_max_count != null && message.hasOwnProperty("friend_max_count"))
                object.friend_max_count = message.friend_max_count;
            if (message.friend_count != null && message.hasOwnProperty("friend_count"))
                object.friend_count = message.friend_count;
            return object;
        };

        ResFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFriendList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFriendList";
        };

        return ResFriendList;
    })();

    lq.ResFriendApplyList = (function() {

        function ResFriendApplyList(properties) {
            this.applies = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFriendApplyList.prototype.error = null;
        ResFriendApplyList.prototype.applies = $util.emptyArray;

        ResFriendApplyList.create = function create(properties) {
            return new ResFriendApplyList(properties);
        };

        ResFriendApplyList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.applies != null && message.applies.length)
                for (let i = 0; i < message.applies.length; ++i)
                    $root.lq.ResFriendApplyList.FriendApply.encode(message.applies[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFriendApplyList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFriendApplyList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFriendApplyList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.applies && message.applies.length))
                            message.applies = [];
                        message.applies.push($root.lq.ResFriendApplyList.FriendApply.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFriendApplyList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFriendApplyList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.applies != null && message.hasOwnProperty("applies")) {
                if (!Array.isArray(message.applies))
                    return "applies: array expected";
                for (let i = 0; i < message.applies.length; ++i) {
                    let error = $root.lq.ResFriendApplyList.FriendApply.verify(message.applies[i], _depth + 1);
                    if (error)
                        return "applies." + error;
                }
            }
            return null;
        };

        ResFriendApplyList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFriendApplyList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFriendApplyList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFriendApplyList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.applies) {
                if (!Array.isArray(object.applies))
                    throw TypeError(".lq.ResFriendApplyList.applies: array expected");
                message.applies = Array(object.applies.length);
                for (let i = 0; i < object.applies.length; ++i) {
                    if (typeof object.applies[i] !== "object")
                        throw TypeError(".lq.ResFriendApplyList.applies: object expected");
                    message.applies[i] = $root.lq.ResFriendApplyList.FriendApply.fromObject(object.applies[i], _depth + 1);
                }
            }
            return message;
        };

        ResFriendApplyList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.applies = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.applies && message.applies.length) {
                object.applies = Array(message.applies.length);
                for (let j = 0; j < message.applies.length; ++j)
                    object.applies[j] = $root.lq.ResFriendApplyList.FriendApply.toObject(message.applies[j], options);
            }
            return object;
        };

        ResFriendApplyList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFriendApplyList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFriendApplyList";
        };

        ResFriendApplyList.FriendApply = (function() {

            function FriendApply(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FriendApply.prototype.account_id = 0;
            FriendApply.prototype.apply_time = 0;

            FriendApply.create = function create(properties) {
                return new FriendApply(properties);
            };

            FriendApply.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.apply_time != null && Object.hasOwnProperty.call(message, "apply_time"))
                    writer.uint32(16).uint32(message.apply_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FriendApply.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FriendApply.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFriendApplyList.FriendApply(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.apply_time = value;
                            else
                                delete message.apply_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FriendApply.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FriendApply.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.apply_time != null && message.hasOwnProperty("apply_time"))
                    if (!$util.isInteger(message.apply_time))
                        return "apply_time: integer expected";
                return null;
            };

            FriendApply.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFriendApplyList.FriendApply)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFriendApplyList.FriendApply();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.apply_time != null)
                    if (Number(object.apply_time) !== 0)
                        message.apply_time = object.apply_time >>> 0;
                return message;
            };

            FriendApply.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.apply_time = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.apply_time != null && message.hasOwnProperty("apply_time"))
                    object.apply_time = message.apply_time;
                return object;
            };

            FriendApply.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FriendApply.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFriendApplyList.FriendApply";
            };

            return FriendApply;
        })();

        return ResFriendApplyList;
    })();

    lq.ReqApplyFriend = (function() {

        function ReqApplyFriend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqApplyFriend.prototype.target_id = 0;

        ReqApplyFriend.create = function create(properties) {
            return new ReqApplyFriend(properties);
        };

        ReqApplyFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqApplyFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqApplyFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqApplyFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqApplyFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqApplyFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            return null;
        };

        ReqApplyFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqApplyFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqApplyFriend();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            return message;
        };

        ReqApplyFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.target_id = 0;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            return object;
        };

        ReqApplyFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqApplyFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqApplyFriend";
        };

        return ReqApplyFriend;
    })();

    lq.ReqHandleFriendApply = (function() {

        function ReqHandleFriendApply(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqHandleFriendApply.prototype.target_id = 0;
        ReqHandleFriendApply.prototype.method = 0;

        ReqHandleFriendApply.create = function create(properties) {
            return new ReqHandleFriendApply(properties);
        };

        ReqHandleFriendApply.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.method != null && Object.hasOwnProperty.call(message, "method"))
                writer.uint32(16).uint32(message.method);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqHandleFriendApply.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqHandleFriendApply.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqHandleFriendApply(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.method = value;
                        else
                            delete message.method;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqHandleFriendApply.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqHandleFriendApply.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.method != null && message.hasOwnProperty("method"))
                if (!$util.isInteger(message.method))
                    return "method: integer expected";
            return null;
        };

        ReqHandleFriendApply.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqHandleFriendApply)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqHandleFriendApply();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.method != null)
                if (Number(object.method) !== 0)
                    message.method = object.method >>> 0;
            return message;
        };

        ReqHandleFriendApply.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.method = 0;
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.method != null && message.hasOwnProperty("method"))
                object.method = message.method;
            return object;
        };

        ReqHandleFriendApply.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqHandleFriendApply.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqHandleFriendApply";
        };

        return ReqHandleFriendApply;
    })();

    lq.ReqRemoveFriend = (function() {

        function ReqRemoveFriend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRemoveFriend.prototype.target_id = 0;

        ReqRemoveFriend.create = function create(properties) {
            return new ReqRemoveFriend(properties);
        };

        ReqRemoveFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRemoveFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRemoveFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRemoveFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRemoveFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRemoveFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            return null;
        };

        ReqRemoveFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRemoveFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRemoveFriend();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            return message;
        };

        ReqRemoveFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.target_id = 0;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            return object;
        };

        ReqRemoveFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRemoveFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRemoveFriend";
        };

        return ReqRemoveFriend;
    })();

    lq.ReqSearchAccountByPattern = (function() {

        function ReqSearchAccountByPattern(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSearchAccountByPattern.prototype.search_next = false;
        ReqSearchAccountByPattern.prototype.pattern = "";

        ReqSearchAccountByPattern.create = function create(properties) {
            return new ReqSearchAccountByPattern(properties);
        };

        ReqSearchAccountByPattern.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.search_next != null && Object.hasOwnProperty.call(message, "search_next"))
                writer.uint32(8).bool(message.search_next);
            if (message.pattern != null && Object.hasOwnProperty.call(message, "pattern"))
                writer.uint32(18).string(message.pattern);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSearchAccountByPattern.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSearchAccountByPattern.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSearchAccountByPattern(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.search_next = value;
                        else
                            delete message.search_next;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.pattern = value;
                        else
                            delete message.pattern;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSearchAccountByPattern.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSearchAccountByPattern.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.search_next != null && message.hasOwnProperty("search_next"))
                if (typeof message.search_next !== "boolean")
                    return "search_next: boolean expected";
            if (message.pattern != null && message.hasOwnProperty("pattern"))
                if (!$util.isString(message.pattern))
                    return "pattern: string expected";
            return null;
        };

        ReqSearchAccountByPattern.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSearchAccountByPattern)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSearchAccountByPattern();
            if (object.search_next != null)
                if (object.search_next)
                    message.search_next = Boolean(object.search_next);
            if (object.pattern != null)
                if (typeof object.pattern !== "string" || object.pattern.length)
                    message.pattern = String(object.pattern);
            return message;
        };

        ReqSearchAccountByPattern.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.search_next = false;
                object.pattern = "";
            }
            if (message.search_next != null && message.hasOwnProperty("search_next"))
                object.search_next = message.search_next;
            if (message.pattern != null && message.hasOwnProperty("pattern"))
                object.pattern = message.pattern;
            return object;
        };

        ReqSearchAccountByPattern.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSearchAccountByPattern.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSearchAccountByPattern";
        };

        return ReqSearchAccountByPattern;
    })();

    lq.ResSearchAccountByPattern = (function() {

        function ResSearchAccountByPattern(properties) {
            this.match_accounts = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSearchAccountByPattern.prototype.error = null;
        ResSearchAccountByPattern.prototype.is_finished = false;
        ResSearchAccountByPattern.prototype.match_accounts = $util.emptyArray;
        ResSearchAccountByPattern.prototype.decode_id = 0;

        ResSearchAccountByPattern.create = function create(properties) {
            return new ResSearchAccountByPattern(properties);
        };

        ResSearchAccountByPattern.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.is_finished != null && Object.hasOwnProperty.call(message, "is_finished"))
                writer.uint32(16).bool(message.is_finished);
            if (message.match_accounts != null && message.match_accounts.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.match_accounts.length; ++i)
                    writer.uint32(message.match_accounts[i]);
                writer.ldelim();
            }
            if (message.decode_id != null && Object.hasOwnProperty.call(message, "decode_id"))
                writer.uint32(32).uint32(message.decode_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSearchAccountByPattern.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSearchAccountByPattern.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSearchAccountByPattern(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_finished = value;
                        else
                            delete message.is_finished;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.match_accounts && message.match_accounts.length))
                                message.match_accounts = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.match_accounts.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.match_accounts && message.match_accounts.length))
                            message.match_accounts = [];
                        message.match_accounts.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.decode_id = value;
                        else
                            delete message.decode_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSearchAccountByPattern.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSearchAccountByPattern.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.is_finished != null && message.hasOwnProperty("is_finished"))
                if (typeof message.is_finished !== "boolean")
                    return "is_finished: boolean expected";
            if (message.match_accounts != null && message.hasOwnProperty("match_accounts")) {
                if (!Array.isArray(message.match_accounts))
                    return "match_accounts: array expected";
                for (let i = 0; i < message.match_accounts.length; ++i)
                    if (!$util.isInteger(message.match_accounts[i]))
                        return "match_accounts: integer[] expected";
            }
            if (message.decode_id != null && message.hasOwnProperty("decode_id"))
                if (!$util.isInteger(message.decode_id))
                    return "decode_id: integer expected";
            return null;
        };

        ResSearchAccountByPattern.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSearchAccountByPattern)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSearchAccountByPattern();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSearchAccountByPattern.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.is_finished != null)
                if (object.is_finished)
                    message.is_finished = Boolean(object.is_finished);
            if (object.match_accounts) {
                if (!Array.isArray(object.match_accounts))
                    throw TypeError(".lq.ResSearchAccountByPattern.match_accounts: array expected");
                message.match_accounts = Array(object.match_accounts.length);
                for (let i = 0; i < object.match_accounts.length; ++i)
                    message.match_accounts[i] = object.match_accounts[i] >>> 0;
            }
            if (object.decode_id != null)
                if (Number(object.decode_id) !== 0)
                    message.decode_id = object.decode_id >>> 0;
            return message;
        };

        ResSearchAccountByPattern.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.match_accounts = [];
            if (options.defaults) {
                object.error = null;
                object.is_finished = false;
                object.decode_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.is_finished != null && message.hasOwnProperty("is_finished"))
                object.is_finished = message.is_finished;
            if (message.match_accounts && message.match_accounts.length) {
                object.match_accounts = Array(message.match_accounts.length);
                for (let j = 0; j < message.match_accounts.length; ++j)
                    object.match_accounts[j] = message.match_accounts[j];
            }
            if (message.decode_id != null && message.hasOwnProperty("decode_id"))
                object.decode_id = message.decode_id;
            return object;
        };

        ResSearchAccountByPattern.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSearchAccountByPattern.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSearchAccountByPattern";
        };

        return ResSearchAccountByPattern;
    })();

    lq.ReqAccountList = (function() {

        function ReqAccountList(properties) {
            this.account_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAccountList.prototype.account_id_list = $util.emptyArray;

        ReqAccountList.create = function create(properties) {
            return new ReqAccountList(properties);
        };

        ReqAccountList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id_list != null && message.account_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.account_id_list.length; ++i)
                    writer.uint32(message.account_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAccountList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAccountList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAccountList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.account_id_list && message.account_id_list.length))
                                message.account_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_id_list && message.account_id_list.length))
                            message.account_id_list = [];
                        message.account_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAccountList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAccountList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id_list != null && message.hasOwnProperty("account_id_list")) {
                if (!Array.isArray(message.account_id_list))
                    return "account_id_list: array expected";
                for (let i = 0; i < message.account_id_list.length; ++i)
                    if (!$util.isInteger(message.account_id_list[i]))
                        return "account_id_list: integer[] expected";
            }
            return null;
        };

        ReqAccountList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAccountList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAccountList();
            if (object.account_id_list) {
                if (!Array.isArray(object.account_id_list))
                    throw TypeError(".lq.ReqAccountList.account_id_list: array expected");
                message.account_id_list = Array(object.account_id_list.length);
                for (let i = 0; i < object.account_id_list.length; ++i)
                    message.account_id_list[i] = object.account_id_list[i] >>> 0;
            }
            return message;
        };

        ReqAccountList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_id_list = [];
            if (message.account_id_list && message.account_id_list.length) {
                object.account_id_list = Array(message.account_id_list.length);
                for (let j = 0; j < message.account_id_list.length; ++j)
                    object.account_id_list[j] = message.account_id_list[j];
            }
            return object;
        };

        ReqAccountList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAccountList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAccountList";
        };

        return ReqAccountList;
    })();

    lq.ResAccountStates = (function() {

        function ResAccountStates(properties) {
            this.states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountStates.prototype.error = null;
        ResAccountStates.prototype.states = $util.emptyArray;

        ResAccountStates.create = function create(properties) {
            return new ResAccountStates(properties);
        };

        ResAccountStates.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.states != null && message.states.length)
                for (let i = 0; i < message.states.length; ++i)
                    $root.lq.AccountActiveState.encode(message.states[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountStates.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountStates.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountStates(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.states && message.states.length))
                            message.states = [];
                        message.states.push($root.lq.AccountActiveState.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountStates.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountStates.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.states != null && message.hasOwnProperty("states")) {
                if (!Array.isArray(message.states))
                    return "states: array expected";
                for (let i = 0; i < message.states.length; ++i) {
                    let error = $root.lq.AccountActiveState.verify(message.states[i], _depth + 1);
                    if (error)
                        return "states." + error;
                }
            }
            return null;
        };

        ResAccountStates.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountStates)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountStates();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountStates.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.states) {
                if (!Array.isArray(object.states))
                    throw TypeError(".lq.ResAccountStates.states: array expected");
                message.states = Array(object.states.length);
                for (let i = 0; i < object.states.length; ++i) {
                    if (typeof object.states[i] !== "object")
                        throw TypeError(".lq.ResAccountStates.states: object expected");
                    message.states[i] = $root.lq.AccountActiveState.fromObject(object.states[i], _depth + 1);
                }
            }
            return message;
        };

        ResAccountStates.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.states = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.states && message.states.length) {
                object.states = Array(message.states.length);
                for (let j = 0; j < message.states.length; ++j)
                    object.states[j] = $root.lq.AccountActiveState.toObject(message.states[j], options);
            }
            return object;
        };

        ResAccountStates.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountStates.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountStates";
        };

        return ResAccountStates;
    })();

    lq.ReqSearchAccountById = (function() {

        function ReqSearchAccountById(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSearchAccountById.prototype.account_id = 0;

        ReqSearchAccountById.create = function create(properties) {
            return new ReqSearchAccountById(properties);
        };

        ReqSearchAccountById.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSearchAccountById.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSearchAccountById.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSearchAccountById(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSearchAccountById.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSearchAccountById.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqSearchAccountById.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSearchAccountById)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSearchAccountById();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqSearchAccountById.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.account_id = 0;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqSearchAccountById.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSearchAccountById.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSearchAccountById";
        };

        return ReqSearchAccountById;
    })();

    lq.ResSearchAccountById = (function() {

        function ResSearchAccountById(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSearchAccountById.prototype.error = null;
        ResSearchAccountById.prototype.player = null;

        ResSearchAccountById.create = function create(properties) {
            return new ResSearchAccountById(properties);
        };

        ResSearchAccountById.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.player != null && Object.hasOwnProperty.call(message, "player"))
                $root.lq.PlayerBaseView.encode(message.player, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSearchAccountById.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSearchAccountById.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSearchAccountById(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.player = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.player);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSearchAccountById.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSearchAccountById.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.player != null && message.hasOwnProperty("player")) {
                let error = $root.lq.PlayerBaseView.verify(message.player, _depth + 1);
                if (error)
                    return "player." + error;
            }
            return null;
        };

        ResSearchAccountById.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSearchAccountById)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSearchAccountById();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSearchAccountById.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.player != null) {
                if (typeof object.player !== "object")
                    throw TypeError(".lq.ResSearchAccountById.player: object expected");
                message.player = $root.lq.PlayerBaseView.fromObject(object.player, _depth + 1);
            }
            return message;
        };

        ResSearchAccountById.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.player = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.player != null && message.hasOwnProperty("player"))
                object.player = $root.lq.PlayerBaseView.toObject(message.player, options);
            return object;
        };

        ResSearchAccountById.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSearchAccountById.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSearchAccountById";
        };

        return ResSearchAccountById;
    })();

    lq.ResBagInfo = (function() {

        function ResBagInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResBagInfo.prototype.error = null;
        ResBagInfo.prototype.bag = null;

        ResBagInfo.create = function create(properties) {
            return new ResBagInfo(properties);
        };

        ResBagInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.bag != null && Object.hasOwnProperty.call(message, "bag"))
                $root.lq.Bag.encode(message.bag, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResBagInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResBagInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResBagInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.bag = $root.lq.Bag.decode(reader, reader.uint32(), undefined, _depth + 1, message.bag);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResBagInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResBagInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.bag != null && message.hasOwnProperty("bag")) {
                let error = $root.lq.Bag.verify(message.bag, _depth + 1);
                if (error)
                    return "bag." + error;
            }
            return null;
        };

        ResBagInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResBagInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResBagInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResBagInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.bag != null) {
                if (typeof object.bag !== "object")
                    throw TypeError(".lq.ResBagInfo.bag: object expected");
                message.bag = $root.lq.Bag.fromObject(object.bag, _depth + 1);
            }
            return message;
        };

        ResBagInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.bag = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.bag != null && message.hasOwnProperty("bag"))
                object.bag = $root.lq.Bag.toObject(message.bag, options);
            return object;
        };

        ResBagInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResBagInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResBagInfo";
        };

        return ResBagInfo;
    })();

    lq.ReqUseBagItem = (function() {

        function ReqUseBagItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUseBagItem.prototype.item_id = 0;

        ReqUseBagItem.create = function create(properties) {
            return new ReqUseBagItem(properties);
        };

        ReqUseBagItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUseBagItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUseBagItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUseBagItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUseBagItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUseBagItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            return null;
        };

        ReqUseBagItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUseBagItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUseBagItem();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            return message;
        };

        ReqUseBagItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.item_id = 0;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            return object;
        };

        ReqUseBagItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUseBagItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUseBagItem";
        };

        return ReqUseBagItem;
    })();

    lq.ReqOpenManualItem = (function() {

        function ReqOpenManualItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenManualItem.prototype.item_id = 0;
        ReqOpenManualItem.prototype.count = 0;
        ReqOpenManualItem.prototype.select_id = 0;

        ReqOpenManualItem.create = function create(properties) {
            return new ReqOpenManualItem(properties);
        };

        ReqOpenManualItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.select_id != null && Object.hasOwnProperty.call(message, "select_id"))
                writer.uint32(24).uint32(message.select_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenManualItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenManualItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenManualItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.select_id = value;
                        else
                            delete message.select_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenManualItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenManualItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.select_id != null && message.hasOwnProperty("select_id"))
                if (!$util.isInteger(message.select_id))
                    return "select_id: integer expected";
            return null;
        };

        ReqOpenManualItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenManualItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenManualItem();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.select_id != null)
                if (Number(object.select_id) !== 0)
                    message.select_id = object.select_id >>> 0;
            return message;
        };

        ReqOpenManualItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.item_id = 0;
                object.count = 0;
                object.select_id = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.select_id != null && message.hasOwnProperty("select_id"))
                object.select_id = message.select_id;
            return object;
        };

        ReqOpenManualItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenManualItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenManualItem";
        };

        return ReqOpenManualItem;
    })();

    lq.ReqOpenRandomRewardItem = (function() {

        function ReqOpenRandomRewardItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenRandomRewardItem.prototype.item_id = 0;
        ReqOpenRandomRewardItem.prototype.count = 0;

        ReqOpenRandomRewardItem.create = function create(properties) {
            return new ReqOpenRandomRewardItem(properties);
        };

        ReqOpenRandomRewardItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenRandomRewardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenRandomRewardItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenRandomRewardItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenRandomRewardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenRandomRewardItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqOpenRandomRewardItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenRandomRewardItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenRandomRewardItem();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqOpenRandomRewardItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.item_id = 0;
                object.count = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqOpenRandomRewardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenRandomRewardItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenRandomRewardItem";
        };

        return ReqOpenRandomRewardItem;
    })();

    lq.ResOpenRandomRewardItem = (function() {

        function ResOpenRandomRewardItem(properties) {
            this.results = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOpenRandomRewardItem.prototype.error = null;
        ResOpenRandomRewardItem.prototype.results = $util.emptyArray;

        ResOpenRandomRewardItem.create = function create(properties) {
            return new ResOpenRandomRewardItem(properties);
        };

        ResOpenRandomRewardItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.lq.OpenResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOpenRandomRewardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOpenRandomRewardItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenRandomRewardItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.lq.OpenResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOpenRandomRewardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOpenRandomRewardItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.results != null && message.hasOwnProperty("results")) {
                if (!Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.lq.OpenResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            return null;
        };

        ResOpenRandomRewardItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOpenRandomRewardItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOpenRandomRewardItem();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOpenRandomRewardItem.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.results) {
                if (!Array.isArray(object.results))
                    throw TypeError(".lq.ResOpenRandomRewardItem.results: array expected");
                message.results = Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (typeof object.results[i] !== "object")
                        throw TypeError(".lq.ResOpenRandomRewardItem.results: object expected");
                    message.results[i] = $root.lq.OpenResult.fromObject(object.results[i], _depth + 1);
                }
            }
            return message;
        };

        ResOpenRandomRewardItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.results = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.results && message.results.length) {
                object.results = Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.lq.OpenResult.toObject(message.results[j], options);
            }
            return object;
        };

        ResOpenRandomRewardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOpenRandomRewardItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOpenRandomRewardItem";
        };

        return ResOpenRandomRewardItem;
    })();

    lq.ReqOpenAllRewardItem = (function() {

        function ReqOpenAllRewardItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenAllRewardItem.prototype.item_id = 0;

        ReqOpenAllRewardItem.create = function create(properties) {
            return new ReqOpenAllRewardItem(properties);
        };

        ReqOpenAllRewardItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenAllRewardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenAllRewardItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenAllRewardItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenAllRewardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenAllRewardItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            return null;
        };

        ReqOpenAllRewardItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenAllRewardItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenAllRewardItem();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            return message;
        };

        ReqOpenAllRewardItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.item_id = 0;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            return object;
        };

        ReqOpenAllRewardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenAllRewardItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenAllRewardItem";
        };

        return ReqOpenAllRewardItem;
    })();

    lq.ResOpenAllRewardItem = (function() {

        function ResOpenAllRewardItem(properties) {
            this.results = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOpenAllRewardItem.prototype.error = null;
        ResOpenAllRewardItem.prototype.results = $util.emptyArray;

        ResOpenAllRewardItem.create = function create(properties) {
            return new ResOpenAllRewardItem(properties);
        };

        ResOpenAllRewardItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.lq.OpenResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOpenAllRewardItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOpenAllRewardItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenAllRewardItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.lq.OpenResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOpenAllRewardItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOpenAllRewardItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.results != null && message.hasOwnProperty("results")) {
                if (!Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.lq.OpenResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            return null;
        };

        ResOpenAllRewardItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOpenAllRewardItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOpenAllRewardItem();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOpenAllRewardItem.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.results) {
                if (!Array.isArray(object.results))
                    throw TypeError(".lq.ResOpenAllRewardItem.results: array expected");
                message.results = Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (typeof object.results[i] !== "object")
                        throw TypeError(".lq.ResOpenAllRewardItem.results: object expected");
                    message.results[i] = $root.lq.OpenResult.fromObject(object.results[i], _depth + 1);
                }
            }
            return message;
        };

        ResOpenAllRewardItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.results = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.results && message.results.length) {
                object.results = Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.lq.OpenResult.toObject(message.results[j], options);
            }
            return object;
        };

        ResOpenAllRewardItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOpenAllRewardItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOpenAllRewardItem";
        };

        return ResOpenAllRewardItem;
    })();

    lq.ReqComposeShard = (function() {

        function ReqComposeShard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqComposeShard.prototype.item_id = 0;

        ReqComposeShard.create = function create(properties) {
            return new ReqComposeShard(properties);
        };

        ReqComposeShard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqComposeShard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqComposeShard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqComposeShard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqComposeShard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqComposeShard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            return null;
        };

        ReqComposeShard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqComposeShard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqComposeShard();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            return message;
        };

        ReqComposeShard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.item_id = 0;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            return object;
        };

        ReqComposeShard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqComposeShard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqComposeShard";
        };

        return ReqComposeShard;
    })();

    lq.ReqFetchAnnouncement = (function() {

        function ReqFetchAnnouncement(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchAnnouncement.prototype.lang = "";
        ReqFetchAnnouncement.prototype.platform = "";

        ReqFetchAnnouncement.create = function create(properties) {
            return new ReqFetchAnnouncement(properties);
        };

        ReqFetchAnnouncement.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                writer.uint32(18).string(message.platform);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchAnnouncement.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchAnnouncement.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchAnnouncement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.platform = value;
                        else
                            delete message.platform;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchAnnouncement.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchAnnouncement.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            if (message.platform != null && message.hasOwnProperty("platform"))
                if (!$util.isString(message.platform))
                    return "platform: string expected";
            return null;
        };

        ReqFetchAnnouncement.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchAnnouncement)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchAnnouncement();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            if (object.platform != null)
                if (typeof object.platform !== "string" || object.platform.length)
                    message.platform = String(object.platform);
            return message;
        };

        ReqFetchAnnouncement.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.lang = "";
                object.platform = "";
            }
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            if (message.platform != null && message.hasOwnProperty("platform"))
                object.platform = message.platform;
            return object;
        };

        ReqFetchAnnouncement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchAnnouncement.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchAnnouncement";
        };

        return ReqFetchAnnouncement;
    })();

    lq.ResAnnouncement = (function() {

        function ResAnnouncement(properties) {
            this.announcements = [];
            this.sort = [];
            this.read_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAnnouncement.prototype.error = null;
        ResAnnouncement.prototype.announcements = $util.emptyArray;
        ResAnnouncement.prototype.sort = $util.emptyArray;
        ResAnnouncement.prototype.read_list = $util.emptyArray;

        ResAnnouncement.create = function create(properties) {
            return new ResAnnouncement(properties);
        };

        ResAnnouncement.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.announcements != null && message.announcements.length)
                for (let i = 0; i < message.announcements.length; ++i)
                    $root.lq.Announcement.encode(message.announcements[i], writer.uint32(18).fork()).ldelim();
            if (message.sort != null && message.sort.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.sort.length; ++i)
                    writer.uint32(message.sort[i]);
                writer.ldelim();
            }
            if (message.read_list != null && message.read_list.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.read_list.length; ++i)
                    writer.uint32(message.read_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAnnouncement.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAnnouncement.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAnnouncement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.announcements && message.announcements.length))
                            message.announcements = [];
                        message.announcements.push($root.lq.Announcement.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.sort && message.sort.length))
                                message.sort = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.sort.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.sort && message.sort.length))
                            message.sort = [];
                        message.sort.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.read_list && message.read_list.length))
                                message.read_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.read_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.read_list && message.read_list.length))
                            message.read_list = [];
                        message.read_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAnnouncement.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAnnouncement.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.announcements != null && message.hasOwnProperty("announcements")) {
                if (!Array.isArray(message.announcements))
                    return "announcements: array expected";
                for (let i = 0; i < message.announcements.length; ++i) {
                    let error = $root.lq.Announcement.verify(message.announcements[i], _depth + 1);
                    if (error)
                        return "announcements." + error;
                }
            }
            if (message.sort != null && message.hasOwnProperty("sort")) {
                if (!Array.isArray(message.sort))
                    return "sort: array expected";
                for (let i = 0; i < message.sort.length; ++i)
                    if (!$util.isInteger(message.sort[i]))
                        return "sort: integer[] expected";
            }
            if (message.read_list != null && message.hasOwnProperty("read_list")) {
                if (!Array.isArray(message.read_list))
                    return "read_list: array expected";
                for (let i = 0; i < message.read_list.length; ++i)
                    if (!$util.isInteger(message.read_list[i]))
                        return "read_list: integer[] expected";
            }
            return null;
        };

        ResAnnouncement.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAnnouncement)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAnnouncement();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAnnouncement.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.announcements) {
                if (!Array.isArray(object.announcements))
                    throw TypeError(".lq.ResAnnouncement.announcements: array expected");
                message.announcements = Array(object.announcements.length);
                for (let i = 0; i < object.announcements.length; ++i) {
                    if (typeof object.announcements[i] !== "object")
                        throw TypeError(".lq.ResAnnouncement.announcements: object expected");
                    message.announcements[i] = $root.lq.Announcement.fromObject(object.announcements[i], _depth + 1);
                }
            }
            if (object.sort) {
                if (!Array.isArray(object.sort))
                    throw TypeError(".lq.ResAnnouncement.sort: array expected");
                message.sort = Array(object.sort.length);
                for (let i = 0; i < object.sort.length; ++i)
                    message.sort[i] = object.sort[i] >>> 0;
            }
            if (object.read_list) {
                if (!Array.isArray(object.read_list))
                    throw TypeError(".lq.ResAnnouncement.read_list: array expected");
                message.read_list = Array(object.read_list.length);
                for (let i = 0; i < object.read_list.length; ++i)
                    message.read_list[i] = object.read_list[i] >>> 0;
            }
            return message;
        };

        ResAnnouncement.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.announcements = [];
                object.sort = [];
                object.read_list = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.announcements && message.announcements.length) {
                object.announcements = Array(message.announcements.length);
                for (let j = 0; j < message.announcements.length; ++j)
                    object.announcements[j] = $root.lq.Announcement.toObject(message.announcements[j], options);
            }
            if (message.sort && message.sort.length) {
                object.sort = Array(message.sort.length);
                for (let j = 0; j < message.sort.length; ++j)
                    object.sort[j] = message.sort[j];
            }
            if (message.read_list && message.read_list.length) {
                object.read_list = Array(message.read_list.length);
                for (let j = 0; j < message.read_list.length; ++j)
                    object.read_list[j] = message.read_list[j];
            }
            return object;
        };

        ResAnnouncement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAnnouncement.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAnnouncement";
        };

        return ResAnnouncement;
    })();

    lq.ResMailInfo = (function() {

        function ResMailInfo(properties) {
            this.mails = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMailInfo.prototype.error = null;
        ResMailInfo.prototype.mails = $util.emptyArray;

        ResMailInfo.create = function create(properties) {
            return new ResMailInfo(properties);
        };

        ResMailInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.mails != null && message.mails.length)
                for (let i = 0; i < message.mails.length; ++i)
                    $root.lq.Mail.encode(message.mails[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMailInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMailInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMailInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mails && message.mails.length))
                            message.mails = [];
                        message.mails.push($root.lq.Mail.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMailInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMailInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.mails != null && message.hasOwnProperty("mails")) {
                if (!Array.isArray(message.mails))
                    return "mails: array expected";
                for (let i = 0; i < message.mails.length; ++i) {
                    let error = $root.lq.Mail.verify(message.mails[i], _depth + 1);
                    if (error)
                        return "mails." + error;
                }
            }
            return null;
        };

        ResMailInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMailInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMailInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMailInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.mails) {
                if (!Array.isArray(object.mails))
                    throw TypeError(".lq.ResMailInfo.mails: array expected");
                message.mails = Array(object.mails.length);
                for (let i = 0; i < object.mails.length; ++i) {
                    if (typeof object.mails[i] !== "object")
                        throw TypeError(".lq.ResMailInfo.mails: object expected");
                    message.mails[i] = $root.lq.Mail.fromObject(object.mails[i], _depth + 1);
                }
            }
            return message;
        };

        ResMailInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.mails = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.mails && message.mails.length) {
                object.mails = Array(message.mails.length);
                for (let j = 0; j < message.mails.length; ++j)
                    object.mails[j] = $root.lq.Mail.toObject(message.mails[j], options);
            }
            return object;
        };

        ResMailInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMailInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMailInfo";
        };

        return ResMailInfo;
    })();

    lq.ReqReadMail = (function() {

        function ReqReadMail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReadMail.prototype.mail_id = 0;

        ReqReadMail.create = function create(properties) {
            return new ReqReadMail(properties);
        };

        ReqReadMail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail_id != null && Object.hasOwnProperty.call(message, "mail_id"))
                writer.uint32(8).uint32(message.mail_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReadMail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReadMail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReadMail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mail_id = value;
                        else
                            delete message.mail_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReadMail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReadMail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                if (!$util.isInteger(message.mail_id))
                    return "mail_id: integer expected";
            return null;
        };

        ReqReadMail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReadMail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReadMail();
            if (object.mail_id != null)
                if (Number(object.mail_id) !== 0)
                    message.mail_id = object.mail_id >>> 0;
            return message;
        };

        ReqReadMail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.mail_id = 0;
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                object.mail_id = message.mail_id;
            return object;
        };

        ReqReadMail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReadMail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReadMail";
        };

        return ReqReadMail;
    })();

    lq.ReqDeleteMail = (function() {

        function ReqDeleteMail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDeleteMail.prototype.mail_id = 0;

        ReqDeleteMail.create = function create(properties) {
            return new ReqDeleteMail(properties);
        };

        ReqDeleteMail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail_id != null && Object.hasOwnProperty.call(message, "mail_id"))
                writer.uint32(8).uint32(message.mail_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDeleteMail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDeleteMail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDeleteMail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mail_id = value;
                        else
                            delete message.mail_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDeleteMail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDeleteMail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                if (!$util.isInteger(message.mail_id))
                    return "mail_id: integer expected";
            return null;
        };

        ReqDeleteMail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDeleteMail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDeleteMail();
            if (object.mail_id != null)
                if (Number(object.mail_id) !== 0)
                    message.mail_id = object.mail_id >>> 0;
            return message;
        };

        ReqDeleteMail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.mail_id = 0;
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                object.mail_id = message.mail_id;
            return object;
        };

        ReqDeleteMail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDeleteMail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDeleteMail";
        };

        return ReqDeleteMail;
    })();

    lq.ReqTakeAttachment = (function() {

        function ReqTakeAttachment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqTakeAttachment.prototype.mail_id = 0;

        ReqTakeAttachment.create = function create(properties) {
            return new ReqTakeAttachment(properties);
        };

        ReqTakeAttachment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mail_id != null && Object.hasOwnProperty.call(message, "mail_id"))
                writer.uint32(8).uint32(message.mail_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqTakeAttachment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqTakeAttachment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqTakeAttachment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mail_id = value;
                        else
                            delete message.mail_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqTakeAttachment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqTakeAttachment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                if (!$util.isInteger(message.mail_id))
                    return "mail_id: integer expected";
            return null;
        };

        ReqTakeAttachment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqTakeAttachment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqTakeAttachment();
            if (object.mail_id != null)
                if (Number(object.mail_id) !== 0)
                    message.mail_id = object.mail_id >>> 0;
            return message;
        };

        ReqTakeAttachment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.mail_id = 0;
            if (message.mail_id != null && message.hasOwnProperty("mail_id"))
                object.mail_id = message.mail_id;
            return object;
        };

        ReqTakeAttachment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqTakeAttachment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqTakeAttachment";
        };

        return ReqTakeAttachment;
    })();

    lq.ReqReceiveAchievementGroupReward = (function() {

        function ReqReceiveAchievementGroupReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveAchievementGroupReward.prototype.group_id = 0;

        ReqReceiveAchievementGroupReward.create = function create(properties) {
            return new ReqReceiveAchievementGroupReward(properties);
        };

        ReqReceiveAchievementGroupReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.group_id != null && Object.hasOwnProperty.call(message, "group_id"))
                writer.uint32(8).uint32(message.group_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveAchievementGroupReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveAchievementGroupReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveAchievementGroupReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.group_id = value;
                        else
                            delete message.group_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveAchievementGroupReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveAchievementGroupReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.group_id != null && message.hasOwnProperty("group_id"))
                if (!$util.isInteger(message.group_id))
                    return "group_id: integer expected";
            return null;
        };

        ReqReceiveAchievementGroupReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveAchievementGroupReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveAchievementGroupReward();
            if (object.group_id != null)
                if (Number(object.group_id) !== 0)
                    message.group_id = object.group_id >>> 0;
            return message;
        };

        ReqReceiveAchievementGroupReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.group_id = 0;
            if (message.group_id != null && message.hasOwnProperty("group_id"))
                object.group_id = message.group_id;
            return object;
        };

        ReqReceiveAchievementGroupReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveAchievementGroupReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveAchievementGroupReward";
        };

        return ReqReceiveAchievementGroupReward;
    })();

    lq.ResReceiveAchievementGroupReward = (function() {

        function ResReceiveAchievementGroupReward(properties) {
            this.execute_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveAchievementGroupReward.prototype.error = null;
        ResReceiveAchievementGroupReward.prototype.execute_reward = $util.emptyArray;

        ResReceiveAchievementGroupReward.create = function create(properties) {
            return new ResReceiveAchievementGroupReward(properties);
        };

        ResReceiveAchievementGroupReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.execute_reward != null && message.execute_reward.length)
                for (let i = 0; i < message.execute_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.execute_reward[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveAchievementGroupReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveAchievementGroupReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveAchievementGroupReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_reward && message.execute_reward.length))
                            message.execute_reward = [];
                        message.execute_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveAchievementGroupReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveAchievementGroupReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.execute_reward != null && message.hasOwnProperty("execute_reward")) {
                if (!Array.isArray(message.execute_reward))
                    return "execute_reward: array expected";
                for (let i = 0; i < message.execute_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.execute_reward[i], _depth + 1);
                    if (error)
                        return "execute_reward." + error;
                }
            }
            return null;
        };

        ResReceiveAchievementGroupReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveAchievementGroupReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveAchievementGroupReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveAchievementGroupReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.execute_reward) {
                if (!Array.isArray(object.execute_reward))
                    throw TypeError(".lq.ResReceiveAchievementGroupReward.execute_reward: array expected");
                message.execute_reward = Array(object.execute_reward.length);
                for (let i = 0; i < object.execute_reward.length; ++i) {
                    if (typeof object.execute_reward[i] !== "object")
                        throw TypeError(".lq.ResReceiveAchievementGroupReward.execute_reward: object expected");
                    message.execute_reward[i] = $root.lq.ExecuteReward.fromObject(object.execute_reward[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveAchievementGroupReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_reward = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.execute_reward && message.execute_reward.length) {
                object.execute_reward = Array(message.execute_reward.length);
                for (let j = 0; j < message.execute_reward.length; ++j)
                    object.execute_reward[j] = $root.lq.ExecuteReward.toObject(message.execute_reward[j], options);
            }
            return object;
        };

        ResReceiveAchievementGroupReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveAchievementGroupReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveAchievementGroupReward";
        };

        return ResReceiveAchievementGroupReward;
    })();

    lq.ReqReceiveAchievementReward = (function() {

        function ReqReceiveAchievementReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveAchievementReward.prototype.achievement_id = 0;

        ReqReceiveAchievementReward.create = function create(properties) {
            return new ReqReceiveAchievementReward(properties);
        };

        ReqReceiveAchievementReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.achievement_id != null && Object.hasOwnProperty.call(message, "achievement_id"))
                writer.uint32(8).uint32(message.achievement_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveAchievementReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveAchievementReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveAchievementReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.achievement_id = value;
                        else
                            delete message.achievement_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveAchievementReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveAchievementReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.achievement_id != null && message.hasOwnProperty("achievement_id"))
                if (!$util.isInteger(message.achievement_id))
                    return "achievement_id: integer expected";
            return null;
        };

        ReqReceiveAchievementReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveAchievementReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveAchievementReward();
            if (object.achievement_id != null)
                if (Number(object.achievement_id) !== 0)
                    message.achievement_id = object.achievement_id >>> 0;
            return message;
        };

        ReqReceiveAchievementReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.achievement_id = 0;
            if (message.achievement_id != null && message.hasOwnProperty("achievement_id"))
                object.achievement_id = message.achievement_id;
            return object;
        };

        ReqReceiveAchievementReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveAchievementReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveAchievementReward";
        };

        return ReqReceiveAchievementReward;
    })();

    lq.ResReceiveAchievementReward = (function() {

        function ResReceiveAchievementReward(properties) {
            this.execute_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveAchievementReward.prototype.error = null;
        ResReceiveAchievementReward.prototype.execute_reward = $util.emptyArray;

        ResReceiveAchievementReward.create = function create(properties) {
            return new ResReceiveAchievementReward(properties);
        };

        ResReceiveAchievementReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.execute_reward != null && message.execute_reward.length)
                for (let i = 0; i < message.execute_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.execute_reward[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveAchievementReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveAchievementReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveAchievementReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_reward && message.execute_reward.length))
                            message.execute_reward = [];
                        message.execute_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveAchievementReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveAchievementReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.execute_reward != null && message.hasOwnProperty("execute_reward")) {
                if (!Array.isArray(message.execute_reward))
                    return "execute_reward: array expected";
                for (let i = 0; i < message.execute_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.execute_reward[i], _depth + 1);
                    if (error)
                        return "execute_reward." + error;
                }
            }
            return null;
        };

        ResReceiveAchievementReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveAchievementReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveAchievementReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveAchievementReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.execute_reward) {
                if (!Array.isArray(object.execute_reward))
                    throw TypeError(".lq.ResReceiveAchievementReward.execute_reward: array expected");
                message.execute_reward = Array(object.execute_reward.length);
                for (let i = 0; i < object.execute_reward.length; ++i) {
                    if (typeof object.execute_reward[i] !== "object")
                        throw TypeError(".lq.ResReceiveAchievementReward.execute_reward: object expected");
                    message.execute_reward[i] = $root.lq.ExecuteReward.fromObject(object.execute_reward[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveAchievementReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_reward = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.execute_reward && message.execute_reward.length) {
                object.execute_reward = Array(message.execute_reward.length);
                for (let j = 0; j < message.execute_reward.length; ++j)
                    object.execute_reward[j] = $root.lq.ExecuteReward.toObject(message.execute_reward[j], options);
            }
            return object;
        };

        ResReceiveAchievementReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveAchievementReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveAchievementReward";
        };

        return ResReceiveAchievementReward;
    })();

    lq.ResFetchAchievementRate = (function() {

        function ResFetchAchievementRate(properties) {
            this.rate = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchAchievementRate.prototype.error = null;
        ResFetchAchievementRate.prototype.rate = $util.emptyArray;

        ResFetchAchievementRate.create = function create(properties) {
            return new ResFetchAchievementRate(properties);
        };

        ResFetchAchievementRate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rate != null && message.rate.length)
                for (let i = 0; i < message.rate.length; ++i)
                    $root.lq.ResFetchAchievementRate.AchievementRate.encode(message.rate[i], writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchAchievementRate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchAchievementRate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAchievementRate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rate && message.rate.length))
                            message.rate = [];
                        message.rate.push($root.lq.ResFetchAchievementRate.AchievementRate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchAchievementRate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchAchievementRate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rate != null && message.hasOwnProperty("rate")) {
                if (!Array.isArray(message.rate))
                    return "rate: array expected";
                for (let i = 0; i < message.rate.length; ++i) {
                    let error = $root.lq.ResFetchAchievementRate.AchievementRate.verify(message.rate[i], _depth + 1);
                    if (error)
                        return "rate." + error;
                }
            }
            return null;
        };

        ResFetchAchievementRate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchAchievementRate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchAchievementRate();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchAchievementRate.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rate) {
                if (!Array.isArray(object.rate))
                    throw TypeError(".lq.ResFetchAchievementRate.rate: array expected");
                message.rate = Array(object.rate.length);
                for (let i = 0; i < object.rate.length; ++i) {
                    if (typeof object.rate[i] !== "object")
                        throw TypeError(".lq.ResFetchAchievementRate.rate: object expected");
                    message.rate[i] = $root.lq.ResFetchAchievementRate.AchievementRate.fromObject(object.rate[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchAchievementRate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rate = [];
            if (options.defaults)
                object.error = null;
            if (message.rate && message.rate.length) {
                object.rate = Array(message.rate.length);
                for (let j = 0; j < message.rate.length; ++j)
                    object.rate[j] = $root.lq.ResFetchAchievementRate.AchievementRate.toObject(message.rate[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResFetchAchievementRate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchAchievementRate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchAchievementRate";
        };

        ResFetchAchievementRate.AchievementRate = (function() {

            function AchievementRate(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AchievementRate.prototype.id = 0;
            AchievementRate.prototype.rate = 0;

            AchievementRate.create = function create(properties) {
                return new AchievementRate(properties);
            };

            AchievementRate.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.rate != null && Object.hasOwnProperty.call(message, "rate"))
                    writer.uint32(16).uint32(message.rate);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AchievementRate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AchievementRate.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAchievementRate.AchievementRate(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rate = value;
                            else
                                delete message.rate;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AchievementRate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AchievementRate.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.rate != null && message.hasOwnProperty("rate"))
                    if (!$util.isInteger(message.rate))
                        return "rate: integer expected";
                return null;
            };

            AchievementRate.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchAchievementRate.AchievementRate)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchAchievementRate.AchievementRate();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.rate != null)
                    if (Number(object.rate) !== 0)
                        message.rate = object.rate >>> 0;
                return message;
            };

            AchievementRate.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.rate = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.rate != null && message.hasOwnProperty("rate"))
                    object.rate = message.rate;
                return object;
            };

            AchievementRate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AchievementRate.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchAchievementRate.AchievementRate";
            };

            return AchievementRate;
        })();

        return ResFetchAchievementRate;
    })();

    lq.ResAchievement = (function() {

        function ResAchievement(properties) {
            this.progresses = [];
            this.rewarded_group = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAchievement.prototype.error = null;
        ResAchievement.prototype.progresses = $util.emptyArray;
        ResAchievement.prototype.rewarded_group = $util.emptyArray;

        ResAchievement.create = function create(properties) {
            return new ResAchievement(properties);
        };

        ResAchievement.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.AchievementProgress.encode(message.progresses[i], writer.uint32(18).fork()).ldelim();
            if (message.rewarded_group != null && message.rewarded_group.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.rewarded_group.length; ++i)
                    writer.uint32(message.rewarded_group[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAchievement.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAchievement.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAchievement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.AchievementProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.rewarded_group && message.rewarded_group.length))
                                message.rewarded_group = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_group.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_group && message.rewarded_group.length))
                            message.rewarded_group = [];
                        message.rewarded_group.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAchievement.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAchievement.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.AchievementProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            if (message.rewarded_group != null && message.hasOwnProperty("rewarded_group")) {
                if (!Array.isArray(message.rewarded_group))
                    return "rewarded_group: array expected";
                for (let i = 0; i < message.rewarded_group.length; ++i)
                    if (!$util.isInteger(message.rewarded_group[i]))
                        return "rewarded_group: integer[] expected";
            }
            return null;
        };

        ResAchievement.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAchievement)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAchievement();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAchievement.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.ResAchievement.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.ResAchievement.progresses: object expected");
                    message.progresses[i] = $root.lq.AchievementProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            if (object.rewarded_group) {
                if (!Array.isArray(object.rewarded_group))
                    throw TypeError(".lq.ResAchievement.rewarded_group: array expected");
                message.rewarded_group = Array(object.rewarded_group.length);
                for (let i = 0; i < object.rewarded_group.length; ++i)
                    message.rewarded_group[i] = object.rewarded_group[i] >>> 0;
            }
            return message;
        };

        ResAchievement.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.progresses = [];
                object.rewarded_group = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.AchievementProgress.toObject(message.progresses[j], options);
            }
            if (message.rewarded_group && message.rewarded_group.length) {
                object.rewarded_group = Array(message.rewarded_group.length);
                for (let j = 0; j < message.rewarded_group.length; ++j)
                    object.rewarded_group[j] = message.rewarded_group[j];
            }
            return object;
        };

        ResAchievement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAchievement.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAchievement";
        };

        return ResAchievement;
    })();

    lq.ResTitleList = (function() {

        function ResTitleList(properties) {
            this.title_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResTitleList.prototype.error = null;
        ResTitleList.prototype.title_list = $util.emptyArray;

        ResTitleList.create = function create(properties) {
            return new ResTitleList(properties);
        };

        ResTitleList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.title_list != null && message.title_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.title_list.length; ++i)
                    writer.uint32(message.title_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResTitleList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResTitleList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResTitleList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.title_list && message.title_list.length))
                                message.title_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.title_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.title_list && message.title_list.length))
                            message.title_list = [];
                        message.title_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResTitleList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResTitleList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.title_list != null && message.hasOwnProperty("title_list")) {
                if (!Array.isArray(message.title_list))
                    return "title_list: array expected";
                for (let i = 0; i < message.title_list.length; ++i)
                    if (!$util.isInteger(message.title_list[i]))
                        return "title_list: integer[] expected";
            }
            return null;
        };

        ResTitleList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResTitleList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResTitleList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResTitleList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.title_list) {
                if (!Array.isArray(object.title_list))
                    throw TypeError(".lq.ResTitleList.title_list: array expected");
                message.title_list = Array(object.title_list.length);
                for (let i = 0; i < object.title_list.length; ++i)
                    message.title_list[i] = object.title_list[i] >>> 0;
            }
            return message;
        };

        ResTitleList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.title_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.title_list && message.title_list.length) {
                object.title_list = Array(message.title_list.length);
                for (let j = 0; j < message.title_list.length; ++j)
                    object.title_list[j] = message.title_list[j];
            }
            return object;
        };

        ResTitleList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResTitleList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResTitleList";
        };

        return ResTitleList;
    })();

    lq.ReqUseTitle = (function() {

        function ReqUseTitle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUseTitle.prototype.title = 0;

        ReqUseTitle.create = function create(properties) {
            return new ReqUseTitle(properties);
        };

        ReqUseTitle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(8).uint32(message.title);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUseTitle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUseTitle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUseTitle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUseTitle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUseTitle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isInteger(message.title))
                    return "title: integer expected";
            return null;
        };

        ReqUseTitle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUseTitle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUseTitle();
            if (object.title != null)
                if (Number(object.title) !== 0)
                    message.title = object.title >>> 0;
            return message;
        };

        ReqUseTitle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.title = 0;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            return object;
        };

        ReqUseTitle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUseTitle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUseTitle";
        };

        return ReqUseTitle;
    })();

    lq.ReqBuyShiLian = (function() {

        function ReqBuyShiLian(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyShiLian.prototype.type = 0;

        ReqBuyShiLian.create = function create(properties) {
            return new ReqBuyShiLian(properties);
        };

        ReqBuyShiLian.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyShiLian.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyShiLian.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyShiLian(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyShiLian.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyShiLian.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqBuyShiLian.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyShiLian)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyShiLian();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqBuyShiLian.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.type = 0;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqBuyShiLian.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyShiLian.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyShiLian";
        };

        return ReqBuyShiLian;
    })();

    lq.ReqUpdateClientValue = (function() {

        function ReqUpdateClientValue(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateClientValue.prototype.key = 0;
        ReqUpdateClientValue.prototype.value = 0;

        ReqUpdateClientValue.create = function create(properties) {
            return new ReqUpdateClientValue(properties);
        };

        ReqUpdateClientValue.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(8).uint32(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(16).uint32(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateClientValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateClientValue.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateClientValue(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateClientValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateClientValue.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.key != null && message.hasOwnProperty("key"))
                if (!$util.isInteger(message.key))
                    return "key: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        ReqUpdateClientValue.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateClientValue)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateClientValue();
            if (object.key != null)
                if (Number(object.key) !== 0)
                    message.key = object.key >>> 0;
            if (object.value != null)
                if (Number(object.value) !== 0)
                    message.value = object.value >>> 0;
            return message;
        };

        ReqUpdateClientValue.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.key = 0;
                object.value = 0;
            }
            if (message.key != null && message.hasOwnProperty("key"))
                object.key = message.key;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        ReqUpdateClientValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateClientValue.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateClientValue";
        };

        return ReqUpdateClientValue;
    })();

    lq.ResClientValue = (function() {

        function ResClientValue(properties) {
            this.datas = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResClientValue.prototype.error = null;
        ResClientValue.prototype.datas = $util.emptyArray;
        ResClientValue.prototype.recharged_count = 0;

        ResClientValue.create = function create(properties) {
            return new ResClientValue(properties);
        };

        ResClientValue.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.datas != null && message.datas.length)
                for (let i = 0; i < message.datas.length; ++i)
                    $root.lq.ResClientValue.Value.encode(message.datas[i], writer.uint32(10).fork()).ldelim();
            if (message.recharged_count != null && Object.hasOwnProperty.call(message, "recharged_count"))
                writer.uint32(16).uint32(message.recharged_count);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResClientValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResClientValue.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResClientValue(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.datas && message.datas.length))
                            message.datas = [];
                        message.datas.push($root.lq.ResClientValue.Value.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.recharged_count = value;
                        else
                            delete message.recharged_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResClientValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResClientValue.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.datas != null && message.hasOwnProperty("datas")) {
                if (!Array.isArray(message.datas))
                    return "datas: array expected";
                for (let i = 0; i < message.datas.length; ++i) {
                    let error = $root.lq.ResClientValue.Value.verify(message.datas[i], _depth + 1);
                    if (error)
                        return "datas." + error;
                }
            }
            if (message.recharged_count != null && message.hasOwnProperty("recharged_count"))
                if (!$util.isInteger(message.recharged_count))
                    return "recharged_count: integer expected";
            return null;
        };

        ResClientValue.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResClientValue)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResClientValue();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResClientValue.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.datas) {
                if (!Array.isArray(object.datas))
                    throw TypeError(".lq.ResClientValue.datas: array expected");
                message.datas = Array(object.datas.length);
                for (let i = 0; i < object.datas.length; ++i) {
                    if (typeof object.datas[i] !== "object")
                        throw TypeError(".lq.ResClientValue.datas: object expected");
                    message.datas[i] = $root.lq.ResClientValue.Value.fromObject(object.datas[i], _depth + 1);
                }
            }
            if (object.recharged_count != null)
                if (Number(object.recharged_count) !== 0)
                    message.recharged_count = object.recharged_count >>> 0;
            return message;
        };

        ResClientValue.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.datas = [];
            if (options.defaults) {
                object.recharged_count = 0;
                object.error = null;
            }
            if (message.datas && message.datas.length) {
                object.datas = Array(message.datas.length);
                for (let j = 0; j < message.datas.length; ++j)
                    object.datas[j] = $root.lq.ResClientValue.Value.toObject(message.datas[j], options);
            }
            if (message.recharged_count != null && message.hasOwnProperty("recharged_count"))
                object.recharged_count = message.recharged_count;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResClientValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResClientValue.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResClientValue";
        };

        ResClientValue.Value = (function() {

            function Value(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Value.prototype.key = 0;
            Value.prototype.value = 0;

            Value.create = function create(properties) {
                return new Value(properties);
            };

            Value.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    writer.uint32(8).uint32(message.key);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(16).uint32(message.value);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Value.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Value.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResClientValue.Value(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.key = value;
                            else
                                delete message.key;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Value.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Value.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.key != null && message.hasOwnProperty("key"))
                    if (!$util.isInteger(message.key))
                        return "key: integer expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            Value.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResClientValue.Value)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResClientValue.Value();
                if (object.key != null)
                    if (Number(object.key) !== 0)
                        message.key = object.key >>> 0;
                if (object.value != null)
                    if (Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            Value.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.key = 0;
                    object.value = 0;
                }
                if (message.key != null && message.hasOwnProperty("key"))
                    object.key = message.key;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                return object;
            };

            Value.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Value.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResClientValue.Value";
            };

            return Value;
        })();

        return ResClientValue;
    })();

    lq.ReqClientMessage = (function() {

        function ReqClientMessage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqClientMessage.prototype.timestamp = 0;
        ReqClientMessage.prototype.message = "";

        ReqClientMessage.create = function create(properties) {
            return new ReqClientMessage(properties);
        };

        ReqClientMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(8).uint32(message.timestamp);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(18).string(message.message);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqClientMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqClientMessage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqClientMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqClientMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqClientMessage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp))
                    return "timestamp: integer expected";
            if (message.message != null && message.hasOwnProperty("message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            return null;
        };

        ReqClientMessage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqClientMessage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqClientMessage();
            if (object.timestamp != null)
                if (Number(object.timestamp) !== 0)
                    message.timestamp = object.timestamp >>> 0;
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = String(object.message);
            return message;
        };

        ReqClientMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.timestamp = 0;
                object.message = "";
            }
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                object.timestamp = message.timestamp;
            if (message.message != null && message.hasOwnProperty("message"))
                object.message = message.message;
            return object;
        };

        ReqClientMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqClientMessage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqClientMessage";
        };

        return ReqClientMessage;
    })();

    lq.ReqCurrentMatchInfo = (function() {

        function ReqCurrentMatchInfo(properties) {
            this.mode_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCurrentMatchInfo.prototype.mode_list = $util.emptyArray;

        ReqCurrentMatchInfo.create = function create(properties) {
            return new ReqCurrentMatchInfo(properties);
        };

        ReqCurrentMatchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode_list != null && message.mode_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.mode_list.length; ++i)
                    writer.uint32(message.mode_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCurrentMatchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCurrentMatchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCurrentMatchInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.mode_list && message.mode_list.length))
                                message.mode_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.mode_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.mode_list && message.mode_list.length))
                            message.mode_list = [];
                        message.mode_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCurrentMatchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCurrentMatchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode_list != null && message.hasOwnProperty("mode_list")) {
                if (!Array.isArray(message.mode_list))
                    return "mode_list: array expected";
                for (let i = 0; i < message.mode_list.length; ++i)
                    if (!$util.isInteger(message.mode_list[i]))
                        return "mode_list: integer[] expected";
            }
            return null;
        };

        ReqCurrentMatchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCurrentMatchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCurrentMatchInfo();
            if (object.mode_list) {
                if (!Array.isArray(object.mode_list))
                    throw TypeError(".lq.ReqCurrentMatchInfo.mode_list: array expected");
                message.mode_list = Array(object.mode_list.length);
                for (let i = 0; i < object.mode_list.length; ++i)
                    message.mode_list[i] = object.mode_list[i] >>> 0;
            }
            return message;
        };

        ReqCurrentMatchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.mode_list = [];
            if (message.mode_list && message.mode_list.length) {
                object.mode_list = Array(message.mode_list.length);
                for (let j = 0; j < message.mode_list.length; ++j)
                    object.mode_list[j] = message.mode_list[j];
            }
            return object;
        };

        ReqCurrentMatchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCurrentMatchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCurrentMatchInfo";
        };

        return ReqCurrentMatchInfo;
    })();

    lq.ResCurrentMatchInfo = (function() {

        function ResCurrentMatchInfo(properties) {
            this.matches = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCurrentMatchInfo.prototype.error = null;
        ResCurrentMatchInfo.prototype.matches = $util.emptyArray;

        ResCurrentMatchInfo.create = function create(properties) {
            return new ResCurrentMatchInfo(properties);
        };

        ResCurrentMatchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.matches != null && message.matches.length)
                for (let i = 0; i < message.matches.length; ++i)
                    $root.lq.ResCurrentMatchInfo.CurrentMatchInfo.encode(message.matches[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCurrentMatchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCurrentMatchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCurrentMatchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.matches && message.matches.length))
                            message.matches = [];
                        message.matches.push($root.lq.ResCurrentMatchInfo.CurrentMatchInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCurrentMatchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCurrentMatchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.matches != null && message.hasOwnProperty("matches")) {
                if (!Array.isArray(message.matches))
                    return "matches: array expected";
                for (let i = 0; i < message.matches.length; ++i) {
                    let error = $root.lq.ResCurrentMatchInfo.CurrentMatchInfo.verify(message.matches[i], _depth + 1);
                    if (error)
                        return "matches." + error;
                }
            }
            return null;
        };

        ResCurrentMatchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCurrentMatchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCurrentMatchInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCurrentMatchInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.matches) {
                if (!Array.isArray(object.matches))
                    throw TypeError(".lq.ResCurrentMatchInfo.matches: array expected");
                message.matches = Array(object.matches.length);
                for (let i = 0; i < object.matches.length; ++i) {
                    if (typeof object.matches[i] !== "object")
                        throw TypeError(".lq.ResCurrentMatchInfo.matches: object expected");
                    message.matches[i] = $root.lq.ResCurrentMatchInfo.CurrentMatchInfo.fromObject(object.matches[i], _depth + 1);
                }
            }
            return message;
        };

        ResCurrentMatchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.matches = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.matches && message.matches.length) {
                object.matches = Array(message.matches.length);
                for (let j = 0; j < message.matches.length; ++j)
                    object.matches[j] = $root.lq.ResCurrentMatchInfo.CurrentMatchInfo.toObject(message.matches[j], options);
            }
            return object;
        };

        ResCurrentMatchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCurrentMatchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCurrentMatchInfo";
        };

        ResCurrentMatchInfo.CurrentMatchInfo = (function() {

            function CurrentMatchInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CurrentMatchInfo.prototype.mode_id = 0;
            CurrentMatchInfo.prototype.playing_count = 0;

            CurrentMatchInfo.create = function create(properties) {
                return new CurrentMatchInfo(properties);
            };

            CurrentMatchInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.mode_id != null && Object.hasOwnProperty.call(message, "mode_id"))
                    writer.uint32(8).uint32(message.mode_id);
                if (message.playing_count != null && Object.hasOwnProperty.call(message, "playing_count"))
                    writer.uint32(16).uint32(message.playing_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CurrentMatchInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CurrentMatchInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCurrentMatchInfo.CurrentMatchInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.mode_id = value;
                            else
                                delete message.mode_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.playing_count = value;
                            else
                                delete message.playing_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CurrentMatchInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CurrentMatchInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                    if (!$util.isInteger(message.mode_id))
                        return "mode_id: integer expected";
                if (message.playing_count != null && message.hasOwnProperty("playing_count"))
                    if (!$util.isInteger(message.playing_count))
                        return "playing_count: integer expected";
                return null;
            };

            CurrentMatchInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResCurrentMatchInfo.CurrentMatchInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResCurrentMatchInfo.CurrentMatchInfo();
                if (object.mode_id != null)
                    if (Number(object.mode_id) !== 0)
                        message.mode_id = object.mode_id >>> 0;
                if (object.playing_count != null)
                    if (Number(object.playing_count) !== 0)
                        message.playing_count = object.playing_count >>> 0;
                return message;
            };

            CurrentMatchInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.mode_id = 0;
                    object.playing_count = 0;
                }
                if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                    object.mode_id = message.mode_id;
                if (message.playing_count != null && message.hasOwnProperty("playing_count"))
                    object.playing_count = message.playing_count;
                return object;
            };

            CurrentMatchInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CurrentMatchInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResCurrentMatchInfo.CurrentMatchInfo";
            };

            return CurrentMatchInfo;
        })();

        return ResCurrentMatchInfo;
    })();

    lq.ReqUserComplain = (function() {

        function ReqUserComplain(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUserComplain.prototype.target_id = 0;
        ReqUserComplain.prototype.type = 0;
        ReqUserComplain.prototype.content = "";
        ReqUserComplain.prototype.game_uuid = "";
        ReqUserComplain.prototype.round_info = null;

        ReqUserComplain.create = function create(properties) {
            return new ReqUserComplain(properties);
        };

        ReqUserComplain.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(26).string(message.content);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(34).string(message.game_uuid);
            if (message.round_info != null && Object.hasOwnProperty.call(message, "round_info"))
                $root.lq.ReqUserComplain.GameRoundInfo.encode(message.round_info, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUserComplain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUserComplain.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUserComplain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.round_info = $root.lq.ReqUserComplain.GameRoundInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.round_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUserComplain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUserComplain.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.round_info != null && message.hasOwnProperty("round_info")) {
                let error = $root.lq.ReqUserComplain.GameRoundInfo.verify(message.round_info, _depth + 1);
                if (error)
                    return "round_info." + error;
            }
            return null;
        };

        ReqUserComplain.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUserComplain)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUserComplain();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.round_info != null) {
                if (typeof object.round_info !== "object")
                    throw TypeError(".lq.ReqUserComplain.round_info: object expected");
                message.round_info = $root.lq.ReqUserComplain.GameRoundInfo.fromObject(object.round_info, _depth + 1);
            }
            return message;
        };

        ReqUserComplain.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.type = 0;
                object.content = "";
                object.game_uuid = "";
                object.round_info = null;
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.round_info != null && message.hasOwnProperty("round_info"))
                object.round_info = $root.lq.ReqUserComplain.GameRoundInfo.toObject(message.round_info, options);
            return object;
        };

        ReqUserComplain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUserComplain.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUserComplain";
        };

        ReqUserComplain.GameRoundInfo = (function() {

            function GameRoundInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameRoundInfo.prototype.chang = 0;
            GameRoundInfo.prototype.ju = 0;
            GameRoundInfo.prototype.ben = 0;
            GameRoundInfo.prototype.seat = 0;
            GameRoundInfo.prototype.xun = 0;

            GameRoundInfo.create = function create(properties) {
                return new GameRoundInfo(properties);
            };

            GameRoundInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                    writer.uint32(8).uint32(message.chang);
                if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                    writer.uint32(16).uint32(message.ju);
                if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                    writer.uint32(24).uint32(message.ben);
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(32).uint32(message.seat);
                if (message.xun != null && Object.hasOwnProperty.call(message, "xun"))
                    writer.uint32(40).uint32(message.xun);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameRoundInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameRoundInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUserComplain.GameRoundInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chang = value;
                            else
                                delete message.chang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ju = value;
                            else
                                delete message.ju;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ben = value;
                            else
                                delete message.ben;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.xun = value;
                            else
                                delete message.xun;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameRoundInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameRoundInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.chang != null && message.hasOwnProperty("chang"))
                    if (!$util.isInteger(message.chang))
                        return "chang: integer expected";
                if (message.ju != null && message.hasOwnProperty("ju"))
                    if (!$util.isInteger(message.ju))
                        return "ju: integer expected";
                if (message.ben != null && message.hasOwnProperty("ben"))
                    if (!$util.isInteger(message.ben))
                        return "ben: integer expected";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.xun != null && message.hasOwnProperty("xun"))
                    if (!$util.isInteger(message.xun))
                        return "xun: integer expected";
                return null;
            };

            GameRoundInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqUserComplain.GameRoundInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqUserComplain.GameRoundInfo();
                if (object.chang != null)
                    if (Number(object.chang) !== 0)
                        message.chang = object.chang >>> 0;
                if (object.ju != null)
                    if (Number(object.ju) !== 0)
                        message.ju = object.ju >>> 0;
                if (object.ben != null)
                    if (Number(object.ben) !== 0)
                        message.ben = object.ben >>> 0;
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.xun != null)
                    if (Number(object.xun) !== 0)
                        message.xun = object.xun >>> 0;
                return message;
            };

            GameRoundInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.chang = 0;
                    object.ju = 0;
                    object.ben = 0;
                    object.seat = 0;
                    object.xun = 0;
                }
                if (message.chang != null && message.hasOwnProperty("chang"))
                    object.chang = message.chang;
                if (message.ju != null && message.hasOwnProperty("ju"))
                    object.ju = message.ju;
                if (message.ben != null && message.hasOwnProperty("ben"))
                    object.ben = message.ben;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.xun != null && message.hasOwnProperty("xun"))
                    object.xun = message.xun;
                return object;
            };

            GameRoundInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameRoundInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqUserComplain.GameRoundInfo";
            };

            return GameRoundInfo;
        })();

        return ReqUserComplain;
    })();

    lq.ReqReadAnnouncement = (function() {

        function ReqReadAnnouncement(properties) {
            this.announcement_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReadAnnouncement.prototype.announcement_id = 0;
        ReqReadAnnouncement.prototype.announcement_list = $util.emptyArray;

        ReqReadAnnouncement.create = function create(properties) {
            return new ReqReadAnnouncement(properties);
        };

        ReqReadAnnouncement.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.announcement_id != null && Object.hasOwnProperty.call(message, "announcement_id"))
                writer.uint32(8).uint32(message.announcement_id);
            if (message.announcement_list != null && message.announcement_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.announcement_list.length; ++i)
                    writer.uint32(message.announcement_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReadAnnouncement.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReadAnnouncement.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReadAnnouncement(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.announcement_id = value;
                        else
                            delete message.announcement_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.announcement_list && message.announcement_list.length))
                                message.announcement_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.announcement_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.announcement_list && message.announcement_list.length))
                            message.announcement_list = [];
                        message.announcement_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReadAnnouncement.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReadAnnouncement.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.announcement_id != null && message.hasOwnProperty("announcement_id"))
                if (!$util.isInteger(message.announcement_id))
                    return "announcement_id: integer expected";
            if (message.announcement_list != null && message.hasOwnProperty("announcement_list")) {
                if (!Array.isArray(message.announcement_list))
                    return "announcement_list: array expected";
                for (let i = 0; i < message.announcement_list.length; ++i)
                    if (!$util.isInteger(message.announcement_list[i]))
                        return "announcement_list: integer[] expected";
            }
            return null;
        };

        ReqReadAnnouncement.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReadAnnouncement)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReadAnnouncement();
            if (object.announcement_id != null)
                if (Number(object.announcement_id) !== 0)
                    message.announcement_id = object.announcement_id >>> 0;
            if (object.announcement_list) {
                if (!Array.isArray(object.announcement_list))
                    throw TypeError(".lq.ReqReadAnnouncement.announcement_list: array expected");
                message.announcement_list = Array(object.announcement_list.length);
                for (let i = 0; i < object.announcement_list.length; ++i)
                    message.announcement_list[i] = object.announcement_list[i] >>> 0;
            }
            return message;
        };

        ReqReadAnnouncement.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.announcement_list = [];
            if (options.defaults)
                object.announcement_id = 0;
            if (message.announcement_id != null && message.hasOwnProperty("announcement_id"))
                object.announcement_id = message.announcement_id;
            if (message.announcement_list && message.announcement_list.length) {
                object.announcement_list = Array(message.announcement_list.length);
                for (let j = 0; j < message.announcement_list.length; ++j)
                    object.announcement_list[j] = message.announcement_list[j];
            }
            return object;
        };

        ReqReadAnnouncement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReadAnnouncement.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReadAnnouncement";
        };

        return ReqReadAnnouncement;
    })();

    lq.ResReviveCoinInfo = (function() {

        function ResReviveCoinInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReviveCoinInfo.prototype.error = null;
        ResReviveCoinInfo.prototype.has_gained = false;

        ResReviveCoinInfo.create = function create(properties) {
            return new ResReviveCoinInfo(properties);
        };

        ResReviveCoinInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.has_gained != null && Object.hasOwnProperty.call(message, "has_gained"))
                writer.uint32(16).bool(message.has_gained);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReviveCoinInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReviveCoinInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReviveCoinInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.has_gained = value;
                        else
                            delete message.has_gained;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReviveCoinInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReviveCoinInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.has_gained != null && message.hasOwnProperty("has_gained"))
                if (typeof message.has_gained !== "boolean")
                    return "has_gained: boolean expected";
            return null;
        };

        ResReviveCoinInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReviveCoinInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReviveCoinInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReviveCoinInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.has_gained != null)
                if (object.has_gained)
                    message.has_gained = Boolean(object.has_gained);
            return message;
        };

        ResReviveCoinInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.has_gained = false;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.has_gained != null && message.hasOwnProperty("has_gained"))
                object.has_gained = message.has_gained;
            return object;
        };

        ResReviveCoinInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReviveCoinInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReviveCoinInfo";
        };

        return ResReviveCoinInfo;
    })();

    lq.ResDailyTask = (function() {

        function ResDailyTask(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDailyTask.prototype.error = null;
        ResDailyTask.prototype.progresses = $util.emptyArray;
        ResDailyTask.prototype.has_refresh_count = false;
        ResDailyTask.prototype.max_daily_task_count = 0;
        ResDailyTask.prototype.refresh_count = 0;

        ResDailyTask.create = function create(properties) {
            return new ResDailyTask(properties);
        };

        ResDailyTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.progresses != null && message.progresses.length)
                for (let i = 0; i < message.progresses.length; ++i)
                    $root.lq.TaskProgress.encode(message.progresses[i], writer.uint32(18).fork()).ldelim();
            if (message.has_refresh_count != null && Object.hasOwnProperty.call(message, "has_refresh_count"))
                writer.uint32(24).bool(message.has_refresh_count);
            if (message.max_daily_task_count != null && Object.hasOwnProperty.call(message, "max_daily_task_count"))
                writer.uint32(32).uint32(message.max_daily_task_count);
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(40).uint32(message.refresh_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDailyTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDailyTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDailyTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.has_refresh_count = value;
                        else
                            delete message.has_refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_daily_task_count = value;
                        else
                            delete message.max_daily_task_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDailyTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDailyTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.progresses[i], _depth + 1);
                    if (error)
                        return "progresses." + error;
                }
            }
            if (message.has_refresh_count != null && message.hasOwnProperty("has_refresh_count"))
                if (typeof message.has_refresh_count !== "boolean")
                    return "has_refresh_count: boolean expected";
            if (message.max_daily_task_count != null && message.hasOwnProperty("max_daily_task_count"))
                if (!$util.isInteger(message.max_daily_task_count))
                    return "max_daily_task_count: integer expected";
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            return null;
        };

        ResDailyTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDailyTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDailyTask();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDailyTask.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.ResDailyTask.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i) {
                    if (typeof object.progresses[i] !== "object")
                        throw TypeError(".lq.ResDailyTask.progresses: object expected");
                    message.progresses[i] = $root.lq.TaskProgress.fromObject(object.progresses[i], _depth + 1);
                }
            }
            if (object.has_refresh_count != null)
                if (object.has_refresh_count)
                    message.has_refresh_count = Boolean(object.has_refresh_count);
            if (object.max_daily_task_count != null)
                if (Number(object.max_daily_task_count) !== 0)
                    message.max_daily_task_count = object.max_daily_task_count >>> 0;
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            return message;
        };

        ResDailyTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (options.defaults) {
                object.error = null;
                object.has_refresh_count = false;
                object.max_daily_task_count = 0;
                object.refresh_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = $root.lq.TaskProgress.toObject(message.progresses[j], options);
            }
            if (message.has_refresh_count != null && message.hasOwnProperty("has_refresh_count"))
                object.has_refresh_count = message.has_refresh_count;
            if (message.max_daily_task_count != null && message.hasOwnProperty("max_daily_task_count"))
                object.max_daily_task_count = message.max_daily_task_count;
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            return object;
        };

        ResDailyTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDailyTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDailyTask";
        };

        return ResDailyTask;
    })();

    lq.ReqRefreshDailyTask = (function() {

        function ReqRefreshDailyTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRefreshDailyTask.prototype.task_id = 0;

        ReqRefreshDailyTask.create = function create(properties) {
            return new ReqRefreshDailyTask(properties);
        };

        ReqRefreshDailyTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRefreshDailyTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRefreshDailyTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRefreshDailyTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRefreshDailyTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRefreshDailyTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            return null;
        };

        ReqRefreshDailyTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRefreshDailyTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRefreshDailyTask();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            return message;
        };

        ReqRefreshDailyTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.task_id = 0;
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            return object;
        };

        ReqRefreshDailyTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRefreshDailyTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRefreshDailyTask";
        };

        return ReqRefreshDailyTask;
    })();

    lq.ResRefreshDailyTask = (function() {

        function ResRefreshDailyTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRefreshDailyTask.prototype.error = null;
        ResRefreshDailyTask.prototype.progress = null;
        ResRefreshDailyTask.prototype.refresh_count = 0;

        ResRefreshDailyTask.create = function create(properties) {
            return new ResRefreshDailyTask(properties);
        };

        ResRefreshDailyTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                $root.lq.TaskProgress.encode(message.progress, writer.uint32(18).fork()).ldelim();
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRefreshDailyTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRefreshDailyTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRefreshDailyTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.progress = $root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1, message.progress);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRefreshDailyTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRefreshDailyTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.progress != null && message.hasOwnProperty("progress")) {
                let error = $root.lq.TaskProgress.verify(message.progress, _depth + 1);
                if (error)
                    return "progress." + error;
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            return null;
        };

        ResRefreshDailyTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRefreshDailyTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRefreshDailyTask();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRefreshDailyTask.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.progress != null) {
                if (typeof object.progress !== "object")
                    throw TypeError(".lq.ResRefreshDailyTask.progress: object expected");
                message.progress = $root.lq.TaskProgress.fromObject(object.progress, _depth + 1);
            }
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            return message;
        };

        ResRefreshDailyTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.progress = null;
                object.refresh_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.progress != null && message.hasOwnProperty("progress"))
                object.progress = $root.lq.TaskProgress.toObject(message.progress, options);
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            return object;
        };

        ResRefreshDailyTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRefreshDailyTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRefreshDailyTask";
        };

        return ResRefreshDailyTask;
    })();

    lq.ReqUseGiftCode = (function() {

        function ReqUseGiftCode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUseGiftCode.prototype.code = "";

        ReqUseGiftCode.create = function create(properties) {
            return new ReqUseGiftCode(properties);
        };

        ReqUseGiftCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(10).string(message.code);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUseGiftCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUseGiftCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUseGiftCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUseGiftCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUseGiftCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            return null;
        };

        ReqUseGiftCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUseGiftCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUseGiftCode();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            return message;
        };

        ReqUseGiftCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.code = "";
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            return object;
        };

        ReqUseGiftCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUseGiftCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUseGiftCode";
        };

        return ReqUseGiftCode;
    })();

    lq.ResUseGiftCode = (function() {

        function ResUseGiftCode(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResUseGiftCode.prototype.error = null;
        ResUseGiftCode.prototype.rewards = $util.emptyArray;

        ResUseGiftCode.create = function create(properties) {
            return new ResUseGiftCode(properties);
        };

        ResUseGiftCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResUseGiftCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResUseGiftCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResUseGiftCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResUseGiftCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResUseGiftCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResUseGiftCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResUseGiftCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResUseGiftCode();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResUseGiftCode.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResUseGiftCode.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResUseGiftCode.rewards: object expected");
                    message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResUseGiftCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResUseGiftCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResUseGiftCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResUseGiftCode";
        };

        return ResUseGiftCode;
    })();

    lq.ResUseSpecialGiftCode = (function() {

        function ResUseSpecialGiftCode(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResUseSpecialGiftCode.prototype.error = null;
        ResUseSpecialGiftCode.prototype.rewards = $util.emptyArray;

        ResUseSpecialGiftCode.create = function create(properties) {
            return new ResUseSpecialGiftCode(properties);
        };

        ResUseSpecialGiftCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResUseSpecialGiftCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResUseSpecialGiftCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResUseSpecialGiftCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResUseSpecialGiftCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResUseSpecialGiftCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResUseSpecialGiftCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResUseSpecialGiftCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResUseSpecialGiftCode();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResUseSpecialGiftCode.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResUseSpecialGiftCode.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResUseSpecialGiftCode.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResUseSpecialGiftCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResUseSpecialGiftCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResUseSpecialGiftCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResUseSpecialGiftCode";
        };

        return ResUseSpecialGiftCode;
    })();

    lq.ReqSendClientMessage = (function() {

        function ReqSendClientMessage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSendClientMessage.prototype.target_id = 0;
        ReqSendClientMessage.prototype.type = 0;
        ReqSendClientMessage.prototype.content = "";

        ReqSendClientMessage.create = function create(properties) {
            return new ReqSendClientMessage(properties);
        };

        ReqSendClientMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(26).string(message.content);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSendClientMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSendClientMessage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSendClientMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSendClientMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSendClientMessage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            return null;
        };

        ReqSendClientMessage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSendClientMessage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSendClientMessage();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            return message;
        };

        ReqSendClientMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.type = 0;
                object.content = "";
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            return object;
        };

        ReqSendClientMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSendClientMessage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSendClientMessage";
        };

        return ReqSendClientMessage;
    })();

    lq.ReqGameLiveInfo = (function() {

        function ReqGameLiveInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameLiveInfo.prototype.game_uuid = "";

        ReqGameLiveInfo.create = function create(properties) {
            return new ReqGameLiveInfo(properties);
        };

        ReqGameLiveInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameLiveInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameLiveInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameLiveInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameLiveInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameLiveInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            return null;
        };

        ReqGameLiveInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameLiveInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameLiveInfo();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            return message;
        };

        ReqGameLiveInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.game_uuid = "";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            return object;
        };

        ReqGameLiveInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameLiveInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameLiveInfo";
        };

        return ReqGameLiveInfo;
    })();

    lq.ResGameLiveInfo = (function() {

        function ResGameLiveInfo(properties) {
            this.segments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameLiveInfo.prototype.error = null;
        ResGameLiveInfo.prototype.left_start_seconds = 0;
        ResGameLiveInfo.prototype.live_head = null;
        ResGameLiveInfo.prototype.segments = $util.emptyArray;
        ResGameLiveInfo.prototype.now_millisecond = 0;

        ResGameLiveInfo.create = function create(properties) {
            return new ResGameLiveInfo(properties);
        };

        ResGameLiveInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.left_start_seconds != null && Object.hasOwnProperty.call(message, "left_start_seconds"))
                writer.uint32(16).uint32(message.left_start_seconds);
            if (message.live_head != null && Object.hasOwnProperty.call(message, "live_head"))
                $root.lq.GameLiveHead.encode(message.live_head, writer.uint32(26).fork()).ldelim();
            if (message.segments != null && message.segments.length)
                for (let i = 0; i < message.segments.length; ++i)
                    $root.lq.GameLiveSegmentUri.encode(message.segments[i], writer.uint32(34).fork()).ldelim();
            if (message.now_millisecond != null && Object.hasOwnProperty.call(message, "now_millisecond"))
                writer.uint32(40).uint32(message.now_millisecond);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameLiveInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameLiveInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameLiveInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_start_seconds = value;
                        else
                            delete message.left_start_seconds;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.live_head = $root.lq.GameLiveHead.decode(reader, reader.uint32(), undefined, _depth + 1, message.live_head);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.segments && message.segments.length))
                            message.segments = [];
                        message.segments.push($root.lq.GameLiveSegmentUri.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.now_millisecond = value;
                        else
                            delete message.now_millisecond;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameLiveInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameLiveInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.left_start_seconds != null && message.hasOwnProperty("left_start_seconds"))
                if (!$util.isInteger(message.left_start_seconds))
                    return "left_start_seconds: integer expected";
            if (message.live_head != null && message.hasOwnProperty("live_head")) {
                let error = $root.lq.GameLiveHead.verify(message.live_head, _depth + 1);
                if (error)
                    return "live_head." + error;
            }
            if (message.segments != null && message.hasOwnProperty("segments")) {
                if (!Array.isArray(message.segments))
                    return "segments: array expected";
                for (let i = 0; i < message.segments.length; ++i) {
                    let error = $root.lq.GameLiveSegmentUri.verify(message.segments[i], _depth + 1);
                    if (error)
                        return "segments." + error;
                }
            }
            if (message.now_millisecond != null && message.hasOwnProperty("now_millisecond"))
                if (!$util.isInteger(message.now_millisecond))
                    return "now_millisecond: integer expected";
            return null;
        };

        ResGameLiveInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameLiveInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameLiveInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameLiveInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.left_start_seconds != null)
                if (Number(object.left_start_seconds) !== 0)
                    message.left_start_seconds = object.left_start_seconds >>> 0;
            if (object.live_head != null) {
                if (typeof object.live_head !== "object")
                    throw TypeError(".lq.ResGameLiveInfo.live_head: object expected");
                message.live_head = $root.lq.GameLiveHead.fromObject(object.live_head, _depth + 1);
            }
            if (object.segments) {
                if (!Array.isArray(object.segments))
                    throw TypeError(".lq.ResGameLiveInfo.segments: array expected");
                message.segments = Array(object.segments.length);
                for (let i = 0; i < object.segments.length; ++i) {
                    if (typeof object.segments[i] !== "object")
                        throw TypeError(".lq.ResGameLiveInfo.segments: object expected");
                    message.segments[i] = $root.lq.GameLiveSegmentUri.fromObject(object.segments[i], _depth + 1);
                }
            }
            if (object.now_millisecond != null)
                if (Number(object.now_millisecond) !== 0)
                    message.now_millisecond = object.now_millisecond >>> 0;
            return message;
        };

        ResGameLiveInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.segments = [];
            if (options.defaults) {
                object.error = null;
                object.left_start_seconds = 0;
                object.live_head = null;
                object.now_millisecond = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.left_start_seconds != null && message.hasOwnProperty("left_start_seconds"))
                object.left_start_seconds = message.left_start_seconds;
            if (message.live_head != null && message.hasOwnProperty("live_head"))
                object.live_head = $root.lq.GameLiveHead.toObject(message.live_head, options);
            if (message.segments && message.segments.length) {
                object.segments = Array(message.segments.length);
                for (let j = 0; j < message.segments.length; ++j)
                    object.segments[j] = $root.lq.GameLiveSegmentUri.toObject(message.segments[j], options);
            }
            if (message.now_millisecond != null && message.hasOwnProperty("now_millisecond"))
                object.now_millisecond = message.now_millisecond;
            return object;
        };

        ResGameLiveInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameLiveInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameLiveInfo";
        };

        return ResGameLiveInfo;
    })();

    lq.ReqGameLiveLeftSegment = (function() {

        function ReqGameLiveLeftSegment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameLiveLeftSegment.prototype.game_uuid = "";
        ReqGameLiveLeftSegment.prototype.last_segment_id = 0;

        ReqGameLiveLeftSegment.create = function create(properties) {
            return new ReqGameLiveLeftSegment(properties);
        };

        ReqGameLiveLeftSegment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.last_segment_id != null && Object.hasOwnProperty.call(message, "last_segment_id"))
                writer.uint32(16).uint32(message.last_segment_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameLiveLeftSegment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameLiveLeftSegment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameLiveLeftSegment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_segment_id = value;
                        else
                            delete message.last_segment_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameLiveLeftSegment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameLiveLeftSegment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.last_segment_id != null && message.hasOwnProperty("last_segment_id"))
                if (!$util.isInteger(message.last_segment_id))
                    return "last_segment_id: integer expected";
            return null;
        };

        ReqGameLiveLeftSegment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameLiveLeftSegment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameLiveLeftSegment();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.last_segment_id != null)
                if (Number(object.last_segment_id) !== 0)
                    message.last_segment_id = object.last_segment_id >>> 0;
            return message;
        };

        ReqGameLiveLeftSegment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_uuid = "";
                object.last_segment_id = 0;
            }
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.last_segment_id != null && message.hasOwnProperty("last_segment_id"))
                object.last_segment_id = message.last_segment_id;
            return object;
        };

        ReqGameLiveLeftSegment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameLiveLeftSegment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameLiveLeftSegment";
        };

        return ReqGameLiveLeftSegment;
    })();

    lq.ResGameLiveLeftSegment = (function() {

        function ResGameLiveLeftSegment(properties) {
            this.segments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameLiveLeftSegment.prototype.error = null;
        ResGameLiveLeftSegment.prototype.live_state = 0;
        ResGameLiveLeftSegment.prototype.segments = $util.emptyArray;
        ResGameLiveLeftSegment.prototype.now_millisecond = 0;
        ResGameLiveLeftSegment.prototype.segment_end_millisecond = 0;

        ResGameLiveLeftSegment.create = function create(properties) {
            return new ResGameLiveLeftSegment(properties);
        };

        ResGameLiveLeftSegment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.live_state != null && Object.hasOwnProperty.call(message, "live_state"))
                writer.uint32(16).uint32(message.live_state);
            if (message.segments != null && message.segments.length)
                for (let i = 0; i < message.segments.length; ++i)
                    $root.lq.GameLiveSegmentUri.encode(message.segments[i], writer.uint32(34).fork()).ldelim();
            if (message.now_millisecond != null && Object.hasOwnProperty.call(message, "now_millisecond"))
                writer.uint32(40).uint32(message.now_millisecond);
            if (message.segment_end_millisecond != null && Object.hasOwnProperty.call(message, "segment_end_millisecond"))
                writer.uint32(48).uint32(message.segment_end_millisecond);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameLiveLeftSegment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameLiveLeftSegment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameLiveLeftSegment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.live_state = value;
                        else
                            delete message.live_state;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.segments && message.segments.length))
                            message.segments = [];
                        message.segments.push($root.lq.GameLiveSegmentUri.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.now_millisecond = value;
                        else
                            delete message.now_millisecond;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.segment_end_millisecond = value;
                        else
                            delete message.segment_end_millisecond;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameLiveLeftSegment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameLiveLeftSegment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.live_state != null && message.hasOwnProperty("live_state"))
                if (!$util.isInteger(message.live_state))
                    return "live_state: integer expected";
            if (message.segments != null && message.hasOwnProperty("segments")) {
                if (!Array.isArray(message.segments))
                    return "segments: array expected";
                for (let i = 0; i < message.segments.length; ++i) {
                    let error = $root.lq.GameLiveSegmentUri.verify(message.segments[i], _depth + 1);
                    if (error)
                        return "segments." + error;
                }
            }
            if (message.now_millisecond != null && message.hasOwnProperty("now_millisecond"))
                if (!$util.isInteger(message.now_millisecond))
                    return "now_millisecond: integer expected";
            if (message.segment_end_millisecond != null && message.hasOwnProperty("segment_end_millisecond"))
                if (!$util.isInteger(message.segment_end_millisecond))
                    return "segment_end_millisecond: integer expected";
            return null;
        };

        ResGameLiveLeftSegment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameLiveLeftSegment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameLiveLeftSegment();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameLiveLeftSegment.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.live_state != null)
                if (Number(object.live_state) !== 0)
                    message.live_state = object.live_state >>> 0;
            if (object.segments) {
                if (!Array.isArray(object.segments))
                    throw TypeError(".lq.ResGameLiveLeftSegment.segments: array expected");
                message.segments = Array(object.segments.length);
                for (let i = 0; i < object.segments.length; ++i) {
                    if (typeof object.segments[i] !== "object")
                        throw TypeError(".lq.ResGameLiveLeftSegment.segments: object expected");
                    message.segments[i] = $root.lq.GameLiveSegmentUri.fromObject(object.segments[i], _depth + 1);
                }
            }
            if (object.now_millisecond != null)
                if (Number(object.now_millisecond) !== 0)
                    message.now_millisecond = object.now_millisecond >>> 0;
            if (object.segment_end_millisecond != null)
                if (Number(object.segment_end_millisecond) !== 0)
                    message.segment_end_millisecond = object.segment_end_millisecond >>> 0;
            return message;
        };

        ResGameLiveLeftSegment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.segments = [];
            if (options.defaults) {
                object.error = null;
                object.live_state = 0;
                object.now_millisecond = 0;
                object.segment_end_millisecond = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.live_state != null && message.hasOwnProperty("live_state"))
                object.live_state = message.live_state;
            if (message.segments && message.segments.length) {
                object.segments = Array(message.segments.length);
                for (let j = 0; j < message.segments.length; ++j)
                    object.segments[j] = $root.lq.GameLiveSegmentUri.toObject(message.segments[j], options);
            }
            if (message.now_millisecond != null && message.hasOwnProperty("now_millisecond"))
                object.now_millisecond = message.now_millisecond;
            if (message.segment_end_millisecond != null && message.hasOwnProperty("segment_end_millisecond"))
                object.segment_end_millisecond = message.segment_end_millisecond;
            return object;
        };

        ResGameLiveLeftSegment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameLiveLeftSegment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameLiveLeftSegment";
        };

        return ResGameLiveLeftSegment;
    })();

    lq.ReqGameLiveList = (function() {

        function ReqGameLiveList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGameLiveList.prototype.filter_id = 0;

        ReqGameLiveList.create = function create(properties) {
            return new ReqGameLiveList(properties);
        };

        ReqGameLiveList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.filter_id != null && Object.hasOwnProperty.call(message, "filter_id"))
                writer.uint32(8).uint32(message.filter_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGameLiveList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGameLiveList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGameLiveList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.filter_id = value;
                        else
                            delete message.filter_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGameLiveList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGameLiveList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.filter_id != null && message.hasOwnProperty("filter_id"))
                if (!$util.isInteger(message.filter_id))
                    return "filter_id: integer expected";
            return null;
        };

        ReqGameLiveList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGameLiveList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGameLiveList();
            if (object.filter_id != null)
                if (Number(object.filter_id) !== 0)
                    message.filter_id = object.filter_id >>> 0;
            return message;
        };

        ReqGameLiveList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.filter_id = 0;
            if (message.filter_id != null && message.hasOwnProperty("filter_id"))
                object.filter_id = message.filter_id;
            return object;
        };

        ReqGameLiveList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGameLiveList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGameLiveList";
        };

        return ReqGameLiveList;
    })();

    lq.ResGameLiveList = (function() {

        function ResGameLiveList(properties) {
            this.live_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameLiveList.prototype.error = null;
        ResGameLiveList.prototype.live_list = $util.emptyArray;

        ResGameLiveList.create = function create(properties) {
            return new ResGameLiveList(properties);
        };

        ResGameLiveList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.live_list != null && message.live_list.length)
                for (let i = 0; i < message.live_list.length; ++i)
                    $root.lq.GameLiveHead.encode(message.live_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameLiveList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameLiveList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameLiveList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.live_list && message.live_list.length))
                            message.live_list = [];
                        message.live_list.push($root.lq.GameLiveHead.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameLiveList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameLiveList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.live_list != null && message.hasOwnProperty("live_list")) {
                if (!Array.isArray(message.live_list))
                    return "live_list: array expected";
                for (let i = 0; i < message.live_list.length; ++i) {
                    let error = $root.lq.GameLiveHead.verify(message.live_list[i], _depth + 1);
                    if (error)
                        return "live_list." + error;
                }
            }
            return null;
        };

        ResGameLiveList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameLiveList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameLiveList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameLiveList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.live_list) {
                if (!Array.isArray(object.live_list))
                    throw TypeError(".lq.ResGameLiveList.live_list: array expected");
                message.live_list = Array(object.live_list.length);
                for (let i = 0; i < object.live_list.length; ++i) {
                    if (typeof object.live_list[i] !== "object")
                        throw TypeError(".lq.ResGameLiveList.live_list: object expected");
                    message.live_list[i] = $root.lq.GameLiveHead.fromObject(object.live_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResGameLiveList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.live_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.live_list && message.live_list.length) {
                object.live_list = Array(message.live_list.length);
                for (let j = 0; j < message.live_list.length; ++j)
                    object.live_list[j] = $root.lq.GameLiveHead.toObject(message.live_list[j], options);
            }
            return object;
        };

        ResGameLiveList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameLiveList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameLiveList";
        };

        return ResGameLiveList;
    })();

    lq.ResCommentSetting = (function() {

        function ResCommentSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCommentSetting.prototype.error = null;
        ResCommentSetting.prototype.comment_allow = 0;

        ResCommentSetting.create = function create(properties) {
            return new ResCommentSetting(properties);
        };

        ResCommentSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.comment_allow != null && Object.hasOwnProperty.call(message, "comment_allow"))
                writer.uint32(16).uint32(message.comment_allow);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCommentSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCommentSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCommentSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.comment_allow = value;
                        else
                            delete message.comment_allow;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCommentSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCommentSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                if (!$util.isInteger(message.comment_allow))
                    return "comment_allow: integer expected";
            return null;
        };

        ResCommentSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCommentSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCommentSetting();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCommentSetting.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.comment_allow != null)
                if (Number(object.comment_allow) !== 0)
                    message.comment_allow = object.comment_allow >>> 0;
            return message;
        };

        ResCommentSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.comment_allow = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                object.comment_allow = message.comment_allow;
            return object;
        };

        ResCommentSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCommentSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCommentSetting";
        };

        return ResCommentSetting;
    })();

    lq.ReqUpdateCommentSetting = (function() {

        function ReqUpdateCommentSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateCommentSetting.prototype.comment_allow = 0;

        ReqUpdateCommentSetting.create = function create(properties) {
            return new ReqUpdateCommentSetting(properties);
        };

        ReqUpdateCommentSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.comment_allow != null && Object.hasOwnProperty.call(message, "comment_allow"))
                writer.uint32(8).uint32(message.comment_allow);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateCommentSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateCommentSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateCommentSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.comment_allow = value;
                        else
                            delete message.comment_allow;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateCommentSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateCommentSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                if (!$util.isInteger(message.comment_allow))
                    return "comment_allow: integer expected";
            return null;
        };

        ReqUpdateCommentSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateCommentSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateCommentSetting();
            if (object.comment_allow != null)
                if (Number(object.comment_allow) !== 0)
                    message.comment_allow = object.comment_allow >>> 0;
            return message;
        };

        ReqUpdateCommentSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.comment_allow = 0;
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                object.comment_allow = message.comment_allow;
            return object;
        };

        ReqUpdateCommentSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateCommentSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateCommentSetting";
        };

        return ReqUpdateCommentSetting;
    })();

    lq.ReqFetchCommentList = (function() {

        function ReqFetchCommentList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCommentList.prototype.target_id = 0;

        ReqFetchCommentList.create = function create(properties) {
            return new ReqFetchCommentList(properties);
        };

        ReqFetchCommentList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCommentList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCommentList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCommentList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCommentList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCommentList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            return null;
        };

        ReqFetchCommentList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCommentList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCommentList();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            return message;
        };

        ReqFetchCommentList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.target_id = 0;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            return object;
        };

        ReqFetchCommentList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCommentList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCommentList";
        };

        return ReqFetchCommentList;
    })();

    lq.ResFetchCommentList = (function() {

        function ResFetchCommentList(properties) {
            this.comment_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCommentList.prototype.error = null;
        ResFetchCommentList.prototype.comment_allow = 0;
        ResFetchCommentList.prototype.comment_id_list = $util.emptyArray;
        ResFetchCommentList.prototype.last_read_id = 0;

        ResFetchCommentList.create = function create(properties) {
            return new ResFetchCommentList(properties);
        };

        ResFetchCommentList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.comment_allow != null && Object.hasOwnProperty.call(message, "comment_allow"))
                writer.uint32(16).uint32(message.comment_allow);
            if (message.comment_id_list != null && message.comment_id_list.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.comment_id_list.length; ++i)
                    writer.uint32(message.comment_id_list[i]);
                writer.ldelim();
            }
            if (message.last_read_id != null && Object.hasOwnProperty.call(message, "last_read_id"))
                writer.uint32(32).uint32(message.last_read_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCommentList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCommentList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCommentList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.comment_allow = value;
                        else
                            delete message.comment_allow;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.comment_id_list && message.comment_id_list.length))
                                message.comment_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.comment_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.comment_id_list && message.comment_id_list.length))
                            message.comment_id_list = [];
                        message.comment_id_list.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_read_id = value;
                        else
                            delete message.last_read_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCommentList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCommentList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                if (!$util.isInteger(message.comment_allow))
                    return "comment_allow: integer expected";
            if (message.comment_id_list != null && message.hasOwnProperty("comment_id_list")) {
                if (!Array.isArray(message.comment_id_list))
                    return "comment_id_list: array expected";
                for (let i = 0; i < message.comment_id_list.length; ++i)
                    if (!$util.isInteger(message.comment_id_list[i]))
                        return "comment_id_list: integer[] expected";
            }
            if (message.last_read_id != null && message.hasOwnProperty("last_read_id"))
                if (!$util.isInteger(message.last_read_id))
                    return "last_read_id: integer expected";
            return null;
        };

        ResFetchCommentList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCommentList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCommentList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCommentList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.comment_allow != null)
                if (Number(object.comment_allow) !== 0)
                    message.comment_allow = object.comment_allow >>> 0;
            if (object.comment_id_list) {
                if (!Array.isArray(object.comment_id_list))
                    throw TypeError(".lq.ResFetchCommentList.comment_id_list: array expected");
                message.comment_id_list = Array(object.comment_id_list.length);
                for (let i = 0; i < object.comment_id_list.length; ++i)
                    message.comment_id_list[i] = object.comment_id_list[i] >>> 0;
            }
            if (object.last_read_id != null)
                if (Number(object.last_read_id) !== 0)
                    message.last_read_id = object.last_read_id >>> 0;
            return message;
        };

        ResFetchCommentList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.comment_id_list = [];
            if (options.defaults) {
                object.error = null;
                object.comment_allow = 0;
                object.last_read_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.comment_allow != null && message.hasOwnProperty("comment_allow"))
                object.comment_allow = message.comment_allow;
            if (message.comment_id_list && message.comment_id_list.length) {
                object.comment_id_list = Array(message.comment_id_list.length);
                for (let j = 0; j < message.comment_id_list.length; ++j)
                    object.comment_id_list[j] = message.comment_id_list[j];
            }
            if (message.last_read_id != null && message.hasOwnProperty("last_read_id"))
                object.last_read_id = message.last_read_id;
            return object;
        };

        ResFetchCommentList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCommentList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCommentList";
        };

        return ResFetchCommentList;
    })();

    lq.ReqFetchCommentContent = (function() {

        function ReqFetchCommentContent(properties) {
            this.comment_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCommentContent.prototype.target_id = 0;
        ReqFetchCommentContent.prototype.comment_id_list = $util.emptyArray;

        ReqFetchCommentContent.create = function create(properties) {
            return new ReqFetchCommentContent(properties);
        };

        ReqFetchCommentContent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.comment_id_list != null && message.comment_id_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.comment_id_list.length; ++i)
                    writer.uint32(message.comment_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCommentContent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCommentContent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCommentContent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.comment_id_list && message.comment_id_list.length))
                                message.comment_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.comment_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.comment_id_list && message.comment_id_list.length))
                            message.comment_id_list = [];
                        message.comment_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCommentContent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCommentContent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.comment_id_list != null && message.hasOwnProperty("comment_id_list")) {
                if (!Array.isArray(message.comment_id_list))
                    return "comment_id_list: array expected";
                for (let i = 0; i < message.comment_id_list.length; ++i)
                    if (!$util.isInteger(message.comment_id_list[i]))
                        return "comment_id_list: integer[] expected";
            }
            return null;
        };

        ReqFetchCommentContent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCommentContent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCommentContent();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.comment_id_list) {
                if (!Array.isArray(object.comment_id_list))
                    throw TypeError(".lq.ReqFetchCommentContent.comment_id_list: array expected");
                message.comment_id_list = Array(object.comment_id_list.length);
                for (let i = 0; i < object.comment_id_list.length; ++i)
                    message.comment_id_list[i] = object.comment_id_list[i] >>> 0;
            }
            return message;
        };

        ReqFetchCommentContent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.comment_id_list = [];
            if (options.defaults)
                object.target_id = 0;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.comment_id_list && message.comment_id_list.length) {
                object.comment_id_list = Array(message.comment_id_list.length);
                for (let j = 0; j < message.comment_id_list.length; ++j)
                    object.comment_id_list[j] = message.comment_id_list[j];
            }
            return object;
        };

        ReqFetchCommentContent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCommentContent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCommentContent";
        };

        return ReqFetchCommentContent;
    })();

    lq.ResFetchCommentContent = (function() {

        function ResFetchCommentContent(properties) {
            this.comments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCommentContent.prototype.error = null;
        ResFetchCommentContent.prototype.comments = $util.emptyArray;

        ResFetchCommentContent.create = function create(properties) {
            return new ResFetchCommentContent(properties);
        };

        ResFetchCommentContent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.comments != null && message.comments.length)
                for (let i = 0; i < message.comments.length; ++i)
                    $root.lq.CommentItem.encode(message.comments[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCommentContent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCommentContent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCommentContent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.comments && message.comments.length))
                            message.comments = [];
                        message.comments.push($root.lq.CommentItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCommentContent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCommentContent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.comments != null && message.hasOwnProperty("comments")) {
                if (!Array.isArray(message.comments))
                    return "comments: array expected";
                for (let i = 0; i < message.comments.length; ++i) {
                    let error = $root.lq.CommentItem.verify(message.comments[i], _depth + 1);
                    if (error)
                        return "comments." + error;
                }
            }
            return null;
        };

        ResFetchCommentContent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCommentContent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCommentContent();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCommentContent.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.comments) {
                if (!Array.isArray(object.comments))
                    throw TypeError(".lq.ResFetchCommentContent.comments: array expected");
                message.comments = Array(object.comments.length);
                for (let i = 0; i < object.comments.length; ++i) {
                    if (typeof object.comments[i] !== "object")
                        throw TypeError(".lq.ResFetchCommentContent.comments: object expected");
                    message.comments[i] = $root.lq.CommentItem.fromObject(object.comments[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchCommentContent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.comments = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.comments && message.comments.length) {
                object.comments = Array(message.comments.length);
                for (let j = 0; j < message.comments.length; ++j)
                    object.comments[j] = $root.lq.CommentItem.toObject(message.comments[j], options);
            }
            return object;
        };

        ResFetchCommentContent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCommentContent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCommentContent";
        };

        return ResFetchCommentContent;
    })();

    lq.ReqLeaveComment = (function() {

        function ReqLeaveComment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLeaveComment.prototype.target_id = 0;
        ReqLeaveComment.prototype.content = "";

        ReqLeaveComment.create = function create(properties) {
            return new ReqLeaveComment(properties);
        };

        ReqLeaveComment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(18).string(message.content);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLeaveComment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLeaveComment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLeaveComment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLeaveComment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLeaveComment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            return null;
        };

        ReqLeaveComment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLeaveComment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLeaveComment();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            return message;
        };

        ReqLeaveComment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.target_id = 0;
                object.content = "";
            }
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            return object;
        };

        ReqLeaveComment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLeaveComment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLeaveComment";
        };

        return ReqLeaveComment;
    })();

    lq.ReqDeleteComment = (function() {

        function ReqDeleteComment(properties) {
            this.delete_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDeleteComment.prototype.target_id = 0;
        ReqDeleteComment.prototype.delete_list = $util.emptyArray;

        ReqDeleteComment.create = function create(properties) {
            return new ReqDeleteComment(properties);
        };

        ReqDeleteComment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(8).uint32(message.target_id);
            if (message.delete_list != null && message.delete_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.delete_list.length; ++i)
                    writer.uint32(message.delete_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDeleteComment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDeleteComment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDeleteComment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.delete_list && message.delete_list.length))
                                message.delete_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delete_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delete_list && message.delete_list.length))
                            message.delete_list = [];
                        message.delete_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDeleteComment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDeleteComment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            if (message.delete_list != null && message.hasOwnProperty("delete_list")) {
                if (!Array.isArray(message.delete_list))
                    return "delete_list: array expected";
                for (let i = 0; i < message.delete_list.length; ++i)
                    if (!$util.isInteger(message.delete_list[i]))
                        return "delete_list: integer[] expected";
            }
            return null;
        };

        ReqDeleteComment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDeleteComment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDeleteComment();
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            if (object.delete_list) {
                if (!Array.isArray(object.delete_list))
                    throw TypeError(".lq.ReqDeleteComment.delete_list: array expected");
                message.delete_list = Array(object.delete_list.length);
                for (let i = 0; i < object.delete_list.length; ++i)
                    message.delete_list[i] = object.delete_list[i] >>> 0;
            }
            return message;
        };

        ReqDeleteComment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.delete_list = [];
            if (options.defaults)
                object.target_id = 0;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            if (message.delete_list && message.delete_list.length) {
                object.delete_list = Array(message.delete_list.length);
                for (let j = 0; j < message.delete_list.length; ++j)
                    object.delete_list[j] = message.delete_list[j];
            }
            return object;
        };

        ReqDeleteComment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDeleteComment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDeleteComment";
        };

        return ReqDeleteComment;
    })();

    lq.ReqUpdateReadComment = (function() {

        function ReqUpdateReadComment(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateReadComment.prototype.read_id = 0;

        ReqUpdateReadComment.create = function create(properties) {
            return new ReqUpdateReadComment(properties);
        };

        ReqUpdateReadComment.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.read_id != null && Object.hasOwnProperty.call(message, "read_id"))
                writer.uint32(8).uint32(message.read_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateReadComment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateReadComment.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateReadComment(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.read_id = value;
                        else
                            delete message.read_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateReadComment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateReadComment.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.read_id != null && message.hasOwnProperty("read_id"))
                if (!$util.isInteger(message.read_id))
                    return "read_id: integer expected";
            return null;
        };

        ReqUpdateReadComment.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateReadComment)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateReadComment();
            if (object.read_id != null)
                if (Number(object.read_id) !== 0)
                    message.read_id = object.read_id >>> 0;
            return message;
        };

        ReqUpdateReadComment.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.read_id = 0;
            if (message.read_id != null && message.hasOwnProperty("read_id"))
                object.read_id = message.read_id;
            return object;
        };

        ReqUpdateReadComment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateReadComment.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateReadComment";
        };

        return ReqUpdateReadComment;
    })();

    lq.ResFetchRollingNotice = (function() {

        function ResFetchRollingNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchRollingNotice.prototype.error = null;
        ResFetchRollingNotice.prototype.notice = null;

        ResFetchRollingNotice.create = function create(properties) {
            return new ResFetchRollingNotice(properties);
        };

        ResFetchRollingNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.notice != null && Object.hasOwnProperty.call(message, "notice"))
                $root.lq.RollingNotice.encode(message.notice, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchRollingNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchRollingNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRollingNotice(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.notice = $root.lq.RollingNotice.decode(reader, reader.uint32(), undefined, _depth + 1, message.notice);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchRollingNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchRollingNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.notice != null && message.hasOwnProperty("notice")) {
                let error = $root.lq.RollingNotice.verify(message.notice, _depth + 1);
                if (error)
                    return "notice." + error;
            }
            return null;
        };

        ResFetchRollingNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchRollingNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchRollingNotice();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchRollingNotice.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.notice != null) {
                if (typeof object.notice !== "object")
                    throw TypeError(".lq.ResFetchRollingNotice.notice: object expected");
                message.notice = $root.lq.RollingNotice.fromObject(object.notice, _depth + 1);
            }
            return message;
        };

        ResFetchRollingNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.notice = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.notice != null && message.hasOwnProperty("notice"))
                object.notice = $root.lq.RollingNotice.toObject(message.notice, options);
            return object;
        };

        ResFetchRollingNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchRollingNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchRollingNotice";
        };

        return ResFetchRollingNotice;
    })();

    lq.ResFetchMaintainNotice = (function() {

        function ResFetchMaintainNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchMaintainNotice.prototype.error = null;
        ResFetchMaintainNotice.prototype.notice = null;

        ResFetchMaintainNotice.create = function create(properties) {
            return new ResFetchMaintainNotice(properties);
        };

        ResFetchMaintainNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.notice != null && Object.hasOwnProperty.call(message, "notice"))
                $root.lq.MaintainNotice.encode(message.notice, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchMaintainNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchMaintainNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchMaintainNotice(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.notice = $root.lq.MaintainNotice.decode(reader, reader.uint32(), undefined, _depth + 1, message.notice);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchMaintainNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchMaintainNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.notice != null && message.hasOwnProperty("notice")) {
                let error = $root.lq.MaintainNotice.verify(message.notice, _depth + 1);
                if (error)
                    return "notice." + error;
            }
            return null;
        };

        ResFetchMaintainNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchMaintainNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchMaintainNotice();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchMaintainNotice.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.notice != null) {
                if (typeof object.notice !== "object")
                    throw TypeError(".lq.ResFetchMaintainNotice.notice: object expected");
                message.notice = $root.lq.MaintainNotice.fromObject(object.notice, _depth + 1);
            }
            return message;
        };

        ResFetchMaintainNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.notice = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.notice != null && message.hasOwnProperty("notice"))
                object.notice = $root.lq.MaintainNotice.toObject(message.notice, options);
            return object;
        };

        ResFetchMaintainNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchMaintainNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchMaintainNotice";
        };

        return ResFetchMaintainNotice;
    })();

    lq.ReqFetchRollingNotice = (function() {

        function ReqFetchRollingNotice(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchRollingNotice.prototype.lang = "";

        ReqFetchRollingNotice.create = function create(properties) {
            return new ReqFetchRollingNotice(properties);
        };

        ReqFetchRollingNotice.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchRollingNotice.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchRollingNotice.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchRollingNotice(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchRollingNotice.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchRollingNotice.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqFetchRollingNotice.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchRollingNotice)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchRollingNotice();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqFetchRollingNotice.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.lang = "";
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqFetchRollingNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchRollingNotice.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchRollingNotice";
        };

        return ReqFetchRollingNotice;
    })();

    lq.ResServerTime = (function() {

        function ResServerTime(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResServerTime.prototype.server_time = 0;
        ResServerTime.prototype.error = null;

        ResServerTime.create = function create(properties) {
            return new ResServerTime(properties);
        };

        ResServerTime.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.server_time != null && Object.hasOwnProperty.call(message, "server_time"))
                writer.uint32(8).uint32(message.server_time);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResServerTime.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResServerTime.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResServerTime(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.server_time = value;
                        else
                            delete message.server_time;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResServerTime.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResServerTime.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.server_time != null && message.hasOwnProperty("server_time"))
                if (!$util.isInteger(message.server_time))
                    return "server_time: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResServerTime.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResServerTime)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResServerTime();
            if (object.server_time != null)
                if (Number(object.server_time) !== 0)
                    message.server_time = object.server_time >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResServerTime.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResServerTime.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.server_time = 0;
                object.error = null;
            }
            if (message.server_time != null && message.hasOwnProperty("server_time"))
                object.server_time = message.server_time;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResServerTime.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResServerTime.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResServerTime";
        };

        return ResServerTime;
    })();

    lq.ReqPlatformBillingProducts = (function() {

        function ReqPlatformBillingProducts(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqPlatformBillingProducts.prototype.shelves_id = 0;

        ReqPlatformBillingProducts.create = function create(properties) {
            return new ReqPlatformBillingProducts(properties);
        };

        ReqPlatformBillingProducts.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.shelves_id != null && Object.hasOwnProperty.call(message, "shelves_id"))
                writer.uint32(8).uint32(message.shelves_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqPlatformBillingProducts.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqPlatformBillingProducts.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqPlatformBillingProducts(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shelves_id = value;
                        else
                            delete message.shelves_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqPlatformBillingProducts.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqPlatformBillingProducts.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.shelves_id != null && message.hasOwnProperty("shelves_id"))
                if (!$util.isInteger(message.shelves_id))
                    return "shelves_id: integer expected";
            return null;
        };

        ReqPlatformBillingProducts.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqPlatformBillingProducts)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqPlatformBillingProducts();
            if (object.shelves_id != null)
                if (Number(object.shelves_id) !== 0)
                    message.shelves_id = object.shelves_id >>> 0;
            return message;
        };

        ReqPlatformBillingProducts.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.shelves_id = 0;
            if (message.shelves_id != null && message.hasOwnProperty("shelves_id"))
                object.shelves_id = message.shelves_id;
            return object;
        };

        ReqPlatformBillingProducts.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqPlatformBillingProducts.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqPlatformBillingProducts";
        };

        return ReqPlatformBillingProducts;
    })();

    lq.ResPlatformBillingProducts = (function() {

        function ResPlatformBillingProducts(properties) {
            this.products = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResPlatformBillingProducts.prototype.error = null;
        ResPlatformBillingProducts.prototype.products = $util.emptyArray;

        ResPlatformBillingProducts.create = function create(properties) {
            return new ResPlatformBillingProducts(properties);
        };

        ResPlatformBillingProducts.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.products != null && message.products.length)
                for (let i = 0; i < message.products.length; ++i)
                    $root.lq.BillingProduct.encode(message.products[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResPlatformBillingProducts.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResPlatformBillingProducts.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResPlatformBillingProducts(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.products && message.products.length))
                            message.products = [];
                        message.products.push($root.lq.BillingProduct.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResPlatformBillingProducts.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResPlatformBillingProducts.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.products != null && message.hasOwnProperty("products")) {
                if (!Array.isArray(message.products))
                    return "products: array expected";
                for (let i = 0; i < message.products.length; ++i) {
                    let error = $root.lq.BillingProduct.verify(message.products[i], _depth + 1);
                    if (error)
                        return "products." + error;
                }
            }
            return null;
        };

        ResPlatformBillingProducts.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResPlatformBillingProducts)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResPlatformBillingProducts();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResPlatformBillingProducts.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.products) {
                if (!Array.isArray(object.products))
                    throw TypeError(".lq.ResPlatformBillingProducts.products: array expected");
                message.products = Array(object.products.length);
                for (let i = 0; i < object.products.length; ++i) {
                    if (typeof object.products[i] !== "object")
                        throw TypeError(".lq.ResPlatformBillingProducts.products: object expected");
                    message.products[i] = $root.lq.BillingProduct.fromObject(object.products[i], _depth + 1);
                }
            }
            return message;
        };

        ResPlatformBillingProducts.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.products = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.products && message.products.length) {
                object.products = Array(message.products.length);
                for (let j = 0; j < message.products.length; ++j)
                    object.products[j] = $root.lq.BillingProduct.toObject(message.products[j], options);
            }
            return object;
        };

        ResPlatformBillingProducts.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResPlatformBillingProducts.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResPlatformBillingProducts";
        };

        return ResPlatformBillingProducts;
    })();

    lq.ReqCreateBillingOrder = (function() {

        function ReqCreateBillingOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateBillingOrder.prototype.goods_id = 0;
        ReqCreateBillingOrder.prototype.payment_platform = 0;
        ReqCreateBillingOrder.prototype.client_type = 0;
        ReqCreateBillingOrder.prototype.account_id = 0;
        ReqCreateBillingOrder.prototype.client_version_string = "";

        ReqCreateBillingOrder.create = function create(properties) {
            return new ReqCreateBillingOrder(properties);
        };

        ReqCreateBillingOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.payment_platform != null && Object.hasOwnProperty.call(message, "payment_platform"))
                writer.uint32(16).uint32(message.payment_platform);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(24).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(32).uint32(message.account_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateBillingOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateBillingOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateBillingOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.payment_platform = value;
                        else
                            delete message.payment_platform;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateBillingOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateBillingOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.payment_platform != null && message.hasOwnProperty("payment_platform"))
                if (!$util.isInteger(message.payment_platform))
                    return "payment_platform: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateBillingOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateBillingOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateBillingOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.payment_platform != null)
                if (Number(object.payment_platform) !== 0)
                    message.payment_platform = object.payment_platform >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateBillingOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.payment_platform = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.payment_platform != null && message.hasOwnProperty("payment_platform"))
                object.payment_platform = message.payment_platform;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateBillingOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateBillingOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateBillingOrder";
        };

        return ReqCreateBillingOrder;
    })();

    lq.ResCreateBillingOrder = (function() {

        function ResCreateBillingOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateBillingOrder.prototype.error = null;
        ResCreateBillingOrder.prototype.order_id = "";

        ResCreateBillingOrder.create = function create(properties) {
            return new ResCreateBillingOrder(properties);
        };

        ResCreateBillingOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateBillingOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateBillingOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateBillingOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateBillingOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateBillingOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateBillingOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateBillingOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateBillingOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateBillingOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateBillingOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateBillingOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateBillingOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateBillingOrder";
        };

        return ResCreateBillingOrder;
    })();

    lq.ReqSolveGooglePlayOrder = (function() {

        function ReqSolveGooglePlayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSolveGooglePlayOrder.prototype.inapp_purchase_data = "";
        ReqSolveGooglePlayOrder.prototype.inapp_data_signature = "";

        ReqSolveGooglePlayOrder.create = function create(properties) {
            return new ReqSolveGooglePlayOrder(properties);
        };

        ReqSolveGooglePlayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.inapp_purchase_data != null && Object.hasOwnProperty.call(message, "inapp_purchase_data"))
                writer.uint32(18).string(message.inapp_purchase_data);
            if (message.inapp_data_signature != null && Object.hasOwnProperty.call(message, "inapp_data_signature"))
                writer.uint32(26).string(message.inapp_data_signature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSolveGooglePlayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSolveGooglePlayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSolveGooglePlayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.inapp_purchase_data = value;
                        else
                            delete message.inapp_purchase_data;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.inapp_data_signature = value;
                        else
                            delete message.inapp_data_signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSolveGooglePlayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSolveGooglePlayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.inapp_purchase_data != null && message.hasOwnProperty("inapp_purchase_data"))
                if (!$util.isString(message.inapp_purchase_data))
                    return "inapp_purchase_data: string expected";
            if (message.inapp_data_signature != null && message.hasOwnProperty("inapp_data_signature"))
                if (!$util.isString(message.inapp_data_signature))
                    return "inapp_data_signature: string expected";
            return null;
        };

        ReqSolveGooglePlayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSolveGooglePlayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSolveGooglePlayOrder();
            if (object.inapp_purchase_data != null)
                if (typeof object.inapp_purchase_data !== "string" || object.inapp_purchase_data.length)
                    message.inapp_purchase_data = String(object.inapp_purchase_data);
            if (object.inapp_data_signature != null)
                if (typeof object.inapp_data_signature !== "string" || object.inapp_data_signature.length)
                    message.inapp_data_signature = String(object.inapp_data_signature);
            return message;
        };

        ReqSolveGooglePlayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.inapp_purchase_data = "";
                object.inapp_data_signature = "";
            }
            if (message.inapp_purchase_data != null && message.hasOwnProperty("inapp_purchase_data"))
                object.inapp_purchase_data = message.inapp_purchase_data;
            if (message.inapp_data_signature != null && message.hasOwnProperty("inapp_data_signature"))
                object.inapp_data_signature = message.inapp_data_signature;
            return object;
        };

        ReqSolveGooglePlayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSolveGooglePlayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSolveGooglePlayOrder";
        };

        return ReqSolveGooglePlayOrder;
    })();

    lq.ReqSolveGooglePlayOrderV3 = (function() {

        function ReqSolveGooglePlayOrderV3(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSolveGooglePlayOrderV3.prototype.order_id = "";
        ReqSolveGooglePlayOrderV3.prototype.transaction_id = "";
        ReqSolveGooglePlayOrderV3.prototype.token = "";
        ReqSolveGooglePlayOrderV3.prototype.account_id = 0;

        ReqSolveGooglePlayOrderV3.create = function create(properties) {
            return new ReqSolveGooglePlayOrderV3(properties);
        };

        ReqSolveGooglePlayOrderV3.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.transaction_id != null && Object.hasOwnProperty.call(message, "transaction_id"))
                writer.uint32(18).string(message.transaction_id);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(26).string(message.token);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(32).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSolveGooglePlayOrderV3.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSolveGooglePlayOrderV3.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSolveGooglePlayOrderV3(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.transaction_id = value;
                        else
                            delete message.transaction_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSolveGooglePlayOrderV3.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSolveGooglePlayOrderV3.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                if (!$util.isString(message.transaction_id))
                    return "transaction_id: string expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqSolveGooglePlayOrderV3.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSolveGooglePlayOrderV3)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSolveGooglePlayOrderV3();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.transaction_id != null)
                if (typeof object.transaction_id !== "string" || object.transaction_id.length)
                    message.transaction_id = String(object.transaction_id);
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqSolveGooglePlayOrderV3.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.order_id = "";
                object.transaction_id = "";
                object.token = "";
                object.account_id = 0;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                object.transaction_id = message.transaction_id;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqSolveGooglePlayOrderV3.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSolveGooglePlayOrderV3.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSolveGooglePlayOrderV3";
        };

        return ReqSolveGooglePlayOrderV3;
    })();

    lq.ReqCancelGooglePlayOrder = (function() {

        function ReqCancelGooglePlayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCancelGooglePlayOrder.prototype.order_id = "";

        ReqCancelGooglePlayOrder.create = function create(properties) {
            return new ReqCancelGooglePlayOrder(properties);
        };

        ReqCancelGooglePlayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCancelGooglePlayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCancelGooglePlayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCancelGooglePlayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCancelGooglePlayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCancelGooglePlayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ReqCancelGooglePlayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCancelGooglePlayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCancelGooglePlayOrder();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ReqCancelGooglePlayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.order_id = "";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ReqCancelGooglePlayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCancelGooglePlayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCancelGooglePlayOrder";
        };

        return ReqCancelGooglePlayOrder;
    })();

    lq.ReqCreateWechatNativeOrder = (function() {

        function ReqCreateWechatNativeOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateWechatNativeOrder.prototype.goods_id = 0;
        ReqCreateWechatNativeOrder.prototype.client_type = 0;
        ReqCreateWechatNativeOrder.prototype.account_id = 0;
        ReqCreateWechatNativeOrder.prototype.account_ip = "";
        ReqCreateWechatNativeOrder.prototype.client_version_string = "";

        ReqCreateWechatNativeOrder.create = function create(properties) {
            return new ReqCreateWechatNativeOrder(properties);
        };

        ReqCreateWechatNativeOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.account_ip != null && Object.hasOwnProperty.call(message, "account_ip"))
                writer.uint32(34).string(message.account_ip);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateWechatNativeOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateWechatNativeOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateWechatNativeOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account_ip = value;
                        else
                            delete message.account_ip;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateWechatNativeOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateWechatNativeOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                if (!$util.isString(message.account_ip))
                    return "account_ip: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateWechatNativeOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateWechatNativeOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateWechatNativeOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.account_ip != null)
                if (typeof object.account_ip !== "string" || object.account_ip.length)
                    message.account_ip = String(object.account_ip);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateWechatNativeOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.account_ip = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                object.account_ip = message.account_ip;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateWechatNativeOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateWechatNativeOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateWechatNativeOrder";
        };

        return ReqCreateWechatNativeOrder;
    })();

    lq.ResCreateWechatNativeOrder = (function() {

        function ResCreateWechatNativeOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateWechatNativeOrder.prototype.error = null;
        ResCreateWechatNativeOrder.prototype.qrcode_buffer = "";
        ResCreateWechatNativeOrder.prototype.order_id = "";

        ResCreateWechatNativeOrder.create = function create(properties) {
            return new ResCreateWechatNativeOrder(properties);
        };

        ResCreateWechatNativeOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.qrcode_buffer != null && Object.hasOwnProperty.call(message, "qrcode_buffer"))
                writer.uint32(18).string(message.qrcode_buffer);
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(26).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateWechatNativeOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateWechatNativeOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateWechatNativeOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.qrcode_buffer = value;
                        else
                            delete message.qrcode_buffer;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateWechatNativeOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateWechatNativeOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.qrcode_buffer != null && message.hasOwnProperty("qrcode_buffer"))
                if (!$util.isString(message.qrcode_buffer))
                    return "qrcode_buffer: string expected";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateWechatNativeOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateWechatNativeOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateWechatNativeOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateWechatNativeOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.qrcode_buffer != null)
                if (typeof object.qrcode_buffer !== "string" || object.qrcode_buffer.length)
                    message.qrcode_buffer = String(object.qrcode_buffer);
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateWechatNativeOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.qrcode_buffer = "";
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.qrcode_buffer != null && message.hasOwnProperty("qrcode_buffer"))
                object.qrcode_buffer = message.qrcode_buffer;
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateWechatNativeOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateWechatNativeOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateWechatNativeOrder";
        };

        return ResCreateWechatNativeOrder;
    })();

    lq.ReqCreateWechatAppOrder = (function() {

        function ReqCreateWechatAppOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateWechatAppOrder.prototype.goods_id = 0;
        ReqCreateWechatAppOrder.prototype.client_type = 0;
        ReqCreateWechatAppOrder.prototype.account_id = 0;
        ReqCreateWechatAppOrder.prototype.account_ip = "";
        ReqCreateWechatAppOrder.prototype.client_version_string = "";

        ReqCreateWechatAppOrder.create = function create(properties) {
            return new ReqCreateWechatAppOrder(properties);
        };

        ReqCreateWechatAppOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.account_ip != null && Object.hasOwnProperty.call(message, "account_ip"))
                writer.uint32(34).string(message.account_ip);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateWechatAppOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateWechatAppOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateWechatAppOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account_ip = value;
                        else
                            delete message.account_ip;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateWechatAppOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateWechatAppOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                if (!$util.isString(message.account_ip))
                    return "account_ip: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateWechatAppOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateWechatAppOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateWechatAppOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.account_ip != null)
                if (typeof object.account_ip !== "string" || object.account_ip.length)
                    message.account_ip = String(object.account_ip);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateWechatAppOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.account_ip = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                object.account_ip = message.account_ip;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateWechatAppOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateWechatAppOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateWechatAppOrder";
        };

        return ReqCreateWechatAppOrder;
    })();

    lq.ResCreateWechatAppOrder = (function() {

        function ResCreateWechatAppOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateWechatAppOrder.prototype.error = null;
        ResCreateWechatAppOrder.prototype.call_wechat_app_param = null;

        ResCreateWechatAppOrder.create = function create(properties) {
            return new ResCreateWechatAppOrder(properties);
        };

        ResCreateWechatAppOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.call_wechat_app_param != null && Object.hasOwnProperty.call(message, "call_wechat_app_param"))
                $root.lq.ResCreateWechatAppOrder.CallWechatAppParam.encode(message.call_wechat_app_param, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateWechatAppOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateWechatAppOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateWechatAppOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.call_wechat_app_param = $root.lq.ResCreateWechatAppOrder.CallWechatAppParam.decode(reader, reader.uint32(), undefined, _depth + 1, message.call_wechat_app_param);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateWechatAppOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateWechatAppOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.call_wechat_app_param != null && message.hasOwnProperty("call_wechat_app_param")) {
                let error = $root.lq.ResCreateWechatAppOrder.CallWechatAppParam.verify(message.call_wechat_app_param, _depth + 1);
                if (error)
                    return "call_wechat_app_param." + error;
            }
            return null;
        };

        ResCreateWechatAppOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateWechatAppOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateWechatAppOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateWechatAppOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.call_wechat_app_param != null) {
                if (typeof object.call_wechat_app_param !== "object")
                    throw TypeError(".lq.ResCreateWechatAppOrder.call_wechat_app_param: object expected");
                message.call_wechat_app_param = $root.lq.ResCreateWechatAppOrder.CallWechatAppParam.fromObject(object.call_wechat_app_param, _depth + 1);
            }
            return message;
        };

        ResCreateWechatAppOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.call_wechat_app_param = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.call_wechat_app_param != null && message.hasOwnProperty("call_wechat_app_param"))
                object.call_wechat_app_param = $root.lq.ResCreateWechatAppOrder.CallWechatAppParam.toObject(message.call_wechat_app_param, options);
            return object;
        };

        ResCreateWechatAppOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateWechatAppOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateWechatAppOrder";
        };

        ResCreateWechatAppOrder.CallWechatAppParam = (function() {

            function CallWechatAppParam(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CallWechatAppParam.prototype.appid = "";
            CallWechatAppParam.prototype.partnerid = "";
            CallWechatAppParam.prototype.prepayid = "";
            CallWechatAppParam.prototype["package"] = "";
            CallWechatAppParam.prototype.noncestr = "";
            CallWechatAppParam.prototype.timestamp = "";
            CallWechatAppParam.prototype.sign = "";

            CallWechatAppParam.create = function create(properties) {
                return new CallWechatAppParam(properties);
            };

            CallWechatAppParam.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.appid != null && Object.hasOwnProperty.call(message, "appid"))
                    writer.uint32(10).string(message.appid);
                if (message.partnerid != null && Object.hasOwnProperty.call(message, "partnerid"))
                    writer.uint32(18).string(message.partnerid);
                if (message.prepayid != null && Object.hasOwnProperty.call(message, "prepayid"))
                    writer.uint32(26).string(message.prepayid);
                if (message["package"] != null && Object.hasOwnProperty.call(message, "package"))
                    writer.uint32(34).string(message["package"]);
                if (message.noncestr != null && Object.hasOwnProperty.call(message, "noncestr"))
                    writer.uint32(42).string(message.noncestr);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(50).string(message.timestamp);
                if (message.sign != null && Object.hasOwnProperty.call(message, "sign"))
                    writer.uint32(58).string(message.sign);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CallWechatAppParam.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CallWechatAppParam.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateWechatAppOrder.CallWechatAppParam(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.appid = value;
                            else
                                delete message.appid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.partnerid = value;
                            else
                                delete message.partnerid;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.prepayid = value;
                            else
                                delete message.prepayid;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message["package"] = value;
                            else
                                delete message["package"];
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.noncestr = value;
                            else
                                delete message.noncestr;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.timestamp = value;
                            else
                                delete message.timestamp;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.sign = value;
                            else
                                delete message.sign;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CallWechatAppParam.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CallWechatAppParam.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.appid != null && message.hasOwnProperty("appid"))
                    if (!$util.isString(message.appid))
                        return "appid: string expected";
                if (message.partnerid != null && message.hasOwnProperty("partnerid"))
                    if (!$util.isString(message.partnerid))
                        return "partnerid: string expected";
                if (message.prepayid != null && message.hasOwnProperty("prepayid"))
                    if (!$util.isString(message.prepayid))
                        return "prepayid: string expected";
                if (message["package"] != null && message.hasOwnProperty("package"))
                    if (!$util.isString(message["package"]))
                        return "package: string expected";
                if (message.noncestr != null && message.hasOwnProperty("noncestr"))
                    if (!$util.isString(message.noncestr))
                        return "noncestr: string expected";
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    if (!$util.isString(message.timestamp))
                        return "timestamp: string expected";
                if (message.sign != null && message.hasOwnProperty("sign"))
                    if (!$util.isString(message.sign))
                        return "sign: string expected";
                return null;
            };

            CallWechatAppParam.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResCreateWechatAppOrder.CallWechatAppParam)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResCreateWechatAppOrder.CallWechatAppParam();
                if (object.appid != null)
                    if (typeof object.appid !== "string" || object.appid.length)
                        message.appid = String(object.appid);
                if (object.partnerid != null)
                    if (typeof object.partnerid !== "string" || object.partnerid.length)
                        message.partnerid = String(object.partnerid);
                if (object.prepayid != null)
                    if (typeof object.prepayid !== "string" || object.prepayid.length)
                        message.prepayid = String(object.prepayid);
                if (object["package"] != null)
                    if (typeof object["package"] !== "string" || object["package"].length)
                        message["package"] = String(object["package"]);
                if (object.noncestr != null)
                    if (typeof object.noncestr !== "string" || object.noncestr.length)
                        message.noncestr = String(object.noncestr);
                if (object.timestamp != null)
                    if (typeof object.timestamp !== "string" || object.timestamp.length)
                        message.timestamp = String(object.timestamp);
                if (object.sign != null)
                    if (typeof object.sign !== "string" || object.sign.length)
                        message.sign = String(object.sign);
                return message;
            };

            CallWechatAppParam.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.appid = "";
                    object.partnerid = "";
                    object.prepayid = "";
                    object["package"] = "";
                    object.noncestr = "";
                    object.timestamp = "";
                    object.sign = "";
                }
                if (message.appid != null && message.hasOwnProperty("appid"))
                    object.appid = message.appid;
                if (message.partnerid != null && message.hasOwnProperty("partnerid"))
                    object.partnerid = message.partnerid;
                if (message.prepayid != null && message.hasOwnProperty("prepayid"))
                    object.prepayid = message.prepayid;
                if (message["package"] != null && message.hasOwnProperty("package"))
                    object["package"] = message["package"];
                if (message.noncestr != null && message.hasOwnProperty("noncestr"))
                    object.noncestr = message.noncestr;
                if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                    object.timestamp = message.timestamp;
                if (message.sign != null && message.hasOwnProperty("sign"))
                    object.sign = message.sign;
                return object;
            };

            CallWechatAppParam.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallWechatAppParam.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResCreateWechatAppOrder.CallWechatAppParam";
            };

            return CallWechatAppParam;
        })();

        return ResCreateWechatAppOrder;
    })();

    lq.ReqCreateAlipayOrder = (function() {

        function ReqCreateAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateAlipayOrder.prototype.goods_id = 0;
        ReqCreateAlipayOrder.prototype.client_type = 0;
        ReqCreateAlipayOrder.prototype.account_id = 0;
        ReqCreateAlipayOrder.prototype.alipay_trade_type = "";
        ReqCreateAlipayOrder.prototype.return_url = "";
        ReqCreateAlipayOrder.prototype.client_version_string = "";

        ReqCreateAlipayOrder.create = function create(properties) {
            return new ReqCreateAlipayOrder(properties);
        };

        ReqCreateAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.alipay_trade_type != null && Object.hasOwnProperty.call(message, "alipay_trade_type"))
                writer.uint32(34).string(message.alipay_trade_type);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(42).string(message.return_url);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.alipay_trade_type = value;
                        else
                            delete message.alipay_trade_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.alipay_trade_type != null && message.hasOwnProperty("alipay_trade_type"))
                if (!$util.isString(message.alipay_trade_type))
                    return "alipay_trade_type: string expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateAlipayOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.alipay_trade_type != null)
                if (typeof object.alipay_trade_type !== "string" || object.alipay_trade_type.length)
                    message.alipay_trade_type = String(object.alipay_trade_type);
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.alipay_trade_type = "";
                object.return_url = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.alipay_trade_type != null && message.hasOwnProperty("alipay_trade_type"))
                object.alipay_trade_type = message.alipay_trade_type;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateAlipayOrder";
        };

        return ReqCreateAlipayOrder;
    })();

    lq.ResCreateAlipayOrder = (function() {

        function ResCreateAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateAlipayOrder.prototype.error = null;
        ResCreateAlipayOrder.prototype.alipay_url = "";

        ResCreateAlipayOrder.create = function create(properties) {
            return new ResCreateAlipayOrder(properties);
        };

        ResCreateAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.alipay_url != null && Object.hasOwnProperty.call(message, "alipay_url"))
                writer.uint32(18).string(message.alipay_url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.alipay_url = value;
                        else
                            delete message.alipay_url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.alipay_url != null && message.hasOwnProperty("alipay_url"))
                if (!$util.isString(message.alipay_url))
                    return "alipay_url: string expected";
            return null;
        };

        ResCreateAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateAlipayOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateAlipayOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.alipay_url != null)
                if (typeof object.alipay_url !== "string" || object.alipay_url.length)
                    message.alipay_url = String(object.alipay_url);
            return message;
        };

        ResCreateAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.alipay_url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.alipay_url != null && message.hasOwnProperty("alipay_url"))
                object.alipay_url = message.alipay_url;
            return object;
        };

        ResCreateAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateAlipayOrder";
        };

        return ResCreateAlipayOrder;
    })();

    lq.ReqCreateAlipayScanOrder = (function() {

        function ReqCreateAlipayScanOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateAlipayScanOrder.prototype.goods_id = 0;
        ReqCreateAlipayScanOrder.prototype.client_type = 0;
        ReqCreateAlipayScanOrder.prototype.account_id = 0;
        ReqCreateAlipayScanOrder.prototype.client_version_string = "";

        ReqCreateAlipayScanOrder.create = function create(properties) {
            return new ReqCreateAlipayScanOrder(properties);
        };

        ReqCreateAlipayScanOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(34).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateAlipayScanOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateAlipayScanOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateAlipayScanOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateAlipayScanOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateAlipayScanOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateAlipayScanOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateAlipayScanOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateAlipayScanOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateAlipayScanOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateAlipayScanOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateAlipayScanOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateAlipayScanOrder";
        };

        return ReqCreateAlipayScanOrder;
    })();

    lq.ResCreateAlipayScanOrder = (function() {

        function ResCreateAlipayScanOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateAlipayScanOrder.prototype.error = null;
        ResCreateAlipayScanOrder.prototype.qrcode_buffer = "";
        ResCreateAlipayScanOrder.prototype.order_id = "";
        ResCreateAlipayScanOrder.prototype.qr_code = "";

        ResCreateAlipayScanOrder.create = function create(properties) {
            return new ResCreateAlipayScanOrder(properties);
        };

        ResCreateAlipayScanOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.qrcode_buffer != null && Object.hasOwnProperty.call(message, "qrcode_buffer"))
                writer.uint32(18).string(message.qrcode_buffer);
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(26).string(message.order_id);
            if (message.qr_code != null && Object.hasOwnProperty.call(message, "qr_code"))
                writer.uint32(34).string(message.qr_code);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateAlipayScanOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateAlipayScanOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateAlipayScanOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.qrcode_buffer = value;
                        else
                            delete message.qrcode_buffer;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.qr_code = value;
                        else
                            delete message.qr_code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateAlipayScanOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateAlipayScanOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.qrcode_buffer != null && message.hasOwnProperty("qrcode_buffer"))
                if (!$util.isString(message.qrcode_buffer))
                    return "qrcode_buffer: string expected";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.qr_code != null && message.hasOwnProperty("qr_code"))
                if (!$util.isString(message.qr_code))
                    return "qr_code: string expected";
            return null;
        };

        ResCreateAlipayScanOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateAlipayScanOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateAlipayScanOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateAlipayScanOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.qrcode_buffer != null)
                if (typeof object.qrcode_buffer !== "string" || object.qrcode_buffer.length)
                    message.qrcode_buffer = String(object.qrcode_buffer);
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.qr_code != null)
                if (typeof object.qr_code !== "string" || object.qr_code.length)
                    message.qr_code = String(object.qr_code);
            return message;
        };

        ResCreateAlipayScanOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.qrcode_buffer = "";
                object.order_id = "";
                object.qr_code = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.qrcode_buffer != null && message.hasOwnProperty("qrcode_buffer"))
                object.qrcode_buffer = message.qrcode_buffer;
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.qr_code != null && message.hasOwnProperty("qr_code"))
                object.qr_code = message.qr_code;
            return object;
        };

        ResCreateAlipayScanOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateAlipayScanOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateAlipayScanOrder";
        };

        return ResCreateAlipayScanOrder;
    })();

    lq.ReqCreateAlipayAppOrder = (function() {

        function ReqCreateAlipayAppOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateAlipayAppOrder.prototype.goods_id = 0;
        ReqCreateAlipayAppOrder.prototype.client_type = 0;
        ReqCreateAlipayAppOrder.prototype.account_id = 0;
        ReqCreateAlipayAppOrder.prototype.client_version_string = "";

        ReqCreateAlipayAppOrder.create = function create(properties) {
            return new ReqCreateAlipayAppOrder(properties);
        };

        ReqCreateAlipayAppOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(34).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateAlipayAppOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateAlipayAppOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateAlipayAppOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateAlipayAppOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateAlipayAppOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateAlipayAppOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateAlipayAppOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateAlipayAppOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateAlipayAppOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateAlipayAppOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateAlipayAppOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateAlipayAppOrder";
        };

        return ReqCreateAlipayAppOrder;
    })();

    lq.ResCreateAlipayAppOrder = (function() {

        function ResCreateAlipayAppOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateAlipayAppOrder.prototype.error = null;
        ResCreateAlipayAppOrder.prototype.alipay_url = "";

        ResCreateAlipayAppOrder.create = function create(properties) {
            return new ResCreateAlipayAppOrder(properties);
        };

        ResCreateAlipayAppOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.alipay_url != null && Object.hasOwnProperty.call(message, "alipay_url"))
                writer.uint32(18).string(message.alipay_url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateAlipayAppOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateAlipayAppOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateAlipayAppOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.alipay_url = value;
                        else
                            delete message.alipay_url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateAlipayAppOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateAlipayAppOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.alipay_url != null && message.hasOwnProperty("alipay_url"))
                if (!$util.isString(message.alipay_url))
                    return "alipay_url: string expected";
            return null;
        };

        ResCreateAlipayAppOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateAlipayAppOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateAlipayAppOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateAlipayAppOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.alipay_url != null)
                if (typeof object.alipay_url !== "string" || object.alipay_url.length)
                    message.alipay_url = String(object.alipay_url);
            return message;
        };

        ResCreateAlipayAppOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.alipay_url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.alipay_url != null && message.hasOwnProperty("alipay_url"))
                object.alipay_url = message.alipay_url;
            return object;
        };

        ResCreateAlipayAppOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateAlipayAppOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateAlipayAppOrder";
        };

        return ResCreateAlipayAppOrder;
    })();

    lq.ReqCreateJPCreditCardOrder = (function() {

        function ReqCreateJPCreditCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPCreditCardOrder.prototype.goods_id = 0;
        ReqCreateJPCreditCardOrder.prototype.client_type = 0;
        ReqCreateJPCreditCardOrder.prototype.account_id = 0;
        ReqCreateJPCreditCardOrder.prototype.return_url = "";
        ReqCreateJPCreditCardOrder.prototype.access_token = "";
        ReqCreateJPCreditCardOrder.prototype.client_version_string = "";

        ReqCreateJPCreditCardOrder.create = function create(properties) {
            return new ReqCreateJPCreditCardOrder(properties);
        };

        ReqCreateJPCreditCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPCreditCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPCreditCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPCreditCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPCreditCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPCreditCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPCreditCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPCreditCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPCreditCardOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPCreditCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPCreditCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPCreditCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPCreditCardOrder";
        };

        return ReqCreateJPCreditCardOrder;
    })();

    lq.ResCreateJPCreditCardOrder = (function() {

        function ResCreateJPCreditCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPCreditCardOrder.prototype.error = null;
        ResCreateJPCreditCardOrder.prototype.order_id = "";

        ResCreateJPCreditCardOrder.create = function create(properties) {
            return new ResCreateJPCreditCardOrder(properties);
        };

        ResCreateJPCreditCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPCreditCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPCreditCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPCreditCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPCreditCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPCreditCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPCreditCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPCreditCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPCreditCardOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPCreditCardOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPCreditCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPCreditCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPCreditCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPCreditCardOrder";
        };

        return ResCreateJPCreditCardOrder;
    })();

    lq.ReqCreateJPPaypalOrder = (function() {

        function ReqCreateJPPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPPaypalOrder.prototype.goods_id = 0;
        ReqCreateJPPaypalOrder.prototype.client_type = 0;
        ReqCreateJPPaypalOrder.prototype.account_id = 0;
        ReqCreateJPPaypalOrder.prototype.return_url = "";
        ReqCreateJPPaypalOrder.prototype.access_token = "";
        ReqCreateJPPaypalOrder.prototype.client_version_string = "";

        ReqCreateJPPaypalOrder.create = function create(properties) {
            return new ReqCreateJPPaypalOrder(properties);
        };

        ReqCreateJPPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPPaypalOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPPaypalOrder";
        };

        return ReqCreateJPPaypalOrder;
    })();

    lq.ResCreateJPPaypalOrder = (function() {

        function ResCreateJPPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPPaypalOrder.prototype.error = null;
        ResCreateJPPaypalOrder.prototype.order_id = "";

        ResCreateJPPaypalOrder.create = function create(properties) {
            return new ResCreateJPPaypalOrder(properties);
        };

        ResCreateJPPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPPaypalOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPPaypalOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPPaypalOrder";
        };

        return ResCreateJPPaypalOrder;
    })();

    lq.ReqCreateJPAuOrder = (function() {

        function ReqCreateJPAuOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPAuOrder.prototype.goods_id = 0;
        ReqCreateJPAuOrder.prototype.client_type = 0;
        ReqCreateJPAuOrder.prototype.account_id = 0;
        ReqCreateJPAuOrder.prototype.return_url = "";
        ReqCreateJPAuOrder.prototype.access_token = "";
        ReqCreateJPAuOrder.prototype.client_version_string = "";

        ReqCreateJPAuOrder.create = function create(properties) {
            return new ReqCreateJPAuOrder(properties);
        };

        ReqCreateJPAuOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPAuOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPAuOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPAuOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPAuOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPAuOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPAuOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPAuOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPAuOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPAuOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPAuOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPAuOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPAuOrder";
        };

        return ReqCreateJPAuOrder;
    })();

    lq.ResCreateJPAuOrder = (function() {

        function ResCreateJPAuOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPAuOrder.prototype.error = null;
        ResCreateJPAuOrder.prototype.order_id = "";

        ResCreateJPAuOrder.create = function create(properties) {
            return new ResCreateJPAuOrder(properties);
        };

        ResCreateJPAuOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPAuOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPAuOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPAuOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPAuOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPAuOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPAuOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPAuOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPAuOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPAuOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPAuOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPAuOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPAuOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPAuOrder";
        };

        return ResCreateJPAuOrder;
    })();

    lq.ReqCreateJPDocomoOrder = (function() {

        function ReqCreateJPDocomoOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPDocomoOrder.prototype.goods_id = 0;
        ReqCreateJPDocomoOrder.prototype.client_type = 0;
        ReqCreateJPDocomoOrder.prototype.account_id = 0;
        ReqCreateJPDocomoOrder.prototype.return_url = "";
        ReqCreateJPDocomoOrder.prototype.access_token = "";
        ReqCreateJPDocomoOrder.prototype.client_version_string = "";

        ReqCreateJPDocomoOrder.create = function create(properties) {
            return new ReqCreateJPDocomoOrder(properties);
        };

        ReqCreateJPDocomoOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPDocomoOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPDocomoOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPDocomoOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPDocomoOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPDocomoOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPDocomoOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPDocomoOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPDocomoOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPDocomoOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPDocomoOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPDocomoOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPDocomoOrder";
        };

        return ReqCreateJPDocomoOrder;
    })();

    lq.ResCreateJPDocomoOrder = (function() {

        function ResCreateJPDocomoOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPDocomoOrder.prototype.error = null;
        ResCreateJPDocomoOrder.prototype.order_id = "";

        ResCreateJPDocomoOrder.create = function create(properties) {
            return new ResCreateJPDocomoOrder(properties);
        };

        ResCreateJPDocomoOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPDocomoOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPDocomoOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPDocomoOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPDocomoOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPDocomoOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPDocomoOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPDocomoOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPDocomoOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPDocomoOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPDocomoOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPDocomoOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPDocomoOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPDocomoOrder";
        };

        return ResCreateJPDocomoOrder;
    })();

    lq.ReqCreateJPWebMoneyOrder = (function() {

        function ReqCreateJPWebMoneyOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPWebMoneyOrder.prototype.goods_id = 0;
        ReqCreateJPWebMoneyOrder.prototype.client_type = 0;
        ReqCreateJPWebMoneyOrder.prototype.account_id = 0;
        ReqCreateJPWebMoneyOrder.prototype.return_url = "";
        ReqCreateJPWebMoneyOrder.prototype.access_token = "";
        ReqCreateJPWebMoneyOrder.prototype.client_version_string = "";

        ReqCreateJPWebMoneyOrder.create = function create(properties) {
            return new ReqCreateJPWebMoneyOrder(properties);
        };

        ReqCreateJPWebMoneyOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPWebMoneyOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPWebMoneyOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPWebMoneyOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPWebMoneyOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPWebMoneyOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPWebMoneyOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPWebMoneyOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPWebMoneyOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPWebMoneyOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPWebMoneyOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPWebMoneyOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPWebMoneyOrder";
        };

        return ReqCreateJPWebMoneyOrder;
    })();

    lq.ResCreateJPWebMoneyOrder = (function() {

        function ResCreateJPWebMoneyOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPWebMoneyOrder.prototype.error = null;
        ResCreateJPWebMoneyOrder.prototype.order_id = "";

        ResCreateJPWebMoneyOrder.create = function create(properties) {
            return new ResCreateJPWebMoneyOrder(properties);
        };

        ResCreateJPWebMoneyOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPWebMoneyOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPWebMoneyOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPWebMoneyOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPWebMoneyOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPWebMoneyOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPWebMoneyOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPWebMoneyOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPWebMoneyOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPWebMoneyOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPWebMoneyOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPWebMoneyOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPWebMoneyOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPWebMoneyOrder";
        };

        return ResCreateJPWebMoneyOrder;
    })();

    lq.ReqCreateJPSoftbankOrder = (function() {

        function ReqCreateJPSoftbankOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPSoftbankOrder.prototype.goods_id = 0;
        ReqCreateJPSoftbankOrder.prototype.client_type = 0;
        ReqCreateJPSoftbankOrder.prototype.account_id = 0;
        ReqCreateJPSoftbankOrder.prototype.return_url = "";
        ReqCreateJPSoftbankOrder.prototype.access_token = "";
        ReqCreateJPSoftbankOrder.prototype.client_version_string = "";

        ReqCreateJPSoftbankOrder.create = function create(properties) {
            return new ReqCreateJPSoftbankOrder(properties);
        };

        ReqCreateJPSoftbankOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPSoftbankOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPSoftbankOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPSoftbankOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPSoftbankOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPSoftbankOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPSoftbankOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPSoftbankOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPSoftbankOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPSoftbankOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPSoftbankOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPSoftbankOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPSoftbankOrder";
        };

        return ReqCreateJPSoftbankOrder;
    })();

    lq.ResCreateJPSoftbankOrder = (function() {

        function ResCreateJPSoftbankOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPSoftbankOrder.prototype.error = null;
        ResCreateJPSoftbankOrder.prototype.order_id = "";

        ResCreateJPSoftbankOrder.create = function create(properties) {
            return new ResCreateJPSoftbankOrder(properties);
        };

        ResCreateJPSoftbankOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPSoftbankOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPSoftbankOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPSoftbankOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPSoftbankOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPSoftbankOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPSoftbankOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPSoftbankOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPSoftbankOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPSoftbankOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPSoftbankOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPSoftbankOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPSoftbankOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPSoftbankOrder";
        };

        return ResCreateJPSoftbankOrder;
    })();

    lq.ReqCreateJPPayPayOrder = (function() {

        function ReqCreateJPPayPayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPPayPayOrder.prototype.goods_id = 0;
        ReqCreateJPPayPayOrder.prototype.client_type = 0;
        ReqCreateJPPayPayOrder.prototype.account_id = 0;
        ReqCreateJPPayPayOrder.prototype.return_url = "";
        ReqCreateJPPayPayOrder.prototype.access_token = "";
        ReqCreateJPPayPayOrder.prototype.client_version_string = "";

        ReqCreateJPPayPayOrder.create = function create(properties) {
            return new ReqCreateJPPayPayOrder(properties);
        };

        ReqCreateJPPayPayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPPayPayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPPayPayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPPayPayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPPayPayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPPayPayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPPayPayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPPayPayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPPayPayOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPPayPayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPPayPayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPPayPayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPPayPayOrder";
        };

        return ReqCreateJPPayPayOrder;
    })();

    lq.ResCreateJPPayPayOrder = (function() {

        function ResCreateJPPayPayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPPayPayOrder.prototype.error = null;
        ResCreateJPPayPayOrder.prototype.order_id = "";

        ResCreateJPPayPayOrder.create = function create(properties) {
            return new ResCreateJPPayPayOrder(properties);
        };

        ResCreateJPPayPayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPPayPayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPPayPayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPPayPayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPPayPayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPPayPayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPPayPayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPPayPayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPPayPayOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPPayPayOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPPayPayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPPayPayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPPayPayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPPayPayOrder";
        };

        return ResCreateJPPayPayOrder;
    })();

    lq.ReqFetchJPCommonCreditCardOrder = (function() {

        function ReqFetchJPCommonCreditCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchJPCommonCreditCardOrder.prototype.order_id = "";
        ReqFetchJPCommonCreditCardOrder.prototype.account_id = 0;

        ReqFetchJPCommonCreditCardOrder.create = function create(properties) {
            return new ReqFetchJPCommonCreditCardOrder(properties);
        };

        ReqFetchJPCommonCreditCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchJPCommonCreditCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchJPCommonCreditCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchJPCommonCreditCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchJPCommonCreditCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchJPCommonCreditCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqFetchJPCommonCreditCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchJPCommonCreditCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchJPCommonCreditCardOrder();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqFetchJPCommonCreditCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.order_id = "";
                object.account_id = 0;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqFetchJPCommonCreditCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchJPCommonCreditCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchJPCommonCreditCardOrder";
        };

        return ReqFetchJPCommonCreditCardOrder;
    })();

    lq.ResFetchJPCommonCreditCardOrder = (function() {

        function ResFetchJPCommonCreditCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchJPCommonCreditCardOrder.prototype.error = null;

        ResFetchJPCommonCreditCardOrder.create = function create(properties) {
            return new ResFetchJPCommonCreditCardOrder(properties);
        };

        ResFetchJPCommonCreditCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchJPCommonCreditCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchJPCommonCreditCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchJPCommonCreditCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchJPCommonCreditCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchJPCommonCreditCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResFetchJPCommonCreditCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchJPCommonCreditCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchJPCommonCreditCardOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchJPCommonCreditCardOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResFetchJPCommonCreditCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResFetchJPCommonCreditCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchJPCommonCreditCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchJPCommonCreditCardOrder";
        };

        return ResFetchJPCommonCreditCardOrder;
    })();

    lq.ReqCreateJPGMOOrder = (function() {

        function ReqCreateJPGMOOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateJPGMOOrder.prototype.goods_id = 0;
        ReqCreateJPGMOOrder.prototype.client_type = 0;
        ReqCreateJPGMOOrder.prototype.account_id = 0;
        ReqCreateJPGMOOrder.prototype.return_url = "";
        ReqCreateJPGMOOrder.prototype.access_token = "";
        ReqCreateJPGMOOrder.prototype.client_version_string = "";

        ReqCreateJPGMOOrder.create = function create(properties) {
            return new ReqCreateJPGMOOrder(properties);
        };

        ReqCreateJPGMOOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateJPGMOOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateJPGMOOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateJPGMOOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateJPGMOOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateJPGMOOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateJPGMOOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateJPGMOOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateJPGMOOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateJPGMOOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateJPGMOOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateJPGMOOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateJPGMOOrder";
        };

        return ReqCreateJPGMOOrder;
    })();

    lq.ResCreateJPGMOOrder = (function() {

        function ResCreateJPGMOOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateJPGMOOrder.prototype.error = null;
        ResCreateJPGMOOrder.prototype.order_id = "";

        ResCreateJPGMOOrder.create = function create(properties) {
            return new ResCreateJPGMOOrder(properties);
        };

        ResCreateJPGMOOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateJPGMOOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateJPGMOOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateJPGMOOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateJPGMOOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateJPGMOOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateJPGMOOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateJPGMOOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateJPGMOOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateJPGMOOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateJPGMOOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateJPGMOOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateJPGMOOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateJPGMOOrder";
        };

        return ResCreateJPGMOOrder;
    })();

    lq.ReqCreateYostarOrder = (function() {

        function ReqCreateYostarOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateYostarOrder.prototype.goods_id = 0;
        ReqCreateYostarOrder.prototype.client_type = 0;
        ReqCreateYostarOrder.prototype.account_id = 0;
        ReqCreateYostarOrder.prototype.order_type = 0;
        ReqCreateYostarOrder.prototype.client_version_string = "";

        ReqCreateYostarOrder.create = function create(properties) {
            return new ReqCreateYostarOrder(properties);
        };

        ReqCreateYostarOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.order_type != null && Object.hasOwnProperty.call(message, "order_type"))
                writer.uint32(32).uint32(message.order_type);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateYostarOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateYostarOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateYostarOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.order_type = value;
                        else
                            delete message.order_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateYostarOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateYostarOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.order_type != null && message.hasOwnProperty("order_type"))
                if (!$util.isInteger(message.order_type))
                    return "order_type: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateYostarOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateYostarOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateYostarOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.order_type != null)
                if (Number(object.order_type) !== 0)
                    message.order_type = object.order_type >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateYostarOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.order_type = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.order_type != null && message.hasOwnProperty("order_type"))
                object.order_type = message.order_type;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateYostarOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateYostarOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateYostarOrder";
        };

        return ReqCreateYostarOrder;
    })();

    lq.ResCreateYostarOrder = (function() {

        function ResCreateYostarOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateYostarOrder.prototype.error = null;
        ResCreateYostarOrder.prototype.order_id = "";

        ResCreateYostarOrder.create = function create(properties) {
            return new ResCreateYostarOrder(properties);
        };

        ResCreateYostarOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateYostarOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateYostarOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateYostarOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateYostarOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateYostarOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateYostarOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateYostarOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateYostarOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateYostarOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateYostarOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateYostarOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateYostarOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateYostarOrder";
        };

        return ResCreateYostarOrder;
    })();

    lq.ReqCreateYostarV4SDKOrder = (function() {

        function ReqCreateYostarV4SDKOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateYostarV4SDKOrder.prototype.goods_id = 0;
        ReqCreateYostarV4SDKOrder.prototype.client_type = 0;
        ReqCreateYostarV4SDKOrder.prototype.account_id = 0;
        ReqCreateYostarV4SDKOrder.prototype.order_type = 0;
        ReqCreateYostarV4SDKOrder.prototype.client_version_string = "";

        ReqCreateYostarV4SDKOrder.create = function create(properties) {
            return new ReqCreateYostarV4SDKOrder(properties);
        };

        ReqCreateYostarV4SDKOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.order_type != null && Object.hasOwnProperty.call(message, "order_type"))
                writer.uint32(32).uint32(message.order_type);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateYostarV4SDKOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateYostarV4SDKOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateYostarV4SDKOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.order_type = value;
                        else
                            delete message.order_type;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateYostarV4SDKOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateYostarV4SDKOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.order_type != null && message.hasOwnProperty("order_type"))
                if (!$util.isInteger(message.order_type))
                    return "order_type: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateYostarV4SDKOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateYostarV4SDKOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateYostarV4SDKOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.order_type != null)
                if (Number(object.order_type) !== 0)
                    message.order_type = object.order_type >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateYostarV4SDKOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.order_type = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.order_type != null && message.hasOwnProperty("order_type"))
                object.order_type = message.order_type;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateYostarV4SDKOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateYostarV4SDKOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateYostarV4SDKOrder";
        };

        return ReqCreateYostarV4SDKOrder;
    })();

    lq.ResCreateYostarV4SDKOrder = (function() {

        function ResCreateYostarV4SDKOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateYostarV4SDKOrder.prototype.error = null;
        ResCreateYostarV4SDKOrder.prototype.order_id = "";
        ResCreateYostarV4SDKOrder.prototype.notify_url = "";
        ResCreateYostarV4SDKOrder.prototype.extra_data = "";

        ResCreateYostarV4SDKOrder.create = function create(properties) {
            return new ResCreateYostarV4SDKOrder(properties);
        };

        ResCreateYostarV4SDKOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.notify_url != null && Object.hasOwnProperty.call(message, "notify_url"))
                writer.uint32(26).string(message.notify_url);
            if (message.extra_data != null && Object.hasOwnProperty.call(message, "extra_data"))
                writer.uint32(34).string(message.extra_data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateYostarV4SDKOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateYostarV4SDKOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateYostarV4SDKOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.notify_url = value;
                        else
                            delete message.notify_url;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.extra_data = value;
                        else
                            delete message.extra_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateYostarV4SDKOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateYostarV4SDKOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.notify_url != null && message.hasOwnProperty("notify_url"))
                if (!$util.isString(message.notify_url))
                    return "notify_url: string expected";
            if (message.extra_data != null && message.hasOwnProperty("extra_data"))
                if (!$util.isString(message.extra_data))
                    return "extra_data: string expected";
            return null;
        };

        ResCreateYostarV4SDKOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateYostarV4SDKOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateYostarV4SDKOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateYostarV4SDKOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.notify_url != null)
                if (typeof object.notify_url !== "string" || object.notify_url.length)
                    message.notify_url = String(object.notify_url);
            if (object.extra_data != null)
                if (typeof object.extra_data !== "string" || object.extra_data.length)
                    message.extra_data = String(object.extra_data);
            return message;
        };

        ResCreateYostarV4SDKOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
                object.notify_url = "";
                object.extra_data = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.notify_url != null && message.hasOwnProperty("notify_url"))
                object.notify_url = message.notify_url;
            if (message.extra_data != null && message.hasOwnProperty("extra_data"))
                object.extra_data = message.extra_data;
            return object;
        };

        ResCreateYostarV4SDKOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateYostarV4SDKOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateYostarV4SDKOrder";
        };

        return ResCreateYostarV4SDKOrder;
    })();

    lq.ReqCreateENPaypalOrder = (function() {

        function ReqCreateENPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateENPaypalOrder.prototype.goods_id = 0;
        ReqCreateENPaypalOrder.prototype.client_type = 0;
        ReqCreateENPaypalOrder.prototype.account_id = 0;
        ReqCreateENPaypalOrder.prototype.return_url = "";
        ReqCreateENPaypalOrder.prototype.access_token = "";
        ReqCreateENPaypalOrder.prototype.client_version_string = "";

        ReqCreateENPaypalOrder.create = function create(properties) {
            return new ReqCreateENPaypalOrder(properties);
        };

        ReqCreateENPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateENPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateENPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateENPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateENPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateENPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateENPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateENPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateENPaypalOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateENPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateENPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateENPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateENPaypalOrder";
        };

        return ReqCreateENPaypalOrder;
    })();

    lq.ResCreateENPaypalOrder = (function() {

        function ResCreateENPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateENPaypalOrder.prototype.error = null;
        ResCreateENPaypalOrder.prototype.order_id = "";

        ResCreateENPaypalOrder.create = function create(properties) {
            return new ResCreateENPaypalOrder(properties);
        };

        ResCreateENPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateENPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateENPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateENPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateENPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateENPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateENPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateENPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateENPaypalOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateENPaypalOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateENPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateENPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateENPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateENPaypalOrder";
        };

        return ResCreateENPaypalOrder;
    })();

    lq.ReqCreateENJCBOrder = (function() {

        function ReqCreateENJCBOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateENJCBOrder.prototype.goods_id = 0;
        ReqCreateENJCBOrder.prototype.client_type = 0;
        ReqCreateENJCBOrder.prototype.account_id = 0;
        ReqCreateENJCBOrder.prototype.return_url = "";
        ReqCreateENJCBOrder.prototype.access_token = "";
        ReqCreateENJCBOrder.prototype.client_version_string = "";

        ReqCreateENJCBOrder.create = function create(properties) {
            return new ReqCreateENJCBOrder(properties);
        };

        ReqCreateENJCBOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateENJCBOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateENJCBOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateENJCBOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateENJCBOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateENJCBOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateENJCBOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateENJCBOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateENJCBOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateENJCBOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateENJCBOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateENJCBOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateENJCBOrder";
        };

        return ReqCreateENJCBOrder;
    })();

    lq.ResCreateENJCBOrder = (function() {

        function ResCreateENJCBOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateENJCBOrder.prototype.error = null;
        ResCreateENJCBOrder.prototype.order_id = "";

        ResCreateENJCBOrder.create = function create(properties) {
            return new ResCreateENJCBOrder(properties);
        };

        ResCreateENJCBOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateENJCBOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateENJCBOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateENJCBOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateENJCBOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateENJCBOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateENJCBOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateENJCBOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateENJCBOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateENJCBOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateENJCBOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateENJCBOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateENJCBOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateENJCBOrder";
        };

        return ResCreateENJCBOrder;
    })();

    lq.ReqCreateENMasterCardOrder = (function() {

        function ReqCreateENMasterCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateENMasterCardOrder.prototype.goods_id = 0;
        ReqCreateENMasterCardOrder.prototype.client_type = 0;
        ReqCreateENMasterCardOrder.prototype.account_id = 0;
        ReqCreateENMasterCardOrder.prototype.return_url = "";
        ReqCreateENMasterCardOrder.prototype.access_token = "";
        ReqCreateENMasterCardOrder.prototype.client_version_string = "";

        ReqCreateENMasterCardOrder.create = function create(properties) {
            return new ReqCreateENMasterCardOrder(properties);
        };

        ReqCreateENMasterCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateENMasterCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateENMasterCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateENMasterCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateENMasterCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateENMasterCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateENMasterCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateENMasterCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateENMasterCardOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateENMasterCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateENMasterCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateENMasterCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateENMasterCardOrder";
        };

        return ReqCreateENMasterCardOrder;
    })();

    lq.ResCreateENMasterCardOrder = (function() {

        function ResCreateENMasterCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateENMasterCardOrder.prototype.error = null;
        ResCreateENMasterCardOrder.prototype.order_id = "";

        ResCreateENMasterCardOrder.create = function create(properties) {
            return new ResCreateENMasterCardOrder(properties);
        };

        ResCreateENMasterCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateENMasterCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateENMasterCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateENMasterCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateENMasterCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateENMasterCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateENMasterCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateENMasterCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateENMasterCardOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateENMasterCardOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateENMasterCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateENMasterCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateENMasterCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateENMasterCardOrder";
        };

        return ResCreateENMasterCardOrder;
    })();

    lq.ReqCreateENVisaOrder = (function() {

        function ReqCreateENVisaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateENVisaOrder.prototype.goods_id = 0;
        ReqCreateENVisaOrder.prototype.client_type = 0;
        ReqCreateENVisaOrder.prototype.account_id = 0;
        ReqCreateENVisaOrder.prototype.return_url = "";
        ReqCreateENVisaOrder.prototype.access_token = "";
        ReqCreateENVisaOrder.prototype.client_version_string = "";

        ReqCreateENVisaOrder.create = function create(properties) {
            return new ReqCreateENVisaOrder(properties);
        };

        ReqCreateENVisaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateENVisaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateENVisaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateENVisaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateENVisaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateENVisaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateENVisaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateENVisaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateENVisaOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateENVisaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateENVisaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateENVisaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateENVisaOrder";
        };

        return ReqCreateENVisaOrder;
    })();

    lq.ResCreateENVisaOrder = (function() {

        function ResCreateENVisaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateENVisaOrder.prototype.error = null;
        ResCreateENVisaOrder.prototype.order_id = "";

        ResCreateENVisaOrder.create = function create(properties) {
            return new ResCreateENVisaOrder(properties);
        };

        ResCreateENVisaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateENVisaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateENVisaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateENVisaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateENVisaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateENVisaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateENVisaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateENVisaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateENVisaOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateENVisaOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateENVisaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateENVisaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateENVisaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateENVisaOrder";
        };

        return ResCreateENVisaOrder;
    })();

    lq.ReqCreateENAlipayOrder = (function() {

        function ReqCreateENAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateENAlipayOrder.prototype.goods_id = 0;
        ReqCreateENAlipayOrder.prototype.client_type = 0;
        ReqCreateENAlipayOrder.prototype.account_id = 0;
        ReqCreateENAlipayOrder.prototype.return_url = "";
        ReqCreateENAlipayOrder.prototype.access_token = "";
        ReqCreateENAlipayOrder.prototype.client_version_string = "";

        ReqCreateENAlipayOrder.create = function create(properties) {
            return new ReqCreateENAlipayOrder(properties);
        };

        ReqCreateENAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateENAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateENAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateENAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateENAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateENAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateENAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateENAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateENAlipayOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateENAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateENAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateENAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateENAlipayOrder";
        };

        return ReqCreateENAlipayOrder;
    })();

    lq.ResCreateENAlipayOrder = (function() {

        function ResCreateENAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateENAlipayOrder.prototype.error = null;
        ResCreateENAlipayOrder.prototype.order_id = "";

        ResCreateENAlipayOrder.create = function create(properties) {
            return new ResCreateENAlipayOrder(properties);
        };

        ResCreateENAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateENAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateENAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateENAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateENAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateENAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateENAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateENAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateENAlipayOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateENAlipayOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateENAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateENAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateENAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateENAlipayOrder";
        };

        return ResCreateENAlipayOrder;
    })();

    lq.ReqCreateKRPaypalOrder = (function() {

        function ReqCreateKRPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateKRPaypalOrder.prototype.goods_id = 0;
        ReqCreateKRPaypalOrder.prototype.client_type = 0;
        ReqCreateKRPaypalOrder.prototype.account_id = 0;
        ReqCreateKRPaypalOrder.prototype.return_url = "";
        ReqCreateKRPaypalOrder.prototype.access_token = "";
        ReqCreateKRPaypalOrder.prototype.client_version_string = "";

        ReqCreateKRPaypalOrder.create = function create(properties) {
            return new ReqCreateKRPaypalOrder(properties);
        };

        ReqCreateKRPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateKRPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateKRPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateKRPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateKRPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateKRPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateKRPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateKRPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateKRPaypalOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateKRPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateKRPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateKRPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateKRPaypalOrder";
        };

        return ReqCreateKRPaypalOrder;
    })();

    lq.ResCreateKRPaypalOrder = (function() {

        function ResCreateKRPaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateKRPaypalOrder.prototype.error = null;
        ResCreateKRPaypalOrder.prototype.order_id = "";

        ResCreateKRPaypalOrder.create = function create(properties) {
            return new ResCreateKRPaypalOrder(properties);
        };

        ResCreateKRPaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateKRPaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateKRPaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateKRPaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateKRPaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateKRPaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateKRPaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateKRPaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateKRPaypalOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateKRPaypalOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateKRPaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateKRPaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateKRPaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateKRPaypalOrder";
        };

        return ResCreateKRPaypalOrder;
    })();

    lq.ReqCreateKRJCBOrder = (function() {

        function ReqCreateKRJCBOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateKRJCBOrder.prototype.goods_id = 0;
        ReqCreateKRJCBOrder.prototype.client_type = 0;
        ReqCreateKRJCBOrder.prototype.account_id = 0;
        ReqCreateKRJCBOrder.prototype.return_url = "";
        ReqCreateKRJCBOrder.prototype.access_token = "";
        ReqCreateKRJCBOrder.prototype.client_version_string = "";

        ReqCreateKRJCBOrder.create = function create(properties) {
            return new ReqCreateKRJCBOrder(properties);
        };

        ReqCreateKRJCBOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateKRJCBOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateKRJCBOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateKRJCBOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateKRJCBOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateKRJCBOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateKRJCBOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateKRJCBOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateKRJCBOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateKRJCBOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateKRJCBOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateKRJCBOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateKRJCBOrder";
        };

        return ReqCreateKRJCBOrder;
    })();

    lq.ResCreateKRJCBOrder = (function() {

        function ResCreateKRJCBOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateKRJCBOrder.prototype.error = null;
        ResCreateKRJCBOrder.prototype.order_id = "";

        ResCreateKRJCBOrder.create = function create(properties) {
            return new ResCreateKRJCBOrder(properties);
        };

        ResCreateKRJCBOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateKRJCBOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateKRJCBOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateKRJCBOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateKRJCBOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateKRJCBOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateKRJCBOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateKRJCBOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateKRJCBOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateKRJCBOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateKRJCBOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateKRJCBOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateKRJCBOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateKRJCBOrder";
        };

        return ResCreateKRJCBOrder;
    })();

    lq.ReqCreateKRMasterCardOrder = (function() {

        function ReqCreateKRMasterCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateKRMasterCardOrder.prototype.goods_id = 0;
        ReqCreateKRMasterCardOrder.prototype.client_type = 0;
        ReqCreateKRMasterCardOrder.prototype.account_id = 0;
        ReqCreateKRMasterCardOrder.prototype.return_url = "";
        ReqCreateKRMasterCardOrder.prototype.access_token = "";
        ReqCreateKRMasterCardOrder.prototype.client_version_string = "";

        ReqCreateKRMasterCardOrder.create = function create(properties) {
            return new ReqCreateKRMasterCardOrder(properties);
        };

        ReqCreateKRMasterCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateKRMasterCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateKRMasterCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateKRMasterCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateKRMasterCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateKRMasterCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateKRMasterCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateKRMasterCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateKRMasterCardOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateKRMasterCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateKRMasterCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateKRMasterCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateKRMasterCardOrder";
        };

        return ReqCreateKRMasterCardOrder;
    })();

    lq.ResCreateKRMasterCardOrder = (function() {

        function ResCreateKRMasterCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateKRMasterCardOrder.prototype.error = null;
        ResCreateKRMasterCardOrder.prototype.order_id = "";

        ResCreateKRMasterCardOrder.create = function create(properties) {
            return new ResCreateKRMasterCardOrder(properties);
        };

        ResCreateKRMasterCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateKRMasterCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateKRMasterCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateKRMasterCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateKRMasterCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateKRMasterCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateKRMasterCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateKRMasterCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateKRMasterCardOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateKRMasterCardOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateKRMasterCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateKRMasterCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateKRMasterCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateKRMasterCardOrder";
        };

        return ResCreateKRMasterCardOrder;
    })();

    lq.ReqCreateKRVisaOrder = (function() {

        function ReqCreateKRVisaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateKRVisaOrder.prototype.goods_id = 0;
        ReqCreateKRVisaOrder.prototype.client_type = 0;
        ReqCreateKRVisaOrder.prototype.account_id = 0;
        ReqCreateKRVisaOrder.prototype.return_url = "";
        ReqCreateKRVisaOrder.prototype.access_token = "";
        ReqCreateKRVisaOrder.prototype.client_version_string = "";

        ReqCreateKRVisaOrder.create = function create(properties) {
            return new ReqCreateKRVisaOrder(properties);
        };

        ReqCreateKRVisaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateKRVisaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateKRVisaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateKRVisaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateKRVisaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateKRVisaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateKRVisaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateKRVisaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateKRVisaOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateKRVisaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateKRVisaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateKRVisaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateKRVisaOrder";
        };

        return ReqCreateKRVisaOrder;
    })();

    lq.ResCreateKRVisaOrder = (function() {

        function ResCreateKRVisaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateKRVisaOrder.prototype.error = null;
        ResCreateKRVisaOrder.prototype.order_id = "";

        ResCreateKRVisaOrder.create = function create(properties) {
            return new ResCreateKRVisaOrder(properties);
        };

        ResCreateKRVisaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateKRVisaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateKRVisaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateKRVisaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateKRVisaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateKRVisaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateKRVisaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateKRVisaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateKRVisaOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateKRVisaOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateKRVisaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateKRVisaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateKRVisaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateKRVisaOrder";
        };

        return ResCreateKRVisaOrder;
    })();

    lq.ReqCreateKRAlipayOrder = (function() {

        function ReqCreateKRAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateKRAlipayOrder.prototype.goods_id = 0;
        ReqCreateKRAlipayOrder.prototype.client_type = 0;
        ReqCreateKRAlipayOrder.prototype.account_id = 0;
        ReqCreateKRAlipayOrder.prototype.return_url = "";
        ReqCreateKRAlipayOrder.prototype.access_token = "";
        ReqCreateKRAlipayOrder.prototype.client_version_string = "";

        ReqCreateKRAlipayOrder.create = function create(properties) {
            return new ReqCreateKRAlipayOrder(properties);
        };

        ReqCreateKRAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.return_url != null && Object.hasOwnProperty.call(message, "return_url"))
                writer.uint32(34).string(message.return_url);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(42).string(message.access_token);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateKRAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateKRAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateKRAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.return_url = value;
                        else
                            delete message.return_url;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateKRAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateKRAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                if (!$util.isString(message.return_url))
                    return "return_url: string expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateKRAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateKRAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateKRAlipayOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.return_url != null)
                if (typeof object.return_url !== "string" || object.return_url.length)
                    message.return_url = String(object.return_url);
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateKRAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.return_url = "";
                object.access_token = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.return_url != null && message.hasOwnProperty("return_url"))
                object.return_url = message.return_url;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateKRAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateKRAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateKRAlipayOrder";
        };

        return ReqCreateKRAlipayOrder;
    })();

    lq.ResCreateKRAlipayOrder = (function() {

        function ResCreateKRAlipayOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateKRAlipayOrder.prototype.error = null;
        ResCreateKRAlipayOrder.prototype.order_id = "";

        ResCreateKRAlipayOrder.create = function create(properties) {
            return new ResCreateKRAlipayOrder(properties);
        };

        ResCreateKRAlipayOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateKRAlipayOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateKRAlipayOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateKRAlipayOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateKRAlipayOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateKRAlipayOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateKRAlipayOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateKRAlipayOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateKRAlipayOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateKRAlipayOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateKRAlipayOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateKRAlipayOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateKRAlipayOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateKRAlipayOrder";
        };

        return ResCreateKRAlipayOrder;
    })();

    lq.ReqCreateDMMOrder = (function() {

        function ReqCreateDMMOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateDMMOrder.prototype.goods_id = 0;
        ReqCreateDMMOrder.prototype.account_id = 0;
        ReqCreateDMMOrder.prototype.client_type = 0;
        ReqCreateDMMOrder.prototype.client_version_string = "";

        ReqCreateDMMOrder.create = function create(properties) {
            return new ReqCreateDMMOrder(properties);
        };

        ReqCreateDMMOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(24).uint32(message.client_type);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(34).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateDMMOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateDMMOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateDMMOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateDMMOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateDMMOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateDMMOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateDMMOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateDMMOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateDMMOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.account_id = 0;
                object.client_type = 0;
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateDMMOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateDMMOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateDMMOrder";
        };

        return ReqCreateDMMOrder;
    })();

    lq.ResCreateDmmOrder = (function() {

        function ResCreateDmmOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateDmmOrder.prototype.error = null;
        ResCreateDmmOrder.prototype.order_id = "";
        ResCreateDmmOrder.prototype.transaction_id = "";
        ResCreateDmmOrder.prototype.dmm_user_id = "";
        ResCreateDmmOrder.prototype.token = "";
        ResCreateDmmOrder.prototype.callback_url = "";
        ResCreateDmmOrder.prototype.request_time = "";
        ResCreateDmmOrder.prototype.dmm_app_id = "";

        ResCreateDmmOrder.create = function create(properties) {
            return new ResCreateDmmOrder(properties);
        };

        ResCreateDmmOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.transaction_id != null && Object.hasOwnProperty.call(message, "transaction_id"))
                writer.uint32(26).string(message.transaction_id);
            if (message.dmm_user_id != null && Object.hasOwnProperty.call(message, "dmm_user_id"))
                writer.uint32(34).string(message.dmm_user_id);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(42).string(message.token);
            if (message.callback_url != null && Object.hasOwnProperty.call(message, "callback_url"))
                writer.uint32(50).string(message.callback_url);
            if (message.request_time != null && Object.hasOwnProperty.call(message, "request_time"))
                writer.uint32(74).string(message.request_time);
            if (message.dmm_app_id != null && Object.hasOwnProperty.call(message, "dmm_app_id"))
                writer.uint32(82).string(message.dmm_app_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateDmmOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateDmmOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateDmmOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.transaction_id = value;
                        else
                            delete message.transaction_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.dmm_user_id = value;
                        else
                            delete message.dmm_user_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.callback_url = value;
                        else
                            delete message.callback_url;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.request_time = value;
                        else
                            delete message.request_time;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.dmm_app_id = value;
                        else
                            delete message.dmm_app_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateDmmOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateDmmOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                if (!$util.isString(message.transaction_id))
                    return "transaction_id: string expected";
            if (message.dmm_user_id != null && message.hasOwnProperty("dmm_user_id"))
                if (!$util.isString(message.dmm_user_id))
                    return "dmm_user_id: string expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.callback_url != null && message.hasOwnProperty("callback_url"))
                if (!$util.isString(message.callback_url))
                    return "callback_url: string expected";
            if (message.request_time != null && message.hasOwnProperty("request_time"))
                if (!$util.isString(message.request_time))
                    return "request_time: string expected";
            if (message.dmm_app_id != null && message.hasOwnProperty("dmm_app_id"))
                if (!$util.isString(message.dmm_app_id))
                    return "dmm_app_id: string expected";
            return null;
        };

        ResCreateDmmOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateDmmOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateDmmOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateDmmOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.transaction_id != null)
                if (typeof object.transaction_id !== "string" || object.transaction_id.length)
                    message.transaction_id = String(object.transaction_id);
            if (object.dmm_user_id != null)
                if (typeof object.dmm_user_id !== "string" || object.dmm_user_id.length)
                    message.dmm_user_id = String(object.dmm_user_id);
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.callback_url != null)
                if (typeof object.callback_url !== "string" || object.callback_url.length)
                    message.callback_url = String(object.callback_url);
            if (object.request_time != null)
                if (typeof object.request_time !== "string" || object.request_time.length)
                    message.request_time = String(object.request_time);
            if (object.dmm_app_id != null)
                if (typeof object.dmm_app_id !== "string" || object.dmm_app_id.length)
                    message.dmm_app_id = String(object.dmm_app_id);
            return message;
        };

        ResCreateDmmOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
                object.transaction_id = "";
                object.dmm_user_id = "";
                object.token = "";
                object.callback_url = "";
                object.request_time = "";
                object.dmm_app_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                object.transaction_id = message.transaction_id;
            if (message.dmm_user_id != null && message.hasOwnProperty("dmm_user_id"))
                object.dmm_user_id = message.dmm_user_id;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.callback_url != null && message.hasOwnProperty("callback_url"))
                object.callback_url = message.callback_url;
            if (message.request_time != null && message.hasOwnProperty("request_time"))
                object.request_time = message.request_time;
            if (message.dmm_app_id != null && message.hasOwnProperty("dmm_app_id"))
                object.dmm_app_id = message.dmm_app_id;
            return object;
        };

        ResCreateDmmOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateDmmOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateDmmOrder";
        };

        return ResCreateDmmOrder;
    })();

    lq.ReqCreateIAPOrder = (function() {

        function ReqCreateIAPOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateIAPOrder.prototype.goods_id = 0;
        ReqCreateIAPOrder.prototype.client_type = 0;
        ReqCreateIAPOrder.prototype.account_id = 0;
        ReqCreateIAPOrder.prototype.access_token = "";
        ReqCreateIAPOrder.prototype.debt_order_id = "";
        ReqCreateIAPOrder.prototype.client_version_string = "";

        ReqCreateIAPOrder.create = function create(properties) {
            return new ReqCreateIAPOrder(properties);
        };

        ReqCreateIAPOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.access_token != null && Object.hasOwnProperty.call(message, "access_token"))
                writer.uint32(34).string(message.access_token);
            if (message.debt_order_id != null && Object.hasOwnProperty.call(message, "debt_order_id"))
                writer.uint32(42).string(message.debt_order_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateIAPOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateIAPOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateIAPOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.access_token = value;
                        else
                            delete message.access_token;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.debt_order_id = value;
                        else
                            delete message.debt_order_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateIAPOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateIAPOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                if (!$util.isString(message.access_token))
                    return "access_token: string expected";
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                if (!$util.isString(message.debt_order_id))
                    return "debt_order_id: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateIAPOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateIAPOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateIAPOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.access_token != null)
                if (typeof object.access_token !== "string" || object.access_token.length)
                    message.access_token = String(object.access_token);
            if (object.debt_order_id != null)
                if (typeof object.debt_order_id !== "string" || object.debt_order_id.length)
                    message.debt_order_id = String(object.debt_order_id);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateIAPOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.access_token = "";
                object.debt_order_id = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.access_token != null && message.hasOwnProperty("access_token"))
                object.access_token = message.access_token;
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                object.debt_order_id = message.debt_order_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateIAPOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateIAPOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateIAPOrder";
        };

        return ReqCreateIAPOrder;
    })();

    lq.ResCreateIAPOrder = (function() {

        function ResCreateIAPOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateIAPOrder.prototype.error = null;
        ResCreateIAPOrder.prototype.order_id = "";

        ResCreateIAPOrder.create = function create(properties) {
            return new ResCreateIAPOrder(properties);
        };

        ResCreateIAPOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateIAPOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateIAPOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateIAPOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateIAPOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateIAPOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateIAPOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateIAPOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateIAPOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateIAPOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateIAPOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateIAPOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateIAPOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateIAPOrder";
        };

        return ResCreateIAPOrder;
    })();

    lq.ReqVerificationIAPOrder = (function() {

        function ReqVerificationIAPOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVerificationIAPOrder.prototype.order_id = "";
        ReqVerificationIAPOrder.prototype.transaction_id = "";
        ReqVerificationIAPOrder.prototype.receipt_data = "";
        ReqVerificationIAPOrder.prototype.account_id = 0;

        ReqVerificationIAPOrder.create = function create(properties) {
            return new ReqVerificationIAPOrder(properties);
        };

        ReqVerificationIAPOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.transaction_id != null && Object.hasOwnProperty.call(message, "transaction_id"))
                writer.uint32(18).string(message.transaction_id);
            if (message.receipt_data != null && Object.hasOwnProperty.call(message, "receipt_data"))
                writer.uint32(26).string(message.receipt_data);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(32).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVerificationIAPOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVerificationIAPOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVerificationIAPOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.transaction_id = value;
                        else
                            delete message.transaction_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.receipt_data = value;
                        else
                            delete message.receipt_data;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVerificationIAPOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVerificationIAPOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                if (!$util.isString(message.transaction_id))
                    return "transaction_id: string expected";
            if (message.receipt_data != null && message.hasOwnProperty("receipt_data"))
                if (!$util.isString(message.receipt_data))
                    return "receipt_data: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqVerificationIAPOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVerificationIAPOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVerificationIAPOrder();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.transaction_id != null)
                if (typeof object.transaction_id !== "string" || object.transaction_id.length)
                    message.transaction_id = String(object.transaction_id);
            if (object.receipt_data != null)
                if (typeof object.receipt_data !== "string" || object.receipt_data.length)
                    message.receipt_data = String(object.receipt_data);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqVerificationIAPOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.order_id = "";
                object.transaction_id = "";
                object.receipt_data = "";
                object.account_id = 0;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.transaction_id != null && message.hasOwnProperty("transaction_id"))
                object.transaction_id = message.transaction_id;
            if (message.receipt_data != null && message.hasOwnProperty("receipt_data"))
                object.receipt_data = message.receipt_data;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqVerificationIAPOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVerificationIAPOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVerificationIAPOrder";
        };

        return ReqVerificationIAPOrder;
    })();

    lq.ResVerificationIAPOrder = (function() {

        function ResVerificationIAPOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResVerificationIAPOrder.prototype.error = null;

        ResVerificationIAPOrder.create = function create(properties) {
            return new ResVerificationIAPOrder(properties);
        };

        ResVerificationIAPOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResVerificationIAPOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResVerificationIAPOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResVerificationIAPOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResVerificationIAPOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResVerificationIAPOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResVerificationIAPOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResVerificationIAPOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResVerificationIAPOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResVerificationIAPOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResVerificationIAPOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResVerificationIAPOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResVerificationIAPOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResVerificationIAPOrder";
        };

        return ResVerificationIAPOrder;
    })();

    lq.ReqCreateSteamOrder = (function() {

        function ReqCreateSteamOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateSteamOrder.prototype.language = "";
        ReqCreateSteamOrder.prototype.account_id = 0;
        ReqCreateSteamOrder.prototype.client_type = 0;
        ReqCreateSteamOrder.prototype.goods_id = 0;
        ReqCreateSteamOrder.prototype.steam_id = "";
        ReqCreateSteamOrder.prototype.debt_order_id = "";
        ReqCreateSteamOrder.prototype.client_version_string = "";

        ReqCreateSteamOrder.create = function create(properties) {
            return new ReqCreateSteamOrder(properties);
        };

        ReqCreateSteamOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.language != null && Object.hasOwnProperty.call(message, "language"))
                writer.uint32(10).string(message.language);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(24).uint32(message.client_type);
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(32).uint32(message.goods_id);
            if (message.steam_id != null && Object.hasOwnProperty.call(message, "steam_id"))
                writer.uint32(42).string(message.steam_id);
            if (message.debt_order_id != null && Object.hasOwnProperty.call(message, "debt_order_id"))
                writer.uint32(50).string(message.debt_order_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(58).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateSteamOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateSteamOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateSteamOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.language = value;
                        else
                            delete message.language;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.steam_id = value;
                        else
                            delete message.steam_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.debt_order_id = value;
                        else
                            delete message.debt_order_id;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateSteamOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateSteamOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.language != null && message.hasOwnProperty("language"))
                if (!$util.isString(message.language))
                    return "language: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.steam_id != null && message.hasOwnProperty("steam_id"))
                if (!$util.isString(message.steam_id))
                    return "steam_id: string expected";
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                if (!$util.isString(message.debt_order_id))
                    return "debt_order_id: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateSteamOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateSteamOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateSteamOrder();
            if (object.language != null)
                if (typeof object.language !== "string" || object.language.length)
                    message.language = String(object.language);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.steam_id != null)
                if (typeof object.steam_id !== "string" || object.steam_id.length)
                    message.steam_id = String(object.steam_id);
            if (object.debt_order_id != null)
                if (typeof object.debt_order_id !== "string" || object.debt_order_id.length)
                    message.debt_order_id = String(object.debt_order_id);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateSteamOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.language = "";
                object.account_id = 0;
                object.client_type = 0;
                object.goods_id = 0;
                object.steam_id = "";
                object.debt_order_id = "";
                object.client_version_string = "";
            }
            if (message.language != null && message.hasOwnProperty("language"))
                object.language = message.language;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.steam_id != null && message.hasOwnProperty("steam_id"))
                object.steam_id = message.steam_id;
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                object.debt_order_id = message.debt_order_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateSteamOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateSteamOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateSteamOrder";
        };

        return ReqCreateSteamOrder;
    })();

    lq.ResCreateSteamOrder = (function() {

        function ResCreateSteamOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateSteamOrder.prototype.error = null;
        ResCreateSteamOrder.prototype.order_id = "";
        ResCreateSteamOrder.prototype.platform_order_id = "";

        ResCreateSteamOrder.create = function create(properties) {
            return new ResCreateSteamOrder(properties);
        };

        ResCreateSteamOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.platform_order_id != null && Object.hasOwnProperty.call(message, "platform_order_id"))
                writer.uint32(26).string(message.platform_order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateSteamOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateSteamOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateSteamOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.platform_order_id = value;
                        else
                            delete message.platform_order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateSteamOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateSteamOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.platform_order_id != null && message.hasOwnProperty("platform_order_id"))
                if (!$util.isString(message.platform_order_id))
                    return "platform_order_id: string expected";
            return null;
        };

        ResCreateSteamOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateSteamOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateSteamOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateSteamOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.platform_order_id != null)
                if (typeof object.platform_order_id !== "string" || object.platform_order_id.length)
                    message.platform_order_id = String(object.platform_order_id);
            return message;
        };

        ResCreateSteamOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
                object.platform_order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.platform_order_id != null && message.hasOwnProperty("platform_order_id"))
                object.platform_order_id = message.platform_order_id;
            return object;
        };

        ResCreateSteamOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateSteamOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateSteamOrder";
        };

        return ResCreateSteamOrder;
    })();

    lq.ResRandomCharacter = (function() {

        function ResRandomCharacter(properties) {
            this.pool = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRandomCharacter.prototype.error = null;
        ResRandomCharacter.prototype.enabled = false;
        ResRandomCharacter.prototype.pool = $util.emptyArray;

        ResRandomCharacter.create = function create(properties) {
            return new ResRandomCharacter(properties);
        };

        ResRandomCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                writer.uint32(16).bool(message.enabled);
            if (message.pool != null && message.pool.length)
                for (let i = 0; i < message.pool.length; ++i)
                    $root.lq.RandomCharacter.encode(message.pool[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRandomCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRandomCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRandomCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enabled = value;
                        else
                            delete message.enabled;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pool && message.pool.length))
                            message.pool = [];
                        message.pool.push($root.lq.RandomCharacter.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRandomCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRandomCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            if (message.pool != null && message.hasOwnProperty("pool")) {
                if (!Array.isArray(message.pool))
                    return "pool: array expected";
                for (let i = 0; i < message.pool.length; ++i) {
                    let error = $root.lq.RandomCharacter.verify(message.pool[i], _depth + 1);
                    if (error)
                        return "pool." + error;
                }
            }
            return null;
        };

        ResRandomCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRandomCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRandomCharacter();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRandomCharacter.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.enabled != null)
                if (object.enabled)
                    message.enabled = Boolean(object.enabled);
            if (object.pool) {
                if (!Array.isArray(object.pool))
                    throw TypeError(".lq.ResRandomCharacter.pool: array expected");
                message.pool = Array(object.pool.length);
                for (let i = 0; i < object.pool.length; ++i) {
                    if (typeof object.pool[i] !== "object")
                        throw TypeError(".lq.ResRandomCharacter.pool: object expected");
                    message.pool[i] = $root.lq.RandomCharacter.fromObject(object.pool[i], _depth + 1);
                }
            }
            return message;
        };

        ResRandomCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.pool = [];
            if (options.defaults) {
                object.error = null;
                object.enabled = false;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                object.enabled = message.enabled;
            if (message.pool && message.pool.length) {
                object.pool = Array(message.pool.length);
                for (let j = 0; j < message.pool.length; ++j)
                    object.pool[j] = $root.lq.RandomCharacter.toObject(message.pool[j], options);
            }
            return object;
        };

        ResRandomCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRandomCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRandomCharacter";
        };

        return ResRandomCharacter;
    })();

    lq.ReqRandomCharacter = (function() {

        function ReqRandomCharacter(properties) {
            this.pool = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRandomCharacter.prototype.enabled = false;
        ReqRandomCharacter.prototype.pool = $util.emptyArray;

        ReqRandomCharacter.create = function create(properties) {
            return new ReqRandomCharacter(properties);
        };

        ReqRandomCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                writer.uint32(8).bool(message.enabled);
            if (message.pool != null && message.pool.length)
                for (let i = 0; i < message.pool.length; ++i)
                    $root.lq.RandomCharacter.encode(message.pool[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRandomCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRandomCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRandomCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enabled = value;
                        else
                            delete message.enabled;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pool && message.pool.length))
                            message.pool = [];
                        message.pool.push($root.lq.RandomCharacter.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRandomCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRandomCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                if (typeof message.enabled !== "boolean")
                    return "enabled: boolean expected";
            if (message.pool != null && message.hasOwnProperty("pool")) {
                if (!Array.isArray(message.pool))
                    return "pool: array expected";
                for (let i = 0; i < message.pool.length; ++i) {
                    let error = $root.lq.RandomCharacter.verify(message.pool[i], _depth + 1);
                    if (error)
                        return "pool." + error;
                }
            }
            return null;
        };

        ReqRandomCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRandomCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRandomCharacter();
            if (object.enabled != null)
                if (object.enabled)
                    message.enabled = Boolean(object.enabled);
            if (object.pool) {
                if (!Array.isArray(object.pool))
                    throw TypeError(".lq.ReqRandomCharacter.pool: array expected");
                message.pool = Array(object.pool.length);
                for (let i = 0; i < object.pool.length; ++i) {
                    if (typeof object.pool[i] !== "object")
                        throw TypeError(".lq.ReqRandomCharacter.pool: object expected");
                    message.pool[i] = $root.lq.RandomCharacter.fromObject(object.pool[i], _depth + 1);
                }
            }
            return message;
        };

        ReqRandomCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.pool = [];
            if (options.defaults)
                object.enabled = false;
            if (message.enabled != null && message.hasOwnProperty("enabled"))
                object.enabled = message.enabled;
            if (message.pool && message.pool.length) {
                object.pool = Array(message.pool.length);
                for (let j = 0; j < message.pool.length; ++j)
                    object.pool[j] = $root.lq.RandomCharacter.toObject(message.pool[j], options);
            }
            return object;
        };

        ReqRandomCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRandomCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRandomCharacter";
        };

        return ReqRandomCharacter;
    })();

    lq.ReqVerifySteamOrder = (function() {

        function ReqVerifySteamOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVerifySteamOrder.prototype.order_id = "";
        ReqVerifySteamOrder.prototype.account_id = 0;

        ReqVerifySteamOrder.create = function create(properties) {
            return new ReqVerifySteamOrder(properties);
        };

        ReqVerifySteamOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVerifySteamOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVerifySteamOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVerifySteamOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVerifySteamOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVerifySteamOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqVerifySteamOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVerifySteamOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVerifySteamOrder();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqVerifySteamOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.order_id = "";
                object.account_id = 0;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqVerifySteamOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVerifySteamOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVerifySteamOrder";
        };

        return ReqVerifySteamOrder;
    })();

    lq.ReqCreateMyCardOrder = (function() {

        function ReqCreateMyCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateMyCardOrder.prototype.goods_id = 0;
        ReqCreateMyCardOrder.prototype.client_type = 0;
        ReqCreateMyCardOrder.prototype.account_id = 0;
        ReqCreateMyCardOrder.prototype.debt_order_id = "";
        ReqCreateMyCardOrder.prototype.client_version_string = "";

        ReqCreateMyCardOrder.create = function create(properties) {
            return new ReqCreateMyCardOrder(properties);
        };

        ReqCreateMyCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.debt_order_id != null && Object.hasOwnProperty.call(message, "debt_order_id"))
                writer.uint32(34).string(message.debt_order_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateMyCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateMyCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateMyCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.debt_order_id = value;
                        else
                            delete message.debt_order_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateMyCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateMyCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                if (!$util.isString(message.debt_order_id))
                    return "debt_order_id: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreateMyCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateMyCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateMyCardOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.debt_order_id != null)
                if (typeof object.debt_order_id !== "string" || object.debt_order_id.length)
                    message.debt_order_id = String(object.debt_order_id);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreateMyCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.debt_order_id = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                object.debt_order_id = message.debt_order_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreateMyCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateMyCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateMyCardOrder";
        };

        return ReqCreateMyCardOrder;
    })();

    lq.ResCreateMyCardOrder = (function() {

        function ResCreateMyCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateMyCardOrder.prototype.error = null;
        ResCreateMyCardOrder.prototype.auth_code = "";
        ResCreateMyCardOrder.prototype.order_id = "";

        ResCreateMyCardOrder.create = function create(properties) {
            return new ResCreateMyCardOrder(properties);
        };

        ResCreateMyCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.auth_code != null && Object.hasOwnProperty.call(message, "auth_code"))
                writer.uint32(18).string(message.auth_code);
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(26).string(message.order_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateMyCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateMyCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateMyCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.auth_code = value;
                        else
                            delete message.auth_code;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateMyCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateMyCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.auth_code != null && message.hasOwnProperty("auth_code"))
                if (!$util.isString(message.auth_code))
                    return "auth_code: string expected";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            return null;
        };

        ResCreateMyCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateMyCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateMyCardOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateMyCardOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.auth_code != null)
                if (typeof object.auth_code !== "string" || object.auth_code.length)
                    message.auth_code = String(object.auth_code);
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            return message;
        };

        ResCreateMyCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.auth_code = "";
                object.order_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.auth_code != null && message.hasOwnProperty("auth_code"))
                object.auth_code = message.auth_code;
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            return object;
        };

        ResCreateMyCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateMyCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateMyCardOrder";
        };

        return ResCreateMyCardOrder;
    })();

    lq.ReqVerifyMyCardOrder = (function() {

        function ReqVerifyMyCardOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVerifyMyCardOrder.prototype.order_id = "";
        ReqVerifyMyCardOrder.prototype.account_id = 0;

        ReqVerifyMyCardOrder.create = function create(properties) {
            return new ReqVerifyMyCardOrder(properties);
        };

        ReqVerifyMyCardOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(10).string(message.order_id);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(16).uint32(message.account_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVerifyMyCardOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVerifyMyCardOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVerifyMyCardOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVerifyMyCardOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVerifyMyCardOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            return null;
        };

        ReqVerifyMyCardOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVerifyMyCardOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVerifyMyCardOrder();
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            return message;
        };

        ReqVerifyMyCardOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.order_id = "";
                object.account_id = 0;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            return object;
        };

        ReqVerifyMyCardOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVerifyMyCardOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVerifyMyCardOrder";
        };

        return ReqVerifyMyCardOrder;
    })();

    lq.ReqCreatePaypalOrder = (function() {

        function ReqCreatePaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreatePaypalOrder.prototype.goods_id = 0;
        ReqCreatePaypalOrder.prototype.client_type = 0;
        ReqCreatePaypalOrder.prototype.account_id = 0;
        ReqCreatePaypalOrder.prototype.debt_order_id = "";
        ReqCreatePaypalOrder.prototype.client_version_string = "";

        ReqCreatePaypalOrder.create = function create(properties) {
            return new ReqCreatePaypalOrder(properties);
        };

        ReqCreatePaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.debt_order_id != null && Object.hasOwnProperty.call(message, "debt_order_id"))
                writer.uint32(34).string(message.debt_order_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(42).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreatePaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreatePaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreatePaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.debt_order_id = value;
                        else
                            delete message.debt_order_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreatePaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreatePaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                if (!$util.isString(message.debt_order_id))
                    return "debt_order_id: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqCreatePaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreatePaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreatePaypalOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.debt_order_id != null)
                if (typeof object.debt_order_id !== "string" || object.debt_order_id.length)
                    message.debt_order_id = String(object.debt_order_id);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqCreatePaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.debt_order_id = "";
                object.client_version_string = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                object.debt_order_id = message.debt_order_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqCreatePaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreatePaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreatePaypalOrder";
        };

        return ReqCreatePaypalOrder;
    })();

    lq.ResCreatePaypalOrder = (function() {

        function ResCreatePaypalOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreatePaypalOrder.prototype.error = null;
        ResCreatePaypalOrder.prototype.order_id = "";
        ResCreatePaypalOrder.prototype.url = "";

        ResCreatePaypalOrder.create = function create(properties) {
            return new ResCreatePaypalOrder(properties);
        };

        ResCreatePaypalOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(26).string(message.url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreatePaypalOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreatePaypalOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreatePaypalOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.url = value;
                        else
                            delete message.url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreatePaypalOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreatePaypalOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.url != null && message.hasOwnProperty("url"))
                if (!$util.isString(message.url))
                    return "url: string expected";
            return null;
        };

        ResCreatePaypalOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreatePaypalOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreatePaypalOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreatePaypalOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.url != null)
                if (typeof object.url !== "string" || object.url.length)
                    message.url = String(object.url);
            return message;
        };

        ResCreatePaypalOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
                object.url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.url != null && message.hasOwnProperty("url"))
                object.url = message.url;
            return object;
        };

        ResCreatePaypalOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreatePaypalOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreatePaypalOrder";
        };

        return ResCreatePaypalOrder;
    })();

    lq.ReqCreateXsollaOrder = (function() {

        function ReqCreateXsollaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateXsollaOrder.prototype.goods_id = 0;
        ReqCreateXsollaOrder.prototype.client_type = 0;
        ReqCreateXsollaOrder.prototype.account_id = 0;
        ReqCreateXsollaOrder.prototype.payment_method = 0;
        ReqCreateXsollaOrder.prototype.debt_order_id = "";
        ReqCreateXsollaOrder.prototype.client_version_string = "";
        ReqCreateXsollaOrder.prototype.account_ip = "";

        ReqCreateXsollaOrder.create = function create(properties) {
            return new ReqCreateXsollaOrder(properties);
        };

        ReqCreateXsollaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.client_type != null && Object.hasOwnProperty.call(message, "client_type"))
                writer.uint32(16).uint32(message.client_type);
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(24).uint32(message.account_id);
            if (message.payment_method != null && Object.hasOwnProperty.call(message, "payment_method"))
                writer.uint32(32).uint32(message.payment_method);
            if (message.debt_order_id != null && Object.hasOwnProperty.call(message, "debt_order_id"))
                writer.uint32(42).string(message.debt_order_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(50).string(message.client_version_string);
            if (message.account_ip != null && Object.hasOwnProperty.call(message, "account_ip"))
                writer.uint32(58).string(message.account_ip);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateXsollaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateXsollaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateXsollaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.client_type = value;
                        else
                            delete message.client_type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.payment_method = value;
                        else
                            delete message.payment_method;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.debt_order_id = value;
                        else
                            delete message.debt_order_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.account_ip = value;
                        else
                            delete message.account_ip;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateXsollaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateXsollaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                if (!$util.isInteger(message.client_type))
                    return "client_type: integer expected";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.payment_method != null && message.hasOwnProperty("payment_method"))
                if (!$util.isInteger(message.payment_method))
                    return "payment_method: integer expected";
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                if (!$util.isString(message.debt_order_id))
                    return "debt_order_id: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                if (!$util.isString(message.account_ip))
                    return "account_ip: string expected";
            return null;
        };

        ReqCreateXsollaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateXsollaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateXsollaOrder();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.client_type != null)
                if (Number(object.client_type) !== 0)
                    message.client_type = object.client_type >>> 0;
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.payment_method != null)
                if (Number(object.payment_method) !== 0)
                    message.payment_method = object.payment_method >>> 0;
            if (object.debt_order_id != null)
                if (typeof object.debt_order_id !== "string" || object.debt_order_id.length)
                    message.debt_order_id = String(object.debt_order_id);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            if (object.account_ip != null)
                if (typeof object.account_ip !== "string" || object.account_ip.length)
                    message.account_ip = String(object.account_ip);
            return message;
        };

        ReqCreateXsollaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.client_type = 0;
                object.account_id = 0;
                object.payment_method = 0;
                object.debt_order_id = "";
                object.client_version_string = "";
                object.account_ip = "";
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.client_type != null && message.hasOwnProperty("client_type"))
                object.client_type = message.client_type;
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.payment_method != null && message.hasOwnProperty("payment_method"))
                object.payment_method = message.payment_method;
            if (message.debt_order_id != null && message.hasOwnProperty("debt_order_id"))
                object.debt_order_id = message.debt_order_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            if (message.account_ip != null && message.hasOwnProperty("account_ip"))
                object.account_ip = message.account_ip;
            return object;
        };

        ReqCreateXsollaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateXsollaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateXsollaOrder";
        };

        return ReqCreateXsollaOrder;
    })();

    lq.ResCreateXsollaOrder = (function() {

        function ResCreateXsollaOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateXsollaOrder.prototype.error = null;
        ResCreateXsollaOrder.prototype.order_id = "";
        ResCreateXsollaOrder.prototype.url = "";

        ResCreateXsollaOrder.create = function create(properties) {
            return new ResCreateXsollaOrder(properties);
        };

        ResCreateXsollaOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                writer.uint32(18).string(message.order_id);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(26).string(message.url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateXsollaOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateXsollaOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateXsollaOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.order_id = value;
                        else
                            delete message.order_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.url = value;
                        else
                            delete message.url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateXsollaOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateXsollaOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                if (!$util.isString(message.order_id))
                    return "order_id: string expected";
            if (message.url != null && message.hasOwnProperty("url"))
                if (!$util.isString(message.url))
                    return "url: string expected";
            return null;
        };

        ResCreateXsollaOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateXsollaOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateXsollaOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateXsollaOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.order_id != null)
                if (typeof object.order_id !== "string" || object.order_id.length)
                    message.order_id = String(object.order_id);
            if (object.url != null)
                if (typeof object.url !== "string" || object.url.length)
                    message.url = String(object.url);
            return message;
        };

        ResCreateXsollaOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.order_id = "";
                object.url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.order_id != null && message.hasOwnProperty("order_id"))
                object.order_id = message.order_id;
            if (message.url != null && message.hasOwnProperty("url"))
                object.url = message.url;
            return object;
        };

        ResCreateXsollaOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateXsollaOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateXsollaOrder";
        };

        return ResCreateXsollaOrder;
    })();

    lq.ReqDeliverAA32Order = (function() {

        function ReqDeliverAA32Order(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDeliverAA32Order.prototype.account_id = 0;
        ReqDeliverAA32Order.prototype.nsa_id = "";
        ReqDeliverAA32Order.prototype.nsa_token = "";

        ReqDeliverAA32Order.create = function create(properties) {
            return new ReqDeliverAA32Order(properties);
        };

        ReqDeliverAA32Order.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.nsa_id != null && Object.hasOwnProperty.call(message, "nsa_id"))
                writer.uint32(18).string(message.nsa_id);
            if (message.nsa_token != null && Object.hasOwnProperty.call(message, "nsa_token"))
                writer.uint32(26).string(message.nsa_token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDeliverAA32Order.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDeliverAA32Order.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDeliverAA32Order(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nsa_id = value;
                        else
                            delete message.nsa_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.nsa_token = value;
                        else
                            delete message.nsa_token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDeliverAA32Order.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDeliverAA32Order.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.nsa_id != null && message.hasOwnProperty("nsa_id"))
                if (!$util.isString(message.nsa_id))
                    return "nsa_id: string expected";
            if (message.nsa_token != null && message.hasOwnProperty("nsa_token"))
                if (!$util.isString(message.nsa_token))
                    return "nsa_token: string expected";
            return null;
        };

        ReqDeliverAA32Order.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDeliverAA32Order)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDeliverAA32Order();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.nsa_id != null)
                if (typeof object.nsa_id !== "string" || object.nsa_id.length)
                    message.nsa_id = String(object.nsa_id);
            if (object.nsa_token != null)
                if (typeof object.nsa_token !== "string" || object.nsa_token.length)
                    message.nsa_token = String(object.nsa_token);
            return message;
        };

        ReqDeliverAA32Order.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.nsa_id = "";
                object.nsa_token = "";
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.nsa_id != null && message.hasOwnProperty("nsa_id"))
                object.nsa_id = message.nsa_id;
            if (message.nsa_token != null && message.hasOwnProperty("nsa_token"))
                object.nsa_token = message.nsa_token;
            return object;
        };

        ReqDeliverAA32Order.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDeliverAA32Order.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDeliverAA32Order";
        };

        return ReqDeliverAA32Order;
    })();

    lq.ReqOpenChest = (function() {

        function ReqOpenChest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenChest.prototype.chest_id = 0;
        ReqOpenChest.prototype.count = 0;
        ReqOpenChest.prototype.use_ticket = false;
        ReqOpenChest.prototype.choose_up_activity_id = 0;
        ReqOpenChest.prototype.choose_group_activity_id = 0;

        ReqOpenChest.create = function create(properties) {
            return new ReqOpenChest(properties);
        };

        ReqOpenChest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(8).uint32(message.chest_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.use_ticket != null && Object.hasOwnProperty.call(message, "use_ticket"))
                writer.uint32(24).bool(message.use_ticket);
            if (message.choose_up_activity_id != null && Object.hasOwnProperty.call(message, "choose_up_activity_id"))
                writer.uint32(32).uint32(message.choose_up_activity_id);
            if (message.choose_group_activity_id != null && Object.hasOwnProperty.call(message, "choose_group_activity_id"))
                writer.uint32(40).uint32(message.choose_group_activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenChest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenChest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenChest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.use_ticket = value;
                        else
                            delete message.use_ticket;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.choose_up_activity_id = value;
                        else
                            delete message.choose_up_activity_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.choose_group_activity_id = value;
                        else
                            delete message.choose_group_activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenChest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenChest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.use_ticket != null && message.hasOwnProperty("use_ticket"))
                if (typeof message.use_ticket !== "boolean")
                    return "use_ticket: boolean expected";
            if (message.choose_up_activity_id != null && message.hasOwnProperty("choose_up_activity_id"))
                if (!$util.isInteger(message.choose_up_activity_id))
                    return "choose_up_activity_id: integer expected";
            if (message.choose_group_activity_id != null && message.hasOwnProperty("choose_group_activity_id"))
                if (!$util.isInteger(message.choose_group_activity_id))
                    return "choose_group_activity_id: integer expected";
            return null;
        };

        ReqOpenChest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenChest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenChest();
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.use_ticket != null)
                if (object.use_ticket)
                    message.use_ticket = Boolean(object.use_ticket);
            if (object.choose_up_activity_id != null)
                if (Number(object.choose_up_activity_id) !== 0)
                    message.choose_up_activity_id = object.choose_up_activity_id >>> 0;
            if (object.choose_group_activity_id != null)
                if (Number(object.choose_group_activity_id) !== 0)
                    message.choose_group_activity_id = object.choose_group_activity_id >>> 0;
            return message;
        };

        ReqOpenChest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.chest_id = 0;
                object.count = 0;
                object.use_ticket = false;
                object.choose_up_activity_id = 0;
                object.choose_group_activity_id = 0;
            }
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.use_ticket != null && message.hasOwnProperty("use_ticket"))
                object.use_ticket = message.use_ticket;
            if (message.choose_up_activity_id != null && message.hasOwnProperty("choose_up_activity_id"))
                object.choose_up_activity_id = message.choose_up_activity_id;
            if (message.choose_group_activity_id != null && message.hasOwnProperty("choose_group_activity_id"))
                object.choose_group_activity_id = message.choose_group_activity_id;
            return object;
        };

        ReqOpenChest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenChest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenChest";
        };

        return ReqOpenChest;
    })();

    lq.ResOpenChest = (function() {

        function ResOpenChest(properties) {
            this.results = [];
            this.chest_replace_up = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOpenChest.prototype.error = null;
        ResOpenChest.prototype.results = $util.emptyArray;
        ResOpenChest.prototype.total_open_count = 0;
        ResOpenChest.prototype.faith_count = 0;
        ResOpenChest.prototype.chest_replace_up = $util.emptyArray;

        ResOpenChest.create = function create(properties) {
            return new ResOpenChest(properties);
        };

        ResOpenChest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.lq.OpenResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
            if (message.total_open_count != null && Object.hasOwnProperty.call(message, "total_open_count"))
                writer.uint32(24).uint32(message.total_open_count);
            if (message.faith_count != null && Object.hasOwnProperty.call(message, "faith_count"))
                writer.uint32(32).uint32(message.faith_count);
            if (message.chest_replace_up != null && message.chest_replace_up.length)
                for (let i = 0; i < message.chest_replace_up.length; ++i)
                    $root.lq.ResOpenChest.ChestReplaceCountData.encode(message.chest_replace_up[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOpenChest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOpenChest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenChest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.lq.OpenResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_open_count = value;
                        else
                            delete message.total_open_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.faith_count = value;
                        else
                            delete message.faith_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chest_replace_up && message.chest_replace_up.length))
                            message.chest_replace_up = [];
                        message.chest_replace_up.push($root.lq.ResOpenChest.ChestReplaceCountData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOpenChest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOpenChest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.results != null && message.hasOwnProperty("results")) {
                if (!Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.lq.OpenResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                if (!$util.isInteger(message.total_open_count))
                    return "total_open_count: integer expected";
            if (message.faith_count != null && message.hasOwnProperty("faith_count"))
                if (!$util.isInteger(message.faith_count))
                    return "faith_count: integer expected";
            if (message.chest_replace_up != null && message.hasOwnProperty("chest_replace_up")) {
                if (!Array.isArray(message.chest_replace_up))
                    return "chest_replace_up: array expected";
                for (let i = 0; i < message.chest_replace_up.length; ++i) {
                    let error = $root.lq.ResOpenChest.ChestReplaceCountData.verify(message.chest_replace_up[i], _depth + 1);
                    if (error)
                        return "chest_replace_up." + error;
                }
            }
            return null;
        };

        ResOpenChest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOpenChest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOpenChest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOpenChest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.results) {
                if (!Array.isArray(object.results))
                    throw TypeError(".lq.ResOpenChest.results: array expected");
                message.results = Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (typeof object.results[i] !== "object")
                        throw TypeError(".lq.ResOpenChest.results: object expected");
                    message.results[i] = $root.lq.OpenResult.fromObject(object.results[i], _depth + 1);
                }
            }
            if (object.total_open_count != null)
                if (Number(object.total_open_count) !== 0)
                    message.total_open_count = object.total_open_count >>> 0;
            if (object.faith_count != null)
                if (Number(object.faith_count) !== 0)
                    message.faith_count = object.faith_count >>> 0;
            if (object.chest_replace_up) {
                if (!Array.isArray(object.chest_replace_up))
                    throw TypeError(".lq.ResOpenChest.chest_replace_up: array expected");
                message.chest_replace_up = Array(object.chest_replace_up.length);
                for (let i = 0; i < object.chest_replace_up.length; ++i) {
                    if (typeof object.chest_replace_up[i] !== "object")
                        throw TypeError(".lq.ResOpenChest.chest_replace_up: object expected");
                    message.chest_replace_up[i] = $root.lq.ResOpenChest.ChestReplaceCountData.fromObject(object.chest_replace_up[i], _depth + 1);
                }
            }
            return message;
        };

        ResOpenChest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.results = [];
                object.chest_replace_up = [];
            }
            if (options.defaults) {
                object.error = null;
                object.total_open_count = 0;
                object.faith_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.results && message.results.length) {
                object.results = Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.lq.OpenResult.toObject(message.results[j], options);
            }
            if (message.total_open_count != null && message.hasOwnProperty("total_open_count"))
                object.total_open_count = message.total_open_count;
            if (message.faith_count != null && message.hasOwnProperty("faith_count"))
                object.faith_count = message.faith_count;
            if (message.chest_replace_up && message.chest_replace_up.length) {
                object.chest_replace_up = Array(message.chest_replace_up.length);
                for (let j = 0; j < message.chest_replace_up.length; ++j)
                    object.chest_replace_up[j] = $root.lq.ResOpenChest.ChestReplaceCountData.toObject(message.chest_replace_up[j], options);
            }
            return object;
        };

        ResOpenChest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOpenChest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOpenChest";
        };

        ResOpenChest.ChestReplaceCountData = (function() {

            function ChestReplaceCountData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChestReplaceCountData.prototype.id = 0;
            ChestReplaceCountData.prototype.count = 0;

            ChestReplaceCountData.create = function create(properties) {
                return new ChestReplaceCountData(properties);
            };

            ChestReplaceCountData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChestReplaceCountData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChestReplaceCountData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenChest.ChestReplaceCountData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChestReplaceCountData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChestReplaceCountData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            ChestReplaceCountData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResOpenChest.ChestReplaceCountData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResOpenChest.ChestReplaceCountData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            ChestReplaceCountData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            ChestReplaceCountData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChestReplaceCountData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResOpenChest.ChestReplaceCountData";
            };

            return ChestReplaceCountData;
        })();

        return ResOpenChest;
    })();

    lq.ReqBuyFromChestShop = (function() {

        function ReqBuyFromChestShop(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyFromChestShop.prototype.goods_id = 0;
        ReqBuyFromChestShop.prototype.count = 0;

        ReqBuyFromChestShop.create = function create(properties) {
            return new ReqBuyFromChestShop(properties);
        };

        ReqBuyFromChestShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyFromChestShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyFromChestShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyFromChestShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyFromChestShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyFromChestShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqBuyFromChestShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyFromChestShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyFromChestShop();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqBuyFromChestShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.count = 0;
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqBuyFromChestShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyFromChestShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyFromChestShop";
        };

        return ReqBuyFromChestShop;
    })();

    lq.ResBuyFromChestShop = (function() {

        function ResBuyFromChestShop(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResBuyFromChestShop.prototype.error = null;
        ResBuyFromChestShop.prototype.chest_id = 0;
        ResBuyFromChestShop.prototype.consume_count = 0;
        ResBuyFromChestShop.prototype.faith_count = 0;

        ResBuyFromChestShop.create = function create(properties) {
            return new ResBuyFromChestShop(properties);
        };

        ResBuyFromChestShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(16).uint32(message.chest_id);
            if (message.consume_count != null && Object.hasOwnProperty.call(message, "consume_count"))
                writer.uint32(24).uint32(message.consume_count);
            if (message.faith_count != null && Object.hasOwnProperty.call(message, "faith_count"))
                writer.uint32(32).int32(message.faith_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResBuyFromChestShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResBuyFromChestShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResBuyFromChestShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.consume_count = value;
                        else
                            delete message.consume_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.faith_count = value;
                        else
                            delete message.faith_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResBuyFromChestShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResBuyFromChestShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                if (!$util.isInteger(message.consume_count))
                    return "consume_count: integer expected";
            if (message.faith_count != null && message.hasOwnProperty("faith_count"))
                if (!$util.isInteger(message.faith_count))
                    return "faith_count: integer expected";
            return null;
        };

        ResBuyFromChestShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResBuyFromChestShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResBuyFromChestShop();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResBuyFromChestShop.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            if (object.consume_count != null)
                if (Number(object.consume_count) !== 0)
                    message.consume_count = object.consume_count >>> 0;
            if (object.faith_count != null)
                if (Number(object.faith_count) !== 0)
                    message.faith_count = object.faith_count | 0;
            return message;
        };

        ResBuyFromChestShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.chest_id = 0;
                object.consume_count = 0;
                object.faith_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            if (message.consume_count != null && message.hasOwnProperty("consume_count"))
                object.consume_count = message.consume_count;
            if (message.faith_count != null && message.hasOwnProperty("faith_count"))
                object.faith_count = message.faith_count;
            return object;
        };

        ResBuyFromChestShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResBuyFromChestShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResBuyFromChestShop";
        };

        return ResBuyFromChestShop;
    })();

    lq.ResDailySignInInfo = (function() {

        function ResDailySignInInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDailySignInInfo.prototype.error = null;
        ResDailySignInInfo.prototype.sign_in_days = 0;

        ResDailySignInInfo.create = function create(properties) {
            return new ResDailySignInInfo(properties);
        };

        ResDailySignInInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.sign_in_days != null && Object.hasOwnProperty.call(message, "sign_in_days"))
                writer.uint32(16).uint32(message.sign_in_days);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDailySignInInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDailySignInInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDailySignInInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sign_in_days = value;
                        else
                            delete message.sign_in_days;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDailySignInInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDailySignInInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.sign_in_days != null && message.hasOwnProperty("sign_in_days"))
                if (!$util.isInteger(message.sign_in_days))
                    return "sign_in_days: integer expected";
            return null;
        };

        ResDailySignInInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDailySignInInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDailySignInInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDailySignInInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.sign_in_days != null)
                if (Number(object.sign_in_days) !== 0)
                    message.sign_in_days = object.sign_in_days >>> 0;
            return message;
        };

        ResDailySignInInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.sign_in_days = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.sign_in_days != null && message.hasOwnProperty("sign_in_days"))
                object.sign_in_days = message.sign_in_days;
            return object;
        };

        ResDailySignInInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDailySignInInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDailySignInInfo";
        };

        return ResDailySignInInfo;
    })();

    lq.ReqDoActivitySignIn = (function() {

        function ReqDoActivitySignIn(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDoActivitySignIn.prototype.activity_id = 0;

        ReqDoActivitySignIn.create = function create(properties) {
            return new ReqDoActivitySignIn(properties);
        };

        ReqDoActivitySignIn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDoActivitySignIn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDoActivitySignIn.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDoActivitySignIn(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDoActivitySignIn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDoActivitySignIn.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqDoActivitySignIn.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDoActivitySignIn)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDoActivitySignIn();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqDoActivitySignIn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqDoActivitySignIn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDoActivitySignIn.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDoActivitySignIn";
        };

        return ReqDoActivitySignIn;
    })();

    lq.ResDoActivitySignIn = (function() {

        function ResDoActivitySignIn(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDoActivitySignIn.prototype.error = null;
        ResDoActivitySignIn.prototype.rewards = $util.emptyArray;
        ResDoActivitySignIn.prototype.sign_in_count = 0;

        ResDoActivitySignIn.create = function create(properties) {
            return new ResDoActivitySignIn(properties);
        };

        ResDoActivitySignIn.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ResDoActivitySignIn.RewardData.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.sign_in_count != null && Object.hasOwnProperty.call(message, "sign_in_count"))
                writer.uint32(24).uint32(message.sign_in_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDoActivitySignIn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDoActivitySignIn.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDoActivitySignIn(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ResDoActivitySignIn.RewardData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sign_in_count = value;
                        else
                            delete message.sign_in_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDoActivitySignIn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDoActivitySignIn.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ResDoActivitySignIn.RewardData.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.sign_in_count != null && message.hasOwnProperty("sign_in_count"))
                if (!$util.isInteger(message.sign_in_count))
                    return "sign_in_count: integer expected";
            return null;
        };

        ResDoActivitySignIn.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDoActivitySignIn)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDoActivitySignIn();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDoActivitySignIn.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResDoActivitySignIn.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResDoActivitySignIn.rewards: object expected");
                    message.rewards[i] = $root.lq.ResDoActivitySignIn.RewardData.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.sign_in_count != null)
                if (Number(object.sign_in_count) !== 0)
                    message.sign_in_count = object.sign_in_count >>> 0;
            return message;
        };

        ResDoActivitySignIn.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.error = null;
                object.sign_in_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ResDoActivitySignIn.RewardData.toObject(message.rewards[j], options);
            }
            if (message.sign_in_count != null && message.hasOwnProperty("sign_in_count"))
                object.sign_in_count = message.sign_in_count;
            return object;
        };

        ResDoActivitySignIn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDoActivitySignIn.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDoActivitySignIn";
        };

        ResDoActivitySignIn.RewardData = (function() {

            function RewardData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardData.prototype.resource_id = 0;
            RewardData.prototype.count = 0;

            RewardData.create = function create(properties) {
                return new RewardData(properties);
            };

            RewardData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                    writer.uint32(8).uint32(message.resource_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDoActivitySignIn.RewardData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_id = value;
                            else
                                delete message.resource_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    if (!$util.isInteger(message.resource_id))
                        return "resource_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResDoActivitySignIn.RewardData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResDoActivitySignIn.RewardData();
                if (object.resource_id != null)
                    if (Number(object.resource_id) !== 0)
                        message.resource_id = object.resource_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.resource_id = 0;
                    object.count = 0;
                }
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    object.resource_id = message.resource_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResDoActivitySignIn.RewardData";
            };

            return RewardData;
        })();

        return ResDoActivitySignIn;
    })();

    lq.ResCharacterInfo = (function() {

        function ResCharacterInfo(properties) {
            this.characters = [];
            this.skins = [];
            this.finished_endings = [];
            this.rewarded_endings = [];
            this.character_sort = [];
            this.hidden_characters = [];
            this.other_character_sort = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCharacterInfo.prototype.error = null;
        ResCharacterInfo.prototype.characters = $util.emptyArray;
        ResCharacterInfo.prototype.skins = $util.emptyArray;
        ResCharacterInfo.prototype.main_character_id = 0;
        ResCharacterInfo.prototype.send_gift_count = 0;
        ResCharacterInfo.prototype.send_gift_limit = 0;
        ResCharacterInfo.prototype.finished_endings = $util.emptyArray;
        ResCharacterInfo.prototype.rewarded_endings = $util.emptyArray;
        ResCharacterInfo.prototype.character_sort = $util.emptyArray;
        ResCharacterInfo.prototype.hidden_characters = $util.emptyArray;
        ResCharacterInfo.prototype.other_character_sort = $util.emptyArray;

        ResCharacterInfo.create = function create(properties) {
            return new ResCharacterInfo(properties);
        };

        ResCharacterInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.characters != null && message.characters.length)
                for (let i = 0; i < message.characters.length; ++i)
                    $root.lq.Character.encode(message.characters[i], writer.uint32(18).fork()).ldelim();
            if (message.skins != null && message.skins.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.skins.length; ++i)
                    writer.uint32(message.skins[i]);
                writer.ldelim();
            }
            if (message.main_character_id != null && Object.hasOwnProperty.call(message, "main_character_id"))
                writer.uint32(32).uint32(message.main_character_id);
            if (message.send_gift_count != null && Object.hasOwnProperty.call(message, "send_gift_count"))
                writer.uint32(40).uint32(message.send_gift_count);
            if (message.send_gift_limit != null && Object.hasOwnProperty.call(message, "send_gift_limit"))
                writer.uint32(48).uint32(message.send_gift_limit);
            if (message.finished_endings != null && message.finished_endings.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.finished_endings.length; ++i)
                    writer.uint32(message.finished_endings[i]);
                writer.ldelim();
            }
            if (message.rewarded_endings != null && message.rewarded_endings.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.rewarded_endings.length; ++i)
                    writer.uint32(message.rewarded_endings[i]);
                writer.ldelim();
            }
            if (message.character_sort != null && message.character_sort.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.character_sort.length; ++i)
                    writer.uint32(message.character_sort[i]);
                writer.ldelim();
            }
            if (message.hidden_characters != null && message.hidden_characters.length) {
                writer.uint32(82).fork();
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    writer.uint32(message.hidden_characters[i]);
                writer.ldelim();
            }
            if (message.other_character_sort != null && message.other_character_sort.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.other_character_sort.length; ++i)
                    writer.uint32(message.other_character_sort[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCharacterInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCharacterInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCharacterInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.characters && message.characters.length))
                            message.characters = [];
                        message.characters.push($root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.skins && message.skins.length))
                                message.skins = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.skins.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.skins && message.skins.length))
                            message.skins = [];
                        message.skins.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.main_character_id = value;
                        else
                            delete message.main_character_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.send_gift_count = value;
                        else
                            delete message.send_gift_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.send_gift_limit = value;
                        else
                            delete message.send_gift_limit;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.finished_endings && message.finished_endings.length))
                                message.finished_endings = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finished_endings.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.finished_endings && message.finished_endings.length))
                            message.finished_endings = [];
                        message.finished_endings.push(reader.uint32());
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.rewarded_endings && message.rewarded_endings.length))
                                message.rewarded_endings = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_endings.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_endings && message.rewarded_endings.length))
                            message.rewarded_endings = [];
                        message.rewarded_endings.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.character_sort && message.character_sort.length))
                                message.character_sort = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.character_sort.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.character_sort && message.character_sort.length))
                            message.character_sort = [];
                        message.character_sort.push(reader.uint32());
                        continue;
                    }
                case 10: {
                        if (wireType === 2) {
                            if (!(message.hidden_characters && message.hidden_characters.length))
                                message.hidden_characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hidden_characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hidden_characters && message.hidden_characters.length))
                            message.hidden_characters = [];
                        message.hidden_characters.push(reader.uint32());
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.other_character_sort && message.other_character_sort.length))
                                message.other_character_sort = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.other_character_sort.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.other_character_sort && message.other_character_sort.length))
                            message.other_character_sort = [];
                        message.other_character_sort.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCharacterInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCharacterInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.characters != null && message.hasOwnProperty("characters")) {
                if (!Array.isArray(message.characters))
                    return "characters: array expected";
                for (let i = 0; i < message.characters.length; ++i) {
                    let error = $root.lq.Character.verify(message.characters[i], _depth + 1);
                    if (error)
                        return "characters." + error;
                }
            }
            if (message.skins != null && message.hasOwnProperty("skins")) {
                if (!Array.isArray(message.skins))
                    return "skins: array expected";
                for (let i = 0; i < message.skins.length; ++i)
                    if (!$util.isInteger(message.skins[i]))
                        return "skins: integer[] expected";
            }
            if (message.main_character_id != null && message.hasOwnProperty("main_character_id"))
                if (!$util.isInteger(message.main_character_id))
                    return "main_character_id: integer expected";
            if (message.send_gift_count != null && message.hasOwnProperty("send_gift_count"))
                if (!$util.isInteger(message.send_gift_count))
                    return "send_gift_count: integer expected";
            if (message.send_gift_limit != null && message.hasOwnProperty("send_gift_limit"))
                if (!$util.isInteger(message.send_gift_limit))
                    return "send_gift_limit: integer expected";
            if (message.finished_endings != null && message.hasOwnProperty("finished_endings")) {
                if (!Array.isArray(message.finished_endings))
                    return "finished_endings: array expected";
                for (let i = 0; i < message.finished_endings.length; ++i)
                    if (!$util.isInteger(message.finished_endings[i]))
                        return "finished_endings: integer[] expected";
            }
            if (message.rewarded_endings != null && message.hasOwnProperty("rewarded_endings")) {
                if (!Array.isArray(message.rewarded_endings))
                    return "rewarded_endings: array expected";
                for (let i = 0; i < message.rewarded_endings.length; ++i)
                    if (!$util.isInteger(message.rewarded_endings[i]))
                        return "rewarded_endings: integer[] expected";
            }
            if (message.character_sort != null && message.hasOwnProperty("character_sort")) {
                if (!Array.isArray(message.character_sort))
                    return "character_sort: array expected";
                for (let i = 0; i < message.character_sort.length; ++i)
                    if (!$util.isInteger(message.character_sort[i]))
                        return "character_sort: integer[] expected";
            }
            if (message.hidden_characters != null && message.hasOwnProperty("hidden_characters")) {
                if (!Array.isArray(message.hidden_characters))
                    return "hidden_characters: array expected";
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    if (!$util.isInteger(message.hidden_characters[i]))
                        return "hidden_characters: integer[] expected";
            }
            if (message.other_character_sort != null && message.hasOwnProperty("other_character_sort")) {
                if (!Array.isArray(message.other_character_sort))
                    return "other_character_sort: array expected";
                for (let i = 0; i < message.other_character_sort.length; ++i)
                    if (!$util.isInteger(message.other_character_sort[i]))
                        return "other_character_sort: integer[] expected";
            }
            return null;
        };

        ResCharacterInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCharacterInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCharacterInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCharacterInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.characters) {
                if (!Array.isArray(object.characters))
                    throw TypeError(".lq.ResCharacterInfo.characters: array expected");
                message.characters = Array(object.characters.length);
                for (let i = 0; i < object.characters.length; ++i) {
                    if (typeof object.characters[i] !== "object")
                        throw TypeError(".lq.ResCharacterInfo.characters: object expected");
                    message.characters[i] = $root.lq.Character.fromObject(object.characters[i], _depth + 1);
                }
            }
            if (object.skins) {
                if (!Array.isArray(object.skins))
                    throw TypeError(".lq.ResCharacterInfo.skins: array expected");
                message.skins = Array(object.skins.length);
                for (let i = 0; i < object.skins.length; ++i)
                    message.skins[i] = object.skins[i] >>> 0;
            }
            if (object.main_character_id != null)
                if (Number(object.main_character_id) !== 0)
                    message.main_character_id = object.main_character_id >>> 0;
            if (object.send_gift_count != null)
                if (Number(object.send_gift_count) !== 0)
                    message.send_gift_count = object.send_gift_count >>> 0;
            if (object.send_gift_limit != null)
                if (Number(object.send_gift_limit) !== 0)
                    message.send_gift_limit = object.send_gift_limit >>> 0;
            if (object.finished_endings) {
                if (!Array.isArray(object.finished_endings))
                    throw TypeError(".lq.ResCharacterInfo.finished_endings: array expected");
                message.finished_endings = Array(object.finished_endings.length);
                for (let i = 0; i < object.finished_endings.length; ++i)
                    message.finished_endings[i] = object.finished_endings[i] >>> 0;
            }
            if (object.rewarded_endings) {
                if (!Array.isArray(object.rewarded_endings))
                    throw TypeError(".lq.ResCharacterInfo.rewarded_endings: array expected");
                message.rewarded_endings = Array(object.rewarded_endings.length);
                for (let i = 0; i < object.rewarded_endings.length; ++i)
                    message.rewarded_endings[i] = object.rewarded_endings[i] >>> 0;
            }
            if (object.character_sort) {
                if (!Array.isArray(object.character_sort))
                    throw TypeError(".lq.ResCharacterInfo.character_sort: array expected");
                message.character_sort = Array(object.character_sort.length);
                for (let i = 0; i < object.character_sort.length; ++i)
                    message.character_sort[i] = object.character_sort[i] >>> 0;
            }
            if (object.hidden_characters) {
                if (!Array.isArray(object.hidden_characters))
                    throw TypeError(".lq.ResCharacterInfo.hidden_characters: array expected");
                message.hidden_characters = Array(object.hidden_characters.length);
                for (let i = 0; i < object.hidden_characters.length; ++i)
                    message.hidden_characters[i] = object.hidden_characters[i] >>> 0;
            }
            if (object.other_character_sort) {
                if (!Array.isArray(object.other_character_sort))
                    throw TypeError(".lq.ResCharacterInfo.other_character_sort: array expected");
                message.other_character_sort = Array(object.other_character_sort.length);
                for (let i = 0; i < object.other_character_sort.length; ++i)
                    message.other_character_sort[i] = object.other_character_sort[i] >>> 0;
            }
            return message;
        };

        ResCharacterInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.characters = [];
                object.skins = [];
                object.finished_endings = [];
                object.rewarded_endings = [];
                object.character_sort = [];
                object.hidden_characters = [];
                object.other_character_sort = [];
            }
            if (options.defaults) {
                object.error = null;
                object.main_character_id = 0;
                object.send_gift_count = 0;
                object.send_gift_limit = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.characters && message.characters.length) {
                object.characters = Array(message.characters.length);
                for (let j = 0; j < message.characters.length; ++j)
                    object.characters[j] = $root.lq.Character.toObject(message.characters[j], options);
            }
            if (message.skins && message.skins.length) {
                object.skins = Array(message.skins.length);
                for (let j = 0; j < message.skins.length; ++j)
                    object.skins[j] = message.skins[j];
            }
            if (message.main_character_id != null && message.hasOwnProperty("main_character_id"))
                object.main_character_id = message.main_character_id;
            if (message.send_gift_count != null && message.hasOwnProperty("send_gift_count"))
                object.send_gift_count = message.send_gift_count;
            if (message.send_gift_limit != null && message.hasOwnProperty("send_gift_limit"))
                object.send_gift_limit = message.send_gift_limit;
            if (message.finished_endings && message.finished_endings.length) {
                object.finished_endings = Array(message.finished_endings.length);
                for (let j = 0; j < message.finished_endings.length; ++j)
                    object.finished_endings[j] = message.finished_endings[j];
            }
            if (message.rewarded_endings && message.rewarded_endings.length) {
                object.rewarded_endings = Array(message.rewarded_endings.length);
                for (let j = 0; j < message.rewarded_endings.length; ++j)
                    object.rewarded_endings[j] = message.rewarded_endings[j];
            }
            if (message.character_sort && message.character_sort.length) {
                object.character_sort = Array(message.character_sort.length);
                for (let j = 0; j < message.character_sort.length; ++j)
                    object.character_sort[j] = message.character_sort[j];
            }
            if (message.hidden_characters && message.hidden_characters.length) {
                object.hidden_characters = Array(message.hidden_characters.length);
                for (let j = 0; j < message.hidden_characters.length; ++j)
                    object.hidden_characters[j] = message.hidden_characters[j];
            }
            if (message.other_character_sort && message.other_character_sort.length) {
                object.other_character_sort = Array(message.other_character_sort.length);
                for (let j = 0; j < message.other_character_sort.length; ++j)
                    object.other_character_sort[j] = message.other_character_sort[j];
            }
            return object;
        };

        ResCharacterInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCharacterInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCharacterInfo";
        };

        return ResCharacterInfo;
    })();

    lq.ReqUpdateCharacterSort = (function() {

        function ReqUpdateCharacterSort(properties) {
            this.sort = [];
            this.other_sort = [];
            this.hidden_characters = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateCharacterSort.prototype.sort = $util.emptyArray;
        ReqUpdateCharacterSort.prototype.other_sort = $util.emptyArray;
        ReqUpdateCharacterSort.prototype.hidden_characters = $util.emptyArray;

        ReqUpdateCharacterSort.create = function create(properties) {
            return new ReqUpdateCharacterSort(properties);
        };

        ReqUpdateCharacterSort.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sort != null && message.sort.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.sort.length; ++i)
                    writer.uint32(message.sort[i]);
                writer.ldelim();
            }
            if (message.other_sort != null && message.other_sort.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.other_sort.length; ++i)
                    writer.uint32(message.other_sort[i]);
                writer.ldelim();
            }
            if (message.hidden_characters != null && message.hidden_characters.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    writer.uint32(message.hidden_characters[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateCharacterSort.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateCharacterSort.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateCharacterSort();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.sort && message.sort.length))
                                message.sort = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.sort.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.sort && message.sort.length))
                            message.sort = [];
                        message.sort.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.other_sort && message.other_sort.length))
                                message.other_sort = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.other_sort.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.other_sort && message.other_sort.length))
                            message.other_sort = [];
                        message.other_sort.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.hidden_characters && message.hidden_characters.length))
                                message.hidden_characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hidden_characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hidden_characters && message.hidden_characters.length))
                            message.hidden_characters = [];
                        message.hidden_characters.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateCharacterSort.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateCharacterSort.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sort != null && message.hasOwnProperty("sort")) {
                if (!Array.isArray(message.sort))
                    return "sort: array expected";
                for (let i = 0; i < message.sort.length; ++i)
                    if (!$util.isInteger(message.sort[i]))
                        return "sort: integer[] expected";
            }
            if (message.other_sort != null && message.hasOwnProperty("other_sort")) {
                if (!Array.isArray(message.other_sort))
                    return "other_sort: array expected";
                for (let i = 0; i < message.other_sort.length; ++i)
                    if (!$util.isInteger(message.other_sort[i]))
                        return "other_sort: integer[] expected";
            }
            if (message.hidden_characters != null && message.hasOwnProperty("hidden_characters")) {
                if (!Array.isArray(message.hidden_characters))
                    return "hidden_characters: array expected";
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    if (!$util.isInteger(message.hidden_characters[i]))
                        return "hidden_characters: integer[] expected";
            }
            return null;
        };

        ReqUpdateCharacterSort.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateCharacterSort)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateCharacterSort();
            if (object.sort) {
                if (!Array.isArray(object.sort))
                    throw TypeError(".lq.ReqUpdateCharacterSort.sort: array expected");
                message.sort = Array(object.sort.length);
                for (let i = 0; i < object.sort.length; ++i)
                    message.sort[i] = object.sort[i] >>> 0;
            }
            if (object.other_sort) {
                if (!Array.isArray(object.other_sort))
                    throw TypeError(".lq.ReqUpdateCharacterSort.other_sort: array expected");
                message.other_sort = Array(object.other_sort.length);
                for (let i = 0; i < object.other_sort.length; ++i)
                    message.other_sort[i] = object.other_sort[i] >>> 0;
            }
            if (object.hidden_characters) {
                if (!Array.isArray(object.hidden_characters))
                    throw TypeError(".lq.ReqUpdateCharacterSort.hidden_characters: array expected");
                message.hidden_characters = Array(object.hidden_characters.length);
                for (let i = 0; i < object.hidden_characters.length; ++i)
                    message.hidden_characters[i] = object.hidden_characters[i] >>> 0;
            }
            return message;
        };

        ReqUpdateCharacterSort.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.sort = [];
                object.other_sort = [];
                object.hidden_characters = [];
            }
            if (message.sort && message.sort.length) {
                object.sort = Array(message.sort.length);
                for (let j = 0; j < message.sort.length; ++j)
                    object.sort[j] = message.sort[j];
            }
            if (message.other_sort && message.other_sort.length) {
                object.other_sort = Array(message.other_sort.length);
                for (let j = 0; j < message.other_sort.length; ++j)
                    object.other_sort[j] = message.other_sort[j];
            }
            if (message.hidden_characters && message.hidden_characters.length) {
                object.hidden_characters = Array(message.hidden_characters.length);
                for (let j = 0; j < message.hidden_characters.length; ++j)
                    object.hidden_characters[j] = message.hidden_characters[j];
            }
            return object;
        };

        ReqUpdateCharacterSort.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateCharacterSort.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateCharacterSort";
        };

        return ReqUpdateCharacterSort;
    })();

    lq.ReqChangeMainCharacter = (function() {

        function ReqChangeMainCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeMainCharacter.prototype.character_id = 0;

        ReqChangeMainCharacter.create = function create(properties) {
            return new ReqChangeMainCharacter(properties);
        };

        ReqChangeMainCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeMainCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeMainCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeMainCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeMainCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeMainCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            return null;
        };

        ReqChangeMainCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeMainCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeMainCharacter();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            return message;
        };

        ReqChangeMainCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.character_id = 0;
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            return object;
        };

        ReqChangeMainCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeMainCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeMainCharacter";
        };

        return ReqChangeMainCharacter;
    })();

    lq.ReqChangeCharacterSkin = (function() {

        function ReqChangeCharacterSkin(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeCharacterSkin.prototype.character_id = 0;
        ReqChangeCharacterSkin.prototype.skin = 0;

        ReqChangeCharacterSkin.create = function create(properties) {
            return new ReqChangeCharacterSkin(properties);
        };

        ReqChangeCharacterSkin.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.skin != null && Object.hasOwnProperty.call(message, "skin"))
                writer.uint32(16).uint32(message.skin);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeCharacterSkin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeCharacterSkin.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeCharacterSkin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.skin = value;
                        else
                            delete message.skin;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeCharacterSkin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeCharacterSkin.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.skin != null && message.hasOwnProperty("skin"))
                if (!$util.isInteger(message.skin))
                    return "skin: integer expected";
            return null;
        };

        ReqChangeCharacterSkin.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeCharacterSkin)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeCharacterSkin();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.skin != null)
                if (Number(object.skin) !== 0)
                    message.skin = object.skin >>> 0;
            return message;
        };

        ReqChangeCharacterSkin.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.character_id = 0;
                object.skin = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.skin != null && message.hasOwnProperty("skin"))
                object.skin = message.skin;
            return object;
        };

        ReqChangeCharacterSkin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeCharacterSkin.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeCharacterSkin";
        };

        return ReqChangeCharacterSkin;
    })();

    lq.ReqChangeCharacterView = (function() {

        function ReqChangeCharacterView(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeCharacterView.prototype.character_id = 0;
        ReqChangeCharacterView.prototype.slot = 0;
        ReqChangeCharacterView.prototype.item_id = 0;

        ReqChangeCharacterView.create = function create(properties) {
            return new ReqChangeCharacterView(properties);
        };

        ReqChangeCharacterView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.slot != null && Object.hasOwnProperty.call(message, "slot"))
                writer.uint32(16).uint32(message.slot);
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(24).uint32(message.item_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeCharacterView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeCharacterView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeCharacterView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.slot = value;
                        else
                            delete message.slot;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeCharacterView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeCharacterView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.slot != null && message.hasOwnProperty("slot"))
                if (!$util.isInteger(message.slot))
                    return "slot: integer expected";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            return null;
        };

        ReqChangeCharacterView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeCharacterView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeCharacterView();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.slot != null)
                if (Number(object.slot) !== 0)
                    message.slot = object.slot >>> 0;
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            return message;
        };

        ReqChangeCharacterView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.character_id = 0;
                object.slot = 0;
                object.item_id = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.slot != null && message.hasOwnProperty("slot"))
                object.slot = message.slot;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            return object;
        };

        ReqChangeCharacterView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeCharacterView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeCharacterView";
        };

        return ReqChangeCharacterView;
    })();

    lq.ReqSetHiddenCharacter = (function() {

        function ReqSetHiddenCharacter(properties) {
            this.chara_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetHiddenCharacter.prototype.chara_list = $util.emptyArray;

        ReqSetHiddenCharacter.create = function create(properties) {
            return new ReqSetHiddenCharacter(properties);
        };

        ReqSetHiddenCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chara_list != null && message.chara_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.chara_list.length; ++i)
                    writer.uint32(message.chara_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetHiddenCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetHiddenCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetHiddenCharacter();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.chara_list && message.chara_list.length))
                                message.chara_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.chara_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.chara_list && message.chara_list.length))
                            message.chara_list = [];
                        message.chara_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetHiddenCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetHiddenCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chara_list != null && message.hasOwnProperty("chara_list")) {
                if (!Array.isArray(message.chara_list))
                    return "chara_list: array expected";
                for (let i = 0; i < message.chara_list.length; ++i)
                    if (!$util.isInteger(message.chara_list[i]))
                        return "chara_list: integer[] expected";
            }
            return null;
        };

        ReqSetHiddenCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetHiddenCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetHiddenCharacter();
            if (object.chara_list) {
                if (!Array.isArray(object.chara_list))
                    throw TypeError(".lq.ReqSetHiddenCharacter.chara_list: array expected");
                message.chara_list = Array(object.chara_list.length);
                for (let i = 0; i < object.chara_list.length; ++i)
                    message.chara_list[i] = object.chara_list[i] >>> 0;
            }
            return message;
        };

        ReqSetHiddenCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.chara_list = [];
            if (message.chara_list && message.chara_list.length) {
                object.chara_list = Array(message.chara_list.length);
                for (let j = 0; j < message.chara_list.length; ++j)
                    object.chara_list[j] = message.chara_list[j];
            }
            return object;
        };

        ReqSetHiddenCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetHiddenCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetHiddenCharacter";
        };

        return ReqSetHiddenCharacter;
    })();

    lq.ResSetHiddenCharacter = (function() {

        function ResSetHiddenCharacter(properties) {
            this.hidden_characters = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSetHiddenCharacter.prototype.error = null;
        ResSetHiddenCharacter.prototype.hidden_characters = $util.emptyArray;

        ResSetHiddenCharacter.create = function create(properties) {
            return new ResSetHiddenCharacter(properties);
        };

        ResSetHiddenCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.hidden_characters != null && message.hidden_characters.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    writer.uint32(message.hidden_characters[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSetHiddenCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSetHiddenCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSetHiddenCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.hidden_characters && message.hidden_characters.length))
                                message.hidden_characters = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hidden_characters.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hidden_characters && message.hidden_characters.length))
                            message.hidden_characters = [];
                        message.hidden_characters.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSetHiddenCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSetHiddenCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.hidden_characters != null && message.hasOwnProperty("hidden_characters")) {
                if (!Array.isArray(message.hidden_characters))
                    return "hidden_characters: array expected";
                for (let i = 0; i < message.hidden_characters.length; ++i)
                    if (!$util.isInteger(message.hidden_characters[i]))
                        return "hidden_characters: integer[] expected";
            }
            return null;
        };

        ResSetHiddenCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSetHiddenCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSetHiddenCharacter();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSetHiddenCharacter.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.hidden_characters) {
                if (!Array.isArray(object.hidden_characters))
                    throw TypeError(".lq.ResSetHiddenCharacter.hidden_characters: array expected");
                message.hidden_characters = Array(object.hidden_characters.length);
                for (let i = 0; i < object.hidden_characters.length; ++i)
                    message.hidden_characters[i] = object.hidden_characters[i] >>> 0;
            }
            return message;
        };

        ResSetHiddenCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.hidden_characters = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.hidden_characters && message.hidden_characters.length) {
                object.hidden_characters = Array(message.hidden_characters.length);
                for (let j = 0; j < message.hidden_characters.length; ++j)
                    object.hidden_characters[j] = message.hidden_characters[j];
            }
            return object;
        };

        ResSetHiddenCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSetHiddenCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSetHiddenCharacter";
        };

        return ResSetHiddenCharacter;
    })();

    lq.ReqSendGiftToCharacter = (function() {

        function ReqSendGiftToCharacter(properties) {
            this.gifts = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSendGiftToCharacter.prototype.character_id = 0;
        ReqSendGiftToCharacter.prototype.gifts = $util.emptyArray;

        ReqSendGiftToCharacter.create = function create(properties) {
            return new ReqSendGiftToCharacter(properties);
        };

        ReqSendGiftToCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.gifts != null && message.gifts.length)
                for (let i = 0; i < message.gifts.length; ++i)
                    $root.lq.ReqSendGiftToCharacter.Gift.encode(message.gifts[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSendGiftToCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSendGiftToCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSendGiftToCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gifts && message.gifts.length))
                            message.gifts = [];
                        message.gifts.push($root.lq.ReqSendGiftToCharacter.Gift.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSendGiftToCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSendGiftToCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.gifts != null && message.hasOwnProperty("gifts")) {
                if (!Array.isArray(message.gifts))
                    return "gifts: array expected";
                for (let i = 0; i < message.gifts.length; ++i) {
                    let error = $root.lq.ReqSendGiftToCharacter.Gift.verify(message.gifts[i], _depth + 1);
                    if (error)
                        return "gifts." + error;
                }
            }
            return null;
        };

        ReqSendGiftToCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSendGiftToCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSendGiftToCharacter();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.gifts) {
                if (!Array.isArray(object.gifts))
                    throw TypeError(".lq.ReqSendGiftToCharacter.gifts: array expected");
                message.gifts = Array(object.gifts.length);
                for (let i = 0; i < object.gifts.length; ++i) {
                    if (typeof object.gifts[i] !== "object")
                        throw TypeError(".lq.ReqSendGiftToCharacter.gifts: object expected");
                    message.gifts[i] = $root.lq.ReqSendGiftToCharacter.Gift.fromObject(object.gifts[i], _depth + 1);
                }
            }
            return message;
        };

        ReqSendGiftToCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gifts = [];
            if (options.defaults)
                object.character_id = 0;
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.gifts && message.gifts.length) {
                object.gifts = Array(message.gifts.length);
                for (let j = 0; j < message.gifts.length; ++j)
                    object.gifts[j] = $root.lq.ReqSendGiftToCharacter.Gift.toObject(message.gifts[j], options);
            }
            return object;
        };

        ReqSendGiftToCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSendGiftToCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSendGiftToCharacter";
        };

        ReqSendGiftToCharacter.Gift = (function() {

            function Gift(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Gift.prototype.item_id = 0;
            Gift.prototype.count = 0;

            Gift.create = function create(properties) {
                return new Gift(properties);
            };

            Gift.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(8).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Gift.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Gift.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSendGiftToCharacter.Gift(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Gift.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Gift.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            Gift.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqSendGiftToCharacter.Gift)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqSendGiftToCharacter.Gift();
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            Gift.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.item_id = 0;
                    object.count = 0;
                }
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            Gift.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Gift.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqSendGiftToCharacter.Gift";
            };

            return Gift;
        })();

        return ReqSendGiftToCharacter;
    })();

    lq.ResSendGiftToCharacter = (function() {

        function ResSendGiftToCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSendGiftToCharacter.prototype.error = null;
        ResSendGiftToCharacter.prototype.level = 0;
        ResSendGiftToCharacter.prototype.exp = 0;

        ResSendGiftToCharacter.create = function create(properties) {
            return new ResSendGiftToCharacter(properties);
        };

        ResSendGiftToCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                writer.uint32(24).uint32(message.exp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSendGiftToCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSendGiftToCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSendGiftToCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSendGiftToCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSendGiftToCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (!$util.isInteger(message.exp))
                    return "exp: integer expected";
            return null;
        };

        ResSendGiftToCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSendGiftToCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSendGiftToCharacter();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSendGiftToCharacter.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.exp != null)
                if (Number(object.exp) !== 0)
                    message.exp = object.exp >>> 0;
            return message;
        };

        ResSendGiftToCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.level = 0;
                object.exp = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.exp != null && message.hasOwnProperty("exp"))
                object.exp = message.exp;
            return object;
        };

        ResSendGiftToCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSendGiftToCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSendGiftToCharacter";
        };

        return ResSendGiftToCharacter;
    })();

    lq.ReqSellItem = (function() {

        function ReqSellItem(properties) {
            this.sells = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSellItem.prototype.sells = $util.emptyArray;

        ReqSellItem.create = function create(properties) {
            return new ReqSellItem(properties);
        };

        ReqSellItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.sells != null && message.sells.length)
                for (let i = 0; i < message.sells.length; ++i)
                    $root.lq.ReqSellItem.Item.encode(message.sells[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSellItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSellItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSellItem();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sells && message.sells.length))
                            message.sells = [];
                        message.sells.push($root.lq.ReqSellItem.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSellItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSellItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sells != null && message.hasOwnProperty("sells")) {
                if (!Array.isArray(message.sells))
                    return "sells: array expected";
                for (let i = 0; i < message.sells.length; ++i) {
                    let error = $root.lq.ReqSellItem.Item.verify(message.sells[i], _depth + 1);
                    if (error)
                        return "sells." + error;
                }
            }
            return null;
        };

        ReqSellItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSellItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSellItem();
            if (object.sells) {
                if (!Array.isArray(object.sells))
                    throw TypeError(".lq.ReqSellItem.sells: array expected");
                message.sells = Array(object.sells.length);
                for (let i = 0; i < object.sells.length; ++i) {
                    if (typeof object.sells[i] !== "object")
                        throw TypeError(".lq.ReqSellItem.sells: object expected");
                    message.sells[i] = $root.lq.ReqSellItem.Item.fromObject(object.sells[i], _depth + 1);
                }
            }
            return message;
        };

        ReqSellItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.sells = [];
            if (message.sells && message.sells.length) {
                object.sells = Array(message.sells.length);
                for (let j = 0; j < message.sells.length; ++j)
                    object.sells[j] = $root.lq.ReqSellItem.Item.toObject(message.sells[j], options);
            }
            return object;
        };

        ReqSellItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSellItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSellItem";
        };

        ReqSellItem.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.item_id = 0;
            Item.prototype.count = 0;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(8).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSellItem.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqSellItem.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqSellItem.Item();
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.item_id = 0;
                    object.count = 0;
                }
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqSellItem.Item";
            };

            return Item;
        })();

        return ReqSellItem;
    })();

    lq.ResCommonView = (function() {

        function ResCommonView(properties) {
            this.slots = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCommonView.prototype.error = null;
        ResCommonView.prototype.slots = $util.emptyArray;

        ResCommonView.create = function create(properties) {
            return new ResCommonView(properties);
        };

        ResCommonView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.slots != null && message.slots.length)
                for (let i = 0; i < message.slots.length; ++i)
                    $root.lq.ResCommonView.Slot.encode(message.slots[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCommonView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCommonView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCommonView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.slots && message.slots.length))
                            message.slots = [];
                        message.slots.push($root.lq.ResCommonView.Slot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCommonView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCommonView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.slots != null && message.hasOwnProperty("slots")) {
                if (!Array.isArray(message.slots))
                    return "slots: array expected";
                for (let i = 0; i < message.slots.length; ++i) {
                    let error = $root.lq.ResCommonView.Slot.verify(message.slots[i], _depth + 1);
                    if (error)
                        return "slots." + error;
                }
            }
            return null;
        };

        ResCommonView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCommonView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCommonView();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCommonView.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.slots) {
                if (!Array.isArray(object.slots))
                    throw TypeError(".lq.ResCommonView.slots: array expected");
                message.slots = Array(object.slots.length);
                for (let i = 0; i < object.slots.length; ++i) {
                    if (typeof object.slots[i] !== "object")
                        throw TypeError(".lq.ResCommonView.slots: object expected");
                    message.slots[i] = $root.lq.ResCommonView.Slot.fromObject(object.slots[i], _depth + 1);
                }
            }
            return message;
        };

        ResCommonView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.slots = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.slots && message.slots.length) {
                object.slots = Array(message.slots.length);
                for (let j = 0; j < message.slots.length; ++j)
                    object.slots[j] = $root.lq.ResCommonView.Slot.toObject(message.slots[j], options);
            }
            return object;
        };

        ResCommonView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCommonView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCommonView";
        };

        ResCommonView.Slot = (function() {

            function Slot(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Slot.prototype.slot = 0;
            Slot.prototype.value = 0;

            Slot.create = function create(properties) {
                return new Slot(properties);
            };

            Slot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.slot != null && Object.hasOwnProperty.call(message, "slot"))
                    writer.uint32(8).uint32(message.slot);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(16).uint32(message.value);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Slot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Slot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCommonView.Slot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.slot = value;
                            else
                                delete message.slot;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Slot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Slot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.slot != null && message.hasOwnProperty("slot"))
                    if (!$util.isInteger(message.slot))
                        return "slot: integer expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                return null;
            };

            Slot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResCommonView.Slot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResCommonView.Slot();
                if (object.slot != null)
                    if (Number(object.slot) !== 0)
                        message.slot = object.slot >>> 0;
                if (object.value != null)
                    if (Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                return message;
            };

            Slot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.slot = 0;
                    object.value = 0;
                }
                if (message.slot != null && message.hasOwnProperty("slot"))
                    object.slot = message.slot;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                return object;
            };

            Slot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Slot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResCommonView.Slot";
            };

            return Slot;
        })();

        return ResCommonView;
    })();

    lq.ReqChangeCommonView = (function() {

        function ReqChangeCommonView(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChangeCommonView.prototype.slot = 0;
        ReqChangeCommonView.prototype.value = 0;

        ReqChangeCommonView.create = function create(properties) {
            return new ReqChangeCommonView(properties);
        };

        ReqChangeCommonView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.slot != null && Object.hasOwnProperty.call(message, "slot"))
                writer.uint32(8).uint32(message.slot);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(16).uint32(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChangeCommonView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChangeCommonView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChangeCommonView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.slot = value;
                        else
                            delete message.slot;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChangeCommonView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChangeCommonView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.slot != null && message.hasOwnProperty("slot"))
                if (!$util.isInteger(message.slot))
                    return "slot: integer expected";
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        ReqChangeCommonView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChangeCommonView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChangeCommonView();
            if (object.slot != null)
                if (Number(object.slot) !== 0)
                    message.slot = object.slot >>> 0;
            if (object.value != null)
                if (Number(object.value) !== 0)
                    message.value = object.value >>> 0;
            return message;
        };

        ReqChangeCommonView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.slot = 0;
                object.value = 0;
            }
            if (message.slot != null && message.hasOwnProperty("slot"))
                object.slot = message.slot;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        ReqChangeCommonView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChangeCommonView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChangeCommonView";
        };

        return ReqChangeCommonView;
    })();

    lq.ReqSaveCommonViews = (function() {

        function ReqSaveCommonViews(properties) {
            this.views = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSaveCommonViews.prototype.views = $util.emptyArray;
        ReqSaveCommonViews.prototype.save_index = 0;
        ReqSaveCommonViews.prototype.is_use = 0;
        ReqSaveCommonViews.prototype.name = "";

        ReqSaveCommonViews.create = function create(properties) {
            return new ReqSaveCommonViews(properties);
        };

        ReqSaveCommonViews.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.views != null && message.views.length)
                for (let i = 0; i < message.views.length; ++i)
                    $root.lq.ViewSlot.encode(message.views[i], writer.uint32(10).fork()).ldelim();
            if (message.save_index != null && Object.hasOwnProperty.call(message, "save_index"))
                writer.uint32(16).uint32(message.save_index);
            if (message.is_use != null && Object.hasOwnProperty.call(message, "is_use"))
                writer.uint32(24).uint32(message.is_use);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(34).string(message.name);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSaveCommonViews.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSaveCommonViews.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSaveCommonViews(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.views && message.views.length))
                            message.views = [];
                        message.views.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.save_index = value;
                        else
                            delete message.save_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.is_use = value;
                        else
                            delete message.is_use;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSaveCommonViews.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSaveCommonViews.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.views != null && message.hasOwnProperty("views")) {
                if (!Array.isArray(message.views))
                    return "views: array expected";
                for (let i = 0; i < message.views.length; ++i) {
                    let error = $root.lq.ViewSlot.verify(message.views[i], _depth + 1);
                    if (error)
                        return "views." + error;
                }
            }
            if (message.save_index != null && message.hasOwnProperty("save_index"))
                if (!$util.isInteger(message.save_index))
                    return "save_index: integer expected";
            if (message.is_use != null && message.hasOwnProperty("is_use"))
                if (!$util.isInteger(message.is_use))
                    return "is_use: integer expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            return null;
        };

        ReqSaveCommonViews.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSaveCommonViews)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSaveCommonViews();
            if (object.views) {
                if (!Array.isArray(object.views))
                    throw TypeError(".lq.ReqSaveCommonViews.views: array expected");
                message.views = Array(object.views.length);
                for (let i = 0; i < object.views.length; ++i) {
                    if (typeof object.views[i] !== "object")
                        throw TypeError(".lq.ReqSaveCommonViews.views: object expected");
                    message.views[i] = $root.lq.ViewSlot.fromObject(object.views[i], _depth + 1);
                }
            }
            if (object.save_index != null)
                if (Number(object.save_index) !== 0)
                    message.save_index = object.save_index >>> 0;
            if (object.is_use != null)
                if (Number(object.is_use) !== 0)
                    message.is_use = object.is_use >>> 0;
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            return message;
        };

        ReqSaveCommonViews.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.views = [];
            if (options.defaults) {
                object.save_index = 0;
                object.is_use = 0;
                object.name = "";
            }
            if (message.views && message.views.length) {
                object.views = Array(message.views.length);
                for (let j = 0; j < message.views.length; ++j)
                    object.views[j] = $root.lq.ViewSlot.toObject(message.views[j], options);
            }
            if (message.save_index != null && message.hasOwnProperty("save_index"))
                object.save_index = message.save_index;
            if (message.is_use != null && message.hasOwnProperty("is_use"))
                object.is_use = message.is_use;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            return object;
        };

        ReqSaveCommonViews.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSaveCommonViews.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSaveCommonViews";
        };

        return ReqSaveCommonViews;
    })();

    lq.ReqCommonViews = (function() {

        function ReqCommonViews(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCommonViews.prototype.index = 0;

        ReqCommonViews.create = function create(properties) {
            return new ReqCommonViews(properties);
        };

        ReqCommonViews.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(8).uint32(message.index);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCommonViews.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCommonViews.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCommonViews(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCommonViews.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCommonViews.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        ReqCommonViews.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCommonViews)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCommonViews();
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            return message;
        };

        ReqCommonViews.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.index = 0;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            return object;
        };

        ReqCommonViews.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCommonViews.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCommonViews";
        };

        return ReqCommonViews;
    })();

    lq.ResCommonViews = (function() {

        function ResCommonViews(properties) {
            this.views = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCommonViews.prototype.error = null;
        ResCommonViews.prototype.views = $util.emptyArray;
        ResCommonViews.prototype.name = "";

        ResCommonViews.create = function create(properties) {
            return new ResCommonViews(properties);
        };

        ResCommonViews.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.views != null && message.views.length)
                for (let i = 0; i < message.views.length; ++i)
                    $root.lq.ViewSlot.encode(message.views[i], writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(26).string(message.name);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCommonViews.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCommonViews.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCommonViews(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.views && message.views.length))
                            message.views = [];
                        message.views.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCommonViews.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCommonViews.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.views != null && message.hasOwnProperty("views")) {
                if (!Array.isArray(message.views))
                    return "views: array expected";
                for (let i = 0; i < message.views.length; ++i) {
                    let error = $root.lq.ViewSlot.verify(message.views[i], _depth + 1);
                    if (error)
                        return "views." + error;
                }
            }
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            return null;
        };

        ResCommonViews.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCommonViews)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCommonViews();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCommonViews.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.views) {
                if (!Array.isArray(object.views))
                    throw TypeError(".lq.ResCommonViews.views: array expected");
                message.views = Array(object.views.length);
                for (let i = 0; i < object.views.length; ++i) {
                    if (typeof object.views[i] !== "object")
                        throw TypeError(".lq.ResCommonViews.views: object expected");
                    message.views[i] = $root.lq.ViewSlot.fromObject(object.views[i], _depth + 1);
                }
            }
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            return message;
        };

        ResCommonViews.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.views = [];
            if (options.defaults) {
                object.error = null;
                object.name = "";
            }
            if (message.views && message.views.length) {
                object.views = Array(message.views.length);
                for (let j = 0; j < message.views.length; ++j)
                    object.views[j] = $root.lq.ViewSlot.toObject(message.views[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            return object;
        };

        ResCommonViews.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCommonViews.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCommonViews";
        };

        return ResCommonViews;
    })();

    lq.ResAllcommonViews = (function() {

        function ResAllcommonViews(properties) {
            this.views = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAllcommonViews.prototype.views = $util.emptyArray;
        ResAllcommonViews.prototype.use = 0;
        ResAllcommonViews.prototype.error = null;

        ResAllcommonViews.create = function create(properties) {
            return new ResAllcommonViews(properties);
        };

        ResAllcommonViews.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.views != null && message.views.length)
                for (let i = 0; i < message.views.length; ++i)
                    $root.lq.ResAllcommonViews.Views.encode(message.views[i], writer.uint32(10).fork()).ldelim();
            if (message.use != null && Object.hasOwnProperty.call(message, "use"))
                writer.uint32(16).uint32(message.use);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAllcommonViews.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAllcommonViews.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAllcommonViews(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.views && message.views.length))
                            message.views = [];
                        message.views.push($root.lq.ResAllcommonViews.Views.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.use = value;
                        else
                            delete message.use;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAllcommonViews.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAllcommonViews.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.views != null && message.hasOwnProperty("views")) {
                if (!Array.isArray(message.views))
                    return "views: array expected";
                for (let i = 0; i < message.views.length; ++i) {
                    let error = $root.lq.ResAllcommonViews.Views.verify(message.views[i], _depth + 1);
                    if (error)
                        return "views." + error;
                }
            }
            if (message.use != null && message.hasOwnProperty("use"))
                if (!$util.isInteger(message.use))
                    return "use: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResAllcommonViews.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAllcommonViews)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAllcommonViews();
            if (object.views) {
                if (!Array.isArray(object.views))
                    throw TypeError(".lq.ResAllcommonViews.views: array expected");
                message.views = Array(object.views.length);
                for (let i = 0; i < object.views.length; ++i) {
                    if (typeof object.views[i] !== "object")
                        throw TypeError(".lq.ResAllcommonViews.views: object expected");
                    message.views[i] = $root.lq.ResAllcommonViews.Views.fromObject(object.views[i], _depth + 1);
                }
            }
            if (object.use != null)
                if (Number(object.use) !== 0)
                    message.use = object.use >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAllcommonViews.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResAllcommonViews.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.views = [];
            if (options.defaults) {
                object.use = 0;
                object.error = null;
            }
            if (message.views && message.views.length) {
                object.views = Array(message.views.length);
                for (let j = 0; j < message.views.length; ++j)
                    object.views[j] = $root.lq.ResAllcommonViews.Views.toObject(message.views[j], options);
            }
            if (message.use != null && message.hasOwnProperty("use"))
                object.use = message.use;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResAllcommonViews.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAllcommonViews.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAllcommonViews";
        };

        ResAllcommonViews.Views = (function() {

            function Views(properties) {
                this.values = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Views.prototype.values = $util.emptyArray;
            Views.prototype.index = 0;
            Views.prototype.name = "";

            Views.create = function create(properties) {
                return new Views(properties);
            };

            Views.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.lq.ViewSlot.encode(message.values[i], writer.uint32(10).fork()).ldelim();
                if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                    writer.uint32(16).uint32(message.index);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(26).string(message.name);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Views.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Views.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAllcommonViews.Views(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.lq.ViewSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.index = value;
                            else
                                delete message.index;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Views.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Views.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.values != null && message.hasOwnProperty("values")) {
                    if (!Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.lq.ViewSlot.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                if (message.index != null && message.hasOwnProperty("index"))
                    if (!$util.isInteger(message.index))
                        return "index: integer expected";
                if (message.name != null && message.hasOwnProperty("name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                return null;
            };

            Views.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAllcommonViews.Views)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAllcommonViews.Views();
                if (object.values) {
                    if (!Array.isArray(object.values))
                        throw TypeError(".lq.ResAllcommonViews.Views.values: array expected");
                    message.values = Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (typeof object.values[i] !== "object")
                            throw TypeError(".lq.ResAllcommonViews.Views.values: object expected");
                        message.values[i] = $root.lq.ViewSlot.fromObject(object.values[i], _depth + 1);
                    }
                }
                if (object.index != null)
                    if (Number(object.index) !== 0)
                        message.index = object.index >>> 0;
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = String(object.name);
                return message;
            };

            Views.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults) {
                    object.index = 0;
                    object.name = "";
                }
                if (message.values && message.values.length) {
                    object.values = Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.lq.ViewSlot.toObject(message.values[j], options);
                }
                if (message.index != null && message.hasOwnProperty("index"))
                    object.index = message.index;
                if (message.name != null && message.hasOwnProperty("name"))
                    object.name = message.name;
                return object;
            };

            Views.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Views.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAllcommonViews.Views";
            };

            return Views;
        })();

        return ResAllcommonViews;
    })();

    lq.ReqUseCommonView = (function() {

        function ReqUseCommonView(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUseCommonView.prototype.index = 0;

        ReqUseCommonView.create = function create(properties) {
            return new ReqUseCommonView(properties);
        };

        ReqUseCommonView.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(24).uint32(message.index);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUseCommonView.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUseCommonView.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUseCommonView(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUseCommonView.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUseCommonView.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            return null;
        };

        ReqUseCommonView.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUseCommonView)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUseCommonView();
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            return message;
        };

        ReqUseCommonView.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.index = 0;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            return object;
        };

        ReqUseCommonView.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUseCommonView.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUseCommonView";
        };

        return ReqUseCommonView;
    })();

    lq.ReqUpgradeCharacter = (function() {

        function ReqUpgradeCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpgradeCharacter.prototype.character_id = 0;

        ReqUpgradeCharacter.create = function create(properties) {
            return new ReqUpgradeCharacter(properties);
        };

        ReqUpgradeCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpgradeCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpgradeCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpgradeCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpgradeCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpgradeCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            return null;
        };

        ReqUpgradeCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpgradeCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpgradeCharacter();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            return message;
        };

        ReqUpgradeCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.character_id = 0;
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            return object;
        };

        ReqUpgradeCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpgradeCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpgradeCharacter";
        };

        return ReqUpgradeCharacter;
    })();

    lq.ResUpgradeCharacter = (function() {

        function ResUpgradeCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResUpgradeCharacter.prototype.error = null;
        ResUpgradeCharacter.prototype.character = null;

        ResUpgradeCharacter.create = function create(properties) {
            return new ResUpgradeCharacter(properties);
        };

        ResUpgradeCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                $root.lq.Character.encode(message.character, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResUpgradeCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResUpgradeCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResUpgradeCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.character = $root.lq.Character.decode(reader, reader.uint32(), undefined, _depth + 1, message.character);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResUpgradeCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResUpgradeCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.character != null && message.hasOwnProperty("character")) {
                let error = $root.lq.Character.verify(message.character, _depth + 1);
                if (error)
                    return "character." + error;
            }
            return null;
        };

        ResUpgradeCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResUpgradeCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResUpgradeCharacter();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResUpgradeCharacter.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.character != null) {
                if (typeof object.character !== "object")
                    throw TypeError(".lq.ResUpgradeCharacter.character: object expected");
                message.character = $root.lq.Character.fromObject(object.character, _depth + 1);
            }
            return message;
        };

        ResUpgradeCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.character = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.character != null && message.hasOwnProperty("character"))
                object.character = $root.lq.Character.toObject(message.character, options);
            return object;
        };

        ResUpgradeCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResUpgradeCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResUpgradeCharacter";
        };

        return ResUpgradeCharacter;
    })();

    lq.ReqFinishedEnding = (function() {

        function ReqFinishedEnding(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFinishedEnding.prototype.character_id = 0;
        ReqFinishedEnding.prototype.story_id = 0;
        ReqFinishedEnding.prototype.ending_id = 0;

        ReqFinishedEnding.create = function create(properties) {
            return new ReqFinishedEnding(properties);
        };

        ReqFinishedEnding.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(24).uint32(message.ending_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFinishedEnding.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFinishedEnding.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFinishedEnding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFinishedEnding.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFinishedEnding.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            return null;
        };

        ReqFinishedEnding.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFinishedEnding)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFinishedEnding();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            return message;
        };

        ReqFinishedEnding.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.character_id = 0;
                object.story_id = 0;
                object.ending_id = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            return object;
        };

        ReqFinishedEnding.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFinishedEnding.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFinishedEnding";
        };

        return ReqFinishedEnding;
    })();

    lq.ReqGMCommand = (function() {

        function ReqGMCommand(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGMCommand.prototype.command = "";

        ReqGMCommand.create = function create(properties) {
            return new ReqGMCommand(properties);
        };

        ReqGMCommand.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                writer.uint32(10).string(message.command);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGMCommand.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGMCommand.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGMCommand(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.command = value;
                        else
                            delete message.command;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGMCommand.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGMCommand.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.command != null && message.hasOwnProperty("command"))
                if (!$util.isString(message.command))
                    return "command: string expected";
            return null;
        };

        ReqGMCommand.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGMCommand)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGMCommand();
            if (object.command != null)
                if (typeof object.command !== "string" || object.command.length)
                    message.command = String(object.command);
            return message;
        };

        ReqGMCommand.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.command = "";
            if (message.command != null && message.hasOwnProperty("command"))
                object.command = message.command;
            return object;
        };

        ReqGMCommand.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGMCommand.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGMCommand";
        };

        return ReqGMCommand;
    })();

    lq.ResShopInfo = (function() {

        function ResShopInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResShopInfo.prototype.error = null;
        ResShopInfo.prototype.shop_info = null;

        ResShopInfo.create = function create(properties) {
            return new ResShopInfo(properties);
        };

        ResShopInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.shop_info != null && Object.hasOwnProperty.call(message, "shop_info"))
                $root.lq.ShopInfo.encode(message.shop_info, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResShopInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResShopInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResShopInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.shop_info = $root.lq.ShopInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResShopInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResShopInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.shop_info != null && message.hasOwnProperty("shop_info")) {
                let error = $root.lq.ShopInfo.verify(message.shop_info, _depth + 1);
                if (error)
                    return "shop_info." + error;
            }
            return null;
        };

        ResShopInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResShopInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResShopInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResShopInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.shop_info != null) {
                if (typeof object.shop_info !== "object")
                    throw TypeError(".lq.ResShopInfo.shop_info: object expected");
                message.shop_info = $root.lq.ShopInfo.fromObject(object.shop_info, _depth + 1);
            }
            return message;
        };

        ResShopInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.shop_info = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.shop_info != null && message.hasOwnProperty("shop_info"))
                object.shop_info = $root.lq.ShopInfo.toObject(message.shop_info, options);
            return object;
        };

        ResShopInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResShopInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResShopInfo";
        };

        return ResShopInfo;
    })();

    lq.ReqBuyFromShop = (function() {

        function ReqBuyFromShop(properties) {
            this.ver_price = [];
            this.ver_goods = [];
            this.package_goods = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyFromShop.prototype.goods_id = 0;
        ReqBuyFromShop.prototype.count = 0;
        ReqBuyFromShop.prototype.ver_price = $util.emptyArray;
        ReqBuyFromShop.prototype.ver_goods = $util.emptyArray;
        ReqBuyFromShop.prototype.package_goods = $util.emptyArray;

        ReqBuyFromShop.create = function create(properties) {
            return new ReqBuyFromShop(properties);
        };

        ReqBuyFromShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.ver_price != null && message.ver_price.length)
                for (let i = 0; i < message.ver_price.length; ++i)
                    $root.lq.ReqBuyFromShop.Item.encode(message.ver_price[i], writer.uint32(26).fork()).ldelim();
            if (message.ver_goods != null && message.ver_goods.length)
                for (let i = 0; i < message.ver_goods.length; ++i)
                    $root.lq.ReqBuyFromShop.Item.encode(message.ver_goods[i], writer.uint32(34).fork()).ldelim();
            if (message.package_goods != null && message.package_goods.length)
                for (let i = 0; i < message.package_goods.length; ++i)
                    $root.lq.ReqBuyFromShop.Item.encode(message.package_goods[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyFromShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyFromShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyFromShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ver_price && message.ver_price.length))
                            message.ver_price = [];
                        message.ver_price.push($root.lq.ReqBuyFromShop.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ver_goods && message.ver_goods.length))
                            message.ver_goods = [];
                        message.ver_goods.push($root.lq.ReqBuyFromShop.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.package_goods && message.package_goods.length))
                            message.package_goods = [];
                        message.package_goods.push($root.lq.ReqBuyFromShop.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyFromShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyFromShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.ver_price != null && message.hasOwnProperty("ver_price")) {
                if (!Array.isArray(message.ver_price))
                    return "ver_price: array expected";
                for (let i = 0; i < message.ver_price.length; ++i) {
                    let error = $root.lq.ReqBuyFromShop.Item.verify(message.ver_price[i], _depth + 1);
                    if (error)
                        return "ver_price." + error;
                }
            }
            if (message.ver_goods != null && message.hasOwnProperty("ver_goods")) {
                if (!Array.isArray(message.ver_goods))
                    return "ver_goods: array expected";
                for (let i = 0; i < message.ver_goods.length; ++i) {
                    let error = $root.lq.ReqBuyFromShop.Item.verify(message.ver_goods[i], _depth + 1);
                    if (error)
                        return "ver_goods." + error;
                }
            }
            if (message.package_goods != null && message.hasOwnProperty("package_goods")) {
                if (!Array.isArray(message.package_goods))
                    return "package_goods: array expected";
                for (let i = 0; i < message.package_goods.length; ++i) {
                    let error = $root.lq.ReqBuyFromShop.Item.verify(message.package_goods[i], _depth + 1);
                    if (error)
                        return "package_goods." + error;
                }
            }
            return null;
        };

        ReqBuyFromShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyFromShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyFromShop();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.ver_price) {
                if (!Array.isArray(object.ver_price))
                    throw TypeError(".lq.ReqBuyFromShop.ver_price: array expected");
                message.ver_price = Array(object.ver_price.length);
                for (let i = 0; i < object.ver_price.length; ++i) {
                    if (typeof object.ver_price[i] !== "object")
                        throw TypeError(".lq.ReqBuyFromShop.ver_price: object expected");
                    message.ver_price[i] = $root.lq.ReqBuyFromShop.Item.fromObject(object.ver_price[i], _depth + 1);
                }
            }
            if (object.ver_goods) {
                if (!Array.isArray(object.ver_goods))
                    throw TypeError(".lq.ReqBuyFromShop.ver_goods: array expected");
                message.ver_goods = Array(object.ver_goods.length);
                for (let i = 0; i < object.ver_goods.length; ++i) {
                    if (typeof object.ver_goods[i] !== "object")
                        throw TypeError(".lq.ReqBuyFromShop.ver_goods: object expected");
                    message.ver_goods[i] = $root.lq.ReqBuyFromShop.Item.fromObject(object.ver_goods[i], _depth + 1);
                }
            }
            if (object.package_goods) {
                if (!Array.isArray(object.package_goods))
                    throw TypeError(".lq.ReqBuyFromShop.package_goods: array expected");
                message.package_goods = Array(object.package_goods.length);
                for (let i = 0; i < object.package_goods.length; ++i) {
                    if (typeof object.package_goods[i] !== "object")
                        throw TypeError(".lq.ReqBuyFromShop.package_goods: object expected");
                    message.package_goods[i] = $root.lq.ReqBuyFromShop.Item.fromObject(object.package_goods[i], _depth + 1);
                }
            }
            return message;
        };

        ReqBuyFromShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ver_price = [];
                object.ver_goods = [];
                object.package_goods = [];
            }
            if (options.defaults) {
                object.goods_id = 0;
                object.count = 0;
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.ver_price && message.ver_price.length) {
                object.ver_price = Array(message.ver_price.length);
                for (let j = 0; j < message.ver_price.length; ++j)
                    object.ver_price[j] = $root.lq.ReqBuyFromShop.Item.toObject(message.ver_price[j], options);
            }
            if (message.ver_goods && message.ver_goods.length) {
                object.ver_goods = Array(message.ver_goods.length);
                for (let j = 0; j < message.ver_goods.length; ++j)
                    object.ver_goods[j] = $root.lq.ReqBuyFromShop.Item.toObject(message.ver_goods[j], options);
            }
            if (message.package_goods && message.package_goods.length) {
                object.package_goods = Array(message.package_goods.length);
                for (let j = 0; j < message.package_goods.length; ++j)
                    object.package_goods[j] = $root.lq.ReqBuyFromShop.Item.toObject(message.package_goods[j], options);
            }
            return object;
        };

        ReqBuyFromShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyFromShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyFromShop";
        };

        ReqBuyFromShop.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.id = 0;
            Item.prototype.count = 0;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyFromShop.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqBuyFromShop.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqBuyFromShop.Item();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqBuyFromShop.Item";
            };

            return Item;
        })();

        return ReqBuyFromShop;
    })();

    lq.ResBuyFromShop = (function() {

        function ResBuyFromShop(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResBuyFromShop.prototype.error = null;
        ResBuyFromShop.prototype.rewards = $util.emptyArray;

        ResBuyFromShop.create = function create(properties) {
            return new ResBuyFromShop(properties);
        };

        ResBuyFromShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResBuyFromShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResBuyFromShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResBuyFromShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResBuyFromShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResBuyFromShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResBuyFromShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResBuyFromShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResBuyFromShop();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResBuyFromShop.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResBuyFromShop.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResBuyFromShop.rewards: object expected");
                    message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResBuyFromShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResBuyFromShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResBuyFromShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResBuyFromShop";
        };

        return ResBuyFromShop;
    })();

    lq.ReqBuyFromZHP = (function() {

        function ReqBuyFromZHP(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyFromZHP.prototype.goods_id = 0;
        ReqBuyFromZHP.prototype.count = 0;

        ReqBuyFromZHP.create = function create(properties) {
            return new ReqBuyFromZHP(properties);
        };

        ReqBuyFromZHP.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(8).uint32(message.goods_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyFromZHP.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyFromZHP.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyFromZHP(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyFromZHP.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyFromZHP.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqBuyFromZHP.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyFromZHP)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyFromZHP();
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqBuyFromZHP.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods_id = 0;
                object.count = 0;
            }
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqBuyFromZHP.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyFromZHP.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyFromZHP";
        };

        return ReqBuyFromZHP;
    })();

    lq.ReqPayMonthTicket = (function() {

        function ReqPayMonthTicket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqPayMonthTicket.prototype.ticket_id = 0;

        ReqPayMonthTicket.create = function create(properties) {
            return new ReqPayMonthTicket(properties);
        };

        ReqPayMonthTicket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                writer.uint32(8).uint32(message.ticket_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqPayMonthTicket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqPayMonthTicket.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqPayMonthTicket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_id = value;
                        else
                            delete message.ticket_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqPayMonthTicket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqPayMonthTicket.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                if (!$util.isInteger(message.ticket_id))
                    return "ticket_id: integer expected";
            return null;
        };

        ReqPayMonthTicket.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqPayMonthTicket)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqPayMonthTicket();
            if (object.ticket_id != null)
                if (Number(object.ticket_id) !== 0)
                    message.ticket_id = object.ticket_id >>> 0;
            return message;
        };

        ReqPayMonthTicket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.ticket_id = 0;
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                object.ticket_id = message.ticket_id;
            return object;
        };

        ReqPayMonthTicket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqPayMonthTicket.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqPayMonthTicket";
        };

        return ReqPayMonthTicket;
    })();

    lq.ResPayMonthTicket = (function() {

        function ResPayMonthTicket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResPayMonthTicket.prototype.error = null;
        ResPayMonthTicket.prototype.resource_id = 0;
        ResPayMonthTicket.prototype.resource_count = 0;

        ResPayMonthTicket.create = function create(properties) {
            return new ResPayMonthTicket(properties);
        };

        ResPayMonthTicket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                writer.uint32(16).uint32(message.resource_id);
            if (message.resource_count != null && Object.hasOwnProperty.call(message, "resource_count"))
                writer.uint32(24).uint32(message.resource_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResPayMonthTicket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResPayMonthTicket.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResPayMonthTicket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.resource_id = value;
                        else
                            delete message.resource_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.resource_count = value;
                        else
                            delete message.resource_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResPayMonthTicket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResPayMonthTicket.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                if (!$util.isInteger(message.resource_id))
                    return "resource_id: integer expected";
            if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                if (!$util.isInteger(message.resource_count))
                    return "resource_count: integer expected";
            return null;
        };

        ResPayMonthTicket.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResPayMonthTicket)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResPayMonthTicket();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResPayMonthTicket.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.resource_id != null)
                if (Number(object.resource_id) !== 0)
                    message.resource_id = object.resource_id >>> 0;
            if (object.resource_count != null)
                if (Number(object.resource_count) !== 0)
                    message.resource_count = object.resource_count >>> 0;
            return message;
        };

        ResPayMonthTicket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.resource_id = 0;
                object.resource_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                object.resource_id = message.resource_id;
            if (message.resource_count != null && message.hasOwnProperty("resource_count"))
                object.resource_count = message.resource_count;
            return object;
        };

        ResPayMonthTicket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResPayMonthTicket.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResPayMonthTicket";
        };

        return ResPayMonthTicket;
    })();

    lq.ReqReshZHPShop = (function() {

        function ReqReshZHPShop(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReshZHPShop.prototype.free_refresh = 0;
        ReqReshZHPShop.prototype.cost_refresh = 0;

        ReqReshZHPShop.create = function create(properties) {
            return new ReqReshZHPShop(properties);
        };

        ReqReshZHPShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.free_refresh != null && Object.hasOwnProperty.call(message, "free_refresh"))
                writer.uint32(8).uint32(message.free_refresh);
            if (message.cost_refresh != null && Object.hasOwnProperty.call(message, "cost_refresh"))
                writer.uint32(16).uint32(message.cost_refresh);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReshZHPShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReshZHPShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReshZHPShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.free_refresh = value;
                        else
                            delete message.free_refresh;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cost_refresh = value;
                        else
                            delete message.cost_refresh;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReshZHPShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReshZHPShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.free_refresh != null && message.hasOwnProperty("free_refresh"))
                if (!$util.isInteger(message.free_refresh))
                    return "free_refresh: integer expected";
            if (message.cost_refresh != null && message.hasOwnProperty("cost_refresh"))
                if (!$util.isInteger(message.cost_refresh))
                    return "cost_refresh: integer expected";
            return null;
        };

        ReqReshZHPShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReshZHPShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReshZHPShop();
            if (object.free_refresh != null)
                if (Number(object.free_refresh) !== 0)
                    message.free_refresh = object.free_refresh >>> 0;
            if (object.cost_refresh != null)
                if (Number(object.cost_refresh) !== 0)
                    message.cost_refresh = object.cost_refresh >>> 0;
            return message;
        };

        ReqReshZHPShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.free_refresh = 0;
                object.cost_refresh = 0;
            }
            if (message.free_refresh != null && message.hasOwnProperty("free_refresh"))
                object.free_refresh = message.free_refresh;
            if (message.cost_refresh != null && message.hasOwnProperty("cost_refresh"))
                object.cost_refresh = message.cost_refresh;
            return object;
        };

        ReqReshZHPShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReshZHPShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReshZHPShop";
        };

        return ReqReshZHPShop;
    })();

    lq.ResRefreshZHPShop = (function() {

        function ResRefreshZHPShop(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRefreshZHPShop.prototype.error = null;
        ResRefreshZHPShop.prototype.zhp = null;

        ResRefreshZHPShop.create = function create(properties) {
            return new ResRefreshZHPShop(properties);
        };

        ResRefreshZHPShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.zhp != null && Object.hasOwnProperty.call(message, "zhp"))
                $root.lq.ZHPShop.encode(message.zhp, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRefreshZHPShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRefreshZHPShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRefreshZHPShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.zhp = $root.lq.ZHPShop.decode(reader, reader.uint32(), undefined, _depth + 1, message.zhp);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRefreshZHPShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRefreshZHPShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.zhp != null && message.hasOwnProperty("zhp")) {
                let error = $root.lq.ZHPShop.verify(message.zhp, _depth + 1);
                if (error)
                    return "zhp." + error;
            }
            return null;
        };

        ResRefreshZHPShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRefreshZHPShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRefreshZHPShop();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRefreshZHPShop.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.zhp != null) {
                if (typeof object.zhp !== "object")
                    throw TypeError(".lq.ResRefreshZHPShop.zhp: object expected");
                message.zhp = $root.lq.ZHPShop.fromObject(object.zhp, _depth + 1);
            }
            return message;
        };

        ResRefreshZHPShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.zhp = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.zhp != null && message.hasOwnProperty("zhp"))
                object.zhp = $root.lq.ZHPShop.toObject(message.zhp, options);
            return object;
        };

        ResRefreshZHPShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRefreshZHPShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRefreshZHPShop";
        };

        return ResRefreshZHPShop;
    })();

    lq.ResMonthTicketInfo = (function() {

        function ResMonthTicketInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMonthTicketInfo.prototype.error = null;
        ResMonthTicketInfo.prototype.month_ticket_info = null;

        ResMonthTicketInfo.create = function create(properties) {
            return new ResMonthTicketInfo(properties);
        };

        ResMonthTicketInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.month_ticket_info != null && Object.hasOwnProperty.call(message, "month_ticket_info"))
                $root.lq.MonthTicketInfo.encode(message.month_ticket_info, writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMonthTicketInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMonthTicketInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMonthTicketInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.month_ticket_info = $root.lq.MonthTicketInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_ticket_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMonthTicketInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMonthTicketInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.month_ticket_info != null && message.hasOwnProperty("month_ticket_info")) {
                let error = $root.lq.MonthTicketInfo.verify(message.month_ticket_info, _depth + 1);
                if (error)
                    return "month_ticket_info." + error;
            }
            return null;
        };

        ResMonthTicketInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMonthTicketInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMonthTicketInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMonthTicketInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.month_ticket_info != null) {
                if (typeof object.month_ticket_info !== "object")
                    throw TypeError(".lq.ResMonthTicketInfo.month_ticket_info: object expected");
                message.month_ticket_info = $root.lq.MonthTicketInfo.fromObject(object.month_ticket_info, _depth + 1);
            }
            return message;
        };

        ResMonthTicketInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.month_ticket_info = null;
                object.error = null;
            }
            if (message.month_ticket_info != null && message.hasOwnProperty("month_ticket_info"))
                object.month_ticket_info = $root.lq.MonthTicketInfo.toObject(message.month_ticket_info, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResMonthTicketInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMonthTicketInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMonthTicketInfo";
        };

        return ResMonthTicketInfo;
    })();

    lq.ReqExchangeCurrency = (function() {

        function ReqExchangeCurrency(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqExchangeCurrency.prototype.id = 0;
        ReqExchangeCurrency.prototype.count = 0;

        ReqExchangeCurrency.create = function create(properties) {
            return new ReqExchangeCurrency(properties);
        };

        ReqExchangeCurrency.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqExchangeCurrency.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqExchangeCurrency.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqExchangeCurrency(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqExchangeCurrency.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqExchangeCurrency.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqExchangeCurrency.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqExchangeCurrency)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqExchangeCurrency();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqExchangeCurrency.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.count = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqExchangeCurrency.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqExchangeCurrency.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqExchangeCurrency";
        };

        return ReqExchangeCurrency;
    })();

    lq.ResServerSettings = (function() {

        function ResServerSettings(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResServerSettings.prototype.error = null;
        ResServerSettings.prototype.settings = null;

        ResServerSettings.create = function create(properties) {
            return new ResServerSettings(properties);
        };

        ResServerSettings.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.settings != null && Object.hasOwnProperty.call(message, "settings"))
                $root.lq.ServerSettings.encode(message.settings, writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResServerSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResServerSettings.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResServerSettings(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.settings = $root.lq.ServerSettings.decode(reader, reader.uint32(), undefined, _depth + 1, message.settings);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResServerSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResServerSettings.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.settings != null && message.hasOwnProperty("settings")) {
                let error = $root.lq.ServerSettings.verify(message.settings, _depth + 1);
                if (error)
                    return "settings." + error;
            }
            return null;
        };

        ResServerSettings.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResServerSettings)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResServerSettings();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResServerSettings.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.settings != null) {
                if (typeof object.settings !== "object")
                    throw TypeError(".lq.ResServerSettings.settings: object expected");
                message.settings = $root.lq.ServerSettings.fromObject(object.settings, _depth + 1);
            }
            return message;
        };

        ResServerSettings.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.settings = null;
                object.error = null;
            }
            if (message.settings != null && message.hasOwnProperty("settings"))
                object.settings = $root.lq.ServerSettings.toObject(message.settings, options);
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResServerSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResServerSettings.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResServerSettings";
        };

        return ResServerSettings;
    })();

    lq.ResAccountSettings = (function() {

        function ResAccountSettings(properties) {
            this.settings = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountSettings.prototype.error = null;
        ResAccountSettings.prototype.settings = $util.emptyArray;

        ResAccountSettings.create = function create(properties) {
            return new ResAccountSettings(properties);
        };

        ResAccountSettings.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.settings != null && message.settings.length)
                for (let i = 0; i < message.settings.length; ++i)
                    $root.lq.AccountSetting.encode(message.settings[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountSettings.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountSettings(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.settings && message.settings.length))
                            message.settings = [];
                        message.settings.push($root.lq.AccountSetting.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountSettings.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.settings != null && message.hasOwnProperty("settings")) {
                if (!Array.isArray(message.settings))
                    return "settings: array expected";
                for (let i = 0; i < message.settings.length; ++i) {
                    let error = $root.lq.AccountSetting.verify(message.settings[i], _depth + 1);
                    if (error)
                        return "settings." + error;
                }
            }
            return null;
        };

        ResAccountSettings.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountSettings)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountSettings();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountSettings.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.settings) {
                if (!Array.isArray(object.settings))
                    throw TypeError(".lq.ResAccountSettings.settings: array expected");
                message.settings = Array(object.settings.length);
                for (let i = 0; i < object.settings.length; ++i) {
                    if (typeof object.settings[i] !== "object")
                        throw TypeError(".lq.ResAccountSettings.settings: object expected");
                    message.settings[i] = $root.lq.AccountSetting.fromObject(object.settings[i], _depth + 1);
                }
            }
            return message;
        };

        ResAccountSettings.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.settings = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.settings && message.settings.length) {
                object.settings = Array(message.settings.length);
                for (let j = 0; j < message.settings.length; ++j)
                    object.settings[j] = $root.lq.AccountSetting.toObject(message.settings[j], options);
            }
            return object;
        };

        ResAccountSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountSettings.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountSettings";
        };

        return ResAccountSettings;
    })();

    lq.ReqUpdateAccountSettings = (function() {

        function ReqUpdateAccountSettings(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateAccountSettings.prototype.setting = null;

        ReqUpdateAccountSettings.create = function create(properties) {
            return new ReqUpdateAccountSettings(properties);
        };

        ReqUpdateAccountSettings.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.setting != null && Object.hasOwnProperty.call(message, "setting"))
                $root.lq.AccountSetting.encode(message.setting, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateAccountSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateAccountSettings.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateAccountSettings(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.setting = $root.lq.AccountSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.setting);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateAccountSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateAccountSettings.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.setting != null && message.hasOwnProperty("setting")) {
                let error = $root.lq.AccountSetting.verify(message.setting, _depth + 1);
                if (error)
                    return "setting." + error;
            }
            return null;
        };

        ReqUpdateAccountSettings.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateAccountSettings)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateAccountSettings();
            if (object.setting != null) {
                if (typeof object.setting !== "object")
                    throw TypeError(".lq.ReqUpdateAccountSettings.setting: object expected");
                message.setting = $root.lq.AccountSetting.fromObject(object.setting, _depth + 1);
            }
            return message;
        };

        ReqUpdateAccountSettings.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.setting = null;
            if (message.setting != null && message.hasOwnProperty("setting"))
                object.setting = $root.lq.AccountSetting.toObject(message.setting, options);
            return object;
        };

        ReqUpdateAccountSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateAccountSettings.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateAccountSettings";
        };

        return ReqUpdateAccountSettings;
    })();

    lq.ResModNicknameTime = (function() {

        function ResModNicknameTime(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResModNicknameTime.prototype.error = null;
        ResModNicknameTime.prototype.last_mod_time = 0;

        ResModNicknameTime.create = function create(properties) {
            return new ResModNicknameTime(properties);
        };

        ResModNicknameTime.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.last_mod_time != null && Object.hasOwnProperty.call(message, "last_mod_time"))
                writer.uint32(8).uint32(message.last_mod_time);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResModNicknameTime.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResModNicknameTime.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResModNicknameTime(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_mod_time = value;
                        else
                            delete message.last_mod_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResModNicknameTime.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResModNicknameTime.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.last_mod_time != null && message.hasOwnProperty("last_mod_time"))
                if (!$util.isInteger(message.last_mod_time))
                    return "last_mod_time: integer expected";
            return null;
        };

        ResModNicknameTime.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResModNicknameTime)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResModNicknameTime();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResModNicknameTime.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.last_mod_time != null)
                if (Number(object.last_mod_time) !== 0)
                    message.last_mod_time = object.last_mod_time >>> 0;
            return message;
        };

        ResModNicknameTime.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.last_mod_time = 0;
                object.error = null;
            }
            if (message.last_mod_time != null && message.hasOwnProperty("last_mod_time"))
                object.last_mod_time = message.last_mod_time;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResModNicknameTime.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResModNicknameTime.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResModNicknameTime";
        };

        return ResModNicknameTime;
    })();

    lq.ResMisc = (function() {

        function ResMisc(properties) {
            this.recharged_list = [];
            this.faiths = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMisc.prototype.error = null;
        ResMisc.prototype.recharged_list = $util.emptyArray;
        ResMisc.prototype.faiths = $util.emptyArray;
        ResMisc.prototype.verified_hidden = 0;
        ResMisc.prototype.verified_value = 0;
        ResMisc.prototype.disable_room_random_bot_char = 0;

        ResMisc.create = function create(properties) {
            return new ResMisc(properties);
        };

        ResMisc.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.recharged_list != null && message.recharged_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.recharged_list.length; ++i)
                    writer.uint32(message.recharged_list[i]);
                writer.ldelim();
            }
            if (message.faiths != null && message.faiths.length)
                for (let i = 0; i < message.faiths.length; ++i)
                    $root.lq.ResMisc.MiscFaithData.encode(message.faiths[i], writer.uint32(26).fork()).ldelim();
            if (message.verified_hidden != null && Object.hasOwnProperty.call(message, "verified_hidden"))
                writer.uint32(32).uint32(message.verified_hidden);
            if (message.verified_value != null && Object.hasOwnProperty.call(message, "verified_value"))
                writer.uint32(40).uint32(message.verified_value);
            if (message.disable_room_random_bot_char != null && Object.hasOwnProperty.call(message, "disable_room_random_bot_char"))
                writer.uint32(48).uint32(message.disable_room_random_bot_char);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMisc.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMisc.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMisc(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.recharged_list && message.recharged_list.length))
                                message.recharged_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.recharged_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.recharged_list && message.recharged_list.length))
                            message.recharged_list = [];
                        message.recharged_list.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.faiths && message.faiths.length))
                            message.faiths = [];
                        message.faiths.push($root.lq.ResMisc.MiscFaithData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified_hidden = value;
                        else
                            delete message.verified_hidden;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified_value = value;
                        else
                            delete message.verified_value;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_room_random_bot_char = value;
                        else
                            delete message.disable_room_random_bot_char;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMisc.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMisc.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.recharged_list != null && message.hasOwnProperty("recharged_list")) {
                if (!Array.isArray(message.recharged_list))
                    return "recharged_list: array expected";
                for (let i = 0; i < message.recharged_list.length; ++i)
                    if (!$util.isInteger(message.recharged_list[i]))
                        return "recharged_list: integer[] expected";
            }
            if (message.faiths != null && message.hasOwnProperty("faiths")) {
                if (!Array.isArray(message.faiths))
                    return "faiths: array expected";
                for (let i = 0; i < message.faiths.length; ++i) {
                    let error = $root.lq.ResMisc.MiscFaithData.verify(message.faiths[i], _depth + 1);
                    if (error)
                        return "faiths." + error;
                }
            }
            if (message.verified_hidden != null && message.hasOwnProperty("verified_hidden"))
                if (!$util.isInteger(message.verified_hidden))
                    return "verified_hidden: integer expected";
            if (message.verified_value != null && message.hasOwnProperty("verified_value"))
                if (!$util.isInteger(message.verified_value))
                    return "verified_value: integer expected";
            if (message.disable_room_random_bot_char != null && message.hasOwnProperty("disable_room_random_bot_char"))
                if (!$util.isInteger(message.disable_room_random_bot_char))
                    return "disable_room_random_bot_char: integer expected";
            return null;
        };

        ResMisc.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMisc)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMisc();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMisc.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.recharged_list) {
                if (!Array.isArray(object.recharged_list))
                    throw TypeError(".lq.ResMisc.recharged_list: array expected");
                message.recharged_list = Array(object.recharged_list.length);
                for (let i = 0; i < object.recharged_list.length; ++i)
                    message.recharged_list[i] = object.recharged_list[i] >>> 0;
            }
            if (object.faiths) {
                if (!Array.isArray(object.faiths))
                    throw TypeError(".lq.ResMisc.faiths: array expected");
                message.faiths = Array(object.faiths.length);
                for (let i = 0; i < object.faiths.length; ++i) {
                    if (typeof object.faiths[i] !== "object")
                        throw TypeError(".lq.ResMisc.faiths: object expected");
                    message.faiths[i] = $root.lq.ResMisc.MiscFaithData.fromObject(object.faiths[i], _depth + 1);
                }
            }
            if (object.verified_hidden != null)
                if (Number(object.verified_hidden) !== 0)
                    message.verified_hidden = object.verified_hidden >>> 0;
            if (object.verified_value != null)
                if (Number(object.verified_value) !== 0)
                    message.verified_value = object.verified_value >>> 0;
            if (object.disable_room_random_bot_char != null)
                if (Number(object.disable_room_random_bot_char) !== 0)
                    message.disable_room_random_bot_char = object.disable_room_random_bot_char >>> 0;
            return message;
        };

        ResMisc.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.recharged_list = [];
                object.faiths = [];
            }
            if (options.defaults) {
                object.error = null;
                object.verified_hidden = 0;
                object.verified_value = 0;
                object.disable_room_random_bot_char = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.recharged_list && message.recharged_list.length) {
                object.recharged_list = Array(message.recharged_list.length);
                for (let j = 0; j < message.recharged_list.length; ++j)
                    object.recharged_list[j] = message.recharged_list[j];
            }
            if (message.faiths && message.faiths.length) {
                object.faiths = Array(message.faiths.length);
                for (let j = 0; j < message.faiths.length; ++j)
                    object.faiths[j] = $root.lq.ResMisc.MiscFaithData.toObject(message.faiths[j], options);
            }
            if (message.verified_hidden != null && message.hasOwnProperty("verified_hidden"))
                object.verified_hidden = message.verified_hidden;
            if (message.verified_value != null && message.hasOwnProperty("verified_value"))
                object.verified_value = message.verified_value;
            if (message.disable_room_random_bot_char != null && message.hasOwnProperty("disable_room_random_bot_char"))
                object.disable_room_random_bot_char = message.disable_room_random_bot_char;
            return object;
        };

        ResMisc.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMisc.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMisc";
        };

        ResMisc.MiscFaithData = (function() {

            function MiscFaithData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MiscFaithData.prototype.faith_id = 0;
            MiscFaithData.prototype.count = 0;

            MiscFaithData.create = function create(properties) {
                return new MiscFaithData(properties);
            };

            MiscFaithData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.faith_id != null && Object.hasOwnProperty.call(message, "faith_id"))
                    writer.uint32(8).uint32(message.faith_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).int32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MiscFaithData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MiscFaithData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMisc.MiscFaithData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.faith_id = value;
                            else
                                delete message.faith_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MiscFaithData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MiscFaithData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.faith_id != null && message.hasOwnProperty("faith_id"))
                    if (!$util.isInteger(message.faith_id))
                        return "faith_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            MiscFaithData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResMisc.MiscFaithData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResMisc.MiscFaithData();
                if (object.faith_id != null)
                    if (Number(object.faith_id) !== 0)
                        message.faith_id = object.faith_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count | 0;
                return message;
            };

            MiscFaithData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.faith_id = 0;
                    object.count = 0;
                }
                if (message.faith_id != null && message.hasOwnProperty("faith_id"))
                    object.faith_id = message.faith_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            MiscFaithData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MiscFaithData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResMisc.MiscFaithData";
            };

            return MiscFaithData;
        })();

        return ResMisc;
    })();

    lq.ReqModifySignature = (function() {

        function ReqModifySignature(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqModifySignature.prototype.signature = "";

        ReqModifySignature.create = function create(properties) {
            return new ReqModifySignature(properties);
        };

        ReqModifySignature.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(10).string(message.signature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqModifySignature.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqModifySignature.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqModifySignature(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.signature = value;
                        else
                            delete message.signature;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqModifySignature.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqModifySignature.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.signature != null && message.hasOwnProperty("signature"))
                if (!$util.isString(message.signature))
                    return "signature: string expected";
            return null;
        };

        ReqModifySignature.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqModifySignature)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqModifySignature();
            if (object.signature != null)
                if (typeof object.signature !== "string" || object.signature.length)
                    message.signature = String(object.signature);
            return message;
        };

        ReqModifySignature.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.signature = "";
            if (message.signature != null && message.hasOwnProperty("signature"))
                object.signature = message.signature;
            return object;
        };

        ReqModifySignature.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqModifySignature.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqModifySignature";
        };

        return ReqModifySignature;
    })();

    lq.ResIDCardInfo = (function() {

        function ResIDCardInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResIDCardInfo.prototype.error = null;
        ResIDCardInfo.prototype.is_authed = false;
        ResIDCardInfo.prototype.country = "";

        ResIDCardInfo.create = function create(properties) {
            return new ResIDCardInfo(properties);
        };

        ResIDCardInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.is_authed != null && Object.hasOwnProperty.call(message, "is_authed"))
                writer.uint32(16).bool(message.is_authed);
            if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                writer.uint32(26).string(message.country);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResIDCardInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResIDCardInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResIDCardInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_authed = value;
                        else
                            delete message.is_authed;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.country = value;
                        else
                            delete message.country;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResIDCardInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResIDCardInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.is_authed != null && message.hasOwnProperty("is_authed"))
                if (typeof message.is_authed !== "boolean")
                    return "is_authed: boolean expected";
            if (message.country != null && message.hasOwnProperty("country"))
                if (!$util.isString(message.country))
                    return "country: string expected";
            return null;
        };

        ResIDCardInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResIDCardInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResIDCardInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResIDCardInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.is_authed != null)
                if (object.is_authed)
                    message.is_authed = Boolean(object.is_authed);
            if (object.country != null)
                if (typeof object.country !== "string" || object.country.length)
                    message.country = String(object.country);
            return message;
        };

        ResIDCardInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.is_authed = false;
                object.country = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.is_authed != null && message.hasOwnProperty("is_authed"))
                object.is_authed = message.is_authed;
            if (message.country != null && message.hasOwnProperty("country"))
                object.country = message.country;
            return object;
        };

        ResIDCardInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResIDCardInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResIDCardInfo";
        };

        return ResIDCardInfo;
    })();

    lq.ReqUpdateIDCardInfo = (function() {

        function ReqUpdateIDCardInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateIDCardInfo.prototype.fullname = "";
        ReqUpdateIDCardInfo.prototype.card_no = "";

        ReqUpdateIDCardInfo.create = function create(properties) {
            return new ReqUpdateIDCardInfo(properties);
        };

        ReqUpdateIDCardInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.fullname != null && Object.hasOwnProperty.call(message, "fullname"))
                writer.uint32(10).string(message.fullname);
            if (message.card_no != null && Object.hasOwnProperty.call(message, "card_no"))
                writer.uint32(18).string(message.card_no);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateIDCardInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateIDCardInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateIDCardInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.fullname = value;
                        else
                            delete message.fullname;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.card_no = value;
                        else
                            delete message.card_no;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateIDCardInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateIDCardInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.fullname != null && message.hasOwnProperty("fullname"))
                if (!$util.isString(message.fullname))
                    return "fullname: string expected";
            if (message.card_no != null && message.hasOwnProperty("card_no"))
                if (!$util.isString(message.card_no))
                    return "card_no: string expected";
            return null;
        };

        ReqUpdateIDCardInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateIDCardInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateIDCardInfo();
            if (object.fullname != null)
                if (typeof object.fullname !== "string" || object.fullname.length)
                    message.fullname = String(object.fullname);
            if (object.card_no != null)
                if (typeof object.card_no !== "string" || object.card_no.length)
                    message.card_no = String(object.card_no);
            return message;
        };

        ReqUpdateIDCardInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.fullname = "";
                object.card_no = "";
            }
            if (message.fullname != null && message.hasOwnProperty("fullname"))
                object.fullname = message.fullname;
            if (message.card_no != null && message.hasOwnProperty("card_no"))
                object.card_no = message.card_no;
            return object;
        };

        ReqUpdateIDCardInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateIDCardInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateIDCardInfo";
        };

        return ReqUpdateIDCardInfo;
    })();

    lq.ResVipReward = (function() {

        function ResVipReward(properties) {
            this.gained_vip_levels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResVipReward.prototype.error = null;
        ResVipReward.prototype.gained_vip_levels = $util.emptyArray;

        ResVipReward.create = function create(properties) {
            return new ResVipReward(properties);
        };

        ResVipReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.gained_vip_levels != null && message.gained_vip_levels.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.gained_vip_levels.length; ++i)
                    writer.uint32(message.gained_vip_levels[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResVipReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResVipReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResVipReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.gained_vip_levels && message.gained_vip_levels.length))
                                message.gained_vip_levels = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gained_vip_levels.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.gained_vip_levels && message.gained_vip_levels.length))
                            message.gained_vip_levels = [];
                        message.gained_vip_levels.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResVipReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResVipReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.gained_vip_levels != null && message.hasOwnProperty("gained_vip_levels")) {
                if (!Array.isArray(message.gained_vip_levels))
                    return "gained_vip_levels: array expected";
                for (let i = 0; i < message.gained_vip_levels.length; ++i)
                    if (!$util.isInteger(message.gained_vip_levels[i]))
                        return "gained_vip_levels: integer[] expected";
            }
            return null;
        };

        ResVipReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResVipReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResVipReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResVipReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.gained_vip_levels) {
                if (!Array.isArray(object.gained_vip_levels))
                    throw TypeError(".lq.ResVipReward.gained_vip_levels: array expected");
                message.gained_vip_levels = Array(object.gained_vip_levels.length);
                for (let i = 0; i < object.gained_vip_levels.length; ++i)
                    message.gained_vip_levels[i] = object.gained_vip_levels[i] >>> 0;
            }
            return message;
        };

        ResVipReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gained_vip_levels = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.gained_vip_levels && message.gained_vip_levels.length) {
                object.gained_vip_levels = Array(message.gained_vip_levels.length);
                for (let j = 0; j < message.gained_vip_levels.length; ++j)
                    object.gained_vip_levels[j] = message.gained_vip_levels[j];
            }
            return object;
        };

        ResVipReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResVipReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResVipReward";
        };

        return ResVipReward;
    })();

    lq.ResFetchRefundOrder = (function() {

        function ResFetchRefundOrder(properties) {
            this.orders = [];
            this.message = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchRefundOrder.prototype.error = null;
        ResFetchRefundOrder.prototype.orders = $util.emptyArray;
        ResFetchRefundOrder.prototype.clear_deadline = 0;
        ResFetchRefundOrder.prototype.message = $util.emptyArray;

        ResFetchRefundOrder.create = function create(properties) {
            return new ResFetchRefundOrder(properties);
        };

        ResFetchRefundOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.orders != null && message.orders.length)
                for (let i = 0; i < message.orders.length; ++i)
                    $root.lq.ResFetchRefundOrder.OrderInfo.encode(message.orders[i], writer.uint32(18).fork()).ldelim();
            if (message.clear_deadline != null && Object.hasOwnProperty.call(message, "clear_deadline"))
                writer.uint32(24).uint32(message.clear_deadline);
            if (message.message != null && message.message.length)
                for (let i = 0; i < message.message.length; ++i)
                    $root.lq.I18nContext.encode(message.message[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchRefundOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchRefundOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRefundOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.orders && message.orders.length))
                            message.orders = [];
                        message.orders.push($root.lq.ResFetchRefundOrder.OrderInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.clear_deadline = value;
                        else
                            delete message.clear_deadline;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.message && message.message.length))
                            message.message = [];
                        message.message.push($root.lq.I18nContext.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchRefundOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchRefundOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.orders != null && message.hasOwnProperty("orders")) {
                if (!Array.isArray(message.orders))
                    return "orders: array expected";
                for (let i = 0; i < message.orders.length; ++i) {
                    let error = $root.lq.ResFetchRefundOrder.OrderInfo.verify(message.orders[i], _depth + 1);
                    if (error)
                        return "orders." + error;
                }
            }
            if (message.clear_deadline != null && message.hasOwnProperty("clear_deadline"))
                if (!$util.isInteger(message.clear_deadline))
                    return "clear_deadline: integer expected";
            if (message.message != null && message.hasOwnProperty("message")) {
                if (!Array.isArray(message.message))
                    return "message: array expected";
                for (let i = 0; i < message.message.length; ++i) {
                    let error = $root.lq.I18nContext.verify(message.message[i], _depth + 1);
                    if (error)
                        return "message." + error;
                }
            }
            return null;
        };

        ResFetchRefundOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchRefundOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchRefundOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchRefundOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.orders) {
                if (!Array.isArray(object.orders))
                    throw TypeError(".lq.ResFetchRefundOrder.orders: array expected");
                message.orders = Array(object.orders.length);
                for (let i = 0; i < object.orders.length; ++i) {
                    if (typeof object.orders[i] !== "object")
                        throw TypeError(".lq.ResFetchRefundOrder.orders: object expected");
                    message.orders[i] = $root.lq.ResFetchRefundOrder.OrderInfo.fromObject(object.orders[i], _depth + 1);
                }
            }
            if (object.clear_deadline != null)
                if (Number(object.clear_deadline) !== 0)
                    message.clear_deadline = object.clear_deadline >>> 0;
            if (object.message) {
                if (!Array.isArray(object.message))
                    throw TypeError(".lq.ResFetchRefundOrder.message: array expected");
                message.message = Array(object.message.length);
                for (let i = 0; i < object.message.length; ++i) {
                    if (typeof object.message[i] !== "object")
                        throw TypeError(".lq.ResFetchRefundOrder.message: object expected");
                    message.message[i] = $root.lq.I18nContext.fromObject(object.message[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchRefundOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.orders = [];
                object.message = [];
            }
            if (options.defaults) {
                object.error = null;
                object.clear_deadline = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.orders && message.orders.length) {
                object.orders = Array(message.orders.length);
                for (let j = 0; j < message.orders.length; ++j)
                    object.orders[j] = $root.lq.ResFetchRefundOrder.OrderInfo.toObject(message.orders[j], options);
            }
            if (message.clear_deadline != null && message.hasOwnProperty("clear_deadline"))
                object.clear_deadline = message.clear_deadline;
            if (message.message && message.message.length) {
                object.message = Array(message.message.length);
                for (let j = 0; j < message.message.length; ++j)
                    object.message[j] = $root.lq.I18nContext.toObject(message.message[j], options);
            }
            return object;
        };

        ResFetchRefundOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchRefundOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchRefundOrder";
        };

        ResFetchRefundOrder.OrderInfo = (function() {

            function OrderInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            OrderInfo.prototype.success_time = 0;
            OrderInfo.prototype.goods_id = 0;
            OrderInfo.prototype.cleared = 0;
            OrderInfo.prototype.order_id = "";

            OrderInfo.create = function create(properties) {
                return new OrderInfo(properties);
            };

            OrderInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.success_time != null && Object.hasOwnProperty.call(message, "success_time"))
                    writer.uint32(8).uint32(message.success_time);
                if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                    writer.uint32(16).uint32(message.goods_id);
                if (message.cleared != null && Object.hasOwnProperty.call(message, "cleared"))
                    writer.uint32(24).uint32(message.cleared);
                if (message.order_id != null && Object.hasOwnProperty.call(message, "order_id"))
                    writer.uint32(34).string(message.order_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            OrderInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            OrderInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRefundOrder.OrderInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.success_time = value;
                            else
                                delete message.success_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.goods_id = value;
                            else
                                delete message.goods_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.cleared = value;
                            else
                                delete message.cleared;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.order_id = value;
                            else
                                delete message.order_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            OrderInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            OrderInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.success_time != null && message.hasOwnProperty("success_time"))
                    if (!$util.isInteger(message.success_time))
                        return "success_time: integer expected";
                if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                    if (!$util.isInteger(message.goods_id))
                        return "goods_id: integer expected";
                if (message.cleared != null && message.hasOwnProperty("cleared"))
                    if (!$util.isInteger(message.cleared))
                        return "cleared: integer expected";
                if (message.order_id != null && message.hasOwnProperty("order_id"))
                    if (!$util.isString(message.order_id))
                        return "order_id: string expected";
                return null;
            };

            OrderInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchRefundOrder.OrderInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchRefundOrder.OrderInfo();
                if (object.success_time != null)
                    if (Number(object.success_time) !== 0)
                        message.success_time = object.success_time >>> 0;
                if (object.goods_id != null)
                    if (Number(object.goods_id) !== 0)
                        message.goods_id = object.goods_id >>> 0;
                if (object.cleared != null)
                    if (Number(object.cleared) !== 0)
                        message.cleared = object.cleared >>> 0;
                if (object.order_id != null)
                    if (typeof object.order_id !== "string" || object.order_id.length)
                        message.order_id = String(object.order_id);
                return message;
            };

            OrderInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.success_time = 0;
                    object.goods_id = 0;
                    object.cleared = 0;
                    object.order_id = "";
                }
                if (message.success_time != null && message.hasOwnProperty("success_time"))
                    object.success_time = message.success_time;
                if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                    object.goods_id = message.goods_id;
                if (message.cleared != null && message.hasOwnProperty("cleared"))
                    object.cleared = message.cleared;
                if (message.order_id != null && message.hasOwnProperty("order_id"))
                    object.order_id = message.order_id;
                return object;
            };

            OrderInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            OrderInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchRefundOrder.OrderInfo";
            };

            return OrderInfo;
        })();

        return ResFetchRefundOrder;
    })();

    lq.ReqGainVipReward = (function() {

        function ReqGainVipReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGainVipReward.prototype.vip_level = 0;

        ReqGainVipReward.create = function create(properties) {
            return new ReqGainVipReward(properties);
        };

        ReqGainVipReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.vip_level != null && Object.hasOwnProperty.call(message, "vip_level"))
                writer.uint32(8).uint32(message.vip_level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGainVipReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGainVipReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGainVipReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vip_level = value;
                        else
                            delete message.vip_level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGainVipReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGainVipReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.vip_level != null && message.hasOwnProperty("vip_level"))
                if (!$util.isInteger(message.vip_level))
                    return "vip_level: integer expected";
            return null;
        };

        ReqGainVipReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGainVipReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGainVipReward();
            if (object.vip_level != null)
                if (Number(object.vip_level) !== 0)
                    message.vip_level = object.vip_level >>> 0;
            return message;
        };

        ReqGainVipReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.vip_level = 0;
            if (message.vip_level != null && message.hasOwnProperty("vip_level"))
                object.vip_level = message.vip_level;
            return object;
        };

        ReqGainVipReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGainVipReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGainVipReward";
        };

        return ReqGainVipReward;
    })();

    lq.ReqFetchCustomizedContestList = (function() {

        function ReqFetchCustomizedContestList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestList.prototype.start = 0;
        ReqFetchCustomizedContestList.prototype.count = 0;
        ReqFetchCustomizedContestList.prototype.lang = "";

        ReqFetchCustomizedContestList.create = function create(properties) {
            return new ReqFetchCustomizedContestList(properties);
        };

        ReqFetchCustomizedContestList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.start != null && Object.hasOwnProperty.call(message, "start"))
                writer.uint32(8).uint32(message.start);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(26).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start = value;
                        else
                            delete message.start;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.start != null && message.hasOwnProperty("start"))
                if (!$util.isInteger(message.start))
                    return "start: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqFetchCustomizedContestList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestList();
            if (object.start != null)
                if (Number(object.start) !== 0)
                    message.start = object.start >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqFetchCustomizedContestList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.start = 0;
                object.count = 0;
                object.lang = "";
            }
            if (message.start != null && message.hasOwnProperty("start"))
                object.start = message.start;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqFetchCustomizedContestList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestList";
        };

        return ReqFetchCustomizedContestList;
    })();

    lq.ResFetchCustomizedContestList = (function() {

        function ResFetchCustomizedContestList(properties) {
            this.contests = [];
            this.follow_contests = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestList.prototype.error = null;
        ResFetchCustomizedContestList.prototype.contests = $util.emptyArray;
        ResFetchCustomizedContestList.prototype.follow_contests = $util.emptyArray;

        ResFetchCustomizedContestList.create = function create(properties) {
            return new ResFetchCustomizedContestList(properties);
        };

        ResFetchCustomizedContestList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.contests != null && message.contests.length)
                for (let i = 0; i < message.contests.length; ++i)
                    $root.lq.CustomizedContestBase.encode(message.contests[i], writer.uint32(18).fork()).ldelim();
            if (message.follow_contests != null && message.follow_contests.length)
                for (let i = 0; i < message.follow_contests.length; ++i)
                    $root.lq.CustomizedContestBase.encode(message.follow_contests[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contests && message.contests.length))
                            message.contests = [];
                        message.contests.push($root.lq.CustomizedContestBase.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.follow_contests && message.follow_contests.length))
                            message.follow_contests = [];
                        message.follow_contests.push($root.lq.CustomizedContestBase.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.contests != null && message.hasOwnProperty("contests")) {
                if (!Array.isArray(message.contests))
                    return "contests: array expected";
                for (let i = 0; i < message.contests.length; ++i) {
                    let error = $root.lq.CustomizedContestBase.verify(message.contests[i], _depth + 1);
                    if (error)
                        return "contests." + error;
                }
            }
            if (message.follow_contests != null && message.hasOwnProperty("follow_contests")) {
                if (!Array.isArray(message.follow_contests))
                    return "follow_contests: array expected";
                for (let i = 0; i < message.follow_contests.length; ++i) {
                    let error = $root.lq.CustomizedContestBase.verify(message.follow_contests[i], _depth + 1);
                    if (error)
                        return "follow_contests." + error;
                }
            }
            return null;
        };

        ResFetchCustomizedContestList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.contests) {
                if (!Array.isArray(object.contests))
                    throw TypeError(".lq.ResFetchCustomizedContestList.contests: array expected");
                message.contests = Array(object.contests.length);
                for (let i = 0; i < object.contests.length; ++i) {
                    if (typeof object.contests[i] !== "object")
                        throw TypeError(".lq.ResFetchCustomizedContestList.contests: object expected");
                    message.contests[i] = $root.lq.CustomizedContestBase.fromObject(object.contests[i], _depth + 1);
                }
            }
            if (object.follow_contests) {
                if (!Array.isArray(object.follow_contests))
                    throw TypeError(".lq.ResFetchCustomizedContestList.follow_contests: array expected");
                message.follow_contests = Array(object.follow_contests.length);
                for (let i = 0; i < object.follow_contests.length; ++i) {
                    if (typeof object.follow_contests[i] !== "object")
                        throw TypeError(".lq.ResFetchCustomizedContestList.follow_contests: object expected");
                    message.follow_contests[i] = $root.lq.CustomizedContestBase.fromObject(object.follow_contests[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchCustomizedContestList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.contests = [];
                object.follow_contests = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.contests && message.contests.length) {
                object.contests = Array(message.contests.length);
                for (let j = 0; j < message.contests.length; ++j)
                    object.contests[j] = $root.lq.CustomizedContestBase.toObject(message.contests[j], options);
            }
            if (message.follow_contests && message.follow_contests.length) {
                object.follow_contests = Array(message.follow_contests.length);
                for (let j = 0; j < message.follow_contests.length; ++j)
                    object.follow_contests[j] = $root.lq.CustomizedContestBase.toObject(message.follow_contests[j], options);
            }
            return object;
        };

        ResFetchCustomizedContestList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestList";
        };

        return ResFetchCustomizedContestList;
    })();

    lq.ReqFetchCustomizedContestAuthInfo = (function() {

        function ReqFetchCustomizedContestAuthInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestAuthInfo.prototype.unique_id = 0;

        ReqFetchCustomizedContestAuthInfo.create = function create(properties) {
            return new ReqFetchCustomizedContestAuthInfo(properties);
        };

        ReqFetchCustomizedContestAuthInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestAuthInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestAuthInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestAuthInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestAuthInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestAuthInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqFetchCustomizedContestAuthInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestAuthInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestAuthInfo();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqFetchCustomizedContestAuthInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqFetchCustomizedContestAuthInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestAuthInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestAuthInfo";
        };

        return ReqFetchCustomizedContestAuthInfo;
    })();

    lq.ResFetchCustomizedContestAuthInfo = (function() {

        function ResFetchCustomizedContestAuthInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestAuthInfo.prototype.error = null;
        ResFetchCustomizedContestAuthInfo.prototype.observer_level = 0;

        ResFetchCustomizedContestAuthInfo.create = function create(properties) {
            return new ResFetchCustomizedContestAuthInfo(properties);
        };

        ResFetchCustomizedContestAuthInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.observer_level != null && Object.hasOwnProperty.call(message, "observer_level"))
                writer.uint32(16).uint32(message.observer_level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestAuthInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestAuthInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestAuthInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.observer_level = value;
                        else
                            delete message.observer_level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestAuthInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestAuthInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.observer_level != null && message.hasOwnProperty("observer_level"))
                if (!$util.isInteger(message.observer_level))
                    return "observer_level: integer expected";
            return null;
        };

        ResFetchCustomizedContestAuthInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestAuthInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestAuthInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestAuthInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.observer_level != null)
                if (Number(object.observer_level) !== 0)
                    message.observer_level = object.observer_level >>> 0;
            return message;
        };

        ResFetchCustomizedContestAuthInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.observer_level = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.observer_level != null && message.hasOwnProperty("observer_level"))
                object.observer_level = message.observer_level;
            return object;
        };

        ResFetchCustomizedContestAuthInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestAuthInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestAuthInfo";
        };

        return ResFetchCustomizedContestAuthInfo;
    })();

    lq.ReqEnterCustomizedContest = (function() {

        function ReqEnterCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqEnterCustomizedContest.prototype.unique_id = 0;
        ReqEnterCustomizedContest.prototype.lang = "";

        ReqEnterCustomizedContest.create = function create(properties) {
            return new ReqEnterCustomizedContest(properties);
        };

        ReqEnterCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(18).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqEnterCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqEnterCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqEnterCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqEnterCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqEnterCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqEnterCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqEnterCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqEnterCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqEnterCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.lang = "";
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqEnterCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqEnterCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqEnterCustomizedContest";
        };

        return ReqEnterCustomizedContest;
    })();

    lq.ResEnterCustomizedContest = (function() {

        function ResEnterCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResEnterCustomizedContest.prototype.error = null;
        ResEnterCustomizedContest.prototype.detail_info = null;
        ResEnterCustomizedContest.prototype.player_report = null;
        ResEnterCustomizedContest.prototype.is_followed = false;
        ResEnterCustomizedContest.prototype.state = 0;
        ResEnterCustomizedContest.prototype.is_admin = false;
        ResEnterCustomizedContest.prototype.game_plan = null;

        ResEnterCustomizedContest.create = function create(properties) {
            return new ResEnterCustomizedContest(properties);
        };

        ResEnterCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.detail_info != null && Object.hasOwnProperty.call(message, "detail_info"))
                $root.lq.CustomizedContestDetail.encode(message.detail_info, writer.uint32(18).fork()).ldelim();
            if (message.player_report != null && Object.hasOwnProperty.call(message, "player_report"))
                $root.lq.CustomizedContestPlayerReport.encode(message.player_report, writer.uint32(26).fork()).ldelim();
            if (message.is_followed != null && Object.hasOwnProperty.call(message, "is_followed"))
                writer.uint32(32).bool(message.is_followed);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(40).uint32(message.state);
            if (message.is_admin != null && Object.hasOwnProperty.call(message, "is_admin"))
                writer.uint32(48).bool(message.is_admin);
            if (message.game_plan != null && Object.hasOwnProperty.call(message, "game_plan"))
                $root.lq.CustomizedContestGamePlan.encode(message.game_plan, writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResEnterCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResEnterCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResEnterCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.detail_info = $root.lq.CustomizedContestDetail.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail_info);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.player_report = $root.lq.CustomizedContestPlayerReport.decode(reader, reader.uint32(), undefined, _depth + 1, message.player_report);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_followed = value;
                        else
                            delete message.is_followed;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_admin = value;
                        else
                            delete message.is_admin;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.game_plan = $root.lq.CustomizedContestGamePlan.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_plan);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResEnterCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResEnterCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.detail_info != null && message.hasOwnProperty("detail_info")) {
                let error = $root.lq.CustomizedContestDetail.verify(message.detail_info, _depth + 1);
                if (error)
                    return "detail_info." + error;
            }
            if (message.player_report != null && message.hasOwnProperty("player_report")) {
                let error = $root.lq.CustomizedContestPlayerReport.verify(message.player_report, _depth + 1);
                if (error)
                    return "player_report." + error;
            }
            if (message.is_followed != null && message.hasOwnProperty("is_followed"))
                if (typeof message.is_followed !== "boolean")
                    return "is_followed: boolean expected";
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            if (message.is_admin != null && message.hasOwnProperty("is_admin"))
                if (typeof message.is_admin !== "boolean")
                    return "is_admin: boolean expected";
            if (message.game_plan != null && message.hasOwnProperty("game_plan")) {
                let error = $root.lq.CustomizedContestGamePlan.verify(message.game_plan, _depth + 1);
                if (error)
                    return "game_plan." + error;
            }
            return null;
        };

        ResEnterCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResEnterCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResEnterCustomizedContest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResEnterCustomizedContest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.detail_info != null) {
                if (typeof object.detail_info !== "object")
                    throw TypeError(".lq.ResEnterCustomizedContest.detail_info: object expected");
                message.detail_info = $root.lq.CustomizedContestDetail.fromObject(object.detail_info, _depth + 1);
            }
            if (object.player_report != null) {
                if (typeof object.player_report !== "object")
                    throw TypeError(".lq.ResEnterCustomizedContest.player_report: object expected");
                message.player_report = $root.lq.CustomizedContestPlayerReport.fromObject(object.player_report, _depth + 1);
            }
            if (object.is_followed != null)
                if (object.is_followed)
                    message.is_followed = Boolean(object.is_followed);
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            if (object.is_admin != null)
                if (object.is_admin)
                    message.is_admin = Boolean(object.is_admin);
            if (object.game_plan != null) {
                if (typeof object.game_plan !== "object")
                    throw TypeError(".lq.ResEnterCustomizedContest.game_plan: object expected");
                message.game_plan = $root.lq.CustomizedContestGamePlan.fromObject(object.game_plan, _depth + 1);
            }
            return message;
        };

        ResEnterCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.detail_info = null;
                object.player_report = null;
                object.is_followed = false;
                object.state = 0;
                object.is_admin = false;
                object.game_plan = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.detail_info != null && message.hasOwnProperty("detail_info"))
                object.detail_info = $root.lq.CustomizedContestDetail.toObject(message.detail_info, options);
            if (message.player_report != null && message.hasOwnProperty("player_report"))
                object.player_report = $root.lq.CustomizedContestPlayerReport.toObject(message.player_report, options);
            if (message.is_followed != null && message.hasOwnProperty("is_followed"))
                object.is_followed = message.is_followed;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            if (message.is_admin != null && message.hasOwnProperty("is_admin"))
                object.is_admin = message.is_admin;
            if (message.game_plan != null && message.hasOwnProperty("game_plan"))
                object.game_plan = $root.lq.CustomizedContestGamePlan.toObject(message.game_plan, options);
            return object;
        };

        ResEnterCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResEnterCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResEnterCustomizedContest";
        };

        return ResEnterCustomizedContest;
    })();

    lq.ReqFetchCustomizedContestOnlineInfo = (function() {

        function ReqFetchCustomizedContestOnlineInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestOnlineInfo.prototype.unique_id = 0;

        ReqFetchCustomizedContestOnlineInfo.create = function create(properties) {
            return new ReqFetchCustomizedContestOnlineInfo(properties);
        };

        ReqFetchCustomizedContestOnlineInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestOnlineInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestOnlineInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestOnlineInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestOnlineInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestOnlineInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqFetchCustomizedContestOnlineInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestOnlineInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestOnlineInfo();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqFetchCustomizedContestOnlineInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqFetchCustomizedContestOnlineInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestOnlineInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestOnlineInfo";
        };

        return ReqFetchCustomizedContestOnlineInfo;
    })();

    lq.ResFetchCustomizedContestOnlineInfo = (function() {

        function ResFetchCustomizedContestOnlineInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestOnlineInfo.prototype.error = null;
        ResFetchCustomizedContestOnlineInfo.prototype.online_player = 0;

        ResFetchCustomizedContestOnlineInfo.create = function create(properties) {
            return new ResFetchCustomizedContestOnlineInfo(properties);
        };

        ResFetchCustomizedContestOnlineInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.online_player != null && Object.hasOwnProperty.call(message, "online_player"))
                writer.uint32(16).uint32(message.online_player);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestOnlineInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestOnlineInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestOnlineInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.online_player = value;
                        else
                            delete message.online_player;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestOnlineInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestOnlineInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.online_player != null && message.hasOwnProperty("online_player"))
                if (!$util.isInteger(message.online_player))
                    return "online_player: integer expected";
            return null;
        };

        ResFetchCustomizedContestOnlineInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestOnlineInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestOnlineInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestOnlineInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.online_player != null)
                if (Number(object.online_player) !== 0)
                    message.online_player = object.online_player >>> 0;
            return message;
        };

        ResFetchCustomizedContestOnlineInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.online_player = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.online_player != null && message.hasOwnProperty("online_player"))
                object.online_player = message.online_player;
            return object;
        };

        ResFetchCustomizedContestOnlineInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestOnlineInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestOnlineInfo";
        };

        return ResFetchCustomizedContestOnlineInfo;
    })();

    lq.ReqFetchCustomizedContestByContestId = (function() {

        function ReqFetchCustomizedContestByContestId(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestByContestId.prototype.contest_id = 0;
        ReqFetchCustomizedContestByContestId.prototype.lang = "";

        ReqFetchCustomizedContestByContestId.create = function create(properties) {
            return new ReqFetchCustomizedContestByContestId(properties);
        };

        ReqFetchCustomizedContestByContestId.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.contest_id != null && Object.hasOwnProperty.call(message, "contest_id"))
                writer.uint32(8).uint32(message.contest_id);
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(18).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestByContestId.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestByContestId.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestByContestId(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.contest_id = value;
                        else
                            delete message.contest_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestByContestId.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestByContestId.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                if (!$util.isInteger(message.contest_id))
                    return "contest_id: integer expected";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqFetchCustomizedContestByContestId.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestByContestId)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestByContestId();
            if (object.contest_id != null)
                if (Number(object.contest_id) !== 0)
                    message.contest_id = object.contest_id >>> 0;
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqFetchCustomizedContestByContestId.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.contest_id = 0;
                object.lang = "";
            }
            if (message.contest_id != null && message.hasOwnProperty("contest_id"))
                object.contest_id = message.contest_id;
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqFetchCustomizedContestByContestId.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestByContestId.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestByContestId";
        };

        return ReqFetchCustomizedContestByContestId;
    })();

    lq.ResFetchCustomizedContestByContestId = (function() {

        function ResFetchCustomizedContestByContestId(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestByContestId.prototype.error = null;
        ResFetchCustomizedContestByContestId.prototype.contest_info = null;

        ResFetchCustomizedContestByContestId.create = function create(properties) {
            return new ResFetchCustomizedContestByContestId(properties);
        };

        ResFetchCustomizedContestByContestId.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.contest_info != null && Object.hasOwnProperty.call(message, "contest_info"))
                $root.lq.CustomizedContestAbstract.encode(message.contest_info, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestByContestId.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestByContestId.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestByContestId(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.contest_info = $root.lq.CustomizedContestAbstract.decode(reader, reader.uint32(), undefined, _depth + 1, message.contest_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestByContestId.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestByContestId.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.contest_info != null && message.hasOwnProperty("contest_info")) {
                let error = $root.lq.CustomizedContestAbstract.verify(message.contest_info, _depth + 1);
                if (error)
                    return "contest_info." + error;
            }
            return null;
        };

        ResFetchCustomizedContestByContestId.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestByContestId)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestByContestId();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestByContestId.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.contest_info != null) {
                if (typeof object.contest_info !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestByContestId.contest_info: object expected");
                message.contest_info = $root.lq.CustomizedContestAbstract.fromObject(object.contest_info, _depth + 1);
            }
            return message;
        };

        ResFetchCustomizedContestByContestId.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.contest_info = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.contest_info != null && message.hasOwnProperty("contest_info"))
                object.contest_info = $root.lq.CustomizedContestAbstract.toObject(message.contest_info, options);
            return object;
        };

        ResFetchCustomizedContestByContestId.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestByContestId.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestByContestId";
        };

        return ResFetchCustomizedContestByContestId;
    })();

    lq.ReqSignupCustomizedContest = (function() {

        function ReqSignupCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSignupCustomizedContest.prototype.unique_id = 0;
        ReqSignupCustomizedContest.prototype.client_version_string = "";

        ReqSignupCustomizedContest.create = function create(properties) {
            return new ReqSignupCustomizedContest(properties);
        };

        ReqSignupCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSignupCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSignupCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSignupCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSignupCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSignupCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqSignupCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSignupCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSignupCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqSignupCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.client_version_string = "";
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqSignupCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSignupCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSignupCustomizedContest";
        };

        return ReqSignupCustomizedContest;
    })();

    lq.ResSignupCustomizedContest = (function() {

        function ResSignupCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSignupCustomizedContest.prototype.error = null;
        ResSignupCustomizedContest.prototype.state = 0;

        ResSignupCustomizedContest.create = function create(properties) {
            return new ResSignupCustomizedContest(properties);
        };

        ResSignupCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).uint32(message.state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSignupCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSignupCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSignupCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSignupCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSignupCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.state != null && message.hasOwnProperty("state"))
                if (!$util.isInteger(message.state))
                    return "state: integer expected";
            return null;
        };

        ResSignupCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSignupCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSignupCustomizedContest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSignupCustomizedContest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.state != null)
                if (Number(object.state) !== 0)
                    message.state = object.state >>> 0;
            return message;
        };

        ResSignupCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.state = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = message.state;
            return object;
        };

        ResSignupCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSignupCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSignupCustomizedContest";
        };

        return ResSignupCustomizedContest;
    })();

    lq.ReqStartCustomizedContest = (function() {

        function ReqStartCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStartCustomizedContest.prototype.unique_id = 0;
        ReqStartCustomizedContest.prototype.client_version_string = "";

        ReqStartCustomizedContest.create = function create(properties) {
            return new ReqStartCustomizedContest(properties);
        };

        ReqStartCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStartCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStartCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStartCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStartCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStartCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqStartCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStartCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStartCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqStartCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.client_version_string = "";
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqStartCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStartCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStartCustomizedContest";
        };

        return ReqStartCustomizedContest;
    })();

    lq.ReqStopCustomizedContest = (function() {

        function ReqStopCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStopCustomizedContest.prototype.unique_id = 0;

        ReqStopCustomizedContest.create = function create(properties) {
            return new ReqStopCustomizedContest(properties);
        };

        ReqStopCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStopCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStopCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStopCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStopCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStopCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqStopCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStopCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStopCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqStopCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqStopCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStopCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStopCustomizedContest";
        };

        return ReqStopCustomizedContest;
    })();

    lq.ReqJoinCustomizedContestChatRoom = (function() {

        function ReqJoinCustomizedContestChatRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqJoinCustomizedContestChatRoom.prototype.unique_id = 0;

        ReqJoinCustomizedContestChatRoom.create = function create(properties) {
            return new ReqJoinCustomizedContestChatRoom(properties);
        };

        ReqJoinCustomizedContestChatRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqJoinCustomizedContestChatRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqJoinCustomizedContestChatRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqJoinCustomizedContestChatRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqJoinCustomizedContestChatRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqJoinCustomizedContestChatRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqJoinCustomizedContestChatRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqJoinCustomizedContestChatRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqJoinCustomizedContestChatRoom();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqJoinCustomizedContestChatRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqJoinCustomizedContestChatRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqJoinCustomizedContestChatRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqJoinCustomizedContestChatRoom";
        };

        return ReqJoinCustomizedContestChatRoom;
    })();

    lq.ResJoinCustomizedContestChatRoom = (function() {

        function ResJoinCustomizedContestChatRoom(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResJoinCustomizedContestChatRoom.prototype.error = null;
        ResJoinCustomizedContestChatRoom.prototype.token = "";

        ResJoinCustomizedContestChatRoom.create = function create(properties) {
            return new ResJoinCustomizedContestChatRoom(properties);
        };

        ResJoinCustomizedContestChatRoom.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResJoinCustomizedContestChatRoom.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResJoinCustomizedContestChatRoom.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResJoinCustomizedContestChatRoom(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResJoinCustomizedContestChatRoom.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResJoinCustomizedContestChatRoom.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        ResJoinCustomizedContestChatRoom.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResJoinCustomizedContestChatRoom)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResJoinCustomizedContestChatRoom();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResJoinCustomizedContestChatRoom.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            return message;
        };

        ResJoinCustomizedContestChatRoom.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.token = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        ResJoinCustomizedContestChatRoom.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResJoinCustomizedContestChatRoom.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResJoinCustomizedContestChatRoom";
        };

        return ResJoinCustomizedContestChatRoom;
    })();

    lq.ReqSayChatMessage = (function() {

        function ReqSayChatMessage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSayChatMessage.prototype.content = "";
        ReqSayChatMessage.prototype.unique_id = 0;

        ReqSayChatMessage.create = function create(properties) {
            return new ReqSayChatMessage(properties);
        };

        ReqSayChatMessage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(10).string(message.content);
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(16).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSayChatMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSayChatMessage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSayChatMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSayChatMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSayChatMessage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqSayChatMessage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSayChatMessage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSayChatMessage();
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqSayChatMessage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.content = "";
                object.unique_id = 0;
            }
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqSayChatMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSayChatMessage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSayChatMessage";
        };

        return ReqSayChatMessage;
    })();

    lq.ReqFetchCustomizedContestGameLiveList = (function() {

        function ReqFetchCustomizedContestGameLiveList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestGameLiveList.prototype.unique_id = 0;

        ReqFetchCustomizedContestGameLiveList.create = function create(properties) {
            return new ReqFetchCustomizedContestGameLiveList(properties);
        };

        ReqFetchCustomizedContestGameLiveList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestGameLiveList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestGameLiveList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestGameLiveList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestGameLiveList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestGameLiveList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqFetchCustomizedContestGameLiveList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestGameLiveList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestGameLiveList();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqFetchCustomizedContestGameLiveList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqFetchCustomizedContestGameLiveList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestGameLiveList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestGameLiveList";
        };

        return ReqFetchCustomizedContestGameLiveList;
    })();

    lq.ResFetchCustomizedContestGameLiveList = (function() {

        function ResFetchCustomizedContestGameLiveList(properties) {
            this.live_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestGameLiveList.prototype.error = null;
        ResFetchCustomizedContestGameLiveList.prototype.live_list = $util.emptyArray;

        ResFetchCustomizedContestGameLiveList.create = function create(properties) {
            return new ResFetchCustomizedContestGameLiveList(properties);
        };

        ResFetchCustomizedContestGameLiveList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.live_list != null && message.live_list.length)
                for (let i = 0; i < message.live_list.length; ++i)
                    $root.lq.GameLiveHead.encode(message.live_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestGameLiveList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestGameLiveList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestGameLiveList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.live_list && message.live_list.length))
                            message.live_list = [];
                        message.live_list.push($root.lq.GameLiveHead.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestGameLiveList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestGameLiveList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.live_list != null && message.hasOwnProperty("live_list")) {
                if (!Array.isArray(message.live_list))
                    return "live_list: array expected";
                for (let i = 0; i < message.live_list.length; ++i) {
                    let error = $root.lq.GameLiveHead.verify(message.live_list[i], _depth + 1);
                    if (error)
                        return "live_list." + error;
                }
            }
            return null;
        };

        ResFetchCustomizedContestGameLiveList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestGameLiveList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestGameLiveList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestGameLiveList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.live_list) {
                if (!Array.isArray(object.live_list))
                    throw TypeError(".lq.ResFetchCustomizedContestGameLiveList.live_list: array expected");
                message.live_list = Array(object.live_list.length);
                for (let i = 0; i < object.live_list.length; ++i) {
                    if (typeof object.live_list[i] !== "object")
                        throw TypeError(".lq.ResFetchCustomizedContestGameLiveList.live_list: object expected");
                    message.live_list[i] = $root.lq.GameLiveHead.fromObject(object.live_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchCustomizedContestGameLiveList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.live_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.live_list && message.live_list.length) {
                object.live_list = Array(message.live_list.length);
                for (let j = 0; j < message.live_list.length; ++j)
                    object.live_list[j] = $root.lq.GameLiveHead.toObject(message.live_list[j], options);
            }
            return object;
        };

        ResFetchCustomizedContestGameLiveList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestGameLiveList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestGameLiveList";
        };

        return ResFetchCustomizedContestGameLiveList;
    })();

    lq.ReqFetchCustomizedContestGameRecords = (function() {

        function ReqFetchCustomizedContestGameRecords(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchCustomizedContestGameRecords.prototype.unique_id = 0;
        ReqFetchCustomizedContestGameRecords.prototype.last_index = 0;
        ReqFetchCustomizedContestGameRecords.prototype.season_id = 0;

        ReqFetchCustomizedContestGameRecords.create = function create(properties) {
            return new ReqFetchCustomizedContestGameRecords(properties);
        };

        ReqFetchCustomizedContestGameRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.last_index != null && Object.hasOwnProperty.call(message, "last_index"))
                writer.uint32(16).uint32(message.last_index);
            if (message.season_id != null && Object.hasOwnProperty.call(message, "season_id"))
                writer.uint32(24).uint32(message.season_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchCustomizedContestGameRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchCustomizedContestGameRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchCustomizedContestGameRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_index = value;
                        else
                            delete message.last_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.season_id = value;
                        else
                            delete message.season_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchCustomizedContestGameRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchCustomizedContestGameRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.last_index != null && message.hasOwnProperty("last_index"))
                if (!$util.isInteger(message.last_index))
                    return "last_index: integer expected";
            if (message.season_id != null && message.hasOwnProperty("season_id"))
                if (!$util.isInteger(message.season_id))
                    return "season_id: integer expected";
            return null;
        };

        ReqFetchCustomizedContestGameRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchCustomizedContestGameRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchCustomizedContestGameRecords();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.last_index != null)
                if (Number(object.last_index) !== 0)
                    message.last_index = object.last_index >>> 0;
            if (object.season_id != null)
                if (Number(object.season_id) !== 0)
                    message.season_id = object.season_id >>> 0;
            return message;
        };

        ReqFetchCustomizedContestGameRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.last_index = 0;
                object.season_id = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.last_index != null && message.hasOwnProperty("last_index"))
                object.last_index = message.last_index;
            if (message.season_id != null && message.hasOwnProperty("season_id"))
                object.season_id = message.season_id;
            return object;
        };

        ReqFetchCustomizedContestGameRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchCustomizedContestGameRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchCustomizedContestGameRecords";
        };

        return ReqFetchCustomizedContestGameRecords;
    })();

    lq.ResFetchCustomizedContestGameRecords = (function() {

        function ResFetchCustomizedContestGameRecords(properties) {
            this.record_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchCustomizedContestGameRecords.prototype.error = null;
        ResFetchCustomizedContestGameRecords.prototype.next_index = 0;
        ResFetchCustomizedContestGameRecords.prototype.record_list = $util.emptyArray;

        ResFetchCustomizedContestGameRecords.create = function create(properties) {
            return new ResFetchCustomizedContestGameRecords(properties);
        };

        ResFetchCustomizedContestGameRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.next_index != null && Object.hasOwnProperty.call(message, "next_index"))
                writer.uint32(16).uint32(message.next_index);
            if (message.record_list != null && message.record_list.length)
                for (let i = 0; i < message.record_list.length; ++i)
                    $root.lq.RecordGame.encode(message.record_list[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchCustomizedContestGameRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchCustomizedContestGameRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchCustomizedContestGameRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.next_index = value;
                        else
                            delete message.next_index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.record_list && message.record_list.length))
                            message.record_list = [];
                        message.record_list.push($root.lq.RecordGame.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchCustomizedContestGameRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchCustomizedContestGameRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.next_index != null && message.hasOwnProperty("next_index"))
                if (!$util.isInteger(message.next_index))
                    return "next_index: integer expected";
            if (message.record_list != null && message.hasOwnProperty("record_list")) {
                if (!Array.isArray(message.record_list))
                    return "record_list: array expected";
                for (let i = 0; i < message.record_list.length; ++i) {
                    let error = $root.lq.RecordGame.verify(message.record_list[i], _depth + 1);
                    if (error)
                        return "record_list." + error;
                }
            }
            return null;
        };

        ResFetchCustomizedContestGameRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchCustomizedContestGameRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchCustomizedContestGameRecords();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchCustomizedContestGameRecords.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.next_index != null)
                if (Number(object.next_index) !== 0)
                    message.next_index = object.next_index >>> 0;
            if (object.record_list) {
                if (!Array.isArray(object.record_list))
                    throw TypeError(".lq.ResFetchCustomizedContestGameRecords.record_list: array expected");
                message.record_list = Array(object.record_list.length);
                for (let i = 0; i < object.record_list.length; ++i) {
                    if (typeof object.record_list[i] !== "object")
                        throw TypeError(".lq.ResFetchCustomizedContestGameRecords.record_list: object expected");
                    message.record_list[i] = $root.lq.RecordGame.fromObject(object.record_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchCustomizedContestGameRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.record_list = [];
            if (options.defaults) {
                object.error = null;
                object.next_index = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.next_index != null && message.hasOwnProperty("next_index"))
                object.next_index = message.next_index;
            if (message.record_list && message.record_list.length) {
                object.record_list = Array(message.record_list.length);
                for (let j = 0; j < message.record_list.length; ++j)
                    object.record_list[j] = $root.lq.RecordGame.toObject(message.record_list[j], options);
            }
            return object;
        };

        ResFetchCustomizedContestGameRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchCustomizedContestGameRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchCustomizedContestGameRecords";
        };

        return ResFetchCustomizedContestGameRecords;
    })();

    lq.ReqTargetCustomizedContest = (function() {

        function ReqTargetCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqTargetCustomizedContest.prototype.unique_id = 0;

        ReqTargetCustomizedContest.create = function create(properties) {
            return new ReqTargetCustomizedContest(properties);
        };

        ReqTargetCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqTargetCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqTargetCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqTargetCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqTargetCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqTargetCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqTargetCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqTargetCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqTargetCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqTargetCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqTargetCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqTargetCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqTargetCustomizedContest";
        };

        return ReqTargetCustomizedContest;
    })();

    lq.ResActivityList = (function() {

        function ResActivityList(properties) {
            this.activities = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResActivityList.prototype.error = null;
        ResActivityList.prototype.activities = $util.emptyArray;

        ResActivityList.create = function create(properties) {
            return new ResActivityList(properties);
        };

        ResActivityList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.activities != null && message.activities.length)
                for (let i = 0; i < message.activities.length; ++i)
                    $root.lq.Activity.encode(message.activities[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResActivityList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResActivityList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResActivityList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activities && message.activities.length))
                            message.activities = [];
                        message.activities.push($root.lq.Activity.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResActivityList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResActivityList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.activities != null && message.hasOwnProperty("activities")) {
                if (!Array.isArray(message.activities))
                    return "activities: array expected";
                for (let i = 0; i < message.activities.length; ++i) {
                    let error = $root.lq.Activity.verify(message.activities[i], _depth + 1);
                    if (error)
                        return "activities." + error;
                }
            }
            return null;
        };

        ResActivityList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResActivityList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResActivityList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResActivityList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.activities) {
                if (!Array.isArray(object.activities))
                    throw TypeError(".lq.ResActivityList.activities: array expected");
                message.activities = Array(object.activities.length);
                for (let i = 0; i < object.activities.length; ++i) {
                    if (typeof object.activities[i] !== "object")
                        throw TypeError(".lq.ResActivityList.activities: object expected");
                    message.activities[i] = $root.lq.Activity.fromObject(object.activities[i], _depth + 1);
                }
            }
            return message;
        };

        ResActivityList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activities = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.activities && message.activities.length) {
                object.activities = Array(message.activities.length);
                for (let j = 0; j < message.activities.length; ++j)
                    object.activities[j] = $root.lq.Activity.toObject(message.activities[j], options);
            }
            return object;
        };

        ResActivityList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResActivityList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResActivityList";
        };

        return ResActivityList;
    })();

    lq.ResAccountActivityData = (function() {

        function ResAccountActivityData(properties) {
            this.exchange_records = [];
            this.task_progress_list = [];
            this.accumulated_point_list = [];
            this.rank_data_list = [];
            this.flip_task_progress_list = [];
            this.sign_in_data = [];
            this.richman_data = [];
            this.period_task_progress_list = [];
            this.random_task_progress_list = [];
            this.chest_up_data = [];
            this.mine_data = [];
            this.rpg_data = [];
            this.arena_data = [];
            this.feed_data = [];
            this.segment_task_progress_list = [];
            this.vote_records = [];
            this.spot_data = [];
            this.friend_gift_data = [];
            this.upgrade_data = [];
            this.gacha_data = [];
            this.simulation_data = [];
            this.combining_data = [];
            this.village_data = [];
            this.festival_data = [];
            this.island_data = [];
            this.story_data = [];
            this.choose_up_data = [];
            this.progress_reward_data = [];
            this.quest_crew_data = [];
            this.shoot_data = [];
            this.bingo_data = [];
            this.snowball_data = [];
            this.marathon_data = [];
            this.choose_group_up_data = [];
            this.mmo_data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAccountActivityData.prototype.error = null;
        ResAccountActivityData.prototype.exchange_records = $util.emptyArray;
        ResAccountActivityData.prototype.task_progress_list = $util.emptyArray;
        ResAccountActivityData.prototype.accumulated_point_list = $util.emptyArray;
        ResAccountActivityData.prototype.rank_data_list = $util.emptyArray;
        ResAccountActivityData.prototype.flip_task_progress_list = $util.emptyArray;
        ResAccountActivityData.prototype.sign_in_data = $util.emptyArray;
        ResAccountActivityData.prototype.richman_data = $util.emptyArray;
        ResAccountActivityData.prototype.period_task_progress_list = $util.emptyArray;
        ResAccountActivityData.prototype.random_task_progress_list = $util.emptyArray;
        ResAccountActivityData.prototype.chest_up_data = $util.emptyArray;
        ResAccountActivityData.prototype.sns_data = null;
        ResAccountActivityData.prototype.mine_data = $util.emptyArray;
        ResAccountActivityData.prototype.rpg_data = $util.emptyArray;
        ResAccountActivityData.prototype.arena_data = $util.emptyArray;
        ResAccountActivityData.prototype.feed_data = $util.emptyArray;
        ResAccountActivityData.prototype.segment_task_progress_list = $util.emptyArray;
        ResAccountActivityData.prototype.vote_records = $util.emptyArray;
        ResAccountActivityData.prototype.spot_data = $util.emptyArray;
        ResAccountActivityData.prototype.friend_gift_data = $util.emptyArray;
        ResAccountActivityData.prototype.upgrade_data = $util.emptyArray;
        ResAccountActivityData.prototype.gacha_data = $util.emptyArray;
        ResAccountActivityData.prototype.simulation_data = $util.emptyArray;
        ResAccountActivityData.prototype.combining_data = $util.emptyArray;
        ResAccountActivityData.prototype.village_data = $util.emptyArray;
        ResAccountActivityData.prototype.festival_data = $util.emptyArray;
        ResAccountActivityData.prototype.island_data = $util.emptyArray;
        ResAccountActivityData.prototype.story_data = $util.emptyArray;
        ResAccountActivityData.prototype.choose_up_data = $util.emptyArray;
        ResAccountActivityData.prototype.progress_reward_data = $util.emptyArray;
        ResAccountActivityData.prototype.quest_crew_data = $util.emptyArray;
        ResAccountActivityData.prototype.shoot_data = $util.emptyArray;
        ResAccountActivityData.prototype.bingo_data = $util.emptyArray;
        ResAccountActivityData.prototype.snowball_data = $util.emptyArray;
        ResAccountActivityData.prototype.marathon_data = $util.emptyArray;
        ResAccountActivityData.prototype.choose_group_up_data = $util.emptyArray;
        ResAccountActivityData.prototype.mmo_data = $util.emptyArray;

        ResAccountActivityData.create = function create(properties) {
            return new ResAccountActivityData(properties);
        };

        ResAccountActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.exchange_records != null && message.exchange_records.length)
                for (let i = 0; i < message.exchange_records.length; ++i)
                    $root.lq.ExchangeRecord.encode(message.exchange_records[i], writer.uint32(18).fork()).ldelim();
            if (message.task_progress_list != null && message.task_progress_list.length)
                for (let i = 0; i < message.task_progress_list.length; ++i)
                    $root.lq.TaskProgress.encode(message.task_progress_list[i], writer.uint32(26).fork()).ldelim();
            if (message.accumulated_point_list != null && message.accumulated_point_list.length)
                for (let i = 0; i < message.accumulated_point_list.length; ++i)
                    $root.lq.ActivityAccumulatedPointData.encode(message.accumulated_point_list[i], writer.uint32(34).fork()).ldelim();
            if (message.rank_data_list != null && message.rank_data_list.length)
                for (let i = 0; i < message.rank_data_list.length; ++i)
                    $root.lq.ActivityRankPointData.encode(message.rank_data_list[i], writer.uint32(42).fork()).ldelim();
            if (message.flip_task_progress_list != null && message.flip_task_progress_list.length)
                for (let i = 0; i < message.flip_task_progress_list.length; ++i)
                    $root.lq.TaskProgress.encode(message.flip_task_progress_list[i], writer.uint32(50).fork()).ldelim();
            if (message.sign_in_data != null && message.sign_in_data.length)
                for (let i = 0; i < message.sign_in_data.length; ++i)
                    $root.lq.ResAccountActivityData.ActivitySignInData.encode(message.sign_in_data[i], writer.uint32(58).fork()).ldelim();
            if (message.richman_data != null && message.richman_data.length)
                for (let i = 0; i < message.richman_data.length; ++i)
                    $root.lq.ResAccountActivityData.ActivityRichmanData.encode(message.richman_data[i], writer.uint32(66).fork()).ldelim();
            if (message.period_task_progress_list != null && message.period_task_progress_list.length)
                for (let i = 0; i < message.period_task_progress_list.length; ++i)
                    $root.lq.TaskProgress.encode(message.period_task_progress_list[i], writer.uint32(74).fork()).ldelim();
            if (message.random_task_progress_list != null && message.random_task_progress_list.length)
                for (let i = 0; i < message.random_task_progress_list.length; ++i)
                    $root.lq.TaskProgress.encode(message.random_task_progress_list[i], writer.uint32(82).fork()).ldelim();
            if (message.chest_up_data != null && message.chest_up_data.length)
                for (let i = 0; i < message.chest_up_data.length; ++i)
                    $root.lq.ResAccountActivityData.ChestUpData.encode(message.chest_up_data[i], writer.uint32(90).fork()).ldelim();
            if (message.sns_data != null && Object.hasOwnProperty.call(message, "sns_data"))
                $root.lq.ResAccountActivityData.ActivitySNSData.encode(message.sns_data, writer.uint32(98).fork()).ldelim();
            if (message.mine_data != null && message.mine_data.length)
                for (let i = 0; i < message.mine_data.length; ++i)
                    $root.lq.MineActivityData.encode(message.mine_data[i], writer.uint32(106).fork()).ldelim();
            if (message.rpg_data != null && message.rpg_data.length)
                for (let i = 0; i < message.rpg_data.length; ++i)
                    $root.lq.RPGActivity.encode(message.rpg_data[i], writer.uint32(114).fork()).ldelim();
            if (message.arena_data != null && message.arena_data.length)
                for (let i = 0; i < message.arena_data.length; ++i)
                    $root.lq.ActivityArenaData.encode(message.arena_data[i], writer.uint32(122).fork()).ldelim();
            if (message.feed_data != null && message.feed_data.length)
                for (let i = 0; i < message.feed_data.length; ++i)
                    $root.lq.FeedActivityData.encode(message.feed_data[i], writer.uint32(130).fork()).ldelim();
            if (message.segment_task_progress_list != null && message.segment_task_progress_list.length)
                for (let i = 0; i < message.segment_task_progress_list.length; ++i)
                    $root.lq.SegmentTaskProgress.encode(message.segment_task_progress_list[i], writer.uint32(138).fork()).ldelim();
            if (message.vote_records != null && message.vote_records.length)
                for (let i = 0; i < message.vote_records.length; ++i)
                    $root.lq.VoteData.encode(message.vote_records[i], writer.uint32(146).fork()).ldelim();
            if (message.spot_data != null && message.spot_data.length)
                for (let i = 0; i < message.spot_data.length; ++i)
                    $root.lq.ActivitySpotData.encode(message.spot_data[i], writer.uint32(154).fork()).ldelim();
            if (message.friend_gift_data != null && message.friend_gift_data.length)
                for (let i = 0; i < message.friend_gift_data.length; ++i)
                    $root.lq.ActivityFriendGiftData.encode(message.friend_gift_data[i], writer.uint32(162).fork()).ldelim();
            if (message.upgrade_data != null && message.upgrade_data.length)
                for (let i = 0; i < message.upgrade_data.length; ++i)
                    $root.lq.ActivityUpgradeData.encode(message.upgrade_data[i], writer.uint32(170).fork()).ldelim();
            if (message.gacha_data != null && message.gacha_data.length)
                for (let i = 0; i < message.gacha_data.length; ++i)
                    $root.lq.ActivityGachaUpdateData.encode(message.gacha_data[i], writer.uint32(178).fork()).ldelim();
            if (message.simulation_data != null && message.simulation_data.length)
                for (let i = 0; i < message.simulation_data.length; ++i)
                    $root.lq.ActivitySimulationData.encode(message.simulation_data[i], writer.uint32(186).fork()).ldelim();
            if (message.combining_data != null && message.combining_data.length)
                for (let i = 0; i < message.combining_data.length; ++i)
                    $root.lq.ActivityCombiningLQData.encode(message.combining_data[i], writer.uint32(194).fork()).ldelim();
            if (message.village_data != null && message.village_data.length)
                for (let i = 0; i < message.village_data.length; ++i)
                    $root.lq.ActivityVillageData.encode(message.village_data[i], writer.uint32(202).fork()).ldelim();
            if (message.festival_data != null && message.festival_data.length)
                for (let i = 0; i < message.festival_data.length; ++i)
                    $root.lq.ActivityFestivalData.encode(message.festival_data[i], writer.uint32(210).fork()).ldelim();
            if (message.island_data != null && message.island_data.length)
                for (let i = 0; i < message.island_data.length; ++i)
                    $root.lq.ActivityIslandData.encode(message.island_data[i], writer.uint32(218).fork()).ldelim();
            if (message.story_data != null && message.story_data.length)
                for (let i = 0; i < message.story_data.length; ++i)
                    $root.lq.ActivityStoryData.encode(message.story_data[i], writer.uint32(234).fork()).ldelim();
            if (message.choose_up_data != null && message.choose_up_data.length)
                for (let i = 0; i < message.choose_up_data.length; ++i)
                    $root.lq.ActivityChooseUpData.encode(message.choose_up_data[i], writer.uint32(242).fork()).ldelim();
            if (message.progress_reward_data != null && message.progress_reward_data.length)
                for (let i = 0; i < message.progress_reward_data.length; ++i)
                    $root.lq.ActivityProgressRewardData.encode(message.progress_reward_data[i], writer.uint32(258).fork()).ldelim();
            if (message.quest_crew_data != null && message.quest_crew_data.length)
                for (let i = 0; i < message.quest_crew_data.length; ++i)
                    $root.lq.ActivityQuestCrewData.encode(message.quest_crew_data[i], writer.uint32(266).fork()).ldelim();
            if (message.shoot_data != null && message.shoot_data.length)
                for (let i = 0; i < message.shoot_data.length; ++i)
                    $root.lq.ActivityShootData.encode(message.shoot_data[i], writer.uint32(274).fork()).ldelim();
            if (message.bingo_data != null && message.bingo_data.length)
                for (let i = 0; i < message.bingo_data.length; ++i)
                    $root.lq.ActivityBingoData.encode(message.bingo_data[i], writer.uint32(282).fork()).ldelim();
            if (message.snowball_data != null && message.snowball_data.length)
                for (let i = 0; i < message.snowball_data.length; ++i)
                    $root.lq.ActivitySnowballData.encode(message.snowball_data[i], writer.uint32(290).fork()).ldelim();
            if (message.marathon_data != null && message.marathon_data.length)
                for (let i = 0; i < message.marathon_data.length; ++i)
                    $root.lq.ActivityMarathonData.encode(message.marathon_data[i], writer.uint32(298).fork()).ldelim();
            if (message.choose_group_up_data != null && message.choose_group_up_data.length)
                for (let i = 0; i < message.choose_group_up_data.length; ++i)
                    $root.lq.ActivityChooseGroupData.encode(message.choose_group_up_data[i], writer.uint32(306).fork()).ldelim();
            if (message.mmo_data != null && message.mmo_data.length)
                for (let i = 0; i < message.mmo_data.length; ++i)
                    $root.lq.ActivityMMOData.encode(message.mmo_data[i], writer.uint32(314).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAccountActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAccountActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.exchange_records && message.exchange_records.length))
                            message.exchange_records = [];
                        message.exchange_records.push($root.lq.ExchangeRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.task_progress_list && message.task_progress_list.length))
                            message.task_progress_list = [];
                        message.task_progress_list.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.accumulated_point_list && message.accumulated_point_list.length))
                            message.accumulated_point_list = [];
                        message.accumulated_point_list.push($root.lq.ActivityAccumulatedPointData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank_data_list && message.rank_data_list.length))
                            message.rank_data_list = [];
                        message.rank_data_list.push($root.lq.ActivityRankPointData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.flip_task_progress_list && message.flip_task_progress_list.length))
                            message.flip_task_progress_list = [];
                        message.flip_task_progress_list.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sign_in_data && message.sign_in_data.length))
                            message.sign_in_data = [];
                        message.sign_in_data.push($root.lq.ResAccountActivityData.ActivitySignInData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.richman_data && message.richman_data.length))
                            message.richman_data = [];
                        message.richman_data.push($root.lq.ResAccountActivityData.ActivityRichmanData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.period_task_progress_list && message.period_task_progress_list.length))
                            message.period_task_progress_list = [];
                        message.period_task_progress_list.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.random_task_progress_list && message.random_task_progress_list.length))
                            message.random_task_progress_list = [];
                        message.random_task_progress_list.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.chest_up_data && message.chest_up_data.length))
                            message.chest_up_data = [];
                        message.chest_up_data.push($root.lq.ResAccountActivityData.ChestUpData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.sns_data = $root.lq.ResAccountActivityData.ActivitySNSData.decode(reader, reader.uint32(), undefined, _depth + 1, message.sns_data);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mine_data && message.mine_data.length))
                            message.mine_data = [];
                        message.mine_data.push($root.lq.MineActivityData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rpg_data && message.rpg_data.length))
                            message.rpg_data = [];
                        message.rpg_data.push($root.lq.RPGActivity.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.arena_data && message.arena_data.length))
                            message.arena_data = [];
                        message.arena_data.push($root.lq.ActivityArenaData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.feed_data && message.feed_data.length))
                            message.feed_data = [];
                        message.feed_data.push($root.lq.FeedActivityData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (!(message.segment_task_progress_list && message.segment_task_progress_list.length))
                            message.segment_task_progress_list = [];
                        message.segment_task_progress_list.push($root.lq.SegmentTaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if (!(message.vote_records && message.vote_records.length))
                            message.vote_records = [];
                        message.vote_records.push($root.lq.VoteData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.spot_data && message.spot_data.length))
                            message.spot_data = [];
                        message.spot_data.push($root.lq.ActivitySpotData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        if (!(message.friend_gift_data && message.friend_gift_data.length))
                            message.friend_gift_data = [];
                        message.friend_gift_data.push($root.lq.ActivityFriendGiftData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (!(message.upgrade_data && message.upgrade_data.length))
                            message.upgrade_data = [];
                        message.upgrade_data.push($root.lq.ActivityUpgradeData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gacha_data && message.gacha_data.length))
                            message.gacha_data = [];
                        message.gacha_data.push($root.lq.ActivityGachaUpdateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if (!(message.simulation_data && message.simulation_data.length))
                            message.simulation_data = [];
                        message.simulation_data.push($root.lq.ActivitySimulationData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        if (!(message.combining_data && message.combining_data.length))
                            message.combining_data = [];
                        message.combining_data.push($root.lq.ActivityCombiningLQData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if (!(message.village_data && message.village_data.length))
                            message.village_data = [];
                        message.village_data.push($root.lq.ActivityVillageData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if (!(message.festival_data && message.festival_data.length))
                            message.festival_data = [];
                        message.festival_data.push($root.lq.ActivityFestivalData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        if (!(message.island_data && message.island_data.length))
                            message.island_data = [];
                        message.island_data.push($root.lq.ActivityIslandData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        if (!(message.story_data && message.story_data.length))
                            message.story_data = [];
                        message.story_data.push($root.lq.ActivityStoryData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        if (!(message.choose_up_data && message.choose_up_data.length))
                            message.choose_up_data = [];
                        message.choose_up_data.push($root.lq.ActivityChooseUpData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        if (!(message.progress_reward_data && message.progress_reward_data.length))
                            message.progress_reward_data = [];
                        message.progress_reward_data.push($root.lq.ActivityProgressRewardData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        if (!(message.quest_crew_data && message.quest_crew_data.length))
                            message.quest_crew_data = [];
                        message.quest_crew_data.push($root.lq.ActivityQuestCrewData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        if (!(message.shoot_data && message.shoot_data.length))
                            message.shoot_data = [];
                        message.shoot_data.push($root.lq.ActivityShootData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 35: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bingo_data && message.bingo_data.length))
                            message.bingo_data = [];
                        message.bingo_data.push($root.lq.ActivityBingoData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        if (!(message.snowball_data && message.snowball_data.length))
                            message.snowball_data = [];
                        message.snowball_data.push($root.lq.ActivitySnowballData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        if (!(message.marathon_data && message.marathon_data.length))
                            message.marathon_data = [];
                        message.marathon_data.push($root.lq.ActivityMarathonData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 38: {
                        if (wireType !== 2)
                            break;
                        if (!(message.choose_group_up_data && message.choose_group_up_data.length))
                            message.choose_group_up_data = [];
                        message.choose_group_up_data.push($root.lq.ActivityChooseGroupData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 39: {
                        if (wireType !== 2)
                            break;
                        if (!(message.mmo_data && message.mmo_data.length))
                            message.mmo_data = [];
                        message.mmo_data.push($root.lq.ActivityMMOData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAccountActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAccountActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.exchange_records != null && message.hasOwnProperty("exchange_records")) {
                if (!Array.isArray(message.exchange_records))
                    return "exchange_records: array expected";
                for (let i = 0; i < message.exchange_records.length; ++i) {
                    let error = $root.lq.ExchangeRecord.verify(message.exchange_records[i], _depth + 1);
                    if (error)
                        return "exchange_records." + error;
                }
            }
            if (message.task_progress_list != null && message.hasOwnProperty("task_progress_list")) {
                if (!Array.isArray(message.task_progress_list))
                    return "task_progress_list: array expected";
                for (let i = 0; i < message.task_progress_list.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.task_progress_list[i], _depth + 1);
                    if (error)
                        return "task_progress_list." + error;
                }
            }
            if (message.accumulated_point_list != null && message.hasOwnProperty("accumulated_point_list")) {
                if (!Array.isArray(message.accumulated_point_list))
                    return "accumulated_point_list: array expected";
                for (let i = 0; i < message.accumulated_point_list.length; ++i) {
                    let error = $root.lq.ActivityAccumulatedPointData.verify(message.accumulated_point_list[i], _depth + 1);
                    if (error)
                        return "accumulated_point_list." + error;
                }
            }
            if (message.rank_data_list != null && message.hasOwnProperty("rank_data_list")) {
                if (!Array.isArray(message.rank_data_list))
                    return "rank_data_list: array expected";
                for (let i = 0; i < message.rank_data_list.length; ++i) {
                    let error = $root.lq.ActivityRankPointData.verify(message.rank_data_list[i], _depth + 1);
                    if (error)
                        return "rank_data_list." + error;
                }
            }
            if (message.flip_task_progress_list != null && message.hasOwnProperty("flip_task_progress_list")) {
                if (!Array.isArray(message.flip_task_progress_list))
                    return "flip_task_progress_list: array expected";
                for (let i = 0; i < message.flip_task_progress_list.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.flip_task_progress_list[i], _depth + 1);
                    if (error)
                        return "flip_task_progress_list." + error;
                }
            }
            if (message.sign_in_data != null && message.hasOwnProperty("sign_in_data")) {
                if (!Array.isArray(message.sign_in_data))
                    return "sign_in_data: array expected";
                for (let i = 0; i < message.sign_in_data.length; ++i) {
                    let error = $root.lq.ResAccountActivityData.ActivitySignInData.verify(message.sign_in_data[i], _depth + 1);
                    if (error)
                        return "sign_in_data." + error;
                }
            }
            if (message.richman_data != null && message.hasOwnProperty("richman_data")) {
                if (!Array.isArray(message.richman_data))
                    return "richman_data: array expected";
                for (let i = 0; i < message.richman_data.length; ++i) {
                    let error = $root.lq.ResAccountActivityData.ActivityRichmanData.verify(message.richman_data[i], _depth + 1);
                    if (error)
                        return "richman_data." + error;
                }
            }
            if (message.period_task_progress_list != null && message.hasOwnProperty("period_task_progress_list")) {
                if (!Array.isArray(message.period_task_progress_list))
                    return "period_task_progress_list: array expected";
                for (let i = 0; i < message.period_task_progress_list.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.period_task_progress_list[i], _depth + 1);
                    if (error)
                        return "period_task_progress_list." + error;
                }
            }
            if (message.random_task_progress_list != null && message.hasOwnProperty("random_task_progress_list")) {
                if (!Array.isArray(message.random_task_progress_list))
                    return "random_task_progress_list: array expected";
                for (let i = 0; i < message.random_task_progress_list.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.random_task_progress_list[i], _depth + 1);
                    if (error)
                        return "random_task_progress_list." + error;
                }
            }
            if (message.chest_up_data != null && message.hasOwnProperty("chest_up_data")) {
                if (!Array.isArray(message.chest_up_data))
                    return "chest_up_data: array expected";
                for (let i = 0; i < message.chest_up_data.length; ++i) {
                    let error = $root.lq.ResAccountActivityData.ChestUpData.verify(message.chest_up_data[i], _depth + 1);
                    if (error)
                        return "chest_up_data." + error;
                }
            }
            if (message.sns_data != null && message.hasOwnProperty("sns_data")) {
                let error = $root.lq.ResAccountActivityData.ActivitySNSData.verify(message.sns_data, _depth + 1);
                if (error)
                    return "sns_data." + error;
            }
            if (message.mine_data != null && message.hasOwnProperty("mine_data")) {
                if (!Array.isArray(message.mine_data))
                    return "mine_data: array expected";
                for (let i = 0; i < message.mine_data.length; ++i) {
                    let error = $root.lq.MineActivityData.verify(message.mine_data[i], _depth + 1);
                    if (error)
                        return "mine_data." + error;
                }
            }
            if (message.rpg_data != null && message.hasOwnProperty("rpg_data")) {
                if (!Array.isArray(message.rpg_data))
                    return "rpg_data: array expected";
                for (let i = 0; i < message.rpg_data.length; ++i) {
                    let error = $root.lq.RPGActivity.verify(message.rpg_data[i], _depth + 1);
                    if (error)
                        return "rpg_data." + error;
                }
            }
            if (message.arena_data != null && message.hasOwnProperty("arena_data")) {
                if (!Array.isArray(message.arena_data))
                    return "arena_data: array expected";
                for (let i = 0; i < message.arena_data.length; ++i) {
                    let error = $root.lq.ActivityArenaData.verify(message.arena_data[i], _depth + 1);
                    if (error)
                        return "arena_data." + error;
                }
            }
            if (message.feed_data != null && message.hasOwnProperty("feed_data")) {
                if (!Array.isArray(message.feed_data))
                    return "feed_data: array expected";
                for (let i = 0; i < message.feed_data.length; ++i) {
                    let error = $root.lq.FeedActivityData.verify(message.feed_data[i], _depth + 1);
                    if (error)
                        return "feed_data." + error;
                }
            }
            if (message.segment_task_progress_list != null && message.hasOwnProperty("segment_task_progress_list")) {
                if (!Array.isArray(message.segment_task_progress_list))
                    return "segment_task_progress_list: array expected";
                for (let i = 0; i < message.segment_task_progress_list.length; ++i) {
                    let error = $root.lq.SegmentTaskProgress.verify(message.segment_task_progress_list[i], _depth + 1);
                    if (error)
                        return "segment_task_progress_list." + error;
                }
            }
            if (message.vote_records != null && message.hasOwnProperty("vote_records")) {
                if (!Array.isArray(message.vote_records))
                    return "vote_records: array expected";
                for (let i = 0; i < message.vote_records.length; ++i) {
                    let error = $root.lq.VoteData.verify(message.vote_records[i], _depth + 1);
                    if (error)
                        return "vote_records." + error;
                }
            }
            if (message.spot_data != null && message.hasOwnProperty("spot_data")) {
                if (!Array.isArray(message.spot_data))
                    return "spot_data: array expected";
                for (let i = 0; i < message.spot_data.length; ++i) {
                    let error = $root.lq.ActivitySpotData.verify(message.spot_data[i], _depth + 1);
                    if (error)
                        return "spot_data." + error;
                }
            }
            if (message.friend_gift_data != null && message.hasOwnProperty("friend_gift_data")) {
                if (!Array.isArray(message.friend_gift_data))
                    return "friend_gift_data: array expected";
                for (let i = 0; i < message.friend_gift_data.length; ++i) {
                    let error = $root.lq.ActivityFriendGiftData.verify(message.friend_gift_data[i], _depth + 1);
                    if (error)
                        return "friend_gift_data." + error;
                }
            }
            if (message.upgrade_data != null && message.hasOwnProperty("upgrade_data")) {
                if (!Array.isArray(message.upgrade_data))
                    return "upgrade_data: array expected";
                for (let i = 0; i < message.upgrade_data.length; ++i) {
                    let error = $root.lq.ActivityUpgradeData.verify(message.upgrade_data[i], _depth + 1);
                    if (error)
                        return "upgrade_data." + error;
                }
            }
            if (message.gacha_data != null && message.hasOwnProperty("gacha_data")) {
                if (!Array.isArray(message.gacha_data))
                    return "gacha_data: array expected";
                for (let i = 0; i < message.gacha_data.length; ++i) {
                    let error = $root.lq.ActivityGachaUpdateData.verify(message.gacha_data[i], _depth + 1);
                    if (error)
                        return "gacha_data." + error;
                }
            }
            if (message.simulation_data != null && message.hasOwnProperty("simulation_data")) {
                if (!Array.isArray(message.simulation_data))
                    return "simulation_data: array expected";
                for (let i = 0; i < message.simulation_data.length; ++i) {
                    let error = $root.lq.ActivitySimulationData.verify(message.simulation_data[i], _depth + 1);
                    if (error)
                        return "simulation_data." + error;
                }
            }
            if (message.combining_data != null && message.hasOwnProperty("combining_data")) {
                if (!Array.isArray(message.combining_data))
                    return "combining_data: array expected";
                for (let i = 0; i < message.combining_data.length; ++i) {
                    let error = $root.lq.ActivityCombiningLQData.verify(message.combining_data[i], _depth + 1);
                    if (error)
                        return "combining_data." + error;
                }
            }
            if (message.village_data != null && message.hasOwnProperty("village_data")) {
                if (!Array.isArray(message.village_data))
                    return "village_data: array expected";
                for (let i = 0; i < message.village_data.length; ++i) {
                    let error = $root.lq.ActivityVillageData.verify(message.village_data[i], _depth + 1);
                    if (error)
                        return "village_data." + error;
                }
            }
            if (message.festival_data != null && message.hasOwnProperty("festival_data")) {
                if (!Array.isArray(message.festival_data))
                    return "festival_data: array expected";
                for (let i = 0; i < message.festival_data.length; ++i) {
                    let error = $root.lq.ActivityFestivalData.verify(message.festival_data[i], _depth + 1);
                    if (error)
                        return "festival_data." + error;
                }
            }
            if (message.island_data != null && message.hasOwnProperty("island_data")) {
                if (!Array.isArray(message.island_data))
                    return "island_data: array expected";
                for (let i = 0; i < message.island_data.length; ++i) {
                    let error = $root.lq.ActivityIslandData.verify(message.island_data[i], _depth + 1);
                    if (error)
                        return "island_data." + error;
                }
            }
            if (message.story_data != null && message.hasOwnProperty("story_data")) {
                if (!Array.isArray(message.story_data))
                    return "story_data: array expected";
                for (let i = 0; i < message.story_data.length; ++i) {
                    let error = $root.lq.ActivityStoryData.verify(message.story_data[i], _depth + 1);
                    if (error)
                        return "story_data." + error;
                }
            }
            if (message.choose_up_data != null && message.hasOwnProperty("choose_up_data")) {
                if (!Array.isArray(message.choose_up_data))
                    return "choose_up_data: array expected";
                for (let i = 0; i < message.choose_up_data.length; ++i) {
                    let error = $root.lq.ActivityChooseUpData.verify(message.choose_up_data[i], _depth + 1);
                    if (error)
                        return "choose_up_data." + error;
                }
            }
            if (message.progress_reward_data != null && message.hasOwnProperty("progress_reward_data")) {
                if (!Array.isArray(message.progress_reward_data))
                    return "progress_reward_data: array expected";
                for (let i = 0; i < message.progress_reward_data.length; ++i) {
                    let error = $root.lq.ActivityProgressRewardData.verify(message.progress_reward_data[i], _depth + 1);
                    if (error)
                        return "progress_reward_data." + error;
                }
            }
            if (message.quest_crew_data != null && message.hasOwnProperty("quest_crew_data")) {
                if (!Array.isArray(message.quest_crew_data))
                    return "quest_crew_data: array expected";
                for (let i = 0; i < message.quest_crew_data.length; ++i) {
                    let error = $root.lq.ActivityQuestCrewData.verify(message.quest_crew_data[i], _depth + 1);
                    if (error)
                        return "quest_crew_data." + error;
                }
            }
            if (message.shoot_data != null && message.hasOwnProperty("shoot_data")) {
                if (!Array.isArray(message.shoot_data))
                    return "shoot_data: array expected";
                for (let i = 0; i < message.shoot_data.length; ++i) {
                    let error = $root.lq.ActivityShootData.verify(message.shoot_data[i], _depth + 1);
                    if (error)
                        return "shoot_data." + error;
                }
            }
            if (message.bingo_data != null && message.hasOwnProperty("bingo_data")) {
                if (!Array.isArray(message.bingo_data))
                    return "bingo_data: array expected";
                for (let i = 0; i < message.bingo_data.length; ++i) {
                    let error = $root.lq.ActivityBingoData.verify(message.bingo_data[i], _depth + 1);
                    if (error)
                        return "bingo_data." + error;
                }
            }
            if (message.snowball_data != null && message.hasOwnProperty("snowball_data")) {
                if (!Array.isArray(message.snowball_data))
                    return "snowball_data: array expected";
                for (let i = 0; i < message.snowball_data.length; ++i) {
                    let error = $root.lq.ActivitySnowballData.verify(message.snowball_data[i], _depth + 1);
                    if (error)
                        return "snowball_data." + error;
                }
            }
            if (message.marathon_data != null && message.hasOwnProperty("marathon_data")) {
                if (!Array.isArray(message.marathon_data))
                    return "marathon_data: array expected";
                for (let i = 0; i < message.marathon_data.length; ++i) {
                    let error = $root.lq.ActivityMarathonData.verify(message.marathon_data[i], _depth + 1);
                    if (error)
                        return "marathon_data." + error;
                }
            }
            if (message.choose_group_up_data != null && message.hasOwnProperty("choose_group_up_data")) {
                if (!Array.isArray(message.choose_group_up_data))
                    return "choose_group_up_data: array expected";
                for (let i = 0; i < message.choose_group_up_data.length; ++i) {
                    let error = $root.lq.ActivityChooseGroupData.verify(message.choose_group_up_data[i], _depth + 1);
                    if (error)
                        return "choose_group_up_data." + error;
                }
            }
            if (message.mmo_data != null && message.hasOwnProperty("mmo_data")) {
                if (!Array.isArray(message.mmo_data))
                    return "mmo_data: array expected";
                for (let i = 0; i < message.mmo_data.length; ++i) {
                    let error = $root.lq.ActivityMMOData.verify(message.mmo_data[i], _depth + 1);
                    if (error)
                        return "mmo_data." + error;
                }
            }
            return null;
        };

        ResAccountActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAccountActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAccountActivityData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAccountActivityData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.exchange_records) {
                if (!Array.isArray(object.exchange_records))
                    throw TypeError(".lq.ResAccountActivityData.exchange_records: array expected");
                message.exchange_records = Array(object.exchange_records.length);
                for (let i = 0; i < object.exchange_records.length; ++i) {
                    if (typeof object.exchange_records[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.exchange_records: object expected");
                    message.exchange_records[i] = $root.lq.ExchangeRecord.fromObject(object.exchange_records[i], _depth + 1);
                }
            }
            if (object.task_progress_list) {
                if (!Array.isArray(object.task_progress_list))
                    throw TypeError(".lq.ResAccountActivityData.task_progress_list: array expected");
                message.task_progress_list = Array(object.task_progress_list.length);
                for (let i = 0; i < object.task_progress_list.length; ++i) {
                    if (typeof object.task_progress_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.task_progress_list: object expected");
                    message.task_progress_list[i] = $root.lq.TaskProgress.fromObject(object.task_progress_list[i], _depth + 1);
                }
            }
            if (object.accumulated_point_list) {
                if (!Array.isArray(object.accumulated_point_list))
                    throw TypeError(".lq.ResAccountActivityData.accumulated_point_list: array expected");
                message.accumulated_point_list = Array(object.accumulated_point_list.length);
                for (let i = 0; i < object.accumulated_point_list.length; ++i) {
                    if (typeof object.accumulated_point_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.accumulated_point_list: object expected");
                    message.accumulated_point_list[i] = $root.lq.ActivityAccumulatedPointData.fromObject(object.accumulated_point_list[i], _depth + 1);
                }
            }
            if (object.rank_data_list) {
                if (!Array.isArray(object.rank_data_list))
                    throw TypeError(".lq.ResAccountActivityData.rank_data_list: array expected");
                message.rank_data_list = Array(object.rank_data_list.length);
                for (let i = 0; i < object.rank_data_list.length; ++i) {
                    if (typeof object.rank_data_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.rank_data_list: object expected");
                    message.rank_data_list[i] = $root.lq.ActivityRankPointData.fromObject(object.rank_data_list[i], _depth + 1);
                }
            }
            if (object.flip_task_progress_list) {
                if (!Array.isArray(object.flip_task_progress_list))
                    throw TypeError(".lq.ResAccountActivityData.flip_task_progress_list: array expected");
                message.flip_task_progress_list = Array(object.flip_task_progress_list.length);
                for (let i = 0; i < object.flip_task_progress_list.length; ++i) {
                    if (typeof object.flip_task_progress_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.flip_task_progress_list: object expected");
                    message.flip_task_progress_list[i] = $root.lq.TaskProgress.fromObject(object.flip_task_progress_list[i], _depth + 1);
                }
            }
            if (object.sign_in_data) {
                if (!Array.isArray(object.sign_in_data))
                    throw TypeError(".lq.ResAccountActivityData.sign_in_data: array expected");
                message.sign_in_data = Array(object.sign_in_data.length);
                for (let i = 0; i < object.sign_in_data.length; ++i) {
                    if (typeof object.sign_in_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.sign_in_data: object expected");
                    message.sign_in_data[i] = $root.lq.ResAccountActivityData.ActivitySignInData.fromObject(object.sign_in_data[i], _depth + 1);
                }
            }
            if (object.richman_data) {
                if (!Array.isArray(object.richman_data))
                    throw TypeError(".lq.ResAccountActivityData.richman_data: array expected");
                message.richman_data = Array(object.richman_data.length);
                for (let i = 0; i < object.richman_data.length; ++i) {
                    if (typeof object.richman_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.richman_data: object expected");
                    message.richman_data[i] = $root.lq.ResAccountActivityData.ActivityRichmanData.fromObject(object.richman_data[i], _depth + 1);
                }
            }
            if (object.period_task_progress_list) {
                if (!Array.isArray(object.period_task_progress_list))
                    throw TypeError(".lq.ResAccountActivityData.period_task_progress_list: array expected");
                message.period_task_progress_list = Array(object.period_task_progress_list.length);
                for (let i = 0; i < object.period_task_progress_list.length; ++i) {
                    if (typeof object.period_task_progress_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.period_task_progress_list: object expected");
                    message.period_task_progress_list[i] = $root.lq.TaskProgress.fromObject(object.period_task_progress_list[i], _depth + 1);
                }
            }
            if (object.random_task_progress_list) {
                if (!Array.isArray(object.random_task_progress_list))
                    throw TypeError(".lq.ResAccountActivityData.random_task_progress_list: array expected");
                message.random_task_progress_list = Array(object.random_task_progress_list.length);
                for (let i = 0; i < object.random_task_progress_list.length; ++i) {
                    if (typeof object.random_task_progress_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.random_task_progress_list: object expected");
                    message.random_task_progress_list[i] = $root.lq.TaskProgress.fromObject(object.random_task_progress_list[i], _depth + 1);
                }
            }
            if (object.chest_up_data) {
                if (!Array.isArray(object.chest_up_data))
                    throw TypeError(".lq.ResAccountActivityData.chest_up_data: array expected");
                message.chest_up_data = Array(object.chest_up_data.length);
                for (let i = 0; i < object.chest_up_data.length; ++i) {
                    if (typeof object.chest_up_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.chest_up_data: object expected");
                    message.chest_up_data[i] = $root.lq.ResAccountActivityData.ChestUpData.fromObject(object.chest_up_data[i], _depth + 1);
                }
            }
            if (object.sns_data != null) {
                if (typeof object.sns_data !== "object")
                    throw TypeError(".lq.ResAccountActivityData.sns_data: object expected");
                message.sns_data = $root.lq.ResAccountActivityData.ActivitySNSData.fromObject(object.sns_data, _depth + 1);
            }
            if (object.mine_data) {
                if (!Array.isArray(object.mine_data))
                    throw TypeError(".lq.ResAccountActivityData.mine_data: array expected");
                message.mine_data = Array(object.mine_data.length);
                for (let i = 0; i < object.mine_data.length; ++i) {
                    if (typeof object.mine_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.mine_data: object expected");
                    message.mine_data[i] = $root.lq.MineActivityData.fromObject(object.mine_data[i], _depth + 1);
                }
            }
            if (object.rpg_data) {
                if (!Array.isArray(object.rpg_data))
                    throw TypeError(".lq.ResAccountActivityData.rpg_data: array expected");
                message.rpg_data = Array(object.rpg_data.length);
                for (let i = 0; i < object.rpg_data.length; ++i) {
                    if (typeof object.rpg_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.rpg_data: object expected");
                    message.rpg_data[i] = $root.lq.RPGActivity.fromObject(object.rpg_data[i], _depth + 1);
                }
            }
            if (object.arena_data) {
                if (!Array.isArray(object.arena_data))
                    throw TypeError(".lq.ResAccountActivityData.arena_data: array expected");
                message.arena_data = Array(object.arena_data.length);
                for (let i = 0; i < object.arena_data.length; ++i) {
                    if (typeof object.arena_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.arena_data: object expected");
                    message.arena_data[i] = $root.lq.ActivityArenaData.fromObject(object.arena_data[i], _depth + 1);
                }
            }
            if (object.feed_data) {
                if (!Array.isArray(object.feed_data))
                    throw TypeError(".lq.ResAccountActivityData.feed_data: array expected");
                message.feed_data = Array(object.feed_data.length);
                for (let i = 0; i < object.feed_data.length; ++i) {
                    if (typeof object.feed_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.feed_data: object expected");
                    message.feed_data[i] = $root.lq.FeedActivityData.fromObject(object.feed_data[i], _depth + 1);
                }
            }
            if (object.segment_task_progress_list) {
                if (!Array.isArray(object.segment_task_progress_list))
                    throw TypeError(".lq.ResAccountActivityData.segment_task_progress_list: array expected");
                message.segment_task_progress_list = Array(object.segment_task_progress_list.length);
                for (let i = 0; i < object.segment_task_progress_list.length; ++i) {
                    if (typeof object.segment_task_progress_list[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.segment_task_progress_list: object expected");
                    message.segment_task_progress_list[i] = $root.lq.SegmentTaskProgress.fromObject(object.segment_task_progress_list[i], _depth + 1);
                }
            }
            if (object.vote_records) {
                if (!Array.isArray(object.vote_records))
                    throw TypeError(".lq.ResAccountActivityData.vote_records: array expected");
                message.vote_records = Array(object.vote_records.length);
                for (let i = 0; i < object.vote_records.length; ++i) {
                    if (typeof object.vote_records[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.vote_records: object expected");
                    message.vote_records[i] = $root.lq.VoteData.fromObject(object.vote_records[i], _depth + 1);
                }
            }
            if (object.spot_data) {
                if (!Array.isArray(object.spot_data))
                    throw TypeError(".lq.ResAccountActivityData.spot_data: array expected");
                message.spot_data = Array(object.spot_data.length);
                for (let i = 0; i < object.spot_data.length; ++i) {
                    if (typeof object.spot_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.spot_data: object expected");
                    message.spot_data[i] = $root.lq.ActivitySpotData.fromObject(object.spot_data[i], _depth + 1);
                }
            }
            if (object.friend_gift_data) {
                if (!Array.isArray(object.friend_gift_data))
                    throw TypeError(".lq.ResAccountActivityData.friend_gift_data: array expected");
                message.friend_gift_data = Array(object.friend_gift_data.length);
                for (let i = 0; i < object.friend_gift_data.length; ++i) {
                    if (typeof object.friend_gift_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.friend_gift_data: object expected");
                    message.friend_gift_data[i] = $root.lq.ActivityFriendGiftData.fromObject(object.friend_gift_data[i], _depth + 1);
                }
            }
            if (object.upgrade_data) {
                if (!Array.isArray(object.upgrade_data))
                    throw TypeError(".lq.ResAccountActivityData.upgrade_data: array expected");
                message.upgrade_data = Array(object.upgrade_data.length);
                for (let i = 0; i < object.upgrade_data.length; ++i) {
                    if (typeof object.upgrade_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.upgrade_data: object expected");
                    message.upgrade_data[i] = $root.lq.ActivityUpgradeData.fromObject(object.upgrade_data[i], _depth + 1);
                }
            }
            if (object.gacha_data) {
                if (!Array.isArray(object.gacha_data))
                    throw TypeError(".lq.ResAccountActivityData.gacha_data: array expected");
                message.gacha_data = Array(object.gacha_data.length);
                for (let i = 0; i < object.gacha_data.length; ++i) {
                    if (typeof object.gacha_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.gacha_data: object expected");
                    message.gacha_data[i] = $root.lq.ActivityGachaUpdateData.fromObject(object.gacha_data[i], _depth + 1);
                }
            }
            if (object.simulation_data) {
                if (!Array.isArray(object.simulation_data))
                    throw TypeError(".lq.ResAccountActivityData.simulation_data: array expected");
                message.simulation_data = Array(object.simulation_data.length);
                for (let i = 0; i < object.simulation_data.length; ++i) {
                    if (typeof object.simulation_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.simulation_data: object expected");
                    message.simulation_data[i] = $root.lq.ActivitySimulationData.fromObject(object.simulation_data[i], _depth + 1);
                }
            }
            if (object.combining_data) {
                if (!Array.isArray(object.combining_data))
                    throw TypeError(".lq.ResAccountActivityData.combining_data: array expected");
                message.combining_data = Array(object.combining_data.length);
                for (let i = 0; i < object.combining_data.length; ++i) {
                    if (typeof object.combining_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.combining_data: object expected");
                    message.combining_data[i] = $root.lq.ActivityCombiningLQData.fromObject(object.combining_data[i], _depth + 1);
                }
            }
            if (object.village_data) {
                if (!Array.isArray(object.village_data))
                    throw TypeError(".lq.ResAccountActivityData.village_data: array expected");
                message.village_data = Array(object.village_data.length);
                for (let i = 0; i < object.village_data.length; ++i) {
                    if (typeof object.village_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.village_data: object expected");
                    message.village_data[i] = $root.lq.ActivityVillageData.fromObject(object.village_data[i], _depth + 1);
                }
            }
            if (object.festival_data) {
                if (!Array.isArray(object.festival_data))
                    throw TypeError(".lq.ResAccountActivityData.festival_data: array expected");
                message.festival_data = Array(object.festival_data.length);
                for (let i = 0; i < object.festival_data.length; ++i) {
                    if (typeof object.festival_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.festival_data: object expected");
                    message.festival_data[i] = $root.lq.ActivityFestivalData.fromObject(object.festival_data[i], _depth + 1);
                }
            }
            if (object.island_data) {
                if (!Array.isArray(object.island_data))
                    throw TypeError(".lq.ResAccountActivityData.island_data: array expected");
                message.island_data = Array(object.island_data.length);
                for (let i = 0; i < object.island_data.length; ++i) {
                    if (typeof object.island_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.island_data: object expected");
                    message.island_data[i] = $root.lq.ActivityIslandData.fromObject(object.island_data[i], _depth + 1);
                }
            }
            if (object.story_data) {
                if (!Array.isArray(object.story_data))
                    throw TypeError(".lq.ResAccountActivityData.story_data: array expected");
                message.story_data = Array(object.story_data.length);
                for (let i = 0; i < object.story_data.length; ++i) {
                    if (typeof object.story_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.story_data: object expected");
                    message.story_data[i] = $root.lq.ActivityStoryData.fromObject(object.story_data[i], _depth + 1);
                }
            }
            if (object.choose_up_data) {
                if (!Array.isArray(object.choose_up_data))
                    throw TypeError(".lq.ResAccountActivityData.choose_up_data: array expected");
                message.choose_up_data = Array(object.choose_up_data.length);
                for (let i = 0; i < object.choose_up_data.length; ++i) {
                    if (typeof object.choose_up_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.choose_up_data: object expected");
                    message.choose_up_data[i] = $root.lq.ActivityChooseUpData.fromObject(object.choose_up_data[i], _depth + 1);
                }
            }
            if (object.progress_reward_data) {
                if (!Array.isArray(object.progress_reward_data))
                    throw TypeError(".lq.ResAccountActivityData.progress_reward_data: array expected");
                message.progress_reward_data = Array(object.progress_reward_data.length);
                for (let i = 0; i < object.progress_reward_data.length; ++i) {
                    if (typeof object.progress_reward_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.progress_reward_data: object expected");
                    message.progress_reward_data[i] = $root.lq.ActivityProgressRewardData.fromObject(object.progress_reward_data[i], _depth + 1);
                }
            }
            if (object.quest_crew_data) {
                if (!Array.isArray(object.quest_crew_data))
                    throw TypeError(".lq.ResAccountActivityData.quest_crew_data: array expected");
                message.quest_crew_data = Array(object.quest_crew_data.length);
                for (let i = 0; i < object.quest_crew_data.length; ++i) {
                    if (typeof object.quest_crew_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.quest_crew_data: object expected");
                    message.quest_crew_data[i] = $root.lq.ActivityQuestCrewData.fromObject(object.quest_crew_data[i], _depth + 1);
                }
            }
            if (object.shoot_data) {
                if (!Array.isArray(object.shoot_data))
                    throw TypeError(".lq.ResAccountActivityData.shoot_data: array expected");
                message.shoot_data = Array(object.shoot_data.length);
                for (let i = 0; i < object.shoot_data.length; ++i) {
                    if (typeof object.shoot_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.shoot_data: object expected");
                    message.shoot_data[i] = $root.lq.ActivityShootData.fromObject(object.shoot_data[i], _depth + 1);
                }
            }
            if (object.bingo_data) {
                if (!Array.isArray(object.bingo_data))
                    throw TypeError(".lq.ResAccountActivityData.bingo_data: array expected");
                message.bingo_data = Array(object.bingo_data.length);
                for (let i = 0; i < object.bingo_data.length; ++i) {
                    if (typeof object.bingo_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.bingo_data: object expected");
                    message.bingo_data[i] = $root.lq.ActivityBingoData.fromObject(object.bingo_data[i], _depth + 1);
                }
            }
            if (object.snowball_data) {
                if (!Array.isArray(object.snowball_data))
                    throw TypeError(".lq.ResAccountActivityData.snowball_data: array expected");
                message.snowball_data = Array(object.snowball_data.length);
                for (let i = 0; i < object.snowball_data.length; ++i) {
                    if (typeof object.snowball_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.snowball_data: object expected");
                    message.snowball_data[i] = $root.lq.ActivitySnowballData.fromObject(object.snowball_data[i], _depth + 1);
                }
            }
            if (object.marathon_data) {
                if (!Array.isArray(object.marathon_data))
                    throw TypeError(".lq.ResAccountActivityData.marathon_data: array expected");
                message.marathon_data = Array(object.marathon_data.length);
                for (let i = 0; i < object.marathon_data.length; ++i) {
                    if (typeof object.marathon_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.marathon_data: object expected");
                    message.marathon_data[i] = $root.lq.ActivityMarathonData.fromObject(object.marathon_data[i], _depth + 1);
                }
            }
            if (object.choose_group_up_data) {
                if (!Array.isArray(object.choose_group_up_data))
                    throw TypeError(".lq.ResAccountActivityData.choose_group_up_data: array expected");
                message.choose_group_up_data = Array(object.choose_group_up_data.length);
                for (let i = 0; i < object.choose_group_up_data.length; ++i) {
                    if (typeof object.choose_group_up_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.choose_group_up_data: object expected");
                    message.choose_group_up_data[i] = $root.lq.ActivityChooseGroupData.fromObject(object.choose_group_up_data[i], _depth + 1);
                }
            }
            if (object.mmo_data) {
                if (!Array.isArray(object.mmo_data))
                    throw TypeError(".lq.ResAccountActivityData.mmo_data: array expected");
                message.mmo_data = Array(object.mmo_data.length);
                for (let i = 0; i < object.mmo_data.length; ++i) {
                    if (typeof object.mmo_data[i] !== "object")
                        throw TypeError(".lq.ResAccountActivityData.mmo_data: object expected");
                    message.mmo_data[i] = $root.lq.ActivityMMOData.fromObject(object.mmo_data[i], _depth + 1);
                }
            }
            return message;
        };

        ResAccountActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.exchange_records = [];
                object.task_progress_list = [];
                object.accumulated_point_list = [];
                object.rank_data_list = [];
                object.flip_task_progress_list = [];
                object.sign_in_data = [];
                object.richman_data = [];
                object.period_task_progress_list = [];
                object.random_task_progress_list = [];
                object.chest_up_data = [];
                object.mine_data = [];
                object.rpg_data = [];
                object.arena_data = [];
                object.feed_data = [];
                object.segment_task_progress_list = [];
                object.vote_records = [];
                object.spot_data = [];
                object.friend_gift_data = [];
                object.upgrade_data = [];
                object.gacha_data = [];
                object.simulation_data = [];
                object.combining_data = [];
                object.village_data = [];
                object.festival_data = [];
                object.island_data = [];
                object.story_data = [];
                object.choose_up_data = [];
                object.progress_reward_data = [];
                object.quest_crew_data = [];
                object.shoot_data = [];
                object.bingo_data = [];
                object.snowball_data = [];
                object.marathon_data = [];
                object.choose_group_up_data = [];
                object.mmo_data = [];
            }
            if (options.defaults) {
                object.error = null;
                object.sns_data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.exchange_records && message.exchange_records.length) {
                object.exchange_records = Array(message.exchange_records.length);
                for (let j = 0; j < message.exchange_records.length; ++j)
                    object.exchange_records[j] = $root.lq.ExchangeRecord.toObject(message.exchange_records[j], options);
            }
            if (message.task_progress_list && message.task_progress_list.length) {
                object.task_progress_list = Array(message.task_progress_list.length);
                for (let j = 0; j < message.task_progress_list.length; ++j)
                    object.task_progress_list[j] = $root.lq.TaskProgress.toObject(message.task_progress_list[j], options);
            }
            if (message.accumulated_point_list && message.accumulated_point_list.length) {
                object.accumulated_point_list = Array(message.accumulated_point_list.length);
                for (let j = 0; j < message.accumulated_point_list.length; ++j)
                    object.accumulated_point_list[j] = $root.lq.ActivityAccumulatedPointData.toObject(message.accumulated_point_list[j], options);
            }
            if (message.rank_data_list && message.rank_data_list.length) {
                object.rank_data_list = Array(message.rank_data_list.length);
                for (let j = 0; j < message.rank_data_list.length; ++j)
                    object.rank_data_list[j] = $root.lq.ActivityRankPointData.toObject(message.rank_data_list[j], options);
            }
            if (message.flip_task_progress_list && message.flip_task_progress_list.length) {
                object.flip_task_progress_list = Array(message.flip_task_progress_list.length);
                for (let j = 0; j < message.flip_task_progress_list.length; ++j)
                    object.flip_task_progress_list[j] = $root.lq.TaskProgress.toObject(message.flip_task_progress_list[j], options);
            }
            if (message.sign_in_data && message.sign_in_data.length) {
                object.sign_in_data = Array(message.sign_in_data.length);
                for (let j = 0; j < message.sign_in_data.length; ++j)
                    object.sign_in_data[j] = $root.lq.ResAccountActivityData.ActivitySignInData.toObject(message.sign_in_data[j], options);
            }
            if (message.richman_data && message.richman_data.length) {
                object.richman_data = Array(message.richman_data.length);
                for (let j = 0; j < message.richman_data.length; ++j)
                    object.richman_data[j] = $root.lq.ResAccountActivityData.ActivityRichmanData.toObject(message.richman_data[j], options);
            }
            if (message.period_task_progress_list && message.period_task_progress_list.length) {
                object.period_task_progress_list = Array(message.period_task_progress_list.length);
                for (let j = 0; j < message.period_task_progress_list.length; ++j)
                    object.period_task_progress_list[j] = $root.lq.TaskProgress.toObject(message.period_task_progress_list[j], options);
            }
            if (message.random_task_progress_list && message.random_task_progress_list.length) {
                object.random_task_progress_list = Array(message.random_task_progress_list.length);
                for (let j = 0; j < message.random_task_progress_list.length; ++j)
                    object.random_task_progress_list[j] = $root.lq.TaskProgress.toObject(message.random_task_progress_list[j], options);
            }
            if (message.chest_up_data && message.chest_up_data.length) {
                object.chest_up_data = Array(message.chest_up_data.length);
                for (let j = 0; j < message.chest_up_data.length; ++j)
                    object.chest_up_data[j] = $root.lq.ResAccountActivityData.ChestUpData.toObject(message.chest_up_data[j], options);
            }
            if (message.sns_data != null && message.hasOwnProperty("sns_data"))
                object.sns_data = $root.lq.ResAccountActivityData.ActivitySNSData.toObject(message.sns_data, options);
            if (message.mine_data && message.mine_data.length) {
                object.mine_data = Array(message.mine_data.length);
                for (let j = 0; j < message.mine_data.length; ++j)
                    object.mine_data[j] = $root.lq.MineActivityData.toObject(message.mine_data[j], options);
            }
            if (message.rpg_data && message.rpg_data.length) {
                object.rpg_data = Array(message.rpg_data.length);
                for (let j = 0; j < message.rpg_data.length; ++j)
                    object.rpg_data[j] = $root.lq.RPGActivity.toObject(message.rpg_data[j], options);
            }
            if (message.arena_data && message.arena_data.length) {
                object.arena_data = Array(message.arena_data.length);
                for (let j = 0; j < message.arena_data.length; ++j)
                    object.arena_data[j] = $root.lq.ActivityArenaData.toObject(message.arena_data[j], options);
            }
            if (message.feed_data && message.feed_data.length) {
                object.feed_data = Array(message.feed_data.length);
                for (let j = 0; j < message.feed_data.length; ++j)
                    object.feed_data[j] = $root.lq.FeedActivityData.toObject(message.feed_data[j], options);
            }
            if (message.segment_task_progress_list && message.segment_task_progress_list.length) {
                object.segment_task_progress_list = Array(message.segment_task_progress_list.length);
                for (let j = 0; j < message.segment_task_progress_list.length; ++j)
                    object.segment_task_progress_list[j] = $root.lq.SegmentTaskProgress.toObject(message.segment_task_progress_list[j], options);
            }
            if (message.vote_records && message.vote_records.length) {
                object.vote_records = Array(message.vote_records.length);
                for (let j = 0; j < message.vote_records.length; ++j)
                    object.vote_records[j] = $root.lq.VoteData.toObject(message.vote_records[j], options);
            }
            if (message.spot_data && message.spot_data.length) {
                object.spot_data = Array(message.spot_data.length);
                for (let j = 0; j < message.spot_data.length; ++j)
                    object.spot_data[j] = $root.lq.ActivitySpotData.toObject(message.spot_data[j], options);
            }
            if (message.friend_gift_data && message.friend_gift_data.length) {
                object.friend_gift_data = Array(message.friend_gift_data.length);
                for (let j = 0; j < message.friend_gift_data.length; ++j)
                    object.friend_gift_data[j] = $root.lq.ActivityFriendGiftData.toObject(message.friend_gift_data[j], options);
            }
            if (message.upgrade_data && message.upgrade_data.length) {
                object.upgrade_data = Array(message.upgrade_data.length);
                for (let j = 0; j < message.upgrade_data.length; ++j)
                    object.upgrade_data[j] = $root.lq.ActivityUpgradeData.toObject(message.upgrade_data[j], options);
            }
            if (message.gacha_data && message.gacha_data.length) {
                object.gacha_data = Array(message.gacha_data.length);
                for (let j = 0; j < message.gacha_data.length; ++j)
                    object.gacha_data[j] = $root.lq.ActivityGachaUpdateData.toObject(message.gacha_data[j], options);
            }
            if (message.simulation_data && message.simulation_data.length) {
                object.simulation_data = Array(message.simulation_data.length);
                for (let j = 0; j < message.simulation_data.length; ++j)
                    object.simulation_data[j] = $root.lq.ActivitySimulationData.toObject(message.simulation_data[j], options);
            }
            if (message.combining_data && message.combining_data.length) {
                object.combining_data = Array(message.combining_data.length);
                for (let j = 0; j < message.combining_data.length; ++j)
                    object.combining_data[j] = $root.lq.ActivityCombiningLQData.toObject(message.combining_data[j], options);
            }
            if (message.village_data && message.village_data.length) {
                object.village_data = Array(message.village_data.length);
                for (let j = 0; j < message.village_data.length; ++j)
                    object.village_data[j] = $root.lq.ActivityVillageData.toObject(message.village_data[j], options);
            }
            if (message.festival_data && message.festival_data.length) {
                object.festival_data = Array(message.festival_data.length);
                for (let j = 0; j < message.festival_data.length; ++j)
                    object.festival_data[j] = $root.lq.ActivityFestivalData.toObject(message.festival_data[j], options);
            }
            if (message.island_data && message.island_data.length) {
                object.island_data = Array(message.island_data.length);
                for (let j = 0; j < message.island_data.length; ++j)
                    object.island_data[j] = $root.lq.ActivityIslandData.toObject(message.island_data[j], options);
            }
            if (message.story_data && message.story_data.length) {
                object.story_data = Array(message.story_data.length);
                for (let j = 0; j < message.story_data.length; ++j)
                    object.story_data[j] = $root.lq.ActivityStoryData.toObject(message.story_data[j], options);
            }
            if (message.choose_up_data && message.choose_up_data.length) {
                object.choose_up_data = Array(message.choose_up_data.length);
                for (let j = 0; j < message.choose_up_data.length; ++j)
                    object.choose_up_data[j] = $root.lq.ActivityChooseUpData.toObject(message.choose_up_data[j], options);
            }
            if (message.progress_reward_data && message.progress_reward_data.length) {
                object.progress_reward_data = Array(message.progress_reward_data.length);
                for (let j = 0; j < message.progress_reward_data.length; ++j)
                    object.progress_reward_data[j] = $root.lq.ActivityProgressRewardData.toObject(message.progress_reward_data[j], options);
            }
            if (message.quest_crew_data && message.quest_crew_data.length) {
                object.quest_crew_data = Array(message.quest_crew_data.length);
                for (let j = 0; j < message.quest_crew_data.length; ++j)
                    object.quest_crew_data[j] = $root.lq.ActivityQuestCrewData.toObject(message.quest_crew_data[j], options);
            }
            if (message.shoot_data && message.shoot_data.length) {
                object.shoot_data = Array(message.shoot_data.length);
                for (let j = 0; j < message.shoot_data.length; ++j)
                    object.shoot_data[j] = $root.lq.ActivityShootData.toObject(message.shoot_data[j], options);
            }
            if (message.bingo_data && message.bingo_data.length) {
                object.bingo_data = Array(message.bingo_data.length);
                for (let j = 0; j < message.bingo_data.length; ++j)
                    object.bingo_data[j] = $root.lq.ActivityBingoData.toObject(message.bingo_data[j], options);
            }
            if (message.snowball_data && message.snowball_data.length) {
                object.snowball_data = Array(message.snowball_data.length);
                for (let j = 0; j < message.snowball_data.length; ++j)
                    object.snowball_data[j] = $root.lq.ActivitySnowballData.toObject(message.snowball_data[j], options);
            }
            if (message.marathon_data && message.marathon_data.length) {
                object.marathon_data = Array(message.marathon_data.length);
                for (let j = 0; j < message.marathon_data.length; ++j)
                    object.marathon_data[j] = $root.lq.ActivityMarathonData.toObject(message.marathon_data[j], options);
            }
            if (message.choose_group_up_data && message.choose_group_up_data.length) {
                object.choose_group_up_data = Array(message.choose_group_up_data.length);
                for (let j = 0; j < message.choose_group_up_data.length; ++j)
                    object.choose_group_up_data[j] = $root.lq.ActivityChooseGroupData.toObject(message.choose_group_up_data[j], options);
            }
            if (message.mmo_data && message.mmo_data.length) {
                object.mmo_data = Array(message.mmo_data.length);
                for (let j = 0; j < message.mmo_data.length; ++j)
                    object.mmo_data[j] = $root.lq.ActivityMMOData.toObject(message.mmo_data[j], options);
            }
            return object;
        };

        ResAccountActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAccountActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAccountActivityData";
        };

        ResAccountActivityData.ActivitySignInData = (function() {

            function ActivitySignInData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivitySignInData.prototype.activity_id = 0;
            ActivitySignInData.prototype.sign_in_count = 0;
            ActivitySignInData.prototype.last_sign_in_time = 0;

            ActivitySignInData.create = function create(properties) {
                return new ActivitySignInData(properties);
            };

            ActivitySignInData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.sign_in_count != null && Object.hasOwnProperty.call(message, "sign_in_count"))
                    writer.uint32(16).uint32(message.sign_in_count);
                if (message.last_sign_in_time != null && Object.hasOwnProperty.call(message, "last_sign_in_time"))
                    writer.uint32(24).uint32(message.last_sign_in_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivitySignInData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivitySignInData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData.ActivitySignInData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.sign_in_count = value;
                            else
                                delete message.sign_in_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.last_sign_in_time = value;
                            else
                                delete message.last_sign_in_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivitySignInData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivitySignInData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.sign_in_count != null && message.hasOwnProperty("sign_in_count"))
                    if (!$util.isInteger(message.sign_in_count))
                        return "sign_in_count: integer expected";
                if (message.last_sign_in_time != null && message.hasOwnProperty("last_sign_in_time"))
                    if (!$util.isInteger(message.last_sign_in_time))
                        return "last_sign_in_time: integer expected";
                return null;
            };

            ActivitySignInData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountActivityData.ActivitySignInData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountActivityData.ActivitySignInData();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.sign_in_count != null)
                    if (Number(object.sign_in_count) !== 0)
                        message.sign_in_count = object.sign_in_count >>> 0;
                if (object.last_sign_in_time != null)
                    if (Number(object.last_sign_in_time) !== 0)
                        message.last_sign_in_time = object.last_sign_in_time >>> 0;
                return message;
            };

            ActivitySignInData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.activity_id = 0;
                    object.sign_in_count = 0;
                    object.last_sign_in_time = 0;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.sign_in_count != null && message.hasOwnProperty("sign_in_count"))
                    object.sign_in_count = message.sign_in_count;
                if (message.last_sign_in_time != null && message.hasOwnProperty("last_sign_in_time"))
                    object.last_sign_in_time = message.last_sign_in_time;
                return object;
            };

            ActivitySignInData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivitySignInData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountActivityData.ActivitySignInData";
            };

            return ActivitySignInData;
        })();

        ResAccountActivityData.BuffData = (function() {

            function BuffData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BuffData.prototype.type = 0;
            BuffData.prototype.remain = 0;
            BuffData.prototype.effect = 0;

            BuffData.create = function create(properties) {
                return new BuffData(properties);
            };

            BuffData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.remain != null && Object.hasOwnProperty.call(message, "remain"))
                    writer.uint32(16).uint32(message.remain);
                if (message.effect != null && Object.hasOwnProperty.call(message, "effect"))
                    writer.uint32(24).uint32(message.effect);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BuffData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BuffData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData.BuffData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.remain = value;
                            else
                                delete message.remain;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.effect = value;
                            else
                                delete message.effect;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BuffData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BuffData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.remain != null && message.hasOwnProperty("remain"))
                    if (!$util.isInteger(message.remain))
                        return "remain: integer expected";
                if (message.effect != null && message.hasOwnProperty("effect"))
                    if (!$util.isInteger(message.effect))
                        return "effect: integer expected";
                return null;
            };

            BuffData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountActivityData.BuffData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountActivityData.BuffData();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.remain != null)
                    if (Number(object.remain) !== 0)
                        message.remain = object.remain >>> 0;
                if (object.effect != null)
                    if (Number(object.effect) !== 0)
                        message.effect = object.effect >>> 0;
                return message;
            };

            BuffData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.remain = 0;
                    object.effect = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.remain != null && message.hasOwnProperty("remain"))
                    object.remain = message.remain;
                if (message.effect != null && message.hasOwnProperty("effect"))
                    object.effect = message.effect;
                return object;
            };

            BuffData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BuffData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountActivityData.BuffData";
            };

            return BuffData;
        })();

        ResAccountActivityData.ActivityRichmanData = (function() {

            function ActivityRichmanData(properties) {
                this.buff = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityRichmanData.prototype.activity_id = 0;
            ActivityRichmanData.prototype.location = 0;
            ActivityRichmanData.prototype.finished_count = 0;
            ActivityRichmanData.prototype.chest_position = 0;
            ActivityRichmanData.prototype.bank_save = 0;
            ActivityRichmanData.prototype.exp = 0;
            ActivityRichmanData.prototype.buff = $util.emptyArray;

            ActivityRichmanData.create = function create(properties) {
                return new ActivityRichmanData(properties);
            };

            ActivityRichmanData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    writer.uint32(16).uint32(message.location);
                if (message.finished_count != null && Object.hasOwnProperty.call(message, "finished_count"))
                    writer.uint32(24).uint32(message.finished_count);
                if (message.chest_position != null && Object.hasOwnProperty.call(message, "chest_position"))
                    writer.uint32(32).uint32(message.chest_position);
                if (message.bank_save != null && Object.hasOwnProperty.call(message, "bank_save"))
                    writer.uint32(40).uint32(message.bank_save);
                if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                    writer.uint32(48).uint32(message.exp);
                if (message.buff != null && message.buff.length)
                    for (let i = 0; i < message.buff.length; ++i)
                        $root.lq.ResAccountActivityData.BuffData.encode(message.buff[i], writer.uint32(58).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityRichmanData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityRichmanData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData.ActivityRichmanData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.location = value;
                            else
                                delete message.location;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.finished_count = value;
                            else
                                delete message.finished_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chest_position = value;
                            else
                                delete message.chest_position;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.bank_save = value;
                            else
                                delete message.bank_save;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.exp = value;
                            else
                                delete message.exp;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.buff && message.buff.length))
                                message.buff = [];
                            message.buff.push($root.lq.ResAccountActivityData.BuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityRichmanData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityRichmanData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.location != null && message.hasOwnProperty("location"))
                    if (!$util.isInteger(message.location))
                        return "location: integer expected";
                if (message.finished_count != null && message.hasOwnProperty("finished_count"))
                    if (!$util.isInteger(message.finished_count))
                        return "finished_count: integer expected";
                if (message.chest_position != null && message.hasOwnProperty("chest_position"))
                    if (!$util.isInteger(message.chest_position))
                        return "chest_position: integer expected";
                if (message.bank_save != null && message.hasOwnProperty("bank_save"))
                    if (!$util.isInteger(message.bank_save))
                        return "bank_save: integer expected";
                if (message.exp != null && message.hasOwnProperty("exp"))
                    if (!$util.isInteger(message.exp))
                        return "exp: integer expected";
                if (message.buff != null && message.hasOwnProperty("buff")) {
                    if (!Array.isArray(message.buff))
                        return "buff: array expected";
                    for (let i = 0; i < message.buff.length; ++i) {
                        let error = $root.lq.ResAccountActivityData.BuffData.verify(message.buff[i], _depth + 1);
                        if (error)
                            return "buff." + error;
                    }
                }
                return null;
            };

            ActivityRichmanData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountActivityData.ActivityRichmanData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountActivityData.ActivityRichmanData();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.location != null)
                    if (Number(object.location) !== 0)
                        message.location = object.location >>> 0;
                if (object.finished_count != null)
                    if (Number(object.finished_count) !== 0)
                        message.finished_count = object.finished_count >>> 0;
                if (object.chest_position != null)
                    if (Number(object.chest_position) !== 0)
                        message.chest_position = object.chest_position >>> 0;
                if (object.bank_save != null)
                    if (Number(object.bank_save) !== 0)
                        message.bank_save = object.bank_save >>> 0;
                if (object.exp != null)
                    if (Number(object.exp) !== 0)
                        message.exp = object.exp >>> 0;
                if (object.buff) {
                    if (!Array.isArray(object.buff))
                        throw TypeError(".lq.ResAccountActivityData.ActivityRichmanData.buff: array expected");
                    message.buff = Array(object.buff.length);
                    for (let i = 0; i < object.buff.length; ++i) {
                        if (typeof object.buff[i] !== "object")
                            throw TypeError(".lq.ResAccountActivityData.ActivityRichmanData.buff: object expected");
                        message.buff[i] = $root.lq.ResAccountActivityData.BuffData.fromObject(object.buff[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityRichmanData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.buff = [];
                if (options.defaults) {
                    object.activity_id = 0;
                    object.location = 0;
                    object.finished_count = 0;
                    object.chest_position = 0;
                    object.bank_save = 0;
                    object.exp = 0;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.location != null && message.hasOwnProperty("location"))
                    object.location = message.location;
                if (message.finished_count != null && message.hasOwnProperty("finished_count"))
                    object.finished_count = message.finished_count;
                if (message.chest_position != null && message.hasOwnProperty("chest_position"))
                    object.chest_position = message.chest_position;
                if (message.bank_save != null && message.hasOwnProperty("bank_save"))
                    object.bank_save = message.bank_save;
                if (message.exp != null && message.hasOwnProperty("exp"))
                    object.exp = message.exp;
                if (message.buff && message.buff.length) {
                    object.buff = Array(message.buff.length);
                    for (let j = 0; j < message.buff.length; ++j)
                        object.buff[j] = $root.lq.ResAccountActivityData.BuffData.toObject(message.buff[j], options);
                }
                return object;
            };

            ActivityRichmanData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityRichmanData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountActivityData.ActivityRichmanData";
            };

            return ActivityRichmanData;
        })();

        ResAccountActivityData.ChestUpData = (function() {

            function ChestUpData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChestUpData.prototype.id = 0;
            ChestUpData.prototype.count = 0;

            ChestUpData.create = function create(properties) {
                return new ChestUpData(properties);
            };

            ChestUpData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChestUpData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChestUpData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData.ChestUpData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChestUpData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChestUpData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            ChestUpData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountActivityData.ChestUpData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountActivityData.ChestUpData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            ChestUpData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            ChestUpData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChestUpData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountActivityData.ChestUpData";
            };

            return ChestUpData;
        })();

        ResAccountActivityData.ActivitySNSData = (function() {

            function ActivitySNSData(properties) {
                this.blog = [];
                this.liked_id = [];
                this.reply = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivitySNSData.prototype.blog = $util.emptyArray;
            ActivitySNSData.prototype.liked_id = $util.emptyArray;
            ActivitySNSData.prototype.reply = $util.emptyArray;

            ActivitySNSData.create = function create(properties) {
                return new ActivitySNSData(properties);
            };

            ActivitySNSData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.blog != null && message.blog.length)
                    for (let i = 0; i < message.blog.length; ++i)
                        $root.lq.SNSBlog.encode(message.blog[i], writer.uint32(10).fork()).ldelim();
                if (message.liked_id != null && message.liked_id.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.liked_id.length; ++i)
                        writer.uint32(message.liked_id[i]);
                    writer.ldelim();
                }
                if (message.reply != null && message.reply.length)
                    for (let i = 0; i < message.reply.length; ++i)
                        $root.lq.SNSReply.encode(message.reply[i], writer.uint32(26).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivitySNSData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivitySNSData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAccountActivityData.ActivitySNSData();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.blog && message.blog.length))
                                message.blog = [];
                            message.blog.push($root.lq.SNSBlog.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.liked_id && message.liked_id.length))
                                    message.liked_id = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.liked_id.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.liked_id && message.liked_id.length))
                                message.liked_id = [];
                            message.liked_id.push(reader.uint32());
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.reply && message.reply.length))
                                message.reply = [];
                            message.reply.push($root.lq.SNSReply.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivitySNSData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivitySNSData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.blog != null && message.hasOwnProperty("blog")) {
                    if (!Array.isArray(message.blog))
                        return "blog: array expected";
                    for (let i = 0; i < message.blog.length; ++i) {
                        let error = $root.lq.SNSBlog.verify(message.blog[i], _depth + 1);
                        if (error)
                            return "blog." + error;
                    }
                }
                if (message.liked_id != null && message.hasOwnProperty("liked_id")) {
                    if (!Array.isArray(message.liked_id))
                        return "liked_id: array expected";
                    for (let i = 0; i < message.liked_id.length; ++i)
                        if (!$util.isInteger(message.liked_id[i]))
                            return "liked_id: integer[] expected";
                }
                if (message.reply != null && message.hasOwnProperty("reply")) {
                    if (!Array.isArray(message.reply))
                        return "reply: array expected";
                    for (let i = 0; i < message.reply.length; ++i) {
                        let error = $root.lq.SNSReply.verify(message.reply[i], _depth + 1);
                        if (error)
                            return "reply." + error;
                    }
                }
                return null;
            };

            ActivitySNSData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResAccountActivityData.ActivitySNSData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResAccountActivityData.ActivitySNSData();
                if (object.blog) {
                    if (!Array.isArray(object.blog))
                        throw TypeError(".lq.ResAccountActivityData.ActivitySNSData.blog: array expected");
                    message.blog = Array(object.blog.length);
                    for (let i = 0; i < object.blog.length; ++i) {
                        if (typeof object.blog[i] !== "object")
                            throw TypeError(".lq.ResAccountActivityData.ActivitySNSData.blog: object expected");
                        message.blog[i] = $root.lq.SNSBlog.fromObject(object.blog[i], _depth + 1);
                    }
                }
                if (object.liked_id) {
                    if (!Array.isArray(object.liked_id))
                        throw TypeError(".lq.ResAccountActivityData.ActivitySNSData.liked_id: array expected");
                    message.liked_id = Array(object.liked_id.length);
                    for (let i = 0; i < object.liked_id.length; ++i)
                        message.liked_id[i] = object.liked_id[i] >>> 0;
                }
                if (object.reply) {
                    if (!Array.isArray(object.reply))
                        throw TypeError(".lq.ResAccountActivityData.ActivitySNSData.reply: array expected");
                    message.reply = Array(object.reply.length);
                    for (let i = 0; i < object.reply.length; ++i) {
                        if (typeof object.reply[i] !== "object")
                            throw TypeError(".lq.ResAccountActivityData.ActivitySNSData.reply: object expected");
                        message.reply[i] = $root.lq.SNSReply.fromObject(object.reply[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivitySNSData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.blog = [];
                    object.liked_id = [];
                    object.reply = [];
                }
                if (message.blog && message.blog.length) {
                    object.blog = Array(message.blog.length);
                    for (let j = 0; j < message.blog.length; ++j)
                        object.blog[j] = $root.lq.SNSBlog.toObject(message.blog[j], options);
                }
                if (message.liked_id && message.liked_id.length) {
                    object.liked_id = Array(message.liked_id.length);
                    for (let j = 0; j < message.liked_id.length; ++j)
                        object.liked_id[j] = message.liked_id[j];
                }
                if (message.reply && message.reply.length) {
                    object.reply = Array(message.reply.length);
                    for (let j = 0; j < message.reply.length; ++j)
                        object.reply[j] = $root.lq.SNSReply.toObject(message.reply[j], options);
                }
                return object;
            };

            ActivitySNSData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivitySNSData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResAccountActivityData.ActivitySNSData";
            };

            return ActivitySNSData;
        })();

        return ResAccountActivityData;
    })();

    lq.SNSBlog = (function() {

        function SNSBlog(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SNSBlog.prototype.id = 0;
        SNSBlog.prototype.read_time = 0;

        SNSBlog.create = function create(properties) {
            return new SNSBlog(properties);
        };

        SNSBlog.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.read_time != null && Object.hasOwnProperty.call(message, "read_time"))
                writer.uint32(16).uint32(message.read_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SNSBlog.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SNSBlog.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SNSBlog(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.read_time = value;
                        else
                            delete message.read_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SNSBlog.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SNSBlog.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.read_time != null && message.hasOwnProperty("read_time"))
                if (!$util.isInteger(message.read_time))
                    return "read_time: integer expected";
            return null;
        };

        SNSBlog.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SNSBlog)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SNSBlog();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.read_time != null)
                if (Number(object.read_time) !== 0)
                    message.read_time = object.read_time >>> 0;
            return message;
        };

        SNSBlog.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.read_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.read_time != null && message.hasOwnProperty("read_time"))
                object.read_time = message.read_time;
            return object;
        };

        SNSBlog.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SNSBlog.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SNSBlog";
        };

        return SNSBlog;
    })();

    lq.SNSReply = (function() {

        function SNSReply(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SNSReply.prototype.id = 0;
        SNSReply.prototype.reply_time = 0;

        SNSReply.create = function create(properties) {
            return new SNSReply(properties);
        };

        SNSReply.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.reply_time != null && Object.hasOwnProperty.call(message, "reply_time"))
                writer.uint32(16).uint32(message.reply_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SNSReply.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SNSReply.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SNSReply(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reply_time = value;
                        else
                            delete message.reply_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SNSReply.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SNSReply.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.reply_time != null && message.hasOwnProperty("reply_time"))
                if (!$util.isInteger(message.reply_time))
                    return "reply_time: integer expected";
            return null;
        };

        SNSReply.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SNSReply)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SNSReply();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.reply_time != null)
                if (Number(object.reply_time) !== 0)
                    message.reply_time = object.reply_time >>> 0;
            return message;
        };

        SNSReply.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.reply_time = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.reply_time != null && message.hasOwnProperty("reply_time"))
                object.reply_time = message.reply_time;
            return object;
        };

        SNSReply.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SNSReply.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SNSReply";
        };

        return SNSReply;
    })();

    lq.ReqExchangeActivityItem = (function() {

        function ReqExchangeActivityItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqExchangeActivityItem.prototype.exchange_id = 0;
        ReqExchangeActivityItem.prototype.count = 0;

        ReqExchangeActivityItem.create = function create(properties) {
            return new ReqExchangeActivityItem(properties);
        };

        ReqExchangeActivityItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.exchange_id != null && Object.hasOwnProperty.call(message, "exchange_id"))
                writer.uint32(8).uint32(message.exchange_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqExchangeActivityItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqExchangeActivityItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqExchangeActivityItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exchange_id = value;
                        else
                            delete message.exchange_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqExchangeActivityItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqExchangeActivityItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.exchange_id != null && message.hasOwnProperty("exchange_id"))
                if (!$util.isInteger(message.exchange_id))
                    return "exchange_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqExchangeActivityItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqExchangeActivityItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqExchangeActivityItem();
            if (object.exchange_id != null)
                if (Number(object.exchange_id) !== 0)
                    message.exchange_id = object.exchange_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqExchangeActivityItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.exchange_id = 0;
                object.count = 0;
            }
            if (message.exchange_id != null && message.hasOwnProperty("exchange_id"))
                object.exchange_id = message.exchange_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqExchangeActivityItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqExchangeActivityItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqExchangeActivityItem";
        };

        return ReqExchangeActivityItem;
    })();

    lq.ResExchangeActivityItem = (function() {

        function ResExchangeActivityItem(properties) {
            this.execute_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResExchangeActivityItem.prototype.error = null;
        ResExchangeActivityItem.prototype.execute_reward = $util.emptyArray;

        ResExchangeActivityItem.create = function create(properties) {
            return new ResExchangeActivityItem(properties);
        };

        ResExchangeActivityItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.execute_reward != null && message.execute_reward.length)
                for (let i = 0; i < message.execute_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.execute_reward[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResExchangeActivityItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResExchangeActivityItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResExchangeActivityItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_reward && message.execute_reward.length))
                            message.execute_reward = [];
                        message.execute_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResExchangeActivityItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResExchangeActivityItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.execute_reward != null && message.hasOwnProperty("execute_reward")) {
                if (!Array.isArray(message.execute_reward))
                    return "execute_reward: array expected";
                for (let i = 0; i < message.execute_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.execute_reward[i], _depth + 1);
                    if (error)
                        return "execute_reward." + error;
                }
            }
            return null;
        };

        ResExchangeActivityItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResExchangeActivityItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResExchangeActivityItem();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResExchangeActivityItem.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.execute_reward) {
                if (!Array.isArray(object.execute_reward))
                    throw TypeError(".lq.ResExchangeActivityItem.execute_reward: array expected");
                message.execute_reward = Array(object.execute_reward.length);
                for (let i = 0; i < object.execute_reward.length; ++i) {
                    if (typeof object.execute_reward[i] !== "object")
                        throw TypeError(".lq.ResExchangeActivityItem.execute_reward: object expected");
                    message.execute_reward[i] = $root.lq.ExecuteReward.fromObject(object.execute_reward[i], _depth + 1);
                }
            }
            return message;
        };

        ResExchangeActivityItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_reward = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.execute_reward && message.execute_reward.length) {
                object.execute_reward = Array(message.execute_reward.length);
                for (let j = 0; j < message.execute_reward.length; ++j)
                    object.execute_reward[j] = $root.lq.ExecuteReward.toObject(message.execute_reward[j], options);
            }
            return object;
        };

        ResExchangeActivityItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResExchangeActivityItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResExchangeActivityItem";
        };

        return ResExchangeActivityItem;
    })();

    lq.ReqCompleteActivityTask = (function() {

        function ReqCompleteActivityTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompleteActivityTask.prototype.task_id = 0;

        ReqCompleteActivityTask.create = function create(properties) {
            return new ReqCompleteActivityTask(properties);
        };

        ReqCompleteActivityTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompleteActivityTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompleteActivityTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompleteActivityTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompleteActivityTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompleteActivityTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            return null;
        };

        ReqCompleteActivityTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompleteActivityTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompleteActivityTask();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            return message;
        };

        ReqCompleteActivityTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.task_id = 0;
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            return object;
        };

        ReqCompleteActivityTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompleteActivityTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompleteActivityTask";
        };

        return ReqCompleteActivityTask;
    })();

    lq.ReqCompleteActivityTaskBatch = (function() {

        function ReqCompleteActivityTaskBatch(properties) {
            this.task_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompleteActivityTaskBatch.prototype.task_list = $util.emptyArray;

        ReqCompleteActivityTaskBatch.create = function create(properties) {
            return new ReqCompleteActivityTaskBatch(properties);
        };

        ReqCompleteActivityTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_list != null && message.task_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.task_list.length; ++i)
                    writer.uint32(message.task_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompleteActivityTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompleteActivityTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompleteActivityTaskBatch();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.task_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.task_list && message.task_list.length))
                            message.task_list = [];
                        message.task_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompleteActivityTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompleteActivityTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_list != null && message.hasOwnProperty("task_list")) {
                if (!Array.isArray(message.task_list))
                    return "task_list: array expected";
                for (let i = 0; i < message.task_list.length; ++i)
                    if (!$util.isInteger(message.task_list[i]))
                        return "task_list: integer[] expected";
            }
            return null;
        };

        ReqCompleteActivityTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompleteActivityTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompleteActivityTaskBatch();
            if (object.task_list) {
                if (!Array.isArray(object.task_list))
                    throw TypeError(".lq.ReqCompleteActivityTaskBatch.task_list: array expected");
                message.task_list = Array(object.task_list.length);
                for (let i = 0; i < object.task_list.length; ++i)
                    message.task_list[i] = object.task_list[i] >>> 0;
            }
            return message;
        };

        ReqCompleteActivityTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_list = [];
            if (message.task_list && message.task_list.length) {
                object.task_list = Array(message.task_list.length);
                for (let j = 0; j < message.task_list.length; ++j)
                    object.task_list[j] = message.task_list[j];
            }
            return object;
        };

        ReqCompleteActivityTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompleteActivityTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompleteActivityTaskBatch";
        };

        return ReqCompleteActivityTaskBatch;
    })();

    lq.ReqCompletePeriodActivityTaskBatch = (function() {

        function ReqCompletePeriodActivityTaskBatch(properties) {
            this.task_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompletePeriodActivityTaskBatch.prototype.task_list = $util.emptyArray;

        ReqCompletePeriodActivityTaskBatch.create = function create(properties) {
            return new ReqCompletePeriodActivityTaskBatch(properties);
        };

        ReqCompletePeriodActivityTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_list != null && message.task_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.task_list.length; ++i)
                    writer.uint32(message.task_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompletePeriodActivityTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompletePeriodActivityTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompletePeriodActivityTaskBatch();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.task_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.task_list && message.task_list.length))
                            message.task_list = [];
                        message.task_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompletePeriodActivityTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompletePeriodActivityTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_list != null && message.hasOwnProperty("task_list")) {
                if (!Array.isArray(message.task_list))
                    return "task_list: array expected";
                for (let i = 0; i < message.task_list.length; ++i)
                    if (!$util.isInteger(message.task_list[i]))
                        return "task_list: integer[] expected";
            }
            return null;
        };

        ReqCompletePeriodActivityTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompletePeriodActivityTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompletePeriodActivityTaskBatch();
            if (object.task_list) {
                if (!Array.isArray(object.task_list))
                    throw TypeError(".lq.ReqCompletePeriodActivityTaskBatch.task_list: array expected");
                message.task_list = Array(object.task_list.length);
                for (let i = 0; i < object.task_list.length; ++i)
                    message.task_list[i] = object.task_list[i] >>> 0;
            }
            return message;
        };

        ReqCompletePeriodActivityTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_list = [];
            if (message.task_list && message.task_list.length) {
                object.task_list = Array(message.task_list.length);
                for (let j = 0; j < message.task_list.length; ++j)
                    object.task_list[j] = message.task_list[j];
            }
            return object;
        };

        ReqCompletePeriodActivityTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompletePeriodActivityTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompletePeriodActivityTaskBatch";
        };

        return ReqCompletePeriodActivityTaskBatch;
    })();

    lq.ReqReceiveActivityFlipTask = (function() {

        function ReqReceiveActivityFlipTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveActivityFlipTask.prototype.task_id = 0;

        ReqReceiveActivityFlipTask.create = function create(properties) {
            return new ReqReceiveActivityFlipTask(properties);
        };

        ReqReceiveActivityFlipTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveActivityFlipTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveActivityFlipTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveActivityFlipTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveActivityFlipTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveActivityFlipTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            return null;
        };

        ReqReceiveActivityFlipTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveActivityFlipTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveActivityFlipTask();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            return message;
        };

        ReqReceiveActivityFlipTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.task_id = 0;
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            return object;
        };

        ReqReceiveActivityFlipTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveActivityFlipTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveActivityFlipTask";
        };

        return ReqReceiveActivityFlipTask;
    })();

    lq.ResReceiveActivityFlipTask = (function() {

        function ResReceiveActivityFlipTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveActivityFlipTask.prototype.count = 0;
        ResReceiveActivityFlipTask.prototype.error = null;

        ResReceiveActivityFlipTask.create = function create(properties) {
            return new ResReceiveActivityFlipTask(properties);
        };

        ResReceiveActivityFlipTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(8).uint32(message.count);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveActivityFlipTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveActivityFlipTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveActivityFlipTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveActivityFlipTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveActivityFlipTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResReceiveActivityFlipTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveActivityFlipTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveActivityFlipTask();
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveActivityFlipTask.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResReceiveActivityFlipTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.count = 0;
                object.error = null;
            }
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResReceiveActivityFlipTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveActivityFlipTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveActivityFlipTask";
        };

        return ResReceiveActivityFlipTask;
    })();

    lq.ReqReceiveActivityFlipTaskBatch = (function() {

        function ReqReceiveActivityFlipTaskBatch(properties) {
            this.task_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveActivityFlipTaskBatch.prototype.task_list = $util.emptyArray;

        ReqReceiveActivityFlipTaskBatch.create = function create(properties) {
            return new ReqReceiveActivityFlipTaskBatch(properties);
        };

        ReqReceiveActivityFlipTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_list != null && message.task_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.task_list.length; ++i)
                    writer.uint32(message.task_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveActivityFlipTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveActivityFlipTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveActivityFlipTaskBatch();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.task_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.task_list && message.task_list.length))
                            message.task_list = [];
                        message.task_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveActivityFlipTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveActivityFlipTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_list != null && message.hasOwnProperty("task_list")) {
                if (!Array.isArray(message.task_list))
                    return "task_list: array expected";
                for (let i = 0; i < message.task_list.length; ++i)
                    if (!$util.isInteger(message.task_list[i]))
                        return "task_list: integer[] expected";
            }
            return null;
        };

        ReqReceiveActivityFlipTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveActivityFlipTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveActivityFlipTaskBatch();
            if (object.task_list) {
                if (!Array.isArray(object.task_list))
                    throw TypeError(".lq.ReqReceiveActivityFlipTaskBatch.task_list: array expected");
                message.task_list = Array(object.task_list.length);
                for (let i = 0; i < object.task_list.length; ++i)
                    message.task_list[i] = object.task_list[i] >>> 0;
            }
            return message;
        };

        ReqReceiveActivityFlipTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_list = [];
            if (message.task_list && message.task_list.length) {
                object.task_list = Array(message.task_list.length);
                for (let j = 0; j < message.task_list.length; ++j)
                    object.task_list[j] = message.task_list[j];
            }
            return object;
        };

        ReqReceiveActivityFlipTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveActivityFlipTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveActivityFlipTaskBatch";
        };

        return ReqReceiveActivityFlipTaskBatch;
    })();

    lq.ResReceiveActivityFlipTaskBatch = (function() {

        function ResReceiveActivityFlipTaskBatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveActivityFlipTaskBatch.prototype.error = null;
        ResReceiveActivityFlipTaskBatch.prototype.count = 0;

        ResReceiveActivityFlipTaskBatch.create = function create(properties) {
            return new ResReceiveActivityFlipTaskBatch(properties);
        };

        ResReceiveActivityFlipTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveActivityFlipTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveActivityFlipTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveActivityFlipTaskBatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveActivityFlipTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveActivityFlipTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ResReceiveActivityFlipTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveActivityFlipTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveActivityFlipTaskBatch();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveActivityFlipTaskBatch.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ResReceiveActivityFlipTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ResReceiveActivityFlipTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveActivityFlipTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveActivityFlipTaskBatch";
        };

        return ResReceiveActivityFlipTaskBatch;
    })();

    lq.ReqCompleteSegmentTaskReward = (function() {

        function ReqCompleteSegmentTaskReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompleteSegmentTaskReward.prototype.task_id = 0;
        ReqCompleteSegmentTaskReward.prototype.count = 0;

        ReqCompleteSegmentTaskReward.create = function create(properties) {
            return new ReqCompleteSegmentTaskReward(properties);
        };

        ReqCompleteSegmentTaskReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompleteSegmentTaskReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompleteSegmentTaskReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompleteSegmentTaskReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompleteSegmentTaskReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompleteSegmentTaskReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqCompleteSegmentTaskReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompleteSegmentTaskReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompleteSegmentTaskReward();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqCompleteSegmentTaskReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.task_id = 0;
                object.count = 0;
            }
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqCompleteSegmentTaskReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompleteSegmentTaskReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompleteSegmentTaskReward";
        };

        return ReqCompleteSegmentTaskReward;
    })();

    lq.ResCompleteSegmentTaskReward = (function() {

        function ResCompleteSegmentTaskReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCompleteSegmentTaskReward.prototype.error = null;
        ResCompleteSegmentTaskReward.prototype.rewards = $util.emptyArray;

        ResCompleteSegmentTaskReward.create = function create(properties) {
            return new ResCompleteSegmentTaskReward(properties);
        };

        ResCompleteSegmentTaskReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCompleteSegmentTaskReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCompleteSegmentTaskReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCompleteSegmentTaskReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCompleteSegmentTaskReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCompleteSegmentTaskReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResCompleteSegmentTaskReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCompleteSegmentTaskReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCompleteSegmentTaskReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCompleteSegmentTaskReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResCompleteSegmentTaskReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResCompleteSegmentTaskReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResCompleteSegmentTaskReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResCompleteSegmentTaskReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCompleteSegmentTaskReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCompleteSegmentTaskReward";
        };

        return ResCompleteSegmentTaskReward;
    })();

    lq.ReqFetchActivityFlipInfo = (function() {

        function ReqFetchActivityFlipInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchActivityFlipInfo.prototype.activity_id = 0;

        ReqFetchActivityFlipInfo.create = function create(properties) {
            return new ReqFetchActivityFlipInfo(properties);
        };

        ReqFetchActivityFlipInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchActivityFlipInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchActivityFlipInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchActivityFlipInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchActivityFlipInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchActivityFlipInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchActivityFlipInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchActivityFlipInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchActivityFlipInfo();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchActivityFlipInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchActivityFlipInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchActivityFlipInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchActivityFlipInfo";
        };

        return ReqFetchActivityFlipInfo;
    })();

    lq.ResFetchActivityFlipInfo = (function() {

        function ResFetchActivityFlipInfo(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchActivityFlipInfo.prototype.rewards = $util.emptyArray;
        ResFetchActivityFlipInfo.prototype.count = 0;
        ResFetchActivityFlipInfo.prototype.error = null;

        ResFetchActivityFlipInfo.create = function create(properties) {
            return new ResFetchActivityFlipInfo(properties);
        };

        ResFetchActivityFlipInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rewards != null && message.rewards.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.rewards.length; ++i)
                    writer.uint32(message.rewards[i]);
                writer.ldelim();
            }
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchActivityFlipInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchActivityFlipInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchActivityFlipInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewards.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchActivityFlipInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchActivityFlipInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i)
                    if (!$util.isInteger(message.rewards[i]))
                        return "rewards: integer[] expected";
            }
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResFetchActivityFlipInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchActivityFlipInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchActivityFlipInfo();
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResFetchActivityFlipInfo.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i)
                    message.rewards[i] = object.rewards[i] >>> 0;
            }
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchActivityFlipInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResFetchActivityFlipInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.count = 0;
                object.error = null;
            }
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = message.rewards[j];
            }
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResFetchActivityFlipInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchActivityFlipInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchActivityFlipInfo";
        };

        return ResFetchActivityFlipInfo;
    })();

    lq.ReqCompleteActivityFlipTaskBatch = (function() {

        function ReqCompleteActivityFlipTaskBatch(properties) {
            this.task_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompleteActivityFlipTaskBatch.prototype.task_list = $util.emptyArray;

        ReqCompleteActivityFlipTaskBatch.create = function create(properties) {
            return new ReqCompleteActivityFlipTaskBatch(properties);
        };

        ReqCompleteActivityFlipTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_list != null && message.task_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.task_list.length; ++i)
                    writer.uint32(message.task_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompleteActivityFlipTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompleteActivityFlipTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompleteActivityFlipTaskBatch();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.task_list && message.task_list.length))
                                message.task_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.task_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.task_list && message.task_list.length))
                            message.task_list = [];
                        message.task_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompleteActivityFlipTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompleteActivityFlipTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_list != null && message.hasOwnProperty("task_list")) {
                if (!Array.isArray(message.task_list))
                    return "task_list: array expected";
                for (let i = 0; i < message.task_list.length; ++i)
                    if (!$util.isInteger(message.task_list[i]))
                        return "task_list: integer[] expected";
            }
            return null;
        };

        ReqCompleteActivityFlipTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompleteActivityFlipTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompleteActivityFlipTaskBatch();
            if (object.task_list) {
                if (!Array.isArray(object.task_list))
                    throw TypeError(".lq.ReqCompleteActivityFlipTaskBatch.task_list: array expected");
                message.task_list = Array(object.task_list.length);
                for (let i = 0; i < object.task_list.length; ++i)
                    message.task_list[i] = object.task_list[i] >>> 0;
            }
            return message;
        };

        ReqCompleteActivityFlipTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_list = [];
            if (message.task_list && message.task_list.length) {
                object.task_list = Array(message.task_list.length);
                for (let j = 0; j < message.task_list.length; ++j)
                    object.task_list[j] = message.task_list[j];
            }
            return object;
        };

        ReqCompleteActivityFlipTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompleteActivityFlipTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompleteActivityFlipTaskBatch";
        };

        return ReqCompleteActivityFlipTaskBatch;
    })();

    lq.ResCompleteActivityFlipTaskBatch = (function() {

        function ResCompleteActivityFlipTaskBatch(properties) {
            this.total_rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCompleteActivityFlipTaskBatch.prototype.error = null;
        ResCompleteActivityFlipTaskBatch.prototype.total_rewards = $util.emptyArray;

        ResCompleteActivityFlipTaskBatch.create = function create(properties) {
            return new ResCompleteActivityFlipTaskBatch(properties);
        };

        ResCompleteActivityFlipTaskBatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.total_rewards != null && message.total_rewards.length)
                for (let i = 0; i < message.total_rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.total_rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCompleteActivityFlipTaskBatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCompleteActivityFlipTaskBatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCompleteActivityFlipTaskBatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.total_rewards && message.total_rewards.length))
                            message.total_rewards = [];
                        message.total_rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCompleteActivityFlipTaskBatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCompleteActivityFlipTaskBatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.total_rewards != null && message.hasOwnProperty("total_rewards")) {
                if (!Array.isArray(message.total_rewards))
                    return "total_rewards: array expected";
                for (let i = 0; i < message.total_rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.total_rewards[i], _depth + 1);
                    if (error)
                        return "total_rewards." + error;
                }
            }
            return null;
        };

        ResCompleteActivityFlipTaskBatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCompleteActivityFlipTaskBatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCompleteActivityFlipTaskBatch();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCompleteActivityFlipTaskBatch.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.total_rewards) {
                if (!Array.isArray(object.total_rewards))
                    throw TypeError(".lq.ResCompleteActivityFlipTaskBatch.total_rewards: array expected");
                message.total_rewards = Array(object.total_rewards.length);
                for (let i = 0; i < object.total_rewards.length; ++i) {
                    if (typeof object.total_rewards[i] !== "object")
                        throw TypeError(".lq.ResCompleteActivityFlipTaskBatch.total_rewards: object expected");
                    message.total_rewards[i] = $root.lq.ExecuteReward.fromObject(object.total_rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResCompleteActivityFlipTaskBatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.total_rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.total_rewards && message.total_rewards.length) {
                object.total_rewards = Array(message.total_rewards.length);
                for (let j = 0; j < message.total_rewards.length; ++j)
                    object.total_rewards[j] = $root.lq.ExecuteReward.toObject(message.total_rewards[j], options);
            }
            return object;
        };

        ResCompleteActivityFlipTaskBatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCompleteActivityFlipTaskBatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCompleteActivityFlipTaskBatch";
        };

        return ResCompleteActivityFlipTaskBatch;
    })();

    lq.ReqGainAccumulatedPointActivityReward = (function() {

        function ReqGainAccumulatedPointActivityReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGainAccumulatedPointActivityReward.prototype.activity_id = 0;
        ReqGainAccumulatedPointActivityReward.prototype.reward_id = 0;

        ReqGainAccumulatedPointActivityReward.create = function create(properties) {
            return new ReqGainAccumulatedPointActivityReward(properties);
        };

        ReqGainAccumulatedPointActivityReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.reward_id != null && Object.hasOwnProperty.call(message, "reward_id"))
                writer.uint32(16).uint32(message.reward_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGainAccumulatedPointActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGainAccumulatedPointActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGainAccumulatedPointActivityReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.reward_id = value;
                        else
                            delete message.reward_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGainAccumulatedPointActivityReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGainAccumulatedPointActivityReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                if (!$util.isInteger(message.reward_id))
                    return "reward_id: integer expected";
            return null;
        };

        ReqGainAccumulatedPointActivityReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGainAccumulatedPointActivityReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGainAccumulatedPointActivityReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.reward_id != null)
                if (Number(object.reward_id) !== 0)
                    message.reward_id = object.reward_id >>> 0;
            return message;
        };

        ReqGainAccumulatedPointActivityReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.reward_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                object.reward_id = message.reward_id;
            return object;
        };

        ReqGainAccumulatedPointActivityReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGainAccumulatedPointActivityReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGainAccumulatedPointActivityReward";
        };

        return ReqGainAccumulatedPointActivityReward;
    })();

    lq.ReqGainMultiPointActivityReward = (function() {

        function ReqGainMultiPointActivityReward(properties) {
            this.reward_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGainMultiPointActivityReward.prototype.activity_id = 0;
        ReqGainMultiPointActivityReward.prototype.reward_id_list = $util.emptyArray;

        ReqGainMultiPointActivityReward.create = function create(properties) {
            return new ReqGainMultiPointActivityReward(properties);
        };

        ReqGainMultiPointActivityReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.reward_id_list != null && message.reward_id_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.reward_id_list.length; ++i)
                    writer.uint32(message.reward_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGainMultiPointActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGainMultiPointActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGainMultiPointActivityReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.reward_id_list && message.reward_id_list.length))
                                message.reward_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.reward_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.reward_id_list && message.reward_id_list.length))
                            message.reward_id_list = [];
                        message.reward_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGainMultiPointActivityReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGainMultiPointActivityReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.reward_id_list != null && message.hasOwnProperty("reward_id_list")) {
                if (!Array.isArray(message.reward_id_list))
                    return "reward_id_list: array expected";
                for (let i = 0; i < message.reward_id_list.length; ++i)
                    if (!$util.isInteger(message.reward_id_list[i]))
                        return "reward_id_list: integer[] expected";
            }
            return null;
        };

        ReqGainMultiPointActivityReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGainMultiPointActivityReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGainMultiPointActivityReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.reward_id_list) {
                if (!Array.isArray(object.reward_id_list))
                    throw TypeError(".lq.ReqGainMultiPointActivityReward.reward_id_list: array expected");
                message.reward_id_list = Array(object.reward_id_list.length);
                for (let i = 0; i < object.reward_id_list.length; ++i)
                    message.reward_id_list[i] = object.reward_id_list[i] >>> 0;
            }
            return message;
        };

        ReqGainMultiPointActivityReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_id_list = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.reward_id_list && message.reward_id_list.length) {
                object.reward_id_list = Array(message.reward_id_list.length);
                for (let j = 0; j < message.reward_id_list.length; ++j)
                    object.reward_id_list[j] = message.reward_id_list[j];
            }
            return object;
        };

        ReqGainMultiPointActivityReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGainMultiPointActivityReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGainMultiPointActivityReward";
        };

        return ReqGainMultiPointActivityReward;
    })();

    lq.ReqFetchRankPointLeaderboard = (function() {

        function ReqFetchRankPointLeaderboard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchRankPointLeaderboard.prototype.leaderboard_id = 0;

        ReqFetchRankPointLeaderboard.create = function create(properties) {
            return new ReqFetchRankPointLeaderboard(properties);
        };

        ReqFetchRankPointLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.leaderboard_id != null && Object.hasOwnProperty.call(message, "leaderboard_id"))
                writer.uint32(8).uint32(message.leaderboard_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchRankPointLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchRankPointLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchRankPointLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.leaderboard_id = value;
                        else
                            delete message.leaderboard_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchRankPointLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchRankPointLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                if (!$util.isInteger(message.leaderboard_id))
                    return "leaderboard_id: integer expected";
            return null;
        };

        ReqFetchRankPointLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchRankPointLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchRankPointLeaderboard();
            if (object.leaderboard_id != null)
                if (Number(object.leaderboard_id) !== 0)
                    message.leaderboard_id = object.leaderboard_id >>> 0;
            return message;
        };

        ReqFetchRankPointLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.leaderboard_id = 0;
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                object.leaderboard_id = message.leaderboard_id;
            return object;
        };

        ReqFetchRankPointLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchRankPointLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchRankPointLeaderboard";
        };

        return ReqFetchRankPointLeaderboard;
    })();

    lq.ResFetchRankPointLeaderboard = (function() {

        function ResFetchRankPointLeaderboard(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchRankPointLeaderboard.prototype.error = null;
        ResFetchRankPointLeaderboard.prototype.items = $util.emptyArray;
        ResFetchRankPointLeaderboard.prototype.last_refresh_time = 0;

        ResFetchRankPointLeaderboard.create = function create(properties) {
            return new ResFetchRankPointLeaderboard(properties);
        };

        ResFetchRankPointLeaderboard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResFetchRankPointLeaderboard.Item.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.last_refresh_time != null && Object.hasOwnProperty.call(message, "last_refresh_time"))
                writer.uint32(24).uint32(message.last_refresh_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchRankPointLeaderboard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchRankPointLeaderboard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRankPointLeaderboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResFetchRankPointLeaderboard.Item.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_refresh_time = value;
                        else
                            delete message.last_refresh_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchRankPointLeaderboard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchRankPointLeaderboard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResFetchRankPointLeaderboard.Item.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.last_refresh_time != null && message.hasOwnProperty("last_refresh_time"))
                if (!$util.isInteger(message.last_refresh_time))
                    return "last_refresh_time: integer expected";
            return null;
        };

        ResFetchRankPointLeaderboard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchRankPointLeaderboard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchRankPointLeaderboard();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchRankPointLeaderboard.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResFetchRankPointLeaderboard.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResFetchRankPointLeaderboard.items: object expected");
                    message.items[i] = $root.lq.ResFetchRankPointLeaderboard.Item.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.last_refresh_time != null)
                if (Number(object.last_refresh_time) !== 0)
                    message.last_refresh_time = object.last_refresh_time >>> 0;
            return message;
        };

        ResFetchRankPointLeaderboard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.error = null;
                object.last_refresh_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResFetchRankPointLeaderboard.Item.toObject(message.items[j], options);
            }
            if (message.last_refresh_time != null && message.hasOwnProperty("last_refresh_time"))
                object.last_refresh_time = message.last_refresh_time;
            return object;
        };

        ResFetchRankPointLeaderboard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchRankPointLeaderboard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchRankPointLeaderboard";
        };

        ResFetchRankPointLeaderboard.Item = (function() {

            function Item(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Item.prototype.account_id = 0;
            Item.prototype.rank = 0;
            Item.prototype.view = null;
            Item.prototype.point = 0;

            Item.create = function create(properties) {
                return new Item(properties);
            };

            Item.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(16).uint32(message.rank);
                if (message.view != null && Object.hasOwnProperty.call(message, "view"))
                    $root.lq.PlayerBaseView.encode(message.view, writer.uint32(26).fork()).ldelim();
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(32).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Item.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Item.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRankPointLeaderboard.Item(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.view = $root.lq.PlayerBaseView.decode(reader, reader.uint32(), undefined, _depth + 1, message.view);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Item.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Item.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.view != null && message.hasOwnProperty("view")) {
                    let error = $root.lq.PlayerBaseView.verify(message.view, _depth + 1);
                    if (error)
                        return "view." + error;
                }
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            Item.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchRankPointLeaderboard.Item)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchRankPointLeaderboard.Item();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.view != null) {
                    if (typeof object.view !== "object")
                        throw TypeError(".lq.ResFetchRankPointLeaderboard.Item.view: object expected");
                    message.view = $root.lq.PlayerBaseView.fromObject(object.view, _depth + 1);
                }
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            Item.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.rank = 0;
                    object.view = null;
                    object.point = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.view != null && message.hasOwnProperty("view"))
                    object.view = $root.lq.PlayerBaseView.toObject(message.view, options);
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            Item.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Item.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchRankPointLeaderboard.Item";
            };

            return Item;
        })();

        return ResFetchRankPointLeaderboard;
    })();

    lq.ReqGainRankPointReward = (function() {

        function ReqGainRankPointReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGainRankPointReward.prototype.leaderboard_id = 0;
        ReqGainRankPointReward.prototype.activity_id = 0;

        ReqGainRankPointReward.create = function create(properties) {
            return new ReqGainRankPointReward(properties);
        };

        ReqGainRankPointReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.leaderboard_id != null && Object.hasOwnProperty.call(message, "leaderboard_id"))
                writer.uint32(8).uint32(message.leaderboard_id);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGainRankPointReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGainRankPointReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGainRankPointReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.leaderboard_id = value;
                        else
                            delete message.leaderboard_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGainRankPointReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGainRankPointReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                if (!$util.isInteger(message.leaderboard_id))
                    return "leaderboard_id: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqGainRankPointReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGainRankPointReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGainRankPointReward();
            if (object.leaderboard_id != null)
                if (Number(object.leaderboard_id) !== 0)
                    message.leaderboard_id = object.leaderboard_id >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqGainRankPointReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.leaderboard_id = 0;
                object.activity_id = 0;
            }
            if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                object.leaderboard_id = message.leaderboard_id;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqGainRankPointReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGainRankPointReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGainRankPointReward";
        };

        return ReqGainRankPointReward;
    })();

    lq.ReqRichmanNextMove = (function() {

        function ReqRichmanNextMove(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRichmanNextMove.prototype.activity_id = 0;

        ReqRichmanNextMove.create = function create(properties) {
            return new ReqRichmanNextMove(properties);
        };

        ReqRichmanNextMove.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRichmanNextMove.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRichmanNextMove.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRichmanNextMove(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRichmanNextMove.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRichmanNextMove.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqRichmanNextMove.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRichmanNextMove)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRichmanNextMove();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqRichmanNextMove.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqRichmanNextMove.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRichmanNextMove.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRichmanNextMove";
        };

        return ReqRichmanNextMove;
    })();

    lq.ResRichmanNextMove = (function() {

        function ResRichmanNextMove(properties) {
            this.paths = [];
            this.buff = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRichmanNextMove.prototype.paths = $util.emptyArray;
        ResRichmanNextMove.prototype.dice = 0;
        ResRichmanNextMove.prototype.location = 0;
        ResRichmanNextMove.prototype.finished_count = 0;
        ResRichmanNextMove.prototype.step = 0;
        ResRichmanNextMove.prototype.buff = $util.emptyArray;
        ResRichmanNextMove.prototype.bank_save = 0;
        ResRichmanNextMove.prototype.chest_position = 0;
        ResRichmanNextMove.prototype.exp = 0;
        ResRichmanNextMove.prototype.bank_save_add = 0;
        ResRichmanNextMove.prototype.error = null;

        ResRichmanNextMove.create = function create(properties) {
            return new ResRichmanNextMove(properties);
        };

        ResRichmanNextMove.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.paths != null && message.paths.length)
                for (let i = 0; i < message.paths.length; ++i)
                    $root.lq.ResRichmanNextMove.PathData.encode(message.paths[i], writer.uint32(10).fork()).ldelim();
            if (message.dice != null && Object.hasOwnProperty.call(message, "dice"))
                writer.uint32(16).uint32(message.dice);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(24).uint32(message.location);
            if (message.finished_count != null && Object.hasOwnProperty.call(message, "finished_count"))
                writer.uint32(32).uint32(message.finished_count);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(40).uint32(message.step);
            if (message.buff != null && message.buff.length)
                for (let i = 0; i < message.buff.length; ++i)
                    $root.lq.ResRichmanNextMove.BuffData.encode(message.buff[i], writer.uint32(50).fork()).ldelim();
            if (message.bank_save != null && Object.hasOwnProperty.call(message, "bank_save"))
                writer.uint32(56).uint32(message.bank_save);
            if (message.chest_position != null && Object.hasOwnProperty.call(message, "chest_position"))
                writer.uint32(64).uint32(message.chest_position);
            if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                writer.uint32(72).uint32(message.exp);
            if (message.bank_save_add != null && Object.hasOwnProperty.call(message, "bank_save_add"))
                writer.uint32(80).uint32(message.bank_save_add);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(90).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRichmanNextMove.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRichmanNextMove.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanNextMove(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.paths && message.paths.length))
                            message.paths = [];
                        message.paths.push($root.lq.ResRichmanNextMove.PathData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dice = value;
                        else
                            delete message.dice;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.location = value;
                        else
                            delete message.location;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finished_count = value;
                        else
                            delete message.finished_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buff && message.buff.length))
                            message.buff = [];
                        message.buff.push($root.lq.ResRichmanNextMove.BuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bank_save = value;
                        else
                            delete message.bank_save;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_position = value;
                        else
                            delete message.chest_position;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.exp = value;
                        else
                            delete message.exp;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bank_save_add = value;
                        else
                            delete message.bank_save_add;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRichmanNextMove.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRichmanNextMove.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.paths != null && message.hasOwnProperty("paths")) {
                if (!Array.isArray(message.paths))
                    return "paths: array expected";
                for (let i = 0; i < message.paths.length; ++i) {
                    let error = $root.lq.ResRichmanNextMove.PathData.verify(message.paths[i], _depth + 1);
                    if (error)
                        return "paths." + error;
                }
            }
            if (message.dice != null && message.hasOwnProperty("dice"))
                if (!$util.isInteger(message.dice))
                    return "dice: integer expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isInteger(message.location))
                    return "location: integer expected";
            if (message.finished_count != null && message.hasOwnProperty("finished_count"))
                if (!$util.isInteger(message.finished_count))
                    return "finished_count: integer expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            if (message.buff != null && message.hasOwnProperty("buff")) {
                if (!Array.isArray(message.buff))
                    return "buff: array expected";
                for (let i = 0; i < message.buff.length; ++i) {
                    let error = $root.lq.ResRichmanNextMove.BuffData.verify(message.buff[i], _depth + 1);
                    if (error)
                        return "buff." + error;
                }
            }
            if (message.bank_save != null && message.hasOwnProperty("bank_save"))
                if (!$util.isInteger(message.bank_save))
                    return "bank_save: integer expected";
            if (message.chest_position != null && message.hasOwnProperty("chest_position"))
                if (!$util.isInteger(message.chest_position))
                    return "chest_position: integer expected";
            if (message.exp != null && message.hasOwnProperty("exp"))
                if (!$util.isInteger(message.exp))
                    return "exp: integer expected";
            if (message.bank_save_add != null && message.hasOwnProperty("bank_save_add"))
                if (!$util.isInteger(message.bank_save_add))
                    return "bank_save_add: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResRichmanNextMove.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRichmanNextMove)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRichmanNextMove();
            if (object.paths) {
                if (!Array.isArray(object.paths))
                    throw TypeError(".lq.ResRichmanNextMove.paths: array expected");
                message.paths = Array(object.paths.length);
                for (let i = 0; i < object.paths.length; ++i) {
                    if (typeof object.paths[i] !== "object")
                        throw TypeError(".lq.ResRichmanNextMove.paths: object expected");
                    message.paths[i] = $root.lq.ResRichmanNextMove.PathData.fromObject(object.paths[i], _depth + 1);
                }
            }
            if (object.dice != null)
                if (Number(object.dice) !== 0)
                    message.dice = object.dice >>> 0;
            if (object.location != null)
                if (Number(object.location) !== 0)
                    message.location = object.location >>> 0;
            if (object.finished_count != null)
                if (Number(object.finished_count) !== 0)
                    message.finished_count = object.finished_count >>> 0;
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            if (object.buff) {
                if (!Array.isArray(object.buff))
                    throw TypeError(".lq.ResRichmanNextMove.buff: array expected");
                message.buff = Array(object.buff.length);
                for (let i = 0; i < object.buff.length; ++i) {
                    if (typeof object.buff[i] !== "object")
                        throw TypeError(".lq.ResRichmanNextMove.buff: object expected");
                    message.buff[i] = $root.lq.ResRichmanNextMove.BuffData.fromObject(object.buff[i], _depth + 1);
                }
            }
            if (object.bank_save != null)
                if (Number(object.bank_save) !== 0)
                    message.bank_save = object.bank_save >>> 0;
            if (object.chest_position != null)
                if (Number(object.chest_position) !== 0)
                    message.chest_position = object.chest_position >>> 0;
            if (object.exp != null)
                if (Number(object.exp) !== 0)
                    message.exp = object.exp >>> 0;
            if (object.bank_save_add != null)
                if (Number(object.bank_save_add) !== 0)
                    message.bank_save_add = object.bank_save_add >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRichmanNextMove.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResRichmanNextMove.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.paths = [];
                object.buff = [];
            }
            if (options.defaults) {
                object.dice = 0;
                object.location = 0;
                object.finished_count = 0;
                object.step = 0;
                object.bank_save = 0;
                object.chest_position = 0;
                object.exp = 0;
                object.bank_save_add = 0;
                object.error = null;
            }
            if (message.paths && message.paths.length) {
                object.paths = Array(message.paths.length);
                for (let j = 0; j < message.paths.length; ++j)
                    object.paths[j] = $root.lq.ResRichmanNextMove.PathData.toObject(message.paths[j], options);
            }
            if (message.dice != null && message.hasOwnProperty("dice"))
                object.dice = message.dice;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            if (message.finished_count != null && message.hasOwnProperty("finished_count"))
                object.finished_count = message.finished_count;
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.buff && message.buff.length) {
                object.buff = Array(message.buff.length);
                for (let j = 0; j < message.buff.length; ++j)
                    object.buff[j] = $root.lq.ResRichmanNextMove.BuffData.toObject(message.buff[j], options);
            }
            if (message.bank_save != null && message.hasOwnProperty("bank_save"))
                object.bank_save = message.bank_save;
            if (message.chest_position != null && message.hasOwnProperty("chest_position"))
                object.chest_position = message.chest_position;
            if (message.exp != null && message.hasOwnProperty("exp"))
                object.exp = message.exp;
            if (message.bank_save_add != null && message.hasOwnProperty("bank_save_add"))
                object.bank_save_add = message.bank_save_add;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResRichmanNextMove.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRichmanNextMove.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRichmanNextMove";
        };

        ResRichmanNextMove.RewardData = (function() {

            function RewardData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardData.prototype.resource_id = 0;
            RewardData.prototype.count = 0;
            RewardData.prototype.origin_count = 0;
            RewardData.prototype.type = 0;

            RewardData.create = function create(properties) {
                return new RewardData(properties);
            };

            RewardData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                    writer.uint32(8).uint32(message.resource_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.origin_count != null && Object.hasOwnProperty.call(message, "origin_count"))
                    writer.uint32(24).uint32(message.origin_count);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(40).uint32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanNextMove.RewardData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_id = value;
                            else
                                delete message.resource_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.origin_count = value;
                            else
                                delete message.origin_count;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    if (!$util.isInteger(message.resource_id))
                        return "resource_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.origin_count != null && message.hasOwnProperty("origin_count"))
                    if (!$util.isInteger(message.origin_count))
                        return "origin_count: integer expected";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                return null;
            };

            RewardData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResRichmanNextMove.RewardData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResRichmanNextMove.RewardData();
                if (object.resource_id != null)
                    if (Number(object.resource_id) !== 0)
                        message.resource_id = object.resource_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.origin_count != null)
                    if (Number(object.origin_count) !== 0)
                        message.origin_count = object.origin_count >>> 0;
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                return message;
            };

            RewardData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.resource_id = 0;
                    object.count = 0;
                    object.origin_count = 0;
                    object.type = 0;
                }
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    object.resource_id = message.resource_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.origin_count != null && message.hasOwnProperty("origin_count"))
                    object.origin_count = message.origin_count;
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                return object;
            };

            RewardData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResRichmanNextMove.RewardData";
            };

            return RewardData;
        })();

        ResRichmanNextMove.PathData = (function() {

            function PathData(properties) {
                this.rewards = [];
                this.events = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PathData.prototype.location = 0;
            PathData.prototype.rewards = $util.emptyArray;
            PathData.prototype.events = $util.emptyArray;

            PathData.create = function create(properties) {
                return new PathData(properties);
            };

            PathData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    writer.uint32(8).uint32(message.location);
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.ResRichmanNextMove.RewardData.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
                if (message.events != null && message.events.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.events.length; ++i)
                        writer.uint32(message.events[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PathData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PathData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanNextMove.PathData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.location = value;
                            else
                                delete message.location;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.ResRichmanNextMove.RewardData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.events && message.events.length))
                                    message.events = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.events.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.events && message.events.length))
                                message.events = [];
                            message.events.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PathData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PathData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.location != null && message.hasOwnProperty("location"))
                    if (!$util.isInteger(message.location))
                        return "location: integer expected";
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.ResRichmanNextMove.RewardData.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                if (message.events != null && message.hasOwnProperty("events")) {
                    if (!Array.isArray(message.events))
                        return "events: array expected";
                    for (let i = 0; i < message.events.length; ++i)
                        if (!$util.isInteger(message.events[i]))
                            return "events: integer[] expected";
                }
                return null;
            };

            PathData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResRichmanNextMove.PathData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResRichmanNextMove.PathData();
                if (object.location != null)
                    if (Number(object.location) !== 0)
                        message.location = object.location >>> 0;
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.ResRichmanNextMove.PathData.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.ResRichmanNextMove.PathData.rewards: object expected");
                        message.rewards[i] = $root.lq.ResRichmanNextMove.RewardData.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                if (object.events) {
                    if (!Array.isArray(object.events))
                        throw TypeError(".lq.ResRichmanNextMove.PathData.events: array expected");
                    message.events = Array(object.events.length);
                    for (let i = 0; i < object.events.length; ++i)
                        message.events[i] = object.events[i] >>> 0;
                }
                return message;
            };

            PathData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.rewards = [];
                    object.events = [];
                }
                if (options.defaults)
                    object.location = 0;
                if (message.location != null && message.hasOwnProperty("location"))
                    object.location = message.location;
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.ResRichmanNextMove.RewardData.toObject(message.rewards[j], options);
                }
                if (message.events && message.events.length) {
                    object.events = Array(message.events.length);
                    for (let j = 0; j < message.events.length; ++j)
                        object.events[j] = message.events[j];
                }
                return object;
            };

            PathData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PathData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResRichmanNextMove.PathData";
            };

            return PathData;
        })();

        ResRichmanNextMove.BuffData = (function() {

            function BuffData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BuffData.prototype.type = 0;
            BuffData.prototype.remain = 0;
            BuffData.prototype.effect = 0;

            BuffData.create = function create(properties) {
                return new BuffData(properties);
            };

            BuffData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.remain != null && Object.hasOwnProperty.call(message, "remain"))
                    writer.uint32(16).uint32(message.remain);
                if (message.effect != null && Object.hasOwnProperty.call(message, "effect"))
                    writer.uint32(24).uint32(message.effect);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BuffData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BuffData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanNextMove.BuffData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.remain = value;
                            else
                                delete message.remain;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.effect = value;
                            else
                                delete message.effect;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BuffData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BuffData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.remain != null && message.hasOwnProperty("remain"))
                    if (!$util.isInteger(message.remain))
                        return "remain: integer expected";
                if (message.effect != null && message.hasOwnProperty("effect"))
                    if (!$util.isInteger(message.effect))
                        return "effect: integer expected";
                return null;
            };

            BuffData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResRichmanNextMove.BuffData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResRichmanNextMove.BuffData();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.remain != null)
                    if (Number(object.remain) !== 0)
                        message.remain = object.remain >>> 0;
                if (object.effect != null)
                    if (Number(object.effect) !== 0)
                        message.effect = object.effect >>> 0;
                return message;
            };

            BuffData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.remain = 0;
                    object.effect = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.remain != null && message.hasOwnProperty("remain"))
                    object.remain = message.remain;
                if (message.effect != null && message.hasOwnProperty("effect"))
                    object.effect = message.effect;
                return object;
            };

            BuffData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BuffData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResRichmanNextMove.BuffData";
            };

            return BuffData;
        })();

        return ResRichmanNextMove;
    })();

    lq.ReqRichmanSpecialMove = (function() {

        function ReqRichmanSpecialMove(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRichmanSpecialMove.prototype.activity_id = 0;
        ReqRichmanSpecialMove.prototype.step = 0;

        ReqRichmanSpecialMove.create = function create(properties) {
            return new ReqRichmanSpecialMove(properties);
        };

        ReqRichmanSpecialMove.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(16).uint32(message.step);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRichmanSpecialMove.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRichmanSpecialMove.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRichmanSpecialMove(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRichmanSpecialMove.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRichmanSpecialMove.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            return null;
        };

        ReqRichmanSpecialMove.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRichmanSpecialMove)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRichmanSpecialMove();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            return message;
        };

        ReqRichmanSpecialMove.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.step = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            return object;
        };

        ReqRichmanSpecialMove.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRichmanSpecialMove.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRichmanSpecialMove";
        };

        return ReqRichmanSpecialMove;
    })();

    lq.ReqRichmanChestInfo = (function() {

        function ReqRichmanChestInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRichmanChestInfo.prototype.activity_id = 0;

        ReqRichmanChestInfo.create = function create(properties) {
            return new ReqRichmanChestInfo(properties);
        };

        ReqRichmanChestInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRichmanChestInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRichmanChestInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRichmanChestInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRichmanChestInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRichmanChestInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqRichmanChestInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRichmanChestInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRichmanChestInfo();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqRichmanChestInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqRichmanChestInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRichmanChestInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRichmanChestInfo";
        };

        return ReqRichmanChestInfo;
    })();

    lq.ResRichmanChestInfo = (function() {

        function ResRichmanChestInfo(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRichmanChestInfo.prototype.items = $util.emptyArray;
        ResRichmanChestInfo.prototype.error = null;

        ResRichmanChestInfo.create = function create(properties) {
            return new ResRichmanChestInfo(properties);
        };

        ResRichmanChestInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResRichmanChestInfo.ItemData.encode(message.items[i], writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRichmanChestInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRichmanChestInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanChestInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResRichmanChestInfo.ItemData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRichmanChestInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRichmanChestInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResRichmanChestInfo.ItemData.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResRichmanChestInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRichmanChestInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRichmanChestInfo();
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResRichmanChestInfo.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResRichmanChestInfo.items: object expected");
                    message.items[i] = $root.lq.ResRichmanChestInfo.ItemData.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRichmanChestInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResRichmanChestInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResRichmanChestInfo.ItemData.toObject(message.items[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResRichmanChestInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRichmanChestInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRichmanChestInfo";
        };

        ResRichmanChestInfo.ItemData = (function() {

            function ItemData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ItemData.prototype.id = 0;
            ItemData.prototype.count = 0;

            ItemData.create = function create(properties) {
                return new ItemData(properties);
            };

            ItemData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ItemData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ItemData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRichmanChestInfo.ItemData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ItemData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ItemData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            ItemData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResRichmanChestInfo.ItemData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResRichmanChestInfo.ItemData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            ItemData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            ItemData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ItemData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResRichmanChestInfo.ItemData";
            };

            return ItemData;
        })();

        return ResRichmanChestInfo;
    })();

    lq.ReqCreateGameObserveAuth = (function() {

        function ReqCreateGameObserveAuth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateGameObserveAuth.prototype.game_uuid = "";

        ReqCreateGameObserveAuth.create = function create(properties) {
            return new ReqCreateGameObserveAuth(properties);
        };

        ReqCreateGameObserveAuth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateGameObserveAuth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateGameObserveAuth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateGameObserveAuth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateGameObserveAuth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateGameObserveAuth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            return null;
        };

        ReqCreateGameObserveAuth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateGameObserveAuth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateGameObserveAuth();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            return message;
        };

        ReqCreateGameObserveAuth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.game_uuid = "";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            return object;
        };

        ReqCreateGameObserveAuth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateGameObserveAuth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateGameObserveAuth";
        };

        return ReqCreateGameObserveAuth;
    })();

    lq.ResCreateGameObserveAuth = (function() {

        function ResCreateGameObserveAuth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateGameObserveAuth.prototype.error = null;
        ResCreateGameObserveAuth.prototype.token = "";
        ResCreateGameObserveAuth.prototype.location = "";

        ResCreateGameObserveAuth.create = function create(properties) {
            return new ResCreateGameObserveAuth(properties);
        };

        ResCreateGameObserveAuth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(26).string(message.location);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateGameObserveAuth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateGameObserveAuth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateGameObserveAuth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.location = value;
                        else
                            delete message.location;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateGameObserveAuth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateGameObserveAuth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.location != null && message.hasOwnProperty("location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        ResCreateGameObserveAuth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateGameObserveAuth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateGameObserveAuth();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateGameObserveAuth.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.location != null)
                if (typeof object.location !== "string" || object.location.length)
                    message.location = String(object.location);
            return message;
        };

        ResCreateGameObserveAuth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.token = "";
                object.location = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.location != null && message.hasOwnProperty("location"))
                object.location = message.location;
            return object;
        };

        ResCreateGameObserveAuth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateGameObserveAuth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateGameObserveAuth";
        };

        return ResCreateGameObserveAuth;
    })();

    lq.ReqRefreshGameObserveAuth = (function() {

        function ReqRefreshGameObserveAuth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRefreshGameObserveAuth.prototype.token = "";

        ReqRefreshGameObserveAuth.create = function create(properties) {
            return new ReqRefreshGameObserveAuth(properties);
        };

        ReqRefreshGameObserveAuth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(10).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRefreshGameObserveAuth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRefreshGameObserveAuth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRefreshGameObserveAuth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRefreshGameObserveAuth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRefreshGameObserveAuth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        ReqRefreshGameObserveAuth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRefreshGameObserveAuth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRefreshGameObserveAuth();
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            return message;
        };

        ReqRefreshGameObserveAuth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.token = "";
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        ReqRefreshGameObserveAuth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRefreshGameObserveAuth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRefreshGameObserveAuth";
        };

        return ReqRefreshGameObserveAuth;
    })();

    lq.ResRefreshGameObserveAuth = (function() {

        function ResRefreshGameObserveAuth(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRefreshGameObserveAuth.prototype.error = null;
        ResRefreshGameObserveAuth.prototype.ttl = 0;

        ResRefreshGameObserveAuth.create = function create(properties) {
            return new ResRefreshGameObserveAuth(properties);
        };

        ResRefreshGameObserveAuth.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.ttl != null && Object.hasOwnProperty.call(message, "ttl"))
                writer.uint32(16).uint32(message.ttl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRefreshGameObserveAuth.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRefreshGameObserveAuth.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRefreshGameObserveAuth(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ttl = value;
                        else
                            delete message.ttl;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRefreshGameObserveAuth.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRefreshGameObserveAuth.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.ttl != null && message.hasOwnProperty("ttl"))
                if (!$util.isInteger(message.ttl))
                    return "ttl: integer expected";
            return null;
        };

        ResRefreshGameObserveAuth.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRefreshGameObserveAuth)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRefreshGameObserveAuth();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRefreshGameObserveAuth.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.ttl != null)
                if (Number(object.ttl) !== 0)
                    message.ttl = object.ttl >>> 0;
            return message;
        };

        ResRefreshGameObserveAuth.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.ttl = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.ttl != null && message.hasOwnProperty("ttl"))
                object.ttl = message.ttl;
            return object;
        };

        ResRefreshGameObserveAuth.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRefreshGameObserveAuth.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRefreshGameObserveAuth";
        };

        return ResRefreshGameObserveAuth;
    })();

    lq.ResActivityBuff = (function() {

        function ResActivityBuff(properties) {
            this.buff_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResActivityBuff.prototype.error = null;
        ResActivityBuff.prototype.buff_list = $util.emptyArray;

        ResActivityBuff.create = function create(properties) {
            return new ResActivityBuff(properties);
        };

        ResActivityBuff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.buff_list != null && message.buff_list.length)
                for (let i = 0; i < message.buff_list.length; ++i)
                    $root.lq.ActivityBuffData.encode(message.buff_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResActivityBuff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResActivityBuff.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResActivityBuff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buff_list && message.buff_list.length))
                            message.buff_list = [];
                        message.buff_list.push($root.lq.ActivityBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResActivityBuff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResActivityBuff.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.buff_list != null && message.hasOwnProperty("buff_list")) {
                if (!Array.isArray(message.buff_list))
                    return "buff_list: array expected";
                for (let i = 0; i < message.buff_list.length; ++i) {
                    let error = $root.lq.ActivityBuffData.verify(message.buff_list[i], _depth + 1);
                    if (error)
                        return "buff_list." + error;
                }
            }
            return null;
        };

        ResActivityBuff.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResActivityBuff)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResActivityBuff();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResActivityBuff.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.buff_list) {
                if (!Array.isArray(object.buff_list))
                    throw TypeError(".lq.ResActivityBuff.buff_list: array expected");
                message.buff_list = Array(object.buff_list.length);
                for (let i = 0; i < object.buff_list.length; ++i) {
                    if (typeof object.buff_list[i] !== "object")
                        throw TypeError(".lq.ResActivityBuff.buff_list: object expected");
                    message.buff_list[i] = $root.lq.ActivityBuffData.fromObject(object.buff_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResActivityBuff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.buff_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.buff_list && message.buff_list.length) {
                object.buff_list = Array(message.buff_list.length);
                for (let j = 0; j < message.buff_list.length; ++j)
                    object.buff_list[j] = $root.lq.ActivityBuffData.toObject(message.buff_list[j], options);
            }
            return object;
        };

        ResActivityBuff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResActivityBuff.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResActivityBuff";
        };

        return ResActivityBuff;
    })();

    lq.ReqUpgradeActivityBuff = (function() {

        function ReqUpgradeActivityBuff(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpgradeActivityBuff.prototype.buff_id = 0;

        ReqUpgradeActivityBuff.create = function create(properties) {
            return new ReqUpgradeActivityBuff(properties);
        };

        ReqUpgradeActivityBuff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.buff_id != null && Object.hasOwnProperty.call(message, "buff_id"))
                writer.uint32(8).uint32(message.buff_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpgradeActivityBuff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpgradeActivityBuff.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpgradeActivityBuff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.buff_id = value;
                        else
                            delete message.buff_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpgradeActivityBuff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpgradeActivityBuff.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                if (!$util.isInteger(message.buff_id))
                    return "buff_id: integer expected";
            return null;
        };

        ReqUpgradeActivityBuff.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpgradeActivityBuff)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpgradeActivityBuff();
            if (object.buff_id != null)
                if (Number(object.buff_id) !== 0)
                    message.buff_id = object.buff_id >>> 0;
            return message;
        };

        ReqUpgradeActivityBuff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.buff_id = 0;
            if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                object.buff_id = message.buff_id;
            return object;
        };

        ReqUpgradeActivityBuff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpgradeActivityBuff.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpgradeActivityBuff";
        };

        return ReqUpgradeActivityBuff;
    })();

    lq.ReqUpgradeActivityLevel = (function() {

        function ReqUpgradeActivityLevel(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpgradeActivityLevel.prototype.activity_id = 0;
        ReqUpgradeActivityLevel.prototype.group = 0;
        ReqUpgradeActivityLevel.prototype.count = 0;

        ReqUpgradeActivityLevel.create = function create(properties) {
            return new ReqUpgradeActivityLevel(properties);
        };

        ReqUpgradeActivityLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.group != null && Object.hasOwnProperty.call(message, "group"))
                writer.uint32(16).uint32(message.group);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(24).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpgradeActivityLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpgradeActivityLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpgradeActivityLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.group = value;
                        else
                            delete message.group;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpgradeActivityLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpgradeActivityLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.group != null && message.hasOwnProperty("group"))
                if (!$util.isInteger(message.group))
                    return "group: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqUpgradeActivityLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpgradeActivityLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpgradeActivityLevel();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.group != null)
                if (Number(object.group) !== 0)
                    message.group = object.group >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqUpgradeActivityLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.group = 0;
                object.count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.group != null && message.hasOwnProperty("group"))
                object.group = message.group;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqUpgradeActivityLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpgradeActivityLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpgradeActivityLevel";
        };

        return ReqUpgradeActivityLevel;
    })();

    lq.ReqSetActivityBuff = (function() {

        function ReqSetActivityBuff(properties) {
            this.buff_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetActivityBuff.prototype.buff_list = $util.emptyArray;
        ReqSetActivityBuff.prototype.activity_id = 0;

        ReqSetActivityBuff.create = function create(properties) {
            return new ReqSetActivityBuff(properties);
        };

        ReqSetActivityBuff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.buff_list != null && message.buff_list.length)
                for (let i = 0; i < message.buff_list.length; ++i)
                    $root.lq.ReqSetActivityBuff.BuffInfo.encode(message.buff_list[i], writer.uint32(10).fork()).ldelim();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetActivityBuff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetActivityBuff.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetActivityBuff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buff_list && message.buff_list.length))
                            message.buff_list = [];
                        message.buff_list.push($root.lq.ReqSetActivityBuff.BuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetActivityBuff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetActivityBuff.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.buff_list != null && message.hasOwnProperty("buff_list")) {
                if (!Array.isArray(message.buff_list))
                    return "buff_list: array expected";
                for (let i = 0; i < message.buff_list.length; ++i) {
                    let error = $root.lq.ReqSetActivityBuff.BuffInfo.verify(message.buff_list[i], _depth + 1);
                    if (error)
                        return "buff_list." + error;
                }
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSetActivityBuff.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetActivityBuff)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetActivityBuff();
            if (object.buff_list) {
                if (!Array.isArray(object.buff_list))
                    throw TypeError(".lq.ReqSetActivityBuff.buff_list: array expected");
                message.buff_list = Array(object.buff_list.length);
                for (let i = 0; i < object.buff_list.length; ++i) {
                    if (typeof object.buff_list[i] !== "object")
                        throw TypeError(".lq.ReqSetActivityBuff.buff_list: object expected");
                    message.buff_list[i] = $root.lq.ReqSetActivityBuff.BuffInfo.fromObject(object.buff_list[i], _depth + 1);
                }
            }
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSetActivityBuff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.buff_list = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.buff_list && message.buff_list.length) {
                object.buff_list = Array(message.buff_list.length);
                for (let j = 0; j < message.buff_list.length; ++j)
                    object.buff_list[j] = $root.lq.ReqSetActivityBuff.BuffInfo.toObject(message.buff_list[j], options);
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSetActivityBuff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetActivityBuff.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetActivityBuff";
        };

        ReqSetActivityBuff.BuffInfo = (function() {

            function BuffInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BuffInfo.prototype.buff_id = 0;
            BuffInfo.prototype.level = 0;

            BuffInfo.create = function create(properties) {
                return new BuffInfo(properties);
            };

            BuffInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.buff_id != null && Object.hasOwnProperty.call(message, "buff_id"))
                    writer.uint32(8).uint32(message.buff_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BuffInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BuffInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetActivityBuff.BuffInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.buff_id = value;
                            else
                                delete message.buff_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BuffInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BuffInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                    if (!$util.isInteger(message.buff_id))
                        return "buff_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                return null;
            };

            BuffInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqSetActivityBuff.BuffInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqSetActivityBuff.BuffInfo();
                if (object.buff_id != null)
                    if (Number(object.buff_id) !== 0)
                        message.buff_id = object.buff_id >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                return message;
            };

            BuffInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.buff_id = 0;
                    object.level = 0;
                }
                if (message.buff_id != null && message.hasOwnProperty("buff_id"))
                    object.buff_id = message.buff_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                return object;
            };

            BuffInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BuffInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqSetActivityBuff.BuffInfo";
            };

            return BuffInfo;
        })();

        return ReqSetActivityBuff;
    })();

    lq.ResUpgradeActivityLevel = (function() {

        function ResUpgradeActivityLevel(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResUpgradeActivityLevel.prototype.error = null;
        ResUpgradeActivityLevel.prototype.rewards = $util.emptyArray;

        ResUpgradeActivityLevel.create = function create(properties) {
            return new ResUpgradeActivityLevel(properties);
        };

        ResUpgradeActivityLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResUpgradeActivityLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResUpgradeActivityLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResUpgradeActivityLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResUpgradeActivityLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResUpgradeActivityLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResUpgradeActivityLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResUpgradeActivityLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResUpgradeActivityLevel();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResUpgradeActivityLevel.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResUpgradeActivityLevel.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResUpgradeActivityLevel.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResUpgradeActivityLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResUpgradeActivityLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResUpgradeActivityLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResUpgradeActivityLevel";
        };

        return ResUpgradeActivityLevel;
    })();

    lq.ReqReceiveUpgradeActivityReward = (function() {

        function ReqReceiveUpgradeActivityReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveUpgradeActivityReward.prototype.activity_id = 0;

        ReqReceiveUpgradeActivityReward.create = function create(properties) {
            return new ReqReceiveUpgradeActivityReward(properties);
        };

        ReqReceiveUpgradeActivityReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveUpgradeActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveUpgradeActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveUpgradeActivityReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveUpgradeActivityReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveUpgradeActivityReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqReceiveUpgradeActivityReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveUpgradeActivityReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveUpgradeActivityReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqReceiveUpgradeActivityReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqReceiveUpgradeActivityReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveUpgradeActivityReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveUpgradeActivityReward";
        };

        return ReqReceiveUpgradeActivityReward;
    })();

    lq.ResReceiveUpgradeActivityReward = (function() {

        function ResReceiveUpgradeActivityReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveUpgradeActivityReward.prototype.error = null;
        ResReceiveUpgradeActivityReward.prototype.rewards = $util.emptyArray;

        ResReceiveUpgradeActivityReward.create = function create(properties) {
            return new ResReceiveUpgradeActivityReward(properties);
        };

        ResReceiveUpgradeActivityReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveUpgradeActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveUpgradeActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveUpgradeActivityReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveUpgradeActivityReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveUpgradeActivityReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResReceiveUpgradeActivityReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveUpgradeActivityReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveUpgradeActivityReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveUpgradeActivityReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResReceiveUpgradeActivityReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResReceiveUpgradeActivityReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveUpgradeActivityReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResReceiveUpgradeActivityReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveUpgradeActivityReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveUpgradeActivityReward";
        };

        return ResReceiveUpgradeActivityReward;
    })();

    lq.ReqReceiveAllActivityGift = (function() {

        function ReqReceiveAllActivityGift(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveAllActivityGift.prototype.activity_id = 0;

        ReqReceiveAllActivityGift.create = function create(properties) {
            return new ReqReceiveAllActivityGift(properties);
        };

        ReqReceiveAllActivityGift.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveAllActivityGift.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveAllActivityGift.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveAllActivityGift(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveAllActivityGift.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveAllActivityGift.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqReceiveAllActivityGift.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveAllActivityGift)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveAllActivityGift();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqReceiveAllActivityGift.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqReceiveAllActivityGift.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveAllActivityGift.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveAllActivityGift";
        };

        return ReqReceiveAllActivityGift;
    })();

    lq.ResReceiveAllActivityGift = (function() {

        function ResReceiveAllActivityGift(properties) {
            this.rewards = [];
            this.receive_gift = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveAllActivityGift.prototype.error = null;
        ResReceiveAllActivityGift.prototype.rewards = $util.emptyArray;
        ResReceiveAllActivityGift.prototype.receive_gift = $util.emptyArray;

        ResReceiveAllActivityGift.create = function create(properties) {
            return new ResReceiveAllActivityGift(properties);
        };

        ResReceiveAllActivityGift.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.receive_gift != null && message.receive_gift.length)
                for (let i = 0; i < message.receive_gift.length; ++i)
                    $root.lq.ResReceiveAllActivityGift.ReceiveRewards.encode(message.receive_gift[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveAllActivityGift.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveAllActivityGift.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveAllActivityGift(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.receive_gift && message.receive_gift.length))
                            message.receive_gift = [];
                        message.receive_gift.push($root.lq.ResReceiveAllActivityGift.ReceiveRewards.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveAllActivityGift.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveAllActivityGift.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.receive_gift != null && message.hasOwnProperty("receive_gift")) {
                if (!Array.isArray(message.receive_gift))
                    return "receive_gift: array expected";
                for (let i = 0; i < message.receive_gift.length; ++i) {
                    let error = $root.lq.ResReceiveAllActivityGift.ReceiveRewards.verify(message.receive_gift[i], _depth + 1);
                    if (error)
                        return "receive_gift." + error;
                }
            }
            return null;
        };

        ResReceiveAllActivityGift.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveAllActivityGift)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveAllActivityGift();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveAllActivityGift.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResReceiveAllActivityGift.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResReceiveAllActivityGift.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.receive_gift) {
                if (!Array.isArray(object.receive_gift))
                    throw TypeError(".lq.ResReceiveAllActivityGift.receive_gift: array expected");
                message.receive_gift = Array(object.receive_gift.length);
                for (let i = 0; i < object.receive_gift.length; ++i) {
                    if (typeof object.receive_gift[i] !== "object")
                        throw TypeError(".lq.ResReceiveAllActivityGift.receive_gift: object expected");
                    message.receive_gift[i] = $root.lq.ResReceiveAllActivityGift.ReceiveRewards.fromObject(object.receive_gift[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveAllActivityGift.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.rewards = [];
                object.receive_gift = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            if (message.receive_gift && message.receive_gift.length) {
                object.receive_gift = Array(message.receive_gift.length);
                for (let j = 0; j < message.receive_gift.length; ++j)
                    object.receive_gift[j] = $root.lq.ResReceiveAllActivityGift.ReceiveRewards.toObject(message.receive_gift[j], options);
            }
            return object;
        };

        ResReceiveAllActivityGift.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveAllActivityGift.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveAllActivityGift";
        };

        ResReceiveAllActivityGift.ReceiveRewards = (function() {

            function ReceiveRewards(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ReceiveRewards.prototype.id = 0;
            ReceiveRewards.prototype.from_account_id = 0;
            ReceiveRewards.prototype.item_id = 0;
            ReceiveRewards.prototype.count = 0;

            ReceiveRewards.create = function create(properties) {
                return new ReceiveRewards(properties);
            };

            ReceiveRewards.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.from_account_id != null && Object.hasOwnProperty.call(message, "from_account_id"))
                    writer.uint32(16).uint32(message.from_account_id);
                if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                    writer.uint32(24).uint32(message.item_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(32).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ReceiveRewards.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ReceiveRewards.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveAllActivityGift.ReceiveRewards(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from_account_id = value;
                            else
                                delete message.from_account_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item_id = value;
                            else
                                delete message.item_id;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ReceiveRewards.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ReceiveRewards.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    if (!$util.isInteger(message.from_account_id))
                        return "from_account_id: integer expected";
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    if (!$util.isInteger(message.item_id))
                        return "item_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            ReceiveRewards.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResReceiveAllActivityGift.ReceiveRewards)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResReceiveAllActivityGift.ReceiveRewards();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.from_account_id != null)
                    if (Number(object.from_account_id) !== 0)
                        message.from_account_id = object.from_account_id >>> 0;
                if (object.item_id != null)
                    if (Number(object.item_id) !== 0)
                        message.item_id = object.item_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            ReceiveRewards.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.from_account_id = 0;
                    object.item_id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.from_account_id != null && message.hasOwnProperty("from_account_id"))
                    object.from_account_id = message.from_account_id;
                if (message.item_id != null && message.hasOwnProperty("item_id"))
                    object.item_id = message.item_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            ReceiveRewards.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ReceiveRewards.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResReceiveAllActivityGift.ReceiveRewards";
            };

            return ReceiveRewards;
        })();

        return ResReceiveAllActivityGift;
    })();

    lq.ResUpgradeChallenge = (function() {

        function ResUpgradeChallenge(properties) {
            this.task_progress = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResUpgradeChallenge.prototype.error = null;
        ResUpgradeChallenge.prototype.task_progress = $util.emptyArray;
        ResUpgradeChallenge.prototype.refresh_count = 0;
        ResUpgradeChallenge.prototype.level = 0;
        ResUpgradeChallenge.prototype.match_count = 0;
        ResUpgradeChallenge.prototype.ticket_id = 0;

        ResUpgradeChallenge.create = function create(properties) {
            return new ResUpgradeChallenge(properties);
        };

        ResUpgradeChallenge.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.task_progress != null && message.task_progress.length)
                for (let i = 0; i < message.task_progress.length; ++i)
                    $root.lq.TaskProgress.encode(message.task_progress[i], writer.uint32(18).fork()).ldelim();
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(32).uint32(message.level);
            if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                writer.uint32(40).uint32(message.match_count);
            if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                writer.uint32(48).uint32(message.ticket_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResUpgradeChallenge.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResUpgradeChallenge.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResUpgradeChallenge(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.task_progress && message.task_progress.length))
                            message.task_progress = [];
                        message.task_progress.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_count = value;
                        else
                            delete message.match_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_id = value;
                        else
                            delete message.ticket_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResUpgradeChallenge.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResUpgradeChallenge.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.task_progress != null && message.hasOwnProperty("task_progress")) {
                if (!Array.isArray(message.task_progress))
                    return "task_progress: array expected";
                for (let i = 0; i < message.task_progress.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.task_progress[i], _depth + 1);
                    if (error)
                        return "task_progress." + error;
                }
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                if (!$util.isInteger(message.match_count))
                    return "match_count: integer expected";
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                if (!$util.isInteger(message.ticket_id))
                    return "ticket_id: integer expected";
            return null;
        };

        ResUpgradeChallenge.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResUpgradeChallenge)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResUpgradeChallenge();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResUpgradeChallenge.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.task_progress) {
                if (!Array.isArray(object.task_progress))
                    throw TypeError(".lq.ResUpgradeChallenge.task_progress: array expected");
                message.task_progress = Array(object.task_progress.length);
                for (let i = 0; i < object.task_progress.length; ++i) {
                    if (typeof object.task_progress[i] !== "object")
                        throw TypeError(".lq.ResUpgradeChallenge.task_progress: object expected");
                    message.task_progress[i] = $root.lq.TaskProgress.fromObject(object.task_progress[i], _depth + 1);
                }
            }
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.match_count != null)
                if (Number(object.match_count) !== 0)
                    message.match_count = object.match_count >>> 0;
            if (object.ticket_id != null)
                if (Number(object.ticket_id) !== 0)
                    message.ticket_id = object.ticket_id >>> 0;
            return message;
        };

        ResUpgradeChallenge.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_progress = [];
            if (options.defaults) {
                object.error = null;
                object.refresh_count = 0;
                object.level = 0;
                object.match_count = 0;
                object.ticket_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.task_progress && message.task_progress.length) {
                object.task_progress = Array(message.task_progress.length);
                for (let j = 0; j < message.task_progress.length; ++j)
                    object.task_progress[j] = $root.lq.TaskProgress.toObject(message.task_progress[j], options);
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                object.match_count = message.match_count;
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                object.ticket_id = message.ticket_id;
            return object;
        };

        ResUpgradeChallenge.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResUpgradeChallenge.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResUpgradeChallenge";
        };

        return ResUpgradeChallenge;
    })();

    lq.ResRefreshChallenge = (function() {

        function ResRefreshChallenge(properties) {
            this.task_progress = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRefreshChallenge.prototype.error = null;
        ResRefreshChallenge.prototype.task_progress = $util.emptyArray;
        ResRefreshChallenge.prototype.refresh_count = 0;
        ResRefreshChallenge.prototype.level = 0;
        ResRefreshChallenge.prototype.match_count = 0;
        ResRefreshChallenge.prototype.ticket_id = 0;

        ResRefreshChallenge.create = function create(properties) {
            return new ResRefreshChallenge(properties);
        };

        ResRefreshChallenge.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.task_progress != null && message.task_progress.length)
                for (let i = 0; i < message.task_progress.length; ++i)
                    $root.lq.TaskProgress.encode(message.task_progress[i], writer.uint32(18).fork()).ldelim();
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(32).uint32(message.level);
            if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                writer.uint32(40).uint32(message.match_count);
            if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                writer.uint32(48).uint32(message.ticket_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRefreshChallenge.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRefreshChallenge.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRefreshChallenge(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.task_progress && message.task_progress.length))
                            message.task_progress = [];
                        message.task_progress.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_count = value;
                        else
                            delete message.match_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_id = value;
                        else
                            delete message.ticket_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRefreshChallenge.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRefreshChallenge.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.task_progress != null && message.hasOwnProperty("task_progress")) {
                if (!Array.isArray(message.task_progress))
                    return "task_progress: array expected";
                for (let i = 0; i < message.task_progress.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.task_progress[i], _depth + 1);
                    if (error)
                        return "task_progress." + error;
                }
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                if (!$util.isInteger(message.match_count))
                    return "match_count: integer expected";
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                if (!$util.isInteger(message.ticket_id))
                    return "ticket_id: integer expected";
            return null;
        };

        ResRefreshChallenge.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRefreshChallenge)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRefreshChallenge();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRefreshChallenge.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.task_progress) {
                if (!Array.isArray(object.task_progress))
                    throw TypeError(".lq.ResRefreshChallenge.task_progress: array expected");
                message.task_progress = Array(object.task_progress.length);
                for (let i = 0; i < object.task_progress.length; ++i) {
                    if (typeof object.task_progress[i] !== "object")
                        throw TypeError(".lq.ResRefreshChallenge.task_progress: object expected");
                    message.task_progress[i] = $root.lq.TaskProgress.fromObject(object.task_progress[i], _depth + 1);
                }
            }
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.match_count != null)
                if (Number(object.match_count) !== 0)
                    message.match_count = object.match_count >>> 0;
            if (object.ticket_id != null)
                if (Number(object.ticket_id) !== 0)
                    message.ticket_id = object.ticket_id >>> 0;
            return message;
        };

        ResRefreshChallenge.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.task_progress = [];
            if (options.defaults) {
                object.error = null;
                object.refresh_count = 0;
                object.level = 0;
                object.match_count = 0;
                object.ticket_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.task_progress && message.task_progress.length) {
                object.task_progress = Array(message.task_progress.length);
                for (let j = 0; j < message.task_progress.length; ++j)
                    object.task_progress[j] = $root.lq.TaskProgress.toObject(message.task_progress[j], options);
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                object.match_count = message.match_count;
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                object.ticket_id = message.ticket_id;
            return object;
        };

        ResRefreshChallenge.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRefreshChallenge.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRefreshChallenge";
        };

        return ResRefreshChallenge;
    })();

    lq.ResFetchChallengeInfo = (function() {

        function ResFetchChallengeInfo(properties) {
            this.task_progress = [];
            this.rewarded_season = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchChallengeInfo.prototype.error = null;
        ResFetchChallengeInfo.prototype.task_progress = $util.emptyArray;
        ResFetchChallengeInfo.prototype.refresh_count = 0;
        ResFetchChallengeInfo.prototype.level = 0;
        ResFetchChallengeInfo.prototype.match_count = 0;
        ResFetchChallengeInfo.prototype.ticket_id = 0;
        ResFetchChallengeInfo.prototype.rewarded_season = $util.emptyArray;

        ResFetchChallengeInfo.create = function create(properties) {
            return new ResFetchChallengeInfo(properties);
        };

        ResFetchChallengeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.task_progress != null && message.task_progress.length)
                for (let i = 0; i < message.task_progress.length; ++i)
                    $root.lq.TaskProgress.encode(message.task_progress[i], writer.uint32(18).fork()).ldelim();
            if (message.refresh_count != null && Object.hasOwnProperty.call(message, "refresh_count"))
                writer.uint32(24).uint32(message.refresh_count);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(32).uint32(message.level);
            if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                writer.uint32(40).uint32(message.match_count);
            if (message.ticket_id != null && Object.hasOwnProperty.call(message, "ticket_id"))
                writer.uint32(48).uint32(message.ticket_id);
            if (message.rewarded_season != null && message.rewarded_season.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.rewarded_season.length; ++i)
                    writer.uint32(message.rewarded_season[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchChallengeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchChallengeInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchChallengeInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.task_progress && message.task_progress.length))
                            message.task_progress = [];
                        message.task_progress.push($root.lq.TaskProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_count = value;
                        else
                            delete message.refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_count = value;
                        else
                            delete message.match_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ticket_id = value;
                        else
                            delete message.ticket_id;
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.rewarded_season && message.rewarded_season.length))
                                message.rewarded_season = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.rewarded_season.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.rewarded_season && message.rewarded_season.length))
                            message.rewarded_season = [];
                        message.rewarded_season.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchChallengeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchChallengeInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.task_progress != null && message.hasOwnProperty("task_progress")) {
                if (!Array.isArray(message.task_progress))
                    return "task_progress: array expected";
                for (let i = 0; i < message.task_progress.length; ++i) {
                    let error = $root.lq.TaskProgress.verify(message.task_progress[i], _depth + 1);
                    if (error)
                        return "task_progress." + error;
                }
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                if (!$util.isInteger(message.refresh_count))
                    return "refresh_count: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                if (!$util.isInteger(message.match_count))
                    return "match_count: integer expected";
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                if (!$util.isInteger(message.ticket_id))
                    return "ticket_id: integer expected";
            if (message.rewarded_season != null && message.hasOwnProperty("rewarded_season")) {
                if (!Array.isArray(message.rewarded_season))
                    return "rewarded_season: array expected";
                for (let i = 0; i < message.rewarded_season.length; ++i)
                    if (!$util.isInteger(message.rewarded_season[i]))
                        return "rewarded_season: integer[] expected";
            }
            return null;
        };

        ResFetchChallengeInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchChallengeInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchChallengeInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchChallengeInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.task_progress) {
                if (!Array.isArray(object.task_progress))
                    throw TypeError(".lq.ResFetchChallengeInfo.task_progress: array expected");
                message.task_progress = Array(object.task_progress.length);
                for (let i = 0; i < object.task_progress.length; ++i) {
                    if (typeof object.task_progress[i] !== "object")
                        throw TypeError(".lq.ResFetchChallengeInfo.task_progress: object expected");
                    message.task_progress[i] = $root.lq.TaskProgress.fromObject(object.task_progress[i], _depth + 1);
                }
            }
            if (object.refresh_count != null)
                if (Number(object.refresh_count) !== 0)
                    message.refresh_count = object.refresh_count >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.match_count != null)
                if (Number(object.match_count) !== 0)
                    message.match_count = object.match_count >>> 0;
            if (object.ticket_id != null)
                if (Number(object.ticket_id) !== 0)
                    message.ticket_id = object.ticket_id >>> 0;
            if (object.rewarded_season) {
                if (!Array.isArray(object.rewarded_season))
                    throw TypeError(".lq.ResFetchChallengeInfo.rewarded_season: array expected");
                message.rewarded_season = Array(object.rewarded_season.length);
                for (let i = 0; i < object.rewarded_season.length; ++i)
                    message.rewarded_season[i] = object.rewarded_season[i] >>> 0;
            }
            return message;
        };

        ResFetchChallengeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.task_progress = [];
                object.rewarded_season = [];
            }
            if (options.defaults) {
                object.error = null;
                object.refresh_count = 0;
                object.level = 0;
                object.match_count = 0;
                object.ticket_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.task_progress && message.task_progress.length) {
                object.task_progress = Array(message.task_progress.length);
                for (let j = 0; j < message.task_progress.length; ++j)
                    object.task_progress[j] = $root.lq.TaskProgress.toObject(message.task_progress[j], options);
            }
            if (message.refresh_count != null && message.hasOwnProperty("refresh_count"))
                object.refresh_count = message.refresh_count;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                object.match_count = message.match_count;
            if (message.ticket_id != null && message.hasOwnProperty("ticket_id"))
                object.ticket_id = message.ticket_id;
            if (message.rewarded_season && message.rewarded_season.length) {
                object.rewarded_season = Array(message.rewarded_season.length);
                for (let j = 0; j < message.rewarded_season.length; ++j)
                    object.rewarded_season[j] = message.rewarded_season[j];
            }
            return object;
        };

        ResFetchChallengeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchChallengeInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchChallengeInfo";
        };

        return ResFetchChallengeInfo;
    })();

    lq.ReqForceCompleteChallengeTask = (function() {

        function ReqForceCompleteChallengeTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqForceCompleteChallengeTask.prototype.task_id = 0;

        ReqForceCompleteChallengeTask.create = function create(properties) {
            return new ReqForceCompleteChallengeTask(properties);
        };

        ReqForceCompleteChallengeTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqForceCompleteChallengeTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqForceCompleteChallengeTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqForceCompleteChallengeTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqForceCompleteChallengeTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqForceCompleteChallengeTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            return null;
        };

        ReqForceCompleteChallengeTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqForceCompleteChallengeTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqForceCompleteChallengeTask();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            return message;
        };

        ReqForceCompleteChallengeTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.task_id = 0;
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            return object;
        };

        ReqForceCompleteChallengeTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqForceCompleteChallengeTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqForceCompleteChallengeTask";
        };

        return ReqForceCompleteChallengeTask;
    })();

    lq.ResFetchABMatch = (function() {

        function ResFetchABMatch(properties) {
            this.match_max_point = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchABMatch.prototype.error = null;
        ResFetchABMatch.prototype.match_id = 0;
        ResFetchABMatch.prototype.match_count = 0;
        ResFetchABMatch.prototype.buy_in_count = 0;
        ResFetchABMatch.prototype.point = 0;
        ResFetchABMatch.prototype.rewarded = false;
        ResFetchABMatch.prototype.match_max_point = $util.emptyArray;
        ResFetchABMatch.prototype.quit = false;

        ResFetchABMatch.create = function create(properties) {
            return new ResFetchABMatch(properties);
        };

        ResFetchABMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.match_id != null && Object.hasOwnProperty.call(message, "match_id"))
                writer.uint32(16).uint32(message.match_id);
            if (message.match_count != null && Object.hasOwnProperty.call(message, "match_count"))
                writer.uint32(24).uint32(message.match_count);
            if (message.buy_in_count != null && Object.hasOwnProperty.call(message, "buy_in_count"))
                writer.uint32(32).uint32(message.buy_in_count);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(40).uint32(message.point);
            if (message.rewarded != null && Object.hasOwnProperty.call(message, "rewarded"))
                writer.uint32(48).bool(message.rewarded);
            if (message.match_max_point != null && message.match_max_point.length)
                for (let i = 0; i < message.match_max_point.length; ++i)
                    $root.lq.ResFetchABMatch.MatchPoint.encode(message.match_max_point[i], writer.uint32(58).fork()).ldelim();
            if (message.quit != null && Object.hasOwnProperty.call(message, "quit"))
                writer.uint32(64).bool(message.quit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchABMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchABMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchABMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_id = value;
                        else
                            delete message.match_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_count = value;
                        else
                            delete message.match_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.buy_in_count = value;
                        else
                            delete message.buy_in_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.rewarded = value;
                        else
                            delete message.rewarded;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.match_max_point && message.match_max_point.length))
                            message.match_max_point = [];
                        message.match_max_point.push($root.lq.ResFetchABMatch.MatchPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.quit = value;
                        else
                            delete message.quit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchABMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchABMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.match_id != null && message.hasOwnProperty("match_id"))
                if (!$util.isInteger(message.match_id))
                    return "match_id: integer expected";
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                if (!$util.isInteger(message.match_count))
                    return "match_count: integer expected";
            if (message.buy_in_count != null && message.hasOwnProperty("buy_in_count"))
                if (!$util.isInteger(message.buy_in_count))
                    return "buy_in_count: integer expected";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isInteger(message.point))
                    return "point: integer expected";
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                if (typeof message.rewarded !== "boolean")
                    return "rewarded: boolean expected";
            if (message.match_max_point != null && message.hasOwnProperty("match_max_point")) {
                if (!Array.isArray(message.match_max_point))
                    return "match_max_point: array expected";
                for (let i = 0; i < message.match_max_point.length; ++i) {
                    let error = $root.lq.ResFetchABMatch.MatchPoint.verify(message.match_max_point[i], _depth + 1);
                    if (error)
                        return "match_max_point." + error;
                }
            }
            if (message.quit != null && message.hasOwnProperty("quit"))
                if (typeof message.quit !== "boolean")
                    return "quit: boolean expected";
            return null;
        };

        ResFetchABMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchABMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchABMatch();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchABMatch.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.match_id != null)
                if (Number(object.match_id) !== 0)
                    message.match_id = object.match_id >>> 0;
            if (object.match_count != null)
                if (Number(object.match_count) !== 0)
                    message.match_count = object.match_count >>> 0;
            if (object.buy_in_count != null)
                if (Number(object.buy_in_count) !== 0)
                    message.buy_in_count = object.buy_in_count >>> 0;
            if (object.point != null)
                if (Number(object.point) !== 0)
                    message.point = object.point >>> 0;
            if (object.rewarded != null)
                if (object.rewarded)
                    message.rewarded = Boolean(object.rewarded);
            if (object.match_max_point) {
                if (!Array.isArray(object.match_max_point))
                    throw TypeError(".lq.ResFetchABMatch.match_max_point: array expected");
                message.match_max_point = Array(object.match_max_point.length);
                for (let i = 0; i < object.match_max_point.length; ++i) {
                    if (typeof object.match_max_point[i] !== "object")
                        throw TypeError(".lq.ResFetchABMatch.match_max_point: object expected");
                    message.match_max_point[i] = $root.lq.ResFetchABMatch.MatchPoint.fromObject(object.match_max_point[i], _depth + 1);
                }
            }
            if (object.quit != null)
                if (object.quit)
                    message.quit = Boolean(object.quit);
            return message;
        };

        ResFetchABMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.match_max_point = [];
            if (options.defaults) {
                object.error = null;
                object.match_id = 0;
                object.match_count = 0;
                object.buy_in_count = 0;
                object.point = 0;
                object.rewarded = false;
                object.quit = false;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.match_id != null && message.hasOwnProperty("match_id"))
                object.match_id = message.match_id;
            if (message.match_count != null && message.hasOwnProperty("match_count"))
                object.match_count = message.match_count;
            if (message.buy_in_count != null && message.hasOwnProperty("buy_in_count"))
                object.buy_in_count = message.buy_in_count;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.rewarded != null && message.hasOwnProperty("rewarded"))
                object.rewarded = message.rewarded;
            if (message.match_max_point && message.match_max_point.length) {
                object.match_max_point = Array(message.match_max_point.length);
                for (let j = 0; j < message.match_max_point.length; ++j)
                    object.match_max_point[j] = $root.lq.ResFetchABMatch.MatchPoint.toObject(message.match_max_point[j], options);
            }
            if (message.quit != null && message.hasOwnProperty("quit"))
                object.quit = message.quit;
            return object;
        };

        ResFetchABMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchABMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchABMatch";
        };

        ResFetchABMatch.MatchPoint = (function() {

            function MatchPoint(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MatchPoint.prototype.match_id = 0;
            MatchPoint.prototype.point = 0;

            MatchPoint.create = function create(properties) {
                return new MatchPoint(properties);
            };

            MatchPoint.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.match_id != null && Object.hasOwnProperty.call(message, "match_id"))
                    writer.uint32(8).uint32(message.match_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MatchPoint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MatchPoint.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchABMatch.MatchPoint(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.match_id = value;
                            else
                                delete message.match_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MatchPoint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MatchPoint.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.match_id != null && message.hasOwnProperty("match_id"))
                    if (!$util.isInteger(message.match_id))
                        return "match_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            MatchPoint.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchABMatch.MatchPoint)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchABMatch.MatchPoint();
                if (object.match_id != null)
                    if (Number(object.match_id) !== 0)
                        message.match_id = object.match_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            MatchPoint.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.match_id = 0;
                    object.point = 0;
                }
                if (message.match_id != null && message.hasOwnProperty("match_id"))
                    object.match_id = message.match_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            MatchPoint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MatchPoint.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchABMatch.MatchPoint";
            };

            return MatchPoint;
        })();

        return ResFetchABMatch;
    })();

    lq.ReqStartUnifiedMatch = (function() {

        function ReqStartUnifiedMatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStartUnifiedMatch.prototype.match_sid = "";
        ReqStartUnifiedMatch.prototype.client_version_string = "";

        ReqStartUnifiedMatch.create = function create(properties) {
            return new ReqStartUnifiedMatch(properties);
        };

        ReqStartUnifiedMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.match_sid != null && Object.hasOwnProperty.call(message, "match_sid"))
                writer.uint32(10).string(message.match_sid);
            if (message.client_version_string != null && Object.hasOwnProperty.call(message, "client_version_string"))
                writer.uint32(18).string(message.client_version_string);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStartUnifiedMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStartUnifiedMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStartUnifiedMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.match_sid = value;
                        else
                            delete message.match_sid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client_version_string = value;
                        else
                            delete message.client_version_string;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStartUnifiedMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStartUnifiedMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.match_sid != null && message.hasOwnProperty("match_sid"))
                if (!$util.isString(message.match_sid))
                    return "match_sid: string expected";
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                if (!$util.isString(message.client_version_string))
                    return "client_version_string: string expected";
            return null;
        };

        ReqStartUnifiedMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStartUnifiedMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStartUnifiedMatch();
            if (object.match_sid != null)
                if (typeof object.match_sid !== "string" || object.match_sid.length)
                    message.match_sid = String(object.match_sid);
            if (object.client_version_string != null)
                if (typeof object.client_version_string !== "string" || object.client_version_string.length)
                    message.client_version_string = String(object.client_version_string);
            return message;
        };

        ReqStartUnifiedMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.match_sid = "";
                object.client_version_string = "";
            }
            if (message.match_sid != null && message.hasOwnProperty("match_sid"))
                object.match_sid = message.match_sid;
            if (message.client_version_string != null && message.hasOwnProperty("client_version_string"))
                object.client_version_string = message.client_version_string;
            return object;
        };

        ReqStartUnifiedMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStartUnifiedMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStartUnifiedMatch";
        };

        return ReqStartUnifiedMatch;
    })();

    lq.ReqCancelUnifiedMatch = (function() {

        function ReqCancelUnifiedMatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCancelUnifiedMatch.prototype.match_sid = "";

        ReqCancelUnifiedMatch.create = function create(properties) {
            return new ReqCancelUnifiedMatch(properties);
        };

        ReqCancelUnifiedMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.match_sid != null && Object.hasOwnProperty.call(message, "match_sid"))
                writer.uint32(10).string(message.match_sid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCancelUnifiedMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCancelUnifiedMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCancelUnifiedMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.match_sid = value;
                        else
                            delete message.match_sid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCancelUnifiedMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCancelUnifiedMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.match_sid != null && message.hasOwnProperty("match_sid"))
                if (!$util.isString(message.match_sid))
                    return "match_sid: string expected";
            return null;
        };

        ReqCancelUnifiedMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCancelUnifiedMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCancelUnifiedMatch();
            if (object.match_sid != null)
                if (typeof object.match_sid !== "string" || object.match_sid.length)
                    message.match_sid = String(object.match_sid);
            return message;
        };

        ReqCancelUnifiedMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.match_sid = "";
            if (message.match_sid != null && message.hasOwnProperty("match_sid"))
                object.match_sid = message.match_sid;
            return object;
        };

        ReqCancelUnifiedMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCancelUnifiedMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCancelUnifiedMatch";
        };

        return ReqCancelUnifiedMatch;
    })();

    lq.ResChallengeSeasonInfo = (function() {

        function ResChallengeSeasonInfo(properties) {
            this.challenge_season_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResChallengeSeasonInfo.prototype.error = null;
        ResChallengeSeasonInfo.prototype.challenge_season_list = $util.emptyArray;

        ResChallengeSeasonInfo.create = function create(properties) {
            return new ResChallengeSeasonInfo(properties);
        };

        ResChallengeSeasonInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.challenge_season_list != null && message.challenge_season_list.length)
                for (let i = 0; i < message.challenge_season_list.length; ++i)
                    $root.lq.ResChallengeSeasonInfo.ChallengeInfo.encode(message.challenge_season_list[i], writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResChallengeSeasonInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResChallengeSeasonInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResChallengeSeasonInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.challenge_season_list && message.challenge_season_list.length))
                            message.challenge_season_list = [];
                        message.challenge_season_list.push($root.lq.ResChallengeSeasonInfo.ChallengeInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResChallengeSeasonInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResChallengeSeasonInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.challenge_season_list != null && message.hasOwnProperty("challenge_season_list")) {
                if (!Array.isArray(message.challenge_season_list))
                    return "challenge_season_list: array expected";
                for (let i = 0; i < message.challenge_season_list.length; ++i) {
                    let error = $root.lq.ResChallengeSeasonInfo.ChallengeInfo.verify(message.challenge_season_list[i], _depth + 1);
                    if (error)
                        return "challenge_season_list." + error;
                }
            }
            return null;
        };

        ResChallengeSeasonInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResChallengeSeasonInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResChallengeSeasonInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResChallengeSeasonInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.challenge_season_list) {
                if (!Array.isArray(object.challenge_season_list))
                    throw TypeError(".lq.ResChallengeSeasonInfo.challenge_season_list: array expected");
                message.challenge_season_list = Array(object.challenge_season_list.length);
                for (let i = 0; i < object.challenge_season_list.length; ++i) {
                    if (typeof object.challenge_season_list[i] !== "object")
                        throw TypeError(".lq.ResChallengeSeasonInfo.challenge_season_list: object expected");
                    message.challenge_season_list[i] = $root.lq.ResChallengeSeasonInfo.ChallengeInfo.fromObject(object.challenge_season_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResChallengeSeasonInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.challenge_season_list = [];
            if (options.defaults)
                object.error = null;
            if (message.challenge_season_list && message.challenge_season_list.length) {
                object.challenge_season_list = Array(message.challenge_season_list.length);
                for (let j = 0; j < message.challenge_season_list.length; ++j)
                    object.challenge_season_list[j] = $root.lq.ResChallengeSeasonInfo.ChallengeInfo.toObject(message.challenge_season_list[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResChallengeSeasonInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResChallengeSeasonInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResChallengeSeasonInfo";
        };

        ResChallengeSeasonInfo.ChallengeInfo = (function() {

            function ChallengeInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChallengeInfo.prototype.season_id = 0;
            ChallengeInfo.prototype.start_time = 0;
            ChallengeInfo.prototype.end_time = 0;
            ChallengeInfo.prototype.state = 0;

            ChallengeInfo.create = function create(properties) {
                return new ChallengeInfo(properties);
            };

            ChallengeInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.season_id != null && Object.hasOwnProperty.call(message, "season_id"))
                    writer.uint32(8).uint32(message.season_id);
                if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                    writer.uint32(16).uint32(message.start_time);
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(24).uint32(message.end_time);
                if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                    writer.uint32(32).uint32(message.state);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChallengeInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChallengeInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResChallengeSeasonInfo.ChallengeInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.season_id = value;
                            else
                                delete message.season_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.start_time = value;
                            else
                                delete message.start_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.state = value;
                            else
                                delete message.state;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChallengeInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChallengeInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.season_id != null && message.hasOwnProperty("season_id"))
                    if (!$util.isInteger(message.season_id))
                        return "season_id: integer expected";
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    if (!$util.isInteger(message.start_time))
                        return "start_time: integer expected";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.state != null && message.hasOwnProperty("state"))
                    if (!$util.isInteger(message.state))
                        return "state: integer expected";
                return null;
            };

            ChallengeInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResChallengeSeasonInfo.ChallengeInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResChallengeSeasonInfo.ChallengeInfo();
                if (object.season_id != null)
                    if (Number(object.season_id) !== 0)
                        message.season_id = object.season_id >>> 0;
                if (object.start_time != null)
                    if (Number(object.start_time) !== 0)
                        message.start_time = object.start_time >>> 0;
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.state != null)
                    if (Number(object.state) !== 0)
                        message.state = object.state >>> 0;
                return message;
            };

            ChallengeInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.season_id = 0;
                    object.start_time = 0;
                    object.end_time = 0;
                    object.state = 0;
                }
                if (message.season_id != null && message.hasOwnProperty("season_id"))
                    object.season_id = message.season_id;
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    object.start_time = message.start_time;
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.state != null && message.hasOwnProperty("state"))
                    object.state = message.state;
                return object;
            };

            ChallengeInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChallengeInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResChallengeSeasonInfo.ChallengeInfo";
            };

            return ChallengeInfo;
        })();

        return ResChallengeSeasonInfo;
    })();

    lq.ReqReceiveChallengeRankReward = (function() {

        function ReqReceiveChallengeRankReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveChallengeRankReward.prototype.season_id = 0;

        ReqReceiveChallengeRankReward.create = function create(properties) {
            return new ReqReceiveChallengeRankReward(properties);
        };

        ReqReceiveChallengeRankReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.season_id != null && Object.hasOwnProperty.call(message, "season_id"))
                writer.uint32(8).uint32(message.season_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveChallengeRankReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveChallengeRankReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveChallengeRankReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.season_id = value;
                        else
                            delete message.season_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveChallengeRankReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveChallengeRankReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.season_id != null && message.hasOwnProperty("season_id"))
                if (!$util.isInteger(message.season_id))
                    return "season_id: integer expected";
            return null;
        };

        ReqReceiveChallengeRankReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveChallengeRankReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveChallengeRankReward();
            if (object.season_id != null)
                if (Number(object.season_id) !== 0)
                    message.season_id = object.season_id >>> 0;
            return message;
        };

        ReqReceiveChallengeRankReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.season_id = 0;
            if (message.season_id != null && message.hasOwnProperty("season_id"))
                object.season_id = message.season_id;
            return object;
        };

        ReqReceiveChallengeRankReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveChallengeRankReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveChallengeRankReward";
        };

        return ReqReceiveChallengeRankReward;
    })();

    lq.ResReceiveChallengeRankReward = (function() {

        function ResReceiveChallengeRankReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveChallengeRankReward.prototype.error = null;
        ResReceiveChallengeRankReward.prototype.rewards = $util.emptyArray;

        ResReceiveChallengeRankReward.create = function create(properties) {
            return new ResReceiveChallengeRankReward(properties);
        };

        ResReceiveChallengeRankReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ResReceiveChallengeRankReward.Reward.encode(message.rewards[i], writer.uint32(10).fork()).ldelim();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveChallengeRankReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveChallengeRankReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveChallengeRankReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ResReceiveChallengeRankReward.Reward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveChallengeRankReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveChallengeRankReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ResReceiveChallengeRankReward.Reward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResReceiveChallengeRankReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveChallengeRankReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveChallengeRankReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveChallengeRankReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResReceiveChallengeRankReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResReceiveChallengeRankReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ResReceiveChallengeRankReward.Reward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveChallengeRankReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.error = null;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ResReceiveChallengeRankReward.Reward.toObject(message.rewards[j], options);
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResReceiveChallengeRankReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveChallengeRankReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveChallengeRankReward";
        };

        ResReceiveChallengeRankReward.Reward = (function() {

            function Reward(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Reward.prototype.resource_id = 0;
            Reward.prototype.count = 0;

            Reward.create = function create(properties) {
                return new Reward(properties);
            };

            Reward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.resource_id != null && Object.hasOwnProperty.call(message, "resource_id"))
                    writer.uint32(8).uint32(message.resource_id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Reward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Reward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveChallengeRankReward.Reward(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.resource_id = value;
                            else
                                delete message.resource_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Reward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Reward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    if (!$util.isInteger(message.resource_id))
                        return "resource_id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            Reward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResReceiveChallengeRankReward.Reward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResReceiveChallengeRankReward.Reward();
                if (object.resource_id != null)
                    if (Number(object.resource_id) !== 0)
                        message.resource_id = object.resource_id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            Reward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.resource_id = 0;
                    object.count = 0;
                }
                if (message.resource_id != null && message.hasOwnProperty("resource_id"))
                    object.resource_id = message.resource_id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            Reward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Reward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResReceiveChallengeRankReward.Reward";
            };

            return Reward;
        })();

        return ResReceiveChallengeRankReward;
    })();

    lq.ReqBuyInABMatch = (function() {

        function ReqBuyInABMatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyInABMatch.prototype.match_id = 0;

        ReqBuyInABMatch.create = function create(properties) {
            return new ReqBuyInABMatch(properties);
        };

        ReqBuyInABMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.match_id != null && Object.hasOwnProperty.call(message, "match_id"))
                writer.uint32(8).uint32(message.match_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyInABMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyInABMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyInABMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_id = value;
                        else
                            delete message.match_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyInABMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyInABMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.match_id != null && message.hasOwnProperty("match_id"))
                if (!$util.isInteger(message.match_id))
                    return "match_id: integer expected";
            return null;
        };

        ReqBuyInABMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyInABMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyInABMatch();
            if (object.match_id != null)
                if (Number(object.match_id) !== 0)
                    message.match_id = object.match_id >>> 0;
            return message;
        };

        ReqBuyInABMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.match_id = 0;
            if (message.match_id != null && message.hasOwnProperty("match_id"))
                object.match_id = message.match_id;
            return object;
        };

        ReqBuyInABMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyInABMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyInABMatch";
        };

        return ReqBuyInABMatch;
    })();

    lq.ReqGamePointRank = (function() {

        function ReqGamePointRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGamePointRank.prototype.activity_id = 0;

        ReqGamePointRank.create = function create(properties) {
            return new ReqGamePointRank(properties);
        };

        ReqGamePointRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGamePointRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGamePointRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGamePointRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGamePointRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGamePointRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqGamePointRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGamePointRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGamePointRank();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqGamePointRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqGamePointRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGamePointRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGamePointRank";
        };

        return ReqGamePointRank;
    })();

    lq.ResGamePointRank = (function() {

        function ResGamePointRank(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGamePointRank.prototype.error = null;
        ResGamePointRank.prototype.rank = $util.emptyArray;
        ResGamePointRank.prototype.self_rank = 0;

        ResGamePointRank.create = function create(properties) {
            return new ResGamePointRank(properties);
        };

        ResGamePointRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rank != null && message.rank.length)
                for (let i = 0; i < message.rank.length; ++i)
                    $root.lq.ResGamePointRank.RankInfo.encode(message.rank[i], writer.uint32(18).fork()).ldelim();
            if (message.self_rank != null && Object.hasOwnProperty.call(message, "self_rank"))
                writer.uint32(24).uint32(message.self_rank);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGamePointRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGamePointRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGamePointRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push($root.lq.ResGamePointRank.RankInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.self_rank = value;
                        else
                            delete message.self_rank;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGamePointRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGamePointRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i) {
                    let error = $root.lq.ResGamePointRank.RankInfo.verify(message.rank[i], _depth + 1);
                    if (error)
                        return "rank." + error;
                }
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                if (!$util.isInteger(message.self_rank))
                    return "self_rank: integer expected";
            return null;
        };

        ResGamePointRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGamePointRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGamePointRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGamePointRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.ResGamePointRank.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i) {
                    if (typeof object.rank[i] !== "object")
                        throw TypeError(".lq.ResGamePointRank.rank: object expected");
                    message.rank[i] = $root.lq.ResGamePointRank.RankInfo.fromObject(object.rank[i], _depth + 1);
                }
            }
            if (object.self_rank != null)
                if (Number(object.self_rank) !== 0)
                    message.self_rank = object.self_rank >>> 0;
            return message;
        };

        ResGamePointRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults) {
                object.error = null;
                object.self_rank = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = $root.lq.ResGamePointRank.RankInfo.toObject(message.rank[j], options);
            }
            if (message.self_rank != null && message.hasOwnProperty("self_rank"))
                object.self_rank = message.self_rank;
            return object;
        };

        ResGamePointRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGamePointRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGamePointRank";
        };

        ResGamePointRank.RankInfo = (function() {

            function RankInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RankInfo.prototype.account_id = 0;
            RankInfo.prototype.point = 0;

            RankInfo.create = function create(properties) {
                return new RankInfo(properties);
            };

            RankInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RankInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RankInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGamePointRank.RankInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RankInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RankInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            RankInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResGamePointRank.RankInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResGamePointRank.RankInfo();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            RankInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.point = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            RankInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RankInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResGamePointRank.RankInfo";
            };

            return RankInfo;
        })();

        return ResGamePointRank;
    })();

    lq.ResFetchSelfGamePointRank = (function() {

        function ResFetchSelfGamePointRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSelfGamePointRank.prototype.error = null;
        ResFetchSelfGamePointRank.prototype.self_rate = 0;

        ResFetchSelfGamePointRank.create = function create(properties) {
            return new ResFetchSelfGamePointRank(properties);
        };

        ResFetchSelfGamePointRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.self_rate != null && Object.hasOwnProperty.call(message, "self_rate"))
                writer.uint32(16).uint32(message.self_rate);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSelfGamePointRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSelfGamePointRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSelfGamePointRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.self_rate = value;
                        else
                            delete message.self_rate;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSelfGamePointRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSelfGamePointRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.self_rate != null && message.hasOwnProperty("self_rate"))
                if (!$util.isInteger(message.self_rate))
                    return "self_rate: integer expected";
            return null;
        };

        ResFetchSelfGamePointRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSelfGamePointRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSelfGamePointRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSelfGamePointRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.self_rate != null)
                if (Number(object.self_rate) !== 0)
                    message.self_rate = object.self_rate >>> 0;
            return message;
        };

        ResFetchSelfGamePointRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.self_rate = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.self_rate != null && message.hasOwnProperty("self_rate"))
                object.self_rate = message.self_rate;
            return object;
        };

        ResFetchSelfGamePointRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSelfGamePointRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSelfGamePointRank";
        };

        return ResFetchSelfGamePointRank;
    })();

    lq.ReqReadSNS = (function() {

        function ReqReadSNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReadSNS.prototype.id = 0;

        ReqReadSNS.create = function create(properties) {
            return new ReqReadSNS(properties);
        };

        ReqReadSNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReadSNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReadSNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReadSNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReadSNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReadSNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqReadSNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReadSNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReadSNS();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqReadSNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqReadSNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReadSNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReadSNS";
        };

        return ReqReadSNS;
    })();

    lq.ResReadSNS = (function() {

        function ResReadSNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReadSNS.prototype.error = null;
        ResReadSNS.prototype.sns_content = null;

        ResReadSNS.create = function create(properties) {
            return new ResReadSNS(properties);
        };

        ResReadSNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.sns_content != null && Object.hasOwnProperty.call(message, "sns_content"))
                $root.lq.SNSBlog.encode(message.sns_content, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReadSNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReadSNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReadSNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.sns_content = $root.lq.SNSBlog.decode(reader, reader.uint32(), undefined, _depth + 1, message.sns_content);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReadSNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReadSNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.sns_content != null && message.hasOwnProperty("sns_content")) {
                let error = $root.lq.SNSBlog.verify(message.sns_content, _depth + 1);
                if (error)
                    return "sns_content." + error;
            }
            return null;
        };

        ResReadSNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReadSNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReadSNS();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReadSNS.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.sns_content != null) {
                if (typeof object.sns_content !== "object")
                    throw TypeError(".lq.ResReadSNS.sns_content: object expected");
                message.sns_content = $root.lq.SNSBlog.fromObject(object.sns_content, _depth + 1);
            }
            return message;
        };

        ResReadSNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.sns_content = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.sns_content != null && message.hasOwnProperty("sns_content"))
                object.sns_content = $root.lq.SNSBlog.toObject(message.sns_content, options);
            return object;
        };

        ResReadSNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReadSNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReadSNS";
        };

        return ResReadSNS;
    })();

    lq.ReqReplySNS = (function() {

        function ReqReplySNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReplySNS.prototype.id = 0;

        ReqReplySNS.create = function create(properties) {
            return new ReqReplySNS(properties);
        };

        ReqReplySNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReplySNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReplySNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReplySNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReplySNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReplySNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqReplySNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReplySNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReplySNS();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqReplySNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqReplySNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReplySNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReplySNS";
        };

        return ReqReplySNS;
    })();

    lq.ResReplySNS = (function() {

        function ResReplySNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReplySNS.prototype.error = null;
        ResReplySNS.prototype.sns_reply = null;

        ResReplySNS.create = function create(properties) {
            return new ResReplySNS(properties);
        };

        ResReplySNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.sns_reply != null && Object.hasOwnProperty.call(message, "sns_reply"))
                $root.lq.SNSReply.encode(message.sns_reply, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReplySNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReplySNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReplySNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.sns_reply = $root.lq.SNSReply.decode(reader, reader.uint32(), undefined, _depth + 1, message.sns_reply);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReplySNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReplySNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.sns_reply != null && message.hasOwnProperty("sns_reply")) {
                let error = $root.lq.SNSReply.verify(message.sns_reply, _depth + 1);
                if (error)
                    return "sns_reply." + error;
            }
            return null;
        };

        ResReplySNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReplySNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReplySNS();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReplySNS.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.sns_reply != null) {
                if (typeof object.sns_reply !== "object")
                    throw TypeError(".lq.ResReplySNS.sns_reply: object expected");
                message.sns_reply = $root.lq.SNSReply.fromObject(object.sns_reply, _depth + 1);
            }
            return message;
        };

        ResReplySNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.sns_reply = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.sns_reply != null && message.hasOwnProperty("sns_reply"))
                object.sns_reply = $root.lq.SNSReply.toObject(message.sns_reply, options);
            return object;
        };

        ResReplySNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReplySNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReplySNS";
        };

        return ResReplySNS;
    })();

    lq.ReqLikeSNS = (function() {

        function ReqLikeSNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLikeSNS.prototype.id = 0;

        ReqLikeSNS.create = function create(properties) {
            return new ReqLikeSNS(properties);
        };

        ReqLikeSNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLikeSNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLikeSNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLikeSNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLikeSNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLikeSNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqLikeSNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLikeSNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLikeSNS();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqLikeSNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqLikeSNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLikeSNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLikeSNS";
        };

        return ReqLikeSNS;
    })();

    lq.ResLikeSNS = (function() {

        function ResLikeSNS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResLikeSNS.prototype.error = null;
        ResLikeSNS.prototype.is_liked = 0;

        ResLikeSNS.create = function create(properties) {
            return new ResLikeSNS(properties);
        };

        ResLikeSNS.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.is_liked != null && Object.hasOwnProperty.call(message, "is_liked"))
                writer.uint32(16).uint32(message.is_liked);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResLikeSNS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResLikeSNS.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResLikeSNS(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.is_liked = value;
                        else
                            delete message.is_liked;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResLikeSNS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResLikeSNS.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.is_liked != null && message.hasOwnProperty("is_liked"))
                if (!$util.isInteger(message.is_liked))
                    return "is_liked: integer expected";
            return null;
        };

        ResLikeSNS.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResLikeSNS)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResLikeSNS();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResLikeSNS.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.is_liked != null)
                if (Number(object.is_liked) !== 0)
                    message.is_liked = object.is_liked >>> 0;
            return message;
        };

        ResLikeSNS.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.is_liked = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.is_liked != null && message.hasOwnProperty("is_liked"))
                object.is_liked = message.is_liked;
            return object;
        };

        ResLikeSNS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResLikeSNS.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResLikeSNS";
        };

        return ResLikeSNS;
    })();

    lq.ReqDigMine = (function() {

        function ReqDigMine(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqDigMine.prototype.activity_id = 0;
        ReqDigMine.prototype.point = null;

        ReqDigMine.create = function create(properties) {
            return new ReqDigMine(properties);
        };

        ReqDigMine.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                $root.lq.Point.encode(message.point, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqDigMine.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqDigMine.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqDigMine(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.point = $root.lq.Point.decode(reader, reader.uint32(), undefined, _depth + 1, message.point);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqDigMine.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqDigMine.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.point != null && message.hasOwnProperty("point")) {
                let error = $root.lq.Point.verify(message.point, _depth + 1);
                if (error)
                    return "point." + error;
            }
            return null;
        };

        ReqDigMine.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqDigMine)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqDigMine();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.point != null) {
                if (typeof object.point !== "object")
                    throw TypeError(".lq.ReqDigMine.point: object expected");
                message.point = $root.lq.Point.fromObject(object.point, _depth + 1);
            }
            return message;
        };

        ReqDigMine.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.point = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = $root.lq.Point.toObject(message.point, options);
            return object;
        };

        ReqDigMine.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqDigMine.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqDigMine";
        };

        return ReqDigMine;
    })();

    lq.ResDigMine = (function() {

        function ResDigMine(properties) {
            this.map = [];
            this.reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDigMine.prototype.error = null;
        ResDigMine.prototype.map = $util.emptyArray;
        ResDigMine.prototype.reward = $util.emptyArray;

        ResDigMine.create = function create(properties) {
            return new ResDigMine(properties);
        };

        ResDigMine.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.map != null && message.map.length)
                for (let i = 0; i < message.map.length; ++i)
                    $root.lq.MineReward.encode(message.map[i], writer.uint32(18).fork()).ldelim();
            if (message.reward != null && message.reward.length)
                for (let i = 0; i < message.reward.length; ++i)
                    $root.lq.RewardSlot.encode(message.reward[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDigMine.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDigMine.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDigMine(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.map && message.map.length))
                            message.map = [];
                        message.map.push($root.lq.MineReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward && message.reward.length))
                            message.reward = [];
                        message.reward.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDigMine.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDigMine.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.map != null && message.hasOwnProperty("map")) {
                if (!Array.isArray(message.map))
                    return "map: array expected";
                for (let i = 0; i < message.map.length; ++i) {
                    let error = $root.lq.MineReward.verify(message.map[i], _depth + 1);
                    if (error)
                        return "map." + error;
                }
            }
            if (message.reward != null && message.hasOwnProperty("reward")) {
                if (!Array.isArray(message.reward))
                    return "reward: array expected";
                for (let i = 0; i < message.reward.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.reward[i], _depth + 1);
                    if (error)
                        return "reward." + error;
                }
            }
            return null;
        };

        ResDigMine.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDigMine)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDigMine();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDigMine.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.map) {
                if (!Array.isArray(object.map))
                    throw TypeError(".lq.ResDigMine.map: array expected");
                message.map = Array(object.map.length);
                for (let i = 0; i < object.map.length; ++i) {
                    if (typeof object.map[i] !== "object")
                        throw TypeError(".lq.ResDigMine.map: object expected");
                    message.map[i] = $root.lq.MineReward.fromObject(object.map[i], _depth + 1);
                }
            }
            if (object.reward) {
                if (!Array.isArray(object.reward))
                    throw TypeError(".lq.ResDigMine.reward: array expected");
                message.reward = Array(object.reward.length);
                for (let i = 0; i < object.reward.length; ++i) {
                    if (typeof object.reward[i] !== "object")
                        throw TypeError(".lq.ResDigMine.reward: object expected");
                    message.reward[i] = $root.lq.RewardSlot.fromObject(object.reward[i], _depth + 1);
                }
            }
            return message;
        };

        ResDigMine.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.map = [];
                object.reward = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.map && message.map.length) {
                object.map = Array(message.map.length);
                for (let j = 0; j < message.map.length; ++j)
                    object.map[j] = $root.lq.MineReward.toObject(message.map[j], options);
            }
            if (message.reward && message.reward.length) {
                object.reward = Array(message.reward.length);
                for (let j = 0; j < message.reward.length; ++j)
                    object.reward[j] = $root.lq.RewardSlot.toObject(message.reward[j], options);
            }
            return object;
        };

        ResDigMine.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDigMine.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDigMine";
        };

        return ResDigMine;
    })();

    lq.ReqFetchLastPrivacy = (function() {

        function ReqFetchLastPrivacy(properties) {
            this.type = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchLastPrivacy.prototype.type = $util.emptyArray;

        ReqFetchLastPrivacy.create = function create(properties) {
            return new ReqFetchLastPrivacy(properties);
        };

        ReqFetchLastPrivacy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && message.type.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.type.length; ++i)
                    writer.uint32(message.type[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchLastPrivacy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchLastPrivacy.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchLastPrivacy();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.type && message.type.length))
                                message.type = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.type.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.type && message.type.length))
                            message.type = [];
                        message.type.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchLastPrivacy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchLastPrivacy.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type")) {
                if (!Array.isArray(message.type))
                    return "type: array expected";
                for (let i = 0; i < message.type.length; ++i)
                    if (!$util.isInteger(message.type[i]))
                        return "type: integer[] expected";
            }
            return null;
        };

        ReqFetchLastPrivacy.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchLastPrivacy)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchLastPrivacy();
            if (object.type) {
                if (!Array.isArray(object.type))
                    throw TypeError(".lq.ReqFetchLastPrivacy.type: array expected");
                message.type = Array(object.type.length);
                for (let i = 0; i < object.type.length; ++i)
                    message.type[i] = object.type[i] >>> 0;
            }
            return message;
        };

        ReqFetchLastPrivacy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.type = [];
            if (message.type && message.type.length) {
                object.type = Array(message.type.length);
                for (let j = 0; j < message.type.length; ++j)
                    object.type[j] = message.type[j];
            }
            return object;
        };

        ReqFetchLastPrivacy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchLastPrivacy.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchLastPrivacy";
        };

        return ReqFetchLastPrivacy;
    })();

    lq.ResFetchLastPrivacy = (function() {

        function ResFetchLastPrivacy(properties) {
            this.privacy = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchLastPrivacy.prototype.error = null;
        ResFetchLastPrivacy.prototype.privacy = $util.emptyArray;

        ResFetchLastPrivacy.create = function create(properties) {
            return new ResFetchLastPrivacy(properties);
        };

        ResFetchLastPrivacy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.privacy != null && message.privacy.length)
                for (let i = 0; i < message.privacy.length; ++i)
                    $root.lq.ResFetchLastPrivacy.PrivacyInfo.encode(message.privacy[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchLastPrivacy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchLastPrivacy.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchLastPrivacy(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.privacy && message.privacy.length))
                            message.privacy = [];
                        message.privacy.push($root.lq.ResFetchLastPrivacy.PrivacyInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchLastPrivacy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchLastPrivacy.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.privacy != null && message.hasOwnProperty("privacy")) {
                if (!Array.isArray(message.privacy))
                    return "privacy: array expected";
                for (let i = 0; i < message.privacy.length; ++i) {
                    let error = $root.lq.ResFetchLastPrivacy.PrivacyInfo.verify(message.privacy[i], _depth + 1);
                    if (error)
                        return "privacy." + error;
                }
            }
            return null;
        };

        ResFetchLastPrivacy.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchLastPrivacy)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchLastPrivacy();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchLastPrivacy.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.privacy) {
                if (!Array.isArray(object.privacy))
                    throw TypeError(".lq.ResFetchLastPrivacy.privacy: array expected");
                message.privacy = Array(object.privacy.length);
                for (let i = 0; i < object.privacy.length; ++i) {
                    if (typeof object.privacy[i] !== "object")
                        throw TypeError(".lq.ResFetchLastPrivacy.privacy: object expected");
                    message.privacy[i] = $root.lq.ResFetchLastPrivacy.PrivacyInfo.fromObject(object.privacy[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchLastPrivacy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.privacy = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.privacy && message.privacy.length) {
                object.privacy = Array(message.privacy.length);
                for (let j = 0; j < message.privacy.length; ++j)
                    object.privacy[j] = $root.lq.ResFetchLastPrivacy.PrivacyInfo.toObject(message.privacy[j], options);
            }
            return object;
        };

        ResFetchLastPrivacy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchLastPrivacy.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchLastPrivacy";
        };

        ResFetchLastPrivacy.PrivacyInfo = (function() {

            function PrivacyInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PrivacyInfo.prototype.type = 0;
            PrivacyInfo.prototype.version = "";

            PrivacyInfo.create = function create(properties) {
                return new PrivacyInfo(properties);
            };

            PrivacyInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(18).string(message.version);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PrivacyInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PrivacyInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchLastPrivacy.PrivacyInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PrivacyInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PrivacyInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.version != null && message.hasOwnProperty("version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                return null;
            };

            PrivacyInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchLastPrivacy.PrivacyInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchLastPrivacy.PrivacyInfo();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.version != null)
                    if (typeof object.version !== "string" || object.version.length)
                        message.version = String(object.version);
                return message;
            };

            PrivacyInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.version = "";
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.version != null && message.hasOwnProperty("version"))
                    object.version = message.version;
                return object;
            };

            PrivacyInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrivacyInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchLastPrivacy.PrivacyInfo";
            };

            return PrivacyInfo;
        })();

        return ResFetchLastPrivacy;
    })();

    lq.ReqCheckPrivacy = (function() {

        function ReqCheckPrivacy(properties) {
            this.versions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCheckPrivacy.prototype.device_type = "";
        ReqCheckPrivacy.prototype.versions = $util.emptyArray;

        ReqCheckPrivacy.create = function create(properties) {
            return new ReqCheckPrivacy(properties);
        };

        ReqCheckPrivacy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.device_type != null && Object.hasOwnProperty.call(message, "device_type"))
                writer.uint32(10).string(message.device_type);
            if (message.versions != null && message.versions.length)
                for (let i = 0; i < message.versions.length; ++i)
                    $root.lq.ReqCheckPrivacy.Versions.encode(message.versions[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCheckPrivacy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCheckPrivacy.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCheckPrivacy(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.device_type = value;
                        else
                            delete message.device_type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.versions && message.versions.length))
                            message.versions = [];
                        message.versions.push($root.lq.ReqCheckPrivacy.Versions.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCheckPrivacy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCheckPrivacy.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.device_type != null && message.hasOwnProperty("device_type"))
                if (!$util.isString(message.device_type))
                    return "device_type: string expected";
            if (message.versions != null && message.hasOwnProperty("versions")) {
                if (!Array.isArray(message.versions))
                    return "versions: array expected";
                for (let i = 0; i < message.versions.length; ++i) {
                    let error = $root.lq.ReqCheckPrivacy.Versions.verify(message.versions[i], _depth + 1);
                    if (error)
                        return "versions." + error;
                }
            }
            return null;
        };

        ReqCheckPrivacy.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCheckPrivacy)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCheckPrivacy();
            if (object.device_type != null)
                if (typeof object.device_type !== "string" || object.device_type.length)
                    message.device_type = String(object.device_type);
            if (object.versions) {
                if (!Array.isArray(object.versions))
                    throw TypeError(".lq.ReqCheckPrivacy.versions: array expected");
                message.versions = Array(object.versions.length);
                for (let i = 0; i < object.versions.length; ++i) {
                    if (typeof object.versions[i] !== "object")
                        throw TypeError(".lq.ReqCheckPrivacy.versions: object expected");
                    message.versions[i] = $root.lq.ReqCheckPrivacy.Versions.fromObject(object.versions[i], _depth + 1);
                }
            }
            return message;
        };

        ReqCheckPrivacy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.versions = [];
            if (options.defaults)
                object.device_type = "";
            if (message.device_type != null && message.hasOwnProperty("device_type"))
                object.device_type = message.device_type;
            if (message.versions && message.versions.length) {
                object.versions = Array(message.versions.length);
                for (let j = 0; j < message.versions.length; ++j)
                    object.versions[j] = $root.lq.ReqCheckPrivacy.Versions.toObject(message.versions[j], options);
            }
            return object;
        };

        ReqCheckPrivacy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCheckPrivacy.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCheckPrivacy";
        };

        ReqCheckPrivacy.Versions = (function() {

            function Versions(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Versions.prototype.version = "";
            Versions.prototype.type = 0;

            Versions.create = function create(properties) {
                return new Versions(properties);
            };

            Versions.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(10).string(message.version);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(24).uint32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Versions.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Versions.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCheckPrivacy.Versions(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.version = value;
                            else
                                delete message.version;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Versions.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Versions.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.version != null && message.hasOwnProperty("version"))
                    if (!$util.isString(message.version))
                        return "version: string expected";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                return null;
            };

            Versions.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqCheckPrivacy.Versions)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqCheckPrivacy.Versions();
                if (object.version != null)
                    if (typeof object.version !== "string" || object.version.length)
                        message.version = String(object.version);
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                return message;
            };

            Versions.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.version = "";
                    object.type = 0;
                }
                if (message.version != null && message.hasOwnProperty("version"))
                    object.version = message.version;
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                return object;
            };

            Versions.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Versions.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqCheckPrivacy.Versions";
            };

            return Versions;
        })();

        return ReqCheckPrivacy;
    })();

    lq.ReqFetchRPGBattleHistory = (function() {

        function ReqFetchRPGBattleHistory(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchRPGBattleHistory.prototype.activity_id = 0;

        ReqFetchRPGBattleHistory.create = function create(properties) {
            return new ReqFetchRPGBattleHistory(properties);
        };

        ReqFetchRPGBattleHistory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchRPGBattleHistory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchRPGBattleHistory.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchRPGBattleHistory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchRPGBattleHistory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchRPGBattleHistory.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchRPGBattleHistory.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchRPGBattleHistory)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchRPGBattleHistory();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchRPGBattleHistory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchRPGBattleHistory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchRPGBattleHistory.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchRPGBattleHistory";
        };

        return ReqFetchRPGBattleHistory;
    })();

    lq.ResFetchRPGBattleHistory = (function() {

        function ResFetchRPGBattleHistory(properties) {
            this.battle_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchRPGBattleHistory.prototype.error = null;
        ResFetchRPGBattleHistory.prototype.battle_result = $util.emptyArray;
        ResFetchRPGBattleHistory.prototype.start_state = null;
        ResFetchRPGBattleHistory.prototype.current_state = null;

        ResFetchRPGBattleHistory.create = function create(properties) {
            return new ResFetchRPGBattleHistory(properties);
        };

        ResFetchRPGBattleHistory.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.battle_result != null && message.battle_result.length)
                for (let i = 0; i < message.battle_result.length; ++i)
                    $root.lq.ResFetchRPGBattleHistory.BattleResult.encode(message.battle_result[i], writer.uint32(18).fork()).ldelim();
            if (message.start_state != null && Object.hasOwnProperty.call(message, "start_state"))
                $root.lq.RPGState.encode(message.start_state, writer.uint32(26).fork()).ldelim();
            if (message.current_state != null && Object.hasOwnProperty.call(message, "current_state"))
                $root.lq.RPGState.encode(message.current_state, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchRPGBattleHistory.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchRPGBattleHistory.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRPGBattleHistory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.battle_result && message.battle_result.length))
                            message.battle_result = [];
                        message.battle_result.push($root.lq.ResFetchRPGBattleHistory.BattleResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.start_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.start_state);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.current_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.current_state);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchRPGBattleHistory.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchRPGBattleHistory.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.battle_result != null && message.hasOwnProperty("battle_result")) {
                if (!Array.isArray(message.battle_result))
                    return "battle_result: array expected";
                for (let i = 0; i < message.battle_result.length; ++i) {
                    let error = $root.lq.ResFetchRPGBattleHistory.BattleResult.verify(message.battle_result[i], _depth + 1);
                    if (error)
                        return "battle_result." + error;
                }
            }
            if (message.start_state != null && message.hasOwnProperty("start_state")) {
                let error = $root.lq.RPGState.verify(message.start_state, _depth + 1);
                if (error)
                    return "start_state." + error;
            }
            if (message.current_state != null && message.hasOwnProperty("current_state")) {
                let error = $root.lq.RPGState.verify(message.current_state, _depth + 1);
                if (error)
                    return "current_state." + error;
            }
            return null;
        };

        ResFetchRPGBattleHistory.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchRPGBattleHistory)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchRPGBattleHistory();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistory.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.battle_result) {
                if (!Array.isArray(object.battle_result))
                    throw TypeError(".lq.ResFetchRPGBattleHistory.battle_result: array expected");
                message.battle_result = Array(object.battle_result.length);
                for (let i = 0; i < object.battle_result.length; ++i) {
                    if (typeof object.battle_result[i] !== "object")
                        throw TypeError(".lq.ResFetchRPGBattleHistory.battle_result: object expected");
                    message.battle_result[i] = $root.lq.ResFetchRPGBattleHistory.BattleResult.fromObject(object.battle_result[i], _depth + 1);
                }
            }
            if (object.start_state != null) {
                if (typeof object.start_state !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistory.start_state: object expected");
                message.start_state = $root.lq.RPGState.fromObject(object.start_state, _depth + 1);
            }
            if (object.current_state != null) {
                if (typeof object.current_state !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistory.current_state: object expected");
                message.current_state = $root.lq.RPGState.fromObject(object.current_state, _depth + 1);
            }
            return message;
        };

        ResFetchRPGBattleHistory.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.battle_result = [];
            if (options.defaults) {
                object.error = null;
                object.start_state = null;
                object.current_state = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.battle_result && message.battle_result.length) {
                object.battle_result = Array(message.battle_result.length);
                for (let j = 0; j < message.battle_result.length; ++j)
                    object.battle_result[j] = $root.lq.ResFetchRPGBattleHistory.BattleResult.toObject(message.battle_result[j], options);
            }
            if (message.start_state != null && message.hasOwnProperty("start_state"))
                object.start_state = $root.lq.RPGState.toObject(message.start_state, options);
            if (message.current_state != null && message.hasOwnProperty("current_state"))
                object.current_state = $root.lq.RPGState.toObject(message.current_state, options);
            return object;
        };

        ResFetchRPGBattleHistory.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchRPGBattleHistory.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchRPGBattleHistory";
        };

        ResFetchRPGBattleHistory.BattleResult = (function() {

            function BattleResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BattleResult.prototype.uuid = "";
            BattleResult.prototype.chang = 0;
            BattleResult.prototype.ju = 0;
            BattleResult.prototype.ben = 0;
            BattleResult.prototype.target = 0;
            BattleResult.prototype.damage = 0;
            BattleResult.prototype.heal = 0;
            BattleResult.prototype.monster_seq = 0;
            BattleResult.prototype.chain_atk = 0;
            BattleResult.prototype.killed = 0;
            BattleResult.prototype.is_luk = 0;
            BattleResult.prototype.is_dex = 0;
            BattleResult.prototype.is_extra = 0;
            BattleResult.prototype.reward = "";
            BattleResult.prototype.points = 0;
            BattleResult.prototype.is_zimo = 0;

            BattleResult.create = function create(properties) {
                return new BattleResult(properties);
            };

            BattleResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                    writer.uint32(8).uint32(message.chang);
                if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                    writer.uint32(16).uint32(message.ju);
                if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                    writer.uint32(24).uint32(message.ben);
                if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                    writer.uint32(32).uint32(message.target);
                if (message.damage != null && Object.hasOwnProperty.call(message, "damage"))
                    writer.uint32(40).uint32(message.damage);
                if (message.heal != null && Object.hasOwnProperty.call(message, "heal"))
                    writer.uint32(48).uint32(message.heal);
                if (message.monster_seq != null && Object.hasOwnProperty.call(message, "monster_seq"))
                    writer.uint32(56).uint32(message.monster_seq);
                if (message.chain_atk != null && Object.hasOwnProperty.call(message, "chain_atk"))
                    writer.uint32(64).uint32(message.chain_atk);
                if (message.killed != null && Object.hasOwnProperty.call(message, "killed"))
                    writer.uint32(72).uint32(message.killed);
                if (message.is_luk != null && Object.hasOwnProperty.call(message, "is_luk"))
                    writer.uint32(80).uint32(message.is_luk);
                if (message.is_dex != null && Object.hasOwnProperty.call(message, "is_dex"))
                    writer.uint32(88).uint32(message.is_dex);
                if (message.is_extra != null && Object.hasOwnProperty.call(message, "is_extra"))
                    writer.uint32(96).uint32(message.is_extra);
                if (message.reward != null && Object.hasOwnProperty.call(message, "reward"))
                    writer.uint32(106).string(message.reward);
                if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                    writer.uint32(114).string(message.uuid);
                if (message.points != null && Object.hasOwnProperty.call(message, "points"))
                    writer.uint32(120).uint32(message.points);
                if (message.is_zimo != null && Object.hasOwnProperty.call(message, "is_zimo"))
                    writer.uint32(128).uint32(message.is_zimo);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BattleResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BattleResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRPGBattleHistory.BattleResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 14: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.uuid = value;
                            else
                                delete message.uuid;
                            continue;
                        }
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chang = value;
                            else
                                delete message.chang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ju = value;
                            else
                                delete message.ju;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ben = value;
                            else
                                delete message.ben;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.target = value;
                            else
                                delete message.target;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.damage = value;
                            else
                                delete message.damage;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.heal = value;
                            else
                                delete message.heal;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.monster_seq = value;
                            else
                                delete message.monster_seq;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chain_atk = value;
                            else
                                delete message.chain_atk;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.killed = value;
                            else
                                delete message.killed;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.is_luk = value;
                            else
                                delete message.is_luk;
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.is_dex = value;
                            else
                                delete message.is_dex;
                            continue;
                        }
                    case 12: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.is_extra = value;
                            else
                                delete message.is_extra;
                            continue;
                        }
                    case 13: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.reward = value;
                            else
                                delete message.reward;
                            continue;
                        }
                    case 15: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.points = value;
                            else
                                delete message.points;
                            continue;
                        }
                    case 16: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.is_zimo = value;
                            else
                                delete message.is_zimo;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BattleResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BattleResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    if (!$util.isString(message.uuid))
                        return "uuid: string expected";
                if (message.chang != null && message.hasOwnProperty("chang"))
                    if (!$util.isInteger(message.chang))
                        return "chang: integer expected";
                if (message.ju != null && message.hasOwnProperty("ju"))
                    if (!$util.isInteger(message.ju))
                        return "ju: integer expected";
                if (message.ben != null && message.hasOwnProperty("ben"))
                    if (!$util.isInteger(message.ben))
                        return "ben: integer expected";
                if (message.target != null && message.hasOwnProperty("target"))
                    if (!$util.isInteger(message.target))
                        return "target: integer expected";
                if (message.damage != null && message.hasOwnProperty("damage"))
                    if (!$util.isInteger(message.damage))
                        return "damage: integer expected";
                if (message.heal != null && message.hasOwnProperty("heal"))
                    if (!$util.isInteger(message.heal))
                        return "heal: integer expected";
                if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                    if (!$util.isInteger(message.monster_seq))
                        return "monster_seq: integer expected";
                if (message.chain_atk != null && message.hasOwnProperty("chain_atk"))
                    if (!$util.isInteger(message.chain_atk))
                        return "chain_atk: integer expected";
                if (message.killed != null && message.hasOwnProperty("killed"))
                    if (!$util.isInteger(message.killed))
                        return "killed: integer expected";
                if (message.is_luk != null && message.hasOwnProperty("is_luk"))
                    if (!$util.isInteger(message.is_luk))
                        return "is_luk: integer expected";
                if (message.is_dex != null && message.hasOwnProperty("is_dex"))
                    if (!$util.isInteger(message.is_dex))
                        return "is_dex: integer expected";
                if (message.is_extra != null && message.hasOwnProperty("is_extra"))
                    if (!$util.isInteger(message.is_extra))
                        return "is_extra: integer expected";
                if (message.reward != null && message.hasOwnProperty("reward"))
                    if (!$util.isString(message.reward))
                        return "reward: string expected";
                if (message.points != null && message.hasOwnProperty("points"))
                    if (!$util.isInteger(message.points))
                        return "points: integer expected";
                if (message.is_zimo != null && message.hasOwnProperty("is_zimo"))
                    if (!$util.isInteger(message.is_zimo))
                        return "is_zimo: integer expected";
                return null;
            };

            BattleResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchRPGBattleHistory.BattleResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchRPGBattleHistory.BattleResult();
                if (object.uuid != null)
                    if (typeof object.uuid !== "string" || object.uuid.length)
                        message.uuid = String(object.uuid);
                if (object.chang != null)
                    if (Number(object.chang) !== 0)
                        message.chang = object.chang >>> 0;
                if (object.ju != null)
                    if (Number(object.ju) !== 0)
                        message.ju = object.ju >>> 0;
                if (object.ben != null)
                    if (Number(object.ben) !== 0)
                        message.ben = object.ben >>> 0;
                if (object.target != null)
                    if (Number(object.target) !== 0)
                        message.target = object.target >>> 0;
                if (object.damage != null)
                    if (Number(object.damage) !== 0)
                        message.damage = object.damage >>> 0;
                if (object.heal != null)
                    if (Number(object.heal) !== 0)
                        message.heal = object.heal >>> 0;
                if (object.monster_seq != null)
                    if (Number(object.monster_seq) !== 0)
                        message.monster_seq = object.monster_seq >>> 0;
                if (object.chain_atk != null)
                    if (Number(object.chain_atk) !== 0)
                        message.chain_atk = object.chain_atk >>> 0;
                if (object.killed != null)
                    if (Number(object.killed) !== 0)
                        message.killed = object.killed >>> 0;
                if (object.is_luk != null)
                    if (Number(object.is_luk) !== 0)
                        message.is_luk = object.is_luk >>> 0;
                if (object.is_dex != null)
                    if (Number(object.is_dex) !== 0)
                        message.is_dex = object.is_dex >>> 0;
                if (object.is_extra != null)
                    if (Number(object.is_extra) !== 0)
                        message.is_extra = object.is_extra >>> 0;
                if (object.reward != null)
                    if (typeof object.reward !== "string" || object.reward.length)
                        message.reward = String(object.reward);
                if (object.points != null)
                    if (Number(object.points) !== 0)
                        message.points = object.points >>> 0;
                if (object.is_zimo != null)
                    if (Number(object.is_zimo) !== 0)
                        message.is_zimo = object.is_zimo >>> 0;
                return message;
            };

            BattleResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.chang = 0;
                    object.ju = 0;
                    object.ben = 0;
                    object.target = 0;
                    object.damage = 0;
                    object.heal = 0;
                    object.monster_seq = 0;
                    object.chain_atk = 0;
                    object.killed = 0;
                    object.is_luk = 0;
                    object.is_dex = 0;
                    object.is_extra = 0;
                    object.reward = "";
                    object.uuid = "";
                    object.points = 0;
                    object.is_zimo = 0;
                }
                if (message.chang != null && message.hasOwnProperty("chang"))
                    object.chang = message.chang;
                if (message.ju != null && message.hasOwnProperty("ju"))
                    object.ju = message.ju;
                if (message.ben != null && message.hasOwnProperty("ben"))
                    object.ben = message.ben;
                if (message.target != null && message.hasOwnProperty("target"))
                    object.target = message.target;
                if (message.damage != null && message.hasOwnProperty("damage"))
                    object.damage = message.damage;
                if (message.heal != null && message.hasOwnProperty("heal"))
                    object.heal = message.heal;
                if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                    object.monster_seq = message.monster_seq;
                if (message.chain_atk != null && message.hasOwnProperty("chain_atk"))
                    object.chain_atk = message.chain_atk;
                if (message.killed != null && message.hasOwnProperty("killed"))
                    object.killed = message.killed;
                if (message.is_luk != null && message.hasOwnProperty("is_luk"))
                    object.is_luk = message.is_luk;
                if (message.is_dex != null && message.hasOwnProperty("is_dex"))
                    object.is_dex = message.is_dex;
                if (message.is_extra != null && message.hasOwnProperty("is_extra"))
                    object.is_extra = message.is_extra;
                if (message.reward != null && message.hasOwnProperty("reward"))
                    object.reward = message.reward;
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    object.uuid = message.uuid;
                if (message.points != null && message.hasOwnProperty("points"))
                    object.points = message.points;
                if (message.is_zimo != null && message.hasOwnProperty("is_zimo"))
                    object.is_zimo = message.is_zimo;
                return object;
            };

            BattleResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BattleResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchRPGBattleHistory.BattleResult";
            };

            return BattleResult;
        })();

        return ResFetchRPGBattleHistory;
    })();

    lq.ResFetchRPGBattleHistoryV2 = (function() {

        function ResFetchRPGBattleHistoryV2(properties) {
            this.battle_result = [];
            this.recent_battle_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchRPGBattleHistoryV2.prototype.error = null;
        ResFetchRPGBattleHistoryV2.prototype.battle_result = $util.emptyArray;
        ResFetchRPGBattleHistoryV2.prototype.start_state = null;
        ResFetchRPGBattleHistoryV2.prototype.current_state = null;
        ResFetchRPGBattleHistoryV2.prototype.recent_battle_result = $util.emptyArray;

        ResFetchRPGBattleHistoryV2.create = function create(properties) {
            return new ResFetchRPGBattleHistoryV2(properties);
        };

        ResFetchRPGBattleHistoryV2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.battle_result != null && message.battle_result.length)
                for (let i = 0; i < message.battle_result.length; ++i)
                    $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.encode(message.battle_result[i], writer.uint32(18).fork()).ldelim();
            if (message.start_state != null && Object.hasOwnProperty.call(message, "start_state"))
                $root.lq.RPGState.encode(message.start_state, writer.uint32(26).fork()).ldelim();
            if (message.current_state != null && Object.hasOwnProperty.call(message, "current_state"))
                $root.lq.RPGState.encode(message.current_state, writer.uint32(34).fork()).ldelim();
            if (message.recent_battle_result != null && message.recent_battle_result.length)
                for (let i = 0; i < message.recent_battle_result.length; ++i)
                    $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.encode(message.recent_battle_result[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchRPGBattleHistoryV2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchRPGBattleHistoryV2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRPGBattleHistoryV2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.battle_result && message.battle_result.length))
                            message.battle_result = [];
                        message.battle_result.push($root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.start_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.start_state);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.current_state = $root.lq.RPGState.decode(reader, reader.uint32(), undefined, _depth + 1, message.current_state);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recent_battle_result && message.recent_battle_result.length))
                            message.recent_battle_result = [];
                        message.recent_battle_result.push($root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchRPGBattleHistoryV2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchRPGBattleHistoryV2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.battle_result != null && message.hasOwnProperty("battle_result")) {
                if (!Array.isArray(message.battle_result))
                    return "battle_result: array expected";
                for (let i = 0; i < message.battle_result.length; ++i) {
                    let error = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.verify(message.battle_result[i], _depth + 1);
                    if (error)
                        return "battle_result." + error;
                }
            }
            if (message.start_state != null && message.hasOwnProperty("start_state")) {
                let error = $root.lq.RPGState.verify(message.start_state, _depth + 1);
                if (error)
                    return "start_state." + error;
            }
            if (message.current_state != null && message.hasOwnProperty("current_state")) {
                let error = $root.lq.RPGState.verify(message.current_state, _depth + 1);
                if (error)
                    return "current_state." + error;
            }
            if (message.recent_battle_result != null && message.hasOwnProperty("recent_battle_result")) {
                if (!Array.isArray(message.recent_battle_result))
                    return "recent_battle_result: array expected";
                for (let i = 0; i < message.recent_battle_result.length; ++i) {
                    let error = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.verify(message.recent_battle_result[i], _depth + 1);
                    if (error)
                        return "recent_battle_result." + error;
                }
            }
            return null;
        };

        ResFetchRPGBattleHistoryV2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchRPGBattleHistoryV2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchRPGBattleHistoryV2();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistoryV2.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.battle_result) {
                if (!Array.isArray(object.battle_result))
                    throw TypeError(".lq.ResFetchRPGBattleHistoryV2.battle_result: array expected");
                message.battle_result = Array(object.battle_result.length);
                for (let i = 0; i < object.battle_result.length; ++i) {
                    if (typeof object.battle_result[i] !== "object")
                        throw TypeError(".lq.ResFetchRPGBattleHistoryV2.battle_result: object expected");
                    message.battle_result[i] = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.fromObject(object.battle_result[i], _depth + 1);
                }
            }
            if (object.start_state != null) {
                if (typeof object.start_state !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistoryV2.start_state: object expected");
                message.start_state = $root.lq.RPGState.fromObject(object.start_state, _depth + 1);
            }
            if (object.current_state != null) {
                if (typeof object.current_state !== "object")
                    throw TypeError(".lq.ResFetchRPGBattleHistoryV2.current_state: object expected");
                message.current_state = $root.lq.RPGState.fromObject(object.current_state, _depth + 1);
            }
            if (object.recent_battle_result) {
                if (!Array.isArray(object.recent_battle_result))
                    throw TypeError(".lq.ResFetchRPGBattleHistoryV2.recent_battle_result: array expected");
                message.recent_battle_result = Array(object.recent_battle_result.length);
                for (let i = 0; i < object.recent_battle_result.length; ++i) {
                    if (typeof object.recent_battle_result[i] !== "object")
                        throw TypeError(".lq.ResFetchRPGBattleHistoryV2.recent_battle_result: object expected");
                    message.recent_battle_result[i] = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.fromObject(object.recent_battle_result[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchRPGBattleHistoryV2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.battle_result = [];
                object.recent_battle_result = [];
            }
            if (options.defaults) {
                object.error = null;
                object.start_state = null;
                object.current_state = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.battle_result && message.battle_result.length) {
                object.battle_result = Array(message.battle_result.length);
                for (let j = 0; j < message.battle_result.length; ++j)
                    object.battle_result[j] = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.toObject(message.battle_result[j], options);
            }
            if (message.start_state != null && message.hasOwnProperty("start_state"))
                object.start_state = $root.lq.RPGState.toObject(message.start_state, options);
            if (message.current_state != null && message.hasOwnProperty("current_state"))
                object.current_state = $root.lq.RPGState.toObject(message.current_state, options);
            if (message.recent_battle_result && message.recent_battle_result.length) {
                object.recent_battle_result = Array(message.recent_battle_result.length);
                for (let j = 0; j < message.recent_battle_result.length; ++j)
                    object.recent_battle_result[j] = $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2.toObject(message.recent_battle_result[j], options);
            }
            return object;
        };

        ResFetchRPGBattleHistoryV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchRPGBattleHistoryV2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchRPGBattleHistoryV2";
        };

        ResFetchRPGBattleHistoryV2.BattleResultV2 = (function() {

            function BattleResultV2(properties) {
                this.buff = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BattleResultV2.prototype.uuid = "";
            BattleResultV2.prototype.chang = 0;
            BattleResultV2.prototype.ju = 0;
            BattleResultV2.prototype.ben = 0;
            BattleResultV2.prototype.damage = 0;
            BattleResultV2.prototype.monster_seq = 0;
            BattleResultV2.prototype.killed = 0;
            BattleResultV2.prototype.buff = $util.emptyArray;
            BattleResultV2.prototype.points = 0;

            BattleResultV2.create = function create(properties) {
                return new BattleResultV2(properties);
            };

            BattleResultV2.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                    writer.uint32(8).uint32(message.chang);
                if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                    writer.uint32(16).uint32(message.ju);
                if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                    writer.uint32(24).uint32(message.ben);
                if (message.damage != null && Object.hasOwnProperty.call(message, "damage"))
                    writer.uint32(40).uint32(message.damage);
                if (message.monster_seq != null && Object.hasOwnProperty.call(message, "monster_seq"))
                    writer.uint32(56).uint32(message.monster_seq);
                if (message.killed != null && Object.hasOwnProperty.call(message, "killed"))
                    writer.uint32(72).uint32(message.killed);
                if (message.buff != null && message.buff.length)
                    for (let i = 0; i < message.buff.length; ++i)
                        $root.lq.ActivityBuffData.encode(message.buff[i], writer.uint32(82).fork()).ldelim();
                if (message.points != null && Object.hasOwnProperty.call(message, "points"))
                    writer.uint32(88).uint32(message.points);
                if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                    writer.uint32(114).string(message.uuid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BattleResultV2.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BattleResultV2.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 14: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.uuid = value;
                            else
                                delete message.uuid;
                            continue;
                        }
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chang = value;
                            else
                                delete message.chang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ju = value;
                            else
                                delete message.ju;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ben = value;
                            else
                                delete message.ben;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.damage = value;
                            else
                                delete message.damage;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.monster_seq = value;
                            else
                                delete message.monster_seq;
                            continue;
                        }
                    case 9: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.killed = value;
                            else
                                delete message.killed;
                            continue;
                        }
                    case 10: {
                            if (wireType !== 2)
                                break;
                            if (!(message.buff && message.buff.length))
                                message.buff = [];
                            message.buff.push($root.lq.ActivityBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 11: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.points = value;
                            else
                                delete message.points;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BattleResultV2.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BattleResultV2.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    if (!$util.isString(message.uuid))
                        return "uuid: string expected";
                if (message.chang != null && message.hasOwnProperty("chang"))
                    if (!$util.isInteger(message.chang))
                        return "chang: integer expected";
                if (message.ju != null && message.hasOwnProperty("ju"))
                    if (!$util.isInteger(message.ju))
                        return "ju: integer expected";
                if (message.ben != null && message.hasOwnProperty("ben"))
                    if (!$util.isInteger(message.ben))
                        return "ben: integer expected";
                if (message.damage != null && message.hasOwnProperty("damage"))
                    if (!$util.isInteger(message.damage))
                        return "damage: integer expected";
                if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                    if (!$util.isInteger(message.monster_seq))
                        return "monster_seq: integer expected";
                if (message.killed != null && message.hasOwnProperty("killed"))
                    if (!$util.isInteger(message.killed))
                        return "killed: integer expected";
                if (message.buff != null && message.hasOwnProperty("buff")) {
                    if (!Array.isArray(message.buff))
                        return "buff: array expected";
                    for (let i = 0; i < message.buff.length; ++i) {
                        let error = $root.lq.ActivityBuffData.verify(message.buff[i], _depth + 1);
                        if (error)
                            return "buff." + error;
                    }
                }
                if (message.points != null && message.hasOwnProperty("points"))
                    if (!$util.isInteger(message.points))
                        return "points: integer expected";
                return null;
            };

            BattleResultV2.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchRPGBattleHistoryV2.BattleResultV2();
                if (object.uuid != null)
                    if (typeof object.uuid !== "string" || object.uuid.length)
                        message.uuid = String(object.uuid);
                if (object.chang != null)
                    if (Number(object.chang) !== 0)
                        message.chang = object.chang >>> 0;
                if (object.ju != null)
                    if (Number(object.ju) !== 0)
                        message.ju = object.ju >>> 0;
                if (object.ben != null)
                    if (Number(object.ben) !== 0)
                        message.ben = object.ben >>> 0;
                if (object.damage != null)
                    if (Number(object.damage) !== 0)
                        message.damage = object.damage >>> 0;
                if (object.monster_seq != null)
                    if (Number(object.monster_seq) !== 0)
                        message.monster_seq = object.monster_seq >>> 0;
                if (object.killed != null)
                    if (Number(object.killed) !== 0)
                        message.killed = object.killed >>> 0;
                if (object.buff) {
                    if (!Array.isArray(object.buff))
                        throw TypeError(".lq.ResFetchRPGBattleHistoryV2.BattleResultV2.buff: array expected");
                    message.buff = Array(object.buff.length);
                    for (let i = 0; i < object.buff.length; ++i) {
                        if (typeof object.buff[i] !== "object")
                            throw TypeError(".lq.ResFetchRPGBattleHistoryV2.BattleResultV2.buff: object expected");
                        message.buff[i] = $root.lq.ActivityBuffData.fromObject(object.buff[i], _depth + 1);
                    }
                }
                if (object.points != null)
                    if (Number(object.points) !== 0)
                        message.points = object.points >>> 0;
                return message;
            };

            BattleResultV2.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.buff = [];
                if (options.defaults) {
                    object.chang = 0;
                    object.ju = 0;
                    object.ben = 0;
                    object.damage = 0;
                    object.monster_seq = 0;
                    object.killed = 0;
                    object.points = 0;
                    object.uuid = "";
                }
                if (message.chang != null && message.hasOwnProperty("chang"))
                    object.chang = message.chang;
                if (message.ju != null && message.hasOwnProperty("ju"))
                    object.ju = message.ju;
                if (message.ben != null && message.hasOwnProperty("ben"))
                    object.ben = message.ben;
                if (message.damage != null && message.hasOwnProperty("damage"))
                    object.damage = message.damage;
                if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                    object.monster_seq = message.monster_seq;
                if (message.killed != null && message.hasOwnProperty("killed"))
                    object.killed = message.killed;
                if (message.buff && message.buff.length) {
                    object.buff = Array(message.buff.length);
                    for (let j = 0; j < message.buff.length; ++j)
                        object.buff[j] = $root.lq.ActivityBuffData.toObject(message.buff[j], options);
                }
                if (message.points != null && message.hasOwnProperty("points"))
                    object.points = message.points;
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    object.uuid = message.uuid;
                return object;
            };

            BattleResultV2.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BattleResultV2.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchRPGBattleHistoryV2.BattleResultV2";
            };

            return BattleResultV2;
        })();

        return ResFetchRPGBattleHistoryV2;
    })();

    lq.ReqBuyArenaTicket = (function() {

        function ReqBuyArenaTicket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyArenaTicket.prototype.activity_id = 0;

        ReqBuyArenaTicket.create = function create(properties) {
            return new ReqBuyArenaTicket(properties);
        };

        ReqBuyArenaTicket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyArenaTicket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyArenaTicket.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyArenaTicket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyArenaTicket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyArenaTicket.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqBuyArenaTicket.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyArenaTicket)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyArenaTicket();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqBuyArenaTicket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqBuyArenaTicket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyArenaTicket.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyArenaTicket";
        };

        return ReqBuyArenaTicket;
    })();

    lq.ReqArenaReward = (function() {

        function ReqArenaReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqArenaReward.prototype.activity_id = 0;

        ReqArenaReward.create = function create(properties) {
            return new ReqArenaReward(properties);
        };

        ReqArenaReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqArenaReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqArenaReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqArenaReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqArenaReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqArenaReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqArenaReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqArenaReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqArenaReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqArenaReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqArenaReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqArenaReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqArenaReward";
        };

        return ReqArenaReward;
    })();

    lq.ReqEnterArena = (function() {

        function ReqEnterArena(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqEnterArena.prototype.activity_id = 0;

        ReqEnterArena.create = function create(properties) {
            return new ReqEnterArena(properties);
        };

        ReqEnterArena.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqEnterArena.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqEnterArena.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqEnterArena(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqEnterArena.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqEnterArena.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqEnterArena.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqEnterArena)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqEnterArena();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqEnterArena.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqEnterArena.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqEnterArena.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqEnterArena";
        };

        return ReqEnterArena;
    })();

    lq.ResArenaReward = (function() {

        function ResArenaReward(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResArenaReward.prototype.error = null;
        ResArenaReward.prototype.items = $util.emptyArray;

        ResArenaReward.create = function create(properties) {
            return new ResArenaReward(properties);
        };

        ResArenaReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResArenaReward.RewardItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResArenaReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResArenaReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResArenaReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResArenaReward.RewardItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResArenaReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResArenaReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResArenaReward.RewardItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ResArenaReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResArenaReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResArenaReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResArenaReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResArenaReward.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResArenaReward.items: object expected");
                    message.items[i] = $root.lq.ResArenaReward.RewardItem.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ResArenaReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResArenaReward.RewardItem.toObject(message.items[j], options);
            }
            return object;
        };

        ResArenaReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResArenaReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResArenaReward";
        };

        ResArenaReward.RewardItem = (function() {

            function RewardItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardItem.prototype.id = 0;
            RewardItem.prototype.count = 0;

            RewardItem.create = function create(properties) {
                return new RewardItem(properties);
            };

            RewardItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResArenaReward.RewardItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResArenaReward.RewardItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResArenaReward.RewardItem();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResArenaReward.RewardItem";
            };

            return RewardItem;
        })();

        return ResArenaReward;
    })();

    lq.ReqReceiveRPGRewards = (function() {

        function ReqReceiveRPGRewards(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveRPGRewards.prototype.activity_id = 0;

        ReqReceiveRPGRewards.create = function create(properties) {
            return new ReqReceiveRPGRewards(properties);
        };

        ReqReceiveRPGRewards.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveRPGRewards.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveRPGRewards.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveRPGRewards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveRPGRewards.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveRPGRewards.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqReceiveRPGRewards.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveRPGRewards)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveRPGRewards();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqReceiveRPGRewards.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqReceiveRPGRewards.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveRPGRewards.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveRPGRewards";
        };

        return ReqReceiveRPGRewards;
    })();

    lq.ReqReceiveRPGReward = (function() {

        function ReqReceiveRPGReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveRPGReward.prototype.activity_id = 0;
        ReqReceiveRPGReward.prototype.monster_seq = 0;

        ReqReceiveRPGReward.create = function create(properties) {
            return new ReqReceiveRPGReward(properties);
        };

        ReqReceiveRPGReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.monster_seq != null && Object.hasOwnProperty.call(message, "monster_seq"))
                writer.uint32(16).uint32(message.monster_seq);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveRPGReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveRPGReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveRPGReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.monster_seq = value;
                        else
                            delete message.monster_seq;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveRPGReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveRPGReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                if (!$util.isInteger(message.monster_seq))
                    return "monster_seq: integer expected";
            return null;
        };

        ReqReceiveRPGReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveRPGReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveRPGReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.monster_seq != null)
                if (Number(object.monster_seq) !== 0)
                    message.monster_seq = object.monster_seq >>> 0;
            return message;
        };

        ReqReceiveRPGReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.monster_seq = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.monster_seq != null && message.hasOwnProperty("monster_seq"))
                object.monster_seq = message.monster_seq;
            return object;
        };

        ReqReceiveRPGReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveRPGReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveRPGReward";
        };

        return ReqReceiveRPGReward;
    })();

    lq.ResReceiveRPGRewards = (function() {

        function ResReceiveRPGRewards(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveRPGRewards.prototype.error = null;
        ResReceiveRPGRewards.prototype.items = $util.emptyArray;

        ResReceiveRPGRewards.create = function create(properties) {
            return new ResReceiveRPGRewards(properties);
        };

        ResReceiveRPGRewards.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResReceiveRPGRewards.RewardItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveRPGRewards.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveRPGRewards.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveRPGRewards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResReceiveRPGRewards.RewardItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveRPGRewards.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveRPGRewards.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResReceiveRPGRewards.RewardItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ResReceiveRPGRewards.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveRPGRewards)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveRPGRewards();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveRPGRewards.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResReceiveRPGRewards.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResReceiveRPGRewards.items: object expected");
                    message.items[i] = $root.lq.ResReceiveRPGRewards.RewardItem.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveRPGRewards.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResReceiveRPGRewards.RewardItem.toObject(message.items[j], options);
            }
            return object;
        };

        ResReceiveRPGRewards.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveRPGRewards.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveRPGRewards";
        };

        ResReceiveRPGRewards.RewardItem = (function() {

            function RewardItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardItem.prototype.id = 0;
            RewardItem.prototype.count = 0;

            RewardItem.create = function create(properties) {
                return new RewardItem(properties);
            };

            RewardItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveRPGRewards.RewardItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResReceiveRPGRewards.RewardItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResReceiveRPGRewards.RewardItem();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResReceiveRPGRewards.RewardItem";
            };

            return RewardItem;
        })();

        return ResReceiveRPGRewards;
    })();

    lq.ReqFetchOBToken = (function() {

        function ReqFetchOBToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchOBToken.prototype.uuid = "";

        ReqFetchOBToken.create = function create(properties) {
            return new ReqFetchOBToken(properties);
        };

        ReqFetchOBToken.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchOBToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchOBToken.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchOBToken(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchOBToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchOBToken.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            return null;
        };

        ReqFetchOBToken.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchOBToken)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchOBToken();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            return message;
        };

        ReqFetchOBToken.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.uuid = "";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            return object;
        };

        ReqFetchOBToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchOBToken.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchOBToken";
        };

        return ReqFetchOBToken;
    })();

    lq.ResFetchOBToken = (function() {

        function ResFetchOBToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchOBToken.prototype.error = null;
        ResFetchOBToken.prototype.token = "";
        ResFetchOBToken.prototype.create_time = 0;
        ResFetchOBToken.prototype.delay = 0;
        ResFetchOBToken.prototype.start_time = 0;

        ResFetchOBToken.create = function create(properties) {
            return new ResFetchOBToken(properties);
        };

        ResFetchOBToken.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                writer.uint32(24).uint32(message.create_time);
            if (message.delay != null && Object.hasOwnProperty.call(message, "delay"))
                writer.uint32(32).uint32(message.delay);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(40).uint32(message.start_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchOBToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchOBToken.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchOBToken(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.create_time = value;
                        else
                            delete message.create_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.delay = value;
                        else
                            delete message.delay;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchOBToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchOBToken.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                if (!$util.isInteger(message.create_time))
                    return "create_time: integer expected";
            if (message.delay != null && message.hasOwnProperty("delay"))
                if (!$util.isInteger(message.delay))
                    return "delay: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            return null;
        };

        ResFetchOBToken.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchOBToken)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchOBToken();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchOBToken.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.create_time != null)
                if (Number(object.create_time) !== 0)
                    message.create_time = object.create_time >>> 0;
            if (object.delay != null)
                if (Number(object.delay) !== 0)
                    message.delay = object.delay >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            return message;
        };

        ResFetchOBToken.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.token = "";
                object.create_time = 0;
                object.delay = 0;
                object.start_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.create_time != null && message.hasOwnProperty("create_time"))
                object.create_time = message.create_time;
            if (message.delay != null && message.hasOwnProperty("delay"))
                object.delay = message.delay;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            return object;
        };

        ResFetchOBToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchOBToken.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchOBToken";
        };

        return ResFetchOBToken;
    })();

    lq.ReqReceiveCharacterRewards = (function() {

        function ReqReceiveCharacterRewards(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveCharacterRewards.prototype.character_id = 0;
        ReqReceiveCharacterRewards.prototype.level = 0;

        ReqReceiveCharacterRewards.create = function create(properties) {
            return new ReqReceiveCharacterRewards(properties);
        };

        ReqReceiveCharacterRewards.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(8).uint32(message.character_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveCharacterRewards.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveCharacterRewards.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveCharacterRewards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveCharacterRewards.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveCharacterRewards.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        ReqReceiveCharacterRewards.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveCharacterRewards)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveCharacterRewards();
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            return message;
        };

        ReqReceiveCharacterRewards.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.character_id = 0;
                object.level = 0;
            }
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        ReqReceiveCharacterRewards.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveCharacterRewards.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveCharacterRewards";
        };

        return ReqReceiveCharacterRewards;
    })();

    lq.ResReceiveCharacterRewards = (function() {

        function ResReceiveCharacterRewards(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveCharacterRewards.prototype.error = null;
        ResReceiveCharacterRewards.prototype.items = $util.emptyArray;

        ResReceiveCharacterRewards.create = function create(properties) {
            return new ResReceiveCharacterRewards(properties);
        };

        ResReceiveCharacterRewards.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResReceiveCharacterRewards.RewardItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveCharacterRewards.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveCharacterRewards.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveCharacterRewards(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResReceiveCharacterRewards.RewardItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveCharacterRewards.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveCharacterRewards.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResReceiveCharacterRewards.RewardItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ResReceiveCharacterRewards.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveCharacterRewards)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveCharacterRewards();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveCharacterRewards.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResReceiveCharacterRewards.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResReceiveCharacterRewards.items: object expected");
                    message.items[i] = $root.lq.ResReceiveCharacterRewards.RewardItem.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveCharacterRewards.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResReceiveCharacterRewards.RewardItem.toObject(message.items[j], options);
            }
            return object;
        };

        ResReceiveCharacterRewards.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveCharacterRewards.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveCharacterRewards";
        };

        ResReceiveCharacterRewards.RewardItem = (function() {

            function RewardItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardItem.prototype.id = 0;
            RewardItem.prototype.count = 0;

            RewardItem.create = function create(properties) {
                return new RewardItem(properties);
            };

            RewardItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveCharacterRewards.RewardItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResReceiveCharacterRewards.RewardItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResReceiveCharacterRewards.RewardItem();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResReceiveCharacterRewards.RewardItem";
            };

            return RewardItem;
        })();

        return ResReceiveCharacterRewards;
    })();

    lq.ReqFeedActivityFeed = (function() {

        function ReqFeedActivityFeed(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFeedActivityFeed.prototype.activity_id = 0;
        ReqFeedActivityFeed.prototype.count = 0;

        ReqFeedActivityFeed.create = function create(properties) {
            return new ReqFeedActivityFeed(properties);
        };

        ReqFeedActivityFeed.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFeedActivityFeed.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFeedActivityFeed.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFeedActivityFeed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFeedActivityFeed.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFeedActivityFeed.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqFeedActivityFeed.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFeedActivityFeed)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFeedActivityFeed();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqFeedActivityFeed.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqFeedActivityFeed.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFeedActivityFeed.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFeedActivityFeed";
        };

        return ReqFeedActivityFeed;
    })();

    lq.ResFeedActivityFeed = (function() {

        function ResFeedActivityFeed(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFeedActivityFeed.prototype.error = null;
        ResFeedActivityFeed.prototype.items = $util.emptyArray;
        ResFeedActivityFeed.prototype.feed_count = 0;

        ResFeedActivityFeed.create = function create(properties) {
            return new ResFeedActivityFeed(properties);
        };

        ResFeedActivityFeed.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResFeedActivityFeed.RewardItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.feed_count != null && Object.hasOwnProperty.call(message, "feed_count"))
                writer.uint32(24).uint32(message.feed_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFeedActivityFeed.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFeedActivityFeed.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFeedActivityFeed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResFeedActivityFeed.RewardItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.feed_count = value;
                        else
                            delete message.feed_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFeedActivityFeed.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFeedActivityFeed.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResFeedActivityFeed.RewardItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                if (!$util.isInteger(message.feed_count))
                    return "feed_count: integer expected";
            return null;
        };

        ResFeedActivityFeed.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFeedActivityFeed)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFeedActivityFeed();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFeedActivityFeed.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResFeedActivityFeed.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResFeedActivityFeed.items: object expected");
                    message.items[i] = $root.lq.ResFeedActivityFeed.RewardItem.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.feed_count != null)
                if (Number(object.feed_count) !== 0)
                    message.feed_count = object.feed_count >>> 0;
            return message;
        };

        ResFeedActivityFeed.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.error = null;
                object.feed_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResFeedActivityFeed.RewardItem.toObject(message.items[j], options);
            }
            if (message.feed_count != null && message.hasOwnProperty("feed_count"))
                object.feed_count = message.feed_count;
            return object;
        };

        ResFeedActivityFeed.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFeedActivityFeed.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFeedActivityFeed";
        };

        ResFeedActivityFeed.RewardItem = (function() {

            function RewardItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardItem.prototype.id = 0;
            RewardItem.prototype.count = 0;

            RewardItem.create = function create(properties) {
                return new RewardItem(properties);
            };

            RewardItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFeedActivityFeed.RewardItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFeedActivityFeed.RewardItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFeedActivityFeed.RewardItem();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFeedActivityFeed.RewardItem";
            };

            return RewardItem;
        })();

        return ResFeedActivityFeed;
    })();

    lq.ReqSendActivityGiftToFriend = (function() {

        function ReqSendActivityGiftToFriend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSendActivityGiftToFriend.prototype.activity_id = 0;
        ReqSendActivityGiftToFriend.prototype.item_id = 0;
        ReqSendActivityGiftToFriend.prototype.target_id = 0;

        ReqSendActivityGiftToFriend.create = function create(properties) {
            return new ReqSendActivityGiftToFriend(properties);
        };

        ReqSendActivityGiftToFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(16).uint32(message.item_id);
            if (message.target_id != null && Object.hasOwnProperty.call(message, "target_id"))
                writer.uint32(24).uint32(message.target_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSendActivityGiftToFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSendActivityGiftToFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSendActivityGiftToFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_id = value;
                        else
                            delete message.target_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSendActivityGiftToFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSendActivityGiftToFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                if (!$util.isInteger(message.target_id))
                    return "target_id: integer expected";
            return null;
        };

        ReqSendActivityGiftToFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSendActivityGiftToFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSendActivityGiftToFriend();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.target_id != null)
                if (Number(object.target_id) !== 0)
                    message.target_id = object.target_id >>> 0;
            return message;
        };

        ReqSendActivityGiftToFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.item_id = 0;
                object.target_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.target_id != null && message.hasOwnProperty("target_id"))
                object.target_id = message.target_id;
            return object;
        };

        ReqSendActivityGiftToFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSendActivityGiftToFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSendActivityGiftToFriend";
        };

        return ReqSendActivityGiftToFriend;
    })();

    lq.ResSendActivityGiftToFriend = (function() {

        function ResSendActivityGiftToFriend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSendActivityGiftToFriend.prototype.error = null;
        ResSendActivityGiftToFriend.prototype.send_gift_count = 0;

        ResSendActivityGiftToFriend.create = function create(properties) {
            return new ResSendActivityGiftToFriend(properties);
        };

        ResSendActivityGiftToFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.send_gift_count != null && Object.hasOwnProperty.call(message, "send_gift_count"))
                writer.uint32(16).uint32(message.send_gift_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSendActivityGiftToFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSendActivityGiftToFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSendActivityGiftToFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.send_gift_count = value;
                        else
                            delete message.send_gift_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSendActivityGiftToFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSendActivityGiftToFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.send_gift_count != null && message.hasOwnProperty("send_gift_count"))
                if (!$util.isInteger(message.send_gift_count))
                    return "send_gift_count: integer expected";
            return null;
        };

        ResSendActivityGiftToFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSendActivityGiftToFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSendActivityGiftToFriend();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSendActivityGiftToFriend.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.send_gift_count != null)
                if (Number(object.send_gift_count) !== 0)
                    message.send_gift_count = object.send_gift_count >>> 0;
            return message;
        };

        ResSendActivityGiftToFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.send_gift_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.send_gift_count != null && message.hasOwnProperty("send_gift_count"))
                object.send_gift_count = message.send_gift_count;
            return object;
        };

        ResSendActivityGiftToFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSendActivityGiftToFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSendActivityGiftToFriend";
        };

        return ResSendActivityGiftToFriend;
    })();

    lq.ReqReceiveActivityGift = (function() {

        function ReqReceiveActivityGift(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveActivityGift.prototype.activity_id = 0;
        ReqReceiveActivityGift.prototype.id = 0;

        ReqReceiveActivityGift.create = function create(properties) {
            return new ReqReceiveActivityGift(properties);
        };

        ReqReceiveActivityGift.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveActivityGift.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveActivityGift.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveActivityGift(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveActivityGift.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveActivityGift.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqReceiveActivityGift.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveActivityGift)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveActivityGift();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqReceiveActivityGift.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqReceiveActivityGift.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveActivityGift.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveActivityGift";
        };

        return ReqReceiveActivityGift;
    })();

    lq.ReqFetchFriendGiftActivityData = (function() {

        function ReqFetchFriendGiftActivityData(properties) {
            this.account_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchFriendGiftActivityData.prototype.activity_id = 0;
        ReqFetchFriendGiftActivityData.prototype.account_list = $util.emptyArray;

        ReqFetchFriendGiftActivityData.create = function create(properties) {
            return new ReqFetchFriendGiftActivityData(properties);
        };

        ReqFetchFriendGiftActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.account_list != null && message.account_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.account_list.length; ++i)
                    writer.uint32(message.account_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchFriendGiftActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchFriendGiftActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchFriendGiftActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.account_list && message.account_list.length))
                                message.account_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_list && message.account_list.length))
                            message.account_list = [];
                        message.account_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchFriendGiftActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchFriendGiftActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                if (!Array.isArray(message.account_list))
                    return "account_list: array expected";
                for (let i = 0; i < message.account_list.length; ++i)
                    if (!$util.isInteger(message.account_list[i]))
                        return "account_list: integer[] expected";
            }
            return null;
        };

        ReqFetchFriendGiftActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchFriendGiftActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchFriendGiftActivityData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.account_list) {
                if (!Array.isArray(object.account_list))
                    throw TypeError(".lq.ReqFetchFriendGiftActivityData.account_list: array expected");
                message.account_list = Array(object.account_list.length);
                for (let i = 0; i < object.account_list.length; ++i)
                    message.account_list[i] = object.account_list[i] >>> 0;
            }
            return message;
        };

        ReqFetchFriendGiftActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_list = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.account_list && message.account_list.length) {
                object.account_list = Array(message.account_list.length);
                for (let j = 0; j < message.account_list.length; ++j)
                    object.account_list[j] = message.account_list[j];
            }
            return object;
        };

        ReqFetchFriendGiftActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchFriendGiftActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchFriendGiftActivityData";
        };

        return ReqFetchFriendGiftActivityData;
    })();

    lq.ResFetchFriendGiftActivityData = (function() {

        function ResFetchFriendGiftActivityData(properties) {
            this.list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchFriendGiftActivityData.prototype.error = null;
        ResFetchFriendGiftActivityData.prototype.list = $util.emptyArray;

        ResFetchFriendGiftActivityData.create = function create(properties) {
            return new ResFetchFriendGiftActivityData(properties);
        };

        ResFetchFriendGiftActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.list != null && message.list.length)
                for (let i = 0; i < message.list.length; ++i)
                    $root.lq.ResFetchFriendGiftActivityData.FriendData.encode(message.list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchFriendGiftActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchFriendGiftActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchFriendGiftActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.lq.ResFetchFriendGiftActivityData.FriendData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchFriendGiftActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchFriendGiftActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (let i = 0; i < message.list.length; ++i) {
                    let error = $root.lq.ResFetchFriendGiftActivityData.FriendData.verify(message.list[i], _depth + 1);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        ResFetchFriendGiftActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchFriendGiftActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchFriendGiftActivityData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchFriendGiftActivityData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".lq.ResFetchFriendGiftActivityData.list: array expected");
                message.list = Array(object.list.length);
                for (let i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".lq.ResFetchFriendGiftActivityData.list: object expected");
                    message.list[i] = $root.lq.ResFetchFriendGiftActivityData.FriendData.fromObject(object.list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchFriendGiftActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.list && message.list.length) {
                object.list = Array(message.list.length);
                for (let j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.lq.ResFetchFriendGiftActivityData.FriendData.toObject(message.list[j], options);
            }
            return object;
        };

        ResFetchFriendGiftActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchFriendGiftActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchFriendGiftActivityData";
        };

        ResFetchFriendGiftActivityData.ItemCountData = (function() {

            function ItemCountData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ItemCountData.prototype.item = 0;
            ItemCountData.prototype.count = 0;

            ItemCountData.create = function create(properties) {
                return new ItemCountData(properties);
            };

            ItemCountData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.item != null && Object.hasOwnProperty.call(message, "item"))
                    writer.uint32(8).uint32(message.item);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ItemCountData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ItemCountData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchFriendGiftActivityData.ItemCountData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.item = value;
                            else
                                delete message.item;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ItemCountData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ItemCountData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.item != null && message.hasOwnProperty("item"))
                    if (!$util.isInteger(message.item))
                        return "item: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            ItemCountData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchFriendGiftActivityData.ItemCountData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchFriendGiftActivityData.ItemCountData();
                if (object.item != null)
                    if (Number(object.item) !== 0)
                        message.item = object.item >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            ItemCountData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.item = 0;
                    object.count = 0;
                }
                if (message.item != null && message.hasOwnProperty("item"))
                    object.item = message.item;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            ItemCountData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ItemCountData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchFriendGiftActivityData.ItemCountData";
            };

            return ItemCountData;
        })();

        ResFetchFriendGiftActivityData.FriendData = (function() {

            function FriendData(properties) {
                this.items = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FriendData.prototype.account_id = 0;
            FriendData.prototype.items = $util.emptyArray;
            FriendData.prototype.receive_count = 0;

            FriendData.create = function create(properties) {
                return new FriendData(properties);
            };

            FriendData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.lq.ResFetchFriendGiftActivityData.ItemCountData.encode(message.items[i], writer.uint32(18).fork()).ldelim();
                if (message.receive_count != null && Object.hasOwnProperty.call(message, "receive_count"))
                    writer.uint32(24).uint32(message.receive_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FriendData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FriendData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchFriendGiftActivityData.FriendData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.lq.ResFetchFriendGiftActivityData.ItemCountData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.receive_count = value;
                            else
                                delete message.receive_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FriendData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FriendData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.items != null && message.hasOwnProperty("items")) {
                    if (!Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.lq.ResFetchFriendGiftActivityData.ItemCountData.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                if (message.receive_count != null && message.hasOwnProperty("receive_count"))
                    if (!$util.isInteger(message.receive_count))
                        return "receive_count: integer expected";
                return null;
            };

            FriendData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchFriendGiftActivityData.FriendData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchFriendGiftActivityData.FriendData();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.items) {
                    if (!Array.isArray(object.items))
                        throw TypeError(".lq.ResFetchFriendGiftActivityData.FriendData.items: array expected");
                    message.items = Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (typeof object.items[i] !== "object")
                            throw TypeError(".lq.ResFetchFriendGiftActivityData.FriendData.items: object expected");
                        message.items[i] = $root.lq.ResFetchFriendGiftActivityData.ItemCountData.fromObject(object.items[i], _depth + 1);
                    }
                }
                if (object.receive_count != null)
                    if (Number(object.receive_count) !== 0)
                        message.receive_count = object.receive_count >>> 0;
                return message;
            };

            FriendData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (options.defaults) {
                    object.account_id = 0;
                    object.receive_count = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.items && message.items.length) {
                    object.items = Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.lq.ResFetchFriendGiftActivityData.ItemCountData.toObject(message.items[j], options);
                }
                if (message.receive_count != null && message.hasOwnProperty("receive_count"))
                    object.receive_count = message.receive_count;
                return object;
            };

            FriendData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FriendData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchFriendGiftActivityData.FriendData";
            };

            return FriendData;
        })();

        return ResFetchFriendGiftActivityData;
    })();

    lq.ReqOpenPreChestItem = (function() {

        function ReqOpenPreChestItem(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenPreChestItem.prototype.item_id = 0;
        ReqOpenPreChestItem.prototype.pool_id = 0;

        ReqOpenPreChestItem.create = function create(properties) {
            return new ReqOpenPreChestItem(properties);
        };

        ReqOpenPreChestItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.item_id != null && Object.hasOwnProperty.call(message, "item_id"))
                writer.uint32(8).uint32(message.item_id);
            if (message.pool_id != null && Object.hasOwnProperty.call(message, "pool_id"))
                writer.uint32(16).uint32(message.pool_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenPreChestItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenPreChestItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenPreChestItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.item_id = value;
                        else
                            delete message.item_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pool_id = value;
                        else
                            delete message.pool_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenPreChestItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenPreChestItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                if (!$util.isInteger(message.item_id))
                    return "item_id: integer expected";
            if (message.pool_id != null && message.hasOwnProperty("pool_id"))
                if (!$util.isInteger(message.pool_id))
                    return "pool_id: integer expected";
            return null;
        };

        ReqOpenPreChestItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenPreChestItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenPreChestItem();
            if (object.item_id != null)
                if (Number(object.item_id) !== 0)
                    message.item_id = object.item_id >>> 0;
            if (object.pool_id != null)
                if (Number(object.pool_id) !== 0)
                    message.pool_id = object.pool_id >>> 0;
            return message;
        };

        ReqOpenPreChestItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.item_id = 0;
                object.pool_id = 0;
            }
            if (message.item_id != null && message.hasOwnProperty("item_id"))
                object.item_id = message.item_id;
            if (message.pool_id != null && message.hasOwnProperty("pool_id"))
                object.pool_id = message.pool_id;
            return object;
        };

        ReqOpenPreChestItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenPreChestItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenPreChestItem";
        };

        return ReqOpenPreChestItem;
    })();

    lq.ResOpenPreChestItem = (function() {

        function ResOpenPreChestItem(properties) {
            this.results = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOpenPreChestItem.prototype.error = null;
        ResOpenPreChestItem.prototype.results = $util.emptyArray;

        ResOpenPreChestItem.create = function create(properties) {
            return new ResOpenPreChestItem(properties);
        };

        ResOpenPreChestItem.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.lq.OpenResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOpenPreChestItem.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOpenPreChestItem.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenPreChestItem(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.lq.OpenResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOpenPreChestItem.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOpenPreChestItem.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.results != null && message.hasOwnProperty("results")) {
                if (!Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.lq.OpenResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            return null;
        };

        ResOpenPreChestItem.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOpenPreChestItem)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOpenPreChestItem();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOpenPreChestItem.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.results) {
                if (!Array.isArray(object.results))
                    throw TypeError(".lq.ResOpenPreChestItem.results: array expected");
                message.results = Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (typeof object.results[i] !== "object")
                        throw TypeError(".lq.ResOpenPreChestItem.results: object expected");
                    message.results[i] = $root.lq.OpenResult.fromObject(object.results[i], _depth + 1);
                }
            }
            return message;
        };

        ResOpenPreChestItem.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.results = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.results && message.results.length) {
                object.results = Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.lq.OpenResult.toObject(message.results[j], options);
            }
            return object;
        };

        ResOpenPreChestItem.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOpenPreChestItem.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOpenPreChestItem";
        };

        return ResOpenPreChestItem;
    })();

    lq.ReqFetchVoteActivity = (function() {

        function ReqFetchVoteActivity(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchVoteActivity.prototype.activity_id = 0;

        ReqFetchVoteActivity.create = function create(properties) {
            return new ReqFetchVoteActivity(properties);
        };

        ReqFetchVoteActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchVoteActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchVoteActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchVoteActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchVoteActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchVoteActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchVoteActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchVoteActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchVoteActivity();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchVoteActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchVoteActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchVoteActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchVoteActivity";
        };

        return ReqFetchVoteActivity;
    })();

    lq.ResFetchVoteActivity = (function() {

        function ResFetchVoteActivity(properties) {
            this.data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchVoteActivity.prototype.error = null;
        ResFetchVoteActivity.prototype.update_time = 0;
        ResFetchVoteActivity.prototype.data = $util.emptyArray;

        ResFetchVoteActivity.create = function create(properties) {
            return new ResFetchVoteActivity(properties);
        };

        ResFetchVoteActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(24).uint32(message.update_time);
            if (message.data != null && message.data.length)
                for (let i = 0; i < message.data.length; ++i)
                    $root.lq.ResFetchVoteActivity.VoteRankData.encode(message.data[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchVoteActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchVoteActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchVoteActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.data && message.data.length))
                            message.data = [];
                        message.data.push($root.lq.ResFetchVoteActivity.VoteRankData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchVoteActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchVoteActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            if (message.data != null && message.hasOwnProperty("data")) {
                if (!Array.isArray(message.data))
                    return "data: array expected";
                for (let i = 0; i < message.data.length; ++i) {
                    let error = $root.lq.ResFetchVoteActivity.VoteRankData.verify(message.data[i], _depth + 1);
                    if (error)
                        return "data." + error;
                }
            }
            return null;
        };

        ResFetchVoteActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchVoteActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchVoteActivity();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchVoteActivity.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            if (object.data) {
                if (!Array.isArray(object.data))
                    throw TypeError(".lq.ResFetchVoteActivity.data: array expected");
                message.data = Array(object.data.length);
                for (let i = 0; i < object.data.length; ++i) {
                    if (typeof object.data[i] !== "object")
                        throw TypeError(".lq.ResFetchVoteActivity.data: object expected");
                    message.data[i] = $root.lq.ResFetchVoteActivity.VoteRankData.fromObject(object.data[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchVoteActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.data = [];
            if (options.defaults) {
                object.error = null;
                object.update_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            if (message.data && message.data.length) {
                object.data = Array(message.data.length);
                for (let j = 0; j < message.data.length; ++j)
                    object.data[j] = $root.lq.ResFetchVoteActivity.VoteRankData.toObject(message.data[j], options);
            }
            return object;
        };

        ResFetchVoteActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchVoteActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchVoteActivity";
        };

        ResFetchVoteActivity.VoteRankData = (function() {

            function VoteRankData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            VoteRankData.prototype.id = 0;
            VoteRankData.prototype.share = 0;

            VoteRankData.create = function create(properties) {
                return new VoteRankData(properties);
            };

            VoteRankData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.share != null && Object.hasOwnProperty.call(message, "share"))
                    writer.uint32(16).uint32(message.share);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            VoteRankData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            VoteRankData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchVoteActivity.VoteRankData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.share = value;
                            else
                                delete message.share;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            VoteRankData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            VoteRankData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.share != null && message.hasOwnProperty("share"))
                    if (!$util.isInteger(message.share))
                        return "share: integer expected";
                return null;
            };

            VoteRankData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchVoteActivity.VoteRankData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchVoteActivity.VoteRankData();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.share != null)
                    if (Number(object.share) !== 0)
                        message.share = object.share >>> 0;
                return message;
            };

            VoteRankData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.share = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.share != null && message.hasOwnProperty("share"))
                    object.share = message.share;
                return object;
            };

            VoteRankData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            VoteRankData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchVoteActivity.VoteRankData";
            };

            return VoteRankData;
        })();

        return ResFetchVoteActivity;
    })();

    lq.ReqVoteActivity = (function() {

        function ReqVoteActivity(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVoteActivity.prototype.vote = 0;
        ReqVoteActivity.prototype.activity_id = 0;
        ReqVoteActivity.prototype.count = 0;

        ReqVoteActivity.create = function create(properties) {
            return new ReqVoteActivity(properties);
        };

        ReqVoteActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                writer.uint32(8).uint32(message.vote);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(24).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVoteActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVoteActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVoteActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vote = value;
                        else
                            delete message.vote;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVoteActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVoteActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.vote != null && message.hasOwnProperty("vote"))
                if (!$util.isInteger(message.vote))
                    return "vote: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqVoteActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVoteActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVoteActivity();
            if (object.vote != null)
                if (Number(object.vote) !== 0)
                    message.vote = object.vote >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqVoteActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.vote = 0;
                object.activity_id = 0;
                object.count = 0;
            }
            if (message.vote != null && message.hasOwnProperty("vote"))
                object.vote = message.vote;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqVoteActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVoteActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVoteActivity";
        };

        return ReqVoteActivity;
    })();

    lq.ResVoteActivity = (function() {

        function ResVoteActivity(properties) {
            this.vote_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResVoteActivity.prototype.error = null;
        ResVoteActivity.prototype.vote_records = $util.emptyArray;

        ResVoteActivity.create = function create(properties) {
            return new ResVoteActivity(properties);
        };

        ResVoteActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.vote_records != null && message.vote_records.length)
                for (let i = 0; i < message.vote_records.length; ++i)
                    $root.lq.VoteData.encode(message.vote_records[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResVoteActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResVoteActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResVoteActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.vote_records && message.vote_records.length))
                            message.vote_records = [];
                        message.vote_records.push($root.lq.VoteData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResVoteActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResVoteActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.vote_records != null && message.hasOwnProperty("vote_records")) {
                if (!Array.isArray(message.vote_records))
                    return "vote_records: array expected";
                for (let i = 0; i < message.vote_records.length; ++i) {
                    let error = $root.lq.VoteData.verify(message.vote_records[i], _depth + 1);
                    if (error)
                        return "vote_records." + error;
                }
            }
            return null;
        };

        ResVoteActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResVoteActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResVoteActivity();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResVoteActivity.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.vote_records) {
                if (!Array.isArray(object.vote_records))
                    throw TypeError(".lq.ResVoteActivity.vote_records: array expected");
                message.vote_records = Array(object.vote_records.length);
                for (let i = 0; i < object.vote_records.length; ++i) {
                    if (typeof object.vote_records[i] !== "object")
                        throw TypeError(".lq.ResVoteActivity.vote_records: object expected");
                    message.vote_records[i] = $root.lq.VoteData.fromObject(object.vote_records[i], _depth + 1);
                }
            }
            return message;
        };

        ResVoteActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.vote_records = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.vote_records && message.vote_records.length) {
                object.vote_records = Array(message.vote_records.length);
                for (let j = 0; j < message.vote_records.length; ++j)
                    object.vote_records[j] = $root.lq.VoteData.toObject(message.vote_records[j], options);
            }
            return object;
        };

        ResVoteActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResVoteActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResVoteActivity";
        };

        return ResVoteActivity;
    })();

    lq.ReqUnlockActivitySpot = (function() {

        function ReqUnlockActivitySpot(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUnlockActivitySpot.prototype.unique_id = 0;

        ReqUnlockActivitySpot.create = function create(properties) {
            return new ReqUnlockActivitySpot(properties);
        };

        ReqUnlockActivitySpot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUnlockActivitySpot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUnlockActivitySpot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUnlockActivitySpot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUnlockActivitySpot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUnlockActivitySpot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqUnlockActivitySpot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUnlockActivitySpot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUnlockActivitySpot();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqUnlockActivitySpot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqUnlockActivitySpot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUnlockActivitySpot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUnlockActivitySpot";
        };

        return ReqUnlockActivitySpot;
    })();

    lq.ReqUnlockActivitySpotEnding = (function() {

        function ReqUnlockActivitySpotEnding(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUnlockActivitySpotEnding.prototype.unique_id = 0;
        ReqUnlockActivitySpotEnding.prototype.ending_id = 0;

        ReqUnlockActivitySpotEnding.create = function create(properties) {
            return new ReqUnlockActivitySpotEnding(properties);
        };

        ReqUnlockActivitySpotEnding.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(16).uint32(message.ending_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUnlockActivitySpotEnding.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUnlockActivitySpotEnding.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUnlockActivitySpotEnding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUnlockActivitySpotEnding.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUnlockActivitySpotEnding.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            return null;
        };

        ReqUnlockActivitySpotEnding.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUnlockActivitySpotEnding)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUnlockActivitySpotEnding();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            return message;
        };

        ReqUnlockActivitySpotEnding.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.ending_id = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            return object;
        };

        ReqUnlockActivitySpotEnding.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUnlockActivitySpotEnding.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUnlockActivitySpotEnding";
        };

        return ReqUnlockActivitySpotEnding;
    })();

    lq.ReqReceiveActivitySpotReward = (function() {

        function ReqReceiveActivitySpotReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveActivitySpotReward.prototype.unique_id = 0;

        ReqReceiveActivitySpotReward.create = function create(properties) {
            return new ReqReceiveActivitySpotReward(properties);
        };

        ReqReceiveActivitySpotReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveActivitySpotReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveActivitySpotReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveActivitySpotReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveActivitySpotReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveActivitySpotReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqReceiveActivitySpotReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveActivitySpotReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveActivitySpotReward();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqReceiveActivitySpotReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqReceiveActivitySpotReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveActivitySpotReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveActivitySpotReward";
        };

        return ReqReceiveActivitySpotReward;
    })();

    lq.ResReceiveActivitySpotReward = (function() {

        function ResReceiveActivitySpotReward(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveActivitySpotReward.prototype.error = null;
        ResReceiveActivitySpotReward.prototype.items = $util.emptyArray;

        ResReceiveActivitySpotReward.create = function create(properties) {
            return new ResReceiveActivitySpotReward(properties);
        };

        ResReceiveActivitySpotReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResReceiveActivitySpotReward.RewardItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveActivitySpotReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveActivitySpotReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveActivitySpotReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResReceiveActivitySpotReward.RewardItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveActivitySpotReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveActivitySpotReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResReceiveActivitySpotReward.RewardItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ResReceiveActivitySpotReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveActivitySpotReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveActivitySpotReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveActivitySpotReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResReceiveActivitySpotReward.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResReceiveActivitySpotReward.items: object expected");
                    message.items[i] = $root.lq.ResReceiveActivitySpotReward.RewardItem.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveActivitySpotReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResReceiveActivitySpotReward.RewardItem.toObject(message.items[j], options);
            }
            return object;
        };

        ResReceiveActivitySpotReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveActivitySpotReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveActivitySpotReward";
        };

        ResReceiveActivitySpotReward.RewardItem = (function() {

            function RewardItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RewardItem.prototype.id = 0;
            RewardItem.prototype.count = 0;

            RewardItem.create = function create(properties) {
                return new RewardItem(properties);
            };

            RewardItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RewardItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RewardItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveActivitySpotReward.RewardItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RewardItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RewardItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            RewardItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResReceiveActivitySpotReward.RewardItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResReceiveActivitySpotReward.RewardItem();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            RewardItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.id = 0;
                    object.count = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            RewardItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RewardItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResReceiveActivitySpotReward.RewardItem";
            };

            return RewardItem;
        })();

        return ResReceiveActivitySpotReward;
    })();

    lq.ReqLogReport = (function() {

        function ReqLogReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqLogReport.prototype.success = 0;
        ReqLogReport.prototype.failed = 0;

        ReqLogReport.create = function create(properties) {
            return new ReqLogReport(properties);
        };

        ReqLogReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                writer.uint32(8).uint32(message.success);
            if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                writer.uint32(16).uint32(message.failed);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqLogReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqLogReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqLogReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.failed = value;
                        else
                            delete message.failed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqLogReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqLogReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.success != null && message.hasOwnProperty("success"))
                if (!$util.isInteger(message.success))
                    return "success: integer expected";
            if (message.failed != null && message.hasOwnProperty("failed"))
                if (!$util.isInteger(message.failed))
                    return "failed: integer expected";
            return null;
        };

        ReqLogReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqLogReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqLogReport();
            if (object.success != null)
                if (Number(object.success) !== 0)
                    message.success = object.success >>> 0;
            if (object.failed != null)
                if (Number(object.failed) !== 0)
                    message.failed = object.failed >>> 0;
            return message;
        };

        ReqLogReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.success = 0;
                object.failed = 0;
            }
            if (message.success != null && message.hasOwnProperty("success"))
                object.success = message.success;
            if (message.failed != null && message.hasOwnProperty("failed"))
                object.failed = message.failed;
            return object;
        };

        ReqLogReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqLogReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqLogReport";
        };

        return ReqLogReport;
    })();

    lq.ReqBindOauth2 = (function() {

        function ReqBindOauth2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBindOauth2.prototype.type = 0;
        ReqBindOauth2.prototype.token = "";

        ReqBindOauth2.create = function create(properties) {
            return new ReqBindOauth2(properties);
        };

        ReqBindOauth2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBindOauth2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBindOauth2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBindOauth2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBindOauth2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBindOauth2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        ReqBindOauth2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBindOauth2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBindOauth2();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            return message;
        };

        ReqBindOauth2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.token = "";
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        ReqBindOauth2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBindOauth2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBindOauth2";
        };

        return ReqBindOauth2;
    })();

    lq.ReqFetchOauth2 = (function() {

        function ReqFetchOauth2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchOauth2.prototype.type = 0;

        ReqFetchOauth2.create = function create(properties) {
            return new ReqFetchOauth2(properties);
        };

        ReqFetchOauth2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchOauth2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchOauth2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchOauth2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchOauth2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchOauth2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqFetchOauth2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchOauth2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchOauth2();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqFetchOauth2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.type = 0;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqFetchOauth2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchOauth2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchOauth2";
        };

        return ReqFetchOauth2;
    })();

    lq.ResFetchOauth2 = (function() {

        function ResFetchOauth2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchOauth2.prototype.error = null;
        ResFetchOauth2.prototype.openid = "";

        ResFetchOauth2.create = function create(properties) {
            return new ResFetchOauth2(properties);
        };

        ResFetchOauth2.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.openid != null && Object.hasOwnProperty.call(message, "openid"))
                writer.uint32(18).string(message.openid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchOauth2.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchOauth2.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchOauth2(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.openid = value;
                        else
                            delete message.openid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchOauth2.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchOauth2.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.openid != null && message.hasOwnProperty("openid"))
                if (!$util.isString(message.openid))
                    return "openid: string expected";
            return null;
        };

        ResFetchOauth2.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchOauth2)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchOauth2();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchOauth2.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.openid != null)
                if (typeof object.openid !== "string" || object.openid.length)
                    message.openid = String(object.openid);
            return message;
        };

        ResFetchOauth2.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.openid = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.openid != null && message.hasOwnProperty("openid"))
                object.openid = message.openid;
            return object;
        };

        ResFetchOauth2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchOauth2.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchOauth2";
        };

        return ResFetchOauth2;
    })();

    lq.ResDeleteAccount = (function() {

        function ResDeleteAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResDeleteAccount.prototype.error = null;
        ResDeleteAccount.prototype.delete_time = 0;

        ResDeleteAccount.create = function create(properties) {
            return new ResDeleteAccount(properties);
        };

        ResDeleteAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.delete_time != null && Object.hasOwnProperty.call(message, "delete_time"))
                writer.uint32(16).uint32(message.delete_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResDeleteAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResDeleteAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResDeleteAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.delete_time = value;
                        else
                            delete message.delete_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResDeleteAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResDeleteAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.delete_time != null && message.hasOwnProperty("delete_time"))
                if (!$util.isInteger(message.delete_time))
                    return "delete_time: integer expected";
            return null;
        };

        ResDeleteAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResDeleteAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResDeleteAccount();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResDeleteAccount.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.delete_time != null)
                if (Number(object.delete_time) !== 0)
                    message.delete_time = object.delete_time >>> 0;
            return message;
        };

        ResDeleteAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.delete_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.delete_time != null && message.hasOwnProperty("delete_time"))
                object.delete_time = message.delete_time;
            return object;
        };

        ResDeleteAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResDeleteAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResDeleteAccount";
        };

        return ResDeleteAccount;
    })();

    lq.ReqSetLoadingImage = (function() {

        function ReqSetLoadingImage(properties) {
            this.images = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetLoadingImage.prototype.images = $util.emptyArray;

        ReqSetLoadingImage.create = function create(properties) {
            return new ReqSetLoadingImage(properties);
        };

        ReqSetLoadingImage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.images != null && message.images.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.images.length; ++i)
                    writer.uint32(message.images[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetLoadingImage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetLoadingImage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetLoadingImage();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.images && message.images.length))
                                message.images = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.images.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.images && message.images.length))
                            message.images = [];
                        message.images.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetLoadingImage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetLoadingImage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.images != null && message.hasOwnProperty("images")) {
                if (!Array.isArray(message.images))
                    return "images: array expected";
                for (let i = 0; i < message.images.length; ++i)
                    if (!$util.isInteger(message.images[i]))
                        return "images: integer[] expected";
            }
            return null;
        };

        ReqSetLoadingImage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetLoadingImage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetLoadingImage();
            if (object.images) {
                if (!Array.isArray(object.images))
                    throw TypeError(".lq.ReqSetLoadingImage.images: array expected");
                message.images = Array(object.images.length);
                for (let i = 0; i < object.images.length; ++i)
                    message.images[i] = object.images[i] >>> 0;
            }
            return message;
        };

        ReqSetLoadingImage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.images = [];
            if (message.images && message.images.length) {
                object.images = Array(message.images.length);
                for (let j = 0; j < message.images.length; ++j)
                    object.images[j] = message.images[j];
            }
            return object;
        };

        ReqSetLoadingImage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetLoadingImage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetLoadingImage";
        };

        return ReqSetLoadingImage;
    })();

    lq.ResFetchShopInterval = (function() {

        function ResFetchShopInterval(properties) {
            this.result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchShopInterval.prototype.error = null;
        ResFetchShopInterval.prototype.result = $util.emptyArray;

        ResFetchShopInterval.create = function create(properties) {
            return new ResFetchShopInterval(properties);
        };

        ResFetchShopInterval.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result != null && message.result.length)
                for (let i = 0; i < message.result.length; ++i)
                    $root.lq.ResFetchShopInterval.ShopInterval.encode(message.result[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchShopInterval.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchShopInterval.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchShopInterval(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.result && message.result.length))
                            message.result = [];
                        message.result.push($root.lq.ResFetchShopInterval.ShopInterval.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchShopInterval.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchShopInterval.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result != null && message.hasOwnProperty("result")) {
                if (!Array.isArray(message.result))
                    return "result: array expected";
                for (let i = 0; i < message.result.length; ++i) {
                    let error = $root.lq.ResFetchShopInterval.ShopInterval.verify(message.result[i], _depth + 1);
                    if (error)
                        return "result." + error;
                }
            }
            return null;
        };

        ResFetchShopInterval.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchShopInterval)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchShopInterval();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchShopInterval.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result) {
                if (!Array.isArray(object.result))
                    throw TypeError(".lq.ResFetchShopInterval.result: array expected");
                message.result = Array(object.result.length);
                for (let i = 0; i < object.result.length; ++i) {
                    if (typeof object.result[i] !== "object")
                        throw TypeError(".lq.ResFetchShopInterval.result: object expected");
                    message.result[i] = $root.lq.ResFetchShopInterval.ShopInterval.fromObject(object.result[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchShopInterval.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.result = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result && message.result.length) {
                object.result = Array(message.result.length);
                for (let j = 0; j < message.result.length; ++j)
                    object.result[j] = $root.lq.ResFetchShopInterval.ShopInterval.toObject(message.result[j], options);
            }
            return object;
        };

        ResFetchShopInterval.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchShopInterval.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchShopInterval";
        };

        ResFetchShopInterval.ShopInterval = (function() {

            function ShopInterval(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ShopInterval.prototype.group_id = 0;
            ShopInterval.prototype.interval = 0;

            ShopInterval.create = function create(properties) {
                return new ShopInterval(properties);
            };

            ShopInterval.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.group_id != null && Object.hasOwnProperty.call(message, "group_id"))
                    writer.uint32(8).uint32(message.group_id);
                if (message.interval != null && Object.hasOwnProperty.call(message, "interval"))
                    writer.uint32(16).uint32(message.interval);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ShopInterval.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ShopInterval.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchShopInterval.ShopInterval(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.group_id = value;
                            else
                                delete message.group_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.interval = value;
                            else
                                delete message.interval;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ShopInterval.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ShopInterval.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.group_id != null && message.hasOwnProperty("group_id"))
                    if (!$util.isInteger(message.group_id))
                        return "group_id: integer expected";
                if (message.interval != null && message.hasOwnProperty("interval"))
                    if (!$util.isInteger(message.interval))
                        return "interval: integer expected";
                return null;
            };

            ShopInterval.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchShopInterval.ShopInterval)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchShopInterval.ShopInterval();
                if (object.group_id != null)
                    if (Number(object.group_id) !== 0)
                        message.group_id = object.group_id >>> 0;
                if (object.interval != null)
                    if (Number(object.interval) !== 0)
                        message.interval = object.interval >>> 0;
                return message;
            };

            ShopInterval.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.group_id = 0;
                    object.interval = 0;
                }
                if (message.group_id != null && message.hasOwnProperty("group_id"))
                    object.group_id = message.group_id;
                if (message.interval != null && message.hasOwnProperty("interval"))
                    object.interval = message.interval;
                return object;
            };

            ShopInterval.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ShopInterval.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchShopInterval.ShopInterval";
            };

            return ShopInterval;
        })();

        return ResFetchShopInterval;
    })();

    lq.ResFetchActivityInterval = (function() {

        function ResFetchActivityInterval(properties) {
            this.result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchActivityInterval.prototype.error = null;
        ResFetchActivityInterval.prototype.result = $util.emptyArray;

        ResFetchActivityInterval.create = function create(properties) {
            return new ResFetchActivityInterval(properties);
        };

        ResFetchActivityInterval.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result != null && message.result.length)
                for (let i = 0; i < message.result.length; ++i)
                    $root.lq.ResFetchActivityInterval.ActivityInterval.encode(message.result[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchActivityInterval.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchActivityInterval.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchActivityInterval(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.result && message.result.length))
                            message.result = [];
                        message.result.push($root.lq.ResFetchActivityInterval.ActivityInterval.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchActivityInterval.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchActivityInterval.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result != null && message.hasOwnProperty("result")) {
                if (!Array.isArray(message.result))
                    return "result: array expected";
                for (let i = 0; i < message.result.length; ++i) {
                    let error = $root.lq.ResFetchActivityInterval.ActivityInterval.verify(message.result[i], _depth + 1);
                    if (error)
                        return "result." + error;
                }
            }
            return null;
        };

        ResFetchActivityInterval.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchActivityInterval)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchActivityInterval();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchActivityInterval.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result) {
                if (!Array.isArray(object.result))
                    throw TypeError(".lq.ResFetchActivityInterval.result: array expected");
                message.result = Array(object.result.length);
                for (let i = 0; i < object.result.length; ++i) {
                    if (typeof object.result[i] !== "object")
                        throw TypeError(".lq.ResFetchActivityInterval.result: object expected");
                    message.result[i] = $root.lq.ResFetchActivityInterval.ActivityInterval.fromObject(object.result[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchActivityInterval.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.result = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result && message.result.length) {
                object.result = Array(message.result.length);
                for (let j = 0; j < message.result.length; ++j)
                    object.result[j] = $root.lq.ResFetchActivityInterval.ActivityInterval.toObject(message.result[j], options);
            }
            return object;
        };

        ResFetchActivityInterval.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchActivityInterval.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchActivityInterval";
        };

        ResFetchActivityInterval.ActivityInterval = (function() {

            function ActivityInterval(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityInterval.prototype.activity_id = 0;
            ActivityInterval.prototype.interval = 0;

            ActivityInterval.create = function create(properties) {
                return new ActivityInterval(properties);
            };

            ActivityInterval.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.interval != null && Object.hasOwnProperty.call(message, "interval"))
                    writer.uint32(16).uint32(message.interval);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityInterval.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityInterval.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchActivityInterval.ActivityInterval(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.interval = value;
                            else
                                delete message.interval;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityInterval.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityInterval.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.interval != null && message.hasOwnProperty("interval"))
                    if (!$util.isInteger(message.interval))
                        return "interval: integer expected";
                return null;
            };

            ActivityInterval.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchActivityInterval.ActivityInterval)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchActivityInterval.ActivityInterval();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.interval != null)
                    if (Number(object.interval) !== 0)
                        message.interval = object.interval >>> 0;
                return message;
            };

            ActivityInterval.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.activity_id = 0;
                    object.interval = 0;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.interval != null && message.hasOwnProperty("interval"))
                    object.interval = message.interval;
                return object;
            };

            ActivityInterval.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityInterval.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchActivityInterval.ActivityInterval";
            };

            return ActivityInterval;
        })();

        return ResFetchActivityInterval;
    })();

    lq.ResFetchrecentFriend = (function() {

        function ResFetchrecentFriend(properties) {
            this.account_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchrecentFriend.prototype.error = null;
        ResFetchrecentFriend.prototype.account_list = $util.emptyArray;

        ResFetchrecentFriend.create = function create(properties) {
            return new ResFetchrecentFriend(properties);
        };

        ResFetchrecentFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.account_list != null && message.account_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.account_list.length; ++i)
                    writer.uint32(message.account_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchrecentFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchrecentFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchrecentFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.account_list && message.account_list.length))
                                message.account_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_list && message.account_list.length))
                            message.account_list = [];
                        message.account_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchrecentFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchrecentFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                if (!Array.isArray(message.account_list))
                    return "account_list: array expected";
                for (let i = 0; i < message.account_list.length; ++i)
                    if (!$util.isInteger(message.account_list[i]))
                        return "account_list: integer[] expected";
            }
            return null;
        };

        ResFetchrecentFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchrecentFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchrecentFriend();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchrecentFriend.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.account_list) {
                if (!Array.isArray(object.account_list))
                    throw TypeError(".lq.ResFetchrecentFriend.account_list: array expected");
                message.account_list = Array(object.account_list.length);
                for (let i = 0; i < object.account_list.length; ++i)
                    message.account_list[i] = object.account_list[i] >>> 0;
            }
            return message;
        };

        ResFetchrecentFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.account_list && message.account_list.length) {
                object.account_list = Array(message.account_list.length);
                for (let j = 0; j < message.account_list.length; ++j)
                    object.account_list[j] = message.account_list[j];
            }
            return object;
        };

        ResFetchrecentFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchrecentFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchrecentFriend";
        };

        return ResFetchrecentFriend;
    })();

    lq.ReqOpenGacha = (function() {

        function ReqOpenGacha(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqOpenGacha.prototype.activity_id = 0;
        ReqOpenGacha.prototype.count = 0;

        ReqOpenGacha.create = function create(properties) {
            return new ReqOpenGacha(properties);
        };

        ReqOpenGacha.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqOpenGacha.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqOpenGacha.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqOpenGacha(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqOpenGacha.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqOpenGacha.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            return null;
        };

        ReqOpenGacha.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqOpenGacha)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqOpenGacha();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            return message;
        };

        ReqOpenGacha.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.count = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            return object;
        };

        ReqOpenGacha.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqOpenGacha.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqOpenGacha";
        };

        return ReqOpenGacha;
    })();

    lq.ResOpenGacha = (function() {

        function ResOpenGacha(properties) {
            this.result_list = [];
            this.reward_items = [];
            this.sp_reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResOpenGacha.prototype.error = null;
        ResOpenGacha.prototype.result_list = $util.emptyArray;
        ResOpenGacha.prototype.reward_items = $util.emptyArray;
        ResOpenGacha.prototype.sp_reward_items = $util.emptyArray;
        ResOpenGacha.prototype.remain_count = 0;

        ResOpenGacha.create = function create(properties) {
            return new ResOpenGacha(properties);
        };

        ResOpenGacha.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result_list != null && message.result_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.result_list.length; ++i)
                    writer.uint32(message.result_list[i]);
                writer.ldelim();
            }
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(26).fork()).ldelim();
            if (message.sp_reward_items != null && message.sp_reward_items.length)
                for (let i = 0; i < message.sp_reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.sp_reward_items[i], writer.uint32(34).fork()).ldelim();
            if (message.remain_count != null && Object.hasOwnProperty.call(message, "remain_count"))
                writer.uint32(40).uint32(message.remain_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResOpenGacha.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResOpenGacha.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResOpenGacha(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.result_list && message.result_list.length))
                                message.result_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.result_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.result_list && message.result_list.length))
                            message.result_list = [];
                        message.result_list.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sp_reward_items && message.sp_reward_items.length))
                            message.sp_reward_items = [];
                        message.sp_reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain_count = value;
                        else
                            delete message.remain_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResOpenGacha.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResOpenGacha.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result_list != null && message.hasOwnProperty("result_list")) {
                if (!Array.isArray(message.result_list))
                    return "result_list: array expected";
                for (let i = 0; i < message.result_list.length; ++i)
                    if (!$util.isInteger(message.result_list[i]))
                        return "result_list: integer[] expected";
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            if (message.sp_reward_items != null && message.hasOwnProperty("sp_reward_items")) {
                if (!Array.isArray(message.sp_reward_items))
                    return "sp_reward_items: array expected";
                for (let i = 0; i < message.sp_reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.sp_reward_items[i], _depth + 1);
                    if (error)
                        return "sp_reward_items." + error;
                }
            }
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                if (!$util.isInteger(message.remain_count))
                    return "remain_count: integer expected";
            return null;
        };

        ResOpenGacha.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResOpenGacha)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResOpenGacha();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResOpenGacha.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result_list) {
                if (!Array.isArray(object.result_list))
                    throw TypeError(".lq.ResOpenGacha.result_list: array expected");
                message.result_list = Array(object.result_list.length);
                for (let i = 0; i < object.result_list.length; ++i)
                    message.result_list[i] = object.result_list[i] >>> 0;
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResOpenGacha.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResOpenGacha.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            if (object.sp_reward_items) {
                if (!Array.isArray(object.sp_reward_items))
                    throw TypeError(".lq.ResOpenGacha.sp_reward_items: array expected");
                message.sp_reward_items = Array(object.sp_reward_items.length);
                for (let i = 0; i < object.sp_reward_items.length; ++i) {
                    if (typeof object.sp_reward_items[i] !== "object")
                        throw TypeError(".lq.ResOpenGacha.sp_reward_items: object expected");
                    message.sp_reward_items[i] = $root.lq.ExecuteReward.fromObject(object.sp_reward_items[i], _depth + 1);
                }
            }
            if (object.remain_count != null)
                if (Number(object.remain_count) !== 0)
                    message.remain_count = object.remain_count >>> 0;
            return message;
        };

        ResOpenGacha.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.result_list = [];
                object.reward_items = [];
                object.sp_reward_items = [];
            }
            if (options.defaults) {
                object.error = null;
                object.remain_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result_list && message.result_list.length) {
                object.result_list = Array(message.result_list.length);
                for (let j = 0; j < message.result_list.length; ++j)
                    object.result_list[j] = message.result_list[j];
            }
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            if (message.sp_reward_items && message.sp_reward_items.length) {
                object.sp_reward_items = Array(message.sp_reward_items.length);
                for (let j = 0; j < message.sp_reward_items.length; ++j)
                    object.sp_reward_items[j] = $root.lq.ExecuteReward.toObject(message.sp_reward_items[j], options);
            }
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                object.remain_count = message.remain_count;
            return object;
        };

        ResOpenGacha.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResOpenGacha.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResOpenGacha";
        };

        return ResOpenGacha;
    })();

    lq.ReqTaskRequest = (function() {

        function ReqTaskRequest(properties) {
            this.params = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqTaskRequest.prototype.params = $util.emptyArray;

        ReqTaskRequest.create = function create(properties) {
            return new ReqTaskRequest(properties);
        };

        ReqTaskRequest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.params != null && message.params.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.params.length; ++i)
                    writer.uint32(message.params[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqTaskRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqTaskRequest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqTaskRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.params && message.params.length))
                                message.params = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.params.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.params && message.params.length))
                            message.params = [];
                        message.params.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqTaskRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqTaskRequest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.params != null && message.hasOwnProperty("params")) {
                if (!Array.isArray(message.params))
                    return "params: array expected";
                for (let i = 0; i < message.params.length; ++i)
                    if (!$util.isInteger(message.params[i]))
                        return "params: integer[] expected";
            }
            return null;
        };

        ReqTaskRequest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqTaskRequest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqTaskRequest();
            if (object.params) {
                if (!Array.isArray(object.params))
                    throw TypeError(".lq.ReqTaskRequest.params: array expected");
                message.params = Array(object.params.length);
                for (let i = 0; i < object.params.length; ++i)
                    message.params[i] = object.params[i] >>> 0;
            }
            return message;
        };

        ReqTaskRequest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.params = [];
            if (message.params && message.params.length) {
                object.params = Array(message.params.length);
                for (let j = 0; j < message.params.length; ++j)
                    object.params[j] = message.params[j];
            }
            return object;
        };

        ReqTaskRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqTaskRequest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqTaskRequest";
        };

        return ReqTaskRequest;
    })();

    lq.ReqYostarDeleteAccount = (function() {

        function ReqYostarDeleteAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqYostarDeleteAccount.prototype.server = 0;

        ReqYostarDeleteAccount.create = function create(properties) {
            return new ReqYostarDeleteAccount(properties);
        };

        ReqYostarDeleteAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.server != null && Object.hasOwnProperty.call(message, "server"))
                writer.uint32(8).uint32(message.server);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqYostarDeleteAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqYostarDeleteAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqYostarDeleteAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.server = value;
                        else
                            delete message.server;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqYostarDeleteAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqYostarDeleteAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.server != null && message.hasOwnProperty("server"))
                if (!$util.isInteger(message.server))
                    return "server: integer expected";
            return null;
        };

        ReqYostarDeleteAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqYostarDeleteAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqYostarDeleteAccount();
            if (object.server != null)
                if (Number(object.server) !== 0)
                    message.server = object.server >>> 0;
            return message;
        };

        ReqYostarDeleteAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.server = 0;
            if (message.server != null && message.hasOwnProperty("server"))
                object.server = message.server;
            return object;
        };

        ReqYostarDeleteAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqYostarDeleteAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqYostarDeleteAccount";
        };

        return ReqYostarDeleteAccount;
    })();

    lq.ResYostarDeleteAccount = (function() {

        function ResYostarDeleteAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResYostarDeleteAccount.prototype.error = null;
        ResYostarDeleteAccount.prototype.notify_url = "";

        ResYostarDeleteAccount.create = function create(properties) {
            return new ResYostarDeleteAccount(properties);
        };

        ResYostarDeleteAccount.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.notify_url != null && Object.hasOwnProperty.call(message, "notify_url"))
                writer.uint32(18).string(message.notify_url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResYostarDeleteAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResYostarDeleteAccount.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResYostarDeleteAccount(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.notify_url = value;
                        else
                            delete message.notify_url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResYostarDeleteAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResYostarDeleteAccount.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.notify_url != null && message.hasOwnProperty("notify_url"))
                if (!$util.isString(message.notify_url))
                    return "notify_url: string expected";
            return null;
        };

        ResYostarDeleteAccount.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResYostarDeleteAccount)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResYostarDeleteAccount();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResYostarDeleteAccount.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.notify_url != null)
                if (typeof object.notify_url !== "string" || object.notify_url.length)
                    message.notify_url = String(object.notify_url);
            return message;
        };

        ResYostarDeleteAccount.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.notify_url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.notify_url != null && message.hasOwnProperty("notify_url"))
                object.notify_url = message.notify_url;
            return object;
        };

        ResYostarDeleteAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResYostarDeleteAccount.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResYostarDeleteAccount";
        };

        return ResYostarDeleteAccount;
    })();

    lq.ReqSimulationActivityTrain = (function() {

        function ReqSimulationActivityTrain(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimulationActivityTrain.prototype.activity_id = 0;
        ReqSimulationActivityTrain.prototype.type = 0;

        ReqSimulationActivityTrain.create = function create(properties) {
            return new ReqSimulationActivityTrain(properties);
        };

        ReqSimulationActivityTrain.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimulationActivityTrain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimulationActivityTrain.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimulationActivityTrain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimulationActivityTrain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimulationActivityTrain.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqSimulationActivityTrain.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimulationActivityTrain)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimulationActivityTrain();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqSimulationActivityTrain.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.type = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqSimulationActivityTrain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimulationActivityTrain.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimulationActivityTrain";
        };

        return ReqSimulationActivityTrain;
    })();

    lq.ResSimulationActivityTrain = (function() {

        function ResSimulationActivityTrain(properties) {
            this.final_stats = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimulationActivityTrain.prototype.error = null;
        ResSimulationActivityTrain.prototype.result_type = 0;
        ResSimulationActivityTrain.prototype.final_stats = $util.emptyArray;

        ResSimulationActivityTrain.create = function create(properties) {
            return new ResSimulationActivityTrain(properties);
        };

        ResSimulationActivityTrain.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result_type != null && Object.hasOwnProperty.call(message, "result_type"))
                writer.uint32(16).uint32(message.result_type);
            if (message.final_stats != null && message.final_stats.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.final_stats.length; ++i)
                    writer.uint32(message.final_stats[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimulationActivityTrain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimulationActivityTrain.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimulationActivityTrain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result_type = value;
                        else
                            delete message.result_type;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.final_stats && message.final_stats.length))
                                message.final_stats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.final_stats.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.final_stats && message.final_stats.length))
                            message.final_stats = [];
                        message.final_stats.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimulationActivityTrain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimulationActivityTrain.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result_type != null && message.hasOwnProperty("result_type"))
                if (!$util.isInteger(message.result_type))
                    return "result_type: integer expected";
            if (message.final_stats != null && message.hasOwnProperty("final_stats")) {
                if (!Array.isArray(message.final_stats))
                    return "final_stats: array expected";
                for (let i = 0; i < message.final_stats.length; ++i)
                    if (!$util.isInteger(message.final_stats[i]))
                        return "final_stats: integer[] expected";
            }
            return null;
        };

        ResSimulationActivityTrain.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimulationActivityTrain)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimulationActivityTrain();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimulationActivityTrain.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result_type != null)
                if (Number(object.result_type) !== 0)
                    message.result_type = object.result_type >>> 0;
            if (object.final_stats) {
                if (!Array.isArray(object.final_stats))
                    throw TypeError(".lq.ResSimulationActivityTrain.final_stats: array expected");
                message.final_stats = Array(object.final_stats.length);
                for (let i = 0; i < object.final_stats.length; ++i)
                    message.final_stats[i] = object.final_stats[i] >>> 0;
            }
            return message;
        };

        ResSimulationActivityTrain.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.final_stats = [];
            if (options.defaults) {
                object.error = null;
                object.result_type = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result_type != null && message.hasOwnProperty("result_type"))
                object.result_type = message.result_type;
            if (message.final_stats && message.final_stats.length) {
                object.final_stats = Array(message.final_stats.length);
                for (let j = 0; j < message.final_stats.length; ++j)
                    object.final_stats[j] = message.final_stats[j];
            }
            return object;
        };

        ResSimulationActivityTrain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimulationActivityTrain.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimulationActivityTrain";
        };

        return ResSimulationActivityTrain;
    })();

    lq.ReqFetchSimulationGameRecord = (function() {

        function ReqFetchSimulationGameRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchSimulationGameRecord.prototype.game_uuid = "";
        ReqFetchSimulationGameRecord.prototype.activity_id = 0;

        ReqFetchSimulationGameRecord.create = function create(properties) {
            return new ReqFetchSimulationGameRecord(properties);
        };

        ReqFetchSimulationGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchSimulationGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchSimulationGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchSimulationGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchSimulationGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchSimulationGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchSimulationGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchSimulationGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchSimulationGameRecord();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchSimulationGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.game_uuid = "";
                object.activity_id = 0;
            }
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchSimulationGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchSimulationGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchSimulationGameRecord";
        };

        return ReqFetchSimulationGameRecord;
    })();

    lq.ResFetchSimulationGameRecord = (function() {

        function ResFetchSimulationGameRecord(properties) {
            this.messages = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSimulationGameRecord.prototype.error = null;
        ResFetchSimulationGameRecord.prototype.messages = $util.emptyArray;

        ResFetchSimulationGameRecord.create = function create(properties) {
            return new ResFetchSimulationGameRecord(properties);
        };

        ResFetchSimulationGameRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.messages != null && message.messages.length)
                for (let i = 0; i < message.messages.length; ++i)
                    $root.lq.ActivitySimulationGameRecordMessage.encode(message.messages[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSimulationGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSimulationGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSimulationGameRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.messages && message.messages.length))
                            message.messages = [];
                        message.messages.push($root.lq.ActivitySimulationGameRecordMessage.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSimulationGameRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSimulationGameRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.messages != null && message.hasOwnProperty("messages")) {
                if (!Array.isArray(message.messages))
                    return "messages: array expected";
                for (let i = 0; i < message.messages.length; ++i) {
                    let error = $root.lq.ActivitySimulationGameRecordMessage.verify(message.messages[i], _depth + 1);
                    if (error)
                        return "messages." + error;
                }
            }
            return null;
        };

        ResFetchSimulationGameRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSimulationGameRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSimulationGameRecord();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSimulationGameRecord.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.messages) {
                if (!Array.isArray(object.messages))
                    throw TypeError(".lq.ResFetchSimulationGameRecord.messages: array expected");
                message.messages = Array(object.messages.length);
                for (let i = 0; i < object.messages.length; ++i) {
                    if (typeof object.messages[i] !== "object")
                        throw TypeError(".lq.ResFetchSimulationGameRecord.messages: object expected");
                    message.messages[i] = $root.lq.ActivitySimulationGameRecordMessage.fromObject(object.messages[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchSimulationGameRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.messages = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.messages && message.messages.length) {
                object.messages = Array(message.messages.length);
                for (let j = 0; j < message.messages.length; ++j)
                    object.messages[j] = $root.lq.ActivitySimulationGameRecordMessage.toObject(message.messages[j], options);
            }
            return object;
        };

        ResFetchSimulationGameRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSimulationGameRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSimulationGameRecord";
        };

        return ResFetchSimulationGameRecord;
    })();

    lq.ReqStartSimulationActivityGame = (function() {

        function ReqStartSimulationActivityGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStartSimulationActivityGame.prototype.activity_id = 0;

        ReqStartSimulationActivityGame.create = function create(properties) {
            return new ReqStartSimulationActivityGame(properties);
        };

        ReqStartSimulationActivityGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStartSimulationActivityGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStartSimulationActivityGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStartSimulationActivityGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStartSimulationActivityGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStartSimulationActivityGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqStartSimulationActivityGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStartSimulationActivityGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStartSimulationActivityGame();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqStartSimulationActivityGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqStartSimulationActivityGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStartSimulationActivityGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStartSimulationActivityGame";
        };

        return ReqStartSimulationActivityGame;
    })();

    lq.ResStartSimulationActivityGame = (function() {

        function ResStartSimulationActivityGame(properties) {
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResStartSimulationActivityGame.prototype.error = null;
        ResStartSimulationActivityGame.prototype.records = $util.emptyArray;

        ResStartSimulationActivityGame.create = function create(properties) {
            return new ResStartSimulationActivityGame(properties);
        };

        ResStartSimulationActivityGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.lq.ActivitySimulationGameRecord.encode(message.records[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResStartSimulationActivityGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResStartSimulationActivityGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResStartSimulationActivityGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.lq.ActivitySimulationGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResStartSimulationActivityGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResStartSimulationActivityGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.lq.ActivitySimulationGameRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        ResStartSimulationActivityGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResStartSimulationActivityGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResStartSimulationActivityGame();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResStartSimulationActivityGame.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.ResStartSimulationActivityGame.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".lq.ResStartSimulationActivityGame.records: object expected");
                    message.records[i] = $root.lq.ActivitySimulationGameRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            return message;
        };

        ResStartSimulationActivityGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.lq.ActivitySimulationGameRecord.toObject(message.records[j], options);
            }
            return object;
        };

        ResStartSimulationActivityGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResStartSimulationActivityGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResStartSimulationActivityGame";
        };

        return ResStartSimulationActivityGame;
    })();

    lq.ReqFetchSimulationGameRank = (function() {

        function ReqFetchSimulationGameRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchSimulationGameRank.prototype.activity_id = 0;
        ReqFetchSimulationGameRank.prototype.day = 0;

        ReqFetchSimulationGameRank.create = function create(properties) {
            return new ReqFetchSimulationGameRank(properties);
        };

        ReqFetchSimulationGameRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.day != null && Object.hasOwnProperty.call(message, "day"))
                writer.uint32(16).uint32(message.day);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchSimulationGameRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchSimulationGameRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchSimulationGameRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.day = value;
                        else
                            delete message.day;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchSimulationGameRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchSimulationGameRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.day != null && message.hasOwnProperty("day"))
                if (!$util.isInteger(message.day))
                    return "day: integer expected";
            return null;
        };

        ReqFetchSimulationGameRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchSimulationGameRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchSimulationGameRank();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.day != null)
                if (Number(object.day) !== 0)
                    message.day = object.day >>> 0;
            return message;
        };

        ReqFetchSimulationGameRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.day = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.day != null && message.hasOwnProperty("day"))
                object.day = message.day;
            return object;
        };

        ReqFetchSimulationGameRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchSimulationGameRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchSimulationGameRank";
        };

        return ReqFetchSimulationGameRank;
    })();

    lq.ResFetchSimulationGameRank = (function() {

        function ResFetchSimulationGameRank(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSimulationGameRank.prototype.error = null;
        ResFetchSimulationGameRank.prototype.rank = $util.emptyArray;

        ResFetchSimulationGameRank.create = function create(properties) {
            return new ResFetchSimulationGameRank(properties);
        };

        ResFetchSimulationGameRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rank != null && message.rank.length)
                for (let i = 0; i < message.rank.length; ++i)
                    $root.lq.ResFetchSimulationGameRank.RankInfo.encode(message.rank[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSimulationGameRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSimulationGameRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSimulationGameRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push($root.lq.ResFetchSimulationGameRank.RankInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSimulationGameRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSimulationGameRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i) {
                    let error = $root.lq.ResFetchSimulationGameRank.RankInfo.verify(message.rank[i], _depth + 1);
                    if (error)
                        return "rank." + error;
                }
            }
            return null;
        };

        ResFetchSimulationGameRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSimulationGameRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSimulationGameRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSimulationGameRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.ResFetchSimulationGameRank.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i) {
                    if (typeof object.rank[i] !== "object")
                        throw TypeError(".lq.ResFetchSimulationGameRank.rank: object expected");
                    message.rank[i] = $root.lq.ResFetchSimulationGameRank.RankInfo.fromObject(object.rank[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchSimulationGameRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = $root.lq.ResFetchSimulationGameRank.RankInfo.toObject(message.rank[j], options);
            }
            return object;
        };

        ResFetchSimulationGameRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSimulationGameRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSimulationGameRank";
        };

        ResFetchSimulationGameRank.RankInfo = (function() {

            function RankInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            RankInfo.prototype.character = 0;
            RankInfo.prototype.score = 0;

            RankInfo.create = function create(properties) {
                return new RankInfo(properties);
            };

            RankInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character != null && Object.hasOwnProperty.call(message, "character"))
                    writer.uint32(8).uint32(message.character);
                if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                    writer.uint32(21).float(message.score);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            RankInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            RankInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSimulationGameRank.RankInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.character = value;
                            else
                                delete message.character;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 5)
                                break;
                            if ((value = reader.float()) !== 0)
                                message.score = value;
                            else
                                delete message.score;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            RankInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            RankInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character != null && message.hasOwnProperty("character"))
                    if (!$util.isInteger(message.character))
                        return "character: integer expected";
                if (message.score != null && message.hasOwnProperty("score"))
                    if (typeof message.score !== "number")
                        return "score: number expected";
                return null;
            };

            RankInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchSimulationGameRank.RankInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchSimulationGameRank.RankInfo();
                if (object.character != null)
                    if (Number(object.character) !== 0)
                        message.character = object.character >>> 0;
                if (object.score != null)
                    if (Number(object.score) !== 0)
                        message.score = Number(object.score);
                return message;
            };

            RankInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.character = 0;
                    object.score = 0;
                }
                if (message.character != null && message.hasOwnProperty("character"))
                    object.character = message.character;
                if (message.score != null && message.hasOwnProperty("score"))
                    object.score = options.json && !isFinite(message.score) ? String(message.score) : message.score;
                return object;
            };

            RankInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RankInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchSimulationGameRank.RankInfo";
            };

            return RankInfo;
        })();

        return ResFetchSimulationGameRank;
    })();

    lq.ReqGenerateCombiningCraft = (function() {

        function ReqGenerateCombiningCraft(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGenerateCombiningCraft.prototype.activity_id = 0;
        ReqGenerateCombiningCraft.prototype.bin_id = 0;

        ReqGenerateCombiningCraft.create = function create(properties) {
            return new ReqGenerateCombiningCraft(properties);
        };

        ReqGenerateCombiningCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.bin_id != null && Object.hasOwnProperty.call(message, "bin_id"))
                writer.uint32(16).uint32(message.bin_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGenerateCombiningCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGenerateCombiningCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGenerateCombiningCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bin_id = value;
                        else
                            delete message.bin_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGenerateCombiningCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGenerateCombiningCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.bin_id != null && message.hasOwnProperty("bin_id"))
                if (!$util.isInteger(message.bin_id))
                    return "bin_id: integer expected";
            return null;
        };

        ReqGenerateCombiningCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGenerateCombiningCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGenerateCombiningCraft();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.bin_id != null)
                if (Number(object.bin_id) !== 0)
                    message.bin_id = object.bin_id >>> 0;
            return message;
        };

        ReqGenerateCombiningCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.bin_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.bin_id != null && message.hasOwnProperty("bin_id"))
                object.bin_id = message.bin_id;
            return object;
        };

        ReqGenerateCombiningCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGenerateCombiningCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGenerateCombiningCraft";
        };

        return ReqGenerateCombiningCraft;
    })();

    lq.ResGenerateCombiningCraft = (function() {

        function ResGenerateCombiningCraft(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGenerateCombiningCraft.prototype.error = null;
        ResGenerateCombiningCraft.prototype.pos = 0;
        ResGenerateCombiningCraft.prototype.craft_id = 0;

        ResGenerateCombiningCraft.create = function create(properties) {
            return new ResGenerateCombiningCraft(properties);
        };

        ResGenerateCombiningCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.craft_id != null && Object.hasOwnProperty.call(message, "craft_id"))
                writer.uint32(24).uint32(message.craft_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGenerateCombiningCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGenerateCombiningCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGenerateCombiningCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.craft_id = value;
                        else
                            delete message.craft_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGenerateCombiningCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGenerateCombiningCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                if (!$util.isInteger(message.craft_id))
                    return "craft_id: integer expected";
            return null;
        };

        ResGenerateCombiningCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGenerateCombiningCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGenerateCombiningCraft();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGenerateCombiningCraft.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            if (object.craft_id != null)
                if (Number(object.craft_id) !== 0)
                    message.craft_id = object.craft_id >>> 0;
            return message;
        };

        ResGenerateCombiningCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.pos = 0;
                object.craft_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                object.craft_id = message.craft_id;
            return object;
        };

        ResGenerateCombiningCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGenerateCombiningCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGenerateCombiningCraft";
        };

        return ResGenerateCombiningCraft;
    })();

    lq.ReqMoveCombiningCraft = (function() {

        function ReqMoveCombiningCraft(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMoveCombiningCraft.prototype.activity_id = 0;
        ReqMoveCombiningCraft.prototype.from = 0;
        ReqMoveCombiningCraft.prototype.to = 0;

        ReqMoveCombiningCraft.create = function create(properties) {
            return new ReqMoveCombiningCraft(properties);
        };

        ReqMoveCombiningCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                writer.uint32(16).uint32(message.from);
            if (message.to != null && Object.hasOwnProperty.call(message, "to"))
                writer.uint32(24).uint32(message.to);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMoveCombiningCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMoveCombiningCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMoveCombiningCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.from = value;
                        else
                            delete message.from;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.to = value;
                        else
                            delete message.to;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMoveCombiningCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMoveCombiningCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.from != null && message.hasOwnProperty("from"))
                if (!$util.isInteger(message.from))
                    return "from: integer expected";
            if (message.to != null && message.hasOwnProperty("to"))
                if (!$util.isInteger(message.to))
                    return "to: integer expected";
            return null;
        };

        ReqMoveCombiningCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMoveCombiningCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMoveCombiningCraft();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.from != null)
                if (Number(object.from) !== 0)
                    message.from = object.from >>> 0;
            if (object.to != null)
                if (Number(object.to) !== 0)
                    message.to = object.to >>> 0;
            return message;
        };

        ReqMoveCombiningCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.from = 0;
                object.to = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.from != null && message.hasOwnProperty("from"))
                object.from = message.from;
            if (message.to != null && message.hasOwnProperty("to"))
                object.to = message.to;
            return object;
        };

        ReqMoveCombiningCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMoveCombiningCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMoveCombiningCraft";
        };

        return ReqMoveCombiningCraft;
    })();

    lq.ResMoveCombiningCraft = (function() {

        function ResMoveCombiningCraft(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMoveCombiningCraft.prototype.error = null;
        ResMoveCombiningCraft.prototype.pos = 0;
        ResMoveCombiningCraft.prototype.combined = 0;
        ResMoveCombiningCraft.prototype.craft_id = 0;
        ResMoveCombiningCraft.prototype.bonus = null;

        ResMoveCombiningCraft.create = function create(properties) {
            return new ResMoveCombiningCraft(properties);
        };

        ResMoveCombiningCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.combined != null && Object.hasOwnProperty.call(message, "combined"))
                writer.uint32(24).uint32(message.combined);
            if (message.craft_id != null && Object.hasOwnProperty.call(message, "craft_id"))
                writer.uint32(32).uint32(message.craft_id);
            if (message.bonus != null && Object.hasOwnProperty.call(message, "bonus"))
                $root.lq.ResMoveCombiningCraft.BonusData.encode(message.bonus, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMoveCombiningCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMoveCombiningCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMoveCombiningCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.combined = value;
                        else
                            delete message.combined;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.craft_id = value;
                        else
                            delete message.craft_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.bonus = $root.lq.ResMoveCombiningCraft.BonusData.decode(reader, reader.uint32(), undefined, _depth + 1, message.bonus);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMoveCombiningCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMoveCombiningCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            if (message.combined != null && message.hasOwnProperty("combined"))
                if (!$util.isInteger(message.combined))
                    return "combined: integer expected";
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                if (!$util.isInteger(message.craft_id))
                    return "craft_id: integer expected";
            if (message.bonus != null && message.hasOwnProperty("bonus")) {
                let error = $root.lq.ResMoveCombiningCraft.BonusData.verify(message.bonus, _depth + 1);
                if (error)
                    return "bonus." + error;
            }
            return null;
        };

        ResMoveCombiningCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMoveCombiningCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMoveCombiningCraft();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMoveCombiningCraft.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            if (object.combined != null)
                if (Number(object.combined) !== 0)
                    message.combined = object.combined >>> 0;
            if (object.craft_id != null)
                if (Number(object.craft_id) !== 0)
                    message.craft_id = object.craft_id >>> 0;
            if (object.bonus != null) {
                if (typeof object.bonus !== "object")
                    throw TypeError(".lq.ResMoveCombiningCraft.bonus: object expected");
                message.bonus = $root.lq.ResMoveCombiningCraft.BonusData.fromObject(object.bonus, _depth + 1);
            }
            return message;
        };

        ResMoveCombiningCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.pos = 0;
                object.combined = 0;
                object.craft_id = 0;
                object.bonus = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            if (message.combined != null && message.hasOwnProperty("combined"))
                object.combined = message.combined;
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                object.craft_id = message.craft_id;
            if (message.bonus != null && message.hasOwnProperty("bonus"))
                object.bonus = $root.lq.ResMoveCombiningCraft.BonusData.toObject(message.bonus, options);
            return object;
        };

        ResMoveCombiningCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMoveCombiningCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMoveCombiningCraft";
        };

        ResMoveCombiningCraft.BonusData = (function() {

            function BonusData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BonusData.prototype.craft_id = 0;
            BonusData.prototype.pos = 0;

            BonusData.create = function create(properties) {
                return new BonusData(properties);
            };

            BonusData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.craft_id != null && Object.hasOwnProperty.call(message, "craft_id"))
                    writer.uint32(8).uint32(message.craft_id);
                if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                    writer.uint32(16).uint32(message.pos);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BonusData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BonusData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMoveCombiningCraft.BonusData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.craft_id = value;
                            else
                                delete message.craft_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.pos = value;
                            else
                                delete message.pos;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BonusData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BonusData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                    if (!$util.isInteger(message.craft_id))
                        return "craft_id: integer expected";
                if (message.pos != null && message.hasOwnProperty("pos"))
                    if (!$util.isInteger(message.pos))
                        return "pos: integer expected";
                return null;
            };

            BonusData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResMoveCombiningCraft.BonusData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResMoveCombiningCraft.BonusData();
                if (object.craft_id != null)
                    if (Number(object.craft_id) !== 0)
                        message.craft_id = object.craft_id >>> 0;
                if (object.pos != null)
                    if (Number(object.pos) !== 0)
                        message.pos = object.pos >>> 0;
                return message;
            };

            BonusData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.craft_id = 0;
                    object.pos = 0;
                }
                if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                    object.craft_id = message.craft_id;
                if (message.pos != null && message.hasOwnProperty("pos"))
                    object.pos = message.pos;
                return object;
            };

            BonusData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BonusData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResMoveCombiningCraft.BonusData";
            };

            return BonusData;
        })();

        return ResMoveCombiningCraft;
    })();

    lq.ReqCombiningRecycleCraft = (function() {

        function ReqCombiningRecycleCraft(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCombiningRecycleCraft.prototype.activity_id = 0;
        ReqCombiningRecycleCraft.prototype.pos = 0;

        ReqCombiningRecycleCraft.create = function create(properties) {
            return new ReqCombiningRecycleCraft(properties);
        };

        ReqCombiningRecycleCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCombiningRecycleCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCombiningRecycleCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCombiningRecycleCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCombiningRecycleCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCombiningRecycleCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            return null;
        };

        ReqCombiningRecycleCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCombiningRecycleCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCombiningRecycleCraft();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            return message;
        };

        ReqCombiningRecycleCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.pos = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            return object;
        };

        ReqCombiningRecycleCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCombiningRecycleCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCombiningRecycleCraft";
        };

        return ReqCombiningRecycleCraft;
    })();

    lq.ResCombiningRecycleCraft = (function() {

        function ResCombiningRecycleCraft(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCombiningRecycleCraft.prototype.error = null;
        ResCombiningRecycleCraft.prototype.reward_items = $util.emptyArray;

        ResCombiningRecycleCraft.create = function create(properties) {
            return new ResCombiningRecycleCraft(properties);
        };

        ResCombiningRecycleCraft.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCombiningRecycleCraft.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCombiningRecycleCraft.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCombiningRecycleCraft(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCombiningRecycleCraft.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCombiningRecycleCraft.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResCombiningRecycleCraft.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCombiningRecycleCraft)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCombiningRecycleCraft();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCombiningRecycleCraft.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResCombiningRecycleCraft.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResCombiningRecycleCraft.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResCombiningRecycleCraft.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResCombiningRecycleCraft.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCombiningRecycleCraft.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCombiningRecycleCraft";
        };

        return ResCombiningRecycleCraft;
    })();

    lq.ReqRecoverCombiningRecycle = (function() {

        function ReqRecoverCombiningRecycle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRecoverCombiningRecycle.prototype.activity_id = 0;

        ReqRecoverCombiningRecycle.create = function create(properties) {
            return new ReqRecoverCombiningRecycle(properties);
        };

        ReqRecoverCombiningRecycle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRecoverCombiningRecycle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRecoverCombiningRecycle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRecoverCombiningRecycle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRecoverCombiningRecycle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRecoverCombiningRecycle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqRecoverCombiningRecycle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRecoverCombiningRecycle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRecoverCombiningRecycle();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqRecoverCombiningRecycle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqRecoverCombiningRecycle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRecoverCombiningRecycle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRecoverCombiningRecycle";
        };

        return ReqRecoverCombiningRecycle;
    })();

    lq.ResRecoverCombiningRecycle = (function() {

        function ResRecoverCombiningRecycle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRecoverCombiningRecycle.prototype.error = null;
        ResRecoverCombiningRecycle.prototype.craft_id = 0;
        ResRecoverCombiningRecycle.prototype.pos = 0;

        ResRecoverCombiningRecycle.create = function create(properties) {
            return new ResRecoverCombiningRecycle(properties);
        };

        ResRecoverCombiningRecycle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.craft_id != null && Object.hasOwnProperty.call(message, "craft_id"))
                writer.uint32(16).uint32(message.craft_id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(24).uint32(message.pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRecoverCombiningRecycle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRecoverCombiningRecycle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRecoverCombiningRecycle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.craft_id = value;
                        else
                            delete message.craft_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRecoverCombiningRecycle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRecoverCombiningRecycle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                if (!$util.isInteger(message.craft_id))
                    return "craft_id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            return null;
        };

        ResRecoverCombiningRecycle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRecoverCombiningRecycle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRecoverCombiningRecycle();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRecoverCombiningRecycle.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.craft_id != null)
                if (Number(object.craft_id) !== 0)
                    message.craft_id = object.craft_id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            return message;
        };

        ResRecoverCombiningRecycle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.craft_id = 0;
                object.pos = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.craft_id != null && message.hasOwnProperty("craft_id"))
                object.craft_id = message.craft_id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            return object;
        };

        ResRecoverCombiningRecycle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRecoverCombiningRecycle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRecoverCombiningRecycle";
        };

        return ResRecoverCombiningRecycle;
    })();

    lq.ReqFinishCombiningOrder = (function() {

        function ReqFinishCombiningOrder(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFinishCombiningOrder.prototype.activity_id = 0;
        ReqFinishCombiningOrder.prototype.craft_pos = 0;
        ReqFinishCombiningOrder.prototype.order_pos = 0;

        ReqFinishCombiningOrder.create = function create(properties) {
            return new ReqFinishCombiningOrder(properties);
        };

        ReqFinishCombiningOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.craft_pos != null && Object.hasOwnProperty.call(message, "craft_pos"))
                writer.uint32(16).uint32(message.craft_pos);
            if (message.order_pos != null && Object.hasOwnProperty.call(message, "order_pos"))
                writer.uint32(24).uint32(message.order_pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFinishCombiningOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFinishCombiningOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFinishCombiningOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.craft_pos = value;
                        else
                            delete message.craft_pos;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.order_pos = value;
                        else
                            delete message.order_pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFinishCombiningOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFinishCombiningOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.craft_pos != null && message.hasOwnProperty("craft_pos"))
                if (!$util.isInteger(message.craft_pos))
                    return "craft_pos: integer expected";
            if (message.order_pos != null && message.hasOwnProperty("order_pos"))
                if (!$util.isInteger(message.order_pos))
                    return "order_pos: integer expected";
            return null;
        };

        ReqFinishCombiningOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFinishCombiningOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFinishCombiningOrder();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.craft_pos != null)
                if (Number(object.craft_pos) !== 0)
                    message.craft_pos = object.craft_pos >>> 0;
            if (object.order_pos != null)
                if (Number(object.order_pos) !== 0)
                    message.order_pos = object.order_pos >>> 0;
            return message;
        };

        ReqFinishCombiningOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.craft_pos = 0;
                object.order_pos = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.craft_pos != null && message.hasOwnProperty("craft_pos"))
                object.craft_pos = message.craft_pos;
            if (message.order_pos != null && message.hasOwnProperty("order_pos"))
                object.order_pos = message.order_pos;
            return object;
        };

        ReqFinishCombiningOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFinishCombiningOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFinishCombiningOrder";
        };

        return ReqFinishCombiningOrder;
    })();

    lq.ResFinishCombiningOrder = (function() {

        function ResFinishCombiningOrder(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFinishCombiningOrder.prototype.error = null;
        ResFinishCombiningOrder.prototype.reward_items = $util.emptyArray;

        ResFinishCombiningOrder.create = function create(properties) {
            return new ResFinishCombiningOrder(properties);
        };

        ResFinishCombiningOrder.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFinishCombiningOrder.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFinishCombiningOrder.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFinishCombiningOrder(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFinishCombiningOrder.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFinishCombiningOrder.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResFinishCombiningOrder.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFinishCombiningOrder)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFinishCombiningOrder();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFinishCombiningOrder.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResFinishCombiningOrder.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResFinishCombiningOrder.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResFinishCombiningOrder.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResFinishCombiningOrder.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFinishCombiningOrder.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFinishCombiningOrder";
        };

        return ResFinishCombiningOrder;
    })();

    lq.ResFetchInfo = (function() {

        function ResFetchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchInfo.prototype.error = null;
        ResFetchInfo.prototype.server_time = null;
        ResFetchInfo.prototype.server_setting = null;
        ResFetchInfo.prototype.client_value = null;
        ResFetchInfo.prototype.friend_list = null;
        ResFetchInfo.prototype.friend_apply_list = null;
        ResFetchInfo.prototype.recent_friend = null;
        ResFetchInfo.prototype.mail_info = null;
        ResFetchInfo.prototype.receive_coin_info = null;
        ResFetchInfo.prototype.title_list = null;
        ResFetchInfo.prototype.bag_info = null;
        ResFetchInfo.prototype.shop_info = null;
        ResFetchInfo.prototype.shop_interval = null;
        ResFetchInfo.prototype.activity_data = null;
        ResFetchInfo.prototype.activity_interval = null;
        ResFetchInfo.prototype.activity_buff = null;
        ResFetchInfo.prototype.vip_reward = null;
        ResFetchInfo.prototype.month_ticket_info = null;
        ResFetchInfo.prototype.achievement = null;
        ResFetchInfo.prototype.comment_setting = null;
        ResFetchInfo.prototype.account_settings = null;
        ResFetchInfo.prototype.mod_nickname_time = null;
        ResFetchInfo.prototype.misc = null;
        ResFetchInfo.prototype.announcement = null;
        ResFetchInfo.prototype.activity_list = null;
        ResFetchInfo.prototype.character_info = null;
        ResFetchInfo.prototype.all_common_views = null;
        ResFetchInfo.prototype.collected_game_record_list = null;
        ResFetchInfo.prototype.maintain_notice = null;
        ResFetchInfo.prototype.random_character = null;
        ResFetchInfo.prototype.maintenance_info = null;
        ResFetchInfo.prototype.seer_info = null;
        ResFetchInfo.prototype.annual_report_info = null;

        ResFetchInfo.create = function create(properties) {
            return new ResFetchInfo(properties);
        };

        ResFetchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.server_time != null && Object.hasOwnProperty.call(message, "server_time"))
                $root.lq.ResServerTime.encode(message.server_time, writer.uint32(18).fork()).ldelim();
            if (message.server_setting != null && Object.hasOwnProperty.call(message, "server_setting"))
                $root.lq.ResServerSettings.encode(message.server_setting, writer.uint32(26).fork()).ldelim();
            if (message.client_value != null && Object.hasOwnProperty.call(message, "client_value"))
                $root.lq.ResClientValue.encode(message.client_value, writer.uint32(34).fork()).ldelim();
            if (message.friend_list != null && Object.hasOwnProperty.call(message, "friend_list"))
                $root.lq.ResFriendList.encode(message.friend_list, writer.uint32(42).fork()).ldelim();
            if (message.friend_apply_list != null && Object.hasOwnProperty.call(message, "friend_apply_list"))
                $root.lq.ResFriendApplyList.encode(message.friend_apply_list, writer.uint32(50).fork()).ldelim();
            if (message.recent_friend != null && Object.hasOwnProperty.call(message, "recent_friend"))
                $root.lq.ResFetchrecentFriend.encode(message.recent_friend, writer.uint32(58).fork()).ldelim();
            if (message.mail_info != null && Object.hasOwnProperty.call(message, "mail_info"))
                $root.lq.ResMailInfo.encode(message.mail_info, writer.uint32(66).fork()).ldelim();
            if (message.receive_coin_info != null && Object.hasOwnProperty.call(message, "receive_coin_info"))
                $root.lq.ResReviveCoinInfo.encode(message.receive_coin_info, writer.uint32(74).fork()).ldelim();
            if (message.title_list != null && Object.hasOwnProperty.call(message, "title_list"))
                $root.lq.ResTitleList.encode(message.title_list, writer.uint32(82).fork()).ldelim();
            if (message.bag_info != null && Object.hasOwnProperty.call(message, "bag_info"))
                $root.lq.ResBagInfo.encode(message.bag_info, writer.uint32(90).fork()).ldelim();
            if (message.shop_info != null && Object.hasOwnProperty.call(message, "shop_info"))
                $root.lq.ResShopInfo.encode(message.shop_info, writer.uint32(98).fork()).ldelim();
            if (message.shop_interval != null && Object.hasOwnProperty.call(message, "shop_interval"))
                $root.lq.ResFetchShopInterval.encode(message.shop_interval, writer.uint32(106).fork()).ldelim();
            if (message.activity_data != null && Object.hasOwnProperty.call(message, "activity_data"))
                $root.lq.ResAccountActivityData.encode(message.activity_data, writer.uint32(114).fork()).ldelim();
            if (message.activity_interval != null && Object.hasOwnProperty.call(message, "activity_interval"))
                $root.lq.ResFetchActivityInterval.encode(message.activity_interval, writer.uint32(122).fork()).ldelim();
            if (message.activity_buff != null && Object.hasOwnProperty.call(message, "activity_buff"))
                $root.lq.ResActivityBuff.encode(message.activity_buff, writer.uint32(130).fork()).ldelim();
            if (message.vip_reward != null && Object.hasOwnProperty.call(message, "vip_reward"))
                $root.lq.ResVipReward.encode(message.vip_reward, writer.uint32(138).fork()).ldelim();
            if (message.month_ticket_info != null && Object.hasOwnProperty.call(message, "month_ticket_info"))
                $root.lq.ResMonthTicketInfo.encode(message.month_ticket_info, writer.uint32(146).fork()).ldelim();
            if (message.achievement != null && Object.hasOwnProperty.call(message, "achievement"))
                $root.lq.ResAchievement.encode(message.achievement, writer.uint32(154).fork()).ldelim();
            if (message.comment_setting != null && Object.hasOwnProperty.call(message, "comment_setting"))
                $root.lq.ResCommentSetting.encode(message.comment_setting, writer.uint32(162).fork()).ldelim();
            if (message.account_settings != null && Object.hasOwnProperty.call(message, "account_settings"))
                $root.lq.ResAccountSettings.encode(message.account_settings, writer.uint32(170).fork()).ldelim();
            if (message.mod_nickname_time != null && Object.hasOwnProperty.call(message, "mod_nickname_time"))
                $root.lq.ResModNicknameTime.encode(message.mod_nickname_time, writer.uint32(178).fork()).ldelim();
            if (message.misc != null && Object.hasOwnProperty.call(message, "misc"))
                $root.lq.ResMisc.encode(message.misc, writer.uint32(186).fork()).ldelim();
            if (message.announcement != null && Object.hasOwnProperty.call(message, "announcement"))
                $root.lq.ResAnnouncement.encode(message.announcement, writer.uint32(194).fork()).ldelim();
            if (message.activity_list != null && Object.hasOwnProperty.call(message, "activity_list"))
                $root.lq.ResActivityList.encode(message.activity_list, writer.uint32(210).fork()).ldelim();
            if (message.character_info != null && Object.hasOwnProperty.call(message, "character_info"))
                $root.lq.ResCharacterInfo.encode(message.character_info, writer.uint32(218).fork()).ldelim();
            if (message.all_common_views != null && Object.hasOwnProperty.call(message, "all_common_views"))
                $root.lq.ResAllcommonViews.encode(message.all_common_views, writer.uint32(226).fork()).ldelim();
            if (message.collected_game_record_list != null && Object.hasOwnProperty.call(message, "collected_game_record_list"))
                $root.lq.ResCollectedGameRecordList.encode(message.collected_game_record_list, writer.uint32(234).fork()).ldelim();
            if (message.maintain_notice != null && Object.hasOwnProperty.call(message, "maintain_notice"))
                $root.lq.ResFetchMaintainNotice.encode(message.maintain_notice, writer.uint32(242).fork()).ldelim();
            if (message.random_character != null && Object.hasOwnProperty.call(message, "random_character"))
                $root.lq.ResRandomCharacter.encode(message.random_character, writer.uint32(250).fork()).ldelim();
            if (message.maintenance_info != null && Object.hasOwnProperty.call(message, "maintenance_info"))
                $root.lq.ResFetchServerMaintenanceInfo.encode(message.maintenance_info, writer.uint32(258).fork()).ldelim();
            if (message.seer_info != null && Object.hasOwnProperty.call(message, "seer_info"))
                $root.lq.ResFetchSeerInfo.encode(message.seer_info, writer.uint32(266).fork()).ldelim();
            if (message.annual_report_info != null && Object.hasOwnProperty.call(message, "annual_report_info"))
                $root.lq.ResFetchAnnualReportInfo.encode(message.annual_report_info, writer.uint32(274).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.server_time = $root.lq.ResServerTime.decode(reader, reader.uint32(), undefined, _depth + 1, message.server_time);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.server_setting = $root.lq.ResServerSettings.decode(reader, reader.uint32(), undefined, _depth + 1, message.server_setting);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.client_value = $root.lq.ResClientValue.decode(reader, reader.uint32(), undefined, _depth + 1, message.client_value);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.friend_list = $root.lq.ResFriendList.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_list);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.friend_apply_list = $root.lq.ResFriendApplyList.decode(reader, reader.uint32(), undefined, _depth + 1, message.friend_apply_list);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.recent_friend = $root.lq.ResFetchrecentFriend.decode(reader, reader.uint32(), undefined, _depth + 1, message.recent_friend);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.mail_info = $root.lq.ResMailInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.mail_info);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.receive_coin_info = $root.lq.ResReviveCoinInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.receive_coin_info);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.title_list = $root.lq.ResTitleList.decode(reader, reader.uint32(), undefined, _depth + 1, message.title_list);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.bag_info = $root.lq.ResBagInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.bag_info);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.shop_info = $root.lq.ResShopInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_info);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.shop_interval = $root.lq.ResFetchShopInterval.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_interval);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.activity_data = $root.lq.ResAccountActivityData.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_data);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.activity_interval = $root.lq.ResFetchActivityInterval.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_interval);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.activity_buff = $root.lq.ResActivityBuff.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_buff);
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.vip_reward = $root.lq.ResVipReward.decode(reader, reader.uint32(), undefined, _depth + 1, message.vip_reward);
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.month_ticket_info = $root.lq.ResMonthTicketInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.month_ticket_info);
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.achievement = $root.lq.ResAchievement.decode(reader, reader.uint32(), undefined, _depth + 1, message.achievement);
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.comment_setting = $root.lq.ResCommentSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.comment_setting);
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.account_settings = $root.lq.ResAccountSettings.decode(reader, reader.uint32(), undefined, _depth + 1, message.account_settings);
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.mod_nickname_time = $root.lq.ResModNicknameTime.decode(reader, reader.uint32(), undefined, _depth + 1, message.mod_nickname_time);
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.misc = $root.lq.ResMisc.decode(reader, reader.uint32(), undefined, _depth + 1, message.misc);
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.announcement = $root.lq.ResAnnouncement.decode(reader, reader.uint32(), undefined, _depth + 1, message.announcement);
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.activity_list = $root.lq.ResActivityList.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_list);
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        message.character_info = $root.lq.ResCharacterInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.character_info);
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.all_common_views = $root.lq.ResAllcommonViews.decode(reader, reader.uint32(), undefined, _depth + 1, message.all_common_views);
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        message.collected_game_record_list = $root.lq.ResCollectedGameRecordList.decode(reader, reader.uint32(), undefined, _depth + 1, message.collected_game_record_list);
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.maintain_notice = $root.lq.ResFetchMaintainNotice.decode(reader, reader.uint32(), undefined, _depth + 1, message.maintain_notice);
                        continue;
                    }
                case 31: {
                        if (wireType !== 2)
                            break;
                        message.random_character = $root.lq.ResRandomCharacter.decode(reader, reader.uint32(), undefined, _depth + 1, message.random_character);
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        message.maintenance_info = $root.lq.ResFetchServerMaintenanceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.maintenance_info);
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        message.seer_info = $root.lq.ResFetchSeerInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.seer_info);
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.annual_report_info = $root.lq.ResFetchAnnualReportInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.annual_report_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.server_time != null && message.hasOwnProperty("server_time")) {
                let error = $root.lq.ResServerTime.verify(message.server_time, _depth + 1);
                if (error)
                    return "server_time." + error;
            }
            if (message.server_setting != null && message.hasOwnProperty("server_setting")) {
                let error = $root.lq.ResServerSettings.verify(message.server_setting, _depth + 1);
                if (error)
                    return "server_setting." + error;
            }
            if (message.client_value != null && message.hasOwnProperty("client_value")) {
                let error = $root.lq.ResClientValue.verify(message.client_value, _depth + 1);
                if (error)
                    return "client_value." + error;
            }
            if (message.friend_list != null && message.hasOwnProperty("friend_list")) {
                let error = $root.lq.ResFriendList.verify(message.friend_list, _depth + 1);
                if (error)
                    return "friend_list." + error;
            }
            if (message.friend_apply_list != null && message.hasOwnProperty("friend_apply_list")) {
                let error = $root.lq.ResFriendApplyList.verify(message.friend_apply_list, _depth + 1);
                if (error)
                    return "friend_apply_list." + error;
            }
            if (message.recent_friend != null && message.hasOwnProperty("recent_friend")) {
                let error = $root.lq.ResFetchrecentFriend.verify(message.recent_friend, _depth + 1);
                if (error)
                    return "recent_friend." + error;
            }
            if (message.mail_info != null && message.hasOwnProperty("mail_info")) {
                let error = $root.lq.ResMailInfo.verify(message.mail_info, _depth + 1);
                if (error)
                    return "mail_info." + error;
            }
            if (message.receive_coin_info != null && message.hasOwnProperty("receive_coin_info")) {
                let error = $root.lq.ResReviveCoinInfo.verify(message.receive_coin_info, _depth + 1);
                if (error)
                    return "receive_coin_info." + error;
            }
            if (message.title_list != null && message.hasOwnProperty("title_list")) {
                let error = $root.lq.ResTitleList.verify(message.title_list, _depth + 1);
                if (error)
                    return "title_list." + error;
            }
            if (message.bag_info != null && message.hasOwnProperty("bag_info")) {
                let error = $root.lq.ResBagInfo.verify(message.bag_info, _depth + 1);
                if (error)
                    return "bag_info." + error;
            }
            if (message.shop_info != null && message.hasOwnProperty("shop_info")) {
                let error = $root.lq.ResShopInfo.verify(message.shop_info, _depth + 1);
                if (error)
                    return "shop_info." + error;
            }
            if (message.shop_interval != null && message.hasOwnProperty("shop_interval")) {
                let error = $root.lq.ResFetchShopInterval.verify(message.shop_interval, _depth + 1);
                if (error)
                    return "shop_interval." + error;
            }
            if (message.activity_data != null && message.hasOwnProperty("activity_data")) {
                let error = $root.lq.ResAccountActivityData.verify(message.activity_data, _depth + 1);
                if (error)
                    return "activity_data." + error;
            }
            if (message.activity_interval != null && message.hasOwnProperty("activity_interval")) {
                let error = $root.lq.ResFetchActivityInterval.verify(message.activity_interval, _depth + 1);
                if (error)
                    return "activity_interval." + error;
            }
            if (message.activity_buff != null && message.hasOwnProperty("activity_buff")) {
                let error = $root.lq.ResActivityBuff.verify(message.activity_buff, _depth + 1);
                if (error)
                    return "activity_buff." + error;
            }
            if (message.vip_reward != null && message.hasOwnProperty("vip_reward")) {
                let error = $root.lq.ResVipReward.verify(message.vip_reward, _depth + 1);
                if (error)
                    return "vip_reward." + error;
            }
            if (message.month_ticket_info != null && message.hasOwnProperty("month_ticket_info")) {
                let error = $root.lq.ResMonthTicketInfo.verify(message.month_ticket_info, _depth + 1);
                if (error)
                    return "month_ticket_info." + error;
            }
            if (message.achievement != null && message.hasOwnProperty("achievement")) {
                let error = $root.lq.ResAchievement.verify(message.achievement, _depth + 1);
                if (error)
                    return "achievement." + error;
            }
            if (message.comment_setting != null && message.hasOwnProperty("comment_setting")) {
                let error = $root.lq.ResCommentSetting.verify(message.comment_setting, _depth + 1);
                if (error)
                    return "comment_setting." + error;
            }
            if (message.account_settings != null && message.hasOwnProperty("account_settings")) {
                let error = $root.lq.ResAccountSettings.verify(message.account_settings, _depth + 1);
                if (error)
                    return "account_settings." + error;
            }
            if (message.mod_nickname_time != null && message.hasOwnProperty("mod_nickname_time")) {
                let error = $root.lq.ResModNicknameTime.verify(message.mod_nickname_time, _depth + 1);
                if (error)
                    return "mod_nickname_time." + error;
            }
            if (message.misc != null && message.hasOwnProperty("misc")) {
                let error = $root.lq.ResMisc.verify(message.misc, _depth + 1);
                if (error)
                    return "misc." + error;
            }
            if (message.announcement != null && message.hasOwnProperty("announcement")) {
                let error = $root.lq.ResAnnouncement.verify(message.announcement, _depth + 1);
                if (error)
                    return "announcement." + error;
            }
            if (message.activity_list != null && message.hasOwnProperty("activity_list")) {
                let error = $root.lq.ResActivityList.verify(message.activity_list, _depth + 1);
                if (error)
                    return "activity_list." + error;
            }
            if (message.character_info != null && message.hasOwnProperty("character_info")) {
                let error = $root.lq.ResCharacterInfo.verify(message.character_info, _depth + 1);
                if (error)
                    return "character_info." + error;
            }
            if (message.all_common_views != null && message.hasOwnProperty("all_common_views")) {
                let error = $root.lq.ResAllcommonViews.verify(message.all_common_views, _depth + 1);
                if (error)
                    return "all_common_views." + error;
            }
            if (message.collected_game_record_list != null && message.hasOwnProperty("collected_game_record_list")) {
                let error = $root.lq.ResCollectedGameRecordList.verify(message.collected_game_record_list, _depth + 1);
                if (error)
                    return "collected_game_record_list." + error;
            }
            if (message.maintain_notice != null && message.hasOwnProperty("maintain_notice")) {
                let error = $root.lq.ResFetchMaintainNotice.verify(message.maintain_notice, _depth + 1);
                if (error)
                    return "maintain_notice." + error;
            }
            if (message.random_character != null && message.hasOwnProperty("random_character")) {
                let error = $root.lq.ResRandomCharacter.verify(message.random_character, _depth + 1);
                if (error)
                    return "random_character." + error;
            }
            if (message.maintenance_info != null && message.hasOwnProperty("maintenance_info")) {
                let error = $root.lq.ResFetchServerMaintenanceInfo.verify(message.maintenance_info, _depth + 1);
                if (error)
                    return "maintenance_info." + error;
            }
            if (message.seer_info != null && message.hasOwnProperty("seer_info")) {
                let error = $root.lq.ResFetchSeerInfo.verify(message.seer_info, _depth + 1);
                if (error)
                    return "seer_info." + error;
            }
            if (message.annual_report_info != null && message.hasOwnProperty("annual_report_info")) {
                let error = $root.lq.ResFetchAnnualReportInfo.verify(message.annual_report_info, _depth + 1);
                if (error)
                    return "annual_report_info." + error;
            }
            return null;
        };

        ResFetchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.server_time != null) {
                if (typeof object.server_time !== "object")
                    throw TypeError(".lq.ResFetchInfo.server_time: object expected");
                message.server_time = $root.lq.ResServerTime.fromObject(object.server_time, _depth + 1);
            }
            if (object.server_setting != null) {
                if (typeof object.server_setting !== "object")
                    throw TypeError(".lq.ResFetchInfo.server_setting: object expected");
                message.server_setting = $root.lq.ResServerSettings.fromObject(object.server_setting, _depth + 1);
            }
            if (object.client_value != null) {
                if (typeof object.client_value !== "object")
                    throw TypeError(".lq.ResFetchInfo.client_value: object expected");
                message.client_value = $root.lq.ResClientValue.fromObject(object.client_value, _depth + 1);
            }
            if (object.friend_list != null) {
                if (typeof object.friend_list !== "object")
                    throw TypeError(".lq.ResFetchInfo.friend_list: object expected");
                message.friend_list = $root.lq.ResFriendList.fromObject(object.friend_list, _depth + 1);
            }
            if (object.friend_apply_list != null) {
                if (typeof object.friend_apply_list !== "object")
                    throw TypeError(".lq.ResFetchInfo.friend_apply_list: object expected");
                message.friend_apply_list = $root.lq.ResFriendApplyList.fromObject(object.friend_apply_list, _depth + 1);
            }
            if (object.recent_friend != null) {
                if (typeof object.recent_friend !== "object")
                    throw TypeError(".lq.ResFetchInfo.recent_friend: object expected");
                message.recent_friend = $root.lq.ResFetchrecentFriend.fromObject(object.recent_friend, _depth + 1);
            }
            if (object.mail_info != null) {
                if (typeof object.mail_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.mail_info: object expected");
                message.mail_info = $root.lq.ResMailInfo.fromObject(object.mail_info, _depth + 1);
            }
            if (object.receive_coin_info != null) {
                if (typeof object.receive_coin_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.receive_coin_info: object expected");
                message.receive_coin_info = $root.lq.ResReviveCoinInfo.fromObject(object.receive_coin_info, _depth + 1);
            }
            if (object.title_list != null) {
                if (typeof object.title_list !== "object")
                    throw TypeError(".lq.ResFetchInfo.title_list: object expected");
                message.title_list = $root.lq.ResTitleList.fromObject(object.title_list, _depth + 1);
            }
            if (object.bag_info != null) {
                if (typeof object.bag_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.bag_info: object expected");
                message.bag_info = $root.lq.ResBagInfo.fromObject(object.bag_info, _depth + 1);
            }
            if (object.shop_info != null) {
                if (typeof object.shop_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.shop_info: object expected");
                message.shop_info = $root.lq.ResShopInfo.fromObject(object.shop_info, _depth + 1);
            }
            if (object.shop_interval != null) {
                if (typeof object.shop_interval !== "object")
                    throw TypeError(".lq.ResFetchInfo.shop_interval: object expected");
                message.shop_interval = $root.lq.ResFetchShopInterval.fromObject(object.shop_interval, _depth + 1);
            }
            if (object.activity_data != null) {
                if (typeof object.activity_data !== "object")
                    throw TypeError(".lq.ResFetchInfo.activity_data: object expected");
                message.activity_data = $root.lq.ResAccountActivityData.fromObject(object.activity_data, _depth + 1);
            }
            if (object.activity_interval != null) {
                if (typeof object.activity_interval !== "object")
                    throw TypeError(".lq.ResFetchInfo.activity_interval: object expected");
                message.activity_interval = $root.lq.ResFetchActivityInterval.fromObject(object.activity_interval, _depth + 1);
            }
            if (object.activity_buff != null) {
                if (typeof object.activity_buff !== "object")
                    throw TypeError(".lq.ResFetchInfo.activity_buff: object expected");
                message.activity_buff = $root.lq.ResActivityBuff.fromObject(object.activity_buff, _depth + 1);
            }
            if (object.vip_reward != null) {
                if (typeof object.vip_reward !== "object")
                    throw TypeError(".lq.ResFetchInfo.vip_reward: object expected");
                message.vip_reward = $root.lq.ResVipReward.fromObject(object.vip_reward, _depth + 1);
            }
            if (object.month_ticket_info != null) {
                if (typeof object.month_ticket_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.month_ticket_info: object expected");
                message.month_ticket_info = $root.lq.ResMonthTicketInfo.fromObject(object.month_ticket_info, _depth + 1);
            }
            if (object.achievement != null) {
                if (typeof object.achievement !== "object")
                    throw TypeError(".lq.ResFetchInfo.achievement: object expected");
                message.achievement = $root.lq.ResAchievement.fromObject(object.achievement, _depth + 1);
            }
            if (object.comment_setting != null) {
                if (typeof object.comment_setting !== "object")
                    throw TypeError(".lq.ResFetchInfo.comment_setting: object expected");
                message.comment_setting = $root.lq.ResCommentSetting.fromObject(object.comment_setting, _depth + 1);
            }
            if (object.account_settings != null) {
                if (typeof object.account_settings !== "object")
                    throw TypeError(".lq.ResFetchInfo.account_settings: object expected");
                message.account_settings = $root.lq.ResAccountSettings.fromObject(object.account_settings, _depth + 1);
            }
            if (object.mod_nickname_time != null) {
                if (typeof object.mod_nickname_time !== "object")
                    throw TypeError(".lq.ResFetchInfo.mod_nickname_time: object expected");
                message.mod_nickname_time = $root.lq.ResModNicknameTime.fromObject(object.mod_nickname_time, _depth + 1);
            }
            if (object.misc != null) {
                if (typeof object.misc !== "object")
                    throw TypeError(".lq.ResFetchInfo.misc: object expected");
                message.misc = $root.lq.ResMisc.fromObject(object.misc, _depth + 1);
            }
            if (object.announcement != null) {
                if (typeof object.announcement !== "object")
                    throw TypeError(".lq.ResFetchInfo.announcement: object expected");
                message.announcement = $root.lq.ResAnnouncement.fromObject(object.announcement, _depth + 1);
            }
            if (object.activity_list != null) {
                if (typeof object.activity_list !== "object")
                    throw TypeError(".lq.ResFetchInfo.activity_list: object expected");
                message.activity_list = $root.lq.ResActivityList.fromObject(object.activity_list, _depth + 1);
            }
            if (object.character_info != null) {
                if (typeof object.character_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.character_info: object expected");
                message.character_info = $root.lq.ResCharacterInfo.fromObject(object.character_info, _depth + 1);
            }
            if (object.all_common_views != null) {
                if (typeof object.all_common_views !== "object")
                    throw TypeError(".lq.ResFetchInfo.all_common_views: object expected");
                message.all_common_views = $root.lq.ResAllcommonViews.fromObject(object.all_common_views, _depth + 1);
            }
            if (object.collected_game_record_list != null) {
                if (typeof object.collected_game_record_list !== "object")
                    throw TypeError(".lq.ResFetchInfo.collected_game_record_list: object expected");
                message.collected_game_record_list = $root.lq.ResCollectedGameRecordList.fromObject(object.collected_game_record_list, _depth + 1);
            }
            if (object.maintain_notice != null) {
                if (typeof object.maintain_notice !== "object")
                    throw TypeError(".lq.ResFetchInfo.maintain_notice: object expected");
                message.maintain_notice = $root.lq.ResFetchMaintainNotice.fromObject(object.maintain_notice, _depth + 1);
            }
            if (object.random_character != null) {
                if (typeof object.random_character !== "object")
                    throw TypeError(".lq.ResFetchInfo.random_character: object expected");
                message.random_character = $root.lq.ResRandomCharacter.fromObject(object.random_character, _depth + 1);
            }
            if (object.maintenance_info != null) {
                if (typeof object.maintenance_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.maintenance_info: object expected");
                message.maintenance_info = $root.lq.ResFetchServerMaintenanceInfo.fromObject(object.maintenance_info, _depth + 1);
            }
            if (object.seer_info != null) {
                if (typeof object.seer_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.seer_info: object expected");
                message.seer_info = $root.lq.ResFetchSeerInfo.fromObject(object.seer_info, _depth + 1);
            }
            if (object.annual_report_info != null) {
                if (typeof object.annual_report_info !== "object")
                    throw TypeError(".lq.ResFetchInfo.annual_report_info: object expected");
                message.annual_report_info = $root.lq.ResFetchAnnualReportInfo.fromObject(object.annual_report_info, _depth + 1);
            }
            return message;
        };

        ResFetchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.server_time = null;
                object.server_setting = null;
                object.client_value = null;
                object.friend_list = null;
                object.friend_apply_list = null;
                object.recent_friend = null;
                object.mail_info = null;
                object.receive_coin_info = null;
                object.title_list = null;
                object.bag_info = null;
                object.shop_info = null;
                object.shop_interval = null;
                object.activity_data = null;
                object.activity_interval = null;
                object.activity_buff = null;
                object.vip_reward = null;
                object.month_ticket_info = null;
                object.achievement = null;
                object.comment_setting = null;
                object.account_settings = null;
                object.mod_nickname_time = null;
                object.misc = null;
                object.announcement = null;
                object.activity_list = null;
                object.character_info = null;
                object.all_common_views = null;
                object.collected_game_record_list = null;
                object.maintain_notice = null;
                object.random_character = null;
                object.maintenance_info = null;
                object.seer_info = null;
                object.annual_report_info = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.server_time != null && message.hasOwnProperty("server_time"))
                object.server_time = $root.lq.ResServerTime.toObject(message.server_time, options);
            if (message.server_setting != null && message.hasOwnProperty("server_setting"))
                object.server_setting = $root.lq.ResServerSettings.toObject(message.server_setting, options);
            if (message.client_value != null && message.hasOwnProperty("client_value"))
                object.client_value = $root.lq.ResClientValue.toObject(message.client_value, options);
            if (message.friend_list != null && message.hasOwnProperty("friend_list"))
                object.friend_list = $root.lq.ResFriendList.toObject(message.friend_list, options);
            if (message.friend_apply_list != null && message.hasOwnProperty("friend_apply_list"))
                object.friend_apply_list = $root.lq.ResFriendApplyList.toObject(message.friend_apply_list, options);
            if (message.recent_friend != null && message.hasOwnProperty("recent_friend"))
                object.recent_friend = $root.lq.ResFetchrecentFriend.toObject(message.recent_friend, options);
            if (message.mail_info != null && message.hasOwnProperty("mail_info"))
                object.mail_info = $root.lq.ResMailInfo.toObject(message.mail_info, options);
            if (message.receive_coin_info != null && message.hasOwnProperty("receive_coin_info"))
                object.receive_coin_info = $root.lq.ResReviveCoinInfo.toObject(message.receive_coin_info, options);
            if (message.title_list != null && message.hasOwnProperty("title_list"))
                object.title_list = $root.lq.ResTitleList.toObject(message.title_list, options);
            if (message.bag_info != null && message.hasOwnProperty("bag_info"))
                object.bag_info = $root.lq.ResBagInfo.toObject(message.bag_info, options);
            if (message.shop_info != null && message.hasOwnProperty("shop_info"))
                object.shop_info = $root.lq.ResShopInfo.toObject(message.shop_info, options);
            if (message.shop_interval != null && message.hasOwnProperty("shop_interval"))
                object.shop_interval = $root.lq.ResFetchShopInterval.toObject(message.shop_interval, options);
            if (message.activity_data != null && message.hasOwnProperty("activity_data"))
                object.activity_data = $root.lq.ResAccountActivityData.toObject(message.activity_data, options);
            if (message.activity_interval != null && message.hasOwnProperty("activity_interval"))
                object.activity_interval = $root.lq.ResFetchActivityInterval.toObject(message.activity_interval, options);
            if (message.activity_buff != null && message.hasOwnProperty("activity_buff"))
                object.activity_buff = $root.lq.ResActivityBuff.toObject(message.activity_buff, options);
            if (message.vip_reward != null && message.hasOwnProperty("vip_reward"))
                object.vip_reward = $root.lq.ResVipReward.toObject(message.vip_reward, options);
            if (message.month_ticket_info != null && message.hasOwnProperty("month_ticket_info"))
                object.month_ticket_info = $root.lq.ResMonthTicketInfo.toObject(message.month_ticket_info, options);
            if (message.achievement != null && message.hasOwnProperty("achievement"))
                object.achievement = $root.lq.ResAchievement.toObject(message.achievement, options);
            if (message.comment_setting != null && message.hasOwnProperty("comment_setting"))
                object.comment_setting = $root.lq.ResCommentSetting.toObject(message.comment_setting, options);
            if (message.account_settings != null && message.hasOwnProperty("account_settings"))
                object.account_settings = $root.lq.ResAccountSettings.toObject(message.account_settings, options);
            if (message.mod_nickname_time != null && message.hasOwnProperty("mod_nickname_time"))
                object.mod_nickname_time = $root.lq.ResModNicknameTime.toObject(message.mod_nickname_time, options);
            if (message.misc != null && message.hasOwnProperty("misc"))
                object.misc = $root.lq.ResMisc.toObject(message.misc, options);
            if (message.announcement != null && message.hasOwnProperty("announcement"))
                object.announcement = $root.lq.ResAnnouncement.toObject(message.announcement, options);
            if (message.activity_list != null && message.hasOwnProperty("activity_list"))
                object.activity_list = $root.lq.ResActivityList.toObject(message.activity_list, options);
            if (message.character_info != null && message.hasOwnProperty("character_info"))
                object.character_info = $root.lq.ResCharacterInfo.toObject(message.character_info, options);
            if (message.all_common_views != null && message.hasOwnProperty("all_common_views"))
                object.all_common_views = $root.lq.ResAllcommonViews.toObject(message.all_common_views, options);
            if (message.collected_game_record_list != null && message.hasOwnProperty("collected_game_record_list"))
                object.collected_game_record_list = $root.lq.ResCollectedGameRecordList.toObject(message.collected_game_record_list, options);
            if (message.maintain_notice != null && message.hasOwnProperty("maintain_notice"))
                object.maintain_notice = $root.lq.ResFetchMaintainNotice.toObject(message.maintain_notice, options);
            if (message.random_character != null && message.hasOwnProperty("random_character"))
                object.random_character = $root.lq.ResRandomCharacter.toObject(message.random_character, options);
            if (message.maintenance_info != null && message.hasOwnProperty("maintenance_info"))
                object.maintenance_info = $root.lq.ResFetchServerMaintenanceInfo.toObject(message.maintenance_info, options);
            if (message.seer_info != null && message.hasOwnProperty("seer_info"))
                object.seer_info = $root.lq.ResFetchSeerInfo.toObject(message.seer_info, options);
            if (message.annual_report_info != null && message.hasOwnProperty("annual_report_info"))
                object.annual_report_info = $root.lq.ResFetchAnnualReportInfo.toObject(message.annual_report_info, options);
            return object;
        };

        ResFetchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchInfo";
        };

        return ResFetchInfo;
    })();

    lq.ResFetchSeerInfo = (function() {

        function ResFetchSeerInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSeerInfo.prototype.error = null;
        ResFetchSeerInfo.prototype.remain_count = 0;
        ResFetchSeerInfo.prototype.date_limit = 0;
        ResFetchSeerInfo.prototype.expire_time = 0;

        ResFetchSeerInfo.create = function create(properties) {
            return new ResFetchSeerInfo(properties);
        };

        ResFetchSeerInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.remain_count != null && Object.hasOwnProperty.call(message, "remain_count"))
                writer.uint32(16).uint32(message.remain_count);
            if (message.date_limit != null && Object.hasOwnProperty.call(message, "date_limit"))
                writer.uint32(24).uint32(message.date_limit);
            if (message.expire_time != null && Object.hasOwnProperty.call(message, "expire_time"))
                writer.uint32(32).uint32(message.expire_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSeerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSeerInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSeerInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.remain_count = value;
                        else
                            delete message.remain_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.date_limit = value;
                        else
                            delete message.date_limit;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expire_time = value;
                        else
                            delete message.expire_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSeerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSeerInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                if (!$util.isInteger(message.remain_count))
                    return "remain_count: integer expected";
            if (message.date_limit != null && message.hasOwnProperty("date_limit"))
                if (!$util.isInteger(message.date_limit))
                    return "date_limit: integer expected";
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                if (!$util.isInteger(message.expire_time))
                    return "expire_time: integer expected";
            return null;
        };

        ResFetchSeerInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSeerInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSeerInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSeerInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.remain_count != null)
                if (Number(object.remain_count) !== 0)
                    message.remain_count = object.remain_count >>> 0;
            if (object.date_limit != null)
                if (Number(object.date_limit) !== 0)
                    message.date_limit = object.date_limit >>> 0;
            if (object.expire_time != null)
                if (Number(object.expire_time) !== 0)
                    message.expire_time = object.expire_time >>> 0;
            return message;
        };

        ResFetchSeerInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.remain_count = 0;
                object.date_limit = 0;
                object.expire_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.remain_count != null && message.hasOwnProperty("remain_count"))
                object.remain_count = message.remain_count;
            if (message.date_limit != null && message.hasOwnProperty("date_limit"))
                object.date_limit = message.date_limit;
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                object.expire_time = message.expire_time;
            return object;
        };

        ResFetchSeerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSeerInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSeerInfo";
        };

        return ResFetchSeerInfo;
    })();

    lq.ResFetchServerMaintenanceInfo = (function() {

        function ResFetchServerMaintenanceInfo(properties) {
            this.function_maintenance = [];
            this.activity_maintenance = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchServerMaintenanceInfo.prototype.function_maintenance = $util.emptyArray;
        ResFetchServerMaintenanceInfo.prototype.activity_maintenance = $util.emptyArray;

        ResFetchServerMaintenanceInfo.create = function create(properties) {
            return new ResFetchServerMaintenanceInfo(properties);
        };

        ResFetchServerMaintenanceInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.function_maintenance != null && message.function_maintenance.length)
                for (let i = 0; i < message.function_maintenance.length; ++i)
                    $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo.encode(message.function_maintenance[i], writer.uint32(10).fork()).ldelim();
            if (message.activity_maintenance != null && message.activity_maintenance.length)
                for (let i = 0; i < message.activity_maintenance.length; ++i)
                    $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo.encode(message.activity_maintenance[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchServerMaintenanceInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchServerMaintenanceInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchServerMaintenanceInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.function_maintenance && message.function_maintenance.length))
                            message.function_maintenance = [];
                        message.function_maintenance.push($root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_maintenance && message.activity_maintenance.length))
                            message.activity_maintenance = [];
                        message.activity_maintenance.push($root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchServerMaintenanceInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchServerMaintenanceInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.function_maintenance != null && message.hasOwnProperty("function_maintenance")) {
                if (!Array.isArray(message.function_maintenance))
                    return "function_maintenance: array expected";
                for (let i = 0; i < message.function_maintenance.length; ++i) {
                    let error = $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo.verify(message.function_maintenance[i], _depth + 1);
                    if (error)
                        return "function_maintenance." + error;
                }
            }
            if (message.activity_maintenance != null && message.hasOwnProperty("activity_maintenance")) {
                if (!Array.isArray(message.activity_maintenance))
                    return "activity_maintenance: array expected";
                for (let i = 0; i < message.activity_maintenance.length; ++i) {
                    let error = $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo.verify(message.activity_maintenance[i], _depth + 1);
                    if (error)
                        return "activity_maintenance." + error;
                }
            }
            return null;
        };

        ResFetchServerMaintenanceInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchServerMaintenanceInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchServerMaintenanceInfo();
            if (object.function_maintenance) {
                if (!Array.isArray(object.function_maintenance))
                    throw TypeError(".lq.ResFetchServerMaintenanceInfo.function_maintenance: array expected");
                message.function_maintenance = Array(object.function_maintenance.length);
                for (let i = 0; i < object.function_maintenance.length; ++i) {
                    if (typeof object.function_maintenance[i] !== "object")
                        throw TypeError(".lq.ResFetchServerMaintenanceInfo.function_maintenance: object expected");
                    message.function_maintenance[i] = $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo.fromObject(object.function_maintenance[i], _depth + 1);
                }
            }
            if (object.activity_maintenance) {
                if (!Array.isArray(object.activity_maintenance))
                    throw TypeError(".lq.ResFetchServerMaintenanceInfo.activity_maintenance: array expected");
                message.activity_maintenance = Array(object.activity_maintenance.length);
                for (let i = 0; i < object.activity_maintenance.length; ++i) {
                    if (typeof object.activity_maintenance[i] !== "object")
                        throw TypeError(".lq.ResFetchServerMaintenanceInfo.activity_maintenance: object expected");
                    message.activity_maintenance[i] = $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo.fromObject(object.activity_maintenance[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchServerMaintenanceInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.function_maintenance = [];
                object.activity_maintenance = [];
            }
            if (message.function_maintenance && message.function_maintenance.length) {
                object.function_maintenance = Array(message.function_maintenance.length);
                for (let j = 0; j < message.function_maintenance.length; ++j)
                    object.function_maintenance[j] = $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo.toObject(message.function_maintenance[j], options);
            }
            if (message.activity_maintenance && message.activity_maintenance.length) {
                object.activity_maintenance = Array(message.activity_maintenance.length);
                for (let j = 0; j < message.activity_maintenance.length; ++j)
                    object.activity_maintenance[j] = $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo.toObject(message.activity_maintenance[j], options);
            }
            return object;
        };

        ResFetchServerMaintenanceInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchServerMaintenanceInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchServerMaintenanceInfo";
        };

        ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo = (function() {

            function ServerFunctionMaintenanceInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ServerFunctionMaintenanceInfo.prototype.name = "";
            ServerFunctionMaintenanceInfo.prototype.open = false;

            ServerFunctionMaintenanceInfo.create = function create(properties) {
                return new ServerFunctionMaintenanceInfo(properties);
            };

            ServerFunctionMaintenanceInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(10).string(message.name);
                if (message.open != null && Object.hasOwnProperty.call(message, "open"))
                    writer.uint32(16).bool(message.open);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ServerFunctionMaintenanceInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ServerFunctionMaintenanceInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.open = value;
                            else
                                delete message.open;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ServerFunctionMaintenanceInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ServerFunctionMaintenanceInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.name != null && message.hasOwnProperty("name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.open != null && message.hasOwnProperty("open"))
                    if (typeof message.open !== "boolean")
                        return "open: boolean expected";
                return null;
            };

            ServerFunctionMaintenanceInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo();
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = String(object.name);
                if (object.open != null)
                    if (object.open)
                        message.open = Boolean(object.open);
                return message;
            };

            ServerFunctionMaintenanceInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.name = "";
                    object.open = false;
                }
                if (message.name != null && message.hasOwnProperty("name"))
                    object.name = message.name;
                if (message.open != null && message.hasOwnProperty("open"))
                    object.open = message.open;
                return object;
            };

            ServerFunctionMaintenanceInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ServerFunctionMaintenanceInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchServerMaintenanceInfo.ServerFunctionMaintenanceInfo";
            };

            return ServerFunctionMaintenanceInfo;
        })();

        ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo = (function() {

            function ServerActivityMaintenanceInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ServerActivityMaintenanceInfo.prototype.activity_type = "";
            ServerActivityMaintenanceInfo.prototype.open = false;

            ServerActivityMaintenanceInfo.create = function create(properties) {
                return new ServerActivityMaintenanceInfo(properties);
            };

            ServerActivityMaintenanceInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_type != null && Object.hasOwnProperty.call(message, "activity_type"))
                    writer.uint32(10).string(message.activity_type);
                if (message.open != null && Object.hasOwnProperty.call(message, "open"))
                    writer.uint32(16).bool(message.open);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ServerActivityMaintenanceInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ServerActivityMaintenanceInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.activity_type = value;
                            else
                                delete message.activity_type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.open = value;
                            else
                                delete message.open;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ServerActivityMaintenanceInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ServerActivityMaintenanceInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_type != null && message.hasOwnProperty("activity_type"))
                    if (!$util.isString(message.activity_type))
                        return "activity_type: string expected";
                if (message.open != null && message.hasOwnProperty("open"))
                    if (typeof message.open !== "boolean")
                        return "open: boolean expected";
                return null;
            };

            ServerActivityMaintenanceInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo();
                if (object.activity_type != null)
                    if (typeof object.activity_type !== "string" || object.activity_type.length)
                        message.activity_type = String(object.activity_type);
                if (object.open != null)
                    if (object.open)
                        message.open = Boolean(object.open);
                return message;
            };

            ServerActivityMaintenanceInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.activity_type = "";
                    object.open = false;
                }
                if (message.activity_type != null && message.hasOwnProperty("activity_type"))
                    object.activity_type = message.activity_type;
                if (message.open != null && message.hasOwnProperty("open"))
                    object.open = message.open;
                return object;
            };

            ServerActivityMaintenanceInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ServerActivityMaintenanceInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchServerMaintenanceInfo.ServerActivityMaintenanceInfo";
            };

            return ServerActivityMaintenanceInfo;
        })();

        return ResFetchServerMaintenanceInfo;
    })();

    lq.ReqUpgradeVillageBuilding = (function() {

        function ReqUpgradeVillageBuilding(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpgradeVillageBuilding.prototype.building_id = 0;
        ReqUpgradeVillageBuilding.prototype.activity_id = 0;

        ReqUpgradeVillageBuilding.create = function create(properties) {
            return new ReqUpgradeVillageBuilding(properties);
        };

        ReqUpgradeVillageBuilding.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.building_id != null && Object.hasOwnProperty.call(message, "building_id"))
                writer.uint32(8).uint32(message.building_id);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpgradeVillageBuilding.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpgradeVillageBuilding.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpgradeVillageBuilding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.building_id = value;
                        else
                            delete message.building_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpgradeVillageBuilding.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpgradeVillageBuilding.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                if (!$util.isInteger(message.building_id))
                    return "building_id: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqUpgradeVillageBuilding.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpgradeVillageBuilding)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpgradeVillageBuilding();
            if (object.building_id != null)
                if (Number(object.building_id) !== 0)
                    message.building_id = object.building_id >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqUpgradeVillageBuilding.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.building_id = 0;
                object.activity_id = 0;
            }
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                object.building_id = message.building_id;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqUpgradeVillageBuilding.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpgradeVillageBuilding.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpgradeVillageBuilding";
        };

        return ReqUpgradeVillageBuilding;
    })();

    lq.ReqReceiveVillageBuildingReward = (function() {

        function ReqReceiveVillageBuildingReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveVillageBuildingReward.prototype.activity_id = 0;
        ReqReceiveVillageBuildingReward.prototype.building_id = 0;
        ReqReceiveVillageBuildingReward.prototype.rewards = $util.emptyArray;

        ReqReceiveVillageBuildingReward.create = function create(properties) {
            return new ReqReceiveVillageBuildingReward(properties);
        };

        ReqReceiveVillageBuildingReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.building_id != null && Object.hasOwnProperty.call(message, "building_id"))
                writer.uint32(16).uint32(message.building_id);
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveVillageBuildingReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveVillageBuildingReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveVillageBuildingReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.building_id = value;
                        else
                            delete message.building_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveVillageBuildingReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveVillageBuildingReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                if (!$util.isInteger(message.building_id))
                    return "building_id: integer expected";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ReqReceiveVillageBuildingReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveVillageBuildingReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveVillageBuildingReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.building_id != null)
                if (Number(object.building_id) !== 0)
                    message.building_id = object.building_id >>> 0;
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ReqReceiveVillageBuildingReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ReqReceiveVillageBuildingReward.rewards: object expected");
                    message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ReqReceiveVillageBuildingReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.building_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                object.building_id = message.building_id;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
            }
            return object;
        };

        ReqReceiveVillageBuildingReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveVillageBuildingReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveVillageBuildingReward";
        };

        return ReqReceiveVillageBuildingReward;
    })();

    lq.ResReceiveVillageBuildingReward = (function() {

        function ResReceiveVillageBuildingReward(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveVillageBuildingReward.prototype.error = null;
        ResReceiveVillageBuildingReward.prototype.reward_items = $util.emptyArray;

        ResReceiveVillageBuildingReward.create = function create(properties) {
            return new ResReceiveVillageBuildingReward(properties);
        };

        ResReceiveVillageBuildingReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveVillageBuildingReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveVillageBuildingReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveVillageBuildingReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveVillageBuildingReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveVillageBuildingReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResReceiveVillageBuildingReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveVillageBuildingReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveVillageBuildingReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveVillageBuildingReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResReceiveVillageBuildingReward.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResReceiveVillageBuildingReward.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveVillageBuildingReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResReceiveVillageBuildingReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveVillageBuildingReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveVillageBuildingReward";
        };

        return ResReceiveVillageBuildingReward;
    })();

    lq.ReqStartVillageTrip = (function() {

        function ReqStartVillageTrip(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStartVillageTrip.prototype.dest = 0;
        ReqStartVillageTrip.prototype.activity_id = 0;

        ReqStartVillageTrip.create = function create(properties) {
            return new ReqStartVillageTrip(properties);
        };

        ReqStartVillageTrip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dest != null && Object.hasOwnProperty.call(message, "dest"))
                writer.uint32(8).uint32(message.dest);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStartVillageTrip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStartVillageTrip.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStartVillageTrip(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dest = value;
                        else
                            delete message.dest;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStartVillageTrip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStartVillageTrip.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dest != null && message.hasOwnProperty("dest"))
                if (!$util.isInteger(message.dest))
                    return "dest: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqStartVillageTrip.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStartVillageTrip)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStartVillageTrip();
            if (object.dest != null)
                if (Number(object.dest) !== 0)
                    message.dest = object.dest >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqStartVillageTrip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dest = 0;
                object.activity_id = 0;
            }
            if (message.dest != null && message.hasOwnProperty("dest"))
                object.dest = message.dest;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqStartVillageTrip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStartVillageTrip.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStartVillageTrip";
        };

        return ReqStartVillageTrip;
    })();

    lq.ReqReceiveVillageTripReward = (function() {

        function ReqReceiveVillageTripReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqReceiveVillageTripReward.prototype.activity_id = 0;
        ReqReceiveVillageTripReward.prototype.dest_id = 0;
        ReqReceiveVillageTripReward.prototype.rewards = $util.emptyArray;

        ReqReceiveVillageTripReward.create = function create(properties) {
            return new ReqReceiveVillageTripReward(properties);
        };

        ReqReceiveVillageTripReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.dest_id != null && Object.hasOwnProperty.call(message, "dest_id"))
                writer.uint32(16).uint32(message.dest_id);
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqReceiveVillageTripReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqReceiveVillageTripReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqReceiveVillageTripReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dest_id = value;
                        else
                            delete message.dest_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqReceiveVillageTripReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqReceiveVillageTripReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.dest_id != null && message.hasOwnProperty("dest_id"))
                if (!$util.isInteger(message.dest_id))
                    return "dest_id: integer expected";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ReqReceiveVillageTripReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqReceiveVillageTripReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqReceiveVillageTripReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.dest_id != null)
                if (Number(object.dest_id) !== 0)
                    message.dest_id = object.dest_id >>> 0;
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ReqReceiveVillageTripReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ReqReceiveVillageTripReward.rewards: object expected");
                    message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ReqReceiveVillageTripReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.dest_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.dest_id != null && message.hasOwnProperty("dest_id"))
                object.dest_id = message.dest_id;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
            }
            return object;
        };

        ReqReceiveVillageTripReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqReceiveVillageTripReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqReceiveVillageTripReward";
        };

        return ReqReceiveVillageTripReward;
    })();

    lq.ResReceiveVillageTripReward = (function() {

        function ResReceiveVillageTripReward(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResReceiveVillageTripReward.prototype.error = null;
        ResReceiveVillageTripReward.prototype.reward_items = $util.emptyArray;

        ResReceiveVillageTripReward.create = function create(properties) {
            return new ResReceiveVillageTripReward(properties);
        };

        ResReceiveVillageTripReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResReceiveVillageTripReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResReceiveVillageTripReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResReceiveVillageTripReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResReceiveVillageTripReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResReceiveVillageTripReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResReceiveVillageTripReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResReceiveVillageTripReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResReceiveVillageTripReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResReceiveVillageTripReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResReceiveVillageTripReward.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResReceiveVillageTripReward.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResReceiveVillageTripReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResReceiveVillageTripReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResReceiveVillageTripReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResReceiveVillageTripReward";
        };

        return ResReceiveVillageTripReward;
    })();

    lq.ReqCompleteVillageTask = (function() {

        function ReqCompleteVillageTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCompleteVillageTask.prototype.task_id = 0;
        ReqCompleteVillageTask.prototype.activity_id = 0;

        ReqCompleteVillageTask.create = function create(properties) {
            return new ReqCompleteVillageTask(properties);
        };

        ReqCompleteVillageTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.task_id != null && Object.hasOwnProperty.call(message, "task_id"))
                writer.uint32(8).uint32(message.task_id);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCompleteVillageTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCompleteVillageTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCompleteVillageTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.task_id = value;
                        else
                            delete message.task_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCompleteVillageTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCompleteVillageTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                if (!$util.isInteger(message.task_id))
                    return "task_id: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqCompleteVillageTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCompleteVillageTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCompleteVillageTask();
            if (object.task_id != null)
                if (Number(object.task_id) !== 0)
                    message.task_id = object.task_id >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqCompleteVillageTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.task_id = 0;
                object.activity_id = 0;
            }
            if (message.task_id != null && message.hasOwnProperty("task_id"))
                object.task_id = message.task_id;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqCompleteVillageTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCompleteVillageTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCompleteVillageTask";
        };

        return ReqCompleteVillageTask;
    })();

    lq.ResCompleteVillageTask = (function() {

        function ResCompleteVillageTask(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCompleteVillageTask.prototype.error = null;
        ResCompleteVillageTask.prototype.reward_items = $util.emptyArray;

        ResCompleteVillageTask.create = function create(properties) {
            return new ResCompleteVillageTask(properties);
        };

        ResCompleteVillageTask.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCompleteVillageTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCompleteVillageTask.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCompleteVillageTask(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCompleteVillageTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCompleteVillageTask.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResCompleteVillageTask.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCompleteVillageTask)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCompleteVillageTask();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCompleteVillageTask.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResCompleteVillageTask.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResCompleteVillageTask.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResCompleteVillageTask.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResCompleteVillageTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCompleteVillageTask.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCompleteVillageTask";
        };

        return ResCompleteVillageTask;
    })();

    lq.ReqGetFriendVillageData = (function() {

        function ReqGetFriendVillageData(properties) {
            this.account_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGetFriendVillageData.prototype.account_list = $util.emptyArray;
        ReqGetFriendVillageData.prototype.activity_id = 0;

        ReqGetFriendVillageData.create = function create(properties) {
            return new ReqGetFriendVillageData(properties);
        };

        ReqGetFriendVillageData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_list != null && message.account_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.account_list.length; ++i)
                    writer.uint32(message.account_list[i]);
                writer.ldelim();
            }
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(16).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGetFriendVillageData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGetFriendVillageData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGetFriendVillageData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.account_list && message.account_list.length))
                                message.account_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_list && message.account_list.length))
                            message.account_list = [];
                        message.account_list.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGetFriendVillageData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGetFriendVillageData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                if (!Array.isArray(message.account_list))
                    return "account_list: array expected";
                for (let i = 0; i < message.account_list.length; ++i)
                    if (!$util.isInteger(message.account_list[i]))
                        return "account_list: integer[] expected";
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqGetFriendVillageData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGetFriendVillageData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGetFriendVillageData();
            if (object.account_list) {
                if (!Array.isArray(object.account_list))
                    throw TypeError(".lq.ReqGetFriendVillageData.account_list: array expected");
                message.account_list = Array(object.account_list.length);
                for (let i = 0; i < object.account_list.length; ++i)
                    message.account_list[i] = object.account_list[i] >>> 0;
            }
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqGetFriendVillageData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_list = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.account_list && message.account_list.length) {
                object.account_list = Array(message.account_list.length);
                for (let j = 0; j < message.account_list.length; ++j)
                    object.account_list[j] = message.account_list[j];
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqGetFriendVillageData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGetFriendVillageData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGetFriendVillageData";
        };

        return ReqGetFriendVillageData;
    })();

    lq.ResGetFriendVillageData = (function() {

        function ResGetFriendVillageData(properties) {
            this.list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGetFriendVillageData.prototype.error = null;
        ResGetFriendVillageData.prototype.list = $util.emptyArray;

        ResGetFriendVillageData.create = function create(properties) {
            return new ResGetFriendVillageData(properties);
        };

        ResGetFriendVillageData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.list != null && message.list.length)
                for (let i = 0; i < message.list.length; ++i)
                    $root.lq.ResGetFriendVillageData.FriendVillageData.encode(message.list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGetFriendVillageData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGetFriendVillageData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGetFriendVillageData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.lq.ResGetFriendVillageData.FriendVillageData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGetFriendVillageData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGetFriendVillageData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (let i = 0; i < message.list.length; ++i) {
                    let error = $root.lq.ResGetFriendVillageData.FriendVillageData.verify(message.list[i], _depth + 1);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        ResGetFriendVillageData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGetFriendVillageData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGetFriendVillageData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGetFriendVillageData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".lq.ResGetFriendVillageData.list: array expected");
                message.list = Array(object.list.length);
                for (let i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".lq.ResGetFriendVillageData.list: object expected");
                    message.list[i] = $root.lq.ResGetFriendVillageData.FriendVillageData.fromObject(object.list[i], _depth + 1);
                }
            }
            return message;
        };

        ResGetFriendVillageData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.list && message.list.length) {
                object.list = Array(message.list.length);
                for (let j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.lq.ResGetFriendVillageData.FriendVillageData.toObject(message.list[j], options);
            }
            return object;
        };

        ResGetFriendVillageData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGetFriendVillageData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGetFriendVillageData";
        };

        ResGetFriendVillageData.FriendVillageData = (function() {

            function FriendVillageData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            FriendVillageData.prototype.account_id = 0;
            FriendVillageData.prototype.level = 0;

            FriendVillageData.create = function create(properties) {
                return new FriendVillageData(properties);
            };

            FriendVillageData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(16).uint32(message.level);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            FriendVillageData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            FriendVillageData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGetFriendVillageData.FriendVillageData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            FriendVillageData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            FriendVillageData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                return null;
            };

            FriendVillageData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResGetFriendVillageData.FriendVillageData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResGetFriendVillageData.FriendVillageData();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                return message;
            };

            FriendVillageData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.level = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                return object;
            };

            FriendVillageData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FriendVillageData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResGetFriendVillageData.FriendVillageData";
            };

            return FriendVillageData;
        })();

        return ResGetFriendVillageData;
    })();

    lq.ReqSetVillageWorker = (function() {

        function ReqSetVillageWorker(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetVillageWorker.prototype.building_id = 0;
        ReqSetVillageWorker.prototype.worker_pos = 0;
        ReqSetVillageWorker.prototype.activity_id = 0;

        ReqSetVillageWorker.create = function create(properties) {
            return new ReqSetVillageWorker(properties);
        };

        ReqSetVillageWorker.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.building_id != null && Object.hasOwnProperty.call(message, "building_id"))
                writer.uint32(8).uint32(message.building_id);
            if (message.worker_pos != null && Object.hasOwnProperty.call(message, "worker_pos"))
                writer.uint32(16).uint32(message.worker_pos);
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(24).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetVillageWorker.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetVillageWorker.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetVillageWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.building_id = value;
                        else
                            delete message.building_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.worker_pos = value;
                        else
                            delete message.worker_pos;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetVillageWorker.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetVillageWorker.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                if (!$util.isInteger(message.building_id))
                    return "building_id: integer expected";
            if (message.worker_pos != null && message.hasOwnProperty("worker_pos"))
                if (!$util.isInteger(message.worker_pos))
                    return "worker_pos: integer expected";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSetVillageWorker.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetVillageWorker)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetVillageWorker();
            if (object.building_id != null)
                if (Number(object.building_id) !== 0)
                    message.building_id = object.building_id >>> 0;
            if (object.worker_pos != null)
                if (Number(object.worker_pos) !== 0)
                    message.worker_pos = object.worker_pos >>> 0;
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSetVillageWorker.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.building_id = 0;
                object.worker_pos = 0;
                object.activity_id = 0;
            }
            if (message.building_id != null && message.hasOwnProperty("building_id"))
                object.building_id = message.building_id;
            if (message.worker_pos != null && message.hasOwnProperty("worker_pos"))
                object.worker_pos = message.worker_pos;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSetVillageWorker.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetVillageWorker.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetVillageWorker";
        };

        return ReqSetVillageWorker;
    })();

    lq.ResSetVillageWorker = (function() {

        function ResSetVillageWorker(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSetVillageWorker.prototype.error = null;
        ResSetVillageWorker.prototype.building = null;
        ResSetVillageWorker.prototype.update_time = 0;

        ResSetVillageWorker.create = function create(properties) {
            return new ResSetVillageWorker(properties);
        };

        ResSetVillageWorker.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.building != null && Object.hasOwnProperty.call(message, "building"))
                $root.lq.VillageBuildingData.encode(message.building, writer.uint32(18).fork()).ldelim();
            if (message.update_time != null && Object.hasOwnProperty.call(message, "update_time"))
                writer.uint32(24).uint32(message.update_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSetVillageWorker.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSetVillageWorker.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSetVillageWorker(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.building = $root.lq.VillageBuildingData.decode(reader, reader.uint32(), undefined, _depth + 1, message.building);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.update_time = value;
                        else
                            delete message.update_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSetVillageWorker.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSetVillageWorker.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.building != null && message.hasOwnProperty("building")) {
                let error = $root.lq.VillageBuildingData.verify(message.building, _depth + 1);
                if (error)
                    return "building." + error;
            }
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                if (!$util.isInteger(message.update_time))
                    return "update_time: integer expected";
            return null;
        };

        ResSetVillageWorker.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSetVillageWorker)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSetVillageWorker();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSetVillageWorker.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.building != null) {
                if (typeof object.building !== "object")
                    throw TypeError(".lq.ResSetVillageWorker.building: object expected");
                message.building = $root.lq.VillageBuildingData.fromObject(object.building, _depth + 1);
            }
            if (object.update_time != null)
                if (Number(object.update_time) !== 0)
                    message.update_time = object.update_time >>> 0;
            return message;
        };

        ResSetVillageWorker.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.building = null;
                object.update_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.building != null && message.hasOwnProperty("building"))
                object.building = $root.lq.VillageBuildingData.toObject(message.building, options);
            if (message.update_time != null && message.hasOwnProperty("update_time"))
                object.update_time = message.update_time;
            return object;
        };

        ResSetVillageWorker.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSetVillageWorker.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSetVillageWorker";
        };

        return ResSetVillageWorker;
    })();

    lq.ReqNextRoundVillage = (function() {

        function ReqNextRoundVillage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqNextRoundVillage.prototype.activity_id = 0;

        ReqNextRoundVillage.create = function create(properties) {
            return new ReqNextRoundVillage(properties);
        };

        ReqNextRoundVillage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqNextRoundVillage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqNextRoundVillage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqNextRoundVillage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqNextRoundVillage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqNextRoundVillage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqNextRoundVillage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqNextRoundVillage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqNextRoundVillage();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqNextRoundVillage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqNextRoundVillage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqNextRoundVillage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqNextRoundVillage";
        };

        return ReqNextRoundVillage;
    })();

    lq.ResNextRoundVillage = (function() {

        function ResNextRoundVillage(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResNextRoundVillage.prototype.error = null;
        ResNextRoundVillage.prototype.activity_data = null;

        ResNextRoundVillage.create = function create(properties) {
            return new ResNextRoundVillage(properties);
        };

        ResNextRoundVillage.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.activity_data != null && Object.hasOwnProperty.call(message, "activity_data"))
                $root.lq.ActivityVillageData.encode(message.activity_data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResNextRoundVillage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResNextRoundVillage.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResNextRoundVillage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.activity_data = $root.lq.ActivityVillageData.decode(reader, reader.uint32(), undefined, _depth + 1, message.activity_data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResNextRoundVillage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResNextRoundVillage.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.activity_data != null && message.hasOwnProperty("activity_data")) {
                let error = $root.lq.ActivityVillageData.verify(message.activity_data, _depth + 1);
                if (error)
                    return "activity_data." + error;
            }
            return null;
        };

        ResNextRoundVillage.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResNextRoundVillage)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResNextRoundVillage();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResNextRoundVillage.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.activity_data != null) {
                if (typeof object.activity_data !== "object")
                    throw TypeError(".lq.ResNextRoundVillage.activity_data: object expected");
                message.activity_data = $root.lq.ActivityVillageData.fromObject(object.activity_data, _depth + 1);
            }
            return message;
        };

        ResNextRoundVillage.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.activity_data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.activity_data != null && message.hasOwnProperty("activity_data"))
                object.activity_data = $root.lq.ActivityVillageData.toObject(message.activity_data, options);
            return object;
        };

        ResNextRoundVillage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResNextRoundVillage.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResNextRoundVillage";
        };

        return ResNextRoundVillage;
    })();

    lq.ReqResolveFestivalActivityProposal = (function() {

        function ReqResolveFestivalActivityProposal(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqResolveFestivalActivityProposal.prototype.activity_id = 0;
        ReqResolveFestivalActivityProposal.prototype.id = 0;
        ReqResolveFestivalActivityProposal.prototype.select = 0;

        ReqResolveFestivalActivityProposal.create = function create(properties) {
            return new ReqResolveFestivalActivityProposal(properties);
        };

        ReqResolveFestivalActivityProposal.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.select != null && Object.hasOwnProperty.call(message, "select"))
                writer.uint32(24).uint32(message.select);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqResolveFestivalActivityProposal.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqResolveFestivalActivityProposal.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqResolveFestivalActivityProposal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.select = value;
                        else
                            delete message.select;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqResolveFestivalActivityProposal.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqResolveFestivalActivityProposal.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.select != null && message.hasOwnProperty("select"))
                if (!$util.isInteger(message.select))
                    return "select: integer expected";
            return null;
        };

        ReqResolveFestivalActivityProposal.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqResolveFestivalActivityProposal)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqResolveFestivalActivityProposal();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.select != null)
                if (Number(object.select) !== 0)
                    message.select = object.select >>> 0;
            return message;
        };

        ReqResolveFestivalActivityProposal.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
                object.select = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.select != null && message.hasOwnProperty("select"))
                object.select = message.select;
            return object;
        };

        ReqResolveFestivalActivityProposal.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqResolveFestivalActivityProposal.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqResolveFestivalActivityProposal";
        };

        return ReqResolveFestivalActivityProposal;
    })();

    lq.ResResolveFestivalActivityProposal = (function() {

        function ResResolveFestivalActivityProposal(properties) {
            this.effected_buff = [];
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResResolveFestivalActivityProposal.prototype.error = null;
        ResResolveFestivalActivityProposal.prototype.effected_buff = $util.emptyArray;
        ResResolveFestivalActivityProposal.prototype.result = 0;
        ResResolveFestivalActivityProposal.prototype.reward_items = $util.emptyArray;
        ResResolveFestivalActivityProposal.prototype.level = 0;

        ResResolveFestivalActivityProposal.create = function create(properties) {
            return new ResResolveFestivalActivityProposal(properties);
        };

        ResResolveFestivalActivityProposal.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.effected_buff != null && message.effected_buff.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.effected_buff.length; ++i)
                    writer.uint32(message.effected_buff[i]);
                writer.ldelim();
            }
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(24).uint32(message.result);
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteResult.encode(message.reward_items[i], writer.uint32(34).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(40).uint32(message.level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResResolveFestivalActivityProposal.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResResolveFestivalActivityProposal.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResResolveFestivalActivityProposal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.effected_buff && message.effected_buff.length))
                                message.effected_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.effected_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.effected_buff && message.effected_buff.length))
                            message.effected_buff = [];
                        message.effected_buff.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResResolveFestivalActivityProposal.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResResolveFestivalActivityProposal.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.effected_buff != null && message.hasOwnProperty("effected_buff")) {
                if (!Array.isArray(message.effected_buff))
                    return "effected_buff: array expected";
                for (let i = 0; i < message.effected_buff.length; ++i)
                    if (!$util.isInteger(message.effected_buff[i]))
                        return "effected_buff: integer[] expected";
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        ResResolveFestivalActivityProposal.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResResolveFestivalActivityProposal)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResResolveFestivalActivityProposal();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResResolveFestivalActivityProposal.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.effected_buff) {
                if (!Array.isArray(object.effected_buff))
                    throw TypeError(".lq.ResResolveFestivalActivityProposal.effected_buff: array expected");
                message.effected_buff = Array(object.effected_buff.length);
                for (let i = 0; i < object.effected_buff.length; ++i)
                    message.effected_buff[i] = object.effected_buff[i] >>> 0;
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResResolveFestivalActivityProposal.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResResolveFestivalActivityProposal.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteResult.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            return message;
        };

        ResResolveFestivalActivityProposal.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effected_buff = [];
                object.reward_items = [];
            }
            if (options.defaults) {
                object.error = null;
                object.result = 0;
                object.level = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.effected_buff && message.effected_buff.length) {
                object.effected_buff = Array(message.effected_buff.length);
                for (let j = 0; j < message.effected_buff.length; ++j)
                    object.effected_buff[j] = message.effected_buff[j];
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteResult.toObject(message.reward_items[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        ResResolveFestivalActivityProposal.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResResolveFestivalActivityProposal.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResResolveFestivalActivityProposal";
        };

        return ResResolveFestivalActivityProposal;
    })();

    lq.ReqResolveFestivalActivityEvent = (function() {

        function ReqResolveFestivalActivityEvent(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqResolveFestivalActivityEvent.prototype.activity_id = 0;
        ReqResolveFestivalActivityEvent.prototype.id = 0;
        ReqResolveFestivalActivityEvent.prototype.select = 0;

        ReqResolveFestivalActivityEvent.create = function create(properties) {
            return new ReqResolveFestivalActivityEvent(properties);
        };

        ReqResolveFestivalActivityEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.select != null && Object.hasOwnProperty.call(message, "select"))
                writer.uint32(24).uint32(message.select);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqResolveFestivalActivityEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqResolveFestivalActivityEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqResolveFestivalActivityEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.select = value;
                        else
                            delete message.select;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqResolveFestivalActivityEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqResolveFestivalActivityEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.select != null && message.hasOwnProperty("select"))
                if (!$util.isInteger(message.select))
                    return "select: integer expected";
            return null;
        };

        ReqResolveFestivalActivityEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqResolveFestivalActivityEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqResolveFestivalActivityEvent();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.select != null)
                if (Number(object.select) !== 0)
                    message.select = object.select >>> 0;
            return message;
        };

        ReqResolveFestivalActivityEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
                object.select = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.select != null && message.hasOwnProperty("select"))
                object.select = message.select;
            return object;
        };

        ReqResolveFestivalActivityEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqResolveFestivalActivityEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqResolveFestivalActivityEvent";
        };

        return ReqResolveFestivalActivityEvent;
    })();

    lq.ResResolveFestivalActivityEvent = (function() {

        function ResResolveFestivalActivityEvent(properties) {
            this.effected_buff = [];
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResResolveFestivalActivityEvent.prototype.error = null;
        ResResolveFestivalActivityEvent.prototype.effected_buff = $util.emptyArray;
        ResResolveFestivalActivityEvent.prototype.reward_items = $util.emptyArray;
        ResResolveFestivalActivityEvent.prototype.ending_id = 0;
        ResResolveFestivalActivityEvent.prototype.level = 0;

        ResResolveFestivalActivityEvent.create = function create(properties) {
            return new ResResolveFestivalActivityEvent(properties);
        };

        ResResolveFestivalActivityEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.effected_buff != null && message.effected_buff.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.effected_buff.length; ++i)
                    writer.uint32(message.effected_buff[i]);
                writer.ldelim();
            }
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteResult.encode(message.reward_items[i], writer.uint32(34).fork()).ldelim();
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(40).uint32(message.ending_id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(48).uint32(message.level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResResolveFestivalActivityEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResResolveFestivalActivityEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResResolveFestivalActivityEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.effected_buff && message.effected_buff.length))
                                message.effected_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.effected_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.effected_buff && message.effected_buff.length))
                            message.effected_buff = [];
                        message.effected_buff.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResResolveFestivalActivityEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResResolveFestivalActivityEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.effected_buff != null && message.hasOwnProperty("effected_buff")) {
                if (!Array.isArray(message.effected_buff))
                    return "effected_buff: array expected";
                for (let i = 0; i < message.effected_buff.length; ++i)
                    if (!$util.isInteger(message.effected_buff[i]))
                        return "effected_buff: integer[] expected";
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        ResResolveFestivalActivityEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResResolveFestivalActivityEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResResolveFestivalActivityEvent();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResResolveFestivalActivityEvent.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.effected_buff) {
                if (!Array.isArray(object.effected_buff))
                    throw TypeError(".lq.ResResolveFestivalActivityEvent.effected_buff: array expected");
                message.effected_buff = Array(object.effected_buff.length);
                for (let i = 0; i < object.effected_buff.length; ++i)
                    message.effected_buff[i] = object.effected_buff[i] >>> 0;
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResResolveFestivalActivityEvent.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResResolveFestivalActivityEvent.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteResult.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            return message;
        };

        ResResolveFestivalActivityEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effected_buff = [];
                object.reward_items = [];
            }
            if (options.defaults) {
                object.error = null;
                object.ending_id = 0;
                object.level = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.effected_buff && message.effected_buff.length) {
                object.effected_buff = Array(message.effected_buff.length);
                for (let j = 0; j < message.effected_buff.length; ++j)
                    object.effected_buff[j] = message.effected_buff[j];
            }
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteResult.toObject(message.reward_items[j], options);
            }
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        ResResolveFestivalActivityEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResResolveFestivalActivityEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResResolveFestivalActivityEvent";
        };

        return ResResolveFestivalActivityEvent;
    })();

    lq.ReqBuyFestivalProposal = (function() {

        function ReqBuyFestivalProposal(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBuyFestivalProposal.prototype.activity_id = 0;

        ReqBuyFestivalProposal.create = function create(properties) {
            return new ReqBuyFestivalProposal(properties);
        };

        ReqBuyFestivalProposal.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBuyFestivalProposal.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBuyFestivalProposal.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBuyFestivalProposal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBuyFestivalProposal.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBuyFestivalProposal.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqBuyFestivalProposal.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBuyFestivalProposal)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBuyFestivalProposal();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqBuyFestivalProposal.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqBuyFestivalProposal.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBuyFestivalProposal.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBuyFestivalProposal";
        };

        return ReqBuyFestivalProposal;
    })();

    lq.ResBuyFestivalProposal = (function() {

        function ResBuyFestivalProposal(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResBuyFestivalProposal.prototype.error = null;
        ResBuyFestivalProposal.prototype.new_proposal = null;

        ResBuyFestivalProposal.create = function create(properties) {
            return new ResBuyFestivalProposal(properties);
        };

        ResBuyFestivalProposal.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.new_proposal != null && Object.hasOwnProperty.call(message, "new_proposal"))
                $root.lq.FestivalProposalData.encode(message.new_proposal, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResBuyFestivalProposal.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResBuyFestivalProposal.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResBuyFestivalProposal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.new_proposal = $root.lq.FestivalProposalData.decode(reader, reader.uint32(), undefined, _depth + 1, message.new_proposal);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResBuyFestivalProposal.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResBuyFestivalProposal.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.new_proposal != null && message.hasOwnProperty("new_proposal")) {
                let error = $root.lq.FestivalProposalData.verify(message.new_proposal, _depth + 1);
                if (error)
                    return "new_proposal." + error;
            }
            return null;
        };

        ResBuyFestivalProposal.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResBuyFestivalProposal)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResBuyFestivalProposal();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResBuyFestivalProposal.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.new_proposal != null) {
                if (typeof object.new_proposal !== "object")
                    throw TypeError(".lq.ResBuyFestivalProposal.new_proposal: object expected");
                message.new_proposal = $root.lq.FestivalProposalData.fromObject(object.new_proposal, _depth + 1);
            }
            return message;
        };

        ResBuyFestivalProposal.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.new_proposal = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.new_proposal != null && message.hasOwnProperty("new_proposal"))
                object.new_proposal = $root.lq.FestivalProposalData.toObject(message.new_proposal, options);
            return object;
        };

        ResBuyFestivalProposal.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResBuyFestivalProposal.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResBuyFestivalProposal";
        };

        return ResBuyFestivalProposal;
    })();

    lq.ReqIslandActivityMove = (function() {

        function ReqIslandActivityMove(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqIslandActivityMove.prototype.activity_id = 0;
        ReqIslandActivityMove.prototype.zone_id = 0;

        ReqIslandActivityMove.create = function create(properties) {
            return new ReqIslandActivityMove(properties);
        };

        ReqIslandActivityMove.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.zone_id != null && Object.hasOwnProperty.call(message, "zone_id"))
                writer.uint32(16).uint32(message.zone_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqIslandActivityMove.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqIslandActivityMove.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityMove(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.zone_id = value;
                        else
                            delete message.zone_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqIslandActivityMove.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqIslandActivityMove.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.zone_id != null && message.hasOwnProperty("zone_id"))
                if (!$util.isInteger(message.zone_id))
                    return "zone_id: integer expected";
            return null;
        };

        ReqIslandActivityMove.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqIslandActivityMove)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqIslandActivityMove();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.zone_id != null)
                if (Number(object.zone_id) !== 0)
                    message.zone_id = object.zone_id >>> 0;
            return message;
        };

        ReqIslandActivityMove.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.zone_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.zone_id != null && message.hasOwnProperty("zone_id"))
                object.zone_id = message.zone_id;
            return object;
        };

        ReqIslandActivityMove.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqIslandActivityMove.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqIslandActivityMove";
        };

        return ReqIslandActivityMove;
    })();

    lq.ReqIslandActivityBuy = (function() {

        function ReqIslandActivityBuy(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqIslandActivityBuy.prototype.activity_id = 0;
        ReqIslandActivityBuy.prototype.items = $util.emptyArray;

        ReqIslandActivityBuy.create = function create(properties) {
            return new ReqIslandActivityBuy(properties);
        };

        ReqIslandActivityBuy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ReqIslandActivityBuy.BuyItems.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqIslandActivityBuy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqIslandActivityBuy.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityBuy(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ReqIslandActivityBuy.BuyItems.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqIslandActivityBuy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqIslandActivityBuy.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ReqIslandActivityBuy.BuyItems.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ReqIslandActivityBuy.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqIslandActivityBuy)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqIslandActivityBuy();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ReqIslandActivityBuy.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ReqIslandActivityBuy.items: object expected");
                    message.items[i] = $root.lq.ReqIslandActivityBuy.BuyItems.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ReqIslandActivityBuy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ReqIslandActivityBuy.BuyItems.toObject(message.items[j], options);
            }
            return object;
        };

        ReqIslandActivityBuy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqIslandActivityBuy.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqIslandActivityBuy";
        };

        ReqIslandActivityBuy.BuyItems = (function() {

            function BuyItems(properties) {
                this.pos = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BuyItems.prototype.goods_id = 0;
            BuyItems.prototype.pos = $util.emptyArray;
            BuyItems.prototype.rotate = 0;
            BuyItems.prototype.bag_id = 0;
            BuyItems.prototype.price = 0;

            BuyItems.create = function create(properties) {
                return new BuyItems(properties);
            };

            BuyItems.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                    writer.uint32(16).uint32(message.goods_id);
                if (message.pos != null && message.pos.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.pos.length; ++i)
                        writer.uint32(message.pos[i]);
                    writer.ldelim();
                }
                if (message.rotate != null && Object.hasOwnProperty.call(message, "rotate"))
                    writer.uint32(32).uint32(message.rotate);
                if (message.bag_id != null && Object.hasOwnProperty.call(message, "bag_id"))
                    writer.uint32(40).uint32(message.bag_id);
                if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                    writer.uint32(48).uint32(message.price);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BuyItems.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BuyItems.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityBuy.BuyItems(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.goods_id = value;
                            else
                                delete message.goods_id;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.pos && message.pos.length))
                                    message.pos = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.pos.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.pos && message.pos.length))
                                message.pos = [];
                            message.pos.push(reader.uint32());
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rotate = value;
                            else
                                delete message.rotate;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.bag_id = value;
                            else
                                delete message.bag_id;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.price = value;
                            else
                                delete message.price;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BuyItems.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BuyItems.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                    if (!$util.isInteger(message.goods_id))
                        return "goods_id: integer expected";
                if (message.pos != null && message.hasOwnProperty("pos")) {
                    if (!Array.isArray(message.pos))
                        return "pos: array expected";
                    for (let i = 0; i < message.pos.length; ++i)
                        if (!$util.isInteger(message.pos[i]))
                            return "pos: integer[] expected";
                }
                if (message.rotate != null && message.hasOwnProperty("rotate"))
                    if (!$util.isInteger(message.rotate))
                        return "rotate: integer expected";
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    if (!$util.isInteger(message.bag_id))
                        return "bag_id: integer expected";
                if (message.price != null && message.hasOwnProperty("price"))
                    if (!$util.isInteger(message.price))
                        return "price: integer expected";
                return null;
            };

            BuyItems.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqIslandActivityBuy.BuyItems)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqIslandActivityBuy.BuyItems();
                if (object.goods_id != null)
                    if (Number(object.goods_id) !== 0)
                        message.goods_id = object.goods_id >>> 0;
                if (object.pos) {
                    if (!Array.isArray(object.pos))
                        throw TypeError(".lq.ReqIslandActivityBuy.BuyItems.pos: array expected");
                    message.pos = Array(object.pos.length);
                    for (let i = 0; i < object.pos.length; ++i)
                        message.pos[i] = object.pos[i] >>> 0;
                }
                if (object.rotate != null)
                    if (Number(object.rotate) !== 0)
                        message.rotate = object.rotate >>> 0;
                if (object.bag_id != null)
                    if (Number(object.bag_id) !== 0)
                        message.bag_id = object.bag_id >>> 0;
                if (object.price != null)
                    if (Number(object.price) !== 0)
                        message.price = object.price >>> 0;
                return message;
            };

            BuyItems.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.pos = [];
                if (options.defaults) {
                    object.goods_id = 0;
                    object.rotate = 0;
                    object.bag_id = 0;
                    object.price = 0;
                }
                if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                    object.goods_id = message.goods_id;
                if (message.pos && message.pos.length) {
                    object.pos = Array(message.pos.length);
                    for (let j = 0; j < message.pos.length; ++j)
                        object.pos[j] = message.pos[j];
                }
                if (message.rotate != null && message.hasOwnProperty("rotate"))
                    object.rotate = message.rotate;
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    object.bag_id = message.bag_id;
                if (message.price != null && message.hasOwnProperty("price"))
                    object.price = message.price;
                return object;
            };

            BuyItems.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BuyItems.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqIslandActivityBuy.BuyItems";
            };

            return BuyItems;
        })();

        return ReqIslandActivityBuy;
    })();

    lq.ReqIslandActivitySell = (function() {

        function ReqIslandActivitySell(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqIslandActivitySell.prototype.activity_id = 0;
        ReqIslandActivitySell.prototype.items = $util.emptyArray;

        ReqIslandActivitySell.create = function create(properties) {
            return new ReqIslandActivitySell(properties);
        };

        ReqIslandActivitySell.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ReqIslandActivitySell.SellItem.encode(message.items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqIslandActivitySell.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqIslandActivitySell.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivitySell(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ReqIslandActivitySell.SellItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqIslandActivitySell.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqIslandActivitySell.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ReqIslandActivitySell.SellItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            return null;
        };

        ReqIslandActivitySell.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqIslandActivitySell)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqIslandActivitySell();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ReqIslandActivitySell.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ReqIslandActivitySell.items: object expected");
                    message.items[i] = $root.lq.ReqIslandActivitySell.SellItem.fromObject(object.items[i], _depth + 1);
                }
            }
            return message;
        };

        ReqIslandActivitySell.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ReqIslandActivitySell.SellItem.toObject(message.items[j], options);
            }
            return object;
        };

        ReqIslandActivitySell.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqIslandActivitySell.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqIslandActivitySell";
        };

        ReqIslandActivitySell.SellItem = (function() {

            function SellItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SellItem.prototype.bag_id = 0;
            SellItem.prototype.id = 0;
            SellItem.prototype.price = 0;

            SellItem.create = function create(properties) {
                return new SellItem(properties);
            };

            SellItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.bag_id != null && Object.hasOwnProperty.call(message, "bag_id"))
                    writer.uint32(16).uint32(message.bag_id);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(24).uint32(message.id);
                if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                    writer.uint32(32).uint32(message.price);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SellItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SellItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivitySell.SellItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.bag_id = value;
                            else
                                delete message.bag_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.price = value;
                            else
                                delete message.price;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SellItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SellItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    if (!$util.isInteger(message.bag_id))
                        return "bag_id: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.price != null && message.hasOwnProperty("price"))
                    if (!$util.isInteger(message.price))
                        return "price: integer expected";
                return null;
            };

            SellItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqIslandActivitySell.SellItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqIslandActivitySell.SellItem();
                if (object.bag_id != null)
                    if (Number(object.bag_id) !== 0)
                        message.bag_id = object.bag_id >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.price != null)
                    if (Number(object.price) !== 0)
                        message.price = object.price >>> 0;
                return message;
            };

            SellItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.bag_id = 0;
                    object.id = 0;
                    object.price = 0;
                }
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    object.bag_id = message.bag_id;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.price != null && message.hasOwnProperty("price"))
                    object.price = message.price;
                return object;
            };

            SellItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SellItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqIslandActivitySell.SellItem";
            };

            return SellItem;
        })();

        return ReqIslandActivitySell;
    })();

    lq.ReqIslandActivityTidyBag = (function() {

        function ReqIslandActivityTidyBag(properties) {
            this.bag_data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqIslandActivityTidyBag.prototype.activity_id = 0;
        ReqIslandActivityTidyBag.prototype.bag_data = $util.emptyArray;

        ReqIslandActivityTidyBag.create = function create(properties) {
            return new ReqIslandActivityTidyBag(properties);
        };

        ReqIslandActivityTidyBag.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.bag_data != null && message.bag_data.length)
                for (let i = 0; i < message.bag_data.length; ++i)
                    $root.lq.ReqIslandActivityTidyBag.BagData.encode(message.bag_data[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqIslandActivityTidyBag.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqIslandActivityTidyBag.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityTidyBag(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.bag_data && message.bag_data.length))
                            message.bag_data = [];
                        message.bag_data.push($root.lq.ReqIslandActivityTidyBag.BagData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqIslandActivityTidyBag.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqIslandActivityTidyBag.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.bag_data != null && message.hasOwnProperty("bag_data")) {
                if (!Array.isArray(message.bag_data))
                    return "bag_data: array expected";
                for (let i = 0; i < message.bag_data.length; ++i) {
                    let error = $root.lq.ReqIslandActivityTidyBag.BagData.verify(message.bag_data[i], _depth + 1);
                    if (error)
                        return "bag_data." + error;
                }
            }
            return null;
        };

        ReqIslandActivityTidyBag.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqIslandActivityTidyBag)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqIslandActivityTidyBag();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.bag_data) {
                if (!Array.isArray(object.bag_data))
                    throw TypeError(".lq.ReqIslandActivityTidyBag.bag_data: array expected");
                message.bag_data = Array(object.bag_data.length);
                for (let i = 0; i < object.bag_data.length; ++i) {
                    if (typeof object.bag_data[i] !== "object")
                        throw TypeError(".lq.ReqIslandActivityTidyBag.bag_data: object expected");
                    message.bag_data[i] = $root.lq.ReqIslandActivityTidyBag.BagData.fromObject(object.bag_data[i], _depth + 1);
                }
            }
            return message;
        };

        ReqIslandActivityTidyBag.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.bag_data = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.bag_data && message.bag_data.length) {
                object.bag_data = Array(message.bag_data.length);
                for (let j = 0; j < message.bag_data.length; ++j)
                    object.bag_data[j] = $root.lq.ReqIslandActivityTidyBag.BagData.toObject(message.bag_data[j], options);
            }
            return object;
        };

        ReqIslandActivityTidyBag.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqIslandActivityTidyBag.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqIslandActivityTidyBag";
        };

        ReqIslandActivityTidyBag.BagData = (function() {

            function BagData(properties) {
                this.items = [];
                this.drops = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BagData.prototype.bag_id = 0;
            BagData.prototype.items = $util.emptyArray;
            BagData.prototype.drops = $util.emptyArray;

            BagData.create = function create(properties) {
                return new BagData(properties);
            };

            BagData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.bag_id != null && Object.hasOwnProperty.call(message, "bag_id"))
                    writer.uint32(16).uint32(message.bag_id);
                if (message.items != null && message.items.length)
                    for (let i = 0; i < message.items.length; ++i)
                        $root.lq.ReqIslandActivityTidyBag.BagData.ITemData.encode(message.items[i], writer.uint32(26).fork()).ldelim();
                if (message.drops != null && message.drops.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.drops.length; ++i)
                        writer.uint32(message.drops[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BagData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BagData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityTidyBag.BagData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.bag_id = value;
                            else
                                delete message.bag_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push($root.lq.ReqIslandActivityTidyBag.BagData.ITemData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.drops && message.drops.length))
                                    message.drops = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.drops.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.drops && message.drops.length))
                                message.drops = [];
                            message.drops.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BagData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BagData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    if (!$util.isInteger(message.bag_id))
                        return "bag_id: integer expected";
                if (message.items != null && message.hasOwnProperty("items")) {
                    if (!Array.isArray(message.items))
                        return "items: array expected";
                    for (let i = 0; i < message.items.length; ++i) {
                        let error = $root.lq.ReqIslandActivityTidyBag.BagData.ITemData.verify(message.items[i], _depth + 1);
                        if (error)
                            return "items." + error;
                    }
                }
                if (message.drops != null && message.hasOwnProperty("drops")) {
                    if (!Array.isArray(message.drops))
                        return "drops: array expected";
                    for (let i = 0; i < message.drops.length; ++i)
                        if (!$util.isInteger(message.drops[i]))
                            return "drops: integer[] expected";
                }
                return null;
            };

            BagData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqIslandActivityTidyBag.BagData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqIslandActivityTidyBag.BagData();
                if (object.bag_id != null)
                    if (Number(object.bag_id) !== 0)
                        message.bag_id = object.bag_id >>> 0;
                if (object.items) {
                    if (!Array.isArray(object.items))
                        throw TypeError(".lq.ReqIslandActivityTidyBag.BagData.items: array expected");
                    message.items = Array(object.items.length);
                    for (let i = 0; i < object.items.length; ++i) {
                        if (typeof object.items[i] !== "object")
                            throw TypeError(".lq.ReqIslandActivityTidyBag.BagData.items: object expected");
                        message.items[i] = $root.lq.ReqIslandActivityTidyBag.BagData.ITemData.fromObject(object.items[i], _depth + 1);
                    }
                }
                if (object.drops) {
                    if (!Array.isArray(object.drops))
                        throw TypeError(".lq.ReqIslandActivityTidyBag.BagData.drops: array expected");
                    message.drops = Array(object.drops.length);
                    for (let i = 0; i < object.drops.length; ++i)
                        message.drops[i] = object.drops[i] >>> 0;
                }
                return message;
            };

            BagData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.items = [];
                    object.drops = [];
                }
                if (options.defaults)
                    object.bag_id = 0;
                if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                    object.bag_id = message.bag_id;
                if (message.items && message.items.length) {
                    object.items = Array(message.items.length);
                    for (let j = 0; j < message.items.length; ++j)
                        object.items[j] = $root.lq.ReqIslandActivityTidyBag.BagData.ITemData.toObject(message.items[j], options);
                }
                if (message.drops && message.drops.length) {
                    object.drops = Array(message.drops.length);
                    for (let j = 0; j < message.drops.length; ++j)
                        object.drops[j] = message.drops[j];
                }
                return object;
            };

            BagData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BagData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqIslandActivityTidyBag.BagData";
            };

            BagData.ITemData = (function() {

                function ITemData(properties) {
                    this.pos = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                ITemData.prototype.id = 0;
                ITemData.prototype.pos = $util.emptyArray;
                ITemData.prototype.rotate = 0;

                ITemData.create = function create(properties) {
                    return new ITemData(properties);
                };

                ITemData.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(8).uint32(message.id);
                    if (message.pos != null && message.pos.length) {
                        writer.uint32(18).fork();
                        for (let i = 0; i < message.pos.length; ++i)
                            writer.uint32(message.pos[i]);
                        writer.ldelim();
                    }
                    if (message.rotate != null && Object.hasOwnProperty.call(message, "rotate"))
                        writer.uint32(24).uint32(message.rotate);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                ITemData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                ITemData.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityTidyBag.BagData.ITemData(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.id = value;
                                else
                                    delete message.id;
                                continue;
                            }
                        case 2: {
                                if (wireType === 2) {
                                    if (!(message.pos && message.pos.length))
                                        message.pos = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.pos.push(reader.uint32());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.pos && message.pos.length))
                                    message.pos = [];
                                message.pos.push(reader.uint32());
                                continue;
                            }
                        case 3: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.rotate = value;
                                else
                                    delete message.rotate;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                ITemData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                ITemData.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.id != null && message.hasOwnProperty("id"))
                        if (!$util.isInteger(message.id))
                            return "id: integer expected";
                    if (message.pos != null && message.hasOwnProperty("pos")) {
                        if (!Array.isArray(message.pos))
                            return "pos: array expected";
                        for (let i = 0; i < message.pos.length; ++i)
                            if (!$util.isInteger(message.pos[i]))
                                return "pos: integer[] expected";
                    }
                    if (message.rotate != null && message.hasOwnProperty("rotate"))
                        if (!$util.isInteger(message.rotate))
                            return "rotate: integer expected";
                    return null;
                };

                ITemData.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ReqIslandActivityTidyBag.BagData.ITemData)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ReqIslandActivityTidyBag.BagData.ITemData();
                    if (object.id != null)
                        if (Number(object.id) !== 0)
                            message.id = object.id >>> 0;
                    if (object.pos) {
                        if (!Array.isArray(object.pos))
                            throw TypeError(".lq.ReqIslandActivityTidyBag.BagData.ITemData.pos: array expected");
                        message.pos = Array(object.pos.length);
                        for (let i = 0; i < object.pos.length; ++i)
                            message.pos[i] = object.pos[i] >>> 0;
                    }
                    if (object.rotate != null)
                        if (Number(object.rotate) !== 0)
                            message.rotate = object.rotate >>> 0;
                    return message;
                };

                ITemData.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.pos = [];
                    if (options.defaults) {
                        object.id = 0;
                        object.rotate = 0;
                    }
                    if (message.id != null && message.hasOwnProperty("id"))
                        object.id = message.id;
                    if (message.pos && message.pos.length) {
                        object.pos = Array(message.pos.length);
                        for (let j = 0; j < message.pos.length; ++j)
                            object.pos[j] = message.pos[j];
                    }
                    if (message.rotate != null && message.hasOwnProperty("rotate"))
                        object.rotate = message.rotate;
                    return object;
                };

                ITemData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                ITemData.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ReqIslandActivityTidyBag.BagData.ITemData";
                };

                return ITemData;
            })();

            return BagData;
        })();

        return ReqIslandActivityTidyBag;
    })();

    lq.ReqIslandActivityUnlockBagGrid = (function() {

        function ReqIslandActivityUnlockBagGrid(properties) {
            this.pos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqIslandActivityUnlockBagGrid.prototype.activity_id = 0;
        ReqIslandActivityUnlockBagGrid.prototype.bag_id = 0;
        ReqIslandActivityUnlockBagGrid.prototype.pos = $util.emptyArray;

        ReqIslandActivityUnlockBagGrid.create = function create(properties) {
            return new ReqIslandActivityUnlockBagGrid(properties);
        };

        ReqIslandActivityUnlockBagGrid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.bag_id != null && Object.hasOwnProperty.call(message, "bag_id"))
                writer.uint32(16).uint32(message.bag_id);
            if (message.pos != null && message.pos.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.pos.length; ++i)
                    writer.uint32(message.pos[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqIslandActivityUnlockBagGrid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqIslandActivityUnlockBagGrid.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqIslandActivityUnlockBagGrid(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bag_id = value;
                        else
                            delete message.bag_id;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.pos && message.pos.length))
                                message.pos = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.pos.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.pos && message.pos.length))
                            message.pos = [];
                        message.pos.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqIslandActivityUnlockBagGrid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqIslandActivityUnlockBagGrid.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                if (!$util.isInteger(message.bag_id))
                    return "bag_id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos")) {
                if (!Array.isArray(message.pos))
                    return "pos: array expected";
                for (let i = 0; i < message.pos.length; ++i)
                    if (!$util.isInteger(message.pos[i]))
                        return "pos: integer[] expected";
            }
            return null;
        };

        ReqIslandActivityUnlockBagGrid.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqIslandActivityUnlockBagGrid)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqIslandActivityUnlockBagGrid();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.bag_id != null)
                if (Number(object.bag_id) !== 0)
                    message.bag_id = object.bag_id >>> 0;
            if (object.pos) {
                if (!Array.isArray(object.pos))
                    throw TypeError(".lq.ReqIslandActivityUnlockBagGrid.pos: array expected");
                message.pos = Array(object.pos.length);
                for (let i = 0; i < object.pos.length; ++i)
                    message.pos[i] = object.pos[i] >>> 0;
            }
            return message;
        };

        ReqIslandActivityUnlockBagGrid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.pos = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.bag_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.bag_id != null && message.hasOwnProperty("bag_id"))
                object.bag_id = message.bag_id;
            if (message.pos && message.pos.length) {
                object.pos = Array(message.pos.length);
                for (let j = 0; j < message.pos.length; ++j)
                    object.pos[j] = message.pos[j];
            }
            return object;
        };

        ReqIslandActivityUnlockBagGrid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqIslandActivityUnlockBagGrid.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqIslandActivityUnlockBagGrid";
        };

        return ReqIslandActivityUnlockBagGrid;
    })();

    lq.ContestSetting = (function() {

        function ContestSetting(properties) {
            this.level_limit = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ContestSetting.prototype.level_limit = $util.emptyArray;
        ContestSetting.prototype.game_limit = 0;
        ContestSetting.prototype.system_broadcast = 0;

        ContestSetting.create = function create(properties) {
            return new ContestSetting(properties);
        };

        ContestSetting.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level_limit != null && message.level_limit.length)
                for (let i = 0; i < message.level_limit.length; ++i)
                    $root.lq.ContestSetting.LevelLimit.encode(message.level_limit[i], writer.uint32(10).fork()).ldelim();
            if (message.game_limit != null && Object.hasOwnProperty.call(message, "game_limit"))
                writer.uint32(16).uint32(message.game_limit);
            if (message.system_broadcast != null && Object.hasOwnProperty.call(message, "system_broadcast"))
                writer.uint32(24).uint32(message.system_broadcast);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ContestSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ContestSetting.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestSetting(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.level_limit && message.level_limit.length))
                            message.level_limit = [];
                        message.level_limit.push($root.lq.ContestSetting.LevelLimit.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_limit = value;
                        else
                            delete message.game_limit;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.system_broadcast = value;
                        else
                            delete message.system_broadcast;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ContestSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ContestSetting.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level_limit != null && message.hasOwnProperty("level_limit")) {
                if (!Array.isArray(message.level_limit))
                    return "level_limit: array expected";
                for (let i = 0; i < message.level_limit.length; ++i) {
                    let error = $root.lq.ContestSetting.LevelLimit.verify(message.level_limit[i], _depth + 1);
                    if (error)
                        return "level_limit." + error;
                }
            }
            if (message.game_limit != null && message.hasOwnProperty("game_limit"))
                if (!$util.isInteger(message.game_limit))
                    return "game_limit: integer expected";
            if (message.system_broadcast != null && message.hasOwnProperty("system_broadcast"))
                if (!$util.isInteger(message.system_broadcast))
                    return "system_broadcast: integer expected";
            return null;
        };

        ContestSetting.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ContestSetting)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ContestSetting();
            if (object.level_limit) {
                if (!Array.isArray(object.level_limit))
                    throw TypeError(".lq.ContestSetting.level_limit: array expected");
                message.level_limit = Array(object.level_limit.length);
                for (let i = 0; i < object.level_limit.length; ++i) {
                    if (typeof object.level_limit[i] !== "object")
                        throw TypeError(".lq.ContestSetting.level_limit: object expected");
                    message.level_limit[i] = $root.lq.ContestSetting.LevelLimit.fromObject(object.level_limit[i], _depth + 1);
                }
            }
            if (object.game_limit != null)
                if (Number(object.game_limit) !== 0)
                    message.game_limit = object.game_limit >>> 0;
            if (object.system_broadcast != null)
                if (Number(object.system_broadcast) !== 0)
                    message.system_broadcast = object.system_broadcast >>> 0;
            return message;
        };

        ContestSetting.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.level_limit = [];
            if (options.defaults) {
                object.game_limit = 0;
                object.system_broadcast = 0;
            }
            if (message.level_limit && message.level_limit.length) {
                object.level_limit = Array(message.level_limit.length);
                for (let j = 0; j < message.level_limit.length; ++j)
                    object.level_limit[j] = $root.lq.ContestSetting.LevelLimit.toObject(message.level_limit[j], options);
            }
            if (message.game_limit != null && message.hasOwnProperty("game_limit"))
                object.game_limit = message.game_limit;
            if (message.system_broadcast != null && message.hasOwnProperty("system_broadcast"))
                object.system_broadcast = message.system_broadcast;
            return object;
        };

        ContestSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContestSetting.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ContestSetting";
        };

        ContestSetting.LevelLimit = (function() {

            function LevelLimit(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LevelLimit.prototype.type = 0;
            LevelLimit.prototype.value = 0;
            LevelLimit.prototype.operate = 0;

            LevelLimit.create = function create(properties) {
                return new LevelLimit(properties);
            };

            LevelLimit.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(16).uint32(message.value);
                if (message.operate != null && Object.hasOwnProperty.call(message, "operate"))
                    writer.uint32(24).uint32(message.operate);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LevelLimit.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LevelLimit.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ContestSetting.LevelLimit(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.operate = value;
                            else
                                delete message.operate;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LevelLimit.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LevelLimit.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                if (message.operate != null && message.hasOwnProperty("operate"))
                    if (!$util.isInteger(message.operate))
                        return "operate: integer expected";
                return null;
            };

            LevelLimit.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ContestSetting.LevelLimit)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ContestSetting.LevelLimit();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.value != null)
                    if (Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                if (object.operate != null)
                    if (Number(object.operate) !== 0)
                        message.operate = object.operate >>> 0;
                return message;
            };

            LevelLimit.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.value = 0;
                    object.operate = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                if (message.operate != null && message.hasOwnProperty("operate"))
                    object.operate = message.operate;
                return object;
            };

            LevelLimit.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LevelLimit.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ContestSetting.LevelLimit";
            };

            return LevelLimit;
        })();

        return ContestSetting;
    })();

    lq.ReqCreateCustomizedContest = (function() {

        function ReqCreateCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateCustomizedContest.prototype.name = "";
        ReqCreateCustomizedContest.prototype.open_show = 0;
        ReqCreateCustomizedContest.prototype.game_rule_setting = null;
        ReqCreateCustomizedContest.prototype.start_time = 0;
        ReqCreateCustomizedContest.prototype.end_time = 0;
        ReqCreateCustomizedContest.prototype.auto_match = 0;
        ReqCreateCustomizedContest.prototype.rank_rule = 0;
        ReqCreateCustomizedContest.prototype.contest_setting = null;
        ReqCreateCustomizedContest.prototype.rank_type = 0;

        ReqCreateCustomizedContest.create = function create(properties) {
            return new ReqCreateCustomizedContest(properties);
        };

        ReqCreateCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(10).string(message.name);
            if (message.open_show != null && Object.hasOwnProperty.call(message, "open_show"))
                writer.uint32(16).uint32(message.open_show);
            if (message.game_rule_setting != null && Object.hasOwnProperty.call(message, "game_rule_setting"))
                $root.lq.GameMode.encode(message.game_rule_setting, writer.uint32(26).fork()).ldelim();
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(32).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(40).uint32(message.end_time);
            if (message.auto_match != null && Object.hasOwnProperty.call(message, "auto_match"))
                writer.uint32(48).uint32(message.auto_match);
            if (message.rank_rule != null && Object.hasOwnProperty.call(message, "rank_rule"))
                writer.uint32(56).uint32(message.rank_rule);
            if (message.contest_setting != null && Object.hasOwnProperty.call(message, "contest_setting"))
                $root.lq.ContestSetting.encode(message.contest_setting, writer.uint32(66).fork()).ldelim();
            if (message.rank_type != null && Object.hasOwnProperty.call(message, "rank_type"))
                writer.uint32(72).uint32(message.rank_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_show = value;
                        else
                            delete message.open_show;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game_rule_setting = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_rule_setting);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.auto_match = value;
                        else
                            delete message.auto_match;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_rule = value;
                        else
                            delete message.rank_rule;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.contest_setting = $root.lq.ContestSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.contest_setting);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_type = value;
                        else
                            delete message.rank_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                if (!$util.isInteger(message.open_show))
                    return "open_show: integer expected";
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting")) {
                let error = $root.lq.GameMode.verify(message.game_rule_setting, _depth + 1);
                if (error)
                    return "game_rule_setting." + error;
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                if (!$util.isInteger(message.auto_match))
                    return "auto_match: integer expected";
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                if (!$util.isInteger(message.rank_rule))
                    return "rank_rule: integer expected";
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting")) {
                let error = $root.lq.ContestSetting.verify(message.contest_setting, _depth + 1);
                if (error)
                    return "contest_setting." + error;
            }
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                if (!$util.isInteger(message.rank_type))
                    return "rank_type: integer expected";
            return null;
        };

        ReqCreateCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateCustomizedContest();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.open_show != null)
                if (Number(object.open_show) !== 0)
                    message.open_show = object.open_show >>> 0;
            if (object.game_rule_setting != null) {
                if (typeof object.game_rule_setting !== "object")
                    throw TypeError(".lq.ReqCreateCustomizedContest.game_rule_setting: object expected");
                message.game_rule_setting = $root.lq.GameMode.fromObject(object.game_rule_setting, _depth + 1);
            }
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.auto_match != null)
                if (Number(object.auto_match) !== 0)
                    message.auto_match = object.auto_match >>> 0;
            if (object.rank_rule != null)
                if (Number(object.rank_rule) !== 0)
                    message.rank_rule = object.rank_rule >>> 0;
            if (object.contest_setting != null) {
                if (typeof object.contest_setting !== "object")
                    throw TypeError(".lq.ReqCreateCustomizedContest.contest_setting: object expected");
                message.contest_setting = $root.lq.ContestSetting.fromObject(object.contest_setting, _depth + 1);
            }
            if (object.rank_type != null)
                if (Number(object.rank_type) !== 0)
                    message.rank_type = object.rank_type >>> 0;
            return message;
        };

        ReqCreateCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.open_show = 0;
                object.game_rule_setting = null;
                object.start_time = 0;
                object.end_time = 0;
                object.auto_match = 0;
                object.rank_rule = 0;
                object.contest_setting = null;
                object.rank_type = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                object.open_show = message.open_show;
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting"))
                object.game_rule_setting = $root.lq.GameMode.toObject(message.game_rule_setting, options);
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                object.auto_match = message.auto_match;
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                object.rank_rule = message.rank_rule;
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting"))
                object.contest_setting = $root.lq.ContestSetting.toObject(message.contest_setting, options);
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                object.rank_type = message.rank_type;
            return object;
        };

        ReqCreateCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateCustomizedContest";
        };

        return ReqCreateCustomizedContest;
    })();

    lq.ResCreateCustomizedContest = (function() {

        function ResCreateCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateCustomizedContest.prototype.error = null;
        ResCreateCustomizedContest.prototype.unique_id = 0;

        ResCreateCustomizedContest.create = function create(properties) {
            return new ResCreateCustomizedContest(properties);
        };

        ResCreateCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(16).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ResCreateCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateCustomizedContest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateCustomizedContest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ResCreateCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.unique_id = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ResCreateCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateCustomizedContest";
        };

        return ResCreateCustomizedContest;
    })();

    lq.ReqFetchmanagerCustomizedContestList = (function() {

        function ReqFetchmanagerCustomizedContestList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchmanagerCustomizedContestList.prototype.lang = "";

        ReqFetchmanagerCustomizedContestList.create = function create(properties) {
            return new ReqFetchmanagerCustomizedContestList(properties);
        };

        ReqFetchmanagerCustomizedContestList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchmanagerCustomizedContestList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchmanagerCustomizedContestList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchmanagerCustomizedContestList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchmanagerCustomizedContestList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchmanagerCustomizedContestList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqFetchmanagerCustomizedContestList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchmanagerCustomizedContestList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchmanagerCustomizedContestList();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqFetchmanagerCustomizedContestList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.lang = "";
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqFetchmanagerCustomizedContestList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchmanagerCustomizedContestList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchmanagerCustomizedContestList";
        };

        return ReqFetchmanagerCustomizedContestList;
    })();

    lq.ResFetchManagerCustomizedContestList = (function() {

        function ResFetchManagerCustomizedContestList(properties) {
            this.contests = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchManagerCustomizedContestList.prototype.error = null;
        ResFetchManagerCustomizedContestList.prototype.contests = $util.emptyArray;

        ResFetchManagerCustomizedContestList.create = function create(properties) {
            return new ResFetchManagerCustomizedContestList(properties);
        };

        ResFetchManagerCustomizedContestList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.contests != null && message.contests.length)
                for (let i = 0; i < message.contests.length; ++i)
                    $root.lq.CustomizedContestBase.encode(message.contests[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchManagerCustomizedContestList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchManagerCustomizedContestList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchManagerCustomizedContestList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.contests && message.contests.length))
                            message.contests = [];
                        message.contests.push($root.lq.CustomizedContestBase.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchManagerCustomizedContestList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchManagerCustomizedContestList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.contests != null && message.hasOwnProperty("contests")) {
                if (!Array.isArray(message.contests))
                    return "contests: array expected";
                for (let i = 0; i < message.contests.length; ++i) {
                    let error = $root.lq.CustomizedContestBase.verify(message.contests[i], _depth + 1);
                    if (error)
                        return "contests." + error;
                }
            }
            return null;
        };

        ResFetchManagerCustomizedContestList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchManagerCustomizedContestList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchManagerCustomizedContestList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchManagerCustomizedContestList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.contests) {
                if (!Array.isArray(object.contests))
                    throw TypeError(".lq.ResFetchManagerCustomizedContestList.contests: array expected");
                message.contests = Array(object.contests.length);
                for (let i = 0; i < object.contests.length; ++i) {
                    if (typeof object.contests[i] !== "object")
                        throw TypeError(".lq.ResFetchManagerCustomizedContestList.contests: object expected");
                    message.contests[i] = $root.lq.CustomizedContestBase.fromObject(object.contests[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchManagerCustomizedContestList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.contests = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.contests && message.contests.length) {
                object.contests = Array(message.contests.length);
                for (let j = 0; j < message.contests.length; ++j)
                    object.contests[j] = $root.lq.CustomizedContestBase.toObject(message.contests[j], options);
            }
            return object;
        };

        ResFetchManagerCustomizedContestList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchManagerCustomizedContestList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchManagerCustomizedContestList";
        };

        return ResFetchManagerCustomizedContestList;
    })();

    lq.ReqFetchManagerCustomizedContest = (function() {

        function ReqFetchManagerCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchManagerCustomizedContest.prototype.unique_id = 0;

        ReqFetchManagerCustomizedContest.create = function create(properties) {
            return new ReqFetchManagerCustomizedContest(properties);
        };

        ReqFetchManagerCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchManagerCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchManagerCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchManagerCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchManagerCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchManagerCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqFetchManagerCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchManagerCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchManagerCustomizedContest();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqFetchManagerCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqFetchManagerCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchManagerCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchManagerCustomizedContest";
        };

        return ReqFetchManagerCustomizedContest;
    })();

    lq.ResFetchManagerCustomizedContest = (function() {

        function ResFetchManagerCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchManagerCustomizedContest.prototype.error = null;
        ResFetchManagerCustomizedContest.prototype.name = "";
        ResFetchManagerCustomizedContest.prototype.open_show = 0;
        ResFetchManagerCustomizedContest.prototype.game_rule_setting = null;
        ResFetchManagerCustomizedContest.prototype.start_time = 0;
        ResFetchManagerCustomizedContest.prototype.end_time = 0;
        ResFetchManagerCustomizedContest.prototype.auto_match = 0;
        ResFetchManagerCustomizedContest.prototype.rank_rule = 0;
        ResFetchManagerCustomizedContest.prototype.check_state = 0;
        ResFetchManagerCustomizedContest.prototype.checking_name = "";
        ResFetchManagerCustomizedContest.prototype.contest_setting = null;
        ResFetchManagerCustomizedContest.prototype.rank_type = 0;
        ResFetchManagerCustomizedContest.prototype.season = null;
        ResFetchManagerCustomizedContest.prototype.match_start_time = 0;
        ResFetchManagerCustomizedContest.prototype.match_end_time = 0;

        ResFetchManagerCustomizedContest.create = function create(properties) {
            return new ResFetchManagerCustomizedContest(properties);
        };

        ResFetchManagerCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(18).string(message.name);
            if (message.open_show != null && Object.hasOwnProperty.call(message, "open_show"))
                writer.uint32(24).uint32(message.open_show);
            if (message.game_rule_setting != null && Object.hasOwnProperty.call(message, "game_rule_setting"))
                $root.lq.GameMode.encode(message.game_rule_setting, writer.uint32(34).fork()).ldelim();
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(40).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(48).uint32(message.end_time);
            if (message.auto_match != null && Object.hasOwnProperty.call(message, "auto_match"))
                writer.uint32(56).uint32(message.auto_match);
            if (message.rank_rule != null && Object.hasOwnProperty.call(message, "rank_rule"))
                writer.uint32(64).uint32(message.rank_rule);
            if (message.check_state != null && Object.hasOwnProperty.call(message, "check_state"))
                writer.uint32(72).uint32(message.check_state);
            if (message.checking_name != null && Object.hasOwnProperty.call(message, "checking_name"))
                writer.uint32(82).string(message.checking_name);
            if (message.contest_setting != null && Object.hasOwnProperty.call(message, "contest_setting"))
                $root.lq.ContestSetting.encode(message.contest_setting, writer.uint32(90).fork()).ldelim();
            if (message.rank_type != null && Object.hasOwnProperty.call(message, "rank_type"))
                writer.uint32(96).uint32(message.rank_type);
            if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                $root.lq.ResFetchManagerCustomizedContest.SeasonInfo.encode(message.season, writer.uint32(106).fork()).ldelim();
            if (message.match_start_time != null && Object.hasOwnProperty.call(message, "match_start_time"))
                writer.uint32(112).uint32(message.match_start_time);
            if (message.match_end_time != null && Object.hasOwnProperty.call(message, "match_end_time"))
                writer.uint32(120).uint32(message.match_end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchManagerCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchManagerCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchManagerCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_show = value;
                        else
                            delete message.open_show;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_rule_setting = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_rule_setting);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.auto_match = value;
                        else
                            delete message.auto_match;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_rule = value;
                        else
                            delete message.rank_rule;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.check_state = value;
                        else
                            delete message.check_state;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.checking_name = value;
                        else
                            delete message.checking_name;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.contest_setting = $root.lq.ContestSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.contest_setting);
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_type = value;
                        else
                            delete message.rank_type;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.season = $root.lq.ResFetchManagerCustomizedContest.SeasonInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.season);
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_start_time = value;
                        else
                            delete message.match_start_time;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.match_end_time = value;
                        else
                            delete message.match_end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchManagerCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchManagerCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                if (!$util.isInteger(message.open_show))
                    return "open_show: integer expected";
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting")) {
                let error = $root.lq.GameMode.verify(message.game_rule_setting, _depth + 1);
                if (error)
                    return "game_rule_setting." + error;
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                if (!$util.isInteger(message.auto_match))
                    return "auto_match: integer expected";
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                if (!$util.isInteger(message.rank_rule))
                    return "rank_rule: integer expected";
            if (message.check_state != null && message.hasOwnProperty("check_state"))
                if (!$util.isInteger(message.check_state))
                    return "check_state: integer expected";
            if (message.checking_name != null && message.hasOwnProperty("checking_name"))
                if (!$util.isString(message.checking_name))
                    return "checking_name: string expected";
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting")) {
                let error = $root.lq.ContestSetting.verify(message.contest_setting, _depth + 1);
                if (error)
                    return "contest_setting." + error;
            }
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                if (!$util.isInteger(message.rank_type))
                    return "rank_type: integer expected";
            if (message.season != null && message.hasOwnProperty("season")) {
                let error = $root.lq.ResFetchManagerCustomizedContest.SeasonInfo.verify(message.season, _depth + 1);
                if (error)
                    return "season." + error;
            }
            if (message.match_start_time != null && message.hasOwnProperty("match_start_time"))
                if (!$util.isInteger(message.match_start_time))
                    return "match_start_time: integer expected";
            if (message.match_end_time != null && message.hasOwnProperty("match_end_time"))
                if (!$util.isInteger(message.match_end_time))
                    return "match_end_time: integer expected";
            return null;
        };

        ResFetchManagerCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchManagerCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchManagerCustomizedContest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchManagerCustomizedContest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.open_show != null)
                if (Number(object.open_show) !== 0)
                    message.open_show = object.open_show >>> 0;
            if (object.game_rule_setting != null) {
                if (typeof object.game_rule_setting !== "object")
                    throw TypeError(".lq.ResFetchManagerCustomizedContest.game_rule_setting: object expected");
                message.game_rule_setting = $root.lq.GameMode.fromObject(object.game_rule_setting, _depth + 1);
            }
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.auto_match != null)
                if (Number(object.auto_match) !== 0)
                    message.auto_match = object.auto_match >>> 0;
            if (object.rank_rule != null)
                if (Number(object.rank_rule) !== 0)
                    message.rank_rule = object.rank_rule >>> 0;
            if (object.check_state != null)
                if (Number(object.check_state) !== 0)
                    message.check_state = object.check_state >>> 0;
            if (object.checking_name != null)
                if (typeof object.checking_name !== "string" || object.checking_name.length)
                    message.checking_name = String(object.checking_name);
            if (object.contest_setting != null) {
                if (typeof object.contest_setting !== "object")
                    throw TypeError(".lq.ResFetchManagerCustomizedContest.contest_setting: object expected");
                message.contest_setting = $root.lq.ContestSetting.fromObject(object.contest_setting, _depth + 1);
            }
            if (object.rank_type != null)
                if (Number(object.rank_type) !== 0)
                    message.rank_type = object.rank_type >>> 0;
            if (object.season != null) {
                if (typeof object.season !== "object")
                    throw TypeError(".lq.ResFetchManagerCustomizedContest.season: object expected");
                message.season = $root.lq.ResFetchManagerCustomizedContest.SeasonInfo.fromObject(object.season, _depth + 1);
            }
            if (object.match_start_time != null)
                if (Number(object.match_start_time) !== 0)
                    message.match_start_time = object.match_start_time >>> 0;
            if (object.match_end_time != null)
                if (Number(object.match_end_time) !== 0)
                    message.match_end_time = object.match_end_time >>> 0;
            return message;
        };

        ResFetchManagerCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.name = "";
                object.open_show = 0;
                object.game_rule_setting = null;
                object.start_time = 0;
                object.end_time = 0;
                object.auto_match = 0;
                object.rank_rule = 0;
                object.check_state = 0;
                object.checking_name = "";
                object.contest_setting = null;
                object.rank_type = 0;
                object.season = null;
                object.match_start_time = 0;
                object.match_end_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                object.open_show = message.open_show;
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting"))
                object.game_rule_setting = $root.lq.GameMode.toObject(message.game_rule_setting, options);
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                object.auto_match = message.auto_match;
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                object.rank_rule = message.rank_rule;
            if (message.check_state != null && message.hasOwnProperty("check_state"))
                object.check_state = message.check_state;
            if (message.checking_name != null && message.hasOwnProperty("checking_name"))
                object.checking_name = message.checking_name;
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting"))
                object.contest_setting = $root.lq.ContestSetting.toObject(message.contest_setting, options);
            if (message.rank_type != null && message.hasOwnProperty("rank_type"))
                object.rank_type = message.rank_type;
            if (message.season != null && message.hasOwnProperty("season"))
                object.season = $root.lq.ResFetchManagerCustomizedContest.SeasonInfo.toObject(message.season, options);
            if (message.match_start_time != null && message.hasOwnProperty("match_start_time"))
                object.match_start_time = message.match_start_time;
            if (message.match_end_time != null && message.hasOwnProperty("match_end_time"))
                object.match_end_time = message.match_end_time;
            return object;
        };

        ResFetchManagerCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchManagerCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchManagerCustomizedContest";
        };

        ResFetchManagerCustomizedContest.SeasonInfo = (function() {

            function SeasonInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SeasonInfo.prototype.create_time = 0;
            SeasonInfo.prototype.start_time = 0;
            SeasonInfo.prototype.end_time = 0;

            SeasonInfo.create = function create(properties) {
                return new SeasonInfo(properties);
            };

            SeasonInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.create_time != null && Object.hasOwnProperty.call(message, "create_time"))
                    writer.uint32(8).uint32(message.create_time);
                if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                    writer.uint32(16).uint32(message.start_time);
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(24).uint32(message.end_time);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SeasonInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SeasonInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchManagerCustomizedContest.SeasonInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.create_time = value;
                            else
                                delete message.create_time;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.start_time = value;
                            else
                                delete message.start_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SeasonInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SeasonInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.create_time != null && message.hasOwnProperty("create_time"))
                    if (!$util.isInteger(message.create_time))
                        return "create_time: integer expected";
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    if (!$util.isInteger(message.start_time))
                        return "start_time: integer expected";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                return null;
            };

            SeasonInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchManagerCustomizedContest.SeasonInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchManagerCustomizedContest.SeasonInfo();
                if (object.create_time != null)
                    if (Number(object.create_time) !== 0)
                        message.create_time = object.create_time >>> 0;
                if (object.start_time != null)
                    if (Number(object.start_time) !== 0)
                        message.start_time = object.start_time >>> 0;
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                return message;
            };

            SeasonInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.create_time = 0;
                    object.start_time = 0;
                    object.end_time = 0;
                }
                if (message.create_time != null && message.hasOwnProperty("create_time"))
                    object.create_time = message.create_time;
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    object.start_time = message.start_time;
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                return object;
            };

            SeasonInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SeasonInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchManagerCustomizedContest.SeasonInfo";
            };

            return SeasonInfo;
        })();

        return ResFetchManagerCustomizedContest;
    })();

    lq.ReqUpdateManagerCustomizedContest = (function() {

        function ReqUpdateManagerCustomizedContest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqUpdateManagerCustomizedContest.prototype.name = "";
        ReqUpdateManagerCustomizedContest.prototype.open_show = 0;
        ReqUpdateManagerCustomizedContest.prototype.game_rule_setting = null;
        ReqUpdateManagerCustomizedContest.prototype.start_time = 0;
        ReqUpdateManagerCustomizedContest.prototype.end_time = 0;
        ReqUpdateManagerCustomizedContest.prototype.unique_id = 0;
        ReqUpdateManagerCustomizedContest.prototype.auto_match = 0;
        ReqUpdateManagerCustomizedContest.prototype.rank_rule = 0;
        ReqUpdateManagerCustomizedContest.prototype.contest_setting = null;

        ReqUpdateManagerCustomizedContest.create = function create(properties) {
            return new ReqUpdateManagerCustomizedContest(properties);
        };

        ReqUpdateManagerCustomizedContest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(10).string(message.name);
            if (message.open_show != null && Object.hasOwnProperty.call(message, "open_show"))
                writer.uint32(16).uint32(message.open_show);
            if (message.game_rule_setting != null && Object.hasOwnProperty.call(message, "game_rule_setting"))
                $root.lq.GameMode.encode(message.game_rule_setting, writer.uint32(26).fork()).ldelim();
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(32).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(40).uint32(message.end_time);
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(48).uint32(message.unique_id);
            if (message.auto_match != null && Object.hasOwnProperty.call(message, "auto_match"))
                writer.uint32(56).uint32(message.auto_match);
            if (message.rank_rule != null && Object.hasOwnProperty.call(message, "rank_rule"))
                writer.uint32(64).uint32(message.rank_rule);
            if (message.contest_setting != null && Object.hasOwnProperty.call(message, "contest_setting"))
                $root.lq.ContestSetting.encode(message.contest_setting, writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqUpdateManagerCustomizedContest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqUpdateManagerCustomizedContest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqUpdateManagerCustomizedContest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_show = value;
                        else
                            delete message.open_show;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game_rule_setting = $root.lq.GameMode.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_rule_setting);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.auto_match = value;
                        else
                            delete message.auto_match;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rank_rule = value;
                        else
                            delete message.rank_rule;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.contest_setting = $root.lq.ContestSetting.decode(reader, reader.uint32(), undefined, _depth + 1, message.contest_setting);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqUpdateManagerCustomizedContest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqUpdateManagerCustomizedContest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                if (!$util.isInteger(message.open_show))
                    return "open_show: integer expected";
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting")) {
                let error = $root.lq.GameMode.verify(message.game_rule_setting, _depth + 1);
                if (error)
                    return "game_rule_setting." + error;
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                if (!$util.isInteger(message.auto_match))
                    return "auto_match: integer expected";
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                if (!$util.isInteger(message.rank_rule))
                    return "rank_rule: integer expected";
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting")) {
                let error = $root.lq.ContestSetting.verify(message.contest_setting, _depth + 1);
                if (error)
                    return "contest_setting." + error;
            }
            return null;
        };

        ReqUpdateManagerCustomizedContest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqUpdateManagerCustomizedContest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqUpdateManagerCustomizedContest();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.open_show != null)
                if (Number(object.open_show) !== 0)
                    message.open_show = object.open_show >>> 0;
            if (object.game_rule_setting != null) {
                if (typeof object.game_rule_setting !== "object")
                    throw TypeError(".lq.ReqUpdateManagerCustomizedContest.game_rule_setting: object expected");
                message.game_rule_setting = $root.lq.GameMode.fromObject(object.game_rule_setting, _depth + 1);
            }
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.auto_match != null)
                if (Number(object.auto_match) !== 0)
                    message.auto_match = object.auto_match >>> 0;
            if (object.rank_rule != null)
                if (Number(object.rank_rule) !== 0)
                    message.rank_rule = object.rank_rule >>> 0;
            if (object.contest_setting != null) {
                if (typeof object.contest_setting !== "object")
                    throw TypeError(".lq.ReqUpdateManagerCustomizedContest.contest_setting: object expected");
                message.contest_setting = $root.lq.ContestSetting.fromObject(object.contest_setting, _depth + 1);
            }
            return message;
        };

        ReqUpdateManagerCustomizedContest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.open_show = 0;
                object.game_rule_setting = null;
                object.start_time = 0;
                object.end_time = 0;
                object.unique_id = 0;
                object.auto_match = 0;
                object.rank_rule = 0;
                object.contest_setting = null;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.open_show != null && message.hasOwnProperty("open_show"))
                object.open_show = message.open_show;
            if (message.game_rule_setting != null && message.hasOwnProperty("game_rule_setting"))
                object.game_rule_setting = $root.lq.GameMode.toObject(message.game_rule_setting, options);
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.auto_match != null && message.hasOwnProperty("auto_match"))
                object.auto_match = message.auto_match;
            if (message.rank_rule != null && message.hasOwnProperty("rank_rule"))
                object.rank_rule = message.rank_rule;
            if (message.contest_setting != null && message.hasOwnProperty("contest_setting"))
                object.contest_setting = $root.lq.ContestSetting.toObject(message.contest_setting, options);
            return object;
        };

        ReqUpdateManagerCustomizedContest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqUpdateManagerCustomizedContest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqUpdateManagerCustomizedContest";
        };

        return ReqUpdateManagerCustomizedContest;
    })();

    lq.ReqFetchContestPlayerRank = (function() {

        function ReqFetchContestPlayerRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchContestPlayerRank.prototype.unique_id = 0;
        ReqFetchContestPlayerRank.prototype.limit = 0;
        ReqFetchContestPlayerRank.prototype.offset = 0;

        ReqFetchContestPlayerRank.create = function create(properties) {
            return new ReqFetchContestPlayerRank(properties);
        };

        ReqFetchContestPlayerRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(16).uint32(message.limit);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(24).uint32(message.offset);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchContestPlayerRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchContestPlayerRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchContestPlayerRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.limit = value;
                        else
                            delete message.limit;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.offset = value;
                        else
                            delete message.offset;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchContestPlayerRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchContestPlayerRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            return null;
        };

        ReqFetchContestPlayerRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchContestPlayerRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchContestPlayerRank();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.limit != null)
                if (Number(object.limit) !== 0)
                    message.limit = object.limit >>> 0;
            if (object.offset != null)
                if (Number(object.offset) !== 0)
                    message.offset = object.offset >>> 0;
            return message;
        };

        ReqFetchContestPlayerRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.limit = 0;
                object.offset = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            return object;
        };

        ReqFetchContestPlayerRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchContestPlayerRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchContestPlayerRank";
        };

        return ReqFetchContestPlayerRank;
    })();

    lq.ResFetchContestPlayerRank = (function() {

        function ResFetchContestPlayerRank(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchContestPlayerRank.prototype.error = null;
        ResFetchContestPlayerRank.prototype.total = 0;
        ResFetchContestPlayerRank.prototype.rank = $util.emptyArray;
        ResFetchContestPlayerRank.prototype.player_data = null;

        ResFetchContestPlayerRank.create = function create(properties) {
            return new ResFetchContestPlayerRank(properties);
        };

        ResFetchContestPlayerRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.total != null && Object.hasOwnProperty.call(message, "total"))
                writer.uint32(16).uint32(message.total);
            if (message.rank != null && message.rank.length)
                for (let i = 0; i < message.rank.length; ++i)
                    $root.lq.ResFetchContestPlayerRank.SeasonRank.encode(message.rank[i], writer.uint32(26).fork()).ldelim();
            if (message.player_data != null && Object.hasOwnProperty.call(message, "player_data"))
                $root.lq.ResFetchContestPlayerRank.PlayerData.encode(message.player_data, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchContestPlayerRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchContestPlayerRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total = value;
                        else
                            delete message.total;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push($root.lq.ResFetchContestPlayerRank.SeasonRank.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.player_data = $root.lq.ResFetchContestPlayerRank.PlayerData.decode(reader, reader.uint32(), undefined, _depth + 1, message.player_data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchContestPlayerRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchContestPlayerRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.total != null && message.hasOwnProperty("total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i) {
                    let error = $root.lq.ResFetchContestPlayerRank.SeasonRank.verify(message.rank[i], _depth + 1);
                    if (error)
                        return "rank." + error;
                }
            }
            if (message.player_data != null && message.hasOwnProperty("player_data")) {
                let error = $root.lq.ResFetchContestPlayerRank.PlayerData.verify(message.player_data, _depth + 1);
                if (error)
                    return "player_data." + error;
            }
            return null;
        };

        ResFetchContestPlayerRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchContestPlayerRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchContestPlayerRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchContestPlayerRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.total != null)
                if (Number(object.total) !== 0)
                    message.total = object.total >>> 0;
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.ResFetchContestPlayerRank.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i) {
                    if (typeof object.rank[i] !== "object")
                        throw TypeError(".lq.ResFetchContestPlayerRank.rank: object expected");
                    message.rank[i] = $root.lq.ResFetchContestPlayerRank.SeasonRank.fromObject(object.rank[i], _depth + 1);
                }
            }
            if (object.player_data != null) {
                if (typeof object.player_data !== "object")
                    throw TypeError(".lq.ResFetchContestPlayerRank.player_data: object expected");
                message.player_data = $root.lq.ResFetchContestPlayerRank.PlayerData.fromObject(object.player_data, _depth + 1);
            }
            return message;
        };

        ResFetchContestPlayerRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults) {
                object.error = null;
                object.total = 0;
                object.player_data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.total != null && message.hasOwnProperty("total"))
                object.total = message.total;
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = $root.lq.ResFetchContestPlayerRank.SeasonRank.toObject(message.rank[j], options);
            }
            if (message.player_data != null && message.hasOwnProperty("player_data"))
                object.player_data = $root.lq.ResFetchContestPlayerRank.PlayerData.toObject(message.player_data, options);
            return object;
        };

        ResFetchContestPlayerRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchContestPlayerRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchContestPlayerRank";
        };

        ResFetchContestPlayerRank.ContestPlayerAccountData = (function() {

            function ContestPlayerAccountData(properties) {
                this.recent_games = [];
                this.highest_series_points = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ContestPlayerAccountData.prototype.total_game_count = 0;
            ContestPlayerAccountData.prototype.recent_games = $util.emptyArray;
            ContestPlayerAccountData.prototype.highest_series_points = $util.emptyArray;
            ContestPlayerAccountData.prototype.accumulate_point = 0;

            ContestPlayerAccountData.create = function create(properties) {
                return new ContestPlayerAccountData(properties);
            };

            ContestPlayerAccountData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_game_count != null && Object.hasOwnProperty.call(message, "total_game_count"))
                    writer.uint32(8).uint32(message.total_game_count);
                if (message.recent_games != null && message.recent_games.length)
                    for (let i = 0; i < message.recent_games.length; ++i)
                        $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.encode(message.recent_games[i], writer.uint32(18).fork()).ldelim();
                if (message.highest_series_points != null && message.highest_series_points.length)
                    for (let i = 0; i < message.highest_series_points.length; ++i)
                        $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.encode(message.highest_series_points[i], writer.uint32(26).fork()).ldelim();
                if (message.accumulate_point != null && Object.hasOwnProperty.call(message, "accumulate_point"))
                    writer.uint32(32).int32(message.accumulate_point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ContestPlayerAccountData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ContestPlayerAccountData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_game_count = value;
                            else
                                delete message.total_game_count;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.recent_games && message.recent_games.length))
                                message.recent_games = [];
                            message.recent_games.push($root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.highest_series_points && message.highest_series_points.length))
                                message.highest_series_points = [];
                            message.highest_series_points.push($root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.accumulate_point = value;
                            else
                                delete message.accumulate_point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ContestPlayerAccountData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ContestPlayerAccountData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    if (!$util.isInteger(message.total_game_count))
                        return "total_game_count: integer expected";
                if (message.recent_games != null && message.hasOwnProperty("recent_games")) {
                    if (!Array.isArray(message.recent_games))
                        return "recent_games: array expected";
                    for (let i = 0; i < message.recent_games.length; ++i) {
                        let error = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.verify(message.recent_games[i], _depth + 1);
                        if (error)
                            return "recent_games." + error;
                    }
                }
                if (message.highest_series_points != null && message.hasOwnProperty("highest_series_points")) {
                    if (!Array.isArray(message.highest_series_points))
                        return "highest_series_points: array expected";
                    for (let i = 0; i < message.highest_series_points.length; ++i) {
                        let error = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.verify(message.highest_series_points[i], _depth + 1);
                        if (error)
                            return "highest_series_points." + error;
                    }
                }
                if (message.accumulate_point != null && message.hasOwnProperty("accumulate_point"))
                    if (!$util.isInteger(message.accumulate_point))
                        return "accumulate_point: integer expected";
                return null;
            };

            ContestPlayerAccountData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData();
                if (object.total_game_count != null)
                    if (Number(object.total_game_count) !== 0)
                        message.total_game_count = object.total_game_count >>> 0;
                if (object.recent_games) {
                    if (!Array.isArray(object.recent_games))
                        throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.recent_games: array expected");
                    message.recent_games = Array(object.recent_games.length);
                    for (let i = 0; i < object.recent_games.length; ++i) {
                        if (typeof object.recent_games[i] !== "object")
                            throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.recent_games: object expected");
                        message.recent_games[i] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.fromObject(object.recent_games[i], _depth + 1);
                    }
                }
                if (object.highest_series_points) {
                    if (!Array.isArray(object.highest_series_points))
                        throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.highest_series_points: array expected");
                    message.highest_series_points = Array(object.highest_series_points.length);
                    for (let i = 0; i < object.highest_series_points.length; ++i) {
                        if (typeof object.highest_series_points[i] !== "object")
                            throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.highest_series_points: object expected");
                        message.highest_series_points[i] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.fromObject(object.highest_series_points[i], _depth + 1);
                    }
                }
                if (object.accumulate_point != null)
                    if (Number(object.accumulate_point) !== 0)
                        message.accumulate_point = object.accumulate_point | 0;
                return message;
            };

            ContestPlayerAccountData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.recent_games = [];
                    object.highest_series_points = [];
                }
                if (options.defaults) {
                    object.total_game_count = 0;
                    object.accumulate_point = 0;
                }
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    object.total_game_count = message.total_game_count;
                if (message.recent_games && message.recent_games.length) {
                    object.recent_games = Array(message.recent_games.length);
                    for (let j = 0; j < message.recent_games.length; ++j)
                        object.recent_games[j] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.toObject(message.recent_games[j], options);
                }
                if (message.highest_series_points && message.highest_series_points.length) {
                    object.highest_series_points = Array(message.highest_series_points.length);
                    for (let j = 0; j < message.highest_series_points.length; ++j)
                        object.highest_series_points[j] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.toObject(message.highest_series_points[j], options);
                }
                if (message.accumulate_point != null && message.hasOwnProperty("accumulate_point"))
                    object.accumulate_point = message.accumulate_point;
                return object;
            };

            ContestPlayerAccountData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContestPlayerAccountData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestPlayerRank.ContestPlayerAccountData";
            };

            ContestPlayerAccountData.ContestGameResult = (function() {

                function ContestGameResult(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                ContestGameResult.prototype.rank = 0;
                ContestGameResult.prototype.total_point = 0;

                ContestGameResult.create = function create(properties) {
                    return new ContestGameResult(properties);
                };

                ContestGameResult.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                        writer.uint32(8).uint32(message.rank);
                    if (message.total_point != null && Object.hasOwnProperty.call(message, "total_point"))
                        writer.uint32(16).int32(message.total_point);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                ContestGameResult.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                ContestGameResult.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.rank = value;
                                else
                                    delete message.rank;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.total_point = value;
                                else
                                    delete message.total_point;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                ContestGameResult.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                ContestGameResult.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.rank != null && message.hasOwnProperty("rank"))
                        if (!$util.isInteger(message.rank))
                            return "rank: integer expected";
                    if (message.total_point != null && message.hasOwnProperty("total_point"))
                        if (!$util.isInteger(message.total_point))
                            return "total_point: integer expected";
                    return null;
                };

                ContestGameResult.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult();
                    if (object.rank != null)
                        if (Number(object.rank) !== 0)
                            message.rank = object.rank >>> 0;
                    if (object.total_point != null)
                        if (Number(object.total_point) !== 0)
                            message.total_point = object.total_point | 0;
                    return message;
                };

                ContestGameResult.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.rank = 0;
                        object.total_point = 0;
                    }
                    if (message.rank != null && message.hasOwnProperty("rank"))
                        object.rank = message.rank;
                    if (message.total_point != null && message.hasOwnProperty("total_point"))
                        object.total_point = message.total_point;
                    return object;
                };

                ContestGameResult.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                ContestGameResult.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult";
                };

                return ContestGameResult;
            })();

            ContestPlayerAccountData.ContestSeriesGameResult = (function() {

                function ContestSeriesGameResult(properties) {
                    this.results = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                ContestSeriesGameResult.prototype.key = 0;
                ContestSeriesGameResult.prototype.results = $util.emptyArray;

                ContestSeriesGameResult.create = function create(properties) {
                    return new ContestSeriesGameResult(properties);
                };

                ContestSeriesGameResult.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        writer.uint32(8).uint32(message.key);
                    if (message.results != null && message.results.length)
                        for (let i = 0; i < message.results.length; ++i)
                            $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.encode(message.results[i], writer.uint32(18).fork()).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                ContestSeriesGameResult.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                ContestSeriesGameResult.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.key = value;
                                else
                                    delete message.key;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.results && message.results.length))
                                    message.results = [];
                                message.results.push($root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                ContestSeriesGameResult.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                ContestSeriesGameResult.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.key != null && message.hasOwnProperty("key"))
                        if (!$util.isInteger(message.key))
                            return "key: integer expected";
                    if (message.results != null && message.hasOwnProperty("results")) {
                        if (!Array.isArray(message.results))
                            return "results: array expected";
                        for (let i = 0; i < message.results.length; ++i) {
                            let error = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.verify(message.results[i], _depth + 1);
                            if (error)
                                return "results." + error;
                        }
                    }
                    return null;
                };

                ContestSeriesGameResult.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult();
                    if (object.key != null)
                        if (Number(object.key) !== 0)
                            message.key = object.key >>> 0;
                    if (object.results) {
                        if (!Array.isArray(object.results))
                            throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.results: array expected");
                        message.results = Array(object.results.length);
                        for (let i = 0; i < object.results.length; ++i) {
                            if (typeof object.results[i] !== "object")
                                throw TypeError(".lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult.results: object expected");
                            message.results[i] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.fromObject(object.results[i], _depth + 1);
                        }
                    }
                    return message;
                };

                ContestSeriesGameResult.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.results = [];
                    if (options.defaults)
                        object.key = 0;
                    if (message.key != null && message.hasOwnProperty("key"))
                        object.key = message.key;
                    if (message.results && message.results.length) {
                        object.results = Array(message.results.length);
                        for (let j = 0; j < message.results.length; ++j)
                            object.results[j] = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestGameResult.toObject(message.results[j], options);
                    }
                    return object;
                };

                ContestSeriesGameResult.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                ContestSeriesGameResult.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ResFetchContestPlayerRank.ContestPlayerAccountData.ContestSeriesGameResult";
                };

                return ContestSeriesGameResult;
            })();

            return ContestPlayerAccountData;
        })();

        ResFetchContestPlayerRank.SeasonRank = (function() {

            function SeasonRank(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SeasonRank.prototype.account_id = 0;
            SeasonRank.prototype.nickname = "";
            SeasonRank.prototype.data = null;
            SeasonRank.prototype.team_name = "";
            SeasonRank.prototype.modify_score = 0;
            SeasonRank.prototype.rank = 0;
            SeasonRank.prototype.rank_percent = 0;

            SeasonRank.create = function create(properties) {
                return new SeasonRank(properties);
            };

            SeasonRank.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(18).string(message.nickname);
                if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                    $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.encode(message.data, writer.uint32(26).fork()).ldelim();
                if (message.team_name != null && Object.hasOwnProperty.call(message, "team_name"))
                    writer.uint32(34).string(message.team_name);
                if (message.modify_score != null && Object.hasOwnProperty.call(message, "modify_score"))
                    writer.uint32(40).int32(message.modify_score);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(48).uint32(message.rank);
                if (message.rank_percent != null && Object.hasOwnProperty.call(message, "rank_percent"))
                    writer.uint32(56).uint32(message.rank_percent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SeasonRank.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SeasonRank.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank.SeasonRank(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.team_name = value;
                            else
                                delete message.team_name;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.modify_score = value;
                            else
                                delete message.modify_score;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank_percent = value;
                            else
                                delete message.rank_percent;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SeasonRank.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SeasonRank.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                if (message.data != null && message.hasOwnProperty("data")) {
                    let error = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    if (!$util.isString(message.team_name))
                        return "team_name: string expected";
                if (message.modify_score != null && message.hasOwnProperty("modify_score"))
                    if (!$util.isInteger(message.modify_score))
                        return "modify_score: integer expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    if (!$util.isInteger(message.rank_percent))
                        return "rank_percent: integer expected";
                return null;
            };

            SeasonRank.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestPlayerRank.SeasonRank)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestPlayerRank.SeasonRank();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                if (object.data != null) {
                    if (typeof object.data !== "object")
                        throw TypeError(".lq.ResFetchContestPlayerRank.SeasonRank.data: object expected");
                    message.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.fromObject(object.data, _depth + 1);
                }
                if (object.team_name != null)
                    if (typeof object.team_name !== "string" || object.team_name.length)
                        message.team_name = String(object.team_name);
                if (object.modify_score != null)
                    if (Number(object.modify_score) !== 0)
                        message.modify_score = object.modify_score | 0;
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.rank_percent != null)
                    if (Number(object.rank_percent) !== 0)
                        message.rank_percent = object.rank_percent >>> 0;
                return message;
            };

            SeasonRank.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.nickname = "";
                    object.data = null;
                    object.team_name = "";
                    object.modify_score = 0;
                    object.rank = 0;
                    object.rank_percent = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                if (message.data != null && message.hasOwnProperty("data"))
                    object.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.toObject(message.data, options);
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    object.team_name = message.team_name;
                if (message.modify_score != null && message.hasOwnProperty("modify_score"))
                    object.modify_score = message.modify_score;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    object.rank_percent = message.rank_percent;
                return object;
            };

            SeasonRank.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SeasonRank.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestPlayerRank.SeasonRank";
            };

            return SeasonRank;
        })();

        ResFetchContestPlayerRank.PlayerData = (function() {

            function PlayerData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PlayerData.prototype.rank = 0;
            PlayerData.prototype.data = null;
            PlayerData.prototype.team_name = "";
            PlayerData.prototype.modify_score = 0;
            PlayerData.prototype.rank_percent = 0;

            PlayerData.create = function create(properties) {
                return new PlayerData(properties);
            };

            PlayerData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(8).uint32(message.rank);
                if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                    $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.encode(message.data, writer.uint32(18).fork()).ldelim();
                if (message.team_name != null && Object.hasOwnProperty.call(message, "team_name"))
                    writer.uint32(26).string(message.team_name);
                if (message.modify_score != null && Object.hasOwnProperty.call(message, "modify_score"))
                    writer.uint32(32).int32(message.modify_score);
                if (message.rank_percent != null && Object.hasOwnProperty.call(message, "rank_percent"))
                    writer.uint32(40).uint32(message.rank_percent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PlayerData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PlayerData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestPlayerRank.PlayerData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.team_name = value;
                            else
                                delete message.team_name;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.modify_score = value;
                            else
                                delete message.modify_score;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank_percent = value;
                            else
                                delete message.rank_percent;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PlayerData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PlayerData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.data != null && message.hasOwnProperty("data")) {
                    let error = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    if (!$util.isString(message.team_name))
                        return "team_name: string expected";
                if (message.modify_score != null && message.hasOwnProperty("modify_score"))
                    if (!$util.isInteger(message.modify_score))
                        return "modify_score: integer expected";
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    if (!$util.isInteger(message.rank_percent))
                        return "rank_percent: integer expected";
                return null;
            };

            PlayerData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestPlayerRank.PlayerData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestPlayerRank.PlayerData();
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.data != null) {
                    if (typeof object.data !== "object")
                        throw TypeError(".lq.ResFetchContestPlayerRank.PlayerData.data: object expected");
                    message.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.fromObject(object.data, _depth + 1);
                }
                if (object.team_name != null)
                    if (typeof object.team_name !== "string" || object.team_name.length)
                        message.team_name = String(object.team_name);
                if (object.modify_score != null)
                    if (Number(object.modify_score) !== 0)
                        message.modify_score = object.modify_score | 0;
                if (object.rank_percent != null)
                    if (Number(object.rank_percent) !== 0)
                        message.rank_percent = object.rank_percent >>> 0;
                return message;
            };

            PlayerData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.rank = 0;
                    object.data = null;
                    object.team_name = "";
                    object.modify_score = 0;
                    object.rank_percent = 0;
                }
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.data != null && message.hasOwnProperty("data"))
                    object.data = $root.lq.ResFetchContestPlayerRank.ContestPlayerAccountData.toObject(message.data, options);
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    object.team_name = message.team_name;
                if (message.modify_score != null && message.hasOwnProperty("modify_score"))
                    object.modify_score = message.modify_score;
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    object.rank_percent = message.rank_percent;
                return object;
            };

            PlayerData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlayerData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestPlayerRank.PlayerData";
            };

            return PlayerData;
        })();

        return ResFetchContestPlayerRank;
    })();

    lq.ReqFetchContestTeamRank = (function() {

        function ReqFetchContestTeamRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchContestTeamRank.prototype.unique_id = 0;
        ReqFetchContestTeamRank.prototype.limit = 0;
        ReqFetchContestTeamRank.prototype.offset = 0;

        ReqFetchContestTeamRank.create = function create(properties) {
            return new ReqFetchContestTeamRank(properties);
        };

        ReqFetchContestTeamRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(16).uint32(message.limit);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(24).uint32(message.offset);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchContestTeamRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchContestTeamRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchContestTeamRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.limit = value;
                        else
                            delete message.limit;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.offset = value;
                        else
                            delete message.offset;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchContestTeamRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchContestTeamRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            return null;
        };

        ReqFetchContestTeamRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchContestTeamRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchContestTeamRank();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.limit != null)
                if (Number(object.limit) !== 0)
                    message.limit = object.limit >>> 0;
            if (object.offset != null)
                if (Number(object.offset) !== 0)
                    message.offset = object.offset >>> 0;
            return message;
        };

        ReqFetchContestTeamRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.limit = 0;
                object.offset = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            return object;
        };

        ReqFetchContestTeamRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchContestTeamRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchContestTeamRank";
        };

        return ReqFetchContestTeamRank;
    })();

    lq.ResFetchContestTeamRank = (function() {

        function ResFetchContestTeamRank(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchContestTeamRank.prototype.error = null;
        ResFetchContestTeamRank.prototype.total = 0;
        ResFetchContestTeamRank.prototype.rank = $util.emptyArray;
        ResFetchContestTeamRank.prototype.self_team_rank = null;

        ResFetchContestTeamRank.create = function create(properties) {
            return new ResFetchContestTeamRank(properties);
        };

        ResFetchContestTeamRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.total != null && Object.hasOwnProperty.call(message, "total"))
                writer.uint32(16).uint32(message.total);
            if (message.rank != null && message.rank.length)
                for (let i = 0; i < message.rank.length; ++i)
                    $root.lq.ResFetchContestTeamRank.SeasonTeamRank.encode(message.rank[i], writer.uint32(26).fork()).ldelim();
            if (message.self_team_rank != null && Object.hasOwnProperty.call(message, "self_team_rank"))
                $root.lq.ResFetchContestTeamRank.SeasonTeamRank.encode(message.self_team_rank, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchContestTeamRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchContestTeamRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestTeamRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total = value;
                        else
                            delete message.total;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push($root.lq.ResFetchContestTeamRank.SeasonTeamRank.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.self_team_rank = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.decode(reader, reader.uint32(), undefined, _depth + 1, message.self_team_rank);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchContestTeamRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchContestTeamRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.total != null && message.hasOwnProperty("total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i) {
                    let error = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.verify(message.rank[i], _depth + 1);
                    if (error)
                        return "rank." + error;
                }
            }
            if (message.self_team_rank != null && message.hasOwnProperty("self_team_rank")) {
                let error = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.verify(message.self_team_rank, _depth + 1);
                if (error)
                    return "self_team_rank." + error;
            }
            return null;
        };

        ResFetchContestTeamRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchContestTeamRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchContestTeamRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchContestTeamRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.total != null)
                if (Number(object.total) !== 0)
                    message.total = object.total >>> 0;
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.ResFetchContestTeamRank.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i) {
                    if (typeof object.rank[i] !== "object")
                        throw TypeError(".lq.ResFetchContestTeamRank.rank: object expected");
                    message.rank[i] = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.fromObject(object.rank[i], _depth + 1);
                }
            }
            if (object.self_team_rank != null) {
                if (typeof object.self_team_rank !== "object")
                    throw TypeError(".lq.ResFetchContestTeamRank.self_team_rank: object expected");
                message.self_team_rank = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.fromObject(object.self_team_rank, _depth + 1);
            }
            return message;
        };

        ResFetchContestTeamRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults) {
                object.error = null;
                object.total = 0;
                object.self_team_rank = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.total != null && message.hasOwnProperty("total"))
                object.total = message.total;
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.toObject(message.rank[j], options);
            }
            if (message.self_team_rank != null && message.hasOwnProperty("self_team_rank"))
                object.self_team_rank = $root.lq.ResFetchContestTeamRank.SeasonTeamRank.toObject(message.self_team_rank, options);
            return object;
        };

        ResFetchContestTeamRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchContestTeamRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchContestTeamRank";
        };

        ResFetchContestTeamRank.ContestTeamData = (function() {

            function ContestTeamData(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ContestTeamData.prototype.total_point = 0;
            ContestTeamData.prototype.total_game_count = 0;
            ContestTeamData.prototype.member_count = 0;

            ContestTeamData.create = function create(properties) {
                return new ContestTeamData(properties);
            };

            ContestTeamData.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.total_point != null && Object.hasOwnProperty.call(message, "total_point"))
                    writer.uint32(8).int32(message.total_point);
                if (message.total_game_count != null && Object.hasOwnProperty.call(message, "total_game_count"))
                    writer.uint32(16).uint32(message.total_game_count);
                if (message.member_count != null && Object.hasOwnProperty.call(message, "member_count"))
                    writer.uint32(24).uint32(message.member_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ContestTeamData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ContestTeamData.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestTeamRank.ContestTeamData(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.total_point = value;
                            else
                                delete message.total_point;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_game_count = value;
                            else
                                delete message.total_game_count;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.member_count = value;
                            else
                                delete message.member_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ContestTeamData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ContestTeamData.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    if (!$util.isInteger(message.total_point))
                        return "total_point: integer expected";
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    if (!$util.isInteger(message.total_game_count))
                        return "total_game_count: integer expected";
                if (message.member_count != null && message.hasOwnProperty("member_count"))
                    if (!$util.isInteger(message.member_count))
                        return "member_count: integer expected";
                return null;
            };

            ContestTeamData.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestTeamRank.ContestTeamData)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestTeamRank.ContestTeamData();
                if (object.total_point != null)
                    if (Number(object.total_point) !== 0)
                        message.total_point = object.total_point | 0;
                if (object.total_game_count != null)
                    if (Number(object.total_game_count) !== 0)
                        message.total_game_count = object.total_game_count >>> 0;
                if (object.member_count != null)
                    if (Number(object.member_count) !== 0)
                        message.member_count = object.member_count >>> 0;
                return message;
            };

            ContestTeamData.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.total_point = 0;
                    object.total_game_count = 0;
                    object.member_count = 0;
                }
                if (message.total_point != null && message.hasOwnProperty("total_point"))
                    object.total_point = message.total_point;
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    object.total_game_count = message.total_game_count;
                if (message.member_count != null && message.hasOwnProperty("member_count"))
                    object.member_count = message.member_count;
                return object;
            };

            ContestTeamData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContestTeamData.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestTeamRank.ContestTeamData";
            };

            return ContestTeamData;
        })();

        ResFetchContestTeamRank.SeasonTeamRank = (function() {

            function SeasonTeamRank(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SeasonTeamRank.prototype.team_id = 0;
            SeasonTeamRank.prototype.name = "";
            SeasonTeamRank.prototype.data = null;
            SeasonTeamRank.prototype.rank = 0;
            SeasonTeamRank.prototype.rank_percent = 0;

            SeasonTeamRank.create = function create(properties) {
                return new SeasonTeamRank(properties);
            };

            SeasonTeamRank.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.team_id != null && Object.hasOwnProperty.call(message, "team_id"))
                    writer.uint32(8).uint32(message.team_id);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(18).string(message.name);
                if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                    $root.lq.ResFetchContestTeamRank.ContestTeamData.encode(message.data, writer.uint32(26).fork()).ldelim();
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(32).uint32(message.rank);
                if (message.rank_percent != null && Object.hasOwnProperty.call(message, "rank_percent"))
                    writer.uint32(40).uint32(message.rank_percent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SeasonTeamRank.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SeasonTeamRank.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestTeamRank.SeasonTeamRank(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.team_id = value;
                            else
                                delete message.team_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.name = value;
                            else
                                delete message.name;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.data = $root.lq.ResFetchContestTeamRank.ContestTeamData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank_percent = value;
                            else
                                delete message.rank_percent;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SeasonTeamRank.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SeasonTeamRank.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.team_id != null && message.hasOwnProperty("team_id"))
                    if (!$util.isInteger(message.team_id))
                        return "team_id: integer expected";
                if (message.name != null && message.hasOwnProperty("name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.data != null && message.hasOwnProperty("data")) {
                    let error = $root.lq.ResFetchContestTeamRank.ContestTeamData.verify(message.data, _depth + 1);
                    if (error)
                        return "data." + error;
                }
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    if (!$util.isInteger(message.rank_percent))
                        return "rank_percent: integer expected";
                return null;
            };

            SeasonTeamRank.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestTeamRank.SeasonTeamRank)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestTeamRank.SeasonTeamRank();
                if (object.team_id != null)
                    if (Number(object.team_id) !== 0)
                        message.team_id = object.team_id >>> 0;
                if (object.name != null)
                    if (typeof object.name !== "string" || object.name.length)
                        message.name = String(object.name);
                if (object.data != null) {
                    if (typeof object.data !== "object")
                        throw TypeError(".lq.ResFetchContestTeamRank.SeasonTeamRank.data: object expected");
                    message.data = $root.lq.ResFetchContestTeamRank.ContestTeamData.fromObject(object.data, _depth + 1);
                }
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.rank_percent != null)
                    if (Number(object.rank_percent) !== 0)
                        message.rank_percent = object.rank_percent >>> 0;
                return message;
            };

            SeasonTeamRank.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.team_id = 0;
                    object.name = "";
                    object.data = null;
                    object.rank = 0;
                    object.rank_percent = 0;
                }
                if (message.team_id != null && message.hasOwnProperty("team_id"))
                    object.team_id = message.team_id;
                if (message.name != null && message.hasOwnProperty("name"))
                    object.name = message.name;
                if (message.data != null && message.hasOwnProperty("data"))
                    object.data = $root.lq.ResFetchContestTeamRank.ContestTeamData.toObject(message.data, options);
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.rank_percent != null && message.hasOwnProperty("rank_percent"))
                    object.rank_percent = message.rank_percent;
                return object;
            };

            SeasonTeamRank.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SeasonTeamRank.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestTeamRank.SeasonTeamRank";
            };

            return SeasonTeamRank;
        })();

        return ResFetchContestTeamRank;
    })();

    lq.ReqFetchContestTeamPlayerRank = (function() {

        function ReqFetchContestTeamPlayerRank(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchContestTeamPlayerRank.prototype.unique_id = 0;
        ReqFetchContestTeamPlayerRank.prototype.offset = 0;
        ReqFetchContestTeamPlayerRank.prototype.limit = 0;

        ReqFetchContestTeamPlayerRank.create = function create(properties) {
            return new ReqFetchContestTeamPlayerRank(properties);
        };

        ReqFetchContestTeamPlayerRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(16).uint32(message.offset);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(24).uint32(message.limit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchContestTeamPlayerRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchContestTeamPlayerRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchContestTeamPlayerRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.offset = value;
                        else
                            delete message.offset;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.limit = value;
                        else
                            delete message.limit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchContestTeamPlayerRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchContestTeamPlayerRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        ReqFetchContestTeamPlayerRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchContestTeamPlayerRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchContestTeamPlayerRank();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.offset != null)
                if (Number(object.offset) !== 0)
                    message.offset = object.offset >>> 0;
            if (object.limit != null)
                if (Number(object.limit) !== 0)
                    message.limit = object.limit >>> 0;
            return message;
        };

        ReqFetchContestTeamPlayerRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.offset = 0;
                object.limit = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        ReqFetchContestTeamPlayerRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchContestTeamPlayerRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchContestTeamPlayerRank";
        };

        return ReqFetchContestTeamPlayerRank;
    })();

    lq.ReqFetchContestTeamMember = (function() {

        function ReqFetchContestTeamMember(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchContestTeamMember.prototype.unique_id = 0;
        ReqFetchContestTeamMember.prototype.team_id = 0;
        ReqFetchContestTeamMember.prototype.offset = 0;
        ReqFetchContestTeamMember.prototype.limit = 0;

        ReqFetchContestTeamMember.create = function create(properties) {
            return new ReqFetchContestTeamMember(properties);
        };

        ReqFetchContestTeamMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.team_id != null && Object.hasOwnProperty.call(message, "team_id"))
                writer.uint32(16).uint32(message.team_id);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(24).uint32(message.offset);
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(32).uint32(message.limit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchContestTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchContestTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchContestTeamMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.team_id = value;
                        else
                            delete message.team_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.offset = value;
                        else
                            delete message.offset;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.limit = value;
                        else
                            delete message.limit;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchContestTeamMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchContestTeamMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.team_id != null && message.hasOwnProperty("team_id"))
                if (!$util.isInteger(message.team_id))
                    return "team_id: integer expected";
            if (message.offset != null && message.hasOwnProperty("offset"))
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            if (message.limit != null && message.hasOwnProperty("limit"))
                if (!$util.isInteger(message.limit))
                    return "limit: integer expected";
            return null;
        };

        ReqFetchContestTeamMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchContestTeamMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchContestTeamMember();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.team_id != null)
                if (Number(object.team_id) !== 0)
                    message.team_id = object.team_id >>> 0;
            if (object.offset != null)
                if (Number(object.offset) !== 0)
                    message.offset = object.offset >>> 0;
            if (object.limit != null)
                if (Number(object.limit) !== 0)
                    message.limit = object.limit >>> 0;
            return message;
        };

        ReqFetchContestTeamMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.unique_id = 0;
                object.team_id = 0;
                object.offset = 0;
                object.limit = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.team_id != null && message.hasOwnProperty("team_id"))
                object.team_id = message.team_id;
            if (message.offset != null && message.hasOwnProperty("offset"))
                object.offset = message.offset;
            if (message.limit != null && message.hasOwnProperty("limit"))
                object.limit = message.limit;
            return object;
        };

        ReqFetchContestTeamMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchContestTeamMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchContestTeamMember";
        };

        return ReqFetchContestTeamMember;
    })();

    lq.ResFetchContestTeamMember = (function() {

        function ResFetchContestTeamMember(properties) {
            this.rank = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchContestTeamMember.prototype.error = null;
        ResFetchContestTeamMember.prototype.total = 0;
        ResFetchContestTeamMember.prototype.rank = $util.emptyArray;

        ResFetchContestTeamMember.create = function create(properties) {
            return new ResFetchContestTeamMember(properties);
        };

        ResFetchContestTeamMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.total != null && Object.hasOwnProperty.call(message, "total"))
                writer.uint32(16).uint32(message.total);
            if (message.rank != null && message.rank.length)
                for (let i = 0; i < message.rank.length; ++i)
                    $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank.encode(message.rank[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchContestTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchContestTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestTeamMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total = value;
                        else
                            delete message.total;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rank && message.rank.length))
                            message.rank = [];
                        message.rank.push($root.lq.ResFetchContestTeamMember.ContestTeamMemberRank.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchContestTeamMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchContestTeamMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.total != null && message.hasOwnProperty("total"))
                if (!$util.isInteger(message.total))
                    return "total: integer expected";
            if (message.rank != null && message.hasOwnProperty("rank")) {
                if (!Array.isArray(message.rank))
                    return "rank: array expected";
                for (let i = 0; i < message.rank.length; ++i) {
                    let error = $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank.verify(message.rank[i], _depth + 1);
                    if (error)
                        return "rank." + error;
                }
            }
            return null;
        };

        ResFetchContestTeamMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchContestTeamMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchContestTeamMember();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchContestTeamMember.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.total != null)
                if (Number(object.total) !== 0)
                    message.total = object.total >>> 0;
            if (object.rank) {
                if (!Array.isArray(object.rank))
                    throw TypeError(".lq.ResFetchContestTeamMember.rank: array expected");
                message.rank = Array(object.rank.length);
                for (let i = 0; i < object.rank.length; ++i) {
                    if (typeof object.rank[i] !== "object")
                        throw TypeError(".lq.ResFetchContestTeamMember.rank: object expected");
                    message.rank[i] = $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank.fromObject(object.rank[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchContestTeamMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rank = [];
            if (options.defaults) {
                object.error = null;
                object.total = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.total != null && message.hasOwnProperty("total"))
                object.total = message.total;
            if (message.rank && message.rank.length) {
                object.rank = Array(message.rank.length);
                for (let j = 0; j < message.rank.length; ++j)
                    object.rank[j] = $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank.toObject(message.rank[j], options);
            }
            return object;
        };

        ResFetchContestTeamMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchContestTeamMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchContestTeamMember";
        };

        ResFetchContestTeamMember.ContestTeamMemberRank = (function() {

            function ContestTeamMemberRank(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ContestTeamMemberRank.prototype.account_id = 0;
            ContestTeamMemberRank.prototype.total_game_count = 0;
            ContestTeamMemberRank.prototype.total_score = 0;
            ContestTeamMemberRank.prototype.nickname = "";

            ContestTeamMemberRank.create = function create(properties) {
                return new ContestTeamMemberRank(properties);
            };

            ContestTeamMemberRank.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.total_game_count != null && Object.hasOwnProperty.call(message, "total_game_count"))
                    writer.uint32(24).uint32(message.total_game_count);
                if (message.total_score != null && Object.hasOwnProperty.call(message, "total_score"))
                    writer.uint32(32).int32(message.total_score);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(42).string(message.nickname);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ContestTeamMemberRank.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ContestTeamMemberRank.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.total_game_count = value;
                            else
                                delete message.total_game_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.total_score = value;
                            else
                                delete message.total_score;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ContestTeamMemberRank.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ContestTeamMemberRank.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    if (!$util.isInteger(message.total_game_count))
                        return "total_game_count: integer expected";
                if (message.total_score != null && message.hasOwnProperty("total_score"))
                    if (!$util.isInteger(message.total_score))
                        return "total_score: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                return null;
            };

            ContestTeamMemberRank.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchContestTeamMember.ContestTeamMemberRank();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.total_game_count != null)
                    if (Number(object.total_game_count) !== 0)
                        message.total_game_count = object.total_game_count >>> 0;
                if (object.total_score != null)
                    if (Number(object.total_score) !== 0)
                        message.total_score = object.total_score | 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                return message;
            };

            ContestTeamMemberRank.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.total_game_count = 0;
                    object.total_score = 0;
                    object.nickname = "";
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.total_game_count != null && message.hasOwnProperty("total_game_count"))
                    object.total_game_count = message.total_game_count;
                if (message.total_score != null && message.hasOwnProperty("total_score"))
                    object.total_score = message.total_score;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                return object;
            };

            ContestTeamMemberRank.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContestTeamMemberRank.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchContestTeamMember.ContestTeamMemberRank";
            };

            return ContestTeamMemberRank;
        })();

        return ResFetchContestTeamMember;
    })();

    lq.ReqFetchReadyPlayerList = (function() {

        function ReqFetchReadyPlayerList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchReadyPlayerList.prototype.unique_id = 0;

        ReqFetchReadyPlayerList.create = function create(properties) {
            return new ReqFetchReadyPlayerList(properties);
        };

        ReqFetchReadyPlayerList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchReadyPlayerList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchReadyPlayerList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchReadyPlayerList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchReadyPlayerList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchReadyPlayerList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            return null;
        };

        ReqFetchReadyPlayerList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchReadyPlayerList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchReadyPlayerList();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            return message;
        };

        ReqFetchReadyPlayerList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unique_id = 0;
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            return object;
        };

        ReqFetchReadyPlayerList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchReadyPlayerList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchReadyPlayerList";
        };

        return ReqFetchReadyPlayerList;
    })();

    lq.ResFetchReadyPlayerList = (function() {

        function ResFetchReadyPlayerList(properties) {
            this.list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchReadyPlayerList.prototype.error = null;
        ResFetchReadyPlayerList.prototype.list = $util.emptyArray;

        ResFetchReadyPlayerList.create = function create(properties) {
            return new ResFetchReadyPlayerList(properties);
        };

        ResFetchReadyPlayerList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.list != null && message.list.length)
                for (let i = 0; i < message.list.length; ++i)
                    $root.lq.ResFetchReadyPlayerList.Player.encode(message.list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchReadyPlayerList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchReadyPlayerList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchReadyPlayerList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.lq.ResFetchReadyPlayerList.Player.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchReadyPlayerList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchReadyPlayerList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (let i = 0; i < message.list.length; ++i) {
                    let error = $root.lq.ResFetchReadyPlayerList.Player.verify(message.list[i], _depth + 1);
                    if (error)
                        return "list." + error;
                }
            }
            return null;
        };

        ResFetchReadyPlayerList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchReadyPlayerList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchReadyPlayerList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchReadyPlayerList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".lq.ResFetchReadyPlayerList.list: array expected");
                message.list = Array(object.list.length);
                for (let i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".lq.ResFetchReadyPlayerList.list: object expected");
                    message.list[i] = $root.lq.ResFetchReadyPlayerList.Player.fromObject(object.list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchReadyPlayerList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.list && message.list.length) {
                object.list = Array(message.list.length);
                for (let j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.lq.ResFetchReadyPlayerList.Player.toObject(message.list[j], options);
            }
            return object;
        };

        ResFetchReadyPlayerList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchReadyPlayerList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchReadyPlayerList";
        };

        ResFetchReadyPlayerList.Player = (function() {

            function Player(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            Player.prototype.account_id = 0;
            Player.prototype.nickname = "";
            Player.prototype.team_name = "";

            Player.create = function create(properties) {
                return new Player(properties);
            };

            Player.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                    writer.uint32(18).string(message.nickname);
                if (message.team_name != null && Object.hasOwnProperty.call(message, "team_name"))
                    writer.uint32(26).string(message.team_name);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            Player.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            Player.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchReadyPlayerList.Player(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.nickname = value;
                            else
                                delete message.nickname;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.team_name = value;
                            else
                                delete message.team_name;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            Player.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            Player.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    if (!$util.isString(message.nickname))
                        return "nickname: string expected";
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    if (!$util.isString(message.team_name))
                        return "team_name: string expected";
                return null;
            };

            Player.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchReadyPlayerList.Player)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchReadyPlayerList.Player();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.nickname != null)
                    if (typeof object.nickname !== "string" || object.nickname.length)
                        message.nickname = String(object.nickname);
                if (object.team_name != null)
                    if (typeof object.team_name !== "string" || object.team_name.length)
                        message.team_name = String(object.team_name);
                return message;
            };

            Player.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.nickname = "";
                    object.team_name = "";
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.nickname != null && message.hasOwnProperty("nickname"))
                    object.nickname = message.nickname;
                if (message.team_name != null && message.hasOwnProperty("team_name"))
                    object.team_name = message.team_name;
                return object;
            };

            Player.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Player.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchReadyPlayerList.Player";
            };

            return Player;
        })();

        return ResFetchReadyPlayerList;
    })();

    lq.ReqCreateGamePlan = (function() {

        function ReqCreateGamePlan(properties) {
            this.account_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateGamePlan.prototype.unique_id = 0;
        ReqCreateGamePlan.prototype.account_list = $util.emptyArray;
        ReqCreateGamePlan.prototype.game_start_time = 0;
        ReqCreateGamePlan.prototype.shuffle_seats = 0;
        ReqCreateGamePlan.prototype.ai_level = 0;

        ReqCreateGamePlan.create = function create(properties) {
            return new ReqCreateGamePlan(properties);
        };

        ReqCreateGamePlan.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unique_id != null && Object.hasOwnProperty.call(message, "unique_id"))
                writer.uint32(8).uint32(message.unique_id);
            if (message.account_list != null && message.account_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.account_list.length; ++i)
                    writer.uint32(message.account_list[i]);
                writer.ldelim();
            }
            if (message.game_start_time != null && Object.hasOwnProperty.call(message, "game_start_time"))
                writer.uint32(24).uint32(message.game_start_time);
            if (message.shuffle_seats != null && Object.hasOwnProperty.call(message, "shuffle_seats"))
                writer.uint32(32).uint32(message.shuffle_seats);
            if (message.ai_level != null && Object.hasOwnProperty.call(message, "ai_level"))
                writer.uint32(40).uint32(message.ai_level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateGamePlan.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateGamePlan.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateGamePlan(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.unique_id = value;
                        else
                            delete message.unique_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.account_list && message.account_list.length))
                                message.account_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_list && message.account_list.length))
                            message.account_list = [];
                        message.account_list.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_start_time = value;
                        else
                            delete message.game_start_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shuffle_seats = value;
                        else
                            delete message.shuffle_seats;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ai_level = value;
                        else
                            delete message.ai_level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateGamePlan.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateGamePlan.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                if (!$util.isInteger(message.unique_id))
                    return "unique_id: integer expected";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                if (!Array.isArray(message.account_list))
                    return "account_list: array expected";
                for (let i = 0; i < message.account_list.length; ++i)
                    if (!$util.isInteger(message.account_list[i]))
                        return "account_list: integer[] expected";
            }
            if (message.game_start_time != null && message.hasOwnProperty("game_start_time"))
                if (!$util.isInteger(message.game_start_time))
                    return "game_start_time: integer expected";
            if (message.shuffle_seats != null && message.hasOwnProperty("shuffle_seats"))
                if (!$util.isInteger(message.shuffle_seats))
                    return "shuffle_seats: integer expected";
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                if (!$util.isInteger(message.ai_level))
                    return "ai_level: integer expected";
            return null;
        };

        ReqCreateGamePlan.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateGamePlan)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateGamePlan();
            if (object.unique_id != null)
                if (Number(object.unique_id) !== 0)
                    message.unique_id = object.unique_id >>> 0;
            if (object.account_list) {
                if (!Array.isArray(object.account_list))
                    throw TypeError(".lq.ReqCreateGamePlan.account_list: array expected");
                message.account_list = Array(object.account_list.length);
                for (let i = 0; i < object.account_list.length; ++i)
                    message.account_list[i] = object.account_list[i] >>> 0;
            }
            if (object.game_start_time != null)
                if (Number(object.game_start_time) !== 0)
                    message.game_start_time = object.game_start_time >>> 0;
            if (object.shuffle_seats != null)
                if (Number(object.shuffle_seats) !== 0)
                    message.shuffle_seats = object.shuffle_seats >>> 0;
            if (object.ai_level != null)
                if (Number(object.ai_level) !== 0)
                    message.ai_level = object.ai_level >>> 0;
            return message;
        };

        ReqCreateGamePlan.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_list = [];
            if (options.defaults) {
                object.unique_id = 0;
                object.game_start_time = 0;
                object.shuffle_seats = 0;
                object.ai_level = 0;
            }
            if (message.unique_id != null && message.hasOwnProperty("unique_id"))
                object.unique_id = message.unique_id;
            if (message.account_list && message.account_list.length) {
                object.account_list = Array(message.account_list.length);
                for (let j = 0; j < message.account_list.length; ++j)
                    object.account_list[j] = message.account_list[j];
            }
            if (message.game_start_time != null && message.hasOwnProperty("game_start_time"))
                object.game_start_time = message.game_start_time;
            if (message.shuffle_seats != null && message.hasOwnProperty("shuffle_seats"))
                object.shuffle_seats = message.shuffle_seats;
            if (message.ai_level != null && message.hasOwnProperty("ai_level"))
                object.ai_level = message.ai_level;
            return object;
        };

        ReqCreateGamePlan.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateGamePlan.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateGamePlan";
        };

        return ReqCreateGamePlan;
    })();

    lq.ResGenerateContestManagerLoginCode = (function() {

        function ResGenerateContestManagerLoginCode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGenerateContestManagerLoginCode.prototype.error = null;
        ResGenerateContestManagerLoginCode.prototype.code = "";

        ResGenerateContestManagerLoginCode.create = function create(properties) {
            return new ResGenerateContestManagerLoginCode(properties);
        };

        ResGenerateContestManagerLoginCode.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(18).string(message.code);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGenerateContestManagerLoginCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGenerateContestManagerLoginCode.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGenerateContestManagerLoginCode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGenerateContestManagerLoginCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGenerateContestManagerLoginCode.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.code != null && message.hasOwnProperty("code"))
                if (!$util.isString(message.code))
                    return "code: string expected";
            return null;
        };

        ResGenerateContestManagerLoginCode.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGenerateContestManagerLoginCode)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGenerateContestManagerLoginCode();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGenerateContestManagerLoginCode.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = String(object.code);
            return message;
        };

        ResGenerateContestManagerLoginCode.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.code = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.code != null && message.hasOwnProperty("code"))
                object.code = message.code;
            return object;
        };

        ResGenerateContestManagerLoginCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGenerateContestManagerLoginCode.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGenerateContestManagerLoginCode";
        };

        return ResGenerateContestManagerLoginCode;
    })();

    lq.ReqAmuletActivityFetchInfo = (function() {

        function ReqAmuletActivityFetchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityFetchInfo.prototype.activity_id = 0;

        ReqAmuletActivityFetchInfo.create = function create(properties) {
            return new ReqAmuletActivityFetchInfo(properties);
        };

        ReqAmuletActivityFetchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityFetchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityFetchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityFetchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityFetchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityFetchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityFetchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityFetchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityFetchInfo();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityFetchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityFetchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityFetchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityFetchInfo";
        };

        return ReqAmuletActivityFetchInfo;
    })();

    lq.ResAmuletActivityFetchInfo = (function() {

        function ResAmuletActivityFetchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAmuletActivityFetchInfo.prototype.error = null;
        ResAmuletActivityFetchInfo.prototype.data = null;

        ResAmuletActivityFetchInfo.create = function create(properties) {
            return new ResAmuletActivityFetchInfo(properties);
        };

        ResAmuletActivityFetchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.lq.ActivityAmuletData.encode(message.data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAmuletActivityFetchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAmuletActivityFetchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAmuletActivityFetchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.lq.ActivityAmuletData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAmuletActivityFetchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAmuletActivityFetchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.lq.ActivityAmuletData.verify(message.data, _depth + 1);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        ResAmuletActivityFetchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAmuletActivityFetchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAmuletActivityFetchInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchInfo.data: object expected");
                message.data = $root.lq.ActivityAmuletData.fromObject(object.data, _depth + 1);
            }
            return message;
        };

        ResAmuletActivityFetchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.lq.ActivityAmuletData.toObject(message.data, options);
            return object;
        };

        ResAmuletActivityFetchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAmuletActivityFetchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAmuletActivityFetchInfo";
        };

        return ResAmuletActivityFetchInfo;
    })();

    lq.ReqAmuletActivityFetchBrief = (function() {

        function ReqAmuletActivityFetchBrief(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityFetchBrief.prototype.activity_id = 0;

        ReqAmuletActivityFetchBrief.create = function create(properties) {
            return new ReqAmuletActivityFetchBrief(properties);
        };

        ReqAmuletActivityFetchBrief.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityFetchBrief.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityFetchBrief.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityFetchBrief(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityFetchBrief.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityFetchBrief.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityFetchBrief.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityFetchBrief)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityFetchBrief();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityFetchBrief.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityFetchBrief.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityFetchBrief.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityFetchBrief";
        };

        return ReqAmuletActivityFetchBrief;
    })();

    lq.ResAmuletActivityFetchBrief = (function() {

        function ResAmuletActivityFetchBrief(properties) {
            this.game_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAmuletActivityFetchBrief.prototype.error = null;
        ResAmuletActivityFetchBrief.prototype.upgrade = null;
        ResAmuletActivityFetchBrief.prototype.illustrated_book = null;
        ResAmuletActivityFetchBrief.prototype.game_records = $util.emptyArray;
        ResAmuletActivityFetchBrief.prototype.statistic = null;

        ResAmuletActivityFetchBrief.create = function create(properties) {
            return new ResAmuletActivityFetchBrief(properties);
        };

        ResAmuletActivityFetchBrief.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.upgrade != null && Object.hasOwnProperty.call(message, "upgrade"))
                $root.lq.ActivityAmuletUpgradeData.encode(message.upgrade, writer.uint32(34).fork()).ldelim();
            if (message.illustrated_book != null && Object.hasOwnProperty.call(message, "illustrated_book"))
                $root.lq.ActivityAmuletIllustratedBookData.encode(message.illustrated_book, writer.uint32(42).fork()).ldelim();
            if (message.game_records != null && message.game_records.length)
                for (let i = 0; i < message.game_records.length; ++i)
                    $root.lq.ActivityAmuletGameRecordData.encode(message.game_records[i], writer.uint32(58).fork()).ldelim();
            if (message.statistic != null && Object.hasOwnProperty.call(message, "statistic"))
                $root.lq.ActivityAmuletStatisticData.encode(message.statistic, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAmuletActivityFetchBrief.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAmuletActivityFetchBrief.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAmuletActivityFetchBrief(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.upgrade = $root.lq.ActivityAmuletUpgradeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.decode(reader, reader.uint32(), undefined, _depth + 1, message.illustrated_book);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.game_records && message.game_records.length))
                            message.game_records = [];
                        message.game_records.push($root.lq.ActivityAmuletGameRecordData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.statistic = $root.lq.ActivityAmuletStatisticData.decode(reader, reader.uint32(), undefined, _depth + 1, message.statistic);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAmuletActivityFetchBrief.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAmuletActivityFetchBrief.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                let error = $root.lq.ActivityAmuletUpgradeData.verify(message.upgrade, _depth + 1);
                if (error)
                    return "upgrade." + error;
            }
            if (message.illustrated_book != null && message.hasOwnProperty("illustrated_book")) {
                let error = $root.lq.ActivityAmuletIllustratedBookData.verify(message.illustrated_book, _depth + 1);
                if (error)
                    return "illustrated_book." + error;
            }
            if (message.game_records != null && message.hasOwnProperty("game_records")) {
                if (!Array.isArray(message.game_records))
                    return "game_records: array expected";
                for (let i = 0; i < message.game_records.length; ++i) {
                    let error = $root.lq.ActivityAmuletGameRecordData.verify(message.game_records[i], _depth + 1);
                    if (error)
                        return "game_records." + error;
                }
            }
            if (message.statistic != null && message.hasOwnProperty("statistic")) {
                let error = $root.lq.ActivityAmuletStatisticData.verify(message.statistic, _depth + 1);
                if (error)
                    return "statistic." + error;
            }
            return null;
        };

        ResAmuletActivityFetchBrief.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAmuletActivityFetchBrief)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAmuletActivityFetchBrief();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchBrief.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.upgrade != null) {
                if (typeof object.upgrade !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchBrief.upgrade: object expected");
                message.upgrade = $root.lq.ActivityAmuletUpgradeData.fromObject(object.upgrade, _depth + 1);
            }
            if (object.illustrated_book != null) {
                if (typeof object.illustrated_book !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchBrief.illustrated_book: object expected");
                message.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.fromObject(object.illustrated_book, _depth + 1);
            }
            if (object.game_records) {
                if (!Array.isArray(object.game_records))
                    throw TypeError(".lq.ResAmuletActivityFetchBrief.game_records: array expected");
                message.game_records = Array(object.game_records.length);
                for (let i = 0; i < object.game_records.length; ++i) {
                    if (typeof object.game_records[i] !== "object")
                        throw TypeError(".lq.ResAmuletActivityFetchBrief.game_records: object expected");
                    message.game_records[i] = $root.lq.ActivityAmuletGameRecordData.fromObject(object.game_records[i], _depth + 1);
                }
            }
            if (object.statistic != null) {
                if (typeof object.statistic !== "object")
                    throw TypeError(".lq.ResAmuletActivityFetchBrief.statistic: object expected");
                message.statistic = $root.lq.ActivityAmuletStatisticData.fromObject(object.statistic, _depth + 1);
            }
            return message;
        };

        ResAmuletActivityFetchBrief.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.game_records = [];
            if (options.defaults) {
                object.error = null;
                object.upgrade = null;
                object.illustrated_book = null;
                object.statistic = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.upgrade != null && message.hasOwnProperty("upgrade"))
                object.upgrade = $root.lq.ActivityAmuletUpgradeData.toObject(message.upgrade, options);
            if (message.illustrated_book != null && message.hasOwnProperty("illustrated_book"))
                object.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.toObject(message.illustrated_book, options);
            if (message.game_records && message.game_records.length) {
                object.game_records = Array(message.game_records.length);
                for (let j = 0; j < message.game_records.length; ++j)
                    object.game_records[j] = $root.lq.ActivityAmuletGameRecordData.toObject(message.game_records[j], options);
            }
            if (message.statistic != null && message.hasOwnProperty("statistic"))
                object.statistic = $root.lq.ActivityAmuletStatisticData.toObject(message.statistic, options);
            return object;
        };

        ResAmuletActivityFetchBrief.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAmuletActivityFetchBrief.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAmuletActivityFetchBrief";
        };

        return ResAmuletActivityFetchBrief;
    })();

    lq.ReqFetchAmuletActivityData = (function() {

        function ReqFetchAmuletActivityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchAmuletActivityData.prototype.activity_id = 0;

        ReqFetchAmuletActivityData.create = function create(properties) {
            return new ReqFetchAmuletActivityData(properties);
        };

        ReqFetchAmuletActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchAmuletActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchAmuletActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchAmuletActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchAmuletActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchAmuletActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchAmuletActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchAmuletActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchAmuletActivityData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchAmuletActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchAmuletActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchAmuletActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchAmuletActivityData";
        };

        return ReqFetchAmuletActivityData;
    })();

    lq.ResFetchAmuletActivityData = (function() {

        function ResFetchAmuletActivityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchAmuletActivityData.prototype.error = null;
        ResFetchAmuletActivityData.prototype.data = null;

        ResFetchAmuletActivityData.create = function create(properties) {
            return new ResFetchAmuletActivityData(properties);
        };

        ResFetchAmuletActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.lq.ActivityAmuletData.encode(message.data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchAmuletActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchAmuletActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAmuletActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.lq.ActivityAmuletData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchAmuletActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchAmuletActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.lq.ActivityAmuletData.verify(message.data, _depth + 1);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        ResFetchAmuletActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchAmuletActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchAmuletActivityData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchAmuletActivityData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".lq.ResFetchAmuletActivityData.data: object expected");
                message.data = $root.lq.ActivityAmuletData.fromObject(object.data, _depth + 1);
            }
            return message;
        };

        ResFetchAmuletActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.lq.ActivityAmuletData.toObject(message.data, options);
            return object;
        };

        ResFetchAmuletActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchAmuletActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchAmuletActivityData";
        };

        return ResFetchAmuletActivityData;
    })();

    lq.ResAmuletEventResponse = (function() {

        function ResAmuletEventResponse(properties) {
            this.events = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAmuletEventResponse.prototype.error = null;
        ResAmuletEventResponse.prototype.events = $util.emptyArray;

        ResAmuletEventResponse.create = function create(properties) {
            return new ResAmuletEventResponse(properties);
        };

        ResAmuletEventResponse.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.events != null && message.events.length)
                for (let i = 0; i < message.events.length; ++i)
                    $root.lq.AmuletEventData.encode(message.events[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAmuletEventResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAmuletEventResponse.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAmuletEventResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.events && message.events.length))
                            message.events = [];
                        message.events.push($root.lq.AmuletEventData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAmuletEventResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAmuletEventResponse.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.events != null && message.hasOwnProperty("events")) {
                if (!Array.isArray(message.events))
                    return "events: array expected";
                for (let i = 0; i < message.events.length; ++i) {
                    let error = $root.lq.AmuletEventData.verify(message.events[i], _depth + 1);
                    if (error)
                        return "events." + error;
                }
            }
            return null;
        };

        ResAmuletEventResponse.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAmuletEventResponse)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAmuletEventResponse();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAmuletEventResponse.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.events) {
                if (!Array.isArray(object.events))
                    throw TypeError(".lq.ResAmuletEventResponse.events: array expected");
                message.events = Array(object.events.length);
                for (let i = 0; i < object.events.length; ++i) {
                    if (typeof object.events[i] !== "object")
                        throw TypeError(".lq.ResAmuletEventResponse.events: object expected");
                    message.events[i] = $root.lq.AmuletEventData.fromObject(object.events[i], _depth + 1);
                }
            }
            return message;
        };

        ResAmuletEventResponse.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.events = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.events && message.events.length) {
                object.events = Array(message.events.length);
                for (let j = 0; j < message.events.length; ++j)
                    object.events[j] = $root.lq.AmuletEventData.toObject(message.events[j], options);
            }
            return object;
        };

        ResAmuletEventResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAmuletEventResponse.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAmuletEventResponse";
        };

        return ResAmuletEventResponse;
    })();

    lq.ReqAmuletActivityStartGame = (function() {

        function ReqAmuletActivityStartGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityStartGame.prototype.activity_id = 0;

        ReqAmuletActivityStartGame.create = function create(properties) {
            return new ReqAmuletActivityStartGame(properties);
        };

        ReqAmuletActivityStartGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityStartGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityStartGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityStartGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityStartGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityStartGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityStartGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityStartGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityStartGame();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityStartGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityStartGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityStartGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityStartGame";
        };

        return ReqAmuletActivityStartGame;
    })();

    lq.ReqAmuletActivityOperate = (function() {

        function ReqAmuletActivityOperate(properties) {
            this.tile_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityOperate.prototype.activity_id = 0;
        ReqAmuletActivityOperate.prototype.type = 0;
        ReqAmuletActivityOperate.prototype.tile_list = $util.emptyArray;

        ReqAmuletActivityOperate.create = function create(properties) {
            return new ReqAmuletActivityOperate(properties);
        };

        ReqAmuletActivityOperate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.tile_list != null && message.tile_list.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.tile_list.length; ++i)
                    writer.uint32(message.tile_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityOperate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityOperate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityOperate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.tile_list && message.tile_list.length))
                                message.tile_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_list && message.tile_list.length))
                            message.tile_list = [];
                        message.tile_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityOperate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityOperate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tile_list != null && message.hasOwnProperty("tile_list")) {
                if (!Array.isArray(message.tile_list))
                    return "tile_list: array expected";
                for (let i = 0; i < message.tile_list.length; ++i)
                    if (!$util.isInteger(message.tile_list[i]))
                        return "tile_list: integer[] expected";
            }
            return null;
        };

        ReqAmuletActivityOperate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityOperate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityOperate();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tile_list) {
                if (!Array.isArray(object.tile_list))
                    throw TypeError(".lq.ReqAmuletActivityOperate.tile_list: array expected");
                message.tile_list = Array(object.tile_list.length);
                for (let i = 0; i < object.tile_list.length; ++i)
                    message.tile_list[i] = object.tile_list[i] >>> 0;
            }
            return message;
        };

        ReqAmuletActivityOperate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.tile_list = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.type = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tile_list && message.tile_list.length) {
                object.tile_list = Array(message.tile_list.length);
                for (let j = 0; j < message.tile_list.length; ++j)
                    object.tile_list[j] = message.tile_list[j];
            }
            return object;
        };

        ReqAmuletActivityOperate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityOperate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityOperate";
        };

        return ReqAmuletActivityOperate;
    })();

    lq.ReqAmuletActivityUpgrade = (function() {

        function ReqAmuletActivityUpgrade(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityUpgrade.prototype.activity_id = 0;

        ReqAmuletActivityUpgrade.create = function create(properties) {
            return new ReqAmuletActivityUpgrade(properties);
        };

        ReqAmuletActivityUpgrade.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityUpgrade.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityUpgrade.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityUpgrade(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityUpgrade.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityUpgrade.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityUpgrade.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityUpgrade)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityUpgrade();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityUpgrade.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityUpgrade.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityUpgrade.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityUpgrade";
        };

        return ReqAmuletActivityUpgrade;
    })();

    lq.ReqAmuletActivitySelectPack = (function() {

        function ReqAmuletActivitySelectPack(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySelectPack.prototype.activity_id = 0;
        ReqAmuletActivitySelectPack.prototype.id = 0;

        ReqAmuletActivitySelectPack.create = function create(properties) {
            return new ReqAmuletActivitySelectPack(properties);
        };

        ReqAmuletActivitySelectPack.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySelectPack.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySelectPack.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySelectPack(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySelectPack.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySelectPack.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqAmuletActivitySelectPack.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySelectPack)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySelectPack();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqAmuletActivitySelectPack.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqAmuletActivitySelectPack.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySelectPack.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySelectPack";
        };

        return ReqAmuletActivitySelectPack;
    })();

    lq.ReqAmuletActivityBuy = (function() {

        function ReqAmuletActivityBuy(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityBuy.prototype.activity_id = 0;
        ReqAmuletActivityBuy.prototype.id = 0;

        ReqAmuletActivityBuy.create = function create(properties) {
            return new ReqAmuletActivityBuy(properties);
        };

        ReqAmuletActivityBuy.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(24).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityBuy.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityBuy.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityBuy(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityBuy.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityBuy.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqAmuletActivityBuy.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityBuy)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityBuy();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqAmuletActivityBuy.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqAmuletActivityBuy.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityBuy.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityBuy";
        };

        return ReqAmuletActivityBuy;
    })();

    lq.ReqAmuletActivitySellEffect = (function() {

        function ReqAmuletActivitySellEffect(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySellEffect.prototype.activity_id = 0;
        ReqAmuletActivitySellEffect.prototype.id = 0;

        ReqAmuletActivitySellEffect.create = function create(properties) {
            return new ReqAmuletActivitySellEffect(properties);
        };

        ReqAmuletActivitySellEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySellEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySellEffect.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySellEffect(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySellEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySellEffect.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqAmuletActivitySellEffect.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySellEffect)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySellEffect();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqAmuletActivitySellEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqAmuletActivitySellEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySellEffect.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySellEffect";
        };

        return ReqAmuletActivitySellEffect;
    })();

    lq.ReqAmuletActivityEffectSort = (function() {

        function ReqAmuletActivityEffectSort(properties) {
            this.sorted_id = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityEffectSort.prototype.activity_id = 0;
        ReqAmuletActivityEffectSort.prototype.sorted_id = $util.emptyArray;

        ReqAmuletActivityEffectSort.create = function create(properties) {
            return new ReqAmuletActivityEffectSort(properties);
        };

        ReqAmuletActivityEffectSort.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.sorted_id != null && message.sorted_id.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.sorted_id.length; ++i)
                    writer.uint32(message.sorted_id[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityEffectSort.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityEffectSort.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityEffectSort(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.sorted_id && message.sorted_id.length))
                                message.sorted_id = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.sorted_id.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.sorted_id && message.sorted_id.length))
                            message.sorted_id = [];
                        message.sorted_id.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityEffectSort.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityEffectSort.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.sorted_id != null && message.hasOwnProperty("sorted_id")) {
                if (!Array.isArray(message.sorted_id))
                    return "sorted_id: array expected";
                for (let i = 0; i < message.sorted_id.length; ++i)
                    if (!$util.isInteger(message.sorted_id[i]))
                        return "sorted_id: integer[] expected";
            }
            return null;
        };

        ReqAmuletActivityEffectSort.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityEffectSort)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityEffectSort();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.sorted_id) {
                if (!Array.isArray(object.sorted_id))
                    throw TypeError(".lq.ReqAmuletActivityEffectSort.sorted_id: array expected");
                message.sorted_id = Array(object.sorted_id.length);
                for (let i = 0; i < object.sorted_id.length; ++i)
                    message.sorted_id[i] = object.sorted_id[i] >>> 0;
            }
            return message;
        };

        ReqAmuletActivityEffectSort.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.sorted_id = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.sorted_id && message.sorted_id.length) {
                object.sorted_id = Array(message.sorted_id.length);
                for (let j = 0; j < message.sorted_id.length; ++j)
                    object.sorted_id[j] = message.sorted_id[j];
            }
            return object;
        };

        ReqAmuletActivityEffectSort.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityEffectSort.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityEffectSort";
        };

        return ReqAmuletActivityEffectSort;
    })();

    lq.ReqAmuletActivityGiveup = (function() {

        function ReqAmuletActivityGiveup(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityGiveup.prototype.activity_id = 0;

        ReqAmuletActivityGiveup.create = function create(properties) {
            return new ReqAmuletActivityGiveup(properties);
        };

        ReqAmuletActivityGiveup.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityGiveup.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityGiveup.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityGiveup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityGiveup.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityGiveup.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityGiveup.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityGiveup)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityGiveup();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityGiveup.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityGiveup.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityGiveup.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityGiveup";
        };

        return ReqAmuletActivityGiveup;
    })();

    lq.ReqAmuletActivityRefreshShop = (function() {

        function ReqAmuletActivityRefreshShop(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityRefreshShop.prototype.activity_id = 0;

        ReqAmuletActivityRefreshShop.create = function create(properties) {
            return new ReqAmuletActivityRefreshShop(properties);
        };

        ReqAmuletActivityRefreshShop.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityRefreshShop.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityRefreshShop.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityRefreshShop(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityRefreshShop.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityRefreshShop.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityRefreshShop.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityRefreshShop)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityRefreshShop();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityRefreshShop.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityRefreshShop.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityRefreshShop.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityRefreshShop";
        };

        return ReqAmuletActivityRefreshShop;
    })();

    lq.ReqAmuletActivitySelectFreeEffect = (function() {

        function ReqAmuletActivitySelectFreeEffect(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySelectFreeEffect.prototype.activity_id = 0;
        ReqAmuletActivitySelectFreeEffect.prototype.selected_id = 0;

        ReqAmuletActivitySelectFreeEffect.create = function create(properties) {
            return new ReqAmuletActivitySelectFreeEffect(properties);
        };

        ReqAmuletActivitySelectFreeEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.selected_id != null && Object.hasOwnProperty.call(message, "selected_id"))
                writer.uint32(16).uint32(message.selected_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySelectFreeEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySelectFreeEffect.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySelectFreeEffect(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selected_id = value;
                        else
                            delete message.selected_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySelectFreeEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySelectFreeEffect.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.selected_id != null && message.hasOwnProperty("selected_id"))
                if (!$util.isInteger(message.selected_id))
                    return "selected_id: integer expected";
            return null;
        };

        ReqAmuletActivitySelectFreeEffect.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySelectFreeEffect)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySelectFreeEffect();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.selected_id != null)
                if (Number(object.selected_id) !== 0)
                    message.selected_id = object.selected_id >>> 0;
            return message;
        };

        ReqAmuletActivitySelectFreeEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.selected_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.selected_id != null && message.hasOwnProperty("selected_id"))
                object.selected_id = message.selected_id;
            return object;
        };

        ReqAmuletActivitySelectFreeEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySelectFreeEffect.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySelectFreeEffect";
        };

        return ReqAmuletActivitySelectFreeEffect;
    })();

    lq.ReqAmuletActivityUpgradeShopBuff = (function() {

        function ReqAmuletActivityUpgradeShopBuff(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityUpgradeShopBuff.prototype.activity_id = 0;
        ReqAmuletActivityUpgradeShopBuff.prototype.id = 0;

        ReqAmuletActivityUpgradeShopBuff.create = function create(properties) {
            return new ReqAmuletActivityUpgradeShopBuff(properties);
        };

        ReqAmuletActivityUpgradeShopBuff.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityUpgradeShopBuff.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityUpgradeShopBuff.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityUpgradeShopBuff(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityUpgradeShopBuff.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityUpgradeShopBuff.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqAmuletActivityUpgradeShopBuff.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityUpgradeShopBuff)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityUpgradeShopBuff();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqAmuletActivityUpgradeShopBuff.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqAmuletActivityUpgradeShopBuff.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityUpgradeShopBuff.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityUpgradeShopBuff";
        };

        return ReqAmuletActivityUpgradeShopBuff;
    })();

    lq.ReqAmuletActivityEndShopping = (function() {

        function ReqAmuletActivityEndShopping(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivityEndShopping.prototype.activity_id = 0;

        ReqAmuletActivityEndShopping.create = function create(properties) {
            return new ReqAmuletActivityEndShopping(properties);
        };

        ReqAmuletActivityEndShopping.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivityEndShopping.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivityEndShopping.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivityEndShopping(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivityEndShopping.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivityEndShopping.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqAmuletActivityEndShopping.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivityEndShopping)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivityEndShopping();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqAmuletActivityEndShopping.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqAmuletActivityEndShopping.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivityEndShopping.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivityEndShopping";
        };

        return ReqAmuletActivityEndShopping;
    })();

    lq.ReqAmuletActivitySetSkillLevel = (function() {

        function ReqAmuletActivitySetSkillLevel(properties) {
            this.skill = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySetSkillLevel.prototype.activity_id = 0;
        ReqAmuletActivitySetSkillLevel.prototype.skill = $util.emptyArray;

        ReqAmuletActivitySetSkillLevel.create = function create(properties) {
            return new ReqAmuletActivitySetSkillLevel(properties);
        };

        ReqAmuletActivitySetSkillLevel.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.skill != null && message.skill.length)
                for (let i = 0; i < message.skill.length; ++i)
                    $root.lq.AmuletSkillData.encode(message.skill[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySetSkillLevel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySetSkillLevel.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySetSkillLevel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.skill && message.skill.length))
                            message.skill = [];
                        message.skill.push($root.lq.AmuletSkillData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySetSkillLevel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySetSkillLevel.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.skill != null && message.hasOwnProperty("skill")) {
                if (!Array.isArray(message.skill))
                    return "skill: array expected";
                for (let i = 0; i < message.skill.length; ++i) {
                    let error = $root.lq.AmuletSkillData.verify(message.skill[i], _depth + 1);
                    if (error)
                        return "skill." + error;
                }
            }
            return null;
        };

        ReqAmuletActivitySetSkillLevel.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySetSkillLevel)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySetSkillLevel();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.skill) {
                if (!Array.isArray(object.skill))
                    throw TypeError(".lq.ReqAmuletActivitySetSkillLevel.skill: array expected");
                message.skill = Array(object.skill.length);
                for (let i = 0; i < object.skill.length; ++i) {
                    if (typeof object.skill[i] !== "object")
                        throw TypeError(".lq.ReqAmuletActivitySetSkillLevel.skill: object expected");
                    message.skill[i] = $root.lq.AmuletSkillData.fromObject(object.skill[i], _depth + 1);
                }
            }
            return message;
        };

        ReqAmuletActivitySetSkillLevel.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.skill = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.skill && message.skill.length) {
                object.skill = Array(message.skill.length);
                for (let j = 0; j < message.skill.length; ++j)
                    object.skill[j] = $root.lq.AmuletSkillData.toObject(message.skill[j], options);
            }
            return object;
        };

        ReqAmuletActivitySetSkillLevel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySetSkillLevel.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySetSkillLevel";
        };

        return ReqAmuletActivitySetSkillLevel;
    })();

    lq.ResAmuletActivityMaintainInfo = (function() {

        function ResAmuletActivityMaintainInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAmuletActivityMaintainInfo.prototype.error = null;
        ResAmuletActivityMaintainInfo.prototype.mode = "";

        ResAmuletActivityMaintainInfo.create = function create(properties) {
            return new ResAmuletActivityMaintainInfo(properties);
        };

        ResAmuletActivityMaintainInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(18).string(message.mode);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAmuletActivityMaintainInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAmuletActivityMaintainInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAmuletActivityMaintainInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAmuletActivityMaintainInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAmuletActivityMaintainInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isString(message.mode))
                    return "mode: string expected";
            return null;
        };

        ResAmuletActivityMaintainInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAmuletActivityMaintainInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAmuletActivityMaintainInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAmuletActivityMaintainInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.mode != null)
                if (typeof object.mode !== "string" || object.mode.length)
                    message.mode = String(object.mode);
            return message;
        };

        ResAmuletActivityMaintainInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.mode = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            return object;
        };

        ResAmuletActivityMaintainInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAmuletActivityMaintainInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAmuletActivityMaintainInfo";
        };

        return ResAmuletActivityMaintainInfo;
    })();

    lq.ReqAmuletActivitySelectRewardPack = (function() {

        function ReqAmuletActivitySelectRewardPack(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySelectRewardPack.prototype.activity_id = 0;
        ReqAmuletActivitySelectRewardPack.prototype.id = 0;

        ReqAmuletActivitySelectRewardPack.create = function create(properties) {
            return new ReqAmuletActivitySelectRewardPack(properties);
        };

        ReqAmuletActivitySelectRewardPack.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySelectRewardPack.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySelectRewardPack.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySelectRewardPack(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySelectRewardPack.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySelectRewardPack.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        ReqAmuletActivitySelectRewardPack.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySelectRewardPack)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySelectRewardPack();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        ReqAmuletActivitySelectRewardPack.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        ReqAmuletActivitySelectRewardPack.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySelectRewardPack.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySelectRewardPack";
        };

        return ReqAmuletActivitySelectRewardPack;
    })();

    lq.ReqAmuletActivitySelectBookEffect = (function() {

        function ReqAmuletActivitySelectBookEffect(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAmuletActivitySelectBookEffect.prototype.activity_id = 0;
        ReqAmuletActivitySelectBookEffect.prototype.effect_id = 0;

        ReqAmuletActivitySelectBookEffect.create = function create(properties) {
            return new ReqAmuletActivitySelectBookEffect(properties);
        };

        ReqAmuletActivitySelectBookEffect.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.effect_id != null && Object.hasOwnProperty.call(message, "effect_id"))
                writer.uint32(16).uint32(message.effect_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAmuletActivitySelectBookEffect.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAmuletActivitySelectBookEffect.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAmuletActivitySelectBookEffect(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effect_id = value;
                        else
                            delete message.effect_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAmuletActivitySelectBookEffect.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAmuletActivitySelectBookEffect.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                if (!$util.isInteger(message.effect_id))
                    return "effect_id: integer expected";
            return null;
        };

        ReqAmuletActivitySelectBookEffect.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAmuletActivitySelectBookEffect)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAmuletActivitySelectBookEffect();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.effect_id != null)
                if (Number(object.effect_id) !== 0)
                    message.effect_id = object.effect_id >>> 0;
            return message;
        };

        ReqAmuletActivitySelectBookEffect.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.effect_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                object.effect_id = message.effect_id;
            return object;
        };

        ReqAmuletActivitySelectBookEffect.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAmuletActivitySelectBookEffect.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAmuletActivitySelectBookEffect";
        };

        return ReqAmuletActivitySelectBookEffect;
    })();

    lq.ReqStoryActivityUnlock = (function() {

        function ReqStoryActivityUnlock(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityUnlock.prototype.activity_id = 0;
        ReqStoryActivityUnlock.prototype.story_id = 0;

        ReqStoryActivityUnlock.create = function create(properties) {
            return new ReqStoryActivityUnlock(properties);
        };

        ReqStoryActivityUnlock.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityUnlock.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityUnlock.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityUnlock(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityUnlock.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityUnlock.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            return null;
        };

        ReqStoryActivityUnlock.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityUnlock)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityUnlock();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            return message;
        };

        ReqStoryActivityUnlock.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            return object;
        };

        ReqStoryActivityUnlock.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityUnlock.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityUnlock";
        };

        return ReqStoryActivityUnlock;
    })();

    lq.ReqStoryActivityUnlockEnding = (function() {

        function ReqStoryActivityUnlockEnding(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityUnlockEnding.prototype.activity_id = 0;
        ReqStoryActivityUnlockEnding.prototype.story_id = 0;
        ReqStoryActivityUnlockEnding.prototype.ending_id = 0;

        ReqStoryActivityUnlockEnding.create = function create(properties) {
            return new ReqStoryActivityUnlockEnding(properties);
        };

        ReqStoryActivityUnlockEnding.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(24).uint32(message.ending_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityUnlockEnding.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityUnlockEnding.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityUnlockEnding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityUnlockEnding.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityUnlockEnding.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            return null;
        };

        ReqStoryActivityUnlockEnding.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityUnlockEnding)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityUnlockEnding();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            return message;
        };

        ReqStoryActivityUnlockEnding.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
                object.ending_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            return object;
        };

        ReqStoryActivityUnlockEnding.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityUnlockEnding.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityUnlockEnding";
        };

        return ReqStoryActivityUnlockEnding;
    })();

    lq.ReqStoryActivityReceiveEndingReward = (function() {

        function ReqStoryActivityReceiveEndingReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityReceiveEndingReward.prototype.activity_id = 0;
        ReqStoryActivityReceiveEndingReward.prototype.story_id = 0;
        ReqStoryActivityReceiveEndingReward.prototype.ending_id = 0;

        ReqStoryActivityReceiveEndingReward.create = function create(properties) {
            return new ReqStoryActivityReceiveEndingReward(properties);
        };

        ReqStoryActivityReceiveEndingReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(24).uint32(message.ending_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityReceiveEndingReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityReceiveEndingReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityReceiveEndingReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityReceiveEndingReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityReceiveEndingReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            return null;
        };

        ReqStoryActivityReceiveEndingReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityReceiveEndingReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityReceiveEndingReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            return message;
        };

        ReqStoryActivityReceiveEndingReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
                object.ending_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            return object;
        };

        ReqStoryActivityReceiveEndingReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityReceiveEndingReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityReceiveEndingReward";
        };

        return ReqStoryActivityReceiveEndingReward;
    })();

    lq.ResStoryReward = (function() {

        function ResStoryReward(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResStoryReward.prototype.error = null;
        ResStoryReward.prototype.reward_items = $util.emptyArray;

        ResStoryReward.create = function create(properties) {
            return new ResStoryReward(properties);
        };

        ResStoryReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResStoryReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResStoryReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResStoryReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResStoryReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResStoryReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResStoryReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResStoryReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResStoryReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResStoryReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResStoryReward.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResStoryReward.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResStoryReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResStoryReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResStoryReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResStoryReward";
        };

        return ResStoryReward;
    })();

    lq.ReqStoryActivityReceiveFinishReward = (function() {

        function ReqStoryActivityReceiveFinishReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityReceiveFinishReward.prototype.activity_id = 0;
        ReqStoryActivityReceiveFinishReward.prototype.story_id = 0;

        ReqStoryActivityReceiveFinishReward.create = function create(properties) {
            return new ReqStoryActivityReceiveFinishReward(properties);
        };

        ReqStoryActivityReceiveFinishReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityReceiveFinishReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityReceiveFinishReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityReceiveFinishReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityReceiveFinishReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityReceiveFinishReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            return null;
        };

        ReqStoryActivityReceiveFinishReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityReceiveFinishReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityReceiveFinishReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            return message;
        };

        ReqStoryActivityReceiveFinishReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            return object;
        };

        ReqStoryActivityReceiveFinishReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityReceiveFinishReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityReceiveFinishReward";
        };

        return ReqStoryActivityReceiveFinishReward;
    })();

    lq.ReqStoryActivityReceiveAllFinishReward = (function() {

        function ReqStoryActivityReceiveAllFinishReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityReceiveAllFinishReward.prototype.activity_id = 0;
        ReqStoryActivityReceiveAllFinishReward.prototype.story_id = 0;

        ReqStoryActivityReceiveAllFinishReward.create = function create(properties) {
            return new ReqStoryActivityReceiveAllFinishReward(properties);
        };

        ReqStoryActivityReceiveAllFinishReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityReceiveAllFinishReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityReceiveAllFinishReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityReceiveAllFinishReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityReceiveAllFinishReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityReceiveAllFinishReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            return null;
        };

        ReqStoryActivityReceiveAllFinishReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityReceiveAllFinishReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityReceiveAllFinishReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            return message;
        };

        ReqStoryActivityReceiveAllFinishReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            return object;
        };

        ReqStoryActivityReceiveAllFinishReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityReceiveAllFinishReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityReceiveAllFinishReward";
        };

        return ReqStoryActivityReceiveAllFinishReward;
    })();

    lq.ReqStoryActivityUnlockEndingAndReceive = (function() {

        function ReqStoryActivityUnlockEndingAndReceive(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqStoryActivityUnlockEndingAndReceive.prototype.activity_id = 0;
        ReqStoryActivityUnlockEndingAndReceive.prototype.story_id = 0;
        ReqStoryActivityUnlockEndingAndReceive.prototype.ending_id = 0;

        ReqStoryActivityUnlockEndingAndReceive.create = function create(properties) {
            return new ReqStoryActivityUnlockEndingAndReceive(properties);
        };

        ReqStoryActivityUnlockEndingAndReceive.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.story_id != null && Object.hasOwnProperty.call(message, "story_id"))
                writer.uint32(16).uint32(message.story_id);
            if (message.ending_id != null && Object.hasOwnProperty.call(message, "ending_id"))
                writer.uint32(24).uint32(message.ending_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqStoryActivityUnlockEndingAndReceive.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqStoryActivityUnlockEndingAndReceive.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqStoryActivityUnlockEndingAndReceive(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.story_id = value;
                        else
                            delete message.story_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ending_id = value;
                        else
                            delete message.ending_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqStoryActivityUnlockEndingAndReceive.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqStoryActivityUnlockEndingAndReceive.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                if (!$util.isInteger(message.story_id))
                    return "story_id: integer expected";
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                if (!$util.isInteger(message.ending_id))
                    return "ending_id: integer expected";
            return null;
        };

        ReqStoryActivityUnlockEndingAndReceive.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqStoryActivityUnlockEndingAndReceive)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqStoryActivityUnlockEndingAndReceive();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.story_id != null)
                if (Number(object.story_id) !== 0)
                    message.story_id = object.story_id >>> 0;
            if (object.ending_id != null)
                if (Number(object.ending_id) !== 0)
                    message.ending_id = object.ending_id >>> 0;
            return message;
        };

        ReqStoryActivityUnlockEndingAndReceive.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.story_id = 0;
                object.ending_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.story_id != null && message.hasOwnProperty("story_id"))
                object.story_id = message.story_id;
            if (message.ending_id != null && message.hasOwnProperty("ending_id"))
                object.ending_id = message.ending_id;
            return object;
        };

        ReqStoryActivityUnlockEndingAndReceive.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqStoryActivityUnlockEndingAndReceive.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqStoryActivityUnlockEndingAndReceive";
        };

        return ReqStoryActivityUnlockEndingAndReceive;
    })();

    lq.ResStoryActivityUnlockEndingAndReceive = (function() {

        function ResStoryActivityUnlockEndingAndReceive(properties) {
            this.ending_reward = [];
            this.finish_reward = [];
            this.all_finish_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResStoryActivityUnlockEndingAndReceive.prototype.error = null;
        ResStoryActivityUnlockEndingAndReceive.prototype.ending_reward = $util.emptyArray;
        ResStoryActivityUnlockEndingAndReceive.prototype.finish_reward = $util.emptyArray;
        ResStoryActivityUnlockEndingAndReceive.prototype.all_finish_reward = $util.emptyArray;

        ResStoryActivityUnlockEndingAndReceive.create = function create(properties) {
            return new ResStoryActivityUnlockEndingAndReceive(properties);
        };

        ResStoryActivityUnlockEndingAndReceive.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.ending_reward != null && message.ending_reward.length)
                for (let i = 0; i < message.ending_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.ending_reward[i], writer.uint32(18).fork()).ldelim();
            if (message.finish_reward != null && message.finish_reward.length)
                for (let i = 0; i < message.finish_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.finish_reward[i], writer.uint32(26).fork()).ldelim();
            if (message.all_finish_reward != null && message.all_finish_reward.length)
                for (let i = 0; i < message.all_finish_reward.length; ++i)
                    $root.lq.ExecuteReward.encode(message.all_finish_reward[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResStoryActivityUnlockEndingAndReceive.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResStoryActivityUnlockEndingAndReceive.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResStoryActivityUnlockEndingAndReceive(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ending_reward && message.ending_reward.length))
                            message.ending_reward = [];
                        message.ending_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.finish_reward && message.finish_reward.length))
                            message.finish_reward = [];
                        message.finish_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.all_finish_reward && message.all_finish_reward.length))
                            message.all_finish_reward = [];
                        message.all_finish_reward.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResStoryActivityUnlockEndingAndReceive.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResStoryActivityUnlockEndingAndReceive.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.ending_reward != null && message.hasOwnProperty("ending_reward")) {
                if (!Array.isArray(message.ending_reward))
                    return "ending_reward: array expected";
                for (let i = 0; i < message.ending_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.ending_reward[i], _depth + 1);
                    if (error)
                        return "ending_reward." + error;
                }
            }
            if (message.finish_reward != null && message.hasOwnProperty("finish_reward")) {
                if (!Array.isArray(message.finish_reward))
                    return "finish_reward: array expected";
                for (let i = 0; i < message.finish_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.finish_reward[i], _depth + 1);
                    if (error)
                        return "finish_reward." + error;
                }
            }
            if (message.all_finish_reward != null && message.hasOwnProperty("all_finish_reward")) {
                if (!Array.isArray(message.all_finish_reward))
                    return "all_finish_reward: array expected";
                for (let i = 0; i < message.all_finish_reward.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.all_finish_reward[i], _depth + 1);
                    if (error)
                        return "all_finish_reward." + error;
                }
            }
            return null;
        };

        ResStoryActivityUnlockEndingAndReceive.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResStoryActivityUnlockEndingAndReceive)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResStoryActivityUnlockEndingAndReceive();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.ending_reward) {
                if (!Array.isArray(object.ending_reward))
                    throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.ending_reward: array expected");
                message.ending_reward = Array(object.ending_reward.length);
                for (let i = 0; i < object.ending_reward.length; ++i) {
                    if (typeof object.ending_reward[i] !== "object")
                        throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.ending_reward: object expected");
                    message.ending_reward[i] = $root.lq.ExecuteReward.fromObject(object.ending_reward[i], _depth + 1);
                }
            }
            if (object.finish_reward) {
                if (!Array.isArray(object.finish_reward))
                    throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.finish_reward: array expected");
                message.finish_reward = Array(object.finish_reward.length);
                for (let i = 0; i < object.finish_reward.length; ++i) {
                    if (typeof object.finish_reward[i] !== "object")
                        throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.finish_reward: object expected");
                    message.finish_reward[i] = $root.lq.ExecuteReward.fromObject(object.finish_reward[i], _depth + 1);
                }
            }
            if (object.all_finish_reward) {
                if (!Array.isArray(object.all_finish_reward))
                    throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.all_finish_reward: array expected");
                message.all_finish_reward = Array(object.all_finish_reward.length);
                for (let i = 0; i < object.all_finish_reward.length; ++i) {
                    if (typeof object.all_finish_reward[i] !== "object")
                        throw TypeError(".lq.ResStoryActivityUnlockEndingAndReceive.all_finish_reward: object expected");
                    message.all_finish_reward[i] = $root.lq.ExecuteReward.fromObject(object.all_finish_reward[i], _depth + 1);
                }
            }
            return message;
        };

        ResStoryActivityUnlockEndingAndReceive.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.ending_reward = [];
                object.finish_reward = [];
                object.all_finish_reward = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.ending_reward && message.ending_reward.length) {
                object.ending_reward = Array(message.ending_reward.length);
                for (let j = 0; j < message.ending_reward.length; ++j)
                    object.ending_reward[j] = $root.lq.ExecuteReward.toObject(message.ending_reward[j], options);
            }
            if (message.finish_reward && message.finish_reward.length) {
                object.finish_reward = Array(message.finish_reward.length);
                for (let j = 0; j < message.finish_reward.length; ++j)
                    object.finish_reward[j] = $root.lq.ExecuteReward.toObject(message.finish_reward[j], options);
            }
            if (message.all_finish_reward && message.all_finish_reward.length) {
                object.all_finish_reward = Array(message.all_finish_reward.length);
                for (let j = 0; j < message.all_finish_reward.length; ++j)
                    object.all_finish_reward[j] = $root.lq.ExecuteReward.toObject(message.all_finish_reward[j], options);
            }
            return object;
        };

        ResStoryActivityUnlockEndingAndReceive.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResStoryActivityUnlockEndingAndReceive.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResStoryActivityUnlockEndingAndReceive";
        };

        return ResStoryActivityUnlockEndingAndReceive;
    })();

    lq.ReqFetchActivityRank = (function() {

        function ReqFetchActivityRank(properties) {
            this.account_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchActivityRank.prototype.activity_id = 0;
        ReqFetchActivityRank.prototype.account_list = $util.emptyArray;

        ReqFetchActivityRank.create = function create(properties) {
            return new ReqFetchActivityRank(properties);
        };

        ReqFetchActivityRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.account_list != null && message.account_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.account_list.length; ++i)
                    writer.uint32(message.account_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchActivityRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchActivityRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchActivityRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.account_list && message.account_list.length))
                                message.account_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.account_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.account_list && message.account_list.length))
                            message.account_list = [];
                        message.account_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchActivityRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchActivityRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.account_list != null && message.hasOwnProperty("account_list")) {
                if (!Array.isArray(message.account_list))
                    return "account_list: array expected";
                for (let i = 0; i < message.account_list.length; ++i)
                    if (!$util.isInteger(message.account_list[i]))
                        return "account_list: integer[] expected";
            }
            return null;
        };

        ReqFetchActivityRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchActivityRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchActivityRank();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.account_list) {
                if (!Array.isArray(object.account_list))
                    throw TypeError(".lq.ReqFetchActivityRank.account_list: array expected");
                message.account_list = Array(object.account_list.length);
                for (let i = 0; i < object.account_list.length; ++i)
                    message.account_list[i] = object.account_list[i] >>> 0;
            }
            return message;
        };

        ReqFetchActivityRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.account_list = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.account_list && message.account_list.length) {
                object.account_list = Array(message.account_list.length);
                for (let j = 0; j < message.account_list.length; ++j)
                    object.account_list[j] = message.account_list[j];
            }
            return object;
        };

        ReqFetchActivityRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchActivityRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchActivityRank";
        };

        return ReqFetchActivityRank;
    })();

    lq.ResFetchActivityRank = (function() {

        function ResFetchActivityRank(properties) {
            this.items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchActivityRank.prototype.error = null;
        ResFetchActivityRank.prototype.items = $util.emptyArray;
        ResFetchActivityRank.prototype.self = null;

        ResFetchActivityRank.create = function create(properties) {
            return new ResFetchActivityRank(properties);
        };

        ResFetchActivityRank.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.items != null && message.items.length)
                for (let i = 0; i < message.items.length; ++i)
                    $root.lq.ResFetchActivityRank.ActivityRankItem.encode(message.items[i], writer.uint32(34).fork()).ldelim();
            if (message.self != null && Object.hasOwnProperty.call(message, "self"))
                $root.lq.ResFetchActivityRank.ActivityRankItem.encode(message.self, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchActivityRank.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchActivityRank.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchActivityRank(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.items && message.items.length))
                            message.items = [];
                        message.items.push($root.lq.ResFetchActivityRank.ActivityRankItem.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.self = $root.lq.ResFetchActivityRank.ActivityRankItem.decode(reader, reader.uint32(), undefined, _depth + 1, message.self);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchActivityRank.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchActivityRank.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.items != null && message.hasOwnProperty("items")) {
                if (!Array.isArray(message.items))
                    return "items: array expected";
                for (let i = 0; i < message.items.length; ++i) {
                    let error = $root.lq.ResFetchActivityRank.ActivityRankItem.verify(message.items[i], _depth + 1);
                    if (error)
                        return "items." + error;
                }
            }
            if (message.self != null && message.hasOwnProperty("self")) {
                let error = $root.lq.ResFetchActivityRank.ActivityRankItem.verify(message.self, _depth + 1);
                if (error)
                    return "self." + error;
            }
            return null;
        };

        ResFetchActivityRank.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchActivityRank)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchActivityRank();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchActivityRank.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.items) {
                if (!Array.isArray(object.items))
                    throw TypeError(".lq.ResFetchActivityRank.items: array expected");
                message.items = Array(object.items.length);
                for (let i = 0; i < object.items.length; ++i) {
                    if (typeof object.items[i] !== "object")
                        throw TypeError(".lq.ResFetchActivityRank.items: object expected");
                    message.items[i] = $root.lq.ResFetchActivityRank.ActivityRankItem.fromObject(object.items[i], _depth + 1);
                }
            }
            if (object.self != null) {
                if (typeof object.self !== "object")
                    throw TypeError(".lq.ResFetchActivityRank.self: object expected");
                message.self = $root.lq.ResFetchActivityRank.ActivityRankItem.fromObject(object.self, _depth + 1);
            }
            return message;
        };

        ResFetchActivityRank.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.items = [];
            if (options.defaults) {
                object.error = null;
                object.self = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.items && message.items.length) {
                object.items = Array(message.items.length);
                for (let j = 0; j < message.items.length; ++j)
                    object.items[j] = $root.lq.ResFetchActivityRank.ActivityRankItem.toObject(message.items[j], options);
            }
            if (message.self != null && message.hasOwnProperty("self"))
                object.self = $root.lq.ResFetchActivityRank.ActivityRankItem.toObject(message.self, options);
            return object;
        };

        ResFetchActivityRank.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchActivityRank.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchActivityRank";
        };

        ResFetchActivityRank.ActivityRankItem = (function() {

            function ActivityRankItem(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityRankItem.prototype.account_id = 0;
            ActivityRankItem.prototype.score = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
            ActivityRankItem.prototype.data = "";
            ActivityRankItem.prototype.rank = 0;

            ActivityRankItem.create = function create(properties) {
                return new ActivityRankItem(properties);
            };

            ActivityRankItem.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                    writer.uint32(16).uint64(message.score);
                if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                    writer.uint32(26).string(message.data);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(32).uint32(message.rank);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityRankItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityRankItem.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchActivityRank.ActivityRankItem(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                                message.score = value;
                            else
                                delete message.score;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.data = value;
                            else
                                delete message.data;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityRankItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityRankItem.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.score != null && message.hasOwnProperty("score"))
                    if (!$util.isInteger(message.score) && !(message.score && $util.isInteger(message.score.low) && $util.isInteger(message.score.high)))
                        return "score: integer|Long expected";
                if (message.data != null && message.hasOwnProperty("data"))
                    if (!$util.isString(message.data))
                        return "data: string expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                return null;
            };

            ActivityRankItem.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchActivityRank.ActivityRankItem)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchActivityRank.ActivityRankItem();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.score != null)
                    if (typeof object.score === "object" ? object.score.low || object.score.high : Number(object.score) !== 0)
                        if ($util.Long)
                            (message.score = $util.Long.fromValue(object.score)).unsigned = true;
                        else if (typeof object.score === "string")
                            message.score = parseInt(object.score, 10);
                        else if (typeof object.score === "number")
                            message.score = object.score;
                        else if (typeof object.score === "object")
                            message.score = new $util.LongBits(object.score.low >>> 0, object.score.high >>> 0).toNumber(true);
                if (object.data != null)
                    if (typeof object.data !== "string" || object.data.length)
                        message.data = String(object.data);
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                return message;
            };

            ActivityRankItem.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, true);
                        object.score = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.score = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.data = "";
                    object.rank = 0;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.score != null && message.hasOwnProperty("score"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.score = typeof message.score === "number" ? BigInt(message.score) : $util.Long.fromBits(message.score.low >>> 0, message.score.high >>> 0, true).toBigInt();
                    else if (typeof message.score === "number")
                        object.score = options.longs === String ? String(message.score) : message.score;
                    else
                        object.score = options.longs === String ? $util.Long.prototype.toString.call(message.score) : options.longs === Number ? new $util.LongBits(message.score.low >>> 0, message.score.high >>> 0).toNumber(true) : message.score;
                if (message.data != null && message.hasOwnProperty("data"))
                    object.data = message.data;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                return object;
            };

            ActivityRankItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityRankItem.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchActivityRank.ActivityRankItem";
            };

            return ActivityRankItem;
        })();

        return ResFetchActivityRank;
    })();

    lq.ReqFetchQuestionnaireList = (function() {

        function ReqFetchQuestionnaireList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchQuestionnaireList.prototype.lang = "";
        ReqFetchQuestionnaireList.prototype.channel = "";

        ReqFetchQuestionnaireList.create = function create(properties) {
            return new ReqFetchQuestionnaireList(properties);
        };

        ReqFetchQuestionnaireList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.channel != null && Object.hasOwnProperty.call(message, "channel"))
                writer.uint32(18).string(message.channel);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchQuestionnaireList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchQuestionnaireList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchQuestionnaireList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.channel = value;
                        else
                            delete message.channel;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchQuestionnaireList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchQuestionnaireList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            if (message.channel != null && message.hasOwnProperty("channel"))
                if (!$util.isString(message.channel))
                    return "channel: string expected";
            return null;
        };

        ReqFetchQuestionnaireList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchQuestionnaireList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchQuestionnaireList();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            if (object.channel != null)
                if (typeof object.channel !== "string" || object.channel.length)
                    message.channel = String(object.channel);
            return message;
        };

        ReqFetchQuestionnaireList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.lang = "";
                object.channel = "";
            }
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            if (message.channel != null && message.hasOwnProperty("channel"))
                object.channel = message.channel;
            return object;
        };

        ReqFetchQuestionnaireList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchQuestionnaireList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchQuestionnaireList";
        };

        return ReqFetchQuestionnaireList;
    })();

    lq.ResFetchQuestionnaireList = (function() {

        function ResFetchQuestionnaireList(properties) {
            this.list = [];
            this.finished_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchQuestionnaireList.prototype.error = null;
        ResFetchQuestionnaireList.prototype.list = $util.emptyArray;
        ResFetchQuestionnaireList.prototype.finished_list = $util.emptyArray;

        ResFetchQuestionnaireList.create = function create(properties) {
            return new ResFetchQuestionnaireList(properties);
        };

        ResFetchQuestionnaireList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.list != null && message.list.length)
                for (let i = 0; i < message.list.length; ++i)
                    $root.lq.QuestionnaireBrief.encode(message.list[i], writer.uint32(18).fork()).ldelim();
            if (message.finished_list != null && message.finished_list.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.finished_list.length; ++i)
                    writer.uint32(message.finished_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchQuestionnaireList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchQuestionnaireList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchQuestionnaireList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.list && message.list.length))
                            message.list = [];
                        message.list.push($root.lq.QuestionnaireBrief.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.finished_list && message.finished_list.length))
                                message.finished_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.finished_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.finished_list && message.finished_list.length))
                            message.finished_list = [];
                        message.finished_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchQuestionnaireList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchQuestionnaireList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.list != null && message.hasOwnProperty("list")) {
                if (!Array.isArray(message.list))
                    return "list: array expected";
                for (let i = 0; i < message.list.length; ++i) {
                    let error = $root.lq.QuestionnaireBrief.verify(message.list[i], _depth + 1);
                    if (error)
                        return "list." + error;
                }
            }
            if (message.finished_list != null && message.hasOwnProperty("finished_list")) {
                if (!Array.isArray(message.finished_list))
                    return "finished_list: array expected";
                for (let i = 0; i < message.finished_list.length; ++i)
                    if (!$util.isInteger(message.finished_list[i]))
                        return "finished_list: integer[] expected";
            }
            return null;
        };

        ResFetchQuestionnaireList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchQuestionnaireList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchQuestionnaireList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchQuestionnaireList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.list) {
                if (!Array.isArray(object.list))
                    throw TypeError(".lq.ResFetchQuestionnaireList.list: array expected");
                message.list = Array(object.list.length);
                for (let i = 0; i < object.list.length; ++i) {
                    if (typeof object.list[i] !== "object")
                        throw TypeError(".lq.ResFetchQuestionnaireList.list: object expected");
                    message.list[i] = $root.lq.QuestionnaireBrief.fromObject(object.list[i], _depth + 1);
                }
            }
            if (object.finished_list) {
                if (!Array.isArray(object.finished_list))
                    throw TypeError(".lq.ResFetchQuestionnaireList.finished_list: array expected");
                message.finished_list = Array(object.finished_list.length);
                for (let i = 0; i < object.finished_list.length; ++i)
                    message.finished_list[i] = object.finished_list[i] >>> 0;
            }
            return message;
        };

        ResFetchQuestionnaireList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.list = [];
                object.finished_list = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.list && message.list.length) {
                object.list = Array(message.list.length);
                for (let j = 0; j < message.list.length; ++j)
                    object.list[j] = $root.lq.QuestionnaireBrief.toObject(message.list[j], options);
            }
            if (message.finished_list && message.finished_list.length) {
                object.finished_list = Array(message.finished_list.length);
                for (let j = 0; j < message.finished_list.length; ++j)
                    object.finished_list[j] = message.finished_list[j];
            }
            return object;
        };

        ResFetchQuestionnaireList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchQuestionnaireList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchQuestionnaireList";
        };

        return ResFetchQuestionnaireList;
    })();

    lq.ReqFetchQuestionnaireDetail = (function() {

        function ReqFetchQuestionnaireDetail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchQuestionnaireDetail.prototype.id = 0;
        ReqFetchQuestionnaireDetail.prototype.lang = "";
        ReqFetchQuestionnaireDetail.prototype.channel = "";

        ReqFetchQuestionnaireDetail.create = function create(properties) {
            return new ReqFetchQuestionnaireDetail(properties);
        };

        ReqFetchQuestionnaireDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(18).string(message.lang);
            if (message.channel != null && Object.hasOwnProperty.call(message, "channel"))
                writer.uint32(26).string(message.channel);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchQuestionnaireDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchQuestionnaireDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchQuestionnaireDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.channel = value;
                        else
                            delete message.channel;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchQuestionnaireDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchQuestionnaireDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            if (message.channel != null && message.hasOwnProperty("channel"))
                if (!$util.isString(message.channel))
                    return "channel: string expected";
            return null;
        };

        ReqFetchQuestionnaireDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchQuestionnaireDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchQuestionnaireDetail();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            if (object.channel != null)
                if (typeof object.channel !== "string" || object.channel.length)
                    message.channel = String(object.channel);
            return message;
        };

        ReqFetchQuestionnaireDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.lang = "";
                object.channel = "";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            if (message.channel != null && message.hasOwnProperty("channel"))
                object.channel = message.channel;
            return object;
        };

        ReqFetchQuestionnaireDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchQuestionnaireDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchQuestionnaireDetail";
        };

        return ReqFetchQuestionnaireDetail;
    })();

    lq.ResFetchQuestionnaireDetail = (function() {

        function ResFetchQuestionnaireDetail(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchQuestionnaireDetail.prototype.error = null;
        ResFetchQuestionnaireDetail.prototype.detail = null;

        ResFetchQuestionnaireDetail.create = function create(properties) {
            return new ResFetchQuestionnaireDetail(properties);
        };

        ResFetchQuestionnaireDetail.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.detail != null && Object.hasOwnProperty.call(message, "detail"))
                $root.lq.QuestionnaireDetail.encode(message.detail, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchQuestionnaireDetail.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchQuestionnaireDetail.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchQuestionnaireDetail(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.detail = $root.lq.QuestionnaireDetail.decode(reader, reader.uint32(), undefined, _depth + 1, message.detail);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchQuestionnaireDetail.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchQuestionnaireDetail.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.detail != null && message.hasOwnProperty("detail")) {
                let error = $root.lq.QuestionnaireDetail.verify(message.detail, _depth + 1);
                if (error)
                    return "detail." + error;
            }
            return null;
        };

        ResFetchQuestionnaireDetail.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchQuestionnaireDetail)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchQuestionnaireDetail();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchQuestionnaireDetail.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.detail != null) {
                if (typeof object.detail !== "object")
                    throw TypeError(".lq.ResFetchQuestionnaireDetail.detail: object expected");
                message.detail = $root.lq.QuestionnaireDetail.fromObject(object.detail, _depth + 1);
            }
            return message;
        };

        ResFetchQuestionnaireDetail.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.detail = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.detail != null && message.hasOwnProperty("detail"))
                object.detail = $root.lq.QuestionnaireDetail.toObject(message.detail, options);
            return object;
        };

        ResFetchQuestionnaireDetail.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchQuestionnaireDetail.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchQuestionnaireDetail";
        };

        return ResFetchQuestionnaireDetail;
    })();

    lq.ReqSetVerifiedHidden = (function() {

        function ReqSetVerifiedHidden(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetVerifiedHidden.prototype.verified_hidden = 0;

        ReqSetVerifiedHidden.create = function create(properties) {
            return new ReqSetVerifiedHidden(properties);
        };

        ReqSetVerifiedHidden.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.verified_hidden != null && Object.hasOwnProperty.call(message, "verified_hidden"))
                writer.uint32(8).uint32(message.verified_hidden);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetVerifiedHidden.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetVerifiedHidden.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetVerifiedHidden(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.verified_hidden = value;
                        else
                            delete message.verified_hidden;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetVerifiedHidden.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetVerifiedHidden.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.verified_hidden != null && message.hasOwnProperty("verified_hidden"))
                if (!$util.isInteger(message.verified_hidden))
                    return "verified_hidden: integer expected";
            return null;
        };

        ReqSetVerifiedHidden.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetVerifiedHidden)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetVerifiedHidden();
            if (object.verified_hidden != null)
                if (Number(object.verified_hidden) !== 0)
                    message.verified_hidden = object.verified_hidden >>> 0;
            return message;
        };

        ReqSetVerifiedHidden.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.verified_hidden = 0;
            if (message.verified_hidden != null && message.hasOwnProperty("verified_hidden"))
                object.verified_hidden = message.verified_hidden;
            return object;
        };

        ReqSetVerifiedHidden.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetVerifiedHidden.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetVerifiedHidden";
        };

        return ReqSetVerifiedHidden;
    })();

    lq.ReqSubmitQuestionnaire = (function() {

        function ReqSubmitQuestionnaire(properties) {
            this.answers = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSubmitQuestionnaire.prototype.questionnaire_id = 0;
        ReqSubmitQuestionnaire.prototype.questionnaire_version_id = 0;
        ReqSubmitQuestionnaire.prototype.answers = $util.emptyArray;
        ReqSubmitQuestionnaire.prototype.open_time = 0;
        ReqSubmitQuestionnaire.prototype.finish_time = 0;
        ReqSubmitQuestionnaire.prototype.client = "";

        ReqSubmitQuestionnaire.create = function create(properties) {
            return new ReqSubmitQuestionnaire(properties);
        };

        ReqSubmitQuestionnaire.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.questionnaire_id != null && Object.hasOwnProperty.call(message, "questionnaire_id"))
                writer.uint32(8).uint32(message.questionnaire_id);
            if (message.questionnaire_version_id != null && Object.hasOwnProperty.call(message, "questionnaire_version_id"))
                writer.uint32(16).uint32(message.questionnaire_version_id);
            if (message.answers != null && message.answers.length)
                for (let i = 0; i < message.answers.length; ++i)
                    $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.encode(message.answers[i], writer.uint32(26).fork()).ldelim();
            if (message.open_time != null && Object.hasOwnProperty.call(message, "open_time"))
                writer.uint32(32).uint32(message.open_time);
            if (message.finish_time != null && Object.hasOwnProperty.call(message, "finish_time"))
                writer.uint32(40).uint32(message.finish_time);
            if (message.client != null && Object.hasOwnProperty.call(message, "client"))
                writer.uint32(50).string(message.client);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSubmitQuestionnaire.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSubmitQuestionnaire.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSubmitQuestionnaire(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.questionnaire_id = value;
                        else
                            delete message.questionnaire_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.questionnaire_version_id = value;
                        else
                            delete message.questionnaire_version_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.answers && message.answers.length))
                            message.answers = [];
                        message.answers.push($root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_time = value;
                        else
                            delete message.open_time;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.finish_time = value;
                        else
                            delete message.finish_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.client = value;
                        else
                            delete message.client;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSubmitQuestionnaire.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSubmitQuestionnaire.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.questionnaire_id != null && message.hasOwnProperty("questionnaire_id"))
                if (!$util.isInteger(message.questionnaire_id))
                    return "questionnaire_id: integer expected";
            if (message.questionnaire_version_id != null && message.hasOwnProperty("questionnaire_version_id"))
                if (!$util.isInteger(message.questionnaire_version_id))
                    return "questionnaire_version_id: integer expected";
            if (message.answers != null && message.hasOwnProperty("answers")) {
                if (!Array.isArray(message.answers))
                    return "answers: array expected";
                for (let i = 0; i < message.answers.length; ++i) {
                    let error = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.verify(message.answers[i], _depth + 1);
                    if (error)
                        return "answers." + error;
                }
            }
            if (message.open_time != null && message.hasOwnProperty("open_time"))
                if (!$util.isInteger(message.open_time))
                    return "open_time: integer expected";
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                if (!$util.isInteger(message.finish_time))
                    return "finish_time: integer expected";
            if (message.client != null && message.hasOwnProperty("client"))
                if (!$util.isString(message.client))
                    return "client: string expected";
            return null;
        };

        ReqSubmitQuestionnaire.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSubmitQuestionnaire)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSubmitQuestionnaire();
            if (object.questionnaire_id != null)
                if (Number(object.questionnaire_id) !== 0)
                    message.questionnaire_id = object.questionnaire_id >>> 0;
            if (object.questionnaire_version_id != null)
                if (Number(object.questionnaire_version_id) !== 0)
                    message.questionnaire_version_id = object.questionnaire_version_id >>> 0;
            if (object.answers) {
                if (!Array.isArray(object.answers))
                    throw TypeError(".lq.ReqSubmitQuestionnaire.answers: array expected");
                message.answers = Array(object.answers.length);
                for (let i = 0; i < object.answers.length; ++i) {
                    if (typeof object.answers[i] !== "object")
                        throw TypeError(".lq.ReqSubmitQuestionnaire.answers: object expected");
                    message.answers[i] = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.fromObject(object.answers[i], _depth + 1);
                }
            }
            if (object.open_time != null)
                if (Number(object.open_time) !== 0)
                    message.open_time = object.open_time >>> 0;
            if (object.finish_time != null)
                if (Number(object.finish_time) !== 0)
                    message.finish_time = object.finish_time >>> 0;
            if (object.client != null)
                if (typeof object.client !== "string" || object.client.length)
                    message.client = String(object.client);
            return message;
        };

        ReqSubmitQuestionnaire.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.answers = [];
            if (options.defaults) {
                object.questionnaire_id = 0;
                object.questionnaire_version_id = 0;
                object.open_time = 0;
                object.finish_time = 0;
                object.client = "";
            }
            if (message.questionnaire_id != null && message.hasOwnProperty("questionnaire_id"))
                object.questionnaire_id = message.questionnaire_id;
            if (message.questionnaire_version_id != null && message.hasOwnProperty("questionnaire_version_id"))
                object.questionnaire_version_id = message.questionnaire_version_id;
            if (message.answers && message.answers.length) {
                object.answers = Array(message.answers.length);
                for (let j = 0; j < message.answers.length; ++j)
                    object.answers[j] = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.toObject(message.answers[j], options);
            }
            if (message.open_time != null && message.hasOwnProperty("open_time"))
                object.open_time = message.open_time;
            if (message.finish_time != null && message.hasOwnProperty("finish_time"))
                object.finish_time = message.finish_time;
            if (message.client != null && message.hasOwnProperty("client"))
                object.client = message.client;
            return object;
        };

        ReqSubmitQuestionnaire.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSubmitQuestionnaire.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSubmitQuestionnaire";
        };

        ReqSubmitQuestionnaire.QuestionnaireAnswer = (function() {

            function QuestionnaireAnswer(properties) {
                this.values = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            QuestionnaireAnswer.prototype.question_id = 0;
            QuestionnaireAnswer.prototype.values = $util.emptyArray;

            QuestionnaireAnswer.create = function create(properties) {
                return new QuestionnaireAnswer(properties);
            };

            QuestionnaireAnswer.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.question_id != null && Object.hasOwnProperty.call(message, "question_id"))
                    writer.uint32(8).uint32(message.question_id);
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue.encode(message.values[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            QuestionnaireAnswer.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            QuestionnaireAnswer.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.question_id = value;
                            else
                                delete message.question_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            QuestionnaireAnswer.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            QuestionnaireAnswer.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.question_id != null && message.hasOwnProperty("question_id"))
                    if (!$util.isInteger(message.question_id))
                        return "question_id: integer expected";
                if (message.values != null && message.hasOwnProperty("values")) {
                    if (!Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue.verify(message.values[i], _depth + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            QuestionnaireAnswer.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer();
                if (object.question_id != null)
                    if (Number(object.question_id) !== 0)
                        message.question_id = object.question_id >>> 0;
                if (object.values) {
                    if (!Array.isArray(object.values))
                        throw TypeError(".lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.values: array expected");
                    message.values = Array(object.values.length);
                    for (let i = 0; i < object.values.length; ++i) {
                        if (typeof object.values[i] !== "object")
                            throw TypeError(".lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.values: object expected");
                        message.values[i] = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue.fromObject(object.values[i], _depth + 1);
                    }
                }
                return message;
            };

            QuestionnaireAnswer.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (options.defaults)
                    object.question_id = 0;
                if (message.question_id != null && message.hasOwnProperty("question_id"))
                    object.question_id = message.question_id;
                if (message.values && message.values.length) {
                    object.values = Array(message.values.length);
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue.toObject(message.values[j], options);
                }
                return object;
            };

            QuestionnaireAnswer.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuestionnaireAnswer.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqSubmitQuestionnaire.QuestionnaireAnswer";
            };

            QuestionnaireAnswer.QuestionnaireAnswerValue = (function() {

                function QuestionnaireAnswerValue(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                QuestionnaireAnswerValue.prototype.value = "";
                QuestionnaireAnswerValue.prototype.custom_input = "";

                QuestionnaireAnswerValue.create = function create(properties) {
                    return new QuestionnaireAnswerValue(properties);
                };

                QuestionnaireAnswerValue.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                        writer.uint32(10).string(message.value);
                    if (message.custom_input != null && Object.hasOwnProperty.call(message, "custom_input"))
                        writer.uint32(18).string(message.custom_input);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                QuestionnaireAnswerValue.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                QuestionnaireAnswerValue.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.value = value;
                                else
                                    delete message.value;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.custom_input = value;
                                else
                                    delete message.custom_input;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                QuestionnaireAnswerValue.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                QuestionnaireAnswerValue.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.value != null && message.hasOwnProperty("value"))
                        if (!$util.isString(message.value))
                            return "value: string expected";
                    if (message.custom_input != null && message.hasOwnProperty("custom_input"))
                        if (!$util.isString(message.custom_input))
                            return "custom_input: string expected";
                    return null;
                };

                QuestionnaireAnswerValue.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue();
                    if (object.value != null)
                        if (typeof object.value !== "string" || object.value.length)
                            message.value = String(object.value);
                    if (object.custom_input != null)
                        if (typeof object.custom_input !== "string" || object.custom_input.length)
                            message.custom_input = String(object.custom_input);
                    return message;
                };

                QuestionnaireAnswerValue.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.value = "";
                        object.custom_input = "";
                    }
                    if (message.value != null && message.hasOwnProperty("value"))
                        object.value = message.value;
                    if (message.custom_input != null && message.hasOwnProperty("custom_input"))
                        object.custom_input = message.custom_input;
                    return object;
                };

                QuestionnaireAnswerValue.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                QuestionnaireAnswerValue.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ReqSubmitQuestionnaire.QuestionnaireAnswer.QuestionnaireAnswerValue";
                };

                return QuestionnaireAnswerValue;
            })();

            return QuestionnaireAnswer;
        })();

        return ReqSubmitQuestionnaire;
    })();

    lq.ReqSetFriendRoomRandomBotChar = (function() {

        function ReqSetFriendRoomRandomBotChar(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetFriendRoomRandomBotChar.prototype.disable_random_char = 0;

        ReqSetFriendRoomRandomBotChar.create = function create(properties) {
            return new ReqSetFriendRoomRandomBotChar(properties);
        };

        ReqSetFriendRoomRandomBotChar.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.disable_random_char != null && Object.hasOwnProperty.call(message, "disable_random_char"))
                writer.uint32(8).uint32(message.disable_random_char);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetFriendRoomRandomBotChar.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetFriendRoomRandomBotChar.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetFriendRoomRandomBotChar(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.disable_random_char = value;
                        else
                            delete message.disable_random_char;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetFriendRoomRandomBotChar.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetFriendRoomRandomBotChar.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.disable_random_char != null && message.hasOwnProperty("disable_random_char"))
                if (!$util.isInteger(message.disable_random_char))
                    return "disable_random_char: integer expected";
            return null;
        };

        ReqSetFriendRoomRandomBotChar.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetFriendRoomRandomBotChar)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetFriendRoomRandomBotChar();
            if (object.disable_random_char != null)
                if (Number(object.disable_random_char) !== 0)
                    message.disable_random_char = object.disable_random_char >>> 0;
            return message;
        };

        ReqSetFriendRoomRandomBotChar.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.disable_random_char = 0;
            if (message.disable_random_char != null && message.hasOwnProperty("disable_random_char"))
                object.disable_random_char = message.disable_random_char;
            return object;
        };

        ReqSetFriendRoomRandomBotChar.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetFriendRoomRandomBotChar.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetFriendRoomRandomBotChar";
        };

        return ReqSetFriendRoomRandomBotChar;
    })();

    lq.ReqFetchAccountGameHuRecords = (function() {

        function ReqFetchAccountGameHuRecords(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchAccountGameHuRecords.prototype.uuid = "";
        ReqFetchAccountGameHuRecords.prototype.category = 0;
        ReqFetchAccountGameHuRecords.prototype.type = 0;

        ReqFetchAccountGameHuRecords.create = function create(properties) {
            return new ReqFetchAccountGameHuRecords(properties);
        };

        ReqFetchAccountGameHuRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(16).uint32(message.category);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchAccountGameHuRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchAccountGameHuRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchAccountGameHuRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchAccountGameHuRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchAccountGameHuRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqFetchAccountGameHuRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchAccountGameHuRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchAccountGameHuRecords();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqFetchAccountGameHuRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.uuid = "";
                object.category = 0;
                object.type = 0;
            }
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqFetchAccountGameHuRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchAccountGameHuRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchAccountGameHuRecords";
        };

        return ReqFetchAccountGameHuRecords;
    })();

    lq.ResFetchAccountGameHuRecords = (function() {

        function ResFetchAccountGameHuRecords(properties) {
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchAccountGameHuRecords.prototype.error = null;
        ResFetchAccountGameHuRecords.prototype.records = $util.emptyArray;

        ResFetchAccountGameHuRecords.create = function create(properties) {
            return new ResFetchAccountGameHuRecords(properties);
        };

        ResFetchAccountGameHuRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.lq.ResFetchAccountGameHuRecords.GameHuRecords.encode(message.records[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchAccountGameHuRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchAccountGameHuRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountGameHuRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.lq.ResFetchAccountGameHuRecords.GameHuRecords.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchAccountGameHuRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchAccountGameHuRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.lq.ResFetchAccountGameHuRecords.GameHuRecords.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            return null;
        };

        ResFetchAccountGameHuRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchAccountGameHuRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchAccountGameHuRecords();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchAccountGameHuRecords.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.ResFetchAccountGameHuRecords.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".lq.ResFetchAccountGameHuRecords.records: object expected");
                    message.records[i] = $root.lq.ResFetchAccountGameHuRecords.GameHuRecords.fromObject(object.records[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchAccountGameHuRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.lq.ResFetchAccountGameHuRecords.GameHuRecords.toObject(message.records[j], options);
            }
            return object;
        };

        ResFetchAccountGameHuRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchAccountGameHuRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchAccountGameHuRecords";
        };

        ResFetchAccountGameHuRecords.GameHuRecords = (function() {

            function GameHuRecords(properties) {
                this.hands = [];
                this.ming = [];
                this.hu_fans = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameHuRecords.prototype.chang = 0;
            GameHuRecords.prototype.ju = 0;
            GameHuRecords.prototype.ben = 0;
            GameHuRecords.prototype.title_id = 0;
            GameHuRecords.prototype.hands = $util.emptyArray;
            GameHuRecords.prototype.ming = $util.emptyArray;
            GameHuRecords.prototype.hupai = "";
            GameHuRecords.prototype.hu_fans = $util.emptyArray;

            GameHuRecords.create = function create(properties) {
                return new GameHuRecords(properties);
            };

            GameHuRecords.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                    writer.uint32(8).uint32(message.chang);
                if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                    writer.uint32(16).uint32(message.ju);
                if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                    writer.uint32(24).uint32(message.ben);
                if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                    writer.uint32(32).uint32(message.title_id);
                if (message.hands != null && message.hands.length)
                    for (let i = 0; i < message.hands.length; ++i)
                        writer.uint32(42).string(message.hands[i]);
                if (message.ming != null && message.ming.length)
                    for (let i = 0; i < message.ming.length; ++i)
                        writer.uint32(50).string(message.ming[i]);
                if (message.hupai != null && Object.hasOwnProperty.call(message, "hupai"))
                    writer.uint32(58).string(message.hupai);
                if (message.hu_fans != null && message.hu_fans.length) {
                    writer.uint32(66).fork();
                    for (let i = 0; i < message.hu_fans.length; ++i)
                        writer.uint32(message.hu_fans[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameHuRecords.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameHuRecords.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountGameHuRecords.GameHuRecords(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chang = value;
                            else
                                delete message.chang;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ju = value;
                            else
                                delete message.ju;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.ben = value;
                            else
                                delete message.ben;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.title_id = value;
                            else
                                delete message.title_id;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.hands && message.hands.length))
                                message.hands = [];
                            message.hands.push(reader.string());
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            if (!(message.ming && message.ming.length))
                                message.ming = [];
                            message.ming.push(reader.string());
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.hupai = value;
                            else
                                delete message.hupai;
                            continue;
                        }
                    case 8: {
                            if (wireType === 2) {
                                if (!(message.hu_fans && message.hu_fans.length))
                                    message.hu_fans = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.hu_fans.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.hu_fans && message.hu_fans.length))
                                message.hu_fans = [];
                            message.hu_fans.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameHuRecords.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameHuRecords.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.chang != null && message.hasOwnProperty("chang"))
                    if (!$util.isInteger(message.chang))
                        return "chang: integer expected";
                if (message.ju != null && message.hasOwnProperty("ju"))
                    if (!$util.isInteger(message.ju))
                        return "ju: integer expected";
                if (message.ben != null && message.hasOwnProperty("ben"))
                    if (!$util.isInteger(message.ben))
                        return "ben: integer expected";
                if (message.title_id != null && message.hasOwnProperty("title_id"))
                    if (!$util.isInteger(message.title_id))
                        return "title_id: integer expected";
                if (message.hands != null && message.hasOwnProperty("hands")) {
                    if (!Array.isArray(message.hands))
                        return "hands: array expected";
                    for (let i = 0; i < message.hands.length; ++i)
                        if (!$util.isString(message.hands[i]))
                            return "hands: string[] expected";
                }
                if (message.ming != null && message.hasOwnProperty("ming")) {
                    if (!Array.isArray(message.ming))
                        return "ming: array expected";
                    for (let i = 0; i < message.ming.length; ++i)
                        if (!$util.isString(message.ming[i]))
                            return "ming: string[] expected";
                }
                if (message.hupai != null && message.hasOwnProperty("hupai"))
                    if (!$util.isString(message.hupai))
                        return "hupai: string expected";
                if (message.hu_fans != null && message.hasOwnProperty("hu_fans")) {
                    if (!Array.isArray(message.hu_fans))
                        return "hu_fans: array expected";
                    for (let i = 0; i < message.hu_fans.length; ++i)
                        if (!$util.isInteger(message.hu_fans[i]))
                            return "hu_fans: integer[] expected";
                }
                return null;
            };

            GameHuRecords.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchAccountGameHuRecords.GameHuRecords)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchAccountGameHuRecords.GameHuRecords();
                if (object.chang != null)
                    if (Number(object.chang) !== 0)
                        message.chang = object.chang >>> 0;
                if (object.ju != null)
                    if (Number(object.ju) !== 0)
                        message.ju = object.ju >>> 0;
                if (object.ben != null)
                    if (Number(object.ben) !== 0)
                        message.ben = object.ben >>> 0;
                if (object.title_id != null)
                    if (Number(object.title_id) !== 0)
                        message.title_id = object.title_id >>> 0;
                if (object.hands) {
                    if (!Array.isArray(object.hands))
                        throw TypeError(".lq.ResFetchAccountGameHuRecords.GameHuRecords.hands: array expected");
                    message.hands = Array(object.hands.length);
                    for (let i = 0; i < object.hands.length; ++i)
                        message.hands[i] = String(object.hands[i]);
                }
                if (object.ming) {
                    if (!Array.isArray(object.ming))
                        throw TypeError(".lq.ResFetchAccountGameHuRecords.GameHuRecords.ming: array expected");
                    message.ming = Array(object.ming.length);
                    for (let i = 0; i < object.ming.length; ++i)
                        message.ming[i] = String(object.ming[i]);
                }
                if (object.hupai != null)
                    if (typeof object.hupai !== "string" || object.hupai.length)
                        message.hupai = String(object.hupai);
                if (object.hu_fans) {
                    if (!Array.isArray(object.hu_fans))
                        throw TypeError(".lq.ResFetchAccountGameHuRecords.GameHuRecords.hu_fans: array expected");
                    message.hu_fans = Array(object.hu_fans.length);
                    for (let i = 0; i < object.hu_fans.length; ++i)
                        message.hu_fans[i] = object.hu_fans[i] >>> 0;
                }
                return message;
            };

            GameHuRecords.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.hands = [];
                    object.ming = [];
                    object.hu_fans = [];
                }
                if (options.defaults) {
                    object.chang = 0;
                    object.ju = 0;
                    object.ben = 0;
                    object.title_id = 0;
                    object.hupai = "";
                }
                if (message.chang != null && message.hasOwnProperty("chang"))
                    object.chang = message.chang;
                if (message.ju != null && message.hasOwnProperty("ju"))
                    object.ju = message.ju;
                if (message.ben != null && message.hasOwnProperty("ben"))
                    object.ben = message.ben;
                if (message.title_id != null && message.hasOwnProperty("title_id"))
                    object.title_id = message.title_id;
                if (message.hands && message.hands.length) {
                    object.hands = Array(message.hands.length);
                    for (let j = 0; j < message.hands.length; ++j)
                        object.hands[j] = message.hands[j];
                }
                if (message.ming && message.ming.length) {
                    object.ming = Array(message.ming.length);
                    for (let j = 0; j < message.ming.length; ++j)
                        object.ming[j] = message.ming[j];
                }
                if (message.hupai != null && message.hasOwnProperty("hupai"))
                    object.hupai = message.hupai;
                if (message.hu_fans && message.hu_fans.length) {
                    object.hu_fans = Array(message.hu_fans.length);
                    for (let j = 0; j < message.hu_fans.length; ++j)
                        object.hu_fans[j] = message.hu_fans[j];
                }
                return object;
            };

            GameHuRecords.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameHuRecords.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchAccountGameHuRecords.GameHuRecords";
            };

            return GameHuRecords;
        })();

        return ResFetchAccountGameHuRecords;
    })();

    lq.ReqFetchAccountInfoExtra = (function() {

        function ReqFetchAccountInfoExtra(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchAccountInfoExtra.prototype.account_id = 0;
        ReqFetchAccountInfoExtra.prototype.category = 0;
        ReqFetchAccountInfoExtra.prototype.type = 0;

        ReqFetchAccountInfoExtra.create = function create(properties) {
            return new ReqFetchAccountInfoExtra(properties);
        };

        ReqFetchAccountInfoExtra.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(16).uint32(message.category);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchAccountInfoExtra.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchAccountInfoExtra.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchAccountInfoExtra(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchAccountInfoExtra.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchAccountInfoExtra.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqFetchAccountInfoExtra.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchAccountInfoExtra)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchAccountInfoExtra();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqFetchAccountInfoExtra.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.category = 0;
                object.type = 0;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqFetchAccountInfoExtra.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchAccountInfoExtra.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchAccountInfoExtra";
        };

        return ReqFetchAccountInfoExtra;
    })();

    lq.ResFetchAccountInfoExtra = (function() {

        function ResFetchAccountInfoExtra(properties) {
            this.recent_games = [];
            this.hu_type_details = [];
            this.game_rank_details = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchAccountInfoExtra.prototype.error = null;
        ResFetchAccountInfoExtra.prototype.recent_games = $util.emptyArray;
        ResFetchAccountInfoExtra.prototype.hu_type_details = $util.emptyArray;
        ResFetchAccountInfoExtra.prototype.game_rank_details = $util.emptyArray;

        ResFetchAccountInfoExtra.create = function create(properties) {
            return new ResFetchAccountInfoExtra(properties);
        };

        ResFetchAccountInfoExtra.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.recent_games != null && message.recent_games.length)
                for (let i = 0; i < message.recent_games.length; ++i)
                    $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.encode(message.recent_games[i], writer.uint32(18).fork()).ldelim();
            if (message.hu_type_details != null && message.hu_type_details.length)
                for (let i = 0; i < message.hu_type_details.length; ++i)
                    $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail.encode(message.hu_type_details[i], writer.uint32(26).fork()).ldelim();
            if (message.game_rank_details != null && message.game_rank_details.length)
                for (let i = 0; i < message.game_rank_details.length; ++i)
                    $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail.encode(message.game_rank_details[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchAccountInfoExtra.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchAccountInfoExtra.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountInfoExtra(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.recent_games && message.recent_games.length))
                            message.recent_games = [];
                        message.recent_games.push($root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hu_type_details && message.hu_type_details.length))
                            message.hu_type_details = [];
                        message.hu_type_details.push($root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.game_rank_details && message.game_rank_details.length))
                            message.game_rank_details = [];
                        message.game_rank_details.push($root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchAccountInfoExtra.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchAccountInfoExtra.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.recent_games != null && message.hasOwnProperty("recent_games")) {
                if (!Array.isArray(message.recent_games))
                    return "recent_games: array expected";
                for (let i = 0; i < message.recent_games.length; ++i) {
                    let error = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.verify(message.recent_games[i], _depth + 1);
                    if (error)
                        return "recent_games." + error;
                }
            }
            if (message.hu_type_details != null && message.hasOwnProperty("hu_type_details")) {
                if (!Array.isArray(message.hu_type_details))
                    return "hu_type_details: array expected";
                for (let i = 0; i < message.hu_type_details.length; ++i) {
                    let error = $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail.verify(message.hu_type_details[i], _depth + 1);
                    if (error)
                        return "hu_type_details." + error;
                }
            }
            if (message.game_rank_details != null && message.hasOwnProperty("game_rank_details")) {
                if (!Array.isArray(message.game_rank_details))
                    return "game_rank_details: array expected";
                for (let i = 0; i < message.game_rank_details.length; ++i) {
                    let error = $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail.verify(message.game_rank_details[i], _depth + 1);
                    if (error)
                        return "game_rank_details." + error;
                }
            }
            return null;
        };

        ResFetchAccountInfoExtra.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchAccountInfoExtra)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchAccountInfoExtra();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchAccountInfoExtra.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.recent_games) {
                if (!Array.isArray(object.recent_games))
                    throw TypeError(".lq.ResFetchAccountInfoExtra.recent_games: array expected");
                message.recent_games = Array(object.recent_games.length);
                for (let i = 0; i < object.recent_games.length; ++i) {
                    if (typeof object.recent_games[i] !== "object")
                        throw TypeError(".lq.ResFetchAccountInfoExtra.recent_games: object expected");
                    message.recent_games[i] = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.fromObject(object.recent_games[i], _depth + 1);
                }
            }
            if (object.hu_type_details) {
                if (!Array.isArray(object.hu_type_details))
                    throw TypeError(".lq.ResFetchAccountInfoExtra.hu_type_details: array expected");
                message.hu_type_details = Array(object.hu_type_details.length);
                for (let i = 0; i < object.hu_type_details.length; ++i) {
                    if (typeof object.hu_type_details[i] !== "object")
                        throw TypeError(".lq.ResFetchAccountInfoExtra.hu_type_details: object expected");
                    message.hu_type_details[i] = $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail.fromObject(object.hu_type_details[i], _depth + 1);
                }
            }
            if (object.game_rank_details) {
                if (!Array.isArray(object.game_rank_details))
                    throw TypeError(".lq.ResFetchAccountInfoExtra.game_rank_details: array expected");
                message.game_rank_details = Array(object.game_rank_details.length);
                for (let i = 0; i < object.game_rank_details.length; ++i) {
                    if (typeof object.game_rank_details[i] !== "object")
                        throw TypeError(".lq.ResFetchAccountInfoExtra.game_rank_details: object expected");
                    message.game_rank_details[i] = $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail.fromObject(object.game_rank_details[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchAccountInfoExtra.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.recent_games = [];
                object.hu_type_details = [];
                object.game_rank_details = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.recent_games && message.recent_games.length) {
                object.recent_games = Array(message.recent_games.length);
                for (let j = 0; j < message.recent_games.length; ++j)
                    object.recent_games[j] = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.toObject(message.recent_games[j], options);
            }
            if (message.hu_type_details && message.hu_type_details.length) {
                object.hu_type_details = Array(message.hu_type_details.length);
                for (let j = 0; j < message.hu_type_details.length; ++j)
                    object.hu_type_details[j] = $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail.toObject(message.hu_type_details[j], options);
            }
            if (message.game_rank_details && message.game_rank_details.length) {
                object.game_rank_details = Array(message.game_rank_details.length);
                for (let j = 0; j < message.game_rank_details.length; ++j)
                    object.game_rank_details[j] = $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail.toObject(message.game_rank_details[j], options);
            }
            return object;
        };

        ResFetchAccountInfoExtra.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchAccountInfoExtra.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchAccountInfoExtra";
        };

        ResFetchAccountInfoExtra.AccountInfoGameRecord = (function() {

            function AccountInfoGameRecord(properties) {
                this.results = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountInfoGameRecord.prototype.uuid = "";
            AccountInfoGameRecord.prototype.start_time = 0;
            AccountInfoGameRecord.prototype.end_time = 0;
            AccountInfoGameRecord.prototype.tag = 0;
            AccountInfoGameRecord.prototype.sub_tag = 0;
            AccountInfoGameRecord.prototype.rank = 0;
            AccountInfoGameRecord.prototype.final_point = 0;
            AccountInfoGameRecord.prototype.results = $util.emptyArray;

            AccountInfoGameRecord.create = function create(properties) {
                return new AccountInfoGameRecord(properties);
            };

            AccountInfoGameRecord.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                    writer.uint32(10).string(message.uuid);
                if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                    writer.uint32(16).uint32(message.start_time);
                if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                    writer.uint32(24).uint32(message.end_time);
                if (message.tag != null && Object.hasOwnProperty.call(message, "tag"))
                    writer.uint32(32).uint32(message.tag);
                if (message.sub_tag != null && Object.hasOwnProperty.call(message, "sub_tag"))
                    writer.uint32(40).uint32(message.sub_tag);
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(48).uint32(message.rank);
                if (message.final_point != null && Object.hasOwnProperty.call(message, "final_point"))
                    writer.uint32(56).uint32(message.final_point);
                if (message.results != null && message.results.length)
                    for (let i = 0; i < message.results.length; ++i)
                        $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.encode(message.results[i], writer.uint32(66).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountInfoGameRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountInfoGameRecord.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.uuid = value;
                            else
                                delete message.uuid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.start_time = value;
                            else
                                delete message.start_time;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.end_time = value;
                            else
                                delete message.end_time;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.tag = value;
                            else
                                delete message.tag;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.sub_tag = value;
                            else
                                delete message.sub_tag;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 7: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final_point = value;
                            else
                                delete message.final_point;
                            continue;
                        }
                    case 8: {
                            if (wireType !== 2)
                                break;
                            if (!(message.results && message.results.length))
                                message.results = [];
                            message.results.push($root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountInfoGameRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountInfoGameRecord.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    if (!$util.isString(message.uuid))
                        return "uuid: string expected";
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    if (!$util.isInteger(message.start_time))
                        return "start_time: integer expected";
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    if (!$util.isInteger(message.end_time))
                        return "end_time: integer expected";
                if (message.tag != null && message.hasOwnProperty("tag"))
                    if (!$util.isInteger(message.tag))
                        return "tag: integer expected";
                if (message.sub_tag != null && message.hasOwnProperty("sub_tag"))
                    if (!$util.isInteger(message.sub_tag))
                        return "sub_tag: integer expected";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.final_point != null && message.hasOwnProperty("final_point"))
                    if (!$util.isInteger(message.final_point))
                        return "final_point: integer expected";
                if (message.results != null && message.hasOwnProperty("results")) {
                    if (!Array.isArray(message.results))
                        return "results: array expected";
                    for (let i = 0; i < message.results.length; ++i) {
                        let error = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.verify(message.results[i], _depth + 1);
                        if (error)
                            return "results." + error;
                    }
                }
                return null;
            };

            AccountInfoGameRecord.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord();
                if (object.uuid != null)
                    if (typeof object.uuid !== "string" || object.uuid.length)
                        message.uuid = String(object.uuid);
                if (object.start_time != null)
                    if (Number(object.start_time) !== 0)
                        message.start_time = object.start_time >>> 0;
                if (object.end_time != null)
                    if (Number(object.end_time) !== 0)
                        message.end_time = object.end_time >>> 0;
                if (object.tag != null)
                    if (Number(object.tag) !== 0)
                        message.tag = object.tag >>> 0;
                if (object.sub_tag != null)
                    if (Number(object.sub_tag) !== 0)
                        message.sub_tag = object.sub_tag >>> 0;
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.final_point != null)
                    if (Number(object.final_point) !== 0)
                        message.final_point = object.final_point >>> 0;
                if (object.results) {
                    if (!Array.isArray(object.results))
                        throw TypeError(".lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.results: array expected");
                    message.results = Array(object.results.length);
                    for (let i = 0; i < object.results.length; ++i) {
                        if (typeof object.results[i] !== "object")
                            throw TypeError(".lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.results: object expected");
                        message.results[i] = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.fromObject(object.results[i], _depth + 1);
                    }
                }
                return message;
            };

            AccountInfoGameRecord.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.results = [];
                if (options.defaults) {
                    object.uuid = "";
                    object.start_time = 0;
                    object.end_time = 0;
                    object.tag = 0;
                    object.sub_tag = 0;
                    object.rank = 0;
                    object.final_point = 0;
                }
                if (message.uuid != null && message.hasOwnProperty("uuid"))
                    object.uuid = message.uuid;
                if (message.start_time != null && message.hasOwnProperty("start_time"))
                    object.start_time = message.start_time;
                if (message.end_time != null && message.hasOwnProperty("end_time"))
                    object.end_time = message.end_time;
                if (message.tag != null && message.hasOwnProperty("tag"))
                    object.tag = message.tag;
                if (message.sub_tag != null && message.hasOwnProperty("sub_tag"))
                    object.sub_tag = message.sub_tag;
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.final_point != null && message.hasOwnProperty("final_point"))
                    object.final_point = message.final_point;
                if (message.results && message.results.length) {
                    object.results = Array(message.results.length);
                    for (let j = 0; j < message.results.length; ++j)
                        object.results[j] = $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.toObject(message.results[j], options);
                }
                return object;
            };

            AccountInfoGameRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountInfoGameRecord.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchAccountInfoExtra.AccountInfoGameRecord";
            };

            AccountInfoGameRecord.AccountGameResult = (function() {

                function AccountGameResult(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                AccountGameResult.prototype.rank = 0;
                AccountGameResult.prototype.account_id = 0;
                AccountGameResult.prototype.nickname = "";
                AccountGameResult.prototype.verified = 0;
                AccountGameResult.prototype.grading_score = 0;
                AccountGameResult.prototype.final_point = 0;
                AccountGameResult.prototype.seat = 0;
                AccountGameResult.prototype.level = null;
                AccountGameResult.prototype.level3 = null;

                AccountGameResult.create = function create(properties) {
                    return new AccountGameResult(properties);
                };

                AccountGameResult.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                        writer.uint32(8).uint32(message.rank);
                    if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                        writer.uint32(16).uint32(message.account_id);
                    if (message.nickname != null && Object.hasOwnProperty.call(message, "nickname"))
                        writer.uint32(26).string(message.nickname);
                    if (message.verified != null && Object.hasOwnProperty.call(message, "verified"))
                        writer.uint32(32).uint32(message.verified);
                    if (message.grading_score != null && Object.hasOwnProperty.call(message, "grading_score"))
                        writer.uint32(40).int32(message.grading_score);
                    if (message.final_point != null && Object.hasOwnProperty.call(message, "final_point"))
                        writer.uint32(48).int32(message.final_point);
                    if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                        writer.uint32(56).uint32(message.seat);
                    if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                        $root.lq.AccountLevel.encode(message.level, writer.uint32(66).fork()).ldelim();
                    if (message.level3 != null && Object.hasOwnProperty.call(message, "level3"))
                        $root.lq.AccountLevel.encode(message.level3, writer.uint32(74).fork()).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                AccountGameResult.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                AccountGameResult.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.rank = value;
                                else
                                    delete message.rank;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.account_id = value;
                                else
                                    delete message.account_id;
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.nickname = value;
                                else
                                    delete message.nickname;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.verified = value;
                                else
                                    delete message.verified;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.grading_score = value;
                                else
                                    delete message.grading_score;
                                continue;
                            }
                        case 6: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.int32())
                                    message.final_point = value;
                                else
                                    delete message.final_point;
                                continue;
                            }
                        case 7: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.seat = value;
                                else
                                    delete message.seat;
                                continue;
                            }
                        case 8: {
                                if (wireType !== 2)
                                    break;
                                message.level = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                                continue;
                            }
                        case 9: {
                                if (wireType !== 2)
                                    break;
                                message.level3 = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.level3);
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                AccountGameResult.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                AccountGameResult.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.rank != null && message.hasOwnProperty("rank"))
                        if (!$util.isInteger(message.rank))
                            return "rank: integer expected";
                    if (message.account_id != null && message.hasOwnProperty("account_id"))
                        if (!$util.isInteger(message.account_id))
                            return "account_id: integer expected";
                    if (message.nickname != null && message.hasOwnProperty("nickname"))
                        if (!$util.isString(message.nickname))
                            return "nickname: string expected";
                    if (message.verified != null && message.hasOwnProperty("verified"))
                        if (!$util.isInteger(message.verified))
                            return "verified: integer expected";
                    if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                        if (!$util.isInteger(message.grading_score))
                            return "grading_score: integer expected";
                    if (message.final_point != null && message.hasOwnProperty("final_point"))
                        if (!$util.isInteger(message.final_point))
                            return "final_point: integer expected";
                    if (message.seat != null && message.hasOwnProperty("seat"))
                        if (!$util.isInteger(message.seat))
                            return "seat: integer expected";
                    if (message.level != null && message.hasOwnProperty("level")) {
                        let error = $root.lq.AccountLevel.verify(message.level, _depth + 1);
                        if (error)
                            return "level." + error;
                    }
                    if (message.level3 != null && message.hasOwnProperty("level3")) {
                        let error = $root.lq.AccountLevel.verify(message.level3, _depth + 1);
                        if (error)
                            return "level3." + error;
                    }
                    return null;
                };

                AccountGameResult.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult();
                    if (object.rank != null)
                        if (Number(object.rank) !== 0)
                            message.rank = object.rank >>> 0;
                    if (object.account_id != null)
                        if (Number(object.account_id) !== 0)
                            message.account_id = object.account_id >>> 0;
                    if (object.nickname != null)
                        if (typeof object.nickname !== "string" || object.nickname.length)
                            message.nickname = String(object.nickname);
                    if (object.verified != null)
                        if (Number(object.verified) !== 0)
                            message.verified = object.verified >>> 0;
                    if (object.grading_score != null)
                        if (Number(object.grading_score) !== 0)
                            message.grading_score = object.grading_score | 0;
                    if (object.final_point != null)
                        if (Number(object.final_point) !== 0)
                            message.final_point = object.final_point | 0;
                    if (object.seat != null)
                        if (Number(object.seat) !== 0)
                            message.seat = object.seat >>> 0;
                    if (object.level != null) {
                        if (typeof object.level !== "object")
                            throw TypeError(".lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.level: object expected");
                        message.level = $root.lq.AccountLevel.fromObject(object.level, _depth + 1);
                    }
                    if (object.level3 != null) {
                        if (typeof object.level3 !== "object")
                            throw TypeError(".lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult.level3: object expected");
                        message.level3 = $root.lq.AccountLevel.fromObject(object.level3, _depth + 1);
                    }
                    return message;
                };

                AccountGameResult.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.defaults) {
                        object.rank = 0;
                        object.account_id = 0;
                        object.nickname = "";
                        object.verified = 0;
                        object.grading_score = 0;
                        object.final_point = 0;
                        object.seat = 0;
                        object.level = null;
                        object.level3 = null;
                    }
                    if (message.rank != null && message.hasOwnProperty("rank"))
                        object.rank = message.rank;
                    if (message.account_id != null && message.hasOwnProperty("account_id"))
                        object.account_id = message.account_id;
                    if (message.nickname != null && message.hasOwnProperty("nickname"))
                        object.nickname = message.nickname;
                    if (message.verified != null && message.hasOwnProperty("verified"))
                        object.verified = message.verified;
                    if (message.grading_score != null && message.hasOwnProperty("grading_score"))
                        object.grading_score = message.grading_score;
                    if (message.final_point != null && message.hasOwnProperty("final_point"))
                        object.final_point = message.final_point;
                    if (message.seat != null && message.hasOwnProperty("seat"))
                        object.seat = message.seat;
                    if (message.level != null && message.hasOwnProperty("level"))
                        object.level = $root.lq.AccountLevel.toObject(message.level, options);
                    if (message.level3 != null && message.hasOwnProperty("level3"))
                        object.level3 = $root.lq.AccountLevel.toObject(message.level3, options);
                    return object;
                };

                AccountGameResult.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                AccountGameResult.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.ResFetchAccountInfoExtra.AccountInfoGameRecord.AccountGameResult";
                };

                return AccountGameResult;
            })();

            return AccountInfoGameRecord;
        })();

        ResFetchAccountInfoExtra.GameHuTypeDetail = (function() {

            function GameHuTypeDetail(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameHuTypeDetail.prototype.type = 0;
            GameHuTypeDetail.prototype.count = 0;

            GameHuTypeDetail.create = function create(properties) {
                return new GameHuTypeDetail(properties);
            };

            GameHuTypeDetail.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameHuTypeDetail.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameHuTypeDetail.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameHuTypeDetail.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameHuTypeDetail.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            GameHuTypeDetail.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchAccountInfoExtra.GameHuTypeDetail();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            GameHuTypeDetail.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.count = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            GameHuTypeDetail.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameHuTypeDetail.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchAccountInfoExtra.GameHuTypeDetail";
            };

            return GameHuTypeDetail;
        })();

        ResFetchAccountInfoExtra.AccountGameRankDetail = (function() {

            function AccountGameRankDetail(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AccountGameRankDetail.prototype.rank = 0;
            AccountGameRankDetail.prototype.count = 0;

            AccountGameRankDetail.create = function create(properties) {
                return new AccountGameRankDetail(properties);
            };

            AccountGameRankDetail.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                    writer.uint32(8).uint32(message.rank);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(16).uint32(message.count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AccountGameRankDetail.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AccountGameRankDetail.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.rank = value;
                            else
                                delete message.rank;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AccountGameRankDetail.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AccountGameRankDetail.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.rank != null && message.hasOwnProperty("rank"))
                    if (!$util.isInteger(message.rank))
                        return "rank: integer expected";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                return null;
            };

            AccountGameRankDetail.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResFetchAccountInfoExtra.AccountGameRankDetail();
                if (object.rank != null)
                    if (Number(object.rank) !== 0)
                        message.rank = object.rank >>> 0;
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                return message;
            };

            AccountGameRankDetail.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.rank = 0;
                    object.count = 0;
                }
                if (message.rank != null && message.hasOwnProperty("rank"))
                    object.rank = message.rank;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                return object;
            };

            AccountGameRankDetail.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AccountGameRankDetail.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResFetchAccountInfoExtra.AccountGameRankDetail";
            };

            return AccountGameRankDetail;
        })();

        return ResFetchAccountInfoExtra;
    })();

    lq.ReqSetAccountFavoriteHu = (function() {

        function ReqSetAccountFavoriteHu(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSetAccountFavoriteHu.prototype.mode = 0;
        ReqSetAccountFavoriteHu.prototype.category = 0;
        ReqSetAccountFavoriteHu.prototype.type = 0;
        ReqSetAccountFavoriteHu.prototype.uuid = "";
        ReqSetAccountFavoriteHu.prototype.chang = 0;
        ReqSetAccountFavoriteHu.prototype.ju = 0;
        ReqSetAccountFavoriteHu.prototype.ben = 0;

        ReqSetAccountFavoriteHu.create = function create(properties) {
            return new ReqSetAccountFavoriteHu(properties);
        };

        ReqSetAccountFavoriteHu.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                writer.uint32(8).uint32(message.mode);
            if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                writer.uint32(16).uint32(message.category);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(34).string(message.uuid);
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(40).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(48).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(56).uint32(message.ben);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSetAccountFavoriteHu.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSetAccountFavoriteHu.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSetAccountFavoriteHu(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.category = value;
                        else
                            delete message.category;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSetAccountFavoriteHu.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSetAccountFavoriteHu.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode != null && message.hasOwnProperty("mode"))
                if (!$util.isInteger(message.mode))
                    return "mode: integer expected";
            if (message.category != null && message.hasOwnProperty("category"))
                if (!$util.isInteger(message.category))
                    return "category: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            return null;
        };

        ReqSetAccountFavoriteHu.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSetAccountFavoriteHu)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSetAccountFavoriteHu();
            if (object.mode != null)
                if (Number(object.mode) !== 0)
                    message.mode = object.mode >>> 0;
            if (object.category != null)
                if (Number(object.category) !== 0)
                    message.category = object.category >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            return message;
        };

        ReqSetAccountFavoriteHu.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.mode = 0;
                object.category = 0;
                object.type = 0;
                object.uuid = "";
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
            }
            if (message.mode != null && message.hasOwnProperty("mode"))
                object.mode = message.mode;
            if (message.category != null && message.hasOwnProperty("category"))
                object.category = message.category;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            return object;
        };

        ReqSetAccountFavoriteHu.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSetAccountFavoriteHu.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSetAccountFavoriteHu";
        };

        return ReqSetAccountFavoriteHu;
    })();

    lq.ReqFetchSeerReport = (function() {

        function ReqFetchSeerReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchSeerReport.prototype.uuid = "";

        ReqFetchSeerReport.create = function create(properties) {
            return new ReqFetchSeerReport(properties);
        };

        ReqFetchSeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchSeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchSeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchSeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchSeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchSeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            return null;
        };

        ReqFetchSeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchSeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchSeerReport();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            return message;
        };

        ReqFetchSeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.uuid = "";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            return object;
        };

        ReqFetchSeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchSeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchSeerReport";
        };

        return ReqFetchSeerReport;
    })();

    lq.ResFetchSeerReport = (function() {

        function ResFetchSeerReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSeerReport.prototype.error = null;
        ResFetchSeerReport.prototype.report = null;

        ResFetchSeerReport.create = function create(properties) {
            return new ResFetchSeerReport(properties);
        };

        ResFetchSeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.report != null && Object.hasOwnProperty.call(message, "report"))
                $root.lq.SeerReport.encode(message.report, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.report = $root.lq.SeerReport.decode(reader, reader.uint32(), undefined, _depth + 1, message.report);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.report != null && message.hasOwnProperty("report")) {
                let error = $root.lq.SeerReport.verify(message.report, _depth + 1);
                if (error)
                    return "report." + error;
            }
            return null;
        };

        ResFetchSeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSeerReport();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSeerReport.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.report != null) {
                if (typeof object.report !== "object")
                    throw TypeError(".lq.ResFetchSeerReport.report: object expected");
                message.report = $root.lq.SeerReport.fromObject(object.report, _depth + 1);
            }
            return message;
        };

        ResFetchSeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.report = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.report != null && message.hasOwnProperty("report"))
                object.report = $root.lq.SeerReport.toObject(message.report, options);
            return object;
        };

        ResFetchSeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSeerReport";
        };

        return ResFetchSeerReport;
    })();

    lq.ReqCreateSeerReport = (function() {

        function ReqCreateSeerReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqCreateSeerReport.prototype.uuid = "";

        ReqCreateSeerReport.create = function create(properties) {
            return new ReqCreateSeerReport(properties);
        };

        ReqCreateSeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                writer.uint32(10).string(message.uuid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqCreateSeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqCreateSeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqCreateSeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqCreateSeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqCreateSeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                if (!$util.isString(message.uuid))
                    return "uuid: string expected";
            return null;
        };

        ReqCreateSeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqCreateSeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqCreateSeerReport();
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = String(object.uuid);
            return message;
        };

        ReqCreateSeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.uuid = "";
            if (message.uuid != null && message.hasOwnProperty("uuid"))
                object.uuid = message.uuid;
            return object;
        };

        ReqCreateSeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqCreateSeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqCreateSeerReport";
        };

        return ReqCreateSeerReport;
    })();

    lq.ResCreateSeerReport = (function() {

        function ResCreateSeerReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResCreateSeerReport.prototype.error = null;
        ResCreateSeerReport.prototype.seer_report = null;

        ResCreateSeerReport.create = function create(properties) {
            return new ResCreateSeerReport(properties);
        };

        ResCreateSeerReport.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.seer_report != null && Object.hasOwnProperty.call(message, "seer_report"))
                $root.lq.SeerBrief.encode(message.seer_report, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResCreateSeerReport.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResCreateSeerReport.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResCreateSeerReport(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.seer_report = $root.lq.SeerBrief.decode(reader, reader.uint32(), undefined, _depth + 1, message.seer_report);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResCreateSeerReport.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResCreateSeerReport.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.seer_report != null && message.hasOwnProperty("seer_report")) {
                let error = $root.lq.SeerBrief.verify(message.seer_report, _depth + 1);
                if (error)
                    return "seer_report." + error;
            }
            return null;
        };

        ResCreateSeerReport.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResCreateSeerReport)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResCreateSeerReport();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResCreateSeerReport.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.seer_report != null) {
                if (typeof object.seer_report !== "object")
                    throw TypeError(".lq.ResCreateSeerReport.seer_report: object expected");
                message.seer_report = $root.lq.SeerBrief.fromObject(object.seer_report, _depth + 1);
            }
            return message;
        };

        ResCreateSeerReport.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.seer_report = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.seer_report != null && message.hasOwnProperty("seer_report"))
                object.seer_report = $root.lq.SeerBrief.toObject(message.seer_report, options);
            return object;
        };

        ResCreateSeerReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResCreateSeerReport.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResCreateSeerReport";
        };

        return ResCreateSeerReport;
    })();

    lq.ResFetchSeerReportList = (function() {

        function ResFetchSeerReportList(properties) {
            this.seer_report_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchSeerReportList.prototype.error = null;
        ResFetchSeerReportList.prototype.seer_report_list = $util.emptyArray;

        ResFetchSeerReportList.create = function create(properties) {
            return new ResFetchSeerReportList(properties);
        };

        ResFetchSeerReportList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.seer_report_list != null && message.seer_report_list.length)
                for (let i = 0; i < message.seer_report_list.length; ++i)
                    $root.lq.SeerBrief.encode(message.seer_report_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchSeerReportList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchSeerReportList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchSeerReportList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.seer_report_list && message.seer_report_list.length))
                            message.seer_report_list = [];
                        message.seer_report_list.push($root.lq.SeerBrief.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchSeerReportList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchSeerReportList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.seer_report_list != null && message.hasOwnProperty("seer_report_list")) {
                if (!Array.isArray(message.seer_report_list))
                    return "seer_report_list: array expected";
                for (let i = 0; i < message.seer_report_list.length; ++i) {
                    let error = $root.lq.SeerBrief.verify(message.seer_report_list[i], _depth + 1);
                    if (error)
                        return "seer_report_list." + error;
                }
            }
            return null;
        };

        ResFetchSeerReportList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchSeerReportList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchSeerReportList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchSeerReportList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.seer_report_list) {
                if (!Array.isArray(object.seer_report_list))
                    throw TypeError(".lq.ResFetchSeerReportList.seer_report_list: array expected");
                message.seer_report_list = Array(object.seer_report_list.length);
                for (let i = 0; i < object.seer_report_list.length; ++i) {
                    if (typeof object.seer_report_list[i] !== "object")
                        throw TypeError(".lq.ResFetchSeerReportList.seer_report_list: object expected");
                    message.seer_report_list[i] = $root.lq.SeerBrief.fromObject(object.seer_report_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchSeerReportList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.seer_report_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.seer_report_list && message.seer_report_list.length) {
                object.seer_report_list = Array(message.seer_report_list.length);
                for (let j = 0; j < message.seer_report_list.length; ++j)
                    object.seer_report_list[j] = $root.lq.SeerBrief.toObject(message.seer_report_list[j], options);
            }
            return object;
        };

        ResFetchSeerReportList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchSeerReportList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchSeerReportList";
        };

        return ResFetchSeerReportList;
    })();

    lq.ReqSelectChestChooseUp = (function() {

        function ReqSelectChestChooseUp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSelectChestChooseUp.prototype.activity_id = 0;
        ReqSelectChestChooseUp.prototype.selection = 0;
        ReqSelectChestChooseUp.prototype.chest_id = 0;

        ReqSelectChestChooseUp.create = function create(properties) {
            return new ReqSelectChestChooseUp(properties);
        };

        ReqSelectChestChooseUp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.selection != null && Object.hasOwnProperty.call(message, "selection"))
                writer.uint32(16).uint32(message.selection);
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(24).uint32(message.chest_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSelectChestChooseUp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSelectChestChooseUp.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSelectChestChooseUp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selection = value;
                        else
                            delete message.selection;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSelectChestChooseUp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSelectChestChooseUp.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.selection != null && message.hasOwnProperty("selection"))
                if (!$util.isInteger(message.selection))
                    return "selection: integer expected";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            return null;
        };

        ReqSelectChestChooseUp.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSelectChestChooseUp)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSelectChestChooseUp();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.selection != null)
                if (Number(object.selection) !== 0)
                    message.selection = object.selection >>> 0;
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            return message;
        };

        ReqSelectChestChooseUp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.selection = 0;
                object.chest_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.selection != null && message.hasOwnProperty("selection"))
                object.selection = message.selection;
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            return object;
        };

        ReqSelectChestChooseUp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSelectChestChooseUp.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSelectChestChooseUp";
        };

        return ReqSelectChestChooseUp;
    })();

    lq.ReqSelectChestChooseGroupActivity = (function() {

        function ReqSelectChestChooseGroupActivity(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSelectChestChooseGroupActivity.prototype.activity_id = 0;
        ReqSelectChestChooseGroupActivity.prototype.selection = 0;
        ReqSelectChestChooseGroupActivity.prototype.chest_id = 0;

        ReqSelectChestChooseGroupActivity.create = function create(properties) {
            return new ReqSelectChestChooseGroupActivity(properties);
        };

        ReqSelectChestChooseGroupActivity.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.selection != null && Object.hasOwnProperty.call(message, "selection"))
                writer.uint32(16).uint32(message.selection);
            if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                writer.uint32(24).uint32(message.chest_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSelectChestChooseGroupActivity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSelectChestChooseGroupActivity.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSelectChestChooseGroupActivity(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selection = value;
                        else
                            delete message.selection;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chest_id = value;
                        else
                            delete message.chest_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSelectChestChooseGroupActivity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSelectChestChooseGroupActivity.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.selection != null && message.hasOwnProperty("selection"))
                if (!$util.isInteger(message.selection))
                    return "selection: integer expected";
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                if (!$util.isInteger(message.chest_id))
                    return "chest_id: integer expected";
            return null;
        };

        ReqSelectChestChooseGroupActivity.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSelectChestChooseGroupActivity)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSelectChestChooseGroupActivity();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.selection != null)
                if (Number(object.selection) !== 0)
                    message.selection = object.selection >>> 0;
            if (object.chest_id != null)
                if (Number(object.chest_id) !== 0)
                    message.chest_id = object.chest_id >>> 0;
            return message;
        };

        ReqSelectChestChooseGroupActivity.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.selection = 0;
                object.chest_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.selection != null && message.hasOwnProperty("selection"))
                object.selection = message.selection;
            if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                object.chest_id = message.chest_id;
            return object;
        };

        ReqSelectChestChooseGroupActivity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSelectChestChooseGroupActivity.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSelectChestChooseGroupActivity";
        };

        return ReqSelectChestChooseGroupActivity;
    })();

    lq.ReqGenerateAnnualReportToken = (function() {

        function ReqGenerateAnnualReportToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGenerateAnnualReportToken.prototype.lang = "";

        ReqGenerateAnnualReportToken.create = function create(properties) {
            return new ReqGenerateAnnualReportToken(properties);
        };

        ReqGenerateAnnualReportToken.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(10).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGenerateAnnualReportToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGenerateAnnualReportToken.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGenerateAnnualReportToken(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGenerateAnnualReportToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGenerateAnnualReportToken.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqGenerateAnnualReportToken.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGenerateAnnualReportToken)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGenerateAnnualReportToken();
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqGenerateAnnualReportToken.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.lang = "";
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqGenerateAnnualReportToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGenerateAnnualReportToken.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGenerateAnnualReportToken";
        };

        return ReqGenerateAnnualReportToken;
    })();

    lq.ResGenerateAnnualReportToken = (function() {

        function ResGenerateAnnualReportToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGenerateAnnualReportToken.prototype.error = null;
        ResGenerateAnnualReportToken.prototype.token = "";
        ResGenerateAnnualReportToken.prototype.url = "";

        ResGenerateAnnualReportToken.create = function create(properties) {
            return new ResGenerateAnnualReportToken(properties);
        };

        ResGenerateAnnualReportToken.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(26).string(message.url);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGenerateAnnualReportToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGenerateAnnualReportToken.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGenerateAnnualReportToken(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.url = value;
                        else
                            delete message.url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGenerateAnnualReportToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGenerateAnnualReportToken.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.url != null && message.hasOwnProperty("url"))
                if (!$util.isString(message.url))
                    return "url: string expected";
            return null;
        };

        ResGenerateAnnualReportToken.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGenerateAnnualReportToken)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGenerateAnnualReportToken();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGenerateAnnualReportToken.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.url != null)
                if (typeof object.url !== "string" || object.url.length)
                    message.url = String(object.url);
            return message;
        };

        ResGenerateAnnualReportToken.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.token = "";
                object.url = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.url != null && message.hasOwnProperty("url"))
                object.url = message.url;
            return object;
        };

        ResGenerateAnnualReportToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGenerateAnnualReportToken.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGenerateAnnualReportToken";
        };

        return ResGenerateAnnualReportToken;
    })();

    lq.ResFetchAnnualReportInfo = (function() {

        function ResFetchAnnualReportInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchAnnualReportInfo.prototype.error = null;
        ResFetchAnnualReportInfo.prototype.start_time = 0;
        ResFetchAnnualReportInfo.prototype.end_time = 0;

        ResFetchAnnualReportInfo.create = function create(properties) {
            return new ResFetchAnnualReportInfo(properties);
        };

        ResFetchAnnualReportInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(16).uint32(message.start_time);
            if (message.end_time != null && Object.hasOwnProperty.call(message, "end_time"))
                writer.uint32(24).uint32(message.end_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchAnnualReportInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchAnnualReportInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchAnnualReportInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.end_time = value;
                        else
                            delete message.end_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchAnnualReportInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchAnnualReportInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                if (!$util.isInteger(message.end_time))
                    return "end_time: integer expected";
            return null;
        };

        ResFetchAnnualReportInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchAnnualReportInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchAnnualReportInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchAnnualReportInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.end_time != null)
                if (Number(object.end_time) !== 0)
                    message.end_time = object.end_time >>> 0;
            return message;
        };

        ResFetchAnnualReportInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.start_time = 0;
                object.end_time = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.end_time != null && message.hasOwnProperty("end_time"))
                object.end_time = message.end_time;
            return object;
        };

        ResFetchAnnualReportInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchAnnualReportInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchAnnualReportInfo";
        };

        return ResFetchAnnualReportInfo;
    })();

    lq.ReqRemarkFriend = (function() {

        function ReqRemarkFriend(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRemarkFriend.prototype.account_id = 0;
        ReqRemarkFriend.prototype.remark = "";

        ReqRemarkFriend.create = function create(properties) {
            return new ReqRemarkFriend(properties);
        };

        ReqRemarkFriend.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.remark != null && Object.hasOwnProperty.call(message, "remark"))
                writer.uint32(18).string(message.remark);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRemarkFriend.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRemarkFriend.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRemarkFriend(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.remark = value;
                        else
                            delete message.remark;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRemarkFriend.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRemarkFriend.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.remark != null && message.hasOwnProperty("remark"))
                if (!$util.isString(message.remark))
                    return "remark: string expected";
            return null;
        };

        ReqRemarkFriend.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRemarkFriend)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRemarkFriend();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.remark != null)
                if (typeof object.remark !== "string" || object.remark.length)
                    message.remark = String(object.remark);
            return message;
        };

        ReqRemarkFriend.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.remark = "";
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.remark != null && message.hasOwnProperty("remark"))
                object.remark = message.remark;
            return object;
        };

        ReqRemarkFriend.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRemarkFriend.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRemarkFriend";
        };

        return ReqRemarkFriend;
    })();

    lq.ReqSimV2ActivityFetchInfo = (function() {

        function ReqSimV2ActivityFetchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityFetchInfo.prototype.activity_id = 0;

        ReqSimV2ActivityFetchInfo.create = function create(properties) {
            return new ReqSimV2ActivityFetchInfo(properties);
        };

        ReqSimV2ActivityFetchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityFetchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityFetchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityFetchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityFetchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityFetchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSimV2ActivityFetchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityFetchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityFetchInfo();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSimV2ActivityFetchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSimV2ActivityFetchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityFetchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityFetchInfo";
        };

        return ReqSimV2ActivityFetchInfo;
    })();

    lq.ResSimV2ActivityFetchInfo = (function() {

        function ResSimV2ActivityFetchInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivityFetchInfo.prototype.error = null;
        ResSimV2ActivityFetchInfo.prototype.data = null;

        ResSimV2ActivityFetchInfo.create = function create(properties) {
            return new ResSimV2ActivityFetchInfo(properties);
        };

        ResSimV2ActivityFetchInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.lq.SimulationV2Data.encode(message.data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivityFetchInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivityFetchInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityFetchInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.lq.SimulationV2Data.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivityFetchInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivityFetchInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.lq.SimulationV2Data.verify(message.data, _depth + 1);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        ResSimV2ActivityFetchInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivityFetchInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivityFetchInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivityFetchInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".lq.ResSimV2ActivityFetchInfo.data: object expected");
                message.data = $root.lq.SimulationV2Data.fromObject(object.data, _depth + 1);
            }
            return message;
        };

        ResSimV2ActivityFetchInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.lq.SimulationV2Data.toObject(message.data, options);
            return object;
        };

        ResSimV2ActivityFetchInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivityFetchInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivityFetchInfo";
        };

        return ResSimV2ActivityFetchInfo;
    })();

    lq.ReqSimV2ActivityStartSeason = (function() {

        function ReqSimV2ActivityStartSeason(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityStartSeason.prototype.activity_id = 0;

        ReqSimV2ActivityStartSeason.create = function create(properties) {
            return new ReqSimV2ActivityStartSeason(properties);
        };

        ReqSimV2ActivityStartSeason.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityStartSeason.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityStartSeason.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityStartSeason(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityStartSeason.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityStartSeason.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSimV2ActivityStartSeason.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityStartSeason)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityStartSeason();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSimV2ActivityStartSeason.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSimV2ActivityStartSeason.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityStartSeason.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityStartSeason";
        };

        return ReqSimV2ActivityStartSeason;
    })();

    lq.ResSimV2ActivityStartSeason = (function() {

        function ResSimV2ActivityStartSeason(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivityStartSeason.prototype.error = null;
        ResSimV2ActivityStartSeason.prototype.season = null;

        ResSimV2ActivityStartSeason.create = function create(properties) {
            return new ResSimV2ActivityStartSeason(properties);
        };

        ResSimV2ActivityStartSeason.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.season != null && Object.hasOwnProperty.call(message, "season"))
                $root.lq.SimulationV2SeasonData.encode(message.season, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivityStartSeason.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivityStartSeason.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityStartSeason(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.season = $root.lq.SimulationV2SeasonData.decode(reader, reader.uint32(), undefined, _depth + 1, message.season);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivityStartSeason.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivityStartSeason.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.season != null && message.hasOwnProperty("season")) {
                let error = $root.lq.SimulationV2SeasonData.verify(message.season, _depth + 1);
                if (error)
                    return "season." + error;
            }
            return null;
        };

        ResSimV2ActivityStartSeason.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivityStartSeason)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivityStartSeason();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivityStartSeason.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.season != null) {
                if (typeof object.season !== "object")
                    throw TypeError(".lq.ResSimV2ActivityStartSeason.season: object expected");
                message.season = $root.lq.SimulationV2SeasonData.fromObject(object.season, _depth + 1);
            }
            return message;
        };

        ResSimV2ActivityStartSeason.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.season = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.season != null && message.hasOwnProperty("season"))
                object.season = $root.lq.SimulationV2SeasonData.toObject(message.season, options);
            return object;
        };

        ResSimV2ActivityStartSeason.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivityStartSeason.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivityStartSeason";
        };

        return ResSimV2ActivityStartSeason;
    })();

    lq.ReqSimV2ActivityTrain = (function() {

        function ReqSimV2ActivityTrain(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityTrain.prototype.activity_id = 0;
        ReqSimV2ActivityTrain.prototype.ability = 0;
        ReqSimV2ActivityTrain.prototype.skip = 0;

        ReqSimV2ActivityTrain.create = function create(properties) {
            return new ReqSimV2ActivityTrain(properties);
        };

        ReqSimV2ActivityTrain.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.ability != null && Object.hasOwnProperty.call(message, "ability"))
                writer.uint32(16).uint32(message.ability);
            if (message.skip != null && Object.hasOwnProperty.call(message, "skip"))
                writer.uint32(24).uint32(message.skip);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityTrain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityTrain.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityTrain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ability = value;
                        else
                            delete message.ability;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.skip = value;
                        else
                            delete message.skip;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityTrain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityTrain.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.ability != null && message.hasOwnProperty("ability"))
                if (!$util.isInteger(message.ability))
                    return "ability: integer expected";
            if (message.skip != null && message.hasOwnProperty("skip"))
                if (!$util.isInteger(message.skip))
                    return "skip: integer expected";
            return null;
        };

        ReqSimV2ActivityTrain.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityTrain)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityTrain();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.ability != null)
                if (Number(object.ability) !== 0)
                    message.ability = object.ability >>> 0;
            if (object.skip != null)
                if (Number(object.skip) !== 0)
                    message.skip = object.skip >>> 0;
            return message;
        };

        ReqSimV2ActivityTrain.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.ability = 0;
                object.skip = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.ability != null && message.hasOwnProperty("ability"))
                object.ability = message.ability;
            if (message.skip != null && message.hasOwnProperty("skip"))
                object.skip = message.skip;
            return object;
        };

        ReqSimV2ActivityTrain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityTrain.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityTrain";
        };

        return ReqSimV2ActivityTrain;
    })();

    lq.ResSimV2ActivityTrain = (function() {

        function ResSimV2ActivityTrain(properties) {
            this.effect_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivityTrain.prototype.error = null;
        ResSimV2ActivityTrain.prototype.event = null;
        ResSimV2ActivityTrain.prototype.ability = null;
        ResSimV2ActivityTrain.prototype.round = 0;
        ResSimV2ActivityTrain.prototype.effect_list = $util.emptyArray;
        ResSimV2ActivityTrain.prototype.train_result = 0;
        ResSimV2ActivityTrain.prototype.is_end = false;
        ResSimV2ActivityTrain.prototype.record = null;

        ResSimV2ActivityTrain.create = function create(properties) {
            return new ResSimV2ActivityTrain(properties);
        };

        ResSimV2ActivityTrain.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.event != null && Object.hasOwnProperty.call(message, "event"))
                $root.lq.SimulationV2Event.encode(message.event, writer.uint32(18).fork()).ldelim();
            if (message.ability != null && Object.hasOwnProperty.call(message, "ability"))
                $root.lq.SimulationV2Ability.encode(message.ability, writer.uint32(26).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(32).uint32(message.round);
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.SimulationV2Effect.encode(message.effect_list[i], writer.uint32(42).fork()).ldelim();
            if (message.train_result != null && Object.hasOwnProperty.call(message, "train_result"))
                writer.uint32(48).uint32(message.train_result);
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(56).bool(message.is_end);
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.SimulationV2Record.encode(message.record, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivityTrain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivityTrain.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityTrain(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.event = $root.lq.SimulationV2Event.decode(reader, reader.uint32(), undefined, _depth + 1, message.event);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.ability = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.ability);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.SimulationV2Effect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.train_result = value;
                        else
                            delete message.train_result;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.SimulationV2Record.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivityTrain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivityTrain.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.event != null && message.hasOwnProperty("event")) {
                let error = $root.lq.SimulationV2Event.verify(message.event, _depth + 1);
                if (error)
                    return "event." + error;
            }
            if (message.ability != null && message.hasOwnProperty("ability")) {
                let error = $root.lq.SimulationV2Ability.verify(message.ability, _depth + 1);
                if (error)
                    return "ability." + error;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.SimulationV2Effect.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.train_result != null && message.hasOwnProperty("train_result"))
                if (!$util.isInteger(message.train_result))
                    return "train_result: integer expected";
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (typeof message.is_end !== "boolean")
                    return "is_end: boolean expected";
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.SimulationV2Record.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            return null;
        };

        ResSimV2ActivityTrain.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivityTrain)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivityTrain();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivityTrain.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.event != null) {
                if (typeof object.event !== "object")
                    throw TypeError(".lq.ResSimV2ActivityTrain.event: object expected");
                message.event = $root.lq.SimulationV2Event.fromObject(object.event, _depth + 1);
            }
            if (object.ability != null) {
                if (typeof object.ability !== "object")
                    throw TypeError(".lq.ResSimV2ActivityTrain.ability: object expected");
                message.ability = $root.lq.SimulationV2Ability.fromObject(object.ability, _depth + 1);
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.ResSimV2ActivityTrain.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivityTrain.effect_list: object expected");
                    message.effect_list[i] = $root.lq.SimulationV2Effect.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.train_result != null)
                if (Number(object.train_result) !== 0)
                    message.train_result = object.train_result >>> 0;
            if (object.is_end != null)
                if (object.is_end)
                    message.is_end = Boolean(object.is_end);
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.ResSimV2ActivityTrain.record: object expected");
                message.record = $root.lq.SimulationV2Record.fromObject(object.record, _depth + 1);
            }
            return message;
        };

        ResSimV2ActivityTrain.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.effect_list = [];
            if (options.defaults) {
                object.error = null;
                object.event = null;
                object.ability = null;
                object.round = 0;
                object.train_result = 0;
                object.is_end = false;
                object.record = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.event != null && message.hasOwnProperty("event"))
                object.event = $root.lq.SimulationV2Event.toObject(message.event, options);
            if (message.ability != null && message.hasOwnProperty("ability"))
                object.ability = $root.lq.SimulationV2Ability.toObject(message.ability, options);
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.SimulationV2Effect.toObject(message.effect_list[j], options);
            }
            if (message.train_result != null && message.hasOwnProperty("train_result"))
                object.train_result = message.train_result;
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.SimulationV2Record.toObject(message.record, options);
            return object;
        };

        ResSimV2ActivityTrain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivityTrain.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivityTrain";
        };

        return ResSimV2ActivityTrain;
    })();

    lq.ReqSimV2ActivitySelectEvent = (function() {

        function ReqSimV2ActivitySelectEvent(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivitySelectEvent.prototype.activity_id = 0;
        ReqSimV2ActivitySelectEvent.prototype.selection_id = 0;

        ReqSimV2ActivitySelectEvent.create = function create(properties) {
            return new ReqSimV2ActivitySelectEvent(properties);
        };

        ReqSimV2ActivitySelectEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.selection_id != null && Object.hasOwnProperty.call(message, "selection_id"))
                writer.uint32(16).uint32(message.selection_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivitySelectEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivitySelectEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivitySelectEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.selection_id = value;
                        else
                            delete message.selection_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivitySelectEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivitySelectEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.selection_id != null && message.hasOwnProperty("selection_id"))
                if (!$util.isInteger(message.selection_id))
                    return "selection_id: integer expected";
            return null;
        };

        ReqSimV2ActivitySelectEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivitySelectEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivitySelectEvent();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.selection_id != null)
                if (Number(object.selection_id) !== 0)
                    message.selection_id = object.selection_id >>> 0;
            return message;
        };

        ReqSimV2ActivitySelectEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.selection_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.selection_id != null && message.hasOwnProperty("selection_id"))
                object.selection_id = message.selection_id;
            return object;
        };

        ReqSimV2ActivitySelectEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivitySelectEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivitySelectEvent";
        };

        return ReqSimV2ActivitySelectEvent;
    })();

    lq.ResSimV2ActivitySelectEvent = (function() {

        function ResSimV2ActivitySelectEvent(properties) {
            this.effect_list = [];
            this.effected_buff_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivitySelectEvent.prototype.error = null;
        ResSimV2ActivitySelectEvent.prototype.event = null;
        ResSimV2ActivitySelectEvent.prototype.ability = null;
        ResSimV2ActivitySelectEvent.prototype.match = null;
        ResSimV2ActivitySelectEvent.prototype.effect_list = $util.emptyArray;
        ResSimV2ActivitySelectEvent.prototype.round = 0;
        ResSimV2ActivitySelectEvent.prototype.is_end = false;
        ResSimV2ActivitySelectEvent.prototype.result_id = 0;
        ResSimV2ActivitySelectEvent.prototype.record = null;
        ResSimV2ActivitySelectEvent.prototype.effected_buff_list = $util.emptyArray;

        ResSimV2ActivitySelectEvent.create = function create(properties) {
            return new ResSimV2ActivitySelectEvent(properties);
        };

        ResSimV2ActivitySelectEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.event != null && Object.hasOwnProperty.call(message, "event"))
                $root.lq.SimulationV2Event.encode(message.event, writer.uint32(18).fork()).ldelim();
            if (message.ability != null && Object.hasOwnProperty.call(message, "ability"))
                $root.lq.SimulationV2Ability.encode(message.ability, writer.uint32(26).fork()).ldelim();
            if (message.match != null && Object.hasOwnProperty.call(message, "match"))
                $root.lq.SimulationV2Match.encode(message.match, writer.uint32(34).fork()).ldelim();
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.SimulationV2Effect.encode(message.effect_list[i], writer.uint32(42).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(56).uint32(message.round);
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(64).bool(message.is_end);
            if (message.result_id != null && Object.hasOwnProperty.call(message, "result_id"))
                writer.uint32(72).uint32(message.result_id);
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.SimulationV2Record.encode(message.record, writer.uint32(82).fork()).ldelim();
            if (message.effected_buff_list != null && message.effected_buff_list.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.effected_buff_list.length; ++i)
                    writer.uint32(message.effected_buff_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivitySelectEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivitySelectEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivitySelectEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.event = $root.lq.SimulationV2Event.decode(reader, reader.uint32(), undefined, _depth + 1, message.event);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.ability = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.ability);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.match = $root.lq.SimulationV2Match.decode(reader, reader.uint32(), undefined, _depth + 1, message.match);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.SimulationV2Effect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result_id = value;
                        else
                            delete message.result_id;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.SimulationV2Record.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.effected_buff_list && message.effected_buff_list.length))
                                message.effected_buff_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.effected_buff_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.effected_buff_list && message.effected_buff_list.length))
                            message.effected_buff_list = [];
                        message.effected_buff_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivitySelectEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivitySelectEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.event != null && message.hasOwnProperty("event")) {
                let error = $root.lq.SimulationV2Event.verify(message.event, _depth + 1);
                if (error)
                    return "event." + error;
            }
            if (message.ability != null && message.hasOwnProperty("ability")) {
                let error = $root.lq.SimulationV2Ability.verify(message.ability, _depth + 1);
                if (error)
                    return "ability." + error;
            }
            if (message.match != null && message.hasOwnProperty("match")) {
                let error = $root.lq.SimulationV2Match.verify(message.match, _depth + 1);
                if (error)
                    return "match." + error;
            }
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.SimulationV2Effect.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (typeof message.is_end !== "boolean")
                    return "is_end: boolean expected";
            if (message.result_id != null && message.hasOwnProperty("result_id"))
                if (!$util.isInteger(message.result_id))
                    return "result_id: integer expected";
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.SimulationV2Record.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.effected_buff_list != null && message.hasOwnProperty("effected_buff_list")) {
                if (!Array.isArray(message.effected_buff_list))
                    return "effected_buff_list: array expected";
                for (let i = 0; i < message.effected_buff_list.length; ++i)
                    if (!$util.isInteger(message.effected_buff_list[i]))
                        return "effected_buff_list: integer[] expected";
            }
            return null;
        };

        ResSimV2ActivitySelectEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivitySelectEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivitySelectEvent();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.event != null) {
                if (typeof object.event !== "object")
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.event: object expected");
                message.event = $root.lq.SimulationV2Event.fromObject(object.event, _depth + 1);
            }
            if (object.ability != null) {
                if (typeof object.ability !== "object")
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.ability: object expected");
                message.ability = $root.lq.SimulationV2Ability.fromObject(object.ability, _depth + 1);
            }
            if (object.match != null) {
                if (typeof object.match !== "object")
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.match: object expected");
                message.match = $root.lq.SimulationV2Match.fromObject(object.match, _depth + 1);
            }
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivitySelectEvent.effect_list: object expected");
                    message.effect_list[i] = $root.lq.SimulationV2Effect.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.is_end != null)
                if (object.is_end)
                    message.is_end = Boolean(object.is_end);
            if (object.result_id != null)
                if (Number(object.result_id) !== 0)
                    message.result_id = object.result_id >>> 0;
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.record: object expected");
                message.record = $root.lq.SimulationV2Record.fromObject(object.record, _depth + 1);
            }
            if (object.effected_buff_list) {
                if (!Array.isArray(object.effected_buff_list))
                    throw TypeError(".lq.ResSimV2ActivitySelectEvent.effected_buff_list: array expected");
                message.effected_buff_list = Array(object.effected_buff_list.length);
                for (let i = 0; i < object.effected_buff_list.length; ++i)
                    message.effected_buff_list[i] = object.effected_buff_list[i] >>> 0;
            }
            return message;
        };

        ResSimV2ActivitySelectEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effect_list = [];
                object.effected_buff_list = [];
            }
            if (options.defaults) {
                object.error = null;
                object.event = null;
                object.ability = null;
                object.match = null;
                object.round = 0;
                object.is_end = false;
                object.result_id = 0;
                object.record = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.event != null && message.hasOwnProperty("event"))
                object.event = $root.lq.SimulationV2Event.toObject(message.event, options);
            if (message.ability != null && message.hasOwnProperty("ability"))
                object.ability = $root.lq.SimulationV2Ability.toObject(message.ability, options);
            if (message.match != null && message.hasOwnProperty("match"))
                object.match = $root.lq.SimulationV2Match.toObject(message.match, options);
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.SimulationV2Effect.toObject(message.effect_list[j], options);
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            if (message.result_id != null && message.hasOwnProperty("result_id"))
                object.result_id = message.result_id;
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.SimulationV2Record.toObject(message.record, options);
            if (message.effected_buff_list && message.effected_buff_list.length) {
                object.effected_buff_list = Array(message.effected_buff_list.length);
                for (let j = 0; j < message.effected_buff_list.length; ++j)
                    object.effected_buff_list[j] = message.effected_buff_list[j];
            }
            return object;
        };

        ResSimV2ActivitySelectEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivitySelectEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivitySelectEvent";
        };

        return ResSimV2ActivitySelectEvent;
    })();

    lq.ReqSimV2ActivityStartMatch = (function() {

        function ReqSimV2ActivityStartMatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityStartMatch.prototype.activity_id = 0;

        ReqSimV2ActivityStartMatch.create = function create(properties) {
            return new ReqSimV2ActivityStartMatch(properties);
        };

        ReqSimV2ActivityStartMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityStartMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityStartMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityStartMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityStartMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityStartMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSimV2ActivityStartMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityStartMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityStartMatch();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSimV2ActivityStartMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSimV2ActivityStartMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityStartMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityStartMatch";
        };

        return ReqSimV2ActivityStartMatch;
    })();

    lq.ResSimV2ActivityStartMatch = (function() {

        function ResSimV2ActivityStartMatch(properties) {
            this.effect_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivityStartMatch.prototype.error = null;
        ResSimV2ActivityStartMatch.prototype.event = null;
        ResSimV2ActivityStartMatch.prototype.match = null;
        ResSimV2ActivityStartMatch.prototype.effect_list = $util.emptyArray;
        ResSimV2ActivityStartMatch.prototype.is_match_end = false;

        ResSimV2ActivityStartMatch.create = function create(properties) {
            return new ResSimV2ActivityStartMatch(properties);
        };

        ResSimV2ActivityStartMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.event != null && Object.hasOwnProperty.call(message, "event"))
                $root.lq.SimulationV2Event.encode(message.event, writer.uint32(18).fork()).ldelim();
            if (message.match != null && Object.hasOwnProperty.call(message, "match"))
                $root.lq.SimulationV2Match.encode(message.match, writer.uint32(34).fork()).ldelim();
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.SimulationV2Effect.encode(message.effect_list[i], writer.uint32(42).fork()).ldelim();
            if (message.is_match_end != null && Object.hasOwnProperty.call(message, "is_match_end"))
                writer.uint32(48).bool(message.is_match_end);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivityStartMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivityStartMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityStartMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.event = $root.lq.SimulationV2Event.decode(reader, reader.uint32(), undefined, _depth + 1, message.event);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.match = $root.lq.SimulationV2Match.decode(reader, reader.uint32(), undefined, _depth + 1, message.match);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.SimulationV2Effect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_match_end = value;
                        else
                            delete message.is_match_end;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivityStartMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivityStartMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.event != null && message.hasOwnProperty("event")) {
                let error = $root.lq.SimulationV2Event.verify(message.event, _depth + 1);
                if (error)
                    return "event." + error;
            }
            if (message.match != null && message.hasOwnProperty("match")) {
                let error = $root.lq.SimulationV2Match.verify(message.match, _depth + 1);
                if (error)
                    return "match." + error;
            }
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.SimulationV2Effect.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.is_match_end != null && message.hasOwnProperty("is_match_end"))
                if (typeof message.is_match_end !== "boolean")
                    return "is_match_end: boolean expected";
            return null;
        };

        ResSimV2ActivityStartMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivityStartMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivityStartMatch();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivityStartMatch.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.event != null) {
                if (typeof object.event !== "object")
                    throw TypeError(".lq.ResSimV2ActivityStartMatch.event: object expected");
                message.event = $root.lq.SimulationV2Event.fromObject(object.event, _depth + 1);
            }
            if (object.match != null) {
                if (typeof object.match !== "object")
                    throw TypeError(".lq.ResSimV2ActivityStartMatch.match: object expected");
                message.match = $root.lq.SimulationV2Match.fromObject(object.match, _depth + 1);
            }
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.ResSimV2ActivityStartMatch.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivityStartMatch.effect_list: object expected");
                    message.effect_list[i] = $root.lq.SimulationV2Effect.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.is_match_end != null)
                if (object.is_match_end)
                    message.is_match_end = Boolean(object.is_match_end);
            return message;
        };

        ResSimV2ActivityStartMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.effect_list = [];
            if (options.defaults) {
                object.error = null;
                object.event = null;
                object.match = null;
                object.is_match_end = false;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.event != null && message.hasOwnProperty("event"))
                object.event = $root.lq.SimulationV2Event.toObject(message.event, options);
            if (message.match != null && message.hasOwnProperty("match"))
                object.match = $root.lq.SimulationV2Match.toObject(message.match, options);
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.SimulationV2Effect.toObject(message.effect_list[j], options);
            }
            if (message.is_match_end != null && message.hasOwnProperty("is_match_end"))
                object.is_match_end = message.is_match_end;
            return object;
        };

        ResSimV2ActivityStartMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivityStartMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivityStartMatch";
        };

        return ResSimV2ActivityStartMatch;
    })();

    lq.ReqSimV2ActivityEndMatch = (function() {

        function ReqSimV2ActivityEndMatch(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityEndMatch.prototype.activity_id = 0;

        ReqSimV2ActivityEndMatch.create = function create(properties) {
            return new ReqSimV2ActivityEndMatch(properties);
        };

        ReqSimV2ActivityEndMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityEndMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityEndMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityEndMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityEndMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityEndMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSimV2ActivityEndMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityEndMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityEndMatch();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSimV2ActivityEndMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSimV2ActivityEndMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityEndMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityEndMatch";
        };

        return ReqSimV2ActivityEndMatch;
    })();

    lq.ResSimV2ActivityEndMatch = (function() {

        function ResSimV2ActivityEndMatch(properties) {
            this.match_history = [];
            this.rewards = [];
            this.effect_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSimV2ActivityEndMatch.prototype.error = null;
        ResSimV2ActivityEndMatch.prototype.round = 0;
        ResSimV2ActivityEndMatch.prototype.is_end = false;
        ResSimV2ActivityEndMatch.prototype.record = null;
        ResSimV2ActivityEndMatch.prototype.total_score = 0;
        ResSimV2ActivityEndMatch.prototype.match_history = $util.emptyArray;
        ResSimV2ActivityEndMatch.prototype.rewards = $util.emptyArray;
        ResSimV2ActivityEndMatch.prototype.effect_list = $util.emptyArray;
        ResSimV2ActivityEndMatch.prototype.ability = null;

        ResSimV2ActivityEndMatch.create = function create(properties) {
            return new ResSimV2ActivityEndMatch(properties);
        };

        ResSimV2ActivityEndMatch.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                writer.uint32(16).uint32(message.round);
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(24).bool(message.is_end);
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.SimulationV2Record.encode(message.record, writer.uint32(34).fork()).ldelim();
            if (message.total_score != null && Object.hasOwnProperty.call(message, "total_score"))
                writer.uint32(40).int32(message.total_score);
            if (message.match_history != null && message.match_history.length)
                for (let i = 0; i < message.match_history.length; ++i)
                    $root.lq.SimulationV2MatchRecord.encode(message.match_history[i], writer.uint32(50).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.encode(message.rewards[i], writer.uint32(58).fork()).ldelim();
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.SimulationV2Effect.encode(message.effect_list[i], writer.uint32(66).fork()).ldelim();
            if (message.ability != null && Object.hasOwnProperty.call(message, "ability"))
                $root.lq.SimulationV2Ability.encode(message.ability, writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSimV2ActivityEndMatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSimV2ActivityEndMatch.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityEndMatch(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round = value;
                        else
                            delete message.round;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.SimulationV2Record.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.total_score = value;
                        else
                            delete message.total_score;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.match_history && message.match_history.length))
                            message.match_history = [];
                        message.match_history.push($root.lq.SimulationV2MatchRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.SimulationV2Effect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.ability = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.ability);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSimV2ActivityEndMatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSimV2ActivityEndMatch.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                if (!$util.isInteger(message.round))
                    return "round: integer expected";
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (typeof message.is_end !== "boolean")
                    return "is_end: boolean expected";
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.SimulationV2Record.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.total_score != null && message.hasOwnProperty("total_score"))
                if (!$util.isInteger(message.total_score))
                    return "total_score: integer expected";
            if (message.match_history != null && message.hasOwnProperty("match_history")) {
                if (!Array.isArray(message.match_history))
                    return "match_history: array expected";
                for (let i = 0; i < message.match_history.length; ++i) {
                    let error = $root.lq.SimulationV2MatchRecord.verify(message.match_history[i], _depth + 1);
                    if (error)
                        return "match_history." + error;
                }
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.SimulationV2Effect.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.ability != null && message.hasOwnProperty("ability")) {
                let error = $root.lq.SimulationV2Ability.verify(message.ability, _depth + 1);
                if (error)
                    return "ability." + error;
            }
            return null;
        };

        ResSimV2ActivityEndMatch.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSimV2ActivityEndMatch)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSimV2ActivityEndMatch();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.round != null)
                if (Number(object.round) !== 0)
                    message.round = object.round >>> 0;
            if (object.is_end != null)
                if (object.is_end)
                    message.is_end = Boolean(object.is_end);
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.record: object expected");
                message.record = $root.lq.SimulationV2Record.fromObject(object.record, _depth + 1);
            }
            if (object.total_score != null)
                if (Number(object.total_score) !== 0)
                    message.total_score = object.total_score | 0;
            if (object.match_history) {
                if (!Array.isArray(object.match_history))
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.match_history: array expected");
                message.match_history = Array(object.match_history.length);
                for (let i = 0; i < object.match_history.length; ++i) {
                    if (typeof object.match_history[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivityEndMatch.match_history: object expected");
                    message.match_history[i] = $root.lq.SimulationV2MatchRecord.fromObject(object.match_history[i], _depth + 1);
                }
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivityEndMatch.rewards: object expected");
                    message.rewards[i] = $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.ResSimV2ActivityEndMatch.effect_list: object expected");
                    message.effect_list[i] = $root.lq.SimulationV2Effect.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.ability != null) {
                if (typeof object.ability !== "object")
                    throw TypeError(".lq.ResSimV2ActivityEndMatch.ability: object expected");
                message.ability = $root.lq.SimulationV2Ability.fromObject(object.ability, _depth + 1);
            }
            return message;
        };

        ResSimV2ActivityEndMatch.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.match_history = [];
                object.rewards = [];
                object.effect_list = [];
            }
            if (options.defaults) {
                object.error = null;
                object.round = 0;
                object.is_end = false;
                object.record = null;
                object.total_score = 0;
                object.ability = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = message.round;
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.SimulationV2Record.toObject(message.record, options);
            if (message.total_score != null && message.hasOwnProperty("total_score"))
                object.total_score = message.total_score;
            if (message.match_history && message.match_history.length) {
                object.match_history = Array(message.match_history.length);
                for (let j = 0; j < message.match_history.length; ++j)
                    object.match_history[j] = $root.lq.SimulationV2MatchRecord.toObject(message.match_history[j], options);
            }
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.toObject(message.rewards[j], options);
            }
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.SimulationV2Effect.toObject(message.effect_list[j], options);
            }
            if (message.ability != null && message.hasOwnProperty("ability"))
                object.ability = $root.lq.SimulationV2Ability.toObject(message.ability, options);
            return object;
        };

        ResSimV2ActivityEndMatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSimV2ActivityEndMatch.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSimV2ActivityEndMatch";
        };

        ResSimV2ActivityEndMatch.SimulationV2MatchReward = (function() {

            function SimulationV2MatchReward(properties) {
                this.params = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SimulationV2MatchReward.prototype.type = 0;
            SimulationV2MatchReward.prototype.params = $util.emptyArray;

            SimulationV2MatchReward.create = function create(properties) {
                return new SimulationV2MatchReward(properties);
            };

            SimulationV2MatchReward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.params != null && message.params.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.params.length; ++i)
                        writer.uint32(message.params[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SimulationV2MatchReward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SimulationV2MatchReward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.params && message.params.length))
                                    message.params = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.params.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SimulationV2MatchReward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SimulationV2MatchReward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.params != null && message.hasOwnProperty("params")) {
                    if (!Array.isArray(message.params))
                        return "params: array expected";
                    for (let i = 0; i < message.params.length; ++i)
                        if (!$util.isInteger(message.params[i]))
                            return "params: integer[] expected";
                }
                return null;
            };

            SimulationV2MatchReward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.params) {
                    if (!Array.isArray(object.params))
                        throw TypeError(".lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward.params: array expected");
                    message.params = Array(object.params.length);
                    for (let i = 0; i < object.params.length; ++i)
                        message.params[i] = object.params[i] >>> 0;
                }
                return message;
            };

            SimulationV2MatchReward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.params = [];
                if (options.defaults)
                    object.type = 0;
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.params && message.params.length) {
                    object.params = Array(message.params.length);
                    for (let j = 0; j < message.params.length; ++j)
                        object.params[j] = message.params[j];
                }
                return object;
            };

            SimulationV2MatchReward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SimulationV2MatchReward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResSimV2ActivityEndMatch.SimulationV2MatchReward";
            };

            return SimulationV2MatchReward;
        })();

        return ResSimV2ActivityEndMatch;
    })();

    lq.ReqSimV2ActivityGiveUp = (function() {

        function ReqSimV2ActivityGiveUp(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivityGiveUp.prototype.activity_id = 0;

        ReqSimV2ActivityGiveUp.create = function create(properties) {
            return new ReqSimV2ActivityGiveUp(properties);
        };

        ReqSimV2ActivityGiveUp.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivityGiveUp.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivityGiveUp.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivityGiveUp(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivityGiveUp.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivityGiveUp.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSimV2ActivityGiveUp.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivityGiveUp)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivityGiveUp();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSimV2ActivityGiveUp.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSimV2ActivityGiveUp.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivityGiveUp.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivityGiveUp";
        };

        return ReqSimV2ActivityGiveUp;
    })();

    lq.ReqSimV2ActivitySetUpgrade = (function() {

        function ReqSimV2ActivitySetUpgrade(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSimV2ActivitySetUpgrade.prototype.activity_id = 0;
        ReqSimV2ActivitySetUpgrade.prototype.upgrade = null;

        ReqSimV2ActivitySetUpgrade.create = function create(properties) {
            return new ReqSimV2ActivitySetUpgrade(properties);
        };

        ReqSimV2ActivitySetUpgrade.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.upgrade != null && Object.hasOwnProperty.call(message, "upgrade"))
                $root.lq.SimulationV2Ability.encode(message.upgrade, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSimV2ActivitySetUpgrade.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSimV2ActivitySetUpgrade.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSimV2ActivitySetUpgrade(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.upgrade = $root.lq.SimulationV2Ability.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSimV2ActivitySetUpgrade.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSimV2ActivitySetUpgrade.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                let error = $root.lq.SimulationV2Ability.verify(message.upgrade, _depth + 1);
                if (error)
                    return "upgrade." + error;
            }
            return null;
        };

        ReqSimV2ActivitySetUpgrade.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSimV2ActivitySetUpgrade)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSimV2ActivitySetUpgrade();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.upgrade != null) {
                if (typeof object.upgrade !== "object")
                    throw TypeError(".lq.ReqSimV2ActivitySetUpgrade.upgrade: object expected");
                message.upgrade = $root.lq.SimulationV2Ability.fromObject(object.upgrade, _depth + 1);
            }
            return message;
        };

        ReqSimV2ActivitySetUpgrade.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.upgrade = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.upgrade != null && message.hasOwnProperty("upgrade"))
                object.upgrade = $root.lq.SimulationV2Ability.toObject(message.upgrade, options);
            return object;
        };

        ReqSimV2ActivitySetUpgrade.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSimV2ActivitySetUpgrade.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSimV2ActivitySetUpgrade";
        };

        return ReqSimV2ActivitySetUpgrade;
    })();

    lq.ReqProgressRewardActivityReceive = (function() {

        function ReqProgressRewardActivityReceive(properties) {
            this.progresses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqProgressRewardActivityReceive.prototype.activity_id = 0;
        ReqProgressRewardActivityReceive.prototype.progresses = $util.emptyArray;

        ReqProgressRewardActivityReceive.create = function create(properties) {
            return new ReqProgressRewardActivityReceive(properties);
        };

        ReqProgressRewardActivityReceive.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.progresses != null && message.progresses.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.progresses.length; ++i)
                    writer.uint32(message.progresses[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqProgressRewardActivityReceive.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqProgressRewardActivityReceive.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqProgressRewardActivityReceive(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.progresses && message.progresses.length))
                                message.progresses = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.progresses.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.progresses && message.progresses.length))
                            message.progresses = [];
                        message.progresses.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqProgressRewardActivityReceive.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqProgressRewardActivityReceive.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.progresses != null && message.hasOwnProperty("progresses")) {
                if (!Array.isArray(message.progresses))
                    return "progresses: array expected";
                for (let i = 0; i < message.progresses.length; ++i)
                    if (!$util.isInteger(message.progresses[i]))
                        return "progresses: integer[] expected";
            }
            return null;
        };

        ReqProgressRewardActivityReceive.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqProgressRewardActivityReceive)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqProgressRewardActivityReceive();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.progresses) {
                if (!Array.isArray(object.progresses))
                    throw TypeError(".lq.ReqProgressRewardActivityReceive.progresses: array expected");
                message.progresses = Array(object.progresses.length);
                for (let i = 0; i < object.progresses.length; ++i)
                    message.progresses[i] = object.progresses[i] >>> 0;
            }
            return message;
        };

        ReqProgressRewardActivityReceive.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.progresses = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.progresses && message.progresses.length) {
                object.progresses = Array(message.progresses.length);
                for (let j = 0; j < message.progresses.length; ++j)
                    object.progresses[j] = message.progresses[j];
            }
            return object;
        };

        ReqProgressRewardActivityReceive.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqProgressRewardActivityReceive.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqProgressRewardActivityReceive";
        };

        return ReqProgressRewardActivityReceive;
    })();

    lq.ResProgressRewardActivityReceive = (function() {

        function ResProgressRewardActivityReceive(properties) {
            this.reward_items = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResProgressRewardActivityReceive.prototype.error = null;
        ResProgressRewardActivityReceive.prototype.reward_items = $util.emptyArray;

        ResProgressRewardActivityReceive.create = function create(properties) {
            return new ResProgressRewardActivityReceive(properties);
        };

        ResProgressRewardActivityReceive.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.reward_items != null && message.reward_items.length)
                for (let i = 0; i < message.reward_items.length; ++i)
                    $root.lq.ExecuteReward.encode(message.reward_items[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResProgressRewardActivityReceive.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResProgressRewardActivityReceive.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResProgressRewardActivityReceive(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.reward_items && message.reward_items.length))
                            message.reward_items = [];
                        message.reward_items.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResProgressRewardActivityReceive.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResProgressRewardActivityReceive.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.reward_items != null && message.hasOwnProperty("reward_items")) {
                if (!Array.isArray(message.reward_items))
                    return "reward_items: array expected";
                for (let i = 0; i < message.reward_items.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.reward_items[i], _depth + 1);
                    if (error)
                        return "reward_items." + error;
                }
            }
            return null;
        };

        ResProgressRewardActivityReceive.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResProgressRewardActivityReceive)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResProgressRewardActivityReceive();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResProgressRewardActivityReceive.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.reward_items) {
                if (!Array.isArray(object.reward_items))
                    throw TypeError(".lq.ResProgressRewardActivityReceive.reward_items: array expected");
                message.reward_items = Array(object.reward_items.length);
                for (let i = 0; i < object.reward_items.length; ++i) {
                    if (typeof object.reward_items[i] !== "object")
                        throw TypeError(".lq.ResProgressRewardActivityReceive.reward_items: object expected");
                    message.reward_items[i] = $root.lq.ExecuteReward.fromObject(object.reward_items[i], _depth + 1);
                }
            }
            return message;
        };

        ResProgressRewardActivityReceive.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.reward_items = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.reward_items && message.reward_items.length) {
                object.reward_items = Array(message.reward_items.length);
                for (let j = 0; j < message.reward_items.length; ++j)
                    object.reward_items[j] = $root.lq.ExecuteReward.toObject(message.reward_items[j], options);
            }
            return object;
        };

        ResProgressRewardActivityReceive.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResProgressRewardActivityReceive.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResProgressRewardActivityReceive";
        };

        return ResProgressRewardActivityReceive;
    })();

    lq.ReqFetchProgressRewardActivityInfo = (function() {

        function ReqFetchProgressRewardActivityInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchProgressRewardActivityInfo.prototype.activity_id = 0;

        ReqFetchProgressRewardActivityInfo.create = function create(properties) {
            return new ReqFetchProgressRewardActivityInfo(properties);
        };

        ReqFetchProgressRewardActivityInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchProgressRewardActivityInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchProgressRewardActivityInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchProgressRewardActivityInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchProgressRewardActivityInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchProgressRewardActivityInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchProgressRewardActivityInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchProgressRewardActivityInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchProgressRewardActivityInfo();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchProgressRewardActivityInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchProgressRewardActivityInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchProgressRewardActivityInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchProgressRewardActivityInfo";
        };

        return ReqFetchProgressRewardActivityInfo;
    })();

    lq.ResFetchProgressRewardActivityInfo = (function() {

        function ResFetchProgressRewardActivityInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchProgressRewardActivityInfo.prototype.error = null;
        ResFetchProgressRewardActivityInfo.prototype.progress = 0;

        ResFetchProgressRewardActivityInfo.create = function create(properties) {
            return new ResFetchProgressRewardActivityInfo(properties);
        };

        ResFetchProgressRewardActivityInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                writer.uint32(16).uint32(message.progress);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchProgressRewardActivityInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchProgressRewardActivityInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchProgressRewardActivityInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.progress = value;
                        else
                            delete message.progress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchProgressRewardActivityInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchProgressRewardActivityInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.progress != null && message.hasOwnProperty("progress"))
                if (!$util.isInteger(message.progress))
                    return "progress: integer expected";
            return null;
        };

        ResFetchProgressRewardActivityInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchProgressRewardActivityInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchProgressRewardActivityInfo();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchProgressRewardActivityInfo.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.progress != null)
                if (Number(object.progress) !== 0)
                    message.progress = object.progress >>> 0;
            return message;
        };

        ResFetchProgressRewardActivityInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.progress = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.progress != null && message.hasOwnProperty("progress"))
                object.progress = message.progress;
            return object;
        };

        ResFetchProgressRewardActivityInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchProgressRewardActivityInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchProgressRewardActivityInfo";
        };

        return ResFetchProgressRewardActivityInfo;
    })();

    lq.ReqShootActivityAttackEnemies = (function() {

        function ReqShootActivityAttackEnemies(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqShootActivityAttackEnemies.prototype.activity_id = 0;
        ReqShootActivityAttackEnemies.prototype.bullets_count = 0;
        ReqShootActivityAttackEnemies.prototype.position = 0;

        ReqShootActivityAttackEnemies.create = function create(properties) {
            return new ReqShootActivityAttackEnemies(properties);
        };

        ReqShootActivityAttackEnemies.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.bullets_count != null && Object.hasOwnProperty.call(message, "bullets_count"))
                writer.uint32(16).uint32(message.bullets_count);
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                writer.uint32(24).uint32(message.position);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqShootActivityAttackEnemies.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqShootActivityAttackEnemies.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqShootActivityAttackEnemies(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.bullets_count = value;
                        else
                            delete message.bullets_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.position = value;
                        else
                            delete message.position;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqShootActivityAttackEnemies.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqShootActivityAttackEnemies.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.bullets_count != null && message.hasOwnProperty("bullets_count"))
                if (!$util.isInteger(message.bullets_count))
                    return "bullets_count: integer expected";
            if (message.position != null && message.hasOwnProperty("position"))
                if (!$util.isInteger(message.position))
                    return "position: integer expected";
            return null;
        };

        ReqShootActivityAttackEnemies.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqShootActivityAttackEnemies)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqShootActivityAttackEnemies();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.bullets_count != null)
                if (Number(object.bullets_count) !== 0)
                    message.bullets_count = object.bullets_count >>> 0;
            if (object.position != null)
                if (Number(object.position) !== 0)
                    message.position = object.position >>> 0;
            return message;
        };

        ReqShootActivityAttackEnemies.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.bullets_count = 0;
                object.position = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.bullets_count != null && message.hasOwnProperty("bullets_count"))
                object.bullets_count = message.bullets_count;
            if (message.position != null && message.hasOwnProperty("position"))
                object.position = message.position;
            return object;
        };

        ReqShootActivityAttackEnemies.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqShootActivityAttackEnemies.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqShootActivityAttackEnemies";
        };

        return ReqShootActivityAttackEnemies;
    })();

    lq.ResShootActivityAttackEnemies = (function() {

        function ResShootActivityAttackEnemies(properties) {
            this.records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResShootActivityAttackEnemies.prototype.error = null;
        ResShootActivityAttackEnemies.prototype.records = $util.emptyArray;
        ResShootActivityAttackEnemies.prototype.value_change = null;

        ResShootActivityAttackEnemies.create = function create(properties) {
            return new ResShootActivityAttackEnemies(properties);
        };

        ResShootActivityAttackEnemies.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.encode(message.records[i], writer.uint32(18).fork()).ldelim();
            if (message.value_change != null && Object.hasOwnProperty.call(message, "value_change"))
                $root.lq.ActivityShootValueChange.encode(message.value_change, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResShootActivityAttackEnemies.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResShootActivityAttackEnemies.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResShootActivityAttackEnemies(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.value_change = $root.lq.ActivityShootValueChange.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_change);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResShootActivityAttackEnemies.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResShootActivityAttackEnemies.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i) {
                    let error = $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.verify(message.records[i], _depth + 1);
                    if (error)
                        return "records." + error;
                }
            }
            if (message.value_change != null && message.hasOwnProperty("value_change")) {
                let error = $root.lq.ActivityShootValueChange.verify(message.value_change, _depth + 1);
                if (error)
                    return "value_change." + error;
            }
            return null;
        };

        ResShootActivityAttackEnemies.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResShootActivityAttackEnemies)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResShootActivityAttackEnemies();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResShootActivityAttackEnemies.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.ResShootActivityAttackEnemies.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i) {
                    if (typeof object.records[i] !== "object")
                        throw TypeError(".lq.ResShootActivityAttackEnemies.records: object expected");
                    message.records[i] = $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.fromObject(object.records[i], _depth + 1);
                }
            }
            if (object.value_change != null) {
                if (typeof object.value_change !== "object")
                    throw TypeError(".lq.ResShootActivityAttackEnemies.value_change: object expected");
                message.value_change = $root.lq.ActivityShootValueChange.fromObject(object.value_change, _depth + 1);
            }
            return message;
        };

        ResShootActivityAttackEnemies.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (options.defaults) {
                object.error = null;
                object.value_change = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.toObject(message.records[j], options);
            }
            if (message.value_change != null && message.hasOwnProperty("value_change"))
                object.value_change = $root.lq.ActivityShootValueChange.toObject(message.value_change, options);
            return object;
        };

        ResShootActivityAttackEnemies.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResShootActivityAttackEnemies.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResShootActivityAttackEnemies";
        };

        ResShootActivityAttackEnemies.ActivityShootAttackRecord = (function() {

            function ActivityShootAttackRecord(properties) {
                this.reward_ids = [];
                this.rewards = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityShootAttackRecord.prototype.position = 0;
            ActivityShootAttackRecord.prototype.enemy = null;
            ActivityShootAttackRecord.prototype.level = 0;
            ActivityShootAttackRecord.prototype.reward_ids = $util.emptyArray;
            ActivityShootAttackRecord.prototype.rewards = $util.emptyArray;

            ActivityShootAttackRecord.create = function create(properties) {
                return new ActivityShootAttackRecord(properties);
            };

            ActivityShootAttackRecord.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                    writer.uint32(8).uint32(message.position);
                if (message.enemy != null && Object.hasOwnProperty.call(message, "enemy"))
                    $root.lq.ActivityShootEnemyInfo.encode(message.enemy, writer.uint32(18).fork()).ldelim();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(24).uint32(message.level);
                if (message.reward_ids != null && message.reward_ids.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.reward_ids.length; ++i)
                        writer.uint32(message.reward_ids[i]);
                    writer.ldelim();
                }
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(42).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityShootAttackRecord.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityShootAttackRecord.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.position = value;
                            else
                                delete message.position;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.enemy = $root.lq.ActivityShootEnemyInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.enemy);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.reward_ids && message.reward_ids.length))
                                    message.reward_ids = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.reward_ids.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.reward_ids && message.reward_ids.length))
                                message.reward_ids = [];
                            message.reward_ids.push(reader.uint32());
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityShootAttackRecord.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityShootAttackRecord.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.position != null && message.hasOwnProperty("position"))
                    if (!$util.isInteger(message.position))
                        return "position: integer expected";
                if (message.enemy != null && message.hasOwnProperty("enemy")) {
                    let error = $root.lq.ActivityShootEnemyInfo.verify(message.enemy, _depth + 1);
                    if (error)
                        return "enemy." + error;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.reward_ids != null && message.hasOwnProperty("reward_ids")) {
                    if (!Array.isArray(message.reward_ids))
                        return "reward_ids: array expected";
                    for (let i = 0; i < message.reward_ids.length; ++i)
                        if (!$util.isInteger(message.reward_ids[i]))
                            return "reward_ids: integer[] expected";
                }
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                return null;
            };

            ActivityShootAttackRecord.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord();
                if (object.position != null)
                    if (Number(object.position) !== 0)
                        message.position = object.position >>> 0;
                if (object.enemy != null) {
                    if (typeof object.enemy !== "object")
                        throw TypeError(".lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.enemy: object expected");
                    message.enemy = $root.lq.ActivityShootEnemyInfo.fromObject(object.enemy, _depth + 1);
                }
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.reward_ids) {
                    if (!Array.isArray(object.reward_ids))
                        throw TypeError(".lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.reward_ids: array expected");
                    message.reward_ids = Array(object.reward_ids.length);
                    for (let i = 0; i < object.reward_ids.length; ++i)
                        message.reward_ids[i] = object.reward_ids[i] >>> 0;
                }
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord.rewards: object expected");
                        message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityShootAttackRecord.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.reward_ids = [];
                    object.rewards = [];
                }
                if (options.defaults) {
                    object.position = 0;
                    object.enemy = null;
                    object.level = 0;
                }
                if (message.position != null && message.hasOwnProperty("position"))
                    object.position = message.position;
                if (message.enemy != null && message.hasOwnProperty("enemy"))
                    object.enemy = $root.lq.ActivityShootEnemyInfo.toObject(message.enemy, options);
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.reward_ids && message.reward_ids.length) {
                    object.reward_ids = Array(message.reward_ids.length);
                    for (let j = 0; j < message.reward_ids.length; ++j)
                        object.reward_ids[j] = message.reward_ids[j];
                }
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
                }
                return object;
            };

            ActivityShootAttackRecord.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityShootAttackRecord.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResShootActivityAttackEnemies.ActivityShootAttackRecord";
            };

            return ActivityShootAttackRecord;
        })();

        return ResShootActivityAttackEnemies;
    })();

    lq.ReqQuestCrewActivityStartQuest = (function() {

        function ReqQuestCrewActivityStartQuest(properties) {
            this.joined_members = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqQuestCrewActivityStartQuest.prototype.activity_id = 0;
        ReqQuestCrewActivityStartQuest.prototype.joined_members = $util.emptyArray;
        ReqQuestCrewActivityStartQuest.prototype.quest_id = 0;

        ReqQuestCrewActivityStartQuest.create = function create(properties) {
            return new ReqQuestCrewActivityStartQuest(properties);
        };

        ReqQuestCrewActivityStartQuest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.joined_members != null && message.joined_members.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.joined_members.length; ++i)
                    writer.uint32(message.joined_members[i]);
                writer.ldelim();
            }
            if (message.quest_id != null && Object.hasOwnProperty.call(message, "quest_id"))
                writer.uint32(24).uint32(message.quest_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqQuestCrewActivityStartQuest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqQuestCrewActivityStartQuest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqQuestCrewActivityStartQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.joined_members && message.joined_members.length))
                                message.joined_members = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.joined_members.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.joined_members && message.joined_members.length))
                            message.joined_members = [];
                        message.joined_members.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.quest_id = value;
                        else
                            delete message.quest_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqQuestCrewActivityStartQuest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqQuestCrewActivityStartQuest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.joined_members != null && message.hasOwnProperty("joined_members")) {
                if (!Array.isArray(message.joined_members))
                    return "joined_members: array expected";
                for (let i = 0; i < message.joined_members.length; ++i)
                    if (!$util.isInteger(message.joined_members[i]))
                        return "joined_members: integer[] expected";
            }
            if (message.quest_id != null && message.hasOwnProperty("quest_id"))
                if (!$util.isInteger(message.quest_id))
                    return "quest_id: integer expected";
            return null;
        };

        ReqQuestCrewActivityStartQuest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqQuestCrewActivityStartQuest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqQuestCrewActivityStartQuest();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.joined_members) {
                if (!Array.isArray(object.joined_members))
                    throw TypeError(".lq.ReqQuestCrewActivityStartQuest.joined_members: array expected");
                message.joined_members = Array(object.joined_members.length);
                for (let i = 0; i < object.joined_members.length; ++i)
                    message.joined_members[i] = object.joined_members[i] >>> 0;
            }
            if (object.quest_id != null)
                if (Number(object.quest_id) !== 0)
                    message.quest_id = object.quest_id >>> 0;
            return message;
        };

        ReqQuestCrewActivityStartQuest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.joined_members = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.quest_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.joined_members && message.joined_members.length) {
                object.joined_members = Array(message.joined_members.length);
                for (let j = 0; j < message.joined_members.length; ++j)
                    object.joined_members[j] = message.joined_members[j];
            }
            if (message.quest_id != null && message.hasOwnProperty("quest_id"))
                object.quest_id = message.quest_id;
            return object;
        };

        ReqQuestCrewActivityStartQuest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqQuestCrewActivityStartQuest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqQuestCrewActivityStartQuest";
        };

        return ReqQuestCrewActivityStartQuest;
    })();

    lq.ResQuestCrewActivityStartQuest = (function() {

        function ResQuestCrewActivityStartQuest(properties) {
            this.effect_info = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResQuestCrewActivityStartQuest.prototype.error = null;
        ResQuestCrewActivityStartQuest.prototype.result = 0;
        ResQuestCrewActivityStartQuest.prototype.value_changes = null;
        ResQuestCrewActivityStartQuest.prototype.effect_info = $util.emptyArray;

        ResQuestCrewActivityStartQuest.create = function create(properties) {
            return new ResQuestCrewActivityStartQuest(properties);
        };

        ResQuestCrewActivityStartQuest.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(16).uint32(message.result);
            if (message.value_changes != null && Object.hasOwnProperty.call(message, "value_changes"))
                $root.lq.ActivityQuestCrewChanges.encode(message.value_changes, writer.uint32(26).fork()).ldelim();
            if (message.effect_info != null && message.effect_info.length)
                for (let i = 0; i < message.effect_info.length; ++i)
                    $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.encode(message.effect_info[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResQuestCrewActivityStartQuest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResQuestCrewActivityStartQuest.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResQuestCrewActivityStartQuest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.value_changes = $root.lq.ActivityQuestCrewChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_changes);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_info && message.effect_info.length))
                            message.effect_info = [];
                        message.effect_info.push($root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResQuestCrewActivityStartQuest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResQuestCrewActivityStartQuest.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.value_changes != null && message.hasOwnProperty("value_changes")) {
                let error = $root.lq.ActivityQuestCrewChanges.verify(message.value_changes, _depth + 1);
                if (error)
                    return "value_changes." + error;
            }
            if (message.effect_info != null && message.hasOwnProperty("effect_info")) {
                if (!Array.isArray(message.effect_info))
                    return "effect_info: array expected";
                for (let i = 0; i < message.effect_info.length; ++i) {
                    let error = $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.verify(message.effect_info[i], _depth + 1);
                    if (error)
                        return "effect_info." + error;
                }
            }
            return null;
        };

        ResQuestCrewActivityStartQuest.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResQuestCrewActivityStartQuest)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResQuestCrewActivityStartQuest();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityStartQuest.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            if (object.value_changes != null) {
                if (typeof object.value_changes !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityStartQuest.value_changes: object expected");
                message.value_changes = $root.lq.ActivityQuestCrewChanges.fromObject(object.value_changes, _depth + 1);
            }
            if (object.effect_info) {
                if (!Array.isArray(object.effect_info))
                    throw TypeError(".lq.ResQuestCrewActivityStartQuest.effect_info: array expected");
                message.effect_info = Array(object.effect_info.length);
                for (let i = 0; i < object.effect_info.length; ++i) {
                    if (typeof object.effect_info[i] !== "object")
                        throw TypeError(".lq.ResQuestCrewActivityStartQuest.effect_info: object expected");
                    message.effect_info[i] = $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.fromObject(object.effect_info[i], _depth + 1);
                }
            }
            return message;
        };

        ResQuestCrewActivityStartQuest.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.effect_info = [];
            if (options.defaults) {
                object.error = null;
                object.result = 0;
                object.value_changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.value_changes != null && message.hasOwnProperty("value_changes"))
                object.value_changes = $root.lq.ActivityQuestCrewChanges.toObject(message.value_changes, options);
            if (message.effect_info && message.effect_info.length) {
                object.effect_info = Array(message.effect_info.length);
                for (let j = 0; j < message.effect_info.length; ++j)
                    object.effect_info[j] = $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.toObject(message.effect_info[j], options);
            }
            return object;
        };

        ResQuestCrewActivityStartQuest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResQuestCrewActivityStartQuest.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResQuestCrewActivityStartQuest";
        };

        ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo = (function() {

            function ActivityQuestCrewEffectInfo(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityQuestCrewEffectInfo.prototype.member_id = 0;
            ActivityQuestCrewEffectInfo.prototype.effect_id = 0;
            ActivityQuestCrewEffectInfo.prototype.result = null;

            ActivityQuestCrewEffectInfo.create = function create(properties) {
                return new ActivityQuestCrewEffectInfo(properties);
            };

            ActivityQuestCrewEffectInfo.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.member_id != null && Object.hasOwnProperty.call(message, "member_id"))
                    writer.uint32(8).uint32(message.member_id);
                if (message.effect_id != null && Object.hasOwnProperty.call(message, "effect_id"))
                    writer.uint32(16).uint32(message.effect_id);
                if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                    $root.lq.ActivityQuestCrewEffectResult.encode(message.result, writer.uint32(26).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityQuestCrewEffectInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityQuestCrewEffectInfo.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.member_id = value;
                            else
                                delete message.member_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.effect_id = value;
                            else
                                delete message.effect_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.result = $root.lq.ActivityQuestCrewEffectResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityQuestCrewEffectInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityQuestCrewEffectInfo.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.member_id != null && message.hasOwnProperty("member_id"))
                    if (!$util.isInteger(message.member_id))
                        return "member_id: integer expected";
                if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                    if (!$util.isInteger(message.effect_id))
                        return "effect_id: integer expected";
                if (message.result != null && message.hasOwnProperty("result")) {
                    let error = $root.lq.ActivityQuestCrewEffectResult.verify(message.result, _depth + 1);
                    if (error)
                        return "result." + error;
                }
                return null;
            };

            ActivityQuestCrewEffectInfo.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo();
                if (object.member_id != null)
                    if (Number(object.member_id) !== 0)
                        message.member_id = object.member_id >>> 0;
                if (object.effect_id != null)
                    if (Number(object.effect_id) !== 0)
                        message.effect_id = object.effect_id >>> 0;
                if (object.result != null) {
                    if (typeof object.result !== "object")
                        throw TypeError(".lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo.result: object expected");
                    message.result = $root.lq.ActivityQuestCrewEffectResult.fromObject(object.result, _depth + 1);
                }
                return message;
            };

            ActivityQuestCrewEffectInfo.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.member_id = 0;
                    object.effect_id = 0;
                    object.result = null;
                }
                if (message.member_id != null && message.hasOwnProperty("member_id"))
                    object.member_id = message.member_id;
                if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                    object.effect_id = message.effect_id;
                if (message.result != null && message.hasOwnProperty("result"))
                    object.result = $root.lq.ActivityQuestCrewEffectResult.toObject(message.result, options);
                return object;
            };

            ActivityQuestCrewEffectInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityQuestCrewEffectInfo.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResQuestCrewActivityStartQuest.ActivityQuestCrewEffectInfo";
            };

            return ActivityQuestCrewEffectInfo;
        })();

        return ResQuestCrewActivityStartQuest;
    })();

    lq.ReqQuestCrewActivityHire = (function() {

        function ReqQuestCrewActivityHire(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqQuestCrewActivityHire.prototype.activity_id = 0;
        ReqQuestCrewActivityHire.prototype.member_id = 0;

        ReqQuestCrewActivityHire.create = function create(properties) {
            return new ReqQuestCrewActivityHire(properties);
        };

        ReqQuestCrewActivityHire.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.member_id != null && Object.hasOwnProperty.call(message, "member_id"))
                writer.uint32(16).uint32(message.member_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqQuestCrewActivityHire.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqQuestCrewActivityHire.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqQuestCrewActivityHire(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.member_id = value;
                        else
                            delete message.member_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqQuestCrewActivityHire.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqQuestCrewActivityHire.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.member_id != null && message.hasOwnProperty("member_id"))
                if (!$util.isInteger(message.member_id))
                    return "member_id: integer expected";
            return null;
        };

        ReqQuestCrewActivityHire.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqQuestCrewActivityHire)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqQuestCrewActivityHire();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.member_id != null)
                if (Number(object.member_id) !== 0)
                    message.member_id = object.member_id >>> 0;
            return message;
        };

        ReqQuestCrewActivityHire.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.member_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.member_id != null && message.hasOwnProperty("member_id"))
                object.member_id = message.member_id;
            return object;
        };

        ReqQuestCrewActivityHire.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqQuestCrewActivityHire.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqQuestCrewActivityHire";
        };

        return ReqQuestCrewActivityHire;
    })();

    lq.ResQuestCrewActivityHire = (function() {

        function ResQuestCrewActivityHire(properties) {
            this.execute_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResQuestCrewActivityHire.prototype.error = null;
        ResQuestCrewActivityHire.prototype.value_changes = null;
        ResQuestCrewActivityHire.prototype.execute_result = $util.emptyArray;

        ResQuestCrewActivityHire.create = function create(properties) {
            return new ResQuestCrewActivityHire(properties);
        };

        ResQuestCrewActivityHire.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.value_changes != null && Object.hasOwnProperty.call(message, "value_changes"))
                $root.lq.ActivityQuestCrewChanges.encode(message.value_changes, writer.uint32(18).fork()).ldelim();
            if (message.execute_result != null && message.execute_result.length)
                for (let i = 0; i < message.execute_result.length; ++i)
                    $root.lq.ExecuteResult.encode(message.execute_result[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResQuestCrewActivityHire.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResQuestCrewActivityHire.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResQuestCrewActivityHire(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value_changes = $root.lq.ActivityQuestCrewChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_changes);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_result && message.execute_result.length))
                            message.execute_result = [];
                        message.execute_result.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResQuestCrewActivityHire.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResQuestCrewActivityHire.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.value_changes != null && message.hasOwnProperty("value_changes")) {
                let error = $root.lq.ActivityQuestCrewChanges.verify(message.value_changes, _depth + 1);
                if (error)
                    return "value_changes." + error;
            }
            if (message.execute_result != null && message.hasOwnProperty("execute_result")) {
                if (!Array.isArray(message.execute_result))
                    return "execute_result: array expected";
                for (let i = 0; i < message.execute_result.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.execute_result[i], _depth + 1);
                    if (error)
                        return "execute_result." + error;
                }
            }
            return null;
        };

        ResQuestCrewActivityHire.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResQuestCrewActivityHire)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResQuestCrewActivityHire();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityHire.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.value_changes != null) {
                if (typeof object.value_changes !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityHire.value_changes: object expected");
                message.value_changes = $root.lq.ActivityQuestCrewChanges.fromObject(object.value_changes, _depth + 1);
            }
            if (object.execute_result) {
                if (!Array.isArray(object.execute_result))
                    throw TypeError(".lq.ResQuestCrewActivityHire.execute_result: array expected");
                message.execute_result = Array(object.execute_result.length);
                for (let i = 0; i < object.execute_result.length; ++i) {
                    if (typeof object.execute_result[i] !== "object")
                        throw TypeError(".lq.ResQuestCrewActivityHire.execute_result: object expected");
                    message.execute_result[i] = $root.lq.ExecuteResult.fromObject(object.execute_result[i], _depth + 1);
                }
            }
            return message;
        };

        ResQuestCrewActivityHire.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_result = [];
            if (options.defaults) {
                object.error = null;
                object.value_changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.value_changes != null && message.hasOwnProperty("value_changes"))
                object.value_changes = $root.lq.ActivityQuestCrewChanges.toObject(message.value_changes, options);
            if (message.execute_result && message.execute_result.length) {
                object.execute_result = Array(message.execute_result.length);
                for (let j = 0; j < message.execute_result.length; ++j)
                    object.execute_result[j] = $root.lq.ExecuteResult.toObject(message.execute_result[j], options);
            }
            return object;
        };

        ResQuestCrewActivityHire.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResQuestCrewActivityHire.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResQuestCrewActivityHire";
        };

        return ResQuestCrewActivityHire;
    })();

    lq.ReqQuestCrewActivityFeed = (function() {

        function ReqQuestCrewActivityFeed(properties) {
            this.member_id = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqQuestCrewActivityFeed.prototype.activity_id = 0;
        ReqQuestCrewActivityFeed.prototype.member_id = $util.emptyArray;

        ReqQuestCrewActivityFeed.create = function create(properties) {
            return new ReqQuestCrewActivityFeed(properties);
        };

        ReqQuestCrewActivityFeed.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.member_id != null && message.member_id.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.member_id.length; ++i)
                    writer.uint32(message.member_id[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqQuestCrewActivityFeed.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqQuestCrewActivityFeed.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqQuestCrewActivityFeed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.member_id && message.member_id.length))
                                message.member_id = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.member_id.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.member_id && message.member_id.length))
                            message.member_id = [];
                        message.member_id.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqQuestCrewActivityFeed.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqQuestCrewActivityFeed.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.member_id != null && message.hasOwnProperty("member_id")) {
                if (!Array.isArray(message.member_id))
                    return "member_id: array expected";
                for (let i = 0; i < message.member_id.length; ++i)
                    if (!$util.isInteger(message.member_id[i]))
                        return "member_id: integer[] expected";
            }
            return null;
        };

        ReqQuestCrewActivityFeed.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqQuestCrewActivityFeed)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqQuestCrewActivityFeed();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.member_id) {
                if (!Array.isArray(object.member_id))
                    throw TypeError(".lq.ReqQuestCrewActivityFeed.member_id: array expected");
                message.member_id = Array(object.member_id.length);
                for (let i = 0; i < object.member_id.length; ++i)
                    message.member_id[i] = object.member_id[i] >>> 0;
            }
            return message;
        };

        ReqQuestCrewActivityFeed.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.member_id = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.member_id && message.member_id.length) {
                object.member_id = Array(message.member_id.length);
                for (let j = 0; j < message.member_id.length; ++j)
                    object.member_id[j] = message.member_id[j];
            }
            return object;
        };

        ReqQuestCrewActivityFeed.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqQuestCrewActivityFeed.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqQuestCrewActivityFeed";
        };

        return ReqQuestCrewActivityFeed;
    })();

    lq.ResQuestCrewActivityFeed = (function() {

        function ResQuestCrewActivityFeed(properties) {
            this.execute_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResQuestCrewActivityFeed.prototype.error = null;
        ResQuestCrewActivityFeed.prototype.value_changes = null;
        ResQuestCrewActivityFeed.prototype.execute_result = $util.emptyArray;

        ResQuestCrewActivityFeed.create = function create(properties) {
            return new ResQuestCrewActivityFeed(properties);
        };

        ResQuestCrewActivityFeed.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.value_changes != null && Object.hasOwnProperty.call(message, "value_changes"))
                $root.lq.ActivityQuestCrewChanges.encode(message.value_changes, writer.uint32(18).fork()).ldelim();
            if (message.execute_result != null && message.execute_result.length)
                for (let i = 0; i < message.execute_result.length; ++i)
                    $root.lq.ExecuteResult.encode(message.execute_result[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResQuestCrewActivityFeed.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResQuestCrewActivityFeed.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResQuestCrewActivityFeed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value_changes = $root.lq.ActivityQuestCrewChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_changes);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_result && message.execute_result.length))
                            message.execute_result = [];
                        message.execute_result.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResQuestCrewActivityFeed.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResQuestCrewActivityFeed.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.value_changes != null && message.hasOwnProperty("value_changes")) {
                let error = $root.lq.ActivityQuestCrewChanges.verify(message.value_changes, _depth + 1);
                if (error)
                    return "value_changes." + error;
            }
            if (message.execute_result != null && message.hasOwnProperty("execute_result")) {
                if (!Array.isArray(message.execute_result))
                    return "execute_result: array expected";
                for (let i = 0; i < message.execute_result.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.execute_result[i], _depth + 1);
                    if (error)
                        return "execute_result." + error;
                }
            }
            return null;
        };

        ResQuestCrewActivityFeed.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResQuestCrewActivityFeed)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResQuestCrewActivityFeed();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityFeed.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.value_changes != null) {
                if (typeof object.value_changes !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityFeed.value_changes: object expected");
                message.value_changes = $root.lq.ActivityQuestCrewChanges.fromObject(object.value_changes, _depth + 1);
            }
            if (object.execute_result) {
                if (!Array.isArray(object.execute_result))
                    throw TypeError(".lq.ResQuestCrewActivityFeed.execute_result: array expected");
                message.execute_result = Array(object.execute_result.length);
                for (let i = 0; i < object.execute_result.length; ++i) {
                    if (typeof object.execute_result[i] !== "object")
                        throw TypeError(".lq.ResQuestCrewActivityFeed.execute_result: object expected");
                    message.execute_result[i] = $root.lq.ExecuteResult.fromObject(object.execute_result[i], _depth + 1);
                }
            }
            return message;
        };

        ResQuestCrewActivityFeed.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_result = [];
            if (options.defaults) {
                object.error = null;
                object.value_changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.value_changes != null && message.hasOwnProperty("value_changes"))
                object.value_changes = $root.lq.ActivityQuestCrewChanges.toObject(message.value_changes, options);
            if (message.execute_result && message.execute_result.length) {
                object.execute_result = Array(message.execute_result.length);
                for (let j = 0; j < message.execute_result.length; ++j)
                    object.execute_result[j] = $root.lq.ExecuteResult.toObject(message.execute_result[j], options);
            }
            return object;
        };

        ResQuestCrewActivityFeed.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResQuestCrewActivityFeed.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResQuestCrewActivityFeed";
        };

        return ResQuestCrewActivityFeed;
    })();

    lq.ReqQuestCrewActivityRefreshMarket = (function() {

        function ReqQuestCrewActivityRefreshMarket(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqQuestCrewActivityRefreshMarket.prototype.activity_id = 0;

        ReqQuestCrewActivityRefreshMarket.create = function create(properties) {
            return new ReqQuestCrewActivityRefreshMarket(properties);
        };

        ReqQuestCrewActivityRefreshMarket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqQuestCrewActivityRefreshMarket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqQuestCrewActivityRefreshMarket.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqQuestCrewActivityRefreshMarket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqQuestCrewActivityRefreshMarket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqQuestCrewActivityRefreshMarket.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqQuestCrewActivityRefreshMarket.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqQuestCrewActivityRefreshMarket)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqQuestCrewActivityRefreshMarket();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqQuestCrewActivityRefreshMarket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqQuestCrewActivityRefreshMarket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqQuestCrewActivityRefreshMarket.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqQuestCrewActivityRefreshMarket";
        };

        return ReqQuestCrewActivityRefreshMarket;
    })();

    lq.ResQuestCrewActivityRefreshMarket = (function() {

        function ResQuestCrewActivityRefreshMarket(properties) {
            this.execute_result = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResQuestCrewActivityRefreshMarket.prototype.error = null;
        ResQuestCrewActivityRefreshMarket.prototype.value_changes = null;
        ResQuestCrewActivityRefreshMarket.prototype.execute_result = $util.emptyArray;

        ResQuestCrewActivityRefreshMarket.create = function create(properties) {
            return new ResQuestCrewActivityRefreshMarket(properties);
        };

        ResQuestCrewActivityRefreshMarket.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.value_changes != null && Object.hasOwnProperty.call(message, "value_changes"))
                $root.lq.ActivityQuestCrewChanges.encode(message.value_changes, writer.uint32(18).fork()).ldelim();
            if (message.execute_result != null && message.execute_result.length)
                for (let i = 0; i < message.execute_result.length; ++i)
                    $root.lq.ExecuteResult.encode(message.execute_result[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResQuestCrewActivityRefreshMarket.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResQuestCrewActivityRefreshMarket.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResQuestCrewActivityRefreshMarket(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value_changes = $root.lq.ActivityQuestCrewChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_changes);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_result && message.execute_result.length))
                            message.execute_result = [];
                        message.execute_result.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResQuestCrewActivityRefreshMarket.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResQuestCrewActivityRefreshMarket.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.value_changes != null && message.hasOwnProperty("value_changes")) {
                let error = $root.lq.ActivityQuestCrewChanges.verify(message.value_changes, _depth + 1);
                if (error)
                    return "value_changes." + error;
            }
            if (message.execute_result != null && message.hasOwnProperty("execute_result")) {
                if (!Array.isArray(message.execute_result))
                    return "execute_result: array expected";
                for (let i = 0; i < message.execute_result.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.execute_result[i], _depth + 1);
                    if (error)
                        return "execute_result." + error;
                }
            }
            return null;
        };

        ResQuestCrewActivityRefreshMarket.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResQuestCrewActivityRefreshMarket)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResQuestCrewActivityRefreshMarket();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityRefreshMarket.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.value_changes != null) {
                if (typeof object.value_changes !== "object")
                    throw TypeError(".lq.ResQuestCrewActivityRefreshMarket.value_changes: object expected");
                message.value_changes = $root.lq.ActivityQuestCrewChanges.fromObject(object.value_changes, _depth + 1);
            }
            if (object.execute_result) {
                if (!Array.isArray(object.execute_result))
                    throw TypeError(".lq.ResQuestCrewActivityRefreshMarket.execute_result: array expected");
                message.execute_result = Array(object.execute_result.length);
                for (let i = 0; i < object.execute_result.length; ++i) {
                    if (typeof object.execute_result[i] !== "object")
                        throw TypeError(".lq.ResQuestCrewActivityRefreshMarket.execute_result: object expected");
                    message.execute_result[i] = $root.lq.ExecuteResult.fromObject(object.execute_result[i], _depth + 1);
                }
            }
            return message;
        };

        ResQuestCrewActivityRefreshMarket.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.execute_result = [];
            if (options.defaults) {
                object.error = null;
                object.value_changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.value_changes != null && message.hasOwnProperty("value_changes"))
                object.value_changes = $root.lq.ActivityQuestCrewChanges.toObject(message.value_changes, options);
            if (message.execute_result && message.execute_result.length) {
                object.execute_result = Array(message.execute_result.length);
                for (let j = 0; j < message.execute_result.length; ++j)
                    object.execute_result[j] = $root.lq.ExecuteResult.toObject(message.execute_result[j], options);
            }
            return object;
        };

        ResQuestCrewActivityRefreshMarket.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResQuestCrewActivityRefreshMarket.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResQuestCrewActivityRefreshMarket";
        };

        return ResQuestCrewActivityRefreshMarket;
    })();

    lq.ReqBingoActivityReceiveReward = (function() {

        function ReqBingoActivityReceiveReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBingoActivityReceiveReward.prototype.activity_id = 0;
        ReqBingoActivityReceiveReward.prototype.rewards = $util.emptyArray;

        ReqBingoActivityReceiveReward.create = function create(properties) {
            return new ReqBingoActivityReceiveReward(properties);
        };

        ReqBingoActivityReceiveReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ReqBingoActivityReceiveReward.BingoReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBingoActivityReceiveReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBingoActivityReceiveReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBingoActivityReceiveReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ReqBingoActivityReceiveReward.BingoReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBingoActivityReceiveReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBingoActivityReceiveReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ReqBingoActivityReceiveReward.BingoReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ReqBingoActivityReceiveReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBingoActivityReceiveReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBingoActivityReceiveReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ReqBingoActivityReceiveReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ReqBingoActivityReceiveReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ReqBingoActivityReceiveReward.BingoReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ReqBingoActivityReceiveReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ReqBingoActivityReceiveReward.BingoReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ReqBingoActivityReceiveReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBingoActivityReceiveReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBingoActivityReceiveReward";
        };

        ReqBingoActivityReceiveReward.BingoReward = (function() {

            function BingoReward(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            BingoReward.prototype.reward_id = 0;
            BingoReward.prototype.card_id = 0;

            BingoReward.create = function create(properties) {
                return new BingoReward(properties);
            };

            BingoReward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.reward_id != null && Object.hasOwnProperty.call(message, "reward_id"))
                    writer.uint32(8).uint32(message.reward_id);
                if (message.card_id != null && Object.hasOwnProperty.call(message, "card_id"))
                    writer.uint32(16).uint32(message.card_id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            BingoReward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            BingoReward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBingoActivityReceiveReward.BingoReward(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.reward_id = value;
                            else
                                delete message.reward_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.card_id = value;
                            else
                                delete message.card_id;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            BingoReward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            BingoReward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                    if (!$util.isInteger(message.reward_id))
                        return "reward_id: integer expected";
                if (message.card_id != null && message.hasOwnProperty("card_id"))
                    if (!$util.isInteger(message.card_id))
                        return "card_id: integer expected";
                return null;
            };

            BingoReward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqBingoActivityReceiveReward.BingoReward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqBingoActivityReceiveReward.BingoReward();
                if (object.reward_id != null)
                    if (Number(object.reward_id) !== 0)
                        message.reward_id = object.reward_id >>> 0;
                if (object.card_id != null)
                    if (Number(object.card_id) !== 0)
                        message.card_id = object.card_id >>> 0;
                return message;
            };

            BingoReward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.reward_id = 0;
                    object.card_id = 0;
                }
                if (message.reward_id != null && message.hasOwnProperty("reward_id"))
                    object.reward_id = message.reward_id;
                if (message.card_id != null && message.hasOwnProperty("card_id"))
                    object.card_id = message.card_id;
                return object;
            };

            BingoReward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BingoReward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqBingoActivityReceiveReward.BingoReward";
            };

            return BingoReward;
        })();

        return ReqBingoActivityReceiveReward;
    })();

    lq.ResBingoActivityReceiveReward = (function() {

        function ResBingoActivityReceiveReward(properties) {
            this.execute_result = [];
            this.cards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResBingoActivityReceiveReward.prototype.error = null;
        ResBingoActivityReceiveReward.prototype.execute_result = $util.emptyArray;
        ResBingoActivityReceiveReward.prototype.cards = $util.emptyArray;

        ResBingoActivityReceiveReward.create = function create(properties) {
            return new ResBingoActivityReceiveReward(properties);
        };

        ResBingoActivityReceiveReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.execute_result != null && message.execute_result.length)
                for (let i = 0; i < message.execute_result.length; ++i)
                    $root.lq.ExecuteResult.encode(message.execute_result[i], writer.uint32(18).fork()).ldelim();
            if (message.cards != null && message.cards.length)
                for (let i = 0; i < message.cards.length; ++i)
                    $root.lq.ActivityBingoCardData.encode(message.cards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResBingoActivityReceiveReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResBingoActivityReceiveReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResBingoActivityReceiveReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.execute_result && message.execute_result.length))
                            message.execute_result = [];
                        message.execute_result.push($root.lq.ExecuteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.cards && message.cards.length))
                            message.cards = [];
                        message.cards.push($root.lq.ActivityBingoCardData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResBingoActivityReceiveReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResBingoActivityReceiveReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.execute_result != null && message.hasOwnProperty("execute_result")) {
                if (!Array.isArray(message.execute_result))
                    return "execute_result: array expected";
                for (let i = 0; i < message.execute_result.length; ++i) {
                    let error = $root.lq.ExecuteResult.verify(message.execute_result[i], _depth + 1);
                    if (error)
                        return "execute_result." + error;
                }
            }
            if (message.cards != null && message.hasOwnProperty("cards")) {
                if (!Array.isArray(message.cards))
                    return "cards: array expected";
                for (let i = 0; i < message.cards.length; ++i) {
                    let error = $root.lq.ActivityBingoCardData.verify(message.cards[i], _depth + 1);
                    if (error)
                        return "cards." + error;
                }
            }
            return null;
        };

        ResBingoActivityReceiveReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResBingoActivityReceiveReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResBingoActivityReceiveReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResBingoActivityReceiveReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.execute_result) {
                if (!Array.isArray(object.execute_result))
                    throw TypeError(".lq.ResBingoActivityReceiveReward.execute_result: array expected");
                message.execute_result = Array(object.execute_result.length);
                for (let i = 0; i < object.execute_result.length; ++i) {
                    if (typeof object.execute_result[i] !== "object")
                        throw TypeError(".lq.ResBingoActivityReceiveReward.execute_result: object expected");
                    message.execute_result[i] = $root.lq.ExecuteResult.fromObject(object.execute_result[i], _depth + 1);
                }
            }
            if (object.cards) {
                if (!Array.isArray(object.cards))
                    throw TypeError(".lq.ResBingoActivityReceiveReward.cards: array expected");
                message.cards = Array(object.cards.length);
                for (let i = 0; i < object.cards.length; ++i) {
                    if (typeof object.cards[i] !== "object")
                        throw TypeError(".lq.ResBingoActivityReceiveReward.cards: object expected");
                    message.cards[i] = $root.lq.ActivityBingoCardData.fromObject(object.cards[i], _depth + 1);
                }
            }
            return message;
        };

        ResBingoActivityReceiveReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.execute_result = [];
                object.cards = [];
            }
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.execute_result && message.execute_result.length) {
                object.execute_result = Array(message.execute_result.length);
                for (let j = 0; j < message.execute_result.length; ++j)
                    object.execute_result[j] = $root.lq.ExecuteResult.toObject(message.execute_result[j], options);
            }
            if (message.cards && message.cards.length) {
                object.cards = Array(message.cards.length);
                for (let j = 0; j < message.cards.length; ++j)
                    object.cards[j] = $root.lq.ActivityBingoCardData.toObject(message.cards[j], options);
            }
            return object;
        };

        ResBingoActivityReceiveReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResBingoActivityReceiveReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResBingoActivityReceiveReward";
        };

        return ResBingoActivityReceiveReward;
    })();

    lq.ReqFetchBingoActivityData = (function() {

        function ReqFetchBingoActivityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchBingoActivityData.prototype.activity_id = 0;

        ReqFetchBingoActivityData.create = function create(properties) {
            return new ReqFetchBingoActivityData(properties);
        };

        ReqFetchBingoActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchBingoActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchBingoActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchBingoActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchBingoActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchBingoActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqFetchBingoActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchBingoActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchBingoActivityData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqFetchBingoActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqFetchBingoActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchBingoActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchBingoActivityData";
        };

        return ReqFetchBingoActivityData;
    })();

    lq.ResFetchBingoActivityData = (function() {

        function ResFetchBingoActivityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchBingoActivityData.prototype.error = null;
        ResFetchBingoActivityData.prototype.data = null;

        ResFetchBingoActivityData.create = function create(properties) {
            return new ResFetchBingoActivityData(properties);
        };

        ResFetchBingoActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                $root.lq.ActivityBingoData.encode(message.data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchBingoActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchBingoActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchBingoActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.lq.ActivityBingoData.decode(reader, reader.uint32(), undefined, _depth + 1, message.data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchBingoActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchBingoActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.data != null && message.hasOwnProperty("data")) {
                let error = $root.lq.ActivityBingoData.verify(message.data, _depth + 1);
                if (error)
                    return "data." + error;
            }
            return null;
        };

        ResFetchBingoActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchBingoActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchBingoActivityData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchBingoActivityData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.data != null) {
                if (typeof object.data !== "object")
                    throw TypeError(".lq.ResFetchBingoActivityData.data: object expected");
                message.data = $root.lq.ActivityBingoData.fromObject(object.data, _depth + 1);
            }
            return message;
        };

        ResFetchBingoActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = $root.lq.ActivityBingoData.toObject(message.data, options);
            return object;
        };

        ResFetchBingoActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchBingoActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchBingoActivityData";
        };

        return ResFetchBingoActivityData;
    })();

    lq.ReqSnowballActivityStartBattle = (function() {

        function ReqSnowballActivityStartBattle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSnowballActivityStartBattle.prototype.activity_id = 0;

        ReqSnowballActivityStartBattle.create = function create(properties) {
            return new ReqSnowballActivityStartBattle(properties);
        };

        ReqSnowballActivityStartBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSnowballActivityStartBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSnowballActivityStartBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSnowballActivityStartBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSnowballActivityStartBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSnowballActivityStartBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqSnowballActivityStartBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSnowballActivityStartBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSnowballActivityStartBattle();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqSnowballActivityStartBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqSnowballActivityStartBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSnowballActivityStartBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSnowballActivityStartBattle";
        };

        return ReqSnowballActivityStartBattle;
    })();

    lq.ResSnowballActivityStartBattle = (function() {

        function ResSnowballActivityStartBattle(properties) {
            this.boss_action = [];
            this.player_state = [];
            this.random_seq = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSnowballActivityStartBattle.prototype.error = null;
        ResSnowballActivityStartBattle.prototype.random_seed = 0;
        ResSnowballActivityStartBattle.prototype.boss_action = $util.emptyArray;
        ResSnowballActivityStartBattle.prototype.battle_id = "";
        ResSnowballActivityStartBattle.prototype.player_state = $util.emptyArray;
        ResSnowballActivityStartBattle.prototype.random_seq = $util.emptyArray;

        ResSnowballActivityStartBattle.create = function create(properties) {
            return new ResSnowballActivityStartBattle(properties);
        };

        ResSnowballActivityStartBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                writer.uint32(16).uint32(message.random_seed);
            if (message.boss_action != null && message.boss_action.length)
                for (let i = 0; i < message.boss_action.length; ++i)
                    $root.lq.SnowballActivityBossAction.encode(message.boss_action[i], writer.uint32(26).fork()).ldelim();
            if (message.battle_id != null && Object.hasOwnProperty.call(message, "battle_id"))
                writer.uint32(34).string(message.battle_id);
            if (message.player_state != null && message.player_state.length)
                for (let i = 0; i < message.player_state.length; ++i)
                    $root.lq.ActivitySnowballPlayerState.encode(message.player_state[i], writer.uint32(42).fork()).ldelim();
            if (message.random_seq != null && message.random_seq.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.random_seq.length; ++i)
                    writer.uint32(message.random_seq[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSnowballActivityStartBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSnowballActivityStartBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSnowballActivityStartBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.random_seed = value;
                        else
                            delete message.random_seed;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.boss_action && message.boss_action.length))
                            message.boss_action = [];
                        message.boss_action.push($root.lq.SnowballActivityBossAction.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.battle_id = value;
                        else
                            delete message.battle_id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_state && message.player_state.length))
                            message.player_state = [];
                        message.player_state.push($root.lq.ActivitySnowballPlayerState.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.random_seq && message.random_seq.length))
                                message.random_seq = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.random_seq.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.random_seq && message.random_seq.length))
                            message.random_seq = [];
                        message.random_seq.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSnowballActivityStartBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSnowballActivityStartBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                if (!$util.isInteger(message.random_seed))
                    return "random_seed: integer expected";
            if (message.boss_action != null && message.hasOwnProperty("boss_action")) {
                if (!Array.isArray(message.boss_action))
                    return "boss_action: array expected";
                for (let i = 0; i < message.boss_action.length; ++i) {
                    let error = $root.lq.SnowballActivityBossAction.verify(message.boss_action[i], _depth + 1);
                    if (error)
                        return "boss_action." + error;
                }
            }
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                if (!$util.isString(message.battle_id))
                    return "battle_id: string expected";
            if (message.player_state != null && message.hasOwnProperty("player_state")) {
                if (!Array.isArray(message.player_state))
                    return "player_state: array expected";
                for (let i = 0; i < message.player_state.length; ++i) {
                    let error = $root.lq.ActivitySnowballPlayerState.verify(message.player_state[i], _depth + 1);
                    if (error)
                        return "player_state." + error;
                }
            }
            if (message.random_seq != null && message.hasOwnProperty("random_seq")) {
                if (!Array.isArray(message.random_seq))
                    return "random_seq: array expected";
                for (let i = 0; i < message.random_seq.length; ++i)
                    if (!$util.isInteger(message.random_seq[i]))
                        return "random_seq: integer[] expected";
            }
            return null;
        };

        ResSnowballActivityStartBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSnowballActivityStartBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSnowballActivityStartBattle();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSnowballActivityStartBattle.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.random_seed != null)
                if (Number(object.random_seed) !== 0)
                    message.random_seed = object.random_seed >>> 0;
            if (object.boss_action) {
                if (!Array.isArray(object.boss_action))
                    throw TypeError(".lq.ResSnowballActivityStartBattle.boss_action: array expected");
                message.boss_action = Array(object.boss_action.length);
                for (let i = 0; i < object.boss_action.length; ++i) {
                    if (typeof object.boss_action[i] !== "object")
                        throw TypeError(".lq.ResSnowballActivityStartBattle.boss_action: object expected");
                    message.boss_action[i] = $root.lq.SnowballActivityBossAction.fromObject(object.boss_action[i], _depth + 1);
                }
            }
            if (object.battle_id != null)
                if (typeof object.battle_id !== "string" || object.battle_id.length)
                    message.battle_id = String(object.battle_id);
            if (object.player_state) {
                if (!Array.isArray(object.player_state))
                    throw TypeError(".lq.ResSnowballActivityStartBattle.player_state: array expected");
                message.player_state = Array(object.player_state.length);
                for (let i = 0; i < object.player_state.length; ++i) {
                    if (typeof object.player_state[i] !== "object")
                        throw TypeError(".lq.ResSnowballActivityStartBattle.player_state: object expected");
                    message.player_state[i] = $root.lq.ActivitySnowballPlayerState.fromObject(object.player_state[i], _depth + 1);
                }
            }
            if (object.random_seq) {
                if (!Array.isArray(object.random_seq))
                    throw TypeError(".lq.ResSnowballActivityStartBattle.random_seq: array expected");
                message.random_seq = Array(object.random_seq.length);
                for (let i = 0; i < object.random_seq.length; ++i)
                    message.random_seq[i] = object.random_seq[i] >>> 0;
            }
            return message;
        };

        ResSnowballActivityStartBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.boss_action = [];
                object.player_state = [];
                object.random_seq = [];
            }
            if (options.defaults) {
                object.error = null;
                object.random_seed = 0;
                object.battle_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                object.random_seed = message.random_seed;
            if (message.boss_action && message.boss_action.length) {
                object.boss_action = Array(message.boss_action.length);
                for (let j = 0; j < message.boss_action.length; ++j)
                    object.boss_action[j] = $root.lq.SnowballActivityBossAction.toObject(message.boss_action[j], options);
            }
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                object.battle_id = message.battle_id;
            if (message.player_state && message.player_state.length) {
                object.player_state = Array(message.player_state.length);
                for (let j = 0; j < message.player_state.length; ++j)
                    object.player_state[j] = $root.lq.ActivitySnowballPlayerState.toObject(message.player_state[j], options);
            }
            if (message.random_seq && message.random_seq.length) {
                object.random_seq = Array(message.random_seq.length);
                for (let j = 0; j < message.random_seq.length; ++j)
                    object.random_seq[j] = message.random_seq[j];
            }
            return object;
        };

        ResSnowballActivityStartBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSnowballActivityStartBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSnowballActivityStartBattle";
        };

        return ResSnowballActivityStartBattle;
    })();

    lq.ReqSnowballActivityFinishBattle = (function() {

        function ReqSnowballActivityFinishBattle(properties) {
            this.player_action = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSnowballActivityFinishBattle.prototype.activity_id = 0;
        ReqSnowballActivityFinishBattle.prototype.player_action = $util.emptyArray;
        ReqSnowballActivityFinishBattle.prototype.result = 0;
        ReqSnowballActivityFinishBattle.prototype.battle_id = "";

        ReqSnowballActivityFinishBattle.create = function create(properties) {
            return new ReqSnowballActivityFinishBattle(properties);
        };

        ReqSnowballActivityFinishBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.player_action != null && message.player_action.length)
                for (let i = 0; i < message.player_action.length; ++i)
                    $root.lq.ActivitySnowballPlayerAction.encode(message.player_action[i], writer.uint32(18).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(24).uint32(message.result);
            if (message.battle_id != null && Object.hasOwnProperty.call(message, "battle_id"))
                writer.uint32(34).string(message.battle_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSnowballActivityFinishBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSnowballActivityFinishBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSnowballActivityFinishBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_action && message.player_action.length))
                            message.player_action = [];
                        message.player_action.push($root.lq.ActivitySnowballPlayerAction.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.battle_id = value;
                        else
                            delete message.battle_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSnowballActivityFinishBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSnowballActivityFinishBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.player_action != null && message.hasOwnProperty("player_action")) {
                if (!Array.isArray(message.player_action))
                    return "player_action: array expected";
                for (let i = 0; i < message.player_action.length; ++i) {
                    let error = $root.lq.ActivitySnowballPlayerAction.verify(message.player_action[i], _depth + 1);
                    if (error)
                        return "player_action." + error;
                }
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                if (!$util.isString(message.battle_id))
                    return "battle_id: string expected";
            return null;
        };

        ReqSnowballActivityFinishBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSnowballActivityFinishBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSnowballActivityFinishBattle();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.player_action) {
                if (!Array.isArray(object.player_action))
                    throw TypeError(".lq.ReqSnowballActivityFinishBattle.player_action: array expected");
                message.player_action = Array(object.player_action.length);
                for (let i = 0; i < object.player_action.length; ++i) {
                    if (typeof object.player_action[i] !== "object")
                        throw TypeError(".lq.ReqSnowballActivityFinishBattle.player_action: object expected");
                    message.player_action[i] = $root.lq.ActivitySnowballPlayerAction.fromObject(object.player_action[i], _depth + 1);
                }
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            if (object.battle_id != null)
                if (typeof object.battle_id !== "string" || object.battle_id.length)
                    message.battle_id = String(object.battle_id);
            return message;
        };

        ReqSnowballActivityFinishBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.player_action = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.result = 0;
                object.battle_id = "";
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.player_action && message.player_action.length) {
                object.player_action = Array(message.player_action.length);
                for (let j = 0; j < message.player_action.length; ++j)
                    object.player_action[j] = $root.lq.ActivitySnowballPlayerAction.toObject(message.player_action[j], options);
            }
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.battle_id != null && message.hasOwnProperty("battle_id"))
                object.battle_id = message.battle_id;
            return object;
        };

        ReqSnowballActivityFinishBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSnowballActivityFinishBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSnowballActivityFinishBattle";
        };

        return ReqSnowballActivityFinishBattle;
    })();

    lq.ResSnowballActivityFinishBattle = (function() {

        function ResSnowballActivityFinishBattle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSnowballActivityFinishBattle.prototype.error = null;
        ResSnowballActivityFinishBattle.prototype.changes = null;

        ResSnowballActivityFinishBattle.create = function create(properties) {
            return new ResSnowballActivityFinishBattle(properties);
        };

        ResSnowballActivityFinishBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivitySnowballValueChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSnowballActivityFinishBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSnowballActivityFinishBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSnowballActivityFinishBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivitySnowballValueChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSnowballActivityFinishBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSnowballActivityFinishBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivitySnowballValueChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResSnowballActivityFinishBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSnowballActivityFinishBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSnowballActivityFinishBattle();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSnowballActivityFinishBattle.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResSnowballActivityFinishBattle.changes: object expected");
                message.changes = $root.lq.ActivitySnowballValueChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResSnowballActivityFinishBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivitySnowballValueChanges.toObject(message.changes, options);
            return object;
        };

        ResSnowballActivityFinishBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSnowballActivityFinishBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSnowballActivityFinishBattle";
        };

        return ResSnowballActivityFinishBattle;
    })();

    lq.ReqSnowballActivityUpgrade = (function() {

        function ReqSnowballActivityUpgrade(properties) {
            this.upgrade = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSnowballActivityUpgrade.prototype.activity_id = 0;
        ReqSnowballActivityUpgrade.prototype.upgrade = $util.emptyArray;

        ReqSnowballActivityUpgrade.create = function create(properties) {
            return new ReqSnowballActivityUpgrade(properties);
        };

        ReqSnowballActivityUpgrade.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.upgrade != null && message.upgrade.length)
                for (let i = 0; i < message.upgrade.length; ++i)
                    $root.lq.ActivitySnowballUpgrade.encode(message.upgrade[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSnowballActivityUpgrade.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSnowballActivityUpgrade.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSnowballActivityUpgrade(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.upgrade && message.upgrade.length))
                            message.upgrade = [];
                        message.upgrade.push($root.lq.ActivitySnowballUpgrade.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSnowballActivityUpgrade.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSnowballActivityUpgrade.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                if (!Array.isArray(message.upgrade))
                    return "upgrade: array expected";
                for (let i = 0; i < message.upgrade.length; ++i) {
                    let error = $root.lq.ActivitySnowballUpgrade.verify(message.upgrade[i], _depth + 1);
                    if (error)
                        return "upgrade." + error;
                }
            }
            return null;
        };

        ReqSnowballActivityUpgrade.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSnowballActivityUpgrade)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSnowballActivityUpgrade();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.upgrade) {
                if (!Array.isArray(object.upgrade))
                    throw TypeError(".lq.ReqSnowballActivityUpgrade.upgrade: array expected");
                message.upgrade = Array(object.upgrade.length);
                for (let i = 0; i < object.upgrade.length; ++i) {
                    if (typeof object.upgrade[i] !== "object")
                        throw TypeError(".lq.ReqSnowballActivityUpgrade.upgrade: object expected");
                    message.upgrade[i] = $root.lq.ActivitySnowballUpgrade.fromObject(object.upgrade[i], _depth + 1);
                }
            }
            return message;
        };

        ReqSnowballActivityUpgrade.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.upgrade = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.upgrade && message.upgrade.length) {
                object.upgrade = Array(message.upgrade.length);
                for (let j = 0; j < message.upgrade.length; ++j)
                    object.upgrade[j] = $root.lq.ActivitySnowballUpgrade.toObject(message.upgrade[j], options);
            }
            return object;
        };

        ReqSnowballActivityUpgrade.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSnowballActivityUpgrade.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSnowballActivityUpgrade";
        };

        return ReqSnowballActivityUpgrade;
    })();

    lq.ResSnowballActivityUpgrade = (function() {

        function ResSnowballActivityUpgrade(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSnowballActivityUpgrade.prototype.error = null;
        ResSnowballActivityUpgrade.prototype.changes = null;

        ResSnowballActivityUpgrade.create = function create(properties) {
            return new ResSnowballActivityUpgrade(properties);
        };

        ResSnowballActivityUpgrade.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivitySnowballValueChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSnowballActivityUpgrade.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSnowballActivityUpgrade.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSnowballActivityUpgrade(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivitySnowballValueChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSnowballActivityUpgrade.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSnowballActivityUpgrade.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivitySnowballValueChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResSnowballActivityUpgrade.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSnowballActivityUpgrade)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSnowballActivityUpgrade();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSnowballActivityUpgrade.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResSnowballActivityUpgrade.changes: object expected");
                message.changes = $root.lq.ActivitySnowballValueChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResSnowballActivityUpgrade.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivitySnowballValueChanges.toObject(message.changes, options);
            return object;
        };

        ResSnowballActivityUpgrade.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSnowballActivityUpgrade.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSnowballActivityUpgrade";
        };

        return ResSnowballActivityUpgrade;
    })();

    lq.ReqSnowballActivityReceiveReward = (function() {

        function ReqSnowballActivityReceiveReward(properties) {
            this.level = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSnowballActivityReceiveReward.prototype.activity_id = 0;
        ReqSnowballActivityReceiveReward.prototype.level = $util.emptyArray;

        ReqSnowballActivityReceiveReward.create = function create(properties) {
            return new ReqSnowballActivityReceiveReward(properties);
        };

        ReqSnowballActivityReceiveReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.level != null && message.level.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.level.length; ++i)
                    writer.uint32(message.level[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSnowballActivityReceiveReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSnowballActivityReceiveReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSnowballActivityReceiveReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.level && message.level.length))
                                message.level = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.level.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.level && message.level.length))
                            message.level = [];
                        message.level.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSnowballActivityReceiveReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSnowballActivityReceiveReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.level != null && message.hasOwnProperty("level")) {
                if (!Array.isArray(message.level))
                    return "level: array expected";
                for (let i = 0; i < message.level.length; ++i)
                    if (!$util.isInteger(message.level[i]))
                        return "level: integer[] expected";
            }
            return null;
        };

        ReqSnowballActivityReceiveReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSnowballActivityReceiveReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSnowballActivityReceiveReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.level) {
                if (!Array.isArray(object.level))
                    throw TypeError(".lq.ReqSnowballActivityReceiveReward.level: array expected");
                message.level = Array(object.level.length);
                for (let i = 0; i < object.level.length; ++i)
                    message.level[i] = object.level[i] >>> 0;
            }
            return message;
        };

        ReqSnowballActivityReceiveReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.level = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.level && message.level.length) {
                object.level = Array(message.level.length);
                for (let j = 0; j < message.level.length; ++j)
                    object.level[j] = message.level[j];
            }
            return object;
        };

        ReqSnowballActivityReceiveReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSnowballActivityReceiveReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSnowballActivityReceiveReward";
        };

        return ReqSnowballActivityReceiveReward;
    })();

    lq.ResSnowballActivityReceiveReward = (function() {

        function ResSnowballActivityReceiveReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSnowballActivityReceiveReward.prototype.error = null;
        ResSnowballActivityReceiveReward.prototype.changes = null;
        ResSnowballActivityReceiveReward.prototype.rewards = $util.emptyArray;

        ResSnowballActivityReceiveReward.create = function create(properties) {
            return new ResSnowballActivityReceiveReward(properties);
        };

        ResSnowballActivityReceiveReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivitySnowballValueChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSnowballActivityReceiveReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSnowballActivityReceiveReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSnowballActivityReceiveReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivitySnowballValueChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSnowballActivityReceiveReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSnowballActivityReceiveReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivitySnowballValueChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResSnowballActivityReceiveReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSnowballActivityReceiveReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSnowballActivityReceiveReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSnowballActivityReceiveReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResSnowballActivityReceiveReward.changes: object expected");
                message.changes = $root.lq.ActivitySnowballValueChanges.fromObject(object.changes, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResSnowballActivityReceiveReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResSnowballActivityReceiveReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResSnowballActivityReceiveReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivitySnowballValueChanges.toObject(message.changes, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResSnowballActivityReceiveReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSnowballActivityReceiveReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSnowballActivityReceiveReward";
        };

        return ResSnowballActivityReceiveReward;
    })();

    lq.ReqFetchBannerActivityData = (function() {

        function ReqFetchBannerActivityData(properties) {
            this.activity_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqFetchBannerActivityData.prototype.activity_id_list = $util.emptyArray;
        ReqFetchBannerActivityData.prototype.lang = "";

        ReqFetchBannerActivityData.create = function create(properties) {
            return new ReqFetchBannerActivityData(properties);
        };

        ReqFetchBannerActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id_list != null && message.activity_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.activity_id_list.length; ++i)
                    writer.uint32(message.activity_id_list[i]);
                writer.ldelim();
            }
            if (message.lang != null && Object.hasOwnProperty.call(message, "lang"))
                writer.uint32(18).string(message.lang);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqFetchBannerActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqFetchBannerActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqFetchBannerActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.activity_id_list && message.activity_id_list.length))
                                message.activity_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.activity_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.activity_id_list && message.activity_id_list.length))
                            message.activity_id_list = [];
                        message.activity_id_list.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.lang = value;
                        else
                            delete message.lang;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqFetchBannerActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqFetchBannerActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id_list != null && message.hasOwnProperty("activity_id_list")) {
                if (!Array.isArray(message.activity_id_list))
                    return "activity_id_list: array expected";
                for (let i = 0; i < message.activity_id_list.length; ++i)
                    if (!$util.isInteger(message.activity_id_list[i]))
                        return "activity_id_list: integer[] expected";
            }
            if (message.lang != null && message.hasOwnProperty("lang"))
                if (!$util.isString(message.lang))
                    return "lang: string expected";
            return null;
        };

        ReqFetchBannerActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqFetchBannerActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqFetchBannerActivityData();
            if (object.activity_id_list) {
                if (!Array.isArray(object.activity_id_list))
                    throw TypeError(".lq.ReqFetchBannerActivityData.activity_id_list: array expected");
                message.activity_id_list = Array(object.activity_id_list.length);
                for (let i = 0; i < object.activity_id_list.length; ++i)
                    message.activity_id_list[i] = object.activity_id_list[i] >>> 0;
            }
            if (object.lang != null)
                if (typeof object.lang !== "string" || object.lang.length)
                    message.lang = String(object.lang);
            return message;
        };

        ReqFetchBannerActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_id_list = [];
            if (options.defaults)
                object.lang = "";
            if (message.activity_id_list && message.activity_id_list.length) {
                object.activity_id_list = Array(message.activity_id_list.length);
                for (let j = 0; j < message.activity_id_list.length; ++j)
                    object.activity_id_list[j] = message.activity_id_list[j];
            }
            if (message.lang != null && message.hasOwnProperty("lang"))
                object.lang = message.lang;
            return object;
        };

        ReqFetchBannerActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqFetchBannerActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqFetchBannerActivityData";
        };

        return ReqFetchBannerActivityData;
    })();

    lq.ResFetchBannerActivityData = (function() {

        function ResFetchBannerActivityData(properties) {
            this.activity_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResFetchBannerActivityData.prototype.error = null;
        ResFetchBannerActivityData.prototype.activity_list = $util.emptyArray;

        ResFetchBannerActivityData.create = function create(properties) {
            return new ResFetchBannerActivityData(properties);
        };

        ResFetchBannerActivityData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.activity_list != null && message.activity_list.length)
                for (let i = 0; i < message.activity_list.length; ++i)
                    $root.lq.BannerActivityData.encode(message.activity_list[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResFetchBannerActivityData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResFetchBannerActivityData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResFetchBannerActivityData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_list && message.activity_list.length))
                            message.activity_list = [];
                        message.activity_list.push($root.lq.BannerActivityData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResFetchBannerActivityData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResFetchBannerActivityData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.activity_list != null && message.hasOwnProperty("activity_list")) {
                if (!Array.isArray(message.activity_list))
                    return "activity_list: array expected";
                for (let i = 0; i < message.activity_list.length; ++i) {
                    let error = $root.lq.BannerActivityData.verify(message.activity_list[i], _depth + 1);
                    if (error)
                        return "activity_list." + error;
                }
            }
            return null;
        };

        ResFetchBannerActivityData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResFetchBannerActivityData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResFetchBannerActivityData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResFetchBannerActivityData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.activity_list) {
                if (!Array.isArray(object.activity_list))
                    throw TypeError(".lq.ResFetchBannerActivityData.activity_list: array expected");
                message.activity_list = Array(object.activity_list.length);
                for (let i = 0; i < object.activity_list.length; ++i) {
                    if (typeof object.activity_list[i] !== "object")
                        throw TypeError(".lq.ResFetchBannerActivityData.activity_list: object expected");
                    message.activity_list[i] = $root.lq.BannerActivityData.fromObject(object.activity_list[i], _depth + 1);
                }
            }
            return message;
        };

        ResFetchBannerActivityData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.activity_list && message.activity_list.length) {
                object.activity_list = Array(message.activity_list.length);
                for (let j = 0; j < message.activity_list.length; ++j)
                    object.activity_list[j] = $root.lq.BannerActivityData.toObject(message.activity_list[j], options);
            }
            return object;
        };

        ResFetchBannerActivityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResFetchBannerActivityData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResFetchBannerActivityData";
        };

        return ResFetchBannerActivityData;
    })();

    lq.ReqMarathonActivityStartRace = (function() {

        function ReqMarathonActivityStartRace(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMarathonActivityStartRace.prototype.activity_id = 0;

        ReqMarathonActivityStartRace.create = function create(properties) {
            return new ReqMarathonActivityStartRace(properties);
        };

        ReqMarathonActivityStartRace.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMarathonActivityStartRace.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMarathonActivityStartRace.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMarathonActivityStartRace(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMarathonActivityStartRace.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMarathonActivityStartRace.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMarathonActivityStartRace.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMarathonActivityStartRace)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMarathonActivityStartRace();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMarathonActivityStartRace.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMarathonActivityStartRace.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMarathonActivityStartRace.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMarathonActivityStartRace";
        };

        return ReqMarathonActivityStartRace;
    })();

    lq.ResMarathonActivityStartRace = (function() {

        function ResMarathonActivityStartRace(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMarathonActivityStartRace.prototype.error = null;
        ResMarathonActivityStartRace.prototype.random_seed = 0;
        ResMarathonActivityStartRace.prototype.race_id = "";

        ResMarathonActivityStartRace.create = function create(properties) {
            return new ResMarathonActivityStartRace(properties);
        };

        ResMarathonActivityStartRace.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.random_seed != null && Object.hasOwnProperty.call(message, "random_seed"))
                writer.uint32(16).uint32(message.random_seed);
            if (message.race_id != null && Object.hasOwnProperty.call(message, "race_id"))
                writer.uint32(26).string(message.race_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMarathonActivityStartRace.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMarathonActivityStartRace.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMarathonActivityStartRace(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.random_seed = value;
                        else
                            delete message.random_seed;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.race_id = value;
                        else
                            delete message.race_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMarathonActivityStartRace.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMarathonActivityStartRace.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                if (!$util.isInteger(message.random_seed))
                    return "random_seed: integer expected";
            if (message.race_id != null && message.hasOwnProperty("race_id"))
                if (!$util.isString(message.race_id))
                    return "race_id: string expected";
            return null;
        };

        ResMarathonActivityStartRace.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMarathonActivityStartRace)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMarathonActivityStartRace();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMarathonActivityStartRace.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.random_seed != null)
                if (Number(object.random_seed) !== 0)
                    message.random_seed = object.random_seed >>> 0;
            if (object.race_id != null)
                if (typeof object.race_id !== "string" || object.race_id.length)
                    message.race_id = String(object.race_id);
            return message;
        };

        ResMarathonActivityStartRace.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.random_seed = 0;
                object.race_id = "";
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.random_seed != null && message.hasOwnProperty("random_seed"))
                object.random_seed = message.random_seed;
            if (message.race_id != null && message.hasOwnProperty("race_id"))
                object.race_id = message.race_id;
            return object;
        };

        ResMarathonActivityStartRace.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMarathonActivityStartRace.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMarathonActivityStartRace";
        };

        return ResMarathonActivityStartRace;
    })();

    lq.ReqMarathonActivityFinishRace = (function() {

        function ReqMarathonActivityFinishRace(properties) {
            this.race_data = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMarathonActivityFinishRace.prototype.activity_id = 0;
        ReqMarathonActivityFinishRace.prototype.race_data = $util.emptyArray;
        ReqMarathonActivityFinishRace.prototype.record = null;
        ReqMarathonActivityFinishRace.prototype.race_id = "";

        ReqMarathonActivityFinishRace.create = function create(properties) {
            return new ReqMarathonActivityFinishRace(properties);
        };

        ReqMarathonActivityFinishRace.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.race_data != null && message.race_data.length)
                for (let i = 0; i < message.race_data.length; ++i)
                    $root.lq.ActivityMarathonCheckData.encode(message.race_data[i], writer.uint32(18).fork()).ldelim();
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.MarathonGameRecord.encode(message.record, writer.uint32(26).fork()).ldelim();
            if (message.race_id != null && Object.hasOwnProperty.call(message, "race_id"))
                writer.uint32(34).string(message.race_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMarathonActivityFinishRace.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMarathonActivityFinishRace.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMarathonActivityFinishRace(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.race_data && message.race_data.length))
                            message.race_data = [];
                        message.race_data.push($root.lq.ActivityMarathonCheckData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.MarathonGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.race_id = value;
                        else
                            delete message.race_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMarathonActivityFinishRace.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMarathonActivityFinishRace.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.race_data != null && message.hasOwnProperty("race_data")) {
                if (!Array.isArray(message.race_data))
                    return "race_data: array expected";
                for (let i = 0; i < message.race_data.length; ++i) {
                    let error = $root.lq.ActivityMarathonCheckData.verify(message.race_data[i], _depth + 1);
                    if (error)
                        return "race_data." + error;
                }
            }
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.MarathonGameRecord.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.race_id != null && message.hasOwnProperty("race_id"))
                if (!$util.isString(message.race_id))
                    return "race_id: string expected";
            return null;
        };

        ReqMarathonActivityFinishRace.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMarathonActivityFinishRace)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMarathonActivityFinishRace();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.race_data) {
                if (!Array.isArray(object.race_data))
                    throw TypeError(".lq.ReqMarathonActivityFinishRace.race_data: array expected");
                message.race_data = Array(object.race_data.length);
                for (let i = 0; i < object.race_data.length; ++i) {
                    if (typeof object.race_data[i] !== "object")
                        throw TypeError(".lq.ReqMarathonActivityFinishRace.race_data: object expected");
                    message.race_data[i] = $root.lq.ActivityMarathonCheckData.fromObject(object.race_data[i], _depth + 1);
                }
            }
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.ReqMarathonActivityFinishRace.record: object expected");
                message.record = $root.lq.MarathonGameRecord.fromObject(object.record, _depth + 1);
            }
            if (object.race_id != null)
                if (typeof object.race_id !== "string" || object.race_id.length)
                    message.race_id = String(object.race_id);
            return message;
        };

        ReqMarathonActivityFinishRace.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.race_data = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.record = null;
                object.race_id = "";
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.race_data && message.race_data.length) {
                object.race_data = Array(message.race_data.length);
                for (let j = 0; j < message.race_data.length; ++j)
                    object.race_data[j] = $root.lq.ActivityMarathonCheckData.toObject(message.race_data[j], options);
            }
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.MarathonGameRecord.toObject(message.record, options);
            if (message.race_id != null && message.hasOwnProperty("race_id"))
                object.race_id = message.race_id;
            return object;
        };

        ReqMarathonActivityFinishRace.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMarathonActivityFinishRace.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMarathonActivityFinishRace";
        };

        return ReqMarathonActivityFinishRace;
    })();

    lq.ResMarathonActivityFinishRace = (function() {

        function ResMarathonActivityFinishRace(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMarathonActivityFinishRace.prototype.error = null;
        ResMarathonActivityFinishRace.prototype.highest_record = null;
        ResMarathonActivityFinishRace.prototype.rewards = $util.emptyArray;

        ResMarathonActivityFinishRace.create = function create(properties) {
            return new ResMarathonActivityFinishRace(properties);
        };

        ResMarathonActivityFinishRace.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.highest_record != null && Object.hasOwnProperty.call(message, "highest_record"))
                $root.lq.MarathonGameRecord.encode(message.highest_record, writer.uint32(18).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMarathonActivityFinishRace.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMarathonActivityFinishRace.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMarathonActivityFinishRace(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.highest_record = $root.lq.MarathonGameRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_record);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMarathonActivityFinishRace.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMarathonActivityFinishRace.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.highest_record != null && message.hasOwnProperty("highest_record")) {
                let error = $root.lq.MarathonGameRecord.verify(message.highest_record, _depth + 1);
                if (error)
                    return "highest_record." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResMarathonActivityFinishRace.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMarathonActivityFinishRace)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMarathonActivityFinishRace();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMarathonActivityFinishRace.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.highest_record != null) {
                if (typeof object.highest_record !== "object")
                    throw TypeError(".lq.ResMarathonActivityFinishRace.highest_record: object expected");
                message.highest_record = $root.lq.MarathonGameRecord.fromObject(object.highest_record, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResMarathonActivityFinishRace.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResMarathonActivityFinishRace.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResMarathonActivityFinishRace.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.error = null;
                object.highest_record = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.highest_record != null && message.hasOwnProperty("highest_record"))
                object.highest_record = $root.lq.MarathonGameRecord.toObject(message.highest_record, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResMarathonActivityFinishRace.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMarathonActivityFinishRace.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMarathonActivityFinishRace";
        };

        return ResMarathonActivityFinishRace;
    })();

    lq.ReqMMOActivityFetchData = (function() {

        function ReqMMOActivityFetchData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityFetchData.prototype.activity_id = 0;

        ReqMMOActivityFetchData.create = function create(properties) {
            return new ReqMMOActivityFetchData(properties);
        };

        ReqMMOActivityFetchData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityFetchData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityFetchData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityFetchData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityFetchData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityFetchData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMMOActivityFetchData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityFetchData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityFetchData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMMOActivityFetchData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMMOActivityFetchData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityFetchData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityFetchData";
        };

        return ReqMMOActivityFetchData;
    })();

    lq.ResMMOActivityFetchData = (function() {

        function ResMMOActivityFetchData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityFetchData.prototype.error = null;
        ResMMOActivityFetchData.prototype.mmo_data = null;

        ResMMOActivityFetchData.create = function create(properties) {
            return new ResMMOActivityFetchData(properties);
        };

        ResMMOActivityFetchData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.mmo_data != null && Object.hasOwnProperty.call(message, "mmo_data"))
                $root.lq.ActivityMMOData.encode(message.mmo_data, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityFetchData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityFetchData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityFetchData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.mmo_data = $root.lq.ActivityMMOData.decode(reader, reader.uint32(), undefined, _depth + 1, message.mmo_data);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityFetchData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityFetchData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.mmo_data != null && message.hasOwnProperty("mmo_data")) {
                let error = $root.lq.ActivityMMOData.verify(message.mmo_data, _depth + 1);
                if (error)
                    return "mmo_data." + error;
            }
            return null;
        };

        ResMMOActivityFetchData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityFetchData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityFetchData();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityFetchData.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.mmo_data != null) {
                if (typeof object.mmo_data !== "object")
                    throw TypeError(".lq.ResMMOActivityFetchData.mmo_data: object expected");
                message.mmo_data = $root.lq.ActivityMMOData.fromObject(object.mmo_data, _depth + 1);
            }
            return message;
        };

        ResMMOActivityFetchData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.mmo_data = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.mmo_data != null && message.hasOwnProperty("mmo_data"))
                object.mmo_data = $root.lq.ActivityMMOData.toObject(message.mmo_data, options);
            return object;
        };

        ResMMOActivityFetchData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityFetchData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityFetchData";
        };

        return ResMMOActivityFetchData;
    })();

    lq.ReqMMOActivityEquipFusion = (function() {

        function ReqMMOActivityEquipFusion(properties) {
            this.equip_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityEquipFusion.prototype.activity_id = 0;
        ReqMMOActivityEquipFusion.prototype.equip_list = $util.emptyArray;
        ReqMMOActivityEquipFusion.prototype.target_type = 0;

        ReqMMOActivityEquipFusion.create = function create(properties) {
            return new ReqMMOActivityEquipFusion(properties);
        };

        ReqMMOActivityEquipFusion.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.equip_list != null && message.equip_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.equip_list.length; ++i)
                    writer.uint32(message.equip_list[i]);
                writer.ldelim();
            }
            if (message.target_type != null && Object.hasOwnProperty.call(message, "target_type"))
                writer.uint32(24).uint32(message.target_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityEquipFusion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityEquipFusion.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityEquipFusion(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.equip_list && message.equip_list.length))
                                message.equip_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equip_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.equip_list && message.equip_list.length))
                            message.equip_list = [];
                        message.equip_list.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.target_type = value;
                        else
                            delete message.target_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityEquipFusion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityEquipFusion.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.equip_list != null && message.hasOwnProperty("equip_list")) {
                if (!Array.isArray(message.equip_list))
                    return "equip_list: array expected";
                for (let i = 0; i < message.equip_list.length; ++i)
                    if (!$util.isInteger(message.equip_list[i]))
                        return "equip_list: integer[] expected";
            }
            if (message.target_type != null && message.hasOwnProperty("target_type"))
                if (!$util.isInteger(message.target_type))
                    return "target_type: integer expected";
            return null;
        };

        ReqMMOActivityEquipFusion.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityEquipFusion)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityEquipFusion();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.equip_list) {
                if (!Array.isArray(object.equip_list))
                    throw TypeError(".lq.ReqMMOActivityEquipFusion.equip_list: array expected");
                message.equip_list = Array(object.equip_list.length);
                for (let i = 0; i < object.equip_list.length; ++i)
                    message.equip_list[i] = object.equip_list[i] >>> 0;
            }
            if (object.target_type != null)
                if (Number(object.target_type) !== 0)
                    message.target_type = object.target_type >>> 0;
            return message;
        };

        ReqMMOActivityEquipFusion.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.equip_list = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.target_type = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.equip_list && message.equip_list.length) {
                object.equip_list = Array(message.equip_list.length);
                for (let j = 0; j < message.equip_list.length; ++j)
                    object.equip_list[j] = message.equip_list[j];
            }
            if (message.target_type != null && message.hasOwnProperty("target_type"))
                object.target_type = message.target_type;
            return object;
        };

        ReqMMOActivityEquipFusion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityEquipFusion.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityEquipFusion";
        };

        return ReqMMOActivityEquipFusion;
    })();

    lq.ResMMOActivityEquipFusion = (function() {

        function ResMMOActivityEquipFusion(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityEquipFusion.prototype.error = null;
        ResMMOActivityEquipFusion.prototype.result = 0;
        ResMMOActivityEquipFusion.prototype.changes = null;

        ResMMOActivityEquipFusion.create = function create(properties) {
            return new ResMMOActivityEquipFusion(properties);
        };

        ResMMOActivityEquipFusion.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(16).uint32(message.result);
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityEquipFusion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityEquipFusion.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityEquipFusion(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityEquipFusion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityEquipFusion.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivityEquipFusion.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityEquipFusion)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityEquipFusion();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityEquipFusion.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityEquipFusion.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivityEquipFusion.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.result = 0;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivityEquipFusion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityEquipFusion.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityEquipFusion";
        };

        return ResMMOActivityEquipFusion;
    })();

    lq.ReqMMOActivitySetCharacter = (function() {

        function ReqMMOActivitySetCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivitySetCharacter.prototype.activity_id = 0;
        ReqMMOActivitySetCharacter.prototype.character_id = 0;

        ReqMMOActivitySetCharacter.create = function create(properties) {
            return new ReqMMOActivitySetCharacter(properties);
        };

        ReqMMOActivitySetCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                writer.uint32(16).uint32(message.character_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivitySetCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivitySetCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivitySetCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.character_id = value;
                        else
                            delete message.character_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivitySetCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivitySetCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                if (!$util.isInteger(message.character_id))
                    return "character_id: integer expected";
            return null;
        };

        ReqMMOActivitySetCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivitySetCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivitySetCharacter();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.character_id != null)
                if (Number(object.character_id) !== 0)
                    message.character_id = object.character_id >>> 0;
            return message;
        };

        ReqMMOActivitySetCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.activity_id = 0;
                object.character_id = 0;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.character_id != null && message.hasOwnProperty("character_id"))
                object.character_id = message.character_id;
            return object;
        };

        ReqMMOActivitySetCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivitySetCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivitySetCharacter";
        };

        return ReqMMOActivitySetCharacter;
    })();

    lq.ResMMOActivitySetCharacter = (function() {

        function ResMMOActivitySetCharacter(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivitySetCharacter.prototype.error = null;
        ResMMOActivitySetCharacter.prototype.changes = null;

        ResMMOActivitySetCharacter.create = function create(properties) {
            return new ResMMOActivitySetCharacter(properties);
        };

        ResMMOActivitySetCharacter.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivitySetCharacter.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivitySetCharacter.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivitySetCharacter(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivitySetCharacter.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivitySetCharacter.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivitySetCharacter.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivitySetCharacter)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivitySetCharacter();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivitySetCharacter.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivitySetCharacter.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivitySetCharacter.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivitySetCharacter.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivitySetCharacter.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivitySetCharacter";
        };

        return ResMMOActivitySetCharacter;
    })();

    lq.ReqMMOActivitySetTeamMember = (function() {

        function ReqMMOActivitySetTeamMember(properties) {
            this.members = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivitySetTeamMember.prototype.activity_id = 0;
        ReqMMOActivitySetTeamMember.prototype.members = $util.emptyArray;

        ReqMMOActivitySetTeamMember.create = function create(properties) {
            return new ReqMMOActivitySetTeamMember(properties);
        };

        ReqMMOActivitySetTeamMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.members != null && message.members.length)
                for (let i = 0; i < message.members.length; ++i)
                    $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember.encode(message.members[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivitySetTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivitySetTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivitySetTeamMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.members && message.members.length))
                            message.members = [];
                        message.members.push($root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivitySetTeamMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivitySetTeamMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.members != null && message.hasOwnProperty("members")) {
                if (!Array.isArray(message.members))
                    return "members: array expected";
                for (let i = 0; i < message.members.length; ++i) {
                    let error = $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember.verify(message.members[i], _depth + 1);
                    if (error)
                        return "members." + error;
                }
            }
            return null;
        };

        ReqMMOActivitySetTeamMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivitySetTeamMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivitySetTeamMember();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.members) {
                if (!Array.isArray(object.members))
                    throw TypeError(".lq.ReqMMOActivitySetTeamMember.members: array expected");
                message.members = Array(object.members.length);
                for (let i = 0; i < object.members.length; ++i) {
                    if (typeof object.members[i] !== "object")
                        throw TypeError(".lq.ReqMMOActivitySetTeamMember.members: object expected");
                    message.members[i] = $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember.fromObject(object.members[i], _depth + 1);
                }
            }
            return message;
        };

        ReqMMOActivitySetTeamMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.members = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.members && message.members.length) {
                object.members = Array(message.members.length);
                for (let j = 0; j < message.members.length; ++j)
                    object.members[j] = $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember.toObject(message.members[j], options);
            }
            return object;
        };

        ReqMMOActivitySetTeamMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivitySetTeamMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivitySetTeamMember";
        };

        ReqMMOActivitySetTeamMember.MMOActivityTeamMember = (function() {

            function MMOActivityTeamMember(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MMOActivityTeamMember.prototype.character_id = 0;
            MMOActivityTeamMember.prototype.id = 0;
            MMOActivityTeamMember.prototype.type = 0;

            MMOActivityTeamMember.create = function create(properties) {
                return new MMOActivityTeamMember(properties);
            };

            MMOActivityTeamMember.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.character_id != null && Object.hasOwnProperty.call(message, "character_id"))
                    writer.uint32(8).uint32(message.character_id);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(16).uint32(message.id);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(24).uint32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MMOActivityTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MMOActivityTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.character_id = value;
                            else
                                delete message.character_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MMOActivityTeamMember.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MMOActivityTeamMember.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    if (!$util.isInteger(message.character_id))
                        return "character_id: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                return null;
            };

            MMOActivityTeamMember.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember();
                if (object.character_id != null)
                    if (Number(object.character_id) !== 0)
                        message.character_id = object.character_id >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                return message;
            };

            MMOActivityTeamMember.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.character_id = 0;
                    object.id = 0;
                    object.type = 0;
                }
                if (message.character_id != null && message.hasOwnProperty("character_id"))
                    object.character_id = message.character_id;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                return object;
            };

            MMOActivityTeamMember.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MMOActivityTeamMember.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ReqMMOActivitySetTeamMember.MMOActivityTeamMember";
            };

            return MMOActivityTeamMember;
        })();

        return ReqMMOActivitySetTeamMember;
    })();

    lq.ResMMOActivitySetTeamMember = (function() {

        function ResMMOActivitySetTeamMember(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivitySetTeamMember.prototype.error = null;
        ResMMOActivitySetTeamMember.prototype.changes = null;

        ResMMOActivitySetTeamMember.create = function create(properties) {
            return new ResMMOActivitySetTeamMember(properties);
        };

        ResMMOActivitySetTeamMember.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivitySetTeamMember.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivitySetTeamMember.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivitySetTeamMember(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivitySetTeamMember.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivitySetTeamMember.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivitySetTeamMember.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivitySetTeamMember)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivitySetTeamMember();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivitySetTeamMember.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivitySetTeamMember.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivitySetTeamMember.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivitySetTeamMember.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivitySetTeamMember.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivitySetTeamMember";
        };

        return ResMMOActivitySetTeamMember;
    })();

    lq.ReqMMOActivityStartBattle = (function() {

        function ReqMMOActivityStartBattle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityStartBattle.prototype.activity_id = 0;

        ReqMMOActivityStartBattle.create = function create(properties) {
            return new ReqMMOActivityStartBattle(properties);
        };

        ReqMMOActivityStartBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityStartBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityStartBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityStartBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityStartBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityStartBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMMOActivityStartBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityStartBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityStartBattle();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMMOActivityStartBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMMOActivityStartBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityStartBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityStartBattle";
        };

        return ReqMMOActivityStartBattle;
    })();

    lq.ResMMOActivityStartBattle = (function() {

        function ResMMOActivityStartBattle(properties) {
            this.actions = [];
            this.units = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityStartBattle.prototype.error = null;
        ResMMOActivityStartBattle.prototype.actions = $util.emptyArray;
        ResMMOActivityStartBattle.prototype.units = $util.emptyArray;
        ResMMOActivityStartBattle.prototype.changes = null;
        ResMMOActivityStartBattle.prototype.result = 0;

        ResMMOActivityStartBattle.create = function create(properties) {
            return new ResMMOActivityStartBattle(properties);
        };

        ResMMOActivityStartBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction.encode(message.actions[i], writer.uint32(18).fork()).ldelim();
            if (message.units != null && message.units.length)
                for (let i = 0; i < message.units.length; ++i)
                    $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.encode(message.units[i], writer.uint32(26).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(34).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(40).uint32(message.result);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityStartBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityStartBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityStartBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.units && message.units.length))
                            message.units = [];
                        message.units.push($root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityStartBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityStartBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.actions != null && message.hasOwnProperty("actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction.verify(message.actions[i], _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.units != null && message.hasOwnProperty("units")) {
                if (!Array.isArray(message.units))
                    return "units: array expected";
                for (let i = 0; i < message.units.length; ++i) {
                    let error = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.verify(message.units[i], _depth + 1);
                    if (error)
                        return "units." + error;
                }
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            return null;
        };

        ResMMOActivityStartBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityStartBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityStartBattle();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityStartBattle.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".lq.ResMMOActivityStartBattle.actions: array expected");
                message.actions = Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i) {
                    if (typeof object.actions[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityStartBattle.actions: object expected");
                    message.actions[i] = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction.fromObject(object.actions[i], _depth + 1);
                }
            }
            if (object.units) {
                if (!Array.isArray(object.units))
                    throw TypeError(".lq.ResMMOActivityStartBattle.units: array expected");
                message.units = Array(object.units.length);
                for (let i = 0; i < object.units.length; ++i) {
                    if (typeof object.units[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityStartBattle.units: object expected");
                    message.units[i] = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.fromObject(object.units[i], _depth + 1);
                }
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityStartBattle.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            return message;
        };

        ResMMOActivityStartBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.actions = [];
                object.units = [];
            }
            if (options.defaults) {
                object.error = null;
                object.changes = null;
                object.result = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.actions && message.actions.length) {
                object.actions = Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction.toObject(message.actions[j], options);
            }
            if (message.units && message.units.length) {
                object.units = Array(message.units.length);
                for (let j = 0; j < message.units.length; ++j)
                    object.units[j] = $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.toObject(message.units[j], options);
            }
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            return object;
        };

        ResMMOActivityStartBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityStartBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityStartBattle";
        };

        ResMMOActivityStartBattle.MMOActivityBattleAction = (function() {

            function MMOActivityBattleAction(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MMOActivityBattleAction.prototype.type = 0;
            MMOActivityBattleAction.prototype.target = 0;
            MMOActivityBattleAction.prototype.from = 0;
            MMOActivityBattleAction.prototype.value = 0;
            MMOActivityBattleAction.prototype.tick = 0;
            MMOActivityBattleAction.prototype.critical = 0;

            MMOActivityBattleAction.create = function create(properties) {
                return new MMOActivityBattleAction(properties);
            };

            MMOActivityBattleAction.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(8).uint32(message.type);
                if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                    writer.uint32(16).uint32(message.target);
                if (message.from != null && Object.hasOwnProperty.call(message, "from"))
                    writer.uint32(24).uint32(message.from);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(32).uint32(message.value);
                if (message.tick != null && Object.hasOwnProperty.call(message, "tick"))
                    writer.uint32(40).uint32(message.tick);
                if (message.critical != null && Object.hasOwnProperty.call(message, "critical"))
                    writer.uint32(48).uint32(message.critical);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MMOActivityBattleAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MMOActivityBattleAction.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.target = value;
                            else
                                delete message.target;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from = value;
                            else
                                delete message.from;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.value = value;
                            else
                                delete message.value;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.tick = value;
                            else
                                delete message.tick;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.critical = value;
                            else
                                delete message.critical;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MMOActivityBattleAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MMOActivityBattleAction.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                if (message.target != null && message.hasOwnProperty("target"))
                    if (!$util.isInteger(message.target))
                        return "target: integer expected";
                if (message.from != null && message.hasOwnProperty("from"))
                    if (!$util.isInteger(message.from))
                        return "from: integer expected";
                if (message.value != null && message.hasOwnProperty("value"))
                    if (!$util.isInteger(message.value))
                        return "value: integer expected";
                if (message.tick != null && message.hasOwnProperty("tick"))
                    if (!$util.isInteger(message.tick))
                        return "tick: integer expected";
                if (message.critical != null && message.hasOwnProperty("critical"))
                    if (!$util.isInteger(message.critical))
                        return "critical: integer expected";
                return null;
            };

            MMOActivityBattleAction.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResMMOActivityStartBattle.MMOActivityBattleAction();
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                if (object.target != null)
                    if (Number(object.target) !== 0)
                        message.target = object.target >>> 0;
                if (object.from != null)
                    if (Number(object.from) !== 0)
                        message.from = object.from >>> 0;
                if (object.value != null)
                    if (Number(object.value) !== 0)
                        message.value = object.value >>> 0;
                if (object.tick != null)
                    if (Number(object.tick) !== 0)
                        message.tick = object.tick >>> 0;
                if (object.critical != null)
                    if (Number(object.critical) !== 0)
                        message.critical = object.critical >>> 0;
                return message;
            };

            MMOActivityBattleAction.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.type = 0;
                    object.target = 0;
                    object.from = 0;
                    object.value = 0;
                    object.tick = 0;
                    object.critical = 0;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                if (message.target != null && message.hasOwnProperty("target"))
                    object.target = message.target;
                if (message.from != null && message.hasOwnProperty("from"))
                    object.from = message.from;
                if (message.value != null && message.hasOwnProperty("value"))
                    object.value = message.value;
                if (message.tick != null && message.hasOwnProperty("tick"))
                    object.tick = message.tick;
                if (message.critical != null && message.hasOwnProperty("critical"))
                    object.critical = message.critical;
                return object;
            };

            MMOActivityBattleAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MMOActivityBattleAction.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResMMOActivityStartBattle.MMOActivityBattleAction";
            };

            return MMOActivityBattleAction;
        })();

        ResMMOActivityStartBattle.MMOActivityBattleUnit = (function() {

            function MMOActivityBattleUnit(properties) {
                this.equipments = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MMOActivityBattleUnit.prototype.id = 0;
            MMOActivityBattleUnit.prototype.hp = 0;
            MMOActivityBattleUnit.prototype.equipments = $util.emptyArray;

            MMOActivityBattleUnit.create = function create(properties) {
                return new MMOActivityBattleUnit(properties);
            };

            MMOActivityBattleUnit.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(8).uint32(message.id);
                if (message.hp != null && Object.hasOwnProperty.call(message, "hp"))
                    writer.uint32(16).uint32(message.hp);
                if (message.equipments != null && message.equipments.length) {
                    writer.uint32(26).fork();
                    for (let i = 0; i < message.equipments.length; ++i)
                        writer.uint32(message.equipments[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MMOActivityBattleUnit.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MMOActivityBattleUnit.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.hp = value;
                            else
                                delete message.hp;
                            continue;
                        }
                    case 3: {
                            if (wireType === 2) {
                                if (!(message.equipments && message.equipments.length))
                                    message.equipments = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.equipments.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            message.equipments.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MMOActivityBattleUnit.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MMOActivityBattleUnit.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.hp != null && message.hasOwnProperty("hp"))
                    if (!$util.isInteger(message.hp))
                        return "hp: integer expected";
                if (message.equipments != null && message.hasOwnProperty("equipments")) {
                    if (!Array.isArray(message.equipments))
                        return "equipments: array expected";
                    for (let i = 0; i < message.equipments.length; ++i)
                        if (!$util.isInteger(message.equipments[i]))
                            return "equipments: integer[] expected";
                }
                return null;
            };

            MMOActivityBattleUnit.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.ResMMOActivityStartBattle.MMOActivityBattleUnit();
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.hp != null)
                    if (Number(object.hp) !== 0)
                        message.hp = object.hp >>> 0;
                if (object.equipments) {
                    if (!Array.isArray(object.equipments))
                        throw TypeError(".lq.ResMMOActivityStartBattle.MMOActivityBattleUnit.equipments: array expected");
                    message.equipments = Array(object.equipments.length);
                    for (let i = 0; i < object.equipments.length; ++i)
                        message.equipments[i] = object.equipments[i] >>> 0;
                }
                return message;
            };

            MMOActivityBattleUnit.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.equipments = [];
                if (options.defaults) {
                    object.id = 0;
                    object.hp = 0;
                }
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.hp != null && message.hasOwnProperty("hp"))
                    object.hp = message.hp;
                if (message.equipments && message.equipments.length) {
                    object.equipments = Array(message.equipments.length);
                    for (let j = 0; j < message.equipments.length; ++j)
                        object.equipments[j] = message.equipments[j];
                }
                return object;
            };

            MMOActivityBattleUnit.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MMOActivityBattleUnit.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.ResMMOActivityStartBattle.MMOActivityBattleUnit";
            };

            return MMOActivityBattleUnit;
        })();

        return ResMMOActivityStartBattle;
    })();

    lq.ReqMMOActivityFinishBattle = (function() {

        function ReqMMOActivityFinishBattle(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityFinishBattle.prototype.activity_id = 0;

        ReqMMOActivityFinishBattle.create = function create(properties) {
            return new ReqMMOActivityFinishBattle(properties);
        };

        ReqMMOActivityFinishBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityFinishBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityFinishBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityFinishBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityFinishBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityFinishBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMMOActivityFinishBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityFinishBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityFinishBattle();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMMOActivityFinishBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMMOActivityFinishBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityFinishBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityFinishBattle";
        };

        return ReqMMOActivityFinishBattle;
    })();

    lq.ResMMOActivityFinishBattle = (function() {

        function ResMMOActivityFinishBattle(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityFinishBattle.prototype.error = null;
        ResMMOActivityFinishBattle.prototype.changes = null;
        ResMMOActivityFinishBattle.prototype.rewards = $util.emptyArray;

        ResMMOActivityFinishBattle.create = function create(properties) {
            return new ResMMOActivityFinishBattle(properties);
        };

        ResMMOActivityFinishBattle.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityFinishBattle.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityFinishBattle.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityFinishBattle(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityFinishBattle.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityFinishBattle.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            return null;
        };

        ResMMOActivityFinishBattle.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityFinishBattle)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityFinishBattle();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityFinishBattle.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityFinishBattle.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResMMOActivityFinishBattle.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityFinishBattle.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            return message;
        };

        ResMMOActivityFinishBattle.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            return object;
        };

        ResMMOActivityFinishBattle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityFinishBattle.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityFinishBattle";
        };

        return ResMMOActivityFinishBattle;
    })();

    lq.ReqMMOActivitySetEquip = (function() {

        function ReqMMOActivitySetEquip(properties) {
            this.equipments = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivitySetEquip.prototype.activity_id = 0;
        ReqMMOActivitySetEquip.prototype.equipments = $util.emptyArray;

        ReqMMOActivitySetEquip.create = function create(properties) {
            return new ReqMMOActivitySetEquip(properties);
        };

        ReqMMOActivitySetEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.equipments != null && message.equipments.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.equipments.length; ++i)
                    writer.uint32(message.equipments[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivitySetEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivitySetEquip.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivitySetEquip(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.equipments && message.equipments.length))
                                message.equipments = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.equipments.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.equipments && message.equipments.length))
                            message.equipments = [];
                        message.equipments.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivitySetEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivitySetEquip.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.equipments != null && message.hasOwnProperty("equipments")) {
                if (!Array.isArray(message.equipments))
                    return "equipments: array expected";
                for (let i = 0; i < message.equipments.length; ++i)
                    if (!$util.isInteger(message.equipments[i]))
                        return "equipments: integer[] expected";
            }
            return null;
        };

        ReqMMOActivitySetEquip.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivitySetEquip)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivitySetEquip();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.equipments) {
                if (!Array.isArray(object.equipments))
                    throw TypeError(".lq.ReqMMOActivitySetEquip.equipments: array expected");
                message.equipments = Array(object.equipments.length);
                for (let i = 0; i < object.equipments.length; ++i)
                    message.equipments[i] = object.equipments[i] >>> 0;
            }
            return message;
        };

        ReqMMOActivitySetEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.equipments = [];
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.equipments && message.equipments.length) {
                object.equipments = Array(message.equipments.length);
                for (let j = 0; j < message.equipments.length; ++j)
                    object.equipments[j] = message.equipments[j];
            }
            return object;
        };

        ReqMMOActivitySetEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivitySetEquip.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivitySetEquip";
        };

        return ReqMMOActivitySetEquip;
    })();

    lq.ResMMOActivitySetEquip = (function() {

        function ResMMOActivitySetEquip(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivitySetEquip.prototype.error = null;
        ResMMOActivitySetEquip.prototype.changes = null;

        ResMMOActivitySetEquip.create = function create(properties) {
            return new ResMMOActivitySetEquip(properties);
        };

        ResMMOActivitySetEquip.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivitySetEquip.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivitySetEquip.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivitySetEquip(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivitySetEquip.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivitySetEquip.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivitySetEquip.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivitySetEquip)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivitySetEquip();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivitySetEquip.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivitySetEquip.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivitySetEquip.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivitySetEquip.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivitySetEquip.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivitySetEquip";
        };

        return ResMMOActivitySetEquip;
    })();

    lq.ReqMMOActivityUpdatehFriendList = (function() {

        function ReqMMOActivityUpdatehFriendList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityUpdatehFriendList.prototype.activity_id = 0;

        ReqMMOActivityUpdatehFriendList.create = function create(properties) {
            return new ReqMMOActivityUpdatehFriendList(properties);
        };

        ReqMMOActivityUpdatehFriendList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityUpdatehFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityUpdatehFriendList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityUpdatehFriendList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityUpdatehFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityUpdatehFriendList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMMOActivityUpdatehFriendList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityUpdatehFriendList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityUpdatehFriendList();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMMOActivityUpdatehFriendList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMMOActivityUpdatehFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityUpdatehFriendList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityUpdatehFriendList";
        };

        return ReqMMOActivityUpdatehFriendList;
    })();

    lq.ResMMOActivityUpdatehFriendList = (function() {

        function ResMMOActivityUpdatehFriendList(properties) {
            this.friend_list = [];
            this.random_friends = [];
            this.npc_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityUpdatehFriendList.prototype.error = null;
        ResMMOActivityUpdatehFriendList.prototype.friend_list = $util.emptyArray;
        ResMMOActivityUpdatehFriendList.prototype.random_friends = $util.emptyArray;
        ResMMOActivityUpdatehFriendList.prototype.npc_list = $util.emptyArray;
        ResMMOActivityUpdatehFriendList.prototype.expire_time = 0;
        ResMMOActivityUpdatehFriendList.prototype.changes = null;

        ResMMOActivityUpdatehFriendList.create = function create(properties) {
            return new ResMMOActivityUpdatehFriendList(properties);
        };

        ResMMOActivityUpdatehFriendList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.friend_list != null && message.friend_list.length)
                for (let i = 0; i < message.friend_list.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.friend_list[i], writer.uint32(18).fork()).ldelim();
            if (message.random_friends != null && message.random_friends.length)
                for (let i = 0; i < message.random_friends.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.random_friends[i], writer.uint32(26).fork()).ldelim();
            if (message.npc_list != null && message.npc_list.length)
                for (let i = 0; i < message.npc_list.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.npc_list[i], writer.uint32(34).fork()).ldelim();
            if (message.expire_time != null && Object.hasOwnProperty.call(message, "expire_time"))
                writer.uint32(40).uint32(message.expire_time);
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityUpdatehFriendList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityUpdatehFriendList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityUpdatehFriendList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.friend_list && message.friend_list.length))
                            message.friend_list = [];
                        message.friend_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.random_friends && message.random_friends.length))
                            message.random_friends = [];
                        message.random_friends.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.npc_list && message.npc_list.length))
                            message.npc_list = [];
                        message.npc_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.expire_time = value;
                        else
                            delete message.expire_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityUpdatehFriendList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityUpdatehFriendList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.friend_list != null && message.hasOwnProperty("friend_list")) {
                if (!Array.isArray(message.friend_list))
                    return "friend_list: array expected";
                for (let i = 0; i < message.friend_list.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.friend_list[i], _depth + 1);
                    if (error)
                        return "friend_list." + error;
                }
            }
            if (message.random_friends != null && message.hasOwnProperty("random_friends")) {
                if (!Array.isArray(message.random_friends))
                    return "random_friends: array expected";
                for (let i = 0; i < message.random_friends.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.random_friends[i], _depth + 1);
                    if (error)
                        return "random_friends." + error;
                }
            }
            if (message.npc_list != null && message.hasOwnProperty("npc_list")) {
                if (!Array.isArray(message.npc_list))
                    return "npc_list: array expected";
                for (let i = 0; i < message.npc_list.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.npc_list[i], _depth + 1);
                    if (error)
                        return "npc_list." + error;
                }
            }
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                if (!$util.isInteger(message.expire_time))
                    return "expire_time: integer expected";
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivityUpdatehFriendList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityUpdatehFriendList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityUpdatehFriendList();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityUpdatehFriendList.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.friend_list) {
                if (!Array.isArray(object.friend_list))
                    throw TypeError(".lq.ResMMOActivityUpdatehFriendList.friend_list: array expected");
                message.friend_list = Array(object.friend_list.length);
                for (let i = 0; i < object.friend_list.length; ++i) {
                    if (typeof object.friend_list[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityUpdatehFriendList.friend_list: object expected");
                    message.friend_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.friend_list[i], _depth + 1);
                }
            }
            if (object.random_friends) {
                if (!Array.isArray(object.random_friends))
                    throw TypeError(".lq.ResMMOActivityUpdatehFriendList.random_friends: array expected");
                message.random_friends = Array(object.random_friends.length);
                for (let i = 0; i < object.random_friends.length; ++i) {
                    if (typeof object.random_friends[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityUpdatehFriendList.random_friends: object expected");
                    message.random_friends[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.random_friends[i], _depth + 1);
                }
            }
            if (object.npc_list) {
                if (!Array.isArray(object.npc_list))
                    throw TypeError(".lq.ResMMOActivityUpdatehFriendList.npc_list: array expected");
                message.npc_list = Array(object.npc_list.length);
                for (let i = 0; i < object.npc_list.length; ++i) {
                    if (typeof object.npc_list[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityUpdatehFriendList.npc_list: object expected");
                    message.npc_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.npc_list[i], _depth + 1);
                }
            }
            if (object.expire_time != null)
                if (Number(object.expire_time) !== 0)
                    message.expire_time = object.expire_time >>> 0;
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityUpdatehFriendList.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivityUpdatehFriendList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.friend_list = [];
                object.random_friends = [];
                object.npc_list = [];
            }
            if (options.defaults) {
                object.error = null;
                object.expire_time = 0;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.friend_list && message.friend_list.length) {
                object.friend_list = Array(message.friend_list.length);
                for (let j = 0; j < message.friend_list.length; ++j)
                    object.friend_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.friend_list[j], options);
            }
            if (message.random_friends && message.random_friends.length) {
                object.random_friends = Array(message.random_friends.length);
                for (let j = 0; j < message.random_friends.length; ++j)
                    object.random_friends[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.random_friends[j], options);
            }
            if (message.npc_list && message.npc_list.length) {
                object.npc_list = Array(message.npc_list.length);
                for (let j = 0; j < message.npc_list.length; ++j)
                    object.npc_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.npc_list[j], options);
            }
            if (message.expire_time != null && message.hasOwnProperty("expire_time"))
                object.expire_time = message.expire_time;
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivityUpdatehFriendList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityUpdatehFriendList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityUpdatehFriendList";
        };

        return ResMMOActivityUpdatehFriendList;
    })();

    lq.ReqMMOActivityReceiveSupportReward = (function() {

        function ReqMMOActivityReceiveSupportReward(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityReceiveSupportReward.prototype.activity_id = 0;

        ReqMMOActivityReceiveSupportReward.create = function create(properties) {
            return new ReqMMOActivityReceiveSupportReward(properties);
        };

        ReqMMOActivityReceiveSupportReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityReceiveSupportReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityReceiveSupportReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityReceiveSupportReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityReceiveSupportReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityReceiveSupportReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            return null;
        };

        ReqMMOActivityReceiveSupportReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityReceiveSupportReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityReceiveSupportReward();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            return message;
        };

        ReqMMOActivityReceiveSupportReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            return object;
        };

        ReqMMOActivityReceiveSupportReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityReceiveSupportReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityReceiveSupportReward";
        };

        return ReqMMOActivityReceiveSupportReward;
    })();

    lq.ResMMOActivityReceiveSupportReward = (function() {

        function ResMMOActivityReceiveSupportReward(properties) {
            this.rewards = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityReceiveSupportReward.prototype.error = null;
        ResMMOActivityReceiveSupportReward.prototype.rewards = $util.emptyArray;
        ResMMOActivityReceiveSupportReward.prototype.changes = null;
        ResMMOActivityReceiveSupportReward.prototype.support_count = 0;
        ResMMOActivityReceiveSupportReward.prototype.total_support_count = 0;

        ResMMOActivityReceiveSupportReward.create = function create(properties) {
            return new ResMMOActivityReceiveSupportReward(properties);
        };

        ResMMOActivityReceiveSupportReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.rewards != null && message.rewards.length)
                for (let i = 0; i < message.rewards.length; ++i)
                    $root.lq.ExecuteReward.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(26).fork()).ldelim();
            if (message.support_count != null && Object.hasOwnProperty.call(message, "support_count"))
                writer.uint32(32).uint32(message.support_count);
            if (message.total_support_count != null && Object.hasOwnProperty.call(message, "total_support_count"))
                writer.uint32(40).uint32(message.total_support_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityReceiveSupportReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityReceiveSupportReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityReceiveSupportReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rewards && message.rewards.length))
                            message.rewards = [];
                        message.rewards.push($root.lq.ExecuteReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.support_count = value;
                        else
                            delete message.support_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_support_count = value;
                        else
                            delete message.total_support_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityReceiveSupportReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityReceiveSupportReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.rewards != null && message.hasOwnProperty("rewards")) {
                if (!Array.isArray(message.rewards))
                    return "rewards: array expected";
                for (let i = 0; i < message.rewards.length; ++i) {
                    let error = $root.lq.ExecuteReward.verify(message.rewards[i], _depth + 1);
                    if (error)
                        return "rewards." + error;
                }
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            if (message.support_count != null && message.hasOwnProperty("support_count"))
                if (!$util.isInteger(message.support_count))
                    return "support_count: integer expected";
            if (message.total_support_count != null && message.hasOwnProperty("total_support_count"))
                if (!$util.isInteger(message.total_support_count))
                    return "total_support_count: integer expected";
            return null;
        };

        ResMMOActivityReceiveSupportReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityReceiveSupportReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityReceiveSupportReward();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityReceiveSupportReward.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.rewards) {
                if (!Array.isArray(object.rewards))
                    throw TypeError(".lq.ResMMOActivityReceiveSupportReward.rewards: array expected");
                message.rewards = Array(object.rewards.length);
                for (let i = 0; i < object.rewards.length; ++i) {
                    if (typeof object.rewards[i] !== "object")
                        throw TypeError(".lq.ResMMOActivityReceiveSupportReward.rewards: object expected");
                    message.rewards[i] = $root.lq.ExecuteReward.fromObject(object.rewards[i], _depth + 1);
                }
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityReceiveSupportReward.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            if (object.support_count != null)
                if (Number(object.support_count) !== 0)
                    message.support_count = object.support_count >>> 0;
            if (object.total_support_count != null)
                if (Number(object.total_support_count) !== 0)
                    message.total_support_count = object.total_support_count >>> 0;
            return message;
        };

        ResMMOActivityReceiveSupportReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.rewards = [];
            if (options.defaults) {
                object.error = null;
                object.changes = null;
                object.support_count = 0;
                object.total_support_count = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.rewards && message.rewards.length) {
                object.rewards = Array(message.rewards.length);
                for (let j = 0; j < message.rewards.length; ++j)
                    object.rewards[j] = $root.lq.ExecuteReward.toObject(message.rewards[j], options);
            }
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            if (message.support_count != null && message.hasOwnProperty("support_count"))
                object.support_count = message.support_count;
            if (message.total_support_count != null && message.hasOwnProperty("total_support_count"))
                object.total_support_count = message.total_support_count;
            return object;
        };

        ResMMOActivityReceiveSupportReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityReceiveSupportReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityReceiveSupportReward";
        };

        return ResMMOActivityReceiveSupportReward;
    })();

    lq.ReqMMOActivityDebugSetTeamCandidate = (function() {

        function ReqMMOActivityDebugSetTeamCandidate(properties) {
            this.friend_list = [];
            this.random_friends = [];
            this.npc_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqMMOActivityDebugSetTeamCandidate.prototype.activity_id = 0;
        ReqMMOActivityDebugSetTeamCandidate.prototype.friend_list = $util.emptyArray;
        ReqMMOActivityDebugSetTeamCandidate.prototype.random_friends = $util.emptyArray;
        ReqMMOActivityDebugSetTeamCandidate.prototype.npc_list = $util.emptyArray;

        ReqMMOActivityDebugSetTeamCandidate.create = function create(properties) {
            return new ReqMMOActivityDebugSetTeamCandidate(properties);
        };

        ReqMMOActivityDebugSetTeamCandidate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.friend_list != null && message.friend_list.length)
                for (let i = 0; i < message.friend_list.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.friend_list[i], writer.uint32(18).fork()).ldelim();
            if (message.random_friends != null && message.random_friends.length)
                for (let i = 0; i < message.random_friends.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.random_friends[i], writer.uint32(26).fork()).ldelim();
            if (message.npc_list != null && message.npc_list.length)
                for (let i = 0; i < message.npc_list.length; ++i)
                    $root.lq.MMOActivityTeamCandidateData.encode(message.npc_list[i], writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqMMOActivityDebugSetTeamCandidate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqMMOActivityDebugSetTeamCandidate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqMMOActivityDebugSetTeamCandidate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.friend_list && message.friend_list.length))
                            message.friend_list = [];
                        message.friend_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.random_friends && message.random_friends.length))
                            message.random_friends = [];
                        message.random_friends.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.npc_list && message.npc_list.length))
                            message.npc_list = [];
                        message.npc_list.push($root.lq.MMOActivityTeamCandidateData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqMMOActivityDebugSetTeamCandidate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqMMOActivityDebugSetTeamCandidate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.friend_list != null && message.hasOwnProperty("friend_list")) {
                if (!Array.isArray(message.friend_list))
                    return "friend_list: array expected";
                for (let i = 0; i < message.friend_list.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.friend_list[i], _depth + 1);
                    if (error)
                        return "friend_list." + error;
                }
            }
            if (message.random_friends != null && message.hasOwnProperty("random_friends")) {
                if (!Array.isArray(message.random_friends))
                    return "random_friends: array expected";
                for (let i = 0; i < message.random_friends.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.random_friends[i], _depth + 1);
                    if (error)
                        return "random_friends." + error;
                }
            }
            if (message.npc_list != null && message.hasOwnProperty("npc_list")) {
                if (!Array.isArray(message.npc_list))
                    return "npc_list: array expected";
                for (let i = 0; i < message.npc_list.length; ++i) {
                    let error = $root.lq.MMOActivityTeamCandidateData.verify(message.npc_list[i], _depth + 1);
                    if (error)
                        return "npc_list." + error;
                }
            }
            return null;
        };

        ReqMMOActivityDebugSetTeamCandidate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqMMOActivityDebugSetTeamCandidate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqMMOActivityDebugSetTeamCandidate();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.friend_list) {
                if (!Array.isArray(object.friend_list))
                    throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.friend_list: array expected");
                message.friend_list = Array(object.friend_list.length);
                for (let i = 0; i < object.friend_list.length; ++i) {
                    if (typeof object.friend_list[i] !== "object")
                        throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.friend_list: object expected");
                    message.friend_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.friend_list[i], _depth + 1);
                }
            }
            if (object.random_friends) {
                if (!Array.isArray(object.random_friends))
                    throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.random_friends: array expected");
                message.random_friends = Array(object.random_friends.length);
                for (let i = 0; i < object.random_friends.length; ++i) {
                    if (typeof object.random_friends[i] !== "object")
                        throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.random_friends: object expected");
                    message.random_friends[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.random_friends[i], _depth + 1);
                }
            }
            if (object.npc_list) {
                if (!Array.isArray(object.npc_list))
                    throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.npc_list: array expected");
                message.npc_list = Array(object.npc_list.length);
                for (let i = 0; i < object.npc_list.length; ++i) {
                    if (typeof object.npc_list[i] !== "object")
                        throw TypeError(".lq.ReqMMOActivityDebugSetTeamCandidate.npc_list: object expected");
                    message.npc_list[i] = $root.lq.MMOActivityTeamCandidateData.fromObject(object.npc_list[i], _depth + 1);
                }
            }
            return message;
        };

        ReqMMOActivityDebugSetTeamCandidate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.friend_list = [];
                object.random_friends = [];
                object.npc_list = [];
            }
            if (options.defaults)
                object.activity_id = 0;
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.friend_list && message.friend_list.length) {
                object.friend_list = Array(message.friend_list.length);
                for (let j = 0; j < message.friend_list.length; ++j)
                    object.friend_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.friend_list[j], options);
            }
            if (message.random_friends && message.random_friends.length) {
                object.random_friends = Array(message.random_friends.length);
                for (let j = 0; j < message.random_friends.length; ++j)
                    object.random_friends[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.random_friends[j], options);
            }
            if (message.npc_list && message.npc_list.length) {
                object.npc_list = Array(message.npc_list.length);
                for (let j = 0; j < message.npc_list.length; ++j)
                    object.npc_list[j] = $root.lq.MMOActivityTeamCandidateData.toObject(message.npc_list[j], options);
            }
            return object;
        };

        ReqMMOActivityDebugSetTeamCandidate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqMMOActivityDebugSetTeamCandidate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqMMOActivityDebugSetTeamCandidate";
        };

        return ReqMMOActivityDebugSetTeamCandidate;
    })();

    lq.ResMMOActivityDebugSetTeamCandidate = (function() {

        function ResMMOActivityDebugSetTeamCandidate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResMMOActivityDebugSetTeamCandidate.prototype.error = null;
        ResMMOActivityDebugSetTeamCandidate.prototype.changes = null;

        ResMMOActivityDebugSetTeamCandidate.create = function create(properties) {
            return new ResMMOActivityDebugSetTeamCandidate(properties);
        };

        ResMMOActivityDebugSetTeamCandidate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.changes != null && Object.hasOwnProperty.call(message, "changes"))
                $root.lq.ActivityMMODataChanges.encode(message.changes, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResMMOActivityDebugSetTeamCandidate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResMMOActivityDebugSetTeamCandidate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResMMOActivityDebugSetTeamCandidate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.changes = $root.lq.ActivityMMODataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.changes);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResMMOActivityDebugSetTeamCandidate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResMMOActivityDebugSetTeamCandidate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.changes != null && message.hasOwnProperty("changes")) {
                let error = $root.lq.ActivityMMODataChanges.verify(message.changes, _depth + 1);
                if (error)
                    return "changes." + error;
            }
            return null;
        };

        ResMMOActivityDebugSetTeamCandidate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResMMOActivityDebugSetTeamCandidate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResMMOActivityDebugSetTeamCandidate();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResMMOActivityDebugSetTeamCandidate.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.changes != null) {
                if (typeof object.changes !== "object")
                    throw TypeError(".lq.ResMMOActivityDebugSetTeamCandidate.changes: object expected");
                message.changes = $root.lq.ActivityMMODataChanges.fromObject(object.changes, _depth + 1);
            }
            return message;
        };

        ResMMOActivityDebugSetTeamCandidate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.changes = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.changes != null && message.hasOwnProperty("changes"))
                object.changes = $root.lq.ActivityMMODataChanges.toObject(message.changes, options);
            return object;
        };

        ResMMOActivityDebugSetTeamCandidate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResMMOActivityDebugSetTeamCandidate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResMMOActivityDebugSetTeamCandidate";
        };

        return ResMMOActivityDebugSetTeamCandidate;
    })();

    lq.AmuletBadgeData = (function() {

        function AmuletBadgeData(properties) {
            this.store = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletBadgeData.prototype.id = 0;
        AmuletBadgeData.prototype.uid = 0;
        AmuletBadgeData.prototype.store = $util.emptyArray;
        AmuletBadgeData.prototype.random = 0;

        AmuletBadgeData.create = function create(properties) {
            return new AmuletBadgeData(properties);
        };

        AmuletBadgeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(16).uint32(message.uid);
            if (message.store != null && message.store.length)
                for (let i = 0; i < message.store.length; ++i)
                    writer.uint32(26).string(message.store[i]);
            if (message.random != null && Object.hasOwnProperty.call(message, "random"))
                writer.uint32(32).uint32(message.random);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletBadgeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletBadgeData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletBadgeData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.uid = value;
                        else
                            delete message.uid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.store && message.store.length))
                            message.store = [];
                        message.store.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.random = value;
                        else
                            delete message.random;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletBadgeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletBadgeData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isInteger(message.uid))
                    return "uid: integer expected";
            if (message.store != null && message.hasOwnProperty("store")) {
                if (!Array.isArray(message.store))
                    return "store: array expected";
                for (let i = 0; i < message.store.length; ++i)
                    if (!$util.isString(message.store[i]))
                        return "store: string[] expected";
            }
            if (message.random != null && message.hasOwnProperty("random"))
                if (!$util.isInteger(message.random))
                    return "random: integer expected";
            return null;
        };

        AmuletBadgeData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletBadgeData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletBadgeData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.uid != null)
                if (Number(object.uid) !== 0)
                    message.uid = object.uid >>> 0;
            if (object.store) {
                if (!Array.isArray(object.store))
                    throw TypeError(".lq.AmuletBadgeData.store: array expected");
                message.store = Array(object.store.length);
                for (let i = 0; i < object.store.length; ++i)
                    message.store[i] = String(object.store[i]);
            }
            if (object.random != null)
                if (Number(object.random) !== 0)
                    message.random = object.random >>> 0;
            return message;
        };

        AmuletBadgeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.store = [];
            if (options.defaults) {
                object.id = 0;
                object.uid = 0;
                object.random = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.store && message.store.length) {
                object.store = Array(message.store.length);
                for (let j = 0; j < message.store.length; ++j)
                    object.store[j] = message.store[j];
            }
            if (message.random != null && message.hasOwnProperty("random"))
                object.random = message.random;
            return object;
        };

        AmuletBadgeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletBadgeData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletBadgeData";
        };

        return AmuletBadgeData;
    })();

    lq.AmuletEffectData = (function() {

        function AmuletEffectData(properties) {
            this.store = [];
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectData.prototype.id = 0;
        AmuletEffectData.prototype.uid = 0;
        AmuletEffectData.prototype.store = $util.emptyArray;
        AmuletEffectData.prototype.badge = null;
        AmuletEffectData.prototype.volume = 0;
        AmuletEffectData.prototype.tags = $util.emptyArray;

        AmuletEffectData.create = function create(properties) {
            return new AmuletEffectData(properties);
        };

        AmuletEffectData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(16).uint32(message.uid);
            if (message.store != null && message.store.length)
                for (let i = 0; i < message.store.length; ++i)
                    writer.uint32(26).string(message.store[i]);
            if (message.badge != null && Object.hasOwnProperty.call(message, "badge"))
                $root.lq.AmuletBadgeData.encode(message.badge, writer.uint32(34).fork()).ldelim();
            if (message.volume != null && Object.hasOwnProperty.call(message, "volume"))
                writer.uint32(40).uint32(message.volume);
            if (message.tags != null && message.tags.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(message.tags[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.uid = value;
                        else
                            delete message.uid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.store && message.store.length))
                            message.store = [];
                        message.store.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.badge = $root.lq.AmuletBadgeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.badge);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.volume = value;
                        else
                            delete message.volume;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tags.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isInteger(message.uid))
                    return "uid: integer expected";
            if (message.store != null && message.hasOwnProperty("store")) {
                if (!Array.isArray(message.store))
                    return "store: array expected";
                for (let i = 0; i < message.store.length; ++i)
                    if (!$util.isString(message.store[i]))
                        return "store: string[] expected";
            }
            if (message.badge != null && message.hasOwnProperty("badge")) {
                let error = $root.lq.AmuletBadgeData.verify(message.badge, _depth + 1);
                if (error)
                    return "badge." + error;
            }
            if (message.volume != null && message.hasOwnProperty("volume"))
                if (!$util.isInteger(message.volume))
                    return "volume: integer expected";
            if (message.tags != null && message.hasOwnProperty("tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isInteger(message.tags[i]))
                        return "tags: integer[] expected";
            }
            return null;
        };

        AmuletEffectData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.uid != null)
                if (Number(object.uid) !== 0)
                    message.uid = object.uid >>> 0;
            if (object.store) {
                if (!Array.isArray(object.store))
                    throw TypeError(".lq.AmuletEffectData.store: array expected");
                message.store = Array(object.store.length);
                for (let i = 0; i < object.store.length; ++i)
                    message.store[i] = String(object.store[i]);
            }
            if (object.badge != null) {
                if (typeof object.badge !== "object")
                    throw TypeError(".lq.AmuletEffectData.badge: object expected");
                message.badge = $root.lq.AmuletBadgeData.fromObject(object.badge, _depth + 1);
            }
            if (object.volume != null)
                if (Number(object.volume) !== 0)
                    message.volume = object.volume >>> 0;
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".lq.AmuletEffectData.tags: array expected");
                message.tags = Array(object.tags.length);
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = object.tags[i] >>> 0;
            }
            return message;
        };

        AmuletEffectData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.store = [];
                object.tags = [];
            }
            if (options.defaults) {
                object.id = 0;
                object.uid = 0;
                object.badge = null;
                object.volume = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.store && message.store.length) {
                object.store = Array(message.store.length);
                for (let j = 0; j < message.store.length; ++j)
                    object.store[j] = message.store[j];
            }
            if (message.badge != null && message.hasOwnProperty("badge"))
                object.badge = $root.lq.AmuletBadgeData.toObject(message.badge, options);
            if (message.volume != null && message.hasOwnProperty("volume"))
                object.volume = message.volume;
            if (message.tags && message.tags.length) {
                object.tags = Array(message.tags.length);
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            return object;
        };

        AmuletEffectData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectData";
        };

        return AmuletEffectData;
    })();

    lq.AmuletTile = (function() {

        function AmuletTile(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletTile.prototype.id = 0;
        AmuletTile.prototype.tile = "";

        AmuletTile.create = function create(properties) {
            return new AmuletTile(properties);
        };

        AmuletTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            return null;
        };

        AmuletTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletTile();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            return message;
        };

        AmuletTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.tile = "";
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            return object;
        };

        AmuletTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletTile";
        };

        return AmuletTile;
    })();

    lq.AmuletBuffData = (function() {

        function AmuletBuffData(properties) {
            this.store = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletBuffData.prototype.id = 0;
        AmuletBuffData.prototype.store = $util.emptyArray;

        AmuletBuffData.create = function create(properties) {
            return new AmuletBuffData(properties);
        };

        AmuletBuffData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.store != null && message.store.length)
                for (let i = 0; i < message.store.length; ++i)
                    writer.uint32(26).string(message.store[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletBuffData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletBuffData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletBuffData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.store && message.store.length))
                            message.store = [];
                        message.store.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletBuffData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletBuffData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.store != null && message.hasOwnProperty("store")) {
                if (!Array.isArray(message.store))
                    return "store: array expected";
                for (let i = 0; i < message.store.length; ++i)
                    if (!$util.isString(message.store[i]))
                        return "store: string[] expected";
            }
            return null;
        };

        AmuletBuffData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletBuffData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletBuffData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.store) {
                if (!Array.isArray(object.store))
                    throw TypeError(".lq.AmuletBuffData.store: array expected");
                message.store = Array(object.store.length);
                for (let i = 0; i < object.store.length; ++i)
                    message.store[i] = String(object.store[i]);
            }
            return message;
        };

        AmuletBuffData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.store = [];
            if (options.defaults)
                object.id = 0;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.store && message.store.length) {
                object.store = Array(message.store.length);
                for (let j = 0; j < message.store.length; ++j)
                    object.store[j] = message.store[j];
            }
            return object;
        };

        AmuletBuffData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletBuffData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletBuffData";
        };

        return AmuletBuffData;
    })();

    lq.AmuletGameShopGoods = (function() {

        function AmuletGameShopGoods(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameShopGoods.prototype.id = 0;
        AmuletGameShopGoods.prototype.sold = false;
        AmuletGameShopGoods.prototype.goods_id = 0;
        AmuletGameShopGoods.prototype.price = 0;

        AmuletGameShopGoods.create = function create(properties) {
            return new AmuletGameShopGoods(properties);
        };

        AmuletGameShopGoods.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.sold != null && Object.hasOwnProperty.call(message, "sold"))
                writer.uint32(16).bool(message.sold);
            if (message.goods_id != null && Object.hasOwnProperty.call(message, "goods_id"))
                writer.uint32(24).uint32(message.goods_id);
            if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                writer.uint32(32).uint32(message.price);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameShopGoods.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameShopGoods.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameShopGoods(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.sold = value;
                        else
                            delete message.sold;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.goods_id = value;
                        else
                            delete message.goods_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.price = value;
                        else
                            delete message.price;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameShopGoods.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameShopGoods.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.sold != null && message.hasOwnProperty("sold"))
                if (typeof message.sold !== "boolean")
                    return "sold: boolean expected";
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                if (!$util.isInteger(message.goods_id))
                    return "goods_id: integer expected";
            if (message.price != null && message.hasOwnProperty("price"))
                if (!$util.isInteger(message.price))
                    return "price: integer expected";
            return null;
        };

        AmuletGameShopGoods.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameShopGoods)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameShopGoods();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.sold != null)
                if (object.sold)
                    message.sold = Boolean(object.sold);
            if (object.goods_id != null)
                if (Number(object.goods_id) !== 0)
                    message.goods_id = object.goods_id >>> 0;
            if (object.price != null)
                if (Number(object.price) !== 0)
                    message.price = object.price >>> 0;
            return message;
        };

        AmuletGameShopGoods.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.sold = false;
                object.goods_id = 0;
                object.price = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.sold != null && message.hasOwnProperty("sold"))
                object.sold = message.sold;
            if (message.goods_id != null && message.hasOwnProperty("goods_id"))
                object.goods_id = message.goods_id;
            if (message.price != null && message.hasOwnProperty("price"))
                object.price = message.price;
            return object;
        };

        AmuletGameShopGoods.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameShopGoods.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameShopGoods";
        };

        return AmuletGameShopGoods;
    })();

    lq.AmuletActivityTingInfo = (function() {

        function AmuletActivityTingInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletActivityTingInfo.prototype.tile = "";
        AmuletActivityTingInfo.prototype.fan = "";
        AmuletActivityTingInfo.prototype.ting_tile = "";

        AmuletActivityTingInfo.create = function create(properties) {
            return new AmuletActivityTingInfo(properties);
        };

        AmuletActivityTingInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(10).string(message.tile);
            if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                writer.uint32(18).string(message.fan);
            if (message.ting_tile != null && Object.hasOwnProperty.call(message, "ting_tile"))
                writer.uint32(26).string(message.ting_tile);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletActivityTingInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletActivityTingInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletActivityTingInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.fan = value;
                        else
                            delete message.fan;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.ting_tile = value;
                        else
                            delete message.ting_tile;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletActivityTingInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletActivityTingInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.fan != null && message.hasOwnProperty("fan"))
                if (!$util.isString(message.fan))
                    return "fan: string expected";
            if (message.ting_tile != null && message.hasOwnProperty("ting_tile"))
                if (!$util.isString(message.ting_tile))
                    return "ting_tile: string expected";
            return null;
        };

        AmuletActivityTingInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletActivityTingInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletActivityTingInfo();
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.fan != null)
                if (typeof object.fan !== "string" || object.fan.length)
                    message.fan = String(object.fan);
            if (object.ting_tile != null)
                if (typeof object.ting_tile !== "string" || object.ting_tile.length)
                    message.ting_tile = String(object.ting_tile);
            return message;
        };

        AmuletActivityTingInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tile = "";
                object.fan = "";
                object.ting_tile = "";
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.fan != null && message.hasOwnProperty("fan"))
                object.fan = message.fan;
            if (message.ting_tile != null && message.hasOwnProperty("ting_tile"))
                object.ting_tile = message.ting_tile;
            return object;
        };

        AmuletActivityTingInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletActivityTingInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletActivityTingInfo";
        };

        return AmuletActivityTingInfo;
    })();

    lq.AmuletShowDesktopTileData = (function() {

        function AmuletShowDesktopTileData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletShowDesktopTileData.prototype.id = 0;
        AmuletShowDesktopTileData.prototype.pos = 0;

        AmuletShowDesktopTileData.create = function create(properties) {
            return new AmuletShowDesktopTileData(properties);
        };

        AmuletShowDesktopTileData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.pos != null && Object.hasOwnProperty.call(message, "pos"))
                writer.uint32(16).uint32(message.pos);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletShowDesktopTileData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletShowDesktopTileData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletShowDesktopTileData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pos = value;
                        else
                            delete message.pos;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletShowDesktopTileData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletShowDesktopTileData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.pos != null && message.hasOwnProperty("pos"))
                if (!$util.isInteger(message.pos))
                    return "pos: integer expected";
            return null;
        };

        AmuletShowDesktopTileData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletShowDesktopTileData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletShowDesktopTileData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.pos != null)
                if (Number(object.pos) !== 0)
                    message.pos = object.pos >>> 0;
            return message;
        };

        AmuletShowDesktopTileData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.pos = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.pos != null && message.hasOwnProperty("pos"))
                object.pos = message.pos;
            return object;
        };

        AmuletShowDesktopTileData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletShowDesktopTileData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletShowDesktopTileData";
        };

        return AmuletShowDesktopTileData;
    })();

    lq.AmuletMingInfo = (function() {

        function AmuletMingInfo(properties) {
            this.tile_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletMingInfo.prototype.type = 0;
        AmuletMingInfo.prototype.tile_list = $util.emptyArray;

        AmuletMingInfo.create = function create(properties) {
            return new AmuletMingInfo(properties);
        };

        AmuletMingInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.tile_list != null && message.tile_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.tile_list.length; ++i)
                    writer.uint32(message.tile_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletMingInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletMingInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletMingInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.tile_list && message.tile_list.length))
                                message.tile_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_list && message.tile_list.length))
                            message.tile_list = [];
                        message.tile_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletMingInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletMingInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tile_list != null && message.hasOwnProperty("tile_list")) {
                if (!Array.isArray(message.tile_list))
                    return "tile_list: array expected";
                for (let i = 0; i < message.tile_list.length; ++i)
                    if (!$util.isInteger(message.tile_list[i]))
                        return "tile_list: integer[] expected";
            }
            return null;
        };

        AmuletMingInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletMingInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletMingInfo();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tile_list) {
                if (!Array.isArray(object.tile_list))
                    throw TypeError(".lq.AmuletMingInfo.tile_list: array expected");
                message.tile_list = Array(object.tile_list.length);
                for (let i = 0; i < object.tile_list.length; ++i)
                    message.tile_list[i] = object.tile_list[i] >>> 0;
            }
            return message;
        };

        AmuletMingInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.tile_list = [];
            if (options.defaults)
                object.type = 0;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tile_list && message.tile_list.length) {
                object.tile_list = Array(message.tile_list.length);
                for (let j = 0; j < message.tile_list.length; ++j)
                    object.tile_list[j] = message.tile_list[j];
            }
            return object;
        };

        AmuletMingInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletMingInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletMingInfo";
        };

        return AmuletMingInfo;
    })();

    lq.AmuletGameOperation = (function() {

        function AmuletGameOperation(properties) {
            this.gang = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameOperation.prototype.type = 0;
        AmuletGameOperation.prototype.gang = $util.emptyArray;
        AmuletGameOperation.prototype.value = 0;

        AmuletGameOperation.create = function create(properties) {
            return new AmuletGameOperation(properties);
        };

        AmuletGameOperation.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.gang != null && message.gang.length)
                for (let i = 0; i < message.gang.length; ++i)
                    $root.lq.AmuletGameOperation.GangTiles.encode(message.gang[i], writer.uint32(18).fork()).ldelim();
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(24).int32(message.value);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameOperation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameOperation.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameOperation(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.gang && message.gang.length))
                            message.gang = [];
                        message.gang.push($root.lq.AmuletGameOperation.GangTiles.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameOperation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameOperation.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.gang != null && message.hasOwnProperty("gang")) {
                if (!Array.isArray(message.gang))
                    return "gang: array expected";
                for (let i = 0; i < message.gang.length; ++i) {
                    let error = $root.lq.AmuletGameOperation.GangTiles.verify(message.gang[i], _depth + 1);
                    if (error)
                        return "gang." + error;
                }
            }
            if (message.value != null && message.hasOwnProperty("value"))
                if (!$util.isInteger(message.value))
                    return "value: integer expected";
            return null;
        };

        AmuletGameOperation.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameOperation)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameOperation();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.gang) {
                if (!Array.isArray(object.gang))
                    throw TypeError(".lq.AmuletGameOperation.gang: array expected");
                message.gang = Array(object.gang.length);
                for (let i = 0; i < object.gang.length; ++i) {
                    if (typeof object.gang[i] !== "object")
                        throw TypeError(".lq.AmuletGameOperation.gang: object expected");
                    message.gang[i] = $root.lq.AmuletGameOperation.GangTiles.fromObject(object.gang[i], _depth + 1);
                }
            }
            if (object.value != null)
                if (Number(object.value) !== 0)
                    message.value = object.value | 0;
            return message;
        };

        AmuletGameOperation.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.gang = [];
            if (options.defaults) {
                object.type = 0;
                object.value = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.gang && message.gang.length) {
                object.gang = Array(message.gang.length);
                for (let j = 0; j < message.gang.length; ++j)
                    object.gang[j] = $root.lq.AmuletGameOperation.GangTiles.toObject(message.gang[j], options);
            }
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = message.value;
            return object;
        };

        AmuletGameOperation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameOperation.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameOperation";
        };

        AmuletGameOperation.GangTiles = (function() {

            function GangTiles(properties) {
                this.tiles = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GangTiles.prototype.tiles = $util.emptyArray;

            GangTiles.create = function create(properties) {
                return new GangTiles(properties);
            };

            GangTiles.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.tiles != null && message.tiles.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.tiles.length; ++i)
                        writer.uint32(message.tiles[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GangTiles.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GangTiles.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameOperation.GangTiles();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.tiles && message.tiles.length))
                                    message.tiles = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.tiles.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.tiles && message.tiles.length))
                                message.tiles = [];
                            message.tiles.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GangTiles.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GangTiles.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tiles != null && message.hasOwnProperty("tiles")) {
                    if (!Array.isArray(message.tiles))
                        return "tiles: array expected";
                    for (let i = 0; i < message.tiles.length; ++i)
                        if (!$util.isInteger(message.tiles[i]))
                            return "tiles: integer[] expected";
                }
                return null;
            };

            GangTiles.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletGameOperation.GangTiles)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletGameOperation.GangTiles();
                if (object.tiles) {
                    if (!Array.isArray(object.tiles))
                        throw TypeError(".lq.AmuletGameOperation.GangTiles.tiles: array expected");
                    message.tiles = Array(object.tiles.length);
                    for (let i = 0; i < object.tiles.length; ++i)
                        message.tiles[i] = object.tiles[i] >>> 0;
                }
                return message;
            };

            GangTiles.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tiles = [];
                if (message.tiles && message.tiles.length) {
                    object.tiles = Array(message.tiles.length);
                    for (let j = 0; j < message.tiles.length; ++j)
                        object.tiles[j] = message.tiles[j];
                }
                return object;
            };

            GangTiles.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GangTiles.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletGameOperation.GangTiles";
            };

            return GangTiles;
        })();

        return AmuletGameOperation;
    })();

    lq.AmuletTileScore = (function() {

        function AmuletTileScore(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletTileScore.prototype.tile = "";
        AmuletTileScore.prototype.score = "";

        AmuletTileScore.create = function create(properties) {
            return new AmuletTileScore(properties);
        };

        AmuletTileScore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(10).string(message.tile);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(18).string(message.score);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletTileScore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletTileScore.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletTileScore(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletTileScore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletTileScore.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isString(message.score))
                    return "score: string expected";
            return null;
        };

        AmuletTileScore.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletTileScore)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletTileScore();
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.score != null)
                if (typeof object.score !== "string" || object.score.length)
                    message.score = String(object.score);
            return message;
        };

        AmuletTileScore.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tile = "";
                object.score = "";
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            return object;
        };

        AmuletTileScore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletTileScore.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletTileScore";
        };

        return AmuletTileScore;
    })();

    lq.ActivityAmuletHuRecord = (function() {

        function ActivityAmuletHuRecord(properties) {
            this.effect_builds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletHuRecord.prototype.point = "";
        ActivityAmuletHuRecord.prototype.pai = "";
        ActivityAmuletHuRecord.prototype.fan = "";
        ActivityAmuletHuRecord.prototype.base = "";
        ActivityAmuletHuRecord.prototype.effect_builds = $util.emptyArray;

        ActivityAmuletHuRecord.create = function create(properties) {
            return new ActivityAmuletHuRecord(properties);
        };

        ActivityAmuletHuRecord.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(10).string(message.point);
            if (message.pai != null && Object.hasOwnProperty.call(message, "pai"))
                writer.uint32(18).string(message.pai);
            if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                writer.uint32(26).string(message.fan);
            if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                writer.uint32(34).string(message.base);
            if (message.effect_builds != null && message.effect_builds.length)
                for (let i = 0; i < message.effect_builds.length; ++i)
                    $root.lq.ActivityAmuletEffectRecordData.encode(message.effect_builds[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletHuRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletHuRecord.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletHuRecord(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.pai = value;
                        else
                            delete message.pai;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.fan = value;
                        else
                            delete message.fan;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.base = value;
                        else
                            delete message.base;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_builds && message.effect_builds.length))
                            message.effect_builds = [];
                        message.effect_builds.push($root.lq.ActivityAmuletEffectRecordData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletHuRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletHuRecord.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isString(message.point))
                    return "point: string expected";
            if (message.pai != null && message.hasOwnProperty("pai"))
                if (!$util.isString(message.pai))
                    return "pai: string expected";
            if (message.fan != null && message.hasOwnProperty("fan"))
                if (!$util.isString(message.fan))
                    return "fan: string expected";
            if (message.base != null && message.hasOwnProperty("base"))
                if (!$util.isString(message.base))
                    return "base: string expected";
            if (message.effect_builds != null && message.hasOwnProperty("effect_builds")) {
                if (!Array.isArray(message.effect_builds))
                    return "effect_builds: array expected";
                for (let i = 0; i < message.effect_builds.length; ++i) {
                    let error = $root.lq.ActivityAmuletEffectRecordData.verify(message.effect_builds[i], _depth + 1);
                    if (error)
                        return "effect_builds." + error;
                }
            }
            return null;
        };

        ActivityAmuletHuRecord.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletHuRecord)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletHuRecord();
            if (object.point != null)
                if (typeof object.point !== "string" || object.point.length)
                    message.point = String(object.point);
            if (object.pai != null)
                if (typeof object.pai !== "string" || object.pai.length)
                    message.pai = String(object.pai);
            if (object.fan != null)
                if (typeof object.fan !== "string" || object.fan.length)
                    message.fan = String(object.fan);
            if (object.base != null)
                if (typeof object.base !== "string" || object.base.length)
                    message.base = String(object.base);
            if (object.effect_builds) {
                if (!Array.isArray(object.effect_builds))
                    throw TypeError(".lq.ActivityAmuletHuRecord.effect_builds: array expected");
                message.effect_builds = Array(object.effect_builds.length);
                for (let i = 0; i < object.effect_builds.length; ++i) {
                    if (typeof object.effect_builds[i] !== "object")
                        throw TypeError(".lq.ActivityAmuletHuRecord.effect_builds: object expected");
                    message.effect_builds[i] = $root.lq.ActivityAmuletEffectRecordData.fromObject(object.effect_builds[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityAmuletHuRecord.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.effect_builds = [];
            if (options.defaults) {
                object.point = "";
                object.pai = "";
                object.fan = "";
                object.base = "";
            }
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.pai != null && message.hasOwnProperty("pai"))
                object.pai = message.pai;
            if (message.fan != null && message.hasOwnProperty("fan"))
                object.fan = message.fan;
            if (message.base != null && message.hasOwnProperty("base"))
                object.base = message.base;
            if (message.effect_builds && message.effect_builds.length) {
                object.effect_builds = Array(message.effect_builds.length);
                for (let j = 0; j < message.effect_builds.length; ++j)
                    object.effect_builds[j] = $root.lq.ActivityAmuletEffectRecordData.toObject(message.effect_builds[j], options);
            }
            return object;
        };

        ActivityAmuletHuRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletHuRecord.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletHuRecord";
        };

        return ActivityAmuletHuRecord;
    })();

    lq.AmuletEffectCounterData = (function() {

        function AmuletEffectCounterData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectCounterData.prototype.effect_id = 0;
        AmuletEffectCounterData.prototype.pack_candidate_count = 0;
        AmuletEffectCounterData.prototype.gain_count = 0;

        AmuletEffectCounterData.create = function create(properties) {
            return new AmuletEffectCounterData(properties);
        };

        AmuletEffectCounterData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.effect_id != null && Object.hasOwnProperty.call(message, "effect_id"))
                writer.uint32(8).uint32(message.effect_id);
            if (message.pack_candidate_count != null && Object.hasOwnProperty.call(message, "pack_candidate_count"))
                writer.uint32(16).uint32(message.pack_candidate_count);
            if (message.gain_count != null && Object.hasOwnProperty.call(message, "gain_count"))
                writer.uint32(24).uint32(message.gain_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectCounterData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectCounterData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectCounterData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effect_id = value;
                        else
                            delete message.effect_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pack_candidate_count = value;
                        else
                            delete message.pack_candidate_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gain_count = value;
                        else
                            delete message.gain_count;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectCounterData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectCounterData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                if (!$util.isInteger(message.effect_id))
                    return "effect_id: integer expected";
            if (message.pack_candidate_count != null && message.hasOwnProperty("pack_candidate_count"))
                if (!$util.isInteger(message.pack_candidate_count))
                    return "pack_candidate_count: integer expected";
            if (message.gain_count != null && message.hasOwnProperty("gain_count"))
                if (!$util.isInteger(message.gain_count))
                    return "gain_count: integer expected";
            return null;
        };

        AmuletEffectCounterData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectCounterData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectCounterData();
            if (object.effect_id != null)
                if (Number(object.effect_id) !== 0)
                    message.effect_id = object.effect_id >>> 0;
            if (object.pack_candidate_count != null)
                if (Number(object.pack_candidate_count) !== 0)
                    message.pack_candidate_count = object.pack_candidate_count >>> 0;
            if (object.gain_count != null)
                if (Number(object.gain_count) !== 0)
                    message.gain_count = object.gain_count >>> 0;
            return message;
        };

        AmuletEffectCounterData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.effect_id = 0;
                object.pack_candidate_count = 0;
                object.gain_count = 0;
            }
            if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                object.effect_id = message.effect_id;
            if (message.pack_candidate_count != null && message.hasOwnProperty("pack_candidate_count"))
                object.pack_candidate_count = message.pack_candidate_count;
            if (message.gain_count != null && message.hasOwnProperty("gain_count"))
                object.gain_count = message.gain_count;
            return object;
        };

        AmuletEffectCounterData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectCounterData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectCounterData";
        };

        return AmuletEffectCounterData;
    })();

    lq.AmuletGameRecordData = (function() {

        function AmuletGameRecordData(properties) {
            this.effect_counter = [];
            this.hu_tiles_id = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameRecordData.prototype.yiman_count = 0;
        AmuletGameRecordData.prototype.level_hu_count = 0;
        AmuletGameRecordData.prototype.game_hu_count = 0;
        AmuletGameRecordData.prototype.effect_gain = 0;
        AmuletGameRecordData.prototype.coin_consume = "";
        AmuletGameRecordData.prototype.coin_gain = "";
        AmuletGameRecordData.prototype.highest_hu = null;
        AmuletGameRecordData.prototype.highest_level_score = "";
        AmuletGameRecordData.prototype.highest_fan = "";
        AmuletGameRecordData.prototype.pack_count = 0;
        AmuletGameRecordData.prototype.round_count = 0;
        AmuletGameRecordData.prototype.effect_counter = $util.emptyArray;
        AmuletGameRecordData.prototype.hu_tiles_id = $util.emptyArray;

        AmuletGameRecordData.create = function create(properties) {
            return new AmuletGameRecordData(properties);
        };

        AmuletGameRecordData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.yiman_count != null && Object.hasOwnProperty.call(message, "yiman_count"))
                writer.uint32(8).uint32(message.yiman_count);
            if (message.level_hu_count != null && Object.hasOwnProperty.call(message, "level_hu_count"))
                writer.uint32(16).uint32(message.level_hu_count);
            if (message.game_hu_count != null && Object.hasOwnProperty.call(message, "game_hu_count"))
                writer.uint32(24).uint32(message.game_hu_count);
            if (message.effect_gain != null && Object.hasOwnProperty.call(message, "effect_gain"))
                writer.uint32(32).uint32(message.effect_gain);
            if (message.coin_consume != null && Object.hasOwnProperty.call(message, "coin_consume"))
                writer.uint32(42).string(message.coin_consume);
            if (message.coin_gain != null && Object.hasOwnProperty.call(message, "coin_gain"))
                writer.uint32(50).string(message.coin_gain);
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                $root.lq.ActivityAmuletHuRecord.encode(message.highest_hu, writer.uint32(58).fork()).ldelim();
            if (message.highest_level_score != null && Object.hasOwnProperty.call(message, "highest_level_score"))
                writer.uint32(66).string(message.highest_level_score);
            if (message.highest_fan != null && Object.hasOwnProperty.call(message, "highest_fan"))
                writer.uint32(74).string(message.highest_fan);
            if (message.pack_count != null && Object.hasOwnProperty.call(message, "pack_count"))
                writer.uint32(80).uint32(message.pack_count);
            if (message.round_count != null && Object.hasOwnProperty.call(message, "round_count"))
                writer.uint32(88).uint32(message.round_count);
            if (message.effect_counter != null && message.effect_counter.length)
                for (let i = 0; i < message.effect_counter.length; ++i)
                    $root.lq.AmuletEffectCounterData.encode(message.effect_counter[i], writer.uint32(98).fork()).ldelim();
            if (message.hu_tiles_id != null && message.hu_tiles_id.length) {
                writer.uint32(106).fork();
                for (let i = 0; i < message.hu_tiles_id.length; ++i)
                    writer.uint32(message.hu_tiles_id[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameRecordData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameRecordData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameRecordData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.yiman_count = value;
                        else
                            delete message.yiman_count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level_hu_count = value;
                        else
                            delete message.level_hu_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_hu_count = value;
                        else
                            delete message.game_hu_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.effect_gain = value;
                        else
                            delete message.effect_gain;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.coin_consume = value;
                        else
                            delete message.coin_consume;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.coin_gain = value;
                        else
                            delete message.coin_gain;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.highest_hu = $root.lq.ActivityAmuletHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_hu);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_level_score = value;
                        else
                            delete message.highest_level_score;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_fan = value;
                        else
                            delete message.highest_fan;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pack_count = value;
                        else
                            delete message.pack_count;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round_count = value;
                        else
                            delete message.round_count;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_counter && message.effect_counter.length))
                            message.effect_counter = [];
                        message.effect_counter.push($root.lq.AmuletEffectCounterData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType === 2) {
                            if (!(message.hu_tiles_id && message.hu_tiles_id.length))
                                message.hu_tiles_id = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hu_tiles_id.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hu_tiles_id && message.hu_tiles_id.length))
                            message.hu_tiles_id = [];
                        message.hu_tiles_id.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameRecordData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameRecordData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.yiman_count != null && message.hasOwnProperty("yiman_count"))
                if (!$util.isInteger(message.yiman_count))
                    return "yiman_count: integer expected";
            if (message.level_hu_count != null && message.hasOwnProperty("level_hu_count"))
                if (!$util.isInteger(message.level_hu_count))
                    return "level_hu_count: integer expected";
            if (message.game_hu_count != null && message.hasOwnProperty("game_hu_count"))
                if (!$util.isInteger(message.game_hu_count))
                    return "game_hu_count: integer expected";
            if (message.effect_gain != null && message.hasOwnProperty("effect_gain"))
                if (!$util.isInteger(message.effect_gain))
                    return "effect_gain: integer expected";
            if (message.coin_consume != null && message.hasOwnProperty("coin_consume"))
                if (!$util.isString(message.coin_consume))
                    return "coin_consume: string expected";
            if (message.coin_gain != null && message.hasOwnProperty("coin_gain"))
                if (!$util.isString(message.coin_gain))
                    return "coin_gain: string expected";
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu")) {
                let error = $root.lq.ActivityAmuletHuRecord.verify(message.highest_hu, _depth + 1);
                if (error)
                    return "highest_hu." + error;
            }
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                if (!$util.isString(message.highest_level_score))
                    return "highest_level_score: string expected";
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                if (!$util.isString(message.highest_fan))
                    return "highest_fan: string expected";
            if (message.pack_count != null && message.hasOwnProperty("pack_count"))
                if (!$util.isInteger(message.pack_count))
                    return "pack_count: integer expected";
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                if (!$util.isInteger(message.round_count))
                    return "round_count: integer expected";
            if (message.effect_counter != null && message.hasOwnProperty("effect_counter")) {
                if (!Array.isArray(message.effect_counter))
                    return "effect_counter: array expected";
                for (let i = 0; i < message.effect_counter.length; ++i) {
                    let error = $root.lq.AmuletEffectCounterData.verify(message.effect_counter[i], _depth + 1);
                    if (error)
                        return "effect_counter." + error;
                }
            }
            if (message.hu_tiles_id != null && message.hasOwnProperty("hu_tiles_id")) {
                if (!Array.isArray(message.hu_tiles_id))
                    return "hu_tiles_id: array expected";
                for (let i = 0; i < message.hu_tiles_id.length; ++i)
                    if (!$util.isInteger(message.hu_tiles_id[i]))
                        return "hu_tiles_id: integer[] expected";
            }
            return null;
        };

        AmuletGameRecordData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameRecordData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameRecordData();
            if (object.yiman_count != null)
                if (Number(object.yiman_count) !== 0)
                    message.yiman_count = object.yiman_count >>> 0;
            if (object.level_hu_count != null)
                if (Number(object.level_hu_count) !== 0)
                    message.level_hu_count = object.level_hu_count >>> 0;
            if (object.game_hu_count != null)
                if (Number(object.game_hu_count) !== 0)
                    message.game_hu_count = object.game_hu_count >>> 0;
            if (object.effect_gain != null)
                if (Number(object.effect_gain) !== 0)
                    message.effect_gain = object.effect_gain >>> 0;
            if (object.coin_consume != null)
                if (typeof object.coin_consume !== "string" || object.coin_consume.length)
                    message.coin_consume = String(object.coin_consume);
            if (object.coin_gain != null)
                if (typeof object.coin_gain !== "string" || object.coin_gain.length)
                    message.coin_gain = String(object.coin_gain);
            if (object.highest_hu != null) {
                if (typeof object.highest_hu !== "object")
                    throw TypeError(".lq.AmuletGameRecordData.highest_hu: object expected");
                message.highest_hu = $root.lq.ActivityAmuletHuRecord.fromObject(object.highest_hu, _depth + 1);
            }
            if (object.highest_level_score != null)
                if (typeof object.highest_level_score !== "string" || object.highest_level_score.length)
                    message.highest_level_score = String(object.highest_level_score);
            if (object.highest_fan != null)
                if (typeof object.highest_fan !== "string" || object.highest_fan.length)
                    message.highest_fan = String(object.highest_fan);
            if (object.pack_count != null)
                if (Number(object.pack_count) !== 0)
                    message.pack_count = object.pack_count >>> 0;
            if (object.round_count != null)
                if (Number(object.round_count) !== 0)
                    message.round_count = object.round_count >>> 0;
            if (object.effect_counter) {
                if (!Array.isArray(object.effect_counter))
                    throw TypeError(".lq.AmuletGameRecordData.effect_counter: array expected");
                message.effect_counter = Array(object.effect_counter.length);
                for (let i = 0; i < object.effect_counter.length; ++i) {
                    if (typeof object.effect_counter[i] !== "object")
                        throw TypeError(".lq.AmuletGameRecordData.effect_counter: object expected");
                    message.effect_counter[i] = $root.lq.AmuletEffectCounterData.fromObject(object.effect_counter[i], _depth + 1);
                }
            }
            if (object.hu_tiles_id) {
                if (!Array.isArray(object.hu_tiles_id))
                    throw TypeError(".lq.AmuletGameRecordData.hu_tiles_id: array expected");
                message.hu_tiles_id = Array(object.hu_tiles_id.length);
                for (let i = 0; i < object.hu_tiles_id.length; ++i)
                    message.hu_tiles_id[i] = object.hu_tiles_id[i] >>> 0;
            }
            return message;
        };

        AmuletGameRecordData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effect_counter = [];
                object.hu_tiles_id = [];
            }
            if (options.defaults) {
                object.yiman_count = 0;
                object.level_hu_count = 0;
                object.game_hu_count = 0;
                object.effect_gain = 0;
                object.coin_consume = "";
                object.coin_gain = "";
                object.highest_hu = null;
                object.highest_level_score = "";
                object.highest_fan = "";
                object.pack_count = 0;
                object.round_count = 0;
            }
            if (message.yiman_count != null && message.hasOwnProperty("yiman_count"))
                object.yiman_count = message.yiman_count;
            if (message.level_hu_count != null && message.hasOwnProperty("level_hu_count"))
                object.level_hu_count = message.level_hu_count;
            if (message.game_hu_count != null && message.hasOwnProperty("game_hu_count"))
                object.game_hu_count = message.game_hu_count;
            if (message.effect_gain != null && message.hasOwnProperty("effect_gain"))
                object.effect_gain = message.effect_gain;
            if (message.coin_consume != null && message.hasOwnProperty("coin_consume"))
                object.coin_consume = message.coin_consume;
            if (message.coin_gain != null && message.hasOwnProperty("coin_gain"))
                object.coin_gain = message.coin_gain;
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = $root.lq.ActivityAmuletHuRecord.toObject(message.highest_hu, options);
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                object.highest_level_score = message.highest_level_score;
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                object.highest_fan = message.highest_fan;
            if (message.pack_count != null && message.hasOwnProperty("pack_count"))
                object.pack_count = message.pack_count;
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                object.round_count = message.round_count;
            if (message.effect_counter && message.effect_counter.length) {
                object.effect_counter = Array(message.effect_counter.length);
                for (let j = 0; j < message.effect_counter.length; ++j)
                    object.effect_counter[j] = $root.lq.AmuletEffectCounterData.toObject(message.effect_counter[j], options);
            }
            if (message.hu_tiles_id && message.hu_tiles_id.length) {
                object.hu_tiles_id = Array(message.hu_tiles_id.length);
                for (let j = 0; j < message.hu_tiles_id.length; ++j)
                    object.hu_tiles_id[j] = message.hu_tiles_id[j];
            }
            return object;
        };

        AmuletGameRecordData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameRecordData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameRecordData";
        };

        return AmuletGameRecordData;
    })();

    lq.AmuletSkillData = (function() {

        function AmuletSkillData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletSkillData.prototype.id = 0;
        AmuletSkillData.prototype.level = 0;

        AmuletSkillData.create = function create(properties) {
            return new AmuletSkillData(properties);
        };

        AmuletSkillData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletSkillData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletSkillData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletSkillData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletSkillData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletSkillData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            return null;
        };

        AmuletSkillData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletSkillData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletSkillData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            return message;
        };

        AmuletSkillData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.level = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            return object;
        };

        AmuletSkillData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletSkillData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletSkillData";
        };

        return AmuletSkillData;
    })();

    lq.ActivityAmuletUpgradeData = (function() {

        function ActivityAmuletUpgradeData(properties) {
            this.skill = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletUpgradeData.prototype.skill = $util.emptyArray;

        ActivityAmuletUpgradeData.create = function create(properties) {
            return new ActivityAmuletUpgradeData(properties);
        };

        ActivityAmuletUpgradeData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.skill != null && message.skill.length)
                for (let i = 0; i < message.skill.length; ++i)
                    $root.lq.AmuletSkillData.encode(message.skill[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletUpgradeData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletUpgradeData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletUpgradeData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.skill && message.skill.length))
                            message.skill = [];
                        message.skill.push($root.lq.AmuletSkillData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletUpgradeData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletUpgradeData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.skill != null && message.hasOwnProperty("skill")) {
                if (!Array.isArray(message.skill))
                    return "skill: array expected";
                for (let i = 0; i < message.skill.length; ++i) {
                    let error = $root.lq.AmuletSkillData.verify(message.skill[i], _depth + 1);
                    if (error)
                        return "skill." + error;
                }
            }
            return null;
        };

        ActivityAmuletUpgradeData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletUpgradeData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletUpgradeData();
            if (object.skill) {
                if (!Array.isArray(object.skill))
                    throw TypeError(".lq.ActivityAmuletUpgradeData.skill: array expected");
                message.skill = Array(object.skill.length);
                for (let i = 0; i < object.skill.length; ++i) {
                    if (typeof object.skill[i] !== "object")
                        throw TypeError(".lq.ActivityAmuletUpgradeData.skill: object expected");
                    message.skill[i] = $root.lq.AmuletSkillData.fromObject(object.skill[i], _depth + 1);
                }
            }
            return message;
        };

        ActivityAmuletUpgradeData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.skill = [];
            if (message.skill && message.skill.length) {
                object.skill = Array(message.skill.length);
                for (let j = 0; j < message.skill.length; ++j)
                    object.skill[j] = $root.lq.AmuletSkillData.toObject(message.skill[j], options);
            }
            return object;
        };

        ActivityAmuletUpgradeData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletUpgradeData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletUpgradeData";
        };

        return ActivityAmuletUpgradeData;
    })();

    lq.AmuletGameRoundData = (function() {

        function AmuletGameRoundData(properties) {
            this.pool = [];
            this.tile_replace = [];
            this.tian_dora = [];
            this.dora = [];
            this.hands = [];
            this.used_desktop = [];
            this.ming = [];
            this.locked_tile = [];
            this.next_operation = [];
            this.ting_list = [];
            this.mountain = [];
            this.used = [];
            this.desktop = [];
            this.show_desktop = [];
            this.show_desktop_tiles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameRoundData.prototype.pool = $util.emptyArray;
        AmuletGameRoundData.prototype.tile_replace = $util.emptyArray;
        AmuletGameRoundData.prototype.tian_dora = $util.emptyArray;
        AmuletGameRoundData.prototype.dora = $util.emptyArray;
        AmuletGameRoundData.prototype.hands = $util.emptyArray;
        AmuletGameRoundData.prototype.used_desktop = $util.emptyArray;
        AmuletGameRoundData.prototype.ming = $util.emptyArray;
        AmuletGameRoundData.prototype.locked_tile = $util.emptyArray;
        AmuletGameRoundData.prototype.change_tile_count = 0;
        AmuletGameRoundData.prototype.total_change_tile_count = 0;
        AmuletGameRoundData.prototype.next_operation = $util.emptyArray;
        AmuletGameRoundData.prototype.ting_list = $util.emptyArray;
        AmuletGameRoundData.prototype.point = "";
        AmuletGameRoundData.prototype.target_point = "";
        AmuletGameRoundData.prototype.locked_tile_count = 0;
        AmuletGameRoundData.prototype.mountain = $util.emptyArray;
        AmuletGameRoundData.prototype.used = $util.emptyArray;
        AmuletGameRoundData.prototype.desktop = $util.emptyArray;
        AmuletGameRoundData.prototype.show_desktop = $util.emptyArray;
        AmuletGameRoundData.prototype.after_gang = 0;
        AmuletGameRoundData.prototype.desktop_remain = 0;
        AmuletGameRoundData.prototype.show_desktop_tiles = $util.emptyArray;

        AmuletGameRoundData.create = function create(properties) {
            return new AmuletGameRoundData(properties);
        };

        AmuletGameRoundData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.pool != null && message.pool.length)
                for (let i = 0; i < message.pool.length; ++i)
                    $root.lq.AmuletTile.encode(message.pool[i], writer.uint32(10).fork()).ldelim();
            if (message.tile_replace != null && message.tile_replace.length)
                for (let i = 0; i < message.tile_replace.length; ++i)
                    $root.lq.AmuletTile.encode(message.tile_replace[i], writer.uint32(18).fork()).ldelim();
            if (message.tian_dora != null && message.tian_dora.length)
                for (let i = 0; i < message.tian_dora.length; ++i)
                    writer.uint32(26).string(message.tian_dora[i]);
            if (message.mountain != null && message.mountain.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.mountain.length; ++i)
                    writer.uint32(message.mountain[i]);
                writer.ldelim();
            }
            if (message.dora != null && message.dora.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.dora.length; ++i)
                    writer.uint32(message.dora[i]);
                writer.ldelim();
            }
            if (message.hands != null && message.hands.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.hands.length; ++i)
                    writer.uint32(message.hands[i]);
                writer.ldelim();
            }
            if (message.used != null && message.used.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.used.length; ++i)
                    writer.uint32(message.used[i]);
                writer.ldelim();
            }
            if (message.used_desktop != null && message.used_desktop.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.used_desktop.length; ++i)
                    writer.uint32(message.used_desktop[i]);
                writer.ldelim();
            }
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    $root.lq.AmuletMingInfo.encode(message.ming[i], writer.uint32(74).fork()).ldelim();
            if (message.desktop != null && message.desktop.length) {
                writer.uint32(82).fork();
                for (let i = 0; i < message.desktop.length; ++i)
                    writer.uint32(message.desktop[i]);
                writer.ldelim();
            }
            if (message.show_desktop != null && message.show_desktop.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.show_desktop.length; ++i)
                    writer.uint32(message.show_desktop[i]);
                writer.ldelim();
            }
            if (message.locked_tile != null && message.locked_tile.length) {
                writer.uint32(98).fork();
                for (let i = 0; i < message.locked_tile.length; ++i)
                    writer.uint32(message.locked_tile[i]);
                writer.ldelim();
            }
            if (message.change_tile_count != null && Object.hasOwnProperty.call(message, "change_tile_count"))
                writer.uint32(104).uint32(message.change_tile_count);
            if (message.total_change_tile_count != null && Object.hasOwnProperty.call(message, "total_change_tile_count"))
                writer.uint32(112).uint32(message.total_change_tile_count);
            if (message.next_operation != null && message.next_operation.length)
                for (let i = 0; i < message.next_operation.length; ++i)
                    $root.lq.AmuletGameOperation.encode(message.next_operation[i], writer.uint32(122).fork()).ldelim();
            if (message.ting_list != null && message.ting_list.length)
                for (let i = 0; i < message.ting_list.length; ++i)
                    $root.lq.AmuletActivityTingInfo.encode(message.ting_list[i], writer.uint32(130).fork()).ldelim();
            if (message.after_gang != null && Object.hasOwnProperty.call(message, "after_gang"))
                writer.uint32(136).uint32(message.after_gang);
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                writer.uint32(146).string(message.point);
            if (message.target_point != null && Object.hasOwnProperty.call(message, "target_point"))
                writer.uint32(154).string(message.target_point);
            if (message.desktop_remain != null && Object.hasOwnProperty.call(message, "desktop_remain"))
                writer.uint32(160).uint32(message.desktop_remain);
            if (message.show_desktop_tiles != null && message.show_desktop_tiles.length)
                for (let i = 0; i < message.show_desktop_tiles.length; ++i)
                    $root.lq.AmuletShowDesktopTileData.encode(message.show_desktop_tiles[i], writer.uint32(170).fork()).ldelim();
            if (message.locked_tile_count != null && Object.hasOwnProperty.call(message, "locked_tile_count"))
                writer.uint32(176).uint32(message.locked_tile_count);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameRoundData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameRoundData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameRoundData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.pool && message.pool.length))
                            message.pool = [];
                        message.pool.push($root.lq.AmuletTile.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tile_replace && message.tile_replace.length))
                            message.tile_replace = [];
                        message.tile_replace.push($root.lq.AmuletTile.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tian_dora && message.tian_dora.length))
                            message.tian_dora = [];
                        message.tian_dora.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.dora && message.dora.length))
                                message.dora = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.dora.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.dora && message.dora.length))
                            message.dora = [];
                        message.dora.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.hands && message.hands.length))
                                message.hands = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.hands.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.hands && message.hands.length))
                            message.hands = [];
                        message.hands.push(reader.uint32());
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.used_desktop && message.used_desktop.length))
                                message.used_desktop = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.used_desktop.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.used_desktop && message.used_desktop.length))
                            message.used_desktop = [];
                        message.used_desktop.push(reader.uint32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push($root.lq.AmuletMingInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType === 2) {
                            if (!(message.locked_tile && message.locked_tile.length))
                                message.locked_tile = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.locked_tile.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.locked_tile && message.locked_tile.length))
                            message.locked_tile = [];
                        message.locked_tile.push(reader.uint32());
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.change_tile_count = value;
                        else
                            delete message.change_tile_count;
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.total_change_tile_count = value;
                        else
                            delete message.total_change_tile_count;
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.next_operation && message.next_operation.length))
                            message.next_operation = [];
                        message.next_operation.push($root.lq.AmuletGameOperation.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ting_list && message.ting_list.length))
                            message.ting_list = [];
                        message.ting_list.push($root.lq.AmuletActivityTingInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.point = value;
                        else
                            delete message.point;
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.target_point = value;
                        else
                            delete message.target_point;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.locked_tile_count = value;
                        else
                            delete message.locked_tile_count;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.mountain && message.mountain.length))
                                message.mountain = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.mountain.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.mountain && message.mountain.length))
                            message.mountain = [];
                        message.mountain.push(reader.uint32());
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.used && message.used.length))
                                message.used = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.used.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.used && message.used.length))
                            message.used = [];
                        message.used.push(reader.uint32());
                        continue;
                    }
                case 10: {
                        if (wireType === 2) {
                            if (!(message.desktop && message.desktop.length))
                                message.desktop = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.desktop.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.desktop && message.desktop.length))
                            message.desktop = [];
                        message.desktop.push(reader.uint32());
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.show_desktop && message.show_desktop.length))
                                message.show_desktop = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.show_desktop.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.show_desktop && message.show_desktop.length))
                            message.show_desktop = [];
                        message.show_desktop.push(reader.uint32());
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.after_gang = value;
                        else
                            delete message.after_gang;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.desktop_remain = value;
                        else
                            delete message.desktop_remain;
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if (!(message.show_desktop_tiles && message.show_desktop_tiles.length))
                            message.show_desktop_tiles = [];
                        message.show_desktop_tiles.push($root.lq.AmuletShowDesktopTileData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameRoundData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameRoundData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pool != null && message.hasOwnProperty("pool")) {
                if (!Array.isArray(message.pool))
                    return "pool: array expected";
                for (let i = 0; i < message.pool.length; ++i) {
                    let error = $root.lq.AmuletTile.verify(message.pool[i], _depth + 1);
                    if (error)
                        return "pool." + error;
                }
            }
            if (message.tile_replace != null && message.hasOwnProperty("tile_replace")) {
                if (!Array.isArray(message.tile_replace))
                    return "tile_replace: array expected";
                for (let i = 0; i < message.tile_replace.length; ++i) {
                    let error = $root.lq.AmuletTile.verify(message.tile_replace[i], _depth + 1);
                    if (error)
                        return "tile_replace." + error;
                }
            }
            if (message.tian_dora != null && message.hasOwnProperty("tian_dora")) {
                if (!Array.isArray(message.tian_dora))
                    return "tian_dora: array expected";
                for (let i = 0; i < message.tian_dora.length; ++i)
                    if (!$util.isString(message.tian_dora[i]))
                        return "tian_dora: string[] expected";
            }
            if (message.dora != null && message.hasOwnProperty("dora")) {
                if (!Array.isArray(message.dora))
                    return "dora: array expected";
                for (let i = 0; i < message.dora.length; ++i)
                    if (!$util.isInteger(message.dora[i]))
                        return "dora: integer[] expected";
            }
            if (message.hands != null && message.hasOwnProperty("hands")) {
                if (!Array.isArray(message.hands))
                    return "hands: array expected";
                for (let i = 0; i < message.hands.length; ++i)
                    if (!$util.isInteger(message.hands[i]))
                        return "hands: integer[] expected";
            }
            if (message.used_desktop != null && message.hasOwnProperty("used_desktop")) {
                if (!Array.isArray(message.used_desktop))
                    return "used_desktop: array expected";
                for (let i = 0; i < message.used_desktop.length; ++i)
                    if (!$util.isInteger(message.used_desktop[i]))
                        return "used_desktop: integer[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i) {
                    let error = $root.lq.AmuletMingInfo.verify(message.ming[i], _depth + 1);
                    if (error)
                        return "ming." + error;
                }
            }
            if (message.locked_tile != null && message.hasOwnProperty("locked_tile")) {
                if (!Array.isArray(message.locked_tile))
                    return "locked_tile: array expected";
                for (let i = 0; i < message.locked_tile.length; ++i)
                    if (!$util.isInteger(message.locked_tile[i]))
                        return "locked_tile: integer[] expected";
            }
            if (message.change_tile_count != null && message.hasOwnProperty("change_tile_count"))
                if (!$util.isInteger(message.change_tile_count))
                    return "change_tile_count: integer expected";
            if (message.total_change_tile_count != null && message.hasOwnProperty("total_change_tile_count"))
                if (!$util.isInteger(message.total_change_tile_count))
                    return "total_change_tile_count: integer expected";
            if (message.next_operation != null && message.hasOwnProperty("next_operation")) {
                if (!Array.isArray(message.next_operation))
                    return "next_operation: array expected";
                for (let i = 0; i < message.next_operation.length; ++i) {
                    let error = $root.lq.AmuletGameOperation.verify(message.next_operation[i], _depth + 1);
                    if (error)
                        return "next_operation." + error;
                }
            }
            if (message.ting_list != null && message.hasOwnProperty("ting_list")) {
                if (!Array.isArray(message.ting_list))
                    return "ting_list: array expected";
                for (let i = 0; i < message.ting_list.length; ++i) {
                    let error = $root.lq.AmuletActivityTingInfo.verify(message.ting_list[i], _depth + 1);
                    if (error)
                        return "ting_list." + error;
                }
            }
            if (message.point != null && message.hasOwnProperty("point"))
                if (!$util.isString(message.point))
                    return "point: string expected";
            if (message.target_point != null && message.hasOwnProperty("target_point"))
                if (!$util.isString(message.target_point))
                    return "target_point: string expected";
            if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count"))
                if (!$util.isInteger(message.locked_tile_count))
                    return "locked_tile_count: integer expected";
            if (message.mountain != null && message.hasOwnProperty("mountain")) {
                if (!Array.isArray(message.mountain))
                    return "mountain: array expected";
                for (let i = 0; i < message.mountain.length; ++i)
                    if (!$util.isInteger(message.mountain[i]))
                        return "mountain: integer[] expected";
            }
            if (message.used != null && message.hasOwnProperty("used")) {
                if (!Array.isArray(message.used))
                    return "used: array expected";
                for (let i = 0; i < message.used.length; ++i)
                    if (!$util.isInteger(message.used[i]))
                        return "used: integer[] expected";
            }
            if (message.desktop != null && message.hasOwnProperty("desktop")) {
                if (!Array.isArray(message.desktop))
                    return "desktop: array expected";
                for (let i = 0; i < message.desktop.length; ++i)
                    if (!$util.isInteger(message.desktop[i]))
                        return "desktop: integer[] expected";
            }
            if (message.show_desktop != null && message.hasOwnProperty("show_desktop")) {
                if (!Array.isArray(message.show_desktop))
                    return "show_desktop: array expected";
                for (let i = 0; i < message.show_desktop.length; ++i)
                    if (!$util.isInteger(message.show_desktop[i]))
                        return "show_desktop: integer[] expected";
            }
            if (message.after_gang != null && message.hasOwnProperty("after_gang"))
                if (!$util.isInteger(message.after_gang))
                    return "after_gang: integer expected";
            if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain"))
                if (!$util.isInteger(message.desktop_remain))
                    return "desktop_remain: integer expected";
            if (message.show_desktop_tiles != null && message.hasOwnProperty("show_desktop_tiles")) {
                if (!Array.isArray(message.show_desktop_tiles))
                    return "show_desktop_tiles: array expected";
                for (let i = 0; i < message.show_desktop_tiles.length; ++i) {
                    let error = $root.lq.AmuletShowDesktopTileData.verify(message.show_desktop_tiles[i], _depth + 1);
                    if (error)
                        return "show_desktop_tiles." + error;
                }
            }
            return null;
        };

        AmuletGameRoundData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameRoundData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameRoundData();
            if (object.pool) {
                if (!Array.isArray(object.pool))
                    throw TypeError(".lq.AmuletGameRoundData.pool: array expected");
                message.pool = Array(object.pool.length);
                for (let i = 0; i < object.pool.length; ++i) {
                    if (typeof object.pool[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.pool: object expected");
                    message.pool[i] = $root.lq.AmuletTile.fromObject(object.pool[i], _depth + 1);
                }
            }
            if (object.tile_replace) {
                if (!Array.isArray(object.tile_replace))
                    throw TypeError(".lq.AmuletGameRoundData.tile_replace: array expected");
                message.tile_replace = Array(object.tile_replace.length);
                for (let i = 0; i < object.tile_replace.length; ++i) {
                    if (typeof object.tile_replace[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.tile_replace: object expected");
                    message.tile_replace[i] = $root.lq.AmuletTile.fromObject(object.tile_replace[i], _depth + 1);
                }
            }
            if (object.tian_dora) {
                if (!Array.isArray(object.tian_dora))
                    throw TypeError(".lq.AmuletGameRoundData.tian_dora: array expected");
                message.tian_dora = Array(object.tian_dora.length);
                for (let i = 0; i < object.tian_dora.length; ++i)
                    message.tian_dora[i] = String(object.tian_dora[i]);
            }
            if (object.dora) {
                if (!Array.isArray(object.dora))
                    throw TypeError(".lq.AmuletGameRoundData.dora: array expected");
                message.dora = Array(object.dora.length);
                for (let i = 0; i < object.dora.length; ++i)
                    message.dora[i] = object.dora[i] >>> 0;
            }
            if (object.hands) {
                if (!Array.isArray(object.hands))
                    throw TypeError(".lq.AmuletGameRoundData.hands: array expected");
                message.hands = Array(object.hands.length);
                for (let i = 0; i < object.hands.length; ++i)
                    message.hands[i] = object.hands[i] >>> 0;
            }
            if (object.used_desktop) {
                if (!Array.isArray(object.used_desktop))
                    throw TypeError(".lq.AmuletGameRoundData.used_desktop: array expected");
                message.used_desktop = Array(object.used_desktop.length);
                for (let i = 0; i < object.used_desktop.length; ++i)
                    message.used_desktop[i] = object.used_desktop[i] >>> 0;
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.AmuletGameRoundData.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i) {
                    if (typeof object.ming[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.ming: object expected");
                    message.ming[i] = $root.lq.AmuletMingInfo.fromObject(object.ming[i], _depth + 1);
                }
            }
            if (object.locked_tile) {
                if (!Array.isArray(object.locked_tile))
                    throw TypeError(".lq.AmuletGameRoundData.locked_tile: array expected");
                message.locked_tile = Array(object.locked_tile.length);
                for (let i = 0; i < object.locked_tile.length; ++i)
                    message.locked_tile[i] = object.locked_tile[i] >>> 0;
            }
            if (object.change_tile_count != null)
                if (Number(object.change_tile_count) !== 0)
                    message.change_tile_count = object.change_tile_count >>> 0;
            if (object.total_change_tile_count != null)
                if (Number(object.total_change_tile_count) !== 0)
                    message.total_change_tile_count = object.total_change_tile_count >>> 0;
            if (object.next_operation) {
                if (!Array.isArray(object.next_operation))
                    throw TypeError(".lq.AmuletGameRoundData.next_operation: array expected");
                message.next_operation = Array(object.next_operation.length);
                for (let i = 0; i < object.next_operation.length; ++i) {
                    if (typeof object.next_operation[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.next_operation: object expected");
                    message.next_operation[i] = $root.lq.AmuletGameOperation.fromObject(object.next_operation[i], _depth + 1);
                }
            }
            if (object.ting_list) {
                if (!Array.isArray(object.ting_list))
                    throw TypeError(".lq.AmuletGameRoundData.ting_list: array expected");
                message.ting_list = Array(object.ting_list.length);
                for (let i = 0; i < object.ting_list.length; ++i) {
                    if (typeof object.ting_list[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.ting_list: object expected");
                    message.ting_list[i] = $root.lq.AmuletActivityTingInfo.fromObject(object.ting_list[i], _depth + 1);
                }
            }
            if (object.point != null)
                if (typeof object.point !== "string" || object.point.length)
                    message.point = String(object.point);
            if (object.target_point != null)
                if (typeof object.target_point !== "string" || object.target_point.length)
                    message.target_point = String(object.target_point);
            if (object.locked_tile_count != null)
                if (Number(object.locked_tile_count) !== 0)
                    message.locked_tile_count = object.locked_tile_count >>> 0;
            if (object.mountain) {
                if (!Array.isArray(object.mountain))
                    throw TypeError(".lq.AmuletGameRoundData.mountain: array expected");
                message.mountain = Array(object.mountain.length);
                for (let i = 0; i < object.mountain.length; ++i)
                    message.mountain[i] = object.mountain[i] >>> 0;
            }
            if (object.used) {
                if (!Array.isArray(object.used))
                    throw TypeError(".lq.AmuletGameRoundData.used: array expected");
                message.used = Array(object.used.length);
                for (let i = 0; i < object.used.length; ++i)
                    message.used[i] = object.used[i] >>> 0;
            }
            if (object.desktop) {
                if (!Array.isArray(object.desktop))
                    throw TypeError(".lq.AmuletGameRoundData.desktop: array expected");
                message.desktop = Array(object.desktop.length);
                for (let i = 0; i < object.desktop.length; ++i)
                    message.desktop[i] = object.desktop[i] >>> 0;
            }
            if (object.show_desktop) {
                if (!Array.isArray(object.show_desktop))
                    throw TypeError(".lq.AmuletGameRoundData.show_desktop: array expected");
                message.show_desktop = Array(object.show_desktop.length);
                for (let i = 0; i < object.show_desktop.length; ++i)
                    message.show_desktop[i] = object.show_desktop[i] >>> 0;
            }
            if (object.after_gang != null)
                if (Number(object.after_gang) !== 0)
                    message.after_gang = object.after_gang >>> 0;
            if (object.desktop_remain != null)
                if (Number(object.desktop_remain) !== 0)
                    message.desktop_remain = object.desktop_remain >>> 0;
            if (object.show_desktop_tiles) {
                if (!Array.isArray(object.show_desktop_tiles))
                    throw TypeError(".lq.AmuletGameRoundData.show_desktop_tiles: array expected");
                message.show_desktop_tiles = Array(object.show_desktop_tiles.length);
                for (let i = 0; i < object.show_desktop_tiles.length; ++i) {
                    if (typeof object.show_desktop_tiles[i] !== "object")
                        throw TypeError(".lq.AmuletGameRoundData.show_desktop_tiles: object expected");
                    message.show_desktop_tiles[i] = $root.lq.AmuletShowDesktopTileData.fromObject(object.show_desktop_tiles[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletGameRoundData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.pool = [];
                object.tile_replace = [];
                object.tian_dora = [];
                object.mountain = [];
                object.dora = [];
                object.hands = [];
                object.used = [];
                object.used_desktop = [];
                object.ming = [];
                object.desktop = [];
                object.show_desktop = [];
                object.locked_tile = [];
                object.next_operation = [];
                object.ting_list = [];
                object.show_desktop_tiles = [];
            }
            if (options.defaults) {
                object.change_tile_count = 0;
                object.total_change_tile_count = 0;
                object.after_gang = 0;
                object.point = "";
                object.target_point = "";
                object.desktop_remain = 0;
                object.locked_tile_count = 0;
            }
            if (message.pool && message.pool.length) {
                object.pool = Array(message.pool.length);
                for (let j = 0; j < message.pool.length; ++j)
                    object.pool[j] = $root.lq.AmuletTile.toObject(message.pool[j], options);
            }
            if (message.tile_replace && message.tile_replace.length) {
                object.tile_replace = Array(message.tile_replace.length);
                for (let j = 0; j < message.tile_replace.length; ++j)
                    object.tile_replace[j] = $root.lq.AmuletTile.toObject(message.tile_replace[j], options);
            }
            if (message.tian_dora && message.tian_dora.length) {
                object.tian_dora = Array(message.tian_dora.length);
                for (let j = 0; j < message.tian_dora.length; ++j)
                    object.tian_dora[j] = message.tian_dora[j];
            }
            if (message.mountain && message.mountain.length) {
                object.mountain = Array(message.mountain.length);
                for (let j = 0; j < message.mountain.length; ++j)
                    object.mountain[j] = message.mountain[j];
            }
            if (message.dora && message.dora.length) {
                object.dora = Array(message.dora.length);
                for (let j = 0; j < message.dora.length; ++j)
                    object.dora[j] = message.dora[j];
            }
            if (message.hands && message.hands.length) {
                object.hands = Array(message.hands.length);
                for (let j = 0; j < message.hands.length; ++j)
                    object.hands[j] = message.hands[j];
            }
            if (message.used && message.used.length) {
                object.used = Array(message.used.length);
                for (let j = 0; j < message.used.length; ++j)
                    object.used[j] = message.used[j];
            }
            if (message.used_desktop && message.used_desktop.length) {
                object.used_desktop = Array(message.used_desktop.length);
                for (let j = 0; j < message.used_desktop.length; ++j)
                    object.used_desktop[j] = message.used_desktop[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = $root.lq.AmuletMingInfo.toObject(message.ming[j], options);
            }
            if (message.desktop && message.desktop.length) {
                object.desktop = Array(message.desktop.length);
                for (let j = 0; j < message.desktop.length; ++j)
                    object.desktop[j] = message.desktop[j];
            }
            if (message.show_desktop && message.show_desktop.length) {
                object.show_desktop = Array(message.show_desktop.length);
                for (let j = 0; j < message.show_desktop.length; ++j)
                    object.show_desktop[j] = message.show_desktop[j];
            }
            if (message.locked_tile && message.locked_tile.length) {
                object.locked_tile = Array(message.locked_tile.length);
                for (let j = 0; j < message.locked_tile.length; ++j)
                    object.locked_tile[j] = message.locked_tile[j];
            }
            if (message.change_tile_count != null && message.hasOwnProperty("change_tile_count"))
                object.change_tile_count = message.change_tile_count;
            if (message.total_change_tile_count != null && message.hasOwnProperty("total_change_tile_count"))
                object.total_change_tile_count = message.total_change_tile_count;
            if (message.next_operation && message.next_operation.length) {
                object.next_operation = Array(message.next_operation.length);
                for (let j = 0; j < message.next_operation.length; ++j)
                    object.next_operation[j] = $root.lq.AmuletGameOperation.toObject(message.next_operation[j], options);
            }
            if (message.ting_list && message.ting_list.length) {
                object.ting_list = Array(message.ting_list.length);
                for (let j = 0; j < message.ting_list.length; ++j)
                    object.ting_list[j] = $root.lq.AmuletActivityTingInfo.toObject(message.ting_list[j], options);
            }
            if (message.after_gang != null && message.hasOwnProperty("after_gang"))
                object.after_gang = message.after_gang;
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = message.point;
            if (message.target_point != null && message.hasOwnProperty("target_point"))
                object.target_point = message.target_point;
            if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain"))
                object.desktop_remain = message.desktop_remain;
            if (message.show_desktop_tiles && message.show_desktop_tiles.length) {
                object.show_desktop_tiles = Array(message.show_desktop_tiles.length);
                for (let j = 0; j < message.show_desktop_tiles.length; ++j)
                    object.show_desktop_tiles[j] = $root.lq.AmuletShowDesktopTileData.toObject(message.show_desktop_tiles[j], options);
            }
            if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count"))
                object.locked_tile_count = message.locked_tile_count;
            return object;
        };

        AmuletGameRoundData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameRoundData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameRoundData";
        };

        return AmuletGameRoundData;
    })();

    lq.AmuletEffectCandidate = (function() {

        function AmuletEffectCandidate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectCandidate.prototype.id = 0;
        AmuletEffectCandidate.prototype.badge_id = 0;

        AmuletEffectCandidate.create = function create(properties) {
            return new AmuletEffectCandidate(properties);
        };

        AmuletEffectCandidate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.badge_id != null && Object.hasOwnProperty.call(message, "badge_id"))
                writer.uint32(16).uint32(message.badge_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectCandidate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectCandidate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectCandidate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.badge_id = value;
                        else
                            delete message.badge_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectCandidate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectCandidate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                if (!$util.isInteger(message.badge_id))
                    return "badge_id: integer expected";
            return null;
        };

        AmuletEffectCandidate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectCandidate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectCandidate();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.badge_id != null)
                if (Number(object.badge_id) !== 0)
                    message.badge_id = object.badge_id >>> 0;
            return message;
        };

        AmuletEffectCandidate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.badge_id = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                object.badge_id = message.badge_id;
            return object;
        };

        AmuletEffectCandidate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectCandidate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectCandidate";
        };

        return AmuletEffectCandidate;
    })();

    lq.AmuletGameEffectData = (function() {

        function AmuletGameEffectData(properties) {
            this.effect_list = [];
            this.buff_list = [];
            this.skill_buff_list = [];
            this.shop_buff_list = [];
            this.free_reward_candidates = [];
            this.level_reward_candidates = [];
            this.level_reward_packs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameEffectData.prototype.effect_list = $util.emptyArray;
        AmuletGameEffectData.prototype.buff_list = $util.emptyArray;
        AmuletGameEffectData.prototype.skill_buff_list = $util.emptyArray;
        AmuletGameEffectData.prototype.shop_buff_list = $util.emptyArray;
        AmuletGameEffectData.prototype.free_reward_candidates = $util.emptyArray;
        AmuletGameEffectData.prototype.level_reward_candidates = $util.emptyArray;
        AmuletGameEffectData.prototype.level_reward_packs = $util.emptyArray;
        AmuletGameEffectData.prototype.current_level_reward_pack = 0;
        AmuletGameEffectData.prototype.max_effect_volume = 0;

        AmuletGameEffectData.create = function create(properties) {
            return new AmuletGameEffectData(properties);
        };

        AmuletGameEffectData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.effect_list != null && message.effect_list.length)
                for (let i = 0; i < message.effect_list.length; ++i)
                    $root.lq.AmuletEffectData.encode(message.effect_list[i], writer.uint32(10).fork()).ldelim();
            if (message.buff_list != null && message.buff_list.length)
                for (let i = 0; i < message.buff_list.length; ++i)
                    $root.lq.AmuletBuffData.encode(message.buff_list[i], writer.uint32(18).fork()).ldelim();
            if (message.skill_buff_list != null && message.skill_buff_list.length)
                for (let i = 0; i < message.skill_buff_list.length; ++i)
                    $root.lq.AmuletBuffData.encode(message.skill_buff_list[i], writer.uint32(26).fork()).ldelim();
            if (message.shop_buff_list != null && message.shop_buff_list.length)
                for (let i = 0; i < message.shop_buff_list.length; ++i)
                    $root.lq.AmuletBuffData.encode(message.shop_buff_list[i], writer.uint32(34).fork()).ldelim();
            if (message.free_reward_candidates != null && message.free_reward_candidates.length)
                for (let i = 0; i < message.free_reward_candidates.length; ++i)
                    $root.lq.AmuletEffectCandidate.encode(message.free_reward_candidates[i], writer.uint32(42).fork()).ldelim();
            if (message.level_reward_candidates != null && message.level_reward_candidates.length)
                for (let i = 0; i < message.level_reward_candidates.length; ++i)
                    $root.lq.AmuletEffectCandidate.encode(message.level_reward_candidates[i], writer.uint32(50).fork()).ldelim();
            if (message.level_reward_packs != null && message.level_reward_packs.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.level_reward_packs.length; ++i)
                    writer.uint32(message.level_reward_packs[i]);
                writer.ldelim();
            }
            if (message.current_level_reward_pack != null && Object.hasOwnProperty.call(message, "current_level_reward_pack"))
                writer.uint32(64).uint32(message.current_level_reward_pack);
            if (message.max_effect_volume != null && Object.hasOwnProperty.call(message, "max_effect_volume"))
                writer.uint32(72).uint32(message.max_effect_volume);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameEffectData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameEffectData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameEffectData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_list && message.effect_list.length))
                            message.effect_list = [];
                        message.effect_list.push($root.lq.AmuletEffectData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.buff_list && message.buff_list.length))
                            message.buff_list = [];
                        message.buff_list.push($root.lq.AmuletBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.skill_buff_list && message.skill_buff_list.length))
                            message.skill_buff_list = [];
                        message.skill_buff_list.push($root.lq.AmuletBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.shop_buff_list && message.shop_buff_list.length))
                            message.shop_buff_list = [];
                        message.shop_buff_list.push($root.lq.AmuletBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.free_reward_candidates && message.free_reward_candidates.length))
                            message.free_reward_candidates = [];
                        message.free_reward_candidates.push($root.lq.AmuletEffectCandidate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.level_reward_candidates && message.level_reward_candidates.length))
                            message.level_reward_candidates = [];
                        message.level_reward_candidates.push($root.lq.AmuletEffectCandidate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.level_reward_packs && message.level_reward_packs.length))
                                message.level_reward_packs = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.level_reward_packs.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.level_reward_packs && message.level_reward_packs.length))
                            message.level_reward_packs = [];
                        message.level_reward_packs.push(reader.uint32());
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.current_level_reward_pack = value;
                        else
                            delete message.current_level_reward_pack;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_effect_volume = value;
                        else
                            delete message.max_effect_volume;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameEffectData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameEffectData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                if (!Array.isArray(message.effect_list))
                    return "effect_list: array expected";
                for (let i = 0; i < message.effect_list.length; ++i) {
                    let error = $root.lq.AmuletEffectData.verify(message.effect_list[i], _depth + 1);
                    if (error)
                        return "effect_list." + error;
                }
            }
            if (message.buff_list != null && message.hasOwnProperty("buff_list")) {
                if (!Array.isArray(message.buff_list))
                    return "buff_list: array expected";
                for (let i = 0; i < message.buff_list.length; ++i) {
                    let error = $root.lq.AmuletBuffData.verify(message.buff_list[i], _depth + 1);
                    if (error)
                        return "buff_list." + error;
                }
            }
            if (message.skill_buff_list != null && message.hasOwnProperty("skill_buff_list")) {
                if (!Array.isArray(message.skill_buff_list))
                    return "skill_buff_list: array expected";
                for (let i = 0; i < message.skill_buff_list.length; ++i) {
                    let error = $root.lq.AmuletBuffData.verify(message.skill_buff_list[i], _depth + 1);
                    if (error)
                        return "skill_buff_list." + error;
                }
            }
            if (message.shop_buff_list != null && message.hasOwnProperty("shop_buff_list")) {
                if (!Array.isArray(message.shop_buff_list))
                    return "shop_buff_list: array expected";
                for (let i = 0; i < message.shop_buff_list.length; ++i) {
                    let error = $root.lq.AmuletBuffData.verify(message.shop_buff_list[i], _depth + 1);
                    if (error)
                        return "shop_buff_list." + error;
                }
            }
            if (message.free_reward_candidates != null && message.hasOwnProperty("free_reward_candidates")) {
                if (!Array.isArray(message.free_reward_candidates))
                    return "free_reward_candidates: array expected";
                for (let i = 0; i < message.free_reward_candidates.length; ++i) {
                    let error = $root.lq.AmuletEffectCandidate.verify(message.free_reward_candidates[i], _depth + 1);
                    if (error)
                        return "free_reward_candidates." + error;
                }
            }
            if (message.level_reward_candidates != null && message.hasOwnProperty("level_reward_candidates")) {
                if (!Array.isArray(message.level_reward_candidates))
                    return "level_reward_candidates: array expected";
                for (let i = 0; i < message.level_reward_candidates.length; ++i) {
                    let error = $root.lq.AmuletEffectCandidate.verify(message.level_reward_candidates[i], _depth + 1);
                    if (error)
                        return "level_reward_candidates." + error;
                }
            }
            if (message.level_reward_packs != null && message.hasOwnProperty("level_reward_packs")) {
                if (!Array.isArray(message.level_reward_packs))
                    return "level_reward_packs: array expected";
                for (let i = 0; i < message.level_reward_packs.length; ++i)
                    if (!$util.isInteger(message.level_reward_packs[i]))
                        return "level_reward_packs: integer[] expected";
            }
            if (message.current_level_reward_pack != null && message.hasOwnProperty("current_level_reward_pack"))
                if (!$util.isInteger(message.current_level_reward_pack))
                    return "current_level_reward_pack: integer expected";
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume"))
                if (!$util.isInteger(message.max_effect_volume))
                    return "max_effect_volume: integer expected";
            return null;
        };

        AmuletGameEffectData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameEffectData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameEffectData();
            if (object.effect_list) {
                if (!Array.isArray(object.effect_list))
                    throw TypeError(".lq.AmuletGameEffectData.effect_list: array expected");
                message.effect_list = Array(object.effect_list.length);
                for (let i = 0; i < object.effect_list.length; ++i) {
                    if (typeof object.effect_list[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.effect_list: object expected");
                    message.effect_list[i] = $root.lq.AmuletEffectData.fromObject(object.effect_list[i], _depth + 1);
                }
            }
            if (object.buff_list) {
                if (!Array.isArray(object.buff_list))
                    throw TypeError(".lq.AmuletGameEffectData.buff_list: array expected");
                message.buff_list = Array(object.buff_list.length);
                for (let i = 0; i < object.buff_list.length; ++i) {
                    if (typeof object.buff_list[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.buff_list: object expected");
                    message.buff_list[i] = $root.lq.AmuletBuffData.fromObject(object.buff_list[i], _depth + 1);
                }
            }
            if (object.skill_buff_list) {
                if (!Array.isArray(object.skill_buff_list))
                    throw TypeError(".lq.AmuletGameEffectData.skill_buff_list: array expected");
                message.skill_buff_list = Array(object.skill_buff_list.length);
                for (let i = 0; i < object.skill_buff_list.length; ++i) {
                    if (typeof object.skill_buff_list[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.skill_buff_list: object expected");
                    message.skill_buff_list[i] = $root.lq.AmuletBuffData.fromObject(object.skill_buff_list[i], _depth + 1);
                }
            }
            if (object.shop_buff_list) {
                if (!Array.isArray(object.shop_buff_list))
                    throw TypeError(".lq.AmuletGameEffectData.shop_buff_list: array expected");
                message.shop_buff_list = Array(object.shop_buff_list.length);
                for (let i = 0; i < object.shop_buff_list.length; ++i) {
                    if (typeof object.shop_buff_list[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.shop_buff_list: object expected");
                    message.shop_buff_list[i] = $root.lq.AmuletBuffData.fromObject(object.shop_buff_list[i], _depth + 1);
                }
            }
            if (object.free_reward_candidates) {
                if (!Array.isArray(object.free_reward_candidates))
                    throw TypeError(".lq.AmuletGameEffectData.free_reward_candidates: array expected");
                message.free_reward_candidates = Array(object.free_reward_candidates.length);
                for (let i = 0; i < object.free_reward_candidates.length; ++i) {
                    if (typeof object.free_reward_candidates[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.free_reward_candidates: object expected");
                    message.free_reward_candidates[i] = $root.lq.AmuletEffectCandidate.fromObject(object.free_reward_candidates[i], _depth + 1);
                }
            }
            if (object.level_reward_candidates) {
                if (!Array.isArray(object.level_reward_candidates))
                    throw TypeError(".lq.AmuletGameEffectData.level_reward_candidates: array expected");
                message.level_reward_candidates = Array(object.level_reward_candidates.length);
                for (let i = 0; i < object.level_reward_candidates.length; ++i) {
                    if (typeof object.level_reward_candidates[i] !== "object")
                        throw TypeError(".lq.AmuletGameEffectData.level_reward_candidates: object expected");
                    message.level_reward_candidates[i] = $root.lq.AmuletEffectCandidate.fromObject(object.level_reward_candidates[i], _depth + 1);
                }
            }
            if (object.level_reward_packs) {
                if (!Array.isArray(object.level_reward_packs))
                    throw TypeError(".lq.AmuletGameEffectData.level_reward_packs: array expected");
                message.level_reward_packs = Array(object.level_reward_packs.length);
                for (let i = 0; i < object.level_reward_packs.length; ++i)
                    message.level_reward_packs[i] = object.level_reward_packs[i] >>> 0;
            }
            if (object.current_level_reward_pack != null)
                if (Number(object.current_level_reward_pack) !== 0)
                    message.current_level_reward_pack = object.current_level_reward_pack >>> 0;
            if (object.max_effect_volume != null)
                if (Number(object.max_effect_volume) !== 0)
                    message.max_effect_volume = object.max_effect_volume >>> 0;
            return message;
        };

        AmuletGameEffectData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effect_list = [];
                object.buff_list = [];
                object.skill_buff_list = [];
                object.shop_buff_list = [];
                object.free_reward_candidates = [];
                object.level_reward_candidates = [];
                object.level_reward_packs = [];
            }
            if (options.defaults) {
                object.current_level_reward_pack = 0;
                object.max_effect_volume = 0;
            }
            if (message.effect_list && message.effect_list.length) {
                object.effect_list = Array(message.effect_list.length);
                for (let j = 0; j < message.effect_list.length; ++j)
                    object.effect_list[j] = $root.lq.AmuletEffectData.toObject(message.effect_list[j], options);
            }
            if (message.buff_list && message.buff_list.length) {
                object.buff_list = Array(message.buff_list.length);
                for (let j = 0; j < message.buff_list.length; ++j)
                    object.buff_list[j] = $root.lq.AmuletBuffData.toObject(message.buff_list[j], options);
            }
            if (message.skill_buff_list && message.skill_buff_list.length) {
                object.skill_buff_list = Array(message.skill_buff_list.length);
                for (let j = 0; j < message.skill_buff_list.length; ++j)
                    object.skill_buff_list[j] = $root.lq.AmuletBuffData.toObject(message.skill_buff_list[j], options);
            }
            if (message.shop_buff_list && message.shop_buff_list.length) {
                object.shop_buff_list = Array(message.shop_buff_list.length);
                for (let j = 0; j < message.shop_buff_list.length; ++j)
                    object.shop_buff_list[j] = $root.lq.AmuletBuffData.toObject(message.shop_buff_list[j], options);
            }
            if (message.free_reward_candidates && message.free_reward_candidates.length) {
                object.free_reward_candidates = Array(message.free_reward_candidates.length);
                for (let j = 0; j < message.free_reward_candidates.length; ++j)
                    object.free_reward_candidates[j] = $root.lq.AmuletEffectCandidate.toObject(message.free_reward_candidates[j], options);
            }
            if (message.level_reward_candidates && message.level_reward_candidates.length) {
                object.level_reward_candidates = Array(message.level_reward_candidates.length);
                for (let j = 0; j < message.level_reward_candidates.length; ++j)
                    object.level_reward_candidates[j] = $root.lq.AmuletEffectCandidate.toObject(message.level_reward_candidates[j], options);
            }
            if (message.level_reward_packs && message.level_reward_packs.length) {
                object.level_reward_packs = Array(message.level_reward_packs.length);
                for (let j = 0; j < message.level_reward_packs.length; ++j)
                    object.level_reward_packs[j] = message.level_reward_packs[j];
            }
            if (message.current_level_reward_pack != null && message.hasOwnProperty("current_level_reward_pack"))
                object.current_level_reward_pack = message.current_level_reward_pack;
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume"))
                object.max_effect_volume = message.max_effect_volume;
            return object;
        };

        AmuletGameEffectData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameEffectData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameEffectData";
        };

        return AmuletGameEffectData;
    })();

    lq.AmuletShopData = (function() {

        function AmuletShopData(properties) {
            this.goods = [];
            this.candidate_effect_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletShopData.prototype.goods = $util.emptyArray;
        AmuletShopData.prototype.candidate_effect_list = $util.emptyArray;
        AmuletShopData.prototype.shop_refresh_count = 0;
        AmuletShopData.prototype.refresh_price = 0;

        AmuletShopData.create = function create(properties) {
            return new AmuletShopData(properties);
        };

        AmuletShopData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods != null && message.goods.length)
                for (let i = 0; i < message.goods.length; ++i)
                    $root.lq.AmuletGameShopGoods.encode(message.goods[i], writer.uint32(10).fork()).ldelim();
            if (message.candidate_effect_list != null && message.candidate_effect_list.length)
                for (let i = 0; i < message.candidate_effect_list.length; ++i)
                    $root.lq.AmuletEffectCandidate.encode(message.candidate_effect_list[i], writer.uint32(18).fork()).ldelim();
            if (message.shop_refresh_count != null && Object.hasOwnProperty.call(message, "shop_refresh_count"))
                writer.uint32(24).uint32(message.shop_refresh_count);
            if (message.refresh_price != null && Object.hasOwnProperty.call(message, "refresh_price"))
                writer.uint32(32).uint32(message.refresh_price);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletShopData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletShopData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletShopData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.goods && message.goods.length))
                            message.goods = [];
                        message.goods.push($root.lq.AmuletGameShopGoods.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.candidate_effect_list && message.candidate_effect_list.length))
                            message.candidate_effect_list = [];
                        message.candidate_effect_list.push($root.lq.AmuletEffectCandidate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shop_refresh_count = value;
                        else
                            delete message.shop_refresh_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.refresh_price = value;
                        else
                            delete message.refresh_price;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletShopData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletShopData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods != null && message.hasOwnProperty("goods")) {
                if (!Array.isArray(message.goods))
                    return "goods: array expected";
                for (let i = 0; i < message.goods.length; ++i) {
                    let error = $root.lq.AmuletGameShopGoods.verify(message.goods[i], _depth + 1);
                    if (error)
                        return "goods." + error;
                }
            }
            if (message.candidate_effect_list != null && message.hasOwnProperty("candidate_effect_list")) {
                if (!Array.isArray(message.candidate_effect_list))
                    return "candidate_effect_list: array expected";
                for (let i = 0; i < message.candidate_effect_list.length; ++i) {
                    let error = $root.lq.AmuletEffectCandidate.verify(message.candidate_effect_list[i], _depth + 1);
                    if (error)
                        return "candidate_effect_list." + error;
                }
            }
            if (message.shop_refresh_count != null && message.hasOwnProperty("shop_refresh_count"))
                if (!$util.isInteger(message.shop_refresh_count))
                    return "shop_refresh_count: integer expected";
            if (message.refresh_price != null && message.hasOwnProperty("refresh_price"))
                if (!$util.isInteger(message.refresh_price))
                    return "refresh_price: integer expected";
            return null;
        };

        AmuletShopData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletShopData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletShopData();
            if (object.goods) {
                if (!Array.isArray(object.goods))
                    throw TypeError(".lq.AmuletShopData.goods: array expected");
                message.goods = Array(object.goods.length);
                for (let i = 0; i < object.goods.length; ++i) {
                    if (typeof object.goods[i] !== "object")
                        throw TypeError(".lq.AmuletShopData.goods: object expected");
                    message.goods[i] = $root.lq.AmuletGameShopGoods.fromObject(object.goods[i], _depth + 1);
                }
            }
            if (object.candidate_effect_list) {
                if (!Array.isArray(object.candidate_effect_list))
                    throw TypeError(".lq.AmuletShopData.candidate_effect_list: array expected");
                message.candidate_effect_list = Array(object.candidate_effect_list.length);
                for (let i = 0; i < object.candidate_effect_list.length; ++i) {
                    if (typeof object.candidate_effect_list[i] !== "object")
                        throw TypeError(".lq.AmuletShopData.candidate_effect_list: object expected");
                    message.candidate_effect_list[i] = $root.lq.AmuletEffectCandidate.fromObject(object.candidate_effect_list[i], _depth + 1);
                }
            }
            if (object.shop_refresh_count != null)
                if (Number(object.shop_refresh_count) !== 0)
                    message.shop_refresh_count = object.shop_refresh_count >>> 0;
            if (object.refresh_price != null)
                if (Number(object.refresh_price) !== 0)
                    message.refresh_price = object.refresh_price >>> 0;
            return message;
        };

        AmuletShopData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.goods = [];
                object.candidate_effect_list = [];
            }
            if (options.defaults) {
                object.shop_refresh_count = 0;
                object.refresh_price = 0;
            }
            if (message.goods && message.goods.length) {
                object.goods = Array(message.goods.length);
                for (let j = 0; j < message.goods.length; ++j)
                    object.goods[j] = $root.lq.AmuletGameShopGoods.toObject(message.goods[j], options);
            }
            if (message.candidate_effect_list && message.candidate_effect_list.length) {
                object.candidate_effect_list = Array(message.candidate_effect_list.length);
                for (let j = 0; j < message.candidate_effect_list.length; ++j)
                    object.candidate_effect_list[j] = $root.lq.AmuletEffectCandidate.toObject(message.candidate_effect_list[j], options);
            }
            if (message.shop_refresh_count != null && message.hasOwnProperty("shop_refresh_count"))
                object.shop_refresh_count = message.shop_refresh_count;
            if (message.refresh_price != null && message.hasOwnProperty("refresh_price"))
                object.refresh_price = message.refresh_price;
            return object;
        };

        AmuletShopData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletShopData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletShopData";
        };

        return AmuletShopData;
    })();

    lq.AmuletGameData = (function() {

        function AmuletGameData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameData.prototype.round = null;
        AmuletGameData.prototype.effect = null;
        AmuletGameData.prototype.game = null;
        AmuletGameData.prototype.stage = 0;
        AmuletGameData.prototype.shop = null;
        AmuletGameData.prototype.record = null;
        AmuletGameData.prototype.ended = false;

        AmuletGameData.create = function create(properties) {
            return new AmuletGameData(properties);
        };

        AmuletGameData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                $root.lq.AmuletGameRoundData.encode(message.round, writer.uint32(10).fork()).ldelim();
            if (message.effect != null && Object.hasOwnProperty.call(message, "effect"))
                $root.lq.AmuletGameEffectData.encode(message.effect, writer.uint32(18).fork()).ldelim();
            if (message.game != null && Object.hasOwnProperty.call(message, "game"))
                $root.lq.AmuletGameInfoData.encode(message.game, writer.uint32(26).fork()).ldelim();
            if (message.stage != null && Object.hasOwnProperty.call(message, "stage"))
                writer.uint32(32).uint32(message.stage);
            if (message.shop != null && Object.hasOwnProperty.call(message, "shop"))
                $root.lq.AmuletShopData.encode(message.shop, writer.uint32(42).fork()).ldelim();
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.AmuletGameRecordData.encode(message.record, writer.uint32(50).fork()).ldelim();
            if (message.ended != null && Object.hasOwnProperty.call(message, "ended"))
                writer.uint32(56).bool(message.ended);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.round = $root.lq.AmuletGameRoundData.decode(reader, reader.uint32(), undefined, _depth + 1, message.round);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.effect = $root.lq.AmuletGameEffectData.decode(reader, reader.uint32(), undefined, _depth + 1, message.effect);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game = $root.lq.AmuletGameInfoData.decode(reader, reader.uint32(), undefined, _depth + 1, message.game);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stage = value;
                        else
                            delete message.stage;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.shop = $root.lq.AmuletShopData.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.AmuletGameRecordData.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round != null && message.hasOwnProperty("round")) {
                let error = $root.lq.AmuletGameRoundData.verify(message.round, _depth + 1);
                if (error)
                    return "round." + error;
            }
            if (message.effect != null && message.hasOwnProperty("effect")) {
                let error = $root.lq.AmuletGameEffectData.verify(message.effect, _depth + 1);
                if (error)
                    return "effect." + error;
            }
            if (message.game != null && message.hasOwnProperty("game")) {
                let error = $root.lq.AmuletGameInfoData.verify(message.game, _depth + 1);
                if (error)
                    return "game." + error;
            }
            if (message.stage != null && message.hasOwnProperty("stage"))
                if (!$util.isInteger(message.stage))
                    return "stage: integer expected";
            if (message.shop != null && message.hasOwnProperty("shop")) {
                let error = $root.lq.AmuletShopData.verify(message.shop, _depth + 1);
                if (error)
                    return "shop." + error;
            }
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.AmuletGameRecordData.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.ended != null && message.hasOwnProperty("ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            return null;
        };

        AmuletGameData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameData();
            if (object.round != null) {
                if (typeof object.round !== "object")
                    throw TypeError(".lq.AmuletGameData.round: object expected");
                message.round = $root.lq.AmuletGameRoundData.fromObject(object.round, _depth + 1);
            }
            if (object.effect != null) {
                if (typeof object.effect !== "object")
                    throw TypeError(".lq.AmuletGameData.effect: object expected");
                message.effect = $root.lq.AmuletGameEffectData.fromObject(object.effect, _depth + 1);
            }
            if (object.game != null) {
                if (typeof object.game !== "object")
                    throw TypeError(".lq.AmuletGameData.game: object expected");
                message.game = $root.lq.AmuletGameInfoData.fromObject(object.game, _depth + 1);
            }
            if (object.stage != null)
                if (Number(object.stage) !== 0)
                    message.stage = object.stage >>> 0;
            if (object.shop != null) {
                if (typeof object.shop !== "object")
                    throw TypeError(".lq.AmuletGameData.shop: object expected");
                message.shop = $root.lq.AmuletShopData.fromObject(object.shop, _depth + 1);
            }
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.AmuletGameData.record: object expected");
                message.record = $root.lq.AmuletGameRecordData.fromObject(object.record, _depth + 1);
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = Boolean(object.ended);
            return message;
        };

        AmuletGameData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.round = null;
                object.effect = null;
                object.game = null;
                object.stage = 0;
                object.shop = null;
                object.record = null;
                object.ended = false;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = $root.lq.AmuletGameRoundData.toObject(message.round, options);
            if (message.effect != null && message.hasOwnProperty("effect"))
                object.effect = $root.lq.AmuletGameEffectData.toObject(message.effect, options);
            if (message.game != null && message.hasOwnProperty("game"))
                object.game = $root.lq.AmuletGameInfoData.toObject(message.game, options);
            if (message.stage != null && message.hasOwnProperty("stage"))
                object.stage = message.stage;
            if (message.shop != null && message.hasOwnProperty("shop"))
                object.shop = $root.lq.AmuletShopData.toObject(message.shop, options);
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.AmuletGameRecordData.toObject(message.record, options);
            if (message.ended != null && message.hasOwnProperty("ended"))
                object.ended = message.ended;
            return object;
        };

        AmuletGameData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameData";
        };

        return AmuletGameData;
    })();

    lq.AmuletGameInfoData = (function() {

        function AmuletGameInfoData(properties) {
            this.next_boss_buff = [];
            this.boss_buff = [];
            this.tile_score_map = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameInfoData.prototype.level = 0;
        AmuletGameInfoData.prototype.coin = "";
        AmuletGameInfoData.prototype.max_effect_volume = 0;
        AmuletGameInfoData.prototype.next_boss_buff = $util.emptyArray;
        AmuletGameInfoData.prototype.boss_buff = $util.emptyArray;
        AmuletGameInfoData.prototype.tile_score_map = $util.emptyArray;
        AmuletGameInfoData.prototype.book_effect_id = 0;

        AmuletGameInfoData.create = function create(properties) {
            return new AmuletGameInfoData(properties);
        };

        AmuletGameInfoData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(8).uint32(message.level);
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                writer.uint32(18).string(message.coin);
            if (message.max_effect_volume != null && Object.hasOwnProperty.call(message, "max_effect_volume"))
                writer.uint32(24).uint32(message.max_effect_volume);
            if (message.next_boss_buff != null && message.next_boss_buff.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.next_boss_buff.length; ++i)
                    writer.uint32(message.next_boss_buff[i]);
                writer.ldelim();
            }
            if (message.boss_buff != null && message.boss_buff.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.boss_buff.length; ++i)
                    writer.uint32(message.boss_buff[i]);
                writer.ldelim();
            }
            if (message.tile_score_map != null && message.tile_score_map.length)
                for (let i = 0; i < message.tile_score_map.length; ++i)
                    $root.lq.AmuletTileScore.encode(message.tile_score_map[i], writer.uint32(50).fork()).ldelim();
            if (message.book_effect_id != null && Object.hasOwnProperty.call(message, "book_effect_id"))
                writer.uint32(64).uint32(message.book_effect_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameInfoData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameInfoData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameInfoData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.coin = value;
                        else
                            delete message.coin;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.max_effect_volume = value;
                        else
                            delete message.max_effect_volume;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.next_boss_buff && message.next_boss_buff.length))
                                message.next_boss_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.next_boss_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.next_boss_buff && message.next_boss_buff.length))
                            message.next_boss_buff = [];
                        message.next_boss_buff.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.boss_buff && message.boss_buff.length))
                                message.boss_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.boss_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.boss_buff && message.boss_buff.length))
                            message.boss_buff = [];
                        message.boss_buff.push(reader.uint32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tile_score_map && message.tile_score_map.length))
                            message.tile_score_map = [];
                        message.tile_score_map.push($root.lq.AmuletTileScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.book_effect_id = value;
                        else
                            delete message.book_effect_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameInfoData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameInfoData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.coin != null && message.hasOwnProperty("coin"))
                if (!$util.isString(message.coin))
                    return "coin: string expected";
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume"))
                if (!$util.isInteger(message.max_effect_volume))
                    return "max_effect_volume: integer expected";
            if (message.next_boss_buff != null && message.hasOwnProperty("next_boss_buff")) {
                if (!Array.isArray(message.next_boss_buff))
                    return "next_boss_buff: array expected";
                for (let i = 0; i < message.next_boss_buff.length; ++i)
                    if (!$util.isInteger(message.next_boss_buff[i]))
                        return "next_boss_buff: integer[] expected";
            }
            if (message.boss_buff != null && message.hasOwnProperty("boss_buff")) {
                if (!Array.isArray(message.boss_buff))
                    return "boss_buff: array expected";
                for (let i = 0; i < message.boss_buff.length; ++i)
                    if (!$util.isInteger(message.boss_buff[i]))
                        return "boss_buff: integer[] expected";
            }
            if (message.tile_score_map != null && message.hasOwnProperty("tile_score_map")) {
                if (!Array.isArray(message.tile_score_map))
                    return "tile_score_map: array expected";
                for (let i = 0; i < message.tile_score_map.length; ++i) {
                    let error = $root.lq.AmuletTileScore.verify(message.tile_score_map[i], _depth + 1);
                    if (error)
                        return "tile_score_map." + error;
                }
            }
            if (message.book_effect_id != null && message.hasOwnProperty("book_effect_id"))
                if (!$util.isInteger(message.book_effect_id))
                    return "book_effect_id: integer expected";
            return null;
        };

        AmuletGameInfoData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameInfoData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameInfoData();
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.coin != null)
                if (typeof object.coin !== "string" || object.coin.length)
                    message.coin = String(object.coin);
            if (object.max_effect_volume != null)
                if (Number(object.max_effect_volume) !== 0)
                    message.max_effect_volume = object.max_effect_volume >>> 0;
            if (object.next_boss_buff) {
                if (!Array.isArray(object.next_boss_buff))
                    throw TypeError(".lq.AmuletGameInfoData.next_boss_buff: array expected");
                message.next_boss_buff = Array(object.next_boss_buff.length);
                for (let i = 0; i < object.next_boss_buff.length; ++i)
                    message.next_boss_buff[i] = object.next_boss_buff[i] >>> 0;
            }
            if (object.boss_buff) {
                if (!Array.isArray(object.boss_buff))
                    throw TypeError(".lq.AmuletGameInfoData.boss_buff: array expected");
                message.boss_buff = Array(object.boss_buff.length);
                for (let i = 0; i < object.boss_buff.length; ++i)
                    message.boss_buff[i] = object.boss_buff[i] >>> 0;
            }
            if (object.tile_score_map) {
                if (!Array.isArray(object.tile_score_map))
                    throw TypeError(".lq.AmuletGameInfoData.tile_score_map: array expected");
                message.tile_score_map = Array(object.tile_score_map.length);
                for (let i = 0; i < object.tile_score_map.length; ++i) {
                    if (typeof object.tile_score_map[i] !== "object")
                        throw TypeError(".lq.AmuletGameInfoData.tile_score_map: object expected");
                    message.tile_score_map[i] = $root.lq.AmuletTileScore.fromObject(object.tile_score_map[i], _depth + 1);
                }
            }
            if (object.book_effect_id != null)
                if (Number(object.book_effect_id) !== 0)
                    message.book_effect_id = object.book_effect_id >>> 0;
            return message;
        };

        AmuletGameInfoData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.next_boss_buff = [];
                object.boss_buff = [];
                object.tile_score_map = [];
            }
            if (options.defaults) {
                object.level = 0;
                object.coin = "";
                object.max_effect_volume = 0;
                object.book_effect_id = 0;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = message.coin;
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume"))
                object.max_effect_volume = message.max_effect_volume;
            if (message.next_boss_buff && message.next_boss_buff.length) {
                object.next_boss_buff = Array(message.next_boss_buff.length);
                for (let j = 0; j < message.next_boss_buff.length; ++j)
                    object.next_boss_buff[j] = message.next_boss_buff[j];
            }
            if (message.boss_buff && message.boss_buff.length) {
                object.boss_buff = Array(message.boss_buff.length);
                for (let j = 0; j < message.boss_buff.length; ++j)
                    object.boss_buff[j] = message.boss_buff[j];
            }
            if (message.tile_score_map && message.tile_score_map.length) {
                object.tile_score_map = Array(message.tile_score_map.length);
                for (let j = 0; j < message.tile_score_map.length; ++j)
                    object.tile_score_map[j] = $root.lq.AmuletTileScore.toObject(message.tile_score_map[j], options);
            }
            if (message.book_effect_id != null && message.hasOwnProperty("book_effect_id"))
                object.book_effect_id = message.book_effect_id;
            return object;
        };

        AmuletGameInfoData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameInfoData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameInfoData";
        };

        return AmuletGameInfoData;
    })();

    lq.ActivityAmuletIllustratedBookData = (function() {

        function ActivityAmuletIllustratedBookData(properties) {
            this.effect_collection = [];
            this.badge_collection = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletIllustratedBookData.prototype.effect_collection = $util.emptyArray;
        ActivityAmuletIllustratedBookData.prototype.badge_collection = $util.emptyArray;

        ActivityAmuletIllustratedBookData.create = function create(properties) {
            return new ActivityAmuletIllustratedBookData(properties);
        };

        ActivityAmuletIllustratedBookData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.effect_collection != null && message.effect_collection.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.effect_collection.length; ++i)
                    writer.uint32(message.effect_collection[i]);
                writer.ldelim();
            }
            if (message.badge_collection != null && message.badge_collection.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.badge_collection.length; ++i)
                    writer.uint32(message.badge_collection[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletIllustratedBookData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletIllustratedBookData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletIllustratedBookData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.effect_collection && message.effect_collection.length))
                                message.effect_collection = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.effect_collection.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.effect_collection && message.effect_collection.length))
                            message.effect_collection = [];
                        message.effect_collection.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.badge_collection && message.badge_collection.length))
                                message.badge_collection = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.badge_collection.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.badge_collection && message.badge_collection.length))
                            message.badge_collection = [];
                        message.badge_collection.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletIllustratedBookData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletIllustratedBookData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.effect_collection != null && message.hasOwnProperty("effect_collection")) {
                if (!Array.isArray(message.effect_collection))
                    return "effect_collection: array expected";
                for (let i = 0; i < message.effect_collection.length; ++i)
                    if (!$util.isInteger(message.effect_collection[i]))
                        return "effect_collection: integer[] expected";
            }
            if (message.badge_collection != null && message.hasOwnProperty("badge_collection")) {
                if (!Array.isArray(message.badge_collection))
                    return "badge_collection: array expected";
                for (let i = 0; i < message.badge_collection.length; ++i)
                    if (!$util.isInteger(message.badge_collection[i]))
                        return "badge_collection: integer[] expected";
            }
            return null;
        };

        ActivityAmuletIllustratedBookData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletIllustratedBookData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletIllustratedBookData();
            if (object.effect_collection) {
                if (!Array.isArray(object.effect_collection))
                    throw TypeError(".lq.ActivityAmuletIllustratedBookData.effect_collection: array expected");
                message.effect_collection = Array(object.effect_collection.length);
                for (let i = 0; i < object.effect_collection.length; ++i)
                    message.effect_collection[i] = object.effect_collection[i] >>> 0;
            }
            if (object.badge_collection) {
                if (!Array.isArray(object.badge_collection))
                    throw TypeError(".lq.ActivityAmuletIllustratedBookData.badge_collection: array expected");
                message.badge_collection = Array(object.badge_collection.length);
                for (let i = 0; i < object.badge_collection.length; ++i)
                    message.badge_collection[i] = object.badge_collection[i] >>> 0;
            }
            return message;
        };

        ActivityAmuletIllustratedBookData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effect_collection = [];
                object.badge_collection = [];
            }
            if (message.effect_collection && message.effect_collection.length) {
                object.effect_collection = Array(message.effect_collection.length);
                for (let j = 0; j < message.effect_collection.length; ++j)
                    object.effect_collection[j] = message.effect_collection[j];
            }
            if (message.badge_collection && message.badge_collection.length) {
                object.badge_collection = Array(message.badge_collection.length);
                for (let j = 0; j < message.badge_collection.length; ++j)
                    object.badge_collection[j] = message.badge_collection[j];
            }
            return object;
        };

        ActivityAmuletIllustratedBookData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletIllustratedBookData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletIllustratedBookData";
        };

        return ActivityAmuletIllustratedBookData;
    })();

    lq.ActivityAmuletEffectRecordData = (function() {

        function ActivityAmuletEffectRecordData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletEffectRecordData.prototype.id = 0;
        ActivityAmuletEffectRecordData.prototype.badge_id = 0;
        ActivityAmuletEffectRecordData.prototype.volume = 0;

        ActivityAmuletEffectRecordData.create = function create(properties) {
            return new ActivityAmuletEffectRecordData(properties);
        };

        ActivityAmuletEffectRecordData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.badge_id != null && Object.hasOwnProperty.call(message, "badge_id"))
                writer.uint32(16).uint32(message.badge_id);
            if (message.volume != null && Object.hasOwnProperty.call(message, "volume"))
                writer.uint32(24).uint32(message.volume);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletEffectRecordData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletEffectRecordData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletEffectRecordData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.badge_id = value;
                        else
                            delete message.badge_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.volume = value;
                        else
                            delete message.volume;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletEffectRecordData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletEffectRecordData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                if (!$util.isInteger(message.badge_id))
                    return "badge_id: integer expected";
            if (message.volume != null && message.hasOwnProperty("volume"))
                if (!$util.isInteger(message.volume))
                    return "volume: integer expected";
            return null;
        };

        ActivityAmuletEffectRecordData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletEffectRecordData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletEffectRecordData();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.badge_id != null)
                if (Number(object.badge_id) !== 0)
                    message.badge_id = object.badge_id >>> 0;
            if (object.volume != null)
                if (Number(object.volume) !== 0)
                    message.volume = object.volume >>> 0;
            return message;
        };

        ActivityAmuletEffectRecordData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.badge_id = 0;
                object.volume = 0;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                object.badge_id = message.badge_id;
            if (message.volume != null && message.hasOwnProperty("volume"))
                object.volume = message.volume;
            return object;
        };

        ActivityAmuletEffectRecordData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletEffectRecordData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletEffectRecordData";
        };

        return ActivityAmuletEffectRecordData;
    })();

    lq.ActivityAmuletGameRecordData = (function() {

        function ActivityAmuletGameRecordData(properties) {
            this.effect_builds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletGameRecordData.prototype.effect_builds = $util.emptyArray;
        ActivityAmuletGameRecordData.prototype.level = 0;
        ActivityAmuletGameRecordData.prototype.highest_level_score = "";
        ActivityAmuletGameRecordData.prototype.highest_fan = "";
        ActivityAmuletGameRecordData.prototype.highest_score = "";
        ActivityAmuletGameRecordData.prototype.coin_consumed = "";
        ActivityAmuletGameRecordData.prototype.pack_count = 0;
        ActivityAmuletGameRecordData.prototype.time = 0;
        ActivityAmuletGameRecordData.prototype.highest_hu = null;

        ActivityAmuletGameRecordData.create = function create(properties) {
            return new ActivityAmuletGameRecordData(properties);
        };

        ActivityAmuletGameRecordData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.effect_builds != null && message.effect_builds.length)
                for (let i = 0; i < message.effect_builds.length; ++i)
                    $root.lq.ActivityAmuletEffectRecordData.encode(message.effect_builds[i], writer.uint32(10).fork()).ldelim();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                writer.uint32(16).uint32(message.level);
            if (message.highest_level_score != null && Object.hasOwnProperty.call(message, "highest_level_score"))
                writer.uint32(26).string(message.highest_level_score);
            if (message.highest_fan != null && Object.hasOwnProperty.call(message, "highest_fan"))
                writer.uint32(34).string(message.highest_fan);
            if (message.highest_score != null && Object.hasOwnProperty.call(message, "highest_score"))
                writer.uint32(42).string(message.highest_score);
            if (message.coin_consumed != null && Object.hasOwnProperty.call(message, "coin_consumed"))
                writer.uint32(50).string(message.coin_consumed);
            if (message.pack_count != null && Object.hasOwnProperty.call(message, "pack_count"))
                writer.uint32(56).uint32(message.pack_count);
            if (message.time != null && Object.hasOwnProperty.call(message, "time"))
                writer.uint32(64).uint32(message.time);
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                $root.lq.ActivityAmuletHuRecord.encode(message.highest_hu, writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletGameRecordData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletGameRecordData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletGameRecordData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effect_builds && message.effect_builds.length))
                            message.effect_builds = [];
                        message.effect_builds.push($root.lq.ActivityAmuletEffectRecordData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.level = value;
                        else
                            delete message.level;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_level_score = value;
                        else
                            delete message.highest_level_score;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_fan = value;
                        else
                            delete message.highest_fan;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_score = value;
                        else
                            delete message.highest_score;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.coin_consumed = value;
                        else
                            delete message.coin_consumed;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pack_count = value;
                        else
                            delete message.pack_count;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.highest_hu = $root.lq.ActivityAmuletHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_hu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletGameRecordData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletGameRecordData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.effect_builds != null && message.hasOwnProperty("effect_builds")) {
                if (!Array.isArray(message.effect_builds))
                    return "effect_builds: array expected";
                for (let i = 0; i < message.effect_builds.length; ++i) {
                    let error = $root.lq.ActivityAmuletEffectRecordData.verify(message.effect_builds[i], _depth + 1);
                    if (error)
                        return "effect_builds." + error;
                }
            }
            if (message.level != null && message.hasOwnProperty("level"))
                if (!$util.isInteger(message.level))
                    return "level: integer expected";
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                if (!$util.isString(message.highest_level_score))
                    return "highest_level_score: string expected";
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                if (!$util.isString(message.highest_fan))
                    return "highest_fan: string expected";
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                if (!$util.isString(message.highest_score))
                    return "highest_score: string expected";
            if (message.coin_consumed != null && message.hasOwnProperty("coin_consumed"))
                if (!$util.isString(message.coin_consumed))
                    return "coin_consumed: string expected";
            if (message.pack_count != null && message.hasOwnProperty("pack_count"))
                if (!$util.isInteger(message.pack_count))
                    return "pack_count: integer expected";
            if (message.time != null && message.hasOwnProperty("time"))
                if (!$util.isInteger(message.time))
                    return "time: integer expected";
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu")) {
                let error = $root.lq.ActivityAmuletHuRecord.verify(message.highest_hu, _depth + 1);
                if (error)
                    return "highest_hu." + error;
            }
            return null;
        };

        ActivityAmuletGameRecordData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletGameRecordData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletGameRecordData();
            if (object.effect_builds) {
                if (!Array.isArray(object.effect_builds))
                    throw TypeError(".lq.ActivityAmuletGameRecordData.effect_builds: array expected");
                message.effect_builds = Array(object.effect_builds.length);
                for (let i = 0; i < object.effect_builds.length; ++i) {
                    if (typeof object.effect_builds[i] !== "object")
                        throw TypeError(".lq.ActivityAmuletGameRecordData.effect_builds: object expected");
                    message.effect_builds[i] = $root.lq.ActivityAmuletEffectRecordData.fromObject(object.effect_builds[i], _depth + 1);
                }
            }
            if (object.level != null)
                if (Number(object.level) !== 0)
                    message.level = object.level >>> 0;
            if (object.highest_level_score != null)
                if (typeof object.highest_level_score !== "string" || object.highest_level_score.length)
                    message.highest_level_score = String(object.highest_level_score);
            if (object.highest_fan != null)
                if (typeof object.highest_fan !== "string" || object.highest_fan.length)
                    message.highest_fan = String(object.highest_fan);
            if (object.highest_score != null)
                if (typeof object.highest_score !== "string" || object.highest_score.length)
                    message.highest_score = String(object.highest_score);
            if (object.coin_consumed != null)
                if (typeof object.coin_consumed !== "string" || object.coin_consumed.length)
                    message.coin_consumed = String(object.coin_consumed);
            if (object.pack_count != null)
                if (Number(object.pack_count) !== 0)
                    message.pack_count = object.pack_count >>> 0;
            if (object.time != null)
                if (Number(object.time) !== 0)
                    message.time = object.time >>> 0;
            if (object.highest_hu != null) {
                if (typeof object.highest_hu !== "object")
                    throw TypeError(".lq.ActivityAmuletGameRecordData.highest_hu: object expected");
                message.highest_hu = $root.lq.ActivityAmuletHuRecord.fromObject(object.highest_hu, _depth + 1);
            }
            return message;
        };

        ActivityAmuletGameRecordData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.effect_builds = [];
            if (options.defaults) {
                object.level = 0;
                object.highest_level_score = "";
                object.highest_fan = "";
                object.highest_score = "";
                object.coin_consumed = "";
                object.pack_count = 0;
                object.time = 0;
                object.highest_hu = null;
            }
            if (message.effect_builds && message.effect_builds.length) {
                object.effect_builds = Array(message.effect_builds.length);
                for (let j = 0; j < message.effect_builds.length; ++j)
                    object.effect_builds[j] = $root.lq.ActivityAmuletEffectRecordData.toObject(message.effect_builds[j], options);
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = message.level;
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                object.highest_level_score = message.highest_level_score;
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                object.highest_fan = message.highest_fan;
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                object.highest_score = message.highest_score;
            if (message.coin_consumed != null && message.hasOwnProperty("coin_consumed"))
                object.coin_consumed = message.coin_consumed;
            if (message.pack_count != null && message.hasOwnProperty("pack_count"))
                object.pack_count = message.pack_count;
            if (message.time != null && message.hasOwnProperty("time"))
                object.time = message.time;
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = $root.lq.ActivityAmuletHuRecord.toObject(message.highest_hu, options);
            return object;
        };

        ActivityAmuletGameRecordData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletGameRecordData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletGameRecordData";
        };

        return ActivityAmuletGameRecordData;
    })();

    lq.ActivityAmuletStatisticData = (function() {

        function ActivityAmuletStatisticData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletStatisticData.prototype.highest_level = 0;
        ActivityAmuletStatisticData.prototype.highest_hu = null;
        ActivityAmuletStatisticData.prototype.highest_level_score = "";
        ActivityAmuletStatisticData.prototype.highest_fan = "";
        ActivityAmuletStatisticData.prototype.highest_score = "";
        ActivityAmuletStatisticData.prototype.pass_game_count = 0;
        ActivityAmuletStatisticData.prototype.round_count = 0;
        ActivityAmuletStatisticData.prototype.open_pack_count = 0;
        ActivityAmuletStatisticData.prototype.highest_coin_consumed = "";

        ActivityAmuletStatisticData.create = function create(properties) {
            return new ActivityAmuletStatisticData(properties);
        };

        ActivityAmuletStatisticData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.highest_level != null && Object.hasOwnProperty.call(message, "highest_level"))
                writer.uint32(8).uint32(message.highest_level);
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                $root.lq.ActivityAmuletHuRecord.encode(message.highest_hu, writer.uint32(18).fork()).ldelim();
            if (message.highest_level_score != null && Object.hasOwnProperty.call(message, "highest_level_score"))
                writer.uint32(26).string(message.highest_level_score);
            if (message.highest_fan != null && Object.hasOwnProperty.call(message, "highest_fan"))
                writer.uint32(34).string(message.highest_fan);
            if (message.highest_score != null && Object.hasOwnProperty.call(message, "highest_score"))
                writer.uint32(42).string(message.highest_score);
            if (message.pass_game_count != null && Object.hasOwnProperty.call(message, "pass_game_count"))
                writer.uint32(48).uint32(message.pass_game_count);
            if (message.round_count != null && Object.hasOwnProperty.call(message, "round_count"))
                writer.uint32(56).uint32(message.round_count);
            if (message.open_pack_count != null && Object.hasOwnProperty.call(message, "open_pack_count"))
                writer.uint32(64).uint32(message.open_pack_count);
            if (message.highest_coin_consumed != null && Object.hasOwnProperty.call(message, "highest_coin_consumed"))
                writer.uint32(74).string(message.highest_coin_consumed);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletStatisticData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletStatisticData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletStatisticData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.highest_level = value;
                        else
                            delete message.highest_level;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.highest_hu = $root.lq.ActivityAmuletHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_hu);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_level_score = value;
                        else
                            delete message.highest_level_score;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_fan = value;
                        else
                            delete message.highest_fan;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_score = value;
                        else
                            delete message.highest_score;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pass_game_count = value;
                        else
                            delete message.pass_game_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.round_count = value;
                        else
                            delete message.round_count;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.open_pack_count = value;
                        else
                            delete message.open_pack_count;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.highest_coin_consumed = value;
                        else
                            delete message.highest_coin_consumed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletStatisticData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletStatisticData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.highest_level != null && message.hasOwnProperty("highest_level"))
                if (!$util.isInteger(message.highest_level))
                    return "highest_level: integer expected";
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu")) {
                let error = $root.lq.ActivityAmuletHuRecord.verify(message.highest_hu, _depth + 1);
                if (error)
                    return "highest_hu." + error;
            }
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                if (!$util.isString(message.highest_level_score))
                    return "highest_level_score: string expected";
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                if (!$util.isString(message.highest_fan))
                    return "highest_fan: string expected";
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                if (!$util.isString(message.highest_score))
                    return "highest_score: string expected";
            if (message.pass_game_count != null && message.hasOwnProperty("pass_game_count"))
                if (!$util.isInteger(message.pass_game_count))
                    return "pass_game_count: integer expected";
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                if (!$util.isInteger(message.round_count))
                    return "round_count: integer expected";
            if (message.open_pack_count != null && message.hasOwnProperty("open_pack_count"))
                if (!$util.isInteger(message.open_pack_count))
                    return "open_pack_count: integer expected";
            if (message.highest_coin_consumed != null && message.hasOwnProperty("highest_coin_consumed"))
                if (!$util.isString(message.highest_coin_consumed))
                    return "highest_coin_consumed: string expected";
            return null;
        };

        ActivityAmuletStatisticData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletStatisticData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletStatisticData();
            if (object.highest_level != null)
                if (Number(object.highest_level) !== 0)
                    message.highest_level = object.highest_level >>> 0;
            if (object.highest_hu != null) {
                if (typeof object.highest_hu !== "object")
                    throw TypeError(".lq.ActivityAmuletStatisticData.highest_hu: object expected");
                message.highest_hu = $root.lq.ActivityAmuletHuRecord.fromObject(object.highest_hu, _depth + 1);
            }
            if (object.highest_level_score != null)
                if (typeof object.highest_level_score !== "string" || object.highest_level_score.length)
                    message.highest_level_score = String(object.highest_level_score);
            if (object.highest_fan != null)
                if (typeof object.highest_fan !== "string" || object.highest_fan.length)
                    message.highest_fan = String(object.highest_fan);
            if (object.highest_score != null)
                if (typeof object.highest_score !== "string" || object.highest_score.length)
                    message.highest_score = String(object.highest_score);
            if (object.pass_game_count != null)
                if (Number(object.pass_game_count) !== 0)
                    message.pass_game_count = object.pass_game_count >>> 0;
            if (object.round_count != null)
                if (Number(object.round_count) !== 0)
                    message.round_count = object.round_count >>> 0;
            if (object.open_pack_count != null)
                if (Number(object.open_pack_count) !== 0)
                    message.open_pack_count = object.open_pack_count >>> 0;
            if (object.highest_coin_consumed != null)
                if (typeof object.highest_coin_consumed !== "string" || object.highest_coin_consumed.length)
                    message.highest_coin_consumed = String(object.highest_coin_consumed);
            return message;
        };

        ActivityAmuletStatisticData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.highest_level = 0;
                object.highest_hu = null;
                object.highest_level_score = "";
                object.highest_fan = "";
                object.highest_score = "";
                object.pass_game_count = 0;
                object.round_count = 0;
                object.open_pack_count = 0;
                object.highest_coin_consumed = "";
            }
            if (message.highest_level != null && message.hasOwnProperty("highest_level"))
                object.highest_level = message.highest_level;
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = $root.lq.ActivityAmuletHuRecord.toObject(message.highest_hu, options);
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                object.highest_level_score = message.highest_level_score;
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                object.highest_fan = message.highest_fan;
            if (message.highest_score != null && message.hasOwnProperty("highest_score"))
                object.highest_score = message.highest_score;
            if (message.pass_game_count != null && message.hasOwnProperty("pass_game_count"))
                object.pass_game_count = message.pass_game_count;
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                object.round_count = message.round_count;
            if (message.open_pack_count != null && message.hasOwnProperty("open_pack_count"))
                object.open_pack_count = message.open_pack_count;
            if (message.highest_coin_consumed != null && message.hasOwnProperty("highest_coin_consumed"))
                object.highest_coin_consumed = message.highest_coin_consumed;
            return object;
        };

        ActivityAmuletStatisticData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletStatisticData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletStatisticData";
        };

        return ActivityAmuletStatisticData;
    })();

    lq.ActivityAmuletData = (function() {

        function ActivityAmuletData(properties) {
            this.game_records = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletData.prototype.activity_id = 0;
        ActivityAmuletData.prototype.game = null;
        ActivityAmuletData.prototype.version = 0;
        ActivityAmuletData.prototype.upgrade = null;
        ActivityAmuletData.prototype.illustrated_book = null;
        ActivityAmuletData.prototype.book_effect_id = 0;
        ActivityAmuletData.prototype.game_records = $util.emptyArray;
        ActivityAmuletData.prototype.statistic = null;

        ActivityAmuletData.create = function create(properties) {
            return new ActivityAmuletData(properties);
        };

        ActivityAmuletData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                writer.uint32(8).uint32(message.activity_id);
            if (message.game != null && Object.hasOwnProperty.call(message, "game"))
                $root.lq.AmuletGameData.encode(message.game, writer.uint32(18).fork()).ldelim();
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(24).uint32(message.version);
            if (message.upgrade != null && Object.hasOwnProperty.call(message, "upgrade"))
                $root.lq.ActivityAmuletUpgradeData.encode(message.upgrade, writer.uint32(34).fork()).ldelim();
            if (message.illustrated_book != null && Object.hasOwnProperty.call(message, "illustrated_book"))
                $root.lq.ActivityAmuletIllustratedBookData.encode(message.illustrated_book, writer.uint32(42).fork()).ldelim();
            if (message.book_effect_id != null && Object.hasOwnProperty.call(message, "book_effect_id"))
                writer.uint32(48).uint32(message.book_effect_id);
            if (message.game_records != null && message.game_records.length)
                for (let i = 0; i < message.game_records.length; ++i)
                    $root.lq.ActivityAmuletGameRecordData.encode(message.game_records[i], writer.uint32(58).fork()).ldelim();
            if (message.statistic != null && Object.hasOwnProperty.call(message, "statistic"))
                $root.lq.ActivityAmuletStatisticData.encode(message.statistic, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.activity_id = value;
                        else
                            delete message.activity_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.game = $root.lq.AmuletGameData.decode(reader, reader.uint32(), undefined, _depth + 1, message.game);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.upgrade = $root.lq.ActivityAmuletUpgradeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.decode(reader, reader.uint32(), undefined, _depth + 1, message.illustrated_book);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.book_effect_id = value;
                        else
                            delete message.book_effect_id;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.game_records && message.game_records.length))
                            message.game_records = [];
                        message.game_records.push($root.lq.ActivityAmuletGameRecordData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.statistic = $root.lq.ActivityAmuletStatisticData.decode(reader, reader.uint32(), undefined, _depth + 1, message.statistic);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                if (!$util.isInteger(message.activity_id))
                    return "activity_id: integer expected";
            if (message.game != null && message.hasOwnProperty("game")) {
                let error = $root.lq.AmuletGameData.verify(message.game, _depth + 1);
                if (error)
                    return "game." + error;
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.upgrade != null && message.hasOwnProperty("upgrade")) {
                let error = $root.lq.ActivityAmuletUpgradeData.verify(message.upgrade, _depth + 1);
                if (error)
                    return "upgrade." + error;
            }
            if (message.illustrated_book != null && message.hasOwnProperty("illustrated_book")) {
                let error = $root.lq.ActivityAmuletIllustratedBookData.verify(message.illustrated_book, _depth + 1);
                if (error)
                    return "illustrated_book." + error;
            }
            if (message.book_effect_id != null && message.hasOwnProperty("book_effect_id"))
                if (!$util.isInteger(message.book_effect_id))
                    return "book_effect_id: integer expected";
            if (message.game_records != null && message.hasOwnProperty("game_records")) {
                if (!Array.isArray(message.game_records))
                    return "game_records: array expected";
                for (let i = 0; i < message.game_records.length; ++i) {
                    let error = $root.lq.ActivityAmuletGameRecordData.verify(message.game_records[i], _depth + 1);
                    if (error)
                        return "game_records." + error;
                }
            }
            if (message.statistic != null && message.hasOwnProperty("statistic")) {
                let error = $root.lq.ActivityAmuletStatisticData.verify(message.statistic, _depth + 1);
                if (error)
                    return "statistic." + error;
            }
            return null;
        };

        ActivityAmuletData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletData();
            if (object.activity_id != null)
                if (Number(object.activity_id) !== 0)
                    message.activity_id = object.activity_id >>> 0;
            if (object.game != null) {
                if (typeof object.game !== "object")
                    throw TypeError(".lq.ActivityAmuletData.game: object expected");
                message.game = $root.lq.AmuletGameData.fromObject(object.game, _depth + 1);
            }
            if (object.version != null)
                if (Number(object.version) !== 0)
                    message.version = object.version >>> 0;
            if (object.upgrade != null) {
                if (typeof object.upgrade !== "object")
                    throw TypeError(".lq.ActivityAmuletData.upgrade: object expected");
                message.upgrade = $root.lq.ActivityAmuletUpgradeData.fromObject(object.upgrade, _depth + 1);
            }
            if (object.illustrated_book != null) {
                if (typeof object.illustrated_book !== "object")
                    throw TypeError(".lq.ActivityAmuletData.illustrated_book: object expected");
                message.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.fromObject(object.illustrated_book, _depth + 1);
            }
            if (object.book_effect_id != null)
                if (Number(object.book_effect_id) !== 0)
                    message.book_effect_id = object.book_effect_id >>> 0;
            if (object.game_records) {
                if (!Array.isArray(object.game_records))
                    throw TypeError(".lq.ActivityAmuletData.game_records: array expected");
                message.game_records = Array(object.game_records.length);
                for (let i = 0; i < object.game_records.length; ++i) {
                    if (typeof object.game_records[i] !== "object")
                        throw TypeError(".lq.ActivityAmuletData.game_records: object expected");
                    message.game_records[i] = $root.lq.ActivityAmuletGameRecordData.fromObject(object.game_records[i], _depth + 1);
                }
            }
            if (object.statistic != null) {
                if (typeof object.statistic !== "object")
                    throw TypeError(".lq.ActivityAmuletData.statistic: object expected");
                message.statistic = $root.lq.ActivityAmuletStatisticData.fromObject(object.statistic, _depth + 1);
            }
            return message;
        };

        ActivityAmuletData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.game_records = [];
            if (options.defaults) {
                object.activity_id = 0;
                object.game = null;
                object.version = 0;
                object.upgrade = null;
                object.illustrated_book = null;
                object.book_effect_id = 0;
                object.statistic = null;
            }
            if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                object.activity_id = message.activity_id;
            if (message.game != null && message.hasOwnProperty("game"))
                object.game = $root.lq.AmuletGameData.toObject(message.game, options);
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.upgrade != null && message.hasOwnProperty("upgrade"))
                object.upgrade = $root.lq.ActivityAmuletUpgradeData.toObject(message.upgrade, options);
            if (message.illustrated_book != null && message.hasOwnProperty("illustrated_book"))
                object.illustrated_book = $root.lq.ActivityAmuletIllustratedBookData.toObject(message.illustrated_book, options);
            if (message.book_effect_id != null && message.hasOwnProperty("book_effect_id"))
                object.book_effect_id = message.book_effect_id;
            if (message.game_records && message.game_records.length) {
                object.game_records = Array(message.game_records.length);
                for (let j = 0; j < message.game_records.length; ++j)
                    object.game_records[j] = $root.lq.ActivityAmuletGameRecordData.toObject(message.game_records[j], options);
            }
            if (message.statistic != null && message.hasOwnProperty("statistic"))
                object.statistic = $root.lq.ActivityAmuletStatisticData.toObject(message.statistic, options);
            return object;
        };

        ActivityAmuletData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletData";
        };

        return ActivityAmuletData;
    })();

    lq.AmuletFan = (function() {

        function AmuletFan(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletFan.prototype.id = 0;
        AmuletFan.prototype.val = "";
        AmuletFan.prototype.count = 0;
        AmuletFan.prototype.yiman = false;

        AmuletFan.create = function create(properties) {
            return new AmuletFan(properties);
        };

        AmuletFan.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(8).uint32(message.id);
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(18).string(message.val);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(24).uint32(message.count);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(32).bool(message.yiman);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletFan.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletFan.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletFan(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.val = value;
                        else
                            delete message.val;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletFan.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletFan.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isString(message.val))
                    return "val: string expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            return null;
        };

        AmuletFan.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletFan)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletFan();
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.val != null)
                if (typeof object.val !== "string" || object.val.length)
                    message.val = String(object.val);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            return message;
        };

        AmuletFan.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.val = "";
                object.count = 0;
                object.yiman = false;
            }
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            return object;
        };

        AmuletFan.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletFan.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletFan";
        };

        return AmuletFan;
    })();

    lq.AmuletHookResult = (function() {

        function AmuletHookResult(properties) {
            this.add_effect = [];
            this.remove_effect = [];
            this.add_buff = [];
            this.remove_buff = [];
            this.add_tian_dora = [];
            this.tile_replace = [];
            this.add_show_tile = [];
            this.modify_tile_score = [];
            this.modify_dora = [];
            this.modify_shop_pack_effect = [];
            this.modify_goods = [];
            this.remove_goods = [];
            this.modify_fan_info = [];
            this.transform_effect = [];
            this.add_badge = [];
            this.remove_badge = [];
            this.copy_effect = [];
            this.modify_change_hands_list = [];
            this.set_tile_score = [];
            this.upgrade_effect = [];
            this.modify_tile_base_score = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletHookResult.prototype.add_effect = $util.emptyArray;
        AmuletHookResult.prototype.remove_effect = $util.emptyArray;
        AmuletHookResult.prototype.add_buff = $util.emptyArray;
        AmuletHookResult.prototype.remove_buff = $util.emptyArray;
        AmuletHookResult.prototype.add_tian_dora = $util.emptyArray;
        AmuletHookResult.prototype.add_dora = null;
        AmuletHookResult.prototype.coin_modify = null;
        AmuletHookResult.prototype.tile_replace = $util.emptyArray;
        AmuletHookResult.prototype.add_show_tile = $util.emptyArray;
        AmuletHookResult.prototype.modify_tile_score = $util.emptyArray;
        AmuletHookResult.prototype.modify_desktop_count = 0;
        AmuletHookResult.prototype.modify_show_desktop_count = 0;
        AmuletHookResult.prototype.modify_lock_tile_count = 0;
        AmuletHookResult.prototype.modify_change_hands_count = 0;
        AmuletHookResult.prototype.modify_change_hands_tile_count = 0;
        AmuletHookResult.prototype.force_moqie = false;
        AmuletHookResult.prototype.replace_hu = false;
        AmuletHookResult.prototype.modify_target_point = "";
        AmuletHookResult.prototype.upgrade_level = false;
        AmuletHookResult.prototype.modify_dora = $util.emptyArray;
        AmuletHookResult.prototype.modify_dora_max_count = 0;
        AmuletHookResult.prototype.modify_shop_goods_count = 0;
        AmuletHookResult.prototype.modify_shop_rare_weight = false;
        AmuletHookResult.prototype.modify_shop_goods_price = false;
        AmuletHookResult.prototype.modify_shop_pack_effect = $util.emptyArray;
        AmuletHookResult.prototype.modify_effect_max_count = 0;
        AmuletHookResult.prototype.modify_goods = $util.emptyArray;
        AmuletHookResult.prototype.remove_goods = $util.emptyArray;
        AmuletHookResult.prototype.modify_base = null;
        AmuletHookResult.prototype.modify_fan = null;
        AmuletHookResult.prototype.modify_fan_info = $util.emptyArray;
        AmuletHookResult.prototype.transform_effect = $util.emptyArray;
        AmuletHookResult.prototype.add_badge = $util.emptyArray;
        AmuletHookResult.prototype.remove_badge = $util.emptyArray;
        AmuletHookResult.prototype.modify_effect_price = "";
        AmuletHookResult.prototype.copy_effect = $util.emptyArray;
        AmuletHookResult.prototype.effect_growth = false;
        AmuletHookResult.prototype.modify_tile_score_aura = "";
        AmuletHookResult.prototype.modify_hule_count = 0;
        AmuletHookResult.prototype.can_gang = false;
        AmuletHookResult.prototype.modify_change_hands_list = $util.emptyArray;
        AmuletHookResult.prototype.modify_change_desktop = null;
        AmuletHookResult.prototype.self_effect_id = 0;
        AmuletHookResult.prototype.modify_change_coin = "";
        AmuletHookResult.prototype.set_tile_score = $util.emptyArray;
        AmuletHookResult.prototype.upgrade_effect = $util.emptyArray;
        AmuletHookResult.prototype.modify_tile_base_score = $util.emptyArray;

        AmuletHookResult.create = function create(properties) {
            return new AmuletHookResult(properties);
        };

        AmuletHookResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.add_effect != null && message.add_effect.length)
                for (let i = 0; i < message.add_effect.length; ++i)
                    $root.lq.AmuletHookResult.AddEffectResult.encode(message.add_effect[i], writer.uint32(10).fork()).ldelim();
            if (message.remove_effect != null && message.remove_effect.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.remove_effect.length; ++i)
                    writer.uint32(message.remove_effect[i]);
                writer.ldelim();
            }
            if (message.add_buff != null && message.add_buff.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.add_buff.length; ++i)
                    writer.uint32(message.add_buff[i]);
                writer.ldelim();
            }
            if (message.remove_buff != null && message.remove_buff.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.remove_buff.length; ++i)
                    writer.uint32(message.remove_buff[i]);
                writer.ldelim();
            }
            if (message.add_tian_dora != null && message.add_tian_dora.length)
                for (let i = 0; i < message.add_tian_dora.length; ++i)
                    writer.uint32(42).string(message.add_tian_dora[i]);
            if (message.add_dora != null && Object.hasOwnProperty.call(message, "add_dora"))
                $root.lq.AmuletHookResult.AddDoraResult.encode(message.add_dora, writer.uint32(50).fork()).ldelim();
            if (message.coin_modify != null && Object.hasOwnProperty.call(message, "coin_modify"))
                $root.lq.AmuletHookResult.ValueResult.encode(message.coin_modify, writer.uint32(66).fork()).ldelim();
            if (message.tile_replace != null && message.tile_replace.length)
                for (let i = 0; i < message.tile_replace.length; ++i)
                    $root.lq.AmuletTile.encode(message.tile_replace[i], writer.uint32(82).fork()).ldelim();
            if (message.add_show_tile != null && message.add_show_tile.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.add_show_tile.length; ++i)
                    writer.uint32(message.add_show_tile[i]);
                writer.ldelim();
            }
            if (message.modify_tile_score != null && message.modify_tile_score.length)
                for (let i = 0; i < message.modify_tile_score.length; ++i)
                    $root.lq.AmuletTileScore.encode(message.modify_tile_score[i], writer.uint32(98).fork()).ldelim();
            if (message.modify_desktop_count != null && Object.hasOwnProperty.call(message, "modify_desktop_count"))
                writer.uint32(112).int32(message.modify_desktop_count);
            if (message.modify_show_desktop_count != null && Object.hasOwnProperty.call(message, "modify_show_desktop_count"))
                writer.uint32(120).int32(message.modify_show_desktop_count);
            if (message.modify_lock_tile_count != null && Object.hasOwnProperty.call(message, "modify_lock_tile_count"))
                writer.uint32(128).int32(message.modify_lock_tile_count);
            if (message.modify_change_hands_count != null && Object.hasOwnProperty.call(message, "modify_change_hands_count"))
                writer.uint32(136).int32(message.modify_change_hands_count);
            if (message.modify_change_hands_tile_count != null && Object.hasOwnProperty.call(message, "modify_change_hands_tile_count"))
                writer.uint32(144).int32(message.modify_change_hands_tile_count);
            if (message.force_moqie != null && Object.hasOwnProperty.call(message, "force_moqie"))
                writer.uint32(152).bool(message.force_moqie);
            if (message.replace_hu != null && Object.hasOwnProperty.call(message, "replace_hu"))
                writer.uint32(160).bool(message.replace_hu);
            if (message.modify_target_point != null && Object.hasOwnProperty.call(message, "modify_target_point"))
                writer.uint32(170).string(message.modify_target_point);
            if (message.upgrade_level != null && Object.hasOwnProperty.call(message, "upgrade_level"))
                writer.uint32(176).bool(message.upgrade_level);
            if (message.modify_dora != null && message.modify_dora.length)
                for (let i = 0; i < message.modify_dora.length; ++i)
                    $root.lq.AmuletHookResult.ModifyDoraResult.encode(message.modify_dora[i], writer.uint32(186).fork()).ldelim();
            if (message.modify_dora_max_count != null && Object.hasOwnProperty.call(message, "modify_dora_max_count"))
                writer.uint32(200).int32(message.modify_dora_max_count);
            if (message.modify_shop_goods_count != null && Object.hasOwnProperty.call(message, "modify_shop_goods_count"))
                writer.uint32(208).int32(message.modify_shop_goods_count);
            if (message.modify_shop_rare_weight != null && Object.hasOwnProperty.call(message, "modify_shop_rare_weight"))
                writer.uint32(216).bool(message.modify_shop_rare_weight);
            if (message.modify_shop_goods_price != null && Object.hasOwnProperty.call(message, "modify_shop_goods_price"))
                writer.uint32(224).bool(message.modify_shop_goods_price);
            if (message.modify_shop_pack_effect != null && message.modify_shop_pack_effect.length) {
                writer.uint32(234).fork();
                for (let i = 0; i < message.modify_shop_pack_effect.length; ++i)
                    writer.uint32(message.modify_shop_pack_effect[i]);
                writer.ldelim();
            }
            if (message.modify_effect_max_count != null && Object.hasOwnProperty.call(message, "modify_effect_max_count"))
                writer.uint32(240).int32(message.modify_effect_max_count);
            if (message.modify_goods != null && message.modify_goods.length)
                for (let i = 0; i < message.modify_goods.length; ++i)
                    $root.lq.AmuletGameShopGoods.encode(message.modify_goods[i], writer.uint32(250).fork()).ldelim();
            if (message.remove_goods != null && message.remove_goods.length) {
                writer.uint32(258).fork();
                for (let i = 0; i < message.remove_goods.length; ++i)
                    writer.uint32(message.remove_goods[i]);
                writer.ldelim();
            }
            if (message.modify_base != null && Object.hasOwnProperty.call(message, "modify_base"))
                $root.lq.AmuletHookResult.ValueResult.encode(message.modify_base, writer.uint32(266).fork()).ldelim();
            if (message.modify_fan != null && Object.hasOwnProperty.call(message, "modify_fan"))
                $root.lq.AmuletHookResult.ValueResult.encode(message.modify_fan, writer.uint32(274).fork()).ldelim();
            if (message.modify_fan_info != null && message.modify_fan_info.length)
                for (let i = 0; i < message.modify_fan_info.length; ++i)
                    $root.lq.AmuletFan.encode(message.modify_fan_info[i], writer.uint32(298).fork()).ldelim();
            if (message.transform_effect != null && message.transform_effect.length)
                for (let i = 0; i < message.transform_effect.length; ++i)
                    $root.lq.AmuletHookResult.TransformResult.encode(message.transform_effect[i], writer.uint32(314).fork()).ldelim();
            if (message.add_badge != null && message.add_badge.length)
                for (let i = 0; i < message.add_badge.length; ++i)
                    $root.lq.AmuletHookResult.AddBadge.encode(message.add_badge[i], writer.uint32(322).fork()).ldelim();
            if (message.remove_badge != null && message.remove_badge.length) {
                writer.uint32(330).fork();
                for (let i = 0; i < message.remove_badge.length; ++i)
                    writer.uint32(message.remove_badge[i]);
                writer.ldelim();
            }
            if (message.modify_effect_price != null && Object.hasOwnProperty.call(message, "modify_effect_price"))
                writer.uint32(338).string(message.modify_effect_price);
            if (message.copy_effect != null && message.copy_effect.length)
                for (let i = 0; i < message.copy_effect.length; ++i)
                    $root.lq.AmuletHookResult.CopyEffect.encode(message.copy_effect[i], writer.uint32(346).fork()).ldelim();
            if (message.effect_growth != null && Object.hasOwnProperty.call(message, "effect_growth"))
                writer.uint32(352).bool(message.effect_growth);
            if (message.modify_tile_score_aura != null && Object.hasOwnProperty.call(message, "modify_tile_score_aura"))
                writer.uint32(362).string(message.modify_tile_score_aura);
            if (message.modify_hule_count != null && Object.hasOwnProperty.call(message, "modify_hule_count"))
                writer.uint32(368).uint32(message.modify_hule_count);
            if (message.can_gang != null && Object.hasOwnProperty.call(message, "can_gang"))
                writer.uint32(376).bool(message.can_gang);
            if (message.modify_change_hands_list != null && message.modify_change_hands_list.length) {
                writer.uint32(386).fork();
                for (let i = 0; i < message.modify_change_hands_list.length; ++i)
                    writer.uint32(message.modify_change_hands_list[i]);
                writer.ldelim();
            }
            if (message.modify_change_desktop != null && Object.hasOwnProperty.call(message, "modify_change_desktop"))
                $root.lq.AmuletHookResult.AmuletChangeDesktopResult.encode(message.modify_change_desktop, writer.uint32(394).fork()).ldelim();
            if (message.self_effect_id != null && Object.hasOwnProperty.call(message, "self_effect_id"))
                writer.uint32(400).uint32(message.self_effect_id);
            if (message.modify_change_coin != null && Object.hasOwnProperty.call(message, "modify_change_coin"))
                writer.uint32(410).string(message.modify_change_coin);
            if (message.set_tile_score != null && message.set_tile_score.length)
                for (let i = 0; i < message.set_tile_score.length; ++i)
                    $root.lq.AmuletTileScore.encode(message.set_tile_score[i], writer.uint32(418).fork()).ldelim();
            if (message.upgrade_effect != null && message.upgrade_effect.length)
                for (let i = 0; i < message.upgrade_effect.length; ++i)
                    $root.lq.AmuletHookResult.UpgradeEffectResult.encode(message.upgrade_effect[i], writer.uint32(426).fork()).ldelim();
            if (message.modify_tile_base_score != null && message.modify_tile_base_score.length)
                for (let i = 0; i < message.modify_tile_base_score.length; ++i)
                    $root.lq.AmuletTileScore.encode(message.modify_tile_base_score[i], writer.uint32(434).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletHookResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletHookResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.add_effect && message.add_effect.length))
                            message.add_effect = [];
                        message.add_effect.push($root.lq.AmuletHookResult.AddEffectResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.remove_effect && message.remove_effect.length))
                                message.remove_effect = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.remove_effect.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.remove_effect && message.remove_effect.length))
                            message.remove_effect = [];
                        message.remove_effect.push(reader.uint32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.add_buff && message.add_buff.length))
                                message.add_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.add_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.add_buff && message.add_buff.length))
                            message.add_buff = [];
                        message.add_buff.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.remove_buff && message.remove_buff.length))
                                message.remove_buff = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.remove_buff.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.remove_buff && message.remove_buff.length))
                            message.remove_buff = [];
                        message.remove_buff.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.add_tian_dora && message.add_tian_dora.length))
                            message.add_tian_dora = [];
                        message.add_tian_dora.push(reader.string());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.add_dora = $root.lq.AmuletHookResult.AddDoraResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.add_dora);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.coin_modify = $root.lq.AmuletHookResult.ValueResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.coin_modify);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tile_replace && message.tile_replace.length))
                            message.tile_replace = [];
                        message.tile_replace.push($root.lq.AmuletTile.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.add_show_tile && message.add_show_tile.length))
                                message.add_show_tile = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.add_show_tile.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.add_show_tile && message.add_show_tile.length))
                            message.add_show_tile = [];
                        message.add_show_tile.push(reader.uint32());
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modify_tile_score && message.modify_tile_score.length))
                            message.modify_tile_score = [];
                        message.modify_tile_score.push($root.lq.AmuletTileScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_desktop_count = value;
                        else
                            delete message.modify_desktop_count;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_show_desktop_count = value;
                        else
                            delete message.modify_show_desktop_count;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_lock_tile_count = value;
                        else
                            delete message.modify_lock_tile_count;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_change_hands_count = value;
                        else
                            delete message.modify_change_hands_count;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_change_hands_tile_count = value;
                        else
                            delete message.modify_change_hands_tile_count;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.force_moqie = value;
                        else
                            delete message.force_moqie;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.replace_hu = value;
                        else
                            delete message.replace_hu;
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.modify_target_point = value;
                        else
                            delete message.modify_target_point;
                        continue;
                    }
                case 22: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.upgrade_level = value;
                        else
                            delete message.upgrade_level;
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modify_dora && message.modify_dora.length))
                            message.modify_dora = [];
                        message.modify_dora.push($root.lq.AmuletHookResult.ModifyDoraResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 25: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_dora_max_count = value;
                        else
                            delete message.modify_dora_max_count;
                        continue;
                    }
                case 26: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_shop_goods_count = value;
                        else
                            delete message.modify_shop_goods_count;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.modify_shop_rare_weight = value;
                        else
                            delete message.modify_shop_rare_weight;
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.modify_shop_goods_price = value;
                        else
                            delete message.modify_shop_goods_price;
                        continue;
                    }
                case 29: {
                        if (wireType === 2) {
                            if (!(message.modify_shop_pack_effect && message.modify_shop_pack_effect.length))
                                message.modify_shop_pack_effect = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.modify_shop_pack_effect.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modify_shop_pack_effect && message.modify_shop_pack_effect.length))
                            message.modify_shop_pack_effect = [];
                        message.modify_shop_pack_effect.push(reader.uint32());
                        continue;
                    }
                case 30: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.modify_effect_max_count = value;
                        else
                            delete message.modify_effect_max_count;
                        continue;
                    }
                case 31: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modify_goods && message.modify_goods.length))
                            message.modify_goods = [];
                        message.modify_goods.push($root.lq.AmuletGameShopGoods.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 32: {
                        if (wireType === 2) {
                            if (!(message.remove_goods && message.remove_goods.length))
                                message.remove_goods = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.remove_goods.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.remove_goods && message.remove_goods.length))
                            message.remove_goods = [];
                        message.remove_goods.push(reader.uint32());
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        message.modify_base = $root.lq.AmuletHookResult.ValueResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.modify_base);
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.modify_fan = $root.lq.AmuletHookResult.ValueResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.modify_fan);
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modify_fan_info && message.modify_fan_info.length))
                            message.modify_fan_info = [];
                        message.modify_fan_info.push($root.lq.AmuletFan.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 39: {
                        if (wireType !== 2)
                            break;
                        if (!(message.transform_effect && message.transform_effect.length))
                            message.transform_effect = [];
                        message.transform_effect.push($root.lq.AmuletHookResult.TransformResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 40: {
                        if (wireType !== 2)
                            break;
                        if (!(message.add_badge && message.add_badge.length))
                            message.add_badge = [];
                        message.add_badge.push($root.lq.AmuletHookResult.AddBadge.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 41: {
                        if (wireType === 2) {
                            if (!(message.remove_badge && message.remove_badge.length))
                                message.remove_badge = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.remove_badge.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.remove_badge && message.remove_badge.length))
                            message.remove_badge = [];
                        message.remove_badge.push(reader.uint32());
                        continue;
                    }
                case 42: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.modify_effect_price = value;
                        else
                            delete message.modify_effect_price;
                        continue;
                    }
                case 43: {
                        if (wireType !== 2)
                            break;
                        if (!(message.copy_effect && message.copy_effect.length))
                            message.copy_effect = [];
                        message.copy_effect.push($root.lq.AmuletHookResult.CopyEffect.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 44: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.effect_growth = value;
                        else
                            delete message.effect_growth;
                        continue;
                    }
                case 45: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.modify_tile_score_aura = value;
                        else
                            delete message.modify_tile_score_aura;
                        continue;
                    }
                case 46: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.modify_hule_count = value;
                        else
                            delete message.modify_hule_count;
                        continue;
                    }
                case 47: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.can_gang = value;
                        else
                            delete message.can_gang;
                        continue;
                    }
                case 48: {
                        if (wireType === 2) {
                            if (!(message.modify_change_hands_list && message.modify_change_hands_list.length))
                                message.modify_change_hands_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.modify_change_hands_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modify_change_hands_list && message.modify_change_hands_list.length))
                            message.modify_change_hands_list = [];
                        message.modify_change_hands_list.push(reader.uint32());
                        continue;
                    }
                case 49: {
                        if (wireType !== 2)
                            break;
                        message.modify_change_desktop = $root.lq.AmuletHookResult.AmuletChangeDesktopResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.modify_change_desktop);
                        continue;
                    }
                case 50: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.self_effect_id = value;
                        else
                            delete message.self_effect_id;
                        continue;
                    }
                case 51: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.modify_change_coin = value;
                        else
                            delete message.modify_change_coin;
                        continue;
                    }
                case 52: {
                        if (wireType !== 2)
                            break;
                        if (!(message.set_tile_score && message.set_tile_score.length))
                            message.set_tile_score = [];
                        message.set_tile_score.push($root.lq.AmuletTileScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 53: {
                        if (wireType !== 2)
                            break;
                        if (!(message.upgrade_effect && message.upgrade_effect.length))
                            message.upgrade_effect = [];
                        message.upgrade_effect.push($root.lq.AmuletHookResult.UpgradeEffectResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 54: {
                        if (wireType !== 2)
                            break;
                        if (!(message.modify_tile_base_score && message.modify_tile_base_score.length))
                            message.modify_tile_base_score = [];
                        message.modify_tile_base_score.push($root.lq.AmuletTileScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletHookResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletHookResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.add_effect != null && message.hasOwnProperty("add_effect")) {
                if (!Array.isArray(message.add_effect))
                    return "add_effect: array expected";
                for (let i = 0; i < message.add_effect.length; ++i) {
                    let error = $root.lq.AmuletHookResult.AddEffectResult.verify(message.add_effect[i], _depth + 1);
                    if (error)
                        return "add_effect." + error;
                }
            }
            if (message.remove_effect != null && message.hasOwnProperty("remove_effect")) {
                if (!Array.isArray(message.remove_effect))
                    return "remove_effect: array expected";
                for (let i = 0; i < message.remove_effect.length; ++i)
                    if (!$util.isInteger(message.remove_effect[i]))
                        return "remove_effect: integer[] expected";
            }
            if (message.add_buff != null && message.hasOwnProperty("add_buff")) {
                if (!Array.isArray(message.add_buff))
                    return "add_buff: array expected";
                for (let i = 0; i < message.add_buff.length; ++i)
                    if (!$util.isInteger(message.add_buff[i]))
                        return "add_buff: integer[] expected";
            }
            if (message.remove_buff != null && message.hasOwnProperty("remove_buff")) {
                if (!Array.isArray(message.remove_buff))
                    return "remove_buff: array expected";
                for (let i = 0; i < message.remove_buff.length; ++i)
                    if (!$util.isInteger(message.remove_buff[i]))
                        return "remove_buff: integer[] expected";
            }
            if (message.add_tian_dora != null && message.hasOwnProperty("add_tian_dora")) {
                if (!Array.isArray(message.add_tian_dora))
                    return "add_tian_dora: array expected";
                for (let i = 0; i < message.add_tian_dora.length; ++i)
                    if (!$util.isString(message.add_tian_dora[i]))
                        return "add_tian_dora: string[] expected";
            }
            if (message.add_dora != null && message.hasOwnProperty("add_dora")) {
                let error = $root.lq.AmuletHookResult.AddDoraResult.verify(message.add_dora, _depth + 1);
                if (error)
                    return "add_dora." + error;
            }
            if (message.coin_modify != null && message.hasOwnProperty("coin_modify")) {
                let error = $root.lq.AmuletHookResult.ValueResult.verify(message.coin_modify, _depth + 1);
                if (error)
                    return "coin_modify." + error;
            }
            if (message.tile_replace != null && message.hasOwnProperty("tile_replace")) {
                if (!Array.isArray(message.tile_replace))
                    return "tile_replace: array expected";
                for (let i = 0; i < message.tile_replace.length; ++i) {
                    let error = $root.lq.AmuletTile.verify(message.tile_replace[i], _depth + 1);
                    if (error)
                        return "tile_replace." + error;
                }
            }
            if (message.add_show_tile != null && message.hasOwnProperty("add_show_tile")) {
                if (!Array.isArray(message.add_show_tile))
                    return "add_show_tile: array expected";
                for (let i = 0; i < message.add_show_tile.length; ++i)
                    if (!$util.isInteger(message.add_show_tile[i]))
                        return "add_show_tile: integer[] expected";
            }
            if (message.modify_tile_score != null && message.hasOwnProperty("modify_tile_score")) {
                if (!Array.isArray(message.modify_tile_score))
                    return "modify_tile_score: array expected";
                for (let i = 0; i < message.modify_tile_score.length; ++i) {
                    let error = $root.lq.AmuletTileScore.verify(message.modify_tile_score[i], _depth + 1);
                    if (error)
                        return "modify_tile_score." + error;
                }
            }
            if (message.modify_desktop_count != null && message.hasOwnProperty("modify_desktop_count"))
                if (!$util.isInteger(message.modify_desktop_count))
                    return "modify_desktop_count: integer expected";
            if (message.modify_show_desktop_count != null && message.hasOwnProperty("modify_show_desktop_count"))
                if (!$util.isInteger(message.modify_show_desktop_count))
                    return "modify_show_desktop_count: integer expected";
            if (message.modify_lock_tile_count != null && message.hasOwnProperty("modify_lock_tile_count"))
                if (!$util.isInteger(message.modify_lock_tile_count))
                    return "modify_lock_tile_count: integer expected";
            if (message.modify_change_hands_count != null && message.hasOwnProperty("modify_change_hands_count"))
                if (!$util.isInteger(message.modify_change_hands_count))
                    return "modify_change_hands_count: integer expected";
            if (message.modify_change_hands_tile_count != null && message.hasOwnProperty("modify_change_hands_tile_count"))
                if (!$util.isInteger(message.modify_change_hands_tile_count))
                    return "modify_change_hands_tile_count: integer expected";
            if (message.force_moqie != null && message.hasOwnProperty("force_moqie"))
                if (typeof message.force_moqie !== "boolean")
                    return "force_moqie: boolean expected";
            if (message.replace_hu != null && message.hasOwnProperty("replace_hu"))
                if (typeof message.replace_hu !== "boolean")
                    return "replace_hu: boolean expected";
            if (message.modify_target_point != null && message.hasOwnProperty("modify_target_point"))
                if (!$util.isString(message.modify_target_point))
                    return "modify_target_point: string expected";
            if (message.upgrade_level != null && message.hasOwnProperty("upgrade_level"))
                if (typeof message.upgrade_level !== "boolean")
                    return "upgrade_level: boolean expected";
            if (message.modify_dora != null && message.hasOwnProperty("modify_dora")) {
                if (!Array.isArray(message.modify_dora))
                    return "modify_dora: array expected";
                for (let i = 0; i < message.modify_dora.length; ++i) {
                    let error = $root.lq.AmuletHookResult.ModifyDoraResult.verify(message.modify_dora[i], _depth + 1);
                    if (error)
                        return "modify_dora." + error;
                }
            }
            if (message.modify_dora_max_count != null && message.hasOwnProperty("modify_dora_max_count"))
                if (!$util.isInteger(message.modify_dora_max_count))
                    return "modify_dora_max_count: integer expected";
            if (message.modify_shop_goods_count != null && message.hasOwnProperty("modify_shop_goods_count"))
                if (!$util.isInteger(message.modify_shop_goods_count))
                    return "modify_shop_goods_count: integer expected";
            if (message.modify_shop_rare_weight != null && message.hasOwnProperty("modify_shop_rare_weight"))
                if (typeof message.modify_shop_rare_weight !== "boolean")
                    return "modify_shop_rare_weight: boolean expected";
            if (message.modify_shop_goods_price != null && message.hasOwnProperty("modify_shop_goods_price"))
                if (typeof message.modify_shop_goods_price !== "boolean")
                    return "modify_shop_goods_price: boolean expected";
            if (message.modify_shop_pack_effect != null && message.hasOwnProperty("modify_shop_pack_effect")) {
                if (!Array.isArray(message.modify_shop_pack_effect))
                    return "modify_shop_pack_effect: array expected";
                for (let i = 0; i < message.modify_shop_pack_effect.length; ++i)
                    if (!$util.isInteger(message.modify_shop_pack_effect[i]))
                        return "modify_shop_pack_effect: integer[] expected";
            }
            if (message.modify_effect_max_count != null && message.hasOwnProperty("modify_effect_max_count"))
                if (!$util.isInteger(message.modify_effect_max_count))
                    return "modify_effect_max_count: integer expected";
            if (message.modify_goods != null && message.hasOwnProperty("modify_goods")) {
                if (!Array.isArray(message.modify_goods))
                    return "modify_goods: array expected";
                for (let i = 0; i < message.modify_goods.length; ++i) {
                    let error = $root.lq.AmuletGameShopGoods.verify(message.modify_goods[i], _depth + 1);
                    if (error)
                        return "modify_goods." + error;
                }
            }
            if (message.remove_goods != null && message.hasOwnProperty("remove_goods")) {
                if (!Array.isArray(message.remove_goods))
                    return "remove_goods: array expected";
                for (let i = 0; i < message.remove_goods.length; ++i)
                    if (!$util.isInteger(message.remove_goods[i]))
                        return "remove_goods: integer[] expected";
            }
            if (message.modify_base != null && message.hasOwnProperty("modify_base")) {
                let error = $root.lq.AmuletHookResult.ValueResult.verify(message.modify_base, _depth + 1);
                if (error)
                    return "modify_base." + error;
            }
            if (message.modify_fan != null && message.hasOwnProperty("modify_fan")) {
                let error = $root.lq.AmuletHookResult.ValueResult.verify(message.modify_fan, _depth + 1);
                if (error)
                    return "modify_fan." + error;
            }
            if (message.modify_fan_info != null && message.hasOwnProperty("modify_fan_info")) {
                if (!Array.isArray(message.modify_fan_info))
                    return "modify_fan_info: array expected";
                for (let i = 0; i < message.modify_fan_info.length; ++i) {
                    let error = $root.lq.AmuletFan.verify(message.modify_fan_info[i], _depth + 1);
                    if (error)
                        return "modify_fan_info." + error;
                }
            }
            if (message.transform_effect != null && message.hasOwnProperty("transform_effect")) {
                if (!Array.isArray(message.transform_effect))
                    return "transform_effect: array expected";
                for (let i = 0; i < message.transform_effect.length; ++i) {
                    let error = $root.lq.AmuletHookResult.TransformResult.verify(message.transform_effect[i], _depth + 1);
                    if (error)
                        return "transform_effect." + error;
                }
            }
            if (message.add_badge != null && message.hasOwnProperty("add_badge")) {
                if (!Array.isArray(message.add_badge))
                    return "add_badge: array expected";
                for (let i = 0; i < message.add_badge.length; ++i) {
                    let error = $root.lq.AmuletHookResult.AddBadge.verify(message.add_badge[i], _depth + 1);
                    if (error)
                        return "add_badge." + error;
                }
            }
            if (message.remove_badge != null && message.hasOwnProperty("remove_badge")) {
                if (!Array.isArray(message.remove_badge))
                    return "remove_badge: array expected";
                for (let i = 0; i < message.remove_badge.length; ++i)
                    if (!$util.isInteger(message.remove_badge[i]))
                        return "remove_badge: integer[] expected";
            }
            if (message.modify_effect_price != null && message.hasOwnProperty("modify_effect_price"))
                if (!$util.isString(message.modify_effect_price))
                    return "modify_effect_price: string expected";
            if (message.copy_effect != null && message.hasOwnProperty("copy_effect")) {
                if (!Array.isArray(message.copy_effect))
                    return "copy_effect: array expected";
                for (let i = 0; i < message.copy_effect.length; ++i) {
                    let error = $root.lq.AmuletHookResult.CopyEffect.verify(message.copy_effect[i], _depth + 1);
                    if (error)
                        return "copy_effect." + error;
                }
            }
            if (message.effect_growth != null && message.hasOwnProperty("effect_growth"))
                if (typeof message.effect_growth !== "boolean")
                    return "effect_growth: boolean expected";
            if (message.modify_tile_score_aura != null && message.hasOwnProperty("modify_tile_score_aura"))
                if (!$util.isString(message.modify_tile_score_aura))
                    return "modify_tile_score_aura: string expected";
            if (message.modify_hule_count != null && message.hasOwnProperty("modify_hule_count"))
                if (!$util.isInteger(message.modify_hule_count))
                    return "modify_hule_count: integer expected";
            if (message.can_gang != null && message.hasOwnProperty("can_gang"))
                if (typeof message.can_gang !== "boolean")
                    return "can_gang: boolean expected";
            if (message.modify_change_hands_list != null && message.hasOwnProperty("modify_change_hands_list")) {
                if (!Array.isArray(message.modify_change_hands_list))
                    return "modify_change_hands_list: array expected";
                for (let i = 0; i < message.modify_change_hands_list.length; ++i)
                    if (!$util.isInteger(message.modify_change_hands_list[i]))
                        return "modify_change_hands_list: integer[] expected";
            }
            if (message.modify_change_desktop != null && message.hasOwnProperty("modify_change_desktop")) {
                let error = $root.lq.AmuletHookResult.AmuletChangeDesktopResult.verify(message.modify_change_desktop, _depth + 1);
                if (error)
                    return "modify_change_desktop." + error;
            }
            if (message.self_effect_id != null && message.hasOwnProperty("self_effect_id"))
                if (!$util.isInteger(message.self_effect_id))
                    return "self_effect_id: integer expected";
            if (message.modify_change_coin != null && message.hasOwnProperty("modify_change_coin"))
                if (!$util.isString(message.modify_change_coin))
                    return "modify_change_coin: string expected";
            if (message.set_tile_score != null && message.hasOwnProperty("set_tile_score")) {
                if (!Array.isArray(message.set_tile_score))
                    return "set_tile_score: array expected";
                for (let i = 0; i < message.set_tile_score.length; ++i) {
                    let error = $root.lq.AmuletTileScore.verify(message.set_tile_score[i], _depth + 1);
                    if (error)
                        return "set_tile_score." + error;
                }
            }
            if (message.upgrade_effect != null && message.hasOwnProperty("upgrade_effect")) {
                if (!Array.isArray(message.upgrade_effect))
                    return "upgrade_effect: array expected";
                for (let i = 0; i < message.upgrade_effect.length; ++i) {
                    let error = $root.lq.AmuletHookResult.UpgradeEffectResult.verify(message.upgrade_effect[i], _depth + 1);
                    if (error)
                        return "upgrade_effect." + error;
                }
            }
            if (message.modify_tile_base_score != null && message.hasOwnProperty("modify_tile_base_score")) {
                if (!Array.isArray(message.modify_tile_base_score))
                    return "modify_tile_base_score: array expected";
                for (let i = 0; i < message.modify_tile_base_score.length; ++i) {
                    let error = $root.lq.AmuletTileScore.verify(message.modify_tile_base_score[i], _depth + 1);
                    if (error)
                        return "modify_tile_base_score." + error;
                }
            }
            return null;
        };

        AmuletHookResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletHookResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletHookResult();
            if (object.add_effect) {
                if (!Array.isArray(object.add_effect))
                    throw TypeError(".lq.AmuletHookResult.add_effect: array expected");
                message.add_effect = Array(object.add_effect.length);
                for (let i = 0; i < object.add_effect.length; ++i) {
                    if (typeof object.add_effect[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.add_effect: object expected");
                    message.add_effect[i] = $root.lq.AmuletHookResult.AddEffectResult.fromObject(object.add_effect[i], _depth + 1);
                }
            }
            if (object.remove_effect) {
                if (!Array.isArray(object.remove_effect))
                    throw TypeError(".lq.AmuletHookResult.remove_effect: array expected");
                message.remove_effect = Array(object.remove_effect.length);
                for (let i = 0; i < object.remove_effect.length; ++i)
                    message.remove_effect[i] = object.remove_effect[i] >>> 0;
            }
            if (object.add_buff) {
                if (!Array.isArray(object.add_buff))
                    throw TypeError(".lq.AmuletHookResult.add_buff: array expected");
                message.add_buff = Array(object.add_buff.length);
                for (let i = 0; i < object.add_buff.length; ++i)
                    message.add_buff[i] = object.add_buff[i] >>> 0;
            }
            if (object.remove_buff) {
                if (!Array.isArray(object.remove_buff))
                    throw TypeError(".lq.AmuletHookResult.remove_buff: array expected");
                message.remove_buff = Array(object.remove_buff.length);
                for (let i = 0; i < object.remove_buff.length; ++i)
                    message.remove_buff[i] = object.remove_buff[i] >>> 0;
            }
            if (object.add_tian_dora) {
                if (!Array.isArray(object.add_tian_dora))
                    throw TypeError(".lq.AmuletHookResult.add_tian_dora: array expected");
                message.add_tian_dora = Array(object.add_tian_dora.length);
                for (let i = 0; i < object.add_tian_dora.length; ++i)
                    message.add_tian_dora[i] = String(object.add_tian_dora[i]);
            }
            if (object.add_dora != null) {
                if (typeof object.add_dora !== "object")
                    throw TypeError(".lq.AmuletHookResult.add_dora: object expected");
                message.add_dora = $root.lq.AmuletHookResult.AddDoraResult.fromObject(object.add_dora, _depth + 1);
            }
            if (object.coin_modify != null) {
                if (typeof object.coin_modify !== "object")
                    throw TypeError(".lq.AmuletHookResult.coin_modify: object expected");
                message.coin_modify = $root.lq.AmuletHookResult.ValueResult.fromObject(object.coin_modify, _depth + 1);
            }
            if (object.tile_replace) {
                if (!Array.isArray(object.tile_replace))
                    throw TypeError(".lq.AmuletHookResult.tile_replace: array expected");
                message.tile_replace = Array(object.tile_replace.length);
                for (let i = 0; i < object.tile_replace.length; ++i) {
                    if (typeof object.tile_replace[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.tile_replace: object expected");
                    message.tile_replace[i] = $root.lq.AmuletTile.fromObject(object.tile_replace[i], _depth + 1);
                }
            }
            if (object.add_show_tile) {
                if (!Array.isArray(object.add_show_tile))
                    throw TypeError(".lq.AmuletHookResult.add_show_tile: array expected");
                message.add_show_tile = Array(object.add_show_tile.length);
                for (let i = 0; i < object.add_show_tile.length; ++i)
                    message.add_show_tile[i] = object.add_show_tile[i] >>> 0;
            }
            if (object.modify_tile_score) {
                if (!Array.isArray(object.modify_tile_score))
                    throw TypeError(".lq.AmuletHookResult.modify_tile_score: array expected");
                message.modify_tile_score = Array(object.modify_tile_score.length);
                for (let i = 0; i < object.modify_tile_score.length; ++i) {
                    if (typeof object.modify_tile_score[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.modify_tile_score: object expected");
                    message.modify_tile_score[i] = $root.lq.AmuletTileScore.fromObject(object.modify_tile_score[i], _depth + 1);
                }
            }
            if (object.modify_desktop_count != null)
                if (Number(object.modify_desktop_count) !== 0)
                    message.modify_desktop_count = object.modify_desktop_count | 0;
            if (object.modify_show_desktop_count != null)
                if (Number(object.modify_show_desktop_count) !== 0)
                    message.modify_show_desktop_count = object.modify_show_desktop_count | 0;
            if (object.modify_lock_tile_count != null)
                if (Number(object.modify_lock_tile_count) !== 0)
                    message.modify_lock_tile_count = object.modify_lock_tile_count | 0;
            if (object.modify_change_hands_count != null)
                if (Number(object.modify_change_hands_count) !== 0)
                    message.modify_change_hands_count = object.modify_change_hands_count | 0;
            if (object.modify_change_hands_tile_count != null)
                if (Number(object.modify_change_hands_tile_count) !== 0)
                    message.modify_change_hands_tile_count = object.modify_change_hands_tile_count | 0;
            if (object.force_moqie != null)
                if (object.force_moqie)
                    message.force_moqie = Boolean(object.force_moqie);
            if (object.replace_hu != null)
                if (object.replace_hu)
                    message.replace_hu = Boolean(object.replace_hu);
            if (object.modify_target_point != null)
                if (typeof object.modify_target_point !== "string" || object.modify_target_point.length)
                    message.modify_target_point = String(object.modify_target_point);
            if (object.upgrade_level != null)
                if (object.upgrade_level)
                    message.upgrade_level = Boolean(object.upgrade_level);
            if (object.modify_dora) {
                if (!Array.isArray(object.modify_dora))
                    throw TypeError(".lq.AmuletHookResult.modify_dora: array expected");
                message.modify_dora = Array(object.modify_dora.length);
                for (let i = 0; i < object.modify_dora.length; ++i) {
                    if (typeof object.modify_dora[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.modify_dora: object expected");
                    message.modify_dora[i] = $root.lq.AmuletHookResult.ModifyDoraResult.fromObject(object.modify_dora[i], _depth + 1);
                }
            }
            if (object.modify_dora_max_count != null)
                if (Number(object.modify_dora_max_count) !== 0)
                    message.modify_dora_max_count = object.modify_dora_max_count | 0;
            if (object.modify_shop_goods_count != null)
                if (Number(object.modify_shop_goods_count) !== 0)
                    message.modify_shop_goods_count = object.modify_shop_goods_count | 0;
            if (object.modify_shop_rare_weight != null)
                if (object.modify_shop_rare_weight)
                    message.modify_shop_rare_weight = Boolean(object.modify_shop_rare_weight);
            if (object.modify_shop_goods_price != null)
                if (object.modify_shop_goods_price)
                    message.modify_shop_goods_price = Boolean(object.modify_shop_goods_price);
            if (object.modify_shop_pack_effect) {
                if (!Array.isArray(object.modify_shop_pack_effect))
                    throw TypeError(".lq.AmuletHookResult.modify_shop_pack_effect: array expected");
                message.modify_shop_pack_effect = Array(object.modify_shop_pack_effect.length);
                for (let i = 0; i < object.modify_shop_pack_effect.length; ++i)
                    message.modify_shop_pack_effect[i] = object.modify_shop_pack_effect[i] >>> 0;
            }
            if (object.modify_effect_max_count != null)
                if (Number(object.modify_effect_max_count) !== 0)
                    message.modify_effect_max_count = object.modify_effect_max_count | 0;
            if (object.modify_goods) {
                if (!Array.isArray(object.modify_goods))
                    throw TypeError(".lq.AmuletHookResult.modify_goods: array expected");
                message.modify_goods = Array(object.modify_goods.length);
                for (let i = 0; i < object.modify_goods.length; ++i) {
                    if (typeof object.modify_goods[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.modify_goods: object expected");
                    message.modify_goods[i] = $root.lq.AmuletGameShopGoods.fromObject(object.modify_goods[i], _depth + 1);
                }
            }
            if (object.remove_goods) {
                if (!Array.isArray(object.remove_goods))
                    throw TypeError(".lq.AmuletHookResult.remove_goods: array expected");
                message.remove_goods = Array(object.remove_goods.length);
                for (let i = 0; i < object.remove_goods.length; ++i)
                    message.remove_goods[i] = object.remove_goods[i] >>> 0;
            }
            if (object.modify_base != null) {
                if (typeof object.modify_base !== "object")
                    throw TypeError(".lq.AmuletHookResult.modify_base: object expected");
                message.modify_base = $root.lq.AmuletHookResult.ValueResult.fromObject(object.modify_base, _depth + 1);
            }
            if (object.modify_fan != null) {
                if (typeof object.modify_fan !== "object")
                    throw TypeError(".lq.AmuletHookResult.modify_fan: object expected");
                message.modify_fan = $root.lq.AmuletHookResult.ValueResult.fromObject(object.modify_fan, _depth + 1);
            }
            if (object.modify_fan_info) {
                if (!Array.isArray(object.modify_fan_info))
                    throw TypeError(".lq.AmuletHookResult.modify_fan_info: array expected");
                message.modify_fan_info = Array(object.modify_fan_info.length);
                for (let i = 0; i < object.modify_fan_info.length; ++i) {
                    if (typeof object.modify_fan_info[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.modify_fan_info: object expected");
                    message.modify_fan_info[i] = $root.lq.AmuletFan.fromObject(object.modify_fan_info[i], _depth + 1);
                }
            }
            if (object.transform_effect) {
                if (!Array.isArray(object.transform_effect))
                    throw TypeError(".lq.AmuletHookResult.transform_effect: array expected");
                message.transform_effect = Array(object.transform_effect.length);
                for (let i = 0; i < object.transform_effect.length; ++i) {
                    if (typeof object.transform_effect[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.transform_effect: object expected");
                    message.transform_effect[i] = $root.lq.AmuletHookResult.TransformResult.fromObject(object.transform_effect[i], _depth + 1);
                }
            }
            if (object.add_badge) {
                if (!Array.isArray(object.add_badge))
                    throw TypeError(".lq.AmuletHookResult.add_badge: array expected");
                message.add_badge = Array(object.add_badge.length);
                for (let i = 0; i < object.add_badge.length; ++i) {
                    if (typeof object.add_badge[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.add_badge: object expected");
                    message.add_badge[i] = $root.lq.AmuletHookResult.AddBadge.fromObject(object.add_badge[i], _depth + 1);
                }
            }
            if (object.remove_badge) {
                if (!Array.isArray(object.remove_badge))
                    throw TypeError(".lq.AmuletHookResult.remove_badge: array expected");
                message.remove_badge = Array(object.remove_badge.length);
                for (let i = 0; i < object.remove_badge.length; ++i)
                    message.remove_badge[i] = object.remove_badge[i] >>> 0;
            }
            if (object.modify_effect_price != null)
                if (typeof object.modify_effect_price !== "string" || object.modify_effect_price.length)
                    message.modify_effect_price = String(object.modify_effect_price);
            if (object.copy_effect) {
                if (!Array.isArray(object.copy_effect))
                    throw TypeError(".lq.AmuletHookResult.copy_effect: array expected");
                message.copy_effect = Array(object.copy_effect.length);
                for (let i = 0; i < object.copy_effect.length; ++i) {
                    if (typeof object.copy_effect[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.copy_effect: object expected");
                    message.copy_effect[i] = $root.lq.AmuletHookResult.CopyEffect.fromObject(object.copy_effect[i], _depth + 1);
                }
            }
            if (object.effect_growth != null)
                if (object.effect_growth)
                    message.effect_growth = Boolean(object.effect_growth);
            if (object.modify_tile_score_aura != null)
                if (typeof object.modify_tile_score_aura !== "string" || object.modify_tile_score_aura.length)
                    message.modify_tile_score_aura = String(object.modify_tile_score_aura);
            if (object.modify_hule_count != null)
                if (Number(object.modify_hule_count) !== 0)
                    message.modify_hule_count = object.modify_hule_count >>> 0;
            if (object.can_gang != null)
                if (object.can_gang)
                    message.can_gang = Boolean(object.can_gang);
            if (object.modify_change_hands_list) {
                if (!Array.isArray(object.modify_change_hands_list))
                    throw TypeError(".lq.AmuletHookResult.modify_change_hands_list: array expected");
                message.modify_change_hands_list = Array(object.modify_change_hands_list.length);
                for (let i = 0; i < object.modify_change_hands_list.length; ++i)
                    message.modify_change_hands_list[i] = object.modify_change_hands_list[i] >>> 0;
            }
            if (object.modify_change_desktop != null) {
                if (typeof object.modify_change_desktop !== "object")
                    throw TypeError(".lq.AmuletHookResult.modify_change_desktop: object expected");
                message.modify_change_desktop = $root.lq.AmuletHookResult.AmuletChangeDesktopResult.fromObject(object.modify_change_desktop, _depth + 1);
            }
            if (object.self_effect_id != null)
                if (Number(object.self_effect_id) !== 0)
                    message.self_effect_id = object.self_effect_id >>> 0;
            if (object.modify_change_coin != null)
                if (typeof object.modify_change_coin !== "string" || object.modify_change_coin.length)
                    message.modify_change_coin = String(object.modify_change_coin);
            if (object.set_tile_score) {
                if (!Array.isArray(object.set_tile_score))
                    throw TypeError(".lq.AmuletHookResult.set_tile_score: array expected");
                message.set_tile_score = Array(object.set_tile_score.length);
                for (let i = 0; i < object.set_tile_score.length; ++i) {
                    if (typeof object.set_tile_score[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.set_tile_score: object expected");
                    message.set_tile_score[i] = $root.lq.AmuletTileScore.fromObject(object.set_tile_score[i], _depth + 1);
                }
            }
            if (object.upgrade_effect) {
                if (!Array.isArray(object.upgrade_effect))
                    throw TypeError(".lq.AmuletHookResult.upgrade_effect: array expected");
                message.upgrade_effect = Array(object.upgrade_effect.length);
                for (let i = 0; i < object.upgrade_effect.length; ++i) {
                    if (typeof object.upgrade_effect[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.upgrade_effect: object expected");
                    message.upgrade_effect[i] = $root.lq.AmuletHookResult.UpgradeEffectResult.fromObject(object.upgrade_effect[i], _depth + 1);
                }
            }
            if (object.modify_tile_base_score) {
                if (!Array.isArray(object.modify_tile_base_score))
                    throw TypeError(".lq.AmuletHookResult.modify_tile_base_score: array expected");
                message.modify_tile_base_score = Array(object.modify_tile_base_score.length);
                for (let i = 0; i < object.modify_tile_base_score.length; ++i) {
                    if (typeof object.modify_tile_base_score[i] !== "object")
                        throw TypeError(".lq.AmuletHookResult.modify_tile_base_score: object expected");
                    message.modify_tile_base_score[i] = $root.lq.AmuletTileScore.fromObject(object.modify_tile_base_score[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletHookResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.add_effect = [];
                object.remove_effect = [];
                object.add_buff = [];
                object.remove_buff = [];
                object.add_tian_dora = [];
                object.tile_replace = [];
                object.add_show_tile = [];
                object.modify_tile_score = [];
                object.modify_dora = [];
                object.modify_shop_pack_effect = [];
                object.modify_goods = [];
                object.remove_goods = [];
                object.modify_fan_info = [];
                object.transform_effect = [];
                object.add_badge = [];
                object.remove_badge = [];
                object.copy_effect = [];
                object.modify_change_hands_list = [];
                object.set_tile_score = [];
                object.upgrade_effect = [];
                object.modify_tile_base_score = [];
            }
            if (options.defaults) {
                object.add_dora = null;
                object.coin_modify = null;
                object.modify_desktop_count = 0;
                object.modify_show_desktop_count = 0;
                object.modify_lock_tile_count = 0;
                object.modify_change_hands_count = 0;
                object.modify_change_hands_tile_count = 0;
                object.force_moqie = false;
                object.replace_hu = false;
                object.modify_target_point = "";
                object.upgrade_level = false;
                object.modify_dora_max_count = 0;
                object.modify_shop_goods_count = 0;
                object.modify_shop_rare_weight = false;
                object.modify_shop_goods_price = false;
                object.modify_effect_max_count = 0;
                object.modify_base = null;
                object.modify_fan = null;
                object.modify_effect_price = "";
                object.effect_growth = false;
                object.modify_tile_score_aura = "";
                object.modify_hule_count = 0;
                object.can_gang = false;
                object.modify_change_desktop = null;
                object.self_effect_id = 0;
                object.modify_change_coin = "";
            }
            if (message.add_effect && message.add_effect.length) {
                object.add_effect = Array(message.add_effect.length);
                for (let j = 0; j < message.add_effect.length; ++j)
                    object.add_effect[j] = $root.lq.AmuletHookResult.AddEffectResult.toObject(message.add_effect[j], options);
            }
            if (message.remove_effect && message.remove_effect.length) {
                object.remove_effect = Array(message.remove_effect.length);
                for (let j = 0; j < message.remove_effect.length; ++j)
                    object.remove_effect[j] = message.remove_effect[j];
            }
            if (message.add_buff && message.add_buff.length) {
                object.add_buff = Array(message.add_buff.length);
                for (let j = 0; j < message.add_buff.length; ++j)
                    object.add_buff[j] = message.add_buff[j];
            }
            if (message.remove_buff && message.remove_buff.length) {
                object.remove_buff = Array(message.remove_buff.length);
                for (let j = 0; j < message.remove_buff.length; ++j)
                    object.remove_buff[j] = message.remove_buff[j];
            }
            if (message.add_tian_dora && message.add_tian_dora.length) {
                object.add_tian_dora = Array(message.add_tian_dora.length);
                for (let j = 0; j < message.add_tian_dora.length; ++j)
                    object.add_tian_dora[j] = message.add_tian_dora[j];
            }
            if (message.add_dora != null && message.hasOwnProperty("add_dora"))
                object.add_dora = $root.lq.AmuletHookResult.AddDoraResult.toObject(message.add_dora, options);
            if (message.coin_modify != null && message.hasOwnProperty("coin_modify"))
                object.coin_modify = $root.lq.AmuletHookResult.ValueResult.toObject(message.coin_modify, options);
            if (message.tile_replace && message.tile_replace.length) {
                object.tile_replace = Array(message.tile_replace.length);
                for (let j = 0; j < message.tile_replace.length; ++j)
                    object.tile_replace[j] = $root.lq.AmuletTile.toObject(message.tile_replace[j], options);
            }
            if (message.add_show_tile && message.add_show_tile.length) {
                object.add_show_tile = Array(message.add_show_tile.length);
                for (let j = 0; j < message.add_show_tile.length; ++j)
                    object.add_show_tile[j] = message.add_show_tile[j];
            }
            if (message.modify_tile_score && message.modify_tile_score.length) {
                object.modify_tile_score = Array(message.modify_tile_score.length);
                for (let j = 0; j < message.modify_tile_score.length; ++j)
                    object.modify_tile_score[j] = $root.lq.AmuletTileScore.toObject(message.modify_tile_score[j], options);
            }
            if (message.modify_desktop_count != null && message.hasOwnProperty("modify_desktop_count"))
                object.modify_desktop_count = message.modify_desktop_count;
            if (message.modify_show_desktop_count != null && message.hasOwnProperty("modify_show_desktop_count"))
                object.modify_show_desktop_count = message.modify_show_desktop_count;
            if (message.modify_lock_tile_count != null && message.hasOwnProperty("modify_lock_tile_count"))
                object.modify_lock_tile_count = message.modify_lock_tile_count;
            if (message.modify_change_hands_count != null && message.hasOwnProperty("modify_change_hands_count"))
                object.modify_change_hands_count = message.modify_change_hands_count;
            if (message.modify_change_hands_tile_count != null && message.hasOwnProperty("modify_change_hands_tile_count"))
                object.modify_change_hands_tile_count = message.modify_change_hands_tile_count;
            if (message.force_moqie != null && message.hasOwnProperty("force_moqie"))
                object.force_moqie = message.force_moqie;
            if (message.replace_hu != null && message.hasOwnProperty("replace_hu"))
                object.replace_hu = message.replace_hu;
            if (message.modify_target_point != null && message.hasOwnProperty("modify_target_point"))
                object.modify_target_point = message.modify_target_point;
            if (message.upgrade_level != null && message.hasOwnProperty("upgrade_level"))
                object.upgrade_level = message.upgrade_level;
            if (message.modify_dora && message.modify_dora.length) {
                object.modify_dora = Array(message.modify_dora.length);
                for (let j = 0; j < message.modify_dora.length; ++j)
                    object.modify_dora[j] = $root.lq.AmuletHookResult.ModifyDoraResult.toObject(message.modify_dora[j], options);
            }
            if (message.modify_dora_max_count != null && message.hasOwnProperty("modify_dora_max_count"))
                object.modify_dora_max_count = message.modify_dora_max_count;
            if (message.modify_shop_goods_count != null && message.hasOwnProperty("modify_shop_goods_count"))
                object.modify_shop_goods_count = message.modify_shop_goods_count;
            if (message.modify_shop_rare_weight != null && message.hasOwnProperty("modify_shop_rare_weight"))
                object.modify_shop_rare_weight = message.modify_shop_rare_weight;
            if (message.modify_shop_goods_price != null && message.hasOwnProperty("modify_shop_goods_price"))
                object.modify_shop_goods_price = message.modify_shop_goods_price;
            if (message.modify_shop_pack_effect && message.modify_shop_pack_effect.length) {
                object.modify_shop_pack_effect = Array(message.modify_shop_pack_effect.length);
                for (let j = 0; j < message.modify_shop_pack_effect.length; ++j)
                    object.modify_shop_pack_effect[j] = message.modify_shop_pack_effect[j];
            }
            if (message.modify_effect_max_count != null && message.hasOwnProperty("modify_effect_max_count"))
                object.modify_effect_max_count = message.modify_effect_max_count;
            if (message.modify_goods && message.modify_goods.length) {
                object.modify_goods = Array(message.modify_goods.length);
                for (let j = 0; j < message.modify_goods.length; ++j)
                    object.modify_goods[j] = $root.lq.AmuletGameShopGoods.toObject(message.modify_goods[j], options);
            }
            if (message.remove_goods && message.remove_goods.length) {
                object.remove_goods = Array(message.remove_goods.length);
                for (let j = 0; j < message.remove_goods.length; ++j)
                    object.remove_goods[j] = message.remove_goods[j];
            }
            if (message.modify_base != null && message.hasOwnProperty("modify_base"))
                object.modify_base = $root.lq.AmuletHookResult.ValueResult.toObject(message.modify_base, options);
            if (message.modify_fan != null && message.hasOwnProperty("modify_fan"))
                object.modify_fan = $root.lq.AmuletHookResult.ValueResult.toObject(message.modify_fan, options);
            if (message.modify_fan_info && message.modify_fan_info.length) {
                object.modify_fan_info = Array(message.modify_fan_info.length);
                for (let j = 0; j < message.modify_fan_info.length; ++j)
                    object.modify_fan_info[j] = $root.lq.AmuletFan.toObject(message.modify_fan_info[j], options);
            }
            if (message.transform_effect && message.transform_effect.length) {
                object.transform_effect = Array(message.transform_effect.length);
                for (let j = 0; j < message.transform_effect.length; ++j)
                    object.transform_effect[j] = $root.lq.AmuletHookResult.TransformResult.toObject(message.transform_effect[j], options);
            }
            if (message.add_badge && message.add_badge.length) {
                object.add_badge = Array(message.add_badge.length);
                for (let j = 0; j < message.add_badge.length; ++j)
                    object.add_badge[j] = $root.lq.AmuletHookResult.AddBadge.toObject(message.add_badge[j], options);
            }
            if (message.remove_badge && message.remove_badge.length) {
                object.remove_badge = Array(message.remove_badge.length);
                for (let j = 0; j < message.remove_badge.length; ++j)
                    object.remove_badge[j] = message.remove_badge[j];
            }
            if (message.modify_effect_price != null && message.hasOwnProperty("modify_effect_price"))
                object.modify_effect_price = message.modify_effect_price;
            if (message.copy_effect && message.copy_effect.length) {
                object.copy_effect = Array(message.copy_effect.length);
                for (let j = 0; j < message.copy_effect.length; ++j)
                    object.copy_effect[j] = $root.lq.AmuletHookResult.CopyEffect.toObject(message.copy_effect[j], options);
            }
            if (message.effect_growth != null && message.hasOwnProperty("effect_growth"))
                object.effect_growth = message.effect_growth;
            if (message.modify_tile_score_aura != null && message.hasOwnProperty("modify_tile_score_aura"))
                object.modify_tile_score_aura = message.modify_tile_score_aura;
            if (message.modify_hule_count != null && message.hasOwnProperty("modify_hule_count"))
                object.modify_hule_count = message.modify_hule_count;
            if (message.can_gang != null && message.hasOwnProperty("can_gang"))
                object.can_gang = message.can_gang;
            if (message.modify_change_hands_list && message.modify_change_hands_list.length) {
                object.modify_change_hands_list = Array(message.modify_change_hands_list.length);
                for (let j = 0; j < message.modify_change_hands_list.length; ++j)
                    object.modify_change_hands_list[j] = message.modify_change_hands_list[j];
            }
            if (message.modify_change_desktop != null && message.hasOwnProperty("modify_change_desktop"))
                object.modify_change_desktop = $root.lq.AmuletHookResult.AmuletChangeDesktopResult.toObject(message.modify_change_desktop, options);
            if (message.self_effect_id != null && message.hasOwnProperty("self_effect_id"))
                object.self_effect_id = message.self_effect_id;
            if (message.modify_change_coin != null && message.hasOwnProperty("modify_change_coin"))
                object.modify_change_coin = message.modify_change_coin;
            if (message.set_tile_score && message.set_tile_score.length) {
                object.set_tile_score = Array(message.set_tile_score.length);
                for (let j = 0; j < message.set_tile_score.length; ++j)
                    object.set_tile_score[j] = $root.lq.AmuletTileScore.toObject(message.set_tile_score[j], options);
            }
            if (message.upgrade_effect && message.upgrade_effect.length) {
                object.upgrade_effect = Array(message.upgrade_effect.length);
                for (let j = 0; j < message.upgrade_effect.length; ++j)
                    object.upgrade_effect[j] = $root.lq.AmuletHookResult.UpgradeEffectResult.toObject(message.upgrade_effect[j], options);
            }
            if (message.modify_tile_base_score && message.modify_tile_base_score.length) {
                object.modify_tile_base_score = Array(message.modify_tile_base_score.length);
                for (let j = 0; j < message.modify_tile_base_score.length; ++j)
                    object.modify_tile_base_score[j] = $root.lq.AmuletTileScore.toObject(message.modify_tile_base_score[j], options);
            }
            return object;
        };

        AmuletHookResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletHookResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletHookResult";
        };

        AmuletHookResult.AddEffectResult = (function() {

            function AddEffectResult(properties) {
                this.merged_list = [];
                this.store = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AddEffectResult.prototype.uid = 0;
            AddEffectResult.prototype.id = 0;
            AddEffectResult.prototype.merge_type = 0;
            AddEffectResult.prototype.merged_list = $util.emptyArray;
            AddEffectResult.prototype.merged_result = 0;
            AddEffectResult.prototype.badge = null;
            AddEffectResult.prototype.store = $util.emptyArray;
            AddEffectResult.prototype.volume = 0;

            AddEffectResult.create = function create(properties) {
                return new AddEffectResult(properties);
            };

            AddEffectResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(16).uint32(message.id);
                if (message.merge_type != null && Object.hasOwnProperty.call(message, "merge_type"))
                    writer.uint32(24).uint32(message.merge_type);
                if (message.merged_list != null && message.merged_list.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.merged_list.length; ++i)
                        writer.uint32(message.merged_list[i]);
                    writer.ldelim();
                }
                if (message.merged_result != null && Object.hasOwnProperty.call(message, "merged_result"))
                    writer.uint32(40).uint32(message.merged_result);
                if (message.badge != null && Object.hasOwnProperty.call(message, "badge"))
                    $root.lq.AmuletBadgeData.encode(message.badge, writer.uint32(50).fork()).ldelim();
                if (message.store != null && message.store.length)
                    for (let i = 0; i < message.store.length; ++i)
                        writer.uint32(58).string(message.store[i]);
                if (message.volume != null && Object.hasOwnProperty.call(message, "volume"))
                    writer.uint32(64).uint32(message.volume);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AddEffectResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AddEffectResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.AddEffectResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.merge_type = value;
                            else
                                delete message.merge_type;
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.merged_list && message.merged_list.length))
                                    message.merged_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.merged_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.merged_list && message.merged_list.length))
                                message.merged_list = [];
                            message.merged_list.push(reader.uint32());
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.merged_result = value;
                            else
                                delete message.merged_result;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.badge = $root.lq.AmuletBadgeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.badge);
                            continue;
                        }
                    case 7: {
                            if (wireType !== 2)
                                break;
                            if (!(message.store && message.store.length))
                                message.store = [];
                            message.store.push(reader.string());
                            continue;
                        }
                    case 8: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.volume = value;
                            else
                                delete message.volume;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AddEffectResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AddEffectResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.merge_type != null && message.hasOwnProperty("merge_type"))
                    if (!$util.isInteger(message.merge_type))
                        return "merge_type: integer expected";
                if (message.merged_list != null && message.hasOwnProperty("merged_list")) {
                    if (!Array.isArray(message.merged_list))
                        return "merged_list: array expected";
                    for (let i = 0; i < message.merged_list.length; ++i)
                        if (!$util.isInteger(message.merged_list[i]))
                            return "merged_list: integer[] expected";
                }
                if (message.merged_result != null && message.hasOwnProperty("merged_result"))
                    if (!$util.isInteger(message.merged_result))
                        return "merged_result: integer expected";
                if (message.badge != null && message.hasOwnProperty("badge")) {
                    let error = $root.lq.AmuletBadgeData.verify(message.badge, _depth + 1);
                    if (error)
                        return "badge." + error;
                }
                if (message.store != null && message.hasOwnProperty("store")) {
                    if (!Array.isArray(message.store))
                        return "store: array expected";
                    for (let i = 0; i < message.store.length; ++i)
                        if (!$util.isString(message.store[i]))
                            return "store: string[] expected";
                }
                if (message.volume != null && message.hasOwnProperty("volume"))
                    if (!$util.isInteger(message.volume))
                        return "volume: integer expected";
                return null;
            };

            AddEffectResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.AddEffectResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.AddEffectResult();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.merge_type != null)
                    if (Number(object.merge_type) !== 0)
                        message.merge_type = object.merge_type >>> 0;
                if (object.merged_list) {
                    if (!Array.isArray(object.merged_list))
                        throw TypeError(".lq.AmuletHookResult.AddEffectResult.merged_list: array expected");
                    message.merged_list = Array(object.merged_list.length);
                    for (let i = 0; i < object.merged_list.length; ++i)
                        message.merged_list[i] = object.merged_list[i] >>> 0;
                }
                if (object.merged_result != null)
                    if (Number(object.merged_result) !== 0)
                        message.merged_result = object.merged_result >>> 0;
                if (object.badge != null) {
                    if (typeof object.badge !== "object")
                        throw TypeError(".lq.AmuletHookResult.AddEffectResult.badge: object expected");
                    message.badge = $root.lq.AmuletBadgeData.fromObject(object.badge, _depth + 1);
                }
                if (object.store) {
                    if (!Array.isArray(object.store))
                        throw TypeError(".lq.AmuletHookResult.AddEffectResult.store: array expected");
                    message.store = Array(object.store.length);
                    for (let i = 0; i < object.store.length; ++i)
                        message.store[i] = String(object.store[i]);
                }
                if (object.volume != null)
                    if (Number(object.volume) !== 0)
                        message.volume = object.volume >>> 0;
                return message;
            };

            AddEffectResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.merged_list = [];
                    object.store = [];
                }
                if (options.defaults) {
                    object.uid = 0;
                    object.id = 0;
                    object.merge_type = 0;
                    object.merged_result = 0;
                    object.badge = null;
                    object.volume = 0;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.merge_type != null && message.hasOwnProperty("merge_type"))
                    object.merge_type = message.merge_type;
                if (message.merged_list && message.merged_list.length) {
                    object.merged_list = Array(message.merged_list.length);
                    for (let j = 0; j < message.merged_list.length; ++j)
                        object.merged_list[j] = message.merged_list[j];
                }
                if (message.merged_result != null && message.hasOwnProperty("merged_result"))
                    object.merged_result = message.merged_result;
                if (message.badge != null && message.hasOwnProperty("badge"))
                    object.badge = $root.lq.AmuletBadgeData.toObject(message.badge, options);
                if (message.store && message.store.length) {
                    object.store = Array(message.store.length);
                    for (let j = 0; j < message.store.length; ++j)
                        object.store[j] = message.store[j];
                }
                if (message.volume != null && message.hasOwnProperty("volume"))
                    object.volume = message.volume;
                return object;
            };

            AddEffectResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AddEffectResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.AddEffectResult";
            };

            return AddEffectResult;
        })();

        AmuletHookResult.AddDoraResult = (function() {

            function AddDoraResult(properties) {
                this.list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AddDoraResult.prototype.count = 0;
            AddDoraResult.prototype.list = $util.emptyArray;

            AddDoraResult.create = function create(properties) {
                return new AddDoraResult(properties);
            };

            AddDoraResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(8).uint32(message.count);
                if (message.list != null && message.list.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.list.length; ++i)
                        writer.uint32(message.list[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AddDoraResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AddDoraResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.AddDoraResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.count = value;
                            else
                                delete message.count;
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.list && message.list.length))
                                    message.list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.list && message.list.length))
                                message.list = [];
                            message.list.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AddDoraResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AddDoraResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.count != null && message.hasOwnProperty("count"))
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                if (message.list != null && message.hasOwnProperty("list")) {
                    if (!Array.isArray(message.list))
                        return "list: array expected";
                    for (let i = 0; i < message.list.length; ++i)
                        if (!$util.isInteger(message.list[i]))
                            return "list: integer[] expected";
                }
                return null;
            };

            AddDoraResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.AddDoraResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.AddDoraResult();
                if (object.count != null)
                    if (Number(object.count) !== 0)
                        message.count = object.count >>> 0;
                if (object.list) {
                    if (!Array.isArray(object.list))
                        throw TypeError(".lq.AmuletHookResult.AddDoraResult.list: array expected");
                    message.list = Array(object.list.length);
                    for (let i = 0; i < object.list.length; ++i)
                        message.list[i] = object.list[i] >>> 0;
                }
                return message;
            };

            AddDoraResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.list = [];
                if (options.defaults)
                    object.count = 0;
                if (message.count != null && message.hasOwnProperty("count"))
                    object.count = message.count;
                if (message.list && message.list.length) {
                    object.list = Array(message.list.length);
                    for (let j = 0; j < message.list.length; ++j)
                        object.list[j] = message.list[j];
                }
                return object;
            };

            AddDoraResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AddDoraResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.AddDoraResult";
            };

            return AddDoraResult;
        })();

        AmuletHookResult.ValueResult = (function() {

            function ValueResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ValueResult.prototype.origin = "";
            ValueResult.prototype.modify = "";
            ValueResult.prototype.final = "";

            ValueResult.create = function create(properties) {
                return new ValueResult(properties);
            };

            ValueResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    writer.uint32(10).string(message.origin);
                if (message.modify != null && Object.hasOwnProperty.call(message, "modify"))
                    writer.uint32(18).string(message.modify);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(26).string(message.final);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ValueResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ValueResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.ValueResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.origin = value;
                            else
                                delete message.origin;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.modify = value;
                            else
                                delete message.modify;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ValueResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ValueResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.origin != null && message.hasOwnProperty("origin"))
                    if (!$util.isString(message.origin))
                        return "origin: string expected";
                if (message.modify != null && message.hasOwnProperty("modify"))
                    if (!$util.isString(message.modify))
                        return "modify: string expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isString(message.final))
                        return "final: string expected";
                return null;
            };

            ValueResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.ValueResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.ValueResult();
                if (object.origin != null)
                    if (typeof object.origin !== "string" || object.origin.length)
                        message.origin = String(object.origin);
                if (object.modify != null)
                    if (typeof object.modify !== "string" || object.modify.length)
                        message.modify = String(object.modify);
                if (object.final != null)
                    if (typeof object.final !== "string" || object.final.length)
                        message.final = String(object.final);
                return message;
            };

            ValueResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.origin = "";
                    object.modify = "";
                    object.final = "";
                }
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = message.origin;
                if (message.modify != null && message.hasOwnProperty("modify"))
                    object.modify = message.modify;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                return object;
            };

            ValueResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ValueResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.ValueResult";
            };

            return ValueResult;
        })();

        AmuletHookResult.ModifyDoraResult = (function() {

            function ModifyDoraResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ModifyDoraResult.prototype.tile = "";
            ModifyDoraResult.prototype.is_dora = false;
            ModifyDoraResult.prototype.is_red_dora = false;
            ModifyDoraResult.prototype.is_tian_dora = false;
            ModifyDoraResult.prototype.dora_count = 0;

            ModifyDoraResult.create = function create(properties) {
                return new ModifyDoraResult(properties);
            };

            ModifyDoraResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                    writer.uint32(10).string(message.tile);
                if (message.is_dora != null && Object.hasOwnProperty.call(message, "is_dora"))
                    writer.uint32(16).bool(message.is_dora);
                if (message.is_red_dora != null && Object.hasOwnProperty.call(message, "is_red_dora"))
                    writer.uint32(24).bool(message.is_red_dora);
                if (message.is_tian_dora != null && Object.hasOwnProperty.call(message, "is_tian_dora"))
                    writer.uint32(32).bool(message.is_tian_dora);
                if (message.dora_count != null && Object.hasOwnProperty.call(message, "dora_count"))
                    writer.uint32(40).uint32(message.dora_count);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ModifyDoraResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ModifyDoraResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.ModifyDoraResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.tile = value;
                            else
                                delete message.tile;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_dora = value;
                            else
                                delete message.is_dora;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_red_dora = value;
                            else
                                delete message.is_red_dora;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_tian_dora = value;
                            else
                                delete message.is_tian_dora;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.dora_count = value;
                            else
                                delete message.dora_count;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ModifyDoraResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ModifyDoraResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tile != null && message.hasOwnProperty("tile"))
                    if (!$util.isString(message.tile))
                        return "tile: string expected";
                if (message.is_dora != null && message.hasOwnProperty("is_dora"))
                    if (typeof message.is_dora !== "boolean")
                        return "is_dora: boolean expected";
                if (message.is_red_dora != null && message.hasOwnProperty("is_red_dora"))
                    if (typeof message.is_red_dora !== "boolean")
                        return "is_red_dora: boolean expected";
                if (message.is_tian_dora != null && message.hasOwnProperty("is_tian_dora"))
                    if (typeof message.is_tian_dora !== "boolean")
                        return "is_tian_dora: boolean expected";
                if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                    if (!$util.isInteger(message.dora_count))
                        return "dora_count: integer expected";
                return null;
            };

            ModifyDoraResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.ModifyDoraResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.ModifyDoraResult();
                if (object.tile != null)
                    if (typeof object.tile !== "string" || object.tile.length)
                        message.tile = String(object.tile);
                if (object.is_dora != null)
                    if (object.is_dora)
                        message.is_dora = Boolean(object.is_dora);
                if (object.is_red_dora != null)
                    if (object.is_red_dora)
                        message.is_red_dora = Boolean(object.is_red_dora);
                if (object.is_tian_dora != null)
                    if (object.is_tian_dora)
                        message.is_tian_dora = Boolean(object.is_tian_dora);
                if (object.dora_count != null)
                    if (Number(object.dora_count) !== 0)
                        message.dora_count = object.dora_count >>> 0;
                return message;
            };

            ModifyDoraResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.tile = "";
                    object.is_dora = false;
                    object.is_red_dora = false;
                    object.is_tian_dora = false;
                    object.dora_count = 0;
                }
                if (message.tile != null && message.hasOwnProperty("tile"))
                    object.tile = message.tile;
                if (message.is_dora != null && message.hasOwnProperty("is_dora"))
                    object.is_dora = message.is_dora;
                if (message.is_red_dora != null && message.hasOwnProperty("is_red_dora"))
                    object.is_red_dora = message.is_red_dora;
                if (message.is_tian_dora != null && message.hasOwnProperty("is_tian_dora"))
                    object.is_tian_dora = message.is_tian_dora;
                if (message.dora_count != null && message.hasOwnProperty("dora_count"))
                    object.dora_count = message.dora_count;
                return object;
            };

            ModifyDoraResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ModifyDoraResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.ModifyDoraResult";
            };

            return ModifyDoraResult;
        })();

        AmuletHookResult.TransformResult = (function() {

            function TransformResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TransformResult.prototype.uid = 0;
            TransformResult.prototype.effect_id = 0;
            TransformResult.prototype.add_result = null;

            TransformResult.create = function create(properties) {
                return new TransformResult(properties);
            };

            TransformResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.effect_id != null && Object.hasOwnProperty.call(message, "effect_id"))
                    writer.uint32(16).uint32(message.effect_id);
                if (message.add_result != null && Object.hasOwnProperty.call(message, "add_result"))
                    $root.lq.AmuletHookResult.AddEffectResult.encode(message.add_result, writer.uint32(26).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TransformResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TransformResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.TransformResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.effect_id = value;
                            else
                                delete message.effect_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.add_result = $root.lq.AmuletHookResult.AddEffectResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.add_result);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TransformResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TransformResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                    if (!$util.isInteger(message.effect_id))
                        return "effect_id: integer expected";
                if (message.add_result != null && message.hasOwnProperty("add_result")) {
                    let error = $root.lq.AmuletHookResult.AddEffectResult.verify(message.add_result, _depth + 1);
                    if (error)
                        return "add_result." + error;
                }
                return null;
            };

            TransformResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.TransformResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.TransformResult();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.effect_id != null)
                    if (Number(object.effect_id) !== 0)
                        message.effect_id = object.effect_id >>> 0;
                if (object.add_result != null) {
                    if (typeof object.add_result !== "object")
                        throw TypeError(".lq.AmuletHookResult.TransformResult.add_result: object expected");
                    message.add_result = $root.lq.AmuletHookResult.AddEffectResult.fromObject(object.add_result, _depth + 1);
                }
                return message;
            };

            TransformResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.uid = 0;
                    object.effect_id = 0;
                    object.add_result = null;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.effect_id != null && message.hasOwnProperty("effect_id"))
                    object.effect_id = message.effect_id;
                if (message.add_result != null && message.hasOwnProperty("add_result"))
                    object.add_result = $root.lq.AmuletHookResult.AddEffectResult.toObject(message.add_result, options);
                return object;
            };

            TransformResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TransformResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.TransformResult";
            };

            return TransformResult;
        })();

        AmuletHookResult.AddBadge = (function() {

            function AddBadge(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AddBadge.prototype.uid = 0;
            AddBadge.prototype.badge_id = 0;
            AddBadge.prototype.badge_uid = 0;

            AddBadge.create = function create(properties) {
                return new AddBadge(properties);
            };

            AddBadge.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.badge_id != null && Object.hasOwnProperty.call(message, "badge_id"))
                    writer.uint32(16).uint32(message.badge_id);
                if (message.badge_uid != null && Object.hasOwnProperty.call(message, "badge_uid"))
                    writer.uint32(24).uint32(message.badge_uid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AddBadge.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AddBadge.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.AddBadge(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.badge_id = value;
                            else
                                delete message.badge_id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.badge_uid = value;
                            else
                                delete message.badge_uid;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AddBadge.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AddBadge.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                    if (!$util.isInteger(message.badge_id))
                        return "badge_id: integer expected";
                if (message.badge_uid != null && message.hasOwnProperty("badge_uid"))
                    if (!$util.isInteger(message.badge_uid))
                        return "badge_uid: integer expected";
                return null;
            };

            AddBadge.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.AddBadge)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.AddBadge();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.badge_id != null)
                    if (Number(object.badge_id) !== 0)
                        message.badge_id = object.badge_id >>> 0;
                if (object.badge_uid != null)
                    if (Number(object.badge_uid) !== 0)
                        message.badge_uid = object.badge_uid >>> 0;
                return message;
            };

            AddBadge.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.uid = 0;
                    object.badge_id = 0;
                    object.badge_uid = 0;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.badge_id != null && message.hasOwnProperty("badge_id"))
                    object.badge_id = message.badge_id;
                if (message.badge_uid != null && message.hasOwnProperty("badge_uid"))
                    object.badge_uid = message.badge_uid;
                return object;
            };

            AddBadge.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AddBadge.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.AddBadge";
            };

            return AddBadge;
        })();

        AmuletHookResult.CopyEffect = (function() {

            function CopyEffect(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CopyEffect.prototype.uid = 0;
            CopyEffect.prototype.from_uid = 0;

            CopyEffect.create = function create(properties) {
                return new CopyEffect(properties);
            };

            CopyEffect.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.from_uid != null && Object.hasOwnProperty.call(message, "from_uid"))
                    writer.uint32(16).uint32(message.from_uid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CopyEffect.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CopyEffect.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.CopyEffect(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.from_uid = value;
                            else
                                delete message.from_uid;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CopyEffect.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CopyEffect.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.from_uid != null && message.hasOwnProperty("from_uid"))
                    if (!$util.isInteger(message.from_uid))
                        return "from_uid: integer expected";
                return null;
            };

            CopyEffect.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.CopyEffect)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.CopyEffect();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.from_uid != null)
                    if (Number(object.from_uid) !== 0)
                        message.from_uid = object.from_uid >>> 0;
                return message;
            };

            CopyEffect.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.uid = 0;
                    object.from_uid = 0;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.from_uid != null && message.hasOwnProperty("from_uid"))
                    object.from_uid = message.from_uid;
                return object;
            };

            CopyEffect.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CopyEffect.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.CopyEffect";
            };

            return CopyEffect;
        })();

        AmuletHookResult.AmuletChangeDesktopResult = (function() {

            function AmuletChangeDesktopResult(properties) {
                this.show_desktop_tiles = [];
                this.locked_tile = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            AmuletChangeDesktopResult.prototype.show_desktop_tiles = $util.emptyArray;
            AmuletChangeDesktopResult.prototype.locked_tile_count = 0;
            AmuletChangeDesktopResult.prototype.desktop_remain = 0;
            AmuletChangeDesktopResult.prototype.locked_tile = $util.emptyArray;

            AmuletChangeDesktopResult.create = function create(properties) {
                return new AmuletChangeDesktopResult(properties);
            };

            AmuletChangeDesktopResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.show_desktop_tiles != null && message.show_desktop_tiles.length)
                    for (let i = 0; i < message.show_desktop_tiles.length; ++i)
                        $root.lq.AmuletShowDesktopTileData.encode(message.show_desktop_tiles[i], writer.uint32(10).fork()).ldelim();
                if (message.locked_tile_count != null && Object.hasOwnProperty.call(message, "locked_tile_count"))
                    writer.uint32(24).uint32(message.locked_tile_count);
                if (message.desktop_remain != null && Object.hasOwnProperty.call(message, "desktop_remain"))
                    writer.uint32(32).uint32(message.desktop_remain);
                if (message.locked_tile != null && message.locked_tile.length) {
                    writer.uint32(42).fork();
                    for (let i = 0; i < message.locked_tile.length; ++i)
                        writer.uint32(message.locked_tile[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            AmuletChangeDesktopResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            AmuletChangeDesktopResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.AmuletChangeDesktopResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.show_desktop_tiles && message.show_desktop_tiles.length))
                                message.show_desktop_tiles = [];
                            message.show_desktop_tiles.push($root.lq.AmuletShowDesktopTileData.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.locked_tile_count = value;
                            else
                                delete message.locked_tile_count;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.desktop_remain = value;
                            else
                                delete message.desktop_remain;
                            continue;
                        }
                    case 5: {
                            if (wireType === 2) {
                                if (!(message.locked_tile && message.locked_tile.length))
                                    message.locked_tile = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.locked_tile.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.locked_tile && message.locked_tile.length))
                                message.locked_tile = [];
                            message.locked_tile.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            AmuletChangeDesktopResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            AmuletChangeDesktopResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.show_desktop_tiles != null && message.hasOwnProperty("show_desktop_tiles")) {
                    if (!Array.isArray(message.show_desktop_tiles))
                        return "show_desktop_tiles: array expected";
                    for (let i = 0; i < message.show_desktop_tiles.length; ++i) {
                        let error = $root.lq.AmuletShowDesktopTileData.verify(message.show_desktop_tiles[i], _depth + 1);
                        if (error)
                            return "show_desktop_tiles." + error;
                    }
                }
                if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count"))
                    if (!$util.isInteger(message.locked_tile_count))
                        return "locked_tile_count: integer expected";
                if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain"))
                    if (!$util.isInteger(message.desktop_remain))
                        return "desktop_remain: integer expected";
                if (message.locked_tile != null && message.hasOwnProperty("locked_tile")) {
                    if (!Array.isArray(message.locked_tile))
                        return "locked_tile: array expected";
                    for (let i = 0; i < message.locked_tile.length; ++i)
                        if (!$util.isInteger(message.locked_tile[i]))
                            return "locked_tile: integer[] expected";
                }
                return null;
            };

            AmuletChangeDesktopResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.AmuletChangeDesktopResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.AmuletChangeDesktopResult();
                if (object.show_desktop_tiles) {
                    if (!Array.isArray(object.show_desktop_tiles))
                        throw TypeError(".lq.AmuletHookResult.AmuletChangeDesktopResult.show_desktop_tiles: array expected");
                    message.show_desktop_tiles = Array(object.show_desktop_tiles.length);
                    for (let i = 0; i < object.show_desktop_tiles.length; ++i) {
                        if (typeof object.show_desktop_tiles[i] !== "object")
                            throw TypeError(".lq.AmuletHookResult.AmuletChangeDesktopResult.show_desktop_tiles: object expected");
                        message.show_desktop_tiles[i] = $root.lq.AmuletShowDesktopTileData.fromObject(object.show_desktop_tiles[i], _depth + 1);
                    }
                }
                if (object.locked_tile_count != null)
                    if (Number(object.locked_tile_count) !== 0)
                        message.locked_tile_count = object.locked_tile_count >>> 0;
                if (object.desktop_remain != null)
                    if (Number(object.desktop_remain) !== 0)
                        message.desktop_remain = object.desktop_remain >>> 0;
                if (object.locked_tile) {
                    if (!Array.isArray(object.locked_tile))
                        throw TypeError(".lq.AmuletHookResult.AmuletChangeDesktopResult.locked_tile: array expected");
                    message.locked_tile = Array(object.locked_tile.length);
                    for (let i = 0; i < object.locked_tile.length; ++i)
                        message.locked_tile[i] = object.locked_tile[i] >>> 0;
                }
                return message;
            };

            AmuletChangeDesktopResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.show_desktop_tiles = [];
                    object.locked_tile = [];
                }
                if (options.defaults) {
                    object.locked_tile_count = 0;
                    object.desktop_remain = 0;
                }
                if (message.show_desktop_tiles && message.show_desktop_tiles.length) {
                    object.show_desktop_tiles = Array(message.show_desktop_tiles.length);
                    for (let j = 0; j < message.show_desktop_tiles.length; ++j)
                        object.show_desktop_tiles[j] = $root.lq.AmuletShowDesktopTileData.toObject(message.show_desktop_tiles[j], options);
                }
                if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count"))
                    object.locked_tile_count = message.locked_tile_count;
                if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain"))
                    object.desktop_remain = message.desktop_remain;
                if (message.locked_tile && message.locked_tile.length) {
                    object.locked_tile = Array(message.locked_tile.length);
                    for (let j = 0; j < message.locked_tile.length; ++j)
                        object.locked_tile[j] = message.locked_tile[j];
                }
                return object;
            };

            AmuletChangeDesktopResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AmuletChangeDesktopResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.AmuletChangeDesktopResult";
            };

            return AmuletChangeDesktopResult;
        })();

        AmuletHookResult.UpgradeEffectResult = (function() {

            function UpgradeEffectResult(properties) {
                this.store = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            UpgradeEffectResult.prototype.uid = 0;
            UpgradeEffectResult.prototype.id = 0;
            UpgradeEffectResult.prototype.badge = null;
            UpgradeEffectResult.prototype.store = $util.emptyArray;
            UpgradeEffectResult.prototype.volume = 0;

            UpgradeEffectResult.create = function create(properties) {
                return new UpgradeEffectResult(properties);
            };

            UpgradeEffectResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(16).uint32(message.id);
                if (message.badge != null && Object.hasOwnProperty.call(message, "badge"))
                    $root.lq.AmuletBadgeData.encode(message.badge, writer.uint32(26).fork()).ldelim();
                if (message.store != null && message.store.length)
                    for (let i = 0; i < message.store.length; ++i)
                        writer.uint32(34).string(message.store[i]);
                if (message.volume != null && Object.hasOwnProperty.call(message, "volume"))
                    writer.uint32(40).uint32(message.volume);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            UpgradeEffectResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            UpgradeEffectResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletHookResult.UpgradeEffectResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            message.badge = $root.lq.AmuletBadgeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.badge);
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.store && message.store.length))
                                message.store = [];
                            message.store.push(reader.string());
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.volume = value;
                            else
                                delete message.volume;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            UpgradeEffectResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            UpgradeEffectResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.badge != null && message.hasOwnProperty("badge")) {
                    let error = $root.lq.AmuletBadgeData.verify(message.badge, _depth + 1);
                    if (error)
                        return "badge." + error;
                }
                if (message.store != null && message.hasOwnProperty("store")) {
                    if (!Array.isArray(message.store))
                        return "store: array expected";
                    for (let i = 0; i < message.store.length; ++i)
                        if (!$util.isString(message.store[i]))
                            return "store: string[] expected";
                }
                if (message.volume != null && message.hasOwnProperty("volume"))
                    if (!$util.isInteger(message.volume))
                        return "volume: integer expected";
                return null;
            };

            UpgradeEffectResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletHookResult.UpgradeEffectResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletHookResult.UpgradeEffectResult();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.badge != null) {
                    if (typeof object.badge !== "object")
                        throw TypeError(".lq.AmuletHookResult.UpgradeEffectResult.badge: object expected");
                    message.badge = $root.lq.AmuletBadgeData.fromObject(object.badge, _depth + 1);
                }
                if (object.store) {
                    if (!Array.isArray(object.store))
                        throw TypeError(".lq.AmuletHookResult.UpgradeEffectResult.store: array expected");
                    message.store = Array(object.store.length);
                    for (let i = 0; i < object.store.length; ++i)
                        message.store[i] = String(object.store[i]);
                }
                if (object.volume != null)
                    if (Number(object.volume) !== 0)
                        message.volume = object.volume >>> 0;
                return message;
            };

            UpgradeEffectResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.store = [];
                if (options.defaults) {
                    object.uid = 0;
                    object.id = 0;
                    object.badge = null;
                    object.volume = 0;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.badge != null && message.hasOwnProperty("badge"))
                    object.badge = $root.lq.AmuletBadgeData.toObject(message.badge, options);
                if (message.store && message.store.length) {
                    object.store = Array(message.store.length);
                    for (let j = 0; j < message.store.length; ++j)
                        object.store[j] = message.store[j];
                }
                if (message.volume != null && message.hasOwnProperty("volume"))
                    object.volume = message.volume;
                return object;
            };

            UpgradeEffectResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            UpgradeEffectResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletHookResult.UpgradeEffectResult";
            };

            return UpgradeEffectResult;
        })();

        return AmuletHookResult;
    })();

    lq.AmuletEffectedHookData = (function() {

        function AmuletEffectedHookData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectedHookData.prototype.uid = 0;
        AmuletEffectedHookData.prototype.id = 0;
        AmuletEffectedHookData.prototype.result = null;
        AmuletEffectedHookData.prototype.type = 0;

        AmuletEffectedHookData.create = function create(properties) {
            return new AmuletEffectedHookData(properties);
        };

        AmuletEffectedHookData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(8).uint32(message.uid);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(16).uint32(message.id);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.lq.AmuletHookResult.encode(message.result, writer.uint32(26).fork()).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(32).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectedHookData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectedHookData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectedHookData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.uid = value;
                        else
                            delete message.uid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.result = $root.lq.AmuletHookResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectedHookData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectedHookData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.uid != null && message.hasOwnProperty("uid"))
                if (!$util.isInteger(message.uid))
                    return "uid: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            if (message.result != null && message.hasOwnProperty("result")) {
                let error = $root.lq.AmuletHookResult.verify(message.result, _depth + 1);
                if (error)
                    return "result." + error;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        AmuletEffectedHookData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectedHookData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectedHookData();
            if (object.uid != null)
                if (Number(object.uid) !== 0)
                    message.uid = object.uid >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".lq.AmuletEffectedHookData.result: object expected");
                message.result = $root.lq.AmuletHookResult.fromObject(object.result, _depth + 1);
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        AmuletEffectedHookData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.uid = 0;
                object.id = 0;
                object.result = null;
                object.type = 0;
            }
            if (message.uid != null && message.hasOwnProperty("uid"))
                object.uid = message.uid;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.lq.AmuletHookResult.toObject(message.result, options);
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        AmuletEffectedHookData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectedHookData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectedHookData";
        };

        return AmuletEffectedHookData;
    })();

    lq.AmuletEffectCandidatesArrayDirty = (function() {

        function AmuletEffectCandidatesArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectCandidatesArrayDirty.prototype.dirty = false;
        AmuletEffectCandidatesArrayDirty.prototype.value = $util.emptyArray;

        AmuletEffectCandidatesArrayDirty.create = function create(properties) {
            return new AmuletEffectCandidatesArrayDirty(properties);
        };

        AmuletEffectCandidatesArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletEffectCandidate.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectCandidatesArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectCandidatesArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectCandidatesArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletEffectCandidate.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectCandidatesArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectCandidatesArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletEffectCandidate.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletEffectCandidatesArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectCandidatesArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectCandidatesArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletEffectCandidatesArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletEffectCandidatesArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletEffectCandidate.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletEffectCandidatesArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletEffectCandidate.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletEffectCandidatesArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectCandidatesArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectCandidatesArrayDirty";
        };

        return AmuletEffectCandidatesArrayDirty;
    })();

    lq.AmuletTileArrayDirty = (function() {

        function AmuletTileArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletTileArrayDirty.prototype.dirty = false;
        AmuletTileArrayDirty.prototype.value = $util.emptyArray;

        AmuletTileArrayDirty.create = function create(properties) {
            return new AmuletTileArrayDirty(properties);
        };

        AmuletTileArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletTile.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletTileArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletTileArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletTileArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletTile.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletTileArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletTileArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletTile.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletTileArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletTileArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletTileArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletTileArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletTileArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletTile.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletTileArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletTile.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletTileArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletTileArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletTileArrayDirty";
        };

        return AmuletTileArrayDirty;
    })();

    lq.AmuletMingInfoArrayDirty = (function() {

        function AmuletMingInfoArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletMingInfoArrayDirty.prototype.dirty = false;
        AmuletMingInfoArrayDirty.prototype.value = $util.emptyArray;

        AmuletMingInfoArrayDirty.create = function create(properties) {
            return new AmuletMingInfoArrayDirty(properties);
        };

        AmuletMingInfoArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletMingInfo.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletMingInfoArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletMingInfoArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletMingInfoArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletMingInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletMingInfoArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletMingInfoArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletMingInfo.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletMingInfoArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletMingInfoArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletMingInfoArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletMingInfoArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletMingInfoArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletMingInfo.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletMingInfoArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletMingInfo.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletMingInfoArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletMingInfoArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletMingInfoArrayDirty";
        };

        return AmuletMingInfoArrayDirty;
    })();

    lq.AmuletGameOperationArrayDirty = (function() {

        function AmuletGameOperationArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameOperationArrayDirty.prototype.dirty = false;
        AmuletGameOperationArrayDirty.prototype.value = $util.emptyArray;

        AmuletGameOperationArrayDirty.create = function create(properties) {
            return new AmuletGameOperationArrayDirty(properties);
        };

        AmuletGameOperationArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletGameOperation.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameOperationArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameOperationArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameOperationArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletGameOperation.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameOperationArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameOperationArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletGameOperation.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletGameOperationArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameOperationArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameOperationArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletGameOperationArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletGameOperationArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletGameOperation.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletGameOperationArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletGameOperation.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletGameOperationArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameOperationArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameOperationArrayDirty";
        };

        return AmuletGameOperationArrayDirty;
    })();

    lq.AmuletTingInfoArrayDirty = (function() {

        function AmuletTingInfoArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletTingInfoArrayDirty.prototype.dirty = false;
        AmuletTingInfoArrayDirty.prototype.value = $util.emptyArray;

        AmuletTingInfoArrayDirty.create = function create(properties) {
            return new AmuletTingInfoArrayDirty(properties);
        };

        AmuletTingInfoArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletActivityTingInfo.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletTingInfoArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletTingInfoArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletTingInfoArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletActivityTingInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletTingInfoArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletTingInfoArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletActivityTingInfo.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletTingInfoArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletTingInfoArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletTingInfoArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletTingInfoArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletTingInfoArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletActivityTingInfo.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletTingInfoArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletActivityTingInfo.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletTingInfoArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletTingInfoArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletTingInfoArrayDirty";
        };

        return AmuletTingInfoArrayDirty;
    })();

    lq.AmuletShowDesktopTileDataArrayDirty = (function() {

        function AmuletShowDesktopTileDataArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletShowDesktopTileDataArrayDirty.prototype.dirty = false;
        AmuletShowDesktopTileDataArrayDirty.prototype.value = $util.emptyArray;

        AmuletShowDesktopTileDataArrayDirty.create = function create(properties) {
            return new AmuletShowDesktopTileDataArrayDirty(properties);
        };

        AmuletShowDesktopTileDataArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletShowDesktopTileData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletShowDesktopTileDataArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletShowDesktopTileDataArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletShowDesktopTileDataArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletShowDesktopTileData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletShowDesktopTileDataArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletShowDesktopTileDataArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletShowDesktopTileData.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletShowDesktopTileDataArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletShowDesktopTileDataArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletShowDesktopTileDataArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletShowDesktopTileDataArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletShowDesktopTileDataArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletShowDesktopTileData.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletShowDesktopTileDataArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletShowDesktopTileData.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletShowDesktopTileDataArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletShowDesktopTileDataArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletShowDesktopTileDataArrayDirty";
        };

        return AmuletShowDesktopTileDataArrayDirty;
    })();

    lq.AmuletRoundDataChanges = (function() {

        function AmuletRoundDataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletRoundDataChanges.prototype.pool = null;
        AmuletRoundDataChanges.prototype.tile_replace = null;
        AmuletRoundDataChanges.prototype.tian_dora = null;
        AmuletRoundDataChanges.prototype.dora = null;
        AmuletRoundDataChanges.prototype.hands = null;
        AmuletRoundDataChanges.prototype.used_desktop = null;
        AmuletRoundDataChanges.prototype.used = null;
        AmuletRoundDataChanges.prototype.ming = null;
        AmuletRoundDataChanges.prototype.locked_tile = null;
        AmuletRoundDataChanges.prototype.change_tile_count = null;
        AmuletRoundDataChanges.prototype.total_change_tile_count = null;
        AmuletRoundDataChanges.prototype.next_operation = null;
        AmuletRoundDataChanges.prototype.ting_list = null;
        AmuletRoundDataChanges.prototype.point = null;
        AmuletRoundDataChanges.prototype.target_point = null;
        AmuletRoundDataChanges.prototype.desktop_remain = null;
        AmuletRoundDataChanges.prototype.show_desktop_tiles = null;
        AmuletRoundDataChanges.prototype.locked_tile_count = null;

        AmuletRoundDataChanges.create = function create(properties) {
            return new AmuletRoundDataChanges(properties);
        };

        AmuletRoundDataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.pool != null && Object.hasOwnProperty.call(message, "pool"))
                $root.lq.AmuletTileArrayDirty.encode(message.pool, writer.uint32(10).fork()).ldelim();
            if (message.tile_replace != null && Object.hasOwnProperty.call(message, "tile_replace"))
                $root.lq.AmuletTileArrayDirty.encode(message.tile_replace, writer.uint32(18).fork()).ldelim();
            if (message.tian_dora != null && Object.hasOwnProperty.call(message, "tian_dora"))
                $root.lq.StringArrayDirty.encode(message.tian_dora, writer.uint32(26).fork()).ldelim();
            if (message.dora != null && Object.hasOwnProperty.call(message, "dora"))
                $root.lq.UInt32ArrayDirty.encode(message.dora, writer.uint32(42).fork()).ldelim();
            if (message.hands != null && Object.hasOwnProperty.call(message, "hands"))
                $root.lq.UInt32ArrayDirty.encode(message.hands, writer.uint32(50).fork()).ldelim();
            if (message.used_desktop != null && Object.hasOwnProperty.call(message, "used_desktop"))
                $root.lq.UInt32ArrayDirty.encode(message.used_desktop, writer.uint32(58).fork()).ldelim();
            if (message.used != null && Object.hasOwnProperty.call(message, "used"))
                $root.lq.UInt32ArrayDirty.encode(message.used, writer.uint32(66).fork()).ldelim();
            if (message.ming != null && Object.hasOwnProperty.call(message, "ming"))
                $root.lq.AmuletMingInfoArrayDirty.encode(message.ming, writer.uint32(74).fork()).ldelim();
            if (message.locked_tile != null && Object.hasOwnProperty.call(message, "locked_tile"))
                $root.lq.UInt32ArrayDirty.encode(message.locked_tile, writer.uint32(98).fork()).ldelim();
            if (message.change_tile_count != null && Object.hasOwnProperty.call(message, "change_tile_count"))
                $root.lq.UInt32Dirty.encode(message.change_tile_count, writer.uint32(106).fork()).ldelim();
            if (message.total_change_tile_count != null && Object.hasOwnProperty.call(message, "total_change_tile_count"))
                $root.lq.UInt32Dirty.encode(message.total_change_tile_count, writer.uint32(114).fork()).ldelim();
            if (message.next_operation != null && Object.hasOwnProperty.call(message, "next_operation"))
                $root.lq.AmuletGameOperationArrayDirty.encode(message.next_operation, writer.uint32(122).fork()).ldelim();
            if (message.ting_list != null && Object.hasOwnProperty.call(message, "ting_list"))
                $root.lq.AmuletTingInfoArrayDirty.encode(message.ting_list, writer.uint32(130).fork()).ldelim();
            if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                $root.lq.StringDirty.encode(message.point, writer.uint32(146).fork()).ldelim();
            if (message.target_point != null && Object.hasOwnProperty.call(message, "target_point"))
                $root.lq.StringDirty.encode(message.target_point, writer.uint32(154).fork()).ldelim();
            if (message.desktop_remain != null && Object.hasOwnProperty.call(message, "desktop_remain"))
                $root.lq.UInt32Dirty.encode(message.desktop_remain, writer.uint32(162).fork()).ldelim();
            if (message.show_desktop_tiles != null && Object.hasOwnProperty.call(message, "show_desktop_tiles"))
                $root.lq.AmuletShowDesktopTileDataArrayDirty.encode(message.show_desktop_tiles, writer.uint32(170).fork()).ldelim();
            if (message.locked_tile_count != null && Object.hasOwnProperty.call(message, "locked_tile_count"))
                $root.lq.UInt32Dirty.encode(message.locked_tile_count, writer.uint32(178).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletRoundDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletRoundDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletRoundDataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.pool = $root.lq.AmuletTileArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.pool);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.tile_replace = $root.lq.AmuletTileArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.tile_replace);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.tian_dora = $root.lq.StringArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.tian_dora);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.dora = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.dora);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.hands = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.hands);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.used_desktop = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.used_desktop);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.used = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.used);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.ming = $root.lq.AmuletMingInfoArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.ming);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.locked_tile = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.locked_tile);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.change_tile_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.change_tile_count);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.total_change_tile_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.total_change_tile_count);
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.next_operation = $root.lq.AmuletGameOperationArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.next_operation);
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.ting_list = $root.lq.AmuletTingInfoArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.ting_list);
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.point = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.point);
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.target_point = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.target_point);
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.desktop_remain = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.desktop_remain);
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.show_desktop_tiles = $root.lq.AmuletShowDesktopTileDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.show_desktop_tiles);
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.locked_tile_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.locked_tile_count);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletRoundDataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletRoundDataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.pool != null && message.hasOwnProperty("pool")) {
                let error = $root.lq.AmuletTileArrayDirty.verify(message.pool, _depth + 1);
                if (error)
                    return "pool." + error;
            }
            if (message.tile_replace != null && message.hasOwnProperty("tile_replace")) {
                let error = $root.lq.AmuletTileArrayDirty.verify(message.tile_replace, _depth + 1);
                if (error)
                    return "tile_replace." + error;
            }
            if (message.tian_dora != null && message.hasOwnProperty("tian_dora")) {
                let error = $root.lq.StringArrayDirty.verify(message.tian_dora, _depth + 1);
                if (error)
                    return "tian_dora." + error;
            }
            if (message.dora != null && message.hasOwnProperty("dora")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.dora, _depth + 1);
                if (error)
                    return "dora." + error;
            }
            if (message.hands != null && message.hasOwnProperty("hands")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.hands, _depth + 1);
                if (error)
                    return "hands." + error;
            }
            if (message.used_desktop != null && message.hasOwnProperty("used_desktop")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.used_desktop, _depth + 1);
                if (error)
                    return "used_desktop." + error;
            }
            if (message.used != null && message.hasOwnProperty("used")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.used, _depth + 1);
                if (error)
                    return "used." + error;
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                let error = $root.lq.AmuletMingInfoArrayDirty.verify(message.ming, _depth + 1);
                if (error)
                    return "ming." + error;
            }
            if (message.locked_tile != null && message.hasOwnProperty("locked_tile")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.locked_tile, _depth + 1);
                if (error)
                    return "locked_tile." + error;
            }
            if (message.change_tile_count != null && message.hasOwnProperty("change_tile_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.change_tile_count, _depth + 1);
                if (error)
                    return "change_tile_count." + error;
            }
            if (message.total_change_tile_count != null && message.hasOwnProperty("total_change_tile_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.total_change_tile_count, _depth + 1);
                if (error)
                    return "total_change_tile_count." + error;
            }
            if (message.next_operation != null && message.hasOwnProperty("next_operation")) {
                let error = $root.lq.AmuletGameOperationArrayDirty.verify(message.next_operation, _depth + 1);
                if (error)
                    return "next_operation." + error;
            }
            if (message.ting_list != null && message.hasOwnProperty("ting_list")) {
                let error = $root.lq.AmuletTingInfoArrayDirty.verify(message.ting_list, _depth + 1);
                if (error)
                    return "ting_list." + error;
            }
            if (message.point != null && message.hasOwnProperty("point")) {
                let error = $root.lq.StringDirty.verify(message.point, _depth + 1);
                if (error)
                    return "point." + error;
            }
            if (message.target_point != null && message.hasOwnProperty("target_point")) {
                let error = $root.lq.StringDirty.verify(message.target_point, _depth + 1);
                if (error)
                    return "target_point." + error;
            }
            if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain")) {
                let error = $root.lq.UInt32Dirty.verify(message.desktop_remain, _depth + 1);
                if (error)
                    return "desktop_remain." + error;
            }
            if (message.show_desktop_tiles != null && message.hasOwnProperty("show_desktop_tiles")) {
                let error = $root.lq.AmuletShowDesktopTileDataArrayDirty.verify(message.show_desktop_tiles, _depth + 1);
                if (error)
                    return "show_desktop_tiles." + error;
            }
            if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.locked_tile_count, _depth + 1);
                if (error)
                    return "locked_tile_count." + error;
            }
            return null;
        };

        AmuletRoundDataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletRoundDataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletRoundDataChanges();
            if (object.pool != null) {
                if (typeof object.pool !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.pool: object expected");
                message.pool = $root.lq.AmuletTileArrayDirty.fromObject(object.pool, _depth + 1);
            }
            if (object.tile_replace != null) {
                if (typeof object.tile_replace !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.tile_replace: object expected");
                message.tile_replace = $root.lq.AmuletTileArrayDirty.fromObject(object.tile_replace, _depth + 1);
            }
            if (object.tian_dora != null) {
                if (typeof object.tian_dora !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.tian_dora: object expected");
                message.tian_dora = $root.lq.StringArrayDirty.fromObject(object.tian_dora, _depth + 1);
            }
            if (object.dora != null) {
                if (typeof object.dora !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.dora: object expected");
                message.dora = $root.lq.UInt32ArrayDirty.fromObject(object.dora, _depth + 1);
            }
            if (object.hands != null) {
                if (typeof object.hands !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.hands: object expected");
                message.hands = $root.lq.UInt32ArrayDirty.fromObject(object.hands, _depth + 1);
            }
            if (object.used_desktop != null) {
                if (typeof object.used_desktop !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.used_desktop: object expected");
                message.used_desktop = $root.lq.UInt32ArrayDirty.fromObject(object.used_desktop, _depth + 1);
            }
            if (object.used != null) {
                if (typeof object.used !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.used: object expected");
                message.used = $root.lq.UInt32ArrayDirty.fromObject(object.used, _depth + 1);
            }
            if (object.ming != null) {
                if (typeof object.ming !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.ming: object expected");
                message.ming = $root.lq.AmuletMingInfoArrayDirty.fromObject(object.ming, _depth + 1);
            }
            if (object.locked_tile != null) {
                if (typeof object.locked_tile !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.locked_tile: object expected");
                message.locked_tile = $root.lq.UInt32ArrayDirty.fromObject(object.locked_tile, _depth + 1);
            }
            if (object.change_tile_count != null) {
                if (typeof object.change_tile_count !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.change_tile_count: object expected");
                message.change_tile_count = $root.lq.UInt32Dirty.fromObject(object.change_tile_count, _depth + 1);
            }
            if (object.total_change_tile_count != null) {
                if (typeof object.total_change_tile_count !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.total_change_tile_count: object expected");
                message.total_change_tile_count = $root.lq.UInt32Dirty.fromObject(object.total_change_tile_count, _depth + 1);
            }
            if (object.next_operation != null) {
                if (typeof object.next_operation !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.next_operation: object expected");
                message.next_operation = $root.lq.AmuletGameOperationArrayDirty.fromObject(object.next_operation, _depth + 1);
            }
            if (object.ting_list != null) {
                if (typeof object.ting_list !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.ting_list: object expected");
                message.ting_list = $root.lq.AmuletTingInfoArrayDirty.fromObject(object.ting_list, _depth + 1);
            }
            if (object.point != null) {
                if (typeof object.point !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.point: object expected");
                message.point = $root.lq.StringDirty.fromObject(object.point, _depth + 1);
            }
            if (object.target_point != null) {
                if (typeof object.target_point !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.target_point: object expected");
                message.target_point = $root.lq.StringDirty.fromObject(object.target_point, _depth + 1);
            }
            if (object.desktop_remain != null) {
                if (typeof object.desktop_remain !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.desktop_remain: object expected");
                message.desktop_remain = $root.lq.UInt32Dirty.fromObject(object.desktop_remain, _depth + 1);
            }
            if (object.show_desktop_tiles != null) {
                if (typeof object.show_desktop_tiles !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.show_desktop_tiles: object expected");
                message.show_desktop_tiles = $root.lq.AmuletShowDesktopTileDataArrayDirty.fromObject(object.show_desktop_tiles, _depth + 1);
            }
            if (object.locked_tile_count != null) {
                if (typeof object.locked_tile_count !== "object")
                    throw TypeError(".lq.AmuletRoundDataChanges.locked_tile_count: object expected");
                message.locked_tile_count = $root.lq.UInt32Dirty.fromObject(object.locked_tile_count, _depth + 1);
            }
            return message;
        };

        AmuletRoundDataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.pool = null;
                object.tile_replace = null;
                object.tian_dora = null;
                object.dora = null;
                object.hands = null;
                object.used_desktop = null;
                object.used = null;
                object.ming = null;
                object.locked_tile = null;
                object.change_tile_count = null;
                object.total_change_tile_count = null;
                object.next_operation = null;
                object.ting_list = null;
                object.point = null;
                object.target_point = null;
                object.desktop_remain = null;
                object.show_desktop_tiles = null;
                object.locked_tile_count = null;
            }
            if (message.pool != null && message.hasOwnProperty("pool"))
                object.pool = $root.lq.AmuletTileArrayDirty.toObject(message.pool, options);
            if (message.tile_replace != null && message.hasOwnProperty("tile_replace"))
                object.tile_replace = $root.lq.AmuletTileArrayDirty.toObject(message.tile_replace, options);
            if (message.tian_dora != null && message.hasOwnProperty("tian_dora"))
                object.tian_dora = $root.lq.StringArrayDirty.toObject(message.tian_dora, options);
            if (message.dora != null && message.hasOwnProperty("dora"))
                object.dora = $root.lq.UInt32ArrayDirty.toObject(message.dora, options);
            if (message.hands != null && message.hasOwnProperty("hands"))
                object.hands = $root.lq.UInt32ArrayDirty.toObject(message.hands, options);
            if (message.used_desktop != null && message.hasOwnProperty("used_desktop"))
                object.used_desktop = $root.lq.UInt32ArrayDirty.toObject(message.used_desktop, options);
            if (message.used != null && message.hasOwnProperty("used"))
                object.used = $root.lq.UInt32ArrayDirty.toObject(message.used, options);
            if (message.ming != null && message.hasOwnProperty("ming"))
                object.ming = $root.lq.AmuletMingInfoArrayDirty.toObject(message.ming, options);
            if (message.locked_tile != null && message.hasOwnProperty("locked_tile"))
                object.locked_tile = $root.lq.UInt32ArrayDirty.toObject(message.locked_tile, options);
            if (message.change_tile_count != null && message.hasOwnProperty("change_tile_count"))
                object.change_tile_count = $root.lq.UInt32Dirty.toObject(message.change_tile_count, options);
            if (message.total_change_tile_count != null && message.hasOwnProperty("total_change_tile_count"))
                object.total_change_tile_count = $root.lq.UInt32Dirty.toObject(message.total_change_tile_count, options);
            if (message.next_operation != null && message.hasOwnProperty("next_operation"))
                object.next_operation = $root.lq.AmuletGameOperationArrayDirty.toObject(message.next_operation, options);
            if (message.ting_list != null && message.hasOwnProperty("ting_list"))
                object.ting_list = $root.lq.AmuletTingInfoArrayDirty.toObject(message.ting_list, options);
            if (message.point != null && message.hasOwnProperty("point"))
                object.point = $root.lq.StringDirty.toObject(message.point, options);
            if (message.target_point != null && message.hasOwnProperty("target_point"))
                object.target_point = $root.lq.StringDirty.toObject(message.target_point, options);
            if (message.desktop_remain != null && message.hasOwnProperty("desktop_remain"))
                object.desktop_remain = $root.lq.UInt32Dirty.toObject(message.desktop_remain, options);
            if (message.show_desktop_tiles != null && message.hasOwnProperty("show_desktop_tiles"))
                object.show_desktop_tiles = $root.lq.AmuletShowDesktopTileDataArrayDirty.toObject(message.show_desktop_tiles, options);
            if (message.locked_tile_count != null && message.hasOwnProperty("locked_tile_count"))
                object.locked_tile_count = $root.lq.UInt32Dirty.toObject(message.locked_tile_count, options);
            return object;
        };

        AmuletRoundDataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletRoundDataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletRoundDataChanges";
        };

        return AmuletRoundDataChanges;
    })();

    lq.AmuletEffectDataArrayDirty = (function() {

        function AmuletEffectDataArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectDataArrayDirty.prototype.dirty = false;
        AmuletEffectDataArrayDirty.prototype.value = $util.emptyArray;

        AmuletEffectDataArrayDirty.create = function create(properties) {
            return new AmuletEffectDataArrayDirty(properties);
        };

        AmuletEffectDataArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletEffectData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectDataArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectDataArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectDataArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletEffectData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectDataArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectDataArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletEffectData.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletEffectDataArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectDataArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectDataArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletEffectDataArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletEffectDataArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletEffectData.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletEffectDataArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletEffectData.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletEffectDataArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectDataArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectDataArrayDirty";
        };

        return AmuletEffectDataArrayDirty;
    })();

    lq.AmuletBuffDataArrayDirty = (function() {

        function AmuletBuffDataArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletBuffDataArrayDirty.prototype.dirty = false;
        AmuletBuffDataArrayDirty.prototype.value = $util.emptyArray;

        AmuletBuffDataArrayDirty.create = function create(properties) {
            return new AmuletBuffDataArrayDirty(properties);
        };

        AmuletBuffDataArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletBuffData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletBuffDataArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletBuffDataArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletBuffDataArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletBuffData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletBuffDataArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletBuffDataArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletBuffData.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletBuffDataArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletBuffDataArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletBuffDataArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletBuffDataArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletBuffDataArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletBuffData.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletBuffDataArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletBuffData.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletBuffDataArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletBuffDataArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletBuffDataArrayDirty";
        };

        return AmuletBuffDataArrayDirty;
    })();

    lq.AmuletEffectDataChanges = (function() {

        function AmuletEffectDataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectDataChanges.prototype.effect_list = null;
        AmuletEffectDataChanges.prototype.buff_list = null;
        AmuletEffectDataChanges.prototype.skill_buff_list = null;
        AmuletEffectDataChanges.prototype.shop_buff_list = null;
        AmuletEffectDataChanges.prototype.free_reward_candidates = null;
        AmuletEffectDataChanges.prototype.level_reward_candidates = null;
        AmuletEffectDataChanges.prototype.current_level_reward_pack = null;

        AmuletEffectDataChanges.create = function create(properties) {
            return new AmuletEffectDataChanges(properties);
        };

        AmuletEffectDataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.effect_list != null && Object.hasOwnProperty.call(message, "effect_list"))
                $root.lq.AmuletEffectDataArrayDirty.encode(message.effect_list, writer.uint32(10).fork()).ldelim();
            if (message.buff_list != null && Object.hasOwnProperty.call(message, "buff_list"))
                $root.lq.AmuletBuffDataArrayDirty.encode(message.buff_list, writer.uint32(18).fork()).ldelim();
            if (message.skill_buff_list != null && Object.hasOwnProperty.call(message, "skill_buff_list"))
                $root.lq.AmuletBuffDataArrayDirty.encode(message.skill_buff_list, writer.uint32(26).fork()).ldelim();
            if (message.shop_buff_list != null && Object.hasOwnProperty.call(message, "shop_buff_list"))
                $root.lq.AmuletBuffDataArrayDirty.encode(message.shop_buff_list, writer.uint32(34).fork()).ldelim();
            if (message.free_reward_candidates != null && Object.hasOwnProperty.call(message, "free_reward_candidates"))
                $root.lq.AmuletEffectCandidatesArrayDirty.encode(message.free_reward_candidates, writer.uint32(42).fork()).ldelim();
            if (message.level_reward_candidates != null && Object.hasOwnProperty.call(message, "level_reward_candidates"))
                $root.lq.AmuletEffectCandidatesArrayDirty.encode(message.level_reward_candidates, writer.uint32(50).fork()).ldelim();
            if (message.current_level_reward_pack != null && Object.hasOwnProperty.call(message, "current_level_reward_pack"))
                $root.lq.UInt32Dirty.encode(message.current_level_reward_pack, writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectDataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.effect_list = $root.lq.AmuletEffectDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.effect_list);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.buff_list = $root.lq.AmuletBuffDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.buff_list);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.skill_buff_list = $root.lq.AmuletBuffDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.skill_buff_list);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.shop_buff_list = $root.lq.AmuletBuffDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_buff_list);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.free_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.free_reward_candidates);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.level_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level_reward_candidates);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.current_level_reward_pack = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.current_level_reward_pack);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectDataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectDataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.effect_list != null && message.hasOwnProperty("effect_list")) {
                let error = $root.lq.AmuletEffectDataArrayDirty.verify(message.effect_list, _depth + 1);
                if (error)
                    return "effect_list." + error;
            }
            if (message.buff_list != null && message.hasOwnProperty("buff_list")) {
                let error = $root.lq.AmuletBuffDataArrayDirty.verify(message.buff_list, _depth + 1);
                if (error)
                    return "buff_list." + error;
            }
            if (message.skill_buff_list != null && message.hasOwnProperty("skill_buff_list")) {
                let error = $root.lq.AmuletBuffDataArrayDirty.verify(message.skill_buff_list, _depth + 1);
                if (error)
                    return "skill_buff_list." + error;
            }
            if (message.shop_buff_list != null && message.hasOwnProperty("shop_buff_list")) {
                let error = $root.lq.AmuletBuffDataArrayDirty.verify(message.shop_buff_list, _depth + 1);
                if (error)
                    return "shop_buff_list." + error;
            }
            if (message.free_reward_candidates != null && message.hasOwnProperty("free_reward_candidates")) {
                let error = $root.lq.AmuletEffectCandidatesArrayDirty.verify(message.free_reward_candidates, _depth + 1);
                if (error)
                    return "free_reward_candidates." + error;
            }
            if (message.level_reward_candidates != null && message.hasOwnProperty("level_reward_candidates")) {
                let error = $root.lq.AmuletEffectCandidatesArrayDirty.verify(message.level_reward_candidates, _depth + 1);
                if (error)
                    return "level_reward_candidates." + error;
            }
            if (message.current_level_reward_pack != null && message.hasOwnProperty("current_level_reward_pack")) {
                let error = $root.lq.UInt32Dirty.verify(message.current_level_reward_pack, _depth + 1);
                if (error)
                    return "current_level_reward_pack." + error;
            }
            return null;
        };

        AmuletEffectDataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectDataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectDataChanges();
            if (object.effect_list != null) {
                if (typeof object.effect_list !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.effect_list: object expected");
                message.effect_list = $root.lq.AmuletEffectDataArrayDirty.fromObject(object.effect_list, _depth + 1);
            }
            if (object.buff_list != null) {
                if (typeof object.buff_list !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.buff_list: object expected");
                message.buff_list = $root.lq.AmuletBuffDataArrayDirty.fromObject(object.buff_list, _depth + 1);
            }
            if (object.skill_buff_list != null) {
                if (typeof object.skill_buff_list !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.skill_buff_list: object expected");
                message.skill_buff_list = $root.lq.AmuletBuffDataArrayDirty.fromObject(object.skill_buff_list, _depth + 1);
            }
            if (object.shop_buff_list != null) {
                if (typeof object.shop_buff_list !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.shop_buff_list: object expected");
                message.shop_buff_list = $root.lq.AmuletBuffDataArrayDirty.fromObject(object.shop_buff_list, _depth + 1);
            }
            if (object.free_reward_candidates != null) {
                if (typeof object.free_reward_candidates !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.free_reward_candidates: object expected");
                message.free_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.fromObject(object.free_reward_candidates, _depth + 1);
            }
            if (object.level_reward_candidates != null) {
                if (typeof object.level_reward_candidates !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.level_reward_candidates: object expected");
                message.level_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.fromObject(object.level_reward_candidates, _depth + 1);
            }
            if (object.current_level_reward_pack != null) {
                if (typeof object.current_level_reward_pack !== "object")
                    throw TypeError(".lq.AmuletEffectDataChanges.current_level_reward_pack: object expected");
                message.current_level_reward_pack = $root.lq.UInt32Dirty.fromObject(object.current_level_reward_pack, _depth + 1);
            }
            return message;
        };

        AmuletEffectDataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.effect_list = null;
                object.buff_list = null;
                object.skill_buff_list = null;
                object.shop_buff_list = null;
                object.free_reward_candidates = null;
                object.level_reward_candidates = null;
                object.current_level_reward_pack = null;
            }
            if (message.effect_list != null && message.hasOwnProperty("effect_list"))
                object.effect_list = $root.lq.AmuletEffectDataArrayDirty.toObject(message.effect_list, options);
            if (message.buff_list != null && message.hasOwnProperty("buff_list"))
                object.buff_list = $root.lq.AmuletBuffDataArrayDirty.toObject(message.buff_list, options);
            if (message.skill_buff_list != null && message.hasOwnProperty("skill_buff_list"))
                object.skill_buff_list = $root.lq.AmuletBuffDataArrayDirty.toObject(message.skill_buff_list, options);
            if (message.shop_buff_list != null && message.hasOwnProperty("shop_buff_list"))
                object.shop_buff_list = $root.lq.AmuletBuffDataArrayDirty.toObject(message.shop_buff_list, options);
            if (message.free_reward_candidates != null && message.hasOwnProperty("free_reward_candidates"))
                object.free_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.toObject(message.free_reward_candidates, options);
            if (message.level_reward_candidates != null && message.hasOwnProperty("level_reward_candidates"))
                object.level_reward_candidates = $root.lq.AmuletEffectCandidatesArrayDirty.toObject(message.level_reward_candidates, options);
            if (message.current_level_reward_pack != null && message.hasOwnProperty("current_level_reward_pack"))
                object.current_level_reward_pack = $root.lq.UInt32Dirty.toObject(message.current_level_reward_pack, options);
            return object;
        };

        AmuletEffectDataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectDataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectDataChanges";
        };

        return AmuletEffectDataChanges;
    })();

    lq.AmuletTileScoreArrayDirty = (function() {

        function AmuletTileScoreArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletTileScoreArrayDirty.prototype.dirty = false;
        AmuletTileScoreArrayDirty.prototype.value = $util.emptyArray;

        AmuletTileScoreArrayDirty.create = function create(properties) {
            return new AmuletTileScoreArrayDirty(properties);
        };

        AmuletTileScoreArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletTileScore.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletTileScoreArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletTileScoreArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletTileScoreArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletTileScore.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletTileScoreArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletTileScoreArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletTileScore.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletTileScoreArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletTileScoreArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletTileScoreArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletTileScoreArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletTileScoreArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletTileScore.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletTileScoreArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletTileScore.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletTileScoreArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletTileScoreArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletTileScoreArrayDirty";
        };

        return AmuletTileScoreArrayDirty;
    })();

    lq.AmuletGameInfoDataChanges = (function() {

        function AmuletGameInfoDataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletGameInfoDataChanges.prototype.level = null;
        AmuletGameInfoDataChanges.prototype.coin = null;
        AmuletGameInfoDataChanges.prototype.max_effect_volume = null;
        AmuletGameInfoDataChanges.prototype.next_boss_buff = null;
        AmuletGameInfoDataChanges.prototype.boss_buff = null;
        AmuletGameInfoDataChanges.prototype.tile_score_map = null;

        AmuletGameInfoDataChanges.create = function create(properties) {
            return new AmuletGameInfoDataChanges(properties);
        };

        AmuletGameInfoDataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                $root.lq.UInt32Dirty.encode(message.level, writer.uint32(10).fork()).ldelim();
            if (message.coin != null && Object.hasOwnProperty.call(message, "coin"))
                $root.lq.StringDirty.encode(message.coin, writer.uint32(18).fork()).ldelim();
            if (message.max_effect_volume != null && Object.hasOwnProperty.call(message, "max_effect_volume"))
                $root.lq.UInt32Dirty.encode(message.max_effect_volume, writer.uint32(26).fork()).ldelim();
            if (message.next_boss_buff != null && Object.hasOwnProperty.call(message, "next_boss_buff"))
                $root.lq.UInt32ArrayDirty.encode(message.next_boss_buff, writer.uint32(34).fork()).ldelim();
            if (message.boss_buff != null && Object.hasOwnProperty.call(message, "boss_buff"))
                $root.lq.UInt32ArrayDirty.encode(message.boss_buff, writer.uint32(42).fork()).ldelim();
            if (message.tile_score_map != null && Object.hasOwnProperty.call(message, "tile_score_map"))
                $root.lq.AmuletTileScoreArrayDirty.encode(message.tile_score_map, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletGameInfoDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletGameInfoDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletGameInfoDataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.level = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.coin = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.coin);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.max_effect_volume = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.max_effect_volume);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.next_boss_buff = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.next_boss_buff);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.boss_buff = $root.lq.UInt32ArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.boss_buff);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.tile_score_map = $root.lq.AmuletTileScoreArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.tile_score_map);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletGameInfoDataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletGameInfoDataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.level != null && message.hasOwnProperty("level")) {
                let error = $root.lq.UInt32Dirty.verify(message.level, _depth + 1);
                if (error)
                    return "level." + error;
            }
            if (message.coin != null && message.hasOwnProperty("coin")) {
                let error = $root.lq.StringDirty.verify(message.coin, _depth + 1);
                if (error)
                    return "coin." + error;
            }
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume")) {
                let error = $root.lq.UInt32Dirty.verify(message.max_effect_volume, _depth + 1);
                if (error)
                    return "max_effect_volume." + error;
            }
            if (message.next_boss_buff != null && message.hasOwnProperty("next_boss_buff")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.next_boss_buff, _depth + 1);
                if (error)
                    return "next_boss_buff." + error;
            }
            if (message.boss_buff != null && message.hasOwnProperty("boss_buff")) {
                let error = $root.lq.UInt32ArrayDirty.verify(message.boss_buff, _depth + 1);
                if (error)
                    return "boss_buff." + error;
            }
            if (message.tile_score_map != null && message.hasOwnProperty("tile_score_map")) {
                let error = $root.lq.AmuletTileScoreArrayDirty.verify(message.tile_score_map, _depth + 1);
                if (error)
                    return "tile_score_map." + error;
            }
            return null;
        };

        AmuletGameInfoDataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletGameInfoDataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletGameInfoDataChanges();
            if (object.level != null) {
                if (typeof object.level !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.level: object expected");
                message.level = $root.lq.UInt32Dirty.fromObject(object.level, _depth + 1);
            }
            if (object.coin != null) {
                if (typeof object.coin !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.coin: object expected");
                message.coin = $root.lq.StringDirty.fromObject(object.coin, _depth + 1);
            }
            if (object.max_effect_volume != null) {
                if (typeof object.max_effect_volume !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.max_effect_volume: object expected");
                message.max_effect_volume = $root.lq.UInt32Dirty.fromObject(object.max_effect_volume, _depth + 1);
            }
            if (object.next_boss_buff != null) {
                if (typeof object.next_boss_buff !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.next_boss_buff: object expected");
                message.next_boss_buff = $root.lq.UInt32ArrayDirty.fromObject(object.next_boss_buff, _depth + 1);
            }
            if (object.boss_buff != null) {
                if (typeof object.boss_buff !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.boss_buff: object expected");
                message.boss_buff = $root.lq.UInt32ArrayDirty.fromObject(object.boss_buff, _depth + 1);
            }
            if (object.tile_score_map != null) {
                if (typeof object.tile_score_map !== "object")
                    throw TypeError(".lq.AmuletGameInfoDataChanges.tile_score_map: object expected");
                message.tile_score_map = $root.lq.AmuletTileScoreArrayDirty.fromObject(object.tile_score_map, _depth + 1);
            }
            return message;
        };

        AmuletGameInfoDataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.level = null;
                object.coin = null;
                object.max_effect_volume = null;
                object.next_boss_buff = null;
                object.boss_buff = null;
                object.tile_score_map = null;
            }
            if (message.level != null && message.hasOwnProperty("level"))
                object.level = $root.lq.UInt32Dirty.toObject(message.level, options);
            if (message.coin != null && message.hasOwnProperty("coin"))
                object.coin = $root.lq.StringDirty.toObject(message.coin, options);
            if (message.max_effect_volume != null && message.hasOwnProperty("max_effect_volume"))
                object.max_effect_volume = $root.lq.UInt32Dirty.toObject(message.max_effect_volume, options);
            if (message.next_boss_buff != null && message.hasOwnProperty("next_boss_buff"))
                object.next_boss_buff = $root.lq.UInt32ArrayDirty.toObject(message.next_boss_buff, options);
            if (message.boss_buff != null && message.hasOwnProperty("boss_buff"))
                object.boss_buff = $root.lq.UInt32ArrayDirty.toObject(message.boss_buff, options);
            if (message.tile_score_map != null && message.hasOwnProperty("tile_score_map"))
                object.tile_score_map = $root.lq.AmuletTileScoreArrayDirty.toObject(message.tile_score_map, options);
            return object;
        };

        AmuletGameInfoDataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletGameInfoDataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletGameInfoDataChanges";
        };

        return AmuletGameInfoDataChanges;
    })();

    lq.AmuletShopGoodsArrayDirty = (function() {

        function AmuletShopGoodsArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletShopGoodsArrayDirty.prototype.dirty = false;
        AmuletShopGoodsArrayDirty.prototype.value = $util.emptyArray;

        AmuletShopGoodsArrayDirty.create = function create(properties) {
            return new AmuletShopGoodsArrayDirty(properties);
        };

        AmuletShopGoodsArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletGameShopGoods.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletShopGoodsArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletShopGoodsArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletShopGoodsArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletGameShopGoods.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletShopGoodsArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletShopGoodsArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletGameShopGoods.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletShopGoodsArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletShopGoodsArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletShopGoodsArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletShopGoodsArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletShopGoodsArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletGameShopGoods.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletShopGoodsArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletGameShopGoods.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletShopGoodsArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletShopGoodsArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletShopGoodsArrayDirty";
        };

        return AmuletShopGoodsArrayDirty;
    })();

    lq.AmuletShopDataChanges = (function() {

        function AmuletShopDataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletShopDataChanges.prototype.goods = null;
        AmuletShopDataChanges.prototype.candidate_effect_list = null;
        AmuletShopDataChanges.prototype.shop_refresh_count = null;
        AmuletShopDataChanges.prototype.refresh_price = null;

        AmuletShopDataChanges.create = function create(properties) {
            return new AmuletShopDataChanges(properties);
        };

        AmuletShopDataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.goods != null && Object.hasOwnProperty.call(message, "goods"))
                $root.lq.AmuletShopGoodsArrayDirty.encode(message.goods, writer.uint32(10).fork()).ldelim();
            if (message.candidate_effect_list != null && Object.hasOwnProperty.call(message, "candidate_effect_list"))
                $root.lq.AmuletEffectCandidatesArrayDirty.encode(message.candidate_effect_list, writer.uint32(18).fork()).ldelim();
            if (message.shop_refresh_count != null && Object.hasOwnProperty.call(message, "shop_refresh_count"))
                $root.lq.UInt32Dirty.encode(message.shop_refresh_count, writer.uint32(26).fork()).ldelim();
            if (message.refresh_price != null && Object.hasOwnProperty.call(message, "refresh_price"))
                $root.lq.UInt32Dirty.encode(message.refresh_price, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletShopDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletShopDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletShopDataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.goods = $root.lq.AmuletShopGoodsArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.goods);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.candidate_effect_list = $root.lq.AmuletEffectCandidatesArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.candidate_effect_list);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.shop_refresh_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop_refresh_count);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.refresh_price = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.refresh_price);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletShopDataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletShopDataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.goods != null && message.hasOwnProperty("goods")) {
                let error = $root.lq.AmuletShopGoodsArrayDirty.verify(message.goods, _depth + 1);
                if (error)
                    return "goods." + error;
            }
            if (message.candidate_effect_list != null && message.hasOwnProperty("candidate_effect_list")) {
                let error = $root.lq.AmuletEffectCandidatesArrayDirty.verify(message.candidate_effect_list, _depth + 1);
                if (error)
                    return "candidate_effect_list." + error;
            }
            if (message.shop_refresh_count != null && message.hasOwnProperty("shop_refresh_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.shop_refresh_count, _depth + 1);
                if (error)
                    return "shop_refresh_count." + error;
            }
            if (message.refresh_price != null && message.hasOwnProperty("refresh_price")) {
                let error = $root.lq.UInt32Dirty.verify(message.refresh_price, _depth + 1);
                if (error)
                    return "refresh_price." + error;
            }
            return null;
        };

        AmuletShopDataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletShopDataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletShopDataChanges();
            if (object.goods != null) {
                if (typeof object.goods !== "object")
                    throw TypeError(".lq.AmuletShopDataChanges.goods: object expected");
                message.goods = $root.lq.AmuletShopGoodsArrayDirty.fromObject(object.goods, _depth + 1);
            }
            if (object.candidate_effect_list != null) {
                if (typeof object.candidate_effect_list !== "object")
                    throw TypeError(".lq.AmuletShopDataChanges.candidate_effect_list: object expected");
                message.candidate_effect_list = $root.lq.AmuletEffectCandidatesArrayDirty.fromObject(object.candidate_effect_list, _depth + 1);
            }
            if (object.shop_refresh_count != null) {
                if (typeof object.shop_refresh_count !== "object")
                    throw TypeError(".lq.AmuletShopDataChanges.shop_refresh_count: object expected");
                message.shop_refresh_count = $root.lq.UInt32Dirty.fromObject(object.shop_refresh_count, _depth + 1);
            }
            if (object.refresh_price != null) {
                if (typeof object.refresh_price !== "object")
                    throw TypeError(".lq.AmuletShopDataChanges.refresh_price: object expected");
                message.refresh_price = $root.lq.UInt32Dirty.fromObject(object.refresh_price, _depth + 1);
            }
            return message;
        };

        AmuletShopDataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.goods = null;
                object.candidate_effect_list = null;
                object.shop_refresh_count = null;
                object.refresh_price = null;
            }
            if (message.goods != null && message.hasOwnProperty("goods"))
                object.goods = $root.lq.AmuletShopGoodsArrayDirty.toObject(message.goods, options);
            if (message.candidate_effect_list != null && message.hasOwnProperty("candidate_effect_list"))
                object.candidate_effect_list = $root.lq.AmuletEffectCandidatesArrayDirty.toObject(message.candidate_effect_list, options);
            if (message.shop_refresh_count != null && message.hasOwnProperty("shop_refresh_count"))
                object.shop_refresh_count = $root.lq.UInt32Dirty.toObject(message.shop_refresh_count, options);
            if (message.refresh_price != null && message.hasOwnProperty("refresh_price"))
                object.refresh_price = $root.lq.UInt32Dirty.toObject(message.refresh_price, options);
            return object;
        };

        AmuletShopDataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletShopDataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletShopDataChanges";
        };

        return AmuletShopDataChanges;
    })();

    lq.ActivityAmuletHuRecordDirty = (function() {

        function ActivityAmuletHuRecordDirty(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActivityAmuletHuRecordDirty.prototype.dirty = false;
        ActivityAmuletHuRecordDirty.prototype.value = null;

        ActivityAmuletHuRecordDirty.create = function create(properties) {
            return new ActivityAmuletHuRecordDirty(properties);
        };

        ActivityAmuletHuRecordDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                $root.lq.ActivityAmuletHuRecord.encode(message.value, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActivityAmuletHuRecordDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActivityAmuletHuRecordDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActivityAmuletHuRecordDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.value = $root.lq.ActivityAmuletHuRecord.decode(reader, reader.uint32(), undefined, _depth + 1, message.value);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActivityAmuletHuRecordDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActivityAmuletHuRecordDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                let error = $root.lq.ActivityAmuletHuRecord.verify(message.value, _depth + 1);
                if (error)
                    return "value." + error;
            }
            return null;
        };

        ActivityAmuletHuRecordDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActivityAmuletHuRecordDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActivityAmuletHuRecordDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value != null) {
                if (typeof object.value !== "object")
                    throw TypeError(".lq.ActivityAmuletHuRecordDirty.value: object expected");
                message.value = $root.lq.ActivityAmuletHuRecord.fromObject(object.value, _depth + 1);
            }
            return message;
        };

        ActivityAmuletHuRecordDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.dirty = false;
                object.value = null;
            }
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value != null && message.hasOwnProperty("value"))
                object.value = $root.lq.ActivityAmuletHuRecord.toObject(message.value, options);
            return object;
        };

        ActivityAmuletHuRecordDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActivityAmuletHuRecordDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActivityAmuletHuRecordDirty";
        };

        return ActivityAmuletHuRecordDirty;
    })();

    lq.AmuletEffectCounterDataArrayDirty = (function() {

        function AmuletEffectCounterDataArrayDirty(properties) {
            this.value = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEffectCounterDataArrayDirty.prototype.dirty = false;
        AmuletEffectCounterDataArrayDirty.prototype.value = $util.emptyArray;

        AmuletEffectCounterDataArrayDirty.create = function create(properties) {
            return new AmuletEffectCounterDataArrayDirty(properties);
        };

        AmuletEffectCounterDataArrayDirty.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.dirty != null && Object.hasOwnProperty.call(message, "dirty"))
                writer.uint32(8).bool(message.dirty);
            if (message.value != null && message.value.length)
                for (let i = 0; i < message.value.length; ++i)
                    $root.lq.AmuletEffectCounterData.encode(message.value[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEffectCounterDataArrayDirty.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEffectCounterDataArrayDirty.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEffectCounterDataArrayDirty(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.dirty = value;
                        else
                            delete message.dirty;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.value && message.value.length))
                            message.value = [];
                        message.value.push($root.lq.AmuletEffectCounterData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEffectCounterDataArrayDirty.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEffectCounterDataArrayDirty.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                if (typeof message.dirty !== "boolean")
                    return "dirty: boolean expected";
            if (message.value != null && message.hasOwnProperty("value")) {
                if (!Array.isArray(message.value))
                    return "value: array expected";
                for (let i = 0; i < message.value.length; ++i) {
                    let error = $root.lq.AmuletEffectCounterData.verify(message.value[i], _depth + 1);
                    if (error)
                        return "value." + error;
                }
            }
            return null;
        };

        AmuletEffectCounterDataArrayDirty.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEffectCounterDataArrayDirty)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEffectCounterDataArrayDirty();
            if (object.dirty != null)
                if (object.dirty)
                    message.dirty = Boolean(object.dirty);
            if (object.value) {
                if (!Array.isArray(object.value))
                    throw TypeError(".lq.AmuletEffectCounterDataArrayDirty.value: array expected");
                message.value = Array(object.value.length);
                for (let i = 0; i < object.value.length; ++i) {
                    if (typeof object.value[i] !== "object")
                        throw TypeError(".lq.AmuletEffectCounterDataArrayDirty.value: object expected");
                    message.value[i] = $root.lq.AmuletEffectCounterData.fromObject(object.value[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletEffectCounterDataArrayDirty.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.value = [];
            if (options.defaults)
                object.dirty = false;
            if (message.dirty != null && message.hasOwnProperty("dirty"))
                object.dirty = message.dirty;
            if (message.value && message.value.length) {
                object.value = Array(message.value.length);
                for (let j = 0; j < message.value.length; ++j)
                    object.value[j] = $root.lq.AmuletEffectCounterData.toObject(message.value[j], options);
            }
            return object;
        };

        AmuletEffectCounterDataArrayDirty.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEffectCounterDataArrayDirty.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEffectCounterDataArrayDirty";
        };

        return AmuletEffectCounterDataArrayDirty;
    })();

    lq.AmuletRecordDataChanges = (function() {

        function AmuletRecordDataChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletRecordDataChanges.prototype.yiman_count = null;
        AmuletRecordDataChanges.prototype.level_hu_count = null;
        AmuletRecordDataChanges.prototype.game_hu_count = null;
        AmuletRecordDataChanges.prototype.effect_gain = null;
        AmuletRecordDataChanges.prototype.coin_consume = null;
        AmuletRecordDataChanges.prototype.coin_gain = null;
        AmuletRecordDataChanges.prototype.highest_hu = null;
        AmuletRecordDataChanges.prototype.highest_level_score = null;
        AmuletRecordDataChanges.prototype.highest_fan = null;
        AmuletRecordDataChanges.prototype.pack_count = null;
        AmuletRecordDataChanges.prototype.round_count = null;
        AmuletRecordDataChanges.prototype.effect_counter = null;

        AmuletRecordDataChanges.create = function create(properties) {
            return new AmuletRecordDataChanges(properties);
        };

        AmuletRecordDataChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.yiman_count != null && Object.hasOwnProperty.call(message, "yiman_count"))
                $root.lq.UInt32Dirty.encode(message.yiman_count, writer.uint32(10).fork()).ldelim();
            if (message.level_hu_count != null && Object.hasOwnProperty.call(message, "level_hu_count"))
                $root.lq.UInt32Dirty.encode(message.level_hu_count, writer.uint32(18).fork()).ldelim();
            if (message.game_hu_count != null && Object.hasOwnProperty.call(message, "game_hu_count"))
                $root.lq.UInt32Dirty.encode(message.game_hu_count, writer.uint32(26).fork()).ldelim();
            if (message.effect_gain != null && Object.hasOwnProperty.call(message, "effect_gain"))
                $root.lq.UInt32Dirty.encode(message.effect_gain, writer.uint32(34).fork()).ldelim();
            if (message.coin_consume != null && Object.hasOwnProperty.call(message, "coin_consume"))
                $root.lq.StringDirty.encode(message.coin_consume, writer.uint32(42).fork()).ldelim();
            if (message.coin_gain != null && Object.hasOwnProperty.call(message, "coin_gain"))
                $root.lq.StringDirty.encode(message.coin_gain, writer.uint32(50).fork()).ldelim();
            if (message.highest_hu != null && Object.hasOwnProperty.call(message, "highest_hu"))
                $root.lq.ActivityAmuletHuRecordDirty.encode(message.highest_hu, writer.uint32(58).fork()).ldelim();
            if (message.highest_level_score != null && Object.hasOwnProperty.call(message, "highest_level_score"))
                $root.lq.StringDirty.encode(message.highest_level_score, writer.uint32(66).fork()).ldelim();
            if (message.highest_fan != null && Object.hasOwnProperty.call(message, "highest_fan"))
                $root.lq.StringDirty.encode(message.highest_fan, writer.uint32(74).fork()).ldelim();
            if (message.pack_count != null && Object.hasOwnProperty.call(message, "pack_count"))
                $root.lq.UInt32Dirty.encode(message.pack_count, writer.uint32(82).fork()).ldelim();
            if (message.round_count != null && Object.hasOwnProperty.call(message, "round_count"))
                $root.lq.UInt32Dirty.encode(message.round_count, writer.uint32(90).fork()).ldelim();
            if (message.effect_counter != null && Object.hasOwnProperty.call(message, "effect_counter"))
                $root.lq.AmuletEffectCounterDataArrayDirty.encode(message.effect_counter, writer.uint32(98).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletRecordDataChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletRecordDataChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletRecordDataChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.yiman_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.yiman_count);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.level_hu_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.level_hu_count);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game_hu_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_hu_count);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.effect_gain = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.effect_gain);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.coin_consume = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.coin_consume);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.coin_gain = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.coin_gain);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.highest_hu = $root.lq.ActivityAmuletHuRecordDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_hu);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.highest_level_score = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_level_score);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.highest_fan = $root.lq.StringDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.highest_fan);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.pack_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.pack_count);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.round_count = $root.lq.UInt32Dirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.round_count);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.effect_counter = $root.lq.AmuletEffectCounterDataArrayDirty.decode(reader, reader.uint32(), undefined, _depth + 1, message.effect_counter);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletRecordDataChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletRecordDataChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.yiman_count != null && message.hasOwnProperty("yiman_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.yiman_count, _depth + 1);
                if (error)
                    return "yiman_count." + error;
            }
            if (message.level_hu_count != null && message.hasOwnProperty("level_hu_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.level_hu_count, _depth + 1);
                if (error)
                    return "level_hu_count." + error;
            }
            if (message.game_hu_count != null && message.hasOwnProperty("game_hu_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.game_hu_count, _depth + 1);
                if (error)
                    return "game_hu_count." + error;
            }
            if (message.effect_gain != null && message.hasOwnProperty("effect_gain")) {
                let error = $root.lq.UInt32Dirty.verify(message.effect_gain, _depth + 1);
                if (error)
                    return "effect_gain." + error;
            }
            if (message.coin_consume != null && message.hasOwnProperty("coin_consume")) {
                let error = $root.lq.StringDirty.verify(message.coin_consume, _depth + 1);
                if (error)
                    return "coin_consume." + error;
            }
            if (message.coin_gain != null && message.hasOwnProperty("coin_gain")) {
                let error = $root.lq.StringDirty.verify(message.coin_gain, _depth + 1);
                if (error)
                    return "coin_gain." + error;
            }
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu")) {
                let error = $root.lq.ActivityAmuletHuRecordDirty.verify(message.highest_hu, _depth + 1);
                if (error)
                    return "highest_hu." + error;
            }
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score")) {
                let error = $root.lq.StringDirty.verify(message.highest_level_score, _depth + 1);
                if (error)
                    return "highest_level_score." + error;
            }
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan")) {
                let error = $root.lq.StringDirty.verify(message.highest_fan, _depth + 1);
                if (error)
                    return "highest_fan." + error;
            }
            if (message.pack_count != null && message.hasOwnProperty("pack_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.pack_count, _depth + 1);
                if (error)
                    return "pack_count." + error;
            }
            if (message.round_count != null && message.hasOwnProperty("round_count")) {
                let error = $root.lq.UInt32Dirty.verify(message.round_count, _depth + 1);
                if (error)
                    return "round_count." + error;
            }
            if (message.effect_counter != null && message.hasOwnProperty("effect_counter")) {
                let error = $root.lq.AmuletEffectCounterDataArrayDirty.verify(message.effect_counter, _depth + 1);
                if (error)
                    return "effect_counter." + error;
            }
            return null;
        };

        AmuletRecordDataChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletRecordDataChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletRecordDataChanges();
            if (object.yiman_count != null) {
                if (typeof object.yiman_count !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.yiman_count: object expected");
                message.yiman_count = $root.lq.UInt32Dirty.fromObject(object.yiman_count, _depth + 1);
            }
            if (object.level_hu_count != null) {
                if (typeof object.level_hu_count !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.level_hu_count: object expected");
                message.level_hu_count = $root.lq.UInt32Dirty.fromObject(object.level_hu_count, _depth + 1);
            }
            if (object.game_hu_count != null) {
                if (typeof object.game_hu_count !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.game_hu_count: object expected");
                message.game_hu_count = $root.lq.UInt32Dirty.fromObject(object.game_hu_count, _depth + 1);
            }
            if (object.effect_gain != null) {
                if (typeof object.effect_gain !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.effect_gain: object expected");
                message.effect_gain = $root.lq.UInt32Dirty.fromObject(object.effect_gain, _depth + 1);
            }
            if (object.coin_consume != null) {
                if (typeof object.coin_consume !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.coin_consume: object expected");
                message.coin_consume = $root.lq.StringDirty.fromObject(object.coin_consume, _depth + 1);
            }
            if (object.coin_gain != null) {
                if (typeof object.coin_gain !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.coin_gain: object expected");
                message.coin_gain = $root.lq.StringDirty.fromObject(object.coin_gain, _depth + 1);
            }
            if (object.highest_hu != null) {
                if (typeof object.highest_hu !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.highest_hu: object expected");
                message.highest_hu = $root.lq.ActivityAmuletHuRecordDirty.fromObject(object.highest_hu, _depth + 1);
            }
            if (object.highest_level_score != null) {
                if (typeof object.highest_level_score !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.highest_level_score: object expected");
                message.highest_level_score = $root.lq.StringDirty.fromObject(object.highest_level_score, _depth + 1);
            }
            if (object.highest_fan != null) {
                if (typeof object.highest_fan !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.highest_fan: object expected");
                message.highest_fan = $root.lq.StringDirty.fromObject(object.highest_fan, _depth + 1);
            }
            if (object.pack_count != null) {
                if (typeof object.pack_count !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.pack_count: object expected");
                message.pack_count = $root.lq.UInt32Dirty.fromObject(object.pack_count, _depth + 1);
            }
            if (object.round_count != null) {
                if (typeof object.round_count !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.round_count: object expected");
                message.round_count = $root.lq.UInt32Dirty.fromObject(object.round_count, _depth + 1);
            }
            if (object.effect_counter != null) {
                if (typeof object.effect_counter !== "object")
                    throw TypeError(".lq.AmuletRecordDataChanges.effect_counter: object expected");
                message.effect_counter = $root.lq.AmuletEffectCounterDataArrayDirty.fromObject(object.effect_counter, _depth + 1);
            }
            return message;
        };

        AmuletRecordDataChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.yiman_count = null;
                object.level_hu_count = null;
                object.game_hu_count = null;
                object.effect_gain = null;
                object.coin_consume = null;
                object.coin_gain = null;
                object.highest_hu = null;
                object.highest_level_score = null;
                object.highest_fan = null;
                object.pack_count = null;
                object.round_count = null;
                object.effect_counter = null;
            }
            if (message.yiman_count != null && message.hasOwnProperty("yiman_count"))
                object.yiman_count = $root.lq.UInt32Dirty.toObject(message.yiman_count, options);
            if (message.level_hu_count != null && message.hasOwnProperty("level_hu_count"))
                object.level_hu_count = $root.lq.UInt32Dirty.toObject(message.level_hu_count, options);
            if (message.game_hu_count != null && message.hasOwnProperty("game_hu_count"))
                object.game_hu_count = $root.lq.UInt32Dirty.toObject(message.game_hu_count, options);
            if (message.effect_gain != null && message.hasOwnProperty("effect_gain"))
                object.effect_gain = $root.lq.UInt32Dirty.toObject(message.effect_gain, options);
            if (message.coin_consume != null && message.hasOwnProperty("coin_consume"))
                object.coin_consume = $root.lq.StringDirty.toObject(message.coin_consume, options);
            if (message.coin_gain != null && message.hasOwnProperty("coin_gain"))
                object.coin_gain = $root.lq.StringDirty.toObject(message.coin_gain, options);
            if (message.highest_hu != null && message.hasOwnProperty("highest_hu"))
                object.highest_hu = $root.lq.ActivityAmuletHuRecordDirty.toObject(message.highest_hu, options);
            if (message.highest_level_score != null && message.hasOwnProperty("highest_level_score"))
                object.highest_level_score = $root.lq.StringDirty.toObject(message.highest_level_score, options);
            if (message.highest_fan != null && message.hasOwnProperty("highest_fan"))
                object.highest_fan = $root.lq.StringDirty.toObject(message.highest_fan, options);
            if (message.pack_count != null && message.hasOwnProperty("pack_count"))
                object.pack_count = $root.lq.UInt32Dirty.toObject(message.pack_count, options);
            if (message.round_count != null && message.hasOwnProperty("round_count"))
                object.round_count = $root.lq.UInt32Dirty.toObject(message.round_count, options);
            if (message.effect_counter != null && message.hasOwnProperty("effect_counter"))
                object.effect_counter = $root.lq.AmuletEffectCounterDataArrayDirty.toObject(message.effect_counter, options);
            return object;
        };

        AmuletRecordDataChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletRecordDataChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletRecordDataChanges";
        };

        return AmuletRecordDataChanges;
    })();

    lq.AmuletValueChanges = (function() {

        function AmuletValueChanges(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletValueChanges.prototype.round = null;
        AmuletValueChanges.prototype.effect = null;
        AmuletValueChanges.prototype.game = null;
        AmuletValueChanges.prototype.stage = 0;
        AmuletValueChanges.prototype.shop = null;
        AmuletValueChanges.prototype.record = null;
        AmuletValueChanges.prototype.ended = false;

        AmuletValueChanges.create = function create(properties) {
            return new AmuletValueChanges(properties);
        };

        AmuletValueChanges.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round != null && Object.hasOwnProperty.call(message, "round"))
                $root.lq.AmuletRoundDataChanges.encode(message.round, writer.uint32(10).fork()).ldelim();
            if (message.effect != null && Object.hasOwnProperty.call(message, "effect"))
                $root.lq.AmuletEffectDataChanges.encode(message.effect, writer.uint32(18).fork()).ldelim();
            if (message.game != null && Object.hasOwnProperty.call(message, "game"))
                $root.lq.AmuletGameInfoDataChanges.encode(message.game, writer.uint32(26).fork()).ldelim();
            if (message.stage != null && Object.hasOwnProperty.call(message, "stage"))
                writer.uint32(32).uint32(message.stage);
            if (message.shop != null && Object.hasOwnProperty.call(message, "shop"))
                $root.lq.AmuletShopDataChanges.encode(message.shop, writer.uint32(42).fork()).ldelim();
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.lq.AmuletRecordDataChanges.encode(message.record, writer.uint32(50).fork()).ldelim();
            if (message.ended != null && Object.hasOwnProperty.call(message, "ended"))
                writer.uint32(56).bool(message.ended);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletValueChanges.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletValueChanges.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletValueChanges(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.round = $root.lq.AmuletRoundDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.round);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.effect = $root.lq.AmuletEffectDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.effect);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game = $root.lq.AmuletGameInfoDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.game);
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.stage = value;
                        else
                            delete message.stage;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.shop = $root.lq.AmuletShopDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.shop);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.record = $root.lq.AmuletRecordDataChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.record);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.ended = value;
                        else
                            delete message.ended;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletValueChanges.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletValueChanges.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round != null && message.hasOwnProperty("round")) {
                let error = $root.lq.AmuletRoundDataChanges.verify(message.round, _depth + 1);
                if (error)
                    return "round." + error;
            }
            if (message.effect != null && message.hasOwnProperty("effect")) {
                let error = $root.lq.AmuletEffectDataChanges.verify(message.effect, _depth + 1);
                if (error)
                    return "effect." + error;
            }
            if (message.game != null && message.hasOwnProperty("game")) {
                let error = $root.lq.AmuletGameInfoDataChanges.verify(message.game, _depth + 1);
                if (error)
                    return "game." + error;
            }
            if (message.stage != null && message.hasOwnProperty("stage"))
                if (!$util.isInteger(message.stage))
                    return "stage: integer expected";
            if (message.shop != null && message.hasOwnProperty("shop")) {
                let error = $root.lq.AmuletShopDataChanges.verify(message.shop, _depth + 1);
                if (error)
                    return "shop." + error;
            }
            if (message.record != null && message.hasOwnProperty("record")) {
                let error = $root.lq.AmuletRecordDataChanges.verify(message.record, _depth + 1);
                if (error)
                    return "record." + error;
            }
            if (message.ended != null && message.hasOwnProperty("ended"))
                if (typeof message.ended !== "boolean")
                    return "ended: boolean expected";
            return null;
        };

        AmuletValueChanges.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletValueChanges)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletValueChanges();
            if (object.round != null) {
                if (typeof object.round !== "object")
                    throw TypeError(".lq.AmuletValueChanges.round: object expected");
                message.round = $root.lq.AmuletRoundDataChanges.fromObject(object.round, _depth + 1);
            }
            if (object.effect != null) {
                if (typeof object.effect !== "object")
                    throw TypeError(".lq.AmuletValueChanges.effect: object expected");
                message.effect = $root.lq.AmuletEffectDataChanges.fromObject(object.effect, _depth + 1);
            }
            if (object.game != null) {
                if (typeof object.game !== "object")
                    throw TypeError(".lq.AmuletValueChanges.game: object expected");
                message.game = $root.lq.AmuletGameInfoDataChanges.fromObject(object.game, _depth + 1);
            }
            if (object.stage != null)
                if (Number(object.stage) !== 0)
                    message.stage = object.stage >>> 0;
            if (object.shop != null) {
                if (typeof object.shop !== "object")
                    throw TypeError(".lq.AmuletValueChanges.shop: object expected");
                message.shop = $root.lq.AmuletShopDataChanges.fromObject(object.shop, _depth + 1);
            }
            if (object.record != null) {
                if (typeof object.record !== "object")
                    throw TypeError(".lq.AmuletValueChanges.record: object expected");
                message.record = $root.lq.AmuletRecordDataChanges.fromObject(object.record, _depth + 1);
            }
            if (object.ended != null)
                if (object.ended)
                    message.ended = Boolean(object.ended);
            return message;
        };

        AmuletValueChanges.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.round = null;
                object.effect = null;
                object.game = null;
                object.stage = 0;
                object.shop = null;
                object.record = null;
                object.ended = false;
            }
            if (message.round != null && message.hasOwnProperty("round"))
                object.round = $root.lq.AmuletRoundDataChanges.toObject(message.round, options);
            if (message.effect != null && message.hasOwnProperty("effect"))
                object.effect = $root.lq.AmuletEffectDataChanges.toObject(message.effect, options);
            if (message.game != null && message.hasOwnProperty("game"))
                object.game = $root.lq.AmuletGameInfoDataChanges.toObject(message.game, options);
            if (message.stage != null && message.hasOwnProperty("stage"))
                object.stage = message.stage;
            if (message.shop != null && message.hasOwnProperty("shop"))
                object.shop = $root.lq.AmuletShopDataChanges.toObject(message.shop, options);
            if (message.record != null && message.hasOwnProperty("record"))
                object.record = $root.lq.AmuletRecordDataChanges.toObject(message.record, options);
            if (message.ended != null && message.hasOwnProperty("ended"))
                object.ended = message.ended;
            return object;
        };

        AmuletValueChanges.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletValueChanges.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletValueChanges";
        };

        return AmuletValueChanges;
    })();

    lq.AmuletEventResult = (function() {

        function AmuletEventResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEventResult.prototype.deal_result = null;
        AmuletEventResult.prototype.hu_result = null;
        AmuletEventResult.prototype.game_end_result = null;
        AmuletEventResult.prototype.gang_result = null;
        AmuletEventResult.prototype.upgrade_result = null;
        AmuletEventResult.prototype.new_game_result = null;
        AmuletEventResult.prototype.sell_effect_result = null;
        AmuletEventResult.prototype.select_pack_result = null;

        AmuletEventResult.create = function create(properties) {
            return new AmuletEventResult(properties);
        };

        AmuletEventResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.deal_result != null && Object.hasOwnProperty.call(message, "deal_result"))
                $root.lq.AmuletEventResult.DealResult.encode(message.deal_result, writer.uint32(10).fork()).ldelim();
            if (message.hu_result != null && Object.hasOwnProperty.call(message, "hu_result"))
                $root.lq.AmuletEventResult.HuResult.encode(message.hu_result, writer.uint32(18).fork()).ldelim();
            if (message.game_end_result != null && Object.hasOwnProperty.call(message, "game_end_result"))
                $root.lq.AmuletEventResult.GameEndResult.encode(message.game_end_result, writer.uint32(26).fork()).ldelim();
            if (message.gang_result != null && Object.hasOwnProperty.call(message, "gang_result"))
                $root.lq.AmuletEventResult.GangResult.encode(message.gang_result, writer.uint32(34).fork()).ldelim();
            if (message.upgrade_result != null && Object.hasOwnProperty.call(message, "upgrade_result"))
                $root.lq.AmuletEventResult.UpgradeResult.encode(message.upgrade_result, writer.uint32(42).fork()).ldelim();
            if (message.new_game_result != null && Object.hasOwnProperty.call(message, "new_game_result"))
                $root.lq.AmuletGameData.encode(message.new_game_result, writer.uint32(50).fork()).ldelim();
            if (message.sell_effect_result != null && Object.hasOwnProperty.call(message, "sell_effect_result"))
                $root.lq.AmuletEventResult.SellEffectResult.encode(message.sell_effect_result, writer.uint32(58).fork()).ldelim();
            if (message.select_pack_result != null && Object.hasOwnProperty.call(message, "select_pack_result"))
                $root.lq.AmuletEventResult.SelectPackResult.encode(message.select_pack_result, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEventResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEventResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.deal_result = $root.lq.AmuletEventResult.DealResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.deal_result);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.hu_result = $root.lq.AmuletEventResult.HuResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.hu_result);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.game_end_result = $root.lq.AmuletEventResult.GameEndResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_end_result);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.gang_result = $root.lq.AmuletEventResult.GangResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.gang_result);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.upgrade_result = $root.lq.AmuletEventResult.UpgradeResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.upgrade_result);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.new_game_result = $root.lq.AmuletGameData.decode(reader, reader.uint32(), undefined, _depth + 1, message.new_game_result);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.sell_effect_result = $root.lq.AmuletEventResult.SellEffectResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.sell_effect_result);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.select_pack_result = $root.lq.AmuletEventResult.SelectPackResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.select_pack_result);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEventResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEventResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.deal_result != null && message.hasOwnProperty("deal_result")) {
                let error = $root.lq.AmuletEventResult.DealResult.verify(message.deal_result, _depth + 1);
                if (error)
                    return "deal_result." + error;
            }
            if (message.hu_result != null && message.hasOwnProperty("hu_result")) {
                let error = $root.lq.AmuletEventResult.HuResult.verify(message.hu_result, _depth + 1);
                if (error)
                    return "hu_result." + error;
            }
            if (message.game_end_result != null && message.hasOwnProperty("game_end_result")) {
                let error = $root.lq.AmuletEventResult.GameEndResult.verify(message.game_end_result, _depth + 1);
                if (error)
                    return "game_end_result." + error;
            }
            if (message.gang_result != null && message.hasOwnProperty("gang_result")) {
                let error = $root.lq.AmuletEventResult.GangResult.verify(message.gang_result, _depth + 1);
                if (error)
                    return "gang_result." + error;
            }
            if (message.upgrade_result != null && message.hasOwnProperty("upgrade_result")) {
                let error = $root.lq.AmuletEventResult.UpgradeResult.verify(message.upgrade_result, _depth + 1);
                if (error)
                    return "upgrade_result." + error;
            }
            if (message.new_game_result != null && message.hasOwnProperty("new_game_result")) {
                let error = $root.lq.AmuletGameData.verify(message.new_game_result, _depth + 1);
                if (error)
                    return "new_game_result." + error;
            }
            if (message.sell_effect_result != null && message.hasOwnProperty("sell_effect_result")) {
                let error = $root.lq.AmuletEventResult.SellEffectResult.verify(message.sell_effect_result, _depth + 1);
                if (error)
                    return "sell_effect_result." + error;
            }
            if (message.select_pack_result != null && message.hasOwnProperty("select_pack_result")) {
                let error = $root.lq.AmuletEventResult.SelectPackResult.verify(message.select_pack_result, _depth + 1);
                if (error)
                    return "select_pack_result." + error;
            }
            return null;
        };

        AmuletEventResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEventResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEventResult();
            if (object.deal_result != null) {
                if (typeof object.deal_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.deal_result: object expected");
                message.deal_result = $root.lq.AmuletEventResult.DealResult.fromObject(object.deal_result, _depth + 1);
            }
            if (object.hu_result != null) {
                if (typeof object.hu_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.hu_result: object expected");
                message.hu_result = $root.lq.AmuletEventResult.HuResult.fromObject(object.hu_result, _depth + 1);
            }
            if (object.game_end_result != null) {
                if (typeof object.game_end_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.game_end_result: object expected");
                message.game_end_result = $root.lq.AmuletEventResult.GameEndResult.fromObject(object.game_end_result, _depth + 1);
            }
            if (object.gang_result != null) {
                if (typeof object.gang_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.gang_result: object expected");
                message.gang_result = $root.lq.AmuletEventResult.GangResult.fromObject(object.gang_result, _depth + 1);
            }
            if (object.upgrade_result != null) {
                if (typeof object.upgrade_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.upgrade_result: object expected");
                message.upgrade_result = $root.lq.AmuletEventResult.UpgradeResult.fromObject(object.upgrade_result, _depth + 1);
            }
            if (object.new_game_result != null) {
                if (typeof object.new_game_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.new_game_result: object expected");
                message.new_game_result = $root.lq.AmuletGameData.fromObject(object.new_game_result, _depth + 1);
            }
            if (object.sell_effect_result != null) {
                if (typeof object.sell_effect_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.sell_effect_result: object expected");
                message.sell_effect_result = $root.lq.AmuletEventResult.SellEffectResult.fromObject(object.sell_effect_result, _depth + 1);
            }
            if (object.select_pack_result != null) {
                if (typeof object.select_pack_result !== "object")
                    throw TypeError(".lq.AmuletEventResult.select_pack_result: object expected");
                message.select_pack_result = $root.lq.AmuletEventResult.SelectPackResult.fromObject(object.select_pack_result, _depth + 1);
            }
            return message;
        };

        AmuletEventResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.deal_result = null;
                object.hu_result = null;
                object.game_end_result = null;
                object.gang_result = null;
                object.upgrade_result = null;
                object.new_game_result = null;
                object.sell_effect_result = null;
                object.select_pack_result = null;
            }
            if (message.deal_result != null && message.hasOwnProperty("deal_result"))
                object.deal_result = $root.lq.AmuletEventResult.DealResult.toObject(message.deal_result, options);
            if (message.hu_result != null && message.hasOwnProperty("hu_result"))
                object.hu_result = $root.lq.AmuletEventResult.HuResult.toObject(message.hu_result, options);
            if (message.game_end_result != null && message.hasOwnProperty("game_end_result"))
                object.game_end_result = $root.lq.AmuletEventResult.GameEndResult.toObject(message.game_end_result, options);
            if (message.gang_result != null && message.hasOwnProperty("gang_result"))
                object.gang_result = $root.lq.AmuletEventResult.GangResult.toObject(message.gang_result, options);
            if (message.upgrade_result != null && message.hasOwnProperty("upgrade_result"))
                object.upgrade_result = $root.lq.AmuletEventResult.UpgradeResult.toObject(message.upgrade_result, options);
            if (message.new_game_result != null && message.hasOwnProperty("new_game_result"))
                object.new_game_result = $root.lq.AmuletGameData.toObject(message.new_game_result, options);
            if (message.sell_effect_result != null && message.hasOwnProperty("sell_effect_result"))
                object.sell_effect_result = $root.lq.AmuletEventResult.SellEffectResult.toObject(message.sell_effect_result, options);
            if (message.select_pack_result != null && message.hasOwnProperty("select_pack_result"))
                object.select_pack_result = $root.lq.AmuletEventResult.SelectPackResult.toObject(message.select_pack_result, options);
            return object;
        };

        AmuletEventResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEventResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEventResult";
        };

        AmuletEventResult.DealResult = (function() {

            function DealResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            DealResult.prototype.tile = 0;

            DealResult.create = function create(properties) {
                return new DealResult(properties);
            };

            DealResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                    writer.uint32(8).uint32(message.tile);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            DealResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            DealResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.DealResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.tile = value;
                            else
                                delete message.tile;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            DealResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            DealResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.tile != null && message.hasOwnProperty("tile"))
                    if (!$util.isInteger(message.tile))
                        return "tile: integer expected";
                return null;
            };

            DealResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.DealResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.DealResult();
                if (object.tile != null)
                    if (Number(object.tile) !== 0)
                        message.tile = object.tile >>> 0;
                return message;
            };

            DealResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.tile = 0;
                if (message.tile != null && message.hasOwnProperty("tile"))
                    object.tile = message.tile;
                return object;
            };

            DealResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            DealResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.DealResult";
            };

            return DealResult;
        })();

        AmuletEventResult.HuResult = (function() {

            function HuResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            HuResult.prototype.hu_final = null;
            HuResult.prototype.hu_base = null;

            HuResult.create = function create(properties) {
                return new HuResult(properties);
            };

            HuResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.hu_final != null && Object.hasOwnProperty.call(message, "hu_final"))
                    $root.lq.AmuletEventResult.HuResult.HuInfo.encode(message.hu_final, writer.uint32(10).fork()).ldelim();
                if (message.hu_base != null && Object.hasOwnProperty.call(message, "hu_base"))
                    $root.lq.AmuletEventResult.HuResult.HuInfo.encode(message.hu_base, writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            HuResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            HuResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.HuResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.hu_final = $root.lq.AmuletEventResult.HuResult.HuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hu_final);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.hu_base = $root.lq.AmuletEventResult.HuResult.HuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hu_base);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            HuResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            HuResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.hu_final != null && message.hasOwnProperty("hu_final")) {
                    let error = $root.lq.AmuletEventResult.HuResult.HuInfo.verify(message.hu_final, _depth + 1);
                    if (error)
                        return "hu_final." + error;
                }
                if (message.hu_base != null && message.hasOwnProperty("hu_base")) {
                    let error = $root.lq.AmuletEventResult.HuResult.HuInfo.verify(message.hu_base, _depth + 1);
                    if (error)
                        return "hu_base." + error;
                }
                return null;
            };

            HuResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.HuResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.HuResult();
                if (object.hu_final != null) {
                    if (typeof object.hu_final !== "object")
                        throw TypeError(".lq.AmuletEventResult.HuResult.hu_final: object expected");
                    message.hu_final = $root.lq.AmuletEventResult.HuResult.HuInfo.fromObject(object.hu_final, _depth + 1);
                }
                if (object.hu_base != null) {
                    if (typeof object.hu_base !== "object")
                        throw TypeError(".lq.AmuletEventResult.HuResult.hu_base: object expected");
                    message.hu_base = $root.lq.AmuletEventResult.HuResult.HuInfo.fromObject(object.hu_base, _depth + 1);
                }
                return message;
            };

            HuResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.hu_final = null;
                    object.hu_base = null;
                }
                if (message.hu_final != null && message.hasOwnProperty("hu_final"))
                    object.hu_final = $root.lq.AmuletEventResult.HuResult.HuInfo.toObject(message.hu_final, options);
                if (message.hu_base != null && message.hasOwnProperty("hu_base"))
                    object.hu_base = $root.lq.AmuletEventResult.HuResult.HuInfo.toObject(message.hu_base, options);
                return object;
            };

            HuResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HuResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.HuResult";
            };

            HuResult.HuInfo = (function() {

                function HuInfo(properties) {
                    this.fan_list = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                HuInfo.prototype.tile = 0;
                HuInfo.prototype.fan_list = $util.emptyArray;
                HuInfo.prototype.fan = "";
                HuInfo.prototype.base = "";
                HuInfo.prototype.point = "";

                HuInfo.create = function create(properties) {
                    return new HuInfo(properties);
                };

                HuInfo.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                        writer.uint32(8).uint32(message.tile);
                    if (message.fan_list != null && message.fan_list.length)
                        for (let i = 0; i < message.fan_list.length; ++i)
                            $root.lq.AmuletFan.encode(message.fan_list[i], writer.uint32(18).fork()).ldelim();
                    if (message.fan != null && Object.hasOwnProperty.call(message, "fan"))
                        writer.uint32(26).string(message.fan);
                    if (message.base != null && Object.hasOwnProperty.call(message, "base"))
                        writer.uint32(34).string(message.base);
                    if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                        writer.uint32(42).string(message.point);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                HuInfo.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                HuInfo.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.HuResult.HuInfo(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.tile = value;
                                else
                                    delete message.tile;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.fan_list && message.fan_list.length))
                                    message.fan_list = [];
                                message.fan_list.push($root.lq.AmuletFan.decode(reader, reader.uint32(), undefined, _depth + 1));
                                continue;
                            }
                        case 3: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.fan = value;
                                else
                                    delete message.fan;
                                continue;
                            }
                        case 4: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.base = value;
                                else
                                    delete message.base;
                                continue;
                            }
                        case 5: {
                                if (wireType !== 2)
                                    break;
                                if ((value = reader.string()).length)
                                    message.point = value;
                                else
                                    delete message.point;
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                HuInfo.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                HuInfo.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.tile != null && message.hasOwnProperty("tile"))
                        if (!$util.isInteger(message.tile))
                            return "tile: integer expected";
                    if (message.fan_list != null && message.hasOwnProperty("fan_list")) {
                        if (!Array.isArray(message.fan_list))
                            return "fan_list: array expected";
                        for (let i = 0; i < message.fan_list.length; ++i) {
                            let error = $root.lq.AmuletFan.verify(message.fan_list[i], _depth + 1);
                            if (error)
                                return "fan_list." + error;
                        }
                    }
                    if (message.fan != null && message.hasOwnProperty("fan"))
                        if (!$util.isString(message.fan))
                            return "fan: string expected";
                    if (message.base != null && message.hasOwnProperty("base"))
                        if (!$util.isString(message.base))
                            return "base: string expected";
                    if (message.point != null && message.hasOwnProperty("point"))
                        if (!$util.isString(message.point))
                            return "point: string expected";
                    return null;
                };

                HuInfo.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.AmuletEventResult.HuResult.HuInfo)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.AmuletEventResult.HuResult.HuInfo();
                    if (object.tile != null)
                        if (Number(object.tile) !== 0)
                            message.tile = object.tile >>> 0;
                    if (object.fan_list) {
                        if (!Array.isArray(object.fan_list))
                            throw TypeError(".lq.AmuletEventResult.HuResult.HuInfo.fan_list: array expected");
                        message.fan_list = Array(object.fan_list.length);
                        for (let i = 0; i < object.fan_list.length; ++i) {
                            if (typeof object.fan_list[i] !== "object")
                                throw TypeError(".lq.AmuletEventResult.HuResult.HuInfo.fan_list: object expected");
                            message.fan_list[i] = $root.lq.AmuletFan.fromObject(object.fan_list[i], _depth + 1);
                        }
                    }
                    if (object.fan != null)
                        if (typeof object.fan !== "string" || object.fan.length)
                            message.fan = String(object.fan);
                    if (object.base != null)
                        if (typeof object.base !== "string" || object.base.length)
                            message.base = String(object.base);
                    if (object.point != null)
                        if (typeof object.point !== "string" || object.point.length)
                            message.point = String(object.point);
                    return message;
                };

                HuInfo.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.fan_list = [];
                    if (options.defaults) {
                        object.tile = 0;
                        object.fan = "";
                        object.base = "";
                        object.point = "";
                    }
                    if (message.tile != null && message.hasOwnProperty("tile"))
                        object.tile = message.tile;
                    if (message.fan_list && message.fan_list.length) {
                        object.fan_list = Array(message.fan_list.length);
                        for (let j = 0; j < message.fan_list.length; ++j)
                            object.fan_list[j] = $root.lq.AmuletFan.toObject(message.fan_list[j], options);
                    }
                    if (message.fan != null && message.hasOwnProperty("fan"))
                        object.fan = message.fan;
                    if (message.base != null && message.hasOwnProperty("base"))
                        object.base = message.base;
                    if (message.point != null && message.hasOwnProperty("point"))
                        object.point = message.point;
                    return object;
                };

                HuInfo.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                HuInfo.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.AmuletEventResult.HuResult.HuInfo";
                };

                return HuInfo;
            })();

            return HuResult;
        })();

        AmuletEventResult.GameEndResult = (function() {

            function GameEndResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GameEndResult.prototype.reason = 0;

            GameEndResult.create = function create(properties) {
                return new GameEndResult(properties);
            };

            GameEndResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                    writer.uint32(8).uint32(message.reason);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GameEndResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GameEndResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.GameEndResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.reason = value;
                            else
                                delete message.reason;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GameEndResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GameEndResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.reason != null && message.hasOwnProperty("reason"))
                    if (!$util.isInteger(message.reason))
                        return "reason: integer expected";
                return null;
            };

            GameEndResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.GameEndResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.GameEndResult();
                if (object.reason != null)
                    if (Number(object.reason) !== 0)
                        message.reason = object.reason >>> 0;
                return message;
            };

            GameEndResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.reason = 0;
                if (message.reason != null && message.hasOwnProperty("reason"))
                    object.reason = message.reason;
                return object;
            };

            GameEndResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GameEndResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.GameEndResult";
            };

            return GameEndResult;
        })();

        AmuletEventResult.GangResult = (function() {

            function GangResult(properties) {
                this.new_dora = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            GangResult.prototype.new_dora = $util.emptyArray;

            GangResult.create = function create(properties) {
                return new GangResult(properties);
            };

            GangResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.new_dora != null && message.new_dora.length) {
                    writer.uint32(10).fork();
                    for (let i = 0; i < message.new_dora.length; ++i)
                        writer.uint32(message.new_dora[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            GangResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            GangResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.GangResult();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType === 2) {
                                if (!(message.new_dora && message.new_dora.length))
                                    message.new_dora = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.new_dora.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.new_dora && message.new_dora.length))
                                message.new_dora = [];
                            message.new_dora.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            GangResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            GangResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.new_dora != null && message.hasOwnProperty("new_dora")) {
                    if (!Array.isArray(message.new_dora))
                        return "new_dora: array expected";
                    for (let i = 0; i < message.new_dora.length; ++i)
                        if (!$util.isInteger(message.new_dora[i]))
                            return "new_dora: integer[] expected";
                }
                return null;
            };

            GangResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.GangResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.GangResult();
                if (object.new_dora) {
                    if (!Array.isArray(object.new_dora))
                        throw TypeError(".lq.AmuletEventResult.GangResult.new_dora: array expected");
                    message.new_dora = Array(object.new_dora.length);
                    for (let i = 0; i < object.new_dora.length; ++i)
                        message.new_dora[i] = object.new_dora[i] >>> 0;
                }
                return message;
            };

            GangResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.new_dora = [];
                if (message.new_dora && message.new_dora.length) {
                    object.new_dora = Array(message.new_dora.length);
                    for (let j = 0; j < message.new_dora.length; ++j)
                        object.new_dora[j] = message.new_dora[j];
                }
                return object;
            };

            GangResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GangResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.GangResult";
            };

            return GangResult;
        })();

        AmuletEventResult.UpgradeResult = (function() {

            function UpgradeResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            UpgradeResult.prototype.level_coin = "";
            UpgradeResult.prototype.point_coin = "";

            UpgradeResult.create = function create(properties) {
                return new UpgradeResult(properties);
            };

            UpgradeResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level_coin != null && Object.hasOwnProperty.call(message, "level_coin"))
                    writer.uint32(10).string(message.level_coin);
                if (message.point_coin != null && Object.hasOwnProperty.call(message, "point_coin"))
                    writer.uint32(18).string(message.point_coin);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            UpgradeResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            UpgradeResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.UpgradeResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.level_coin = value;
                            else
                                delete message.level_coin;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.point_coin = value;
                            else
                                delete message.point_coin;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            UpgradeResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            UpgradeResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level_coin != null && message.hasOwnProperty("level_coin"))
                    if (!$util.isString(message.level_coin))
                        return "level_coin: string expected";
                if (message.point_coin != null && message.hasOwnProperty("point_coin"))
                    if (!$util.isString(message.point_coin))
                        return "point_coin: string expected";
                return null;
            };

            UpgradeResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.UpgradeResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.UpgradeResult();
                if (object.level_coin != null)
                    if (typeof object.level_coin !== "string" || object.level_coin.length)
                        message.level_coin = String(object.level_coin);
                if (object.point_coin != null)
                    if (typeof object.point_coin !== "string" || object.point_coin.length)
                        message.point_coin = String(object.point_coin);
                return message;
            };

            UpgradeResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level_coin = "";
                    object.point_coin = "";
                }
                if (message.level_coin != null && message.hasOwnProperty("level_coin"))
                    object.level_coin = message.level_coin;
                if (message.point_coin != null && message.hasOwnProperty("point_coin"))
                    object.point_coin = message.point_coin;
                return object;
            };

            UpgradeResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            UpgradeResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.UpgradeResult";
            };

            return UpgradeResult;
        })();

        AmuletEventResult.SellEffectResult = (function() {

            function SellEffectResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SellEffectResult.prototype.price = "";

            SellEffectResult.create = function create(properties) {
                return new SellEffectResult(properties);
            };

            SellEffectResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.price != null && Object.hasOwnProperty.call(message, "price"))
                    writer.uint32(10).string(message.price);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SellEffectResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SellEffectResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.SellEffectResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if ((value = reader.string()).length)
                                message.price = value;
                            else
                                delete message.price;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SellEffectResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SellEffectResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.price != null && message.hasOwnProperty("price"))
                    if (!$util.isString(message.price))
                        return "price: string expected";
                return null;
            };

            SellEffectResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.SellEffectResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.SellEffectResult();
                if (object.price != null)
                    if (typeof object.price !== "string" || object.price.length)
                        message.price = String(object.price);
                return message;
            };

            SellEffectResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults)
                    object.price = "";
                if (message.price != null && message.hasOwnProperty("price"))
                    object.price = message.price;
                return object;
            };

            SellEffectResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SellEffectResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.SellEffectResult";
            };

            return SellEffectResult;
        })();

        AmuletEventResult.SelectPackResult = (function() {

            function SelectPackResult(properties) {
                this.merged_list = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            SelectPackResult.prototype.uid = 0;
            SelectPackResult.prototype.id = 0;
            SelectPackResult.prototype.merge_type = 0;
            SelectPackResult.prototype.merged_list = $util.emptyArray;
            SelectPackResult.prototype.merged_result = 0;
            SelectPackResult.prototype.badge = null;

            SelectPackResult.create = function create(properties) {
                return new SelectPackResult(properties);
            };

            SelectPackResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                    writer.uint32(8).uint32(message.uid);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(16).uint32(message.id);
                if (message.merge_type != null && Object.hasOwnProperty.call(message, "merge_type"))
                    writer.uint32(24).uint32(message.merge_type);
                if (message.merged_list != null && message.merged_list.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.merged_list.length; ++i)
                        writer.uint32(message.merged_list[i]);
                    writer.ldelim();
                }
                if (message.merged_result != null && Object.hasOwnProperty.call(message, "merged_result"))
                    writer.uint32(40).uint32(message.merged_result);
                if (message.badge != null && Object.hasOwnProperty.call(message, "badge"))
                    $root.lq.AmuletBadgeData.encode(message.badge, writer.uint32(50).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            SelectPackResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            SelectPackResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventResult.SelectPackResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.uid = value;
                            else
                                delete message.uid;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.id = value;
                            else
                                delete message.id;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.merge_type = value;
                            else
                                delete message.merge_type;
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.merged_list && message.merged_list.length))
                                    message.merged_list = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.merged_list.push(reader.uint32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.merged_list && message.merged_list.length))
                                message.merged_list = [];
                            message.merged_list.push(reader.uint32());
                            continue;
                        }
                    case 5: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.merged_result = value;
                            else
                                delete message.merged_result;
                            continue;
                        }
                    case 6: {
                            if (wireType !== 2)
                                break;
                            message.badge = $root.lq.AmuletBadgeData.decode(reader, reader.uint32(), undefined, _depth + 1, message.badge);
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            SelectPackResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            SelectPackResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.uid != null && message.hasOwnProperty("uid"))
                    if (!$util.isInteger(message.uid))
                        return "uid: integer expected";
                if (message.id != null && message.hasOwnProperty("id"))
                    if (!$util.isInteger(message.id))
                        return "id: integer expected";
                if (message.merge_type != null && message.hasOwnProperty("merge_type"))
                    if (!$util.isInteger(message.merge_type))
                        return "merge_type: integer expected";
                if (message.merged_list != null && message.hasOwnProperty("merged_list")) {
                    if (!Array.isArray(message.merged_list))
                        return "merged_list: array expected";
                    for (let i = 0; i < message.merged_list.length; ++i)
                        if (!$util.isInteger(message.merged_list[i]))
                            return "merged_list: integer[] expected";
                }
                if (message.merged_result != null && message.hasOwnProperty("merged_result"))
                    if (!$util.isInteger(message.merged_result))
                        return "merged_result: integer expected";
                if (message.badge != null && message.hasOwnProperty("badge")) {
                    let error = $root.lq.AmuletBadgeData.verify(message.badge, _depth + 1);
                    if (error)
                        return "badge." + error;
                }
                return null;
            };

            SelectPackResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.AmuletEventResult.SelectPackResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.AmuletEventResult.SelectPackResult();
                if (object.uid != null)
                    if (Number(object.uid) !== 0)
                        message.uid = object.uid >>> 0;
                if (object.id != null)
                    if (Number(object.id) !== 0)
                        message.id = object.id >>> 0;
                if (object.merge_type != null)
                    if (Number(object.merge_type) !== 0)
                        message.merge_type = object.merge_type >>> 0;
                if (object.merged_list) {
                    if (!Array.isArray(object.merged_list))
                        throw TypeError(".lq.AmuletEventResult.SelectPackResult.merged_list: array expected");
                    message.merged_list = Array(object.merged_list.length);
                    for (let i = 0; i < object.merged_list.length; ++i)
                        message.merged_list[i] = object.merged_list[i] >>> 0;
                }
                if (object.merged_result != null)
                    if (Number(object.merged_result) !== 0)
                        message.merged_result = object.merged_result >>> 0;
                if (object.badge != null) {
                    if (typeof object.badge !== "object")
                        throw TypeError(".lq.AmuletEventResult.SelectPackResult.badge: object expected");
                    message.badge = $root.lq.AmuletBadgeData.fromObject(object.badge, _depth + 1);
                }
                return message;
            };

            SelectPackResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.merged_list = [];
                if (options.defaults) {
                    object.uid = 0;
                    object.id = 0;
                    object.merge_type = 0;
                    object.merged_result = 0;
                    object.badge = null;
                }
                if (message.uid != null && message.hasOwnProperty("uid"))
                    object.uid = message.uid;
                if (message.id != null && message.hasOwnProperty("id"))
                    object.id = message.id;
                if (message.merge_type != null && message.hasOwnProperty("merge_type"))
                    object.merge_type = message.merge_type;
                if (message.merged_list && message.merged_list.length) {
                    object.merged_list = Array(message.merged_list.length);
                    for (let j = 0; j < message.merged_list.length; ++j)
                        object.merged_list[j] = message.merged_list[j];
                }
                if (message.merged_result != null && message.hasOwnProperty("merged_result"))
                    object.merged_result = message.merged_result;
                if (message.badge != null && message.hasOwnProperty("badge"))
                    object.badge = $root.lq.AmuletBadgeData.toObject(message.badge, options);
                return object;
            };

            SelectPackResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SelectPackResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.AmuletEventResult.SelectPackResult";
            };

            return SelectPackResult;
        })();

        return AmuletEventResult;
    })();

    lq.AmuletEventHookData = (function() {

        function AmuletEventHookData(properties) {
            this.remove_effect = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEventHookData.prototype.remove_effect = $util.emptyArray;

        AmuletEventHookData.create = function create(properties) {
            return new AmuletEventHookData(properties);
        };

        AmuletEventHookData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.remove_effect != null && message.remove_effect.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.remove_effect.length; ++i)
                    writer.uint32(message.remove_effect[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEventHookData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEventHookData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventHookData();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.remove_effect && message.remove_effect.length))
                                message.remove_effect = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.remove_effect.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.remove_effect && message.remove_effect.length))
                            message.remove_effect = [];
                        message.remove_effect.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEventHookData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEventHookData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.remove_effect != null && message.hasOwnProperty("remove_effect")) {
                if (!Array.isArray(message.remove_effect))
                    return "remove_effect: array expected";
                for (let i = 0; i < message.remove_effect.length; ++i)
                    if (!$util.isInteger(message.remove_effect[i]))
                        return "remove_effect: integer[] expected";
            }
            return null;
        };

        AmuletEventHookData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEventHookData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEventHookData();
            if (object.remove_effect) {
                if (!Array.isArray(object.remove_effect))
                    throw TypeError(".lq.AmuletEventHookData.remove_effect: array expected");
                message.remove_effect = Array(object.remove_effect.length);
                for (let i = 0; i < object.remove_effect.length; ++i)
                    message.remove_effect[i] = object.remove_effect[i] >>> 0;
            }
            return message;
        };

        AmuletEventHookData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.remove_effect = [];
            if (message.remove_effect && message.remove_effect.length) {
                object.remove_effect = Array(message.remove_effect.length);
                for (let j = 0; j < message.remove_effect.length; ++j)
                    object.remove_effect[j] = message.remove_effect[j];
            }
            return object;
        };

        AmuletEventHookData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEventHookData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEventHookData";
        };

        return AmuletEventHookData;
    })();

    lq.AmuletEventData = (function() {

        function AmuletEventData(properties) {
            this.effected_hooks = [];
            this.event_hooks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        AmuletEventData.prototype.type = 0;
        AmuletEventData.prototype.effected_hooks = $util.emptyArray;
        AmuletEventData.prototype.value_changes = null;
        AmuletEventData.prototype.result = null;
        AmuletEventData.prototype.event_hooks = $util.emptyArray;

        AmuletEventData.create = function create(properties) {
            return new AmuletEventData(properties);
        };

        AmuletEventData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.effected_hooks != null && message.effected_hooks.length)
                for (let i = 0; i < message.effected_hooks.length; ++i)
                    $root.lq.AmuletEffectedHookData.encode(message.effected_hooks[i], writer.uint32(18).fork()).ldelim();
            if (message.value_changes != null && Object.hasOwnProperty.call(message, "value_changes"))
                $root.lq.AmuletValueChanges.encode(message.value_changes, writer.uint32(26).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.lq.AmuletEventResult.encode(message.result, writer.uint32(34).fork()).ldelim();
            if (message.event_hooks != null && message.event_hooks.length)
                for (let i = 0; i < message.event_hooks.length; ++i)
                    $root.lq.AmuletEventHookData.encode(message.event_hooks[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AmuletEventData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        AmuletEventData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.AmuletEventData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.effected_hooks && message.effected_hooks.length))
                            message.effected_hooks = [];
                        message.effected_hooks.push($root.lq.AmuletEffectedHookData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.value_changes = $root.lq.AmuletValueChanges.decode(reader, reader.uint32(), undefined, _depth + 1, message.value_changes);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.result = $root.lq.AmuletEventResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.event_hooks && message.event_hooks.length))
                            message.event_hooks = [];
                        message.event_hooks.push($root.lq.AmuletEventHookData.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        AmuletEventData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        AmuletEventData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.effected_hooks != null && message.hasOwnProperty("effected_hooks")) {
                if (!Array.isArray(message.effected_hooks))
                    return "effected_hooks: array expected";
                for (let i = 0; i < message.effected_hooks.length; ++i) {
                    let error = $root.lq.AmuletEffectedHookData.verify(message.effected_hooks[i], _depth + 1);
                    if (error)
                        return "effected_hooks." + error;
                }
            }
            if (message.value_changes != null && message.hasOwnProperty("value_changes")) {
                let error = $root.lq.AmuletValueChanges.verify(message.value_changes, _depth + 1);
                if (error)
                    return "value_changes." + error;
            }
            if (message.result != null && message.hasOwnProperty("result")) {
                let error = $root.lq.AmuletEventResult.verify(message.result, _depth + 1);
                if (error)
                    return "result." + error;
            }
            if (message.event_hooks != null && message.hasOwnProperty("event_hooks")) {
                if (!Array.isArray(message.event_hooks))
                    return "event_hooks: array expected";
                for (let i = 0; i < message.event_hooks.length; ++i) {
                    let error = $root.lq.AmuletEventHookData.verify(message.event_hooks[i], _depth + 1);
                    if (error)
                        return "event_hooks." + error;
                }
            }
            return null;
        };

        AmuletEventData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.AmuletEventData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.AmuletEventData();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.effected_hooks) {
                if (!Array.isArray(object.effected_hooks))
                    throw TypeError(".lq.AmuletEventData.effected_hooks: array expected");
                message.effected_hooks = Array(object.effected_hooks.length);
                for (let i = 0; i < object.effected_hooks.length; ++i) {
                    if (typeof object.effected_hooks[i] !== "object")
                        throw TypeError(".lq.AmuletEventData.effected_hooks: object expected");
                    message.effected_hooks[i] = $root.lq.AmuletEffectedHookData.fromObject(object.effected_hooks[i], _depth + 1);
                }
            }
            if (object.value_changes != null) {
                if (typeof object.value_changes !== "object")
                    throw TypeError(".lq.AmuletEventData.value_changes: object expected");
                message.value_changes = $root.lq.AmuletValueChanges.fromObject(object.value_changes, _depth + 1);
            }
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".lq.AmuletEventData.result: object expected");
                message.result = $root.lq.AmuletEventResult.fromObject(object.result, _depth + 1);
            }
            if (object.event_hooks) {
                if (!Array.isArray(object.event_hooks))
                    throw TypeError(".lq.AmuletEventData.event_hooks: array expected");
                message.event_hooks = Array(object.event_hooks.length);
                for (let i = 0; i < object.event_hooks.length; ++i) {
                    if (typeof object.event_hooks[i] !== "object")
                        throw TypeError(".lq.AmuletEventData.event_hooks: object expected");
                    message.event_hooks[i] = $root.lq.AmuletEventHookData.fromObject(object.event_hooks[i], _depth + 1);
                }
            }
            return message;
        };

        AmuletEventData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.effected_hooks = [];
                object.event_hooks = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.value_changes = null;
                object.result = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.effected_hooks && message.effected_hooks.length) {
                object.effected_hooks = Array(message.effected_hooks.length);
                for (let j = 0; j < message.effected_hooks.length; ++j)
                    object.effected_hooks[j] = $root.lq.AmuletEffectedHookData.toObject(message.effected_hooks[j], options);
            }
            if (message.value_changes != null && message.hasOwnProperty("value_changes"))
                object.value_changes = $root.lq.AmuletValueChanges.toObject(message.value_changes, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.lq.AmuletEventResult.toObject(message.result, options);
            if (message.event_hooks && message.event_hooks.length) {
                object.event_hooks = Array(message.event_hooks.length);
                for (let j = 0; j < message.event_hooks.length; ++j)
                    object.event_hooks[j] = $root.lq.AmuletEventHookData.toObject(message.event_hooks[j], options);
            }
            return object;
        };

        AmuletEventData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AmuletEventData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.AmuletEventData";
        };

        return AmuletEventData;
    })();

    lq.FastTest = (function() {

        function FastTest(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        }

        (FastTest.prototype = Object.create($protobuf.rpc.Service.prototype)).constructor = FastTest;

        FastTest.create = function create(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };


        Object.defineProperty(FastTest.prototype.authGame = function authGame(request, callback) {
            return this.rpcCall(authGame, $root.lq.ReqAuthGame, $root.lq.ResAuthGame, request, callback);
        }, "name", { value: "authGame" });


        Object.defineProperty(FastTest.prototype.enterGame = function enterGame(request, callback) {
            return this.rpcCall(enterGame, $root.lq.ReqCommon, $root.lq.ResEnterGame, request, callback);
        }, "name", { value: "enterGame" });


        Object.defineProperty(FastTest.prototype.syncGame = function syncGame(request, callback) {
            return this.rpcCall(syncGame, $root.lq.ReqSyncGame, $root.lq.ResSyncGame, request, callback);
        }, "name", { value: "syncGame" });


        Object.defineProperty(FastTest.prototype.finishSyncGame = function finishSyncGame(request, callback) {
            return this.rpcCall(finishSyncGame, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "finishSyncGame" });


        Object.defineProperty(FastTest.prototype.terminateGame = function terminateGame(request, callback) {
            return this.rpcCall(terminateGame, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "terminateGame" });


        Object.defineProperty(FastTest.prototype.inputOperation = function inputOperation(request, callback) {
            return this.rpcCall(inputOperation, $root.lq.ReqSelfOperation, $root.lq.ResCommon, request, callback);
        }, "name", { value: "inputOperation" });


        Object.defineProperty(FastTest.prototype.inputChiPengGang = function inputChiPengGang(request, callback) {
            return this.rpcCall(inputChiPengGang, $root.lq.ReqChiPengGang, $root.lq.ResCommon, request, callback);
        }, "name", { value: "inputChiPengGang" });


        Object.defineProperty(FastTest.prototype.confirmNewRound = function confirmNewRound(request, callback) {
            return this.rpcCall(confirmNewRound, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "confirmNewRound" });


        Object.defineProperty(FastTest.prototype.broadcastInGame = function broadcastInGame(request, callback) {
            return this.rpcCall(broadcastInGame, $root.lq.ReqBroadcastInGame, $root.lq.ResCommon, request, callback);
        }, "name", { value: "broadcastInGame" });


        Object.defineProperty(FastTest.prototype.inputGameGMCommand = function inputGameGMCommand(request, callback) {
            return this.rpcCall(inputGameGMCommand, $root.lq.ReqGMCommandInGaming, $root.lq.ResCommon, request, callback);
        }, "name", { value: "inputGameGMCommand" });


        Object.defineProperty(FastTest.prototype.fetchGamePlayerState = function fetchGamePlayerState(request, callback) {
            return this.rpcCall(fetchGamePlayerState, $root.lq.ReqCommon, $root.lq.ResGamePlayerState, request, callback);
        }, "name", { value: "fetchGamePlayerState" });


        Object.defineProperty(FastTest.prototype.checkNetworkDelay = function checkNetworkDelay(request, callback) {
            return this.rpcCall(checkNetworkDelay, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "checkNetworkDelay" });


        Object.defineProperty(FastTest.prototype.clearLeaving = function clearLeaving(request, callback) {
            return this.rpcCall(clearLeaving, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "clearLeaving" });


        Object.defineProperty(FastTest.prototype.voteGameEnd = function voteGameEnd(request, callback) {
            return this.rpcCall(voteGameEnd, $root.lq.ReqVoteGameEnd, $root.lq.ResGameEndVote, request, callback);
        }, "name", { value: "voteGameEnd" });


        Object.defineProperty(FastTest.prototype.authObserve = function authObserve(request, callback) {
            return this.rpcCall(authObserve, $root.lq.ReqAuthObserve, $root.lq.ResCommon, request, callback);
        }, "name", { value: "authObserve" });


        Object.defineProperty(FastTest.prototype.startObserve = function startObserve(request, callback) {
            return this.rpcCall(startObserve, $root.lq.ReqCommon, $root.lq.ResStartObserve, request, callback);
        }, "name", { value: "startObserve" });


        Object.defineProperty(FastTest.prototype.stopObserve = function stopObserve(request, callback) {
            return this.rpcCall(stopObserve, $root.lq.ReqCommon, $root.lq.ResCommon, request, callback);
        }, "name", { value: "stopObserve" });

        return FastTest;
    })();

    lq.ReqAuthGame = (function() {

        function ReqAuthGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAuthGame.prototype.account_id = 0;
        ReqAuthGame.prototype.token = "";
        ReqAuthGame.prototype.game_uuid = "";
        ReqAuthGame.prototype.device = null;

        ReqAuthGame.create = function create(properties) {
            return new ReqAuthGame(properties);
        };

        ReqAuthGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                writer.uint32(8).uint32(message.account_id);
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(18).string(message.token);
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(26).string(message.game_uuid);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                $root.lq.ClientDeviceInfo.encode(message.device, writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAuthGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAuthGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAuthGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.account_id = value;
                        else
                            delete message.account_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.device = $root.lq.ClientDeviceInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.device);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAuthGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAuthGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                if (!$util.isInteger(message.account_id))
                    return "account_id: integer expected";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.device != null && message.hasOwnProperty("device")) {
                let error = $root.lq.ClientDeviceInfo.verify(message.device, _depth + 1);
                if (error)
                    return "device." + error;
            }
            return null;
        };

        ReqAuthGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAuthGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAuthGame();
            if (object.account_id != null)
                if (Number(object.account_id) !== 0)
                    message.account_id = object.account_id >>> 0;
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.device != null) {
                if (typeof object.device !== "object")
                    throw TypeError(".lq.ReqAuthGame.device: object expected");
                message.device = $root.lq.ClientDeviceInfo.fromObject(object.device, _depth + 1);
            }
            return message;
        };

        ReqAuthGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.account_id = 0;
                object.token = "";
                object.game_uuid = "";
                object.device = null;
            }
            if (message.account_id != null && message.hasOwnProperty("account_id"))
                object.account_id = message.account_id;
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.device != null && message.hasOwnProperty("device"))
                object.device = $root.lq.ClientDeviceInfo.toObject(message.device, options);
            return object;
        };

        ReqAuthGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAuthGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAuthGame";
        };

        return ReqAuthGame;
    })();

    lq.ResAuthGame = (function() {

        function ResAuthGame(properties) {
            this.players = [];
            this.seat_list = [];
            this.ready_id_list = [];
            this.robots = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResAuthGame.prototype.error = null;
        ResAuthGame.prototype.players = $util.emptyArray;
        ResAuthGame.prototype.seat_list = $util.emptyArray;
        ResAuthGame.prototype.is_game_start = false;
        ResAuthGame.prototype.game_config = null;
        ResAuthGame.prototype.ready_id_list = $util.emptyArray;
        ResAuthGame.prototype.robots = $util.emptyArray;

        ResAuthGame.create = function create(properties) {
            return new ResAuthGame(properties);
        };

        ResAuthGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.PlayerGameView.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.seat_list != null && message.seat_list.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.seat_list.length; ++i)
                    writer.uint32(message.seat_list[i]);
                writer.ldelim();
            }
            if (message.is_game_start != null && Object.hasOwnProperty.call(message, "is_game_start"))
                writer.uint32(32).bool(message.is_game_start);
            if (message.game_config != null && Object.hasOwnProperty.call(message, "game_config"))
                $root.lq.GameConfig.encode(message.game_config, writer.uint32(42).fork()).ldelim();
            if (message.ready_id_list != null && message.ready_id_list.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.ready_id_list.length; ++i)
                    writer.uint32(message.ready_id_list[i]);
                writer.ldelim();
            }
            if (message.robots != null && message.robots.length)
                for (let i = 0; i < message.robots.length; ++i)
                    $root.lq.PlayerGameView.encode(message.robots[i], writer.uint32(58).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResAuthGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResAuthGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResAuthGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.seat_list && message.seat_list.length))
                                message.seat_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.seat_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.seat_list && message.seat_list.length))
                            message.seat_list = [];
                        message.seat_list.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_game_start = value;
                        else
                            delete message.is_game_start;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.game_config = $root.lq.GameConfig.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_config);
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.ready_id_list && message.ready_id_list.length))
                                message.ready_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ready_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.ready_id_list && message.ready_id_list.length))
                            message.ready_id_list = [];
                        message.ready_id_list.push(reader.uint32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.robots && message.robots.length))
                            message.robots = [];
                        message.robots.push($root.lq.PlayerGameView.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResAuthGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResAuthGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.seat_list != null && message.hasOwnProperty("seat_list")) {
                if (!Array.isArray(message.seat_list))
                    return "seat_list: array expected";
                for (let i = 0; i < message.seat_list.length; ++i)
                    if (!$util.isInteger(message.seat_list[i]))
                        return "seat_list: integer[] expected";
            }
            if (message.is_game_start != null && message.hasOwnProperty("is_game_start"))
                if (typeof message.is_game_start !== "boolean")
                    return "is_game_start: boolean expected";
            if (message.game_config != null && message.hasOwnProperty("game_config")) {
                let error = $root.lq.GameConfig.verify(message.game_config, _depth + 1);
                if (error)
                    return "game_config." + error;
            }
            if (message.ready_id_list != null && message.hasOwnProperty("ready_id_list")) {
                if (!Array.isArray(message.ready_id_list))
                    return "ready_id_list: array expected";
                for (let i = 0; i < message.ready_id_list.length; ++i)
                    if (!$util.isInteger(message.ready_id_list[i]))
                        return "ready_id_list: integer[] expected";
            }
            if (message.robots != null && message.hasOwnProperty("robots")) {
                if (!Array.isArray(message.robots))
                    return "robots: array expected";
                for (let i = 0; i < message.robots.length; ++i) {
                    let error = $root.lq.PlayerGameView.verify(message.robots[i], _depth + 1);
                    if (error)
                        return "robots." + error;
                }
            }
            return null;
        };

        ResAuthGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResAuthGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResAuthGame();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResAuthGame.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.ResAuthGame.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.ResAuthGame.players: object expected");
                    message.players[i] = $root.lq.PlayerGameView.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.seat_list) {
                if (!Array.isArray(object.seat_list))
                    throw TypeError(".lq.ResAuthGame.seat_list: array expected");
                message.seat_list = Array(object.seat_list.length);
                for (let i = 0; i < object.seat_list.length; ++i)
                    message.seat_list[i] = object.seat_list[i] >>> 0;
            }
            if (object.is_game_start != null)
                if (object.is_game_start)
                    message.is_game_start = Boolean(object.is_game_start);
            if (object.game_config != null) {
                if (typeof object.game_config !== "object")
                    throw TypeError(".lq.ResAuthGame.game_config: object expected");
                message.game_config = $root.lq.GameConfig.fromObject(object.game_config, _depth + 1);
            }
            if (object.ready_id_list) {
                if (!Array.isArray(object.ready_id_list))
                    throw TypeError(".lq.ResAuthGame.ready_id_list: array expected");
                message.ready_id_list = Array(object.ready_id_list.length);
                for (let i = 0; i < object.ready_id_list.length; ++i)
                    message.ready_id_list[i] = object.ready_id_list[i] >>> 0;
            }
            if (object.robots) {
                if (!Array.isArray(object.robots))
                    throw TypeError(".lq.ResAuthGame.robots: array expected");
                message.robots = Array(object.robots.length);
                for (let i = 0; i < object.robots.length; ++i) {
                    if (typeof object.robots[i] !== "object")
                        throw TypeError(".lq.ResAuthGame.robots: object expected");
                    message.robots[i] = $root.lq.PlayerGameView.fromObject(object.robots[i], _depth + 1);
                }
            }
            return message;
        };

        ResAuthGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.seat_list = [];
                object.ready_id_list = [];
                object.robots = [];
            }
            if (options.defaults) {
                object.error = null;
                object.is_game_start = false;
                object.game_config = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.PlayerGameView.toObject(message.players[j], options);
            }
            if (message.seat_list && message.seat_list.length) {
                object.seat_list = Array(message.seat_list.length);
                for (let j = 0; j < message.seat_list.length; ++j)
                    object.seat_list[j] = message.seat_list[j];
            }
            if (message.is_game_start != null && message.hasOwnProperty("is_game_start"))
                object.is_game_start = message.is_game_start;
            if (message.game_config != null && message.hasOwnProperty("game_config"))
                object.game_config = $root.lq.GameConfig.toObject(message.game_config, options);
            if (message.ready_id_list && message.ready_id_list.length) {
                object.ready_id_list = Array(message.ready_id_list.length);
                for (let j = 0; j < message.ready_id_list.length; ++j)
                    object.ready_id_list[j] = message.ready_id_list[j];
            }
            if (message.robots && message.robots.length) {
                object.robots = Array(message.robots.length);
                for (let j = 0; j < message.robots.length; ++j)
                    object.robots[j] = $root.lq.PlayerGameView.toObject(message.robots[j], options);
            }
            return object;
        };

        ResAuthGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResAuthGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResAuthGame";
        };

        return ResAuthGame;
    })();

    lq.GameRestore = (function() {

        function GameRestore(properties) {
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameRestore.prototype.snapshot = null;
        GameRestore.prototype.actions = $util.emptyArray;
        GameRestore.prototype.passed_waiting_time = 0;
        GameRestore.prototype.game_state = 0;
        GameRestore.prototype.start_time = 0;
        GameRestore.prototype.last_pause_time_ms = 0;

        GameRestore.create = function create(properties) {
            return new GameRestore(properties);
        };

        GameRestore.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.snapshot != null && Object.hasOwnProperty.call(message, "snapshot"))
                $root.lq.GameSnapshot.encode(message.snapshot, writer.uint32(10).fork()).ldelim();
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.lq.ActionPrototype.encode(message.actions[i], writer.uint32(18).fork()).ldelim();
            if (message.passed_waiting_time != null && Object.hasOwnProperty.call(message, "passed_waiting_time"))
                writer.uint32(24).uint32(message.passed_waiting_time);
            if (message.game_state != null && Object.hasOwnProperty.call(message, "game_state"))
                writer.uint32(32).uint32(message.game_state);
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(40).uint32(message.start_time);
            if (message.last_pause_time_ms != null && Object.hasOwnProperty.call(message, "last_pause_time_ms"))
                writer.uint32(48).uint32(message.last_pause_time_ms);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameRestore.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameRestore.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameRestore(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.snapshot = $root.lq.GameSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1, message.snapshot);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.lq.ActionPrototype.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.passed_waiting_time = value;
                        else
                            delete message.passed_waiting_time;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_state = value;
                        else
                            delete message.game_state;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.last_pause_time_ms = value;
                        else
                            delete message.last_pause_time_ms;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameRestore.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameRestore.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.snapshot != null && message.hasOwnProperty("snapshot")) {
                let error = $root.lq.GameSnapshot.verify(message.snapshot, _depth + 1);
                if (error)
                    return "snapshot." + error;
            }
            if (message.actions != null && message.hasOwnProperty("actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.lq.ActionPrototype.verify(message.actions[i], _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.passed_waiting_time != null && message.hasOwnProperty("passed_waiting_time"))
                if (!$util.isInteger(message.passed_waiting_time))
                    return "passed_waiting_time: integer expected";
            if (message.game_state != null && message.hasOwnProperty("game_state"))
                if (!$util.isInteger(message.game_state))
                    return "game_state: integer expected";
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.last_pause_time_ms != null && message.hasOwnProperty("last_pause_time_ms"))
                if (!$util.isInteger(message.last_pause_time_ms))
                    return "last_pause_time_ms: integer expected";
            return null;
        };

        GameRestore.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameRestore)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameRestore();
            if (object.snapshot != null) {
                if (typeof object.snapshot !== "object")
                    throw TypeError(".lq.GameRestore.snapshot: object expected");
                message.snapshot = $root.lq.GameSnapshot.fromObject(object.snapshot, _depth + 1);
            }
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".lq.GameRestore.actions: array expected");
                message.actions = Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i) {
                    if (typeof object.actions[i] !== "object")
                        throw TypeError(".lq.GameRestore.actions: object expected");
                    message.actions[i] = $root.lq.ActionPrototype.fromObject(object.actions[i], _depth + 1);
                }
            }
            if (object.passed_waiting_time != null)
                if (Number(object.passed_waiting_time) !== 0)
                    message.passed_waiting_time = object.passed_waiting_time >>> 0;
            if (object.game_state != null)
                if (Number(object.game_state) !== 0)
                    message.game_state = object.game_state >>> 0;
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.last_pause_time_ms != null)
                if (Number(object.last_pause_time_ms) !== 0)
                    message.last_pause_time_ms = object.last_pause_time_ms >>> 0;
            return message;
        };

        GameRestore.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.actions = [];
            if (options.defaults) {
                object.snapshot = null;
                object.passed_waiting_time = 0;
                object.game_state = 0;
                object.start_time = 0;
                object.last_pause_time_ms = 0;
            }
            if (message.snapshot != null && message.hasOwnProperty("snapshot"))
                object.snapshot = $root.lq.GameSnapshot.toObject(message.snapshot, options);
            if (message.actions && message.actions.length) {
                object.actions = Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.lq.ActionPrototype.toObject(message.actions[j], options);
            }
            if (message.passed_waiting_time != null && message.hasOwnProperty("passed_waiting_time"))
                object.passed_waiting_time = message.passed_waiting_time;
            if (message.game_state != null && message.hasOwnProperty("game_state"))
                object.game_state = message.game_state;
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.last_pause_time_ms != null && message.hasOwnProperty("last_pause_time_ms"))
                object.last_pause_time_ms = message.last_pause_time_ms;
            return object;
        };

        GameRestore.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameRestore.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameRestore";
        };

        return GameRestore;
    })();

    lq.ResEnterGame = (function() {

        function ResEnterGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResEnterGame.prototype.error = null;
        ResEnterGame.prototype.is_end = false;
        ResEnterGame.prototype.step = 0;
        ResEnterGame.prototype.game_restore = null;

        ResEnterGame.create = function create(properties) {
            return new ResEnterGame(properties);
        };

        ResEnterGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(16).bool(message.is_end);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(24).uint32(message.step);
            if (message.game_restore != null && Object.hasOwnProperty.call(message, "game_restore"))
                $root.lq.GameRestore.encode(message.game_restore, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResEnterGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResEnterGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResEnterGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_restore = $root.lq.GameRestore.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_restore);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResEnterGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResEnterGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (typeof message.is_end !== "boolean")
                    return "is_end: boolean expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            if (message.game_restore != null && message.hasOwnProperty("game_restore")) {
                let error = $root.lq.GameRestore.verify(message.game_restore, _depth + 1);
                if (error)
                    return "game_restore." + error;
            }
            return null;
        };

        ResEnterGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResEnterGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResEnterGame();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResEnterGame.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.is_end != null)
                if (object.is_end)
                    message.is_end = Boolean(object.is_end);
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            if (object.game_restore != null) {
                if (typeof object.game_restore !== "object")
                    throw TypeError(".lq.ResEnterGame.game_restore: object expected");
                message.game_restore = $root.lq.GameRestore.fromObject(object.game_restore, _depth + 1);
            }
            return message;
        };

        ResEnterGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.is_end = false;
                object.step = 0;
                object.game_restore = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.game_restore != null && message.hasOwnProperty("game_restore"))
                object.game_restore = $root.lq.GameRestore.toObject(message.game_restore, options);
            return object;
        };

        ResEnterGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResEnterGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResEnterGame";
        };

        return ResEnterGame;
    })();

    lq.ReqSyncGame = (function() {

        function ReqSyncGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSyncGame.prototype.round_id = "";
        ReqSyncGame.prototype.step = 0;

        ReqSyncGame.create = function create(properties) {
            return new ReqSyncGame(properties);
        };

        ReqSyncGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.round_id != null && Object.hasOwnProperty.call(message, "round_id"))
                writer.uint32(10).string(message.round_id);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(16).uint32(message.step);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSyncGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSyncGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSyncGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.round_id = value;
                        else
                            delete message.round_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSyncGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSyncGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.round_id != null && message.hasOwnProperty("round_id"))
                if (!$util.isString(message.round_id))
                    return "round_id: string expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            return null;
        };

        ReqSyncGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSyncGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSyncGame();
            if (object.round_id != null)
                if (typeof object.round_id !== "string" || object.round_id.length)
                    message.round_id = String(object.round_id);
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            return message;
        };

        ReqSyncGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.round_id = "";
                object.step = 0;
            }
            if (message.round_id != null && message.hasOwnProperty("round_id"))
                object.round_id = message.round_id;
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            return object;
        };

        ReqSyncGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSyncGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSyncGame";
        };

        return ReqSyncGame;
    })();

    lq.ResSyncGame = (function() {

        function ResSyncGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResSyncGame.prototype.error = null;
        ResSyncGame.prototype.is_end = false;
        ResSyncGame.prototype.step = 0;
        ResSyncGame.prototype.game_restore = null;

        ResSyncGame.create = function create(properties) {
            return new ResSyncGame(properties);
        };

        ResSyncGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.is_end != null && Object.hasOwnProperty.call(message, "is_end"))
                writer.uint32(16).bool(message.is_end);
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(24).uint32(message.step);
            if (message.game_restore != null && Object.hasOwnProperty.call(message, "game_restore"))
                $root.lq.GameRestore.encode(message.game_restore, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResSyncGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResSyncGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResSyncGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_end = value;
                        else
                            delete message.is_end;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.game_restore = $root.lq.GameRestore.decode(reader, reader.uint32(), undefined, _depth + 1, message.game_restore);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResSyncGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResSyncGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                if (typeof message.is_end !== "boolean")
                    return "is_end: boolean expected";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            if (message.game_restore != null && message.hasOwnProperty("game_restore")) {
                let error = $root.lq.GameRestore.verify(message.game_restore, _depth + 1);
                if (error)
                    return "game_restore." + error;
            }
            return null;
        };

        ResSyncGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResSyncGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResSyncGame();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResSyncGame.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.is_end != null)
                if (object.is_end)
                    message.is_end = Boolean(object.is_end);
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            if (object.game_restore != null) {
                if (typeof object.game_restore !== "object")
                    throw TypeError(".lq.ResSyncGame.game_restore: object expected");
                message.game_restore = $root.lq.GameRestore.fromObject(object.game_restore, _depth + 1);
            }
            return message;
        };

        ResSyncGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.is_end = false;
                object.step = 0;
                object.game_restore = null;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.is_end != null && message.hasOwnProperty("is_end"))
                object.is_end = message.is_end;
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.game_restore != null && message.hasOwnProperty("game_restore"))
                object.game_restore = $root.lq.GameRestore.toObject(message.game_restore, options);
            return object;
        };

        ResSyncGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResSyncGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResSyncGame";
        };

        return ResSyncGame;
    })();

    lq.ReqSelfOperation = (function() {

        function ReqSelfOperation(properties) {
            this.change_tiles = [];
            this.tile_states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqSelfOperation.prototype.type = 0;
        ReqSelfOperation.prototype.index = 0;
        ReqSelfOperation.prototype.tile = "";
        ReqSelfOperation.prototype.cancel_operation = false;
        ReqSelfOperation.prototype.moqie = false;
        ReqSelfOperation.prototype.timeuse = 0;
        ReqSelfOperation.prototype.tile_state = 0;
        ReqSelfOperation.prototype.change_tiles = $util.emptyArray;
        ReqSelfOperation.prototype.tile_states = $util.emptyArray;
        ReqSelfOperation.prototype.gap_type = 0;

        ReqSelfOperation.create = function create(properties) {
            return new ReqSelfOperation(properties);
        };

        ReqSelfOperation.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(16).uint32(message.index);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(26).string(message.tile);
            if (message.cancel_operation != null && Object.hasOwnProperty.call(message, "cancel_operation"))
                writer.uint32(32).bool(message.cancel_operation);
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(40).bool(message.moqie);
            if (message.timeuse != null && Object.hasOwnProperty.call(message, "timeuse"))
                writer.uint32(48).uint32(message.timeuse);
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(56).int32(message.tile_state);
            if (message.change_tiles != null && message.change_tiles.length)
                for (let i = 0; i < message.change_tiles.length; ++i)
                    writer.uint32(66).string(message.change_tiles[i]);
            if (message.tile_states != null && message.tile_states.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.tile_states.length; ++i)
                    writer.int32(message.tile_states[i]);
                writer.ldelim();
            }
            if (message.gap_type != null && Object.hasOwnProperty.call(message, "gap_type"))
                writer.uint32(80).uint32(message.gap_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqSelfOperation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqSelfOperation.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqSelfOperation(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancel_operation = value;
                        else
                            delete message.cancel_operation;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timeuse = value;
                        else
                            delete message.timeuse;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.change_tiles && message.change_tiles.length))
                            message.change_tiles = [];
                        message.change_tiles.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.tile_states && message.tile_states.length))
                                message.tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_states.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_states && message.tile_states.length))
                            message.tile_states = [];
                        message.tile_states.push(reader.int32());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gap_type = value;
                        else
                            delete message.gap_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqSelfOperation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqSelfOperation.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                if (typeof message.cancel_operation !== "boolean")
                    return "cancel_operation: boolean expected";
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                if (!$util.isInteger(message.timeuse))
                    return "timeuse: integer expected";
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.change_tiles != null && message.hasOwnProperty("change_tiles")) {
                if (!Array.isArray(message.change_tiles))
                    return "change_tiles: array expected";
                for (let i = 0; i < message.change_tiles.length; ++i)
                    if (!$util.isString(message.change_tiles[i]))
                        return "change_tiles: string[] expected";
            }
            if (message.tile_states != null && message.hasOwnProperty("tile_states")) {
                if (!Array.isArray(message.tile_states))
                    return "tile_states: array expected";
                for (let i = 0; i < message.tile_states.length; ++i)
                    if (!$util.isInteger(message.tile_states[i]))
                        return "tile_states: integer[] expected";
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                if (!$util.isInteger(message.gap_type))
                    return "gap_type: integer expected";
            return null;
        };

        ReqSelfOperation.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqSelfOperation)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqSelfOperation();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.cancel_operation != null)
                if (object.cancel_operation)
                    message.cancel_operation = Boolean(object.cancel_operation);
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.timeuse != null)
                if (Number(object.timeuse) !== 0)
                    message.timeuse = object.timeuse >>> 0;
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state | 0;
            if (object.change_tiles) {
                if (!Array.isArray(object.change_tiles))
                    throw TypeError(".lq.ReqSelfOperation.change_tiles: array expected");
                message.change_tiles = Array(object.change_tiles.length);
                for (let i = 0; i < object.change_tiles.length; ++i)
                    message.change_tiles[i] = String(object.change_tiles[i]);
            }
            if (object.tile_states) {
                if (!Array.isArray(object.tile_states))
                    throw TypeError(".lq.ReqSelfOperation.tile_states: array expected");
                message.tile_states = Array(object.tile_states.length);
                for (let i = 0; i < object.tile_states.length; ++i)
                    message.tile_states[i] = object.tile_states[i] | 0;
            }
            if (object.gap_type != null)
                if (Number(object.gap_type) !== 0)
                    message.gap_type = object.gap_type >>> 0;
            return message;
        };

        ReqSelfOperation.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change_tiles = [];
                object.tile_states = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.index = 0;
                object.tile = "";
                object.cancel_operation = false;
                object.moqie = false;
                object.timeuse = 0;
                object.tile_state = 0;
                object.gap_type = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                object.cancel_operation = message.cancel_operation;
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                object.timeuse = message.timeuse;
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.change_tiles && message.change_tiles.length) {
                object.change_tiles = Array(message.change_tiles.length);
                for (let j = 0; j < message.change_tiles.length; ++j)
                    object.change_tiles[j] = message.change_tiles[j];
            }
            if (message.tile_states && message.tile_states.length) {
                object.tile_states = Array(message.tile_states.length);
                for (let j = 0; j < message.tile_states.length; ++j)
                    object.tile_states[j] = message.tile_states[j];
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                object.gap_type = message.gap_type;
            return object;
        };

        ReqSelfOperation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqSelfOperation.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqSelfOperation";
        };

        return ReqSelfOperation;
    })();

    lq.ReqChiPengGang = (function() {

        function ReqChiPengGang(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqChiPengGang.prototype.type = 0;
        ReqChiPengGang.prototype.index = 0;
        ReqChiPengGang.prototype.cancel_operation = false;
        ReqChiPengGang.prototype.timeuse = 0;

        ReqChiPengGang.create = function create(properties) {
            return new ReqChiPengGang(properties);
        };

        ReqChiPengGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(16).uint32(message.index);
            if (message.cancel_operation != null && Object.hasOwnProperty.call(message, "cancel_operation"))
                writer.uint32(24).bool(message.cancel_operation);
            if (message.timeuse != null && Object.hasOwnProperty.call(message, "timeuse"))
                writer.uint32(48).uint32(message.timeuse);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqChiPengGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqChiPengGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqChiPengGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancel_operation = value;
                        else
                            delete message.cancel_operation;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timeuse = value;
                        else
                            delete message.timeuse;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqChiPengGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqChiPengGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                if (typeof message.cancel_operation !== "boolean")
                    return "cancel_operation: boolean expected";
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                if (!$util.isInteger(message.timeuse))
                    return "timeuse: integer expected";
            return null;
        };

        ReqChiPengGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqChiPengGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqChiPengGang();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            if (object.cancel_operation != null)
                if (object.cancel_operation)
                    message.cancel_operation = Boolean(object.cancel_operation);
            if (object.timeuse != null)
                if (Number(object.timeuse) !== 0)
                    message.timeuse = object.timeuse >>> 0;
            return message;
        };

        ReqChiPengGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.index = 0;
                object.cancel_operation = false;
                object.timeuse = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                object.cancel_operation = message.cancel_operation;
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                object.timeuse = message.timeuse;
            return object;
        };

        ReqChiPengGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqChiPengGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqChiPengGang";
        };

        return ReqChiPengGang;
    })();

    lq.ReqBroadcastInGame = (function() {

        function ReqBroadcastInGame(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqBroadcastInGame.prototype.content = "";
        ReqBroadcastInGame.prototype.except_self = false;

        ReqBroadcastInGame.create = function create(properties) {
            return new ReqBroadcastInGame(properties);
        };

        ReqBroadcastInGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(10).string(message.content);
            if (message.except_self != null && Object.hasOwnProperty.call(message, "except_self"))
                writer.uint32(16).bool(message.except_self);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqBroadcastInGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqBroadcastInGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqBroadcastInGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.except_self = value;
                        else
                            delete message.except_self;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqBroadcastInGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqBroadcastInGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            if (message.except_self != null && message.hasOwnProperty("except_self"))
                if (typeof message.except_self !== "boolean")
                    return "except_self: boolean expected";
            return null;
        };

        ReqBroadcastInGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqBroadcastInGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqBroadcastInGame();
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            if (object.except_self != null)
                if (object.except_self)
                    message.except_self = Boolean(object.except_self);
            return message;
        };

        ReqBroadcastInGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.content = "";
                object.except_self = false;
            }
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            if (message.except_self != null && message.hasOwnProperty("except_self"))
                object.except_self = message.except_self;
            return object;
        };

        ReqBroadcastInGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqBroadcastInGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqBroadcastInGame";
        };

        return ReqBroadcastInGame;
    })();

    lq.ReqGMCommandInGaming = (function() {

        function ReqGMCommandInGaming(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqGMCommandInGaming.prototype.json_data = "";

        ReqGMCommandInGaming.create = function create(properties) {
            return new ReqGMCommandInGaming(properties);
        };

        ReqGMCommandInGaming.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.json_data != null && Object.hasOwnProperty.call(message, "json_data"))
                writer.uint32(10).string(message.json_data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqGMCommandInGaming.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqGMCommandInGaming.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqGMCommandInGaming(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.json_data = value;
                        else
                            delete message.json_data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqGMCommandInGaming.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqGMCommandInGaming.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.json_data != null && message.hasOwnProperty("json_data"))
                if (!$util.isString(message.json_data))
                    return "json_data: string expected";
            return null;
        };

        ReqGMCommandInGaming.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqGMCommandInGaming)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqGMCommandInGaming();
            if (object.json_data != null)
                if (typeof object.json_data !== "string" || object.json_data.length)
                    message.json_data = String(object.json_data);
            return message;
        };

        ReqGMCommandInGaming.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.json_data = "";
            if (message.json_data != null && message.hasOwnProperty("json_data"))
                object.json_data = message.json_data;
            return object;
        };

        ReqGMCommandInGaming.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqGMCommandInGaming.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqGMCommandInGaming";
        };

        return ReqGMCommandInGaming;
    })();

    lq.ResGamePlayerState = (function() {

        function ResGamePlayerState(properties) {
            this.state_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGamePlayerState.prototype.error = null;
        ResGamePlayerState.prototype.state_list = $util.emptyArray;

        ResGamePlayerState.create = function create(properties) {
            return new ResGamePlayerState(properties);
        };

        ResGamePlayerState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.state_list != null && message.state_list.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.state_list.length; ++i)
                    writer.int32(message.state_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGamePlayerState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGamePlayerState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGamePlayerState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.state_list && message.state_list.length))
                                message.state_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.state_list.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.state_list && message.state_list.length))
                            message.state_list = [];
                        message.state_list.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGamePlayerState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGamePlayerState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.state_list != null && message.hasOwnProperty("state_list")) {
                if (!Array.isArray(message.state_list))
                    return "state_list: array expected";
                for (let i = 0; i < message.state_list.length; ++i)
                    switch (message.state_list[i]) {
                    default:
                        return "state_list: enum value[] expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                        break;
                    }
            }
            return null;
        };

        ResGamePlayerState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGamePlayerState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGamePlayerState();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGamePlayerState.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.state_list) {
                if (!Array.isArray(object.state_list))
                    throw TypeError(".lq.ResGamePlayerState.state_list: array expected");
                message.state_list = Array(object.state_list.length);
                for (let i = 0; i < object.state_list.length; ++i)
                    switch (object.state_list[i]) {
                    default:
                        if (typeof object.state_list[i] === "number") {
                            message.state_list[i] = object.state_list[i];
                            break;
                        }
                    case "NULL":
                    case 0:
                        message.state_list[i] = 0;
                        break;
                    case "AUTH":
                    case 1:
                        message.state_list[i] = 1;
                        break;
                    case "SYNCING":
                    case 2:
                        message.state_list[i] = 2;
                        break;
                    case "READY":
                    case 3:
                        message.state_list[i] = 3;
                        break;
                    }
            }
            return message;
        };

        ResGamePlayerState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.state_list = [];
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.state_list && message.state_list.length) {
                object.state_list = Array(message.state_list.length);
                for (let j = 0; j < message.state_list.length; ++j)
                    object.state_list[j] = options.enums === String ? $root.lq.GamePlayerState[message.state_list[j]] === undefined ? message.state_list[j] : $root.lq.GamePlayerState[message.state_list[j]] : message.state_list[j];
            }
            return object;
        };

        ResGamePlayerState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGamePlayerState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGamePlayerState";
        };

        return ResGamePlayerState;
    })();

    lq.ReqVoteGameEnd = (function() {

        function ReqVoteGameEnd(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqVoteGameEnd.prototype.yes = false;

        ReqVoteGameEnd.create = function create(properties) {
            return new ReqVoteGameEnd(properties);
        };

        ReqVoteGameEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.yes != null && Object.hasOwnProperty.call(message, "yes"))
                writer.uint32(8).bool(message.yes);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqVoteGameEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqVoteGameEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqVoteGameEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yes = value;
                        else
                            delete message.yes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqVoteGameEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqVoteGameEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.yes != null && message.hasOwnProperty("yes"))
                if (typeof message.yes !== "boolean")
                    return "yes: boolean expected";
            return null;
        };

        ReqVoteGameEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqVoteGameEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqVoteGameEnd();
            if (object.yes != null)
                if (object.yes)
                    message.yes = Boolean(object.yes);
            return message;
        };

        ReqVoteGameEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.yes = false;
            if (message.yes != null && message.hasOwnProperty("yes"))
                object.yes = message.yes;
            return object;
        };

        ReqVoteGameEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqVoteGameEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqVoteGameEnd";
        };

        return ReqVoteGameEnd;
    })();

    lq.ResGameEndVote = (function() {

        function ResGameEndVote(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResGameEndVote.prototype.success = false;
        ResGameEndVote.prototype.vote_cd_end_time = 0;
        ResGameEndVote.prototype.error = null;

        ResGameEndVote.create = function create(properties) {
            return new ResGameEndVote(properties);
        };

        ResGameEndVote.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.success != null && Object.hasOwnProperty.call(message, "success"))
                writer.uint32(8).bool(message.success);
            if (message.vote_cd_end_time != null && Object.hasOwnProperty.call(message, "vote_cd_end_time"))
                writer.uint32(16).uint32(message.vote_cd_end_time);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResGameEndVote.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResGameEndVote.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResGameEndVote(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.vote_cd_end_time = value;
                        else
                            delete message.vote_cd_end_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResGameEndVote.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResGameEndVote.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.success != null && message.hasOwnProperty("success"))
                if (typeof message.success !== "boolean")
                    return "success: boolean expected";
            if (message.vote_cd_end_time != null && message.hasOwnProperty("vote_cd_end_time"))
                if (!$util.isInteger(message.vote_cd_end_time))
                    return "vote_cd_end_time: integer expected";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResGameEndVote.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResGameEndVote)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResGameEndVote();
            if (object.success != null)
                if (object.success)
                    message.success = Boolean(object.success);
            if (object.vote_cd_end_time != null)
                if (Number(object.vote_cd_end_time) !== 0)
                    message.vote_cd_end_time = object.vote_cd_end_time >>> 0;
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResGameEndVote.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResGameEndVote.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.success = false;
                object.vote_cd_end_time = 0;
                object.error = null;
            }
            if (message.success != null && message.hasOwnProperty("success"))
                object.success = message.success;
            if (message.vote_cd_end_time != null && message.hasOwnProperty("vote_cd_end_time"))
                object.vote_cd_end_time = message.vote_cd_end_time;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResGameEndVote.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResGameEndVote.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResGameEndVote";
        };

        return ResGameEndVote;
    })();

    lq.ReqAuthObserve = (function() {

        function ReqAuthObserve(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqAuthObserve.prototype.token = "";

        ReqAuthObserve.create = function create(properties) {
            return new ReqAuthObserve(properties);
        };

        ReqAuthObserve.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(10).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqAuthObserve.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqAuthObserve.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqAuthObserve(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqAuthObserve.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqAuthObserve.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.token != null && message.hasOwnProperty("token"))
                if (!$util.isString(message.token))
                    return "token: string expected";
            return null;
        };

        ReqAuthObserve.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqAuthObserve)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqAuthObserve();
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = String(object.token);
            return message;
        };

        ReqAuthObserve.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.token = "";
            if (message.token != null && message.hasOwnProperty("token"))
                object.token = message.token;
            return object;
        };

        ReqAuthObserve.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqAuthObserve.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqAuthObserve";
        };

        return ReqAuthObserve;
    })();

    lq.ResStartObserve = (function() {

        function ResStartObserve(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResStartObserve.prototype.head = null;
        ResStartObserve.prototype.passed = null;

        ResStartObserve.create = function create(properties) {
            return new ResStartObserve(properties);
        };

        ResStartObserve.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.head != null && Object.hasOwnProperty.call(message, "head"))
                $root.lq.GameLiveHead.encode(message.head, writer.uint32(10).fork()).ldelim();
            if (message.passed != null && Object.hasOwnProperty.call(message, "passed"))
                $root.lq.GameLiveSegment.encode(message.passed, writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResStartObserve.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResStartObserve.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResStartObserve(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.head = $root.lq.GameLiveHead.decode(reader, reader.uint32(), undefined, _depth + 1, message.head);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.passed = $root.lq.GameLiveSegment.decode(reader, reader.uint32(), undefined, _depth + 1, message.passed);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResStartObserve.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResStartObserve.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.head != null && message.hasOwnProperty("head")) {
                let error = $root.lq.GameLiveHead.verify(message.head, _depth + 1);
                if (error)
                    return "head." + error;
            }
            if (message.passed != null && message.hasOwnProperty("passed")) {
                let error = $root.lq.GameLiveSegment.verify(message.passed, _depth + 1);
                if (error)
                    return "passed." + error;
            }
            return null;
        };

        ResStartObserve.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResStartObserve)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResStartObserve();
            if (object.head != null) {
                if (typeof object.head !== "object")
                    throw TypeError(".lq.ResStartObserve.head: object expected");
                message.head = $root.lq.GameLiveHead.fromObject(object.head, _depth + 1);
            }
            if (object.passed != null) {
                if (typeof object.passed !== "object")
                    throw TypeError(".lq.ResStartObserve.passed: object expected");
                message.passed = $root.lq.GameLiveSegment.fromObject(object.passed, _depth + 1);
            }
            return message;
        };

        ResStartObserve.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.head = null;
                object.passed = null;
            }
            if (message.head != null && message.hasOwnProperty("head"))
                object.head = $root.lq.GameLiveHead.toObject(message.head, options);
            if (message.passed != null && message.hasOwnProperty("passed"))
                object.passed = $root.lq.GameLiveSegment.toObject(message.passed, options);
            return object;
        };

        ResStartObserve.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResStartObserve.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResStartObserve";
        };

        return ResStartObserve;
    })();

    lq.NotifyNewGame = (function() {

        function NotifyNewGame(properties) {
            this.player_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyNewGame.prototype.game_uuid = "";
        NotifyNewGame.prototype.player_list = $util.emptyArray;

        NotifyNewGame.create = function create(properties) {
            return new NotifyNewGame(properties);
        };

        NotifyNewGame.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.game_uuid != null && Object.hasOwnProperty.call(message, "game_uuid"))
                writer.uint32(10).string(message.game_uuid);
            if (message.player_list != null && message.player_list.length)
                for (let i = 0; i < message.player_list.length; ++i)
                    writer.uint32(18).string(message.player_list[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyNewGame.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyNewGame.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyNewGame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.game_uuid = value;
                        else
                            delete message.game_uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.player_list && message.player_list.length))
                            message.player_list = [];
                        message.player_list.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyNewGame.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyNewGame.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                if (!$util.isString(message.game_uuid))
                    return "game_uuid: string expected";
            if (message.player_list != null && message.hasOwnProperty("player_list")) {
                if (!Array.isArray(message.player_list))
                    return "player_list: array expected";
                for (let i = 0; i < message.player_list.length; ++i)
                    if (!$util.isString(message.player_list[i]))
                        return "player_list: string[] expected";
            }
            return null;
        };

        NotifyNewGame.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyNewGame)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyNewGame();
            if (object.game_uuid != null)
                if (typeof object.game_uuid !== "string" || object.game_uuid.length)
                    message.game_uuid = String(object.game_uuid);
            if (object.player_list) {
                if (!Array.isArray(object.player_list))
                    throw TypeError(".lq.NotifyNewGame.player_list: array expected");
                message.player_list = Array(object.player_list.length);
                for (let i = 0; i < object.player_list.length; ++i)
                    message.player_list[i] = String(object.player_list[i]);
            }
            return message;
        };

        NotifyNewGame.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.player_list = [];
            if (options.defaults)
                object.game_uuid = "";
            if (message.game_uuid != null && message.hasOwnProperty("game_uuid"))
                object.game_uuid = message.game_uuid;
            if (message.player_list && message.player_list.length) {
                object.player_list = Array(message.player_list.length);
                for (let j = 0; j < message.player_list.length; ++j)
                    object.player_list[j] = message.player_list[j];
            }
            return object;
        };

        NotifyNewGame.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyNewGame.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyNewGame";
        };

        return NotifyNewGame;
    })();

    lq.NotifyPlayerLoadGameReady = (function() {

        function NotifyPlayerLoadGameReady(properties) {
            this.ready_id_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyPlayerLoadGameReady.prototype.ready_id_list = $util.emptyArray;

        NotifyPlayerLoadGameReady.create = function create(properties) {
            return new NotifyPlayerLoadGameReady(properties);
        };

        NotifyPlayerLoadGameReady.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.ready_id_list != null && message.ready_id_list.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.ready_id_list.length; ++i)
                    writer.uint32(message.ready_id_list[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyPlayerLoadGameReady.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyPlayerLoadGameReady.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyPlayerLoadGameReady();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.ready_id_list && message.ready_id_list.length))
                                message.ready_id_list = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.ready_id_list.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.ready_id_list && message.ready_id_list.length))
                            message.ready_id_list = [];
                        message.ready_id_list.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyPlayerLoadGameReady.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyPlayerLoadGameReady.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.ready_id_list != null && message.hasOwnProperty("ready_id_list")) {
                if (!Array.isArray(message.ready_id_list))
                    return "ready_id_list: array expected";
                for (let i = 0; i < message.ready_id_list.length; ++i)
                    if (!$util.isInteger(message.ready_id_list[i]))
                        return "ready_id_list: integer[] expected";
            }
            return null;
        };

        NotifyPlayerLoadGameReady.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyPlayerLoadGameReady)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyPlayerLoadGameReady();
            if (object.ready_id_list) {
                if (!Array.isArray(object.ready_id_list))
                    throw TypeError(".lq.NotifyPlayerLoadGameReady.ready_id_list: array expected");
                message.ready_id_list = Array(object.ready_id_list.length);
                for (let i = 0; i < object.ready_id_list.length; ++i)
                    message.ready_id_list[i] = object.ready_id_list[i] >>> 0;
            }
            return message;
        };

        NotifyPlayerLoadGameReady.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.ready_id_list = [];
            if (message.ready_id_list && message.ready_id_list.length) {
                object.ready_id_list = Array(message.ready_id_list.length);
                for (let j = 0; j < message.ready_id_list.length; ++j)
                    object.ready_id_list[j] = message.ready_id_list[j];
            }
            return object;
        };

        NotifyPlayerLoadGameReady.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyPlayerLoadGameReady.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyPlayerLoadGameReady";
        };

        return NotifyPlayerLoadGameReady;
    })();

    lq.NotifyGameBroadcast = (function() {

        function NotifyGameBroadcast(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGameBroadcast.prototype.seat = 0;
        NotifyGameBroadcast.prototype.content = "";

        NotifyGameBroadcast.create = function create(properties) {
            return new NotifyGameBroadcast(properties);
        };

        NotifyGameBroadcast.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                writer.uint32(18).string(message.content);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGameBroadcast.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGameBroadcast.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameBroadcast(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGameBroadcast.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGameBroadcast.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.content != null && message.hasOwnProperty("content"))
                if (!$util.isString(message.content))
                    return "content: string expected";
            return null;
        };

        NotifyGameBroadcast.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGameBroadcast)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGameBroadcast();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.content != null)
                if (typeof object.content !== "string" || object.content.length)
                    message.content = String(object.content);
            return message;
        };

        NotifyGameBroadcast.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.content = "";
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.content != null && message.hasOwnProperty("content"))
                object.content = message.content;
            return object;
        };

        NotifyGameBroadcast.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGameBroadcast.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGameBroadcast";
        };

        return NotifyGameBroadcast;
    })();

    lq.NotifyGameEndResult = (function() {

        function NotifyGameEndResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGameEndResult.prototype.result = null;

        NotifyGameEndResult.create = function create(properties) {
            return new NotifyGameEndResult(properties);
        };

        NotifyGameEndResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                $root.lq.GameEndResult.encode(message.result, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGameEndResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGameEndResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameEndResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.result = $root.lq.GameEndResult.decode(reader, reader.uint32(), undefined, _depth + 1, message.result);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGameEndResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGameEndResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.result != null && message.hasOwnProperty("result")) {
                let error = $root.lq.GameEndResult.verify(message.result, _depth + 1);
                if (error)
                    return "result." + error;
            }
            return null;
        };

        NotifyGameEndResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGameEndResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGameEndResult();
            if (object.result != null) {
                if (typeof object.result !== "object")
                    throw TypeError(".lq.NotifyGameEndResult.result: object expected");
                message.result = $root.lq.GameEndResult.fromObject(object.result, _depth + 1);
            }
            return message;
        };

        NotifyGameEndResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.result = null;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = $root.lq.GameEndResult.toObject(message.result, options);
            return object;
        };

        NotifyGameEndResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGameEndResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGameEndResult";
        };

        return NotifyGameEndResult;
    })();

    lq.NotifyGameTerminate = (function() {

        function NotifyGameTerminate(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGameTerminate.prototype.reason = "";

        NotifyGameTerminate.create = function create(properties) {
            return new NotifyGameTerminate(properties);
        };

        NotifyGameTerminate.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(10).string(message.reason);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGameTerminate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGameTerminate.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameTerminate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.reason = value;
                        else
                            delete message.reason;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGameTerminate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGameTerminate.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.reason != null && message.hasOwnProperty("reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            return null;
        };

        NotifyGameTerminate.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGameTerminate)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGameTerminate();
            if (object.reason != null)
                if (typeof object.reason !== "string" || object.reason.length)
                    message.reason = String(object.reason);
            return message;
        };

        NotifyGameTerminate.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.reason = "";
            if (message.reason != null && message.hasOwnProperty("reason"))
                object.reason = message.reason;
            return object;
        };

        NotifyGameTerminate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGameTerminate.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGameTerminate";
        };

        return NotifyGameTerminate;
    })();

    lq.NotifyPlayerConnectionState = (function() {

        function NotifyPlayerConnectionState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyPlayerConnectionState.prototype.seat = 0;
        NotifyPlayerConnectionState.prototype.state = 0;

        NotifyPlayerConnectionState.create = function create(properties) {
            return new NotifyPlayerConnectionState(properties);
        };

        NotifyPlayerConnectionState.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(16).int32(message.state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyPlayerConnectionState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyPlayerConnectionState.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyPlayerConnectionState(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.state = value;
                        else
                            delete message.state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyPlayerConnectionState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyPlayerConnectionState.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.state != null && message.hasOwnProperty("state"))
                switch (message.state) {
                default:
                    return "state: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            return null;
        };

        NotifyPlayerConnectionState.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyPlayerConnectionState)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyPlayerConnectionState();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.state !== 0 && (typeof object.state !== "string" || $root.lq.GamePlayerState[object.state] !== 0))
                switch (object.state) {
                default:
                    if (typeof object.state === "number") {
                        message.state = object.state;
                        break;
                    }
                    break;
                case "NULL":
                case 0:
                    message.state = 0;
                    break;
                case "AUTH":
                case 1:
                    message.state = 1;
                    break;
                case "SYNCING":
                case 2:
                    message.state = 2;
                    break;
                case "READY":
                case 3:
                    message.state = 3;
                    break;
                }
            return message;
        };

        NotifyPlayerConnectionState.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.state = options.enums === String ? "NULL" : 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.state != null && message.hasOwnProperty("state"))
                object.state = options.enums === String ? $root.lq.GamePlayerState[message.state] === undefined ? message.state : $root.lq.GamePlayerState[message.state] : message.state;
            return object;
        };

        NotifyPlayerConnectionState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyPlayerConnectionState.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyPlayerConnectionState";
        };

        return NotifyPlayerConnectionState;
    })();

    lq.NotifyAccountLevelChange = (function() {

        function NotifyAccountLevelChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyAccountLevelChange.prototype.origin = null;
        NotifyAccountLevelChange.prototype.final = null;
        NotifyAccountLevelChange.prototype.type = 0;

        NotifyAccountLevelChange.create = function create(properties) {
            return new NotifyAccountLevelChange(properties);
        };

        NotifyAccountLevelChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                $root.lq.AccountLevel.encode(message.origin, writer.uint32(10).fork()).ldelim();
            if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                $root.lq.AccountLevel.encode(message.final, writer.uint32(18).fork()).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyAccountLevelChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyAccountLevelChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyAccountLevelChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.origin = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.origin);
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.final = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.final);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyAccountLevelChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyAccountLevelChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.origin != null && message.hasOwnProperty("origin")) {
                let error = $root.lq.AccountLevel.verify(message.origin, _depth + 1);
                if (error)
                    return "origin." + error;
            }
            if (message.final != null && message.hasOwnProperty("final")) {
                let error = $root.lq.AccountLevel.verify(message.final, _depth + 1);
                if (error)
                    return "final." + error;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        NotifyAccountLevelChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyAccountLevelChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyAccountLevelChange();
            if (object.origin != null) {
                if (typeof object.origin !== "object")
                    throw TypeError(".lq.NotifyAccountLevelChange.origin: object expected");
                message.origin = $root.lq.AccountLevel.fromObject(object.origin, _depth + 1);
            }
            if (object.final != null) {
                if (typeof object.final !== "object")
                    throw TypeError(".lq.NotifyAccountLevelChange.final: object expected");
                message.final = $root.lq.AccountLevel.fromObject(object.final, _depth + 1);
            }
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        NotifyAccountLevelChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.origin = null;
                object.final = null;
                object.type = 0;
            }
            if (message.origin != null && message.hasOwnProperty("origin"))
                object.origin = $root.lq.AccountLevel.toObject(message.origin, options);
            if (message.final != null && message.hasOwnProperty("final"))
                object.final = $root.lq.AccountLevel.toObject(message.final, options);
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        NotifyAccountLevelChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyAccountLevelChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyAccountLevelChange";
        };

        return NotifyAccountLevelChange;
    })();

    lq.NotifyGameFinishReward = (function() {

        function NotifyGameFinishReward(properties) {
            this.badges = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGameFinishReward.prototype.mode_id = 0;
        NotifyGameFinishReward.prototype.level_change = null;
        NotifyGameFinishReward.prototype.match_chest = null;
        NotifyGameFinishReward.prototype.main_character = null;
        NotifyGameFinishReward.prototype.character_gift = null;
        NotifyGameFinishReward.prototype.badges = $util.emptyArray;

        NotifyGameFinishReward.create = function create(properties) {
            return new NotifyGameFinishReward(properties);
        };

        NotifyGameFinishReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.mode_id != null && Object.hasOwnProperty.call(message, "mode_id"))
                writer.uint32(8).uint32(message.mode_id);
            if (message.level_change != null && Object.hasOwnProperty.call(message, "level_change"))
                $root.lq.NotifyGameFinishReward.LevelChange.encode(message.level_change, writer.uint32(18).fork()).ldelim();
            if (message.match_chest != null && Object.hasOwnProperty.call(message, "match_chest"))
                $root.lq.NotifyGameFinishReward.MatchChest.encode(message.match_chest, writer.uint32(26).fork()).ldelim();
            if (message.main_character != null && Object.hasOwnProperty.call(message, "main_character"))
                $root.lq.NotifyGameFinishReward.MainCharacter.encode(message.main_character, writer.uint32(34).fork()).ldelim();
            if (message.character_gift != null && Object.hasOwnProperty.call(message, "character_gift"))
                $root.lq.NotifyGameFinishReward.CharacterGift.encode(message.character_gift, writer.uint32(42).fork()).ldelim();
            if (message.badges != null && message.badges.length)
                for (let i = 0; i < message.badges.length; ++i)
                    $root.lq.BadgeAchieveProgress.encode(message.badges[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGameFinishReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGameFinishReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishReward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.mode_id = value;
                        else
                            delete message.mode_id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.level_change = $root.lq.NotifyGameFinishReward.LevelChange.decode(reader, reader.uint32(), undefined, _depth + 1, message.level_change);
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.match_chest = $root.lq.NotifyGameFinishReward.MatchChest.decode(reader, reader.uint32(), undefined, _depth + 1, message.match_chest);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.main_character = $root.lq.NotifyGameFinishReward.MainCharacter.decode(reader, reader.uint32(), undefined, _depth + 1, message.main_character);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.character_gift = $root.lq.NotifyGameFinishReward.CharacterGift.decode(reader, reader.uint32(), undefined, _depth + 1, message.character_gift);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.badges && message.badges.length))
                            message.badges = [];
                        message.badges.push($root.lq.BadgeAchieveProgress.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGameFinishReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGameFinishReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                if (!$util.isInteger(message.mode_id))
                    return "mode_id: integer expected";
            if (message.level_change != null && message.hasOwnProperty("level_change")) {
                let error = $root.lq.NotifyGameFinishReward.LevelChange.verify(message.level_change, _depth + 1);
                if (error)
                    return "level_change." + error;
            }
            if (message.match_chest != null && message.hasOwnProperty("match_chest")) {
                let error = $root.lq.NotifyGameFinishReward.MatchChest.verify(message.match_chest, _depth + 1);
                if (error)
                    return "match_chest." + error;
            }
            if (message.main_character != null && message.hasOwnProperty("main_character")) {
                let error = $root.lq.NotifyGameFinishReward.MainCharacter.verify(message.main_character, _depth + 1);
                if (error)
                    return "main_character." + error;
            }
            if (message.character_gift != null && message.hasOwnProperty("character_gift")) {
                let error = $root.lq.NotifyGameFinishReward.CharacterGift.verify(message.character_gift, _depth + 1);
                if (error)
                    return "character_gift." + error;
            }
            if (message.badges != null && message.hasOwnProperty("badges")) {
                if (!Array.isArray(message.badges))
                    return "badges: array expected";
                for (let i = 0; i < message.badges.length; ++i) {
                    let error = $root.lq.BadgeAchieveProgress.verify(message.badges[i], _depth + 1);
                    if (error)
                        return "badges." + error;
                }
            }
            return null;
        };

        NotifyGameFinishReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGameFinishReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGameFinishReward();
            if (object.mode_id != null)
                if (Number(object.mode_id) !== 0)
                    message.mode_id = object.mode_id >>> 0;
            if (object.level_change != null) {
                if (typeof object.level_change !== "object")
                    throw TypeError(".lq.NotifyGameFinishReward.level_change: object expected");
                message.level_change = $root.lq.NotifyGameFinishReward.LevelChange.fromObject(object.level_change, _depth + 1);
            }
            if (object.match_chest != null) {
                if (typeof object.match_chest !== "object")
                    throw TypeError(".lq.NotifyGameFinishReward.match_chest: object expected");
                message.match_chest = $root.lq.NotifyGameFinishReward.MatchChest.fromObject(object.match_chest, _depth + 1);
            }
            if (object.main_character != null) {
                if (typeof object.main_character !== "object")
                    throw TypeError(".lq.NotifyGameFinishReward.main_character: object expected");
                message.main_character = $root.lq.NotifyGameFinishReward.MainCharacter.fromObject(object.main_character, _depth + 1);
            }
            if (object.character_gift != null) {
                if (typeof object.character_gift !== "object")
                    throw TypeError(".lq.NotifyGameFinishReward.character_gift: object expected");
                message.character_gift = $root.lq.NotifyGameFinishReward.CharacterGift.fromObject(object.character_gift, _depth + 1);
            }
            if (object.badges) {
                if (!Array.isArray(object.badges))
                    throw TypeError(".lq.NotifyGameFinishReward.badges: array expected");
                message.badges = Array(object.badges.length);
                for (let i = 0; i < object.badges.length; ++i) {
                    if (typeof object.badges[i] !== "object")
                        throw TypeError(".lq.NotifyGameFinishReward.badges: object expected");
                    message.badges[i] = $root.lq.BadgeAchieveProgress.fromObject(object.badges[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyGameFinishReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.badges = [];
            if (options.defaults) {
                object.mode_id = 0;
                object.level_change = null;
                object.match_chest = null;
                object.main_character = null;
                object.character_gift = null;
            }
            if (message.mode_id != null && message.hasOwnProperty("mode_id"))
                object.mode_id = message.mode_id;
            if (message.level_change != null && message.hasOwnProperty("level_change"))
                object.level_change = $root.lq.NotifyGameFinishReward.LevelChange.toObject(message.level_change, options);
            if (message.match_chest != null && message.hasOwnProperty("match_chest"))
                object.match_chest = $root.lq.NotifyGameFinishReward.MatchChest.toObject(message.match_chest, options);
            if (message.main_character != null && message.hasOwnProperty("main_character"))
                object.main_character = $root.lq.NotifyGameFinishReward.MainCharacter.toObject(message.main_character, options);
            if (message.character_gift != null && message.hasOwnProperty("character_gift"))
                object.character_gift = $root.lq.NotifyGameFinishReward.CharacterGift.toObject(message.character_gift, options);
            if (message.badges && message.badges.length) {
                object.badges = Array(message.badges.length);
                for (let j = 0; j < message.badges.length; ++j)
                    object.badges[j] = $root.lq.BadgeAchieveProgress.toObject(message.badges[j], options);
            }
            return object;
        };

        NotifyGameFinishReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGameFinishReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGameFinishReward";
        };

        NotifyGameFinishReward.LevelChange = (function() {

            function LevelChange(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LevelChange.prototype.origin = null;
            LevelChange.prototype.final = null;
            LevelChange.prototype.type = 0;

            LevelChange.create = function create(properties) {
                return new LevelChange(properties);
            };

            LevelChange.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    $root.lq.AccountLevel.encode(message.origin, writer.uint32(10).fork()).ldelim();
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    $root.lq.AccountLevel.encode(message.final, writer.uint32(18).fork()).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(24).uint32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LevelChange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LevelChange.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishReward.LevelChange(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            message.origin = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.origin);
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            message.final = $root.lq.AccountLevel.decode(reader, reader.uint32(), undefined, _depth + 1, message.final);
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.type = value;
                            else
                                delete message.type;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LevelChange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LevelChange.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.origin != null && message.hasOwnProperty("origin")) {
                    let error = $root.lq.AccountLevel.verify(message.origin, _depth + 1);
                    if (error)
                        return "origin." + error;
                }
                if (message.final != null && message.hasOwnProperty("final")) {
                    let error = $root.lq.AccountLevel.verify(message.final, _depth + 1);
                    if (error)
                        return "final." + error;
                }
                if (message.type != null && message.hasOwnProperty("type"))
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                return null;
            };

            LevelChange.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishReward.LevelChange)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishReward.LevelChange();
                if (object.origin != null) {
                    if (typeof object.origin !== "object")
                        throw TypeError(".lq.NotifyGameFinishReward.LevelChange.origin: object expected");
                    message.origin = $root.lq.AccountLevel.fromObject(object.origin, _depth + 1);
                }
                if (object.final != null) {
                    if (typeof object.final !== "object")
                        throw TypeError(".lq.NotifyGameFinishReward.LevelChange.final: object expected");
                    message.final = $root.lq.AccountLevel.fromObject(object.final, _depth + 1);
                }
                if (object.type != null)
                    if (Number(object.type) !== 0)
                        message.type = object.type >>> 0;
                return message;
            };

            LevelChange.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.origin = null;
                    object.final = null;
                    object.type = 0;
                }
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = $root.lq.AccountLevel.toObject(message.origin, options);
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = $root.lq.AccountLevel.toObject(message.final, options);
                if (message.type != null && message.hasOwnProperty("type"))
                    object.type = message.type;
                return object;
            };

            LevelChange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LevelChange.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishReward.LevelChange";
            };

            return LevelChange;
        })();

        NotifyGameFinishReward.MatchChest = (function() {

            function MatchChest(properties) {
                this.rewards = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MatchChest.prototype.chest_id = 0;
            MatchChest.prototype.origin = 0;
            MatchChest.prototype.final = 0;
            MatchChest.prototype.is_graded = false;
            MatchChest.prototype.rewards = $util.emptyArray;

            MatchChest.create = function create(properties) {
                return new MatchChest(properties);
            };

            MatchChest.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.chest_id != null && Object.hasOwnProperty.call(message, "chest_id"))
                    writer.uint32(8).uint32(message.chest_id);
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    writer.uint32(16).uint32(message.origin);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(24).uint32(message.final);
                if (message.is_graded != null && Object.hasOwnProperty.call(message, "is_graded"))
                    writer.uint32(32).bool(message.is_graded);
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(42).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MatchChest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MatchChest.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishReward.MatchChest(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.chest_id = value;
                            else
                                delete message.chest_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.origin = value;
                            else
                                delete message.origin;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_graded = value;
                            else
                                delete message.is_graded;
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MatchChest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MatchChest.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                    if (!$util.isInteger(message.chest_id))
                        return "chest_id: integer expected";
                if (message.origin != null && message.hasOwnProperty("origin"))
                    if (!$util.isInteger(message.origin))
                        return "origin: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    if (typeof message.is_graded !== "boolean")
                        return "is_graded: boolean expected";
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                return null;
            };

            MatchChest.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishReward.MatchChest)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishReward.MatchChest();
                if (object.chest_id != null)
                    if (Number(object.chest_id) !== 0)
                        message.chest_id = object.chest_id >>> 0;
                if (object.origin != null)
                    if (Number(object.origin) !== 0)
                        message.origin = object.origin >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                if (object.is_graded != null)
                    if (object.is_graded)
                        message.is_graded = Boolean(object.is_graded);
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.NotifyGameFinishReward.MatchChest.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.NotifyGameFinishReward.MatchChest.rewards: object expected");
                        message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                return message;
            };

            MatchChest.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rewards = [];
                if (options.defaults) {
                    object.chest_id = 0;
                    object.origin = 0;
                    object.final = 0;
                    object.is_graded = false;
                }
                if (message.chest_id != null && message.hasOwnProperty("chest_id"))
                    object.chest_id = message.chest_id;
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = message.origin;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    object.is_graded = message.is_graded;
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
                }
                return object;
            };

            MatchChest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MatchChest.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishReward.MatchChest";
            };

            return MatchChest;
        })();

        NotifyGameFinishReward.MainCharacter = (function() {

            function MainCharacter(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            MainCharacter.prototype.level = 0;
            MainCharacter.prototype.exp = 0;
            MainCharacter.prototype.add = 0;

            MainCharacter.create = function create(properties) {
                return new MainCharacter(properties);
            };

            MainCharacter.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.level != null && Object.hasOwnProperty.call(message, "level"))
                    writer.uint32(8).uint32(message.level);
                if (message.exp != null && Object.hasOwnProperty.call(message, "exp"))
                    writer.uint32(16).uint32(message.exp);
                if (message.add != null && Object.hasOwnProperty.call(message, "add"))
                    writer.uint32(24).uint32(message.add);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            MainCharacter.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            MainCharacter.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishReward.MainCharacter(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.level = value;
                            else
                                delete message.level;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.exp = value;
                            else
                                delete message.exp;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.add = value;
                            else
                                delete message.add;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            MainCharacter.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            MainCharacter.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.level != null && message.hasOwnProperty("level"))
                    if (!$util.isInteger(message.level))
                        return "level: integer expected";
                if (message.exp != null && message.hasOwnProperty("exp"))
                    if (!$util.isInteger(message.exp))
                        return "exp: integer expected";
                if (message.add != null && message.hasOwnProperty("add"))
                    if (!$util.isInteger(message.add))
                        return "add: integer expected";
                return null;
            };

            MainCharacter.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishReward.MainCharacter)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishReward.MainCharacter();
                if (object.level != null)
                    if (Number(object.level) !== 0)
                        message.level = object.level >>> 0;
                if (object.exp != null)
                    if (Number(object.exp) !== 0)
                        message.exp = object.exp >>> 0;
                if (object.add != null)
                    if (Number(object.add) !== 0)
                        message.add = object.add >>> 0;
                return message;
            };

            MainCharacter.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.level = 0;
                    object.exp = 0;
                    object.add = 0;
                }
                if (message.level != null && message.hasOwnProperty("level"))
                    object.level = message.level;
                if (message.exp != null && message.hasOwnProperty("exp"))
                    object.exp = message.exp;
                if (message.add != null && message.hasOwnProperty("add"))
                    object.add = message.add;
                return object;
            };

            MainCharacter.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MainCharacter.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishReward.MainCharacter";
            };

            return MainCharacter;
        })();

        NotifyGameFinishReward.CharacterGift = (function() {

            function CharacterGift(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            CharacterGift.prototype.origin = 0;
            CharacterGift.prototype.final = 0;
            CharacterGift.prototype.add = 0;
            CharacterGift.prototype.is_graded = false;

            CharacterGift.create = function create(properties) {
                return new CharacterGift(properties);
            };

            CharacterGift.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.origin != null && Object.hasOwnProperty.call(message, "origin"))
                    writer.uint32(8).uint32(message.origin);
                if (message.final != null && Object.hasOwnProperty.call(message, "final"))
                    writer.uint32(16).uint32(message.final);
                if (message.add != null && Object.hasOwnProperty.call(message, "add"))
                    writer.uint32(24).uint32(message.add);
                if (message.is_graded != null && Object.hasOwnProperty.call(message, "is_graded"))
                    writer.uint32(32).bool(message.is_graded);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            CharacterGift.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            CharacterGift.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGameFinishReward.CharacterGift(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.origin = value;
                            else
                                delete message.origin;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.final = value;
                            else
                                delete message.final;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.add = value;
                            else
                                delete message.add;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.is_graded = value;
                            else
                                delete message.is_graded;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            CharacterGift.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            CharacterGift.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.origin != null && message.hasOwnProperty("origin"))
                    if (!$util.isInteger(message.origin))
                        return "origin: integer expected";
                if (message.final != null && message.hasOwnProperty("final"))
                    if (!$util.isInteger(message.final))
                        return "final: integer expected";
                if (message.add != null && message.hasOwnProperty("add"))
                    if (!$util.isInteger(message.add))
                        return "add: integer expected";
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    if (typeof message.is_graded !== "boolean")
                        return "is_graded: boolean expected";
                return null;
            };

            CharacterGift.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyGameFinishReward.CharacterGift)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyGameFinishReward.CharacterGift();
                if (object.origin != null)
                    if (Number(object.origin) !== 0)
                        message.origin = object.origin >>> 0;
                if (object.final != null)
                    if (Number(object.final) !== 0)
                        message.final = object.final >>> 0;
                if (object.add != null)
                    if (Number(object.add) !== 0)
                        message.add = object.add >>> 0;
                if (object.is_graded != null)
                    if (object.is_graded)
                        message.is_graded = Boolean(object.is_graded);
                return message;
            };

            CharacterGift.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.origin = 0;
                    object.final = 0;
                    object.add = 0;
                    object.is_graded = false;
                }
                if (message.origin != null && message.hasOwnProperty("origin"))
                    object.origin = message.origin;
                if (message.final != null && message.hasOwnProperty("final"))
                    object.final = message.final;
                if (message.add != null && message.hasOwnProperty("add"))
                    object.add = message.add;
                if (message.is_graded != null && message.hasOwnProperty("is_graded"))
                    object.is_graded = message.is_graded;
                return object;
            };

            CharacterGift.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CharacterGift.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyGameFinishReward.CharacterGift";
            };

            return CharacterGift;
        })();

        return NotifyGameFinishReward;
    })();

    lq.NotifyActivityReward = (function() {

        function NotifyActivityReward(properties) {
            this.activity_reward = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityReward.prototype.activity_reward = $util.emptyArray;

        NotifyActivityReward.create = function create(properties) {
            return new NotifyActivityReward(properties);
        };

        NotifyActivityReward.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_reward != null && message.activity_reward.length)
                for (let i = 0; i < message.activity_reward.length; ++i)
                    $root.lq.NotifyActivityReward.ActivityReward.encode(message.activity_reward[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityReward();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_reward && message.activity_reward.length))
                            message.activity_reward = [];
                        message.activity_reward.push($root.lq.NotifyActivityReward.ActivityReward.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityReward.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityReward.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_reward != null && message.hasOwnProperty("activity_reward")) {
                if (!Array.isArray(message.activity_reward))
                    return "activity_reward: array expected";
                for (let i = 0; i < message.activity_reward.length; ++i) {
                    let error = $root.lq.NotifyActivityReward.ActivityReward.verify(message.activity_reward[i], _depth + 1);
                    if (error)
                        return "activity_reward." + error;
                }
            }
            return null;
        };

        NotifyActivityReward.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityReward)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityReward();
            if (object.activity_reward) {
                if (!Array.isArray(object.activity_reward))
                    throw TypeError(".lq.NotifyActivityReward.activity_reward: array expected");
                message.activity_reward = Array(object.activity_reward.length);
                for (let i = 0; i < object.activity_reward.length; ++i) {
                    if (typeof object.activity_reward[i] !== "object")
                        throw TypeError(".lq.NotifyActivityReward.activity_reward: object expected");
                    message.activity_reward[i] = $root.lq.NotifyActivityReward.ActivityReward.fromObject(object.activity_reward[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityReward.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_reward = [];
            if (message.activity_reward && message.activity_reward.length) {
                object.activity_reward = Array(message.activity_reward.length);
                for (let j = 0; j < message.activity_reward.length; ++j)
                    object.activity_reward[j] = $root.lq.NotifyActivityReward.ActivityReward.toObject(message.activity_reward[j], options);
            }
            return object;
        };

        NotifyActivityReward.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityReward.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityReward";
        };

        NotifyActivityReward.ActivityReward = (function() {

            function ActivityReward(properties) {
                this.rewards = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityReward.prototype.activity_id = 0;
            ActivityReward.prototype.rewards = $util.emptyArray;

            ActivityReward.create = function create(properties) {
                return new ActivityReward(properties);
            };

            ActivityReward.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.rewards != null && message.rewards.length)
                    for (let i = 0; i < message.rewards.length; ++i)
                        $root.lq.RewardSlot.encode(message.rewards[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityReward.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityReward.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityReward.ActivityReward(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.rewards && message.rewards.length))
                                message.rewards = [];
                            message.rewards.push($root.lq.RewardSlot.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityReward.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityReward.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.rewards != null && message.hasOwnProperty("rewards")) {
                    if (!Array.isArray(message.rewards))
                        return "rewards: array expected";
                    for (let i = 0; i < message.rewards.length; ++i) {
                        let error = $root.lq.RewardSlot.verify(message.rewards[i], _depth + 1);
                        if (error)
                            return "rewards." + error;
                    }
                }
                return null;
            };

            ActivityReward.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyActivityReward.ActivityReward)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyActivityReward.ActivityReward();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.rewards) {
                    if (!Array.isArray(object.rewards))
                        throw TypeError(".lq.NotifyActivityReward.ActivityReward.rewards: array expected");
                    message.rewards = Array(object.rewards.length);
                    for (let i = 0; i < object.rewards.length; ++i) {
                        if (typeof object.rewards[i] !== "object")
                            throw TypeError(".lq.NotifyActivityReward.ActivityReward.rewards: object expected");
                        message.rewards[i] = $root.lq.RewardSlot.fromObject(object.rewards[i], _depth + 1);
                    }
                }
                return message;
            };

            ActivityReward.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.rewards = [];
                if (options.defaults)
                    object.activity_id = 0;
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.rewards && message.rewards.length) {
                    object.rewards = Array(message.rewards.length);
                    for (let j = 0; j < message.rewards.length; ++j)
                        object.rewards[j] = $root.lq.RewardSlot.toObject(message.rewards[j], options);
                }
                return object;
            };

            ActivityReward.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityReward.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyActivityReward.ActivityReward";
            };

            return ActivityReward;
        })();

        return NotifyActivityReward;
    })();

    lq.NotifyActivityPoint = (function() {

        function NotifyActivityPoint(properties) {
            this.activity_points = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyActivityPoint.prototype.activity_points = $util.emptyArray;

        NotifyActivityPoint.create = function create(properties) {
            return new NotifyActivityPoint(properties);
        };

        NotifyActivityPoint.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.activity_points != null && message.activity_points.length)
                for (let i = 0; i < message.activity_points.length; ++i)
                    $root.lq.NotifyActivityPoint.ActivityPoint.encode(message.activity_points[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyActivityPoint.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyActivityPoint.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityPoint();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.activity_points && message.activity_points.length))
                            message.activity_points = [];
                        message.activity_points.push($root.lq.NotifyActivityPoint.ActivityPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyActivityPoint.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyActivityPoint.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.activity_points != null && message.hasOwnProperty("activity_points")) {
                if (!Array.isArray(message.activity_points))
                    return "activity_points: array expected";
                for (let i = 0; i < message.activity_points.length; ++i) {
                    let error = $root.lq.NotifyActivityPoint.ActivityPoint.verify(message.activity_points[i], _depth + 1);
                    if (error)
                        return "activity_points." + error;
                }
            }
            return null;
        };

        NotifyActivityPoint.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyActivityPoint)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyActivityPoint();
            if (object.activity_points) {
                if (!Array.isArray(object.activity_points))
                    throw TypeError(".lq.NotifyActivityPoint.activity_points: array expected");
                message.activity_points = Array(object.activity_points.length);
                for (let i = 0; i < object.activity_points.length; ++i) {
                    if (typeof object.activity_points[i] !== "object")
                        throw TypeError(".lq.NotifyActivityPoint.activity_points: object expected");
                    message.activity_points[i] = $root.lq.NotifyActivityPoint.ActivityPoint.fromObject(object.activity_points[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyActivityPoint.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.activity_points = [];
            if (message.activity_points && message.activity_points.length) {
                object.activity_points = Array(message.activity_points.length);
                for (let j = 0; j < message.activity_points.length; ++j)
                    object.activity_points[j] = $root.lq.NotifyActivityPoint.ActivityPoint.toObject(message.activity_points[j], options);
            }
            return object;
        };

        NotifyActivityPoint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyActivityPoint.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyActivityPoint";
        };

        NotifyActivityPoint.ActivityPoint = (function() {

            function ActivityPoint(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ActivityPoint.prototype.activity_id = 0;
            ActivityPoint.prototype.point = 0;

            ActivityPoint.create = function create(properties) {
                return new ActivityPoint(properties);
            };

            ActivityPoint.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.activity_id != null && Object.hasOwnProperty.call(message, "activity_id"))
                    writer.uint32(8).uint32(message.activity_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ActivityPoint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ActivityPoint.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyActivityPoint.ActivityPoint(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.activity_id = value;
                            else
                                delete message.activity_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ActivityPoint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ActivityPoint.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    if (!$util.isInteger(message.activity_id))
                        return "activity_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            ActivityPoint.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyActivityPoint.ActivityPoint)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyActivityPoint.ActivityPoint();
                if (object.activity_id != null)
                    if (Number(object.activity_id) !== 0)
                        message.activity_id = object.activity_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            ActivityPoint.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.activity_id = 0;
                    object.point = 0;
                }
                if (message.activity_id != null && message.hasOwnProperty("activity_id"))
                    object.activity_id = message.activity_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            ActivityPoint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ActivityPoint.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyActivityPoint.ActivityPoint";
            };

            return ActivityPoint;
        })();

        return NotifyActivityPoint;
    })();

    lq.NotifyLeaderboardPoint = (function() {

        function NotifyLeaderboardPoint(properties) {
            this.leaderboard_points = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyLeaderboardPoint.prototype.leaderboard_points = $util.emptyArray;

        NotifyLeaderboardPoint.create = function create(properties) {
            return new NotifyLeaderboardPoint(properties);
        };

        NotifyLeaderboardPoint.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.leaderboard_points != null && message.leaderboard_points.length)
                for (let i = 0; i < message.leaderboard_points.length; ++i)
                    $root.lq.NotifyLeaderboardPoint.LeaderboardPoint.encode(message.leaderboard_points[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyLeaderboardPoint.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyLeaderboardPoint.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyLeaderboardPoint();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.leaderboard_points && message.leaderboard_points.length))
                            message.leaderboard_points = [];
                        message.leaderboard_points.push($root.lq.NotifyLeaderboardPoint.LeaderboardPoint.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyLeaderboardPoint.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyLeaderboardPoint.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.leaderboard_points != null && message.hasOwnProperty("leaderboard_points")) {
                if (!Array.isArray(message.leaderboard_points))
                    return "leaderboard_points: array expected";
                for (let i = 0; i < message.leaderboard_points.length; ++i) {
                    let error = $root.lq.NotifyLeaderboardPoint.LeaderboardPoint.verify(message.leaderboard_points[i], _depth + 1);
                    if (error)
                        return "leaderboard_points." + error;
                }
            }
            return null;
        };

        NotifyLeaderboardPoint.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyLeaderboardPoint)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyLeaderboardPoint();
            if (object.leaderboard_points) {
                if (!Array.isArray(object.leaderboard_points))
                    throw TypeError(".lq.NotifyLeaderboardPoint.leaderboard_points: array expected");
                message.leaderboard_points = Array(object.leaderboard_points.length);
                for (let i = 0; i < object.leaderboard_points.length; ++i) {
                    if (typeof object.leaderboard_points[i] !== "object")
                        throw TypeError(".lq.NotifyLeaderboardPoint.leaderboard_points: object expected");
                    message.leaderboard_points[i] = $root.lq.NotifyLeaderboardPoint.LeaderboardPoint.fromObject(object.leaderboard_points[i], _depth + 1);
                }
            }
            return message;
        };

        NotifyLeaderboardPoint.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.leaderboard_points = [];
            if (message.leaderboard_points && message.leaderboard_points.length) {
                object.leaderboard_points = Array(message.leaderboard_points.length);
                for (let j = 0; j < message.leaderboard_points.length; ++j)
                    object.leaderboard_points[j] = $root.lq.NotifyLeaderboardPoint.LeaderboardPoint.toObject(message.leaderboard_points[j], options);
            }
            return object;
        };

        NotifyLeaderboardPoint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyLeaderboardPoint.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyLeaderboardPoint";
        };

        NotifyLeaderboardPoint.LeaderboardPoint = (function() {

            function LeaderboardPoint(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            LeaderboardPoint.prototype.leaderboard_id = 0;
            LeaderboardPoint.prototype.point = 0;

            LeaderboardPoint.create = function create(properties) {
                return new LeaderboardPoint(properties);
            };

            LeaderboardPoint.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.leaderboard_id != null && Object.hasOwnProperty.call(message, "leaderboard_id"))
                    writer.uint32(8).uint32(message.leaderboard_id);
                if (message.point != null && Object.hasOwnProperty.call(message, "point"))
                    writer.uint32(16).uint32(message.point);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            LeaderboardPoint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            LeaderboardPoint.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyLeaderboardPoint.LeaderboardPoint(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.leaderboard_id = value;
                            else
                                delete message.leaderboard_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.point = value;
                            else
                                delete message.point;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            LeaderboardPoint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            LeaderboardPoint.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                    if (!$util.isInteger(message.leaderboard_id))
                        return "leaderboard_id: integer expected";
                if (message.point != null && message.hasOwnProperty("point"))
                    if (!$util.isInteger(message.point))
                        return "point: integer expected";
                return null;
            };

            LeaderboardPoint.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyLeaderboardPoint.LeaderboardPoint)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyLeaderboardPoint.LeaderboardPoint();
                if (object.leaderboard_id != null)
                    if (Number(object.leaderboard_id) !== 0)
                        message.leaderboard_id = object.leaderboard_id >>> 0;
                if (object.point != null)
                    if (Number(object.point) !== 0)
                        message.point = object.point >>> 0;
                return message;
            };

            LeaderboardPoint.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.leaderboard_id = 0;
                    object.point = 0;
                }
                if (message.leaderboard_id != null && message.hasOwnProperty("leaderboard_id"))
                    object.leaderboard_id = message.leaderboard_id;
                if (message.point != null && message.hasOwnProperty("point"))
                    object.point = message.point;
                return object;
            };

            LeaderboardPoint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            LeaderboardPoint.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyLeaderboardPoint.LeaderboardPoint";
            };

            return LeaderboardPoint;
        })();

        return NotifyLeaderboardPoint;
    })();

    lq.NotifyGamePause = (function() {

        function NotifyGamePause(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyGamePause.prototype.paused = false;

        NotifyGamePause.create = function create(properties) {
            return new NotifyGamePause(properties);
        };

        NotifyGamePause.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.paused != null && Object.hasOwnProperty.call(message, "paused"))
                writer.uint32(8).bool(message.paused);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyGamePause.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyGamePause.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyGamePause(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.paused = value;
                        else
                            delete message.paused;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyGamePause.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyGamePause.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.paused != null && message.hasOwnProperty("paused"))
                if (typeof message.paused !== "boolean")
                    return "paused: boolean expected";
            return null;
        };

        NotifyGamePause.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyGamePause)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyGamePause();
            if (object.paused != null)
                if (object.paused)
                    message.paused = Boolean(object.paused);
            return message;
        };

        NotifyGamePause.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.paused = false;
            if (message.paused != null && message.hasOwnProperty("paused"))
                object.paused = message.paused;
            return object;
        };

        NotifyGamePause.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyGamePause.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyGamePause";
        };

        return NotifyGamePause;
    })();

    lq.NotifyEndGameVote = (function() {

        function NotifyEndGameVote(properties) {
            this.results = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyEndGameVote.prototype.results = $util.emptyArray;
        NotifyEndGameVote.prototype.start_time = 0;
        NotifyEndGameVote.prototype.duration_time = 0;

        NotifyEndGameVote.create = function create(properties) {
            return new NotifyEndGameVote(properties);
        };

        NotifyEndGameVote.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.results != null && message.results.length)
                for (let i = 0; i < message.results.length; ++i)
                    $root.lq.NotifyEndGameVote.VoteResult.encode(message.results[i], writer.uint32(10).fork()).ldelim();
            if (message.start_time != null && Object.hasOwnProperty.call(message, "start_time"))
                writer.uint32(16).uint32(message.start_time);
            if (message.duration_time != null && Object.hasOwnProperty.call(message, "duration_time"))
                writer.uint32(24).uint32(message.duration_time);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyEndGameVote.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyEndGameVote.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyEndGameVote(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.results && message.results.length))
                            message.results = [];
                        message.results.push($root.lq.NotifyEndGameVote.VoteResult.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.start_time = value;
                        else
                            delete message.start_time;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.duration_time = value;
                        else
                            delete message.duration_time;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyEndGameVote.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyEndGameVote.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.results != null && message.hasOwnProperty("results")) {
                if (!Array.isArray(message.results))
                    return "results: array expected";
                for (let i = 0; i < message.results.length; ++i) {
                    let error = $root.lq.NotifyEndGameVote.VoteResult.verify(message.results[i], _depth + 1);
                    if (error)
                        return "results." + error;
                }
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                if (!$util.isInteger(message.start_time))
                    return "start_time: integer expected";
            if (message.duration_time != null && message.hasOwnProperty("duration_time"))
                if (!$util.isInteger(message.duration_time))
                    return "duration_time: integer expected";
            return null;
        };

        NotifyEndGameVote.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyEndGameVote)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyEndGameVote();
            if (object.results) {
                if (!Array.isArray(object.results))
                    throw TypeError(".lq.NotifyEndGameVote.results: array expected");
                message.results = Array(object.results.length);
                for (let i = 0; i < object.results.length; ++i) {
                    if (typeof object.results[i] !== "object")
                        throw TypeError(".lq.NotifyEndGameVote.results: object expected");
                    message.results[i] = $root.lq.NotifyEndGameVote.VoteResult.fromObject(object.results[i], _depth + 1);
                }
            }
            if (object.start_time != null)
                if (Number(object.start_time) !== 0)
                    message.start_time = object.start_time >>> 0;
            if (object.duration_time != null)
                if (Number(object.duration_time) !== 0)
                    message.duration_time = object.duration_time >>> 0;
            return message;
        };

        NotifyEndGameVote.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.results = [];
            if (options.defaults) {
                object.start_time = 0;
                object.duration_time = 0;
            }
            if (message.results && message.results.length) {
                object.results = Array(message.results.length);
                for (let j = 0; j < message.results.length; ++j)
                    object.results[j] = $root.lq.NotifyEndGameVote.VoteResult.toObject(message.results[j], options);
            }
            if (message.start_time != null && message.hasOwnProperty("start_time"))
                object.start_time = message.start_time;
            if (message.duration_time != null && message.hasOwnProperty("duration_time"))
                object.duration_time = message.duration_time;
            return object;
        };

        NotifyEndGameVote.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyEndGameVote.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyEndGameVote";
        };

        NotifyEndGameVote.VoteResult = (function() {

            function VoteResult(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            VoteResult.prototype.account_id = 0;
            VoteResult.prototype.yes = false;

            VoteResult.create = function create(properties) {
                return new VoteResult(properties);
            };

            VoteResult.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.account_id != null && Object.hasOwnProperty.call(message, "account_id"))
                    writer.uint32(8).uint32(message.account_id);
                if (message.yes != null && Object.hasOwnProperty.call(message, "yes"))
                    writer.uint32(16).bool(message.yes);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            VoteResult.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            VoteResult.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyEndGameVote.VoteResult(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.account_id = value;
                            else
                                delete message.account_id;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.bool())
                                message.yes = value;
                            else
                                delete message.yes;
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            VoteResult.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            VoteResult.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    if (!$util.isInteger(message.account_id))
                        return "account_id: integer expected";
                if (message.yes != null && message.hasOwnProperty("yes"))
                    if (typeof message.yes !== "boolean")
                        return "yes: boolean expected";
                return null;
            };

            VoteResult.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.NotifyEndGameVote.VoteResult)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.NotifyEndGameVote.VoteResult();
                if (object.account_id != null)
                    if (Number(object.account_id) !== 0)
                        message.account_id = object.account_id >>> 0;
                if (object.yes != null)
                    if (object.yes)
                        message.yes = Boolean(object.yes);
                return message;
            };

            VoteResult.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.defaults) {
                    object.account_id = 0;
                    object.yes = false;
                }
                if (message.account_id != null && message.hasOwnProperty("account_id"))
                    object.account_id = message.account_id;
                if (message.yes != null && message.hasOwnProperty("yes"))
                    object.yes = message.yes;
                return object;
            };

            VoteResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            VoteResult.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.NotifyEndGameVote.VoteResult";
            };

            return VoteResult;
        })();

        return NotifyEndGameVote;
    })();

    lq.NotifyObserveData = (function() {

        function NotifyObserveData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NotifyObserveData.prototype.unit = null;

        NotifyObserveData.create = function create(properties) {
            return new NotifyObserveData(properties);
        };

        NotifyObserveData.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.unit != null && Object.hasOwnProperty.call(message, "unit"))
                $root.lq.GameLiveUnit.encode(message.unit, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NotifyObserveData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NotifyObserveData.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NotifyObserveData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.unit = $root.lq.GameLiveUnit.decode(reader, reader.uint32(), undefined, _depth + 1, message.unit);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NotifyObserveData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NotifyObserveData.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.unit != null && message.hasOwnProperty("unit")) {
                let error = $root.lq.GameLiveUnit.verify(message.unit, _depth + 1);
                if (error)
                    return "unit." + error;
            }
            return null;
        };

        NotifyObserveData.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NotifyObserveData)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NotifyObserveData();
            if (object.unit != null) {
                if (typeof object.unit !== "object")
                    throw TypeError(".lq.NotifyObserveData.unit: object expected");
                message.unit = $root.lq.GameLiveUnit.fromObject(object.unit, _depth + 1);
            }
            return message;
        };

        NotifyObserveData.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.unit = null;
            if (message.unit != null && message.hasOwnProperty("unit"))
                object.unit = $root.lq.GameLiveUnit.toObject(message.unit, options);
            return object;
        };

        NotifyObserveData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotifyObserveData.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NotifyObserveData";
        };

        return NotifyObserveData;
    })();

    lq.ActionMJStart = (function() {

        function ActionMJStart(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionMJStart.create = function create(properties) {
            return new ActionMJStart(properties);
        };

        ActionMJStart.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionMJStart.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionMJStart.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionMJStart();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionMJStart.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionMJStart.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            return null;
        };

        ActionMJStart.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionMJStart)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            return new $root.lq.ActionMJStart();
        };

        ActionMJStart.toObject = function toObject() {
            return {};
        };

        ActionMJStart.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionMJStart.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionMJStart";
        };

        return ActionMJStart;
    })();

    lq.NewRoundOpenedTiles = (function() {

        function NewRoundOpenedTiles(properties) {
            this.tiles = [];
            this.count = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NewRoundOpenedTiles.prototype.seat = 0;
        NewRoundOpenedTiles.prototype.tiles = $util.emptyArray;
        NewRoundOpenedTiles.prototype.count = $util.emptyArray;

        NewRoundOpenedTiles.create = function create(properties) {
            return new NewRoundOpenedTiles(properties);
        };

        NewRoundOpenedTiles.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(18).string(message.tiles[i]);
            if (message.count != null && message.count.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.count.length; ++i)
                    writer.uint32(message.count[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NewRoundOpenedTiles.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NewRoundOpenedTiles.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NewRoundOpenedTiles(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.count && message.count.length))
                                message.count = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.count.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.count && message.count.length))
                            message.count = [];
                        message.count.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NewRoundOpenedTiles.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NewRoundOpenedTiles.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.count != null && message.hasOwnProperty("count")) {
                if (!Array.isArray(message.count))
                    return "count: array expected";
                for (let i = 0; i < message.count.length; ++i)
                    if (!$util.isInteger(message.count[i]))
                        return "count: integer[] expected";
            }
            return null;
        };

        NewRoundOpenedTiles.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NewRoundOpenedTiles)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NewRoundOpenedTiles();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.NewRoundOpenedTiles.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.count) {
                if (!Array.isArray(object.count))
                    throw TypeError(".lq.NewRoundOpenedTiles.count: array expected");
                message.count = Array(object.count.length);
                for (let i = 0; i < object.count.length; ++i)
                    message.count[i] = object.count[i] >>> 0;
            }
            return message;
        };

        NewRoundOpenedTiles.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.count = [];
            }
            if (options.defaults)
                object.seat = 0;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.count && message.count.length) {
                object.count = Array(message.count.length);
                for (let j = 0; j < message.count.length; ++j)
                    object.count[j] = message.count[j];
            }
            return object;
        };

        NewRoundOpenedTiles.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NewRoundOpenedTiles.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NewRoundOpenedTiles";
        };

        return NewRoundOpenedTiles;
    })();

    lq.MuyuInfo = (function() {

        function MuyuInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        MuyuInfo.prototype.seat = 0;
        MuyuInfo.prototype.count = 0;
        MuyuInfo.prototype.count_max = 0;
        MuyuInfo.prototype.id = 0;

        MuyuInfo.create = function create(properties) {
            return new MuyuInfo(properties);
        };

        MuyuInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(16).uint32(message.count);
            if (message.count_max != null && Object.hasOwnProperty.call(message, "count_max"))
                writer.uint32(24).uint32(message.count_max);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(32).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MuyuInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        MuyuInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.MuyuInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count_max = value;
                        else
                            delete message.count_max;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        MuyuInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        MuyuInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.count_max != null && message.hasOwnProperty("count_max"))
                if (!$util.isInteger(message.count_max))
                    return "count_max: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        MuyuInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.MuyuInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.MuyuInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.count_max != null)
                if (Number(object.count_max) !== 0)
                    message.count_max = object.count_max >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        MuyuInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.count = 0;
                object.count_max = 0;
                object.id = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.count_max != null && message.hasOwnProperty("count_max"))
                object.count_max = message.count_max;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        MuyuInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MuyuInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.MuyuInfo";
        };

        return MuyuInfo;
    })();

    lq.ChuanmaGang = (function() {

        function ChuanmaGang(properties) {
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ChuanmaGang.prototype.old_scores = $util.emptyArray;
        ChuanmaGang.prototype.delta_scores = $util.emptyArray;
        ChuanmaGang.prototype.scores = $util.emptyArray;
        ChuanmaGang.prototype.gameend = null;
        ChuanmaGang.prototype.hules_history = $util.emptyArray;

        ChuanmaGang.create = function create(properties) {
            return new ChuanmaGang(properties);
        };

        ChuanmaGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.scores != null && message.scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(34).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(42).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ChuanmaGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ChuanmaGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ChuanmaGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ChuanmaGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ChuanmaGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        ChuanmaGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ChuanmaGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ChuanmaGang();
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.ChuanmaGang.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.ChuanmaGang.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ChuanmaGang.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.ChuanmaGang.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.ChuanmaGang.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.ChuanmaGang.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        ChuanmaGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.hules_history = [];
            }
            if (options.defaults)
                object.gameend = null;
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        ChuanmaGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChuanmaGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ChuanmaGang";
        };

        return ChuanmaGang;
    })();

    lq.YongchangInfo = (function() {

        function YongchangInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        YongchangInfo.prototype.seat = 0;
        YongchangInfo.prototype.moqie_count = 0;
        YongchangInfo.prototype.moqie_bonus = 0;
        YongchangInfo.prototype.shouqie_count = 0;
        YongchangInfo.prototype.shouqie_bonus = 0;

        YongchangInfo.create = function create(properties) {
            return new YongchangInfo(properties);
        };

        YongchangInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.moqie_count != null && Object.hasOwnProperty.call(message, "moqie_count"))
                writer.uint32(16).uint32(message.moqie_count);
            if (message.moqie_bonus != null && Object.hasOwnProperty.call(message, "moqie_bonus"))
                writer.uint32(24).uint32(message.moqie_bonus);
            if (message.shouqie_count != null && Object.hasOwnProperty.call(message, "shouqie_count"))
                writer.uint32(32).uint32(message.shouqie_count);
            if (message.shouqie_bonus != null && Object.hasOwnProperty.call(message, "shouqie_bonus"))
                writer.uint32(40).uint32(message.shouqie_bonus);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        YongchangInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        YongchangInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.YongchangInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.moqie_count = value;
                        else
                            delete message.moqie_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.moqie_bonus = value;
                        else
                            delete message.moqie_bonus;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shouqie_count = value;
                        else
                            delete message.shouqie_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.shouqie_bonus = value;
                        else
                            delete message.shouqie_bonus;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        YongchangInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        YongchangInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.moqie_count != null && message.hasOwnProperty("moqie_count"))
                if (!$util.isInteger(message.moqie_count))
                    return "moqie_count: integer expected";
            if (message.moqie_bonus != null && message.hasOwnProperty("moqie_bonus"))
                if (!$util.isInteger(message.moqie_bonus))
                    return "moqie_bonus: integer expected";
            if (message.shouqie_count != null && message.hasOwnProperty("shouqie_count"))
                if (!$util.isInteger(message.shouqie_count))
                    return "shouqie_count: integer expected";
            if (message.shouqie_bonus != null && message.hasOwnProperty("shouqie_bonus"))
                if (!$util.isInteger(message.shouqie_bonus))
                    return "shouqie_bonus: integer expected";
            return null;
        };

        YongchangInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.YongchangInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.YongchangInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.moqie_count != null)
                if (Number(object.moqie_count) !== 0)
                    message.moqie_count = object.moqie_count >>> 0;
            if (object.moqie_bonus != null)
                if (Number(object.moqie_bonus) !== 0)
                    message.moqie_bonus = object.moqie_bonus >>> 0;
            if (object.shouqie_count != null)
                if (Number(object.shouqie_count) !== 0)
                    message.shouqie_count = object.shouqie_count >>> 0;
            if (object.shouqie_bonus != null)
                if (Number(object.shouqie_bonus) !== 0)
                    message.shouqie_bonus = object.shouqie_bonus >>> 0;
            return message;
        };

        YongchangInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.moqie_count = 0;
                object.moqie_bonus = 0;
                object.shouqie_count = 0;
                object.shouqie_bonus = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.moqie_count != null && message.hasOwnProperty("moqie_count"))
                object.moqie_count = message.moqie_count;
            if (message.moqie_bonus != null && message.hasOwnProperty("moqie_bonus"))
                object.moqie_bonus = message.moqie_bonus;
            if (message.shouqie_count != null && message.hasOwnProperty("shouqie_count"))
                object.shouqie_count = message.shouqie_count;
            if (message.shouqie_bonus != null && message.hasOwnProperty("shouqie_bonus"))
                object.shouqie_bonus = message.shouqie_bonus;
            return object;
        };

        YongchangInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        YongchangInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.YongchangInfo";
        };

        return YongchangInfo;
    })();

    lq.XiaKeShangInfo = (function() {

        function XiaKeShangInfo(properties) {
            this.score_coefficients = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        XiaKeShangInfo.prototype.score_coefficients = $util.emptyArray;

        XiaKeShangInfo.create = function create(properties) {
            return new XiaKeShangInfo(properties);
        };

        XiaKeShangInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.score_coefficients != null && message.score_coefficients.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.score_coefficients.length; ++i)
                    writer.uint32(message.score_coefficients[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        XiaKeShangInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        XiaKeShangInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.XiaKeShangInfo();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.score_coefficients && message.score_coefficients.length))
                                message.score_coefficients = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.score_coefficients.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.score_coefficients && message.score_coefficients.length))
                            message.score_coefficients = [];
                        message.score_coefficients.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        XiaKeShangInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        XiaKeShangInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.score_coefficients != null && message.hasOwnProperty("score_coefficients")) {
                if (!Array.isArray(message.score_coefficients))
                    return "score_coefficients: array expected";
                for (let i = 0; i < message.score_coefficients.length; ++i)
                    if (!$util.isInteger(message.score_coefficients[i]))
                        return "score_coefficients: integer[] expected";
            }
            return null;
        };

        XiaKeShangInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.XiaKeShangInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.XiaKeShangInfo();
            if (object.score_coefficients) {
                if (!Array.isArray(object.score_coefficients))
                    throw TypeError(".lq.XiaKeShangInfo.score_coefficients: array expected");
                message.score_coefficients = Array(object.score_coefficients.length);
                for (let i = 0; i < object.score_coefficients.length; ++i)
                    message.score_coefficients[i] = object.score_coefficients[i] >>> 0;
            }
            return message;
        };

        XiaKeShangInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.score_coefficients = [];
            if (message.score_coefficients && message.score_coefficients.length) {
                object.score_coefficients = Array(message.score_coefficients.length);
                for (let j = 0; j < message.score_coefficients.length; ++j)
                    object.score_coefficients[j] = message.score_coefficients[j];
            }
            return object;
        };

        XiaKeShangInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        XiaKeShangInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.XiaKeShangInfo";
        };

        return XiaKeShangInfo;
    })();

    lq.ActionNewCard = (function() {

        function ActionNewCard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionNewCard.prototype.field_spell = 0;

        ActionNewCard.create = function create(properties) {
            return new ActionNewCard(properties);
        };

        ActionNewCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.field_spell != null && Object.hasOwnProperty.call(message, "field_spell"))
                writer.uint32(8).uint32(message.field_spell);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionNewCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionNewCard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionNewCard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell = value;
                        else
                            delete message.field_spell;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionNewCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionNewCard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                if (!$util.isInteger(message.field_spell))
                    return "field_spell: integer expected";
            return null;
        };

        ActionNewCard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionNewCard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionNewCard();
            if (object.field_spell != null)
                if (Number(object.field_spell) !== 0)
                    message.field_spell = object.field_spell >>> 0;
            return message;
        };

        ActionNewCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.field_spell = 0;
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                object.field_spell = message.field_spell;
            return object;
        };

        ActionNewCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionNewCard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionNewCard";
        };

        return ActionNewCard;
    })();

    lq.RecordNewCard = (function() {

        function RecordNewCard(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordNewCard.prototype.field_spell = 0;

        RecordNewCard.create = function create(properties) {
            return new RecordNewCard(properties);
        };

        RecordNewCard.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.field_spell != null && Object.hasOwnProperty.call(message, "field_spell"))
                writer.uint32(8).uint32(message.field_spell);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordNewCard.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordNewCard.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNewCard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell = value;
                        else
                            delete message.field_spell;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordNewCard.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordNewCard.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                if (!$util.isInteger(message.field_spell))
                    return "field_spell: integer expected";
            return null;
        };

        RecordNewCard.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordNewCard)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordNewCard();
            if (object.field_spell != null)
                if (Number(object.field_spell) !== 0)
                    message.field_spell = object.field_spell >>> 0;
            return message;
        };

        RecordNewCard.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.field_spell = 0;
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                object.field_spell = message.field_spell;
            return object;
        };

        RecordNewCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordNewCard.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordNewCard";
        };

        return RecordNewCard;
    })();

    lq.ActionNewRound = (function() {

        function ActionNewRound(properties) {
            this.tiles = [];
            this.scores = [];
            this.tingpais0 = [];
            this.tingpais1 = [];
            this.doras = [];
            this.opens = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionNewRound.prototype.chang = 0;
        ActionNewRound.prototype.ju = 0;
        ActionNewRound.prototype.ben = 0;
        ActionNewRound.prototype.tiles = $util.emptyArray;
        ActionNewRound.prototype.dora = "";
        ActionNewRound.prototype.scores = $util.emptyArray;
        ActionNewRound.prototype.operation = null;
        ActionNewRound.prototype.liqibang = 0;
        ActionNewRound.prototype.tingpais0 = $util.emptyArray;
        ActionNewRound.prototype.tingpais1 = $util.emptyArray;
        ActionNewRound.prototype.al = false;
        ActionNewRound.prototype.md5 = "";
        ActionNewRound.prototype.left_tile_count = 0;
        ActionNewRound.prototype.doras = $util.emptyArray;
        ActionNewRound.prototype.opens = $util.emptyArray;
        ActionNewRound.prototype.muyu = null;
        ActionNewRound.prototype.ju_count = 0;
        ActionNewRound.prototype.field_spell = 0;
        ActionNewRound.prototype.sha256 = "";
        ActionNewRound.prototype.yongchang = null;
        ActionNewRound.prototype.saltSha256 = "";
        ActionNewRound.prototype.xia_ke_shang = null;

        ActionNewRound.create = function create(properties) {
            return new ActionNewRound(properties);
        };

        ActionNewRound.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(8).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(16).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(24).uint32(message.ben);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(34).string(message.tiles[i]);
            if (message.dora != null && Object.hasOwnProperty.call(message, "dora"))
                writer.uint32(42).string(message.dora);
            if (message.scores != null && message.scores.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(58).fork()).ldelim();
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(64).uint32(message.liqibang);
            if (message.tingpais0 != null && message.tingpais0.length)
                for (let i = 0; i < message.tingpais0.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais0[i], writer.uint32(74).fork()).ldelim();
            if (message.tingpais1 != null && message.tingpais1.length)
                for (let i = 0; i < message.tingpais1.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(82).fork()).ldelim();
            if (message.al != null && Object.hasOwnProperty.call(message, "al"))
                writer.uint32(88).bool(message.al);
            if (message.md5 != null && Object.hasOwnProperty.call(message, "md5"))
                writer.uint32(98).string(message.md5);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(104).uint32(message.left_tile_count);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(114).string(message.doras[i]);
            if (message.opens != null && message.opens.length)
                for (let i = 0; i < message.opens.length; ++i)
                    $root.lq.NewRoundOpenedTiles.encode(message.opens[i], writer.uint32(122).fork()).ldelim();
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(130).fork()).ldelim();
            if (message.ju_count != null && Object.hasOwnProperty.call(message, "ju_count"))
                writer.uint32(136).uint32(message.ju_count);
            if (message.field_spell != null && Object.hasOwnProperty.call(message, "field_spell"))
                writer.uint32(144).uint32(message.field_spell);
            if (message.sha256 != null && Object.hasOwnProperty.call(message, "sha256"))
                writer.uint32(154).string(message.sha256);
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(162).fork()).ldelim();
            if (message.saltSha256 != null && Object.hasOwnProperty.call(message, "saltSha256"))
                writer.uint32(170).string(message.saltSha256);
            if (message.xia_ke_shang != null && Object.hasOwnProperty.call(message, "xia_ke_shang"))
                $root.lq.XiaKeShangInfo.encode(message.xia_ke_shang, writer.uint32(210).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionNewRound.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionNewRound.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionNewRound(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.dora = value;
                        else
                            delete message.dora;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais0 && message.tingpais0.length))
                            message.tingpais0 = [];
                        message.tingpais0.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais1 && message.tingpais1.length))
                            message.tingpais1 = [];
                        message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.al = value;
                        else
                            delete message.al;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.md5 = value;
                        else
                            delete message.md5;
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        if (!(message.opens && message.opens.length))
                            message.opens = [];
                        message.opens.push($root.lq.NewRoundOpenedTiles.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju_count = value;
                        else
                            delete message.ju_count;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell = value;
                        else
                            delete message.field_spell;
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sha256 = value;
                        else
                            delete message.sha256;
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.saltSha256 = value;
                        else
                            delete message.saltSha256;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.xia_ke_shang = $root.lq.XiaKeShangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.xia_ke_shang);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionNewRound.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionNewRound.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.dora != null && message.hasOwnProperty("dora"))
                if (!$util.isString(message.dora))
                    return "dora: string expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.tingpais0 != null && message.hasOwnProperty("tingpais0")) {
                if (!Array.isArray(message.tingpais0))
                    return "tingpais0: array expected";
                for (let i = 0; i < message.tingpais0.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais0[i], _depth + 1);
                    if (error)
                        return "tingpais0." + error;
                }
            }
            if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                if (!Array.isArray(message.tingpais1))
                    return "tingpais1: array expected";
                for (let i = 0; i < message.tingpais1.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                    if (error)
                        return "tingpais1." + error;
                }
            }
            if (message.al != null && message.hasOwnProperty("al"))
                if (typeof message.al !== "boolean")
                    return "al: boolean expected";
            if (message.md5 != null && message.hasOwnProperty("md5"))
                if (!$util.isString(message.md5))
                    return "md5: string expected";
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.opens != null && message.hasOwnProperty("opens")) {
                if (!Array.isArray(message.opens))
                    return "opens: array expected";
                for (let i = 0; i < message.opens.length; ++i) {
                    let error = $root.lq.NewRoundOpenedTiles.verify(message.opens[i], _depth + 1);
                    if (error)
                        return "opens." + error;
                }
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.ju_count != null && message.hasOwnProperty("ju_count"))
                if (!$util.isInteger(message.ju_count))
                    return "ju_count: integer expected";
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                if (!$util.isInteger(message.field_spell))
                    return "field_spell: integer expected";
            if (message.sha256 != null && message.hasOwnProperty("sha256"))
                if (!$util.isString(message.sha256))
                    return "sha256: string expected";
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.saltSha256 != null && message.hasOwnProperty("saltSha256"))
                if (!$util.isString(message.saltSha256))
                    return "saltSha256: string expected";
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang")) {
                let error = $root.lq.XiaKeShangInfo.verify(message.xia_ke_shang, _depth + 1);
                if (error)
                    return "xia_ke_shang." + error;
            }
            return null;
        };

        ActionNewRound.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionNewRound)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionNewRound();
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.ActionNewRound.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.dora != null)
                if (typeof object.dora !== "string" || object.dora.length)
                    message.dora = String(object.dora);
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionNewRound.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionNewRound.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.tingpais0) {
                if (!Array.isArray(object.tingpais0))
                    throw TypeError(".lq.ActionNewRound.tingpais0: array expected");
                message.tingpais0 = Array(object.tingpais0.length);
                for (let i = 0; i < object.tingpais0.length; ++i) {
                    if (typeof object.tingpais0[i] !== "object")
                        throw TypeError(".lq.ActionNewRound.tingpais0: object expected");
                    message.tingpais0[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais0[i], _depth + 1);
                }
            }
            if (object.tingpais1) {
                if (!Array.isArray(object.tingpais1))
                    throw TypeError(".lq.ActionNewRound.tingpais1: array expected");
                message.tingpais1 = Array(object.tingpais1.length);
                for (let i = 0; i < object.tingpais1.length; ++i) {
                    if (typeof object.tingpais1[i] !== "object")
                        throw TypeError(".lq.ActionNewRound.tingpais1: object expected");
                    message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                }
            }
            if (object.al != null)
                if (object.al)
                    message.al = Boolean(object.al);
            if (object.md5 != null)
                if (typeof object.md5 !== "string" || object.md5.length)
                    message.md5 = String(object.md5);
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionNewRound.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.opens) {
                if (!Array.isArray(object.opens))
                    throw TypeError(".lq.ActionNewRound.opens: array expected");
                message.opens = Array(object.opens.length);
                for (let i = 0; i < object.opens.length; ++i) {
                    if (typeof object.opens[i] !== "object")
                        throw TypeError(".lq.ActionNewRound.opens: object expected");
                    message.opens[i] = $root.lq.NewRoundOpenedTiles.fromObject(object.opens[i], _depth + 1);
                }
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionNewRound.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.ju_count != null)
                if (Number(object.ju_count) !== 0)
                    message.ju_count = object.ju_count >>> 0;
            if (object.field_spell != null)
                if (Number(object.field_spell) !== 0)
                    message.field_spell = object.field_spell >>> 0;
            if (object.sha256 != null)
                if (typeof object.sha256 !== "string" || object.sha256.length)
                    message.sha256 = String(object.sha256);
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.ActionNewRound.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.saltSha256 != null)
                if (typeof object.saltSha256 !== "string" || object.saltSha256.length)
                    message.saltSha256 = String(object.saltSha256);
            if (object.xia_ke_shang != null) {
                if (typeof object.xia_ke_shang !== "object")
                    throw TypeError(".lq.ActionNewRound.xia_ke_shang: object expected");
                message.xia_ke_shang = $root.lq.XiaKeShangInfo.fromObject(object.xia_ke_shang, _depth + 1);
            }
            return message;
        };

        ActionNewRound.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.scores = [];
                object.tingpais0 = [];
                object.tingpais1 = [];
                object.doras = [];
                object.opens = [];
            }
            if (options.defaults) {
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
                object.dora = "";
                object.operation = null;
                object.liqibang = 0;
                object.al = false;
                object.md5 = "";
                object.left_tile_count = 0;
                object.muyu = null;
                object.ju_count = 0;
                object.field_spell = 0;
                object.sha256 = "";
                object.yongchang = null;
                object.saltSha256 = "";
                object.xia_ke_shang = null;
            }
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.dora != null && message.hasOwnProperty("dora"))
                object.dora = message.dora;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.tingpais0 && message.tingpais0.length) {
                object.tingpais0 = Array(message.tingpais0.length);
                for (let j = 0; j < message.tingpais0.length; ++j)
                    object.tingpais0[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais0[j], options);
            }
            if (message.tingpais1 && message.tingpais1.length) {
                object.tingpais1 = Array(message.tingpais1.length);
                for (let j = 0; j < message.tingpais1.length; ++j)
                    object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
            }
            if (message.al != null && message.hasOwnProperty("al"))
                object.al = message.al;
            if (message.md5 != null && message.hasOwnProperty("md5"))
                object.md5 = message.md5;
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.opens && message.opens.length) {
                object.opens = Array(message.opens.length);
                for (let j = 0; j < message.opens.length; ++j)
                    object.opens[j] = $root.lq.NewRoundOpenedTiles.toObject(message.opens[j], options);
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.ju_count != null && message.hasOwnProperty("ju_count"))
                object.ju_count = message.ju_count;
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                object.field_spell = message.field_spell;
            if (message.sha256 != null && message.hasOwnProperty("sha256"))
                object.sha256 = message.sha256;
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.saltSha256 != null && message.hasOwnProperty("saltSha256"))
                object.saltSha256 = message.saltSha256;
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang"))
                object.xia_ke_shang = $root.lq.XiaKeShangInfo.toObject(message.xia_ke_shang, options);
            return object;
        };

        ActionNewRound.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionNewRound.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionNewRound";
        };

        return ActionNewRound;
    })();

    lq.RecordNewRound = (function() {

        function RecordNewRound(properties) {
            this.scores = [];
            this.tiles0 = [];
            this.tiles1 = [];
            this.tiles2 = [];
            this.tiles3 = [];
            this.tingpai = [];
            this.doras = [];
            this.opens = [];
            this.operations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordNewRound.prototype.chang = 0;
        RecordNewRound.prototype.ju = 0;
        RecordNewRound.prototype.ben = 0;
        RecordNewRound.prototype.dora = "";
        RecordNewRound.prototype.scores = $util.emptyArray;
        RecordNewRound.prototype.liqibang = 0;
        RecordNewRound.prototype.tiles0 = $util.emptyArray;
        RecordNewRound.prototype.tiles1 = $util.emptyArray;
        RecordNewRound.prototype.tiles2 = $util.emptyArray;
        RecordNewRound.prototype.tiles3 = $util.emptyArray;
        RecordNewRound.prototype.tingpai = $util.emptyArray;
        RecordNewRound.prototype.operation = null;
        RecordNewRound.prototype.md5 = "";
        RecordNewRound.prototype.paishan = "";
        RecordNewRound.prototype.left_tile_count = 0;
        RecordNewRound.prototype.doras = $util.emptyArray;
        RecordNewRound.prototype.opens = $util.emptyArray;
        RecordNewRound.prototype.muyu = null;
        RecordNewRound.prototype.operations = $util.emptyArray;
        RecordNewRound.prototype.ju_count = 0;
        RecordNewRound.prototype.field_spell = 0;
        RecordNewRound.prototype.sha256 = "";
        RecordNewRound.prototype.yongchang = null;
        RecordNewRound.prototype.saltSha256 = "";
        RecordNewRound.prototype.salt = "";
        RecordNewRound.prototype.xia_ke_shang = null;

        RecordNewRound.create = function create(properties) {
            return new RecordNewRound(properties);
        };

        RecordNewRound.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(8).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(16).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(24).uint32(message.ben);
            if (message.dora != null && Object.hasOwnProperty.call(message, "dora"))
                writer.uint32(34).string(message.dora);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(48).uint32(message.liqibang);
            if (message.tiles0 != null && message.tiles0.length)
                for (let i = 0; i < message.tiles0.length; ++i)
                    writer.uint32(58).string(message.tiles0[i]);
            if (message.tiles1 != null && message.tiles1.length)
                for (let i = 0; i < message.tiles1.length; ++i)
                    writer.uint32(66).string(message.tiles1[i]);
            if (message.tiles2 != null && message.tiles2.length)
                for (let i = 0; i < message.tiles2.length; ++i)
                    writer.uint32(74).string(message.tiles2[i]);
            if (message.tiles3 != null && message.tiles3.length)
                for (let i = 0; i < message.tiles3.length; ++i)
                    writer.uint32(82).string(message.tiles3[i]);
            if (message.tingpai != null && message.tingpai.length)
                for (let i = 0; i < message.tingpai.length; ++i)
                    $root.lq.RecordNewRound.TingPai.encode(message.tingpai[i], writer.uint32(90).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(98).fork()).ldelim();
            if (message.md5 != null && Object.hasOwnProperty.call(message, "md5"))
                writer.uint32(106).string(message.md5);
            if (message.paishan != null && Object.hasOwnProperty.call(message, "paishan"))
                writer.uint32(114).string(message.paishan);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(120).uint32(message.left_tile_count);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(130).string(message.doras[i]);
            if (message.opens != null && message.opens.length)
                for (let i = 0; i < message.opens.length; ++i)
                    $root.lq.NewRoundOpenedTiles.encode(message.opens[i], writer.uint32(138).fork()).ldelim();
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(146).fork()).ldelim();
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(154).fork()).ldelim();
            if (message.ju_count != null && Object.hasOwnProperty.call(message, "ju_count"))
                writer.uint32(160).uint32(message.ju_count);
            if (message.field_spell != null && Object.hasOwnProperty.call(message, "field_spell"))
                writer.uint32(168).uint32(message.field_spell);
            if (message.sha256 != null && Object.hasOwnProperty.call(message, "sha256"))
                writer.uint32(178).string(message.sha256);
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(186).fork()).ldelim();
            if (message.saltSha256 != null && Object.hasOwnProperty.call(message, "saltSha256"))
                writer.uint32(194).string(message.saltSha256);
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                writer.uint32(202).string(message.salt);
            if (message.xia_ke_shang != null && Object.hasOwnProperty.call(message, "xia_ke_shang"))
                $root.lq.XiaKeShangInfo.encode(message.xia_ke_shang, writer.uint32(210).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordNewRound.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordNewRound.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNewRound(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.dora = value;
                        else
                            delete message.dora;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles0 && message.tiles0.length))
                            message.tiles0 = [];
                        message.tiles0.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles1 && message.tiles1.length))
                            message.tiles1 = [];
                        message.tiles1.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles2 && message.tiles2.length))
                            message.tiles2 = [];
                        message.tiles2.push(reader.string());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles3 && message.tiles3.length))
                            message.tiles3 = [];
                        message.tiles3.push(reader.string());
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpai && message.tingpai.length))
                            message.tingpai = [];
                        message.tingpai.push($root.lq.RecordNewRound.TingPai.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.md5 = value;
                        else
                            delete message.md5;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.paishan = value;
                        else
                            delete message.paishan;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if (!(message.opens && message.opens.length))
                            message.opens = [];
                        message.opens.push($root.lq.NewRoundOpenedTiles.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju_count = value;
                        else
                            delete message.ju_count;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.field_spell = value;
                        else
                            delete message.field_spell;
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.sha256 = value;
                        else
                            delete message.sha256;
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.saltSha256 = value;
                        else
                            delete message.saltSha256;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.salt = value;
                        else
                            delete message.salt;
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.xia_ke_shang = $root.lq.XiaKeShangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.xia_ke_shang);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordNewRound.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordNewRound.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.dora != null && message.hasOwnProperty("dora"))
                if (!$util.isString(message.dora))
                    return "dora: string expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.tiles0 != null && message.hasOwnProperty("tiles0")) {
                if (!Array.isArray(message.tiles0))
                    return "tiles0: array expected";
                for (let i = 0; i < message.tiles0.length; ++i)
                    if (!$util.isString(message.tiles0[i]))
                        return "tiles0: string[] expected";
            }
            if (message.tiles1 != null && message.hasOwnProperty("tiles1")) {
                if (!Array.isArray(message.tiles1))
                    return "tiles1: array expected";
                for (let i = 0; i < message.tiles1.length; ++i)
                    if (!$util.isString(message.tiles1[i]))
                        return "tiles1: string[] expected";
            }
            if (message.tiles2 != null && message.hasOwnProperty("tiles2")) {
                if (!Array.isArray(message.tiles2))
                    return "tiles2: array expected";
                for (let i = 0; i < message.tiles2.length; ++i)
                    if (!$util.isString(message.tiles2[i]))
                        return "tiles2: string[] expected";
            }
            if (message.tiles3 != null && message.hasOwnProperty("tiles3")) {
                if (!Array.isArray(message.tiles3))
                    return "tiles3: array expected";
                for (let i = 0; i < message.tiles3.length; ++i)
                    if (!$util.isString(message.tiles3[i]))
                        return "tiles3: string[] expected";
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai")) {
                if (!Array.isArray(message.tingpai))
                    return "tingpai: array expected";
                for (let i = 0; i < message.tingpai.length; ++i) {
                    let error = $root.lq.RecordNewRound.TingPai.verify(message.tingpai[i], _depth + 1);
                    if (error)
                        return "tingpai." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.md5 != null && message.hasOwnProperty("md5"))
                if (!$util.isString(message.md5))
                    return "md5: string expected";
            if (message.paishan != null && message.hasOwnProperty("paishan"))
                if (!$util.isString(message.paishan))
                    return "paishan: string expected";
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.opens != null && message.hasOwnProperty("opens")) {
                if (!Array.isArray(message.opens))
                    return "opens: array expected";
                for (let i = 0; i < message.opens.length; ++i) {
                    let error = $root.lq.NewRoundOpenedTiles.verify(message.opens[i], _depth + 1);
                    if (error)
                        return "opens." + error;
                }
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.ju_count != null && message.hasOwnProperty("ju_count"))
                if (!$util.isInteger(message.ju_count))
                    return "ju_count: integer expected";
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                if (!$util.isInteger(message.field_spell))
                    return "field_spell: integer expected";
            if (message.sha256 != null && message.hasOwnProperty("sha256"))
                if (!$util.isString(message.sha256))
                    return "sha256: string expected";
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.saltSha256 != null && message.hasOwnProperty("saltSha256"))
                if (!$util.isString(message.saltSha256))
                    return "saltSha256: string expected";
            if (message.salt != null && message.hasOwnProperty("salt"))
                if (!$util.isString(message.salt))
                    return "salt: string expected";
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang")) {
                let error = $root.lq.XiaKeShangInfo.verify(message.xia_ke_shang, _depth + 1);
                if (error)
                    return "xia_ke_shang." + error;
            }
            return null;
        };

        RecordNewRound.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordNewRound)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordNewRound();
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.dora != null)
                if (typeof object.dora !== "string" || object.dora.length)
                    message.dora = String(object.dora);
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordNewRound.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.tiles0) {
                if (!Array.isArray(object.tiles0))
                    throw TypeError(".lq.RecordNewRound.tiles0: array expected");
                message.tiles0 = Array(object.tiles0.length);
                for (let i = 0; i < object.tiles0.length; ++i)
                    message.tiles0[i] = String(object.tiles0[i]);
            }
            if (object.tiles1) {
                if (!Array.isArray(object.tiles1))
                    throw TypeError(".lq.RecordNewRound.tiles1: array expected");
                message.tiles1 = Array(object.tiles1.length);
                for (let i = 0; i < object.tiles1.length; ++i)
                    message.tiles1[i] = String(object.tiles1[i]);
            }
            if (object.tiles2) {
                if (!Array.isArray(object.tiles2))
                    throw TypeError(".lq.RecordNewRound.tiles2: array expected");
                message.tiles2 = Array(object.tiles2.length);
                for (let i = 0; i < object.tiles2.length; ++i)
                    message.tiles2[i] = String(object.tiles2[i]);
            }
            if (object.tiles3) {
                if (!Array.isArray(object.tiles3))
                    throw TypeError(".lq.RecordNewRound.tiles3: array expected");
                message.tiles3 = Array(object.tiles3.length);
                for (let i = 0; i < object.tiles3.length; ++i)
                    message.tiles3[i] = String(object.tiles3[i]);
            }
            if (object.tingpai) {
                if (!Array.isArray(object.tingpai))
                    throw TypeError(".lq.RecordNewRound.tingpai: array expected");
                message.tingpai = Array(object.tingpai.length);
                for (let i = 0; i < object.tingpai.length; ++i) {
                    if (typeof object.tingpai[i] !== "object")
                        throw TypeError(".lq.RecordNewRound.tingpai: object expected");
                    message.tingpai[i] = $root.lq.RecordNewRound.TingPai.fromObject(object.tingpai[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordNewRound.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.md5 != null)
                if (typeof object.md5 !== "string" || object.md5.length)
                    message.md5 = String(object.md5);
            if (object.paishan != null)
                if (typeof object.paishan !== "string" || object.paishan.length)
                    message.paishan = String(object.paishan);
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordNewRound.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.opens) {
                if (!Array.isArray(object.opens))
                    throw TypeError(".lq.RecordNewRound.opens: array expected");
                message.opens = Array(object.opens.length);
                for (let i = 0; i < object.opens.length; ++i) {
                    if (typeof object.opens[i] !== "object")
                        throw TypeError(".lq.RecordNewRound.opens: object expected");
                    message.opens[i] = $root.lq.NewRoundOpenedTiles.fromObject(object.opens[i], _depth + 1);
                }
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordNewRound.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordNewRound.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordNewRound.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            if (object.ju_count != null)
                if (Number(object.ju_count) !== 0)
                    message.ju_count = object.ju_count >>> 0;
            if (object.field_spell != null)
                if (Number(object.field_spell) !== 0)
                    message.field_spell = object.field_spell >>> 0;
            if (object.sha256 != null)
                if (typeof object.sha256 !== "string" || object.sha256.length)
                    message.sha256 = String(object.sha256);
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.RecordNewRound.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.saltSha256 != null)
                if (typeof object.saltSha256 !== "string" || object.saltSha256.length)
                    message.saltSha256 = String(object.saltSha256);
            if (object.salt != null)
                if (typeof object.salt !== "string" || object.salt.length)
                    message.salt = String(object.salt);
            if (object.xia_ke_shang != null) {
                if (typeof object.xia_ke_shang !== "object")
                    throw TypeError(".lq.RecordNewRound.xia_ke_shang: object expected");
                message.xia_ke_shang = $root.lq.XiaKeShangInfo.fromObject(object.xia_ke_shang, _depth + 1);
            }
            return message;
        };

        RecordNewRound.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.tiles0 = [];
                object.tiles1 = [];
                object.tiles2 = [];
                object.tiles3 = [];
                object.tingpai = [];
                object.doras = [];
                object.opens = [];
                object.operations = [];
            }
            if (options.defaults) {
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
                object.dora = "";
                object.liqibang = 0;
                object.operation = null;
                object.md5 = "";
                object.paishan = "";
                object.left_tile_count = 0;
                object.muyu = null;
                object.ju_count = 0;
                object.field_spell = 0;
                object.sha256 = "";
                object.yongchang = null;
                object.saltSha256 = "";
                object.salt = "";
                object.xia_ke_shang = null;
            }
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.dora != null && message.hasOwnProperty("dora"))
                object.dora = message.dora;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.tiles0 && message.tiles0.length) {
                object.tiles0 = Array(message.tiles0.length);
                for (let j = 0; j < message.tiles0.length; ++j)
                    object.tiles0[j] = message.tiles0[j];
            }
            if (message.tiles1 && message.tiles1.length) {
                object.tiles1 = Array(message.tiles1.length);
                for (let j = 0; j < message.tiles1.length; ++j)
                    object.tiles1[j] = message.tiles1[j];
            }
            if (message.tiles2 && message.tiles2.length) {
                object.tiles2 = Array(message.tiles2.length);
                for (let j = 0; j < message.tiles2.length; ++j)
                    object.tiles2[j] = message.tiles2[j];
            }
            if (message.tiles3 && message.tiles3.length) {
                object.tiles3 = Array(message.tiles3.length);
                for (let j = 0; j < message.tiles3.length; ++j)
                    object.tiles3[j] = message.tiles3[j];
            }
            if (message.tingpai && message.tingpai.length) {
                object.tingpai = Array(message.tingpai.length);
                for (let j = 0; j < message.tingpai.length; ++j)
                    object.tingpai[j] = $root.lq.RecordNewRound.TingPai.toObject(message.tingpai[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.md5 != null && message.hasOwnProperty("md5"))
                object.md5 = message.md5;
            if (message.paishan != null && message.hasOwnProperty("paishan"))
                object.paishan = message.paishan;
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.opens && message.opens.length) {
                object.opens = Array(message.opens.length);
                for (let j = 0; j < message.opens.length; ++j)
                    object.opens[j] = $root.lq.NewRoundOpenedTiles.toObject(message.opens[j], options);
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            if (message.ju_count != null && message.hasOwnProperty("ju_count"))
                object.ju_count = message.ju_count;
            if (message.field_spell != null && message.hasOwnProperty("field_spell"))
                object.field_spell = message.field_spell;
            if (message.sha256 != null && message.hasOwnProperty("sha256"))
                object.sha256 = message.sha256;
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.saltSha256 != null && message.hasOwnProperty("saltSha256"))
                object.saltSha256 = message.saltSha256;
            if (message.salt != null && message.hasOwnProperty("salt"))
                object.salt = message.salt;
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang"))
                object.xia_ke_shang = $root.lq.XiaKeShangInfo.toObject(message.xia_ke_shang, options);
            return object;
        };

        RecordNewRound.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordNewRound.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordNewRound";
        };

        RecordNewRound.TingPai = (function() {

            function TingPai(properties) {
                this.tingpais1 = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TingPai.prototype.seat = 0;
            TingPai.prototype.tingpais1 = $util.emptyArray;

            TingPai.create = function create(properties) {
                return new TingPai(properties);
            };

            TingPai.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.tingpais1 != null && message.tingpais1.length)
                    for (let i = 0; i < message.tingpais1.length; ++i)
                        $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TingPai.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TingPai.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNewRound.TingPai(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tingpais1 && message.tingpais1.length))
                                message.tingpais1 = [];
                            message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TingPai.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TingPai.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                    if (!Array.isArray(message.tingpais1))
                        return "tingpais1: array expected";
                    for (let i = 0; i < message.tingpais1.length; ++i) {
                        let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                        if (error)
                            return "tingpais1." + error;
                    }
                }
                return null;
            };

            TingPai.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordNewRound.TingPai)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordNewRound.TingPai();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.tingpais1) {
                    if (!Array.isArray(object.tingpais1))
                        throw TypeError(".lq.RecordNewRound.TingPai.tingpais1: array expected");
                    message.tingpais1 = Array(object.tingpais1.length);
                    for (let i = 0; i < object.tingpais1.length; ++i) {
                        if (typeof object.tingpais1[i] !== "object")
                            throw TypeError(".lq.RecordNewRound.TingPai.tingpais1: object expected");
                        message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                    }
                }
                return message;
            };

            TingPai.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tingpais1 = [];
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.tingpais1 && message.tingpais1.length) {
                    object.tingpais1 = Array(message.tingpais1.length);
                    for (let j = 0; j < message.tingpais1.length; ++j)
                        object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
                }
                return object;
            };

            TingPai.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TingPai.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordNewRound.TingPai";
            };

            return TingPai;
        })();

        return RecordNewRound;
    })();

    lq.GameSnapshot = (function() {

        function GameSnapshot(properties) {
            this.hands = [];
            this.doras = [];
            this.players = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameSnapshot.prototype.chang = 0;
        GameSnapshot.prototype.ju = 0;
        GameSnapshot.prototype.ben = 0;
        GameSnapshot.prototype.index_player = 0;
        GameSnapshot.prototype.left_tile_count = 0;
        GameSnapshot.prototype.hands = $util.emptyArray;
        GameSnapshot.prototype.doras = $util.emptyArray;
        GameSnapshot.prototype.liqibang = 0;
        GameSnapshot.prototype.players = $util.emptyArray;
        GameSnapshot.prototype.zhenting = false;

        GameSnapshot.create = function create(properties) {
            return new GameSnapshot(properties);
        };

        GameSnapshot.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.chang != null && Object.hasOwnProperty.call(message, "chang"))
                writer.uint32(8).uint32(message.chang);
            if (message.ju != null && Object.hasOwnProperty.call(message, "ju"))
                writer.uint32(16).uint32(message.ju);
            if (message.ben != null && Object.hasOwnProperty.call(message, "ben"))
                writer.uint32(24).uint32(message.ben);
            if (message.index_player != null && Object.hasOwnProperty.call(message, "index_player"))
                writer.uint32(32).uint32(message.index_player);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(40).uint32(message.left_tile_count);
            if (message.hands != null && message.hands.length)
                for (let i = 0; i < message.hands.length; ++i)
                    writer.uint32(50).string(message.hands[i]);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(64).uint32(message.liqibang);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.GameSnapshot.PlayerSnapshot.encode(message.players[i], writer.uint32(74).fork()).ldelim();
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(80).bool(message.zhenting);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameSnapshot(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.chang = value;
                        else
                            delete message.chang;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ju = value;
                        else
                            delete message.ju;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.ben = value;
                        else
                            delete message.ben;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index_player = value;
                        else
                            delete message.index_player;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hands && message.hands.length))
                            message.hands = [];
                        message.hands.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.GameSnapshot.PlayerSnapshot.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameSnapshot.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.chang != null && message.hasOwnProperty("chang"))
                if (!$util.isInteger(message.chang))
                    return "chang: integer expected";
            if (message.ju != null && message.hasOwnProperty("ju"))
                if (!$util.isInteger(message.ju))
                    return "ju: integer expected";
            if (message.ben != null && message.hasOwnProperty("ben"))
                if (!$util.isInteger(message.ben))
                    return "ben: integer expected";
            if (message.index_player != null && message.hasOwnProperty("index_player"))
                if (!$util.isInteger(message.index_player))
                    return "index_player: integer expected";
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.hands != null && message.hasOwnProperty("hands")) {
                if (!Array.isArray(message.hands))
                    return "hands: array expected";
                for (let i = 0; i < message.hands.length; ++i)
                    if (!$util.isString(message.hands[i]))
                        return "hands: string[] expected";
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.GameSnapshot.PlayerSnapshot.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            return null;
        };

        GameSnapshot.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameSnapshot)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameSnapshot();
            if (object.chang != null)
                if (Number(object.chang) !== 0)
                    message.chang = object.chang >>> 0;
            if (object.ju != null)
                if (Number(object.ju) !== 0)
                    message.ju = object.ju >>> 0;
            if (object.ben != null)
                if (Number(object.ben) !== 0)
                    message.ben = object.ben >>> 0;
            if (object.index_player != null)
                if (Number(object.index_player) !== 0)
                    message.index_player = object.index_player >>> 0;
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.hands) {
                if (!Array.isArray(object.hands))
                    throw TypeError(".lq.GameSnapshot.hands: array expected");
                message.hands = Array(object.hands.length);
                for (let i = 0; i < object.hands.length; ++i)
                    message.hands[i] = String(object.hands[i]);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.GameSnapshot.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.GameSnapshot.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.GameSnapshot.players: object expected");
                    message.players[i] = $root.lq.GameSnapshot.PlayerSnapshot.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            return message;
        };

        GameSnapshot.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hands = [];
                object.doras = [];
                object.players = [];
            }
            if (options.defaults) {
                object.chang = 0;
                object.ju = 0;
                object.ben = 0;
                object.index_player = 0;
                object.left_tile_count = 0;
                object.liqibang = 0;
                object.zhenting = false;
            }
            if (message.chang != null && message.hasOwnProperty("chang"))
                object.chang = message.chang;
            if (message.ju != null && message.hasOwnProperty("ju"))
                object.ju = message.ju;
            if (message.ben != null && message.hasOwnProperty("ben"))
                object.ben = message.ben;
            if (message.index_player != null && message.hasOwnProperty("index_player"))
                object.index_player = message.index_player;
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.hands && message.hands.length) {
                object.hands = Array(message.hands.length);
                for (let j = 0; j < message.hands.length; ++j)
                    object.hands[j] = message.hands[j];
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.GameSnapshot.PlayerSnapshot.toObject(message.players[j], options);
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            return object;
        };

        GameSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameSnapshot.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameSnapshot";
        };

        GameSnapshot.PlayerSnapshot = (function() {

            function PlayerSnapshot(properties) {
                this.qipais = [];
                this.mings = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            PlayerSnapshot.prototype.score = 0;
            PlayerSnapshot.prototype.liqiposition = 0;
            PlayerSnapshot.prototype.tilenum = 0;
            PlayerSnapshot.prototype.qipais = $util.emptyArray;
            PlayerSnapshot.prototype.mings = $util.emptyArray;

            PlayerSnapshot.create = function create(properties) {
                return new PlayerSnapshot(properties);
            };

            PlayerSnapshot.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                    writer.uint32(8).int32(message.score);
                if (message.liqiposition != null && Object.hasOwnProperty.call(message, "liqiposition"))
                    writer.uint32(16).int32(message.liqiposition);
                if (message.tilenum != null && Object.hasOwnProperty.call(message, "tilenum"))
                    writer.uint32(24).uint32(message.tilenum);
                if (message.qipais != null && message.qipais.length)
                    for (let i = 0; i < message.qipais.length; ++i)
                        writer.uint32(34).string(message.qipais[i]);
                if (message.mings != null && message.mings.length)
                    for (let i = 0; i < message.mings.length; ++i)
                        $root.lq.GameSnapshot.PlayerSnapshot.Fulu.encode(message.mings[i], writer.uint32(42).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            PlayerSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            PlayerSnapshot.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameSnapshot.PlayerSnapshot(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.score = value;
                            else
                                delete message.score;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.int32())
                                message.liqiposition = value;
                            else
                                delete message.liqiposition;
                            continue;
                        }
                    case 3: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.tilenum = value;
                            else
                                delete message.tilenum;
                            continue;
                        }
                    case 4: {
                            if (wireType !== 2)
                                break;
                            if (!(message.qipais && message.qipais.length))
                                message.qipais = [];
                            message.qipais.push(reader.string());
                            continue;
                        }
                    case 5: {
                            if (wireType !== 2)
                                break;
                            if (!(message.mings && message.mings.length))
                                message.mings = [];
                            message.mings.push($root.lq.GameSnapshot.PlayerSnapshot.Fulu.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            PlayerSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            PlayerSnapshot.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.score != null && message.hasOwnProperty("score"))
                    if (!$util.isInteger(message.score))
                        return "score: integer expected";
                if (message.liqiposition != null && message.hasOwnProperty("liqiposition"))
                    if (!$util.isInteger(message.liqiposition))
                        return "liqiposition: integer expected";
                if (message.tilenum != null && message.hasOwnProperty("tilenum"))
                    if (!$util.isInteger(message.tilenum))
                        return "tilenum: integer expected";
                if (message.qipais != null && message.hasOwnProperty("qipais")) {
                    if (!Array.isArray(message.qipais))
                        return "qipais: array expected";
                    for (let i = 0; i < message.qipais.length; ++i)
                        if (!$util.isString(message.qipais[i]))
                            return "qipais: string[] expected";
                }
                if (message.mings != null && message.hasOwnProperty("mings")) {
                    if (!Array.isArray(message.mings))
                        return "mings: array expected";
                    for (let i = 0; i < message.mings.length; ++i) {
                        let error = $root.lq.GameSnapshot.PlayerSnapshot.Fulu.verify(message.mings[i], _depth + 1);
                        if (error)
                            return "mings." + error;
                    }
                }
                return null;
            };

            PlayerSnapshot.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.GameSnapshot.PlayerSnapshot)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.GameSnapshot.PlayerSnapshot();
                if (object.score != null)
                    if (Number(object.score) !== 0)
                        message.score = object.score | 0;
                if (object.liqiposition != null)
                    if (Number(object.liqiposition) !== 0)
                        message.liqiposition = object.liqiposition | 0;
                if (object.tilenum != null)
                    if (Number(object.tilenum) !== 0)
                        message.tilenum = object.tilenum >>> 0;
                if (object.qipais) {
                    if (!Array.isArray(object.qipais))
                        throw TypeError(".lq.GameSnapshot.PlayerSnapshot.qipais: array expected");
                    message.qipais = Array(object.qipais.length);
                    for (let i = 0; i < object.qipais.length; ++i)
                        message.qipais[i] = String(object.qipais[i]);
                }
                if (object.mings) {
                    if (!Array.isArray(object.mings))
                        throw TypeError(".lq.GameSnapshot.PlayerSnapshot.mings: array expected");
                    message.mings = Array(object.mings.length);
                    for (let i = 0; i < object.mings.length; ++i) {
                        if (typeof object.mings[i] !== "object")
                            throw TypeError(".lq.GameSnapshot.PlayerSnapshot.mings: object expected");
                        message.mings[i] = $root.lq.GameSnapshot.PlayerSnapshot.Fulu.fromObject(object.mings[i], _depth + 1);
                    }
                }
                return message;
            };

            PlayerSnapshot.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.qipais = [];
                    object.mings = [];
                }
                if (options.defaults) {
                    object.score = 0;
                    object.liqiposition = 0;
                    object.tilenum = 0;
                }
                if (message.score != null && message.hasOwnProperty("score"))
                    object.score = message.score;
                if (message.liqiposition != null && message.hasOwnProperty("liqiposition"))
                    object.liqiposition = message.liqiposition;
                if (message.tilenum != null && message.hasOwnProperty("tilenum"))
                    object.tilenum = message.tilenum;
                if (message.qipais && message.qipais.length) {
                    object.qipais = Array(message.qipais.length);
                    for (let j = 0; j < message.qipais.length; ++j)
                        object.qipais[j] = message.qipais[j];
                }
                if (message.mings && message.mings.length) {
                    object.mings = Array(message.mings.length);
                    for (let j = 0; j < message.mings.length; ++j)
                        object.mings[j] = $root.lq.GameSnapshot.PlayerSnapshot.Fulu.toObject(message.mings[j], options);
                }
                return object;
            };

            PlayerSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlayerSnapshot.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.GameSnapshot.PlayerSnapshot";
            };

            PlayerSnapshot.Fulu = (function() {

                function Fulu(properties) {
                    this.tile = [];
                    this.from = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                Fulu.prototype.type = 0;
                Fulu.prototype.tile = $util.emptyArray;
                Fulu.prototype.from = $util.emptyArray;

                Fulu.create = function create(properties) {
                    return new Fulu(properties);
                };

                Fulu.encode = function encode(message, writer) {
                    if (!writer)
                        writer = $Writer.create();
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        writer.uint32(8).uint32(message.type);
                    if (message.tile != null && message.tile.length)
                        for (let i = 0; i < message.tile.length; ++i)
                            writer.uint32(18).string(message.tile[i]);
                    if (message.from != null && message.from.length) {
                        writer.uint32(26).fork();
                        for (let i = 0; i < message.from.length; ++i)
                            writer.uint32(message.from[i]);
                        writer.ldelim();
                    }
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (let i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                Fulu.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer).ldelim();
                };

                Fulu.decode = function decode(reader, length, _end, _depth, _target) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameSnapshot.PlayerSnapshot.Fulu(), value;
                    while (reader.pos < end) {
                        let start = reader.pos;
                        let tag = reader.tag();
                        if (tag === _end) {
                            _end = undefined;
                            break;
                        }
                        let wireType = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (wireType !== 0)
                                    break;
                                if (value = reader.uint32())
                                    message.type = value;
                                else
                                    delete message.type;
                                continue;
                            }
                        case 2: {
                                if (wireType !== 2)
                                    break;
                                if (!(message.tile && message.tile.length))
                                    message.tile = [];
                                message.tile.push(reader.string());
                                continue;
                            }
                        case 3: {
                                if (wireType === 2) {
                                    if (!(message.from && message.from.length))
                                        message.from = [];
                                    let end2 = reader.uint32() + reader.pos;
                                    while (reader.pos < end2)
                                        message.from.push(reader.uint32());
                                    continue;
                                }
                                if (wireType !== 0)
                                    break;
                                if (!(message.from && message.from.length))
                                    message.from = [];
                                message.from.push(reader.uint32());
                                continue;
                            }
                        }
                        reader.skipType(wireType, _depth, tag);
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                    }
                    if (_end !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                Fulu.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                Fulu.verify = function verify(message, _depth) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        return "max depth exceeded";
                    if (message.type != null && message.hasOwnProperty("type"))
                        if (!$util.isInteger(message.type))
                            return "type: integer expected";
                    if (message.tile != null && message.hasOwnProperty("tile")) {
                        if (!Array.isArray(message.tile))
                            return "tile: array expected";
                        for (let i = 0; i < message.tile.length; ++i)
                            if (!$util.isString(message.tile[i]))
                                return "tile: string[] expected";
                    }
                    if (message.from != null && message.hasOwnProperty("from")) {
                        if (!Array.isArray(message.from))
                            return "from: array expected";
                        for (let i = 0; i < message.from.length; ++i)
                            if (!$util.isInteger(message.from[i]))
                                return "from: integer[] expected";
                    }
                    return null;
                };

                Fulu.fromObject = function fromObject(object, _depth) {
                    if (object instanceof $root.lq.GameSnapshot.PlayerSnapshot.Fulu)
                        return object;
                    if (_depth === undefined)
                        _depth = 0;
                    if (_depth > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let message = new $root.lq.GameSnapshot.PlayerSnapshot.Fulu();
                    if (object.type != null)
                        if (Number(object.type) !== 0)
                            message.type = object.type >>> 0;
                    if (object.tile) {
                        if (!Array.isArray(object.tile))
                            throw TypeError(".lq.GameSnapshot.PlayerSnapshot.Fulu.tile: array expected");
                        message.tile = Array(object.tile.length);
                        for (let i = 0; i < object.tile.length; ++i)
                            message.tile[i] = String(object.tile[i]);
                    }
                    if (object.from) {
                        if (!Array.isArray(object.from))
                            throw TypeError(".lq.GameSnapshot.PlayerSnapshot.Fulu.from: array expected");
                        message.from = Array(object.from.length);
                        for (let i = 0; i < object.from.length; ++i)
                            message.from[i] = object.from[i] >>> 0;
                    }
                    return message;
                };

                Fulu.toObject = function toObject(message, options) {
                    if (!options)
                        options = {};
                    let object = {};
                    if (options.arrays || options.defaults) {
                        object.tile = [];
                        object.from = [];
                    }
                    if (options.defaults)
                        object.type = 0;
                    if (message.type != null && message.hasOwnProperty("type"))
                        object.type = message.type;
                    if (message.tile && message.tile.length) {
                        object.tile = Array(message.tile.length);
                        for (let j = 0; j < message.tile.length; ++j)
                            object.tile[j] = message.tile[j];
                    }
                    if (message.from && message.from.length) {
                        object.from = Array(message.from.length);
                        for (let j = 0; j < message.from.length; ++j)
                            object.from[j] = message.from[j];
                    }
                    return object;
                };

                Fulu.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                Fulu.getTypeUrl = function getTypeUrl(prefix) {
                    if (prefix === undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/lq.GameSnapshot.PlayerSnapshot.Fulu";
                };

                return Fulu;
            })();

            return PlayerSnapshot;
        })();

        return GameSnapshot;
    })();

    lq.ActionPrototype = (function() {

        function ActionPrototype(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionPrototype.prototype.step = 0;
        ActionPrototype.prototype.name = "";
        ActionPrototype.prototype.data = $util.newBuffer([]);

        ActionPrototype.create = function create(properties) {
            return new ActionPrototype(properties);
        };

        ActionPrototype.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.step != null && Object.hasOwnProperty.call(message, "step"))
                writer.uint32(8).uint32(message.step);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(18).string(message.name);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(26).bytes(message.data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionPrototype.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionPrototype.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionPrototype(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.step = value;
                        else
                            delete message.step;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionPrototype.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionPrototype.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.step != null && message.hasOwnProperty("step"))
                if (!$util.isInteger(message.step))
                    return "step: integer expected";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.data != null && message.hasOwnProperty("data"))
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            return null;
        };

        ActionPrototype.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionPrototype)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionPrototype();
            if (object.step != null)
                if (Number(object.step) !== 0)
                    message.step = object.step >>> 0;
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        ActionPrototype.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.step = 0;
                object.name = "";
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.step != null && message.hasOwnProperty("step"))
                object.step = message.step;
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.data != null && message.hasOwnProperty("data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        ActionPrototype.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionPrototype.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionPrototype";
        };

        return ActionPrototype;
    })();

    lq.GameDetailRecords = (function() {

        function GameDetailRecords(properties) {
            this.records = [];
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameDetailRecords.prototype.records = $util.emptyArray;
        GameDetailRecords.prototype.version = 0;
        GameDetailRecords.prototype.actions = $util.emptyArray;
        GameDetailRecords.prototype.bar = $util.newBuffer([]);

        GameDetailRecords.create = function create(properties) {
            return new GameDetailRecords(properties);
        };

        GameDetailRecords.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.records != null && message.records.length)
                for (let i = 0; i < message.records.length; ++i)
                    writer.uint32(10).bytes(message.records[i]);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(16).uint32(message.version);
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.lq.GameAction.encode(message.actions[i], writer.uint32(26).fork()).ldelim();
            if (message.bar != null && Object.hasOwnProperty.call(message, "bar"))
                writer.uint32(34).bytes(message.bar);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameDetailRecords.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameDetailRecords.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameDetailRecords(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push(reader.bytes());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.lq.GameAction.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.bar = value;
                        else
                            delete message.bar;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameDetailRecords.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameDetailRecords.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.records != null && message.hasOwnProperty("records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (let i = 0; i < message.records.length; ++i)
                    if (!(message.records[i] && typeof message.records[i].length === "number" || $util.isString(message.records[i])))
                        return "records: buffer[] expected";
            }
            if (message.version != null && message.hasOwnProperty("version"))
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            if (message.actions != null && message.hasOwnProperty("actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.lq.GameAction.verify(message.actions[i], _depth + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.bar != null && message.hasOwnProperty("bar"))
                if (!(message.bar && typeof message.bar.length === "number" || $util.isString(message.bar)))
                    return "bar: buffer expected";
            return null;
        };

        GameDetailRecords.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameDetailRecords)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameDetailRecords();
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".lq.GameDetailRecords.records: array expected");
                message.records = Array(object.records.length);
                for (let i = 0; i < object.records.length; ++i)
                    if (typeof object.records[i] === "string")
                        $util.base64.decode(object.records[i], message.records[i] = $util.newBuffer($util.base64.length(object.records[i])), 0);
                    else if (object.records[i].length >= 0)
                        message.records[i] = object.records[i];
            }
            if (object.version != null)
                if (Number(object.version) !== 0)
                    message.version = object.version >>> 0;
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".lq.GameDetailRecords.actions: array expected");
                message.actions = Array(object.actions.length);
                for (let i = 0; i < object.actions.length; ++i) {
                    if (typeof object.actions[i] !== "object")
                        throw TypeError(".lq.GameDetailRecords.actions: object expected");
                    message.actions[i] = $root.lq.GameAction.fromObject(object.actions[i], _depth + 1);
                }
            }
            if (object.bar != null)
                if (object.bar.length)
                    if (typeof object.bar === "string")
                        $util.base64.decode(object.bar, message.bar = $util.newBuffer($util.base64.length(object.bar)), 0);
                    else if (object.bar.length >= 0)
                        message.bar = object.bar;
            return message;
        };

        GameDetailRecords.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.records = [];
                object.actions = [];
            }
            if (options.defaults) {
                object.version = 0;
                if (options.bytes === String)
                    object.bar = "";
                else {
                    object.bar = [];
                    if (options.bytes !== Array)
                        object.bar = $util.newBuffer(object.bar);
                }
            }
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (let j = 0; j < message.records.length; ++j)
                    object.records[j] = options.bytes === String ? $util.base64.encode(message.records[j], 0, message.records[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.records[j]) : message.records[j];
            }
            if (message.version != null && message.hasOwnProperty("version"))
                object.version = message.version;
            if (message.actions && message.actions.length) {
                object.actions = Array(message.actions.length);
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.lq.GameAction.toObject(message.actions[j], options);
            }
            if (message.bar != null && message.hasOwnProperty("bar"))
                object.bar = options.bytes === String ? $util.base64.encode(message.bar, 0, message.bar.length) : options.bytes === Array ? Array.prototype.slice.call(message.bar) : message.bar;
            return object;
        };

        GameDetailRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameDetailRecords.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameDetailRecords";
        };

        return GameDetailRecords;
    })();

    lq.GameSelfOperation = (function() {

        function GameSelfOperation(properties) {
            this.change_tiles = [];
            this.tile_states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameSelfOperation.prototype.type = 0;
        GameSelfOperation.prototype.index = 0;
        GameSelfOperation.prototype.tile = "";
        GameSelfOperation.prototype.cancel_operation = false;
        GameSelfOperation.prototype.moqie = false;
        GameSelfOperation.prototype.timeuse = 0;
        GameSelfOperation.prototype.tile_state = 0;
        GameSelfOperation.prototype.change_tiles = $util.emptyArray;
        GameSelfOperation.prototype.tile_states = $util.emptyArray;
        GameSelfOperation.prototype.gap_type = 0;

        GameSelfOperation.create = function create(properties) {
            return new GameSelfOperation(properties);
        };

        GameSelfOperation.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(16).uint32(message.index);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(26).string(message.tile);
            if (message.cancel_operation != null && Object.hasOwnProperty.call(message, "cancel_operation"))
                writer.uint32(32).bool(message.cancel_operation);
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(40).bool(message.moqie);
            if (message.timeuse != null && Object.hasOwnProperty.call(message, "timeuse"))
                writer.uint32(48).uint32(message.timeuse);
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(56).int32(message.tile_state);
            if (message.change_tiles != null && message.change_tiles.length)
                for (let i = 0; i < message.change_tiles.length; ++i)
                    writer.uint32(66).string(message.change_tiles[i]);
            if (message.tile_states != null && message.tile_states.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.tile_states.length; ++i)
                    writer.int32(message.tile_states[i]);
                writer.ldelim();
            }
            if (message.gap_type != null && Object.hasOwnProperty.call(message, "gap_type"))
                writer.uint32(80).uint32(message.gap_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameSelfOperation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameSelfOperation.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameSelfOperation(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancel_operation = value;
                        else
                            delete message.cancel_operation;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timeuse = value;
                        else
                            delete message.timeuse;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.change_tiles && message.change_tiles.length))
                            message.change_tiles = [];
                        message.change_tiles.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.tile_states && message.tile_states.length))
                                message.tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_states.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_states && message.tile_states.length))
                            message.tile_states = [];
                        message.tile_states.push(reader.int32());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gap_type = value;
                        else
                            delete message.gap_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameSelfOperation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameSelfOperation.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                if (typeof message.cancel_operation !== "boolean")
                    return "cancel_operation: boolean expected";
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                if (!$util.isInteger(message.timeuse))
                    return "timeuse: integer expected";
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.change_tiles != null && message.hasOwnProperty("change_tiles")) {
                if (!Array.isArray(message.change_tiles))
                    return "change_tiles: array expected";
                for (let i = 0; i < message.change_tiles.length; ++i)
                    if (!$util.isString(message.change_tiles[i]))
                        return "change_tiles: string[] expected";
            }
            if (message.tile_states != null && message.hasOwnProperty("tile_states")) {
                if (!Array.isArray(message.tile_states))
                    return "tile_states: array expected";
                for (let i = 0; i < message.tile_states.length; ++i)
                    if (!$util.isInteger(message.tile_states[i]))
                        return "tile_states: integer[] expected";
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                if (!$util.isInteger(message.gap_type))
                    return "gap_type: integer expected";
            return null;
        };

        GameSelfOperation.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameSelfOperation)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameSelfOperation();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.cancel_operation != null)
                if (object.cancel_operation)
                    message.cancel_operation = Boolean(object.cancel_operation);
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.timeuse != null)
                if (Number(object.timeuse) !== 0)
                    message.timeuse = object.timeuse >>> 0;
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state | 0;
            if (object.change_tiles) {
                if (!Array.isArray(object.change_tiles))
                    throw TypeError(".lq.GameSelfOperation.change_tiles: array expected");
                message.change_tiles = Array(object.change_tiles.length);
                for (let i = 0; i < object.change_tiles.length; ++i)
                    message.change_tiles[i] = String(object.change_tiles[i]);
            }
            if (object.tile_states) {
                if (!Array.isArray(object.tile_states))
                    throw TypeError(".lq.GameSelfOperation.tile_states: array expected");
                message.tile_states = Array(object.tile_states.length);
                for (let i = 0; i < object.tile_states.length; ++i)
                    message.tile_states[i] = object.tile_states[i] | 0;
            }
            if (object.gap_type != null)
                if (Number(object.gap_type) !== 0)
                    message.gap_type = object.gap_type >>> 0;
            return message;
        };

        GameSelfOperation.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.change_tiles = [];
                object.tile_states = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.index = 0;
                object.tile = "";
                object.cancel_operation = false;
                object.moqie = false;
                object.timeuse = 0;
                object.tile_state = 0;
                object.gap_type = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                object.cancel_operation = message.cancel_operation;
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                object.timeuse = message.timeuse;
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.change_tiles && message.change_tiles.length) {
                object.change_tiles = Array(message.change_tiles.length);
                for (let j = 0; j < message.change_tiles.length; ++j)
                    object.change_tiles[j] = message.change_tiles[j];
            }
            if (message.tile_states && message.tile_states.length) {
                object.tile_states = Array(message.tile_states.length);
                for (let j = 0; j < message.tile_states.length; ++j)
                    object.tile_states[j] = message.tile_states[j];
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                object.gap_type = message.gap_type;
            return object;
        };

        GameSelfOperation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameSelfOperation.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameSelfOperation";
        };

        return GameSelfOperation;
    })();

    lq.GameChiPengGang = (function() {

        function GameChiPengGang(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameChiPengGang.prototype.type = 0;
        GameChiPengGang.prototype.index = 0;
        GameChiPengGang.prototype.cancel_operation = false;
        GameChiPengGang.prototype.timeuse = 0;

        GameChiPengGang.create = function create(properties) {
            return new GameChiPengGang(properties);
        };

        GameChiPengGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(16).uint32(message.index);
            if (message.cancel_operation != null && Object.hasOwnProperty.call(message, "cancel_operation"))
                writer.uint32(24).bool(message.cancel_operation);
            if (message.timeuse != null && Object.hasOwnProperty.call(message, "timeuse"))
                writer.uint32(48).uint32(message.timeuse);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameChiPengGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameChiPengGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameChiPengGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.index = value;
                        else
                            delete message.index;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cancel_operation = value;
                        else
                            delete message.cancel_operation;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.timeuse = value;
                        else
                            delete message.timeuse;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameChiPengGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameChiPengGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.index != null && message.hasOwnProperty("index"))
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                if (typeof message.cancel_operation !== "boolean")
                    return "cancel_operation: boolean expected";
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                if (!$util.isInteger(message.timeuse))
                    return "timeuse: integer expected";
            return null;
        };

        GameChiPengGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameChiPengGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameChiPengGang();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.index != null)
                if (Number(object.index) !== 0)
                    message.index = object.index >>> 0;
            if (object.cancel_operation != null)
                if (object.cancel_operation)
                    message.cancel_operation = Boolean(object.cancel_operation);
            if (object.timeuse != null)
                if (Number(object.timeuse) !== 0)
                    message.timeuse = object.timeuse >>> 0;
            return message;
        };

        GameChiPengGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.index = 0;
                object.cancel_operation = false;
                object.timeuse = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.index != null && message.hasOwnProperty("index"))
                object.index = message.index;
            if (message.cancel_operation != null && message.hasOwnProperty("cancel_operation"))
                object.cancel_operation = message.cancel_operation;
            if (message.timeuse != null && message.hasOwnProperty("timeuse"))
                object.timeuse = message.timeuse;
            return object;
        };

        GameChiPengGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameChiPengGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameChiPengGang";
        };

        return GameChiPengGang;
    })();

    lq.GameVoteGameEnd = (function() {

        function GameVoteGameEnd(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameVoteGameEnd.prototype.yes = false;

        GameVoteGameEnd.create = function create(properties) {
            return new GameVoteGameEnd(properties);
        };

        GameVoteGameEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.yes != null && Object.hasOwnProperty.call(message, "yes"))
                writer.uint32(8).bool(message.yes);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameVoteGameEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameVoteGameEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameVoteGameEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yes = value;
                        else
                            delete message.yes;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameVoteGameEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameVoteGameEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.yes != null && message.hasOwnProperty("yes"))
                if (typeof message.yes !== "boolean")
                    return "yes: boolean expected";
            return null;
        };

        GameVoteGameEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameVoteGameEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameVoteGameEnd();
            if (object.yes != null)
                if (object.yes)
                    message.yes = Boolean(object.yes);
            return message;
        };

        GameVoteGameEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.yes = false;
            if (message.yes != null && message.hasOwnProperty("yes"))
                object.yes = message.yes;
            return object;
        };

        GameVoteGameEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameVoteGameEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameVoteGameEnd";
        };

        return GameVoteGameEnd;
    })();

    lq.GameUserInput = (function() {

        function GameUserInput(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameUserInput.prototype.seat = 0;
        GameUserInput.prototype.type = 0;
        GameUserInput.prototype.emo = 0;
        GameUserInput.prototype.operation = null;
        GameUserInput.prototype.cpg = null;
        GameUserInput.prototype.vote = null;

        GameUserInput.create = function create(properties) {
            return new GameUserInput(properties);
        };

        GameUserInput.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.emo != null && Object.hasOwnProperty.call(message, "emo"))
                writer.uint32(24).uint32(message.emo);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.GameSelfOperation.encode(message.operation, writer.uint32(82).fork()).ldelim();
            if (message.cpg != null && Object.hasOwnProperty.call(message, "cpg"))
                $root.lq.GameChiPengGang.encode(message.cpg, writer.uint32(90).fork()).ldelim();
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                $root.lq.GameVoteGameEnd.encode(message.vote, writer.uint32(98).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameUserInput.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameUserInput.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameUserInput(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.emo = value;
                        else
                            delete message.emo;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.GameSelfOperation.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.cpg = $root.lq.GameChiPengGang.decode(reader, reader.uint32(), undefined, _depth + 1, message.cpg);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.vote = $root.lq.GameVoteGameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.vote);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameUserInput.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameUserInput.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.emo != null && message.hasOwnProperty("emo"))
                if (!$util.isInteger(message.emo))
                    return "emo: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.GameSelfOperation.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.cpg != null && message.hasOwnProperty("cpg")) {
                let error = $root.lq.GameChiPengGang.verify(message.cpg, _depth + 1);
                if (error)
                    return "cpg." + error;
            }
            if (message.vote != null && message.hasOwnProperty("vote")) {
                let error = $root.lq.GameVoteGameEnd.verify(message.vote, _depth + 1);
                if (error)
                    return "vote." + error;
            }
            return null;
        };

        GameUserInput.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameUserInput)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameUserInput();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.emo != null)
                if (Number(object.emo) !== 0)
                    message.emo = object.emo >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.GameUserInput.operation: object expected");
                message.operation = $root.lq.GameSelfOperation.fromObject(object.operation, _depth + 1);
            }
            if (object.cpg != null) {
                if (typeof object.cpg !== "object")
                    throw TypeError(".lq.GameUserInput.cpg: object expected");
                message.cpg = $root.lq.GameChiPengGang.fromObject(object.cpg, _depth + 1);
            }
            if (object.vote != null) {
                if (typeof object.vote !== "object")
                    throw TypeError(".lq.GameUserInput.vote: object expected");
                message.vote = $root.lq.GameVoteGameEnd.fromObject(object.vote, _depth + 1);
            }
            return message;
        };

        GameUserInput.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.emo = 0;
                object.operation = null;
                object.cpg = null;
                object.vote = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.emo != null && message.hasOwnProperty("emo"))
                object.emo = message.emo;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.GameSelfOperation.toObject(message.operation, options);
            if (message.cpg != null && message.hasOwnProperty("cpg"))
                object.cpg = $root.lq.GameChiPengGang.toObject(message.cpg, options);
            if (message.vote != null && message.hasOwnProperty("vote"))
                object.vote = $root.lq.GameVoteGameEnd.toObject(message.vote, options);
            return object;
        };

        GameUserInput.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameUserInput.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameUserInput";
        };

        return GameUserInput;
    })();

    lq.GameUserEvent = (function() {

        function GameUserEvent(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameUserEvent.prototype.seat = 0;
        GameUserEvent.prototype.type = 0;

        GameUserEvent.create = function create(properties) {
            return new GameUserEvent(properties);
        };

        GameUserEvent.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameUserEvent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameUserEvent.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameUserEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameUserEvent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameUserEvent.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        GameUserEvent.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameUserEvent)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameUserEvent();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        GameUserEvent.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        GameUserEvent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameUserEvent.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameUserEvent";
        };

        return GameUserEvent;
    })();

    lq.GameAction = (function() {

        function GameAction(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameAction.prototype.passed = 0;
        GameAction.prototype.type = 0;
        GameAction.prototype.result = $util.newBuffer([]);
        GameAction.prototype.user_input = null;
        GameAction.prototype.user_event = null;
        GameAction.prototype.game_event = 0;

        GameAction.create = function create(properties) {
            return new GameAction(properties);
        };

        GameAction.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.passed != null && Object.hasOwnProperty.call(message, "passed"))
                writer.uint32(8).uint32(message.passed);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(26).bytes(message.result);
            if (message.user_input != null && Object.hasOwnProperty.call(message, "user_input"))
                $root.lq.GameUserInput.encode(message.user_input, writer.uint32(34).fork()).ldelim();
            if (message.user_event != null && Object.hasOwnProperty.call(message, "user_event"))
                $root.lq.GameUserEvent.encode(message.user_event, writer.uint32(42).fork()).ldelim();
            if (message.game_event != null && Object.hasOwnProperty.call(message, "game_event"))
                writer.uint32(48).uint32(message.game_event);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameAction.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameAction(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.passed = value;
                        else
                            delete message.passed;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.user_input = $root.lq.GameUserInput.decode(reader, reader.uint32(), undefined, _depth + 1, message.user_input);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.user_event = $root.lq.GameUserEvent.decode(reader, reader.uint32(), undefined, _depth + 1, message.user_event);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.game_event = value;
                        else
                            delete message.game_event;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameAction.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.passed != null && message.hasOwnProperty("passed"))
                if (!$util.isInteger(message.passed))
                    return "passed: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!(message.result && typeof message.result.length === "number" || $util.isString(message.result)))
                    return "result: buffer expected";
            if (message.user_input != null && message.hasOwnProperty("user_input")) {
                let error = $root.lq.GameUserInput.verify(message.user_input, _depth + 1);
                if (error)
                    return "user_input." + error;
            }
            if (message.user_event != null && message.hasOwnProperty("user_event")) {
                let error = $root.lq.GameUserEvent.verify(message.user_event, _depth + 1);
                if (error)
                    return "user_event." + error;
            }
            if (message.game_event != null && message.hasOwnProperty("game_event"))
                if (!$util.isInteger(message.game_event))
                    return "game_event: integer expected";
            return null;
        };

        GameAction.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameAction)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameAction();
            if (object.passed != null)
                if (Number(object.passed) !== 0)
                    message.passed = object.passed >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.result != null)
                if (object.result.length)
                    if (typeof object.result === "string")
                        $util.base64.decode(object.result, message.result = $util.newBuffer($util.base64.length(object.result)), 0);
                    else if (object.result.length >= 0)
                        message.result = object.result;
            if (object.user_input != null) {
                if (typeof object.user_input !== "object")
                    throw TypeError(".lq.GameAction.user_input: object expected");
                message.user_input = $root.lq.GameUserInput.fromObject(object.user_input, _depth + 1);
            }
            if (object.user_event != null) {
                if (typeof object.user_event !== "object")
                    throw TypeError(".lq.GameAction.user_event: object expected");
                message.user_event = $root.lq.GameUserEvent.fromObject(object.user_event, _depth + 1);
            }
            if (object.game_event != null)
                if (Number(object.game_event) !== 0)
                    message.game_event = object.game_event >>> 0;
            return message;
        };

        GameAction.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.passed = 0;
                object.type = 0;
                if (options.bytes === String)
                    object.result = "";
                else {
                    object.result = [];
                    if (options.bytes !== Array)
                        object.result = $util.newBuffer(object.result);
                }
                object.user_input = null;
                object.user_event = null;
                object.game_event = 0;
            }
            if (message.passed != null && message.hasOwnProperty("passed"))
                object.passed = message.passed;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = options.bytes === String ? $util.base64.encode(message.result, 0, message.result.length) : options.bytes === Array ? Array.prototype.slice.call(message.result) : message.result;
            if (message.user_input != null && message.hasOwnProperty("user_input"))
                object.user_input = $root.lq.GameUserInput.toObject(message.user_input, options);
            if (message.user_event != null && message.hasOwnProperty("user_event"))
                object.user_event = $root.lq.GameUserEvent.toObject(message.user_event, options);
            if (message.game_event != null && message.hasOwnProperty("game_event"))
                object.game_event = message.game_event;
            return object;
        };

        GameAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameAction.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameAction";
        };

        return GameAction;
    })();

    lq.OptionalOperation = (function() {

        function OptionalOperation(properties) {
            this.combination = [];
            this.change_tiles = [];
            this.change_tile_states = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        OptionalOperation.prototype.type = 0;
        OptionalOperation.prototype.combination = $util.emptyArray;
        OptionalOperation.prototype.change_tiles = $util.emptyArray;
        OptionalOperation.prototype.change_tile_states = $util.emptyArray;
        OptionalOperation.prototype.gap_type = 0;

        OptionalOperation.create = function create(properties) {
            return new OptionalOperation(properties);
        };

        OptionalOperation.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.combination != null && message.combination.length)
                for (let i = 0; i < message.combination.length; ++i)
                    writer.uint32(18).string(message.combination[i]);
            if (message.change_tiles != null && message.change_tiles.length)
                for (let i = 0; i < message.change_tiles.length; ++i)
                    writer.uint32(26).string(message.change_tiles[i]);
            if (message.change_tile_states != null && message.change_tile_states.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.change_tile_states.length; ++i)
                    writer.int32(message.change_tile_states[i]);
                writer.ldelim();
            }
            if (message.gap_type != null && Object.hasOwnProperty.call(message, "gap_type"))
                writer.uint32(40).uint32(message.gap_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OptionalOperation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        OptionalOperation.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.OptionalOperation(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.combination && message.combination.length))
                            message.combination = [];
                        message.combination.push(reader.string());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.change_tiles && message.change_tiles.length))
                            message.change_tiles = [];
                        message.change_tiles.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.change_tile_states && message.change_tile_states.length))
                                message.change_tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.change_tile_states.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.change_tile_states && message.change_tile_states.length))
                            message.change_tile_states = [];
                        message.change_tile_states.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.gap_type = value;
                        else
                            delete message.gap_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        OptionalOperation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        OptionalOperation.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.combination != null && message.hasOwnProperty("combination")) {
                if (!Array.isArray(message.combination))
                    return "combination: array expected";
                for (let i = 0; i < message.combination.length; ++i)
                    if (!$util.isString(message.combination[i]))
                        return "combination: string[] expected";
            }
            if (message.change_tiles != null && message.hasOwnProperty("change_tiles")) {
                if (!Array.isArray(message.change_tiles))
                    return "change_tiles: array expected";
                for (let i = 0; i < message.change_tiles.length; ++i)
                    if (!$util.isString(message.change_tiles[i]))
                        return "change_tiles: string[] expected";
            }
            if (message.change_tile_states != null && message.hasOwnProperty("change_tile_states")) {
                if (!Array.isArray(message.change_tile_states))
                    return "change_tile_states: array expected";
                for (let i = 0; i < message.change_tile_states.length; ++i)
                    if (!$util.isInteger(message.change_tile_states[i]))
                        return "change_tile_states: integer[] expected";
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                if (!$util.isInteger(message.gap_type))
                    return "gap_type: integer expected";
            return null;
        };

        OptionalOperation.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.OptionalOperation)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.OptionalOperation();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.combination) {
                if (!Array.isArray(object.combination))
                    throw TypeError(".lq.OptionalOperation.combination: array expected");
                message.combination = Array(object.combination.length);
                for (let i = 0; i < object.combination.length; ++i)
                    message.combination[i] = String(object.combination[i]);
            }
            if (object.change_tiles) {
                if (!Array.isArray(object.change_tiles))
                    throw TypeError(".lq.OptionalOperation.change_tiles: array expected");
                message.change_tiles = Array(object.change_tiles.length);
                for (let i = 0; i < object.change_tiles.length; ++i)
                    message.change_tiles[i] = String(object.change_tiles[i]);
            }
            if (object.change_tile_states) {
                if (!Array.isArray(object.change_tile_states))
                    throw TypeError(".lq.OptionalOperation.change_tile_states: array expected");
                message.change_tile_states = Array(object.change_tile_states.length);
                for (let i = 0; i < object.change_tile_states.length; ++i)
                    message.change_tile_states[i] = object.change_tile_states[i] | 0;
            }
            if (object.gap_type != null)
                if (Number(object.gap_type) !== 0)
                    message.gap_type = object.gap_type >>> 0;
            return message;
        };

        OptionalOperation.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.combination = [];
                object.change_tiles = [];
                object.change_tile_states = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.gap_type = 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.combination && message.combination.length) {
                object.combination = Array(message.combination.length);
                for (let j = 0; j < message.combination.length; ++j)
                    object.combination[j] = message.combination[j];
            }
            if (message.change_tiles && message.change_tiles.length) {
                object.change_tiles = Array(message.change_tiles.length);
                for (let j = 0; j < message.change_tiles.length; ++j)
                    object.change_tiles[j] = message.change_tiles[j];
            }
            if (message.change_tile_states && message.change_tile_states.length) {
                object.change_tile_states = Array(message.change_tile_states.length);
                for (let j = 0; j < message.change_tile_states.length; ++j)
                    object.change_tile_states[j] = message.change_tile_states[j];
            }
            if (message.gap_type != null && message.hasOwnProperty("gap_type"))
                object.gap_type = message.gap_type;
            return object;
        };

        OptionalOperation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        OptionalOperation.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.OptionalOperation";
        };

        return OptionalOperation;
    })();

    lq.OptionalOperationList = (function() {

        function OptionalOperationList(properties) {
            this.operation_list = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        OptionalOperationList.prototype.seat = 0;
        OptionalOperationList.prototype.operation_list = $util.emptyArray;
        OptionalOperationList.prototype.time_add = 0;
        OptionalOperationList.prototype.time_fixed = 0;

        OptionalOperationList.create = function create(properties) {
            return new OptionalOperationList(properties);
        };

        OptionalOperationList.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.operation_list != null && message.operation_list.length)
                for (let i = 0; i < message.operation_list.length; ++i)
                    $root.lq.OptionalOperation.encode(message.operation_list[i], writer.uint32(18).fork()).ldelim();
            if (message.time_add != null && Object.hasOwnProperty.call(message, "time_add"))
                writer.uint32(32).uint32(message.time_add);
            if (message.time_fixed != null && Object.hasOwnProperty.call(message, "time_fixed"))
                writer.uint32(40).uint32(message.time_fixed);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OptionalOperationList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        OptionalOperationList.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.OptionalOperationList(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operation_list && message.operation_list.length))
                            message.operation_list = [];
                        message.operation_list.push($root.lq.OptionalOperation.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_add = value;
                        else
                            delete message.time_add;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.time_fixed = value;
                        else
                            delete message.time_fixed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        OptionalOperationList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        OptionalOperationList.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.operation_list != null && message.hasOwnProperty("operation_list")) {
                if (!Array.isArray(message.operation_list))
                    return "operation_list: array expected";
                for (let i = 0; i < message.operation_list.length; ++i) {
                    let error = $root.lq.OptionalOperation.verify(message.operation_list[i], _depth + 1);
                    if (error)
                        return "operation_list." + error;
                }
            }
            if (message.time_add != null && message.hasOwnProperty("time_add"))
                if (!$util.isInteger(message.time_add))
                    return "time_add: integer expected";
            if (message.time_fixed != null && message.hasOwnProperty("time_fixed"))
                if (!$util.isInteger(message.time_fixed))
                    return "time_fixed: integer expected";
            return null;
        };

        OptionalOperationList.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.OptionalOperationList)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.OptionalOperationList();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.operation_list) {
                if (!Array.isArray(object.operation_list))
                    throw TypeError(".lq.OptionalOperationList.operation_list: array expected");
                message.operation_list = Array(object.operation_list.length);
                for (let i = 0; i < object.operation_list.length; ++i) {
                    if (typeof object.operation_list[i] !== "object")
                        throw TypeError(".lq.OptionalOperationList.operation_list: object expected");
                    message.operation_list[i] = $root.lq.OptionalOperation.fromObject(object.operation_list[i], _depth + 1);
                }
            }
            if (object.time_add != null)
                if (Number(object.time_add) !== 0)
                    message.time_add = object.time_add >>> 0;
            if (object.time_fixed != null)
                if (Number(object.time_fixed) !== 0)
                    message.time_fixed = object.time_fixed >>> 0;
            return message;
        };

        OptionalOperationList.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.operation_list = [];
            if (options.defaults) {
                object.seat = 0;
                object.time_add = 0;
                object.time_fixed = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.operation_list && message.operation_list.length) {
                object.operation_list = Array(message.operation_list.length);
                for (let j = 0; j < message.operation_list.length; ++j)
                    object.operation_list[j] = $root.lq.OptionalOperation.toObject(message.operation_list[j], options);
            }
            if (message.time_add != null && message.hasOwnProperty("time_add"))
                object.time_add = message.time_add;
            if (message.time_fixed != null && message.hasOwnProperty("time_fixed"))
                object.time_fixed = message.time_fixed;
            return object;
        };

        OptionalOperationList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        OptionalOperationList.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.OptionalOperationList";
        };

        return OptionalOperationList;
    })();

    lq.LiQiSuccess = (function() {

        function LiQiSuccess(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        LiQiSuccess.prototype.seat = 0;
        LiQiSuccess.prototype.score = 0;
        LiQiSuccess.prototype.liqibang = 0;
        LiQiSuccess.prototype.failed = false;
        LiQiSuccess.prototype.liqi_type_beishuizhizhan = 0;
        LiQiSuccess.prototype.xia_ke_shang = null;

        LiQiSuccess.create = function create(properties) {
            return new LiQiSuccess(properties);
        };

        LiQiSuccess.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(16).int32(message.score);
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(24).uint32(message.liqibang);
            if (message.failed != null && Object.hasOwnProperty.call(message, "failed"))
                writer.uint32(32).bool(message.failed);
            if (message.liqi_type_beishuizhizhan != null && Object.hasOwnProperty.call(message, "liqi_type_beishuizhizhan"))
                writer.uint32(40).uint32(message.liqi_type_beishuizhizhan);
            if (message.xia_ke_shang != null && Object.hasOwnProperty.call(message, "xia_ke_shang"))
                $root.lq.XiaKeShangInfo.encode(message.xia_ke_shang, writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        LiQiSuccess.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        LiQiSuccess.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.LiQiSuccess(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.failed = value;
                        else
                            delete message.failed;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqi_type_beishuizhizhan = value;
                        else
                            delete message.liqi_type_beishuizhizhan;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.xia_ke_shang = $root.lq.XiaKeShangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.xia_ke_shang);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        LiQiSuccess.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        LiQiSuccess.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.failed != null && message.hasOwnProperty("failed"))
                if (typeof message.failed !== "boolean")
                    return "failed: boolean expected";
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                if (!$util.isInteger(message.liqi_type_beishuizhizhan))
                    return "liqi_type_beishuizhizhan: integer expected";
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang")) {
                let error = $root.lq.XiaKeShangInfo.verify(message.xia_ke_shang, _depth + 1);
                if (error)
                    return "xia_ke_shang." + error;
            }
            return null;
        };

        LiQiSuccess.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.LiQiSuccess)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.LiQiSuccess();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score | 0;
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.failed != null)
                if (object.failed)
                    message.failed = Boolean(object.failed);
            if (object.liqi_type_beishuizhizhan != null)
                if (Number(object.liqi_type_beishuizhizhan) !== 0)
                    message.liqi_type_beishuizhizhan = object.liqi_type_beishuizhizhan >>> 0;
            if (object.xia_ke_shang != null) {
                if (typeof object.xia_ke_shang !== "object")
                    throw TypeError(".lq.LiQiSuccess.xia_ke_shang: object expected");
                message.xia_ke_shang = $root.lq.XiaKeShangInfo.fromObject(object.xia_ke_shang, _depth + 1);
            }
            return message;
        };

        LiQiSuccess.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.score = 0;
                object.liqibang = 0;
                object.failed = false;
                object.liqi_type_beishuizhizhan = 0;
                object.xia_ke_shang = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.failed != null && message.hasOwnProperty("failed"))
                object.failed = message.failed;
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                object.liqi_type_beishuizhizhan = message.liqi_type_beishuizhizhan;
            if (message.xia_ke_shang != null && message.hasOwnProperty("xia_ke_shang"))
                object.xia_ke_shang = $root.lq.XiaKeShangInfo.toObject(message.xia_ke_shang, options);
            return object;
        };

        LiQiSuccess.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LiQiSuccess.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.LiQiSuccess";
        };

        return LiQiSuccess;
    })();

    lq.FanInfo = (function() {

        function FanInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        FanInfo.prototype.name = "";
        FanInfo.prototype.val = 0;
        FanInfo.prototype.id = 0;

        FanInfo.create = function create(properties) {
            return new FanInfo(properties);
        };

        FanInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(10).string(message.name);
            if (message.val != null && Object.hasOwnProperty.call(message, "val"))
                writer.uint32(16).uint32(message.val);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(24).uint32(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FanInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        FanInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.FanInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.val = value;
                        else
                            delete message.val;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        FanInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        FanInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.name != null && message.hasOwnProperty("name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.val != null && message.hasOwnProperty("val"))
                if (!$util.isInteger(message.val))
                    return "val: integer expected";
            if (message.id != null && message.hasOwnProperty("id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        FanInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.FanInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.FanInfo();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = String(object.name);
            if (object.val != null)
                if (Number(object.val) !== 0)
                    message.val = object.val >>> 0;
            if (object.id != null)
                if (Number(object.id) !== 0)
                    message.id = object.id >>> 0;
            return message;
        };

        FanInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.val = 0;
                object.id = 0;
            }
            if (message.name != null && message.hasOwnProperty("name"))
                object.name = message.name;
            if (message.val != null && message.hasOwnProperty("val"))
                object.val = message.val;
            if (message.id != null && message.hasOwnProperty("id"))
                object.id = message.id;
            return object;
        };

        FanInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FanInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.FanInfo";
        };

        return FanInfo;
    })();

    lq.HuleInfo = (function() {

        function HuleInfo(properties) {
            this.hand = [];
            this.ming = [];
            this.doras = [];
            this.li_doras = [];
            this.fans = [];
            this.baopai_seats = [];
            this.lines = [];
            this.baida_changed = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        HuleInfo.prototype.hand = $util.emptyArray;
        HuleInfo.prototype.ming = $util.emptyArray;
        HuleInfo.prototype.hu_tile = "";
        HuleInfo.prototype.seat = 0;
        HuleInfo.prototype.zimo = false;
        HuleInfo.prototype.qinjia = false;
        HuleInfo.prototype.liqi = false;
        HuleInfo.prototype.doras = $util.emptyArray;
        HuleInfo.prototype.li_doras = $util.emptyArray;
        HuleInfo.prototype.yiman = false;
        HuleInfo.prototype.count = 0;
        HuleInfo.prototype.fans = $util.emptyArray;
        HuleInfo.prototype.fu = 0;
        HuleInfo.prototype.title = "";
        HuleInfo.prototype.point_rong = 0;
        HuleInfo.prototype.point_zimo_qin = 0;
        HuleInfo.prototype.point_zimo_xian = 0;
        HuleInfo.prototype.title_id = 0;
        HuleInfo.prototype.point_sum = 0;
        HuleInfo.prototype.dadian = 0;
        HuleInfo.prototype.baopai = 0;
        HuleInfo.prototype.baopai_seats = $util.emptyArray;
        HuleInfo.prototype.lines = $util.emptyArray;
        HuleInfo.prototype.tianming_bonus = 0;
        HuleInfo.prototype.baida_changed = $util.emptyArray;
        HuleInfo.prototype.hu_tile_baiDa_changed = "";
        HuleInfo.prototype.xia_ke_shang_coefficient = 0;

        HuleInfo.create = function create(properties) {
            return new HuleInfo(properties);
        };

        HuleInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(10).string(message.hand[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(18).string(message.ming[i]);
            if (message.hu_tile != null && Object.hasOwnProperty.call(message, "hu_tile"))
                writer.uint32(26).string(message.hu_tile);
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(32).uint32(message.seat);
            if (message.zimo != null && Object.hasOwnProperty.call(message, "zimo"))
                writer.uint32(40).bool(message.zimo);
            if (message.qinjia != null && Object.hasOwnProperty.call(message, "qinjia"))
                writer.uint32(48).bool(message.qinjia);
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                writer.uint32(56).bool(message.liqi);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.li_doras != null && message.li_doras.length)
                for (let i = 0; i < message.li_doras.length; ++i)
                    writer.uint32(74).string(message.li_doras[i]);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(80).bool(message.yiman);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(88).uint32(message.count);
            if (message.fans != null && message.fans.length)
                for (let i = 0; i < message.fans.length; ++i)
                    $root.lq.FanInfo.encode(message.fans[i], writer.uint32(98).fork()).ldelim();
            if (message.fu != null && Object.hasOwnProperty.call(message, "fu"))
                writer.uint32(104).uint32(message.fu);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(114).string(message.title);
            if (message.point_rong != null && Object.hasOwnProperty.call(message, "point_rong"))
                writer.uint32(120).uint32(message.point_rong);
            if (message.point_zimo_qin != null && Object.hasOwnProperty.call(message, "point_zimo_qin"))
                writer.uint32(128).uint32(message.point_zimo_qin);
            if (message.point_zimo_xian != null && Object.hasOwnProperty.call(message, "point_zimo_xian"))
                writer.uint32(136).uint32(message.point_zimo_xian);
            if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                writer.uint32(144).uint32(message.title_id);
            if (message.point_sum != null && Object.hasOwnProperty.call(message, "point_sum"))
                writer.uint32(152).uint32(message.point_sum);
            if (message.dadian != null && Object.hasOwnProperty.call(message, "dadian"))
                writer.uint32(160).uint32(message.dadian);
            if (message.baopai != null && Object.hasOwnProperty.call(message, "baopai"))
                writer.uint32(168).uint32(message.baopai);
            if (message.baopai_seats != null && message.baopai_seats.length) {
                writer.uint32(178).fork();
                for (let i = 0; i < message.baopai_seats.length; ++i)
                    writer.uint32(message.baopai_seats[i]);
                writer.ldelim();
            }
            if (message.lines != null && message.lines.length)
                for (let i = 0; i < message.lines.length; ++i)
                    writer.uint32(186).string(message.lines[i]);
            if (message.tianming_bonus != null && Object.hasOwnProperty.call(message, "tianming_bonus"))
                writer.uint32(192).uint32(message.tianming_bonus);
            if (message.baida_changed != null && message.baida_changed.length)
                for (let i = 0; i < message.baida_changed.length; ++i)
                    writer.uint32(202).string(message.baida_changed[i]);
            if (message.hu_tile_baiDa_changed != null && Object.hasOwnProperty.call(message, "hu_tile_baiDa_changed"))
                writer.uint32(210).string(message.hu_tile_baiDa_changed);
            if (message.xia_ke_shang_coefficient != null && Object.hasOwnProperty.call(message, "xia_ke_shang_coefficient"))
                writer.uint32(216).uint32(message.xia_ke_shang_coefficient);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HuleInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        HuleInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.HuleInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hu_tile = value;
                        else
                            delete message.hu_tile;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zimo = value;
                        else
                            delete message.zimo;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.qinjia = value;
                        else
                            delete message.qinjia;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liqi = value;
                        else
                            delete message.liqi;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.li_doras && message.li_doras.length))
                            message.li_doras = [];
                        message.li_doras.push(reader.string());
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fans && message.fans.length))
                            message.fans = [];
                        message.fans.push($root.lq.FanInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 13: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu = value;
                        else
                            delete message.fu;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_rong = value;
                        else
                            delete message.point_rong;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_zimo_qin = value;
                        else
                            delete message.point_zimo_qin;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_zimo_xian = value;
                        else
                            delete message.point_zimo_xian;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title_id = value;
                        else
                            delete message.title_id;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.point_sum = value;
                        else
                            delete message.point_sum;
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dadian = value;
                        else
                            delete message.dadian;
                        continue;
                    }
                case 21: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.baopai = value;
                        else
                            delete message.baopai;
                        continue;
                    }
                case 22: {
                        if (wireType === 2) {
                            if (!(message.baopai_seats && message.baopai_seats.length))
                                message.baopai_seats = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.baopai_seats.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.baopai_seats && message.baopai_seats.length))
                            message.baopai_seats = [];
                        message.baopai_seats.push(reader.uint32());
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        if (!(message.lines && message.lines.length))
                            message.lines = [];
                        message.lines.push(reader.string());
                        continue;
                    }
                case 24: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tianming_bonus = value;
                        else
                            delete message.tianming_bonus;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        if (!(message.baida_changed && message.baida_changed.length))
                            message.baida_changed = [];
                        message.baida_changed.push(reader.string());
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hu_tile_baiDa_changed = value;
                        else
                            delete message.hu_tile_baiDa_changed;
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.xia_ke_shang_coefficient = value;
                        else
                            delete message.xia_ke_shang_coefficient;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        HuleInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        HuleInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                if (!$util.isString(message.hu_tile))
                    return "hu_tile: string expected";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                if (typeof message.zimo !== "boolean")
                    return "zimo: boolean expected";
            if (message.qinjia != null && message.hasOwnProperty("qinjia"))
                if (typeof message.qinjia !== "boolean")
                    return "qinjia: boolean expected";
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                if (typeof message.liqi !== "boolean")
                    return "liqi: boolean expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.li_doras != null && message.hasOwnProperty("li_doras")) {
                if (!Array.isArray(message.li_doras))
                    return "li_doras: array expected";
                for (let i = 0; i < message.li_doras.length; ++i)
                    if (!$util.isString(message.li_doras[i]))
                        return "li_doras: string[] expected";
            }
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.fans != null && message.hasOwnProperty("fans")) {
                if (!Array.isArray(message.fans))
                    return "fans: array expected";
                for (let i = 0; i < message.fans.length; ++i) {
                    let error = $root.lq.FanInfo.verify(message.fans[i], _depth + 1);
                    if (error)
                        return "fans." + error;
                }
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                if (!$util.isInteger(message.fu))
                    return "fu: integer expected";
            if (message.title != null && message.hasOwnProperty("title"))
                if (!$util.isString(message.title))
                    return "title: string expected";
            if (message.point_rong != null && message.hasOwnProperty("point_rong"))
                if (!$util.isInteger(message.point_rong))
                    return "point_rong: integer expected";
            if (message.point_zimo_qin != null && message.hasOwnProperty("point_zimo_qin"))
                if (!$util.isInteger(message.point_zimo_qin))
                    return "point_zimo_qin: integer expected";
            if (message.point_zimo_xian != null && message.hasOwnProperty("point_zimo_xian"))
                if (!$util.isInteger(message.point_zimo_xian))
                    return "point_zimo_xian: integer expected";
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                if (!$util.isInteger(message.title_id))
                    return "title_id: integer expected";
            if (message.point_sum != null && message.hasOwnProperty("point_sum"))
                if (!$util.isInteger(message.point_sum))
                    return "point_sum: integer expected";
            if (message.dadian != null && message.hasOwnProperty("dadian"))
                if (!$util.isInteger(message.dadian))
                    return "dadian: integer expected";
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                if (!$util.isInteger(message.baopai))
                    return "baopai: integer expected";
            if (message.baopai_seats != null && message.hasOwnProperty("baopai_seats")) {
                if (!Array.isArray(message.baopai_seats))
                    return "baopai_seats: array expected";
                for (let i = 0; i < message.baopai_seats.length; ++i)
                    if (!$util.isInteger(message.baopai_seats[i]))
                        return "baopai_seats: integer[] expected";
            }
            if (message.lines != null && message.hasOwnProperty("lines")) {
                if (!Array.isArray(message.lines))
                    return "lines: array expected";
                for (let i = 0; i < message.lines.length; ++i)
                    if (!$util.isString(message.lines[i]))
                        return "lines: string[] expected";
            }
            if (message.tianming_bonus != null && message.hasOwnProperty("tianming_bonus"))
                if (!$util.isInteger(message.tianming_bonus))
                    return "tianming_bonus: integer expected";
            if (message.baida_changed != null && message.hasOwnProperty("baida_changed")) {
                if (!Array.isArray(message.baida_changed))
                    return "baida_changed: array expected";
                for (let i = 0; i < message.baida_changed.length; ++i)
                    if (!$util.isString(message.baida_changed[i]))
                        return "baida_changed: string[] expected";
            }
            if (message.hu_tile_baiDa_changed != null && message.hasOwnProperty("hu_tile_baiDa_changed"))
                if (!$util.isString(message.hu_tile_baiDa_changed))
                    return "hu_tile_baiDa_changed: string expected";
            if (message.xia_ke_shang_coefficient != null && message.hasOwnProperty("xia_ke_shang_coefficient"))
                if (!$util.isInteger(message.xia_ke_shang_coefficient))
                    return "xia_ke_shang_coefficient: integer expected";
            return null;
        };

        HuleInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.HuleInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.HuleInfo();
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.HuleInfo.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.HuleInfo.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.hu_tile != null)
                if (typeof object.hu_tile !== "string" || object.hu_tile.length)
                    message.hu_tile = String(object.hu_tile);
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.zimo != null)
                if (object.zimo)
                    message.zimo = Boolean(object.zimo);
            if (object.qinjia != null)
                if (object.qinjia)
                    message.qinjia = Boolean(object.qinjia);
            if (object.liqi != null)
                if (object.liqi)
                    message.liqi = Boolean(object.liqi);
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.HuleInfo.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.li_doras) {
                if (!Array.isArray(object.li_doras))
                    throw TypeError(".lq.HuleInfo.li_doras: array expected");
                message.li_doras = Array(object.li_doras.length);
                for (let i = 0; i < object.li_doras.length; ++i)
                    message.li_doras[i] = String(object.li_doras[i]);
            }
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.fans) {
                if (!Array.isArray(object.fans))
                    throw TypeError(".lq.HuleInfo.fans: array expected");
                message.fans = Array(object.fans.length);
                for (let i = 0; i < object.fans.length; ++i) {
                    if (typeof object.fans[i] !== "object")
                        throw TypeError(".lq.HuleInfo.fans: object expected");
                    message.fans[i] = $root.lq.FanInfo.fromObject(object.fans[i], _depth + 1);
                }
            }
            if (object.fu != null)
                if (Number(object.fu) !== 0)
                    message.fu = object.fu >>> 0;
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = String(object.title);
            if (object.point_rong != null)
                if (Number(object.point_rong) !== 0)
                    message.point_rong = object.point_rong >>> 0;
            if (object.point_zimo_qin != null)
                if (Number(object.point_zimo_qin) !== 0)
                    message.point_zimo_qin = object.point_zimo_qin >>> 0;
            if (object.point_zimo_xian != null)
                if (Number(object.point_zimo_xian) !== 0)
                    message.point_zimo_xian = object.point_zimo_xian >>> 0;
            if (object.title_id != null)
                if (Number(object.title_id) !== 0)
                    message.title_id = object.title_id >>> 0;
            if (object.point_sum != null)
                if (Number(object.point_sum) !== 0)
                    message.point_sum = object.point_sum >>> 0;
            if (object.dadian != null)
                if (Number(object.dadian) !== 0)
                    message.dadian = object.dadian >>> 0;
            if (object.baopai != null)
                if (Number(object.baopai) !== 0)
                    message.baopai = object.baopai >>> 0;
            if (object.baopai_seats) {
                if (!Array.isArray(object.baopai_seats))
                    throw TypeError(".lq.HuleInfo.baopai_seats: array expected");
                message.baopai_seats = Array(object.baopai_seats.length);
                for (let i = 0; i < object.baopai_seats.length; ++i)
                    message.baopai_seats[i] = object.baopai_seats[i] >>> 0;
            }
            if (object.lines) {
                if (!Array.isArray(object.lines))
                    throw TypeError(".lq.HuleInfo.lines: array expected");
                message.lines = Array(object.lines.length);
                for (let i = 0; i < object.lines.length; ++i)
                    message.lines[i] = String(object.lines[i]);
            }
            if (object.tianming_bonus != null)
                if (Number(object.tianming_bonus) !== 0)
                    message.tianming_bonus = object.tianming_bonus >>> 0;
            if (object.baida_changed) {
                if (!Array.isArray(object.baida_changed))
                    throw TypeError(".lq.HuleInfo.baida_changed: array expected");
                message.baida_changed = Array(object.baida_changed.length);
                for (let i = 0; i < object.baida_changed.length; ++i)
                    message.baida_changed[i] = String(object.baida_changed[i]);
            }
            if (object.hu_tile_baiDa_changed != null)
                if (typeof object.hu_tile_baiDa_changed !== "string" || object.hu_tile_baiDa_changed.length)
                    message.hu_tile_baiDa_changed = String(object.hu_tile_baiDa_changed);
            if (object.xia_ke_shang_coefficient != null)
                if (Number(object.xia_ke_shang_coefficient) !== 0)
                    message.xia_ke_shang_coefficient = object.xia_ke_shang_coefficient >>> 0;
            return message;
        };

        HuleInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hand = [];
                object.ming = [];
                object.doras = [];
                object.li_doras = [];
                object.fans = [];
                object.baopai_seats = [];
                object.lines = [];
                object.baida_changed = [];
            }
            if (options.defaults) {
                object.hu_tile = "";
                object.seat = 0;
                object.zimo = false;
                object.qinjia = false;
                object.liqi = false;
                object.yiman = false;
                object.count = 0;
                object.fu = 0;
                object.title = "";
                object.point_rong = 0;
                object.point_zimo_qin = 0;
                object.point_zimo_xian = 0;
                object.title_id = 0;
                object.point_sum = 0;
                object.dadian = 0;
                object.baopai = 0;
                object.tianming_bonus = 0;
                object.hu_tile_baiDa_changed = "";
                object.xia_ke_shang_coefficient = 0;
            }
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                object.hu_tile = message.hu_tile;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                object.zimo = message.zimo;
            if (message.qinjia != null && message.hasOwnProperty("qinjia"))
                object.qinjia = message.qinjia;
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = message.liqi;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.li_doras && message.li_doras.length) {
                object.li_doras = Array(message.li_doras.length);
                for (let j = 0; j < message.li_doras.length; ++j)
                    object.li_doras[j] = message.li_doras[j];
            }
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.fans && message.fans.length) {
                object.fans = Array(message.fans.length);
                for (let j = 0; j < message.fans.length; ++j)
                    object.fans[j] = $root.lq.FanInfo.toObject(message.fans[j], options);
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                object.fu = message.fu;
            if (message.title != null && message.hasOwnProperty("title"))
                object.title = message.title;
            if (message.point_rong != null && message.hasOwnProperty("point_rong"))
                object.point_rong = message.point_rong;
            if (message.point_zimo_qin != null && message.hasOwnProperty("point_zimo_qin"))
                object.point_zimo_qin = message.point_zimo_qin;
            if (message.point_zimo_xian != null && message.hasOwnProperty("point_zimo_xian"))
                object.point_zimo_xian = message.point_zimo_xian;
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                object.title_id = message.title_id;
            if (message.point_sum != null && message.hasOwnProperty("point_sum"))
                object.point_sum = message.point_sum;
            if (message.dadian != null && message.hasOwnProperty("dadian"))
                object.dadian = message.dadian;
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                object.baopai = message.baopai;
            if (message.baopai_seats && message.baopai_seats.length) {
                object.baopai_seats = Array(message.baopai_seats.length);
                for (let j = 0; j < message.baopai_seats.length; ++j)
                    object.baopai_seats[j] = message.baopai_seats[j];
            }
            if (message.lines && message.lines.length) {
                object.lines = Array(message.lines.length);
                for (let j = 0; j < message.lines.length; ++j)
                    object.lines[j] = message.lines[j];
            }
            if (message.tianming_bonus != null && message.hasOwnProperty("tianming_bonus"))
                object.tianming_bonus = message.tianming_bonus;
            if (message.baida_changed && message.baida_changed.length) {
                object.baida_changed = Array(message.baida_changed.length);
                for (let j = 0; j < message.baida_changed.length; ++j)
                    object.baida_changed[j] = message.baida_changed[j];
            }
            if (message.hu_tile_baiDa_changed != null && message.hasOwnProperty("hu_tile_baiDa_changed"))
                object.hu_tile_baiDa_changed = message.hu_tile_baiDa_changed;
            if (message.xia_ke_shang_coefficient != null && message.hasOwnProperty("xia_ke_shang_coefficient"))
                object.xia_ke_shang_coefficient = message.xia_ke_shang_coefficient;
            return object;
        };

        HuleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HuleInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.HuleInfo";
        };

        return HuleInfo;
    })();

    lq.TingPaiInfo = (function() {

        function TingPaiInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TingPaiInfo.prototype.tile = "";
        TingPaiInfo.prototype.haveyi = false;
        TingPaiInfo.prototype.yiman = false;
        TingPaiInfo.prototype.count = 0;
        TingPaiInfo.prototype.fu = 0;
        TingPaiInfo.prototype.biao_dora_count = 0;
        TingPaiInfo.prototype.yiman_zimo = false;
        TingPaiInfo.prototype.count_zimo = 0;
        TingPaiInfo.prototype.fu_zimo = 0;

        TingPaiInfo.create = function create(properties) {
            return new TingPaiInfo(properties);
        };

        TingPaiInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(10).string(message.tile);
            if (message.haveyi != null && Object.hasOwnProperty.call(message, "haveyi"))
                writer.uint32(16).bool(message.haveyi);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(24).bool(message.yiman);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(32).uint32(message.count);
            if (message.fu != null && Object.hasOwnProperty.call(message, "fu"))
                writer.uint32(40).uint32(message.fu);
            if (message.biao_dora_count != null && Object.hasOwnProperty.call(message, "biao_dora_count"))
                writer.uint32(48).uint32(message.biao_dora_count);
            if (message.yiman_zimo != null && Object.hasOwnProperty.call(message, "yiman_zimo"))
                writer.uint32(56).bool(message.yiman_zimo);
            if (message.count_zimo != null && Object.hasOwnProperty.call(message, "count_zimo"))
                writer.uint32(64).uint32(message.count_zimo);
            if (message.fu_zimo != null && Object.hasOwnProperty.call(message, "fu_zimo"))
                writer.uint32(72).uint32(message.fu_zimo);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TingPaiInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TingPaiInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TingPaiInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.haveyi = value;
                        else
                            delete message.haveyi;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu = value;
                        else
                            delete message.fu;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.biao_dora_count = value;
                        else
                            delete message.biao_dora_count;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman_zimo = value;
                        else
                            delete message.yiman_zimo;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count_zimo = value;
                        else
                            delete message.count_zimo;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu_zimo = value;
                        else
                            delete message.fu_zimo;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TingPaiInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TingPaiInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.haveyi != null && message.hasOwnProperty("haveyi"))
                if (typeof message.haveyi !== "boolean")
                    return "haveyi: boolean expected";
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.fu != null && message.hasOwnProperty("fu"))
                if (!$util.isInteger(message.fu))
                    return "fu: integer expected";
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                if (!$util.isInteger(message.biao_dora_count))
                    return "biao_dora_count: integer expected";
            if (message.yiman_zimo != null && message.hasOwnProperty("yiman_zimo"))
                if (typeof message.yiman_zimo !== "boolean")
                    return "yiman_zimo: boolean expected";
            if (message.count_zimo != null && message.hasOwnProperty("count_zimo"))
                if (!$util.isInteger(message.count_zimo))
                    return "count_zimo: integer expected";
            if (message.fu_zimo != null && message.hasOwnProperty("fu_zimo"))
                if (!$util.isInteger(message.fu_zimo))
                    return "fu_zimo: integer expected";
            return null;
        };

        TingPaiInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TingPaiInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TingPaiInfo();
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.haveyi != null)
                if (object.haveyi)
                    message.haveyi = Boolean(object.haveyi);
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.fu != null)
                if (Number(object.fu) !== 0)
                    message.fu = object.fu >>> 0;
            if (object.biao_dora_count != null)
                if (Number(object.biao_dora_count) !== 0)
                    message.biao_dora_count = object.biao_dora_count >>> 0;
            if (object.yiman_zimo != null)
                if (object.yiman_zimo)
                    message.yiman_zimo = Boolean(object.yiman_zimo);
            if (object.count_zimo != null)
                if (Number(object.count_zimo) !== 0)
                    message.count_zimo = object.count_zimo >>> 0;
            if (object.fu_zimo != null)
                if (Number(object.fu_zimo) !== 0)
                    message.fu_zimo = object.fu_zimo >>> 0;
            return message;
        };

        TingPaiInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.tile = "";
                object.haveyi = false;
                object.yiman = false;
                object.count = 0;
                object.fu = 0;
                object.biao_dora_count = 0;
                object.yiman_zimo = false;
                object.count_zimo = 0;
                object.fu_zimo = 0;
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.haveyi != null && message.hasOwnProperty("haveyi"))
                object.haveyi = message.haveyi;
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.fu != null && message.hasOwnProperty("fu"))
                object.fu = message.fu;
            if (message.biao_dora_count != null && message.hasOwnProperty("biao_dora_count"))
                object.biao_dora_count = message.biao_dora_count;
            if (message.yiman_zimo != null && message.hasOwnProperty("yiman_zimo"))
                object.yiman_zimo = message.yiman_zimo;
            if (message.count_zimo != null && message.hasOwnProperty("count_zimo"))
                object.count_zimo = message.count_zimo;
            if (message.fu_zimo != null && message.hasOwnProperty("fu_zimo"))
                object.fu_zimo = message.fu_zimo;
            return object;
        };

        TingPaiInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TingPaiInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TingPaiInfo";
        };

        return TingPaiInfo;
    })();

    lq.TingPaiDiscardInfo = (function() {

        function TingPaiDiscardInfo(properties) {
            this.infos = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TingPaiDiscardInfo.prototype.tile = "";
        TingPaiDiscardInfo.prototype.zhenting = false;
        TingPaiDiscardInfo.prototype.infos = $util.emptyArray;

        TingPaiDiscardInfo.create = function create(properties) {
            return new TingPaiDiscardInfo(properties);
        };

        TingPaiDiscardInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(10).string(message.tile);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(16).bool(message.zhenting);
            if (message.infos != null && message.infos.length)
                for (let i = 0; i < message.infos.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.infos[i], writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TingPaiDiscardInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TingPaiDiscardInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TingPaiDiscardInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.infos && message.infos.length))
                            message.infos = [];
                        message.infos.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TingPaiDiscardInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TingPaiDiscardInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.infos != null && message.hasOwnProperty("infos")) {
                if (!Array.isArray(message.infos))
                    return "infos: array expected";
                for (let i = 0; i < message.infos.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.infos[i], _depth + 1);
                    if (error)
                        return "infos." + error;
                }
            }
            return null;
        };

        TingPaiDiscardInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TingPaiDiscardInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TingPaiDiscardInfo();
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.infos) {
                if (!Array.isArray(object.infos))
                    throw TypeError(".lq.TingPaiDiscardInfo.infos: array expected");
                message.infos = Array(object.infos.length);
                for (let i = 0; i < object.infos.length; ++i) {
                    if (typeof object.infos[i] !== "object")
                        throw TypeError(".lq.TingPaiDiscardInfo.infos: object expected");
                    message.infos[i] = $root.lq.TingPaiInfo.fromObject(object.infos[i], _depth + 1);
                }
            }
            return message;
        };

        TingPaiDiscardInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.infos = [];
            if (options.defaults) {
                object.tile = "";
                object.zhenting = false;
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.infos && message.infos.length) {
                object.infos = Array(message.infos.length);
                for (let j = 0; j < message.infos.length; ++j)
                    object.infos[j] = $root.lq.TingPaiInfo.toObject(message.infos[j], options);
            }
            return object;
        };

        TingPaiDiscardInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TingPaiDiscardInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TingPaiDiscardInfo";
        };

        return TingPaiDiscardInfo;
    })();

    lq.HunZhiYiJiBuffInfo = (function() {

        function HunZhiYiJiBuffInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        HunZhiYiJiBuffInfo.prototype.seat = 0;
        HunZhiYiJiBuffInfo.prototype.continue_deal_count = 0;
        HunZhiYiJiBuffInfo.prototype.overload = false;

        HunZhiYiJiBuffInfo.create = function create(properties) {
            return new HunZhiYiJiBuffInfo(properties);
        };

        HunZhiYiJiBuffInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.continue_deal_count != null && Object.hasOwnProperty.call(message, "continue_deal_count"))
                writer.uint32(16).uint32(message.continue_deal_count);
            if (message.overload != null && Object.hasOwnProperty.call(message, "overload"))
                writer.uint32(24).bool(message.overload);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HunZhiYiJiBuffInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        HunZhiYiJiBuffInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.HunZhiYiJiBuffInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.continue_deal_count = value;
                        else
                            delete message.continue_deal_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.overload = value;
                        else
                            delete message.overload;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        HunZhiYiJiBuffInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        HunZhiYiJiBuffInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.continue_deal_count != null && message.hasOwnProperty("continue_deal_count"))
                if (!$util.isInteger(message.continue_deal_count))
                    return "continue_deal_count: integer expected";
            if (message.overload != null && message.hasOwnProperty("overload"))
                if (typeof message.overload !== "boolean")
                    return "overload: boolean expected";
            return null;
        };

        HunZhiYiJiBuffInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.HunZhiYiJiBuffInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.HunZhiYiJiBuffInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.continue_deal_count != null)
                if (Number(object.continue_deal_count) !== 0)
                    message.continue_deal_count = object.continue_deal_count >>> 0;
            if (object.overload != null)
                if (object.overload)
                    message.overload = Boolean(object.overload);
            return message;
        };

        HunZhiYiJiBuffInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.seat = 0;
                object.continue_deal_count = 0;
                object.overload = false;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.continue_deal_count != null && message.hasOwnProperty("continue_deal_count"))
                object.continue_deal_count = message.continue_deal_count;
            if (message.overload != null && message.hasOwnProperty("overload"))
                object.overload = message.overload;
            return object;
        };

        HunZhiYiJiBuffInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HunZhiYiJiBuffInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.HunZhiYiJiBuffInfo";
        };

        return HunZhiYiJiBuffInfo;
    })();

    lq.GameEnd = (function() {

        function GameEnd(properties) {
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        GameEnd.prototype.scores = $util.emptyArray;

        GameEnd.create = function create(properties) {
            return new GameEnd(properties);
        };

        GameEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.scores != null && message.scores.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        GameEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        GameEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.GameEnd();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        GameEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        GameEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            return null;
        };

        GameEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.GameEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.GameEnd();
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.GameEnd.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            return message;
        };

        GameEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.scores = [];
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            return object;
        };

        GameEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GameEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.GameEnd";
        };

        return GameEnd;
    })();

    lq.ActionSelectGap = (function() {

        function ActionSelectGap(properties) {
            this.gap_types = [];
            this.tingpais0 = [];
            this.tingpais1 = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionSelectGap.prototype.gap_types = $util.emptyArray;
        ActionSelectGap.prototype.tingpais0 = $util.emptyArray;
        ActionSelectGap.prototype.tingpais1 = $util.emptyArray;
        ActionSelectGap.prototype.operation = null;

        ActionSelectGap.create = function create(properties) {
            return new ActionSelectGap(properties);
        };

        ActionSelectGap.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gap_types != null && message.gap_types.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.gap_types.length; ++i)
                    writer.uint32(message.gap_types[i]);
                writer.ldelim();
            }
            if (message.tingpais0 != null && message.tingpais0.length)
                for (let i = 0; i < message.tingpais0.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais0[i], writer.uint32(18).fork()).ldelim();
            if (message.tingpais1 != null && message.tingpais1.length)
                for (let i = 0; i < message.tingpais1.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(26).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionSelectGap.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionSelectGap.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionSelectGap(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.gap_types && message.gap_types.length))
                                message.gap_types = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gap_types.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.gap_types && message.gap_types.length))
                            message.gap_types = [];
                        message.gap_types.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais0 && message.tingpais0.length))
                            message.tingpais0 = [];
                        message.tingpais0.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais1 && message.tingpais1.length))
                            message.tingpais1 = [];
                        message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionSelectGap.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionSelectGap.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gap_types != null && message.hasOwnProperty("gap_types")) {
                if (!Array.isArray(message.gap_types))
                    return "gap_types: array expected";
                for (let i = 0; i < message.gap_types.length; ++i)
                    if (!$util.isInteger(message.gap_types[i]))
                        return "gap_types: integer[] expected";
            }
            if (message.tingpais0 != null && message.hasOwnProperty("tingpais0")) {
                if (!Array.isArray(message.tingpais0))
                    return "tingpais0: array expected";
                for (let i = 0; i < message.tingpais0.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais0[i], _depth + 1);
                    if (error)
                        return "tingpais0." + error;
                }
            }
            if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                if (!Array.isArray(message.tingpais1))
                    return "tingpais1: array expected";
                for (let i = 0; i < message.tingpais1.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                    if (error)
                        return "tingpais1." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        ActionSelectGap.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionSelectGap)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionSelectGap();
            if (object.gap_types) {
                if (!Array.isArray(object.gap_types))
                    throw TypeError(".lq.ActionSelectGap.gap_types: array expected");
                message.gap_types = Array(object.gap_types.length);
                for (let i = 0; i < object.gap_types.length; ++i)
                    message.gap_types[i] = object.gap_types[i] >>> 0;
            }
            if (object.tingpais0) {
                if (!Array.isArray(object.tingpais0))
                    throw TypeError(".lq.ActionSelectGap.tingpais0: array expected");
                message.tingpais0 = Array(object.tingpais0.length);
                for (let i = 0; i < object.tingpais0.length; ++i) {
                    if (typeof object.tingpais0[i] !== "object")
                        throw TypeError(".lq.ActionSelectGap.tingpais0: object expected");
                    message.tingpais0[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais0[i], _depth + 1);
                }
            }
            if (object.tingpais1) {
                if (!Array.isArray(object.tingpais1))
                    throw TypeError(".lq.ActionSelectGap.tingpais1: array expected");
                message.tingpais1 = Array(object.tingpais1.length);
                for (let i = 0; i < object.tingpais1.length; ++i) {
                    if (typeof object.tingpais1[i] !== "object")
                        throw TypeError(".lq.ActionSelectGap.tingpais1: object expected");
                    message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionSelectGap.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        ActionSelectGap.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.gap_types = [];
                object.tingpais0 = [];
                object.tingpais1 = [];
            }
            if (options.defaults)
                object.operation = null;
            if (message.gap_types && message.gap_types.length) {
                object.gap_types = Array(message.gap_types.length);
                for (let j = 0; j < message.gap_types.length; ++j)
                    object.gap_types[j] = message.gap_types[j];
            }
            if (message.tingpais0 && message.tingpais0.length) {
                object.tingpais0 = Array(message.tingpais0.length);
                for (let j = 0; j < message.tingpais0.length; ++j)
                    object.tingpais0[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais0[j], options);
            }
            if (message.tingpais1 && message.tingpais1.length) {
                object.tingpais1 = Array(message.tingpais1.length);
                for (let j = 0; j < message.tingpais1.length; ++j)
                    object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        ActionSelectGap.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionSelectGap.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionSelectGap";
        };

        return ActionSelectGap;
    })();

    lq.RecordSelectGap = (function() {

        function RecordSelectGap(properties) {
            this.gap_types = [];
            this.tingpai = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordSelectGap.prototype.gap_types = $util.emptyArray;
        RecordSelectGap.prototype.tingpai = $util.emptyArray;
        RecordSelectGap.prototype.operation = null;

        RecordSelectGap.create = function create(properties) {
            return new RecordSelectGap(properties);
        };

        RecordSelectGap.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gap_types != null && message.gap_types.length) {
                writer.uint32(10).fork();
                for (let i = 0; i < message.gap_types.length; ++i)
                    writer.uint32(message.gap_types[i]);
                writer.ldelim();
            }
            if (message.tingpai != null && message.tingpai.length)
                for (let i = 0; i < message.tingpai.length; ++i)
                    $root.lq.RecordSelectGap.TingPai.encode(message.tingpai[i], writer.uint32(18).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(26).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordSelectGap.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordSelectGap.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordSelectGap(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.gap_types && message.gap_types.length))
                                message.gap_types = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.gap_types.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.gap_types && message.gap_types.length))
                            message.gap_types = [];
                        message.gap_types.push(reader.uint32());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpai && message.tingpai.length))
                            message.tingpai = [];
                        message.tingpai.push($root.lq.RecordSelectGap.TingPai.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordSelectGap.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordSelectGap.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gap_types != null && message.hasOwnProperty("gap_types")) {
                if (!Array.isArray(message.gap_types))
                    return "gap_types: array expected";
                for (let i = 0; i < message.gap_types.length; ++i)
                    if (!$util.isInteger(message.gap_types[i]))
                        return "gap_types: integer[] expected";
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai")) {
                if (!Array.isArray(message.tingpai))
                    return "tingpai: array expected";
                for (let i = 0; i < message.tingpai.length; ++i) {
                    let error = $root.lq.RecordSelectGap.TingPai.verify(message.tingpai[i], _depth + 1);
                    if (error)
                        return "tingpai." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        RecordSelectGap.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordSelectGap)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordSelectGap();
            if (object.gap_types) {
                if (!Array.isArray(object.gap_types))
                    throw TypeError(".lq.RecordSelectGap.gap_types: array expected");
                message.gap_types = Array(object.gap_types.length);
                for (let i = 0; i < object.gap_types.length; ++i)
                    message.gap_types[i] = object.gap_types[i] >>> 0;
            }
            if (object.tingpai) {
                if (!Array.isArray(object.tingpai))
                    throw TypeError(".lq.RecordSelectGap.tingpai: array expected");
                message.tingpai = Array(object.tingpai.length);
                for (let i = 0; i < object.tingpai.length; ++i) {
                    if (typeof object.tingpai[i] !== "object")
                        throw TypeError(".lq.RecordSelectGap.tingpai: object expected");
                    message.tingpai[i] = $root.lq.RecordSelectGap.TingPai.fromObject(object.tingpai[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordSelectGap.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        RecordSelectGap.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.gap_types = [];
                object.tingpai = [];
            }
            if (options.defaults)
                object.operation = null;
            if (message.gap_types && message.gap_types.length) {
                object.gap_types = Array(message.gap_types.length);
                for (let j = 0; j < message.gap_types.length; ++j)
                    object.gap_types[j] = message.gap_types[j];
            }
            if (message.tingpai && message.tingpai.length) {
                object.tingpai = Array(message.tingpai.length);
                for (let j = 0; j < message.tingpai.length; ++j)
                    object.tingpai[j] = $root.lq.RecordSelectGap.TingPai.toObject(message.tingpai[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        RecordSelectGap.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordSelectGap.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordSelectGap";
        };

        RecordSelectGap.TingPai = (function() {

            function TingPai(properties) {
                this.tingpais1 = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TingPai.prototype.seat = 0;
            TingPai.prototype.tingpais1 = $util.emptyArray;

            TingPai.create = function create(properties) {
                return new TingPai(properties);
            };

            TingPai.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.tingpais1 != null && message.tingpais1.length)
                    for (let i = 0; i < message.tingpais1.length; ++i)
                        $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TingPai.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TingPai.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordSelectGap.TingPai(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tingpais1 && message.tingpais1.length))
                                message.tingpais1 = [];
                            message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TingPai.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TingPai.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                    if (!Array.isArray(message.tingpais1))
                        return "tingpais1: array expected";
                    for (let i = 0; i < message.tingpais1.length; ++i) {
                        let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                        if (error)
                            return "tingpais1." + error;
                    }
                }
                return null;
            };

            TingPai.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordSelectGap.TingPai)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordSelectGap.TingPai();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.tingpais1) {
                    if (!Array.isArray(object.tingpais1))
                        throw TypeError(".lq.RecordSelectGap.TingPai.tingpais1: array expected");
                    message.tingpais1 = Array(object.tingpais1.length);
                    for (let i = 0; i < object.tingpais1.length; ++i) {
                        if (typeof object.tingpais1[i] !== "object")
                            throw TypeError(".lq.RecordSelectGap.TingPai.tingpais1: object expected");
                        message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                    }
                }
                return message;
            };

            TingPai.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tingpais1 = [];
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.tingpais1 && message.tingpais1.length) {
                    object.tingpais1 = Array(message.tingpais1.length);
                    for (let j = 0; j < message.tingpais1.length; ++j)
                        object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
                }
                return object;
            };

            TingPai.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TingPai.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordSelectGap.TingPai";
            };

            return TingPai;
        })();

        return RecordSelectGap;
    })();

    lq.ActionChangeTile = (function() {

        function ActionChangeTile(properties) {
            this.in_tiles = [];
            this.in_tile_states = [];
            this.out_tiles = [];
            this.out_tile_states = [];
            this.doras = [];
            this.tingpais0 = [];
            this.tingpais1 = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionChangeTile.prototype.in_tiles = $util.emptyArray;
        ActionChangeTile.prototype.in_tile_states = $util.emptyArray;
        ActionChangeTile.prototype.out_tiles = $util.emptyArray;
        ActionChangeTile.prototype.out_tile_states = $util.emptyArray;
        ActionChangeTile.prototype.doras = $util.emptyArray;
        ActionChangeTile.prototype.tingpais0 = $util.emptyArray;
        ActionChangeTile.prototype.tingpais1 = $util.emptyArray;
        ActionChangeTile.prototype.operation = null;
        ActionChangeTile.prototype.change_type = 0;

        ActionChangeTile.create = function create(properties) {
            return new ActionChangeTile(properties);
        };

        ActionChangeTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.in_tiles != null && message.in_tiles.length)
                for (let i = 0; i < message.in_tiles.length; ++i)
                    writer.uint32(10).string(message.in_tiles[i]);
            if (message.in_tile_states != null && message.in_tile_states.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.in_tile_states.length; ++i)
                    writer.int32(message.in_tile_states[i]);
                writer.ldelim();
            }
            if (message.out_tiles != null && message.out_tiles.length)
                for (let i = 0; i < message.out_tiles.length; ++i)
                    writer.uint32(26).string(message.out_tiles[i]);
            if (message.out_tile_states != null && message.out_tile_states.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.out_tile_states.length; ++i)
                    writer.int32(message.out_tile_states[i]);
                writer.ldelim();
            }
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(42).string(message.doras[i]);
            if (message.tingpais0 != null && message.tingpais0.length)
                for (let i = 0; i < message.tingpais0.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais0[i], writer.uint32(50).fork()).ldelim();
            if (message.tingpais1 != null && message.tingpais1.length)
                for (let i = 0; i < message.tingpais1.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(58).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(66).fork()).ldelim();
            if (message.change_type != null && Object.hasOwnProperty.call(message, "change_type"))
                writer.uint32(72).uint32(message.change_type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionChangeTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionChangeTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionChangeTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.in_tiles && message.in_tiles.length))
                            message.in_tiles = [];
                        message.in_tiles.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.in_tile_states && message.in_tile_states.length))
                                message.in_tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.in_tile_states.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.in_tile_states && message.in_tile_states.length))
                            message.in_tile_states = [];
                        message.in_tile_states.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.out_tiles && message.out_tiles.length))
                            message.out_tiles = [];
                        message.out_tiles.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.out_tile_states && message.out_tile_states.length))
                                message.out_tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.out_tile_states.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.out_tile_states && message.out_tile_states.length))
                            message.out_tile_states = [];
                        message.out_tile_states.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais0 && message.tingpais0.length))
                            message.tingpais0 = [];
                        message.tingpais0.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais1 && message.tingpais1.length))
                            message.tingpais1 = [];
                        message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.change_type = value;
                        else
                            delete message.change_type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionChangeTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionChangeTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.in_tiles != null && message.hasOwnProperty("in_tiles")) {
                if (!Array.isArray(message.in_tiles))
                    return "in_tiles: array expected";
                for (let i = 0; i < message.in_tiles.length; ++i)
                    if (!$util.isString(message.in_tiles[i]))
                        return "in_tiles: string[] expected";
            }
            if (message.in_tile_states != null && message.hasOwnProperty("in_tile_states")) {
                if (!Array.isArray(message.in_tile_states))
                    return "in_tile_states: array expected";
                for (let i = 0; i < message.in_tile_states.length; ++i)
                    if (!$util.isInteger(message.in_tile_states[i]))
                        return "in_tile_states: integer[] expected";
            }
            if (message.out_tiles != null && message.hasOwnProperty("out_tiles")) {
                if (!Array.isArray(message.out_tiles))
                    return "out_tiles: array expected";
                for (let i = 0; i < message.out_tiles.length; ++i)
                    if (!$util.isString(message.out_tiles[i]))
                        return "out_tiles: string[] expected";
            }
            if (message.out_tile_states != null && message.hasOwnProperty("out_tile_states")) {
                if (!Array.isArray(message.out_tile_states))
                    return "out_tile_states: array expected";
                for (let i = 0; i < message.out_tile_states.length; ++i)
                    if (!$util.isInteger(message.out_tile_states[i]))
                        return "out_tile_states: integer[] expected";
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.tingpais0 != null && message.hasOwnProperty("tingpais0")) {
                if (!Array.isArray(message.tingpais0))
                    return "tingpais0: array expected";
                for (let i = 0; i < message.tingpais0.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais0[i], _depth + 1);
                    if (error)
                        return "tingpais0." + error;
                }
            }
            if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                if (!Array.isArray(message.tingpais1))
                    return "tingpais1: array expected";
                for (let i = 0; i < message.tingpais1.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                    if (error)
                        return "tingpais1." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.change_type != null && message.hasOwnProperty("change_type"))
                if (!$util.isInteger(message.change_type))
                    return "change_type: integer expected";
            return null;
        };

        ActionChangeTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionChangeTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionChangeTile();
            if (object.in_tiles) {
                if (!Array.isArray(object.in_tiles))
                    throw TypeError(".lq.ActionChangeTile.in_tiles: array expected");
                message.in_tiles = Array(object.in_tiles.length);
                for (let i = 0; i < object.in_tiles.length; ++i)
                    message.in_tiles[i] = String(object.in_tiles[i]);
            }
            if (object.in_tile_states) {
                if (!Array.isArray(object.in_tile_states))
                    throw TypeError(".lq.ActionChangeTile.in_tile_states: array expected");
                message.in_tile_states = Array(object.in_tile_states.length);
                for (let i = 0; i < object.in_tile_states.length; ++i)
                    message.in_tile_states[i] = object.in_tile_states[i] | 0;
            }
            if (object.out_tiles) {
                if (!Array.isArray(object.out_tiles))
                    throw TypeError(".lq.ActionChangeTile.out_tiles: array expected");
                message.out_tiles = Array(object.out_tiles.length);
                for (let i = 0; i < object.out_tiles.length; ++i)
                    message.out_tiles[i] = String(object.out_tiles[i]);
            }
            if (object.out_tile_states) {
                if (!Array.isArray(object.out_tile_states))
                    throw TypeError(".lq.ActionChangeTile.out_tile_states: array expected");
                message.out_tile_states = Array(object.out_tile_states.length);
                for (let i = 0; i < object.out_tile_states.length; ++i)
                    message.out_tile_states[i] = object.out_tile_states[i] | 0;
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionChangeTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.tingpais0) {
                if (!Array.isArray(object.tingpais0))
                    throw TypeError(".lq.ActionChangeTile.tingpais0: array expected");
                message.tingpais0 = Array(object.tingpais0.length);
                for (let i = 0; i < object.tingpais0.length; ++i) {
                    if (typeof object.tingpais0[i] !== "object")
                        throw TypeError(".lq.ActionChangeTile.tingpais0: object expected");
                    message.tingpais0[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais0[i], _depth + 1);
                }
            }
            if (object.tingpais1) {
                if (!Array.isArray(object.tingpais1))
                    throw TypeError(".lq.ActionChangeTile.tingpais1: array expected");
                message.tingpais1 = Array(object.tingpais1.length);
                for (let i = 0; i < object.tingpais1.length; ++i) {
                    if (typeof object.tingpais1[i] !== "object")
                        throw TypeError(".lq.ActionChangeTile.tingpais1: object expected");
                    message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionChangeTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.change_type != null)
                if (Number(object.change_type) !== 0)
                    message.change_type = object.change_type >>> 0;
            return message;
        };

        ActionChangeTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.in_tiles = [];
                object.in_tile_states = [];
                object.out_tiles = [];
                object.out_tile_states = [];
                object.doras = [];
                object.tingpais0 = [];
                object.tingpais1 = [];
            }
            if (options.defaults) {
                object.operation = null;
                object.change_type = 0;
            }
            if (message.in_tiles && message.in_tiles.length) {
                object.in_tiles = Array(message.in_tiles.length);
                for (let j = 0; j < message.in_tiles.length; ++j)
                    object.in_tiles[j] = message.in_tiles[j];
            }
            if (message.in_tile_states && message.in_tile_states.length) {
                object.in_tile_states = Array(message.in_tile_states.length);
                for (let j = 0; j < message.in_tile_states.length; ++j)
                    object.in_tile_states[j] = message.in_tile_states[j];
            }
            if (message.out_tiles && message.out_tiles.length) {
                object.out_tiles = Array(message.out_tiles.length);
                for (let j = 0; j < message.out_tiles.length; ++j)
                    object.out_tiles[j] = message.out_tiles[j];
            }
            if (message.out_tile_states && message.out_tile_states.length) {
                object.out_tile_states = Array(message.out_tile_states.length);
                for (let j = 0; j < message.out_tile_states.length; ++j)
                    object.out_tile_states[j] = message.out_tile_states[j];
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.tingpais0 && message.tingpais0.length) {
                object.tingpais0 = Array(message.tingpais0.length);
                for (let j = 0; j < message.tingpais0.length; ++j)
                    object.tingpais0[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais0[j], options);
            }
            if (message.tingpais1 && message.tingpais1.length) {
                object.tingpais1 = Array(message.tingpais1.length);
                for (let j = 0; j < message.tingpais1.length; ++j)
                    object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.change_type != null && message.hasOwnProperty("change_type"))
                object.change_type = message.change_type;
            return object;
        };

        ActionChangeTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionChangeTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionChangeTile";
        };

        return ActionChangeTile;
    })();

    lq.RecordChangeTile = (function() {

        function RecordChangeTile(properties) {
            this.doras = [];
            this.tingpai = [];
            this.change_tile_infos = [];
            this.operations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordChangeTile.prototype.doras = $util.emptyArray;
        RecordChangeTile.prototype.tingpai = $util.emptyArray;
        RecordChangeTile.prototype.change_tile_infos = $util.emptyArray;
        RecordChangeTile.prototype.operation = null;
        RecordChangeTile.prototype.change_type = 0;
        RecordChangeTile.prototype.operations = $util.emptyArray;

        RecordChangeTile.create = function create(properties) {
            return new RecordChangeTile(properties);
        };

        RecordChangeTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(10).string(message.doras[i]);
            if (message.tingpai != null && message.tingpai.length)
                for (let i = 0; i < message.tingpai.length; ++i)
                    $root.lq.RecordChangeTile.TingPai.encode(message.tingpai[i], writer.uint32(18).fork()).ldelim();
            if (message.change_tile_infos != null && message.change_tile_infos.length)
                for (let i = 0; i < message.change_tile_infos.length; ++i)
                    $root.lq.RecordChangeTile.ChangeTile.encode(message.change_tile_infos[i], writer.uint32(26).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.change_type != null && Object.hasOwnProperty.call(message, "change_type"))
                writer.uint32(40).uint32(message.change_type);
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(50).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordChangeTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordChangeTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordChangeTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpai && message.tingpai.length))
                            message.tingpai = [];
                        message.tingpai.push($root.lq.RecordChangeTile.TingPai.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.change_tile_infos && message.change_tile_infos.length))
                            message.change_tile_infos = [];
                        message.change_tile_infos.push($root.lq.RecordChangeTile.ChangeTile.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.change_type = value;
                        else
                            delete message.change_type;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordChangeTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordChangeTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai")) {
                if (!Array.isArray(message.tingpai))
                    return "tingpai: array expected";
                for (let i = 0; i < message.tingpai.length; ++i) {
                    let error = $root.lq.RecordChangeTile.TingPai.verify(message.tingpai[i], _depth + 1);
                    if (error)
                        return "tingpai." + error;
                }
            }
            if (message.change_tile_infos != null && message.hasOwnProperty("change_tile_infos")) {
                if (!Array.isArray(message.change_tile_infos))
                    return "change_tile_infos: array expected";
                for (let i = 0; i < message.change_tile_infos.length; ++i) {
                    let error = $root.lq.RecordChangeTile.ChangeTile.verify(message.change_tile_infos[i], _depth + 1);
                    if (error)
                        return "change_tile_infos." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.change_type != null && message.hasOwnProperty("change_type"))
                if (!$util.isInteger(message.change_type))
                    return "change_type: integer expected";
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            return null;
        };

        RecordChangeTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordChangeTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordChangeTile();
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordChangeTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.tingpai) {
                if (!Array.isArray(object.tingpai))
                    throw TypeError(".lq.RecordChangeTile.tingpai: array expected");
                message.tingpai = Array(object.tingpai.length);
                for (let i = 0; i < object.tingpai.length; ++i) {
                    if (typeof object.tingpai[i] !== "object")
                        throw TypeError(".lq.RecordChangeTile.tingpai: object expected");
                    message.tingpai[i] = $root.lq.RecordChangeTile.TingPai.fromObject(object.tingpai[i], _depth + 1);
                }
            }
            if (object.change_tile_infos) {
                if (!Array.isArray(object.change_tile_infos))
                    throw TypeError(".lq.RecordChangeTile.change_tile_infos: array expected");
                message.change_tile_infos = Array(object.change_tile_infos.length);
                for (let i = 0; i < object.change_tile_infos.length; ++i) {
                    if (typeof object.change_tile_infos[i] !== "object")
                        throw TypeError(".lq.RecordChangeTile.change_tile_infos: object expected");
                    message.change_tile_infos[i] = $root.lq.RecordChangeTile.ChangeTile.fromObject(object.change_tile_infos[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordChangeTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.change_type != null)
                if (Number(object.change_type) !== 0)
                    message.change_type = object.change_type >>> 0;
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordChangeTile.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordChangeTile.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            return message;
        };

        RecordChangeTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.tingpai = [];
                object.change_tile_infos = [];
                object.operations = [];
            }
            if (options.defaults) {
                object.operation = null;
                object.change_type = 0;
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.tingpai && message.tingpai.length) {
                object.tingpai = Array(message.tingpai.length);
                for (let j = 0; j < message.tingpai.length; ++j)
                    object.tingpai[j] = $root.lq.RecordChangeTile.TingPai.toObject(message.tingpai[j], options);
            }
            if (message.change_tile_infos && message.change_tile_infos.length) {
                object.change_tile_infos = Array(message.change_tile_infos.length);
                for (let j = 0; j < message.change_tile_infos.length; ++j)
                    object.change_tile_infos[j] = $root.lq.RecordChangeTile.ChangeTile.toObject(message.change_tile_infos[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.change_type != null && message.hasOwnProperty("change_type"))
                object.change_type = message.change_type;
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            return object;
        };

        RecordChangeTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordChangeTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordChangeTile";
        };

        RecordChangeTile.TingPai = (function() {

            function TingPai(properties) {
                this.tingpais1 = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TingPai.prototype.seat = 0;
            TingPai.prototype.tingpais1 = $util.emptyArray;

            TingPai.create = function create(properties) {
                return new TingPai(properties);
            };

            TingPai.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.tingpais1 != null && message.tingpais1.length)
                    for (let i = 0; i < message.tingpais1.length; ++i)
                        $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TingPai.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TingPai.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordChangeTile.TingPai(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tingpais1 && message.tingpais1.length))
                                message.tingpais1 = [];
                            message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TingPai.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TingPai.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                    if (!Array.isArray(message.tingpais1))
                        return "tingpais1: array expected";
                    for (let i = 0; i < message.tingpais1.length; ++i) {
                        let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                        if (error)
                            return "tingpais1." + error;
                    }
                }
                return null;
            };

            TingPai.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordChangeTile.TingPai)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordChangeTile.TingPai();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.tingpais1) {
                    if (!Array.isArray(object.tingpais1))
                        throw TypeError(".lq.RecordChangeTile.TingPai.tingpais1: array expected");
                    message.tingpais1 = Array(object.tingpais1.length);
                    for (let i = 0; i < object.tingpais1.length; ++i) {
                        if (typeof object.tingpais1[i] !== "object")
                            throw TypeError(".lq.RecordChangeTile.TingPai.tingpais1: object expected");
                        message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                    }
                }
                return message;
            };

            TingPai.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tingpais1 = [];
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.tingpais1 && message.tingpais1.length) {
                    object.tingpais1 = Array(message.tingpais1.length);
                    for (let j = 0; j < message.tingpais1.length; ++j)
                        object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
                }
                return object;
            };

            TingPai.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TingPai.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordChangeTile.TingPai";
            };

            return TingPai;
        })();

        RecordChangeTile.ChangeTile = (function() {

            function ChangeTile(properties) {
                this.in_tiles = [];
                this.in_tile_states = [];
                this.out_tiles = [];
                this.out_tile_states = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            ChangeTile.prototype.in_tiles = $util.emptyArray;
            ChangeTile.prototype.in_tile_states = $util.emptyArray;
            ChangeTile.prototype.out_tiles = $util.emptyArray;
            ChangeTile.prototype.out_tile_states = $util.emptyArray;

            ChangeTile.create = function create(properties) {
                return new ChangeTile(properties);
            };

            ChangeTile.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.in_tiles != null && message.in_tiles.length)
                    for (let i = 0; i < message.in_tiles.length; ++i)
                        writer.uint32(10).string(message.in_tiles[i]);
                if (message.in_tile_states != null && message.in_tile_states.length) {
                    writer.uint32(18).fork();
                    for (let i = 0; i < message.in_tile_states.length; ++i)
                        writer.int32(message.in_tile_states[i]);
                    writer.ldelim();
                }
                if (message.out_tiles != null && message.out_tiles.length)
                    for (let i = 0; i < message.out_tiles.length; ++i)
                        writer.uint32(26).string(message.out_tiles[i]);
                if (message.out_tile_states != null && message.out_tile_states.length) {
                    writer.uint32(34).fork();
                    for (let i = 0; i < message.out_tile_states.length; ++i)
                        writer.int32(message.out_tile_states[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            ChangeTile.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            ChangeTile.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordChangeTile.ChangeTile();
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 2)
                                break;
                            if (!(message.in_tiles && message.in_tiles.length))
                                message.in_tiles = [];
                            message.in_tiles.push(reader.string());
                            continue;
                        }
                    case 2: {
                            if (wireType === 2) {
                                if (!(message.in_tile_states && message.in_tile_states.length))
                                    message.in_tile_states = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.in_tile_states.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.in_tile_states && message.in_tile_states.length))
                                message.in_tile_states = [];
                            message.in_tile_states.push(reader.int32());
                            continue;
                        }
                    case 3: {
                            if (wireType !== 2)
                                break;
                            if (!(message.out_tiles && message.out_tiles.length))
                                message.out_tiles = [];
                            message.out_tiles.push(reader.string());
                            continue;
                        }
                    case 4: {
                            if (wireType === 2) {
                                if (!(message.out_tile_states && message.out_tile_states.length))
                                    message.out_tile_states = [];
                                let end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.out_tile_states.push(reader.int32());
                                continue;
                            }
                            if (wireType !== 0)
                                break;
                            if (!(message.out_tile_states && message.out_tile_states.length))
                                message.out_tile_states = [];
                            message.out_tile_states.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            ChangeTile.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            ChangeTile.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.in_tiles != null && message.hasOwnProperty("in_tiles")) {
                    if (!Array.isArray(message.in_tiles))
                        return "in_tiles: array expected";
                    for (let i = 0; i < message.in_tiles.length; ++i)
                        if (!$util.isString(message.in_tiles[i]))
                            return "in_tiles: string[] expected";
                }
                if (message.in_tile_states != null && message.hasOwnProperty("in_tile_states")) {
                    if (!Array.isArray(message.in_tile_states))
                        return "in_tile_states: array expected";
                    for (let i = 0; i < message.in_tile_states.length; ++i)
                        if (!$util.isInteger(message.in_tile_states[i]))
                            return "in_tile_states: integer[] expected";
                }
                if (message.out_tiles != null && message.hasOwnProperty("out_tiles")) {
                    if (!Array.isArray(message.out_tiles))
                        return "out_tiles: array expected";
                    for (let i = 0; i < message.out_tiles.length; ++i)
                        if (!$util.isString(message.out_tiles[i]))
                            return "out_tiles: string[] expected";
                }
                if (message.out_tile_states != null && message.hasOwnProperty("out_tile_states")) {
                    if (!Array.isArray(message.out_tile_states))
                        return "out_tile_states: array expected";
                    for (let i = 0; i < message.out_tile_states.length; ++i)
                        if (!$util.isInteger(message.out_tile_states[i]))
                            return "out_tile_states: integer[] expected";
                }
                return null;
            };

            ChangeTile.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordChangeTile.ChangeTile)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordChangeTile.ChangeTile();
                if (object.in_tiles) {
                    if (!Array.isArray(object.in_tiles))
                        throw TypeError(".lq.RecordChangeTile.ChangeTile.in_tiles: array expected");
                    message.in_tiles = Array(object.in_tiles.length);
                    for (let i = 0; i < object.in_tiles.length; ++i)
                        message.in_tiles[i] = String(object.in_tiles[i]);
                }
                if (object.in_tile_states) {
                    if (!Array.isArray(object.in_tile_states))
                        throw TypeError(".lq.RecordChangeTile.ChangeTile.in_tile_states: array expected");
                    message.in_tile_states = Array(object.in_tile_states.length);
                    for (let i = 0; i < object.in_tile_states.length; ++i)
                        message.in_tile_states[i] = object.in_tile_states[i] | 0;
                }
                if (object.out_tiles) {
                    if (!Array.isArray(object.out_tiles))
                        throw TypeError(".lq.RecordChangeTile.ChangeTile.out_tiles: array expected");
                    message.out_tiles = Array(object.out_tiles.length);
                    for (let i = 0; i < object.out_tiles.length; ++i)
                        message.out_tiles[i] = String(object.out_tiles[i]);
                }
                if (object.out_tile_states) {
                    if (!Array.isArray(object.out_tile_states))
                        throw TypeError(".lq.RecordChangeTile.ChangeTile.out_tile_states: array expected");
                    message.out_tile_states = Array(object.out_tile_states.length);
                    for (let i = 0; i < object.out_tile_states.length; ++i)
                        message.out_tile_states[i] = object.out_tile_states[i] | 0;
                }
                return message;
            };

            ChangeTile.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults) {
                    object.in_tiles = [];
                    object.in_tile_states = [];
                    object.out_tiles = [];
                    object.out_tile_states = [];
                }
                if (message.in_tiles && message.in_tiles.length) {
                    object.in_tiles = Array(message.in_tiles.length);
                    for (let j = 0; j < message.in_tiles.length; ++j)
                        object.in_tiles[j] = message.in_tiles[j];
                }
                if (message.in_tile_states && message.in_tile_states.length) {
                    object.in_tile_states = Array(message.in_tile_states.length);
                    for (let j = 0; j < message.in_tile_states.length; ++j)
                        object.in_tile_states[j] = message.in_tile_states[j];
                }
                if (message.out_tiles && message.out_tiles.length) {
                    object.out_tiles = Array(message.out_tiles.length);
                    for (let j = 0; j < message.out_tiles.length; ++j)
                        object.out_tiles[j] = message.out_tiles[j];
                }
                if (message.out_tile_states && message.out_tile_states.length) {
                    object.out_tile_states = Array(message.out_tile_states.length);
                    for (let j = 0; j < message.out_tile_states.length; ++j)
                        object.out_tile_states[j] = message.out_tile_states[j];
                }
                return object;
            };

            ChangeTile.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChangeTile.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordChangeTile.ChangeTile";
            };

            return ChangeTile;
        })();

        return RecordChangeTile;
    })();

    lq.ActionRevealTile = (function() {

        function ActionRevealTile(properties) {
            this.scores = [];
            this.tingpais = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionRevealTile.prototype.seat = 0;
        ActionRevealTile.prototype.is_liqi = false;
        ActionRevealTile.prototype.is_wliqi = false;
        ActionRevealTile.prototype.moqie = false;
        ActionRevealTile.prototype.scores = $util.emptyArray;
        ActionRevealTile.prototype.liqibang = 0;
        ActionRevealTile.prototype.operation = null;
        ActionRevealTile.prototype.tingpais = $util.emptyArray;
        ActionRevealTile.prototype.tile = "";
        ActionRevealTile.prototype.zhenting = false;

        ActionRevealTile.create = function create(properties) {
            return new ActionRevealTile(properties);
        };

        ActionRevealTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.is_liqi != null && Object.hasOwnProperty.call(message, "is_liqi"))
                writer.uint32(16).bool(message.is_liqi);
            if (message.is_wliqi != null && Object.hasOwnProperty.call(message, "is_wliqi"))
                writer.uint32(24).bool(message.is_wliqi);
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(32).bool(message.moqie);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(48).uint32(message.liqibang);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(58).fork()).ldelim();
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(74).string(message.tile);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(80).bool(message.zhenting);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionRevealTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionRevealTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionRevealTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_liqi = value;
                        else
                            delete message.is_liqi;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_wliqi = value;
                        else
                            delete message.is_wliqi;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionRevealTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionRevealTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                if (typeof message.is_liqi !== "boolean")
                    return "is_liqi: boolean expected";
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                if (typeof message.is_wliqi !== "boolean")
                    return "is_wliqi: boolean expected";
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            return null;
        };

        ActionRevealTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionRevealTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionRevealTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.is_liqi != null)
                if (object.is_liqi)
                    message.is_liqi = Boolean(object.is_liqi);
            if (object.is_wliqi != null)
                if (object.is_wliqi)
                    message.is_wliqi = Boolean(object.is_wliqi);
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionRevealTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionRevealTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionRevealTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionRevealTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            return message;
        };

        ActionRevealTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.tingpais = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.is_liqi = false;
                object.is_wliqi = false;
                object.moqie = false;
                object.liqibang = 0;
                object.operation = null;
                object.tile = "";
                object.zhenting = false;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                object.is_liqi = message.is_liqi;
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                object.is_wliqi = message.is_wliqi;
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            return object;
        };

        ActionRevealTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionRevealTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionRevealTile";
        };

        return ActionRevealTile;
    })();

    lq.RecordRevealTile = (function() {

        function RecordRevealTile(properties) {
            this.scores = [];
            this.operations = [];
            this.tingpais = [];
            this.zhenting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordRevealTile.prototype.seat = 0;
        RecordRevealTile.prototype.is_liqi = false;
        RecordRevealTile.prototype.is_wliqi = false;
        RecordRevealTile.prototype.moqie = false;
        RecordRevealTile.prototype.scores = $util.emptyArray;
        RecordRevealTile.prototype.liqibang = 0;
        RecordRevealTile.prototype.operations = $util.emptyArray;
        RecordRevealTile.prototype.tingpais = $util.emptyArray;
        RecordRevealTile.prototype.tile = "";
        RecordRevealTile.prototype.zhenting = $util.emptyArray;

        RecordRevealTile.create = function create(properties) {
            return new RecordRevealTile(properties);
        };

        RecordRevealTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.is_liqi != null && Object.hasOwnProperty.call(message, "is_liqi"))
                writer.uint32(16).bool(message.is_liqi);
            if (message.is_wliqi != null && Object.hasOwnProperty.call(message, "is_wliqi"))
                writer.uint32(24).bool(message.is_wliqi);
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(32).bool(message.moqie);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(48).uint32(message.liqibang);
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(58).fork()).ldelim();
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(74).string(message.tile);
            if (message.zhenting != null && message.zhenting.length) {
                writer.uint32(82).fork();
                for (let i = 0; i < message.zhenting.length; ++i)
                    writer.bool(message.zhenting[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordRevealTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordRevealTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordRevealTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_liqi = value;
                        else
                            delete message.is_liqi;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_wliqi = value;
                        else
                            delete message.is_wliqi;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 10: {
                        if (wireType === 2) {
                            if (!(message.zhenting && message.zhenting.length))
                                message.zhenting = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhenting.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhenting && message.zhenting.length))
                            message.zhenting = [];
                        message.zhenting.push(reader.bool());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordRevealTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordRevealTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                if (typeof message.is_liqi !== "boolean")
                    return "is_liqi: boolean expected";
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                if (typeof message.is_wliqi !== "boolean")
                    return "is_wliqi: boolean expected";
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.zhenting != null && message.hasOwnProperty("zhenting")) {
                if (!Array.isArray(message.zhenting))
                    return "zhenting: array expected";
                for (let i = 0; i < message.zhenting.length; ++i)
                    if (typeof message.zhenting[i] !== "boolean")
                        return "zhenting: boolean[] expected";
            }
            return null;
        };

        RecordRevealTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordRevealTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordRevealTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.is_liqi != null)
                if (object.is_liqi)
                    message.is_liqi = Boolean(object.is_liqi);
            if (object.is_wliqi != null)
                if (object.is_wliqi)
                    message.is_wliqi = Boolean(object.is_wliqi);
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordRevealTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordRevealTile.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordRevealTile.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.RecordRevealTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.RecordRevealTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.zhenting) {
                if (!Array.isArray(object.zhenting))
                    throw TypeError(".lq.RecordRevealTile.zhenting: array expected");
                message.zhenting = Array(object.zhenting.length);
                for (let i = 0; i < object.zhenting.length; ++i)
                    message.zhenting[i] = Boolean(object.zhenting[i]);
            }
            return message;
        };

        RecordRevealTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.operations = [];
                object.tingpais = [];
                object.zhenting = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.is_liqi = false;
                object.is_wliqi = false;
                object.moqie = false;
                object.liqibang = 0;
                object.tile = "";
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                object.is_liqi = message.is_liqi;
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                object.is_wliqi = message.is_wliqi;
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.zhenting && message.zhenting.length) {
                object.zhenting = Array(message.zhenting.length);
                for (let j = 0; j < message.zhenting.length; ++j)
                    object.zhenting[j] = message.zhenting[j];
            }
            return object;
        };

        RecordRevealTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordRevealTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordRevealTile";
        };

        return RecordRevealTile;
    })();

    lq.ActionUnveilTile = (function() {

        function ActionUnveilTile(properties) {
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionUnveilTile.prototype.seat = 0;
        ActionUnveilTile.prototype.scores = $util.emptyArray;
        ActionUnveilTile.prototype.liqibang = 0;
        ActionUnveilTile.prototype.operation = null;

        ActionUnveilTile.create = function create(properties) {
            return new ActionUnveilTile(properties);
        };

        ActionUnveilTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).int32(message.seat);
            if (message.scores != null && message.scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(24).uint32(message.liqibang);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionUnveilTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionUnveilTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionUnveilTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionUnveilTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionUnveilTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        ActionUnveilTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionUnveilTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionUnveilTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat | 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionUnveilTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionUnveilTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        ActionUnveilTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.scores = [];
            if (options.defaults) {
                object.seat = 0;
                object.liqibang = 0;
                object.operation = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        ActionUnveilTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionUnveilTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionUnveilTile";
        };

        return ActionUnveilTile;
    })();

    lq.RecordUnveilTile = (function() {

        function RecordUnveilTile(properties) {
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordUnveilTile.prototype.seat = 0;
        RecordUnveilTile.prototype.scores = $util.emptyArray;
        RecordUnveilTile.prototype.liqibang = 0;
        RecordUnveilTile.prototype.operation = null;

        RecordUnveilTile.create = function create(properties) {
            return new RecordUnveilTile(properties);
        };

        RecordUnveilTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).int32(message.seat);
            if (message.scores != null && message.scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(24).uint32(message.liqibang);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordUnveilTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordUnveilTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordUnveilTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordUnveilTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordUnveilTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        RecordUnveilTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordUnveilTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordUnveilTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat | 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordUnveilTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordUnveilTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        RecordUnveilTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.scores = [];
            if (options.defaults) {
                object.seat = 0;
                object.liqibang = 0;
                object.operation = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        RecordUnveilTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordUnveilTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordUnveilTile";
        };

        return RecordUnveilTile;
    })();

    lq.ActionLockTile = (function() {

        function ActionLockTile(properties) {
            this.scores = [];
            this.tingpais = [];
            this.doras = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionLockTile.prototype.seat = 0;
        ActionLockTile.prototype.scores = $util.emptyArray;
        ActionLockTile.prototype.liqibang = 0;
        ActionLockTile.prototype.tile = "";
        ActionLockTile.prototype.operation = null;
        ActionLockTile.prototype.zhenting = false;
        ActionLockTile.prototype.tingpais = $util.emptyArray;
        ActionLockTile.prototype.doras = $util.emptyArray;
        ActionLockTile.prototype.lock_state = 0;

        ActionLockTile.create = function create(properties) {
            return new ActionLockTile(properties);
        };

        ActionLockTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.scores != null && message.scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(24).uint32(message.liqibang);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(34).string(message.tile);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(42).fork()).ldelim();
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(48).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(58).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.lock_state != null && Object.hasOwnProperty.call(message, "lock_state"))
                writer.uint32(72).int32(message.lock_state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionLockTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionLockTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionLockTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lock_state = value;
                        else
                            delete message.lock_state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionLockTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionLockTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.lock_state != null && message.hasOwnProperty("lock_state"))
                if (!$util.isInteger(message.lock_state))
                    return "lock_state: integer expected";
            return null;
        };

        ActionLockTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionLockTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionLockTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionLockTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionLockTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionLockTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionLockTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionLockTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.lock_state != null)
                if (Number(object.lock_state) !== 0)
                    message.lock_state = object.lock_state | 0;
            return message;
        };

        ActionLockTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.tingpais = [];
                object.doras = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.liqibang = 0;
                object.tile = "";
                object.operation = null;
                object.zhenting = false;
                object.lock_state = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.lock_state != null && message.hasOwnProperty("lock_state"))
                object.lock_state = message.lock_state;
            return object;
        };

        ActionLockTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionLockTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionLockTile";
        };

        return ActionLockTile;
    })();

    lq.RecordLockTile = (function() {

        function RecordLockTile(properties) {
            this.scores = [];
            this.operation = [];
            this.zhentings = [];
            this.tingpais = [];
            this.doras = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordLockTile.prototype.seat = 0;
        RecordLockTile.prototype.scores = $util.emptyArray;
        RecordLockTile.prototype.liqibang = 0;
        RecordLockTile.prototype.tile = "";
        RecordLockTile.prototype.operation = $util.emptyArray;
        RecordLockTile.prototype.zhentings = $util.emptyArray;
        RecordLockTile.prototype.tingpais = $util.emptyArray;
        RecordLockTile.prototype.doras = $util.emptyArray;
        RecordLockTile.prototype.lock_state = 0;

        RecordLockTile.create = function create(properties) {
            return new RecordLockTile(properties);
        };

        RecordLockTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.scores != null && message.scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(24).uint32(message.liqibang);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(34).string(message.tile);
            if (message.operation != null && message.operation.length)
                for (let i = 0; i < message.operation.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operation[i], writer.uint32(42).fork()).ldelim();
            if (message.zhentings != null && message.zhentings.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.zhentings.length; ++i)
                    writer.bool(message.zhentings[i]);
                writer.ldelim();
            }
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(58).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.lock_state != null && Object.hasOwnProperty.call(message, "lock_state"))
                writer.uint32(72).int32(message.lock_state);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordLockTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordLockTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordLockTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operation && message.operation.length))
                            message.operation = [];
                        message.operation.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.zhentings && message.zhentings.length))
                                message.zhentings = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhentings.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhentings && message.zhentings.length))
                            message.zhentings = [];
                        message.zhentings.push(reader.bool());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lock_state = value;
                        else
                            delete message.lock_state;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordLockTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordLockTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                if (!Array.isArray(message.operation))
                    return "operation: array expected";
                for (let i = 0; i < message.operation.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operation[i], _depth + 1);
                    if (error)
                        return "operation." + error;
                }
            }
            if (message.zhentings != null && message.hasOwnProperty("zhentings")) {
                if (!Array.isArray(message.zhentings))
                    return "zhentings: array expected";
                for (let i = 0; i < message.zhentings.length; ++i)
                    if (typeof message.zhentings[i] !== "boolean")
                        return "zhentings: boolean[] expected";
            }
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.lock_state != null && message.hasOwnProperty("lock_state"))
                if (!$util.isInteger(message.lock_state))
                    return "lock_state: integer expected";
            return null;
        };

        RecordLockTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordLockTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordLockTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordLockTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.operation) {
                if (!Array.isArray(object.operation))
                    throw TypeError(".lq.RecordLockTile.operation: array expected");
                message.operation = Array(object.operation.length);
                for (let i = 0; i < object.operation.length; ++i) {
                    if (typeof object.operation[i] !== "object")
                        throw TypeError(".lq.RecordLockTile.operation: object expected");
                    message.operation[i] = $root.lq.OptionalOperationList.fromObject(object.operation[i], _depth + 1);
                }
            }
            if (object.zhentings) {
                if (!Array.isArray(object.zhentings))
                    throw TypeError(".lq.RecordLockTile.zhentings: array expected");
                message.zhentings = Array(object.zhentings.length);
                for (let i = 0; i < object.zhentings.length; ++i)
                    message.zhentings[i] = Boolean(object.zhentings[i]);
            }
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.RecordLockTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.RecordLockTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordLockTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.lock_state != null)
                if (Number(object.lock_state) !== 0)
                    message.lock_state = object.lock_state | 0;
            return message;
        };

        RecordLockTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.scores = [];
                object.operation = [];
                object.zhentings = [];
                object.tingpais = [];
                object.doras = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.liqibang = 0;
                object.tile = "";
                object.lock_state = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.operation && message.operation.length) {
                object.operation = Array(message.operation.length);
                for (let j = 0; j < message.operation.length; ++j)
                    object.operation[j] = $root.lq.OptionalOperationList.toObject(message.operation[j], options);
            }
            if (message.zhentings && message.zhentings.length) {
                object.zhentings = Array(message.zhentings.length);
                for (let j = 0; j < message.zhentings.length; ++j)
                    object.zhentings[j] = message.zhentings[j];
            }
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.lock_state != null && message.hasOwnProperty("lock_state"))
                object.lock_state = message.lock_state;
            return object;
        };

        RecordLockTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordLockTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordLockTile";
        };

        return RecordLockTile;
    })();

    lq.ActionDiscardTile = (function() {

        function ActionDiscardTile(properties) {
            this.tingpais = [];
            this.doras = [];
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionDiscardTile.prototype.seat = 0;
        ActionDiscardTile.prototype.tile = "";
        ActionDiscardTile.prototype.is_liqi = false;
        ActionDiscardTile.prototype.operation = null;
        ActionDiscardTile.prototype.moqie = false;
        ActionDiscardTile.prototype.zhenting = false;
        ActionDiscardTile.prototype.tingpais = $util.emptyArray;
        ActionDiscardTile.prototype.doras = $util.emptyArray;
        ActionDiscardTile.prototype.is_wliqi = false;
        ActionDiscardTile.prototype.tile_state = 0;
        ActionDiscardTile.prototype.muyu = null;
        ActionDiscardTile.prototype.revealed = false;
        ActionDiscardTile.prototype.scores = $util.emptyArray;
        ActionDiscardTile.prototype.liqibang = 0;
        ActionDiscardTile.prototype.yongchang = null;
        ActionDiscardTile.prototype.hun_zhi_yi_ji_info = null;
        ActionDiscardTile.prototype.liqi_type_beishuizhizhan = 0;

        ActionDiscardTile.create = function create(properties) {
            return new ActionDiscardTile(properties);
        };

        ActionDiscardTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.is_liqi != null && Object.hasOwnProperty.call(message, "is_liqi"))
                writer.uint32(24).bool(message.is_liqi);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(40).bool(message.moqie);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(48).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(58).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.is_wliqi != null && Object.hasOwnProperty.call(message, "is_wliqi"))
                writer.uint32(72).bool(message.is_wliqi);
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(80).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(90).fork()).ldelim();
            if (message.revealed != null && Object.hasOwnProperty.call(message, "revealed"))
                writer.uint32(96).bool(message.revealed);
            if (message.scores != null && message.scores.length) {
                writer.uint32(106).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(112).uint32(message.liqibang);
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(202).fork()).ldelim();
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(210).fork()).ldelim();
            if (message.liqi_type_beishuizhizhan != null && Object.hasOwnProperty.call(message, "liqi_type_beishuizhizhan"))
                writer.uint32(216).uint32(message.liqi_type_beishuizhizhan);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionDiscardTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionDiscardTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionDiscardTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_liqi = value;
                        else
                            delete message.is_liqi;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_wliqi = value;
                        else
                            delete message.is_wliqi;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.revealed = value;
                        else
                            delete message.revealed;
                        continue;
                    }
                case 13: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqi_type_beishuizhizhan = value;
                        else
                            delete message.liqi_type_beishuizhizhan;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionDiscardTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionDiscardTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                if (typeof message.is_liqi !== "boolean")
                    return "is_liqi: boolean expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                if (typeof message.is_wliqi !== "boolean")
                    return "is_wliqi: boolean expected";
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.revealed != null && message.hasOwnProperty("revealed"))
                if (typeof message.revealed !== "boolean")
                    return "revealed: boolean expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                if (!$util.isInteger(message.liqi_type_beishuizhizhan))
                    return "liqi_type_beishuizhizhan: integer expected";
            return null;
        };

        ActionDiscardTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionDiscardTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionDiscardTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.is_liqi != null)
                if (object.is_liqi)
                    message.is_liqi = Boolean(object.is_liqi);
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionDiscardTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionDiscardTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionDiscardTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionDiscardTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.is_wliqi != null)
                if (object.is_wliqi)
                    message.is_wliqi = Boolean(object.is_wliqi);
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionDiscardTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.revealed != null)
                if (object.revealed)
                    message.revealed = Boolean(object.revealed);
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionDiscardTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.ActionDiscardTile.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.ActionDiscardTile.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            if (object.liqi_type_beishuizhizhan != null)
                if (Number(object.liqi_type_beishuizhizhan) !== 0)
                    message.liqi_type_beishuizhizhan = object.liqi_type_beishuizhizhan >>> 0;
            return message;
        };

        ActionDiscardTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tingpais = [];
                object.doras = [];
                object.scores = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.tile = "";
                object.is_liqi = false;
                object.operation = null;
                object.moqie = false;
                object.zhenting = false;
                object.is_wliqi = false;
                object.tile_state = 0;
                object.muyu = null;
                object.revealed = false;
                object.liqibang = 0;
                object.yongchang = null;
                object.hun_zhi_yi_ji_info = null;
                object.liqi_type_beishuizhizhan = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                object.is_liqi = message.is_liqi;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                object.is_wliqi = message.is_wliqi;
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.revealed != null && message.hasOwnProperty("revealed"))
                object.revealed = message.revealed;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                object.liqi_type_beishuizhizhan = message.liqi_type_beishuizhizhan;
            return object;
        };

        ActionDiscardTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionDiscardTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionDiscardTile";
        };

        return ActionDiscardTile;
    })();

    lq.RecordDiscardTile = (function() {

        function RecordDiscardTile(properties) {
            this.zhenting = [];
            this.tingpais = [];
            this.doras = [];
            this.operations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordDiscardTile.prototype.seat = 0;
        RecordDiscardTile.prototype.tile = "";
        RecordDiscardTile.prototype.is_liqi = false;
        RecordDiscardTile.prototype.moqie = false;
        RecordDiscardTile.prototype.zhenting = $util.emptyArray;
        RecordDiscardTile.prototype.tingpais = $util.emptyArray;
        RecordDiscardTile.prototype.doras = $util.emptyArray;
        RecordDiscardTile.prototype.is_wliqi = false;
        RecordDiscardTile.prototype.operations = $util.emptyArray;
        RecordDiscardTile.prototype.tile_state = 0;
        RecordDiscardTile.prototype.muyu = null;
        RecordDiscardTile.prototype.yongchang = null;
        RecordDiscardTile.prototype.hun_zhi_yi_ji_info = null;
        RecordDiscardTile.prototype.liqi_type_beishuizhizhan = 0;

        RecordDiscardTile.create = function create(properties) {
            return new RecordDiscardTile(properties);
        };

        RecordDiscardTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.is_liqi != null && Object.hasOwnProperty.call(message, "is_liqi"))
                writer.uint32(24).bool(message.is_liqi);
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(40).bool(message.moqie);
            if (message.zhenting != null && message.zhenting.length) {
                writer.uint32(50).fork();
                for (let i = 0; i < message.zhenting.length; ++i)
                    writer.bool(message.zhenting[i]);
                writer.ldelim();
            }
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(58).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(66).string(message.doras[i]);
            if (message.is_wliqi != null && Object.hasOwnProperty.call(message, "is_wliqi"))
                writer.uint32(72).bool(message.is_wliqi);
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(82).fork()).ldelim();
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(88).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(98).fork()).ldelim();
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(106).fork()).ldelim();
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(114).fork()).ldelim();
            if (message.liqi_type_beishuizhizhan != null && Object.hasOwnProperty.call(message, "liqi_type_beishuizhizhan"))
                writer.uint32(216).uint32(message.liqi_type_beishuizhizhan);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordDiscardTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordDiscardTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordDiscardTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_liqi = value;
                        else
                            delete message.is_liqi;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.zhenting && message.zhenting.length))
                                message.zhenting = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhenting.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhenting && message.zhenting.length))
                            message.zhenting = [];
                        message.zhenting.push(reader.bool());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.is_wliqi = value;
                        else
                            delete message.is_wliqi;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqi_type_beishuizhizhan = value;
                        else
                            delete message.liqi_type_beishuizhizhan;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordDiscardTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordDiscardTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                if (typeof message.is_liqi !== "boolean")
                    return "is_liqi: boolean expected";
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.zhenting != null && message.hasOwnProperty("zhenting")) {
                if (!Array.isArray(message.zhenting))
                    return "zhenting: array expected";
                for (let i = 0; i < message.zhenting.length; ++i)
                    if (typeof message.zhenting[i] !== "boolean")
                        return "zhenting: boolean[] expected";
            }
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                if (typeof message.is_wliqi !== "boolean")
                    return "is_wliqi: boolean expected";
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                if (!$util.isInteger(message.liqi_type_beishuizhizhan))
                    return "liqi_type_beishuizhizhan: integer expected";
            return null;
        };

        RecordDiscardTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordDiscardTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordDiscardTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.is_liqi != null)
                if (object.is_liqi)
                    message.is_liqi = Boolean(object.is_liqi);
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.zhenting) {
                if (!Array.isArray(object.zhenting))
                    throw TypeError(".lq.RecordDiscardTile.zhenting: array expected");
                message.zhenting = Array(object.zhenting.length);
                for (let i = 0; i < object.zhenting.length; ++i)
                    message.zhenting[i] = Boolean(object.zhenting[i]);
            }
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.RecordDiscardTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.RecordDiscardTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordDiscardTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.is_wliqi != null)
                if (object.is_wliqi)
                    message.is_wliqi = Boolean(object.is_wliqi);
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordDiscardTile.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordDiscardTile.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordDiscardTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.RecordDiscardTile.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.RecordDiscardTile.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            if (object.liqi_type_beishuizhizhan != null)
                if (Number(object.liqi_type_beishuizhizhan) !== 0)
                    message.liqi_type_beishuizhizhan = object.liqi_type_beishuizhizhan >>> 0;
            return message;
        };

        RecordDiscardTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.zhenting = [];
                object.tingpais = [];
                object.doras = [];
                object.operations = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.tile = "";
                object.is_liqi = false;
                object.moqie = false;
                object.is_wliqi = false;
                object.tile_state = 0;
                object.muyu = null;
                object.yongchang = null;
                object.hun_zhi_yi_ji_info = null;
                object.liqi_type_beishuizhizhan = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.is_liqi != null && message.hasOwnProperty("is_liqi"))
                object.is_liqi = message.is_liqi;
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.zhenting && message.zhenting.length) {
                object.zhenting = Array(message.zhenting.length);
                for (let j = 0; j < message.zhenting.length; ++j)
                    object.zhenting[j] = message.zhenting[j];
            }
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.is_wliqi != null && message.hasOwnProperty("is_wliqi"))
                object.is_wliqi = message.is_wliqi;
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            if (message.liqi_type_beishuizhizhan != null && message.hasOwnProperty("liqi_type_beishuizhizhan"))
                object.liqi_type_beishuizhizhan = message.liqi_type_beishuizhizhan;
            return object;
        };

        RecordDiscardTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordDiscardTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordDiscardTile";
        };

        return RecordDiscardTile;
    })();

    lq.ActionDealTile = (function() {

        function ActionDealTile(properties) {
            this.doras = [];
            this.tingpais = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionDealTile.prototype.seat = 0;
        ActionDealTile.prototype.tile = "";
        ActionDealTile.prototype.left_tile_count = 0;
        ActionDealTile.prototype.operation = null;
        ActionDealTile.prototype.liqi = null;
        ActionDealTile.prototype.doras = $util.emptyArray;
        ActionDealTile.prototype.zhenting = false;
        ActionDealTile.prototype.tingpais = $util.emptyArray;
        ActionDealTile.prototype.tile_state = 0;
        ActionDealTile.prototype.muyu = null;
        ActionDealTile.prototype.tile_index = 0;
        ActionDealTile.prototype.hun_zhi_yi_ji_info = null;

        ActionDealTile.create = function create(properties) {
            return new ActionDealTile(properties);
        };

        ActionDealTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(24).uint32(message.left_tile_count);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(56).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(72).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(82).fork()).ldelim();
            if (message.tile_index != null && Object.hasOwnProperty.call(message, "tile_index"))
                writer.uint32(88).uint32(message.tile_index);
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(98).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionDealTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionDealTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionDealTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_index = value;
                        else
                            delete message.tile_index;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionDealTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionDealTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.tile_index != null && message.hasOwnProperty("tile_index"))
                if (!$util.isInteger(message.tile_index))
                    return "tile_index: integer expected";
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        ActionDealTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionDealTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionDealTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionDealTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.ActionDealTile.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionDealTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionDealTile.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionDealTile.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionDealTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.tile_index != null)
                if (Number(object.tile_index) !== 0)
                    message.tile_index = object.tile_index >>> 0;
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.ActionDealTile.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        ActionDealTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.tingpais = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.tile = "";
                object.left_tile_count = 0;
                object.operation = null;
                object.liqi = null;
                object.zhenting = false;
                object.tile_state = 0;
                object.muyu = null;
                object.tile_index = 0;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais[j], options);
            }
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.tile_index != null && message.hasOwnProperty("tile_index"))
                object.tile_index = message.tile_index;
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        ActionDealTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionDealTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionDealTile";
        };

        return ActionDealTile;
    })();

    lq.RecordDealTile = (function() {

        function RecordDealTile(properties) {
            this.doras = [];
            this.zhenting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordDealTile.prototype.seat = 0;
        RecordDealTile.prototype.tile = "";
        RecordDealTile.prototype.left_tile_count = 0;
        RecordDealTile.prototype.liqi = null;
        RecordDealTile.prototype.doras = $util.emptyArray;
        RecordDealTile.prototype.zhenting = $util.emptyArray;
        RecordDealTile.prototype.operation = null;
        RecordDealTile.prototype.tile_state = 0;
        RecordDealTile.prototype.muyu = null;
        RecordDealTile.prototype.tile_index = 0;
        RecordDealTile.prototype.hun_zhi_yi_ji_info = null;

        RecordDealTile.create = function create(properties) {
            return new RecordDealTile(properties);
        };

        RecordDealTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.tile != null && Object.hasOwnProperty.call(message, "tile"))
                writer.uint32(18).string(message.tile);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(24).uint32(message.left_tile_count);
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.zhenting != null && message.zhenting.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.zhenting.length; ++i)
                    writer.bool(message.zhenting[i]);
                writer.ldelim();
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(66).fork()).ldelim();
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(72).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(90).fork()).ldelim();
            if (message.tile_index != null && Object.hasOwnProperty.call(message, "tile_index"))
                writer.uint32(96).uint32(message.tile_index);
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(106).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordDealTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordDealTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordDealTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tile = value;
                        else
                            delete message.tile;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.zhenting && message.zhenting.length))
                                message.zhenting = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhenting.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhenting && message.zhenting.length))
                            message.zhenting = [];
                        message.zhenting.push(reader.bool());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_index = value;
                        else
                            delete message.tile_index;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordDealTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordDealTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tile != null && message.hasOwnProperty("tile"))
                if (!$util.isString(message.tile))
                    return "tile: string expected";
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting")) {
                if (!Array.isArray(message.zhenting))
                    return "zhenting: array expected";
                for (let i = 0; i < message.zhenting.length; ++i)
                    if (typeof message.zhenting[i] !== "boolean")
                        return "zhenting: boolean[] expected";
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.tile_index != null && message.hasOwnProperty("tile_index"))
                if (!$util.isInteger(message.tile_index))
                    return "tile_index: integer expected";
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        RecordDealTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordDealTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordDealTile();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tile != null)
                if (typeof object.tile !== "string" || object.tile.length)
                    message.tile = String(object.tile);
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.RecordDealTile.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordDealTile.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.zhenting) {
                if (!Array.isArray(object.zhenting))
                    throw TypeError(".lq.RecordDealTile.zhenting: array expected");
                message.zhenting = Array(object.zhenting.length);
                for (let i = 0; i < object.zhenting.length; ++i)
                    message.zhenting[i] = Boolean(object.zhenting[i]);
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordDealTile.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordDealTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.tile_index != null)
                if (Number(object.tile_index) !== 0)
                    message.tile_index = object.tile_index >>> 0;
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.RecordDealTile.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        RecordDealTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.zhenting = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.tile = "";
                object.left_tile_count = 0;
                object.liqi = null;
                object.operation = null;
                object.tile_state = 0;
                object.muyu = null;
                object.tile_index = 0;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tile != null && message.hasOwnProperty("tile"))
                object.tile = message.tile;
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.zhenting && message.zhenting.length) {
                object.zhenting = Array(message.zhenting.length);
                for (let j = 0; j < message.zhenting.length; ++j)
                    object.zhenting[j] = message.zhenting[j];
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.tile_index != null && message.hasOwnProperty("tile_index"))
                object.tile_index = message.tile_index;
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        RecordDealTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordDealTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordDealTile";
        };

        return RecordDealTile;
    })();

    lq.ActionFillAwaitingTiles = (function() {

        function ActionFillAwaitingTiles(properties) {
            this.awaiting_tiles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionFillAwaitingTiles.prototype.awaiting_tiles = $util.emptyArray;
        ActionFillAwaitingTiles.prototype.left_tile_count = 0;
        ActionFillAwaitingTiles.prototype.operation = null;
        ActionFillAwaitingTiles.prototype.liqi = null;

        ActionFillAwaitingTiles.create = function create(properties) {
            return new ActionFillAwaitingTiles(properties);
        };

        ActionFillAwaitingTiles.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awaiting_tiles != null && message.awaiting_tiles.length)
                for (let i = 0; i < message.awaiting_tiles.length; ++i)
                    writer.uint32(10).string(message.awaiting_tiles[i]);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(16).uint32(message.left_tile_count);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(26).fork()).ldelim();
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionFillAwaitingTiles.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionFillAwaitingTiles.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionFillAwaitingTiles(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.awaiting_tiles && message.awaiting_tiles.length))
                            message.awaiting_tiles = [];
                        message.awaiting_tiles.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionFillAwaitingTiles.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionFillAwaitingTiles.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.awaiting_tiles != null && message.hasOwnProperty("awaiting_tiles")) {
                if (!Array.isArray(message.awaiting_tiles))
                    return "awaiting_tiles: array expected";
                for (let i = 0; i < message.awaiting_tiles.length; ++i)
                    if (!$util.isString(message.awaiting_tiles[i]))
                        return "awaiting_tiles: string[] expected";
            }
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            return null;
        };

        ActionFillAwaitingTiles.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionFillAwaitingTiles)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionFillAwaitingTiles();
            if (object.awaiting_tiles) {
                if (!Array.isArray(object.awaiting_tiles))
                    throw TypeError(".lq.ActionFillAwaitingTiles.awaiting_tiles: array expected");
                message.awaiting_tiles = Array(object.awaiting_tiles.length);
                for (let i = 0; i < object.awaiting_tiles.length; ++i)
                    message.awaiting_tiles[i] = String(object.awaiting_tiles[i]);
            }
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionFillAwaitingTiles.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.ActionFillAwaitingTiles.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            return message;
        };

        ActionFillAwaitingTiles.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.awaiting_tiles = [];
            if (options.defaults) {
                object.left_tile_count = 0;
                object.operation = null;
                object.liqi = null;
            }
            if (message.awaiting_tiles && message.awaiting_tiles.length) {
                object.awaiting_tiles = Array(message.awaiting_tiles.length);
                for (let j = 0; j < message.awaiting_tiles.length; ++j)
                    object.awaiting_tiles[j] = message.awaiting_tiles[j];
            }
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            return object;
        };

        ActionFillAwaitingTiles.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionFillAwaitingTiles.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionFillAwaitingTiles";
        };

        return ActionFillAwaitingTiles;
    })();

    lq.RecordFillAwaitingTiles = (function() {

        function RecordFillAwaitingTiles(properties) {
            this.awaiting_tiles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordFillAwaitingTiles.prototype.awaiting_tiles = $util.emptyArray;
        RecordFillAwaitingTiles.prototype.left_tile_count = 0;
        RecordFillAwaitingTiles.prototype.operation = null;
        RecordFillAwaitingTiles.prototype.liqi = null;

        RecordFillAwaitingTiles.create = function create(properties) {
            return new RecordFillAwaitingTiles(properties);
        };

        RecordFillAwaitingTiles.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.awaiting_tiles != null && message.awaiting_tiles.length)
                for (let i = 0; i < message.awaiting_tiles.length; ++i)
                    writer.uint32(10).string(message.awaiting_tiles[i]);
            if (message.left_tile_count != null && Object.hasOwnProperty.call(message, "left_tile_count"))
                writer.uint32(16).uint32(message.left_tile_count);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(26).fork()).ldelim();
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordFillAwaitingTiles.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordFillAwaitingTiles.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordFillAwaitingTiles(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.awaiting_tiles && message.awaiting_tiles.length))
                            message.awaiting_tiles = [];
                        message.awaiting_tiles.push(reader.string());
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.left_tile_count = value;
                        else
                            delete message.left_tile_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordFillAwaitingTiles.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordFillAwaitingTiles.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.awaiting_tiles != null && message.hasOwnProperty("awaiting_tiles")) {
                if (!Array.isArray(message.awaiting_tiles))
                    return "awaiting_tiles: array expected";
                for (let i = 0; i < message.awaiting_tiles.length; ++i)
                    if (!$util.isString(message.awaiting_tiles[i]))
                        return "awaiting_tiles: string[] expected";
            }
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                if (!$util.isInteger(message.left_tile_count))
                    return "left_tile_count: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            return null;
        };

        RecordFillAwaitingTiles.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordFillAwaitingTiles)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordFillAwaitingTiles();
            if (object.awaiting_tiles) {
                if (!Array.isArray(object.awaiting_tiles))
                    throw TypeError(".lq.RecordFillAwaitingTiles.awaiting_tiles: array expected");
                message.awaiting_tiles = Array(object.awaiting_tiles.length);
                for (let i = 0; i < object.awaiting_tiles.length; ++i)
                    message.awaiting_tiles[i] = String(object.awaiting_tiles[i]);
            }
            if (object.left_tile_count != null)
                if (Number(object.left_tile_count) !== 0)
                    message.left_tile_count = object.left_tile_count >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordFillAwaitingTiles.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.RecordFillAwaitingTiles.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            return message;
        };

        RecordFillAwaitingTiles.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.awaiting_tiles = [];
            if (options.defaults) {
                object.left_tile_count = 0;
                object.operation = null;
                object.liqi = null;
            }
            if (message.awaiting_tiles && message.awaiting_tiles.length) {
                object.awaiting_tiles = Array(message.awaiting_tiles.length);
                for (let j = 0; j < message.awaiting_tiles.length; ++j)
                    object.awaiting_tiles[j] = message.awaiting_tiles[j];
            }
            if (message.left_tile_count != null && message.hasOwnProperty("left_tile_count"))
                object.left_tile_count = message.left_tile_count;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            return object;
        };

        RecordFillAwaitingTiles.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordFillAwaitingTiles.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordFillAwaitingTiles";
        };

        return RecordFillAwaitingTiles;
    })();

    lq.ActionChiPengGang = (function() {

        function ActionChiPengGang(properties) {
            this.tiles = [];
            this.froms = [];
            this.tingpais = [];
            this.tile_states = [];
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionChiPengGang.prototype.seat = 0;
        ActionChiPengGang.prototype.type = 0;
        ActionChiPengGang.prototype.tiles = $util.emptyArray;
        ActionChiPengGang.prototype.froms = $util.emptyArray;
        ActionChiPengGang.prototype.liqi = null;
        ActionChiPengGang.prototype.operation = null;
        ActionChiPengGang.prototype.zhenting = false;
        ActionChiPengGang.prototype.tingpais = $util.emptyArray;
        ActionChiPengGang.prototype.tile_states = $util.emptyArray;
        ActionChiPengGang.prototype.muyu = null;
        ActionChiPengGang.prototype.scores = $util.emptyArray;
        ActionChiPengGang.prototype.liqibang = 0;
        ActionChiPengGang.prototype.yongchang = null;
        ActionChiPengGang.prototype.hun_zhi_yi_ji_info = null;

        ActionChiPengGang.create = function create(properties) {
            return new ActionChiPengGang(properties);
        };

        ActionChiPengGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(26).string(message.tiles[i]);
            if (message.froms != null && message.froms.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.froms.length; ++i)
                    writer.uint32(message.froms[i]);
                writer.ldelim();
            }
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(50).fork()).ldelim();
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(56).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.tile_states != null && message.tile_states.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.tile_states.length; ++i)
                    writer.uint32(message.tile_states[i]);
                writer.ldelim();
            }
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(82).fork()).ldelim();
            if (message.scores != null && message.scores.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(96).uint32(message.liqibang);
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(106).fork()).ldelim();
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(114).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionChiPengGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionChiPengGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionChiPengGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.froms && message.froms.length))
                                message.froms = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.froms.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.froms && message.froms.length))
                            message.froms = [];
                        message.froms.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.tile_states && message.tile_states.length))
                                message.tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_states.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_states && message.tile_states.length))
                            message.tile_states = [];
                        message.tile_states.push(reader.uint32());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionChiPengGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionChiPengGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.froms != null && message.hasOwnProperty("froms")) {
                if (!Array.isArray(message.froms))
                    return "froms: array expected";
                for (let i = 0; i < message.froms.length; ++i)
                    if (!$util.isInteger(message.froms[i]))
                        return "froms: integer[] expected";
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.tile_states != null && message.hasOwnProperty("tile_states")) {
                if (!Array.isArray(message.tile_states))
                    return "tile_states: array expected";
                for (let i = 0; i < message.tile_states.length; ++i)
                    if (!$util.isInteger(message.tile_states[i]))
                        return "tile_states: integer[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        ActionChiPengGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionChiPengGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionChiPengGang();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.ActionChiPengGang.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.froms) {
                if (!Array.isArray(object.froms))
                    throw TypeError(".lq.ActionChiPengGang.froms: array expected");
                message.froms = Array(object.froms.length);
                for (let i = 0; i < object.froms.length; ++i)
                    message.froms[i] = object.froms[i] >>> 0;
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.ActionChiPengGang.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionChiPengGang.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionChiPengGang.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionChiPengGang.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.tile_states) {
                if (!Array.isArray(object.tile_states))
                    throw TypeError(".lq.ActionChiPengGang.tile_states: array expected");
                message.tile_states = Array(object.tile_states.length);
                for (let i = 0; i < object.tile_states.length; ++i)
                    message.tile_states[i] = object.tile_states[i] >>> 0;
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionChiPengGang.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionChiPengGang.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.ActionChiPengGang.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.ActionChiPengGang.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        ActionChiPengGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.froms = [];
                object.tingpais = [];
                object.tile_states = [];
                object.scores = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.liqi = null;
                object.operation = null;
                object.zhenting = false;
                object.muyu = null;
                object.liqibang = 0;
                object.yongchang = null;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.froms && message.froms.length) {
                object.froms = Array(message.froms.length);
                for (let j = 0; j < message.froms.length; ++j)
                    object.froms[j] = message.froms[j];
            }
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais[j], options);
            }
            if (message.tile_states && message.tile_states.length) {
                object.tile_states = Array(message.tile_states.length);
                for (let j = 0; j < message.tile_states.length; ++j)
                    object.tile_states[j] = message.tile_states[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        ActionChiPengGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionChiPengGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionChiPengGang";
        };

        return ActionChiPengGang;
    })();

    lq.RecordChiPengGang = (function() {

        function RecordChiPengGang(properties) {
            this.tiles = [];
            this.froms = [];
            this.zhenting = [];
            this.tile_states = [];
            this.scores = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordChiPengGang.prototype.seat = 0;
        RecordChiPengGang.prototype.type = 0;
        RecordChiPengGang.prototype.tiles = $util.emptyArray;
        RecordChiPengGang.prototype.froms = $util.emptyArray;
        RecordChiPengGang.prototype.liqi = null;
        RecordChiPengGang.prototype.zhenting = $util.emptyArray;
        RecordChiPengGang.prototype.operation = null;
        RecordChiPengGang.prototype.tile_states = $util.emptyArray;
        RecordChiPengGang.prototype.muyu = null;
        RecordChiPengGang.prototype.scores = $util.emptyArray;
        RecordChiPengGang.prototype.liqibang = 0;
        RecordChiPengGang.prototype.yongchang = null;
        RecordChiPengGang.prototype.hun_zhi_yi_ji_info = null;

        RecordChiPengGang.create = function create(properties) {
            return new RecordChiPengGang(properties);
        };

        RecordChiPengGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(26).string(message.tiles[i]);
            if (message.froms != null && message.froms.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.froms.length; ++i)
                    writer.uint32(message.froms[i]);
                writer.ldelim();
            }
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.zhenting != null && message.zhenting.length) {
                writer.uint32(58).fork();
                for (let i = 0; i < message.zhenting.length; ++i)
                    writer.bool(message.zhenting[i]);
                writer.ldelim();
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(66).fork()).ldelim();
            if (message.tile_states != null && message.tile_states.length) {
                writer.uint32(74).fork();
                for (let i = 0; i < message.tile_states.length; ++i)
                    writer.uint32(message.tile_states[i]);
                writer.ldelim();
            }
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(82).fork()).ldelim();
            if (message.scores != null && message.scores.length) {
                writer.uint32(90).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.liqibang != null && Object.hasOwnProperty.call(message, "liqibang"))
                writer.uint32(96).uint32(message.liqibang);
            if (message.yongchang != null && Object.hasOwnProperty.call(message, "yongchang"))
                $root.lq.YongchangInfo.encode(message.yongchang, writer.uint32(106).fork()).ldelim();
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(114).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordChiPengGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordChiPengGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordChiPengGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.froms && message.froms.length))
                                message.froms = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.froms.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.froms && message.froms.length))
                            message.froms = [];
                        message.froms.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 7: {
                        if (wireType === 2) {
                            if (!(message.zhenting && message.zhenting.length))
                                message.zhenting = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhenting.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhenting && message.zhenting.length))
                            message.zhenting = [];
                        message.zhenting.push(reader.bool());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 9: {
                        if (wireType === 2) {
                            if (!(message.tile_states && message.tile_states.length))
                                message.tile_states = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.tile_states.push(reader.uint32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.tile_states && message.tile_states.length))
                            message.tile_states = [];
                        message.tile_states.push(reader.uint32());
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 11: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.liqibang = value;
                        else
                            delete message.liqibang;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.yongchang = $root.lq.YongchangInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.yongchang);
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordChiPengGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordChiPengGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.froms != null && message.hasOwnProperty("froms")) {
                if (!Array.isArray(message.froms))
                    return "froms: array expected";
                for (let i = 0; i < message.froms.length; ++i)
                    if (!$util.isInteger(message.froms[i]))
                        return "froms: integer[] expected";
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting")) {
                if (!Array.isArray(message.zhenting))
                    return "zhenting: array expected";
                for (let i = 0; i < message.zhenting.length; ++i)
                    if (typeof message.zhenting[i] !== "boolean")
                        return "zhenting: boolean[] expected";
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.tile_states != null && message.hasOwnProperty("tile_states")) {
                if (!Array.isArray(message.tile_states))
                    return "tile_states: array expected";
                for (let i = 0; i < message.tile_states.length; ++i)
                    if (!$util.isInteger(message.tile_states[i]))
                        return "tile_states: integer[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                if (!$util.isInteger(message.liqibang))
                    return "liqibang: integer expected";
            if (message.yongchang != null && message.hasOwnProperty("yongchang")) {
                let error = $root.lq.YongchangInfo.verify(message.yongchang, _depth + 1);
                if (error)
                    return "yongchang." + error;
            }
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        RecordChiPengGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordChiPengGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordChiPengGang();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.RecordChiPengGang.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.froms) {
                if (!Array.isArray(object.froms))
                    throw TypeError(".lq.RecordChiPengGang.froms: array expected");
                message.froms = Array(object.froms.length);
                for (let i = 0; i < object.froms.length; ++i)
                    message.froms[i] = object.froms[i] >>> 0;
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.RecordChiPengGang.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.zhenting) {
                if (!Array.isArray(object.zhenting))
                    throw TypeError(".lq.RecordChiPengGang.zhenting: array expected");
                message.zhenting = Array(object.zhenting.length);
                for (let i = 0; i < object.zhenting.length; ++i)
                    message.zhenting[i] = Boolean(object.zhenting[i]);
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordChiPengGang.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.tile_states) {
                if (!Array.isArray(object.tile_states))
                    throw TypeError(".lq.RecordChiPengGang.tile_states: array expected");
                message.tile_states = Array(object.tile_states.length);
                for (let i = 0; i < object.tile_states.length; ++i)
                    message.tile_states[i] = object.tile_states[i] >>> 0;
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordChiPengGang.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordChiPengGang.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.liqibang != null)
                if (Number(object.liqibang) !== 0)
                    message.liqibang = object.liqibang >>> 0;
            if (object.yongchang != null) {
                if (typeof object.yongchang !== "object")
                    throw TypeError(".lq.RecordChiPengGang.yongchang: object expected");
                message.yongchang = $root.lq.YongchangInfo.fromObject(object.yongchang, _depth + 1);
            }
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.RecordChiPengGang.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        RecordChiPengGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.froms = [];
                object.zhenting = [];
                object.tile_states = [];
                object.scores = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.liqi = null;
                object.operation = null;
                object.muyu = null;
                object.liqibang = 0;
                object.yongchang = null;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.froms && message.froms.length) {
                object.froms = Array(message.froms.length);
                for (let j = 0; j < message.froms.length; ++j)
                    object.froms[j] = message.froms[j];
            }
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.zhenting && message.zhenting.length) {
                object.zhenting = Array(message.zhenting.length);
                for (let j = 0; j < message.zhenting.length; ++j)
                    object.zhenting[j] = message.zhenting[j];
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.tile_states && message.tile_states.length) {
                object.tile_states = Array(message.tile_states.length);
                for (let j = 0; j < message.tile_states.length; ++j)
                    object.tile_states[j] = message.tile_states[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.liqibang != null && message.hasOwnProperty("liqibang"))
                object.liqibang = message.liqibang;
            if (message.yongchang != null && message.hasOwnProperty("yongchang"))
                object.yongchang = $root.lq.YongchangInfo.toObject(message.yongchang, options);
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        RecordChiPengGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordChiPengGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordChiPengGang";
        };

        return RecordChiPengGang;
    })();

    lq.ActionGangResult = (function() {

        function ActionGangResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionGangResult.prototype.gang_infos = null;

        ActionGangResult.create = function create(properties) {
            return new ActionGangResult(properties);
        };

        ActionGangResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gang_infos != null && Object.hasOwnProperty.call(message, "gang_infos"))
                $root.lq.ChuanmaGang.encode(message.gang_infos, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionGangResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionGangResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionGangResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.gang_infos = $root.lq.ChuanmaGang.decode(reader, reader.uint32(), undefined, _depth + 1, message.gang_infos);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionGangResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionGangResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos")) {
                let error = $root.lq.ChuanmaGang.verify(message.gang_infos, _depth + 1);
                if (error)
                    return "gang_infos." + error;
            }
            return null;
        };

        ActionGangResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionGangResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionGangResult();
            if (object.gang_infos != null) {
                if (typeof object.gang_infos !== "object")
                    throw TypeError(".lq.ActionGangResult.gang_infos: object expected");
                message.gang_infos = $root.lq.ChuanmaGang.fromObject(object.gang_infos, _depth + 1);
            }
            return message;
        };

        ActionGangResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.gang_infos = null;
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos"))
                object.gang_infos = $root.lq.ChuanmaGang.toObject(message.gang_infos, options);
            return object;
        };

        ActionGangResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionGangResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionGangResult";
        };

        return ActionGangResult;
    })();

    lq.RecordGangResult = (function() {

        function RecordGangResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordGangResult.prototype.gang_infos = null;

        RecordGangResult.create = function create(properties) {
            return new RecordGangResult(properties);
        };

        RecordGangResult.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gang_infos != null && Object.hasOwnProperty.call(message, "gang_infos"))
                $root.lq.ChuanmaGang.encode(message.gang_infos, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordGangResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordGangResult.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordGangResult(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.gang_infos = $root.lq.ChuanmaGang.decode(reader, reader.uint32(), undefined, _depth + 1, message.gang_infos);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordGangResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordGangResult.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos")) {
                let error = $root.lq.ChuanmaGang.verify(message.gang_infos, _depth + 1);
                if (error)
                    return "gang_infos." + error;
            }
            return null;
        };

        RecordGangResult.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordGangResult)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordGangResult();
            if (object.gang_infos != null) {
                if (typeof object.gang_infos !== "object")
                    throw TypeError(".lq.RecordGangResult.gang_infos: object expected");
                message.gang_infos = $root.lq.ChuanmaGang.fromObject(object.gang_infos, _depth + 1);
            }
            return message;
        };

        RecordGangResult.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.gang_infos = null;
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos"))
                object.gang_infos = $root.lq.ChuanmaGang.toObject(message.gang_infos, options);
            return object;
        };

        RecordGangResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordGangResult.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordGangResult";
        };

        return RecordGangResult;
    })();

    lq.ActionGangResultEnd = (function() {

        function ActionGangResultEnd(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionGangResultEnd.prototype.gang_infos = null;

        ActionGangResultEnd.create = function create(properties) {
            return new ActionGangResultEnd(properties);
        };

        ActionGangResultEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gang_infos != null && Object.hasOwnProperty.call(message, "gang_infos"))
                $root.lq.ChuanmaGang.encode(message.gang_infos, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionGangResultEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionGangResultEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionGangResultEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.gang_infos = $root.lq.ChuanmaGang.decode(reader, reader.uint32(), undefined, _depth + 1, message.gang_infos);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionGangResultEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionGangResultEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos")) {
                let error = $root.lq.ChuanmaGang.verify(message.gang_infos, _depth + 1);
                if (error)
                    return "gang_infos." + error;
            }
            return null;
        };

        ActionGangResultEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionGangResultEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionGangResultEnd();
            if (object.gang_infos != null) {
                if (typeof object.gang_infos !== "object")
                    throw TypeError(".lq.ActionGangResultEnd.gang_infos: object expected");
                message.gang_infos = $root.lq.ChuanmaGang.fromObject(object.gang_infos, _depth + 1);
            }
            return message;
        };

        ActionGangResultEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.gang_infos = null;
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos"))
                object.gang_infos = $root.lq.ChuanmaGang.toObject(message.gang_infos, options);
            return object;
        };

        ActionGangResultEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionGangResultEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionGangResultEnd";
        };

        return ActionGangResultEnd;
    })();

    lq.RecordGangResultEnd = (function() {

        function RecordGangResultEnd(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordGangResultEnd.prototype.gang_infos = null;

        RecordGangResultEnd.create = function create(properties) {
            return new RecordGangResultEnd(properties);
        };

        RecordGangResultEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.gang_infos != null && Object.hasOwnProperty.call(message, "gang_infos"))
                $root.lq.ChuanmaGang.encode(message.gang_infos, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordGangResultEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordGangResultEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordGangResultEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.gang_infos = $root.lq.ChuanmaGang.decode(reader, reader.uint32(), undefined, _depth + 1, message.gang_infos);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordGangResultEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordGangResultEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos")) {
                let error = $root.lq.ChuanmaGang.verify(message.gang_infos, _depth + 1);
                if (error)
                    return "gang_infos." + error;
            }
            return null;
        };

        RecordGangResultEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordGangResultEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordGangResultEnd();
            if (object.gang_infos != null) {
                if (typeof object.gang_infos !== "object")
                    throw TypeError(".lq.RecordGangResultEnd.gang_infos: object expected");
                message.gang_infos = $root.lq.ChuanmaGang.fromObject(object.gang_infos, _depth + 1);
            }
            return message;
        };

        RecordGangResultEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.gang_infos = null;
            if (message.gang_infos != null && message.hasOwnProperty("gang_infos"))
                object.gang_infos = $root.lq.ChuanmaGang.toObject(message.gang_infos, options);
            return object;
        };

        RecordGangResultEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordGangResultEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordGangResultEnd";
        };

        return RecordGangResultEnd;
    })();

    lq.ActionAnGangAddGang = (function() {

        function ActionAnGangAddGang(properties) {
            this.doras = [];
            this.tingpais = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionAnGangAddGang.prototype.seat = 0;
        ActionAnGangAddGang.prototype.type = 0;
        ActionAnGangAddGang.prototype.tiles = "";
        ActionAnGangAddGang.prototype.operation = null;
        ActionAnGangAddGang.prototype.doras = $util.emptyArray;
        ActionAnGangAddGang.prototype.zhenting = false;
        ActionAnGangAddGang.prototype.tingpais = $util.emptyArray;
        ActionAnGangAddGang.prototype.muyu = null;

        ActionAnGangAddGang.create = function create(properties) {
            return new ActionAnGangAddGang(properties);
        };

        ActionAnGangAddGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.tiles != null && Object.hasOwnProperty.call(message, "tiles"))
                writer.uint32(26).string(message.tiles);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(56).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionAnGangAddGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionAnGangAddGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionAnGangAddGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tiles = value;
                        else
                            delete message.tiles;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionAnGangAddGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionAnGangAddGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles"))
                if (!$util.isString(message.tiles))
                    return "tiles: string expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            return null;
        };

        ActionAnGangAddGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionAnGangAddGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionAnGangAddGang();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tiles != null)
                if (typeof object.tiles !== "string" || object.tiles.length)
                    message.tiles = String(object.tiles);
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionAnGangAddGang.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionAnGangAddGang.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionAnGangAddGang.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionAnGangAddGang.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionAnGangAddGang.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            return message;
        };

        ActionAnGangAddGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.tingpais = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.tiles = "";
                object.operation = null;
                object.zhenting = false;
                object.muyu = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tiles != null && message.hasOwnProperty("tiles"))
                object.tiles = message.tiles;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            return object;
        };

        ActionAnGangAddGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionAnGangAddGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionAnGangAddGang";
        };

        return ActionAnGangAddGang;
    })();

    lq.RecordAnGangAddGang = (function() {

        function RecordAnGangAddGang(properties) {
            this.doras = [];
            this.operations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordAnGangAddGang.prototype.seat = 0;
        RecordAnGangAddGang.prototype.type = 0;
        RecordAnGangAddGang.prototype.tiles = "";
        RecordAnGangAddGang.prototype.doras = $util.emptyArray;
        RecordAnGangAddGang.prototype.operations = $util.emptyArray;
        RecordAnGangAddGang.prototype.muyu = null;

        RecordAnGangAddGang.create = function create(properties) {
            return new RecordAnGangAddGang(properties);
        };

        RecordAnGangAddGang.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.tiles != null && Object.hasOwnProperty.call(message, "tiles"))
                writer.uint32(26).string(message.tiles);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(58).fork()).ldelim();
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordAnGangAddGang.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordAnGangAddGang.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordAnGangAddGang(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.tiles = value;
                        else
                            delete message.tiles;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordAnGangAddGang.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordAnGangAddGang.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles"))
                if (!$util.isString(message.tiles))
                    return "tiles: string expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            return null;
        };

        RecordAnGangAddGang.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordAnGangAddGang)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordAnGangAddGang();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.tiles != null)
                if (typeof object.tiles !== "string" || object.tiles.length)
                    message.tiles = String(object.tiles);
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordAnGangAddGang.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordAnGangAddGang.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordAnGangAddGang.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordAnGangAddGang.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            return message;
        };

        RecordAnGangAddGang.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.operations = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.type = 0;
                object.tiles = "";
                object.muyu = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.tiles != null && message.hasOwnProperty("tiles"))
                object.tiles = message.tiles;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            return object;
        };

        RecordAnGangAddGang.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordAnGangAddGang.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordAnGangAddGang";
        };

        return RecordAnGangAddGang;
    })();

    lq.ActionBaBei = (function() {

        function ActionBaBei(properties) {
            this.doras = [];
            this.tingpais = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionBaBei.prototype.seat = 0;
        ActionBaBei.prototype.operation = null;
        ActionBaBei.prototype.doras = $util.emptyArray;
        ActionBaBei.prototype.zhenting = false;
        ActionBaBei.prototype.tingpais = $util.emptyArray;
        ActionBaBei.prototype.moqie = false;
        ActionBaBei.prototype.tile_state = 0;
        ActionBaBei.prototype.muyu = null;

        ActionBaBei.create = function create(properties) {
            return new ActionBaBei(properties);
        };

        ActionBaBei.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(56).bool(message.zhenting);
            if (message.tingpais != null && message.tingpais.length)
                for (let i = 0; i < message.tingpais.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais[i], writer.uint32(66).fork()).ldelim();
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(72).bool(message.moqie);
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(80).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(90).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionBaBei.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionBaBei.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionBaBei(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais && message.tingpais.length))
                            message.tingpais = [];
                        message.tingpais.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionBaBei.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionBaBei.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            if (message.tingpais != null && message.hasOwnProperty("tingpais")) {
                if (!Array.isArray(message.tingpais))
                    return "tingpais: array expected";
                for (let i = 0; i < message.tingpais.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais[i], _depth + 1);
                    if (error)
                        return "tingpais." + error;
                }
            }
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            return null;
        };

        ActionBaBei.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionBaBei)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionBaBei();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionBaBei.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionBaBei.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            if (object.tingpais) {
                if (!Array.isArray(object.tingpais))
                    throw TypeError(".lq.ActionBaBei.tingpais: array expected");
                message.tingpais = Array(object.tingpais.length);
                for (let i = 0; i < object.tingpais.length; ++i) {
                    if (typeof object.tingpais[i] !== "object")
                        throw TypeError(".lq.ActionBaBei.tingpais: object expected");
                    message.tingpais[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais[i], _depth + 1);
                }
            }
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionBaBei.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            return message;
        };

        ActionBaBei.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.tingpais = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.operation = null;
                object.zhenting = false;
                object.moqie = false;
                object.tile_state = 0;
                object.muyu = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            if (message.tingpais && message.tingpais.length) {
                object.tingpais = Array(message.tingpais.length);
                for (let j = 0; j < message.tingpais.length; ++j)
                    object.tingpais[j] = $root.lq.TingPaiInfo.toObject(message.tingpais[j], options);
            }
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            return object;
        };

        ActionBaBei.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionBaBei.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionBaBei";
        };

        return ActionBaBei;
    })();

    lq.RecordBaBei = (function() {

        function RecordBaBei(properties) {
            this.doras = [];
            this.operations = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordBaBei.prototype.seat = 0;
        RecordBaBei.prototype.doras = $util.emptyArray;
        RecordBaBei.prototype.operations = $util.emptyArray;
        RecordBaBei.prototype.moqie = false;
        RecordBaBei.prototype.tile_state = 0;
        RecordBaBei.prototype.muyu = null;

        RecordBaBei.create = function create(properties) {
            return new RecordBaBei(properties);
        };

        RecordBaBei.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.lq.OptionalOperationList.encode(message.operations[i], writer.uint32(58).fork()).ldelim();
            if (message.moqie != null && Object.hasOwnProperty.call(message, "moqie"))
                writer.uint32(64).bool(message.moqie);
            if (message.tile_state != null && Object.hasOwnProperty.call(message, "tile_state"))
                writer.uint32(80).uint32(message.tile_state);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(90).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordBaBei.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordBaBei.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordBaBei(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.moqie = value;
                        else
                            delete message.moqie;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.tile_state = value;
                        else
                            delete message.tile_state;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordBaBei.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordBaBei.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.operations != null && message.hasOwnProperty("operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.lq.OptionalOperationList.verify(message.operations[i], _depth + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                if (typeof message.moqie !== "boolean")
                    return "moqie: boolean expected";
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                if (!$util.isInteger(message.tile_state))
                    return "tile_state: integer expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            return null;
        };

        RecordBaBei.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordBaBei)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordBaBei();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordBaBei.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".lq.RecordBaBei.operations: array expected");
                message.operations = Array(object.operations.length);
                for (let i = 0; i < object.operations.length; ++i) {
                    if (typeof object.operations[i] !== "object")
                        throw TypeError(".lq.RecordBaBei.operations: object expected");
                    message.operations[i] = $root.lq.OptionalOperationList.fromObject(object.operations[i], _depth + 1);
                }
            }
            if (object.moqie != null)
                if (object.moqie)
                    message.moqie = Boolean(object.moqie);
            if (object.tile_state != null)
                if (Number(object.tile_state) !== 0)
                    message.tile_state = object.tile_state >>> 0;
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordBaBei.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            return message;
        };

        RecordBaBei.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.doras = [];
                object.operations = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.moqie = false;
                object.tile_state = 0;
                object.muyu = null;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.operations && message.operations.length) {
                object.operations = Array(message.operations.length);
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.lq.OptionalOperationList.toObject(message.operations[j], options);
            }
            if (message.moqie != null && message.hasOwnProperty("moqie"))
                object.moqie = message.moqie;
            if (message.tile_state != null && message.hasOwnProperty("tile_state"))
                object.tile_state = message.tile_state;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            return object;
        };

        RecordBaBei.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordBaBei.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordBaBei";
        };

        return RecordBaBei;
    })();

    lq.ActionHule = (function() {

        function ActionHule(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionHule.prototype.hules = $util.emptyArray;
        ActionHule.prototype.old_scores = $util.emptyArray;
        ActionHule.prototype.delta_scores = $util.emptyArray;
        ActionHule.prototype.wait_timeout = 0;
        ActionHule.prototype.scores = $util.emptyArray;
        ActionHule.prototype.gameend = null;
        ActionHule.prototype.doras = $util.emptyArray;
        ActionHule.prototype.muyu = null;
        ActionHule.prototype.baopai = 0;
        ActionHule.prototype.hun_zhi_yi_ji_info = null;

        ActionHule.create = function create(properties) {
            return new ActionHule(properties);
        };

        ActionHule.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.wait_timeout != null && Object.hasOwnProperty.call(message, "wait_timeout"))
                writer.uint32(32).uint32(message.wait_timeout);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(50).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.baopai != null && Object.hasOwnProperty.call(message, "baopai"))
                writer.uint32(72).int32(message.baopai);
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(82).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionHule.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionHule.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionHule(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.wait_timeout = value;
                        else
                            delete message.wait_timeout;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.baopai = value;
                        else
                            delete message.baopai;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionHule.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionHule.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                if (!$util.isInteger(message.wait_timeout))
                    return "wait_timeout: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                if (!$util.isInteger(message.baopai))
                    return "baopai: integer expected";
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        ActionHule.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionHule)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionHule();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.ActionHule.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.ActionHule.hules: object expected");
                    message.hules[i] = $root.lq.HuleInfo.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.ActionHule.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.ActionHule.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.wait_timeout != null)
                if (Number(object.wait_timeout) !== 0)
                    message.wait_timeout = object.wait_timeout >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionHule.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.ActionHule.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionHule.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionHule.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.baopai != null)
                if (Number(object.baopai) !== 0)
                    message.baopai = object.baopai | 0;
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.ActionHule.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        ActionHule.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
            }
            if (options.defaults) {
                object.wait_timeout = 0;
                object.gameend = null;
                object.muyu = null;
                object.baopai = 0;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuleInfo.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                object.wait_timeout = message.wait_timeout;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                object.baopai = message.baopai;
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        ActionHule.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionHule.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionHule";
        };

        return ActionHule;
    })();

    lq.RecordHule = (function() {

        function RecordHule(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordHule.prototype.hules = $util.emptyArray;
        RecordHule.prototype.old_scores = $util.emptyArray;
        RecordHule.prototype.delta_scores = $util.emptyArray;
        RecordHule.prototype.wait_timeout = 0;
        RecordHule.prototype.scores = $util.emptyArray;
        RecordHule.prototype.gameend = null;
        RecordHule.prototype.doras = $util.emptyArray;
        RecordHule.prototype.muyu = null;
        RecordHule.prototype.baopai = 0;
        RecordHule.prototype.hun_zhi_yi_ji_info = null;

        RecordHule.create = function create(properties) {
            return new RecordHule(properties);
        };

        RecordHule.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.wait_timeout != null && Object.hasOwnProperty.call(message, "wait_timeout"))
                writer.uint32(32).uint32(message.wait_timeout);
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(50).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.baopai != null && Object.hasOwnProperty.call(message, "baopai"))
                writer.uint32(72).int32(message.baopai);
            if (message.hun_zhi_yi_ji_info != null && Object.hasOwnProperty.call(message, "hun_zhi_yi_ji_info"))
                $root.lq.HunZhiYiJiBuffInfo.encode(message.hun_zhi_yi_ji_info, writer.uint32(82).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordHule.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordHule.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHule(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.wait_timeout = value;
                        else
                            delete message.wait_timeout;
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.baopai = value;
                        else
                            delete message.baopai;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.hun_zhi_yi_ji_info);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordHule.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordHule.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                if (!$util.isInteger(message.wait_timeout))
                    return "wait_timeout: integer expected";
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                if (!$util.isInteger(message.baopai))
                    return "baopai: integer expected";
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info")) {
                let error = $root.lq.HunZhiYiJiBuffInfo.verify(message.hun_zhi_yi_ji_info, _depth + 1);
                if (error)
                    return "hun_zhi_yi_ji_info." + error;
            }
            return null;
        };

        RecordHule.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordHule)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordHule();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.RecordHule.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.RecordHule.hules: object expected");
                    message.hules[i] = $root.lq.HuleInfo.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.RecordHule.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.RecordHule.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.wait_timeout != null)
                if (Number(object.wait_timeout) !== 0)
                    message.wait_timeout = object.wait_timeout >>> 0;
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordHule.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.RecordHule.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordHule.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordHule.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.baopai != null)
                if (Number(object.baopai) !== 0)
                    message.baopai = object.baopai | 0;
            if (object.hun_zhi_yi_ji_info != null) {
                if (typeof object.hun_zhi_yi_ji_info !== "object")
                    throw TypeError(".lq.RecordHule.hun_zhi_yi_ji_info: object expected");
                message.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.fromObject(object.hun_zhi_yi_ji_info, _depth + 1);
            }
            return message;
        };

        RecordHule.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
            }
            if (options.defaults) {
                object.wait_timeout = 0;
                object.gameend = null;
                object.muyu = null;
                object.baopai = 0;
                object.hun_zhi_yi_ji_info = null;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuleInfo.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                object.wait_timeout = message.wait_timeout;
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.baopai != null && message.hasOwnProperty("baopai"))
                object.baopai = message.baopai;
            if (message.hun_zhi_yi_ji_info != null && message.hasOwnProperty("hun_zhi_yi_ji_info"))
                object.hun_zhi_yi_ji_info = $root.lq.HunZhiYiJiBuffInfo.toObject(message.hun_zhi_yi_ji_info, options);
            return object;
        };

        RecordHule.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordHule.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordHule";
        };

        return RecordHule;
    })();

    lq.HuInfoXueZhanMid = (function() {

        function HuInfoXueZhanMid(properties) {
            this.hand = [];
            this.ming = [];
            this.fans = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        HuInfoXueZhanMid.prototype.seat = 0;
        HuInfoXueZhanMid.prototype.hand_count = 0;
        HuInfoXueZhanMid.prototype.hand = $util.emptyArray;
        HuInfoXueZhanMid.prototype.ming = $util.emptyArray;
        HuInfoXueZhanMid.prototype.hu_tile = "";
        HuInfoXueZhanMid.prototype.zimo = false;
        HuInfoXueZhanMid.prototype.yiman = false;
        HuInfoXueZhanMid.prototype.count = 0;
        HuInfoXueZhanMid.prototype.fans = $util.emptyArray;
        HuInfoXueZhanMid.prototype.fu = 0;
        HuInfoXueZhanMid.prototype.title_id = 0;

        HuInfoXueZhanMid.create = function create(properties) {
            return new HuInfoXueZhanMid(properties);
        };

        HuInfoXueZhanMid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.hand_count != null && Object.hasOwnProperty.call(message, "hand_count"))
                writer.uint32(16).uint32(message.hand_count);
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(26).string(message.hand[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(34).string(message.ming[i]);
            if (message.hu_tile != null && Object.hasOwnProperty.call(message, "hu_tile"))
                writer.uint32(42).string(message.hu_tile);
            if (message.zimo != null && Object.hasOwnProperty.call(message, "zimo"))
                writer.uint32(48).bool(message.zimo);
            if (message.yiman != null && Object.hasOwnProperty.call(message, "yiman"))
                writer.uint32(56).bool(message.yiman);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(64).uint32(message.count);
            if (message.fans != null && message.fans.length)
                for (let i = 0; i < message.fans.length; ++i)
                    $root.lq.FanInfo.encode(message.fans[i], writer.uint32(74).fork()).ldelim();
            if (message.fu != null && Object.hasOwnProperty.call(message, "fu"))
                writer.uint32(80).uint32(message.fu);
            if (message.title_id != null && Object.hasOwnProperty.call(message, "title_id"))
                writer.uint32(88).uint32(message.title_id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HuInfoXueZhanMid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        HuInfoXueZhanMid.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.HuInfoXueZhanMid(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.hand_count = value;
                        else
                            delete message.hand_count;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.hu_tile = value;
                        else
                            delete message.hu_tile;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zimo = value;
                        else
                            delete message.zimo;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.yiman = value;
                        else
                            delete message.yiman;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.fans && message.fans.length))
                            message.fans = [];
                        message.fans.push($root.lq.FanInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fu = value;
                        else
                            delete message.fu;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.title_id = value;
                        else
                            delete message.title_id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        HuInfoXueZhanMid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        HuInfoXueZhanMid.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.hand_count != null && message.hasOwnProperty("hand_count"))
                if (!$util.isInteger(message.hand_count))
                    return "hand_count: integer expected";
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                if (!$util.isString(message.hu_tile))
                    return "hu_tile: string expected";
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                if (typeof message.zimo !== "boolean")
                    return "zimo: boolean expected";
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                if (typeof message.yiman !== "boolean")
                    return "yiman: boolean expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.fans != null && message.hasOwnProperty("fans")) {
                if (!Array.isArray(message.fans))
                    return "fans: array expected";
                for (let i = 0; i < message.fans.length; ++i) {
                    let error = $root.lq.FanInfo.verify(message.fans[i], _depth + 1);
                    if (error)
                        return "fans." + error;
                }
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                if (!$util.isInteger(message.fu))
                    return "fu: integer expected";
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                if (!$util.isInteger(message.title_id))
                    return "title_id: integer expected";
            return null;
        };

        HuInfoXueZhanMid.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.HuInfoXueZhanMid)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.HuInfoXueZhanMid();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.hand_count != null)
                if (Number(object.hand_count) !== 0)
                    message.hand_count = object.hand_count >>> 0;
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.HuInfoXueZhanMid.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.HuInfoXueZhanMid.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.hu_tile != null)
                if (typeof object.hu_tile !== "string" || object.hu_tile.length)
                    message.hu_tile = String(object.hu_tile);
            if (object.zimo != null)
                if (object.zimo)
                    message.zimo = Boolean(object.zimo);
            if (object.yiman != null)
                if (object.yiman)
                    message.yiman = Boolean(object.yiman);
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.fans) {
                if (!Array.isArray(object.fans))
                    throw TypeError(".lq.HuInfoXueZhanMid.fans: array expected");
                message.fans = Array(object.fans.length);
                for (let i = 0; i < object.fans.length; ++i) {
                    if (typeof object.fans[i] !== "object")
                        throw TypeError(".lq.HuInfoXueZhanMid.fans: object expected");
                    message.fans[i] = $root.lq.FanInfo.fromObject(object.fans[i], _depth + 1);
                }
            }
            if (object.fu != null)
                if (Number(object.fu) !== 0)
                    message.fu = object.fu >>> 0;
            if (object.title_id != null)
                if (Number(object.title_id) !== 0)
                    message.title_id = object.title_id >>> 0;
            return message;
        };

        HuInfoXueZhanMid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hand = [];
                object.ming = [];
                object.fans = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.hand_count = 0;
                object.hu_tile = "";
                object.zimo = false;
                object.yiman = false;
                object.count = 0;
                object.fu = 0;
                object.title_id = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.hand_count != null && message.hasOwnProperty("hand_count"))
                object.hand_count = message.hand_count;
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.hu_tile != null && message.hasOwnProperty("hu_tile"))
                object.hu_tile = message.hu_tile;
            if (message.zimo != null && message.hasOwnProperty("zimo"))
                object.zimo = message.zimo;
            if (message.yiman != null && message.hasOwnProperty("yiman"))
                object.yiman = message.yiman;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.fans && message.fans.length) {
                object.fans = Array(message.fans.length);
                for (let j = 0; j < message.fans.length; ++j)
                    object.fans[j] = $root.lq.FanInfo.toObject(message.fans[j], options);
            }
            if (message.fu != null && message.hasOwnProperty("fu"))
                object.fu = message.fu;
            if (message.title_id != null && message.hasOwnProperty("title_id"))
                object.title_id = message.title_id;
            return object;
        };

        HuInfoXueZhanMid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HuInfoXueZhanMid.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.HuInfoXueZhanMid";
        };

        return HuInfoXueZhanMid;
    })();

    lq.ActionHuleXueZhanMid = (function() {

        function ActionHuleXueZhanMid(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionHuleXueZhanMid.prototype.hules = $util.emptyArray;
        ActionHuleXueZhanMid.prototype.old_scores = $util.emptyArray;
        ActionHuleXueZhanMid.prototype.delta_scores = $util.emptyArray;
        ActionHuleXueZhanMid.prototype.scores = $util.emptyArray;
        ActionHuleXueZhanMid.prototype.doras = $util.emptyArray;
        ActionHuleXueZhanMid.prototype.muyu = null;
        ActionHuleXueZhanMid.prototype.liqi = null;
        ActionHuleXueZhanMid.prototype.zhenting = false;

        ActionHuleXueZhanMid.create = function create(properties) {
            return new ActionHuleXueZhanMid(properties);
        };

        ActionHuleXueZhanMid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuInfoXueZhanMid.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(74).fork()).ldelim();
            if (message.zhenting != null && Object.hasOwnProperty.call(message, "zhenting"))
                writer.uint32(80).bool(message.zhenting);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionHuleXueZhanMid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionHuleXueZhanMid.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionHuleXueZhanMid(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuInfoXueZhanMid.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.zhenting = value;
                        else
                            delete message.zhenting;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionHuleXueZhanMid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionHuleXueZhanMid.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuInfoXueZhanMid.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                if (typeof message.zhenting !== "boolean")
                    return "zhenting: boolean expected";
            return null;
        };

        ActionHuleXueZhanMid.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionHuleXueZhanMid)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionHuleXueZhanMid();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.ActionHuleXueZhanMid.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.ActionHuleXueZhanMid.hules: object expected");
                    message.hules[i] = $root.lq.HuInfoXueZhanMid.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.ActionHuleXueZhanMid.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.ActionHuleXueZhanMid.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionHuleXueZhanMid.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionHuleXueZhanMid.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionHuleXueZhanMid.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.ActionHuleXueZhanMid.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.zhenting != null)
                if (object.zhenting)
                    message.zhenting = Boolean(object.zhenting);
            return message;
        };

        ActionHuleXueZhanMid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
            }
            if (options.defaults) {
                object.muyu = null;
                object.liqi = null;
                object.zhenting = false;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuInfoXueZhanMid.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.zhenting != null && message.hasOwnProperty("zhenting"))
                object.zhenting = message.zhenting;
            return object;
        };

        ActionHuleXueZhanMid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionHuleXueZhanMid.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionHuleXueZhanMid";
        };

        return ActionHuleXueZhanMid;
    })();

    lq.RecordHuleXueZhanMid = (function() {

        function RecordHuleXueZhanMid(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            this.zhenting = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordHuleXueZhanMid.prototype.hules = $util.emptyArray;
        RecordHuleXueZhanMid.prototype.old_scores = $util.emptyArray;
        RecordHuleXueZhanMid.prototype.delta_scores = $util.emptyArray;
        RecordHuleXueZhanMid.prototype.scores = $util.emptyArray;
        RecordHuleXueZhanMid.prototype.doras = $util.emptyArray;
        RecordHuleXueZhanMid.prototype.muyu = null;
        RecordHuleXueZhanMid.prototype.liqi = null;
        RecordHuleXueZhanMid.prototype.zhenting = $util.emptyArray;

        RecordHuleXueZhanMid.create = function create(properties) {
            return new RecordHuleXueZhanMid(properties);
        };

        RecordHuleXueZhanMid.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuInfoXueZhanMid.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.scores != null && message.scores.length) {
                writer.uint32(42).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(74).fork()).ldelim();
            if (message.zhenting != null && message.zhenting.length) {
                writer.uint32(82).fork();
                for (let i = 0; i < message.zhenting.length; ++i)
                    writer.bool(message.zhenting[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordHuleXueZhanMid.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordHuleXueZhanMid.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHuleXueZhanMid(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuInfoXueZhanMid.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 10: {
                        if (wireType === 2) {
                            if (!(message.zhenting && message.zhenting.length))
                                message.zhenting = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.zhenting.push(reader.bool());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.zhenting && message.zhenting.length))
                            message.zhenting = [];
                        message.zhenting.push(reader.bool());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordHuleXueZhanMid.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordHuleXueZhanMid.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuInfoXueZhanMid.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.zhenting != null && message.hasOwnProperty("zhenting")) {
                if (!Array.isArray(message.zhenting))
                    return "zhenting: array expected";
                for (let i = 0; i < message.zhenting.length; ++i)
                    if (typeof message.zhenting[i] !== "boolean")
                        return "zhenting: boolean[] expected";
            }
            return null;
        };

        RecordHuleXueZhanMid.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordHuleXueZhanMid)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordHuleXueZhanMid();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.RecordHuleXueZhanMid.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.RecordHuleXueZhanMid.hules: object expected");
                    message.hules[i] = $root.lq.HuInfoXueZhanMid.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.RecordHuleXueZhanMid.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.RecordHuleXueZhanMid.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordHuleXueZhanMid.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordHuleXueZhanMid.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordHuleXueZhanMid.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.RecordHuleXueZhanMid.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.zhenting) {
                if (!Array.isArray(object.zhenting))
                    throw TypeError(".lq.RecordHuleXueZhanMid.zhenting: array expected");
                message.zhenting = Array(object.zhenting.length);
                for (let i = 0; i < object.zhenting.length; ++i)
                    message.zhenting[i] = Boolean(object.zhenting[i]);
            }
            return message;
        };

        RecordHuleXueZhanMid.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
                object.zhenting = [];
            }
            if (options.defaults) {
                object.muyu = null;
                object.liqi = null;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuInfoXueZhanMid.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.zhenting && message.zhenting.length) {
                object.zhenting = Array(message.zhenting.length);
                for (let j = 0; j < message.zhenting.length; ++j)
                    object.zhenting[j] = message.zhenting[j];
            }
            return object;
        };

        RecordHuleXueZhanMid.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordHuleXueZhanMid.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordHuleXueZhanMid";
        };

        return RecordHuleXueZhanMid;
    })();

    lq.ActionHuleXueZhanEnd = (function() {

        function ActionHuleXueZhanEnd(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionHuleXueZhanEnd.prototype.hules = $util.emptyArray;
        ActionHuleXueZhanEnd.prototype.old_scores = $util.emptyArray;
        ActionHuleXueZhanEnd.prototype.delta_scores = $util.emptyArray;
        ActionHuleXueZhanEnd.prototype.scores = $util.emptyArray;
        ActionHuleXueZhanEnd.prototype.wait_timeout = 0;
        ActionHuleXueZhanEnd.prototype.gameend = null;
        ActionHuleXueZhanEnd.prototype.doras = $util.emptyArray;
        ActionHuleXueZhanEnd.prototype.muyu = null;
        ActionHuleXueZhanEnd.prototype.hules_history = $util.emptyArray;

        ActionHuleXueZhanEnd.create = function create(properties) {
            return new ActionHuleXueZhanEnd(properties);
        };

        ActionHuleXueZhanEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuInfoXueZhanMid.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.scores != null && message.scores.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.wait_timeout != null && Object.hasOwnProperty.call(message, "wait_timeout"))
                writer.uint32(40).uint32(message.wait_timeout);
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(50).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionHuleXueZhanEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionHuleXueZhanEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionHuleXueZhanEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuInfoXueZhanMid.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.wait_timeout = value;
                        else
                            delete message.wait_timeout;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionHuleXueZhanEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionHuleXueZhanEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuInfoXueZhanMid.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                if (!$util.isInteger(message.wait_timeout))
                    return "wait_timeout: integer expected";
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        ActionHuleXueZhanEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionHuleXueZhanEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionHuleXueZhanEnd();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.ActionHuleXueZhanEnd.hules: object expected");
                    message.hules[i] = $root.lq.HuInfoXueZhanMid.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.wait_timeout != null)
                if (Number(object.wait_timeout) !== 0)
                    message.wait_timeout = object.wait_timeout >>> 0;
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.ActionHuleXueZhanEnd.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionHuleXueZhanEnd.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.ActionHuleXueZhanEnd.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.ActionHuleXueZhanEnd.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        ActionHuleXueZhanEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.wait_timeout = 0;
                object.gameend = null;
                object.muyu = null;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuInfoXueZhanMid.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                object.wait_timeout = message.wait_timeout;
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        ActionHuleXueZhanEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionHuleXueZhanEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionHuleXueZhanEnd";
        };

        return ActionHuleXueZhanEnd;
    })();

    lq.RecordHuleXueZhanEnd = (function() {

        function RecordHuleXueZhanEnd(properties) {
            this.hules = [];
            this.old_scores = [];
            this.delta_scores = [];
            this.scores = [];
            this.doras = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordHuleXueZhanEnd.prototype.hules = $util.emptyArray;
        RecordHuleXueZhanEnd.prototype.old_scores = $util.emptyArray;
        RecordHuleXueZhanEnd.prototype.delta_scores = $util.emptyArray;
        RecordHuleXueZhanEnd.prototype.scores = $util.emptyArray;
        RecordHuleXueZhanEnd.prototype.wait_timeout = 0;
        RecordHuleXueZhanEnd.prototype.gameend = null;
        RecordHuleXueZhanEnd.prototype.doras = $util.emptyArray;
        RecordHuleXueZhanEnd.prototype.muyu = null;
        RecordHuleXueZhanEnd.prototype.hules_history = $util.emptyArray;

        RecordHuleXueZhanEnd.create = function create(properties) {
            return new RecordHuleXueZhanEnd(properties);
        };

        RecordHuleXueZhanEnd.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.hules != null && message.hules.length)
                for (let i = 0; i < message.hules.length; ++i)
                    $root.lq.HuInfoXueZhanMid.encode(message.hules[i], writer.uint32(10).fork()).ldelim();
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.scores != null && message.scores.length) {
                writer.uint32(34).fork();
                for (let i = 0; i < message.scores.length; ++i)
                    writer.int32(message.scores[i]);
                writer.ldelim();
            }
            if (message.wait_timeout != null && Object.hasOwnProperty.call(message, "wait_timeout"))
                writer.uint32(40).uint32(message.wait_timeout);
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(50).fork()).ldelim();
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(58).string(message.doras[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(66).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordHuleXueZhanEnd.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordHuleXueZhanEnd.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordHuleXueZhanEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules && message.hules.length))
                            message.hules = [];
                        message.hules.push($root.lq.HuInfoXueZhanMid.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.scores && message.scores.length))
                                message.scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push(reader.int32());
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.wait_timeout = value;
                        else
                            delete message.wait_timeout;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordHuleXueZhanEnd.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordHuleXueZhanEnd.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.hules != null && message.hasOwnProperty("hules")) {
                if (!Array.isArray(message.hules))
                    return "hules: array expected";
                for (let i = 0; i < message.hules.length; ++i) {
                    let error = $root.lq.HuInfoXueZhanMid.verify(message.hules[i], _depth + 1);
                    if (error)
                        return "hules." + error;
                }
            }
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i)
                    if (!$util.isInteger(message.scores[i]))
                        return "scores: integer[] expected";
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                if (!$util.isInteger(message.wait_timeout))
                    return "wait_timeout: integer expected";
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        RecordHuleXueZhanEnd.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordHuleXueZhanEnd)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordHuleXueZhanEnd();
            if (object.hules) {
                if (!Array.isArray(object.hules))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.hules: array expected");
                message.hules = Array(object.hules.length);
                for (let i = 0; i < object.hules.length; ++i) {
                    if (typeof object.hules[i] !== "object")
                        throw TypeError(".lq.RecordHuleXueZhanEnd.hules: object expected");
                    message.hules[i] = $root.lq.HuInfoXueZhanMid.fromObject(object.hules[i], _depth + 1);
                }
            }
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i)
                    message.scores[i] = object.scores[i] | 0;
            }
            if (object.wait_timeout != null)
                if (Number(object.wait_timeout) !== 0)
                    message.wait_timeout = object.wait_timeout >>> 0;
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.RecordHuleXueZhanEnd.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordHuleXueZhanEnd.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.RecordHuleXueZhanEnd.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.RecordHuleXueZhanEnd.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        RecordHuleXueZhanEnd.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hules = [];
                object.old_scores = [];
                object.delta_scores = [];
                object.scores = [];
                object.doras = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.wait_timeout = 0;
                object.gameend = null;
                object.muyu = null;
            }
            if (message.hules && message.hules.length) {
                object.hules = Array(message.hules.length);
                for (let j = 0; j < message.hules.length; ++j)
                    object.hules[j] = $root.lq.HuInfoXueZhanMid.toObject(message.hules[j], options);
            }
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = message.scores[j];
            }
            if (message.wait_timeout != null && message.hasOwnProperty("wait_timeout"))
                object.wait_timeout = message.wait_timeout;
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        RecordHuleXueZhanEnd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordHuleXueZhanEnd.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordHuleXueZhanEnd";
        };

        return RecordHuleXueZhanEnd;
    })();

    lq.ActionLiuJu = (function() {

        function ActionLiuJu(properties) {
            this.tiles = [];
            this.allplayertiles = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionLiuJu.prototype.type = 0;
        ActionLiuJu.prototype.gameend = null;
        ActionLiuJu.prototype.seat = 0;
        ActionLiuJu.prototype.tiles = $util.emptyArray;
        ActionLiuJu.prototype.liqi = null;
        ActionLiuJu.prototype.allplayertiles = $util.emptyArray;
        ActionLiuJu.prototype.muyu = null;
        ActionLiuJu.prototype.hules_history = $util.emptyArray;

        ActionLiuJu.create = function create(properties) {
            return new ActionLiuJu(properties);
        };

        ActionLiuJu.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(18).fork()).ldelim();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(24).uint32(message.seat);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(34).string(message.tiles[i]);
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.allplayertiles != null && message.allplayertiles.length)
                for (let i = 0; i < message.allplayertiles.length; ++i)
                    writer.uint32(50).string(message.allplayertiles[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(58).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionLiuJu.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionLiuJu.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionLiuJu(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.allplayertiles && message.allplayertiles.length))
                            message.allplayertiles = [];
                        message.allplayertiles.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionLiuJu.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionLiuJu.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.allplayertiles != null && message.hasOwnProperty("allplayertiles")) {
                if (!Array.isArray(message.allplayertiles))
                    return "allplayertiles: array expected";
                for (let i = 0; i < message.allplayertiles.length; ++i)
                    if (!$util.isString(message.allplayertiles[i]))
                        return "allplayertiles: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        ActionLiuJu.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionLiuJu)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionLiuJu();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.ActionLiuJu.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.ActionLiuJu.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.ActionLiuJu.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.allplayertiles) {
                if (!Array.isArray(object.allplayertiles))
                    throw TypeError(".lq.ActionLiuJu.allplayertiles: array expected");
                message.allplayertiles = Array(object.allplayertiles.length);
                for (let i = 0; i < object.allplayertiles.length; ++i)
                    message.allplayertiles[i] = String(object.allplayertiles[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionLiuJu.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.ActionLiuJu.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.ActionLiuJu.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        ActionLiuJu.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.allplayertiles = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.gameend = null;
                object.seat = 0;
                object.liqi = null;
                object.muyu = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.allplayertiles && message.allplayertiles.length) {
                object.allplayertiles = Array(message.allplayertiles.length);
                for (let j = 0; j < message.allplayertiles.length; ++j)
                    object.allplayertiles[j] = message.allplayertiles[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        ActionLiuJu.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionLiuJu.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionLiuJu";
        };

        return ActionLiuJu;
    })();

    lq.RecordLiuJu = (function() {

        function RecordLiuJu(properties) {
            this.tiles = [];
            this.allplayertiles = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordLiuJu.prototype.type = 0;
        RecordLiuJu.prototype.gameend = null;
        RecordLiuJu.prototype.seat = 0;
        RecordLiuJu.prototype.tiles = $util.emptyArray;
        RecordLiuJu.prototype.liqi = null;
        RecordLiuJu.prototype.allplayertiles = $util.emptyArray;
        RecordLiuJu.prototype.muyu = null;
        RecordLiuJu.prototype.hules_history = $util.emptyArray;

        RecordLiuJu.create = function create(properties) {
            return new RecordLiuJu(properties);
        };

        RecordLiuJu.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(8).uint32(message.type);
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                $root.lq.GameEnd.encode(message.gameend, writer.uint32(18).fork()).ldelim();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(24).uint32(message.seat);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(34).string(message.tiles[i]);
            if (message.liqi != null && Object.hasOwnProperty.call(message, "liqi"))
                $root.lq.LiQiSuccess.encode(message.liqi, writer.uint32(42).fork()).ldelim();
            if (message.allplayertiles != null && message.allplayertiles.length)
                for (let i = 0; i < message.allplayertiles.length; ++i)
                    writer.uint32(50).string(message.allplayertiles[i]);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(58).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordLiuJu.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordLiuJu.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordLiuJu(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.gameend = $root.lq.GameEnd.decode(reader, reader.uint32(), undefined, _depth + 1, message.gameend);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.liqi = $root.lq.LiQiSuccess.decode(reader, reader.uint32(), undefined, _depth + 1, message.liqi);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.allplayertiles && message.allplayertiles.length))
                            message.allplayertiles = [];
                        message.allplayertiles.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordLiuJu.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordLiuJu.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.gameend != null && message.hasOwnProperty("gameend")) {
                let error = $root.lq.GameEnd.verify(message.gameend, _depth + 1);
                if (error)
                    return "gameend." + error;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            if (message.liqi != null && message.hasOwnProperty("liqi")) {
                let error = $root.lq.LiQiSuccess.verify(message.liqi, _depth + 1);
                if (error)
                    return "liqi." + error;
            }
            if (message.allplayertiles != null && message.hasOwnProperty("allplayertiles")) {
                if (!Array.isArray(message.allplayertiles))
                    return "allplayertiles: array expected";
                for (let i = 0; i < message.allplayertiles.length; ++i)
                    if (!$util.isString(message.allplayertiles[i]))
                        return "allplayertiles: string[] expected";
            }
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        RecordLiuJu.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordLiuJu)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordLiuJu();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.gameend != null) {
                if (typeof object.gameend !== "object")
                    throw TypeError(".lq.RecordLiuJu.gameend: object expected");
                message.gameend = $root.lq.GameEnd.fromObject(object.gameend, _depth + 1);
            }
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.RecordLiuJu.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            if (object.liqi != null) {
                if (typeof object.liqi !== "object")
                    throw TypeError(".lq.RecordLiuJu.liqi: object expected");
                message.liqi = $root.lq.LiQiSuccess.fromObject(object.liqi, _depth + 1);
            }
            if (object.allplayertiles) {
                if (!Array.isArray(object.allplayertiles))
                    throw TypeError(".lq.RecordLiuJu.allplayertiles: array expected");
                message.allplayertiles = Array(object.allplayertiles.length);
                for (let i = 0; i < object.allplayertiles.length; ++i)
                    message.allplayertiles[i] = String(object.allplayertiles[i]);
            }
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordLiuJu.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.RecordLiuJu.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.RecordLiuJu.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        RecordLiuJu.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.tiles = [];
                object.allplayertiles = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.type = 0;
                object.gameend = null;
                object.seat = 0;
                object.liqi = null;
                object.muyu = null;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = $root.lq.GameEnd.toObject(message.gameend, options);
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            if (message.liqi != null && message.hasOwnProperty("liqi"))
                object.liqi = $root.lq.LiQiSuccess.toObject(message.liqi, options);
            if (message.allplayertiles && message.allplayertiles.length) {
                object.allplayertiles = Array(message.allplayertiles.length);
                for (let j = 0; j < message.allplayertiles.length; ++j)
                    object.allplayertiles[j] = message.allplayertiles[j];
            }
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        RecordLiuJu.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordLiuJu.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordLiuJu";
        };

        return RecordLiuJu;
    })();

    lq.NoTilePlayerInfo = (function() {

        function NoTilePlayerInfo(properties) {
            this.hand = [];
            this.tings = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NoTilePlayerInfo.prototype.tingpai = false;
        NoTilePlayerInfo.prototype.hand = $util.emptyArray;
        NoTilePlayerInfo.prototype.tings = $util.emptyArray;
        NoTilePlayerInfo.prototype.already_hule = false;

        NoTilePlayerInfo.create = function create(properties) {
            return new NoTilePlayerInfo(properties);
        };

        NoTilePlayerInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.tingpai != null && Object.hasOwnProperty.call(message, "tingpai"))
                writer.uint32(24).bool(message.tingpai);
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(34).string(message.hand[i]);
            if (message.tings != null && message.tings.length)
                for (let i = 0; i < message.tings.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tings[i], writer.uint32(42).fork()).ldelim();
            if (message.already_hule != null && Object.hasOwnProperty.call(message, "already_hule"))
                writer.uint32(48).bool(message.already_hule);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NoTilePlayerInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NoTilePlayerInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NoTilePlayerInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.tingpai = value;
                        else
                            delete message.tingpai;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tings && message.tings.length))
                            message.tings = [];
                        message.tings.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.already_hule = value;
                        else
                            delete message.already_hule;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NoTilePlayerInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NoTilePlayerInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.tingpai != null && message.hasOwnProperty("tingpai"))
                if (typeof message.tingpai !== "boolean")
                    return "tingpai: boolean expected";
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.tings != null && message.hasOwnProperty("tings")) {
                if (!Array.isArray(message.tings))
                    return "tings: array expected";
                for (let i = 0; i < message.tings.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tings[i], _depth + 1);
                    if (error)
                        return "tings." + error;
                }
            }
            if (message.already_hule != null && message.hasOwnProperty("already_hule"))
                if (typeof message.already_hule !== "boolean")
                    return "already_hule: boolean expected";
            return null;
        };

        NoTilePlayerInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NoTilePlayerInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NoTilePlayerInfo();
            if (object.tingpai != null)
                if (object.tingpai)
                    message.tingpai = Boolean(object.tingpai);
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.NoTilePlayerInfo.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.tings) {
                if (!Array.isArray(object.tings))
                    throw TypeError(".lq.NoTilePlayerInfo.tings: array expected");
                message.tings = Array(object.tings.length);
                for (let i = 0; i < object.tings.length; ++i) {
                    if (typeof object.tings[i] !== "object")
                        throw TypeError(".lq.NoTilePlayerInfo.tings: object expected");
                    message.tings[i] = $root.lq.TingPaiInfo.fromObject(object.tings[i], _depth + 1);
                }
            }
            if (object.already_hule != null)
                if (object.already_hule)
                    message.already_hule = Boolean(object.already_hule);
            return message;
        };

        NoTilePlayerInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.hand = [];
                object.tings = [];
            }
            if (options.defaults) {
                object.tingpai = false;
                object.already_hule = false;
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai"))
                object.tingpai = message.tingpai;
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.tings && message.tings.length) {
                object.tings = Array(message.tings.length);
                for (let j = 0; j < message.tings.length; ++j)
                    object.tings[j] = $root.lq.TingPaiInfo.toObject(message.tings[j], options);
            }
            if (message.already_hule != null && message.hasOwnProperty("already_hule"))
                object.already_hule = message.already_hule;
            return object;
        };

        NoTilePlayerInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NoTilePlayerInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NoTilePlayerInfo";
        };

        return NoTilePlayerInfo;
    })();

    lq.NoTileScoreInfo = (function() {

        function NoTileScoreInfo(properties) {
            this.old_scores = [];
            this.delta_scores = [];
            this.hand = [];
            this.ming = [];
            this.doras = [];
            this.taxes = [];
            this.lines = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        NoTileScoreInfo.prototype.seat = 0;
        NoTileScoreInfo.prototype.old_scores = $util.emptyArray;
        NoTileScoreInfo.prototype.delta_scores = $util.emptyArray;
        NoTileScoreInfo.prototype.hand = $util.emptyArray;
        NoTileScoreInfo.prototype.ming = $util.emptyArray;
        NoTileScoreInfo.prototype.doras = $util.emptyArray;
        NoTileScoreInfo.prototype.score = 0;
        NoTileScoreInfo.prototype.taxes = $util.emptyArray;
        NoTileScoreInfo.prototype.lines = $util.emptyArray;

        NoTileScoreInfo.create = function create(properties) {
            return new NoTileScoreInfo(properties);
        };

        NoTileScoreInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.old_scores != null && message.old_scores.length) {
                writer.uint32(18).fork();
                for (let i = 0; i < message.old_scores.length; ++i)
                    writer.int32(message.old_scores[i]);
                writer.ldelim();
            }
            if (message.delta_scores != null && message.delta_scores.length) {
                writer.uint32(26).fork();
                for (let i = 0; i < message.delta_scores.length; ++i)
                    writer.int32(message.delta_scores[i]);
                writer.ldelim();
            }
            if (message.hand != null && message.hand.length)
                for (let i = 0; i < message.hand.length; ++i)
                    writer.uint32(34).string(message.hand[i]);
            if (message.ming != null && message.ming.length)
                for (let i = 0; i < message.ming.length; ++i)
                    writer.uint32(42).string(message.ming[i]);
            if (message.doras != null && message.doras.length)
                for (let i = 0; i < message.doras.length; ++i)
                    writer.uint32(50).string(message.doras[i]);
            if (message.score != null && Object.hasOwnProperty.call(message, "score"))
                writer.uint32(56).uint32(message.score);
            if (message.taxes != null && message.taxes.length) {
                writer.uint32(66).fork();
                for (let i = 0; i < message.taxes.length; ++i)
                    writer.int32(message.taxes[i]);
                writer.ldelim();
            }
            if (message.lines != null && message.lines.length)
                for (let i = 0; i < message.lines.length; ++i)
                    writer.uint32(74).string(message.lines[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        NoTileScoreInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        NoTileScoreInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.NoTileScoreInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.old_scores && message.old_scores.length))
                                message.old_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.old_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.old_scores && message.old_scores.length))
                            message.old_scores = [];
                        message.old_scores.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.delta_scores && message.delta_scores.length))
                                message.delta_scores = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.delta_scores.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.delta_scores && message.delta_scores.length))
                            message.delta_scores = [];
                        message.delta_scores.push(reader.int32());
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hand && message.hand.length))
                            message.hand = [];
                        message.hand.push(reader.string());
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if (!(message.ming && message.ming.length))
                            message.ming = [];
                        message.ming.push(reader.string());
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if (!(message.doras && message.doras.length))
                            message.doras = [];
                        message.doras.push(reader.string());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.score = value;
                        else
                            delete message.score;
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.taxes && message.taxes.length))
                                message.taxes = [];
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.taxes.push(reader.int32());
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.taxes && message.taxes.length))
                            message.taxes = [];
                        message.taxes.push(reader.int32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.lines && message.lines.length))
                            message.lines = [];
                        message.lines.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        NoTileScoreInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        NoTileScoreInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            if (message.old_scores != null && message.hasOwnProperty("old_scores")) {
                if (!Array.isArray(message.old_scores))
                    return "old_scores: array expected";
                for (let i = 0; i < message.old_scores.length; ++i)
                    if (!$util.isInteger(message.old_scores[i]))
                        return "old_scores: integer[] expected";
            }
            if (message.delta_scores != null && message.hasOwnProperty("delta_scores")) {
                if (!Array.isArray(message.delta_scores))
                    return "delta_scores: array expected";
                for (let i = 0; i < message.delta_scores.length; ++i)
                    if (!$util.isInteger(message.delta_scores[i]))
                        return "delta_scores: integer[] expected";
            }
            if (message.hand != null && message.hasOwnProperty("hand")) {
                if (!Array.isArray(message.hand))
                    return "hand: array expected";
                for (let i = 0; i < message.hand.length; ++i)
                    if (!$util.isString(message.hand[i]))
                        return "hand: string[] expected";
            }
            if (message.ming != null && message.hasOwnProperty("ming")) {
                if (!Array.isArray(message.ming))
                    return "ming: array expected";
                for (let i = 0; i < message.ming.length; ++i)
                    if (!$util.isString(message.ming[i]))
                        return "ming: string[] expected";
            }
            if (message.doras != null && message.hasOwnProperty("doras")) {
                if (!Array.isArray(message.doras))
                    return "doras: array expected";
                for (let i = 0; i < message.doras.length; ++i)
                    if (!$util.isString(message.doras[i]))
                        return "doras: string[] expected";
            }
            if (message.score != null && message.hasOwnProperty("score"))
                if (!$util.isInteger(message.score))
                    return "score: integer expected";
            if (message.taxes != null && message.hasOwnProperty("taxes")) {
                if (!Array.isArray(message.taxes))
                    return "taxes: array expected";
                for (let i = 0; i < message.taxes.length; ++i)
                    if (!$util.isInteger(message.taxes[i]))
                        return "taxes: integer[] expected";
            }
            if (message.lines != null && message.hasOwnProperty("lines")) {
                if (!Array.isArray(message.lines))
                    return "lines: array expected";
                for (let i = 0; i < message.lines.length; ++i)
                    if (!$util.isString(message.lines[i]))
                        return "lines: string[] expected";
            }
            return null;
        };

        NoTileScoreInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.NoTileScoreInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.NoTileScoreInfo();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            if (object.old_scores) {
                if (!Array.isArray(object.old_scores))
                    throw TypeError(".lq.NoTileScoreInfo.old_scores: array expected");
                message.old_scores = Array(object.old_scores.length);
                for (let i = 0; i < object.old_scores.length; ++i)
                    message.old_scores[i] = object.old_scores[i] | 0;
            }
            if (object.delta_scores) {
                if (!Array.isArray(object.delta_scores))
                    throw TypeError(".lq.NoTileScoreInfo.delta_scores: array expected");
                message.delta_scores = Array(object.delta_scores.length);
                for (let i = 0; i < object.delta_scores.length; ++i)
                    message.delta_scores[i] = object.delta_scores[i] | 0;
            }
            if (object.hand) {
                if (!Array.isArray(object.hand))
                    throw TypeError(".lq.NoTileScoreInfo.hand: array expected");
                message.hand = Array(object.hand.length);
                for (let i = 0; i < object.hand.length; ++i)
                    message.hand[i] = String(object.hand[i]);
            }
            if (object.ming) {
                if (!Array.isArray(object.ming))
                    throw TypeError(".lq.NoTileScoreInfo.ming: array expected");
                message.ming = Array(object.ming.length);
                for (let i = 0; i < object.ming.length; ++i)
                    message.ming[i] = String(object.ming[i]);
            }
            if (object.doras) {
                if (!Array.isArray(object.doras))
                    throw TypeError(".lq.NoTileScoreInfo.doras: array expected");
                message.doras = Array(object.doras.length);
                for (let i = 0; i < object.doras.length; ++i)
                    message.doras[i] = String(object.doras[i]);
            }
            if (object.score != null)
                if (Number(object.score) !== 0)
                    message.score = object.score >>> 0;
            if (object.taxes) {
                if (!Array.isArray(object.taxes))
                    throw TypeError(".lq.NoTileScoreInfo.taxes: array expected");
                message.taxes = Array(object.taxes.length);
                for (let i = 0; i < object.taxes.length; ++i)
                    message.taxes[i] = object.taxes[i] | 0;
            }
            if (object.lines) {
                if (!Array.isArray(object.lines))
                    throw TypeError(".lq.NoTileScoreInfo.lines: array expected");
                message.lines = Array(object.lines.length);
                for (let i = 0; i < object.lines.length; ++i)
                    message.lines[i] = String(object.lines[i]);
            }
            return message;
        };

        NoTileScoreInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.old_scores = [];
                object.delta_scores = [];
                object.hand = [];
                object.ming = [];
                object.doras = [];
                object.taxes = [];
                object.lines = [];
            }
            if (options.defaults) {
                object.seat = 0;
                object.score = 0;
            }
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            if (message.old_scores && message.old_scores.length) {
                object.old_scores = Array(message.old_scores.length);
                for (let j = 0; j < message.old_scores.length; ++j)
                    object.old_scores[j] = message.old_scores[j];
            }
            if (message.delta_scores && message.delta_scores.length) {
                object.delta_scores = Array(message.delta_scores.length);
                for (let j = 0; j < message.delta_scores.length; ++j)
                    object.delta_scores[j] = message.delta_scores[j];
            }
            if (message.hand && message.hand.length) {
                object.hand = Array(message.hand.length);
                for (let j = 0; j < message.hand.length; ++j)
                    object.hand[j] = message.hand[j];
            }
            if (message.ming && message.ming.length) {
                object.ming = Array(message.ming.length);
                for (let j = 0; j < message.ming.length; ++j)
                    object.ming[j] = message.ming[j];
            }
            if (message.doras && message.doras.length) {
                object.doras = Array(message.doras.length);
                for (let j = 0; j < message.doras.length; ++j)
                    object.doras[j] = message.doras[j];
            }
            if (message.score != null && message.hasOwnProperty("score"))
                object.score = message.score;
            if (message.taxes && message.taxes.length) {
                object.taxes = Array(message.taxes.length);
                for (let j = 0; j < message.taxes.length; ++j)
                    object.taxes[j] = message.taxes[j];
            }
            if (message.lines && message.lines.length) {
                object.lines = Array(message.lines.length);
                for (let j = 0; j < message.lines.length; ++j)
                    object.lines[j] = message.lines[j];
            }
            return object;
        };

        NoTileScoreInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NoTileScoreInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.NoTileScoreInfo";
        };

        return NoTileScoreInfo;
    })();

    lq.ActionNoTile = (function() {

        function ActionNoTile(properties) {
            this.players = [];
            this.scores = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionNoTile.prototype.liujumanguan = false;
        ActionNoTile.prototype.players = $util.emptyArray;
        ActionNoTile.prototype.scores = $util.emptyArray;
        ActionNoTile.prototype.gameend = false;
        ActionNoTile.prototype.muyu = null;
        ActionNoTile.prototype.hules_history = $util.emptyArray;

        ActionNoTile.create = function create(properties) {
            return new ActionNoTile(properties);
        };

        ActionNoTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.liujumanguan != null && Object.hasOwnProperty.call(message, "liujumanguan"))
                writer.uint32(8).bool(message.liujumanguan);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.NoTilePlayerInfo.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.scores != null && message.scores.length)
                for (let i = 0; i < message.scores.length; ++i)
                    $root.lq.NoTileScoreInfo.encode(message.scores[i], writer.uint32(26).fork()).ldelim();
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                writer.uint32(32).bool(message.gameend);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(42).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionNoTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionNoTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionNoTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liujumanguan = value;
                        else
                            delete message.liujumanguan;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.NoTilePlayerInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push($root.lq.NoTileScoreInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gameend = value;
                        else
                            delete message.gameend;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionNoTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionNoTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                if (typeof message.liujumanguan !== "boolean")
                    return "liujumanguan: boolean expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.NoTilePlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i) {
                    let error = $root.lq.NoTileScoreInfo.verify(message.scores[i], _depth + 1);
                    if (error)
                        return "scores." + error;
                }
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                if (typeof message.gameend !== "boolean")
                    return "gameend: boolean expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        ActionNoTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionNoTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionNoTile();
            if (object.liujumanguan != null)
                if (object.liujumanguan)
                    message.liujumanguan = Boolean(object.liujumanguan);
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.ActionNoTile.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.ActionNoTile.players: object expected");
                    message.players[i] = $root.lq.NoTilePlayerInfo.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.ActionNoTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i) {
                    if (typeof object.scores[i] !== "object")
                        throw TypeError(".lq.ActionNoTile.scores: object expected");
                    message.scores[i] = $root.lq.NoTileScoreInfo.fromObject(object.scores[i], _depth + 1);
                }
            }
            if (object.gameend != null)
                if (object.gameend)
                    message.gameend = Boolean(object.gameend);
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.ActionNoTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.ActionNoTile.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.ActionNoTile.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        ActionNoTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.scores = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.liujumanguan = false;
                object.gameend = false;
                object.muyu = null;
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                object.liujumanguan = message.liujumanguan;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.NoTilePlayerInfo.toObject(message.players[j], options);
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = $root.lq.NoTileScoreInfo.toObject(message.scores[j], options);
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = message.gameend;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        ActionNoTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionNoTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionNoTile";
        };

        return ActionNoTile;
    })();

    lq.RecordNoTile = (function() {

        function RecordNoTile(properties) {
            this.players = [];
            this.scores = [];
            this.hules_history = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordNoTile.prototype.liujumanguan = false;
        RecordNoTile.prototype.players = $util.emptyArray;
        RecordNoTile.prototype.scores = $util.emptyArray;
        RecordNoTile.prototype.gameend = false;
        RecordNoTile.prototype.muyu = null;
        RecordNoTile.prototype.hules_history = $util.emptyArray;

        RecordNoTile.create = function create(properties) {
            return new RecordNoTile(properties);
        };

        RecordNoTile.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.liujumanguan != null && Object.hasOwnProperty.call(message, "liujumanguan"))
                writer.uint32(8).bool(message.liujumanguan);
            if (message.players != null && message.players.length)
                for (let i = 0; i < message.players.length; ++i)
                    $root.lq.NoTilePlayerInfo.encode(message.players[i], writer.uint32(18).fork()).ldelim();
            if (message.scores != null && message.scores.length)
                for (let i = 0; i < message.scores.length; ++i)
                    $root.lq.NoTileScoreInfo.encode(message.scores[i], writer.uint32(26).fork()).ldelim();
            if (message.gameend != null && Object.hasOwnProperty.call(message, "gameend"))
                writer.uint32(32).bool(message.gameend);
            if (message.muyu != null && Object.hasOwnProperty.call(message, "muyu"))
                $root.lq.MuyuInfo.encode(message.muyu, writer.uint32(42).fork()).ldelim();
            if (message.hules_history != null && message.hules_history.length)
                for (let i = 0; i < message.hules_history.length; ++i)
                    $root.lq.HuleInfo.encode(message.hules_history[i], writer.uint32(74).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordNoTile.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordNoTile.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordNoTile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.liujumanguan = value;
                        else
                            delete message.liujumanguan;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.players && message.players.length))
                            message.players = [];
                        message.players.push($root.lq.NoTilePlayerInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.scores && message.scores.length))
                            message.scores = [];
                        message.scores.push($root.lq.NoTileScoreInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.gameend = value;
                        else
                            delete message.gameend;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.muyu = $root.lq.MuyuInfo.decode(reader, reader.uint32(), undefined, _depth + 1, message.muyu);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        if (!(message.hules_history && message.hules_history.length))
                            message.hules_history = [];
                        message.hules_history.push($root.lq.HuleInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordNoTile.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordNoTile.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                if (typeof message.liujumanguan !== "boolean")
                    return "liujumanguan: boolean expected";
            if (message.players != null && message.hasOwnProperty("players")) {
                if (!Array.isArray(message.players))
                    return "players: array expected";
                for (let i = 0; i < message.players.length; ++i) {
                    let error = $root.lq.NoTilePlayerInfo.verify(message.players[i], _depth + 1);
                    if (error)
                        return "players." + error;
                }
            }
            if (message.scores != null && message.hasOwnProperty("scores")) {
                if (!Array.isArray(message.scores))
                    return "scores: array expected";
                for (let i = 0; i < message.scores.length; ++i) {
                    let error = $root.lq.NoTileScoreInfo.verify(message.scores[i], _depth + 1);
                    if (error)
                        return "scores." + error;
                }
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                if (typeof message.gameend !== "boolean")
                    return "gameend: boolean expected";
            if (message.muyu != null && message.hasOwnProperty("muyu")) {
                let error = $root.lq.MuyuInfo.verify(message.muyu, _depth + 1);
                if (error)
                    return "muyu." + error;
            }
            if (message.hules_history != null && message.hasOwnProperty("hules_history")) {
                if (!Array.isArray(message.hules_history))
                    return "hules_history: array expected";
                for (let i = 0; i < message.hules_history.length; ++i) {
                    let error = $root.lq.HuleInfo.verify(message.hules_history[i], _depth + 1);
                    if (error)
                        return "hules_history." + error;
                }
            }
            return null;
        };

        RecordNoTile.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordNoTile)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordNoTile();
            if (object.liujumanguan != null)
                if (object.liujumanguan)
                    message.liujumanguan = Boolean(object.liujumanguan);
            if (object.players) {
                if (!Array.isArray(object.players))
                    throw TypeError(".lq.RecordNoTile.players: array expected");
                message.players = Array(object.players.length);
                for (let i = 0; i < object.players.length; ++i) {
                    if (typeof object.players[i] !== "object")
                        throw TypeError(".lq.RecordNoTile.players: object expected");
                    message.players[i] = $root.lq.NoTilePlayerInfo.fromObject(object.players[i], _depth + 1);
                }
            }
            if (object.scores) {
                if (!Array.isArray(object.scores))
                    throw TypeError(".lq.RecordNoTile.scores: array expected");
                message.scores = Array(object.scores.length);
                for (let i = 0; i < object.scores.length; ++i) {
                    if (typeof object.scores[i] !== "object")
                        throw TypeError(".lq.RecordNoTile.scores: object expected");
                    message.scores[i] = $root.lq.NoTileScoreInfo.fromObject(object.scores[i], _depth + 1);
                }
            }
            if (object.gameend != null)
                if (object.gameend)
                    message.gameend = Boolean(object.gameend);
            if (object.muyu != null) {
                if (typeof object.muyu !== "object")
                    throw TypeError(".lq.RecordNoTile.muyu: object expected");
                message.muyu = $root.lq.MuyuInfo.fromObject(object.muyu, _depth + 1);
            }
            if (object.hules_history) {
                if (!Array.isArray(object.hules_history))
                    throw TypeError(".lq.RecordNoTile.hules_history: array expected");
                message.hules_history = Array(object.hules_history.length);
                for (let i = 0; i < object.hules_history.length; ++i) {
                    if (typeof object.hules_history[i] !== "object")
                        throw TypeError(".lq.RecordNoTile.hules_history: object expected");
                    message.hules_history[i] = $root.lq.HuleInfo.fromObject(object.hules_history[i], _depth + 1);
                }
            }
            return message;
        };

        RecordNoTile.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.players = [];
                object.scores = [];
                object.hules_history = [];
            }
            if (options.defaults) {
                object.liujumanguan = false;
                object.gameend = false;
                object.muyu = null;
            }
            if (message.liujumanguan != null && message.hasOwnProperty("liujumanguan"))
                object.liujumanguan = message.liujumanguan;
            if (message.players && message.players.length) {
                object.players = Array(message.players.length);
                for (let j = 0; j < message.players.length; ++j)
                    object.players[j] = $root.lq.NoTilePlayerInfo.toObject(message.players[j], options);
            }
            if (message.scores && message.scores.length) {
                object.scores = Array(message.scores.length);
                for (let j = 0; j < message.scores.length; ++j)
                    object.scores[j] = $root.lq.NoTileScoreInfo.toObject(message.scores[j], options);
            }
            if (message.gameend != null && message.hasOwnProperty("gameend"))
                object.gameend = message.gameend;
            if (message.muyu != null && message.hasOwnProperty("muyu"))
                object.muyu = $root.lq.MuyuInfo.toObject(message.muyu, options);
            if (message.hules_history && message.hules_history.length) {
                object.hules_history = Array(message.hules_history.length);
                for (let j = 0; j < message.hules_history.length; ++j)
                    object.hules_history[j] = $root.lq.HuleInfo.toObject(message.hules_history[j], options);
            }
            return object;
        };

        RecordNoTile.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordNoTile.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordNoTile";
        };

        return RecordNoTile;
    })();

    lq.PlayerLeaving = (function() {

        function PlayerLeaving(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        PlayerLeaving.prototype.seat = 0;

        PlayerLeaving.create = function create(properties) {
            return new PlayerLeaving(properties);
        };

        PlayerLeaving.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                writer.uint32(8).uint32(message.seat);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PlayerLeaving.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        PlayerLeaving.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.PlayerLeaving(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.seat = value;
                        else
                            delete message.seat;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        PlayerLeaving.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        PlayerLeaving.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.seat != null && message.hasOwnProperty("seat"))
                if (!$util.isInteger(message.seat))
                    return "seat: integer expected";
            return null;
        };

        PlayerLeaving.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.PlayerLeaving)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.PlayerLeaving();
            if (object.seat != null)
                if (Number(object.seat) !== 0)
                    message.seat = object.seat >>> 0;
            return message;
        };

        PlayerLeaving.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.seat = 0;
            if (message.seat != null && message.hasOwnProperty("seat"))
                object.seat = message.seat;
            return object;
        };

        PlayerLeaving.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PlayerLeaving.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.PlayerLeaving";
        };

        return PlayerLeaving;
    })();

    lq.SubmitGroup = (function() {

        function SubmitGroup(properties) {
            this.tiles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SubmitGroup.prototype.count = 0;
        SubmitGroup.prototype.tiles = $util.emptyArray;

        SubmitGroup.create = function create(properties) {
            return new SubmitGroup(properties);
        };

        SubmitGroup.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(8).uint32(message.count);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(18).string(message.tiles[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SubmitGroup.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SubmitGroup.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SubmitGroup(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SubmitGroup.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SubmitGroup.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            return null;
        };

        SubmitGroup.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SubmitGroup)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SubmitGroup();
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.SubmitGroup.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            return message;
        };

        SubmitGroup.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.tiles = [];
            if (options.defaults)
                object.count = 0;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            return object;
        };

        SubmitGroup.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SubmitGroup.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SubmitGroup";
        };

        return SubmitGroup;
    })();

    lq.SubmitInfo = (function() {

        function SubmitInfo(properties) {
            this.groups = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        SubmitInfo.prototype.submit_seat = 0;
        SubmitInfo.prototype.groups = $util.emptyArray;

        SubmitInfo.create = function create(properties) {
            return new SubmitInfo(properties);
        };

        SubmitInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.submit_seat != null && Object.hasOwnProperty.call(message, "submit_seat"))
                writer.uint32(8).uint32(message.submit_seat);
            if (message.groups != null && message.groups.length)
                for (let i = 0; i < message.groups.length; ++i)
                    $root.lq.SubmitGroup.encode(message.groups[i], writer.uint32(18).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SubmitInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        SubmitInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.SubmitInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.submit_seat = value;
                        else
                            delete message.submit_seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.groups && message.groups.length))
                            message.groups = [];
                        message.groups.push($root.lq.SubmitGroup.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        SubmitInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        SubmitInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.submit_seat != null && message.hasOwnProperty("submit_seat"))
                if (!$util.isInteger(message.submit_seat))
                    return "submit_seat: integer expected";
            if (message.groups != null && message.hasOwnProperty("groups")) {
                if (!Array.isArray(message.groups))
                    return "groups: array expected";
                for (let i = 0; i < message.groups.length; ++i) {
                    let error = $root.lq.SubmitGroup.verify(message.groups[i], _depth + 1);
                    if (error)
                        return "groups." + error;
                }
            }
            return null;
        };

        SubmitInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.SubmitInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.SubmitInfo();
            if (object.submit_seat != null)
                if (Number(object.submit_seat) !== 0)
                    message.submit_seat = object.submit_seat >>> 0;
            if (object.groups) {
                if (!Array.isArray(object.groups))
                    throw TypeError(".lq.SubmitInfo.groups: array expected");
                message.groups = Array(object.groups.length);
                for (let i = 0; i < object.groups.length; ++i) {
                    if (typeof object.groups[i] !== "object")
                        throw TypeError(".lq.SubmitInfo.groups: object expected");
                    message.groups[i] = $root.lq.SubmitGroup.fromObject(object.groups[i], _depth + 1);
                }
            }
            return message;
        };

        SubmitInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.groups = [];
            if (options.defaults)
                object.submit_seat = 0;
            if (message.submit_seat != null && message.hasOwnProperty("submit_seat"))
                object.submit_seat = message.submit_seat;
            if (message.groups && message.groups.length) {
                object.groups = Array(message.groups.length);
                for (let j = 0; j < message.groups.length; ++j)
                    object.groups[j] = $root.lq.SubmitGroup.toObject(message.groups[j], options);
            }
            return object;
        };

        SubmitInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SubmitInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.SubmitInfo";
        };

        return SubmitInfo;
    })();

    lq.ActionSubmit = (function() {

        function ActionSubmit(properties) {
            this.submits = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionSubmit.prototype.submits = $util.emptyArray;

        ActionSubmit.create = function create(properties) {
            return new ActionSubmit(properties);
        };

        ActionSubmit.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.submits != null && message.submits.length)
                for (let i = 0; i < message.submits.length; ++i)
                    $root.lq.SubmitInfo.encode(message.submits[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionSubmit.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionSubmit.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionSubmit();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.submits && message.submits.length))
                            message.submits = [];
                        message.submits.push($root.lq.SubmitInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionSubmit.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionSubmit.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.submits != null && message.hasOwnProperty("submits")) {
                if (!Array.isArray(message.submits))
                    return "submits: array expected";
                for (let i = 0; i < message.submits.length; ++i) {
                    let error = $root.lq.SubmitInfo.verify(message.submits[i], _depth + 1);
                    if (error)
                        return "submits." + error;
                }
            }
            return null;
        };

        ActionSubmit.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionSubmit)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionSubmit();
            if (object.submits) {
                if (!Array.isArray(object.submits))
                    throw TypeError(".lq.ActionSubmit.submits: array expected");
                message.submits = Array(object.submits.length);
                for (let i = 0; i < object.submits.length; ++i) {
                    if (typeof object.submits[i] !== "object")
                        throw TypeError(".lq.ActionSubmit.submits: object expected");
                    message.submits[i] = $root.lq.SubmitInfo.fromObject(object.submits[i], _depth + 1);
                }
            }
            return message;
        };

        ActionSubmit.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.submits = [];
            if (message.submits && message.submits.length) {
                object.submits = Array(message.submits.length);
                for (let j = 0; j < message.submits.length; ++j)
                    object.submits[j] = $root.lq.SubmitInfo.toObject(message.submits[j], options);
            }
            return object;
        };

        ActionSubmit.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionSubmit.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionSubmit";
        };

        return ActionSubmit;
    })();

    lq.RecordSubmit = (function() {

        function RecordSubmit(properties) {
            this.submits = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordSubmit.prototype.submits = $util.emptyArray;

        RecordSubmit.create = function create(properties) {
            return new RecordSubmit(properties);
        };

        RecordSubmit.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.submits != null && message.submits.length)
                for (let i = 0; i < message.submits.length; ++i)
                    $root.lq.SubmitInfo.encode(message.submits[i], writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordSubmit.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordSubmit.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordSubmit();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.submits && message.submits.length))
                            message.submits = [];
                        message.submits.push($root.lq.SubmitInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordSubmit.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordSubmit.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.submits != null && message.hasOwnProperty("submits")) {
                if (!Array.isArray(message.submits))
                    return "submits: array expected";
                for (let i = 0; i < message.submits.length; ++i) {
                    let error = $root.lq.SubmitInfo.verify(message.submits[i], _depth + 1);
                    if (error)
                        return "submits." + error;
                }
            }
            return null;
        };

        RecordSubmit.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordSubmit)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordSubmit();
            if (object.submits) {
                if (!Array.isArray(object.submits))
                    throw TypeError(".lq.RecordSubmit.submits: array expected");
                message.submits = Array(object.submits.length);
                for (let i = 0; i < object.submits.length; ++i) {
                    if (typeof object.submits[i] !== "object")
                        throw TypeError(".lq.RecordSubmit.submits: object expected");
                    message.submits[i] = $root.lq.SubmitInfo.fromObject(object.submits[i], _depth + 1);
                }
            }
            return message;
        };

        RecordSubmit.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.submits = [];
            if (message.submits && message.submits.length) {
                object.submits = Array(message.submits.length);
                for (let j = 0; j < message.submits.length; ++j)
                    object.submits[j] = $root.lq.SubmitInfo.toObject(message.submits[j], options);
            }
            return object;
        };

        RecordSubmit.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordSubmit.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordSubmit";
        };

        return RecordSubmit;
    })();

    lq.TakeInfo = (function() {

        function TakeInfo(properties) {
            this.tiles = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        TakeInfo.prototype.take_seat = 0;
        TakeInfo.prototype.from_seat = 0;
        TakeInfo.prototype.count = 0;
        TakeInfo.prototype.tiles = $util.emptyArray;

        TakeInfo.create = function create(properties) {
            return new TakeInfo(properties);
        };

        TakeInfo.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.take_seat != null && Object.hasOwnProperty.call(message, "take_seat"))
                writer.uint32(8).uint32(message.take_seat);
            if (message.from_seat != null && Object.hasOwnProperty.call(message, "from_seat"))
                writer.uint32(16).uint32(message.from_seat);
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(24).uint32(message.count);
            if (message.tiles != null && message.tiles.length)
                for (let i = 0; i < message.tiles.length; ++i)
                    writer.uint32(34).string(message.tiles[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TakeInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        TakeInfo.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.TakeInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.take_seat = value;
                        else
                            delete message.take_seat;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.from_seat = value;
                        else
                            delete message.from_seat;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.count = value;
                        else
                            delete message.count;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tiles && message.tiles.length))
                            message.tiles = [];
                        message.tiles.push(reader.string());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        TakeInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        TakeInfo.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.take_seat != null && message.hasOwnProperty("take_seat"))
                if (!$util.isInteger(message.take_seat))
                    return "take_seat: integer expected";
            if (message.from_seat != null && message.hasOwnProperty("from_seat"))
                if (!$util.isInteger(message.from_seat))
                    return "from_seat: integer expected";
            if (message.count != null && message.hasOwnProperty("count"))
                if (!$util.isInteger(message.count))
                    return "count: integer expected";
            if (message.tiles != null && message.hasOwnProperty("tiles")) {
                if (!Array.isArray(message.tiles))
                    return "tiles: array expected";
                for (let i = 0; i < message.tiles.length; ++i)
                    if (!$util.isString(message.tiles[i]))
                        return "tiles: string[] expected";
            }
            return null;
        };

        TakeInfo.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.TakeInfo)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.TakeInfo();
            if (object.take_seat != null)
                if (Number(object.take_seat) !== 0)
                    message.take_seat = object.take_seat >>> 0;
            if (object.from_seat != null)
                if (Number(object.from_seat) !== 0)
                    message.from_seat = object.from_seat >>> 0;
            if (object.count != null)
                if (Number(object.count) !== 0)
                    message.count = object.count >>> 0;
            if (object.tiles) {
                if (!Array.isArray(object.tiles))
                    throw TypeError(".lq.TakeInfo.tiles: array expected");
                message.tiles = Array(object.tiles.length);
                for (let i = 0; i < object.tiles.length; ++i)
                    message.tiles[i] = String(object.tiles[i]);
            }
            return message;
        };

        TakeInfo.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults)
                object.tiles = [];
            if (options.defaults) {
                object.take_seat = 0;
                object.from_seat = 0;
                object.count = 0;
            }
            if (message.take_seat != null && message.hasOwnProperty("take_seat"))
                object.take_seat = message.take_seat;
            if (message.from_seat != null && message.hasOwnProperty("from_seat"))
                object.from_seat = message.from_seat;
            if (message.count != null && message.hasOwnProperty("count"))
                object.count = message.count;
            if (message.tiles && message.tiles.length) {
                object.tiles = Array(message.tiles.length);
                for (let j = 0; j < message.tiles.length; ++j)
                    object.tiles[j] = message.tiles[j];
            }
            return object;
        };

        TakeInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TakeInfo.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.TakeInfo";
        };

        return TakeInfo;
    })();

    lq.ActionTake = (function() {

        function ActionTake(properties) {
            this.takes = [];
            this.tingpais0 = [];
            this.tingpais1 = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ActionTake.prototype.takes = $util.emptyArray;
        ActionTake.prototype.tingpais0 = $util.emptyArray;
        ActionTake.prototype.tingpais1 = $util.emptyArray;
        ActionTake.prototype.operation = null;

        ActionTake.create = function create(properties) {
            return new ActionTake(properties);
        };

        ActionTake.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.takes != null && message.takes.length)
                for (let i = 0; i < message.takes.length; ++i)
                    $root.lq.TakeInfo.encode(message.takes[i], writer.uint32(10).fork()).ldelim();
            if (message.tingpais0 != null && message.tingpais0.length)
                for (let i = 0; i < message.tingpais0.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais0[i], writer.uint32(18).fork()).ldelim();
            if (message.tingpais1 != null && message.tingpais1.length)
                for (let i = 0; i < message.tingpais1.length; ++i)
                    $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(26).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ActionTake.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ActionTake.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ActionTake(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.takes && message.takes.length))
                            message.takes = [];
                        message.takes.push($root.lq.TakeInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais0 && message.tingpais0.length))
                            message.tingpais0 = [];
                        message.tingpais0.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais1 && message.tingpais1.length))
                            message.tingpais1 = [];
                        message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ActionTake.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ActionTake.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.takes != null && message.hasOwnProperty("takes")) {
                if (!Array.isArray(message.takes))
                    return "takes: array expected";
                for (let i = 0; i < message.takes.length; ++i) {
                    let error = $root.lq.TakeInfo.verify(message.takes[i], _depth + 1);
                    if (error)
                        return "takes." + error;
                }
            }
            if (message.tingpais0 != null && message.hasOwnProperty("tingpais0")) {
                if (!Array.isArray(message.tingpais0))
                    return "tingpais0: array expected";
                for (let i = 0; i < message.tingpais0.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais0[i], _depth + 1);
                    if (error)
                        return "tingpais0." + error;
                }
            }
            if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                if (!Array.isArray(message.tingpais1))
                    return "tingpais1: array expected";
                for (let i = 0; i < message.tingpais1.length; ++i) {
                    let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                    if (error)
                        return "tingpais1." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        ActionTake.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ActionTake)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ActionTake();
            if (object.takes) {
                if (!Array.isArray(object.takes))
                    throw TypeError(".lq.ActionTake.takes: array expected");
                message.takes = Array(object.takes.length);
                for (let i = 0; i < object.takes.length; ++i) {
                    if (typeof object.takes[i] !== "object")
                        throw TypeError(".lq.ActionTake.takes: object expected");
                    message.takes[i] = $root.lq.TakeInfo.fromObject(object.takes[i], _depth + 1);
                }
            }
            if (object.tingpais0) {
                if (!Array.isArray(object.tingpais0))
                    throw TypeError(".lq.ActionTake.tingpais0: array expected");
                message.tingpais0 = Array(object.tingpais0.length);
                for (let i = 0; i < object.tingpais0.length; ++i) {
                    if (typeof object.tingpais0[i] !== "object")
                        throw TypeError(".lq.ActionTake.tingpais0: object expected");
                    message.tingpais0[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais0[i], _depth + 1);
                }
            }
            if (object.tingpais1) {
                if (!Array.isArray(object.tingpais1))
                    throw TypeError(".lq.ActionTake.tingpais1: array expected");
                message.tingpais1 = Array(object.tingpais1.length);
                for (let i = 0; i < object.tingpais1.length; ++i) {
                    if (typeof object.tingpais1[i] !== "object")
                        throw TypeError(".lq.ActionTake.tingpais1: object expected");
                    message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.ActionTake.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        ActionTake.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.takes = [];
                object.tingpais0 = [];
                object.tingpais1 = [];
            }
            if (options.defaults)
                object.operation = null;
            if (message.takes && message.takes.length) {
                object.takes = Array(message.takes.length);
                for (let j = 0; j < message.takes.length; ++j)
                    object.takes[j] = $root.lq.TakeInfo.toObject(message.takes[j], options);
            }
            if (message.tingpais0 && message.tingpais0.length) {
                object.tingpais0 = Array(message.tingpais0.length);
                for (let j = 0; j < message.tingpais0.length; ++j)
                    object.tingpais0[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais0[j], options);
            }
            if (message.tingpais1 && message.tingpais1.length) {
                object.tingpais1 = Array(message.tingpais1.length);
                for (let j = 0; j < message.tingpais1.length; ++j)
                    object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        ActionTake.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionTake.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ActionTake";
        };

        return ActionTake;
    })();

    lq.RecordTake = (function() {

        function RecordTake(properties) {
            this.takes = [];
            this.tingpais0 = [];
            this.tingpai = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        RecordTake.prototype.takes = $util.emptyArray;
        RecordTake.prototype.tingpais0 = $util.emptyArray;
        RecordTake.prototype.tingpai = $util.emptyArray;
        RecordTake.prototype.operation = null;

        RecordTake.create = function create(properties) {
            return new RecordTake(properties);
        };

        RecordTake.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.takes != null && message.takes.length)
                for (let i = 0; i < message.takes.length; ++i)
                    $root.lq.TakeInfo.encode(message.takes[i], writer.uint32(10).fork()).ldelim();
            if (message.tingpais0 != null && message.tingpais0.length)
                for (let i = 0; i < message.tingpais0.length; ++i)
                    $root.lq.TingPaiDiscardInfo.encode(message.tingpais0[i], writer.uint32(18).fork()).ldelim();
            if (message.tingpai != null && message.tingpai.length)
                for (let i = 0; i < message.tingpai.length; ++i)
                    $root.lq.RecordTake.TingPai.encode(message.tingpai[i], writer.uint32(26).fork()).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.lq.OptionalOperationList.encode(message.operation, writer.uint32(34).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RecordTake.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        RecordTake.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordTake(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.takes && message.takes.length))
                            message.takes = [];
                        message.takes.push($root.lq.TakeInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpais0 && message.tingpais0.length))
                            message.tingpais0 = [];
                        message.tingpais0.push($root.lq.TingPaiDiscardInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.tingpai && message.tingpai.length))
                            message.tingpai = [];
                        message.tingpai.push($root.lq.RecordTake.TingPai.decode(reader, reader.uint32(), undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.operation = $root.lq.OptionalOperationList.decode(reader, reader.uint32(), undefined, _depth + 1, message.operation);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        RecordTake.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        RecordTake.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.takes != null && message.hasOwnProperty("takes")) {
                if (!Array.isArray(message.takes))
                    return "takes: array expected";
                for (let i = 0; i < message.takes.length; ++i) {
                    let error = $root.lq.TakeInfo.verify(message.takes[i], _depth + 1);
                    if (error)
                        return "takes." + error;
                }
            }
            if (message.tingpais0 != null && message.hasOwnProperty("tingpais0")) {
                if (!Array.isArray(message.tingpais0))
                    return "tingpais0: array expected";
                for (let i = 0; i < message.tingpais0.length; ++i) {
                    let error = $root.lq.TingPaiDiscardInfo.verify(message.tingpais0[i], _depth + 1);
                    if (error)
                        return "tingpais0." + error;
                }
            }
            if (message.tingpai != null && message.hasOwnProperty("tingpai")) {
                if (!Array.isArray(message.tingpai))
                    return "tingpai: array expected";
                for (let i = 0; i < message.tingpai.length; ++i) {
                    let error = $root.lq.RecordTake.TingPai.verify(message.tingpai[i], _depth + 1);
                    if (error)
                        return "tingpai." + error;
                }
            }
            if (message.operation != null && message.hasOwnProperty("operation")) {
                let error = $root.lq.OptionalOperationList.verify(message.operation, _depth + 1);
                if (error)
                    return "operation." + error;
            }
            return null;
        };

        RecordTake.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.RecordTake)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.RecordTake();
            if (object.takes) {
                if (!Array.isArray(object.takes))
                    throw TypeError(".lq.RecordTake.takes: array expected");
                message.takes = Array(object.takes.length);
                for (let i = 0; i < object.takes.length; ++i) {
                    if (typeof object.takes[i] !== "object")
                        throw TypeError(".lq.RecordTake.takes: object expected");
                    message.takes[i] = $root.lq.TakeInfo.fromObject(object.takes[i], _depth + 1);
                }
            }
            if (object.tingpais0) {
                if (!Array.isArray(object.tingpais0))
                    throw TypeError(".lq.RecordTake.tingpais0: array expected");
                message.tingpais0 = Array(object.tingpais0.length);
                for (let i = 0; i < object.tingpais0.length; ++i) {
                    if (typeof object.tingpais0[i] !== "object")
                        throw TypeError(".lq.RecordTake.tingpais0: object expected");
                    message.tingpais0[i] = $root.lq.TingPaiDiscardInfo.fromObject(object.tingpais0[i], _depth + 1);
                }
            }
            if (object.tingpai) {
                if (!Array.isArray(object.tingpai))
                    throw TypeError(".lq.RecordTake.tingpai: array expected");
                message.tingpai = Array(object.tingpai.length);
                for (let i = 0; i < object.tingpai.length; ++i) {
                    if (typeof object.tingpai[i] !== "object")
                        throw TypeError(".lq.RecordTake.tingpai: object expected");
                    message.tingpai[i] = $root.lq.RecordTake.TingPai.fromObject(object.tingpai[i], _depth + 1);
                }
            }
            if (object.operation != null) {
                if (typeof object.operation !== "object")
                    throw TypeError(".lq.RecordTake.operation: object expected");
                message.operation = $root.lq.OptionalOperationList.fromObject(object.operation, _depth + 1);
            }
            return message;
        };

        RecordTake.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.arrays || options.defaults) {
                object.takes = [];
                object.tingpais0 = [];
                object.tingpai = [];
            }
            if (options.defaults)
                object.operation = null;
            if (message.takes && message.takes.length) {
                object.takes = Array(message.takes.length);
                for (let j = 0; j < message.takes.length; ++j)
                    object.takes[j] = $root.lq.TakeInfo.toObject(message.takes[j], options);
            }
            if (message.tingpais0 && message.tingpais0.length) {
                object.tingpais0 = Array(message.tingpais0.length);
                for (let j = 0; j < message.tingpais0.length; ++j)
                    object.tingpais0[j] = $root.lq.TingPaiDiscardInfo.toObject(message.tingpais0[j], options);
            }
            if (message.tingpai && message.tingpai.length) {
                object.tingpai = Array(message.tingpai.length);
                for (let j = 0; j < message.tingpai.length; ++j)
                    object.tingpai[j] = $root.lq.RecordTake.TingPai.toObject(message.tingpai[j], options);
            }
            if (message.operation != null && message.hasOwnProperty("operation"))
                object.operation = $root.lq.OptionalOperationList.toObject(message.operation, options);
            return object;
        };

        RecordTake.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordTake.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.RecordTake";
        };

        RecordTake.TingPai = (function() {

            function TingPai(properties) {
                this.tingpais1 = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            TingPai.prototype.seat = 0;
            TingPai.prototype.tingpais1 = $util.emptyArray;

            TingPai.create = function create(properties) {
                return new TingPai(properties);
            };

            TingPai.encode = function encode(message, writer) {
                if (!writer)
                    writer = $Writer.create();
                if (message.seat != null && Object.hasOwnProperty.call(message, "seat"))
                    writer.uint32(8).uint32(message.seat);
                if (message.tingpais1 != null && message.tingpais1.length)
                    for (let i = 0; i < message.tingpais1.length; ++i)
                        $root.lq.TingPaiInfo.encode(message.tingpais1[i], writer.uint32(18).fork()).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (let i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            TingPai.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer).ldelim();
            };

            TingPai.decode = function decode(reader, length, _end, _depth, _target) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.RecordTake.TingPai(), value;
                while (reader.pos < end) {
                    let start = reader.pos;
                    let tag = reader.tag();
                    if (tag === _end) {
                        _end = undefined;
                        break;
                    }
                    let wireType = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (wireType !== 0)
                                break;
                            if (value = reader.uint32())
                                message.seat = value;
                            else
                                delete message.seat;
                            continue;
                        }
                    case 2: {
                            if (wireType !== 2)
                                break;
                            if (!(message.tingpais1 && message.tingpais1.length))
                                message.tingpais1 = [];
                            message.tingpais1.push($root.lq.TingPaiInfo.decode(reader, reader.uint32(), undefined, _depth + 1));
                            continue;
                        }
                    }
                    reader.skipType(wireType, _depth, tag);
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
                if (_end !== undefined)
                    throw Error("missing end group");
                return message;
            };

            TingPai.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            TingPai.verify = function verify(message, _depth) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.seat != null && message.hasOwnProperty("seat"))
                    if (!$util.isInteger(message.seat))
                        return "seat: integer expected";
                if (message.tingpais1 != null && message.hasOwnProperty("tingpais1")) {
                    if (!Array.isArray(message.tingpais1))
                        return "tingpais1: array expected";
                    for (let i = 0; i < message.tingpais1.length; ++i) {
                        let error = $root.lq.TingPaiInfo.verify(message.tingpais1[i], _depth + 1);
                        if (error)
                            return "tingpais1." + error;
                    }
                }
                return null;
            };

            TingPai.fromObject = function fromObject(object, _depth) {
                if (object instanceof $root.lq.RecordTake.TingPai)
                    return object;
                if (_depth === undefined)
                    _depth = 0;
                if (_depth > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let message = new $root.lq.RecordTake.TingPai();
                if (object.seat != null)
                    if (Number(object.seat) !== 0)
                        message.seat = object.seat >>> 0;
                if (object.tingpais1) {
                    if (!Array.isArray(object.tingpais1))
                        throw TypeError(".lq.RecordTake.TingPai.tingpais1: array expected");
                    message.tingpais1 = Array(object.tingpais1.length);
                    for (let i = 0; i < object.tingpais1.length; ++i) {
                        if (typeof object.tingpais1[i] !== "object")
                            throw TypeError(".lq.RecordTake.TingPai.tingpais1: object expected");
                        message.tingpais1[i] = $root.lq.TingPaiInfo.fromObject(object.tingpais1[i], _depth + 1);
                    }
                }
                return message;
            };

            TingPai.toObject = function toObject(message, options) {
                if (!options)
                    options = {};
                let object = {};
                if (options.arrays || options.defaults)
                    object.tingpais1 = [];
                if (options.defaults)
                    object.seat = 0;
                if (message.seat != null && message.hasOwnProperty("seat"))
                    object.seat = message.seat;
                if (message.tingpais1 && message.tingpais1.length) {
                    object.tingpais1 = Array(message.tingpais1.length);
                    for (let j = 0; j < message.tingpais1.length; ++j)
                        object.tingpais1[j] = $root.lq.TingPaiInfo.toObject(message.tingpais1[j], options);
                }
                return object;
            };

            TingPai.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            TingPai.getTypeUrl = function getTypeUrl(prefix) {
                if (prefix === undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/lq.RecordTake.TingPai";
            };

            return TingPai;
        })();

        return RecordTake;
    })();

    lq.Route = (function() {

        function Route(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        }

        (Route.prototype = Object.create($protobuf.rpc.Service.prototype)).constructor = Route;

        Route.create = function create(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };


        Object.defineProperty(Route.prototype.requestConnection = function requestConnection(request, callback) {
            return this.rpcCall(requestConnection, $root.lq.ReqRequestConnection, $root.lq.ResRequestConnection, request, callback);
        }, "name", { value: "requestConnection" });


        Object.defineProperty(Route.prototype.requestRouteChange = function requestRouteChange(request, callback) {
            return this.rpcCall(requestRouteChange, $root.lq.ReqRequestRouteChange, $root.lq.ResRequestRouteChange, request, callback);
        }, "name", { value: "requestRouteChange" });


        Object.defineProperty(Route.prototype.heartbeat = function heartbeat(request, callback) {
            return this.rpcCall(heartbeat, $root.lq.ReqHeartbeat, $root.lq.ResHeartbeat, request, callback);
        }, "name", { value: "heartbeat" });

        return Route;
    })();

    lq.ReqRequestConnection = (function() {

        function ReqRequestConnection(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRequestConnection.prototype.type = 0;
        ReqRequestConnection.prototype.route_id = "";
        ReqRequestConnection.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        ReqRequestConnection.create = function create(properties) {
            return new ReqRequestConnection(properties);
        };

        ReqRequestConnection.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(16).uint32(message.type);
            if (message.route_id != null && Object.hasOwnProperty.call(message, "route_id"))
                writer.uint32(26).string(message.route_id);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(32).uint64(message.timestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRequestConnection.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRequestConnection.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRequestConnection(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.route_id = value;
                        else
                            delete message.route_id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRequestConnection.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRequestConnection.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            if (message.route_id != null && message.hasOwnProperty("route_id"))
                if (!$util.isString(message.route_id))
                    return "route_id: string expected";
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            return null;
        };

        ReqRequestConnection.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRequestConnection)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRequestConnection();
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            if (object.route_id != null)
                if (typeof object.route_id !== "string" || object.route_id.length)
                    message.route_id = String(object.route_id);
            if (object.timestamp != null)
                if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : Number(object.timestamp) !== 0)
                    if ($util.Long)
                        (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = true;
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            return message;
        };

        ReqRequestConnection.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.type = 0;
                object.route_id = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            if (message.route_id != null && message.hasOwnProperty("route_id"))
                object.route_id = message.route_id;
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            return object;
        };

        ReqRequestConnection.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRequestConnection.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRequestConnection";
        };

        return ReqRequestConnection;
    })();

    lq.ResRequestConnection = (function() {

        function ResRequestConnection(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRequestConnection.prototype.error = null;
        ResRequestConnection.prototype.timestamp = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        ResRequestConnection.prototype.result = 0;

        ResRequestConnection.create = function create(properties) {
            return new ResRequestConnection(properties);
        };

        ResRequestConnection.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(16).uint64(message.timestamp);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(24).uint32(message.result);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRequestConnection.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRequestConnection.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRequestConnection(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.timestamp = value;
                        else
                            delete message.timestamp;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRequestConnection.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRequestConnection.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            return null;
        };

        ResRequestConnection.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRequestConnection)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRequestConnection();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRequestConnection.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.timestamp != null)
                if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : Number(object.timestamp) !== 0)
                    if ($util.Long)
                        (message.timestamp = $util.Long.fromValue(object.timestamp)).unsigned = true;
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            return message;
        };

        ResRequestConnection.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.timestamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.timestamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.result = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.timestamp != null && message.hasOwnProperty("timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            return object;
        };

        ResRequestConnection.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRequestConnection.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRequestConnection";
        };

        return ResRequestConnection;
    })();

    lq.ReqRequestRouteChange = (function() {

        function ReqRequestRouteChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqRequestRouteChange.prototype.before = "";
        ReqRequestRouteChange.prototype.route_id = "";
        ReqRequestRouteChange.prototype.type = 0;

        ReqRequestRouteChange.create = function create(properties) {
            return new ReqRequestRouteChange(properties);
        };

        ReqRequestRouteChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.before != null && Object.hasOwnProperty.call(message, "before"))
                writer.uint32(10).string(message.before);
            if (message.route_id != null && Object.hasOwnProperty.call(message, "route_id"))
                writer.uint32(18).string(message.route_id);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(24).uint32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqRequestRouteChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqRequestRouteChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqRequestRouteChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.before = value;
                        else
                            delete message.before;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.string()).length)
                            message.route_id = value;
                        else
                            delete message.route_id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.type = value;
                        else
                            delete message.type;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqRequestRouteChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqRequestRouteChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.before != null && message.hasOwnProperty("before"))
                if (!$util.isString(message.before))
                    return "before: string expected";
            if (message.route_id != null && message.hasOwnProperty("route_id"))
                if (!$util.isString(message.route_id))
                    return "route_id: string expected";
            if (message.type != null && message.hasOwnProperty("type"))
                if (!$util.isInteger(message.type))
                    return "type: integer expected";
            return null;
        };

        ReqRequestRouteChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqRequestRouteChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqRequestRouteChange();
            if (object.before != null)
                if (typeof object.before !== "string" || object.before.length)
                    message.before = String(object.before);
            if (object.route_id != null)
                if (typeof object.route_id !== "string" || object.route_id.length)
                    message.route_id = String(object.route_id);
            if (object.type != null)
                if (Number(object.type) !== 0)
                    message.type = object.type >>> 0;
            return message;
        };

        ReqRequestRouteChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.before = "";
                object.route_id = "";
                object.type = 0;
            }
            if (message.before != null && message.hasOwnProperty("before"))
                object.before = message.before;
            if (message.route_id != null && message.hasOwnProperty("route_id"))
                object.route_id = message.route_id;
            if (message.type != null && message.hasOwnProperty("type"))
                object.type = message.type;
            return object;
        };

        ReqRequestRouteChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqRequestRouteChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqRequestRouteChange";
        };

        return ReqRequestRouteChange;
    })();

    lq.ResRequestRouteChange = (function() {

        function ResRequestRouteChange(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResRequestRouteChange.prototype.error = null;
        ResRequestRouteChange.prototype.result = 0;

        ResRequestRouteChange.create = function create(properties) {
            return new ResRequestRouteChange(properties);
        };

        ResRequestRouteChange.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(24).uint32(message.result);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResRequestRouteChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResRequestRouteChange.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResRequestRouteChange(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResRequestRouteChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResRequestRouteChange.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            if (message.result != null && message.hasOwnProperty("result"))
                if (!$util.isInteger(message.result))
                    return "result: integer expected";
            return null;
        };

        ResRequestRouteChange.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResRequestRouteChange)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResRequestRouteChange();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResRequestRouteChange.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            if (object.result != null)
                if (Number(object.result) !== 0)
                    message.result = object.result >>> 0;
            return message;
        };

        ResRequestRouteChange.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.error = null;
                object.result = 0;
            }
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            if (message.result != null && message.hasOwnProperty("result"))
                object.result = message.result;
            return object;
        };

        ResRequestRouteChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResRequestRouteChange.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResRequestRouteChange";
        };

        return ResRequestRouteChange;
    })();

    lq.ReqHeartbeat = (function() {

        function ReqHeartbeat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ReqHeartbeat.prototype.delay = 0;
        ReqHeartbeat.prototype.no_operation_counter = 0;
        ReqHeartbeat.prototype.platform = 0;
        ReqHeartbeat.prototype.network_quality = 0;

        ReqHeartbeat.create = function create(properties) {
            return new ReqHeartbeat(properties);
        };

        ReqHeartbeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.delay != null && Object.hasOwnProperty.call(message, "delay"))
                writer.uint32(8).uint32(message.delay);
            if (message.no_operation_counter != null && Object.hasOwnProperty.call(message, "no_operation_counter"))
                writer.uint32(16).uint32(message.no_operation_counter);
            if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                writer.uint32(24).uint32(message.platform);
            if (message.network_quality != null && Object.hasOwnProperty.call(message, "network_quality"))
                writer.uint32(32).uint32(message.network_quality);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReqHeartbeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ReqHeartbeat.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ReqHeartbeat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.delay = value;
                        else
                            delete message.delay;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.no_operation_counter = value;
                        else
                            delete message.no_operation_counter;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.platform = value;
                        else
                            delete message.platform;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.network_quality = value;
                        else
                            delete message.network_quality;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ReqHeartbeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ReqHeartbeat.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.delay != null && message.hasOwnProperty("delay"))
                if (!$util.isInteger(message.delay))
                    return "delay: integer expected";
            if (message.no_operation_counter != null && message.hasOwnProperty("no_operation_counter"))
                if (!$util.isInteger(message.no_operation_counter))
                    return "no_operation_counter: integer expected";
            if (message.platform != null && message.hasOwnProperty("platform"))
                if (!$util.isInteger(message.platform))
                    return "platform: integer expected";
            if (message.network_quality != null && message.hasOwnProperty("network_quality"))
                if (!$util.isInteger(message.network_quality))
                    return "network_quality: integer expected";
            return null;
        };

        ReqHeartbeat.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ReqHeartbeat)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ReqHeartbeat();
            if (object.delay != null)
                if (Number(object.delay) !== 0)
                    message.delay = object.delay >>> 0;
            if (object.no_operation_counter != null)
                if (Number(object.no_operation_counter) !== 0)
                    message.no_operation_counter = object.no_operation_counter >>> 0;
            if (object.platform != null)
                if (Number(object.platform) !== 0)
                    message.platform = object.platform >>> 0;
            if (object.network_quality != null)
                if (Number(object.network_quality) !== 0)
                    message.network_quality = object.network_quality >>> 0;
            return message;
        };

        ReqHeartbeat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults) {
                object.delay = 0;
                object.no_operation_counter = 0;
                object.platform = 0;
                object.network_quality = 0;
            }
            if (message.delay != null && message.hasOwnProperty("delay"))
                object.delay = message.delay;
            if (message.no_operation_counter != null && message.hasOwnProperty("no_operation_counter"))
                object.no_operation_counter = message.no_operation_counter;
            if (message.platform != null && message.hasOwnProperty("platform"))
                object.platform = message.platform;
            if (message.network_quality != null && message.hasOwnProperty("network_quality"))
                object.network_quality = message.network_quality;
            return object;
        };

        ReqHeartbeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReqHeartbeat.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ReqHeartbeat";
        };

        return ReqHeartbeat;
    })();

    lq.ResHeartbeat = (function() {

        function ResHeartbeat(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        ResHeartbeat.prototype.error = null;

        ResHeartbeat.create = function create(properties) {
            return new ResHeartbeat(properties);
        };

        ResHeartbeat.encode = function encode(message, writer) {
            if (!writer)
                writer = $Writer.create();
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.lq.Error.encode(message.error, writer.uint32(10).fork()).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResHeartbeat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer).ldelim();
        };

        ResHeartbeat.decode = function decode(reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = _target || new $root.lq.ResHeartbeat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.lq.Error.decode(reader, reader.uint32(), undefined, _depth + 1, message.error);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
            if (_end !== undefined)
                throw Error("missing end group");
            return message;
        };

        ResHeartbeat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        ResHeartbeat.verify = function verify(message, _depth) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                return "max depth exceeded";
            if (message.error != null && message.hasOwnProperty("error")) {
                let error = $root.lq.Error.verify(message.error, _depth + 1);
                if (error)
                    return "error." + error;
            }
            return null;
        };

        ResHeartbeat.fromObject = function fromObject(object, _depth) {
            if (object instanceof $root.lq.ResHeartbeat)
                return object;
            if (_depth === undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw Error("max depth exceeded");
            let message = new $root.lq.ResHeartbeat();
            if (object.error != null) {
                if (typeof object.error !== "object")
                    throw TypeError(".lq.ResHeartbeat.error: object expected");
                message.error = $root.lq.Error.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        ResHeartbeat.toObject = function toObject(message, options) {
            if (!options)
                options = {};
            let object = {};
            if (options.defaults)
                object.error = null;
            if (message.error != null && message.hasOwnProperty("error"))
                object.error = $root.lq.Error.toObject(message.error, options);
            return object;
        };

        ResHeartbeat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ResHeartbeat.getTypeUrl = function getTypeUrl(prefix) {
            if (prefix === undefined)
                prefix = "type.googleapis.com";
            return prefix + "/lq.ResHeartbeat";
        };

        return ResHeartbeat;
    })();

    return lq;
})();

export {
  $root as default
};
